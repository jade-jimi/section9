---
id: DOC-20260830-003-62x6
type: knowledge
title: 살아있음 판정 재설계
summary: 멈춤·깨우기·귀속·완료 판정 갈래의 전수 점검 — 근원 세 가지와 재설계 원칙, 반영 순서
status: published
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: 9854e394
project: section9
relates: ["REQ-20260830-016-62x6"]
relates_why: {"REQ-20260830-016-62x6": "이 요청의 점검 결과와 설계 원문"}
tags: ["test", "workflow", "sync"]
created: 2026-08-30T13:47:58+09:00
updated: 2026-08-30T14:08:36+09:00
priority: 50
---

## Original

## 왜 이 문서인가

DOC-20260830-002(깨우기)·REQ-20260830-012(거짓 멈춤)·REQ-20260830-014(프로세스 모델)는
같은 갈래의 세 증상이고, 그 앞에 같은 갈래의 수선이 최소 열두 번 있었다
(REQ-20260826-013·021·023·031·035 / 20260827-046·074 / 20260828-003·005·036·041 /
20260829-034·036 / 20260830-002·007·009·012). 전부 사고 후 국소 패치였다.
이번에는 갈래 전체를 한 판으로 놓고 근원부터 본다 (REQ-20260830-016).

## 본질 — 이 갈래가 답해야 하는 질문은 셋뿐이다

보드의 in-progress 카드 하나마다:

1. **누가 맡았나** (귀속) — 바인딩 last_req/active_reqs · 문서 session 도장 ·
   위임 지명 · 워커 마커 · 리스
2. **지금 움직이나** (진전) — 시계는 문서 `updated` **하나**
3. **끝났나** (완료) — 사람의 `s9 status done` **하나**

셋 다 판정 함수는 이미 한 곳에 모여 있다(stall_verdict·chat_live·_spawn_worker —
선대 수선의 성과다). 문제는 함수가 아니라 **그 함수들이 먹는 신호**다.

## 근원 — 셋

### 근원 A. 진전의 시계가 문서 쓰기 하나뿐이다

일은 코드·시험·터미널에서 일어나는데, 시계는 문서에 쓸 때만 간다.
- 리드가 037 을 조용히 파는 동안 보드는 「멈춤 74분」을 그렸다 (REQ-012,
  사용자: "멈춤으로 나오는건 진짜인가? 신뢰경험이 아직인것같다").
- 반대로 클레임 도장이 시계를 되감아 "한 일이 없는데 경보만 꺼졌다"
  (REQ-20260829-034 — 그래서 stamp_doc_session 이 updated 를 안 건드리게 됐다).
- attached/waiting/unknown 같은 상태 갈래는 전부 이 한 시계의 거짓 멈춤을
  사고 하나당 하나씩 깎아 만든 **예외들**이다.

### 근원 B. 귀속이 선언이 아니라 추론이다

위임이 어느 REQ 의 것인지를 훅이 자유 텍스트에서 정규식으로 **추측**한다
(delegation_targets 의 id·세자리 번호 매칭, --guess 클레임). 추측이 빗나가면
한 번에 사고가 둘 난다 — 손이 안 보이는 문서는 거짓 멈춤이 되고(→ 깨우기가
일하는 파일 위에 두 번째 손을 얹는다, 2026-08-29 20:34 실사고), 손이 잘못
보이는 문서는 진짜 멈춤이 가려진다. unassigned_hands 는 이 추측 실패를 전역
정지(아무 데도 안 띄움)로 때우는 안전판이다 — 안전하지만, 모르는 손 하나가
전체 스폰을 세운다.

### 근원 C. 완료가 선언에만 달려 있다

시스템은 이미 안다(문서의 commit 노트·시험 결과·워커의 완결 보고). 그런데
아는 것으로 하는 일이 "다음 세션 시작 때 ⚑ 로 잔소리"뿐이다(loose_requests).
2026-08-30 실사고: 038 이 커밋 3c63e9c 로 사실상 끝나 있었는데 상태만
in-progress 로 남아 「700분 멈춤」이 됐고, 사람이 깨우기를 눌러 **끝난 일에
프로세스 하나가 통째로** 떴다. 근원 C 가 근원 A 의 거짓 멈춤을 만들고, 그것이
깨우기 낭비(DOC-002 의 비용)를 만든다 — 갈래가 하나인 이유다.

(깨우기가 새 프로세스일 수밖에 없는 채널 한계 — transcript 단일 작가·입력
IPC 부재·수신함은 끌어오기 — 는 DOC-20260830-002 참조. 그 한계 자체는
구조적이라 받아들이고, **깨울 일 자체를 줄이는 것**이 이 설계의 방향이다.)

## 설계 원칙

**진전은 선언이 아니라 관측으로. 귀속은 추론이 아니라 선언으로.
완료 증거는 잔소리가 아니라 행동으로.**

### 반영 1 — 주의(attention) 신호를 둘째 시계로 (근원 A)

`state/heartbeat/<REQ>.json` — 문서가 아니라 상태 디렉토리에, 어떤 주체든
그 REQ 를 **만질 때** 시각·주체를 찍는다. 찍는 자리는 s9 진입점 한 곳:
문서 id 를 인자로 받는 모든 s9 명령(show·note·status·claim·last·link…)이
지나는 dispatch 에서 한 줄. 문서 updated 는 안 건드리므로 REQ-034 의 교훈
(도장이 시계를 되감는다)과 충돌하지 않는다.

stall_verdict 는 이것을 **moving 이 아니라 attached** 로 먹는다: "N분째
문서에 진전 없음 · 다만 M분 전까지 손길 있음". 거짓 멈춤(깨우기 유도)은
사라지고, 조용함은 계속 보인다(감추지 않는다 원칙 유지). 깨우기 손잡이는
attached 인 동안 「손길이 있습니다」로 한 단계 확인을 요구한다.

### 반영 2 — 위임 귀속을 선언 의무로 (근원 B)

Agent 위임 시 리드가 `s9 delegate-target` 류의 **명시 등록**을 먼저 하게
규약·훅을 바꾼다(등록 없는 위임은 훅이 스폰 시점에 경고). 추론(정규식)은
폐지가 아니라 **최후 수단**으로 강등하고, 추론으로 귀속했으면 그 사실을
문서에 --guess 로 남기는 현행을 유지한다. unassigned_hands 의 전역 정지는
선언이 서면 자연히 0 에 수렴한다 — 장치를 없애는 게 아니라 발동을 없앤다.

### 반영 3 — 완료 드리프트를 깨우기 경로가 스스로 수습 (근원 C)

_spawn_wake 앞에 한 문: 그 REQ 가 loose(committed) 이면 "이어서 일하라"
프롬프트 대신 **"커밋이 이미 있다 — goal 대비 검증해 닫는 것이 네 일"**
프롬프트로 바꾼다(스폰은 한다 — 검증도 일이다. 다만 헛돌지 않게 한다).
대시보드 카드도 committed 드리프트에는 「깨우기」 대신 「닫기 검토」를 그린다.
자동 done 전이는 하지 않는다 — 커밋 존재 ≠ goal 충족이고, 그 판정까지
기계가 하면 검증 없는 done 이 쌓인다(verify-then-done 규율 위반).

### 반영 순서와 크기

1(하트비트+stall_verdict 소비) → 3(깨우기 수습) → 2(위임 선언). 1·3 은 각각
S~M, 2 는 규약 변경이 절반이라 M. 각 단계마다 TDD 시나리오를 REQ 에 먼저
세우고, 기존 시험 계약(test_stall_*, test_wake_*, test_platform_live)을
회귀선으로 삼는다.

## 재발 방지

- 판정 함수는 지금처럼 한 곳(stall_verdict)만 남긴다 — 새 신호는 새 함수가
  아니라 새 **입력**이다.
- 각 반영의 시험은 "신호가 없을 때 지금과 똑같이 동작"을 계약 1번으로 세운다
  (하트비트 디렉토리가 없어도·등록 없는 위임도·loose 판정 실패도 현행 유지).
- 이 갈래에서 또 사고가 나면 이 문서에 각주로 잇는다 — 국소 패치 열두 번의
  역사가 반복되면 그것이 이 설계의 반증이다.

## Notes

### 2026-08-30T14:02:35+09:00 response (by nicehugepark)

### 다각 검토 협의 결과 (v2 확정, REQ-20260830-017-62x6)

po·pm·architect·backend-developer·white-hacker 다섯 전원 조건부 승인. 상충 둘은 협의로 해소(pm 이 순서를 접었고, white-hacker 가 git 실측 요구를 접었다). 확정 설계는 원안에서 다음이 바뀐다.

**순서: 3 → 1 → 2** (po 의 임팩트/비용 논거 + backend 실측: 3 은 1 과 실질 의존이 없다 — 완료 드리프트 REQ 는 손이 없어 하트비트와 무관).

**반영 3 (최우선, S)** — ① committed 판정을 본문 부분문자열 " commit (" 스캔에서 앵커 정규식 `(?m)^### \S+ commit \(` 로 교체(loose_requests·_spawn_wake 공용, 산문 오탐 제거, git 서브프로세스 없음) ② 프롬프트는 양면: "커밋이 이미 있다 — goal 대비 검증해 충족이면 닫고 **미충족이면 이어서 하라**" (이 양면 문구가 위조 완화의 하중을 지는 부품 — "committed+goal 미충족 → 워커가 닫지 않고 이어감"을 회귀시험 계약으로 고정) ③ 카드 「닫기 검토」 라벨과 프롬프트는 한 커밋 ④ 깨우기 프롬프트의 문서 제목 인라인을 재작업 노트와 같은 <<참고>> 델리미터로 감싼다(주입 표면 봉합).

**반영 1 (S~M)** — ① state/heartbeat/<safe_name(canon_id(id))> 파일, dispatch 한 자리(args.fn 직전, getattr(args,"id"))에서 씀. 내용은 actor·session 한 줄 JSON 이되 소비는 mtime 만. 무lock 원자 치환, 실패는 try/except 로 무해, terminal 전이에서 gc 한 줄 ② 대시보드 서버는 in-process 라 main() 을 안 지나 오염 경로가 없다 — "서버 경로 미통과"를 시험 계약 1번으로 ③ 소비는 stall_verdict 에서 attached 주석 한정("N분째 진전 없음 · M분 전 손길") — moving 아님, 스폰 게이트 불변, 진전 시계로 승격 금지(계약시험) ④ 화면은 서버 판정 필드를 그리기만(클라이언트 재판정 금지) ⑤ 한계 명기: s9 호출 없는 코드 작업은 여전히 안 보인다 — 파일 감시로 확장하지 않는다.

**반영 2 (축소판, 경고-only)** — 새 명령·레지스트리 없음: 위임 description 자체가 선언이다. PreToolUse(Agent) 훅이 기존 delegate-target 파서로 검사해 지명이 안 풀리면 경고만. 선언에 세션 provenance 기록, 비클레임 세션의 선언은 --guess 강등(거짓 선언이 추측보다 신뢰받는 역전 차단). 차단 승격은 관측 지표 달성 후 별도 판단.

**완료 기준 (2주 관측창, pm)** — (a) 끝난/일하는 REQ 에 스폰 0건 (b) unassigned_hands 전역 정지 발동 0건 (c) 새 예외 상태 추가 0건. 이 갈래에서 또 사고가 나면 이 문서에 각주로 잇는다.

**단순성 결산 (architect)** — 새 프로세스 0 · 새 상태 0 · 새 판정 함수 0 · 새 명령 0 · 새 디렉토리 1.

### 2026-08-30T14:05:37+09:00 response (by nicehugepark)

### v3 보정 (staff-engineer 최종 검토 반영)

staff-engineer 가 v2 에서 결함 둘을 찾았고 둘 다 수용한다 — 둘 다 복잡도를 더하지 않고 좁힌다.

**보정 1 — 손길의 의미를 쓰기로 좁힌다.** dispatch 도장을 조회(show·ls·search)에는 찍지 않고 **쓰기 명령(note·status·claim·last·link·set) 한정**으로 좁힌다. 남의 문서를 열람만 해도 멈춤 경보가 숨는 결함(제3자 show 오염)이 뿌리에서 사라지고, white-hacker 가 지적한 "워커가 s9 show 만 쳐도 도장" 위조 표면도 같이 닫힌다. attached 유효창 상수(HEARTBEAT_ATTACH_WIN)를 두고 기본값은 구현 TDD 에서 정한다.

**보정 2 — committed 게이트는 모든 스폰 경로가 지난다.** 앵커 정규식 판정을 공용 함수로 빼고 _spawn_wake 만이 아니라 **_spawn_worker 공통 경로**(rework·follow-up·resume-item·wake 전 reason)에 세운다 — "게이트 두 벌이면 성긴 쪽으로 샌다"(REQ-20260830-002-62x6 교훈)를 이 갈래가 반복하지 않도록. 고아 하트비트(terminal 전이가 영영 없는 잘못된 id)는 terminal gc 에 더해 오래된 파일 sweep 한 줄로 거둔다.

**계약시험 추가 (C5~C9)** — C5 cmd_commit_note 헤더 포맷 ↔ 앵커 정규식 왕복(상수 공유) · C6 무관 세션의 show 만으로 깨우기 억제 안 됨 · C7 하트비트 손상·미래시각은 무신호 취급 · C8 committed 드리프트에 워처 경로가 옛 프롬프트로 안 뜸(038 회귀) · C9 하트비트 쓰기 실패가 원 명령을 안 죽임.

**남는 부채 (명시적 수용, staff-engineer)** — 정규식 귀속 추측기+전역 정지 존속 · stall_verdict 내부 예외 사다리(waiting/unknown/dead-win) 미정리 · 완료 증거가 노트 텍스트 포맷에 결합 · 스폰 프롬프트 3벌 중복. v2+v3 는 스트랭글러 1보이고, 이 부채들은 관측창 결과를 본 뒤 별도 REQ 로 판단한다.

이로써 여섯 검토자(po·pm·architect·backend-developer·white-hacker·staff-engineer) 전원 조건부 승인 조건이 설계에 반영됐다.

### 2026-08-30T14:07:56+09:00 response (by nicehugepark)

### 확정 설계 · 요약본 (사람이 읽는 판)

**지금 무엇이 문제인가.** 보드는 카드마다 세 가지를 알아야 한다 — 누가 맡았나, 지금 움직이나, 끝났나. 그런데 "움직이나"의 시계가 문서에 글을 쓸 때만 가서, 조용히 일하는 중이면 멈춤으로 뜨고(어제의 037), 끝났는데 닫기를 잊으면 영원히 멈춤으로 뜬다(700분의 038). 멈춤으로 뜨면 사람이 깨우기를 누르고, 그러면 프로세스가 하나 떠서 — 끝난 일이면 헛돈다.

**무엇을 바꾸나 — 셋, 이 순서로.**

1. **끝난 일을 헛깨우지 않는다.** 문서에 커밋 흔적(커밋 훅이 남기는 정형 노트)이 있는 카드는, 깨우거나 자동으로 작업자가 붙을 때 "이어서 하라" 대신 **"커밋이 이미 있다 — 목표에 비춰 검증해서, 됐으면 닫고 안 됐으면 이어서 하라"**는 지시를 받는다. 카드에도 「닫기 검토」라고 표시된다. 기계가 멋대로 닫는 일은 없다 — 닫는 것은 검증을 통과한 작업자다.

2. **손길을 본다.** s9 로 그 문서를 **고치는** 명령(노트·상태 전이·클레임·링크)이 실행될 때마다 상태 폴더에 그 문서의 손길 파일 하나가 갱신된다. 보드는 "N분째 문서에 진전 없음"과 함께 "다만 M분 전까지 손길 있음"을 보여 주고, 손길이 신선한 카드의 깨우기는 한 번 더 물어본다. 열람(show)은 손길이 아니다 — 남의 문서를 읽기만 해도 경보가 꺼지는 일은 없다. 손길은 "진전"으로는 절대 세지 않는다 — 조용함은 계속 보인다.

3. **맡길 때 이름을 부른다.** 리드가 서브에이전트에 일을 맡길 때 지시문에 REQ 번호가 없으면 훅이 경고한다. 강제는 아직 하지 않는다 — 경고만으로 추측 귀속이 얼마나 줄어드는지 2주 관측하고 결정한다.

**무엇이 늘지 않나.** 새 프로세스 0 · 새 화면 상태 0 · 새 판정 함수 0 · 새 명령 0. 느는 것은 상태 폴더 하나(state/heartbeat/)뿐이다.

**성공의 잣대 (2주).** 끝난/일하는 카드에 작업자가 뜨는 일 0건 · "누구 것인지 모르는 손" 전역 정지 0건 · 새 예외 상태 추가 0건.

### 2026-08-30T14:08:36+09:00 response (by nicehugepark)

### 확정 설계 · 상세본 (구현 명세)

원안 + v2 협의 + v3 보정의 합본이며, 구현은 이 명세를 따른다. 순서: 반영3 → 반영1 → 반영2. 각 반영은 착수 전 TDD 노트(정상/경계/실패/회귀)를 해당 REQ 에 먼저 세운다.

**공통 원칙** — 새 판정 함수를 만들지 않는다: 신호는 stall_verdict 의 새 입력이다. 신호가 없으면(디렉토리 부재·파일 손상·미래 시각) 예외 없이 현행과 동일하게 동작한다. 화면은 서버가 낸 판정 필드를 그리기만 한다.

**반영 3 — 완료 드리프트 수습 (S)**
- 상수 COMMIT_NOTE_RE = r"(?m)^### \S+ commit \(" 를 bin/s9 한 곳에 두고, cmd_commit_note 의 헤더 생성 문자열과 같은 원천을 쓴다(갈리면 탐지가 조용히 죽는다 — C5 왕복 시험).
- 공용 판정 committed_evidence(body)->bool. 호출처 둘: loose_requests(현행 " commit (" in read 를 교체 — 산문 오탐 제거), _spawn_worker **공통 경로**(모든 reason: wake·rework·follow-up·resume-item). 깨우기에만 세우면 워처가 옛 프롬프트로 샌다(v3 보정 2).
- 게이트 동작: 스폰은 한다. 프롬프트만 양면으로 교체 — "이 문서에 커밋 기록이 이미 있다. goal 대비 검증해 충족이면 done 으로 닫고(근거를 --note 에), 미충족이면 남은 것을 이어서 하라". 자동 done 없음.
- 카드: 카탈로그 행에 서버가 commit_drift 불리언을 싣고, web/ 카드는 그 필드로 「닫기 검토」 라벨을 그린다(클라이언트 재판정 금지). 프롬프트와 카드는 한 커밋.
- 주입 봉합: _spawn_wake·_spawn_rework 가 프롬프트에 넣는 문서 제목을 재작업 노트와 같은 <<참고>>…<</참고>> 델리미터로 감싼다.

**반영 1 — 손길(하트비트) (S~M)**
- 파일: state/heartbeat/<safe_name(canon_id(id))> (REQ 당 1개, 유계). 내용 한 줄 JSON {"actor","session"} — 소비는 mtime 만, actor 는 화면 문구·감사용.
- 쓰는 자리 **한 곳**: main() 의 args.fn(args) 직전. 조건: 명령이 쓰기 집합 {note,status,claim,last,link,set,tag} 에 속하고 getattr(args,"id",None) 이 있을 때(v3 보정 1 — show·ls·search 는 제외). tmp+os.replace 원자 치환, 전체 try/except — 실패가 원 명령을 죽이지 않는다(C9).
- 대시보드 서버·워처는 in-process 함수 호출이라 main() 을 지나지 않는다 — 오염 경로 없음(C1). 내부 subprocess 호출(_stop_note 등)은 쓰기 명령이므로 찍히지만, 그 경로는 어차피 문서 updated 를 함께 올리는 경로라 판정 변화 없음.
- 소비: stall_verdict 에서 delegated_running 다음, 최종 stalled 판정 직전 — heartbeat age < HEARTBEAT_ATTACH_WIN(상수, 기본 15분·TDD 에서 확정)이면 state=attached, why="문서는 N분째 조용하나 M분 전 손길(actor)". quiet_mins 는 그대로 낸다. moving 승격 금지(C2) — 스폰 게이트·unassigned_hands·리스 불변.
- 깨우기: attached 카드의 wake 는 거부가 아니라 확인 한 단계(기존 _wake_refusal 흐름의 message 재사용).
- gc: do_transition 의 terminal 전이에서 삭제 한 줄 + 워처 틱에서 7일 초과 파일 sweep(고아 회수).
- 한계 명기: s9 를 안 부르는 코드 작업은 여전히 안 보인다. 파일 감시로 확장하지 않는다.

**반영 2 — 귀속 선언 경고 (S, 경고-only)**
- 새 명령·레지스트리 없음. harness/claude/hooks.json 에 PreToolUse(matcher Agent) 추가, 훅 스크립트는 기존 delegate-target 파서를 불러 지명(src=지명)이 안 풀리면 stderr 경고 한 줄, exit 0(차단 아님).
- 추론 귀속은 존속하되 --guess 강등 유지. 비클레임 세션의 선언도 --guess 로 적는다(위조 선언이 추측보다 신뢰받는 역전 차단).
- 차단 승격은 관측 지표 달성 후 별도 REQ.

**계약시험 (구현 착수 전 목록 고정)**
C1 서버 in-process 경로가 하트비트를 안 만든다 · C2 하트비트는 attached 까지만(moving·스폰 게이트 불변) · C3 디렉토리 없음=현행 동일 · C4 committed+goal 미충족 → 워커가 닫지 않고 이어간다(양면 프롬프트 하중) · C5 커밋 노트 헤더 ↔ COMMIT_NOTE_RE 왕복 · C6 무관 세션 show 로 깨우기 억제 안 됨 · C7 손상·미래 mtime=무신호 · C8 committed 드리프트에 워처가 옛 프롬프트로 안 뜸(038 회귀) · C9 하트비트 쓰기 실패가 원 명령을 안 죽인다.
회귀선: test_stall_* · test_wake_* · test_platform_live · 스폰 캡 소진(REQ-021형) 시험 추가.

**남는 부채 (수용 명시)** — 귀속 추측기+전역 정지 존속 · stall_verdict 예외 사다리 미정리 · 완료 증거의 노트 텍스트 결합 · 스폰 프롬프트 3벌 중복. 관측창 후 별도 REQ 로 판단.

## History
- 2026-08-30T13:47:58+09:00 created by nicehugepark (status: published)
