---
id: REQ-20260830-007-62x6
type: request
title: 팝업이 개발자 말을 한다
summary: 깨우기·세우기 등 팝업 문구를 엔지니어가 아닌 일반 사용자 기준으로 다시 쓴다
goal: 팝업의 모든 문구가 내부 낱말(무인 작업자·본 저장소·카드의 점 등) 없이, 존댓말 한 결로, 무슨 일이 일어났고 다음에 무엇을 보면 되는지 말한다
status: review
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: a5a0449c
agents: ["sub:ux-writer:ae5f7c54", "sub:subagent:a4fb2c4b"]
contributions: [{"actor": "sub:ux-writer:ae5f7c54", "item": "팝업 문구 전수 재작성", "started": "2026-08-30T10:46:56+09:00", "ended": "2026-08-30T11:10:37+09:00", "result": "stalled", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/a5a0449c-cfa8-4c94-a5e0-31f87ada1131/tasks/ae5f7c549b80a8cc9.output", "reason": "191초째 진전 없음 (기준 180초)"}, {"actor": "sub:subagent:a4fb2c4b", "item": "Writing s9 note on REQ-20260830-007-62x6", "started": "2026-08-30T11:27:57+09:00", "ended": "2026-08-30T11:27:57+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/a5a0449c-cfa8-4c94-a5e0-31f87ada1131/subagents/agent-a4fb2c4b5b532bb91.jsonl"}, {"actor": "sub:ux-writer:ae5f7c54", "item": "`[2026-08-30 11:32:14 KST - ux-writer]`", "started": "2026-08-30T11:29:36+09:00", "ended": "2026-08-30T11:29:36+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/a5a0449c-cfa8-4c94-a5e0-31f87ada1131/subagents/agent-ae5f7c549b80a8cc9.jsonl"}]
project: section9
relates: ["REQ-20260830-008-62x6"]
relates_why: {"REQ-20260830-008-62x6": "이 창의 문구를 다시 쓰다 발견한 인접 결함이다 — 글자가 아니라 기본 초점의 문제라 따로 뗐다"}
tags: ["auto-audit", "assets", "terminal", "attached", "png", "image", "workflow"]
created: 2026-08-30T10:44:55+09:00
status_since: 2026-08-30T11:32:05+09:00
updated: 2026-08-30T13:05:26+09:00
priority: 50
---

## Original

이 팝업 이미지의 문구도, 생각해보면 일반 사용자는 무슨 내용인지 이해를 할 수가 없다. 디자이너, ux-writer 깨우기, 세우기 등등 팝업의 메시지를 개발자,엔지니어가 아닌 일반 사용자를 대상으로 다시 검토해
[Image: assets/REQ-20260830-007-62x6/20260830T104357-image.png]

## Notes

### 2026-08-30T11:27:57+09:00 subagent (agent: sub:subagent:a4fb2c4b, by nicehugepark)

Writing s9 note on REQ-20260830-007-62x6

### 2026-08-30T11:28:41+09:00 ux-writer (by nicehugepark)

### 팝업 문구 전수 재작성 — 사용자의 말로, 한 결로

**결론**: 팝업으로 뜨는 모든 문구에서 내부 낱말을 걷어내고, 서버가 지어 보내는 문장까지 존댓말 한 결로 맞췄다. 그리고 다음에 또 지어지지 않도록 **시험으로 못박았다**(`tests/test_dialog_voice.py`).

### 용어 사전 (한 낱말은 한 곳에서만)

| 내부 낱말 | 사용자의 말 | 왜 |
|---|---|---|
| `무인 작업자` | **자동 작업** | "무인"은 이 시스템이 자기 구조를 부르는 말이다. 사람이 알아야 하는 것은 "사람이 아닌 것이 스스로 한다" 하나 |
| `무인 이어받기` | **자동 이어가기** | 위와 같은 낱말 |
| `본 저장소` · `워크트리` (깨우기 창) | **뜻으로 바꿔 말한다** — "고친 것은 지금 보고 있는 이 화면에 바로 나타납니다" / "…따로 떼어 둔 사본에 있어, 커밋되기 전까지 이 화면에 나타나지 않습니다" | 깃을 모르는 사람에게 자리 이름은 읽을 것도 할 일도 아니다(REQ-20260829-030-62x6 이 다섯 번 받은 그 반려). 그래도 **뜻은 남아야 한다** — 워크트리에서 고친 화면을 9909 에서 영영 찾는 헛수고를 막는 것이 그 문장의 존재 이유였다 |
| `카드의 점` · `Stream` | **Board 탭의 카드** · **Stream 탭** | 어디를 보라는 말이 화면에 있는 이름으로 서야 한다 |
| `서브에이전트` | **작업** | |
| `머신` | **컴퓨터** | |
| `pid 424242` (손 위의 글) | 뺐다 | 창에서 사람이 할 수 있는 일이 없는 수 |
| `깨움` · `세움` (눈썹) | **깨우기** · **세우기** | 동사를 명사로 굳힌 시스템의 말. 사람이 누른 것은 「깨우기」라 그 낱말이 그대로 돌아와야 답으로 읽힌다 |

`깨우기`·`세우기` 자체는 **바꾸지 않았다** — 카드 버튼에 같은 글자가 박혀 있어 창만 고치면 갈린다. `세우다`는 국어에서 도는 기계를 멈춘다는 뜻으로 이미 쓰이고, 대안 `멈추기`는 멈춘 카드의 `멈춤 44분` 과 한 화면에서 부딪친다(상태와 행동이 같은 낱말이 된다).

### before / after — 사용자가 캡처로 보낸 그 창

- **전**: 「REQ-20260830-003-62x6 를 깨웠다 — 무인 작업자가 본 저장소에서 이어받는다. 진행은 카드의 점과 Stream 에서 보인다.」
- **후**: 「이 요청을 깨웠습니다 — 자동 작업이 이어서 진행합니다. 고친 것은 지금 보고 있는 이 화면에 바로 나타납니다. 진행 상황은 Board 탭의 카드와 Stream 탭에서 볼 수 있습니다.」

문서 번호를 본문에서 뺐다 — 창머리가 이미 주소를 달고 있다(REQ-20260828-007-62x6 이 판정 창에서 세운 "주소는 머리에, 말은 본문에"). 기계가 읽을 번호는 응답의 `id` 에 그대로 있다.

### 그 밖에 고친 문장 (전 → 후)

| 자리 | 전 | 후 |
|---|---|---|
| 세우기 확인 | 이 요청을 맡아 도는 무인 작업자를 세울까요? | 진행 중인 자동 작업을 세울까요? |
| 이미 도는 중 | 무인 작업자가 이미 이 요청을 맡아 돌고 있다 (3분 전 기동). | 자동 작업이 이미 이 요청을 진행하고 있습니다 (3분 전 시작했습니다). 어디까지 왔는지는 Stream 탭에서 볼 수 있습니다. |
| 차례 대기 | … — 차례를 기다린다(30초마다 다시 본다). | … — 차례를 기다립니다. 앞 작업이 끝나면 30초 안에 저절로 시작하니 그대로 두셔도 됩니다. |
| 누가 붙어 있음 | 이미 designer 가 붙어 있다 (2분 전 움직였다). 겹쳐 띄우면 같은 파일에 두 번째 손이 붙는다. | 이미 designer 가 이 요청을 맡고 있습니다 (2분 전 움직였습니다). 그 일이 끝난 뒤에 다시 눌러 주세요. |
| 아직 안 멈춤 | 아직 멈춘 게 아니다 — 3분 전 문서가 움직였다. 15분 넘게 조용할 때만 깨운다. | 아직 멈추지 않았습니다 — 3분 전 이 요청이 움직였습니다. 15분 넘게 아무 움직임이 없을 때만 깨울 수 있습니다. |
| 한도 소진 | 오늘 사람이 누른 깨우기 한도를 다 썼다 … 계정 설정의 auto_resume_wake_per_day 를 늘려라 | 오늘 깨울 수 있는 횟수를 다 썼습니다 … Settings 의 계정 설정에서 auto_resume_wake_per_day 를 올려 주세요 |
| 쿨다운 | 방금 이 요청에 작업자를 띄웠다 — 4분 뒤에 다시 눌러라 (쿨다운 600초) | 방금 이 요청을 한 번 깨웠습니다 — 4분 뒤에 다시 눌러 주세요 |
| 옵트인 꺼짐 | … 무인 이어받기가 꺼져 있다 — 설정에서 auto_resume 을 켜야 깨울 수 있다 | … 자동 이어가기가 꺼져 있습니다 — Settings 의 계정 설정에서 auto_resume 을 켜야 깨울 수 있습니다 |
| 끝난 문서 | … 는 이미 done 다 — 끝난 문서에는 무인 작업자를 띄우지 않는다. 할 일이 남았으면 새 요청으로 열어라. | … 는 이미 done 상태입니다 — 끝난 요청은 다시 진행하지 않습니다. 할 일이 남았으면 새 요청으로 올려 주세요. |
| 다른 머신 | 이 요청은 다른 머신(X)의 것이다 — 여기서는 띄우지 않는다 | 이 요청은 다른 컴퓨터(X)의 것입니다 — 여기(Y)서는 시작할 수 없습니다 |
| 세우기 결과 | REQ-… 의 무인 작업자를 세웠다 (pid 4242). 사유는 문서에 남겼다. | 이 요청의 자동 작업을 세웠습니다. 멈춘 사실과 사유는 문서에 남겼습니다. |
| 계정·모델 창 | 도는 무인 작업자 2건을 세우고 … 바꿉니다 | 진행 중인 자동 작업 2건을 세우고 … 바꿉니다 |
| 순서 창 | 위에 둔 것이 다음에 집힙니다 — 이어받기와 무인 작업자가 이 순서를 봅니다. | 위에 둔 것을 다음에 집습니다 — 자동 작업도 이 순서를 따릅니다. |
| 작업 자리 창 (사유·푸는 법 11줄) | …커밋되지 않았다 / 커밋하면 다음 작업자부터 다시 워크트리로 간다 | …커밋되지 않았습니다 / 커밋하면 다음 작업부터 다시 워크트리로 갑니다 |
| 카드 줄 | `작업 중` 무인 작업자가 12분째 돌고 있다 | `진행 중` 자동 작업 12분째 |

작업 자리 창의 `본 저장소`·`워크트리` 는 **그대로 뒀다** — 사용자가 "문서에 포함은 되어도 상관없다"고 한 그 한 자리이고, 거기서 이름을 빼면 그 창이 무엇을 가리키는지 말할 것이 없어진다. 대신 그 창의 열한 줄이 반말이던 것을 존댓말로 맞춰 한 창 안에서 말이 갈리지 않게 했다.

### 한 낱말이 두 곳에 있는 자리를 묶었다

자리의 뜻을 말하는 문장은 서버(`bin/s9`)와 화면(`web/app/card.js` 의 `WS_MEANS`) 두 런타임에 있어야 해서 문자열 하나를 나눠 가질 수 없다. 그래서 서버에 `WS_MEANS_KO` 상수를 세워 한 곳으로 모으고, **둘이 같은 말인지를 시험이 지킨다**(V3). 서버가 자리 **이름**을 짓던 `_workspace_text()` 는 뜻을 짓는 `_workspace_means()` 로 바뀌었다.

### 시험 (새로 만듦) — `tests/test_dialog_voice.py`

`python3 tests/ dialog_voice` · 6건 전부 통과.

- **V1 / V1b** 팝업에 서는 문장에 내부 낱말이 없다. 화면은 `s9dlg(...)` 호출 안의 문자열 282개 + 창 껍데기(`s9dlg`·`s9choose`) + 창이 쓰는 상수(`WS_MEANS`·`WS_WHY`·`DLG_ATTACH_HINT` 등), 서버는 `message`·`why`·`block()`·`out()` 자리를 ast 로 걷어 잰다. **주석은 건너뛴다** — 이 저장소의 주석은 반려문을 그대로 인용하고 있어서 함께 재면 계약이 자기 근거 때문에 깨진다.
- **V2 / V2b** 말결이 하나다 — `…한다`·`…없다`·`…눌러라` 를 잡고 `…습니다` 는 지난다.
- **V3** 서버와 화면이 자리의 뜻을 같은 말로 말한다.
- **V4** 창머리가 사람이 누른 낱말(`깨우기`·`세우기`)을 그대로 쓴다.

검증: 사용자가 캡처로 보낸 그 문장을 이 검사에 넣으면 반말 셋(`깨웠다`·`이어받는다`·`보인다`)과 금지 낱말 셋(`무인 작업자`·`카드의 점`·`본 저장소`)을 정확히 집어낸다.

갱신한 기존 시험 넷 (전부 문구 계약이 바뀐 자리):
- `test_wake.py` W1 — 창 본문의 문서 번호 → 응답 `id` 로 (본문이 주소를 되풀이하지 않는다는 계약으로 뒤집음)
- `test_spawn_workspace.py` V3 — 자리 **이름**이 아니라 `WS_MEANS_KO` 의 **뜻**이 실렸는지로
- `test_wake_effect.py` R5b · `test_wake_reach.py` W6b · `test_workspace_chip.py` W16 — 바뀐 낱말로

### 진단 손잡이 하나 추가

`?work=12&dlg=stopask` — 세우기의 묻는 창을 실제로 연다. 이 창은 도는 작업이 있어야만 열리는데 캡처를 찍으려는 그 순간에는 대개 없다(`wsat`·`priolive` 가 낸 그 길). **확인은 대신 누르지 않으므로 `/api/stop` 은 나가지 않는다.** 평소 주소에서는 아무 일도 하지 않는다.

### 시험 결과

관련 스위트 371건 전부 통과: `dialog_voice wake wake_effect wake_reach wake_handle spawn_workspace stop_reach worker_stop workspace_chip stall_pair stall_trust stall_visible closed_no_worker auto_resume_gate claim_clock judge_dialog judge_attach_kinds web_split anchor_note session_pick account_and_model priority_handle`.

전체 스위트(2335건)에서는 훅 계열 스위트 여럿이 함께 넘어졌으나 — `response_stamp`·`question_queue`·`subagent_attr`·`session_attach`·`project_agents`·`session_mirror_switch`·`question_qst` — **따로 돌리면 55건 전부 통과**한다(이 코드와 닿는 자리가 없고, 훅 설정 파일을 함께 만지는 스위트끼리의 간섭이다).

### 넘기는 것 하나

세우기 확인 창은 맨 Enter 가 `세우기` 에 닿는다(`safe` 를 켜지 않았다). 되돌릴 수 없는 쪽에 어려운 키를 준다는 이 화면의 규칙과는 어긋나 보이지만, 세우기는 카드가 멈춤으로 바뀐 뒤 깨우기로 되살릴 수 있어 되돌릴 수 없는 일이 아니다 — 문구만 고치는 이번 범위에서는 손대지 않았다. 바꿀 값이 있다고 보면 별건으로 여는 것이 맞다.

### 캡처 (실브라우저, 9909)

깨우기 성공 · 차례 대기 · 작업 자리 · 세우기 확인 · 다크 tone 순으로 붙인다.

[Image: assets/REQ-20260830-007-62x6/02-wakespawn.png]
[Image: assets/REQ-20260830-007-62x6/04-wakewait.png]
[Image: assets/REQ-20260830-007-62x6/06-wsat.png]
[Image: assets/REQ-20260830-007-62x6/10-stopask.png]
[Image: assets/REQ-20260830-007-62x6/11-wakespawn-dark.png]

### 2026-08-30T11:29:36+09:00 subagent (agent: sub:ux-writer:ae5f7c54, by nicehugepark)

`[2026-08-30 11:32:14 KST - ux-writer]`

팝업 문구 전수 재작성을 끝냈다. 보고는 `bin/s9 note REQ-20260830-007-62x6 --label ux-writer` 로 캡처 5장과 함께 문서에 남겼다.

### 결론

사용자가 캡처로 보낸 그 문장이 병 셋을 다 갖고 있었고, 셋 다 **문구가 아니라 자리 문제**였다 — 창을 짓는 곳이 서버와 화면 둘로 나뉘어 있어 서버가 반말·내부 낱말로 쓰면 창이 그대로 옮겨 적는다. 그래서 문장을 고치고 **시험으로 못박았다**.

**전** 「REQ-20260830-003-62x6 를 깨웠다 — 무인 작업자가 본 저장소에서 이어받는다. 진행은 카드의 점과 Stream 에서 보인다.」
**후** 「이 요청을 깨웠습니다 — 자동 작업이 이어서 진행합니다. 고친 것은 지금 보고 있는 이 화면에 바로 나타납니다. 진행 상황은 Board 탭의 카드와 Stream 탭에서 볼 수 있습니다.」

### 용어 사전

`무인 작업자`→**자동 작업** · `무인 이어받기`→**자동 이어가기** · `본 저장소`/`워크트리`(깨우기 창)→**뜻으로**("이 화면에 바로 나타납니다"/"…커밋되기 전까지 나타나지 않습니다") · `카드의 점`→**Board 탭의 카드** · `Stream`→**Stream 탭** · `서브에이전트`→**작업** · `머신`→**컴퓨터** · `깨움`/`세움`(눈썹)→**깨우기**/**세우기** · `pid`→뺌.

`깨우기`·`세우기` 동사 자체는 **바꾸지 않았다** — 카드 버튼에 같은 글자가 박혀 있어 창만 고치면 갈리고, 대안 `멈추기` 는 멈춘 카드의 `멈춤 44분` 과 한 화면에서 부딪친다(상태와 행동이 같은 낱말이 된다).

작업 자리 창의 `본 저장소`·`워크트리` 는 남겼다 — 사용자가 "문서에 포함은 되어도 상관없다"고 한 그 한 자리다. 대신 그 창 열한 줄의 반말을 존댓말로 맞췄다.

### 한 낱말 한 곳

자리의 뜻은 서버·화면 두 런타임에 있어야 해 문자열을 나눠 가질 수 없다. 서버에 `WS_MEANS_KO` 상수를 세워 모으고, **둘이 같은 말인지를 시험(V3)이 지킨다**. `_workspace_text()`(자리 이름) → `_workspace_means()`(자리의 뜻).

### 시험

`/home/sjpark1/section9/tests/test_dialog_voice.py` (새로 만듦, 6건 통과)
- V1/V1b 내부 낱말 없음 — 화면은 `s9dlg(...)` 안 문자열 282개 + 창 껍데기 + 창이 쓰는 상수, 서버는 `message`/`why`/`block()`/`out()` 자리를 ast 로 걷는다. **주석은 건너뛴다**(이 저장소 주석은 반려문을 인용하고 있어 함께 재면 계약이 자기 근거로 깨진다).
- V2/V2b 말결 하나 · V3 두 런타임 동일 문장 · V4 눈썹 = 누른 낱말.
- 검증: 사용자가 보낸 원문을 넣으면 반말 셋(`깨웠다`·`이어받는다`·`보인다`)과 금지 낱말 셋을 정확히 집는다.

갱신한 기존 시험 넷: `test_wake.py`(W1 — 본문이 주소를 되풀이하지 않는다로 뒤집음) · `test_spawn_workspace.py`(V3 — 이름 대신 뜻) · `test_wake_effect.py`(R5b) · `test_wake_reach.py`(W6b) · `test_workspace_chip.py`(W16).

관련 스위트 **371건 전부 통과**. 전체(2335건)에서는 훅 계열 7스위트가 함께 넘어졌으나 따로 돌리면 55건 전부 통과 — 이 코드와 닿는 자리가 없고 훅 설정 파일을 함께 만지는 스위트끼리의 간섭이다.

### 진단 손잡이 하나 추가

`?work=12&dlg=stopask` — 세우기의 묻는 창을 실제로 연다(`wsat`·`priolive` 가 낸 길). 확인을 대신 누르지 않으므로 `/api/stop` 은 나가지 않고, 평소 주소에서는 아무 일도 하지 않는다.

### 만진 파일 (전부 Edit 문장 단위 · `git add`/`checkout`/`stash` 안 씀)

`/home/sjpark1/section9/bin/s9` · `/home/sjpark1/section9/web/app/card.js` · `diag.js` · `dialog.js` · `restart.js` · `session.js` · `/home/sjpark1/section9/tests/test_dialog_voice.py`(신규) · `test_wake.py` · `test_wake_effect.py` · `test_wake_reach.py` · `test_spawn_workspace.py` · `test_workspace_chip.py`

`git diff --stat` 으로 확인했다 — 다른 세션이 살려 둔 `const.js`·`events.js`·`tidy.js`·`notice.js`·`docs.js`·`board.css`·`tidy.css`·`test_doc_tidy.py` 의 편집과 `bin/s9` 의 `work_order`·`cmd_set`·`cmd_serve` 훅에는 손대지 않았다.

### 캡처 (실브라우저 9909, 재기동 안 함)

`/tmp/claude-1000/-home-sjpark1-section9/a5a0449c-cfa8-4c94-a5e0-31f87ada1131/scratchpad/` 아래 `02-wakespawn.png`(깨우기 성공) · `04-wakewait.png`(차례 대기) · `06-wsat.png`(작업 자리) · `10-stopask.png`(세우기 확인) · `11-wakespawn-dark.png`(다크 tone). 다섯 장 다 문서에 붙였다.

### 넘기는 것 하나

세우기 확인 창은 맨 Enter 가 `세우기` 에 닿는다(`safe` 미설정). 다만 세우기는 카드가 멈춤으로 바뀐 뒤 깨우기로 되살릴 수 있어 되돌릴 수 없는 일은 아니다 — 문구만 고치는 이번 범위에서는 손대지 않았다. 값이 있다고 보면 별건이다.

### 2026-08-30T11:31:56+09:00 response (by nicehugepark)

리드 검수 — ux-writer 의 작업을 캡처와 시험으로 확인했다.

**확인한 것**
- 캡처 다섯 장을 직접 열어 봤다. 깨우기 창은 「이 요청을 깨웠습니다 — 자동 작업이 이어서 진행합니다. 고친 것은 지금 보고 있는 이 화면에 바로 나타납니다. 진행 상황은 Board 탭의 카드와 Stream 탭에서 볼 수 있습니다.」로 서고, 세우기 창도 존댓말·다음 행동까지 말한다. 내부 낱말과 반말이 실제 화면에서 사라졌다.
- 관련 스위트를 다시 돌렸다 — `dialog_voice · wake · wake_effect · wake_reach · spawn_workspace · workspace_chip · doc_tidy` **139건 전부 통과**.
- `git diff --stat` 으로 범위를 봤다. 다른 세션이 살려 둔 편집(`const.js`·`events.js`·`notice.js`·`board.css` 등)은 그대로다.

**한 자리를 남긴 판단**: 작업 자리 창(`?dlg=wsat`)의 제목은 아직 `본 저장소` 라는 낱말을 쓴다. 그 창은 문서 메타 표의 `workspace` 칸을 눌렀을 때 그 칸을 설명하는 창이라, 칸의 낱말과 창의 낱말이 갈라지면 사람이 배울 것이 둘이 된다. **그 낱말을 아예 없앨지는 REQ-20260829-030-62x6 의 판정 자리**다(지금 review 에 올라가 있다) — 거기서 "헤더 것까지 빼라"가 나오면 이 창도 함께 뜻으로만 말하게 바꾼다. 두 문서가 같은 낱말을 두고 각자 결정하면 또 갈린다.

**따로 뗀 것**: 세우기 확인 창의 맨 Enter 가 「그대로 두기」가 아니라 「세우기」에 닿는다 → REQ-20260830-008-62x6. 되돌릴 수 있는 일이라 이번 범위(문구)에서 손대지 않았다.

### 2026-08-30T13:05:26+09:00 note (by nicehugepark)

인접 알림 (REQ-20260829-024-62x6 라운드4에서 리드가 남김). 이 문서가 판정을 기다리는 동안 사용자가 024 를 반려하며 "깨우기, 세우기 라는 용어가 너무 어색한데 ux-writer, designer 다시 검토해줘" 라고 했다 — 이 문서가 "「깨우기」·「세우기」 자체는 바꾸지 않았다"고 적은 그 판단이 뒤집힌 것이다.

그래서 024 에서 낱말을 바꿨다: 깨우기 → **이어가기**, 세우기 → **중단하기**, 세션 창의 「멈추고 바꾸기」 → 「중단하고 바꾸기」. 이 문서가 세운 논거(세우다는 도는 기계를 멈춘다는 뜻이 있고, 멈추기는 상태 낱말 `멈춤` 과 부딪친다)는 그대로 지켰다 — `멈춤` 은 저절로 조용해진 상태, `중단` 은 사람이 끝낸 행동으로 주체를 갈랐다. 용어 사전(무인 작업자→자동 작업 등)과 존댓말 한 결은 손대지 않았다.

이 문서가 낸 시험 `tests/test_dialog_voice.py` 의 V4 도 함께 고쳤다. 창머리와 버튼이 같은 낱말인지를 **글자로** 비교하던 것을, 둘이 같은 상수(WAKE_LABEL·STOP_LABEL)를 읽는지로 세웠다 — 낱말이 두 곳에 글자로 있어서 이번 개명 도중 실제로 갈렸기 때문이다(처음 그려진 글자와 눌렀다 돌아온 글자가 달랐다). 계약은 세졌고 문장은 그대로다.

판정하실 때 이 사실을 함께 보시면 된다: 이 문서의 before/after 표에 적힌 「깨우기」·「세우기」는 지금 화면에서 「이어가기」·「중단하기」로 서 있다.

## History
- 2026-08-30T10:44:55+09:00 created by nicehugepark (status: open)
- 2026-08-30T10:46:12+09:00 status: open -> in-progress (by nicehugepark) — ux-writer 에 위임 — 문구 전수 검토
- 2026-08-30T11:32:05+09:00 status: in-progress -> review (by nicehugepark) — 팝업 문구를 사용자 말로 다시 썼습니다. F5 후 Board 에서 멈춘 카드의 [깨우기]와 도는 카드의 [세우기]를 눌러 보세요. 창이 존댓말로 말하고, 무슨 일이 일어났는지에 이어 "어디를 보면 되는지"까지 알려 주면 승인입니다. 아직 무슨 말인지 모르겠는 문장이 있으면 그 문장을 그대로 적어 반려해 주세요.
