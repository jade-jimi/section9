---
id: REQ-20260824-022
type: request
title: 스트림 열람 격리
summary: Stream 탭(/api/stream,/api/streams)이 무필터로 남아 세션 원문 우회 열람 가능 — 017 가시성 규칙을 스트림에도 적용(세션 문서 가시성 기준). 017 검수에서 식별된 후속
goal: Stream 탭이 017과 동일한 가시성 규칙을 따라 비멤버 세션 원문 열람이 차단된다
status: done
size: S
user: sjpark1
machine: DESKTOP-TEHV1KR
session: 104b4fe3
agents: ["backend-developer", "subagent"]
project: section9
parent: REQ-20260824-017
tags: ["access", "dashboard", "session"]
created: 2026-08-24T14:59:20+09:00
status_since: 2026-08-24T18:00:18+09:00
updated: 2026-08-24T18:00:18+09:00
---

## Original

(017 한계 항목에서 파생)

## Notes

### 2026-08-24T17:56:55+09:00 tdd (agent: backend-developer, by sjpark1)

### TDD 시나리오
- [ ] S1. 목록 필터: /api/streams 가 비멤버 시점(as=bob)에서 타인(alice) 세션 스트림을 목록에서 제외
- [ ] S2. 직접 조회 404: 비가시 스트림을 /api/stream?session= 으로 직접 조회하면 404 + 미존재 스트림과 동일 응답(존재 여부 비누설)
- [ ] S3. admin 전체 열람: admin 시점 /api/streams 는 전체 목록, /api/stream 전부 200
- [ ] S4. 본인·프로젝트 멤버 허용: 본인 세션(무소속 SES=작성자) 200+목록 포함, SES가 project 소속이면 활성 멤버도 200 (비admin whoami 서버의 기본 자기 시점 포함)
- [ ] S5. SES 문서 없는 스트림 = 보수 기본: 그 세션 바인딩의 user 본인·admin만 가시, 바인딩도 없으면 admin만
- [ ] S6. 회귀: 가시 스트림의 /api/stream 파싱(events/offset/after 증분)이 기존과 동일 동작 + 기존 테스트 스위트 전부 OK

### 2026-08-24T17:57:09+09:00 subagent (agent: subagent, by sjpark1)

Noting TDD scenarios on REQ-20260824-022

### 2026-08-24T17:59:13+09:00 tdd (agent: backend-developer, by sjpark1)

### TDD 시나리오 (최종 상태 — tests/test_stream_isolation.py 6/6 OK)
- [x] S1. 목록 필터: /api/streams 가 비멤버 시점(as=bob)에서 타인(alice) 세션 스트림을 목록에서 제외
- [x] S2. 직접 조회 404: 비가시 스트림 /api/stream 직접 조회 404 + 미존재와 응답 본문까지 동일(존재 여부 비누설), 비admin의 as= 상승 차단
- [x] S3. admin 전체 열람: /api/streams 전체 목록 + 전 스트림 /api/stream 200
- [x] S4. 본인·프로젝트 멤버 허용: 본인 세션(무소속 SES=작성자) 및 project 소속 SES의 활성 멤버 200+목록 포함, 비admin whoami 서버 기본 자기 시점 포함
- [x] S5. SES 문서 없는 스트림 = 보수 기본: 바인딩 user 본인·admin만, 바인딩도 없으면 admin만
- [x] S6. 회귀: 가시 스트림 파싱(events/offset/after 증분) 기존 동작 + 기존 13종 스위트 개별 실행 전부 OK

### 2026-08-24T17:59:34+09:00 backend (agent: backend-developer, by sjpark1)

구현 완료 (backend). 변경: bin/s9 두 곳.
[1] stream_visible(session, me, rows=None) 신설 — doc_visible 바로 옆, 같은 가드레일 모델 재사용(새 권한 모델 없음): admin=전부 / 미등록 뷰어=비강제(doc_visible·project_can의 부트스트랩 원칙과 동일) / sid8에 대응하는 SES 문서(catalog type=session, session[:8] 매칭)가 있으면 doc_visible 그대로(무소속=작성자만, project 소속=활성 멤버만) / SES 문서가 없으면 그 세션 바인딩(state/sessions/*__sid8.json)의 user 본인만(보수적 기본 — 바인딩도 없으면 admin만). rows 파라미터는 목록 필터가 스트림마다 catalog를 재로드하지 않게 하는 재사용 훅.
[2] GET 핸들러: /api/streams 는 viewer_of(qs)(서버 파생 whoami + admin 전용 ?as=) 기준으로 streams 배열을 필터, /api/stream 은 비가시 시 stream_events 를 호출하지 않고 미존재와 동일한 404 {error: stream not found} (존재 여부 비누설 — /api/doc·reqstream 패턴 동일). 필터는 HTTP 응답 직전에만 — streams_list/stream_events 내부 반환은 무필터 유지(워처·chat 로직 불변).
goal 충족 근거: 비멤버 세션 원문 우회 열람 경로였던 /api/streams·/api/stream 이 017의 doc_visible 규칙(SES 문서 가시성)과 동일 기준으로 차단됨 — 테스트 S1(목록 제외)·S2(직접 조회 404)·S5(SES 없는 스트림 보수 기본)로 검증, 허용측 S3(admin)·S4(본인/멤버)·S6(파싱 회귀)로 과차단 없음 확인. 전체 회귀: tests/test_*.py 14종 개별 실행 전부 OK (stdlib-only 가드 포함). 클라이언트(web/index.html)는 무수정 — Stream/Chat 탭은 서버가 걸러준 {streams:[...]} 동일 스키마를 소비.
라이브 9909 재시작 완료(pid 4015850): root 200, admin 스트림 18개 전체·개별 200, ?as=user1 은 목록 0개·직접 조회 404 — 서버 계정 sjpark1=admin이라 기존 사용자 체감 변화 없음.
한계: viewer 신원은 서버 파생 whoami 기반 가드레일이며 실인증 아님(017과 동일, DOC-20260823-001 정책 연장). SES session 매칭은 sid8 prefix — 8자 충돌 시 두 SES 중 하나라도 가시면 허용(any).

### 2026-08-24T17:59:47+09:00 subagent (agent: subagent, by sjpark1)

Moving REQ-20260824-022 to review

### 2026-08-24T17:59:53+09:00 subagent (agent: backend-developer, by sjpark1)

### 최종 보고 — REQ-20260824-022 스트림 열람 격리

### 결론
Stream 탭 API가 REQ-20260824-017의 `doc_visible` 규칙과 동일한 격리를 따르게 됐다. 비멤버는 타인 세션 스트림을 목록에서 볼 수 없고, 직접 조회해도 미존재와 동일한 404를 받는다. TDD 6/6, 전체 회귀 14종 OK, 라이브 9909 재기동·실측 완료. 상태는 `review`로 전이했다(리드 검수 대기).

### TDD 시나리오 체크 (tests/test_stream_isolation.py — Red 3건 확인 후 Green 6/6)
- [x] S1. 목록 필터: `/api/streams`가 비멤버(as=bob) 시점에서 alice 개인 세션·px 프로젝트 세션·기록 없는 세션을 제외
- [x] S2. 직접 조회 404: 비가시 스트림 404 + 미존재와 응답 본문까지 동일(존재 여부 비누설), 비admin의 `?as=` 상승 차단
- [x] S3. admin 전체 열람: 목록 전체 + 전 스트림 200
- [x] S4. 본인·멤버 허용: 무소속 SES=작성자 본인, project 소속 SES=활성 멤버 200, 비admin whoami 서버의 기본 자기 시점 포함
- [x] S5. SES 문서 없는 스트림 보수 기본: 바인딩 user 본인·admin만, 바인딩도 없으면 admin만
- [x] S6. 회귀: 파싱(events/offset/after 증분) 기존 동작 유지

### 테스트 실행 요약
- `python3 tests/test_stream_isolation.py`: Red에서 S1/S2/S5 실패(무필터 확인) → 구현 후 **6/6 OK** (허용측 S3/S4/S6는 Red 시점에도 통과 — 과차단 없음의 기준선)
- `for f in tests/test_*.py; do python3 "$f"; done`: **14종 전부 OK** (access_isolation, dashboard_chat, whoami, stdlib_only 가드 포함)
- 라이브 9909(pid 4015850, kill 후 nohup 재기동): root 200, admin 스트림 18개·개별 200, `?as=user1` 목록 0개·직접 조회 404. 서버 계정 sjpark1=admin이라 기존 사용자 체감 변화 없음.

### 변경 파일·함수
- `/home/sjpark1/section9/bin/s9`
  - `stream_visible(session, me, rows=None)` 신설 (`doc_visible` 직후, 모듈 레벨): admin=전부 / 미등록 뷰어=비강제(기존 부트스트랩 원칙 동일) / sid8 대응 SES 문서 있으면 `doc_visible` 위임 / 없으면 세션 바인딩 user 본인만. 새 권한 모델 없이 기존 헬퍼 재사용.
  - GET `/api/streams` 핸들러: `viewer_of(qs)` 기준 목록 필터 (catalog 1회 로드 재사용)
  - GET `/api/stream` 핸들러: 비가시 시 `stream_events` 미호출, 미존재와 동일 404 (`/api/doc` 패턴)
- `/home/sjpark1/section9/tests/test_stream_isolation.py` 신규 (S9_ROOT=mktemp 격리, admin/비admin 이중 서버 픽스처)
- `web/index.html` 무수정 — 서버 응답 스키마(`{streams:[...]}`) 불변으로 Stream/Chat 탭 회귀 없음(dashboard_chat 스위트로 확인)

### 한계
- viewer 신원은 서버 파생 whoami 가드레일이며 실인증 아님 (017과 동일, DOC-20260823-001 정책 연장)
- SES 매칭은 sid8 prefix — 8자 충돌 시 해당 SES 중 하나라도 가시면 허용(`any`)

## History
- 2026-08-24T14:59:20+09:00 created by sjpark1 (status: open)
- 2026-08-24T17:54:01+09:00 status: open -> in-progress (by sjpark1) — 리드 착수 — backend 워커 위임 (스트림 API 가시성 필터)
- 2026-08-24T17:59:34+09:00 status: in-progress -> review (by sjpark1) — TDD 6/6(tests/test_stream_isolation.py) + 회귀 14종 전부 OK, 라이브 9909 재기동·격리 실측(admin 18/200, user1 0/404) — goal(비멤버 세션 원문 열람 차단) 충족, 리드 검수 대기
- 2026-08-24T18:00:18+09:00 status: review -> done (by sjpark1) — goal 충족: 비멤버는 /api/streams 목록에서 제외되고 /api/stream 직접 조회도 미존재 동일 404 (017 doc_visible 규칙 재사용, stream_visible). 리드 재검증: test_stream_isolation 6/6 + 회귀 14종 OK + 라이브 실측(admin 18개·as=user1 0개/404, chat 라우트 무영향). 객관 검증 완료라 review 대신 done
