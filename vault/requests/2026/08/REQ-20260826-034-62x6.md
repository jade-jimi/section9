---
id: REQ-20260826-034-62x6
type: request
title: 첨부가 질문 표지를 가린다
summary: classify가 마지막 줄로 질문을 판정하는데 [Image:] 첨부 줄이 물음표를 밀어낸다
goal: 캡처를 붙여 물어도 질문으로 분류된다
status: done
size: S
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: adf77fd2
sessions: ["8e60e4af", "796fabb5", "c34b322f", "adf77fd2"]
agents: ["lead:claude-opus-5", "worker:rework"]
contributions: [{"actor": "lead:claude-opus-5", "item": "`[2026-08-27 00:26:36 KST - lead]`", "started": "2026-08-27T00:26:36+09:00", "ended": "2026-08-27T00:26:36+09:00", "result": "done", "transcript": ""}, {"actor": "worker:rework", "item": "재작업 검증", "started": "2026-08-27T00:35:03+09:00", "ended": "2026-08-27T00:35:03+09:00", "result": "done", "transcript": ""}]
project: section9
children: ["REQ-20260828-025-62x6"]
relates: ["QST-20260826-003-62x6", "REQ-20260826-031-62x6", "REQ-20260827-006-62x6"]
tags: ["assets", "audit", "dashboard"]
created: 2026-08-26T22:48:55+09:00
status_since: 2026-08-27T09:16:13+09:00
updated: 2026-08-28T16:32:31+09:00
priority: 50
---

## Original

bin/s9-audit-prompt classify() 는 QUESTION_TAIL 을 text.splitlines()[-1] 에서 찾는다. 이미지/파일을 붙이면 원문 마지막 줄이 `[Image: …]` 가 되어 물음표가 사라지고, 질문이 request 로 분류된다. 실사고: REQ-20260826-032 "이 요청은 붉은색 점은 무슨 의미야?" — 질문인데 request 카드가 됐다. 첨부 참조 줄을 판정 전에 걷어내야 한다.

## Notes

### 2026-08-26T23:55:01+09:00 response (by nicehugepark)

분류는 질문 표지(물음표·종결어미)를 **마지막 줄**에서 찾는다. 그런데 화면을 캡처해 붙이면 원문의 마지막 줄이 `[Image: /경로]` 가 되어 물음표가 밀려난다. 그래서 명백한 질문이 request 카드가 됐다. 실사고 둘, 같은 날 한 시간 안에:

- "이 요청은 붉은색 점은 무슨 의미야? 녹색점멸도 아니고" → REQ-20260826-032-62x6
- "idle인 이유가 뭐라고?" → REQ-20260826-031-62x6

**이게 특히 나쁜 이유**: 사람은 화면이 이상할 때 캡처해서 묻는다. 즉 **가장 물어보고 싶을 때 정확히 이 경로를 밟는다.** 질문 타입을 만들어 놓고 캡처가 붙은 질문만 골라 놓치면, 그 타입이 가장 필요한 자리에서 비어 있게 된다.

**고침**(`bin/s9-audit-prompt`): 분류 전에 첨부 참조 줄을 걷어낸다 — **첨부는 분류의 재료가 아니다.** `strip_attachment_refs()` 는 `[Image: …]`·`[Image #1]`·`[첨부: …]` 형태를 줄 단위로 제거한다. 첨부뿐인 메시지는 원문 그대로 둔다(걷어내면 빈 문자열이 되고, 빈 문자열은 엉뚱한 갈래로 떨어진다).

길이 판정(`is_durable_question`)도 같이 고쳤다. 경로 한 줄이 짧은 확인 발화를 20자 문턱 위로 밀어 올리면, 남길 값어치가 없는 발화가 문서가 된다 — 반대 방향의 잡음이다.

판정 함수는 훅이 소유하고 채팅이 빌려 쓰므로(REQ-20260826-033-62x6), **두 입구에 동시에 적용된다.**

**테스트** `tests/test_attachment_classify.py` 6건: 그날의 두 발화가 질문으로 잡힘(T1) · 첨부 유무가 판정을 바꾸지 않음(T2) · 붙여넣기 형태 `[Image #1]` 도 같음(T3) · **첨부 붙은 요청은 그대로 요청**(T4, 반대 방향 유실 금지) · 첨부뿐이면 기존 동작(T5) · 길이도 첨부 빼고 셈(T6).

### 2026-08-26T23:55:01+09:00 tdd (by nicehugepark)

- [ ] T1. 그날 오분류된 두 발화가 질문으로 잡힌다
- [ ] T2. 첨부 유무가 판정을 바꾸지 않는다
- [ ] T3. 붙여넣기 형태(`[Image #1]`)도 걷힌다
- [ ] T4. 첨부 붙은 요청은 그대로 요청 (반대 방향 유실 금지)
- [ ] T5. 첨부뿐이면 원문 그대로 (기존 동작 보존)
- [ ] T6. 길이 판정도 첨부를 빼고 센다

### 2026-08-27T00:23:48+09:00 response (by nicehugepark)

반려 사유("REQ-20260827-006-62x6 요청 문서가 생겼다")는 관찰로는 정확했지만, 원인은 이 고침이 아니었다. **채팅을 처리하는 서버가 22:43 코드로 돌고 있었다.**

증명: 그 메시지 **원문 그대로**를 지금 코드에 넣었다.

    chat_audit("이거 시킨대로 하는건데 이렇게 하는거 맞아?\n[Image: …001811-image.png]")
      → 반환 None (요청 문서를 만들지 않았다)
      → 만들어진 문서: QST-…  「이거 시킨대로 하는건데 이렇게 하는거 맞아?」

코드는 옳았고 돌고 있는 서버가 낡았다. **00:22 에 서버를 올렸다**(감시자가 재기동, pid 729699).

**내 잘못이 하나 있다.** 첫 상신 노트에 "서버가 옛 코드라 재시작 후 반영된다"를 **맨 끝에 덧붙였다.** 확인 절차를 먼저 읽고 그대로 해 본 사람에게 그 한 줄은 사실상 없는 것과 같다. 확인 요청의 첫 줄이어야 했다 — 아니, 애초에 내가 먼저 서버를 올리고 나서 상신했어야 했다. 사용자가 시킨 대로 했는데 안 되는 상황을 만든 것은 순전히 내 순서 실수다.

**교훈은 문장이 아니라 순서다**: 채팅 경로(서버가 처리하는 것)를 고쳤으면 **상신 전에 서버를 올린다.** 오늘 채팅 경로를 셋 고쳤는데(REQ-20260826-033-62x6 질문 문서화 · 034 첨부 분류 · 040 근접 연관) 셋 다 낡은 서버 아래서는 아무 일도 하지 않았다. "고쳤다"와 "돌고 있다"는 다른 말이다.

### 2026-08-27T00:24:44+09:00 decision (by nicehugepark)

**리드는 이 문서에서 손을 뗀다.** 무인 작업자(세션 796fabb5, 00:19:44 스폰)가 이미 붙어 있다.

경위가 이 문서 자체의 값어치를 말한다. 나는 방금 `tests/test_attachment_classify.py` 를 고치려던 참이었고, 그 파일은 그 작업자가 **쓰고 있는 중**이었다(문서 문자열은 새로 쓰였는데 테스트 본문은 아직 옛 것이었다 — 반쯤 쓰인 상태). REQ-20260826-021-62x6 로 넣은 C1(도는 작업자를 리드에게 보인다)이 아니었으면 그대로 덮었을 것이다. **오늘 여섯 번째가 될 뻔했고, 40분 전에 넣은 장치가 그것을 막았다.**

부수로 확인된 것 둘:
- REQ-20260826-035-62x6 의 고침이 실제로 동작했다 — 스폰 로그가 `SPAWN(fresh)` 다. 예전이라면 리드 세션 id 를 뺏으려다 즉사했을 자리다.
- 작업자가 **내 고침의 진짜 구멍**을 찾았다. `[File: …]` 이 안 걷힌다. 대시보드는 확장자로 `[Image:]` 와 `[File:]` 을 가르는데(web/index.html sendChat) 내 정규식에는 `file` 이 없었다. 직접 확인했다:

      "이 로그 왜 이래?\n[File: /x/a.log]"   → request  ✕
      "이 로그 왜 이래?\n[Image: /x/a.png]"  → question ○

  로그·PDF 를 붙여 물으면 같은 함정을 그대로 밟는다. 내가 이미지만 보고 두 사고(032·031)의 형태에 맞춰 정규식을 좁게 쓴 탓이다.

작업자가 그 구멍까지 닫고 상신할 것이므로 나는 개입하지 않는다. 위의 서버 재시작(00:22)과 원문 재현 증거는 그 작업자가 이 문서를 읽을 때 함께 보게 된다.

### 2026-08-27T00:26:29+09:00 response (by nicehugepark)

반려 사유는 "REQ-20260827-006-62x6 요청 문서가 생겼다" — 캡처를 붙여 물었는데 또 요청 카드가 됐다.

**원인은 코드가 아니라 그 코드가 돌지 않고 있었다는 것이다.** 증거를 시각으로 맞춰 뒀다:

- 고침이 파일에 들어간 시각: `bin/s9-audit-prompt` 2026-08-26 23:53:56
- 그 메시지를 받은 서버가 뜬 시각: 2026-08-26 22:43:08 (`state/serve-guard.log` 의 "5957s 생존" 역산)
- 메시지가 온 시각: 2026-08-27 00:18:23

서버는 기동 시점 코드를 메모리에 들고 돈다. 그래서 **한 시간 반 낡은 판정자**가 그 질문을 받았다. 00:22 에 서버가 다시 떴고(리드), 지금 화면에는 낡은 코드 배너가 없다 — 캡처: `state/shots/req034-rework-oldcode.png`.

### 이번에 한 일 — 함수가 아니라 **입구**를 못 박았다

지난번 테스트 6건은 전부 `classify()` 라는 **함수**를 불렀다. 그런데 반려는 함수가 옳은 채로 났다. 사용자가 만나는 것은 함수가 아니라 입구(대시보드 채팅 → `chat_audit`)이고, 그 사이에 서버 프로세스라는 층이 하나 더 있다. 테스트가 그 층을 건너뛰고 있었으니 "전부 통과"가 아무것도 보증하지 못했다.

`tests/test_attachment_classify.py` 에 두 계열을 더했다 (11건, 전부 통과 / 예정된 실패 1):

- **E7** — 반려를 부른 그 발화 원문 그대로. 기존 T1 의 두 발화는 물음표가 문장 중간에 있거나 의문사로 시작해 다른 갈래로도 잡힐 수 있었는데, 이 발화("…이렇게 하는거 맞아?")는 물음표가 마지막 글자라 첨부 한 줄에 정확히 가려진다 — 이 결함의 가장 순수한 모양이다.
- **C1·C2·C3** — 격리된 `S9_ROOT` 에서 `chat_audit` 을 **직접 통과**시킨다. C1: 요청 카드가 생기지 않는다(반환 None). C2: 그러면서 질문으로 **남는다**(조용히 흘려보내면 REQ-20260826-033-62x6 의 '질문 한 장' 상태로 되돌아간다). C3: 경계 — 캡처 붙은 요청은 여전히 요청 카드다.

### 아직 남은 구멍 하나 — 이미지가 아닌 첨부

대시보드는 확장자로 마크를 가른다(`web/index.html` sendChat): png/jpg 류는 `[Image: …]`, **나머지는 `[File: …]`**. 걷어내는 규칙(`ATTACH_LINE`)은 image·screenshot·attachment·파일·이미지·첨부를 알지만 **File 은 모른다.** 로그나 PDF 를 붙여 물으면 캡처와 똑같이 물음표가 밀려난다.

수정 자리가 `bin/` 이라 이 세션의 범위 밖이다. 구멍만 `test_x_non_image_attachment_is_not_yet_stripped` 로 못 박아 뒀다(`expectedFailure` — test_question_intake.py 가 쓴 방식). 수리되는 순간 `unexpected success` 로 울린다.

**제안 (bin/s9-audit-prompt, 한 줄)**:

    ATTACH_LINE = re.compile(
        r"^\s*\[(?:image|file|screenshot|attachment|파일|이미지|첨부)\s*[#:][^\]]*\]\s*$",
        re.I)

### 제안 둘 — 낡은 코드 배너가 이번 결함을 못 봤다

`bin/s9` 의 `SERVE_CODE_STAMP = code_stamp()` 는 **인자 없이** 부른다 = `bin/s9` 하나만 지문을 뜬다. 그런데 채팅 판정자는 `bin/s9-audit-prompt` 에 있고, 서버는 그것을 첫 채팅 때 한 번 로드해 `_chat_classifier` 로 **캐시**한다. 즉 **훅만 고치면 배너는 침묵한다** — 이번에 배너가 뜬 것은 우연히 `bin/s9` 도 같이 만져지고 있었기 때문이다.

    SERVE_CODE_STAMP = {"s9": code_stamp(),
                        "hook": code_stamp(<bin/s9-audit-prompt>)}
    # code_is_stale 은 두 지문 중 하나라도 어긋나면 낡음으로

서버가 메모리에 들고 도는 것이 그 둘이니, 지문도 그 둘이어야 한다.

### 2026-08-27T00:26:36+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-08-27 00:26:36 KST - lead]`

전체 스위트가 아직 돌고 있다. 끝나면 결과를 보고 review로 전이하겠다.

### 2026-08-27T00:35:03+09:00 response (agent: worker:rework, by nicehugepark)

### 재작업 검증 — 이번엔 "돌고 있는 것"까지 확인했다

반려의 원인은 코드가 아니라 **낡은 서버**였다(00:22 재기동으로 해소). 같은 실수를 다시 하지 않도록, 이번에는 상신 전에 시각을 맞춰 확인했다:

- 고침이 파일에 들어간 시각 — `bin/s9-audit-prompt` **2026-08-26 23:53:56**
- 지금 채팅을 받고 있는 서버가 뜬 시각 — **2026-08-27 00:22:30** (`state/serve-guard.log`: 00:22:25 SIGTERM → 5s 뒤 재기동)

서버는 기동 시점 코드를 메모리에 들고 돈다. 기동이 고침보다 **28분 뒤**이므로, 지금 대시보드 채팅을 받는 판정자는 고쳐진 쪽이다. 첫 채팅 때 한 번 로드해 캐시하는 판정자 역시 이 기동 이후 로드라 같다.

### 테스트

- `python3 tests/ attachment_classify` — **11건 전부 통과** (예정된 실패 1: 아래 X)
  - T1~T6 판정 함수 · E7 반려를 부른 발화 원문 · C1~C3 채팅 입구(`chat_audit`) 직접 통과
- `python3 tests/` 전체 — **687건 OK** (skipped 1, expected failure 1). 회귀 없음.

### 남은 구멍 하나 — 이미지가 아닌 첨부 (수정 자리가 범위 밖)

로그·PDF 를 붙여 물으면 캡처와 똑같이 물음표가 밀려난다. 대시보드는 확장자로 마크를 가르는데(`web/index.html` sendChat) png/jpg 류만 `[Image:]` 이고 나머지는 `[File:]` 이며, 걷어내는 규칙에 `file` 이 없다. 확인:

    "이 로그 왜 이래?\n[File: /x/a.log]"   → request  ✕
    "이 로그 왜 이래?\n[Image: /x/a.png]"  → question ○

`test_x_non_image_attachment_is_not_yet_stripped` 로 못 박아 뒀다(`expectedFailure`) — 수리되는 순간 unexpected success 로 운다.

**제안 (bin/s9-audit-prompt, 한 줄 — 이 세션의 수정 범위는 web/·vault/·tests/ 라 손대지 않았다)**:

    ATTACH_LINE = re.compile(
        r"^\s*\[(?:image|file|screenshot|attachment|파일|이미지|첨부)\s*[#:][^\]]*\]\s*$",
        re.I)

대안으로 대시보드가 `[File:]` 대신 `[첨부:]` 를 쓰게 하는 길(web/ 안)도 있으나 택하지 않았다 — ① 첨부 본문을 뽑는 쪽(REQ-20260826-020-62x6)이 이 마크를 읽고 있어 표기를 바꾸면 그쪽이 흔들리고, ② 터미널 붙여넣기(`[Image #1]`)처럼 대시보드를 거치지 않는 입구는 여전히 새므로 판정자 쪽을 고치는 것이 옳다. 덧붙여 `web/index.html` 은 지금 다른 작업(빈 그래프)의 미커밋 변경이 올라가 있어 건드리지 않았다.

### 제안 둘 — 낡은 코드 배너가 이번 결함을 못 봤다 (bin/, 범위 밖)

`SERVE_CODE_STAMP = code_stamp()` 는 `bin/s9` 하나만 지문을 뜬다. 그런데 채팅 판정자는 `bin/s9-audit-prompt` 에 있다 — **훅만 고치면 배너는 침묵한다.** 이번에 배너가 뜬 것은 우연히 `bin/s9` 도 같이 만져지던 덕이었다. 서버가 메모리에 들고 도는 것이 그 둘이니 지문도 그 둘이어야 한다.

### 2026-08-27T00:35:51+09:00 tdd (by nicehugepark)

재작업 세대 — 판정 함수(T·E)에 **입구**(C)를 더했다. 반려가 함수는 옳은 채로 났기 때문이다. 전부 `python3 tests/ attachment_classify` 로 확인(11건 통과, 예정된 실패 1).

- [x] T1. 그날 오분류된 두 발화가 질문으로 잡힌다
- [x] T2. 첨부 유무가 판정을 바꾸지 않는다
- [x] T3. 붙여넣기 형태(`[Image #1]`)도 걷힌다
- [x] T4. 첨부 붙은 요청은 그대로 요청 (반대 방향 유실 금지)
- [x] T5. 첨부뿐이면 원문 그대로 (기존 동작 보존)
- [x] T6. 길이 판정도 첨부를 빼고 센다
- [x] E7. 반려를 부른 그 발화 원문 — 물음표가 마지막 글자인 가장 순수한 모양
- [x] C1. 채팅 입구를 직접 통과시켜 요청 카드가 생기지 않는다
- [x] C2. 그러면서 질문으로 남는다 (조용히 흘려보내지 않는다)
- [x] C3. 캡처 붙은 요청은 여전히 요청 카드다
- [x] R. 전체 스위트 687건 OK — 회귀 없음
- [x] S. 돌고 있는 서버가 고침 이후 코드다 (고침 23:53:56 < 기동 00:22:30)

미해결로 남기는 것(범위 밖, `expectedFailure` 로 못 박음): 이미지가 아닌 첨부(`[File: …]`)는 아직 걷히지 않는다.

### 2026-08-27T00:42:23+09:00 response (by nicehugepark)

워커가 남긴 구멍을 리드가 닫았다 — `ATTACH_LINE` 에 `file` 을 더했다.

    "이 로그 왜 이래?\n[File: /x/a.log]"   → 이전 request ✕ / 지금 question ○
    "이 로그 왜 이래?\n[Image: /x/a.png]"  → question ○ (불변)
    "로그 좀 봐 줘. 확인해서 고쳐라.\n[File: …]" → request ○ (경계 유지)

워커의 처리가 옳았다는 것을 적어 둔다. 봉투가 `bin/` 밖이라 고칠 수 없는 자리였는데, **말로 넘기지 않고 `expectedFailure` 로 못 박았다.** 노트에만 적었으면 나는 그 문장을 읽고도 지나쳤을 가능성이 크다 — 오늘만 그런 식으로 놓친 것이 둘이다(서버 재시작 안내를 노트 끝에 덧붙였다가 사용자가 그대로 밟은 것, 그리고 041 의 "남은 위험"). **테스트는 읽지 않아도 울린다.**

지금 그 테스트는 회귀 테스트가 됐다. 워커가 함께 남긴 C 계열(채팅 입구 전체를 실제로 통과시키는 계약)도 값어치가 크다 — 판정 함수만 옳고 입구가 옛날이면 사용자에게는 아무것도 고쳐지지 않은 것과 같고, 오늘 서버가 낡아 그 상태를 실제로 겪었다.

전체 스위트: 이 변경과 무관한 2건 실패(designer 가 REQ-20260826-018-62x6 로 진행 중인 red 테스트)를 빼면 전부 통과. 커밋 82a4b48.

### 2026-08-28T16:32:31+09:00 response (by nicehugepark)

**승인 종결 후속** — 이 문서에 남아 있던 제안 둘(낡은 코드 배너가 `bin/s9-audit-prompt` 를 안 본다)을 REQ-20260828-025-62x6 으로 등록해 닫았다.

그 제안은 이 문서의 반려를 낳은 원인 그 자체였다. 첨부 분류 고침은 훅에 들어갔는데 채팅을 받던 서버는 1시간 반 낡은 판정자를 들고 있었고, 배너는 그 사실을 말했어야 할 자리에서 침묵했을 뻔했다 — 그때 배너가 뜬 것은 우연히 `bin/s9` 도 같이 만져지던 덕이었다. 분류는 고쳤지만 **그 고침이 도는지 알려주는 장치**는 여전히 반쪽이었다.

지금은 지문이 `bin/s9` 와 `bin/s9-audit-prompt` 둘을 뜨고, 어느 쪽이 바뀌었는지 이름까지 말한다(CLI `s9 serve-stale` · `/api/serveinfo` · 대시보드 배너 모두 같은 판정). 테스트 12건은 REQ-20260828-025-62x6 에 있다.

## History
- 2026-08-26T22:48:55+09:00 created by nicehugepark (status: open)
- 2026-08-26T23:49:15+09:00 status: open -> in-progress (by nicehugepark) — drag 이동 [via dashboard]
- 2026-08-26T23:58:21+09:00 status: in-progress -> review (by nicehugepark) — 캡처를 붙여서 질문해 보라 — 화면을 찍어 "이거 왜 이래?" 하고 보내면, 예전에는 요청 카드가 생겼는데 이제 질문으로 잡혀야 한다. 캡처 없이 같은 말을 했을 때와 같은 결과가 나오는지가 핵심이다(첨부가 뜻을 바꾸면 안 된다). 반대로 캡처 붙은 "이거 고쳐줘"는 여전히 요청 카드여야 한다. 다만 지금 대시보드 서버가 옛 코드로 돌고 있어 채팅 경로는 s9 serve --restart 후에 반영된다.
- 2026-08-27T00:19:09+09:00 status: review -> in-progress (by nicehugepark) — 반려: REQ-20260827-006-62x6 요청 문서가 생겼다. [via dashboard]
- 2026-08-27T00:36:04+09:00 status: in-progress -> review (by nicehugepark) — 지난번과 달리 이번엔 지금 바로 해 보면 된다 — 채팅을 받는 서버가 00:22 에 새로 떴고 고침은 그보다 28분 전에 들어갔다. 재시작이 필요 없다. 확인 절차 셋: ① 아무 화면이나 캡처해 붙이고 "이거 왜 이래?" 처럼 물어보라 → 요청 카드가 생기지 않고 질문으로 남아야 한다(지난번 반려 때는 요청 카드가 생겼다). ② 캡처 없이 똑같은 말을 해 보라 → ①과 같은 결과여야 한다. 사진을 붙였다는 것만으로 말의 뜻이 바뀌면 안 된다. ③ 반대 방향으로, 캡처를 붙이고 "이거 고쳐줘" 라고 하라 → 이건 여전히 요청 카드여야 한다. 고침이 지나쳐 부탁까지 질문으로 삼키면 기록이 사라진다. 아직 안 되는 것 하나를 알고 남긴다: 사진이 아닌 파일 — 로그나 PDF 를 붙여 물으면 여전히 요청 카드가 된다. 붙임 표시 문구가 사진일 때와 달라 걷어내는 규칙이 못 알아본다. 고칠 자리가 이 작업의 허용 범위 밖(명령어 쪽)이라 손대지 않고 한 줄짜리 고침안을 문서에 적어 뒀으며, 그 자리가 고쳐지는 순간 테스트가 스스로 울도록 미리 걸어 뒀다.
- 2026-08-27T09:16:13+09:00 status: review -> done (by nicehugepark) — 리드 판정. 사용자가 볼 수단이 없는 내부 분류라 판정을 넘기지 않는다(REQ-20260827-015-62x6). 근거: 반려를 부른 그 메시지 원문 그대로를 지금 코드에 넣으면 question 으로 잡힌다. 워커가 찾은 [File:] 구멍도 닫았고 로그·PDF 첨부도 같다. 반대 방향(캡처 붙은 요청은 요청)도 확인. 계약 11건.
