---
id: REQ-20260829-019-62x6
type: request
title: 문서의 그림이 깨져 보인다
summary: 한 문서에 그림이 여럿이면 일부가 깨진 그림 칸으로 남는다
goal: 그림이 여럿인 문서를 열어도 깨진 그림 칸이 남지 않는다
status: done
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: 911949e0
sessions: ["01c62d83", "911949e0", "d79cd381", "95a2888d"]
agents: ["sub:quality-assurance:a74c0274", "sub:subagent:a484f4ac", "sub:quality-assurance:acb77915"]
contributions: [{"actor": "sub:quality-assurance:a74c0274", "item": "`[2026-08-29 18:18:48 KST - quality-assurance]`", "started": "2026-08-29T18:20:30+09:00", "ended": "2026-08-29T18:20:30+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/911949e0-5d1b-4cd2-b08d-70fd0415a19a/subagents/workflows/wf_06651bea-9e8/agent-a74c0274e6050d414.jsonl"}, {"actor": "sub:subagent:a484f4ac", "item": "Writing s9 note for REQ-20260829-019-62x6", "started": "2026-08-29T18:55:44+09:00", "ended": "2026-08-29T18:55:44+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/911949e0-5d1b-4cd2-b08d-70fd0415a19a/subagents/agent-a484f4ac29c459065.jsonl"}, {"actor": "sub:quality-assurance:acb77915", "item": "`[2026-08-29 18:56:20 KST - quality-assurance]`", "started": "2026-08-29T18:56:33+09:00", "ended": "2026-08-29T18:56:33+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/911949e0-5d1b-4cd2-b08d-70fd0415a19a/subagents/agent-acb7791588d2950ef.jsonl"}]
project: section9
children: ["REQ-20260830-003-62x6"]
relates: ["DOC-20260827-004-62x6", "REQ-20260829-039-62x6"]
relates_why: {"DOC-20260827-004-62x6": "깨진 그림의 원인이 이 문서가 규명한 루프백 동시 도착 벼랑이다 — 처방(재시도)이 그림에만 적용돼 있지 않았다", "REQ-20260829-039-62x6": "같은 벼랑을 그림에서 이미 갚았다 — 재시도·백오프의 수치와 근거를 그대로 쓴다"}
tags: ["auto-audit", "assets", "design", "terminal", "attached", "png", "image"]
created: 2026-08-29T15:02:00+09:00
status_since: 2026-08-29T18:56:55+09:00
updated: 2026-08-30T00:36:05+09:00
priority: 50
---

## Original

문서에 이미지 렌더링이 깨진 것 처럼 보이는 문서가 있다. 제대로 된건지 아닌지 다시 점검해줘.
[Image: assets/REQ-20260829-019-62x6/20260829T150145-image.png]

## Notes

### 2026-08-29T15:04:49+09:00 response (by nicehugepark)

### 점검 결과 — 그림은 멀쩡하고, 잘린 것은 연결이다

먼저 확인한 것: **파일은 다 있다.** 캡처에서 깨져 보인 `20260827T232526-image.png`
는 `vault/requests/2026/08/assets/REQ-20260827-079-62x6/` 에 24,409바이트로
실재하고, 한 건씩 부르면 `200` 에 온전한 PNG 가 온다. 문서의 표기도 정상이고
(`[Image: assets/REQ-…/…png]`) 렌더 정규식도 그것을 집는다.

**잘리는 것은 연결이다.** 같은 파일을 동시에 16번 부르니 **8건이 즉시
거절**됐다(curl exit 7 = 연결 실패, exit 56 = 리셋). 서버 로그에는 아무것도
안 남는다 — 요청이 서버에 닿지도 않았기 때문이다. 리슨 큐는 32이고 그 순간
`Recv-Q` 는 0이었다. 대기열이 넘친 게 아니다.

이것은 이미 규명해 둔 것이다 — DOC-20260827-004-62x6(WSL2 루프백 동시 연결 벼랑):
이 환경의 루프백이 **같은 순간에 도착한 SYN 을 열 개쯤에서 자른다.** 서버
대기열·처리 속도·서버 코드와 무관하고(맨살 stdlib 서버로도 같다), 몇 ms 만
벌리면 전부 통과한다. 그 문서가 적어 둔 처방도 하나다: **클라이언트가 재시도한다.**

### 그런데 그림만 그 처방 밖에 있었다

대시보드의 데이터 요청은 `ccFetch`·`loadSupply` 를 타고 재시도를 받는다.
그런데 그림은 `<img src>` 다 — 브라우저가 직접 부르고, 실패하면 **다시 걸지
않는다.** 깨진 칸 하나를 남기고 끝이다. 문서 하나에 그림이 열다섯 장이면
(REQ-20260827-079-62x6 가 지금 그렇다) 한 번의 스크롤이 그 벼랑을 정확히 만든다.
그러니 이것은 "가끔 깨지는" 것이 아니라 **그림이 많은 문서일수록 반드시**
생기는 일이다.

두 겹으로 갚는다.
- **서버**: 지금 첨부 응답은 `Cache-Control: no-store` 라 문서를 다시 그릴
  때마다 열다섯 장을 전부 새로 부른다. 첨부는 내용이 바뀌지 않는 파일이니
  검증 캐시(ETag/Last-Modified + 304)를 준다 — 두 번째부터는 부르는 횟수
  자체가 줄어 벼랑에 덜 닿는다.
- **화면**: `<img>` 가 실패하면 짧은 백오프로 다시 건다. 그래도 안 되면
  브라우저의 깨진 칸 대신 "다시" 를 누를 수 있는 자리를 준다. (같은 파일을
  designer 가 지금 쓰고 있어, 그 작업이 끝나는 대로 잇는다.)

### 2026-08-29T15:34:17+09:00 response (by nicehugepark)

### 서버 몫 — 부르는 횟수를 줄였다 (리드)

벼랑 자체는 서버가 못 옮긴다(DOC-20260827-004-62x6 가 배제 목록으로 확정했다).
서버가 할 수 있는 것은 **그 앞에 던지는 수를 줄이는 것**이다.

첨부 응답이 `Cache-Control: no-store` 였다. 그래서 문서를 다시 그릴
때마다(폴링·탭 전환·스크롤) 열다섯 장을 전부 새로 불렀다 — 벼랑 앞에 매번
열다섯을 던지는 셈이다. 이제 `/api/asset` 만 자기 문(`_send_asset`)으로 나간다:
`ETag`(변경 시각+크기) + `private, max-age=300`, 그리고 `If-None-Match` 가 맞으면
본문 없는 `304`.

**수명을 300초로 잡은 이유**: 이 저장소에는 **같은 이름으로 다시 찍는 캡처**가
실제로 있다(designer 가 `n-model.png` 를 두 번 찍었다). 영구 캐시를 물리면
판정이 옛 그림을 보고 이뤄진다. 5분이면 한 번 보는 동안의 다시 그리기는 전부
덮으면서, 다시 찍은 것도 곧 따라온다.

**판·목록·상태는 그대로 `no-store` 다.** 그것들은 사람이 옮긴 결과를 곧바로
보여야 하고 첨부와 성질이 다르다 — `_send` 의 기본값은 손대지 않았고, 시험이
그것을 못박는다(`tests/test_asset_cache.py` 10건).

실측(실서버 9909): 첫 요청 `200 · ETag "18cfb10430e498ff-d48d" · max-age=300`,
같은 ETag 로 다시 물으니 `304 · 본문 0바이트`.

**화면 몫으로 남는 것**은 재시도다 — 첫 로드의 동시 도착은 캐시로 못 줄인다.
designer 에게 넘겼다.

### 2026-08-29T15:39:29+09:00 designer (by nicehugepark)

그림이 실패하면 **스스로 다시 겁니다**. `web/index.html` 만 고쳤습니다(+ 계약 시험 `tests/test_asset_retry.py`).

### 큐를 둘지 말지는 재서 정했습니다 — 짐작이 아닙니다

19장짜리 문서(REQ-20260827-079-62x6)의 실제 첨부 주소로 직접 쟀습니다.

| 전략 | 결과 |
|---|---|
| 한꺼번에 19장 | 성공 9~11 / **실패 8~10** (증상 재현) |
| 동시 상한 4 | 성공 15~18 / 실패 1~4 |
| 동시 상한 6 | 성공 16~18 / 실패 1~3 |
| 동시 상한 8 | 성공 14~17 / 실패 2~5 |
| 20ms 간격 | 성공 19 / 실패 0 |
| **재시도(120·320·800ms + 지터)** | **5회 시도 모두 19/19** |

**작은 큐는 두지 않았습니다.** 상한은 듣지 않습니다 — 4·6·8 어느 값에서도 여전히 잘립니다. 벼랑은 "몇 개가 떠 있나"가 아니라 **"같은 순간에 몇 개가 도착하나"** 라서, 앞의 것이 끝나 자리가 나는 순간 여러 개가 다시 같은 순간에 출발합니다. 반대로 **간격을 벌리면 전부 통과**하고(20ms 19/19), 재시도의 백오프+지터가 정확히 그 간격을 만듭니다. 게다가 브라우저는 이미 한 호스트에 동시 연결을 여섯으로 묶으므로, 큐를 또 얹으면 같은 일을 두 번 하면서 첫 그림만 늦어집니다. 이 표를 `ATT_BACKOFF` 위 주석에 그대로 적어 뒀습니다 — 다음 사람이 "큐를 얹지 그랬어"로 되돌리지 않게, 그리고 시험이 그 기록의 존재를 붙잡습니다.

### 만든 것

**1. 실패한 그림이 스스로 다시 겁니다.** `attImg` 을 md2html 안의 지역 함수에서 **맨 위 함수 하나로** 꺼냈습니다 — 재시도와 못 받은 자리가 거기 매여야 두 벌이 생기지 않습니다. `error` 는 거품처럼 올라오지 않으므로 **문서에 잡는 단계(capture)로 한 번만** 답니다(그림마다 달면 다시 그릴 때마다 새로 달아야 합니다). 120·320·800ms + 지터로 세 번 다시 걸고, **재시도 주소만 `&r=` 로 가릅니다** — 실패한 시도가 캐시에 물리면 다시 걸어도 같은 답이 옵니다(성공한 응답은 그대로 캐시를 타야 하므로 첫 주소는 건드리지 않습니다. 리드가 붙이는 ETag/max-age 와 함께 성립합니다).

**깜빡임은 없습니다.** 그림과 **못 받았을 때의 자리**를 한 덩이(`.attbox`)로 처음부터 함께 냅니다 — 실패한 순간 DOM 을 짓느라 한 프레임을 흘리면 깨진 아이콘이 지나갑니다. 기다리는 동안은 조용히 `파일이름 · 그림을 다시 받는 중… (1/3)` 입니다(가만한 문구는 멈춘 것으로 읽히므로 몇 번째인지 적습니다).

**고치다 찾은 결함 하나**: `hidden` 이 지고 있었습니다. `.attlink{display:inline-block}` 이 브라우저 기본 `[hidden]{display:none}` 보다 특이도가 높아, **감췄다고 믿은 깨진 그림이 자리 위에 그대로 겹쳐 서 있었습니다.** 첫 캡처에서 눈으로 잡았습니다(`.attbox [hidden]` 규칙으로 고침).

**2. 그래도 안 오면 사람의 자리.** 브라우저의 깨진 아이콘 대신 점선 한 겹 안에 `파일이름 · 그림을 받지 못했습니다 — 한 번에 여러 장을 부르면 몇 장이 밀립니다. · [다시] · 새 탭에서 열기`. 두 손잡이는 한 덩이로 묶어 좁은 폭에서 따로 떨어지지 않게 했습니다. **`다시` 는 링크 밖에 둡니다** — 안에 두면 누르는 순간 링크가 먼저 열립니다. 문구에 루프백·SYN·소켓 같은 말은 없고, 시험이 그 낱말들을 금지합니다.

**3. 색면도 경고색도 안 씁니다.** 점선 1px 은 "자리는 있는데 아직 내용이 없다"는 뜻입니다. 그림이 늦게 오는 것은 고장이 아니라 지나가는 상태라 붉히지 않았습니다.

### 검증 — 실브라우저에서 재시도가 실제로 돌았습니다

`s9 shot` 으로 그 19장짜리 문서를 직접 열었습니다. 진단 배너(`?attstat`)가 실브라우저 안에서 셉니다.

- 보통: `그림 19 · 뜸 19 (다시 걸어서 0) · 받는 중 0 · 못 받음 0` — 전부 옵니다.
- 다른 실행에서: `뜸 16 (**다시 걸어서 4**)` — **브라우저도 벼랑을 만나고, 재시도가 네 장을 건져 왔습니다.** 고치기 전이었다면 그 네 장이 깨진 칸으로 남았을 것입니다.
- `?attfail=3&attslow` 로 기다리는 자리를, `?attfail=3&attdead` 로 끝내 못 받은 자리를 라이트/다크로 직접 봤습니다.

진단 주소: `?attstat`(몇 장 왔나) · `?attfail=N`(앞 N 장의 첫 요청만 빗나가게 — 재시도가 실제로 도는 것까지 한 화면에서 보입니다) · `&attdead`(끝까지 못 받은 자리) · `&attslow`(지나가는 화면인 "받는 중"을 멈춰 세워 찍기). 못 받은 그림의 자리는 **연결이 실제로 잘려야만** 보이는 화면이고 그 순간은 캡처로 재현할 수 없어서, `?conn=`·`?ccsay=` 와 같은 자리를 하나 더 둔 것입니다.

### 시험

새 계약 `python3 tests/ asset_retry` 12건 — 짓는 곳이 하나인가 · 자리가 미리 서는가 · 백오프와 지터가 있는가 · 재시도 주소가 갈라지는가 · 손을 문서에 한 번만 다는가 · 못 받았을 때 이름·다시·여는 길이 있는가 · `다시` 가 링크 밖인가 · 내부 용어를 안 쓰는가 · 손 없이 볼 수 있는가 · **측정 기록이 남아 있는가** · **큐가 안 들어왔는가**.

곁 스위트 회귀 확인: `assets`·`bare_asset_path`·`asset_text_ui`·`asset_text_api`·`asset_dir_canon`·`attachment_classify`·`note_attach`·`judge_attach`·`doc_table`·`article_screen`·`code_block_look`·`account_and_model`·`account_switch`·`session_restart`·`judge_dialog`·`term_conn`·`pick_state_look`·`review_hover`·`doclink_keyboard`·`priority_visible`·`shot_hygiene`·`stdlib_only` 전부 OK.

`test_bare_asset_path` 의 한 줄을 고쳤습니다 — 계약("표기와 맨 경로가 **같은 함수**로 그림을 낸다")은 그대로고 **찾는 모양**만 바뀐 것입니다(지역 화살표 함수 → 맨 위 함수 선언). 경위를 그 시험의 docstring 에 적었습니다.

### 남긴 것

헤드리스 캡처는 사람이 보는 것보다 **덜 깨집니다** — 좁은 창에서는 `loading="lazy"` 가 아래쪽 그림을 안 부르고, 브라우저가 동시 연결을 여섯으로 묶기 때문입니다. 그래서 "고치기 전"을 캡처로 재현하지는 못했고, 대신 ① 파이썬 실측으로 벼랑을 재현했고(9~11/19) ② 고친 뒤 실브라우저에서 **재시도가 실제로 네 장을 건져 오는 것**을 배너 숫자로 확인했습니다.

[Image: assets/REQ-20260829-019-62x6/1-문서-그림19장-전부.png]
[Image: assets/REQ-20260829-019-62x6/2-다시받는중.png]
[Image: assets/REQ-20260829-019-62x6/3-못받은자리.png]
[Image: assets/REQ-20260829-019-62x6/4-못받은자리-다크.png]

### 2026-08-29T15:41:50+09:00 response (by nicehugepark)

리드 확인: `?attstat` 로 REQ-20260827-079-62x6(그림 19장) 문서를 직접 열어 `그림 19 · 뜸 19 (다시 걸어서 0) · 못 받음 0` 을 눈으로 봤다. 캡처: scratchpad/attstat.png

### 2026-08-29T18:17:39+09:00 response (by nicehugepark)

### 이어받아 마무리 — 전 스위트와 실브라우저로 확인했다 (lead, 세션 d79cd381)

15:41 리드 확인 이후 81분 멈춰 있던 것을 이어받았다. 남은 것은 구현이 아니라 **검증과 상신**이었다 — 서버 몫(ETag/304)과 화면 몫(백오프 재시도 + 못 받은 자리)은 이미 들어가 있었고, 그것이 지금 코드에서 실제로 도는지를 다시 재고 닫는다.

**전 스위트**: `python3 tests/` **2026건 OK** (skipped 3, 493초). 이 건의 계약 두 벌도 그 안에 있다 — `asset_retry` 12건(짓는 곳이 하나인가·자리가 미리 서는가·백오프와 지터·재시도 주소 분기·문서에 한 번만 다는가·`다시` 가 링크 밖인가·내부 용어 금지·측정 기록 존재·큐 미도입), `asset_cache` 10건(첨부만 자기 문으로·ETag 는 따옴표 토큰·파일이 바뀌면 태그도 바뀐다·304 는 본문 0·`_send` 기본은 여전히 no-store).

**실브라우저 재측정**: `s9 shot http://127.0.0.1:9909/?attstat#docs/REQ-20260827-079-62x6` — 그 문서는 그 사이 그림이 35장으로 늘었다. 진단 배너가 `그림 35 · 뜸 35 (다시 걸어서 1) · 받는 중 0 · 못 받음 0`. **35장 전부 왔고, 그중 한 장은 첫 요청이 잘렸다가 재시도가 건져 온 것이다.** 고치기 전이었다면 그 한 장이 깨진 칸으로 남았다. 캡처: `scratchpad/019-attstat-verify2.png`.

**곁가지로 본 것(이 건 밖)**: 그 캡처의 상단에 `서버가 옛 코드로 돌고 있습니다 — 18:04 기동 이후 bin/s9 가 바뀌었습니다` 배너가 떠 있다. 이 건의 서버 몫(ETag)은 18:04 기동에 이미 포함돼 있어 측정에 영향이 없다(배너가 가리키는 변경은 다른 작업의 것이다). 수정 범위 밖이라 손대지 않고 적어만 둔다.

**남은 한계는 그대로다**: 헤드리스 캡처는 사람이 보는 것보다 덜 깨진다(`loading=lazy` + 브라우저의 호스트당 6 연결). 그래서 "고치기 전"은 캡처로 재현하지 못하고, 대신 재시도가 실제로 건져 온 장수(이번엔 1)로 벼랑을 만났다는 것을 확인한다.

### 2026-08-29T18:20:30+09:00 subagent (agent: sub:quality-assurance:a74c0274, by nicehugepark)

`[2026-08-29 18:18:48 KST - quality-assurance]`

두 기능 모두 살아 있는 서버에서 사람이 하는 그대로 동작한다. 결정적 근거: 대시보드 `깨우기` 를 눌러 스폰된 무인 워커가 10분 21초 뒤 REQ-20260829-019-62x6 를 실제로 마무리하고 `in-progress → review` 로 올려 Board 의 REVIEW 열에 승인/반려 카드로 나타났다.

### 2026-08-29T18:55:44+09:00 subagent (agent: sub:subagent:a484f4ac, by nicehugepark)

Writing s9 note for REQ-20260829-019-62x6

### 2026-08-29T18:55:50+09:00 qa (by nicehugepark)

### QA 판정 — 고쳐졌다. 살아 있는 서버에서 10회 다시 그려 한 장도 안 빠졌다

문서는 그 사이 그림이 **40장**으로 늘었다(`[Image:]` 38 + 맨 경로 2). 그 문서를
`http://127.0.0.1:9909/?attstat#docs/REQ-20260827-079-62x6` 로 열어 **찬 캐시에서** 다시 그렸다
(`s9 shot` 은 캡처마다 새 프로필을 쓰므로 매 회차가 첫 로드다).

| 회차 | 조건 | 그림 | 뜸 | 다시 걸어서 | 못 받음 |
|---|---|---|---|---|---|
| 1~2 | 1440×14000 (문서 전체가 한 화면) | 40 | 40 | 1 · 1 | 0 |
| 3~4 | 같음 | 40 | 40 | 0 · 0 | 0 |
| 5 | 같음 + **연결 홍수**(같은 순간 30개씩 20초, 660건 거절) | 40 | 40 | 0 | 0 |
| 6~8 | **브라우저 3개 동시** | 40 | 40 | 2 · 1 · 0 | 0 |
| 9~10 | **1440×900 — 사람이 보는 크기** | 40 | 40 | 1 · 2 | 0 |

**10회 모두 40/40, 못 받음 0.** 그중 **여덟 장은 첫 요청이 잘렸다가 재시도가 건져 온 것**이다
(회차별 `다시 걸어서` 합). 고치기 전이었다면 그 여덟 장이 깨진 칸이었다.

**벼랑은 지금도 살아 있다.** 파이썬으로 같은 순간에 30개씩 걸어 재니 **270건 중 159건이 거절**
됐고(홍수 20초로는 990건 중 660건), DOC-20260827-004-62x6 가 규명한 그대로다. 서버가 그 상태인 동안에도
브라우저 화면은 40/40 이었다 — 재시도가 실제로 그 벼랑을 넘고 있다는 뜻이다.

### 서버 몫 — 실측으로 확인

    첫 요청  200 · ETag "18cfb10430e498ff-d48d" · Cache-Control: private, max-age=300 · 54413바이트
    같은 ETag 로 If-None-Match  →  304 · 본문 0바이트
    틀린 ETag 로 If-None-Match  →  200 · 54413바이트 (제대로 다시 준다)
    /api/* 의 다른 길은 그대로 no-store

### 못 받은 자리 · 기다리는 자리 — 눈으로 봤다

`?attfail=3&attdead` 로 세 장을 실제 404 로 만들어 세웠다. 브라우저의 깨진 아이콘은 **한 개도 없고**,
점선 한 겹 안에 `파일이름 · 그림을 받지 못했습니다 — 한 번에 여러 장을 부르면 몇 장이 밀립니다.
· [다시] · 새 탭에서 열기` 가 선다(배너: 그림 40 · 뜸 37 · 못 받음 3). `?attslow` 로 멈춰 세운
기다리는 자리는 `그림을 다시 받는 중… (1/3)` 로 몇 번째인지 말한다.

**회수도 봤다.** `?attfail=5` (앞 다섯 장의 첫 요청만 실제 404) → `그림 40 · 뜸 40 (다시 걸어서 5)
· 못 받음 0`. 재시도 루프가 진짜로 404 난 다섯 장을 전부 되찾았고, 그 자리에 그림이 온전히 그려졌다.

### 시험

`python3 tests/ asset_retry asset_cache bare_asset_path` → **30건 OK** (0.095s).

### 확인하지 못한 것 (지어내지 않는다)

- **`다시` 단추를 실제로 눌러 보지 못했다.** 이 환경에 브라우저 조작 수단이 없다
  (`s9 shot` 은 캡처만, Playwright·CDP·PIL·node 전부 없음). 대신 ① 구조로 확인했다 —
  `.attmiss`(단추가 든 자리)는 `.attlink` `<a>` 의 **형제**라 링크 안이 아니고, 클릭 처리기가
  `preventDefault()` + `stopPropagation()` 을 건다. ② `test_the_retry_button_is_outside_the_link`
  가 그 계약을 못박고 통과한다. **눌러서 회수되는 것 자체는 미검증**이다.
- **헤드리스는 사람 손보다 덜 깨진다** — 이미 적힌 한계 그대로다. 다만 이번엔 사람 크기
  뷰포트(1440×900)와 브라우저 3개 동시, 연결 홍수까지 얹어 그 약점을 최대한 메웠다.

### 곁가지로 본 것 (이 건 밖 · 손대지 않음)

- 측정 중 `web/index.html` 이 두 번 바뀌었고(18:43:46 → 18:44:46) 서버 워커도 18:45:24 에
  갈아탔다 — 다른 작업자가 같은 파일을 쓰는 중이다. 회차 1~5 와 6~10 은 서로 다른 판일 수
  있으나 **결과가 10회 모두 동일**해 판정은 갈리지 않는다. 마지막 회차는 현재 판
  (`md5 9a745da2df1c`)에서 찍었다.
- 9909 를 잡고 있는 감독 프로세스는 16:44:58 기동이다(워커만 18:45 에 갈아탐). 이 건의
  서버 몫은 살아 있는 응답으로 직접 확인했으므로 판정에는 영향이 없다.

붙인 그림: ① 사람 크기 화면 전체(배너 `그림 40 · 뜸 40 (다시 걸어서 2) · 못 받음 0`)
② 못 받은 자리 셋 ③ 다시 받는 중 ④ 연결 홍수 중의 배너.

[Image: assets/REQ-20260829-019-62x6/qa-a-40장전부-사람크기.png]
[Image: assets/REQ-20260829-019-62x6/qa-b-못받은자리.png]
[Image: assets/REQ-20260829-019-62x6/qa-c-다시받는중.png]
[Image: assets/REQ-20260829-019-62x6/qa-d-홍수중-40장.png]

### 2026-08-29T18:56:33+09:00 subagent (agent: sub:quality-assurance:acb77915, by nicehugepark)

`[2026-08-29 18:56:20 KST - quality-assurance]`

### 판정: 고쳐졌다 — 승인 권고

살아 있는 서버(127.0.0.1:9909)에서 **10회** 다시 그려 **매회 40장 전부** 떴다. 못 받은 장 0.

### 실제로 한 것

문서는 그 사이 그림이 **19장 → 40장**으로 늘었다(`[Image:]` 38 + 맨 경로 2). 더 가혹한 조건이다.

| 회차 | 조건 | 그림 | 뜸 | 다시 걸어서 | 받는 중 | 못 받음 |
|---|---|---|---|---|---|---|
| 1~2 | 1440×14000 (문서 전체가 한 화면) | 40 | 40 | 1 · 1 | 0 | 0 |
| 3~4 | 같음 | 40 | 40 | 0 · 0 | 0 | 0 |
| 5 | 같음 + 연결 홍수(같은 순간 30개씩 20초, 660건 거절) | 40 | 40 | 0 | 0 | 0 |
| 6~8 | 브라우저 **3개 동시** | 40 | 40 | 2 · 1 · 0 | 0 | 0 |
| 9~10 | **1440×900 — 사람이 보는 크기** | 40 | 40 | 1 · 2 | 0 | 0 |

매 회차가 **찬 캐시**다 — `s9 shot` 은 캡처마다 새 프로필(`s9shot-<pid>`)을 쓴다. 10회 합쳐 **여덟 장**이 첫 요청에서 잘렸다가 재시도가 건져 왔다. 고치기 전이었다면 그 여덟 장이 깨진 칸이다.

**벼랑은 지금도 살아 있다**: 파이썬으로 같은 순간에 30개씩 걸어 재니 270건 중 **159건 거절**(홍수 20초로는 990건 중 660건). 서버가 그 상태인 동안에도 브라우저는 40/40이었다.

**리드가 겪은 "본문이 안 나온다"는 창 높이 문제였다.** 900px 기본에서는 문서 판이 짧게 잘려 shot의 공백 경고가 떴을 뿐, 본문·판정 카드·그림 모두 정상 렌더된다(1440×900 캡처로 확인).

### 서버 몫 — 실측

```
첫 요청  200 · ETag "18cfb10430e498ff-d48d" · Cache-Control: private, max-age=300 · 54413바이트
같은 ETag 로 If-None-Match  →  304 · 본문 0바이트
틀린 ETag 로 If-None-Match  →  200 · 54413바이트
/api/* 의 다른 길                →  여전히 no-store
```

### 못 받은 자리 · 회수

- `?attfail=3&attdead`(실제 404 세 장): 깨진 아이콘 **0개**. 점선 안에 `파일이름 · 그림을 받지 못했습니다 — 한 번에 여러 장을 부르면 몇 장이 밀립니다. · [다시] · 새 탭에서 열기`. 배너 `그림 40 · 뜸 37 · 못 받음 3`.
- `?attslow`: `그림을 다시 받는 중… (1/3)` — 몇 번째인지 말한다.
- `?attfail=5`(앞 다섯 장 첫 요청만 실제 404): `그림 40 · 뜸 40 (다시 걸어서 5) · 못 받음 0` — **재시도 루프가 진짜 404 다섯 장을 전부 되찾았고** 그 자리에 그림이 온전히 그려졌다.

### 시험

`python3 tests/ asset_retry asset_cache bare_asset_path` → **30건 OK** (0.095s).

### 확인하지 못한 것

- **`다시` 단추를 실제로 눌러 보지 못했다.** 이 환경에 브라우저 조작 수단이 전무하다(Playwright·CDP·websocket·PIL·node 없음, `s9 shot`은 캡처 전용). 구조로만 확인했다 — `.attmiss`(단추가 든 자리)는 `.attlink` `<a>`의 **형제**이고 클릭 처리기가 `preventDefault()`+`stopPropagation()`을 건다. `test_the_retry_button_is_outside_the_link`가 그 계약을 못박고 통과한다. **눌러서 회수되는 것 자체는 미검증**이다.
- 헤드리스가 사람 손보다 덜 깨진다는 기존 한계는 그대로다. 사람 크기 뷰포트 + 3개 동시 + 연결 홍수로 최대한 메웠다.

### 곁가지 (이 건 밖 · 손대지 않음)

- 측정 중 `/home/sjpark1/section9/web/index.html`이 두 번 바뀌었고(18:43:46 → 18:44:46) 서버 워커도 18:45:24에 갈아탔다 — 다른 작업자가 같은 파일을 쓰는 중이다. 회차 1~5와 6~10은 서로 다른 판일 수 있으나 결과가 10회 동일해 판정은 갈리지 않는다. 마지막 회차는 현재 판(`md5 9a745da2df1c`)에서 찍었다.
- 9909를 잡은 감독 프로세스는 **16:44:58 기동**이다(18:33 재기동이 아니다 — 워커만 18:45에 갈아탔다). 이 건의 서버 몫은 살아 있는 응답으로 직접 확인했으므로 판정에 영향 없다.

### 산출물

문서에 `--label qa` 노트 + 캡처 4장 첨부 완료(`/home/sjpark1/section9/vault/requests/2026/08/assets/REQ-20260829-019-62x6/qa-a…d….png`).

원본 캡처(회차별 배너 포함): `/tmp/claude-1000/-home-sjpark1-section9/911949e0-5d1b-4cd2-b08d-70fd0415a19a/scratchpad/r1.png` ~ `r9.png`, `dead.png`, `slow.png`, `recover.png`, 배너 크롭 `r{1..8}-banner.png`. PNG 크롭 도구는 `.../scratchpad/pngcrop.py`(순수 stdlib — 이 환경에 이미지 도구가 없어 직접 만들었다).

파일은 하나도 고치지 않았고 git 쓰기 명령도 쓰지 않았다.

### 2026-08-30T00:30:48+09:00 workspace (by nicehugepark)

본 저장소에서 이어받는다 — 화면 작업이라 살아 있는 서버가 읽는 자리에서 해야 한다. 커밋하면 다음 라운드부터 작업자가 다시 자기 워크트리를 받는다.

### 2026-08-30T00:36:05+09:00 stop (by nicehugepark)

무인 작업자를 세웠다 (pid 930298, 사람 911949e0) — 019 는 done 인데 작업자가 떠 bin/s9 를 잡고 대기 중인 일들을 막는다. 그 하나만 세우는 길이 막혀 있어(REQ-20260830-002-62x6) 전부 세운다 — 030 재작업은 리드가 designer 로 곧바로 다시 붙인다

## History
- 2026-08-29T15:02:00+09:00 created by nicehugepark (status: open)
- 2026-08-29T15:04:25+09:00 status: open -> in-progress (by nicehugepark) — 재현·원인 확정 — 동시 도착 한계에서 잘린 요청이 브라우저의 깨진 그림으로 남는다
- 2026-08-29T18:17:45+09:00 status: in-progress -> review (by nicehugepark) — 그림이 많은 문서에서 몇 장이 깨진 칸으로 남던 것이 없어졌습니다. F5 후 Docs 에서 "계정 전환 자리와 모델 팝업"(REQ-20260827-079-62x6)을 열어 끝까지 내려 보세요. 그림이 하나도 빠짐없이 보이면 승인, 깨진 아이콘이 남으면 반려입니다. 늦게 오는 그림은 점선 칸에 "다시 받는 중…" 으로 잠깐 보였다가 채워집니다.
- 2026-08-29T18:56:55+09:00 status: review -> done (by nicehugepark) — 충족 근거: 살아 있는 서버에서 40장짜리 문서를 10회 찬 캐시로 다시 그려 매회 40/40, 못 받은 장 0. 그중 여덟 장이 첫 요청에서 잘렸다가 재시도가 건져 왔다(고치기 전이라면 깨진 칸). 연결 홍수(같은 순간 30개, 660건 거절)와 브라우저 3개 동시, 사람 크기 뷰포트에서도 40/40. 서버 몫은 200+ETag+max-age=300 / 같은 ETag 재요청 304 본문 0바이트 실측. 못 받은 자리는 깨진 아이콘 0개에 점선 안내+다시 버튼. 시험 30건 OK. 미검증 하나: 다시 버튼을 실제 클릭으로 눌러 보지 못했다(이 환경에 브라우저 조작 수단 없음) — 링크 밖 배치와 preventDefault 는 시험이 붙잡는다.
