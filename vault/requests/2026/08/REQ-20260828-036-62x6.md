---
id: REQ-20260828-036-62x6
type: request
title: 멈춘 것이 화면에 보이게
summary: in-progress 카드의 초록 점이 요청의 진전이 아니라 세션 맥박을 재고 있어, 손 뗀 요청도 초록으로 뜬다
goal: 보드에서 도는 것·줄 선 것·멈춘 것이 구별되고, 멈춘 카드는 얼마나 조용한지를 글자로 말한다
status: done
size: M
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: b8fda8f9
agents: ["sub:designer:a5fb47b5"]
contributions: [{"actor": "sub:designer:a5fb47b5", "item": "`[2026-08-28 20:03:12 KST - designer]`", "started": "2026-08-28T19:59:06+09:00", "ended": "2026-08-28T19:59:06+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/subagents/agent-a5fb47b53d09d4b99.jsonl"}]
project: section9
relates_why: {}
tags: ["auto-audit", "dashboard", "docs", "attached", "png", "image"]
created: 2026-08-28T18:04:12+09:00
status_since: 2026-08-28T20:00:48+09:00
updated: 2026-08-28T20:00:48+09:00
priority: 50
---

## Original

[선행 대화 — 직전 미문서화 메시지]
- (17:56) 몇개 요청들이 몇십분째 진행중으로 뜨는데 진짜 진행중인건가?
- (18:02) 그래서 방치되던 것들 진행하는거야? 뭐야? 상황보고만 하고 개선은 없는건가?

그냥 애초에 in-progress 중인 카드나 문서에 상태체크 기능을 만들고 굳이 프롬프트로 물어보지 않고 진행할 수 있게 하는건 어때? 아키텍트, 개발자들, 디자이너 모두 검토해봐.

## Notes

### 2026-08-28T19:51:54+09:00 response (by nicehugepark)

화면 몫을 넣었다. 이제 보드가 "도는 것 · 줄 선 것 · 멈춘 것"을 갈라 말한다.

**판정은 한 벌로 합쳤다.** 새 함수 `stall_mins(r, now, win)` 하나가 "이 요청에
진전이 있나"를 재고, `catalog_with_live()` 가 행마다 `stalled_mins` 를 실어
보낸다. `stalled_requests()`(= `s9 stalled`)는 이제 그 값을 **읽어 거르기만**
한다 — 나이를 다시 재는 코드가 한 줄도 없다. 이번 사고의 원인이 정확히 판정
두 벌이었다(2026-08-27 에 CLI 만 고치고 화면을 두고 갔다). 계약 시험으로
`stalled_requests` 본문에 `fromisoformat` 이 없음을 못 박았다.

**화면에서 고친 것 넷.**

1. **점이 거짓말을 멈췄다.** 점 사다리에서 멈춤 판정이 초록보다 **앞**에 선다.
   전에는 `r.live` 가 맨 위라, 문서가 한 시간째 안 움직여도 그걸 잡아 둔 세션만
   살아 있으면 초록으로 뛰었다. 점이 재던 것은 요청의 진전이 아니라 세션의
   맥박이었다. (죽은 워커의 정지등은 더 구체적인 근거를 가지므로 그대로 위에 둔다.)
2. **꺼진 점을 속 빈 링으로.** `background:transparent; border:1.5px solid var(--muted)`.
   실측 대비: 라이트 2.98:1 → **4.16:1**, 다크 2.98:1 → **5.05:1** (캡처 화소에서
   직접 쟀다. 안티에일리어싱 전 토큰값은 5.8:1 / 7.1:1). 라이트에서 초록보다
   밝던 역전도 없어졌다.
3. **멈춤 한 줄.** 기존 `.rvpt` 형제로 `멈춤 / 40분째 진전 없음 · 마지막 19:02`.
   새 배지·색면·경고 띠·깜빡임 없음. 캡션 잉크는 in-progress 색 — 이 줄은 새
   상태가 아니라 "그 상태가 나아가지 않는다"는 말이라 상태 자신의 잉크로 쓴다.
   **선행 대기 줄이 있으면 그쪽이 이긴다**(같은 사실을 두 줄로 말하지 않는 기존 규칙).
   하루가 넘으면 `2일 3시간째` 로 센다.
4. **열 머리 + 정렬.** `in-progress  5  멈춤 2` (mono, 글자색만, 0건이면 안 나온다).
   in-progress 열은 오래 멈춘 순 → 도는 중 순. 기본 정렬(최근 갱신 순)은 정확히
   반대로 세워서, 손 뗀 지 오래된 것이 접힌 아래로 내려가 있었다.

**직접 눌러 확인했다** (실브라우저, 사용자 실제 설정 skin=calm·tone=carbon).
열 머리 `in-progress 5 멈춤 2`, 카드 차례 `030(멈춤) → 036(멈춤) → 039(도는중)
→ 027(도는중) → 028(도는중)`, 점 종류 `off · off · sess · on · on`. 밝은 판
(ledger·paper)과 900px 좁은 폭에서도 한 줄이 안 잘린다.

**남는 구분 하나** — 멈춤(회색 링)과 "세션은 도는 중"(초록 링)은 이제 모양이
같고 색만 다르다. 색을 못 보는 눈에는 붙어 보인다. 다만 **멈춤에는 늘 글자 줄이
붙고 그쪽에는 안 붙으므로**, 색 없이도 갈린다 — 모양을 더 쪼개는 대신 글자에
맡겼다.

시험: `tests/test_stall_visible.py` 13건 신규(서버 6 · 화면 7).
`tests/test_stalled_claim.py` 의 계약 하나는 판정이 옮겨 간 자리(`stall_mins`)를
보도록 고쳤다 — 계약의 뜻은 그대로다.

[Image: assets/REQ-20260828-036-62x6/멈춤이-보이는-보드.png]
[Image: assets/REQ-20260828-036-62x6/밝은판-멈춤-줄과-열머리.png]
[Image: assets/REQ-20260828-036-62x6/좁은-폭에서도-한-줄.png]

### 2026-08-28T19:59:06+09:00 subagent (agent: sub:designer:a5fb47b5, by nicehugepark)

`[2026-08-28 20:03:12 KST - designer]`

두 건 다 끝냈고 `review` 로 올렸다. 전체 스위트 **1573건 통과**(skipped 1).

### 1) REQ-20260828-036-62x6 — 멈춘 것이 화면에 보이게

**판정을 한 벌로 합쳤다.** 새 `stall_mins(r, now, win)` 하나가 "이 요청에 진전이 있나"를 재고, `catalog_with_live()` 가 행마다 `stalled_mins` 를 실어 보낸다. `stalled_requests()`(=`s9 stalled`)는 그 값을 **읽어 거르기만** 한다 — 나이를 다시 재는 코드가 한 줄도 없고, 계약 시험이 그걸 못 박는다.

화면 넷:
1. **점이 거짓말을 멈췄다.** 점 사다리에서 멈춤이 초록보다 **앞**에 선다. 안 그러면 같은 카드가 점으로는 "돈다", 글자로는 "멈췄다"고 말한다 — 둘이 어긋나면 사람은 둘 다 안 믿는다.
2. **꺼진 점 = 속 빈 링.** 실측 대비 라이트 2.98 → **4.16:1**, 다크 2.98 → **5.05:1** (캡처 화소에서 직접 쟀다). 라이트에서 초록보다 밝던 역전도 없어졌다.
3. **멈춤 한 줄** — 기존 `.rvpt` 형제, `멈춤 / 40분째 진전 없음 · 마지막 19:02`. 선행 대기 줄이 있으면 그쪽이 이긴다. 하루 넘으면 `2일 3시간째`.
4. **열 머리 `in-progress 5 멈춤 2`** (0이면 안 뜸) + 오래 멈춘 순 정렬. 기본 정렬은 정확히 반대(최근 갱신 순)라 손 뗀 지 오래된 것이 접힌 아래에 있었다.

실브라우저(사용자 실제 설정 calm/carbon, 그리고 ledger·glass·grid·900px)에서 확인: 카드 차례 `030(멈춤) → 036(멈춤) → 039 → 027 → 028`, 점 `off·off·sess·on·on`, 어느 스킨에서도 줄이 안 잘린다.

**남는 구분 하나**: 멈춤(회색 링)과 "세션은 도는 중"(초록 링)이 이제 모양이 같고 색만 다르다. 다만 **멈춤에는 늘 글자 줄이 붙고 그쪽에는 안 붙으므로** 색 없이도 갈린다 — 모양을 더 쪼개는 대신 글자에 맡겼다.

### 2) REQ-20260828-027-62x6 — 주소로 바로 연 문서의 빈 버튼줄

**추측이 아니라 재현했고, 문서의 추정과 다른 자리였다.** 부트는 전이표를 실제로 **먼저 기다린다** — 문제는 순서가 아니라 **놓친 것을 다시 안 받는 것**이었다.

측정: 부트가 던지는 여덟 API 를 15회 동시에 → **120 요청 중 30건 `Connection reset by peer`**, 전이표만 세면 15번 중 2번. 순차로는 50번 중 1번. 그런데 `try{ TRANS = await fetch(...) }catch(e){}` 가 그 실패를 삼켜, 한 번 놓치면 세션 내내 빈 채로 남았다.

고친 것: `loadTrans()` 한 문(400·800·1200ms 물러서며 3회) · `targets.length` 가 아니라 `transReady()` 로 "없는 것 vs 안 온 것"을 가름 · 못 받으면 `상태 옮기기 버튼을 받지 못했습니다 + 다시 받기` · 표가 오면 열린 문서 자동 재렌더, 15초 배경 갱신도 메움(직후 10초 유예 — 없으면 "받는 중"이라 써 놓고 아무도 안 받는 화면이 된다. 실제로 그렇게 한 번 찍혔다).

**CDP 로 실제 눌러 확인**: 끊긴 판 → `다시 받기` **클릭** → 버튼 넷 채워짐 → F5 유지. 키보드(Tab→Enter)로도 동작. 보드에서 표를 비우고 손 떼면 **12초 뒤 스스로 메웠다**. `?transfail` / `?transfail=once` 진단 스위치를 남겼다.

### 리드가 알아야 할 것 셋

1. **`bin/s9` 에 다른 손의 미커밋 작업이 이미 있었다** — REQ-030 몫(`display_tz`·`local_ts`·`user_config_local_path`·`_read_json`). 내 변경(`stall_mins`, `catalog_with_live(stall_win=)`, `stalled_requests` 슬림화)과 같은 파일에 섞여 있으니 **커밋을 갈라야 한다.** 손대지 않고 그대로 뒀다.
2. **REQ-036 문서에 팀 analysis 노트 셋이 없었다.** `## Notes` 가 비어 있고 git 이력에도 없다(커밋 5f6bcb5 에 생성될 때부터 없음). 네 지시문에 담긴 designer 결론으로 진행했다 — 그 노트들이 어딘가에서 유실됐을 수 있다.
3. **파생 요청 하나 등록**: `REQ-20260828-039-62x6` (부모 027) — 캡처 한 장에서 `/api/users` 가 끊긴 판을 잡았다. 사용자 설정(skin=calm)이 안 실려 **화면이 기본 스킨으로 떴다.** 사용자에겐 "가끔 내 화면 설정이 초기화된다"로 보인다. 조용히 기본값으로 물러나는 자리가 더 있다는 뜻이다.

### 파일

- `/home/sjpark1/section9/bin/s9` — `stall_mins()` 신규, `catalog_with_live(stall_win=None)` 가 `stalled_mins` 적재, `stalled_requests()` 는 읽기만
- `/home/sjpark1/section9/web/index.html` — `.livedot.off` 링, `.rvpt.stall`, `.col h2 .stn`, `.transwait`, `fmtStall`/`fmtLast`, `loadTrans`/`transRefill`/`transReady`, 점 사다리·정렬·열 머리
- `/home/sjpark1/section9/tests/test_stall_visible.py` (신규 13건) · `/home/sjpark1/section9/tests/test_trans_late.py` (신규 7건)
- `/home/sjpark1/section9/tests/test_stalled_claim.py` — 계약 하나를 판정이 옮겨 간 자리(`stall_mins`)로 (뜻은 그대로)
- `/home/sjpark1/section9/vault/requests/2026/08/REQ-20260828-039-62x6.md` (신규)
- 캡처는 두 REQ 문서에 그림으로 붙였다 (`vault/requests/2026/08/assets/REQ-20260828-{027,036}-62x6/`)

## History
- 2026-08-28T18:04:12+09:00 created by nicehugepark (status: open)
- 2026-08-28T19:02:10+09:00 status: open -> in-progress (by nicehugepark) — 서버 몫은 커밋 5f6bcb5 에 들어갔다 — 화면 몫 착수
- 2026-08-28T19:57:56+09:00 status: in-progress -> review (by nicehugepark) — 보드의 IN-PROGRESS 열을 보고 답해 주세요 (F5 한 번 하고). 전에는 카드의 초록 점이 "이 요청이 나아가고 있다"가 아니라 "이 요청을 잡아 둔 세션이 살아 있다"를 재고 있었습니다. 리드 세션은 늘 살아 있어서, 손을 떼고 다른 일을 하는 동안에도 잡아 둔 요청이 전부 초록으로 뛰었습니다 — 물으셨던 "몇십분째 진행중으로 뜨는데 진짜 진행중인건가"가 그 자리입니다. 지금 볼 것 셋: ① 열 머리에 `멈춤 N`이 붙습니다. 그 수만큼의 카드가 열 맨 위에 모여 있나요? ② 그 카드는 번호 앞이 **속 빈 동그라미**이고, 아래에 `멈춤 / 40분째 진전 없음 · 마지막 19:02` 한 줄이 있습니다. 읽자마자 "이건 지금 아무도 안 붙어 있구나"가 오나요? ③ 실제로 손대고 있는 요청은 그대로 초록 점이 뛰나요? 둘이 섞여 보이면 반려해 주세요. 터미널에서 `bin/s9 stalled` 를 쳐 보시면 화면과 **같은 건수·같은 분**이 나와야 합니다. 다르면 반려해 주세요 — 그 어긋남이 이번에 고친 바로 그것입니다.
- 2026-08-28T20:00:48+09:00 status: review -> done (by nicehugepark) — 승인 (메모 없음) [via dashboard]
