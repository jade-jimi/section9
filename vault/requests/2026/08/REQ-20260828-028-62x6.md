---
id: REQ-20260828-028-62x6
type: request
title: 코드 파일을 화면에 내주는 경계
summary: 터미널·문서에서 언급된 저장소 파일(web/index.html:4016 같은 것)을 화면에서 보려면 무엇을 내줄지의 경계를 먼저 정해야 한다
goal: 저장소 파일을 화면에 내주는 판정 함수가 하나 정해지고, 그 함수가 막는 것(비밀·상태·리포 밖)을 테스트가 고정한다
status: review
size: M
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: b8fda8f9
agents: ["sub:subagent:a3856b2d", "sub:designer:a5fb47b5", "sub:security-engineer:a526959b", "sub:designer:a978bfd2", "sub:backend-developer:a30daab1", "sub:subagent:a57d0206", "sub:designer:ab64fee2"]
contributions: [{"actor": "sub:subagent:a3856b2d", "item": "Registering REQ-20260828-028-62x6 boundary request", "started": "2026-08-28T16:52:25+09:00", "ended": "2026-08-28T16:52:25+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/subagents/agent-a3856b2d5d3ca45e2.jsonl"}, {"actor": "sub:designer:a5fb47b5", "item": "멈춘 것 화면에 · 빈 버튼줄", "started": "2026-08-28T19:02:48+09:00", "ended": "2026-08-28T20:02:09+09:00", "result": "stalled", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/tasks/a5fb47b53d09d4b99.output", "reason": "183초째 진전 없음 (기준 180초)"}, {"actor": "sub:security-engineer:a526959b", "item": "코드 파일 노출 경계 판정", "started": "2026-08-28T19:03:13+09:00", "ended": "2026-08-28T19:10:01+09:00", "result": "done", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/tasks/a526959beaed57cd1.output"}, {"actor": "sub:security-engineer:a526959b", "item": "`[2026-08-28 19:12:41 KST - security-engineer]`", "started": "2026-08-28T19:10:01+09:00", "ended": "2026-08-28T19:10:01+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/subagents/agent-a526959beaed57cd1.jsonl"}, {"actor": "sub:designer:a978bfd2", "item": "뜨는 순간 끊기는 요청들", "started": "2026-08-28T20:09:07+09:00", "ended": "2026-08-28T21:11:16+09:00", "result": "stalled", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/tasks/a978bfd2ccb4bc91b.output", "reason": "181초째 진전 없음 (기준 180초)"}, {"actor": "sub:backend-developer:a30daab1", "item": "코드 읽기 게이트 구현", "started": "2026-08-28T20:09:39+09:00", "ended": "2026-08-28T20:20:32+09:00", "result": "stalled", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/tasks/a30daab19f9804590.output", "reason": "210초째 진전 없음 (기준 180초)"}, {"actor": "sub:backend-developer:a30daab1", "item": "`[2026-08-28 20:37:12 KST - backend-developer]`", "started": "2026-08-28T20:37:58+09:00", "ended": "2026-08-28T20:37:58+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/subagents/agent-a30daab19f9804590.jsonl"}, {"actor": "sub:subagent:a57d0206", "item": "Attaching captures to REQ-20260828-028-62x6 note", "started": "2026-08-28T22:31:42+09:00", "ended": "2026-08-28T22:31:42+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/subagents/agent-a57d0206a8ecdb627.jsonl"}, {"actor": "sub:designer:ab64fee2", "item": "`[2026-08-28 22:32:58 KST - designer]`", "started": "2026-08-28T22:33:36+09:00", "ended": "2026-08-28T22:33:36+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/subagents/agent-ab64fee2fe18971af.jsonl"}]
project: section9
parent: REQ-20260828-021-62x6
relates_why: {}
tags: ["access", "dashboard", "assets", "attached", "png", "image"]
created: 2026-08-28T16:52:15+09:00
status_since: 2026-08-28T22:32:55+09:00
updated: 2026-08-28T22:33:36+09:00
priority: 50
---

## Original

021 을 하며 남긴 부분이다. 문서 id 링크와 문서 첨부 경로(/api/asset — 문서 가시성을 그대로 상속하는 **이미 있는** 게이트)는 냈고, **코드 파일 경로는 손대지 않았다.**

왜 안 냈는가 — 새 게이트를 만들지 않기로 했다(DOC-20260827-007).

지금 저장소 파일을 화면에 내주는 길은 `/api/asset` 하나뿐이고, 그것은 문서 첨부 전용이며 판정은 `doc_visible(meta, me)` 다. 코드 파일에는 대응하는 판정이 없다. 새로 만들면 그것이 곧 옆문이다.

그럴듯한 경계를 하나 떠올렸다가 버렸다: "git 이 추적하는 파일만". 이 저장소는 `state/sessions/*.json` 과 `users/*/profile.md` 를 추적한다. 그리고 바로 어제 REQ-20260828-012 는 비밀을 다루며 **"값도 경로도 주지 않는다"** 고 정했다 — `users/*/config.json` 에는 바깥 비밀 폴더 경로가 들어 있고, 그 파일이 서빙되면 어제의 결정이 조용히 뒤집힌다. 하루 전 결정을 뒤집는 규칙이라면 그것은 내가 혼자 정할 경계가 아니다.

정해야 할 것
1. 판정 함수 하나. 이름과 자리(bin/s9), 그리고 `doc_visible` 과의 관계.
2. 막을 것: `users/*/secrets`·`users/*/config.json`·`state/`·리포 밖(realpath 확인)·심볼릭 링크 탈출.
3. 낼 것의 상한: 텍스트 파일만·크기 상한·줄 범위 요청(`?from=&to=`).
4. 누구에게: 등록 사용자 전원인가, admin 인가. (대시보드는 127.0.0.1 지만 `as=` 대리와 등록 사용자 개념이 이미 있다.)
5. 테스트: 막아야 할 경로 각각이 404 인가 — 존재 여부도 새지 않는가.

화면 쪽 몫 (경계가 정해진 뒤)
- 터미널·문서 본문의 `web/index.html:4016` 꼴을 손잡이로 만든다. 리드 판단: 파일 전체를 새 탭으로 여는 것보다 **그 줄 언저리만** 보이는 미리보기가 낫다.
- 021 이 만든 터미널 미리보기 카드(`.ccpeek`)를 그대로 쓴다 — 새 컴포넌트를 만들지 않는다. 머리에 경로와 줄 번호, 본문에 그 줄 앞뒤 몇 줄, 그 줄만 잉크로.

## Notes

### 2026-08-28T16:52:25+09:00 subagent (agent: sub:subagent:a3856b2d, by nicehugepark)

Registering REQ-20260828-028-62x6 boundary request

### 2026-08-28T17:07:49+09:00 commit (by nicehugepark)

커밋 87ead56 — 한 줄 한 문법, 그리고 언급한 것을 눌러서 연다 (REQ-007 · 021 · 022)

### 2026-08-28T19:10:01+09:00 subagent (agent: sub:security-engineer:a526959b, by nicehugepark)

`[2026-08-28 19:12:41 KST - security-engineer]`

**결론: 허용목록으로 하되, 허용하는 것은 "주인이 없는 디렉터리" 다섯 개뿐이다 — `bin/ docs/ harness/ tests/ web/` + 최상위 `README.md·CLAUDE.md·pyproject.toml`. "git 추적 파일만"은 쓰면 안 된다. 그 규칙은 오늘 확인한 실물에서 곧바로 어제 결정을 뒤집는다.** 기능 자체는 해도 된다(값 > 위험). 다만 `/api/code` 는 루프백 바인드일 때만 응답해야 한다.

---

### 1. "git 추적 파일만" 이 왜 죽는가 — 실물로 확인한 것

designer 가 떠올렸다 버린 그 경계를 실제로 돌려 봤다. 추적되는데 내주면 안 되는 것이 **네 종류** 있다.

| 실제 경로 | 추적됨 | 내주면 깨지는 것 |
|---|---|---|
| `users/nicehugepark/config/settings.json` | ✅ | `"external_secrets_path": "/home/sjpark1/s9-secrets"` — REQ-20260828-012-62x6 의 **"값도 경로도 주지 않는다"** 가 그 자리에서 무효 |
| `state/sessions/DESKTOP-TEHV1KR__*.json` (91개) | ✅ | `transcript_path`(원문 절대경로) · `user` · `active_reqs` — REQ-20260824-022 스트림 격리(`stream_visible`)가 **경로와 소유자 수준에서** 새어 나감 |
| `vault/**/*.md` (약 900개) | ✅ | **`doc_visible` 전면 무효.** 못 보는 문서가 그냥 파일로 열린다. 이게 이 기능의 가장 큰 옆문이다 |
| `users/*/profile.md` | ✅ | `emails` · `os_accounts` · `machine_accounts` — DOC-20260827-008-62x6 이 "가리지 않는다"고 한 것들이지만, **화면 격리와는 별개** 문제(남의 프로필을 내 화면에서 읽음) |

반대로 **추적되지 않는데 디스크에 있어서** 경로 접두 차단만으로는 못 막는 것:

- `users/nicehugepark/secrets/` — `.gitignore` 로만 막혀 있다. 파일시스템에는 있다.
- `users/nicehugepark/streams/*.jsonl` · `streams/` — 대화 원문. REQ-20260827-047-62x6 이 157MB 걷어낸 그것.
- `state/*` (sessions 제외) — `serve.log`, `auto_resume/`, `worktree_owners`, `terminal/`
- **`.claude/settings.json`** — DOC-20260827-006-62x6 이 "이건 설정이 아니라 **공격 표면 지도**"라고 부른 그 파일. 차단목록을 쓰면 이걸 빼먹는 날이 온다.
- `index/catalog.jsonl` — 전 문서 메타의 통짜 덤프. 한 줄로 `doc_visible` 우회.

**그래서 차단목록은 성립하지 않는다.** 위 열 줄을 다 적어도, 내일 누가 `ops/`·`.env.local`·`terraform/` 을 만드는 순간 기본값이 "열림"이다. 허용목록은 그 반대다 — 새 디렉터리는 기본으로 닫히고, 여는 사람이 이유를 대야 한다.

그리고 허용목록이 어제 결정과 충돌하지 않는 이유는 규칙 한 줄로 말할 수 있다: **허용 뿌리 다섯 개에는 "사용자별 가시성"이라는 개념 자체가 없다.** `vault/`·`users/`·`state/`·`index/` 는 전부 주인이 있고 이미 판정 함수가 하나씩 붙어 있다(`doc_visible`·`stream_visible`·`secret_keys`). 주인 있는 것은 그 판정으로만 나가고, 주인 없는 것만 이 문으로 나간다.

---

### 2. 판정 함수 — 그대로 옮길 수 있게

**자리: `bin/s9` 8950행 `doc_visible` 바로 위.** 형제로 붙여야 다음에 `doc_visible` 을 읽는 사람이 세 번째 게이트를 발명하지 않는다. 이름도 같은 계열(`doc_visible` · `stream_visible` · `code_visible`).

```python
### ── 소스 파일 열람 (REQ-20260828-028-62x6) ────────────────────────────────
### doc_visible 의 형제다. doc_visible 이 '주인 있는 문서'를 주인으로 가르고,
### 이것은 '주인 없는 소스'만 연다 — 그래서 me 를 인자로 받지 않는다.
### ⚠ vault/·users/·state/·index/ 는 여기 없다. 그 파일들에는 판정이 따로
###   있고(doc_visible·stream_visible·secret_keys), 여기로 내주면 그 판정들이
###   전부 옆문으로 무효가 된다. 뿌리를 늘리기 전에 이 줄을 읽어라.
CODE_ROOTS = ("bin", "docs", "harness", "tests", "web")
CODE_FILES = ("CLAUDE.md", "README.md", "pyproject.toml")
CODE_EXT = (".py", ".md", ".html", ".css", ".js", ".json", ".toml",
            ".txt", ".sh", ".cmd", ".yml", ".yaml")
CODE_MAX_BYTES = 4 * 1024 * 1024     # web/guide.html 1.7MB 가 최대 실물
CODE_MAX_LINES = 400                 # 한 응답이 낼 수 있는 줄 수 상한


def _code_shape_ok(rel):
    """경로 '모양'만 본다 — 파일시스템을 만지지 않는다.
    정규화 전(글자)과 후(실경로) 두 번 부른다. 한 번만 부르면 심링크가 샌다."""
    if not rel or len(rel) > 512 or "\0" in rel:
        return False
    segs = rel.split("/")
    if any(s in ("", ".", "..") or s.startswith(".") for s in segs):
        return False          # 빈 조각·상대참조·점파일(.git/.claude/.github)
    if len(segs) == 1:
        return segs[0] in CODE_FILES
    if segs[0] not in CODE_ROOTS:
        return False
    base = segs[-1]
    if "." not in base:       # 확장자 없는 것은 bin/ 바로 밑에서만 (bin/s9)
        return segs[0] == "bin" and len(segs) == 2
    return base.lower().endswith(CODE_EXT)


def code_visible(rel):
    """소스 파일 열람 판정. 통과하면 절대경로, 아니면 "" — 이유는 돌려주지 않는다.
    (없는 것과 막힌 것을 구분해 주면 그 차이가 곧 목록이 된다.)"""
    rel = (rel or "").replace("\\", "/").strip()
    if rel.startswith("/") or re.match(r"^[A-Za-z]:", rel):
        return ""                                   # 절대경로 주입
    if not _code_shape_ok(rel):
        return ""
    root = os.path.realpath(ROOT)                   # ROOT 는 S9_ROOT 로 바뀔 수 있다
    full = os.path.realpath(os.path.join(root, rel))
    try:
        if os.path.commonpath([full, root]) != root:
            return ""                               # 리포 밖 (5978행과 같은 관용구)
    except ValueError:
        return ""                                   # 드라이브가 다름(Windows)
    # 실경로로 같은 모양 검사를 한 번 더 — 모양 검사는 심링크를 모른다.
    # web/x.html -> ../users/.../settings.json 이 여기서 죽는다.
    if not _code_shape_ok(os.path.relpath(full, root).replace(os.sep, "/")):
        return ""
    if not os.path.isfile(full) or os.path.getsize(full) > CODE_MAX_BYTES:
        return ""
    return full
```

**라우트: 11638행 `/api/asset-text` 옆.** 게이트를 새로 쓰지 않고 `code_visible` 하나만 부른다 — `/api/asset-text` 가 `/api/asset` 과 "글자 그대로 같은 게이트"를 쓴 그 규율 그대로다.

```python
elif parsed.path == "/api/code":
    # 소스 한 줄 언저리 (REQ-20260828-028-62x6). 파일 전체를 내지 않는다 —
    # 화면이 원하는 것은 그 줄이고, 전체를 내면 비용도 노출도 파일 크기만큼 는다.
    #
    # ⚠ 이 라우트는 **루프백 전제 위에서만** 성립한다. whoami_info 가
    #   "브라우저 사용자 = 서버 기동 계정"을 파생으로 쓰기 때문이다(279행).
    #   --host 0.0.0.0 으로 띄운 서버에서는 소스를 내주지 않는다 — 전제가
    #   깨진 자리에서 조용히 동작하는 것이 가장 나쁜 경우다.
    if SERVE_HOST not in ("127.0.0.1", "localhost", "::1"):
        self._send(404, "text/plain", b"not found"); return
    rel = qs.get("path", [""])[0]
    def _int(k, d, lo, hi):
        try:    return max(lo, min(hi, int(qs.get(k, [str(d)])[0])))
        except ValueError: return d
    line, ctx = _int("line", 0, 0, 10**7), _int("ctx", 12, 0, 60)
    full = code_visible(rel)
    ok, out, total = False, [], 0
    if full:
        try:
            with open(full, "rb") as f:
                bin_ = b"\0" in f.read(8192)        # 이진 파일은 내지 않는다
            if not bin_:
                lo = max(1, line - ctx) if line else 1
                hi = lo + min(CODE_MAX_LINES, 2 * ctx + 1) - 1
                with open(full, encoding="utf-8", errors="replace") as f:
                    for i, ln in enumerate(f, 1):
                        total = i
                        if lo <= i <= hi:
                            out.append(ln.rstrip("\n")[:400])
                ok = True
        except OSError:
            ok = False
    if not ok:
        # 막힌 것·없는 것·이진 파일이 **바이트까지 같은 404** 다.
        self._send(404, "text/plain", b"not found")
    else:
        self._json({"path": os.path.relpath(full, os.path.realpath(ROOT))
                            .replace(os.sep, "/"),
                    "line": line, "from": lo, "to": min(hi, total),
                    "total": total, "lines": out})
```

`SERVE_HOST` 는 `SERVE_STARTED`/`SERVE_CODE_STAMP` 옆에 `args.host` 로 한 번 박아 두면 된다.

되돌려 주는 `path` 는 **입력이 아니라 정규화된 실경로**다. 입력을 그대로 되비추면 그 글자가 화면에 들어간다 — 화면 쪽은 `textContent` 로만 그려야 한다(`innerHTML` 금지). 경로의 출처가 **터미널 원문**, 즉 에이전트가 쓴 글자라는 점이 중요하다: 어떤 링크를 그릴지로 막을 수 없고, 서버에서만 막힌다.

---

### 3. 공격 표면 — 기존 방식과 대응

| 표면 | 이 저장소의 기존 방식 | 여기서 |
|---|---|---|
| 경로 탈출 `../` | `/api/asset`: `basename()` + `safe_name()` | 조각 단위 `..` 거부 + `commonpath` (5978행 관용구) |
| 심볼릭 링크 | 없음(첨부는 basename 이라 무의미) | **realpath 후 모양 검사 재실행** — 새 방어, 여기서 처음 필요해짐 |
| 절대경로 주입 | `basename()` 이 흡수 | `/`·`C:` 접두 거부 |
| 대소문자·유니코드 | `safe_name` 이 `[^\w.@-]` → `_` | 확장자는 `.lower()` 비교. 조각 검사가 문자셋을 좁히므로 정규화 우회 여지 없음 |
| 존재 여부 누설 | `/api/asset-text`: "사이드카 없음"과 "못 봄"을 **같은 404** | 동일. 막힘·부재·이진을 바이트까지 같은 404 |
| 자원 소진 | 없음(첨부는 통째 읽음) | 창(window)만 읽는다 + 4MB 상한 + 줄 400 상한 + 한 줄 400자 |
| 대리 상승 | `viewer_of`: 비admin 의 `as=` 는 조용히 무시 | **인자를 안 받는다** — 상승할 축이 없다 |

---

### 4. 다중 사용자 — 누가 보나

**등록 사용자 전원. admin 제한을 걸지 않는다.**

근거: 허용 뿌리 다섯 개의 내용물은 (a) 사람마다 다르지 않고, (b) 이 인스턴스에서는 이미 **공개 GitHub 에 있는 바로 그 바이트**다(`github.com/nicehugepark/section9`, PUBLIC 확인함). admin 만 열면 GitHub 에서 누구나 읽는 파일을 대시보드에서만 가리는 셈이라 보안이 아니라 연극이고, "터미널에 나온 경로를 눌러 본다"는 기능 자체가 대부분의 사용자에게 죽는다.

**인스턴스 저장소에서는 답이 달라지나 — 허용목록은 안 달라지고, 스위치가 하나 필요하다.** 뿌리를 *디렉터리로 고정*했기 때문에 private fork 가 `ops/`·`infra/` 를 추가해도 자동으로 열리지 않는다. 달라지는 경우는 하나뿐이다: 포크가 **`bin/` 안에** 인스턴스 고유의 것(사내 엔드포인트·계정명·배포 스크립트)을 넣는 경우. 그래서 계정 설정이 아니라 **인스턴스 설정**으로 `code_read: on|off`(기본 on) 하나만 두고, off 면 `code_visible` 이 무조건 `""` 를 돌려준다. 사용자별 등급은 두지 않는다 — 등급을 두면 "코드에 주인이 없다"는 이 판정의 전제가 흐려지고, 그 순간 `vault/` 를 뿌리에 넣자는 말이 논리적으로 가능해진다.

---

### 5. 테스트가 고정할 것 (`tests/test_code_read_gate.py`, `python3 tests/ code_read`)

1. **열린다**: `web/index.html?line=4016&ctx=12` → 25줄, `from=4004`, `total=11468`.
2. **확장자 없는 것도 열린다**: `bin/s9?line=8950`.
3. **막힌다 — 응답이 바이트까지 같다.** 아래 전부 404 이고 본문이 서로 동일, 그리고 `docs/이런건없다.md`(진짜 부재)와도 동일해야 한다:
   `users/nicehugepark/config/settings.json` · `users/nicehugepark/secrets/X` · `state/sessions/DESKTOP-TEHV1KR__0116660e.json` · `vault/requests/2026/08/REQ-20260828-012-62x6.md` · `index/catalog.jsonl` · `.claude/settings.json` · `.git/config` · `bin/../users/nicehugepark/config/settings.json` · `../../../etc/passwd` · `/etc/passwd` · `bin\..\users\nicehugepark\config\settings.json`
4. **심링크**: `web/_t.html -> ../users/nicehugepark/config/settings.json` 을 만들고 404, 테스트가 지운다.
5. **상한**: `ctx=100000` → 줄 수 ≤ 400. 4MB 초과 임시 파일 → 404.
6. **이진**: `docs/guide-assets/*.png` → 404.
7. **클래스 단위 (이게 핵심)**: `git ls-files` 전수를 돌려 **허용 뿌리 5개 + 파일 3개 밖의 모든 추적 파일에 대해 `code_visible()==""`** 임을 확인. 1192개가 지금 이 시험을 지난다. 누가 `CODE_ROOTS` 에 한 줄 더하면 이 시험이 즉시 빨개진다 — 허용목록이 조용히 넓어지는 것을 막는 유일한 장치다.
8. **루프백 전제**: `--host 0.0.0.0` 으로 띄운 서버에서 `/api/code` 는 404, `/api/doc` 은 그대로 동작.

---

### 6. 하지 말아야 할 것 (범위를 줄이는 쪽)

기능 전체를 접을 이유는 없다. 다만 **접어야 할 세 조각**이 있다:

- **파일 전체를 새 탭으로 여는 길을 만들지 마라.** 리드의 판단(줄 언저리 미리보기)이 보안적으로도 맞다. 전체 보기를 열면 상한·창(window)·비용 논의가 전부 무의미해지고, 다음 요청은 반드시 "그럼 편집도"가 된다.
- **`?path=` 를 받는 범용 뷰어로 일반화하지 마라.** 이 API 의 이름은 `/api/code` 이고 뿌리는 코드다. `/api/file` 이 되는 순간 허용목록은 유지되지 않는다.
- **"추적 여부"를 조건에 넣지 마라.** 방금 만들어 커밋 전인 `tests/test_새것.py:12` 를 못 여는데, 정작 막아야 할 `vault/`·`state/sessions/` 는 추적되어 통과한다. 방향이 정확히 반대다.

---

### 7. 이번 건과 별개로 나온 것 두 개 (리드가 REQ 로 떼 주길)

**(A) 어제 결정이 화면이 아니라 리포에서 이미 뒤집혀 있다.** `users/nicehugepark/config/settings.json` 은 추적될 뿐 아니라 **`origin/main` 에 올라가 있고 그 리포는 PUBLIC** 이다. 안에 `external_secrets_path: /home/sjpark1/s9-secrets` 가 있다. 재현: `git ls-tree -r origin/main --name-only | grep users/nicehugepark/config/settings.json` → `git show origin/main:users/nicehugepark/config/settings.json`. REQ-20260828-012-62x6 는 화면에서 "값도 경로도" 막았는데 리포가 경로를 준다. DOC-20260827-006-62x6 의 세 층 기준을 그대로 적용하면 이 키는 `permissions` 와 같은 칸("그 자체가 비밀 / 능력을 연다")이다 — **리포에 싣지 않는다**가 답이다. 클래스 단위 수정: `secret_leak` 이 값만 보지 말고 `external_secrets_path` 같은 **비밀 위치 키**를 추적 대상 설정에서 잡아야 하고, 동기화 대상 설정 키에 허용목록(DOC-006 의 `env` 방식)을 그대로 적용해야 한다. 자격증명 자체는 전수 검색 결과 **추적 파일에 0건**이다(토큰/키/사설키 패턴 전부) — 값은 안 샜고 위치만 샜다.

**(B) `Host`/`Origin` 검증이 어디에도 없다.** `grep` 결과 0건이고 `--host` 는 인자로 열려 있다. DNS 리바인딩이면 사용자가 방문한 아무 페이지나 `http://127.0.0.1:9909/api/*` 를 same-origin 으로 읽는다 — 뷰어는 서버 기동 계정(여기서는 admin)이다. 이건 `/api/doc` 부터 이미 그런 상태이고 이번 기능이 만든 것이 아니지만, **소스 읽기를 붙이면 폭이 리포 전체로 넓어진다.** 값싼 대응은 한 줄이다: `do_GET`/`do_POST` 초입에서 `Host` 헤더가 `127.0.0.1|localhost|[::1]` (+포트)가 아니면 421/404. 위 6절의 루프백 조건은 그 절반짜리 임시방편이다.

---

### 8. 내가 틀렸다는 걸 어떻게 알아채나

- **`CODE_ROOTS` 에 줄이 하나 늘어나는 커밋이 나오면** 내 전제("다섯 개로 충분하다")가 틀린 것이다. 7-③ 시험이 그때 빨개지므로 조용히 넘어가지 않는다. 그 시점에 늘리는 대신 다시 판정하라.
- **서버 로그의 `/api/code` 404 가 특정 경로군으로 몰리면** 막을 것을 막은 게 아니라 쓸 것을 막은 것이다(예: 사람들이 `projects/section9/CONTEXT.md` 를 계속 두드림). 나는 `projects/` 를 일부러 뺐다 — 프로젝트 멤버십 축이 있어서다. 두드림이 잦으면 그 판단이 틀린 것이고, 답은 뿌리 추가가 아니라 `project_can` 을 태우는 별도 판정이다.
- **허용 뿌리 안에서 비밀·절대경로·이메일이 하나라도 나오면** "이 뿌리에는 주인이 없다"가 무너진다. 지금은 0건임을 확인했다(`git ls-files bin web tests docs harness | xargs grep -lE '<비밀 패턴>'`). 이 grep 을 커밋 훅이나 주기 점검에 붙여 두면 전제가 깨지는 날을 자동으로 알 수 있다.
- **이 리포가 private 로 바뀌거나 포크가 `bin/` 에 인스턴스 고유의 것을 넣으면** "이미 GitHub 에 공개된 바이트"라는 4절의 근거가 사라진다. 그때는 `code_read: off` 가 기본이 되어야 한다.
- **가장 확실한 반증**: 작업 중 커밋 전 워킹트리에 붙여넣은 토큰은 이 판정이 못 막는다. 루프백 전제가 깨진 서버에서 이게 터지면 내 설계가 틀린 것이다 — 그래서 6절의 루프백 조건이 "있으면 좋은 것"이 아니라 이 판정의 **전제를 강제하는 부분**이다.

관련 파일: `/home/sjpark1/section9/bin/s9` (`doc_visible` 8950행 · `viewer_of` 11249행 · `/api/asset` 11616행 · `/api/asset-text` 11638행 · `commonpath` 관용구 5978행 · `--host` 기본값 12829행), `/home/sjpark1/section9/.gitignore`, `/home/sjpark1/section9/users/nicehugepark/config/settings.json`.

### 2026-08-28T20:06:56+09:00 decision (by nicehugepark)

security-engineer 판정 (요약)

**결론: 허용목록으로 하되 허용하는 것은 "주인이 없는 디렉터리" 다섯뿐 — `bin/ docs/ harness/ tests/ web/` + 최상위 `README.md·CLAUDE.md·pyproject.toml`. "git 추적 파일만" 은 쓰면 안 된다.**

"추적 파일만" 이 죽는 이유 (실물 확인)
- `users/*/config/settings.json` 추적됨 → REQ-20260828-012-62x6 의 "값도 경로도 주지 않는다" 가 그 자리에서 무효
- `state/sessions/*.json` 91개 추적됨 → transcript 절대경로·소유자·active_reqs 노출, 스트림 격리가 경로 수준에서 샘
- **`vault/**/*.md` 약 900개 추적됨 → `doc_visible` 전면 무효.** 못 보는 문서가 그냥 파일로 열린다 — 가장 큰 옆문
- 반대로 추적 안 되는데 디스크에 있는 것: `users/*/secrets/` · 스트림 원문 · `state/*` · **`.claude/settings.json`**(DOC-20260827-006-62x6 이 "공격 표면 지도" 라 부른 것) · `index/catalog.jsonl`(한 줄로 doc_visible 우회)
→ 차단목록은 성립하지 않는다. 내일 누가 `ops/`·`.env.local` 을 만들면 기본이 "열림" 이다. 허용목록은 반대다.

허용목록이 어제 결정과 충돌하지 않는 이유 한 줄: **허용 뿌리 다섯에는 "사용자별 가시성" 이라는 개념 자체가 없다.** vault·users·state·index 는 전부 주인이 있고 판정 함수가 하나씩 붙어 있다(doc_visible·stream_visible·secret_keys). 주인 있는 것은 그 판정으로만 나가고 주인 없는 것만 이 문으로 나간다.

판정 자리: `bin/s9` 의 `doc_visible` **바로 위**에 형제로 — 이름도 같은 계열(`code_visible`). 그래야 다음에 읽는 사람이 세 번째 게이트를 발명하지 않는다.
막는 것: 조각 단위 `..` 거부 + `commonpath`(기존 관용구) + **realpath 후 모양 검사 재실행**(심링크 — 여기서 처음 필요해진 새 방어) + 절대경로·드라이브 접두 거부 + 점파일 거부 + 확장자 허용목록 + 4MB·400줄·400자 상한 + 이진 거부. **막힘·부재·이진이 바이트까지 같은 404** (기존 /api/asset-text 의 규율 그대로).
`/api/code` 는 **루프백 바인드일 때만** 응답한다 — whoami 가 "브라우저 사용자 = 서버 기동 계정" 을 파생으로 쓰기 때문이다. 전제가 깨진 자리에서 조용히 동작하는 것이 가장 나쁘다.
누가 보나: 등록 사용자 전원. 뿌리 다섯의 내용물은 이미 **공개 GitHub 에 있는 바로 그 바이트**다 — admin 만 열면 보안이 아니라 연극이다. 인스턴스 포크용으로 `code_read: on|off` 하나만.
클래스 단위 테스트가 핵심: `git ls-files` 전수를 돌려 허용 뿌리 밖 추적 파일 전부에 `code_visible()==""` 임을 확인(지금 1192개가 지난다). **누가 CODE_ROOTS 에 한 줄 더하면 즉시 빨개진다** — 허용목록이 조용히 넓어지는 것을 막는 유일한 장치다.
하지 말 것: 파일 전체를 새 탭으로 여는 길(상한·창 논의가 무의미해지고 다음 요청은 반드시 "그럼 편집도" 다) · `?path=` 범용 뷰어로 일반화(`/api/file` 이 되는 순간 허용목록은 유지되지 않는다) · "추적 여부" 를 조건에 넣기(방금 만든 파일은 못 열고 정작 막아야 할 vault 는 통과 — 방향이 정반대).

함께 나온 별건: **Host/Origin 검증이 어디에도 없다**(grep 0건). DNS 리바인딩이면 사용자가 방문한 아무 페이지나 `127.0.0.1:9909/api/*` 를 same-origin 으로 읽는다 — 뷰어는 서버 기동 계정이다. `/api/doc` 부터 이미 그런 상태이고 이번 기능이 만든 것이 아니지만, **소스 읽기를 붙이면 폭이 리포 전체로 넓어진다.** 한 줄 대응: do_GET/do_POST 초입에서 Host 가 루프백이 아니면 404.

### 2026-08-28T20:11:52+09:00 tdd (by nicehugepark)

### TDD 시나리오 (서버 몫 — tests/test_code_read_gate.py)

- [x] S1. 열린다: `/api/code?path=web/index.html&line=4016&ctx=12` → 200, 25줄, from=4004, total 일치
- [x] S2. 확장자 없는 것도 열린다: `path=bin/s9&line=100`
- [x] S3. 막힌다 — 응답이 바이트까지 같다: users config·secrets·state/sessions·vault·index/catalog.jsonl·.claude/settings.json·.git/config·`bin/../users/...`·`../../../etc/passwd`·`/etc/passwd`·역슬래시 변형, 그리고 진짜 부재(`docs/없다.md`) 전부 동일 바디
- [x] S4. 심링크: `web/_t.html -> ../users/*/config/settings.json` → 404 (시험이 지운다)
- [x] S5. 상한: `ctx=100000` → 줄 수 ≤ 400 · 4MB 초과 파일 → 404 · 한 줄 400자 절단
- [x] S6. 이진: NUL 바이트 든 파일 → 404 (같은 바디)
- [x] S7. 클래스 단위: `git ls-files` 전수 → 허용 뿌리 5 + 파일 3 밖의 모든 추적 파일에 `code_visible()==""`
- [x] S8. 루프백 전제: `--host 0.0.0.0` 서버에서 `/api/code` 404, `/api/doc` 은 정상
- [x] S9. 인스턴스 스위치: `S9_CODE_READ=off` 면 열리던 것도 404
- [x] S10. Host 검증(별건 판단분): 루프백 바인드 서버에 `Host: evil.example.com` → 거부, `Host: 127.0.0.1:PORT` → 통과, 0.0.0.0 바인드 서버는 임의 Host 통과(원격 접속을 깨지 않는다)
- [x] S11. 정적 html 라우트(`/_t.html`)도 심링크 탈출을 막는다 — /api/code 만 막으면 옆문이 남는다

### 2026-08-28T20:37:11+09:00 response (by nicehugepark)

`[2026-08-28 20:34 KST - backend-developer]`

**서버 몫 완료 — security-engineer 판정을 그대로 옮겼다. 화면 몫이 남아 in-progress 유지.**

### 넣은 것 (`bin/s9`, +207줄)

| 자리 | 무엇 |
|---|---|
| `doc_visible` **바로 위** (9016행 부근) | `CODE_ROOTS`·`CODE_FILES`·`CODE_EXT`·상한 상수 · `code_read_on()` · `_code_shape_ok()` · `code_visible(rel)` |
| `SERVE_STARTED` 옆 (7205행) | `SERVE_HOST` · `LOOPBACK_BINDS` · `bind_is_loopback()` · `host_header_ok()` |
| `/api/asset-text` 와 `/api/doc` 사이 | `/api/code` 라우트 |
| `do_GET`/`do_POST` 초입 | `_host_guard()` |
| 정적 html 라우트 | `abspath` → `realpath` (아래 "덤으로 막은 것") |
| `cmd_serve` | `SERVE_HOST = args.host` (지문 박는 자리와 같은 곳) |

세 번째 게이트는 만들지 않았다 — `/api/code` 는 `code_visible` 하나만 부르고 자체 판정이 없다. 시험(`test_f1`)이 두 함수가 형제 자리에 붙어 있음을 못 박는다.

### 시험 (`tests/test_code_read_gate.py`, 19개 · 전부 초록)

`python3 tests/ code_read` → **Ran 19 tests, OK**. 문자열 검사가 아니라 **임시 ROOT 에 실제 서버 두 대(127.0.0.1 · 0.0.0.0)를 띄워** 묻는다.

- [x] S1 창 열림 `web/index.html&line=250&ctx=12` → from=238·to=262·25줄, `path` 는 정규화된 실경로
- [x] S2 `bin/s9` (확장자 없음, bin/ 바로 밑에서만)
- [x] S3 막힘 20종 + vault 문서 + 빈 path 가 **진짜 부재와 (status, body) 바이트까지 동일**
- [x] S4 심링크 `web/_t.html -> ../users/*/config/settings.json` → 404 (시험이 만들고 지운다)
- [x] S5 `ctx=100000` → 창 상한 · 한 줄 400자 절단 · 4MB 초과 404
- [x] S6 이진(NUL) → 404, 같은 바디
- [x] **S7 클래스 단위**: `git ls-files` 전수 1206개 → 허용 뿌리 밖 **937개 전부 `code_visible()==""`**, 누수 0. 뿌리 안 269개 중 256개가 열리고 13개는 png·pdf(이진) — 막을 것만 막는다. `CODE_ROOTS` 에 한 줄 더하면 여기가 즉시 빨개진다
- [x] S8 `--host 0.0.0.0` 서버: `/api/code` 404, `/api/doc` 200 (기존 경로 안 깼음)
- [x] S9 `S9_CODE_READ=off` → 열리던 `README.md` 도 닫힘
- [x] S10 Host 검증 (아래)
- [x] S11 정적 html 라우트도 같은 심링크를 막는다

### 판단 ①: Host 검증 — **넣었다. 단, 루프백 바인드일 때만.**

넣지 않을 이유를 먼저 찾았는데, "깨진다" 는 것이 실물에서 하나도 없었다.

- 저장소 전체에서 대시보드에 붙는 경로를 전수 확인했다 — `bin/s9`(9580·9594행) · `bin/s9-install`(373행) · `bin/s9-doctor`(90행) · `tests/portpool.py` · `harness/` 안내문까지 **전부 `127.0.0.1` 리터럴**이다. 화면(`web/index.html`)은 절대 URL 없이 상대경로로만 fetch 한다. WSL↔Windows 도 `127.0.0.1` 공유라 Host 가 루프백 리터럴이다.
- **그래서 조건을 반으로 갈랐다**: 루프백 바인드일 때만 Host 를 검사하고, `--host 0.0.0.0`·LAN IP 로 띄운 서버는 **아무 Host 나 통과**시킨다. 원격 접속은 운영자가 의도적으로 연 것이고, 거기서 루프백 Host 를 요구하면 그 접속이 통째로 죽는다. 리바인딩이 노리는 것도 원래 "밖에서는 못 닿는 서버" 라 방어를 잃지 않는다. 시험 `test_e3` 이 이 비대칭을 못 박는다.
- 통과시키는 Host: `127.0.0.0/8` · `localhost` · `::1`(`[::1]:port` 포함), 포트 유무 무관. Host 부재는 거부(HTTP/1.1 필수 헤더).
- 응답은 404 가 아니라 **421 + 한국어 안내**다. 여기서는 숨길 것이 없고(공격자는 이미 붙어 있다) 사람이 이 응답을 보면 원인을 바로 알아야 한다. `/api/code` 의 404 규율과는 목적이 다른 자리다.
- 적용 범위는 `do_GET`·`do_POST` **전부** — `/api/code` 만 막으면 `/api/doc`·`/api/catalog` 는 그대로 새고, 이미 그 상태였다.

### 판단 ②: 덤으로 막은 것 — 정적 html 라우트의 심링크 (별건이지만 같은 구멍)

`/[\w.-]+\.html` 라우트가 `os.path.abspath` 로 접두를 봤다. **abspath 는 심링크를 모른다.** 이 요청의 시험이 만드는 바로 그 심링크(`web/_t.html -> ../users/*/config/settings.json`)를 이 라우트는 **200 으로 그대로 서빙했다** — `/api/code` 만 막으면 옆문이 그대로 남는다. `realpath` + 부모 디렉터리 일치로 고쳤고 `test_b4` 가 지킨다. 재현: 그 심링크를 만들고 `GET /_t.html`.

### 인스턴스 스위치의 자리

`code_read: on|off` 는 **환경변수 `S9_CODE_READ=off`** 로 뒀다. 이 저장소에 인스턴스 설정 파일이라는 것이 없고, 같은 성격의 기존 스위치(`S9_SYNC=off`, 4586행)가 정확히 이 모양이다 — 새 설정 체계를 발명하는 것보다 있는 관용구를 쓰는 쪽을 골랐다. `code_visible` 이 **호출 시점에** 읽으므로 서버 재기동 없이도 반영된다.

### 전체 스위트

`python3 tests/` → **Ran 1592 tests, 실패 3.** 세 개 전부 `web/index.html` 의 JS 소스를 글자로 검사하는 시험이고(`test_stream_switch.Screen.test_tab_hidden_when_off` · `test_stream_switch_ui.test_the_tab_follows_immediately` · `test_trans_late.test_t1_retries`), 그 파일은 지금 다른 세션(designer, REQ-20260828-039-62x6)이 쥐고 편집 중이다(HEAD 대비 +312/-85). **내 변경은 `bin/s9` 한 파일이고 세 시험 중 어느 것도 `bin/s9` 를 읽지 않는다.** 직전 회차에서 유일하게 났던 `test_access_isolation` 포트 오류는 단독 실행 시 통과했고 재실행에서 사라진 포트 경합 flake 다.

### 화면 몫에 넘기는 계약 (다음 회차)

```
GET /api/code?path=<리포 상대경로>&line=<n>&ctx=<0..60>
200 {"path": "web/index.html",   // 입력이 아니라 정규화된 실경로
     "line": 4016, "from": 4004, "to": 4028, "total": 11468,
     "lines": ["...", ...]}      // 한 줄 400자에서 잘림, 개행 없음
404 "not found"                  // 막힘·부재·이진·비루프백 바인드 — 전부 동일
```

- `lines` 는 **`textContent` 로만** 그려라(`innerHTML` 금지). 이 경로의 출처는 터미널 원문, 즉 **에이전트가 쓴 글자**다 — 어떤 링크를 그릴지로 막을 수 없고 서버에서만 막힌다. 되돌려 주는 `path` 도 마찬가지다.
- 파일 전체를 새 탭으로 여는 길은 만들지 마라(판정 §6). 021 이 만든 `.ccpeek` 카드를 그대로 쓴다.
- 404 의 이유를 화면이 추측해 문구를 갈라 쓰면(“막힘” vs “없음”) 서버가 지운 차이를 화면이 되살린다. 하나의 문구로.

### 남은 것 / 한계

- **화면 몫 미착수** — 그래서 review 가 아니라 in-progress 다.
- 판정 §7-(A)(공개 리포에 `external_secrets_path` 가 올라가 있다)는 이 요청의 범위 밖이고 손대지 않았다. 별건 REQ 가 필요하다.
- 워킹트리에 커밋 전 붙여넣은 토큰이 허용 뿌리 안에 있으면 이 판정은 못 막는다(판정 §8의 자백 그대로). 루프백 조건 + Host 검증이 그 노출 범위를 브라우저 밖으로 못 나가게 붙든다.

### 2026-08-28T20:37:58+09:00 subagent (agent: sub:backend-developer:a30daab1, by nicehugepark)

`[2026-08-28 20:37:12 KST - backend-developer]`

**서버 몫 완료. 커밋하지 않았고 REQ 는 in-progress 그대로 뒀다 — 화면 몫이 남았다.**

### 변경 파일 (2개, 커밋 안 함)

- `/home/sjpark1/section9/bin/s9` (+207줄) — `web/index.html` 은 읽기만 했다
- `/home/sjpark1/section9/tests/test_code_read_gate.py` (신규, 19 시험)

### 넣은 것

판정 `code_visible(rel)` 을 `doc_visible` **바로 위 형제 자리**에 붙였다. 세 번째 게이트는 없다 — `/api/code` 는 `code_visible` 하나만 부르고 자체 판정을 하지 않는다. 시험이 두 함수의 인접성까지 못 박는다.

막는 것: 조각 단위 `..`/점파일/빈 조각 거부 → `commonpath`(6014행 관용구) → **realpath 후 모양 검사 재실행**(심링크) → 절대경로·드라이브 접두 거부 → 확장자 허용목록 → 4MB·400줄·400자·ctx≤60 상한 → 이진 거부. 막힘·부재·이진·비루프백이 **(status, body) 바이트까지 같은 404**.

스위치는 `S9_CODE_READ=off` 환경변수다 — 인스턴스 설정 파일이 이 저장소에 없고, 같은 성격의 기존 스위치 `S9_SYNC=off`(4586행)가 정확히 이 모양이라 새 체계를 만드는 대신 그 관용구를 썼다. 호출 시점에 읽으므로 재기동이 필요 없다.

### 판단 ①: Host 검증 — 넣었다, 단 **루프백 바인드일 때만**

"넣으면 깨진다"는 것을 실물에서 찾아봤는데 하나도 없었다. 대시보드에 붙는 경로 전수(`bin/s9` 9580·9594행, `bin/s9-install` 373행, `bin/s9-doctor` 90행, `tests/portpool.py`, harness 안내문)가 **전부 `127.0.0.1` 리터럴**이고, 화면은 절대 URL 없이 상대경로로만 fetch 한다.

그래서 조건을 반으로 갈랐다: **루프백 바인드일 때만 Host 를 검사하고, `--host 0.0.0.0`·LAN IP 로 띄운 서버는 아무 Host 나 통과**시킨다. 원격 접속은 운영자가 의도적으로 연 것이고 거기서 루프백 Host 를 요구하면 그 접속이 통째로 죽는다. 리바인딩이 노리는 것도 원래 "밖에서는 못 닿는 서버"라 방어를 잃지 않는다. 응답은 404 가 아니라 **421 + 한국어 안내** — 여기서는 숨길 게 없고(공격자는 이미 붙어 있다) 사람이 원인을 바로 알아야 한다. 적용은 `do_GET`·`do_POST` 전부.

### 판단 ②: 덤으로 막은 옆문

정적 html 라우트(`/[\w.-]+\.html`)가 `os.path.abspath` 로 접두를 봤다. **abspath 는 심링크를 모른다** — 이 요청의 시험이 만드는 바로 그 심링크(`web/_t.html -> ../users/*/config/settings.json`)를 그 라우트가 **200 으로 그대로 서빙**하고 있었다. `/api/code` 만 막으면 옆문이 남는다. `realpath` + 부모 디렉터리 일치로 고쳤고 `test_b4` 가 지킨다.

### 검증

`python3 tests/ code_read` → **Ran 19 tests, OK**. 소스 문자열 검사가 아니라 임시 ROOT 에 **실제 서버 두 대(127.0.0.1 · 0.0.0.0)를 띄워** 묻는다.

클래스 단위(S7) 실측: `git ls-files` 1206개 중 **허용 뿌리 밖 937개 전부 `code_visible()==""`, 누수 0.** 뿌리 안 269개 중 256개가 열리고 막힌 13개는 png·pdf(이진) 뿐이다.

`python3 tests/` 전체 → **Ran 1592, 실패 3.** 세 개 전부 `web/index.html` 의 JS 를 글자로 검사하는 시험(`test_stream_switch.Screen.test_tab_hidden_when_off` · `test_stream_switch_ui.test_the_tab_follows_immediately` · `test_trans_late.test_t1_retries`)이고, 그 파일은 designer 세션이 편집 중이다(HEAD 대비 +312/-85). 내 변경은 `bin/s9` 하나이고 세 시험 중 어느 것도 `bin/s9` 를 읽지 않는다. 직전 회차의 `test_access_isolation` 포트 오류는 단독 실행 통과·재실행에서 소멸한 포트 경합 flake 다.

### 화면 몫에 넘기는 계약

```
GET /api/code?path=<리포 상대경로>&line=<n>&ctx=<0..60>
200 {"path":"web/index.html", "line":4016, "from":4004, "to":4028,
     "total":11468, "lines":[...]}     // 한 줄 400자에서 잘림, 개행 없음
404 "not found"                        // 막힘·부재·이진·비루프백 전부 동일
```

`lines` 와 `path` 는 **`textContent` 로만** 그려야 한다(`innerHTML` 금지) — 이 경로의 출처는 터미널 원문, 즉 에이전트가 쓴 글자다. 404 의 이유를 화면이 추측해 문구를 갈라 쓰면 서버가 지운 차이를 화면이 되살린다.

### 한계

- 화면 몫 미착수 — 그래서 review 가 아니다.
- 판정 §7-(A)(공개 리포에 `external_secrets_path` 가 올라가 있다)는 범위 밖이라 손대지 않았다. 별건 REQ 가 필요하다.
- 커밋 전 워킹트리에 붙여넣은 토큰이 허용 뿌리 안에 있으면 이 판정은 못 막는다(판정 §8의 자백 그대로). 루프백 조건 + Host 검증이 그 노출을 브라우저 밖으로 못 나가게 붙든다.

보고는 `s9 note REQ-20260828-028-62x6 --label response` 로 문서에 남겼고, TDD 시나리오 S1~S11 은 `--label tdd` 노트에 있다.

### 2026-08-28T21:38:07+09:00 delegate (by nicehugepark)

위임 착수 21:33 — designer 에게 맡겼다: 화면 몫 — 터미널의 파일 경로를 눌러 그 줄 언저리 보기

리드가 위임 사실을 문서에 직접 적는다. 지금은 위임 귀속이 세션 등록 목록에서만 고르기 때문에, 리드가 클레임하지 않았거나 훅이 놓치면 **문서에 아무 흔적이 안 남고 화면은 멈춤으로 그린다.** 사용자가 그것 때문에 오늘만 세 번 물어야 했다. 귀속을 고치는 것이 REQ-20260828-041-62x6 의 ① 이고, 그 전까지는 이 한 줄을 손으로 적는다.

### 2026-08-28T22:10:59+09:00 designer (by nicehugepark)

화면 몫 설계 결정 — 무엇을 링크로 세우고 무엇을 안 세우나

**손잡이가 서는 자리**: `.ccval`(터미널의 파일 경로 값) 자리 그대로. 다만 지금 `.ccval` 은 **절대경로만** 집는다(`~?/…`). 이 팀이 실제로 쓰는 `web/index.html:4016` 꼴은 아무 표시도 못 받고 있었으므로, 허용 뿌리(bin·docs·harness·tests·web)와 최상위 파일 셋으로 시작하는 **리포 상대경로 규칙을 하나 더** 얹었다.

**절대경로는 워크스페이스 이름 뒤부터만 읽는다.** 서버는 리포 상대경로만 받는데 화면은 리포 뿌리를 모르고, 알려 달라고 하지도 않았다(경로도 값이다 — REQ-20260828-012-62x6). 이미 손에 있는 `whoami.workspace` 로 자르고, 그 이름이 없으면 링크를 안 세운다. 짐작으로 잘라 **남의 리포 경로를 이 리포의 파일로 열어 보이는 것**이 못 여는 것보다 나쁘다.

**열리지 않는 경로에 밑줄을 세우지 않는 방법 — 두 겹.**
① 미리 아는 것은 미리 거른다: 허용 뿌리·확장자·점파일·`..` 은 **모양**만으로 판정된다. 그래서 화면에 서버 판정의 **사본**을 뒀다. 이것은 게이트가 아니다(막는 것은 서버 `code_visible` 하나이고, 여기 글자를 고쳐도 열리는 파일은 한 개도 안 는다) — 죽은 링크를 안 그리기 위한 **그리기 규칙**이다. 두 벌이 갈라지면 화면은 열리지도 않는 밑줄을 세우거나 열리는 파일을 감춘다. 그래서 시험이 두 벌을 글자까지 맞대 본다(`tests/test_code_peek_ui.py::test_a1`) — 서버가 뿌리를 하나 늘리는 날 화면도 같이 결정하게 된다.
② 눌러 봐야 아는 것(지워졌거나·심링크거나·글자 파일이 아니거나)은 **한 번만** 눌리게 하고, 그 뒤로는 지금 화면에 떠 있는 밑줄까지 거둔다(글자는 원문 그대로 남는다). 같은 자리를 두 번 세 번 눌러 보게 만드는 것이 죽은 링크의 정체다. **서버에 못 닿은 것은 여기 해당하지 않는다** — 그건 그 경로가 못 열린다는 뜻이 아니라서, 묻지 않고 `다시 받기` 를 준다.

**못 연 이유는 한 문구다.** 서버가 막힘·부재·이진을 바이트까지 같은 404 로 만들었는데 화면이 그중 하나를 골라 말하면 서버가 지운 차이를 화면이 되살린다. 문구는 상수 하나(`CODE_FAIL`)이고, 문서 미리보기의 '찾지 못했습니다' 카드와 같은 모양이다: `이 파일을 열지 못했습니다` / `지워졌거나, 코드 폴더 밖이거나, 글자로 읽을 수 없는 파일입니다.`

**새 컴포넌트를 만들지 않았다.** REQ-20260828-021-62x6 의 `.ccpeek` 카드를 **같은 자리(줄 바로 아래)·같은 슬롯**으로 나눠 쓴다 — 한 줄에 문서 id 와 파일 경로가 나란히 서는 자리라 카드가 둘씩 열리면 무엇을 눌렀는지 알 수 없다. 칸 쓰임도 같다: `.pk` 무엇인가(문서 id ↔ 파일 경로) / `.pt` 지금 보는 것(제목 ↔ 그 줄) / `.pk` 곁들이(상태·프로젝트 ↔ 보이는 구간·전체 줄 수). 더 보기도 문서 카드의 `+ N줄 더 보기` 와 같은 텍스트 버튼이다.

**코드 블록 안 경로에는 안 건다.** 블록은 자리표시자로 먼저 빠지고 경로 규칙은 그 뒤에 돈다 — 블록의 내용은 읽을 것이 아니라 붙일 것이고, 앵커가 섞이면 드래그 선택이 링크 드래그가 된다(REQ-20260828-023-62x6). 인라인 백틱 안에는 건다(문서 id 가 이미 그렇다).

**색면·세로 띠 없음.** 그 줄만 잉크(--cc-text)로 밝히고 나머지는 --cc-dim. 줄 번호 칸은 색이 아니라 오른쪽 정렬·고정 폭으로 나누고, **선택에는 끼지 않는다**(긁어 복사한 코드 앞에 번호가 붙으면 그대로 못 붙인다). 줄 번호 색은 --cc-faint(2.8:1)에서 --cc-dim(5.2:1)으로 올렸다 — "몇 번째 줄인가" 가 이 카드의 용건이라 흐리면 셀 수가 없다.

### 2026-08-28T22:31:26+09:00 response (by nicehugepark)

`[designer]`

**화면 몫 완료.** 터미널 대답 속 `web/index.html:4016` 꼴 경로가 손잡이가 되고, 누르면 **그 줄 아래**에 REQ-20260828-021-62x6 이 만든 그 카드(`.ccpeek`)가 펴진다. 파일은 `web/index.html` 과 `tests/test_code_peek_ui.py` 둘뿐이다 — `bin/s9` 는 읽기만 했고 커밋하지 않았다.

### 넣은 것 (`web/index.html`)

| 자리 | 무엇 |
|---|---|
| `ccDocs` 옆 | 서버 허용목록의 **사본**(CODE_ROOTS·CODE_FILES·CODE_EXT) · `codeShapeOk` · `codeRel` · `ccPathSpan` · `CODE_ABS_RE`/`CODE_REL_RE`/`ccPathRules` |
| `ccText` 의 md 체인 | 절대경로 규칙을 `ccPathRules` 로 바꾸고 **리포 상대경로 규칙을 하나 더**. 인라인 백틱 안에서도 같은 함수를 부른다 |
| `ccPeek` 아래 | `ccCodePeek` · `ccCodeLoad` · `ccCodeDraw` · `ccCodeFail` · `ccCodeBury` (+ `ccEl`·`ccBtn`) |
| 클릭 위임 | `[data-tcode]` 를 `[data-tdoc]` 바로 앞에, 카드 행동에 `[data-cmore]`·`[data-cretry]` |
| CSS | `a.ccpath`(= `a.ccasset` 와 같은 획) · `.pcode` 코드 창 · `.pmore` 텍스트 버튼 정리 |
| 진단 | `?codepeek` / `=peek` / `=fail` |

### 지킨 것

- **`lines`·`path` 는 `textContent` 로만.** 카드를 세우는 네 함수에 `innerHTML` 이 한 자도 없다(시험 B1~B3 이 못 박는다). 카드의 칸을 만드는 헬퍼(`ccEl`)부터 textContent 밖으로 못 나간다.
- **404 는 한 문구.** 상수 하나(`CODE_FAIL`)이고 `ccCodeFail` 에는 이유를 가르는 분기가 없다. 문구는 문서 미리보기의 '찾지 못했습니다' 카드와 같은 모양: `이 파일을 열지 못했습니다 / 지워졌거나, 코드 폴더 밖이거나, 글자로 읽을 수 없는 파일입니다.` 시험이 "권한·비밀·이진·차단…" 같은 갈래 말이 카드에 없음을 지킨다.
- **같은 어휘.** 새 카드가 아니라 `.ccpeek` 을 **같은 자리(줄 바로 아래)·같은 슬롯(`ccPeekEl`)** 으로 나눠 쓴다 — 하나가 열리면 다른 하나는 닫힌다. 칸 쓰임도 같다(`.pk` 무엇인가 / `.pt` 지금 보는 것 / `.pk` 곁들이), 더 보기도 `+ N줄 더 보기` 와 같은 텍스트 버튼이다.
- **코드 블록 안에는 안 건다.** 블록은 자리표시자로 먼저 빠지고 경로 규칙이 그 뒤에 돈다. 실측: fenced 블록 안 손잡이 **0개**, 블록 밖 1개.
- **색면·세로 띠 없음.** 그 줄만 잉크(`--cc-text`)로, 나머지는 `--cc-dim`. 줄 번호 칸은 오른쪽 정렬·고정 폭으로만 나누고 **선택에 끼지 않는다**(긁어 복사한 코드 앞에 번호가 붙으면 그대로 못 붙인다).

### 판단 ①: 열리지 않는 경로는 **두 겹**으로 막았다

요청이 "네가 정하고 근거를 남겨라" 한 자리다.

**① 미리 아는 것은 미리 거른다.** 허용 뿌리·확장자·점파일·`..` 은 **모양**만으로 판정되므로 화면이 미리 알 수 있다. 그래서 서버 판정의 사본을 화면에 뒀다. `users/…`·`state/…`·`vault/…`·`index/…`·`.claude/…` 는 아예 정규식에 걸리지 않아 **밑줄이 서지 않는다.** 실측으로 확인했다 — 한 줄에 셋을 섞어 진짜 렌더러에 통과시키면 `users/nicehugepark/config/settings.json` 은 앵커가 아니라 맨 글자다.

이 사본은 **게이트가 아니다.** 막는 것은 서버 `code_visible` 하나이고, 화면 목록을 고쳐도 열리는 파일은 한 개도 늘지 않는다. 사본이 새로 흘리는 것도 없다 — 이 목록은 이미 `bin/s9` 안에 있고 그 파일은 이 기능으로 읽힌다. 위험은 **갈라짐** 하나뿐이라, 시험이 두 벌을 글자까지 맞대 본다(`test_a1`). 서버가 뿌리를 하나 늘리는 날 화면도 같이 결정하게 된다.

**② 눌러 봐야 아는 것은 한 번만 눌리게 한다.** 모양은 맞는데 안 열리는 것이 남는다(지워졌거나·심링크거나·글자 파일이 아니거나). 한 번 실패하면 그 경로를 기억하고, **지금 화면에 떠 있는 밑줄까지 거둔다**(글자는 원문 그대로 남는다 — 터미널은 통째로 긁어 복사하는 자리다). 실측: 실패 전 `A.ccval.ccpath / underline`, 실패 뒤 `SPAN.ccval / none`, 옆의 다른 손잡이는 그대로.

**서버에 못 닿은 것은 여기 해당하지 않는다.** 그건 그 경로가 못 열린다는 뜻이 아니라서 묻지 않고, 대신 `다시 받기` 를 준다(이 화면이 이미 쓰는 `data-retrans`·`data-resupply` 와 같은 어휘). 실제로 검증 중 WSL 중계가 연결을 끊어 이 상태가 저절로 재현됐고, 밑줄이 유지되는 것을 확인했다.

### 판단 ②: 절대경로는 워크스페이스 이름 뒤부터만 읽는다

이 팀은 `관련 파일: /home/sjpark1/section9/bin/s9:9074` 처럼도 쓴다. 서버는 리포 상대경로만 받는데 화면은 리포 뿌리를 모르고, **알려 달라고 하지 않았다** — 경로도 값이다(REQ-20260828-012-62x6). 이미 손에 있는 `whoami.workspace` 로 자르고, 그 이름이 없으면 링크를 세우지 않는다. 실측: `/home/sjpark1/section9/bin/s9:9074` → `bin/s9` 로 열리고, `/home/other/elsewhere/bin/s9:1` 은 **맨 값으로 남는다**(짐작으로 잘라 남의 리포 경로를 이 리포 파일로 열어 보이는 것이 못 여는 것보다 나쁘다).

### 덤으로 고친 것 둘

- **초점 링이 종이 톤에서 사라졌다.** 전역 규칙이 `--text`(판의 잉크)를 쓰는데 터미널은 상시 다크라 검은 선이 검은 판에 앉는다. 손잡이의 초점 링을 `--cc-text` 로 뒀다.
- **`+ 더 보기` 에 브라우저 기본 테두리가 남아 있었다**(문서 미리보기 카드도 같이). 이 판의 텍스트 버튼 어휘로 정리했다.

### 이웃 시험 하나를 손봤다 (그리고 둘은 안 건드리게 코드를 고쳤다)

`ccText` 는 REQ-20260828-021-62x6·022·023·026 이 함께 쓰는 자리라, 여기 손대면 이웃 계약 시험이 글자로 걸린다. 셋이 걸렸고 **둘은 코드 쪽을 고쳐 원상복구**했다:

- `test_term_mention::…no_colour_field_and_no_side_bar` — 카드 규칙 선택자를 `.ccterm .ccpeek` 단독으로 되돌리고, `.term` 몫은 형제 규칙으로 따로 붙였다. 시험은 안 건드렸다.
- `test_terminal_table::blocks_run_after_inline_and_before_restore` — 자리표시자 복원을 재귀 대신 **되풀이**로 바꿔 `ccBlocks()` 뒤에 오게 했다(상한 4). 순서 계약이 그대로 산다. 시험은 안 건드렸다.
- `test_term_mention::the_link_never_leaks_into_a_code_block` — 이것 하나는 손봤다. 백틱 안에서 `ccDocs(c)` 를 **글자 그대로** 짚고 있었는데, 같은 자리에서 경로 손잡이를 겹쳐 걸어야 해서 `ccDocs(ccPathRules(c, keep))` 가 됐다. 시험이 지키려던 것은 호출의 모양이 아니라 "백틱 몸통에 ccDocs 가 걸린다" 는 사실이라, 그 사실을 짚는 정규식으로 바꾸고 이유를 주석에 남겼다. 블록 순서 계약(코드블록 → 문서 id)은 그대로 지킨다.

### 검증 — 실브라우저로 **눌러서**

Windows Chrome 을 CDP 로 붙여 사람이 하듯 클릭했다(진단 스위치로 세우기만 한 것이 아니다).

- 열리는 경로 `web/index.html:4016` → 25줄(4004–4028), 4016 이 잉크, `전체 12,251줄`
- `+ 앞뒤 더 보기` → 121줄, 버튼 사라짐 · 같은 손잡이를 다시 누르면 접힘
- 없는 경로 `tests/test_gone.py:12` → 한 문구 카드 + 밑줄 거둠 + 다시 그려도 링크 안 섬
- 내주지 않는 경로 `users/nicehugepark/config/settings.json` → **앵커가 아예 없음**
- 코드 블록 안 0개 / 블록 밖 1개 · 문서 id 링크·첨부 칩·굵게·해시 강조 모두 그대로 · 콘솔 에러 0
- 키보드: Tab 으로 손잡이에 닿고(`role=button`·tabindex 0) Enter 로 열림

### 시험

새 시험 `tests/test_code_peek_ui.py` (25개) — 계약 아홉을 못 박는다: 허용목록 동기화 · textContent · 한 문구 · 죽은 링크 · 블록 안 금지 · 같은 카드 · 전체 보기 없음 · 키보드 · 사용자 언어.

**전체 스위트 `python3 tests/` → Ran 1653, OK (skipped=1). 실패 0.**

### 한계

- 화면의 허용목록 사본은 **그리기 규칙**이라 서버와 갈라질 수 있다. 갈라지면 `test_a1` 이 빨개진다 — 그것이 이 사본을 두기로 한 조건이다.
- 절대경로 자르기는 워크스페이스 **이름**만 본다. 같은 이름의 다른 리포 경로가 대답에 섞이면 이 리포의 같은 파일을 연다. 카드 머리에 뜨는 경로는 서버가 정규화해 돌려준 것이라 무엇을 보고 있는지는 어긋나지 않는다.

[Image: assets/REQ-20260828-028-62x6/code-peek-open.png]
[Image: assets/REQ-20260828-028-62x6/code-peek-notfound.png]
[Image: assets/REQ-20260828-028-62x6/code-peek-offline.png]

### 2026-08-28T22:31:42+09:00 subagent (agent: sub:subagent:a57d0206, by nicehugepark)

Attaching captures to REQ-20260828-028-62x6 note

### 2026-08-28T22:32:42+09:00 tdd (by nicehugepark)

### TDD 시나리오 — 화면 몫 (tests/test_code_peek_ui.py, 25개 · 전부 초록)

- [x] C1. 허용 뿌리·파일·확장자 사본이 `bin/s9` 의 원본과 **글자까지 같다**. 갈라지면 이 시험이 먼저 빨개진다
- [x] C2. 경로를 집는 정규식이 그 사본에서 **만들어진다** (손으로 적은 목록 금지)
- [x] C3. 화면이 읽는 이름과 서버가 보내는 이름이 같다 (path·line·from·to·total·lines)
- [x] C4. `lines`·`path` 가 `textContent` 로만 들어간다 — 카드를 세우는 네 함수에 `innerHTML` 0
- [x] C5. 못 연 이유는 상수 하나(`CODE_FAIL`), `ccCodeFail` 에 이유를 가르는 분기 없음
- [x] C6. "권한·비밀·이진·차단…" 같은 갈래 말이 카드 어디에도 없다
- [x] C7. 서버에 못 닿은 것은 **묻지 않는다**(catch 에 `ccCodeBury` 없음) + `다시 받기` 를 준다
- [x] C8. 카드의 모든 상태에 `닫기` 가 있다 — 여는 순간부터
- [x] C9. 열 수 없는 경로는 앵커가 아니라 맨 `.ccval` 로 남는다
- [x] C10. 모양 검사가 서버 `_code_shape_ok` 의 다섯 갈래를 그대로 갖는다
- [x] C11. 한 번 실패한 경로는 기억되고 **떠 있는 밑줄까지 거둔다**(글자는 원문 그대로)
- [x] C12. 리포 뿌리를 모르면 절대경로를 짐작으로 자르지 않는다
- [x] C13. 코드 블록은 자리표시자로 먼저 빠지고 경로 규칙이 그 뒤에 돈다
- [x] C14. 카드·자리·슬롯이 문서 미리보기의 것과 같다 (`ccpeek`·afterend·`ccPeekEl`)
- [x] C15. 칸 이름이 `.pk`/`.pt`/`.pa`/`.pmore` — 새 이름을 만들지 않았다
- [x] C16. 코드 창에 색면·세로 띠 없음 · 긴 줄은 상자 안에서만 흐름 · 줄 번호는 선택에 안 낌
- [x] C17. 코드를 부르는 자리가 하나이고, 줄과 창 없이는 못 부른다 (파일 전체 보기 없음)
- [x] C18. 창 상한이 서버 `CODE_MAX_CTX` 와 같다
- [x] C19. 손잡이가 `role="button"`+tabindex 로 이 화면의 Enter/Space 길에 얹힌다
- [x] C20. 초점 링이 터미널 팔레트(`--cc-text`)를 쓴다 — 종이 톤에서 사라지지 않게
- [x] C21. 화면 문구에 내부 용어(404·API·게이트·라우트…)가 없다
- [x] C22. 진단 손잡이(`?codepeek`)가 **진짜 렌더러**를 통과시킨다 (화면을 속이지 않는다)

서버 몫 S1~S11 은 backend-developer 보고대로 `python3 tests/ code_read` 19개 초록으로 검증돼 있었고, 표시만 안 돼 있어 여기서 체크했다. 방금 전체 스위트(`python3 tests/`) **Ran 1653, OK** 로 다시 확인했다.

### 2026-08-28T22:33:36+09:00 subagent (agent: sub:designer:ab64fee2, by nicehugepark)

`[2026-08-28 22:32:58 KST - designer]`

REQ-20260828-028-62x6 화면 몫 완료. `review` 로 올렸다. 커밋하지 않았고 `bin/s9` 는 읽기만 했다.

### 결론

터미널 대답 속 `web/index.html:4016` 이 손잡이가 되고, 누르면 **그 줄 아래**에 REQ-20260828-021-62x6 이 만든 그 카드(`.ccpeek`)가 펴진다. 새 컴포넌트는 없다 — 같은 카드·같은 자리·같은 슬롯(`ccPeekEl`)을 나눠 쓴다(하나가 열리면 다른 하나가 닫힌다). 칸 쓰임도 같다: `.pk` 무엇인가(문서 id ↔ 파일 경로) / `.pt` 지금 보는 것(제목 ↔ 그 줄) / `.pk` 곁들이(상태 ↔ 보이는 구간·전체 줄 수).

### 판단 — 열리지 않는 경로는 두 겹으로 막았다

**① 미리 아는 것은 미리 거른다.** 허용 뿌리·확장자·점파일·`..` 은 *모양*만으로 판정되므로 화면이 미리 안다. 서버 판정의 **사본**을 화면에 두고, 상대경로 정규식이 그 사본에서 만들어진다. `users/…`·`state/…`·`vault/…`·`.claude/…` 는 아예 정규식에 안 걸려 **밑줄이 서지 않는다**(실브라우저 실측: 앵커 자체가 없음). 이 사본은 게이트가 아니다 — 막는 것은 서버 `code_visible` 하나이고, 여기 글자를 고쳐도 열리는 파일은 한 개도 안 는다. 새로 흘리는 것도 없다(이 목록은 이미 `bin/s9` 안에 있고 그 파일은 이 기능으로 읽힌다). 위험은 갈라짐 하나뿐이라 시험이 두 벌을 글자까지 맞대 본다.

**② 눌러 봐야 아는 것은 한 번만 눌리게 한다.** 실패하면 그 경로를 기억하고 **떠 있는 밑줄까지 거둔다**(글자는 원문 그대로 — 터미널은 통째로 긁어 복사하는 자리다). 실측: 실패 전 `A.ccval.ccpath / underline` → 실패 뒤 `SPAN.ccval / none`, 옆 손잡이는 그대로.

**서버에 못 닿은 것은 여기 해당하지 않는다** — 그건 그 경로가 못 열린다는 뜻이 아니라서 묻지 않고 `다시 받기` 를 준다(`data-retrans`·`data-resupply` 와 같은 어휘). 검증 중 WSL 중계가 끊겨 이 상태가 저절로 재현됐고 밑줄이 유지되는 것을 확인했다.

**절대경로**(`/home/sjpark1/section9/bin/s9:9074`)는 `whoami.workspace` 뒤부터만 읽는다. 그 이름이 없으면 링크를 안 세운다 — 짐작으로 잘라 남의 리포 경로를 이 리포 파일로 열어 보이는 것이 못 여는 것보다 나쁘다. 실측: `/home/other/elsewhere/bin/s9:1` 은 맨 값으로 남는다.

**404 는 한 문구**(상수 `CODE_FAIL`, 분기 없음): `이 파일을 열지 못했습니다 / 지워졌거나, 코드 폴더 밖이거나, 글자로 읽을 수 없는 파일입니다.` 문서 미리보기의 '찾지 못했습니다' 카드와 같은 모양이다.

### 이웃 시험 셋이 걸렸다 — 둘은 코드로 되돌리고 하나만 손봤다

- `test_term_mention::…no_colour_field_and_no_side_bar` → 카드 선택자를 `.ccterm .ccpeek` 단독으로 되돌리고 `.term` 몫은 형제 규칙으로. **시험 안 건드림**
- `test_terminal_table::blocks_run_after_inline_and_before_restore` → 자리표시자 복원을 재귀 대신 되풀이(상한 4)로 바꿔 `ccBlocks()` 뒤에 오게. **시험 안 건드림**
- `test_term_mention::the_link_never_leaks_into_a_code_block` → 백틱 안 `ccDocs(c)` 를 글자 그대로 짚고 있었는데 `ccDocs(ccPathRules(c, keep))` 가 됐다. 지키려던 사실("백틱 몸통에 ccDocs 가 걸린다")을 짚는 정규식으로 바꾸고 이유를 주석에 남겼다.

### 검증

Windows Chrome 을 CDP 로 붙여 **사람이 하듯 클릭**했다(진단 스위치로 세우기만 한 것이 아니다): 열림 25줄→121줄, 토글 접힘, 404 카드+밑줄 거둠+재렌더 시 링크 안 섬, `users/…` 앵커 0, 코드 블록 안 손잡이 0 / 밖 1, 문서 id·첨부·굵게·해시 강조 그대로, 콘솔 에러 0, Tab→Enter 로 열림. 캡처 3장(열림·못 엶·서버 못 닿음)은 REQ 문서에 붙였다.

`python3 tests/` → **Ran 1653, OK (skipped=1)**. 새 시험 `tests/test_code_peek_ui.py` 25개 포함.

### 파일

- `/home/sjpark1/section9/web/index.html`
- `/home/sjpark1/section9/tests/test_code_peek_ui.py` (신규)
- `/home/sjpark1/section9/tests/test_term_mention.py` (단언 한 줄)
- `/home/sjpark1/section9/vault/requests/2026/08/REQ-20260828-028-62x6.md` (노트·상태·TDD 체크)

### 한계

- 화면의 허용목록 사본은 그리기 규칙이라 서버와 갈라질 수 있다. 갈라지면 `test_a1` 이 빨개진다 — 그것이 사본을 두기로 한 조건이다.
- 절대경로 자르기는 워크스페이스 **이름**만 본다. 같은 이름의 다른 리포 경로가 섞이면 이 리포의 같은 파일을 연다. 카드 머리의 경로는 서버가 정규화해 돌려준 것이라 무엇을 보고 있는지는 어긋나지 않는다.
- REQ 문서의 서버 TDD 체크박스 11개는 backend-developer 가 검증해 놓고 표시만 안 한 것이라 여기서 체크했다(`review` 게이트가 막았다). 그 근거는 그들의 response 노트와 방금의 전체 초록이다.

## History
- 2026-08-28T16:52:15+09:00 created by nicehugepark (status: open)
- 2026-08-28T19:02:11+09:00 status: open -> in-progress (by nicehugepark) — security-engineer 에 경계 판정 위임
- 2026-08-28T22:32:55+09:00 status: in-progress -> review (by nicehugepark) — 터미널 대답에 나온 파일 경로를 눌러 그 줄을 그 자리에서 봅니다. Terminal 탭에서 F5 로 새로고침한 뒤 봐 주세요. 1) 대답 속 `web/index.html:4016` 같은 글자에 **점선 밑줄**이 생겼습니다. 누르면 그 줄 바로 아래에 카드가 펴지고 4016번 줄이 밝은 글씨로 나옵니다 — 문서 번호를 눌렀을 때 뜨는 그 카드와 같은 모양이고, 화면은 갈아치우지 않습니다. 2) `users/…/settings.json` · `state/…` · `vault/…` 같은 경로에는 **밑줄이 없습니다.** 눌러도 안 열릴 자리에 밑줄을 세우지 않았습니다. 3) 지워진 파일처럼 밑줄은 있는데 안 열리는 경우가 있습니다. 한 번 누르면 "이 파일을 열지 못했습니다" 가 뜨고 **그 뒤로는 그 경로의 밑줄이 사라집니다** — 두 번 세 번 눌러 보게 만들지 않습니다. 4) 붙여넣기용 코드 상자 안의 경로에는 밑줄이 없습니다 (긁어 복사할 때 링크가 섞이지 않게). 5) 카드의 `+ 앞뒤 더 보기` 로 앞뒤를 더 볼 수 있고, `닫기` 는 여는 순간부터 있습니다. 직접 세워 보시려면 주소 뒤에 `?codepeek=peek`(열리는 경로) 또는 `?codepeek=fail`(안 열리는 경로) 을 붙이고 Terminal 탭으로 가시면 됩니다. 화면 그림 세 장은 바로 위 보고에 붙여 뒀습니다.
