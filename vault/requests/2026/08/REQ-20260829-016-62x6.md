---
id: REQ-20260829-016-62x6
type: request
title: 리드가 붙은 일에 작업자가 또 뜬다
summary: 반려가 무인 작업자를 띄우는데 리드가 이미 그 일을 하고 있어도 막지 않고, 쿨다운마다 다시 뜬다
goal: 리드나 다른 작업자가 이미 붙어 있는 REQ 에 무인 작업자가 겹쳐 뜨지 않고, 도는 워커가 화면에서 멈춤으로 읽히지 않으면 완료
status: done
size: M
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: 81526441
sessions: ["2607e0d6", "81526441"]
agents: ["worker:rework"]
contributions: [{"actor": "worker:rework", "item": "재스폰 원인 규명 + bin/s9 패치 제안", "started": "2026-08-29T16:56:48+09:00", "ended": "2026-08-29T16:56:48+09:00", "result": "done", "transcript": ""}]
project: section9
relates: ["REQ-20260829-013-62x6", "REQ-20260829-021-62x6", "REQ-20260829-032-62x6", "REQ-20260828-041-62x6"]
relates_why: {"REQ-20260829-013-62x6": "둘 다 \"일하는 중\"을 화면과 기계가 못 읽는 문제 — 하나는 사람이 멈춘 줄 알고, 하나는 기계가 멈춘 줄 알고 작업자를 또 띄운다", "REQ-20260829-021-62x6": "같은 구멍의 양면 — 016 은 리드가 붙은 일에 작업자가 뜨는 것, 021 은 뜬 작업자를 리드가 세울 수 없는 것", "REQ-20260829-032-62x6": "032 의 실체가 016 이다 — 겹쳐 뜬 워커의 뿌리가 스폰 판정이 도는 워커를 클레임으로 안 보는 것", "REQ-20260828-041-62x6": "겹쳐 띄우지 않는다는 같은 계약의 양면 — 041 은 사람이 누른 손잡이가, 016 은 워처가 도는 워커 위에 두 번째 손을 붙이는 자리다 (worker_running 을 두 판정이 함께 쓴다)"}
tags: ["workflow", "design", "access"]
created: 2026-08-29T11:54:06+09:00
status_since: 2026-08-29T18:53:19+09:00
updated: 2026-08-29T18:53:19+09:00
priority: 50
---

## Original

2026-08-29 11:02~11:39 실측. 반려 셋에 작업자가 셋 뜨고, 그중 REQ-20260829-011 하나에만 넷이 떴다(11:04·11:16·11:26·11:39). 시간당 상한 6건을 다 써서 11:43 사용자의 깨우기가 막혔다 — 그 한도가 폭주를 끊었다.

원인 둘.
① 리드가 이미 그 건을 잡고 있다는 사실이 스폰 판정에 안 들어간다. 리드는 designer 에게 맡겨 실제로 진행 중이었는데, 문서에는 '아무도 안 붙었다' 로 보였다. s9 claim 이 그 표시를 만드는 자리인데 반려 경로가 그것을 요구하지도, 보지도 않는다.
② 쿨다운(600s)이 지나면 '아직 진전 없음' 으로 다시 띄운다. 진전 판정이 문서의 updated 만 보므로, 사람이나 다른 에이전트가 코드에서 일하는 동안은 늘 정지로 읽힌다.

결과: 워크트리 넷이 같은 문제에 같은 답(테두리를 걷어내고 낱말로 말하기)을 따로 냈다. 셋은 폐기하고 하나는 본 저장소의 designer 작업이 대체했다. 낭비였을 뿐 위험은 없었다 — 워크트리는 격리돼 본 저장소를 못 건드린다.

고칠 자리: 스폰 판정(_auto_cap_block 인접)이 claim 을 보게 하고, 리드가 위임할 때 claim 이 자동으로 서게 한다.

## Notes

### 2026-08-29T16:56:48+09:00 response (agent: worker:rework, by nicehugepark)

### 재작업 보고 — 손대지 않았다. 고칠 자리가 이 세션의 봉투 밖이다

### 판정 먼저

이 요청이 지목한 고칠 자리는 `bin/s9` 한 곳이다. 이 세션(무인 워커)의 봉투는
수정을 `web/`·`vault/`·`tests/` 안으로 한정하고 `bin/`·`harness/`·설정 파일은
**제안(note)으로만** 남기라고 못 박는다. 그래서 적용하지 않고 blocked 로 넘긴다.
아래에 그대로 붙여 넣을 수 있는 패치와 회귀 시험을 다 써 뒀다 — 리드가 붙이면 끝난다.

곁가지 구조 지적 하나: 이 요청은 본문에 고칠 자리를 `bin/s9` 로 적어 두고
무인 워커에게 배달됐다. 그 조합은 언제나 blocked 로 끝난다. 대시보드 드래그 착수가
워커를 띄우는 경로에는 "이 건은 도구 자체를 고쳐야 한다" 는 표식이 없다.
같은 낭비를 또 만들지 않으려면 그 표식(예: `tool` 태그면 무인 스폰 대신 리드 대기)이
필요하다 — 이것도 bin/ 이라 제안으로만 남긴다.

### 무엇이 일어났나 — 원인 ②(쿨다운 재스폰)는 확정이다

- 스폰은 대상 REQ 마커(`state/auto_resume/<id>.json`)에 워커 pid 를 적는다
  (`_auto_mark_pid`). `live_workers()`/`_worker_spawn_targets()` 가 그 pid 로
  "지금 도는 워커 → 어느 REQ" 를 만든다. **방금 실측**: 이 세션에서
  `s9 workers` 가 `REQ-20260829-016-62x6 (pid 5356)`·`REQ-20260829-021-62x6 (pid 5429)`
  를 정확히 짚었다. 사실은 이미 기록되고 있고, 이미 읽히고 있다.
- 그런데 **스폰 판정은 그 사실을 한 번도 묻지 않는다.** `rework_watch_tick` 가 보는
  클레임 판정 `rework_claimed()` 의 축은 둘뿐이다 — 세션 바인딩(`chat_live`)과
  위임 기여(`delegated_live`). 워커 pid 는 그 어느 쪽도 아니다.
- headless 워커는 `chat_live` 가 잘 서지 않는다: 봉투에 Monitor 가 없어 inbox tail
  프로세스가 없고(`_inbox_watch_alive` 실패), `attach_pid` 도 없다. 남는 것은
  transcript mtime 120초 하나뿐이다. 결국 **클레임이 워커의 협조 한 줄
  (`s9 last --add`)에 달려 있다.** 그 한 줄이 거부되거나(옛 봉투는 `S9_SESSION=…`
  환경변수 접두를 allowlist 로 막았다 — 지금 프롬프트가 `--session` 플래그를 따로
  당부하는 이유가 그것이다) 워커가 긴 도구 호출로 조용하면, 도는 워커가 그대로
  "아무도 안 집었다" 로 읽힌다.
- 창의 크기가 우연이 아니다: `SPAWN_WIN = 600` 과 `auto_resume_cooldown_sec = 600`
  이 같다. **화면에서 "기동 중" 앰버가 꺼지는 바로 그 순간 쿨다운이 풀린다.**
  그래서 재스폰이 정확히 10~13분 간격으로 반복된다(실측 11:04·11:16·11:26·11:39).
  앞 워커의 생사와 무관하게.

### 원인 ①(리드 위임)은 대부분 이미 서 있다 — 남은 구멍은 좁다

`PostToolUse(matcher=Agent)` 훅 `bin/s9-audit-agent` 이 위임 순간 `claim` +
`contributions --result running` 을 세운다(REQ-20260825-088-62x6). 그래서 `delegated_live`
가 이미 `rework_claimed` 안에 들어 있다. "리드가 위임할 때 claim 이 자동으로 선다"
는 요구는 새로 만들 것이 아니라 **이미 있는 장치의 구멍을 좁히는 일**이다.

남은 구멍은 `named_target()` 이다. 프롬프트에 살아 있는 REQ 가 **여럿** 지명되면
아무것도 고르지 않고(남의 문서에 running 을 심지 않으려는 의도된 보수성) 세션 클레임
경로로 물러난다. 리드가 그 문서를 클레임하지 않은 채(대시보드 드래그 착수는 리드의
클레임을 만들지 않는다 — 전이를 한 것은 서버다) 관련 REQ 를 배경으로 여럿 적어
위임하면 → `위임 등록 보류: 대상 REQ 없음` → 화면에도 워처에도 아무도 안 붙은 것으로
보인다. 이번 실사고의 리드-designer 조합이 그 모양이다.

좁히는 방법(제안): 지명이 여럿이라 물러나기 전에 **`description` 만으로 한 번 더
좁혀 본다.** 배경 id 는 대개 prompt 본문에 실리고 대상 id 는 description 에 적힌다 —
`named_reqs()` 가 이미 그 우선순위를 알고 있는데 합쳐서 세느라 잃고 있다.

### 고칠 방법 — 붙여 넣을 것 (bin/s9)

### 패치 1. 도는 워커도 클레임이다

`delegated_live()` 바로 아래(= `rework_claimed()` 바로 위)에 추가:

```python
WORKER_WIN = 3600       # 도는 워커를 클레임으로 인정하는 한도 (위임과 같은 규모)


def worker_running(req_id, win=WORKER_WIN, now=None):
    """이 REQ 를 맡아 **지금 도는** 무인 워커 — 있으면 {"pid","age"}, 없으면 None.

    실사고 2026-08-29 11:04~11:39 (REQ-20260829-016-62x6): 반려 한 건에 워커가 넷 떴다.
    간격은 쿨다운 그대로였다. 워크트리 넷이 같은 문제에 같은 답을 따로 냈고,
    시간당 상한 6건을 다 써서 11:43 사람의 깨우기가 막혔다.

    원인은 클레임의 눈이다. `rework_claimed` 는 세션 바인딩과 위임 기여만 본다.
    스폰된 워커는 headless 라 inbox tail 도 attach_pid 도 없어 `chat_live` 가
    서지 않는다 — 클레임이 워커의 협조 한 줄(`s9 last --add`)에 달려 있고,
    그 한 줄이 실패한 워커는 쿨다운마다 자기 위에 자기를 다시 띄운다.

    그런데 그 사실은 **이미 기록되고 있었다**: 스폰이 `_auto_mark_pid` 로 대상
    REQ 마커에 pid 를 적고 `live_workers` 가 그것으로 목록을 만든다. 새 표를
    만들지 않고 이미 있는 사실을 판정에 꽂는다. pid 재사용 방어는
    `_worker_alive`(=`_pid_is_claude`) 가 이미 한다.

    **한도를 둔다** (`delegated_running` 과 같은 이유): 워커가 멎은 채 프로세스만
    남으면 그 클레임이 영원해져 멈춘 작업을 조용히 감춘다. 한 시간이 지나면
    다시 스폰 대상이다.
    """
    import time as _t
    now = _t.time() if now is None else now
    d = _auto_dir()
    pf = os.path.join(d, safe_name(req_id) + ".json")
    if not os.path.exists(pf):      # 짧은 id 로 물어 온 경우만 정식 id 로 되짚는다
        cid = canon_id(req_id)
        if cid != req_id:
            pf = os.path.join(d, safe_name(cid) + ".json")
    try:
        with open(pf, encoding="utf-8") as f:
            m = json.load(f)
    except (OSError, ValueError):
        return None
    age = now - float(m.get("last") or 0)
    if not 0 <= age < win:
        return None
    return {"pid": m.get("pid"), "age": int(age)} \
        if _worker_alive(m.get("pid")) else None
```

`rework_claimed()` 안, `delegated_live` 검사 **바로 다음**에:

```python
    # 스폰된 무인 워커도 클레임이다 (REQ-20260829-016-62x6). 워커의 협조 한 줄에
    # 기대지 않는다 — 프로세스가 도는 것이 더 단단한 증거다.
    if worker_running(req_id):
        return True
```

이 두 조각이 실사고의 재스폰 셋(11:16·11:26·11:39)을 전부 막는다.

### 패치 2 (선택). 화면도 같은 눈으로 본다

`catalog_with_live()` 의 `spawned` 판정은 `SPAWN_WIN`(600초) 신선도만 본다. 그래서
10분 넘게 도는 워커는 화면에서 "기동 중" 이 꺼지고 `stall_mins` 가 그 행을
"멈췄다" 로 부른다 — 도는 워커를 사람이 멈춘 것으로 읽는다(REQ-20260829-013-62x6 과
같은 축의 문제다). 위 `worker_running()` 이 True 면 `live_kind="spawned"` 를
유지하도록 그 자리도 함께 고치는 것을 권한다.

한 몸으로 묶지 않고 선택으로 남기는 이유: 이쪽은 **멈춤 표시를 감추는 방향**이라,
멎은 워커를 한 시간 동안 못 보게 만들 위험이 있다. `WORKER_WIN` 한도가 그 위험의
상한이지만, 판단은 화면을 보는 사람 몫이다.

### 회귀 시험 (tests/test_worker_claim.py) — 패치와 **함께** 넣어야 초록이다

지금 넣으면 빨강이라 이 세션에서는 커밋하지 않았다. 패치와 한 커밋에 함께 넣어라.
실행: `python3 tests/ worker_claim`

```python
"""도는 워커는 그 자체로 클레임이다 (REQ-20260829-016-62x6).

실사고 2026-08-29 11:04~11:39: 반려 한 건에 워커가 넷 떴다(간격은 쿨다운 그대로).
워크트리 넷이 같은 답을 따로 냈고 시간당 상한 6건을 다 써서 사람의 깨우기가 막혔다.
스폰 판정이 보는 클레임에 "이 REQ 를 맡아 지금 도는 워커" 축이 없었기 때문이다.

격리: S9_ROOT=mktemp. 실행: python3 tests/ worker_claim
"""
import json
import os
import shutil
import subprocess
import tempfile
import time
import unittest

HERE = os.path.dirname(os.path.abspath(__file__))
S9 = os.path.join(HERE, "..", "bin", "s9")


def s9mod(root):
    """bin/s9 를 모듈로 적재해 판정 함수를 직접 부른다(서브프로세스보다 정확)."""
    import importlib.util
    import importlib.machinery
    os.environ["S9_ROOT"] = root
    spec = importlib.util.spec_from_loader(
        "s9mod_" + os.path.basename(root),
        importlib.machinery.SourceFileLoader(
            "s9mod_" + os.path.basename(root), S9))
    m = importlib.util.module_from_spec(spec)
    spec.loader.exec_module(m)
    return m


class WorkerClaim(unittest.TestCase):
    RID = "REQ-20260829-016-62x6"

    def setUp(self):
        self.tmp = tempfile.mkdtemp(prefix="s9wclaim-")
        env = {**os.environ, "S9_ROOT": self.tmp, "S9_MACHINE": "testbox",
               "S9_USER": "tester"}
        env.pop("S9_SESSION", None)
        subprocess.run([S9, "init"], capture_output=True, text=True,
                       env=env, timeout=30, stdin=subprocess.DEVNULL)
        self.m = s9mod(self.tmp)
        self.m.ROOT = self.tmp
        self.m.STATE = os.path.join(self.tmp, "state", "sessions")
        os.makedirs(self.m.STATE, exist_ok=True)
        os.makedirs(os.path.join(self.tmp, "state", "auto_resume"),
                    exist_ok=True)
        # 살아 있는 'claude' 프로세스 한 벌 — _pid_is_claude 는 comm 을 본다.
        self.fake = os.path.join(self.tmp, "claude")
        os.symlink(shutil.which("sleep") or "/bin/sleep", self.fake)
        self.proc = subprocess.Popen(
            [self.fake, "60"], stdin=subprocess.DEVNULL,
            stdout=subprocess.DEVNULL, stderr=subprocess.DEVNULL)
        for _ in range(50):          # comm 이 exec 뒤에 바뀐다 — 잠깐 기다린다
            if self.m._pid_is_claude(self.proc.pid):
                break
            time.sleep(0.02)

    def tearDown(self):
        try:
            self.proc.kill()
            self.proc.wait(timeout=5)
        except Exception:
            pass
        shutil.rmtree(self.tmp, ignore_errors=True)

    def marker(self, pid, last=None):
        p = os.path.join(self.tmp, "state", "auto_resume",
                         self.m.safe_name(self.RID) + ".json")
        with open(p, "w") as f:
            json.dump({"pid": pid, "last": last if last is not None
                       else time.time(), "count": 1}, f)

    def test_running_worker_holds_the_claim(self):
        """도는 워커가 있으면 워처는 손대지 않는다 — 겹침 스폰 차단."""
        self.assertFalse(self.m.rework_claimed(self.RID),
                         "마커가 없으면 클레임도 없다")
        self.marker(self.proc.pid)
        self.assertTrue(self.m.rework_claimed(self.RID),
                        "도는 워커가 있는데 워처가 하나 더 띄운다")

    def test_dead_worker_releases_the_claim(self):
        """즉사한 워커의 마커는 클레임이 아니다 — 아무도 안 붙은 것이 사실이다."""
        self.proc.kill()
        self.proc.wait(timeout=5)
        self.marker(self.proc.pid)
        self.assertFalse(self.m.rework_claimed(self.RID))

    def test_hung_worker_expires(self):
        """멎은 채 프로세스만 남은 워커는 한도가 지나면 클레임을 놓는다."""
        self.marker(self.proc.pid, last=time.time() - self.m.WORKER_WIN - 60)
        self.assertFalse(self.m.rework_claimed(self.RID))


if __name__ == "__main__":
    unittest.main()
```

### 검증

- **적용하지 않았으므로 돌릴 대상이 없다.** 이 세션의 봉투는 `python3 tests/` 만
  허용하는데, 그 시험이 읽는 것은 `bin/s9` 다 — 고치지 못하는 파일에 대한 시험은
  빨강밖에 나오지 않는다. 그래서 시험도 커밋하지 않고 여기 본문에 둔다.
- 손으로 확인한 것: `s9 workers` 가 지금 도는 워커 둘의 pid→REQ 매핑을 정확히
  낸다(위 실측). 그 사실이 `rework_claimed` 로 전달되는 경로가 코드에 없다는 것은
  본문 대조로 확인했다.
- 이 워크트리에 커밋은 없다(코드 변경 0). 문서 기록만 본 저장소에 남는다.

### 2026-08-29T18:23:39+09:00 response (by nicehugepark)

### 패치 적용 완료 (backend-developer · 리드 세션 위임)

blocked 사유는 봉투뿐이었다 — 이 자리는 그 밖이라 열린다. 노트의 패치를 **베끼지 않고** 지금 코드에 맞춰 확인해 넣었다. 전체 스위트 `python3 tests/` **2026건 OK(skipped=2)**.

### 패치 1 (적용) — 도는 워커도 클레임이다

`bin/s9`: `delegated_live()` 아래에 `WORKER_WIN=3600` + `worker_running(req_id, win, now, alias=None)`, `rework_claimed()` 의 `delegated_live` 검사 **다음**에 두 줄. 노트 원안에서 바꾼 것 둘:

- `alias` 인자를 더했다. 원안은 마커가 없을 때마다 `canon_id()`→`id_alias()`→`load_catalog()` 로 카탈로그를 **한 벌 더** 읽는다. `rework_claimed` 은 어차피 alias 를 만들므로 그것을 앞으로 올려 넘긴다 — 워처가 30초마다 도는 자리라 같은 파일을 두 번 읽을 이유가 없다.
- `float(m.get("last"))` 를 try 로 감쌌다. 판정이 예외를 올리면 워처 스레드가 죽고 **진행 보장이 통째로 사라진다** — 이 저장소가 judge_health 에서 이미 세운 규율이다.

시험 `tests/test_worker_claim.py` 6건(원안 3 + 3). 더한 셋: 근거(pid·age)를 함께 내는가 · pid 없는 구 마커를 클레임으로 치지 않는가(치면 하루 종일 아무도 못 이어받는다) · 깨진 마커에 예외를 올리지 않는가. pid 는 지어내지 않고 comm=claude 인 진짜 프로세스를 띄워 쓴다 — 이 판정의 핵심이 pid 재사용 방어라 그걸 건너뛴 시험은 고장을 못 잡는다. `python3 tests/ worker_claim` → 6건 OK (적용 전에는 5 error + 1 fail 로 빨강임을 먼저 확인했다).

### 패치 2 (적용) — 근거

적용했다. **041 라운드2의 핵심과 같은 자리**여서다: `SPAWN_WIN`(600초)과 쿨다운(600초)이 같으므로, 화면에서 앰버가 꺼지는 바로 그 순간 쿨다운이 풀린다. 그 사이 카드는 '멈춤'으로 그려지고 **사람이 누르는 깨우기가 곧 중복 스폰**이 된다 — 실사고의 재스폰 간격(11:04·11:16·11:26·11:39)이 정확히 그 값이다. 패치 1은 워처만 막고 사람 손잡이는 그대로 열어 두므로, 둘을 함께 넣어야 '겹쳐 띄우지 않는다'가 한 벌이 된다.

노트가 경계한 위험(멎은 워커를 한 시간 감춘다)은 조건 두 개로 막는다: ① `spawned` 를 유지하는 것은 `_worker_alive`(=`_pid_is_claude`)가 **참일 때만**이다 — pid 가 죽으면 종전대로 `spawn_failed` 로 떨어지고, 그것도 SPAWN_WIN 안에서만이다. ② 살아 있어도 `WORKER_WIN`(1시간)이 상한이다. 즉 감춰지는 것은 '진짜 도는 프로세스' 뿐이고, 그건 감추는 게 아니라 사실이다.

곁들여 고친 것: `stall_mins` 는 `spawn_failed` 행의 잣대를 180초로 좁히는데 그 근거 시각을 기여 요약(`agent_state.at`)에서만 읽었다. 마커에서 온 spawn_failed 에는 그 요약이 없어 15분 잣대가 그대로 남았다 — 사람이 깨웠는데 워커가 즉사하면 카드는 점만 빨갛게 켠 채 15분간 다시 누를 것을 주지 않았다. 없으면 `live_age`(스폰 나이)로 대신한다.

### 041 과 어긋나지 않는가 — 한 벌로 맞췄다

라운드1이 고친 `judge_health`(gone·claim_age)·`delegated_running`(actor 생존 확인)과 겹치지 않는다. 축이 다르다: 저쪽은 **문서에 적힌 기여**를, 이쪽은 **프로세스 마커**를 본다. 셋 다 `rework_claimed` 한 함수에 모여 있고 각자 자기 한도(DELEGATE_WIN·UNKNOWN_GRACE·WORKER_WIN = 모두 3600초)를 갖는다 — 판정이 두 벌로 갈릴 자리는 만들지 않았다.

### 정직하게 적어 둘 것

이 REQ 를 지목한 오늘 17:36 의 겹침 스폰은 **워처가 아니라 내 검증 탐침**이 낸 것이다(reason=wake). `_spawn_worker` 안의 지역 `import subprocess` 가 내가 모듈 수준에 심은 스텁을 가려 진짜 Popen 이 불렸다. 세션 0755c082 가 실제로 떴고, 파일은 하나도 고치지 않은 채 겹침을 보고하고 물러났다(그 세션의 보고가 이 패치를 지시하게 만든 그 통지다). 원인은 내 탐침이지만 **드러난 결함은 진짜다**: 그때 서브에이전트 둘이 실제로 돌고 있었고, 스폰 판정에는 그것을 볼 눈이 없었다. 이후 검증은 `unittest.mock.patch("subprocess.Popen")` 로 바꿨다 — 지역 import 도 호출 시점에 속성을 찾으므로 그쪽은 확실히 가로챈다.

파일: /home/sjpark1/section9/bin/s9 · /home/sjpark1/section9/tests/test_worker_claim.py(신규 6건)

## History
- 2026-08-29T11:54:06+09:00 created by nicehugepark (status: open)
- 2026-08-29T16:47:33+09:00 status: open -> in-progress (by nicehugepark) — drag 이동 [via dashboard]
- 2026-08-29T16:56:54+09:00 status: in-progress -> blocked (by nicehugepark) — 고칠 자리가 bin/s9 한 곳인데 이 무인 세션의 봉투가 bin/ 수정을 금한다(web·vault·tests만 허용). 원인은 확정했다 — rework_claimed 가 "이 REQ 를 맡아 지금 도는 워커 pid" 를 안 본다. 붙여 넣을 패치와 회귀 시험을 response 노트에 다 써 뒀다. 여는 방법 둘: ① 리드가 노트의 패치 1(worker_running + rework_claimed 두 줄)과 tests/test_worker_claim.py 를 한 커밋에 넣는다 ② bin/ 을 허용하는 자리(리드 세션)에서 이 건을 다시 집는다.
- 2026-08-29T17:42:27+09:00 status: blocked -> in-progress (by nicehugepark) — 봉투 밖이라 막혔던 자리를 리드 세션에서 연다 — bin/s9 를 잡고 있는 backend-developer 에게 패치 1(worker_running + rework_claimed)과 tests/test_worker_claim.py 를 한 몸으로 적용시킨다. 041 라운드2 의 "겹쳐 띄우지 않는다" 계약과 같은 자리다.
- 2026-08-29T18:53:19+09:00 status: in-progress -> done (by nicehugepark) — 목표 대비: 스폰 판정(rework_claimed)이 이제 "이 REQ 를 맡아 지금 도는 워커 pid" 를 클레임으로 인정한다(worker_running + WORKER_WIN 1시간 상한) — 겹쳐 뜨지 않는다. 화면도 같은 눈으로 본다: SPAWN_WIN(600초)과 쿨다운(600초)이 같아 앰버가 꺼지는 순간 카드가 멈춤이 되고 사람이 누르는 깨우기가 곧 중복 스폰이던 자리를, 프로세스가 실제로 도는 동안 spawned 를 유지하도록 고쳤다. 회귀 tests/test_worker_claim.py 6건 신규 green, 전체 2051건 OK. 기계로 확인되는 계약이라 리드가 닫는다.
