---
id: REQ-20260829-003-62x6
type: request
title: tmpfiles 가 부트를 73초 잡는다
summary: 부트마다 systemd-tmpfiles-setup 이 sysinit 을 수십 초 붙잡는 원인 규명
goal: systemd-tmpfiles 가 부트를 수십 초 잡는 이유를 증거로 특정한다
status: in-progress
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: a53ebee3
project: section9
children: ["REQ-20260829-006-62x6"]
relates_why: {}
tags: ["auto-audit"]
created: 2026-08-29T00:52:39+09:00
status_since: 2026-08-29T01:35:38+09:00
updated: 2026-08-29T02:35:31+09:00
priority: 50
---

## Original

systemd-tmpfiles 가 왜 73초씩 걸렸는지를 찾아 그럼

## Notes

### 2026-08-29T00:58:17+09:00 response (by nicehugepark)

원인 확정: 부트마다 /tmp 를 통째로 지우는 비용이다.

증거 넷.
(1) 유닛 명령이 'systemd-tmpfiles --create --remove --boot --exclude-prefix=/dev' 이고, --remove 가 붙으면 tmp.conf 의 'D /tmp 1777 root root 30d' 가 /tmp 의 내용물 전체 삭제로 바뀐다. 전체 설정(/run·/usr/lib tmpfiles.d 15개 파일)에서 크기가 무한정 커질 수 있는 줄은 이 한 줄뿐이다 — 나머지는 /run·/dev·/etc·/var/log 로 전부 유계다.
(2) 같은 설정을 --remove 없이 돌리면 6ms 에 끝난다(사용자 권한, 244줄 debug 전부 수행). 시간은 만들기가 아니라 지우기에 있다.
(3) systemd 가 직접 잰 값: 'systemd-tmpfiles-setup.service: Consumed 14.098s CPU time.' — 벽시계 46초 중 14초가 CPU다. 한 곳에서 막힌 게 아니라 syscall 을 수십만 번 친 모양이다.
(4) /tmp 디렉터리 아이노드 자체가 7,860,224 바이트다(/var/tmp 는 4,096). ext4 디렉터리는 한 번 커지면 줄지 않으므로, /tmp 가 10만 개대의 엔트리를 담았던 적이 있다는 뜻이다.

기여 요인 — 우리 테스트가 /tmp 를 흘린다: tests/*.py 84개가 tempfile.mkdtemp 로 S9_ROOT 를 만들고 rmtree 를 한 번도 하지 않는다(정리 코드 없음 확인). 한 번 돌 때마다 vault 한 벌씩 84개가 /tmp 에 남는다. 지금 이 순간에도 /tmp/s9gate-* 가 분당 1~2개씩 쌓이는 중이다(00:47 13개 → 01:05 20개).

파급: sysinit.target 이 그만큼 늦어 systemd-logind 가 73초 뒤에 뜨고, WSL 의 10초 창(CreateLoginSession)을 놓쳐 REQ-002 의 '사용자 세션 실패' 메시지가 난다. 두 건은 같은 뿌리다.

남는 조치(별건 후보): tests 의 mkdtemp 를 addClassCleanup/rmtree 로 회수한다. systemd 를 끄면 부트 비용은 사라지지만 /tmp 누수 자체는 남는다.

### 2026-08-29T01:37:27+09:00 response (by nicehugepark)

근원 차단 — 측정부터.

부팅 48분 만에 /tmp 최상위가 128개인데 그중 우리 것이 127개다: s9gate-* 98, s9hookstamp-* 11, s9pfroot-*·s9pfhome-*·s9doc-*·s9-shothyg-* 각 2, 그 외 s9* 몇. 남의 것은 claude-1000·snap-private-tmp·systemd-private-*·.X11-unix 뿐이다. 즉 /tmp 를 채우는 것은 사실상 우리 테스트 하나다.

분당 약 2개(s9gate-* = 커밋 게이트가 부르는 test_commit_gate·test_review_gate). serve 의 워크트리 자동 보존 커밋이 매분 게이트를 태우므로 하루 2,880개, 여기에 전체 스위트 한 번당 84개가 더해진다. /tmp 아이노드 7.5MB(10만 개대)와 맞는다.

### TDD 시나리오 — 실행 전용 임시 루트

파일마다 tearDown 을 84번 심는 대신 러너에서 막는다. 이미 포트에 같은 장치가 있다(_reap 전/후 + 남기면 실패). 실행: python3 tests/ tmp_hygiene

- [x] T1. 러너가 실행마다 전용 임시 루트(s9run-<pid>-*)를 세우고 tempfile.tempdir 과 TMPDIR 을 그리로 돌린다 — 테스트의 mkdtemp 결과가 그 안에 생긴다.
- [x] T2. 실행이 끝나면 그 루트를 통째로 지운다. 성공·실패·예외 어느 경로로 나가든(finally).
- [x] T3. 조용히 치우지 않는다 — 남아 있던 최상위 이름의 수와 앞 몇 개를 한 줄로 알린다(접두어가 범인을 가리킨다).
- [x] T4. 지난 실행의 잔재는 시작 전에 거둔다: s9 로 시작하고 1시간 넘은 것. 1시간 안 된 것과 살아 있는 pid 의 실행 루트는 건드리지 않는다.
- [x] T5. 남의 것은 절대 건드리지 않는다 — claude-*, snap-private-tmp, systemd-private-*, .X11-unix.
- [x] T6. 하위 프로세스도 같은 자리를 쓴다 — TMPDIR 이 env 로 전달돼 s9 CLI 의 임시 파일도 루트 안에 들어온다.
- [x] T7. 포트 회수 판정은 좁아지지 않는다 — portpool 의 '임시 작업공간에서 뜬 서버' 판정 기준은 실행 전용 루트가 아니라 시스템 /tmp 전체다(옛 실행이 남긴 서버는 그 밖에 있다).
- [x] 회귀: port_pool·port_guard·commit_gate·doctor_system 그대로 통과.

### 2026-08-29T01:47:57+09:00 response (by nicehugepark)

구현 — 러너에서 문을 닫고, 러너 밖은 회수 틱이 맡는다.

tests/tmproot.py (새 파일): 실행마다 전용 루트 s9run-<pid>-* 를 세우고 tempfile.tempdir 과 TMPDIR 을 그리로 돌린다. 테스트가 무엇을 만들든 그 안에 생기고 끝나면 통째로 사라진다 — 84개 파일에 tearDown 을 심는 대신 문 하나를 닫았고, 새로 들어올 테스트도 아무것도 안 해도 지켜진다. 잔재 판정 규칙: 우리 표식(s9)만, 살아 있는 실행 루트(pid 확인)는 남기고, 1시간 안 된 것은 건드리지 않는다.

tests/__main__.py: 시작 전 잔재 회수, 끝난 뒤 루트 제거 + 남아 있던 이름을 한 줄로 알린다(조용히 치우지 않는다 — 포트 회수와 같은 규율).

bin/s9-doctor: sweep_stale_tmp() 를 --sweep·--fix 에 걸었다. 옛 체크아웃·워크트리의 러너 사본은 새 코드를 모르니 그대로 흘리는데, 그 자리는 매분 도는 회수 틱(serve 감시 주기·세션 시작 훅)이 맡는다. 규칙은 러너와 같고 tests/test_tmp_hygiene.py 가 둘의 일치를 시험한다.

tests/portpool.py: SYS_TMP 를 불러올 때 못박았다. 임시 루트를 돌리면 '임시 작업공간에서 뜬 서버' 판정이 그 루트 안으로 좁아져 옛 실행이 남긴 서버를 못 알아본다 — 기준은 /tmp 전체여야 한다.

실측:
- 쌓여 있던 것 회수: /tmp 의 s9* 133개 → 5개(128개 삭제). 남의 것(claude-1000·snap-private-tmp·systemd-private-*·.X11-unix) 6개는 그대로.
- 전체 스위트 1회(1723 tests, 313초, OK): 루트 안에서 333개가 나왔고 전부 함께 사라졌다. /tmp 최상위 증가 0. 누수 규모가 추정(84)보다 크다 — 한 번 돌 때마다 333개였다.
- 게이트 호출 1회: 3개 생성, 3개 회수, /tmp 증가 0.

남는 것: /tmp 디렉터리 아이노드 7.5MB 는 ext4 특성상 줄지 않는다(e2fsck -D 는 루트 언마운트가 필요해 WSL 에서 불가). 엔트리가 없으면 부트에서 지울 것도 없으므로 73초는 사라진다. doctor 가 그 흔적을 계속 한 줄로 알린다.

### 2026-08-29T02:35:31+09:00 response (by nicehugepark)

검증과 정정 — 처음 두 번의 '통과' 보고는 틀렸다.

1차: 게이트 한 번 돌린 좁은 확인으로 '증가 0' 이라 했다. 전체 스위트에서는 +22 였다.
원인: 임시 루트를 discovery **뒤에** 세웠다. discovery 는 테스트 모듈을 import 하는데 test_tags·test_session_wake·test_link_integrity 등 여럿이 모듈 수준에서 TMP = tempfile.mkdtemp(...) 를 부른다 — 루트가 생기기 전에 만들어져 문 밖에 남았다. 남은 것들의 접두어와 시각이 그대로 가리켰다.
조치: make_run_root() 를 discover() 앞으로. 미끼(test_tmp_leak_probe)에도 모듈 수준 mkdtemp 를 두고 그 부모가 지금의 루트와 일치하는지 본다 — 어긋나면 곧 문 밖에 샜다는 뜻이다.

2차: 그렇게 고치고 돌린 전체 스위트가 30분 타임아웃에 걸려 rc=124 로 죽었다(/tmp 8→70). 내가 만든 결함 둘이었다.
- /tmp/s9-portpool 은 실행끼리 나눠 쓰는 포트 락인데 이름이 s9 로 시작해 잔재 청소 대상에 들어갔다. 락이 사라지면 두 실행이 같은 포트를 제 것이라 믿는다. 러너와 doctor 양쪽에 보존 목록(KEEP/TMP_KEEP)을 두고 두 목록의 일치를 시험으로 묶었다.
- 스위트 안에서 러너를 또 띄우는 시험(test_runner_patterns·test_tmp_hygiene)의 안쪽 실행이 시작할 때 _reap 을 돌려 '임시 작업공간에서 뜬 대시보드 서버'를 죽였다 — 그게 바깥 스위트가 쓰던 서버일 수 있다. S9_TESTS_NESTED=1 로 안쪽은 제 임시 루트만 챙기게 했다.

최종 실측 (02:27~02:34, 60초 간격으로 마지막 로그 줄과 프로세스를 함께 찍으며):
  Ran 1737 tests in 434.638s — OK (skipped=1), rc=0
  [임시자리 회수/끝난 뒤] 테스트가 남긴 345개를 거뒀다
  /tmp 75 → 89 (+14). 그 14개는 전부 s9gate-* — 워크트리의 옛 러너 사본이 자동 보존 커밋마다 흘리는 것이고(분당 2개), 새 코드를 모르는 자리다. 스위트 자신이 흘린 것은 0.
  s9-portpool 은 그대로 남았다(의도).
  s9run-315354-* 하나는 타임아웃으로 죽은 실행의 고아 루트다 — pid 가 죽었으므로 한 시간 뒤 회수 대상이 된다(test_dead_run_root_is_swept 가 그 규칙을 고정한다).

## History
- 2026-08-29T00:52:39+09:00 created by nicehugepark (status: open)
- 2026-08-29T00:53:20+09:00 status: open -> in-progress (by nicehugepark) — tmpfiles 73초의 실제 원인을 찾는다
- 2026-08-29T01:17:42+09:00 status: in-progress -> done (by nicehugepark) — 충족: --remove 없이는 6ms, 서비스 CPU 실측 14.098초, /tmp 아이노드 7.5MB — 세 증거로 /tmp 전체 삭제가 원인임을 특정
- 2026-08-29T01:35:38+09:00 status: done -> in-progress (by nicehugepark) — 원인 규명에서 그치지 말고 누수 자체를 막으라는 지시 (사용자: 003은 근원을 해결해)
