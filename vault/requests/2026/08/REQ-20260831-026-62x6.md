---
id: REQ-20260831-026-62x6
type: request
title: 프로젝트 관리 화면
summary: 프로젝트 개념이 시스템에 있으나 화면에 노출되지 않음 — 신규 프로젝트 추가·멤버 관리·프로젝트별 설정·관리 정보를 대시보드에서 처리하는 기능을 10개 역할 협업으로 설계·구현한다
goal: 대시보드에서 프로젝트 목록·상세를 보고, 새 프로젝트를 만들고, 멤버를 넣고 빼고, 프로젝트 설정을 고칠 수 있는 것이 실브라우저로 확인된다
status: in-progress
size: L
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: 3ed7d9fd
sessions: ["38ad98c8", "a8186437", "3ed7d9fd"]
agents: ["sub:product-owner:a6ecbeb6", "sub:architect:a534bae1", "sub:designer:a9eb556d", "sub:project-manager:a5028790", "sub:architect", "sub:product-owner", "sub:project-manager", "sub:designer", "lead:claude-fable-5", "sub:subagent:acb62273", "sub:frontend-developer:a28941ed"]
contributions: [{"actor": "sub:product-owner:a6ecbeb6", "item": "026 프로젝트 관리 요구 정의", "started": "2026-08-31T22:43:27+09:00", "ended": "2026-08-31T22:46:27+09:00", "result": "done", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/38ad98c8-51fd-4b50-a26a-495b58404817/tasks/a6ecbeb6edf814fd1.output"}, {"actor": "sub:architect:a534bae1", "item": "026 프로젝트 데이터·API 설계", "started": "2026-08-31T22:43:39+09:00", "ended": "2026-08-31T22:46:18+09:00", "result": "done", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/38ad98c8-51fd-4b50-a26a-495b58404817/tasks/a534bae1d8aae23bd.output"}, {"actor": "sub:designer:a9eb556d", "item": "026 프로젝트 화면 구조 설계", "started": "2026-08-31T22:43:52+09:00", "ended": "2026-08-31T22:51:33+09:00", "result": "done", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/38ad98c8-51fd-4b50-a26a-495b58404817/tasks/a9eb556d76ecae5db.output"}, {"actor": "sub:project-manager:a5028790", "item": "026 작업 분해·순서", "started": "2026-08-31T22:44:01+09:00", "ended": "2026-08-31T22:47:27+09:00", "result": "done", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/38ad98c8-51fd-4b50-a26a-495b58404817/tasks/a5028790f9c013d70.output"}, {"actor": "sub:architect", "item": "[architect 설계 — 프로젝트 관리 데이터·API]", "started": "2026-08-31T22:45:59+09:00", "ended": "2026-08-31T22:45:59+09:00", "result": "done", "transcript": ""}, {"actor": "sub:product-owner", "item": "[PO 요구·범위 정의 — 실물 조사 기반]", "started": "2026-08-31T22:46:10+09:00", "ended": "2026-08-31T22:46:10+09:00", "result": "done", "transcript": ""}, {"actor": "sub:architect:a534bae1", "item": "`[2026-08-31 22:49:52 KST - architect]`", "started": "2026-08-31T22:46:18+09:00", "ended": "2026-08-31T22:46:18+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/38ad98c8-51fd-4b50-a26a-495b58404817/subagents/agent-a534bae1d8aae23bd.jsonl"}, {"actor": "sub:product-owner:a6ecbeb6", "item": "`[2026-08-31 22:49:12 KST - product-owner]`", "started": "2026-08-31T22:46:27+09:00", "ended": "2026-08-31T22:46:27+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/38ad98c8-51fd-4b50-a26a-495b58404817/subagents/agent-a6ecbeb6edf814fd1.jsonl"}, {"actor": "sub:project-manager", "item": "[PM 실행 계획 — 2단계 구현 분해·순서·리스크·게이트]", "started": "2026-08-31T22:47:08+09:00", "ended": "2026-08-31T22:47:08+09:00", "result": "done", "transcript": ""}, {"actor": "sub:project-manager:a5028790", "item": "`[2026-08-31 22:48:20 KST - project-manager]`", "started": "2026-08-31T22:47:27+09:00", "ended": "2026-08-31T22:47:27+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/38ad98c8-51fd-4b50-a26a-495b58404817/subagents/agent-a5028790f9c013d70.jsonl"}, {"actor": "sub:designer", "item": "결론 — 프로젝트의 집은 문서다. 새 탭도 새 화면도 만들지 않는다", "started": "2026-08-31T22:50:55+09:00", "ended": "2026-08-31T22:50:55+09:00", "result": "done", "transcript": ""}, {"actor": "sub:designer:a9eb556d", "item": "`[2026-08-31 23:05:12 KST - designer]`", "started": "2026-08-31T22:51:33+09:00", "ended": "2026-08-31T22:51:33+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/38ad98c8-51fd-4b50-a26a-495b58404817/subagents/agent-a9eb556d76ecae5db.jsonl"}, {"actor": "lead:claude-fable-5", "item": "G0 종합(4인 검토 확정): 방향 = 노출 부재 해소, 새 탭 0·새 화면 0(designer C안). ① 목록·생성 = D", "started": "2026-08-31T22:52:14+09:00", "ended": "2026-08-31T22:52:14+09:00", "result": "done", "transcript": ""}, {"actor": "sub:subagent:acb62273", "item": "Reading REQ-20260831-026-62x6.md notes", "started": "2026-08-31T22:54:22+09:00", "ended": "2026-08-31T22:54:22+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/38ad98c8-51fd-4b50-a26a-495b58404817/subagents/agent-acb622736f1420192.jsonl"}, {"actor": "sub:frontend-developer:a28941ed", "item": "026 Projects 탭 구현 이어받기", "started": "2026-09-01T11:34:31+09:00", "ended": "2026-09-01T11:39:46+09:00", "result": "stalled", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/38ad98c8-51fd-4b50-a26a-495b58404817/tasks/a28941edb90628aed.output", "reason": "195초째 진전 없음 (기준 180초)"}, {"actor": "lead:claude-fable-5", "item": "리드 조정(a8186437 이어받음) — ① B트랙 마감: 028·030 done(QA 12항 PASS·전체 회귀 exit 0", "started": "2026-09-01T11:35:28+09:00", "ended": "2026-09-01T11:35:28+09:00", "result": "done", "transcript": ""}, {"actor": "lead:claude-fable-5", "item": "TDD 시나리오 (G0′ Projects 탭 — tests/test_project_tab.py 20건, 전체 스위트 exit ", "started": "2026-09-01T13:00:36+09:00", "ended": "2026-09-01T13:00:36+09:00", "result": "done", "transcript": ""}]
project: section9
children: ["REQ-20260831-027-62x6", "REQ-20260831-028-62x6", "REQ-20260831-029-62x6", "REQ-20260831-030-62x6"]
relates_why: {}
tags: ["auto-audit", "project", "access", "dashboard", "attached", "png", "image", "docs"]
created: 2026-08-31T22:42:40+09:00
status_since: 2026-08-31T22:43:07+09:00
updated: 2026-09-01T13:00:36+09:00
priority: 50
---

## Original

이 시스템에는 프로젝트라는 개념이 존재한다. 하지만, 정작 프로젝트라는 기능이 시각적으로 노출되어 있지 않은데, 신규 프로젝트 추가, 멤버 관리, 프로젝트별 설정정보, 관리정보 등을 대시보드에서 처리할 수 있도록 po, pm, 아키텍트, 프론트개발자, 백엔드개발자, 디자이너, ux라이터, 테크라이터, 트랜슬레이터, qa 등 모두 힘을 합쳐서 준비해서 완성해줘.

## Notes

### 2026-08-31T22:45:59+09:00 response (agent: sub:architect, by nicehugepark)

[architect 설계 — 프로젝트 관리 데이터·API]

■ 기존 실물 (조사 결과 — 새로 만들 것보다 이미 있는 것이 많다)
· 데이터: vault/projects/<slug>.md (PRJ 문서, 평면 배치). frontmatter = id/type=project/slug/title/summary/status(active|archived)/customer/contact_name·email·phone·org/members[{user,role,since,until,position}]/created/updated. 변경 이력은 body ## History 에 한 줄씩 append. 에셋 공간 projects/<slug>/ = CONTEXT.md + assets/ + agents/ (scaffold_asset_space, 멱등).
· 함수: project_path/load_project/all_projects/project_role/user_projects/member_active/project_owner_count/project_can(read1<contribute2<manage3<own4, admin 우회, 미등록 프로젝트 비강제)/scaffold_asset_space/sync_project_agents.
· CLI: s9 project add|ls|show|set|member(add/rm/ls)|authz|scaffold|agents. add·set 은 cmd_project 안 인라인, member 는 do_member_set/do_member_rm 단일 경로.
· 서버 GET /api/projects → projects_data(): 전 프로젝트+멤버(active 판정 포함)+연락처. 뷰어 필터: admin·미등록=전체, 등록 member=자기 참여 프로젝트만. **web/index.html 은 현재 이 API를 한 번도 호출하지 않는다** — 화면이 통째로 신규.
· 서버 POST /api/project/member, /api/project/member/rm 이미 존재 — CLI와 같은 do_member_* 를 지난다(권한·마지막 owner 가드·History 기록 동일).
· POST 공통 전제(do_POST 서두): actor=서버 파생 whoami(클라이언트 user 불신), admin 만 as=대리, _POLL_EPOCH+1(행동 직후 스냅샷 무효화).

■ API 표면 (신규는 ②③ 둘뿐)
| # | 엔드포인트 | 게이트(단일 경로) | 검증 | 쓰는 파일 |
| ① | GET /api/projects (기존) | 뷰어 필터 | - | 읽기만. 확장: 프로젝트당 history_tail(최근 ~10줄, body ## History 파싱) 추가 → 관리정보 패널용 |
| ② | POST /api/project/add {slug,name,summary?,customer?,contact_*?} | **do_project_add (신규 단일 경로 — cmd_project add 에서 추출, CLI도 이걸 호출)** | slug 정규식 [A-Za-z0-9._-]+ · 중복 거부 · 생성자=owner 자동 | vault/projects/<slug>.md 생성 + projects/<slug>/ 스캐폴드 |
| ③ | POST /api/project/set {slug, name?/summary?/customer?/status?/contact_*?} | **do_project_set (신규 단일 경로 — cmd_project set 에서 추출)** | project_can(manage) · status enum · **status 변경(보관/복원)만 own 요구** | vault/projects/<slug>.md |
| ④ | POST /api/project/member (기존) | do_member_set | manage, owner 건은 own, 마지막 owner 강등 차단, role enum, until 날짜형식 | 동 문서 |
| ⑤ | POST /api/project/member/rm (기존) | do_member_rm | manage, owner 제거는 own+마지막 owner 차단 | 동 문서 |
삭제 API 없음 — archived 로 충분(1단계). 화면은 판정하지 않는다: 거부는 전부 ValueError→서버 응답 문구를 그대로 그린다.

■ 권한 (ADR: 생성을 admin 전용으로 좁히지 않는다)
대안 A) 생성=admin 전용: 사용자 role 부여(user/add)와 대칭이지만, CLI(s9 project add)는 이미 누구나 허용 — 화면만 좁히면 CLI가 옆문이 된다. 대안 B) 등록 사용자 누구나 생성, 생성자=owner (채택): CLI 패리티 유지, 자가 서비스, 이후 관리 권한은 project_can 이 지배. admin 은 as= 대리로 타인 명의 생성 가능. 발견한 불일치: project_can 독스트링은 "own: 프로젝트 보관/삭제"인데 cmd_project set 은 status 변경을 manage 에 열어 둠 — do_project_set 추출 시 status 만 own 으로 올려 코드를 독스트링에 맞춘다(단일 경로라 CLI·화면이 같이 고쳐진다).

■ 동시성·원자성
모든 변이는 acquire_lock() 안에서 write_doc + rebuild_index(quiet=True) 후 release — do_member_set 패턴 그대로(신규 do_* 도 동일). next_id("PRJ")도 lock 안(기존 add 그대로). 파일락 하나가 서버 스레드·동시 CLI 를 함께 직렬화한다. 스캐폴드는 lock 밖(멱등, 인덱스 무관). 폴링 폭풍 불변: /api/projects 는 폴링 벨트에 올리지 않는다 — 탭 진입 시 1회 + 변이 성공 응답 후 재조회가 계약. 변이는 _POLL_EPOCH 로 catalog 스냅샷을 이미 무효화한다(REQ-20260831-002-62x6 구조 그대로).

■ 구현 규모 (backend 몫)
신규: do_project_add(~40줄) · do_project_set(~30줄) · do_POST 분기 2개(~15줄) · projects_data history_tail(~10줄). 수정: cmd_project add/set → do_* 호출로 축소. 시험: test_project_api.py 신규 ~8케이스(생성/중복slug/설정/권한거부/archived own 게이트/마지막 owner/멤버 upsert 회귀). 프론트는 전량 신규(현재 /api/projects 참조 0건) — designer 산출 대기.

### 2026-08-31T22:46:10+09:00 response (agent: sub:product-owner, by nicehugepark)

[PO 요구·범위 정의 — 실물 조사 기반]

**배후 문제 재서술**: 프로젝트 개념은 데이터(PRJ 문서·멤버·role)·CLI(add/ls/show/set/member/authz/scaffold/agents)·서버(GET /api/projects, POST /api/project/member[/rm])에 이미 상당히 완성돼 있으나, 화면 진입점이 「프로젝트: 전체」 필터 하나뿐이고 정보 패널(#proj-info)은 특정 프로젝트를 필터로 골랐을 때만 나타난다. 즉 **기능 부재가 아니라 발견 불가(discoverability) 문제 + 생성·설정 두 동사의 화면 부재**다.

**사용자**: 현재는 owner 1인 체제(section9, 멤버 nicehugepark 1명)지만 데이터 모델은 다인 팀 전제(role 4단계 owner/maintainer/contributor/viewer, 만료일, doc_visible/project_can). 화면은 role-aware여야 한다 — manage 권한이 없으면 같은 화면이 읽기 전용으로 보인다.

**각 기능이 해결하는 실제 일**
- 생성: 새 고객/업무 단위를 열 때 CLI 없이 대시보드에서 시작 (현재 s9 project add 전용, 인가 게이트 없음 — 화면 개방 시 게이트 신설 필요)
- 멤버: 참여자 넣고 빼고 권한·직무·만료 조정 (이미 완성 — do_member_set 단일 경로, owner 가드, via dashboard audit)
- 설정: title/summary/customer/status/contact×4 8필드 수정 (CLI set만 존재, manage 인가 있음. set은 History 미기록 — 멤버 변경만 기록. 이 비대칭은 구현 시 교정)
- 관리정보(실물 판정): ①작업 현황 = catalog의 project 필드로 상태별 REQ 집계·최근 활동(기존 데이터 재사용, 신규 저장소 불요) ②멤버 활성/만료 집계(API에 이미 있음) ③변경 이력 = PRJ 문서 History(문서는 catalog에 있어 Docs 탭에서 열람 가능 — 새 화면은 문서 링크만). "설정 이력" 별도 저장소는 실물에 없다 — 만들지 않는다.

**MVP 경계**
IN: ①프로젝트 목록·상세 노출면(필터 뒤가 아닌 발견 가능한 자리, 기존 proj-info 확장·재배치) ②생성 폼(slug·name·summary·customer / 서버에 do_project_add 단일 경로 추출 + 최소 인가: 등록 사용자, 생성자=owner, admin 우회 — architect 확정) ③설정 편집 8필드(manage 인가, History 기록 추가) ④관리 요약(상태별 REQ 수·최근 활동·멤버 집계·PRJ 문서 링크) ⑤보관(archive — status 필드에 이미 존재, own 인가)
OUT(non-goals): 프로젝트 삭제(데이터 모델에 없음, archive로 갈음) / role 4단계 세분화 변경 / slug 변경(참조 무결성) / CONTEXT.md·agents/ 파일 편집 화면 / 멤버 초대·알림 등 외부 연동 / 프로젝트별 대시보드 커스터마이즈

**우선순위 (임팩트×확신도/비용)**: ①노출면 9×0.9/2=4.1 ②생성 8×0.9/3=2.4 ③설정 편집 6×0.9/2=2.7 ④관리 요약 6×0.6/3=1.2 ⑤보관 4×0.8/1=3.2 → 순서: 노출면 → 보관·설정 → 생성 → 관리 요약 (단 생성은 사용자 원문 1순위 언급이라 노출면 직후 배치 권고)

**수용 조건(AC)**
- 목록: 필터 선택 없이도 프로젝트 목록(제목·상태·고객·멤버 n/n)에 도달할 수 있다; viewer 권한 격리는 기존 /api/projects 필터 그대로다
- 생성: 화면에서 slug·이름 입력→생성하면 PRJ 문서·projects/<slug>/ 스캐폴드가 생기고 생성자가 owner가 된다; 잘못된 slug(영문/숫자/._- 외)·중복은 서버 거부 사유가 화면에 보인다; CLI add와 같은 코드 경로다
- 멤버: 기존 패널 기능(추가·제거·role/until/position 변경·마지막 owner 강등 차단)이 새 자리에서 동일 동작한다 — 회귀 없음
- 설정: manage 권한자가 8필드를 수정하면 PRJ 문서에 반영되고 History 줄이 남는다; 권한 없으면 읽기 전용 + 거부 사유
- 관리정보: 상세에서 그 프로젝트의 상태별 문서 수와 최근 활동이 보이고, PRJ 문서(이력 포함)로 한 번에 이동한다
- 보관: archive 하면 목록에서 구분 표시되고 새 REQ 배정 대상에서 빠진다(기존 필터 동작은 유지)

**CLI와의 관계**: 화면은 CLI를 대체하지 않고 보완한다. 원칙은 이미 코드에 있다 — do_member_set처럼 판정(인가·검증·가드)은 서버 함수 한 곳, CLI와 POST가 같은 함수를 부른다. 생성·설정도 같은 패턴으로 추출하라(do_project_add·do_project_set). 화면 전용 로직 금지.

**잃는 것 명시**: 삭제를 뺐으므로 잘못 만든 프로젝트는 archive로만 치울 수 있다(테스트 slug 오염 가능 — 허용). 생성 인가를 느슨히(등록 사용자) 열면 프로젝트 난립 위험 — 1인 체제 현실에서 수용, 다인 확장 시 admin 한정 재검토.

### 2026-08-31T22:46:18+09:00 subagent (agent: sub:architect:a534bae1, by nicehugepark)

`[2026-08-31 22:49:52 KST - architect]`

**결론 — 서버 설계 완료, 노트 기록됨** (`s9 note REQ-20260831-026-62x6 --label response`, 3238자). 핵심 판정: **신규 API는 둘뿐이다** — 나머지는 이미 있다.

**① 기존 실물** (`/home/sjpark1/section9/bin/s9`)
- 데이터: `vault/projects/<slug>.md` — frontmatter에 slug/title/summary/status(active|archived)/customer/contact_4종/members[{user,role,since,until,position}], 이력은 body `## History` append. 에셋 공간 `projects/<slug>/`(CONTEXT.md+assets/+agents/, scaffold 멱등).
- 함수군: L12399~12680 (project_path/load_project/all_projects/project_role/project_can 등). CLI `s9 project add|ls|show|set|member|authz|scaffold|agents` (L12909~).
- 서버: GET `/api/projects`(L15846, projects_data L15178 — 뷰어 필터 포함), POST `/api/project/member`·`/member/rm`(L16414~ — CLI와 `do_member_set`/`do_member_rm` L12813~12907 단일 경로 공유, 마지막 owner 가드까지 완비).
- **web/index.html은 `/api/projects`를 0회 호출** — 화면은 통째 신규다.

**② 신규 API 표면**: POST `/api/project/add`와 `/api/project/set` 둘. 각각 `do_project_add`/`do_project_set`을 **cmd_project에서 추출한 단일 경로**로 만들어 CLI·서버가 공유한다(do_member_* 패턴 그대로). GET은 관리정보 패널용 history_tail 확장만.

**③ 권한 (ADR)**: 생성은 admin 전용이 아니라 **등록 사용자 누구나, 생성자=owner** — CLI가 이미 그렇고 화면만 좁히면 CLI가 옆문이 된다. 설정=manage(maintainer+), 멤버=manage/owner건은 own, admin은 project_can 내장 우회+`as` 대리. **불일치 발견**: project_can 독스트링은 보관을 own으로 규정하는데 `cmd_project set`은 status 변경을 manage에 열어 둠(L13012~) — do_project_set 추출 시 status만 own으로 올려 해소.

**④ 동시성**: 모든 변이 = `acquire_lock()` 안 write_doc+rebuild_index — 기존 패턴 그대로, 파일락 하나가 서버 스레드·동시 CLI를 직렬화. 폴링 불변: `/api/projects`는 벨트에 안 올린다(탭 진입 1회+변이 후 재조회); `_POLL_EPOCH`은 do_POST 서두 공통이라 신설 엔드포인트도 자동 커버.

**⑤ 규모**: backend 신규 ~95줄(do_project_add 40+do_project_set 30+분기 15+history_tail 10)+cmd_project 축소, 시험 `test_project_api.py` ~8케이스. 프론트 전량 신규.

한계: 프로젝트 삭제 API는 설계에서 제외(archived로 충분, 1단계) — 필요해지면 own 게이트+confirm 이중 확인으로 별도 REQ.

### 2026-08-31T22:46:27+09:00 subagent (agent: sub:product-owner:a6ecbeb6, by nicehugepark)

`[2026-08-31 22:49:12 KST - product-owner]`

### 결론

**문제는 기능 부재가 아니라 노출 부재다.** 프로젝트는 데이터·CLI·서버·화면 패널까지 이미 절반 이상 지어져 있고, 화면에 없는 동사는 **생성·설정 편집** 둘뿐이다. 산출(스토리·MVP 경계·우선순위·AC 표)은 `s9 note REQ-20260831-026-62x6 --label response` 로 기록했다 (2,590자).

### 실물 조사 핵심 (전부 코드로 확인)

- **이미 있음**: `s9 project add|ls|show|set|member|authz|scaffold|agents` (`/home/sjpark1/section9/bin/s9` cmd_project, 12909행~), role 4단계 + `project_can` 인가 매트릭스(12636행), GET `/api/projects`(15846행), POST `/api/project/member`·`/rm`(16414행), 그리고 **멤버 관리 UI가 이미 완성돼 있다** — `web/app/app.js` `renderProjectInfo`·`wireMemberControls`(153~272행): 추가·제거·role/until/position 편집·마지막 owner 강등 차단까지. 단지 `#f-project` 필터로 특정 프로젝트를 골랐을 때만 나타나 아무도 발견 못 한다.
- **없음**: 생성(POST 엔드포인트 없음 + CLI `add`에 인가 게이트 없음 — 화면 개방 시 신설 필요), 설정 편집 화면(8필드, CLI `set`만·History 미기록 비대칭), 프로젝트 목록 진입점.
- **"관리정보" 판정**: 실물 기준 ①작업 현황(catalog `project` 필드 집계 — 신규 저장소 불요) ②멤버 활성/만료(API에 이미 있음) ③이력(PRJ 문서 History — catalog에 올라 Docs 탭 열람 가능, 링크만). 별도 설정 이력 저장소는 없으므로 만들지 않는다.

### MVP 경계

- **IN**: 목록·상세 노출면 / 생성 폼(do_project_add 단일 경로 추출) / 설정 편집 8필드(+History 기록 교정) / 관리 요약 / archive(status에 이미 존재).
- **OUT**: 삭제(모델에 없음) · role 세분화 · slug 변경 · CONTEXT.md/agents 편집 화면 · 초대/알림 연동.
- **우선순위**(임팩트×확신도/비용): 노출면 4.1 > archive 3.2 > 설정 2.7 > 생성 2.4 > 관리 요약 1.2.

### architect 에게 넘길 판정 2건

① 생성 인가 수준(제안: 등록 사용자 + 생성자=owner + admin 우회) ② `set`의 History 미기록 교정 위치. **원칙 답변**: 화면은 CLI 보완이며, `do_member_set` 선례대로 판정은 서버 함수 한 곳 — 생성·설정도 같은 패턴(do_project_add/do_project_set)으로 추출하면 된다.

### 2026-08-31T22:47:08+09:00 response (agent: sub:project-manager, by nicehugepark)

[PM 실행 계획 — 2단계 구현 분해·순서·리스크·게이트]

전제(코드 실측): 프로젝트 기반은 이미 있다 — PRJ 문서(멤버 role/until/position·고객·담당자·status), 권한(project_can, owner/maintainer/contributor/viewer, 마지막 owner 가드), CLI(s9 project add/set/ls/show/member/authz/scaffold), 서버 GET /api/projects + POST /api/project/member(+rm, do_member_set 단일 경로), 화면 #proj-info 멤버 편집·MY PROJECTS 스트립. **빈 곳은 넷**: ① 대시보드 생성 API 없음 ② 설정·관리정보(고객/담당자/status/summary) 수정 API 없음 ③ 1급 프로젝트 화면 없음(#f-project 필터 종속 패널뿐) ④ 새 화면의 문구·검증. 2단계는 이 넷만 — CONTEXT.md/프로젝트 에이전트 노출은 범위 밖으로 명시 컷(암묵 드롭 아님, 필요 시 후속 REQ).

### ① 작업 분해 — 파생 REQ 4건 (모두 --parent REQ-20260831-026-62x6)

**A. 프로젝트 생성·설정 API** (backend-developer, M)
- 범위: do_project_create / do_project_update 단일 코드 경로 신설 → CLI(project add/set)와 POST /api/project/add · /api/project/set 이 공유(do_member_set 전례 그대로). 인가: 생성=admin(G0에서 PO 판정 확정), 설정=maintainer↑, status 변경=owner. 스키마 additive-only.
- goal 초안: "CLI와 대시보드가 같은 코드 경로로 프로젝트를 만들고 설정을 고치며, 무권한 호출 거부(미참여/viewer/contributor/만료 4경로)와 기존 PRJ 하위 호환이 테스트로 판정된다"
- 파일: bin/s9 · tests/test_project_*.py(신규)

**B. 프로젝트 관리 화면** (frontend-developer + designer, L)
- 범위: 목록·상세(멤버/설정/관리정보 편집)·생성 폼. **새 파일 web/app/project.js + web/css/project.css 로 격리** — app.js/index.html/boot.js/skins.css 는 등록 지점만 최소 diff. 기존 #proj-info·멤버 API 배선은 재사용(재작성 금지).
- B1(정적 골격·mock 데이터, A와 병렬) → B2(실 API 연동+문구 반영, A done·C 확정 후).
- goal 초안: "대시보드에서 프로젝트 목록을 열고 새 프로젝트 생성·멤버 추가/제거·설정 수정이 실브라우저(s9 shot 캡처 첨부)로 확인된다"

**C. 화면 문구 4인 판정** (ux-writer 주도 + tech-writer·translator, S)
- 범위: B1 요소 목록 기준 라벨·다이얼로그·오류·빈상태 문구 표. 산출은 노트의 표 — 코드 반영은 frontend 한 손만(두 손 사고 방지).
- goal 초안: "프로젝트 화면의 모든 사용자 노출 문구가 4인 판정 표로 노트에 확정되어 있고 B2 반영분과 1:1 대조된다"

**D. 회귀·브라우저 검증** (quality-assurance, M)
- 범위: 전체 스위트 + 프로젝트 시나리오(생성/멤버/설정/거부) + 스킨×테마 s9 shot 매트릭스 + 폴링 중 폼 보존 회귀.
- goal 초안: "python3 tests/ 전체 녹색(echo $? 실측)과 프로젝트 화면 검증 매트릭스 캡처가 문서에 붙어 parent review 근거가 된다"

### 파일 충돌 지도 (같은 파일 두 손 금지 — 이 저장소 반복 실사고)

| 파일 | 잡는 손 | 시기·규칙 |
|---|---|---|
| bin/s9 (18k줄 단일 파일) | backend(A) 단독 | W1. 이 기간 타 REQ의 bin/s9 수정 보류 — 리드가 조정 |
| tests/ | A(test_project_* 신규) · D(실행+추가) | 신규 파일이라 무충돌, D 추가분도 별도 파일 |
| web/app/project.js · web/css/project.css | frontend(B) 신설 | W1~W2, 단독 소유 |
| web/app/app.js · index.html · boot.js | frontend(B)만 | W2 후반 등록 지점 한 번에(최소 diff) |
| web/css/skins.css | B(designer 판정 반영) | W2. 타 화면 REQ와 겹치면 리드가 순서 결정 |
| 문구(C) | 코드 파일 안 잡음 | 표만 산출, 반영은 B2 |

커밋은 전부 리드 단독. 위임 지시문에 소유 파일을 명시해 내려보낸다.

### ② 순서·병렬

```
G0 리드 종합(지금): PO 범위 + architect 계약 + designer 구조 병합, 생성 권한 판정
  → 파생 A·B·C 등록·즉시 착수 (D는 등록만)
W1 (병렬): A(TDD→구현) ∥ B1(mock 골격) ∥ C(문구 표)
W2: B2(실 연동 + 문구 반영)   ← A done(G1) + C 표 확정 대기
W3: D(검증)                    ← B2 완료 대기, B review 전 수행
  → parent 026 review (리드, 300자 사용자 언어 카드)
```
크리티컬 패스 = A → B2 → D. 일정(기대/최악): W1 반나절/1일 · W2 반나절/1일 · W3 2h/반나절.

### ③ 리스크 (확률×영향 · 조기 신호 · 대응)

- **R1 PRJ 스키마 하위 호환** (중×상): by-project 인덱스·cmd_new 단일 프로젝트 자동매핑·무인 워커 preamble·notice.js mine 판정이 전부 기존 필드를 읽는다. 신호=기존 스위트 실패, 보드 프로젝트 필터/MY STRIP 깨짐. 대응=additive-only 스키마 + 기존 vault/projects/section9.md 로드 회귀를 A의 TDD에 선행 배치.
- **R2 폴링 계약 — 재렌더 폼 파괴** (상×중): 주기 폴링이 render() 전면 재그림. settings 탭은 이미 가드(REQ-055 E7: 입력 중 재렌더 금지)가 있으나 새 화면이 가드 밖이면 생성/설정 폼이 입력 중 지워진다. 신호=입력 중 값 소실. 대응=B TDD에 '편집 중 재렌더 금지' 케이스 명시 + verify-*.html 방식 회귀 페이지.
- **R3 권한 옆문** (중×상): viewer_of 는 자기신고 기반(인증 아님). API 핸들러가 do_* 단일 경로·project_can 을 우회해 인라인 인가를 쓰면 CLI 게이트에 옆문이 생긴다. 신호=핸들러 안의 role 비교 코드. 대응=A에서 거부 경로 테스트 4종 의무 + 핸들러는 do_* 호출·ValueError 중계만.
- **R4 같은 파일 두 손** (상×상): 대응=위 충돌 지도 + 신규 파일 격리 + 리드 단독 커밋 + 반려 워커 스폰 시 active_reqs 클레임 확인.

### ④ 검증 게이트 (통과 전 다음 손 금지)

- **G1 (A→done)**: TDD 노트 N/N 체크 + `python3 tests/` 전체 `echo $?` 실측 0 + CLI/웹 동일 경로 확인. 통과해야 B2 착수.
- **G2 (B→review)**: s9 shot(포트 9909, ?skin=&theme= 매트릭스) 캡처 `--attach` + C 문구 표 1:1 대조 + designer·ux-writer·tech-writer·translator 4인 판정(하나라도 빠지면 반려 대상, 규약 7). review 카드는 사용자 언어 300자·갈래 3.
- **G3 (D→done)**: 전체 스위트 녹색 + 거부 경로 실측 + 폼 보존 회귀. 통과 후 parent 026 review — 확인 포인트는 "프로젝트 화면에서 생성→멤버→설정이 눌러서 되는가"만.

한계: 생성 권한의 주체(admin 한정 vs 등록 사용자 전원)는 PO 판정 대기 — G0에서 확정해야 A의 TDD 가 닫힌다. 1단계 타 역할(PO·architect·designer) 산출과 이 분해가 어긋나면 리드 종합에서 이 표를 개정한다.

### 2026-08-31T22:47:27+09:00 subagent (agent: sub:project-manager:a5028790, by nicehugepark)

`[2026-08-31 22:48:20 KST - project-manager]`

계획 수립 완료. 노트는 `/home/sjpark1/section9/vault/requests/2026/08/REQ-20260831-003-62x6.md`가 아니라 대상 문서 REQ-20260831-026-62x6 에 `--label response --agent project-manager` 로 4,112자 기록했다(코드·git 무수정).

### 결론 먼저

**이 기능은 절반이 이미 만들어져 있다.** bin/s9 실측 결과 PRJ 문서 모델·권한 4단계(project_can, 마지막 owner 가드)·CLI(project add/set/member/authz)·GET /api/projects·멤버 편집 API(do_member_set 단일 경로)·#proj-info 멤버 편집 패널까지 전부 존재한다. 2단계는 **빈 곳 넷**만 채우면 된다: 생성/설정 API, 1급 화면, 문구, 검증. 이 발견이 계획의 크기를 L 전면 신축이 아니라 "증축 4건"으로 줄인다.

### 요지 (전체 표는 노트에)

**① 파생 REQ 4건** — A 생성·설정 API(backend, M: do_project_create/update 단일 경로 → CLI와 POST /api/project/add·set 공유), B 프로젝트 화면(frontend+designer, L: **신규 web/app/project.js + web/css/project.css 로 격리**, B1 mock 골격 → B2 실연동), C 문구 4인 판정(ux-writer 주도, S: 표만 산출, 코드는 안 잡음), D 회귀·브라우저 검증(QA, M). 각각 goal 판별 문장 초안 포함.

**② 순서** — W1 병렬(A ∥ B1 ∥ C) → W2 B2(A done + C 확정 대기) → W3 D → parent review. 크리티컬 패스 A→B2→D, 기대 1.5일/최악 2.5일.

**③ 파일 충돌 지도** — bin/s9 는 W1 동안 backend 단독(타 REQ 보류), 프론트는 신규 파일 격리로 app.js/index.html 은 W2 후반 등록 지점 최소 diff 한 번, skins.css 접점은 리드가 순서 조정, 커밋은 리드 단독.

**④ 리스크 4건** — R1 PRJ 하위 호환(by-project 인덱스·자동매핑·워커 preamble이 기존 필드를 읽는다 → additive-only+회귀 선행), R2 폴링 재렌더 폼 파괴(REQ-055 E7 가드 밖 신화면 → TDD에 명시), R3 권한 옆문(viewer_of 자기신고 → 핸들러는 do_* 중계만, 거부 테스트 4종 의무), R4 같은 파일 두 손(지도+격리+리드 커밋).

**⑤ 게이트** — G1(A: TDD N/N + `python3 tests/` echo $? 실측 0) 통과 전 B2 금지, G2(B: s9 shot 스킨×테마 캡처 --attach + 4인 스탬프) 없으면 반려 대상, G3(D 녹색) 후에만 parent review.

**리드 판정 필요 1건**: 프로젝트 생성 권한 주체(admin 한정 vs 등록 사용자 전원) — PO 산출과 함께 G0 종합에서 확정해야 A의 TDD가 닫힌다.

### 2026-08-31T22:50:55+09:00 response (agent: sub:designer, by nicehugepark)

### 결론 — 프로젝트의 집은 문서다. 새 탭도 새 화면도 만들지 않는다

**전제부터 고친다: 프로젝트 관리 화면은 이미 절반이 지어져 있다.** REQ 원문은 "시각적으로
노출되어 있지 않다"고 하지만, 실물은 다르다 — 헤더 필터에서 프로젝트를 고르면 Board 위에
`#proj-info` 패널이 서고, 그 안에 **고객·현업 담당자·멤버 표(권한 셀렉트·직무·참여일·만료·
상태)와 추가/제거 버튼이 전부 배선돼 있다**(`web/app/app.js:152-275`, `web/css/header.css:163-235`,
서버 `/api/project/member`, `/api/project/member/rm`).

그러니 진짜 결함은 "없다"가 아니라 **셋**이다.

1. **찾을 수 없다** — 그 패널은 헤더 필터를 특정 프로젝트로 바꿔야만 나타난다. 기본값이
   「프로젝트: 전체」라 아무도 마주치지 않는다. 기능은 있는데 문이 없다.
2. **한 사물에 얼굴이 둘인데 양쪽 다 반쪽이다** — 같은 프로젝트가 두 자리에 산다.
   Board 위 패널은 고객·담당자·멤버를 보여 주되 History·Notes가 없고, Docs의 PRJ 문서 뷰
   (`#docs/PRJ-20260823-001`)는 status·summary·History를 보여 주되 **멤버도 고객도 안 그린다**.
   어느 쪽도 "이 프로젝트가 무엇인가"에 온전히 답하지 못한다.
3. **관리 도구가 작업 화면을 밀어냈다** — 실측: 1440×900에서 패널이 세로 **244px**
   (여백 포함)을 먹어 Board 컬럼 머리가 y=169 대신 y=413에서 시작한다. 뷰포트의 27%,
   카드 두 장 높이다. 900×760에서도 190px(25%)이다. Board의 결정은 "무엇을 다음에
   옮길까"인데 그 위에 권한 셀렉트 표가 앉아 있다 — 「한 화면 한 결정」 위반.

빠진 것은 **생성 흐름 · 프로젝트 목록 · 설정(고객·담당자·상태·개요) 편집** 셋뿐이다.
이 셋을 어디에 놓느냐가 이번 판정이다.

---

### 1. 자리 판정 — 대안 셋과 권고

### 대안 A — 새 탭 「Projects」

Board·Docs·Graph·Audit·Stream·Terminal·Settings 옆에 여덟 번째 탭을 세우고 목록·상세·
생성을 전부 담는다.

- 얻는 것: 진입이 가장 짧다(1클릭). 목록↔상세 왕복이 한 화면 안이다.
- 잃는 것 ①: **탭은 "일하는 모드"의 축이지 사물의 목록이 아니다.** 지금 일곱은 전부
  동사다 — 옮긴다·읽는다·본다·감사한다·흘려본다·친다·설정한다. 여기에 명사 하나가
  끼면 축이 깨진다.
- 잃는 것 ②: **빈도가 자리를 정당화하지 못한다.** 프로젝트 생성은 수명에 한 번,
  멤버 변경은 드물다. 매일 여섯 번 쓰는 탭들의 시선 비용을 늘려 1년에 세 번 쓰는 일에
  상시 자리를 주는 거래다.
- 잃는 것 ③(실측): 900px 폭에서 툴바가 이미 2행→3행으로 wrap 한다(태그 필터가 아래로
  떨어짐). 탭 줄은 아직 한 줄이지만 서버 상태 칩과 남은 여유가 얼마 없다 — 여덟 번째
  탭은 좁은 화면에서 브랜드 행마저 두 줄로 민다.
- 잃는 것 ④: 얼굴이 셋이 된다. 지금 둘로 갈라진 진실을 셋으로 늘린다.

### 대안 B — Settings 안 「프로젝트」 섹션

Settings는 이미 좌측 섹션 목록 + 우측 패널의 **2단 관리 셸**이고, 섹션이 디스플레이 ·
내 계정 · **사용자 관리** · 시스템 · 가이드다.

- 얻는 것: 셸이 공짜다. 탭 0 추가. **사람(사용자 관리) ↔ 프로젝트**의 대칭이 곱다.
  「조직 관리는 Settings에 산다」는 선을 사용자 관리가 이미 넘어 놓았다.
- 잃는 것 ①: **두 번째 진실이 굳는다.** 이 시스템의 뼈대는 "모든 것은 문서다"이고
  `PRJ-20260823-001`이 프로젝트의 정본이다. 관리 화면이 문서 밖에 서면 History·Notes·
  백링크·검색·Graph 노드를 전부 버리고 시작한다 — 그리고 그것들이 바로 "관리 정보"다.
- 잃는 것 ②: Settings는 **내가 나에게 하는 설정**의 자리다(스킨·톤·계정). 사용자 관리가
  예외로 들어와 있으나, 그건 admin 한 명의 전역 작업이라 성격이 다르다. 프로젝트는
  여럿이 오가는 협업 대상이다.
- 잃는 것 ③: Settings는 **필터가 걸리지 않는 섬**이다. 헤더의 프로젝트 필터·검색·태그가
  Settings에서는 아무 일도 하지 않는다. 프로젝트를 그 안에 넣으면 "지금 보고 있는 범위"와
  "관리하는 프로젝트"가 서로 모르는 채로 있게 된다.

### 대안 C — 권고: **문서가 집, 목록은 이미 있는 자리, Board 위는 한 줄로**

새 탭 0, 새 화면 0. 있는 두 자리를 각각 온전하게 만들고, 셋째 자리(Board 위 패널)는
문맥 띠로 줄인다.

| 무엇 | 어디 | 왜 거기 |
|---|---|---|
| **목록 · 생성** | Docs 좌측 타입 진입점 `PROJECT` | 이미 목록이다(캡처에 `PROJECT 1`). 누르면 프로젝트만 걸러진 문서 목록이 뜬다. 머리에 「+ 새 프로젝트」 한 줄만 얹으면 끝 |
| **상세 · 멤버 · 설정 편집** | PRJ 문서 뷰 | 정본이 여기다. History가 이미 아래에 있어 "누가 언제 무엇을 바꿨나"가 같은 화면에서 읽힌다 — 관리 정보의 정의가 그것이다 |
| **지금 보는 범위** | Board/Graph 위 `#proj-info` | 표를 걷어내고 한 줄 문맥 띠로. 결정을 가리지 않는다 |

권고 근거 넷:

1. **진실이 하나가 된다.** 지금 반쪽씩 나뉜 정보가 문서 한 자리로 합쳐진다. 멤버를
   바꾸면 History에 줄이 남고(서버가 이미 그렇게 쓴다 — `member update … (by nicehugepark)`),
   그 줄을 같은 화면 아래에서 바로 읽는다. 관리 화면을 따로 지으면 이 연결이 끊긴다.
2. **세 화면 모두에서 「한 화면 한 결정」이 선다.**
   Board = 무엇을 다음에 옮길까 / Docs›PROJECT = 어느 프로젝트인가, 새로 만들까 /
   PRJ 문서 = 이 프로젝트를 어떻게 고칠까.
3. **탭 비용 0, 신규 컴포넌트 0.** 문서 뷰의 메타 격자·표·`.acts` 버튼·`s9dlg` 다이얼로그가
   전부 있는 부품이다. 새 패턴 발명은 최후 수단이라는 규율에 맞는다.
4. **Board가 244px를 돌려받는다.**

**왕복 우려에 대한 답**: 목록(Docs)과 상세(같은 Docs 뷰어)는 **같은 탭 안**이다 —
Docs는 좌측 목록 + 우측 뷰어의 2단이라, 프로젝트 행을 누르면 오른쪽에 상세가 뜬다.
Settings에 목록을 두는 B는 오히려 Settings→Docs 탭 점프가 생긴다. C는 그 점프가 없다.

---

### 2. 화면별 와이어프레임 (서술)

### (a) Docs › PROJECT — 목록 + 생성 진입

좌측 타입 진입점에서 `PROJECT n` 을 누른 상태. 좌측 목록 패널 안:

```
PROJECT                                    + 새 프로젝트
─────────────────────────────────────────────────────
● Section9 하네스 시스템                        ACTIVE
  PRJ-20260823-001 · section9
  멤버 1 · 열린 요청 3 · 4분 전
─────────────────────────────────────────────────────
● 고객사 포털 개편                              ACTIVE
  PRJ-20260901-002 · portal
  멤버 5 · 열린 요청 12 · 어제
─────────────────────────────────────────────────────
+ 보관됨 2개
```

- 「+ 새 프로젝트」는 mono 소형 텍스트 버튼(밑줄 hover) — 「N개 더 보기」와 같은 문법.
  만들 권한이 없으면 **아예 안 그린다**(회색 버튼 금지).
- 행 첫 글리프는 7px 사각점(`.cdot`) — active는 프로젝트 타입 잉크, archived는
  `--c-cancelled` #868c96. 색 없이도 `ACTIVE`/`ARCHIVED` 글자가 오른쪽에서 같은 말을 한다.
- 셋째 줄(멤버 수 · 열린 요청 수 · 마지막 활동)이 이 목록의 값이다 — **관리 정보의 요약**.
  마지막 활동은 그 프로젝트에 속한 문서의 최신 `updated`.
- **정렬 기본은 최근 활동순**, 이름순이 아니다. 일하는 사람은 최근 것을 찾는다.
- 20개 초과 시 20 + 「N개 더 보기」(밀도 규칙).

### (b) PRJ 문서 뷰 — 상세 · 멤버 · 설정

지금의 뷰 구조(경로 줄 / 제목+ID / 메타 격자 / NOTES / HISTORY / 치우기)를 **유지한 채**
메타 격자를 넓히고 그 아래 멤버 표를 끼운다.

```
vault/projects/section9.md
Section9 하네스 시스템  PRJ-20260823-001                       ● ACTIVE
[이어 말하기]

SLUG              section9
STATUS            ● active                        ← maintainer+ 면 셀렉트
SUMMARY           프롬프트 audit·외부기억 하네스…    ← 클릭하면 그 자리 입력
CUSTOMER          내부(자체)
CONTACT           — (이름 · 소속 · 메일 · 전화)
CREATED/UPDATED   2026-08-23T19:12 → 2026-08-23T20:33

MEMBERS  1 활성 / 1                                         (표 캡션)
사용자          권한        직무      참여일      만료      상태
nicehugepark·ME [owner ▾]  [PM/오너]  2026-08-23 [연-월-일] 활성   [− 제거]
[e7test ▾]      [contrib▾]  [직무    ] —          [연-월-일]        [+ 추가]

NOTES
HISTORY
 · 2026-08-23T20:33 member update nicehugepark role=owner position=PM/오너
이 문서 치우기 [보관][삭제]
```

- 멤버 표는 **지금 `#proj-info` 의 표를 그대로 옮긴다** — 마크업·CSS·배선이 이미 있다.
  `.proj-info` 셀렉터를 문서 뷰 맥락으로 옮기거나 공통 클래스로 승격하는 것이 2단계 몫.
- 메타 격자 라벨은 mono 10.5px uppercase(기존 `.pi-k`/문서 뷰 라벨과 동일).
- 표 위 「MEMBERS 1 활성 / 1」이 표의 이름이자 `<caption>`이다.

### (c) Board/Graph 위 — 문맥 띠 (표 제거)

```
● Section9 하네스 시스템 · section9 · ACTIVE · 멤버 1 · 요청 45      프로젝트 문서 열기 →
────────────────────────────────────────────────────────────── (hairline)
```

- 한 줄. 높이 ~32px(244px → 32px). 아래 hairline 한 줄로 Board와 나눈다.
  **좌측 세로 색 띠 금지 · 배경 색면 금지** — 상태는 앞머리 점이 말한다.
- 「프로젝트 문서 열기」는 doclink 문법(점선 밑줄 mono). 누르면 (b)로 간다.
- `#my-strip`(MY PROJECTS)은 그대로 둔다 — 그것은 "내 범위 고르기"라 축이 다르다.

---

### 3. 상태 표 (전부)

| 상황 | 어디 | 무엇이 보이나 |
|---|---|---|
| **프로젝트 0개** | Docs›PROJECT | 「아직 프로젝트가 없습니다」 + 「+ 새 프로젝트」 버튼 하나. 안내가 아니라 다음 행동 |
| | 헤더 필터 | 「프로젝트: 전체」만. 지금과 같음 |
| | Board 띠 | 없음(자리도 없음) |
| **1개** | 목록 | 1행 + 생성 버튼. **UI를 줄이지 않는다** — 0→1→N에서 구조가 바뀌면 학습이 깨진다 |
| | 헤더 필터 | 사실상 무용이나 유지(같은 이유) |
| **여럿(N)** | 목록 | 최근 활동순 정렬, 20 + 더 보기, 보관된 것은 아래로 접음 |
| **보관됨(archived)** | 목록 | 「+ 보관됨 N개」로 접힘. 펼치면 점·글자 모두 `--c-cancelled`, 잉크 흐림 |
| **멤버 0명** | 문서 뷰 | 「아직 아무도 없습니다 — 이 프로젝트를 볼 수 있는 사람이 없습니다」 + 추가 행이 바로 표 자리에 (지금의 "멤버 없음" 한 마디는 안내일 뿐 행동이 아니다) |
| **멤버 0명 + 등록 사용자도 0명** | 문서 뷰 | 다른 문구다: 「먼저 사용자를 등록해야 합니다」 + Settings›사용자 관리 링크. 후보가 없는데 추가 폼을 보여 주면 막다른 길 |
| **뷰어(viewer/contributor)** | 문서 뷰 | 표는 값만, 편집 컨트롤은 **그리지 않는다**(disabled 회색 나열 금지 — 눌릴 것 같은 거짓 약속). 표 아래 한 줄: 「보기 권한입니다 — 멤버를 바꾸려면 maintainer 이상이 필요합니다」 |
| | 목록 | 「+ 새 프로젝트」 없음 |
| **owner 행 · 내가 owner 아님** | 문서 뷰 | 그 셀렉트만 disabled + 이유 title. 여기만 회색을 허용한다 — "왜 이 한 행만 다른가"에 답해야 하므로 |
| **마지막 owner** | 문서 뷰 | 「− 제거」 disabled + 「마지막 owner는 뺄 수 없습니다」. 서버도 막아야 한다 |
| **비멤버** | 전부 | 문서가 아예 안 보인다(서버 access isolation이 이미 그렇게 한다). 화면은 "없는 것"으로 — 존재를 흘리지 않는다 |
| **만료 임박(≤14일)** | 표 | 만료일 잉크만 `--c-inprogress`. 색면 아님. 툴팁 「만료 14일 이내」(`.soon` 규칙 재사용) |
| **만료됨** | 표 | 상태 열 「만료」 + 행 잉크 흐림. 지금 `.m-exp`가 `--c-blocked`인데, 만료는 고장이 아니다 → `--muted`로 낮출 것 |
| **로딩** | 목록·문서 | 이 화면들은 catalog/projects 부트 공급에 실린다. 공급이 끊기면 `supplyLine()` 기존 경로 재사용 — 새 로딩 문법 만들지 말 것 |
| **저장 실패** | 문서 뷰 | 컨트롤 원복 + 표 아래 실패 줄(붉은 **잉크**, 색면 아님). 지금 `.pi-err` 규칙 그대로 |

---

### 4. 조작별 인터랙션 스펙

### (1) 프로젝트 만들기 — 다이얼로그

- **진입**: Docs›PROJECT 목록 머리 「+ 새 프로젝트」.
- **형식 판정**: 인라인 폼이 아니라 **`s9dlg` 다이얼로그**. 근거 — 입력이 여럿이고
  생성은 문서를 만드는(되돌리기 비싼) 행위다. 인라인 폼은 목록을 밀어낸다.
- **필드는 넷만**: 표시명(필수) · slug(자동 후보, 고칠 수 있음) · 개요(선택) · 고객(선택).
  **담당자 4필드(이름·소속·메일·전화)는 넣지 않는다** — 만들 때 필요한 정보가 아니다.
  만든 뒤 문서에서 채운다. 이 다이얼로그의 결정은 하나다: "이 프로젝트를 무엇이라 부를까".
- slug는 표시명을 치는 동안 후보를 보여 주되(한글은 자동 로마자화하지 않는다) 비면 진행 불가.
  중복이면 그 자리에서 「이미 있는 이름입니다」.
- **만든 사람은 자동으로 owner** — 묻지 않는다. 정해 주고 나중에 바꿀 수 있게.
- **성공 → 방금 만든 프로젝트 문서로 이동**한다. 목록으로 되돌리지 않는다: 갓 만든
  프로젝트에는 할 일(멤버·담당자)이 남아 있고, 그 자리가 문서다.
- 실패 → 다이얼로그 안 실패 줄. 입력값 보존.

### (2) 멤버 추가 — 표의 마지막 행, 즉시 저장

- 후보 셀렉트(아직 멤버 아닌 등록 사용자만) · 권한 · 직무 · 만료일 · 「+ 추가」.
- 확인 창 없음(되돌리기 = 제거가 바로 옆에 있다).
- 후보 0명: 위 상태 표 참조.

### (3) 멤버 빼기 — 확인 창 (예외 인정)

- 규율은 "파괴적 행동에 확인 대신 되돌리기"지만, **여기는 그 예외 조건에 해당한다**:
  제거 즉시 서버가 접근을 끊고 History에 줄을 쓴다 — 5초 취소로 되돌릴 수 있는 상태가
  아니다. 지금의 `s9dlg confirm`을 유지한다.
- 지금 문구가 좋다: 「프로젝트 문서는 그대로 남고, 이 사람의 접근만 끊깁니다」.
- **자기 자신을 뺄 때는 무게가 다르다** → 다른 문구. 초안: 「자신을 빼면 이 프로젝트를
  더 볼 수 없습니다」 (확정은 ux-writer 몫).
- 마지막 owner: 버튼 disabled + 이유.

### (4) 설정 고치기 — 인라인 즉시 저장

- **판정: 「설정 고치기」 버튼 + 다이얼로그가 아니라 값 자리 인라인 편집.**
  근거 ① 대개 한 번에 하나만 고친다(상태를 archived로, 고객명 오타). 다이얼로그는 여섯
  필드를 다 열어 "무엇을 고칠지"를 다시 고르게 만든다. ② 바로 아래 멤버 표가 이미
  인라인 즉시 저장이다 — 한 화면에 저장 규칙이 둘이면 안 된다.
- 실패 시 원복 + 실패 줄(멤버 표의 `post()` 규칙 그대로 재사용).
- **status만 예외 — 확인 창을 준다.** active→archived는 그 프로젝트의 모든 문서가
  목록에서 접히는 큰 사건이다. 셀렉트가 툭 바뀌고 끝나서는 안 된다.
- maintainer 미만: 컨트롤 없이 값만.

### (5) 프로젝트 지우기 — 새로 만들지 않는다

문서 뷰 하단에 「이 문서 치우기 · 보관 · 삭제」가 **이미 있다**. 두 번째 삭제 버튼을
만들면 어느 쪽이 진짜인지 묻게 된다. 일상적 "치우기"는 `status=archived`가 맡는다.

---

### 5. 시각 언어 · 접근성 준수

**금지 준수**
- 색면 하이라이트 없음 — 상태는 7px 사각점 + 글자색 + 대문자 mono 라벨.
- 좌측 세로 띠 없음 — Board 문맥 띠는 아래 hairline 하나로만 나눈다.
- 무채색 미니멀 아님 — 상태색(active/archived/만료임박)이 잉크로 살아 있고, 목록의
  「멤버 · 열린 요청 · 마지막 활동」이 밀도를 만든다.
- 라운드 0 · 그림자 0. 표는 `border-collapse` + hairline 행 구분(현행 `.proj-info table` 그대로).

**타이포**: 메타 라벨·ID·slug·카운트·상태는 전부 mono 10~11px uppercase + letter-spacing.
제목만 본문체 600. 위계는 크기·굵기로, 색은 신호로만.

**배율 불변**: 새 홀수 치수·소수 테두리를 만들지 않는다. 점은 확정된 7px 무대
(`.livedot`/`.cdot`)를 그대로 쓰고 새로 그리지 않는다. 표 행 높이는 4/8 배수.

**접근성**
- 멤버 표에 `<caption>`(「MEMBERS 1 활성 / 1」), `<th scope="col">` — 지금 `scope`가 없다.
- 「− 제거」는 어느 행인지 스크린리더가 모른다 → `aria-label="<이름> 멤버 빼기"`.
- 표 행 안 컨트롤의 터치 타깃 44px 최소 높이 확보 — 특히 `density=compact`에서.
  s9-design 「칠은 과녁이 아니다」: 시각을 맞추려고 과녁을 깎지 말 것.
- 포커스 링은 기존 `.proj-info select:focus` 규칙(보더 잉크색)이 있으나 outline이
  `none`이다 — 키보드 사용자에게 보더 색 변화만으로는 약하다. 2단계에서 재검토 대상.
- 대비: 만료 행을 `--faint`까지 낮추면 4.5:1을 놓친다 → `--muted` 선에서 멈춘다.

**부품의 여섯 얼굴** — 쉼·hover·포커스·눌림·busy·disabled. 이 설계에서 새로 서는
컨트롤은 「+ 새 프로젝트」·「프로젝트 문서 열기」·인라인 메타 편집 셋이다. **hover 칠이
이웃 글자를 파고들지 않는지, 반전 스킨(terminal)에서 피드백이 배경과 같은 색으로
사라지지 않는지**는 정적 캡처로 판정되지 않는다 — 2단계 구현에서 CDP 실포인터로
1·1.25·1.5 배율 캡처 필수.

---

### 6. 2단계로 넘기는 것

- **문구 전량 초안이다.** 「+ 새 프로젝트」·「보기 권한입니다」·「자신을 빼면…」·빈 상태
  두 갈래·확인 창 문구 — ux-writer(형식) · tech-writer(무엇을 옮기고 무엇을 둘지:
  slug·owner·maintainer·contributor·viewer·archived) · translator(옮길 것) 3인 판정.
  특히 **role 넷은 확립 업계어라 그대로 두는 쪽에 무게가 있다**(순우리말 되돌림은
  s9-design 6절이 기계 직역과 같은 결함으로 본다) — 판단은 tech-writer 몫.
- **서버**: 프로젝트 생성 API(`/api/project/add`)와 설정 변경 API(`/api/project/set`)가
  없다. CLI `s9 project add|set`만 있다. architect·backend 몫.
- **미검증**: 프로젝트가 1개뿐이라 N개 목록·보관 접힘·정렬을 실물로 못 봤다. 2단계에서
  더미 프로젝트를 넣고 확인해야 한다.
- **경계 하나**: `.proj-info` CSS를 문서 뷰로 옮길 때 셀렉터를 공통 클래스로 승격할지
  복제할지 — 복제하면 스킨 수만큼 색을 다시 적게 된다(벨트 글리프 때의 그 함정).
  관문은 한 곳이어야 한다.

캡처 4장: Board+패널(1440·900폭) · Settings 2단 셸 · Docs PRJ 문서 뷰 · 좁은 폭 wrap.

[Image: assets/REQ-20260831-026-62x6/04-projinfo.png]
[Image: assets/REQ-20260831-026-62x6/02-settings.png]
[Image: assets/REQ-20260831-026-62x6/05-prjdoc.png]
[Image: assets/REQ-20260831-026-62x6/06-narrow.png]

### 2026-08-31T22:51:33+09:00 subagent (agent: sub:designer:a9eb556d, by nicehugepark)

`[2026-08-31 23:05:12 KST - designer]`

### 결론

**전제부터 뒤집힌다 — 프로젝트 관리 화면은 이미 절반이 지어져 있다.** 헤더 필터에서 프로젝트를 고르면 Board 위에 `#proj-info` 패널이 서고, 그 안에 **고객·현업 담당자·멤버 표(권한·직무·참여일·만료·상태) + 추가/제거가 전부 배선돼 있다** (`web/app/app.js:152-275`, `web/css/header.css:163-235`, 서버 `/api/project/member`, `/api/project/member/rm`).

진짜 결함은 셋이다.

1. **문이 없다** — 그 패널은 기본값 「프로젝트: 전체」를 특정 프로젝트로 바꿔야만 나타난다. 기능은 있는데 아무도 마주치지 않는다.
2. **한 사물에 얼굴이 둘인데 양쪽 다 반쪽** — Board 위 패널은 멤버·고객을 보여주되 History가 없고, Docs의 PRJ 문서 뷰는 status·summary·History를 보여주되 **멤버도 고객도 안 그린다**.
3. **관리 도구가 작업 화면을 밀어냈다** — 실측: 1440×900에서 패널이 **244px**을 먹어 Board 컬럼 머리가 y=169 대신 y=413에서 시작한다(뷰포트 27%, 카드 두 장). 900×760에서도 190px.

빠진 것은 **생성 흐름 · 목록 · 설정 편집** 셋뿐이다.

### 자리 판정 — 권고 C: 새 탭 0, 새 화면 0

| 대안 | 판정 |
|---|---|
| A. 새 탭 「Projects」 | **탈락**. 지금 탭 일곱은 전부 동사(옮긴다·읽는다·본다…)인데 명사 하나가 끼면 축이 깨진다. 수명에 한 번 쓰는 일에 상시 자리를 주는 거래. 실측: 900px에서 툴바가 이미 3행으로 wrap — 여덟 번째 탭은 브랜드 행마저 민다. 얼굴이 셋이 된다 |
| B. Settings 「프로젝트」 섹션 | **탈락**(가장 아깝다). 2단 관리 셸이 공짜고 사용자 관리와 대칭이 곱다. 하지만 문서 밖에 서면 History·백링크·Graph 노드를 버리고 시작하는데 **그것들이 바로 관리 정보다**. Settings는 헤더 필터가 안 걸리는 섬이다 |
| **C. 문서가 집** | **권고**. 목록·생성 = Docs 좌측 타입 진입점 `PROJECT`(이미 목록이다, 머리에 「+ 새 프로젝트」만 얹음) / 상세·멤버·설정 = PRJ 문서 뷰 / Board 위 패널 = 244px 표를 32px 한 줄 문맥 띠로 축소 |

왕복 우려에 대한 답: Docs는 좌측 목록 + 우측 뷰어 2단이라 **목록↔상세가 같은 탭 안**이다. B가 오히려 Settings→Docs 점프를 만든다.

세 화면 모두에서 「한 화면 한 결정」이 선다 — Board: 무엇을 옮길까 / 목록: 어느 프로젝트인가 / 문서: 이 프로젝트를 어떻게 고칠까.

### 인터랙션 판정 셋

- **생성 = 다이얼로그**, 필드는 **넷만**(표시명·slug·개요·고객). 담당자 4필드는 뺀다 — 만들 때 필요한 정보가 아니다. 만든 사람은 자동 owner(묻지 않음). 성공하면 목록이 아니라 **방금 만든 문서로 이동**한다(할 일이 남았다).
- **설정 = 인라인 즉시 저장**(다이얼로그 아님) — 대개 하나만 고치고, 바로 아래 멤버 표가 이미 인라인이라 한 화면에 저장 규칙이 둘일 수 없다. **status만 확인 창**(archived는 전 문서가 접히는 사건).
- **멤버 빼기 = 확인 창 유지** — "확인 대신 되돌리기" 규율의 예외 조건에 해당한다(서버가 즉시 접근을 끊고 History를 쓴다). 단 자기 자신을 뺄 때는 문구가 달라야 한다.
- **삭제 버튼은 만들지 않는다** — 문서 뷰 하단 「치우기·보관·삭제」가 이미 있다.

### 상태 스펙에서 고칠 것

- 지금의 「멤버 없음」은 안내일 뿐 행동이 아니다 → 추가 행을 표 자리에 바로. 단 **등록 사용자도 0명이면 다른 문구**(막다른 길 방지).
- 뷰어 권한: 편집 컨트롤을 disabled 회색으로 나열하지 말고 **그리지 않는다**. 예외는 owner 행 하나(왜 이 행만 다른가에 답해야 하므로).
- `.m-exp`가 지금 `--c-blocked`(붉은색)인데 **만료는 고장이 아니다** → `--muted`로 낮출 것.
- 0→1→N에서 UI 구조를 바꾸지 않는다(1개일 때 필터가 무용해도 유지 — 학습이 깨진다).

### 2단계로 넘기는 것

- **문구는 전량 초안** — 특히 role 넷(owner/maintainer/contributor/viewer)은 확립 업계어라 그대로 두는 쪽에 무게가 있으나 tech-writer 판정.
- **서버에 없는 것**: 생성 API·설정 변경 API. CLI `s9 project add|set`만 있다.
- **미검증**: 프로젝트가 1개뿐이라 N개 목록·보관 접힘·정렬을 실물로 못 봤다.
- **함정 경고**: `.proj-info` CSS를 문서 뷰로 옮길 때 복제하면 스킨 수만큼 색을 다시 적게 된다 — 관문은 한 곳이어야 한다.
- 새로 서는 컨트롤 셋(「+ 새 프로젝트」·「문서 열기」·인라인 편집)의 hover/포커스는 정적 캡처로 판정 불가 — CDP 실포인터 + 1·1.25·1.5 배율 캡처 필수.

설계 노트와 캡처 4장(Board+패널 · Settings 2단 셸 · PRJ 문서 뷰 · 좁은 폭 wrap)을 REQ 문서에 붙였다: `/home/sjpark1/section9/vault/requests/2026/08/REQ-20260831-026-62x6.md`. 코드는 건드리지 않았고 git 조작도 없다.

### 2026-08-31T22:52:14+09:00 response (agent: lead:claude-fable-5, by nicehugepark)

G0 종합(4인 검토 확정): 방향 = 노출 부재 해소, 새 탭 0·새 화면 0(designer C안). ① 목록·생성 = Docs 좌측 PROJECT 타입 진입점 + 「+ 새 프로젝트」(다이얼로그 4필드: 표시명·slug·개요·고객, 성공 시 그 문서로 이동, 생성자 자동 owner) ② 상세·멤버·설정 = PRJ 문서 뷰(기존 #proj-info 멤버 패널을 문서 뷰로 통합 — CSS 복제 금지, 관문 한 곳) ③ Board 위 244px 패널 → 32px 문맥 띠 ④ 설정 인라인 즉시 저장(status 만 확인 창), 멤버 빼기 확인 창 유지, 만료색 --muted. 서버 = do_project_add/do_project_set 추출 + POST /api/project/add·set + history_tail(신규 ~95줄), 권한 확정: 생성 = 등록 사용자 + 생성자 owner + admin 우회 / 설정 manage / status 는 own(독스트링-구현 불일치 해소) / set 의 History 미기록 교정. 폴링 계약: /api/projects 는 벨트 밖(탭 진입 1회 + 변이 후 재조회). 실행 = PM 분해대로 A(API) ∥ B1(화면 골격, 신규 project.js/css 격리) ∥ C(문구 4인) → B2(실연동) → D(QA) → parent review. 게이트: G1 TDD 실측 → G2 스킨 캡처+4인 스탬프 → G3 회귀 green

### 2026-08-31T22:54:22+09:00 subagent (agent: sub:subagent:acb62273, by nicehugepark)

Reading REQ-20260831-026-62x6.md notes

### 2026-09-01T11:35:28+09:00 response (agent: lead:claude-fable-5, by nicehugepark)

리드 조정(a8186437 이어받음) — ① B트랙 마감: 028·030 done(QA 12항 PASS·전체 회귀 exit 0·D1 은 97d379f 수정, goal 근거는 각 문서 note). ② 관측② 판정: 보관 프로젝트가 헤더 「프로젝트 고르기」에 구분 없이 남는 것은 G0 「필터 무용이어도 유지」 판을 따른다 — 화면이 약속한 문장이 없어 거짓 없음, PO AC 문면(배정 대상 제외)과의 간극은 구분 표시가 필요해지는 시점에 후속 REQ 로. ③ 026 review 보류: 오전 자동 재작업(e9942370)이 시작한 G0′ Projects 탭 작업(web/index.html 탭·web/app/project.js·tests/test_project_tab.py, const.js 주석의 「두 번째 반려」 근거)이 미커밋·미기록 상태이고, 사용자가 11:30 깨운 워커(38ad98c8 재개)가 본 저장소에서 이어받는 중 — 그 작업이 착지한 뒤 026 을 review 로 올린다. 리드는 충돌 회피를 위해 web/app/project.js 계열에 손대지 않는다.

### 2026-09-01T13:00:36+09:00 tdd (agent: lead:claude-fable-5, by nicehugepark)

TDD 시나리오 (G0′ Projects 탭 — tests/test_project_tab.py 20건, 전체 스위트 exit 0 실측 2026-09-01 12:58)

정상:
- [x] 탭 여덟이 확정 순서(board…terminal·projects·settings)로 서고 라우터 TABS 와 일치
- [x] 탭을 누르면 2단 셸(좌 목록·우 문서 판)이 서고 만들기 손잡이가 보인다
- [x] 행을 고르면 우측에 PRJ 문서 판(멤버 표·History)이 서고 주소가 #projects/PRJ-… 를 싣는다
- [x] 실브라우저 1440·1280·900 에서 탭 줄이 한 줄을 유지

경계:
- [x] 헤더 프로젝트 필터는 목록을 좁히지 않고 「◂ 보는 중」 표식만 세운다(렌즈≠집)
- [x] 행 선택이 전역 필터를 바꾸지 않는다
- [x] 다른 탭에서 읽던 비-프로젝트 문서를 들고 오면 우측은 선택 안내로 선다

실패:
- [x] 문서 종류 목록(TYPE_ORDER·종류 셀렉트·Docs 진입점)에 project 가 없다 — 문은 하나
- [x] 선언 안 한 종류는 Docs 목록에 동적으로 붙지 않는다(1차 반려 근원 제거)
- [x] 탭이 문서 판을 제 손으로 다시 그리지 않는다(prjPanelHTML 호출 금지 — 얼굴 불변)

회귀:
- [x] Docs 얼음 조건이 주인 이름(data-pane) 붙은 셀렉터로 유지(REQ-20260828-009-62x6)
- [x] 배경 갱신 편집 가드·docFresh 구분 유지, 15초 폴이 손을 밀지 않는다
- [x] 전체 스위트 재실행 exit 0 (test_port_pool 단독 재검 29/29 포함)

## History
- 2026-08-31T22:42:40+09:00 created by nicehugepark (status: open)
- 2026-08-31T22:43:07+09:00 status: open -> in-progress (by nicehugepark) — 1단계 정의·설계 착수 — PO(요구·범위)·architect(데이터·API)·designer(화면 구조)·PM(분해·순서) 병렬, 리드 종합 후 2단계 구현(backend·frontend·ux/tech/translator·QA)
