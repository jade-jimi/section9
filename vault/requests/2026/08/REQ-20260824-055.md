---
id: REQ-20260824-055
type: request
title: 프로필 이메일·깃헙 확장
summary: 사용자 프로필에 회사 이메일 N개(emails[])·github(개인)·github_org(조직) 필드 추가 — user add/update CLI·API·Settings UI, 미기재 시 세션 시작·Settings에서 입력 촉구(반드시 입력 유도)
goal: emails 복수·github/github_org 분리 저장이 CLI·API·UI에서 되고, 미기재 사용자는 세션 시작 시 촉구를 받는다(테스트 검증)
status: done
size: M
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: 104b4fe3
agents: ["lead:claude-fable-5", "frontend-developer", "subagent"]
project: section9
children: ["REQ-20260824-056"]
tags: ["auto-audit", "sync", "session", "test"]
created: 2026-08-24T21:43:36+09:00
status_since: 2026-08-24T22:00:31+09:00
updated: 2026-08-24T22:04:36+09:00
---

## Original

개인설정에 회사 이메일 n개와 github 계정을 반드시 입력하게 해야겠네 그리고 github도 개인 깃헙과 조직 github도 나눠서 설정할 수 있어야겠네. 그리고 코드오너에는 nicehugepark으로 해줘

## Notes

### 2026-08-24T21:44:31+09:00 tdd (by nicehugepark)

### TDD 시나리오
- [ ] E1. user add 확장: --email 복수 지정(emails 리스트), --github, --github-org 저장(profile.md frontmatter)
- [ ] E2. user update: emails 전체 교체·github/github_org 개별 갱신, 잘못된 형식(공백 계정 등) 거부
- [ ] E3. CLI 표시: user show/ls 또는 config에서 확인 가능
- [ ] E4. API: /api/user/add·update가 동일 필드 처리(본인 또는 admin), /api/users에 노출
- [ ] E5. 미기재 촉구: emails 또는 github 미기재 등록 사용자의 세션 시작(digest) 컨텍스트에 입력 촉구 1회 주입
- [ ] E6. 회귀: 기존 email 단수 필드 하위 호환(기존 프로필 파손 없음), 전체 스위트 OK
- [ ] E7. UI: Settings 프로필 편집(이메일 목록 추가/삭제·github 2종) + 미기재 배지 — frontend 위임, 실브라우저 검증

### 2026-08-24T21:48:06+09:00 response (by nicehugepark)

### 백엔드 완료 (E1~E6)
- [x] E1. add: --emails 복수·--github·--github-org 저장(@ 접두 제거, frontmatter json)
- [x] E2. update: emails 전체 교체·개별 갱신·형식 거부(잘못된 메일/계정)
- [x] E3. CLI: user update 액션 신설, 변경 필드 출력
- [x] E4. API: update 반영·users 노출 (서버 실검증)
- [x] E5. 미기재 촉구: digest에 '⚠ 프로필 미완성' — 채우면 소멸
- [x] E6. 하위 호환: email 단수 필드 인정
- [ ] E7. Settings UI — frontend 워커 진행 중
테스트 5/5 + 전체 스위트 21종 OK. 커밋 푸시済. 실적용: nicehugepark emails=회사메일 1건·github=nicehugepark 기입(github_org 미정 — 촉구가 다음 세션에 뜸).

### 2026-08-24T21:48:21+09:00 response (agent: lead:claude-fable-5, by nicehugepark)

진행 상황입니다.

**CODEOWNERS — 완료(REQ-056 done, 커밋 114c846 푸시).** 5개 보호 경로 전부 `@nicehugepark`로 지정했습니다. 실효를 가지려면 GitHub Settings → Branches → main에서 "Require a pull request" + "Require review from Code Owners"를 켜셔야 합니다(가이드 8장 절차와 동일) — private 전환하실 때 함께 켜시면 됩니다.

**프로필 확장 — 백엔드 완료(커밋 9cd1828 푸시), UI 작업 중.**
- 저장 구조: `emails`(회사 이메일 N개, 형식 검증·중복 제거), `github`(개인), `github_org`(조직) — 요청하신 분리 그대로입니다
- CLI: `s9 user update <이름> --emails a@corp.com,b@corp.com --github 계정 --github-org 조직계정` (add 시에도 동일 옵션)
- **"반드시 입력" 강제**: 미기재 사용자는 세션 시작 시 digest에 `⚠ 프로필 미완성` 촉구가 뜨고 기입 명령이 안내됩니다. 리드는 사용자가 값을 말하면 즉시 채우도록 지시받습니다. Settings UI(이메일 목록 편집·미기재 ⚠ 배지)는 frontend 워커가 진행 중입니다
- 테스트 5종 + 전체 스위트 21종 통과

본인 프로필에는 회사 이메일(claude01.pfe@example.invalid)과 개인 GitHub(nicehugepark)을 채워 뒀습니다. **조직 GitHub 계정은 아직 비어 있습니다** — 계정명(예: 회사 org)을 알려주시면 채우겠습니다. UI 워커 완료 보고가 오면 검증 후 055를 마감합니다.

### 2026-08-24T21:49:20+09:00 response (agent: lead:claude-fable-5, by nicehugepark)

**브랜치 보호 활성화 완료** — gh API로 직접 켰고 설정 조회로 확인했습니다.

적용된 규칙(main 브랜치):
- **Require a pull request + Require review from Code Owners(승인 1명)** — 이제 bin/·web/·harness/·.github/·.gitignore 변경은 code owner(@nicehugepark) 승인 없이 main에 못 들어갑니다
- **enforce_admins=false** — 관리자(본인)는 우회 가능하게 뒀습니다. 지금처럼 리드가 main에 직접 푸시하는 운영을 유지하기 위해서입니다(방금 push로 정상 동작 확인). 협업자가 생기면 그들에게만 게이트가 걸립니다

주의 하나: **리포를 private으로 전환하면 무료 플랜에서는 branch protection이 비활성화될 수 있습니다**(private 리포 보호는 GitHub Pro 필요). 전환하실 때 설정이 유지되는지 확인이 필요하고, 풀리면 알려주시면 대응 방안(Pro 또는 조직 리포 이전)을 정리하겠습니다. 문서에도 기록해 두었습니다.

Settings 프로필 UI 워커는 계속 진행 중입니다.

### 2026-08-24T21:49:54+09:00 tdd (agent: frontend-developer, by nicehugepark)

### E7 (UI) TDD 시나리오 — frontend-developer
- [ ] E7.1 렌더: Settings>내 계정 폼에 회사 이메일 목록(행 추가/삭제)·github(개인)·github_org(조직) 입력이 뜬다 (emails 없고 legacy email만 있으면 목록에 시드)
- [ ] E7.2 저장: '프로필 저장' 1회로 emails[]·github·github_org 일괄 POST /api/user/update, 성공 시 재조회 반영
- [ ] E7.3 실패 경로: 서버 400(형식 오류)이 alert 팝업 없이 폼 인라인 mono로 표시
- [ ] E7.4 미기재 배지: emails(legacy email 포함)·github·github_org 중 빈 필드 있으면 Settings 탭 ⚠(mono·앰버) + 입력에 '필수 권장' 안내, 전부 기재 시 소멸
- [ ] E7.5 admin: 사용자 관리에서 타 사용자(e7test) 동일 폼 편집·실저장 (profile.md 반영)
- [ ] E7.6 L0/회귀: Settings 입력 중 15s 폴링(catalog 변경)이 폼을 재생성하지 않아 값 보존; 기존 스킨/tone/pref/사용자 관리 무영향·JS 예외 0

### 2026-08-24T21:52:49+09:00 response (agent: lead:claude-fable-5, by nicehugepark)

외부기억에서 찾았습니다 — 컨텍스트가 아니라 vault 검색으로 답합니다.

**최초 디자인 요청: 2026-08-23 오전 10시 51분, REQ-20260823-006** ("스트림 개선/설치 체계/디자인")의 일부였습니다. 스트림 KST 표시·설치 체계 등 여러 요청이 섞인 프롬프트였고, 디자인은 "추가로"로 시작하는 마지막 단락이었습니다. 원문 그대로는:

> "추가로, 디자인이 대시보드 디자인이 너무 허전하다. frontend-design 스킬을 사용하고 훨씬 더 ux 지수가 높은 화면을 만들어줘. 지금은 색감이나, 박스 모양 같은 요소들의 디자인도 그렇지만, 요청이 늘어날 경우 무제한으로 화면에 보이게 된다는 것도 문제다. 같이 고민하고 방법을 찾도록 해."

이 문서의 History를 보면 당일 11:00 리뷰 → 11:09 "사용자 확인(일단 알겠다), 디자인 후속은 REQ-007에서"로 종결됐고, 이후 계보가 이어집니다: 007(디자인 2차) → 008(Obsidian풍 그래프 3차) → 012(디자인 언어 교체) → 014(스킨) → 016·017·025 → 077(스킨 다채화·glass 부활, 어제) → 037/040(Terminal 탭, 오늘). "무제한으로 화면에 보이는 문제" 지적은 지금의 목록 제한("+N개 더 보기") 규칙의 기원이기도 합니다.

점검 결과로 말씀드리면: 이 답은 제 대화 컨텍스트가 아니라 `s9 search 디자인 --body` → 문서 Original 섹션에서 나온 것입니다. 오늘 컨텍스트 컴팩션이 여러 번 있었지만 원문·시각·전이 이력이 전부 문서로 남아 있어 복원됐습니다 — 외부기억이 설계 목적대로 동작하고 있습니다.

### 2026-08-24T21:54:55+09:00 frontend (by nicehugepark)

E7 진행: 폼 렌더·미기재 배지·인라인 400 확인. L0 폴링 보존 테스트 중.

### 2026-08-24T21:57:27+09:00 frontend (agent: frontend-developer, by nicehugepark)

### E7 구현·검증 완료 (web/index.html만 수정, 서버 9909 재시작으로 신필드 반영)

구현:
- showUserForm(내 계정/사용자 관리 공용 단일 경로) 확장 — 회사 이메일 목록(행 추가 `＋ 이메일 추가`·행별 `− 삭제`, DOM만 조작해 타 입력 보존), github(개인)·github_org(조직) 입력. legacy email 단수는 emails 비어 있으면 목록에 시드(저장 시 emails로 이관), 더 이상 전송하지 않음(서버 미전달=무변경).
- 저장 버튼 1개(프로필 저장) → /api/user/update 일괄 POST {name, display, emails[], github, github_org(, role)}. 400은 postJSONRaw로 받아 버튼 옆 인라인 mono(.ferr, --c-blocked)로 표시 — 팝업 금지. 기존 postJSON은 Raw 위임으로 리팩터(단일 쓰기 경로 유지).
- 미기재 배지: profileMissing()(서버 세션 촉구와 동일 기준 — legacy email도 기재 인정) 기반으로 ① 헤더 Settings 탭에 ⚠(.pbadge, mono·앰버 --c-inprogress, title에 빈 필드 나열) — 모든 탭에서 보임, ② 내 계정 nav에 "⚠ 미기재 N", ③ admin 사용자 목록 행별 ⚠, ④ 빈 입력 옆 "필수 권장 — 미기재"(.reqhint, mono·앰버).
- L0: refreshCatalog 폴링발 render()를 tab==="settings"일 때 스킵 — Settings 폼 입력 중 재렌더로 값 소실 금지(탭 이탈 시 최신 catalog로 재렌더).

TDD 시나리오 결과 (CDP headless=new, Windows Chrome 9226, 프로필 cdp-prof-e7, 실서버 9909):
- [x] E7.1 렌더 — 이메일 행·github 2종·힌트 렌더 확인(스크린샷 눈검증)
- [x] E7.2 저장 — 1클릭 일괄 저장, users/e7test/profile.md frontmatter에 emails(2)·github·github_org 기록 + audit Note "(by nicehugepark via dashboard)"
- [x] E7.3 실패 경로 — github_org='bad org!!' 저장 → 서버 400 "invalid github_org …"가 인라인 mono로 표시, dialog 이벤트 0
- [x] E7.4 배지 — nicehugepark(조직 GitHub 미기재) 탭 ⚠+title, e7test 행 ⚠이 저장 후 소멸
- [x] E7.5 admin — 사용자 관리에서 e7test 클릭→동일 폼 편집·실저장 (e7test는 실검증 근거로 유지)
- [x] E7.6 L0/회귀 — 입력 중 s9 note로 catalog 변경 강제 후 20s(폴링 1회+) 경과: DOM 노드 동일성·SENTINEL 값·추가 행 전부 보존. display(skin/tone/density select)·pref/config 폼·사용자 등록 폼·Board(5컬럼 14카드) 정상, JS 예외 0, 4xx는 favicon 404(기존)뿐.

한계: 다크 tone·타 스킨에서의 배지 대비는 토큰(--c-inprogress 보정본) 의존으로 별도 눈검증 생략. legacy email 필드 편집 UI는 제거(의도 — emails로 단일화).

### 2026-08-24T21:57:30+09:00 subagent (agent: subagent, by nicehugepark)

Appending E7 note to REQ-20260824-055

### 2026-08-24T21:57:46+09:00 subagent (agent: frontend-developer, by nicehugepark)

E7 완료. `/home/sjpark1/section9/web/index.html`만 수정했고, 실브라우저(CDP) 검증까지 마쳤다.

### 구현

- **프로필 확장 폼** — `showUserForm`(내 계정 / admin 사용자 관리 공용 단일 경로)에 회사 이메일 목록(`＋ 이메일 추가` / 행별 `− 삭제`, DOM만 조작해 다른 입력값 보존), github(개인)·github_org(조직) 입력 추가. legacy `email` 단수 값은 emails가 비어 있을 때 목록에 시드되고 저장 시 emails[]로 이관되며 더 이상 전송하지 않는다(서버는 미전달 필드 무변경).
- **저장 1버튼** — 기존 "프로필 저장"이 `/api/user/update`로 `{name, display, emails[], github, github_org(, role)}` 일괄 POST. 400은 새 `postJSONRaw`로 받아 버튼 옆 인라인 mono(`.ferr`, `--c-blocked`)로 표시 — 팝업 없음. 기존 `postJSON`은 Raw에 위임하도록 리팩터해 쓰기 경로는 하나로 유지.
- **미기재 배지** — `profileMissing()`(서버 세션 촉구와 동일 기준: legacy email도 기재로 인정) 기반으로 ① 헤더 Settings 탭 ⚠(`.pbadge`, mono·앰버 `--c-inprogress`, title에 빈 필드 나열 — 모든 탭에서 보임), ② 내 계정 nav "⚠ 미기재 N", ③ admin 목록 행별 ⚠, ④ 빈 입력 옆 "필수 권장 — 미기재"(`.reqhint`). 색은 신호로만, 라운드 0 — ledger 언어 준수.
- **L0 가드** — `refreshCatalog`의 폴링발 `render()`를 `tab === "settings"`일 때 스킵. 입력 중 재렌더로 폼이 파괴되던 경로 차단.

### 검증 (CDP headless=new, Windows Chrome, 포트 9226, 실서버 9909 재시작 후)

- **렌더/배지**: 스크린샷 눈검증 — 이메일 행·github 2종·앰버 힌트·탭 ⚠(title: "프로필 미기재: 조직 GitHub …") 확인.
- **400 인라인**: `github_org='bad org!!'` 저장 → `✕ invalid github_org: 'bad org!!' (GitHub 계정 형식)` 인라인 표시, dialog 이벤트 0.
- **실저장**: admin으로 e7test 편집 — 1클릭 저장 후 `/home/sjpark1/section9/users/e7test/profile.md`에 `emails: [2개]`, `github: e7test-gh`, `github_org: example-org` + audit Note 기록, 목록 행 ⚠ 소멸. e7test는 지시대로 유지.
- **L0**: 입력 중 `s9 note`로 catalog 변경을 강제하고 20초(폴링 1회+) 경과 — DOM 노드 동일성, SENTINEL 값, 추가한 이메일 행 전부 보존.
- **회귀**: display(skin/tone/density select)·pref/config 저장 폼·사용자 등록 폼·Board(5컬럼 14카드) 정상, JS 예외 0, 4xx는 기존 favicon 404뿐.

기록: REQ-20260824-055에 tdd 시나리오(E7.1~E7.6)와 완료 노트(`--label frontend`) 남김. 상태 전이는 하지 않았다. 한계: 다크 tone·타 스킨에서의 배지 대비는 토큰 보정본 의존으로 별도 눈검증 생략.

### 2026-08-24T22:00:46+09:00 response (agent: lead:claude-fable-5, by nicehugepark)

두 건 모두 마감했습니다(커밋 bf4f284 푸시).

**REQ-057(스트립 선택 표시) done** — 보고하신 그대로였습니다: 본문은 에이전트 뷰로 바뀌는데 스트립 표시는 main에 남는 불일치. 이제 에이전트 행을 클릭하면 그 행이 **●(초록) + `viewing` 라벨 + 배경 틴트**로 활성화되고 main은 ○/흐림으로 바뀌며, esc·←·main 클릭으로 복귀하면 반전됩니다. 10초 주기 스트립 갱신에도 선택 상태가 유지되는 것까지 CDP로 실측했습니다.

**REQ-055(프로필 확장) done** — Settings UI도 완료됐습니다: 내 계정/사용자 관리 폼에서 **회사 이메일 목록(행 추가/삭제)·개인 GitHub·조직 GitHub**을 한 번에 저장하고, 미기재 필드가 있으면 **Settings 탭에 ⚠ 배지**(어느 탭에서든 보임)와 필드별 "필수 권장" 안내가 뜹니다. 형식 오류는 팝업 없이 인라인 표시, 입력 중 폴링 재렌더로 값이 사라지지 않는 것(L0 원칙)까지 검증됐습니다.

지금 Settings를 여시면 본인 프로필의 **조직 GitHub이 비어 ⚠가 떠 있을 겁니다** — 화면에서 직접 채우시거나 계정명을 알려주시면 됩니다. 리뷰 대기: REQ-018(가이드 개편판), REQ-048(동기화 주기 자동화 여부).

### 2026-08-24T22:04:36+09:00 tdd (by nicehugepark)

### TDD 최종 (기록 정정 — 0/1 표시 결함의 원인이 된 미체크 tdd 노트 개정)
- [x] E1. add: emails 복수·github·github_org 저장
- [x] E2. update: 전체 교체·개별 갱신·형식 거부
- [x] E3. CLI user update 액션
- [x] E4. API update·users 노출
- [x] E5. digest 미기재 촉구(채우면 소멸)
- [x] E6. legacy email 하위 호환
- [x] E7.1 UI 렌더  
- [x] E7.2 일괄 저장(실저장 profile.md 확인)
- [x] E7.3 400 인라인 표시
- [x] E7.4 미기재 ⚠ 배지(기재 시 소멸)
- [x] E7.5 admin 타 사용자 편집
- [x] E7.6 입력 보존·회귀·JS 예외 0
(전부 이전 노트들의 실검증 결과를 현행 세대로 집계한 것 — 새 검증 아님)

## History
- 2026-08-24T21:43:36+09:00 created by nicehugepark (status: open)
- 2026-08-24T21:44:16+09:00 status: open -> in-progress (by nicehugepark) — 백엔드 리드, UI 위임 예정
- 2026-08-24T22:00:31+09:00 status: in-progress -> done (by nicehugepark) — goal 충족: emails 복수·github/github_org 분리(CLI·API·Settings UI 편집·미기재 ⚠ 배지+세션 촉구) — 백엔드 5종+워커 CDP 검증(실저장·400 인라인·입력 보존), 전체 스위트 21종 OK
