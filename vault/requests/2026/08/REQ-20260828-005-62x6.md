---
id: REQ-20260828-005-62x6
type: request
title: 상태가 거짓말하는 구조
summary: 커밋과 전이, 클레임과 착수가 끊겨 있어 보드가 몇 시간씩 거짓을 말한다
goal: 구현을 커밋하면 그 사실이 문서에 자동으로 남고, 착수하지 않은 클레임은 스스로 풀리며, 매 턴 경고가 두 종류로 좁혀져 고칠 명령까지 함께 나온다
status: done
size: M
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: e0b624ca
sessions: ["3da5b9de", "e0b624ca"]
agents: ["worker:rework", "lead:claude-opus-5"]
contributions: [{"actor": "worker:rework", "item": "재작업 착수 판단 — 범위 밖·동시 편집", "started": "2026-08-28T07:29:28+09:00", "ended": "2026-08-28T07:29:28+09:00", "result": "done", "transcript": ""}, {"actor": "lead:claude-opus-5", "item": "`[2026-08-28 07:30:19 KST - lead]`", "started": "2026-08-28T07:30:19+09:00", "ended": "2026-08-28T07:30:19+09:00", "result": "done", "transcript": ""}]
project: section9
relates_why: {}
tags: ["auto-audit", "docs"]
created: 2026-08-28T07:24:15+09:00
status_since: 2026-08-28T07:32:06+09:00
updated: 2026-08-28T07:32:06+09:00
priority: 50
---

## Original

[선행 대화 — 직전 미문서화 메시지]
- (07:17) 077, 078 동작 중인게 맞나?

같은 문제가 발생하지 않게 해야지. 진지하게 검토부터 해. 단순 대응을 하지말고.

## Notes

### 2026-08-28T07:25:37+09:00 decision (by nicehugepark)

### 검토 — 왜 같은 일이 계속 나는가

### 실측

커밋 시각과 문서가 `done` 이 된 시각의 차이다. 커밋 메시지에는 그 REQ 번호가
적혀 있었다 — 필요한 정보는 이미 있었다.

    REQ-045   커밋 21:43   전이 23:03   1시간 20분
    REQ-20260827-074-62x6   커밋 23:05   전이 23:58      53분
    REQ-077   커밋 01:02   전이 07:18   6시간 16분
    REQ-078   클레임 23:57  착수 없음    7시간 30분

넷 다 같은 모양이다. **일은 끝났는데 문서가 몇 시간씩 거짓을 말했다.**

### 원인 셋

**1. 커밋과 전이가 끊겨 있다.** 구현이 끝나는 지점은 커밋이고, 커밋 메시지는
그 REQ 번호를 담고 있다. 그런데 그 둘을 잇는 것이 없어서 사람(나)이 손으로 한 번
더 해야 한다. 손으로 하는 일은 다음 요청이 오면 밀린다 — 오늘 네 번 밀렸다.

**2. 클레임이 착수를 뜻하지 않는다.** `s9 last --add` 는 "내가 하겠다"는 표시일
뿐인데, 보드는 그것만으로 '진행 중'을 그린다. 078 은 클레임하고 7시간 반 동안
아무 일도 없었고 보드는 내내 일하는 중이라고 말했다.

**3. 매 턴 경고가 너무 넓어 안 읽힌다.** `stalled` 는 15분간 안 움직인 것을 전부
싣는다. 디자이너 큐에서 순번을 기다리는 정상 항목까지 섞이니 목록이 길어지고,
길어진 목록은 훑고 넘긴다. **실제로 나는 매 턴 그 경고를 받으면서 넘겼다.**
장치는 울렸고 내가 안 들었다 — 그러니 "다음부터 잘 보겠다"는 답은 답이 아니다.

### 고칠 방향 셋 (이 순서로)

**A. 커밋이 스스로 기록하게 한다.** `post-commit` git hook 이 커밋 메시지에서
REQ 번호를 읽어 그 문서에 `commit` 노트를 남긴다. 자동으로 `done` 까지 옮기지는
않는다 — 커밋이 곧 완료는 아니고, `done` 은 목표 대비 근거를 요구하기 때문이다.
대신 **"커밋은 됐는데 전이가 안 된 요청"** 이라는 또렷한 상태가 생긴다.

**B. 착수 없는 클레임은 스스로 풀린다.** 클레임 뒤 정해진 시간 안에 노트·커밋·
전이 중 아무것도 없으면 클레임을 놓고 보드에서 대기로 돌아간다. 078 은 이 규칙
하나로 정직해진다.

**C. 경고를 두 종류로 좁힌다.** 지금의 넓은 목록 대신 (1) 커밋됐는데 전이 안 됨
(2) 클레임만 하고 착수 안 함 — 둘만, 각각 **고칠 명령 한 줄과 함께** 낸다.
짧고 손에 잡히는 것만 읽힌다.

### 하지 않을 것

- "앞으로 잘 챙기겠다"는 약속. 오늘 그 약속의 실패가 네 건이다.
- 커밋에서 곧바로 `done` 자동 전이. 목표 대비 근거 없이 완료가 찍히면 이번엔
  **거짓 완료**가 쌓인다 — 거짓 진행보다 나쁘다.

### 2026-08-28T07:29:28+09:00 conflict (agent: worker:rework, by nicehugepark)

### 물러남 — 이 요청은 지금 다른 주체가 직접 고치는 중이다

무인 재작업 워커(세션 e0b624ca)로 07:26 에 붙었으나, **적용하지 않고 물러난다.**

### 왜 물러나는가

**1. 고칠 자리가 전부 내 범위 밖이다.** 이 봉투는 수정 범위를 `web/`·`vault/`·
`tests/` 로 한정한다. 그런데 A·B·C 셋의 구현 지점은 전부 `bin/` 과 git hook 이다 —
`bin/s9`(claim_at·claim_dead·commit-note·loose), `bin/s9-install`(post-commit 설치),
`bin/s9-audit-prompt`(매 턴 경고 주입). 범위 안에서 손댈 것이 하나도 없다.

**2. 그 파일들이 지금 이 순간 남의 손에 있다.** 07:26~07:28 사이 관측:

    07:26:31  bin/s9              +104줄 (claim_at·claim_dead)
    07:26:56  bin/s9-install      post-commit hook 설치 추가
    07:27:33  bin/s9              +224줄로 증가, bin/s9-audit-prompt 도 수정됨

세 파일 모두 미커밋 상태로 계속 자라고 있다. 동시 편집 규율상 남의 미커밋 변경이
있는 파일은 덮어쓰지 않는다 — 이 저장소는 그 충돌로 테스트 파일 하나를 잃은 적이
있다(REQ-20260826-021-62x6).

### 지금 들어가 있는 것 (관측 결과, 미커밋)

- **A 커밋이 스스로 기록한다** — `bin/s9-install` 이 `post-commit` hook 을 깔고,
  그 hook 이 `s9 commit-note` 를 부른다. `done` 자동 전이는 하지 않는다(문서가
  하지 않겠다고 한 그대로).
- **B 착수 없는 클레임이 풀린다** — `claim_at` 을 클레임 시각으로 남기고,
  `CLAIM_GRACE`(1800초) 안에 문서가 한 번도 안 바뀌면 `claim_dead` 가 참이 된다.
  판정을 `binding_req_ids` 한 곳에 넣어 화면 live·워처 클레임·멈춤 목록이 함께
  움직인다.
- **C 경고를 좁힌다** — `s9 loose --quiet` 를 새로 만들어 `s9-audit-prompt` 가 넓은
  `stalled` 목록보다 **앞에** 주입한다.

셋 다 문서의 "고칠 방향 셋"과 일치한다. 내가 같은 것을 또 쓰면 덮어쓰기다.

### 반려 사유에 대하여

이 요청이 재작업으로 스폰된 사유는 `drag 이동 [via dashboard]` 였다. 이력을 보면
07:25:47 에 `open -> in-progress` 로 옮긴 것이고, **되돌림이 아니라 착수**다.
워처가 그 전이를 반려로 읽어 워커를 띄웠다. 반영할 지적 내용이 없다.

### 남기는 제안 (범위 밖이라 적용하지 않음)

`tests/` 는 내 범위지만, 대상 API 가 지금 초 단위로 바뀌고 있어 지금 쓰는 테스트는
어느 쪽으로도 근거가 되지 못한다. 구현이 멎은 뒤 다음 세 가지를 덮기를 제안한다:

1. `claim_dead` 경계 — 유예 직전/직후, 클레임 뒤 문서가 바뀐 경우(살아 있음),
   `claim_at` 이 없는 옛 바인딩(판정 안 함). 셋째가 특히 중요하다: 근거 없이 남의
   클레임을 빼앗으면 이번엔 반대 방향으로 거짓이 된다.
2. `commit-note` 멱등성 — 같은 커밋으로 두 번 불려도(hook 재발화·rebase) 노트가
   두 벌 쌓이지 않을 것. `NOTE_DUP_WINDOW_SEC` 억제가 이 경로에도 걸리는지.
3. `loose` 의 좁음 — 정상 대기 항목이 섞이지 않을 것. 넓어지는 순간 C 는 원래의
   `stalled` 와 같아지고, 같아지면 다시 훑고 넘어가게 된다.

### 2026-08-28T07:30:19+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-08-28 07:30:19 KST - lead]`

재작업을 **적용하지 않고 물러났습니다.** 이유를 보고드립니다.

### 1. 반려가 아니었습니다

이 요청이 재작업 워커로 스폰된 사유는 `drag 이동 [via dashboard]` 인데, 이력을 보면 07:25:47 에 `open -> in-progress` 로 옮긴 것입니다. 되돌림이 아니라 **착수**입니다. 워처가 그 전이를 반려로 읽고 워커를 띄웠습니다. 반영할 지적 내용이 없습니다.

### 2. 고칠 자리가 전부 범위 밖이고, 지금 남의 손에 있습니다

이 봉투는 수정 범위를 `web/`·`vault/`·`tests/` 로 한정합니다. 그런데 문서가 정한 A·B·C 셋의 구현 지점은 전부 `bin/` 과 git hook 입니다. 게다가 그 파일들이 제가 붙어 있는 동안 실시간으로 자라고 있었습니다:

```
07:26:31  bin/s9              +104줄 (claim_at·claim_dead)
07:26:56  bin/s9-install      post-commit hook 설치 추가
07:27:33  bin/s9              +224줄로 증가, bin/s9-audit-prompt 도 수정됨
```

셋 다 미커밋 상태였습니다. 동시 편집 규율상 남의 미커밋 변경이 있는 파일은 덮어쓰지 않습니다 — 이 저장소는 그 충돌로 테스트 파일 하나를 잃은 적이 있습니다(REQ-20260826-021-62x6).

### 3. 관측한 바로는 이미 셋 다 들어가 있습니다

- **A** — `bin/s9-install` 이 `post-commit` hook 을 깔고 그 hook 이 `s9 commit-note` 를 부릅니다. `done` 자동 전이는 하지 않습니다.
- **B** — `claim_at` 에 클레임 시각을 남기고 `CLAIM_GRACE`(1800초) 안에 문서가 안 바뀌면 `claim_dead` 가 참이 됩니다. 판정을 `binding_req_ids` 한 곳에 넣어 화면 live·워처 클레임·멈춤 목록이 함께 움직입니다.
- **C** — `s9 loose` 를 새로 만들어 `s9-audit-prompt` 가 넓은 `stalled` 보다 **앞에** 주입합니다.

문서의 "고칠 방향 셋"과 일치합니다. 제가 같은 것을 또 쓰면 덮어쓰기입니다.

### 남긴 것

`s9 note REQ-20260828-005-62x6 --label conflict` 로 위 관측과 제가 하려던 것을 문서에 남겼습니다. 함께 남긴 제안은 — 구현이 멎은 뒤 `tests/` 에서 (1) `claim_dead` 경계, 특히 `claim_at` 없는 옛 바인딩을 판정하지 않는지(근거 없이 남의 클레임을 빼앗으면 반대 방향의 거짓이 됩니다) (2) `commit-note` 멱등성(hook 재발화·rebase 시 노트 두 벌) (3) `loose` 가 정상 대기 항목까지 싣지 않는지 — 셋을 덮는 것입니다.

상태는 `in-progress` 그대로 두었습니다. 리드가 실제로 작업 중이므로 그것이 참인 상태이고, `blocked` 나 `review` 로 옮기면 보드가 다시 거짓을 말합니다. 커밋은 하지 않았습니다.

### 2026-08-28T07:31:43+09:00 commit (by nicehugepark)

커밋 32ceaa6 — 상태가 거짓말하지 않게 (REQ-20260828-005-62x6 · 003)

## History
- 2026-08-28T07:24:15+09:00 created by nicehugepark (status: open)
- 2026-08-28T07:25:47+09:00 status: open -> in-progress (by nicehugepark) — drag 이동 [via dashboard]
- 2026-08-28T07:32:06+09:00 status: in-progress -> done (by nicehugepark) — 세 장치 다 동작 확인: post-commit 이 방금 이 커밋을 두 문서에 남겼고, loose 가 즉시 "커밋됐는데 전이 안 됨"으로 자기 자신을 짚었으며, 클레임 스탬프가 두 입구(전이·last --add) 모두에 남는다. 커밋 32ceaa6, 테스트 12/12
