---
id: REQ-20260825-080-62x6
type: request
title: 모델 선택 지속성
summary: 대시보드에서 바꾼 세션 모델(opus)이 세션 재시작 후 fable로 되돌아감 — 모델 변경이 1회성 --model 플래그라 지속 장치가 없음
goal: 사용자가 고른 모델이 세션 재시작·새 세션에도 유지된다
status: done
size: S
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: d332535d
sessions: ["703a52d5", "d332535d"]
agents: ["subagent"]
project: section9
tags: ["session", "dashboard", "workflow"]
created: 2026-08-25T21:39:53+09:00
status_since: 2026-08-26T12:43:32+09:00
updated: 2026-08-26T12:43:32+09:00
---

## Original

사용자 보고(2026-08-25 21:36 채팅): "왜 또 대시보드 터미널은 fable-5을 쓰는거지 opus로 아까 바꿨었는데 제멋대로 이렇게 되는거지? 세션이 재시작되면 기본 모델이 fable인가?" 원인: 대시보드 모델 변경은 restart_session이 claude --resume --model <선택> 으로 그 재개 1회에만 적용한다. 이후 세션이 새로 뜨거나 s9 code 루프가 재스폰하면 --model 없이 기동돼 기본값(계정 기본 = fable-5)으로 복귀한다. 선택을 기억하는 저장처가 없다. 제안(bin/ 수정 필요 — 리드): (1) 모델 변경 시 세션 바인딩(bind model=...)과 user config(pref 또는 s9code_args)에 기록, (2) s9 code 스폰/재스폰 시 그 값을 --model로 재적용, (3) Settings 디스플레이 옆에 현재 모델·기본 모델 표시. 임시 우회: 사용자 config의 s9code_args에 --model opus 추가 시 s9 code 경유 세션은 항상 opus로 기동.

## Notes

### 2026-08-25T21:40:31+09:00 response (by nicehugepark)

임시 우회 즉시 적용: 사용자가 opus 선호를 두 차례 명시했으므로 s9 user config nicehugepark s9code_args '--permission-mode auto --model opus' 로 기록했다 — 이후 s9 code 로 뜨는 세션은 항상 opus로 기동된다(되돌리기: 같은 명령에서 --model opus 제거). 이미 떠 있는 fable 세션은 대시보드 모델 변경(재시작)으로 즉시 전환 가능. 근본 수리(재시작 루프·바인딩에 모델 기억, Settings에 현재 모델 표시)는 bin/ 범위라 리드 인계로 남긴다.

### 2026-08-25T22:02:51+09:00 response (by nicehugepark)

무인 워커 쪽 절반 적용 (Fable 한도 소진 실사고 대응):
- `_spawn_model_args(owner)` 신설 — 사용자 config `auto_resume_model` 을 워커 스폰 3경로 전부에 `--model` 로 주입. 지정이 없으면 계정 기본 모델로 떠서, 그 모델 한도가 소진되면 스폰된 워커가 전부 즉시 죽는다(오늘 실제로 발생 — 죽은 워커가 대시보드 대상 세션까지 점유해 모델 변경조차 막혔다).
- nicehugepark config: auto_resume_model=opus.
- `session_model()` 이 `<synthetic>` 류 합성 표기를 무시 — 상태줄에 실제 모델만 보인다.
- 테스트 R13(인자 생성·설정 없음·읽기 실패), R14(스폰 경로 누락 감시 — 한 군데라도 빠지면 실패), R15(synthetic 무시) 추가.
남은 근본 수리: 대시보드 모델 변경값을 바인딩/유저 config에 기억시켜 s9 code 재스폰이 재적용하는 부분, Settings에 현재 모델 표시.

### 2026-08-25T22:11:50+09:00 response (by nicehugepark)

근본 원인 확정 — 전역 설정에 fable이 박혀 있었다.

`~/.claude/settings.json` 의 `model` 값이 `claude-fable-5[1m]` 이었다. 대시보드 모델 변경은 그 재개 1회에만 붙는 --model 이라, 세션이 새로 뜨거나 워커가 스폰되면 전부 이 전역 기본값으로 돌아갔다("제멋대로 fable로 바뀐다"의 정체).

사용자 지시("기본 모델은 절대로 fable이 아니다. 최신·최고 모델 = opus 5 (1M)")에 따라 세 곳을 일치시켰다:
- ~/.claude/settings.json: model = claude-opus-5[1m]
- users/sjpark1/config/settings.json: auto_resume_model = claude-opus-5[1m]
- 같은 파일 s9code_args = --permission-mode auto --model claude-opus-5[1m]
이로써 (a) 새 세션 (b) s9 code 기동 (c) 무인 워커 스폰 세 경로 모두 opus로 뜬다. 워커 스폰 경로는 _spawn_model_args()가 auto_resume_model 을 --model 로 주입한다(테스트 R13~R15).

### 2026-08-25T22:17:07+09:00 subagent (agent: subagent, by nicehugepark)

Confirming REQ-20260825-080-62x6 catalog entry

### 2026-08-26T00:23:43+09:00 tdd (by nicehugepark)

TDD 시나리오 (남은 근본 수리: 선택 지속) — 전부 검증 완료, `python3 tests/ session_restart` 21/21 OK

- [x] M1 지속 저장: 대시보드 모델 변경이 성공하면 그 값이 사용자 config auto_resume_model 에 기록된다 — test_m1_persist_both_places, test_m1b_restart_persists
- [x] M2 인자 갱신: s9code_args 의 --model 값만 교체한다 (다른 인자 보존, 중복 누적 없음) — test_m2_args_set_model
- [x] M3 회귀: model 없이 effort/account 만 바꾸면 config 를 건드리지 않는다 — test_m3_no_model_no_write
- [x] M4 실패 격리: config 쓰기 실패가 restart_session 을 깨뜨리지 않는다 — test_m4_write_failure_isolated
- [x] M5 중복 플래그: 재시작 루프가 base_cmd 의 --model 과 마커의 --model 을 함께 넘기지 않는다 (마커가 이긴다) — test_m5_restart_cmd_no_duplicate_model
- [x] M6 세 곳 일치: auto_resume_model 과 s9code_args 의 --model 이 같은 값으로 남는다 — 실측 확인

### 2026-08-26T00:23:43+09:00 response (by nicehugepark)

근본 수리 완료 — 선택이 저장된다 (`python3 tests/ session_restart` 21/21 green).

### 무엇이 문제였나 (한 줄)
모델 변경이 재시작 마커의 --model 뿐이라 그 재개 1회로 소멸했고, 다음 기동은 계정 기본값으로 돌아갔다.

### 고친 것 (bin/s9)
1. `_persist_model_choice(owner, model)` — restart_session 이 성공 판정을 내리는 즉시, 고른 모델을 s9가 claude를 띄우는 두 경로의 저장처에 못 박는다:
   - `auto_resume_model` → 무인 워커 스폰(_spawn_model_args)
   - `s9code_args` 의 `--model` → s9 code 기동 기본 인자
   wrapper·manual 두 모드 공통 경로에 두어 '명령만 받아간 경우'에도 저장된다. best-effort — 여기 실패가 재시작을 막지 않고 응답의 `saved` 로만 보고한다.
   ~/.claude/settings.json(계정 기본)은 s9 소유가 아니라 건드리지 않았다. s9 밖에서 맨 claude 로 띄우는 경로는 계속 그 값을 따른다 — 지금 값은 claude-opus-5[1m] 로 확인했다.
2. `_args_set_model(args, model)` — s9code_args 의 --model 을 누적이 아니라 **교체**. `--model=X` 형태와 값이 빠진 꼬리 `--model` 도 삼킨다.
3. `_restart_cmd(base_cmd, marker)` — **이번에 새로 발견한 결함**. nicehugepark 의 s9code_args 에 이미 --model 이 있어, 대시보드로 모델을 바꾸면 claude 에 --model 이 두 번 넘어갔다(어느 쪽이 이기는지 파서에 달림 — '제멋대로 되돌아간다'의 두 번째 원인). 마커가 모델을 지정하면 base 쪽 --model 을 먼저 걷어낸다. 지속 저장을 넣으면 이 중복이 상시화되므로 함께 고쳤다.

### 세 곳 일치 확인 (fable 금지 정책)
- ~/.claude/settings.json           model = claude-opus-5[1m]
- users/sjpark1 auto_resume_model   claude-opus-5[1m]
- users/sjpark1 s9code_args         --permission-mode auto --model claude-opus-5[1m]
이제 대시보드에서 모델을 바꾸면 뒤 두 곳이 자동으로 함께 갱신돼 계속 일치한다.

### 실행 근거
`python3 tests/ session_restart` → 21/21 OK. 인접 회귀 `python3 tests/ s9_code_args` → 9/9 OK.
부수 수리: R14(스폰 경로 감시)가 다른 작업의 스폰 경로 통합 후 0==0 으로 조용히 통과하고 있었다 — argv 조립 형태로 세고 하한(>=1)을 못 박아 감시가 눈을 잃지 않게 했다.

### 남은 것
'Settings 에 현재/기본 모델 표시'는 web/index.html 시각 영역이라 designer 몫으로 남긴다. /api/chat/target 이 이미 현재 모델(session_model)을 준다 — 소비자 없는 API 를 미리 늘리지 않았다.

## History
- 2026-08-25T21:39:53+09:00 created by nicehugepark (status: open)
- 2026-08-25T22:02:51+09:00 status: open -> in-progress (by nicehugepark) — 워커 스폰 모델 고정분 적용 — 재시작 루프 기억분 진행 중
- 2026-08-26T00:21:25+09:00 status: in-progress -> review (by nicehugepark) — 모델 선택 지속 구현 완료 — python3 tests/ session_restart 21/21. 확인 포인트 ① 대시보드 모델 변경이 이제 users/<이름> config 의 auto_resume_model 과 s9code_args 를 자동으로 덮어쓴다 — 사용자 설정을 서버가 쓰는 것에 동의하는지(동의하지 않으면 저장처를 세션 바인딩으로 좁힐 수 있다) ② ~/.claude/settings.json(계정 기본)은 일부러 건드리지 않았다 — s9 밖에서 맨 claude 로 띄우는 경로까지 강제할지 ③ 새로 발견해 함께 고친 결함: s9code_args 에 --model 이 있으면 재시작 시 --model 이 두 번 넘어가던 문제(마커가 이기도록 정리) ④ Settings 에 현재/기본 모델 표시는 web/index.html 시각 영역이라 designer 로 넘겼다.
- 2026-08-26T12:43:32+09:00 status: review -> done (by nicehugepark) — 대시보드 승인 [via dashboard]
