---
id: REQ-20260828-017-62x6
type: request
title: 비밀 바깥 경로 칸
summary: external_secrets_path 가 계정 설정값인데 설정 화면에 칸이 없어 CLI 로만 정할 수 있다
goal: 설정 화면에서 external_secrets_path 를 보고 넣고 지울 수 있고, 폴더가 없으면 그 사실이 화면에서 읽힌다
status: review
size: S
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: b8fda8f9
agents: ["sub:subagent:ac3989af", "sub:designer:a0aa5d99"]
contributions: [{"actor": "sub:subagent:ac3989af", "item": "Attaching captures to REQ-20260828-017-62x6", "started": "2026-08-28T14:48:35+09:00", "ended": "2026-08-28T14:48:35+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/subagents/agent-ac3989af1985f9439.jsonl"}, {"actor": "sub:designer:a0aa5d99", "item": "`[2026-08-28 15:18:12 KST - designer]`", "started": "2026-08-28T14:58:41+09:00", "ended": "2026-08-28T14:58:41+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/subagents/agent-a0aa5d9962793df61.jsonl"}, {"actor": "sub:designer:a0aa5d99", "item": "`[2026-08-28 15:22:41 KST - designer]`", "started": "2026-08-28T15:03:58+09:00", "ended": "2026-08-28T15:03:58+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/subagents/agent-a0aa5d9962793df61.jsonl"}]
project: section9
parent: REQ-20260828-012-62x6
relates_why: {}
tags: ["dashboard", "session", "attached", "png", "image"]
created: 2026-08-28T14:17:31+09:00
status_since: 2026-08-28T14:48:38+09:00
updated: 2026-08-28T15:03:58+09:00
priority: 50
---

## Original

사용자(14:16): "external_secrets_path 계정 설정값이면 settings 화면에 있어야하는데 안보이는데?"

맞는 지적이다. REQ-20260828-012 가 비밀 키 목록은 화면에 냈지만 **바깥 경로를 정하는 칸은 안 냈다** — 그래서 012 가 제기한 문제("CLI 에만 있으면 없는 기능")가 이 한 칸에 그대로 남았다.

주의: external_secret_dir() 은 os.path.isdir 이 참일 때만 그 값을 인정하고 아니면 조용히 "" 로 떨어진다. 경로만 적고 폴더가 없으면 아무 말 없이 무시되므로, 화면이 그 상태를 말해 줘야 한다.

## Notes

### 2026-08-28T14:30:56+09:00 ask (by nicehugepark)

주의: external_secret_dir() 은 os.path.isdir 이 참일 때만 그 값을 인정하고 아니면 조용히 "" 로 떨어진다 라고 하지말고, 디렉토리가 없으면 만들면 되는 것 아닌가?

### 2026-08-28T14:31:34+09:00 decision (by nicehugepark)

설계 변경 (사용자 14:30): "디렉토리가 없으면 만들면 되는 것 아닌가?"

맞다. 앞선 지시("폴더가 없으면 무시된다는 사실을 화면에서 읽히게 하라")는 문제를 설명만 하고 남겨 두는 것이었다. 경로를 적어 저장하면 그 자리에서 만든다.

다만 만드는 자리는 **저장할 때 한 곳뿐**이다:
- 읽는 쪽 `external_secret_dir()` 은 그대로 둔다. 비밀을 읽을 때마다 불리고 커밋 가드·훅에서도 불리는 함수다. 읽기가 디렉토리를 만드는 부작용을 가지면 오타 하나가 엉뚱한 자리에 폴더를 만들고 아무도 모른다. 읽기는 계속 "있으면 쓰고 없으면 조용히 internal 만".
- 새로 만들 때만 0700. 이미 있는 폴더의 권한은 덮지 않는다.
- 만들지 못하면(권한 없음·상위 경로 없음) 저장을 성공으로 처리하지 않고 사유를 화면에 그대로 낸다 — 여기서 조용히 넘어가면 사용자가 처음 물은 그 상황("설정했는데 안 먹는다")이 다시 생긴다.
- 리포 안 경로는 거부한다(리포는 공유·공개된다 — 바깥에 두려는 설정의 뜻이 사라진다). ~ 는 펴서 저장한다.
- "지금 이 경로가 쓰이고 있는지" 표시는 그래도 남긴다: 저장 뒤 폴더가 지워지거나 마운트가 빠지는 경우가 있다.

담당 designer 에 중계했다.

### 2026-08-28T14:39:26+09:00 designer (by nicehugepark)

화면 몫 진행: 바깥 폴더 칸을 비밀 키 판에 냈다. 판정(읽는 중/폴더 없음/쓰면 안 됨/정하지 않음)은 서버 external_secret_state() 한 곳이 내고 CLI 와 화면이 같은 것을 읽는다.

### 2026-08-28T14:48:24+09:00 response (by nicehugepark)

**설정 화면에 바깥 폴더 칸을 냈고, 없는 폴더는 저장할 때 만든다.**

### 무엇이 생겼나
Settings → 내 계정 → 비밀 키 판 아래에 "바깥 폴더" 한 칸. 경로를 적고 `경로 저장`을 누르면 계정 설정 `external_secrets_path` 로 저장된다(스트림 칸이 쓰는 것과 같은 길 — `POST /api/user/config`). 칸 옆 한 줄이 지금 그 경로가 실제로 쓰이는지 말한다.

### 침묵을 깼다 → 이제는 아예 만든다
`external_secret_dir()` 은 `os.path.isdir` 이 참일 때만 그 값을 인정하고 아니면 조용히 "" 로 떨어진다. 처음에는 그 사실을 화면에서 읽히게만 했는데, 그건 문제를 설명하고 남겨 두는 것이라 **저장할 때 폴더를 만들도록** 바꿨다(권한 0700, 새로 만들 때만 — 이미 있는 폴더의 권한은 건드리지 않는다).

- **만드는 자리는 `do_user_config_set()` 한 곳뿐이다.** CLI(`s9 user config`)도 대시보드도 이 함수를 지나므로 두 길이 갈리지 않는다.
- **읽는 쪽은 그대로 두었다.** `external_secret_dir()` 은 비밀을 읽을 때마다·커밋 가드·훅에서도 불린다. 읽기가 폴더를 만드는 부작용을 가지면 오타 하나가 엉뚱한 자리에 폴더를 만들고 아무도 모른다.
- **못 만들면 성공으로 치지 않는다.** 권한 없음·상위 경로 없음은 그 사유가 칸 옆에 그대로 뜨고 설정도 저장되지 않는다.
- **저장소 안 경로는 거부한다** (저장소는 공유·공개된다). `~` 는 펴서 절대 경로로 저장한다.

### 칸 옆 한 줄 (판정은 서버가 낸다)
화면은 `os.path.isdir` 을 볼 수 없고, 두 곳에서 재면 언젠가 답이 갈린다. `external_secret_state()` 한 곳이 내고 `/api/secrets` 가 실어 보내며 `s9 secret ls` 도 같은 것을 읽는다.

| 상태 | 화면 | 언제 |
|---|---|---|
| ok | **읽는 중** · 이 폴더의 파일도 비밀로 함께 읽고 있습니다. | 평상시 (저장하면 곧바로 여기) |
| missing | **폴더 없음** · 지워졌거나 연결이 끊긴 것 같습니다… 경로 저장을 다시 누르면 만듭니다. | 만든 뒤 폴더가 사라짐 |
| inrepo | **쓰면 안 됩니다** · 저장소 안의 폴더입니다… | 설정을 손으로 고친 경우 |
| unset | **정하지 않음** · 저장소 안에 둔 비밀만 씁니다. | 안 적었을 때 |

색만으로 구분하지 않는다 — 앞의 낱말이 먼저 답하고 색(--c-done/--c-inprogress/--c-blocked)은 글자에만 얹힌다. 색면·세로 띠 없음.

### 값을 넣는 곳은 여전히 저장소 안이다
`s9 secret set` 도 화면의 ＋넣기도 **언제나 저장소 안**에 쓰고, s9 는 바깥 폴더를 **읽기만** 한다. 오해하면 값이 엉뚱한 데로 가므로 설명문에 그대로 적었다: "s9 는 이 폴더를 읽기만 합니다: 위의 ＋넣기도 s9 secret set 도 언제나 저장소 안에 씁니다. 밖에 둘 값은 그 폴더에 직접 파일로 넣어 주세요 — 파일 이름이 곧 키 이름입니다."

### 곁가지로 고친 것
목록의 "둔 곳" 칸이 바깥 경로 접두사로 판정하고 있었다. 바깥 경로가 저장소 안(예: `users/`)을 가리키면 **저장소 안의 키까지 "저장소 밖"으로 뒤집혀** 표가 거짓말을 했다(실제로 캡처로 봤다). 내부 폴더와 견주는 방식으로 바꿨다 — CLI 와 화면 둘 다.

### 확인 방법
`?extdbg` 로 손 없이 눌러 본 결과(넷째 캡처): 못 만드는 경로 → 사유가 뜸 · 없던 폴더 → 저장만으로 생기고 곧바로 "읽는 중" · 원래 값으로 되돌림. 값 비노출 규율은 그대로다(경로는 본인 것만, 키 값은 여전히 응답에 없다).

테스트: `python3 tests/ secret_external_path` 23건. 화면 순서대로 캡처 — 정하지 않음 / 읽는 중(어두운 판) / 읽는 중(밝은 판) / 손 없이 눌러 본 결과.

[Image: assets/REQ-20260828-017-62x6/ext-unset.png]
[Image: assets/REQ-20260828-017-62x6/ext-ok.png]
[Image: assets/REQ-20260828-017-62x6/ext-ok-paper.png]
[Image: assets/REQ-20260828-017-62x6/ext-dbg.png]

### 2026-08-28T14:48:35+09:00 subagent (agent: sub:subagent:ac3989af, by nicehugepark)

Attaching captures to REQ-20260828-017-62x6

### 2026-08-28T14:58:41+09:00 subagent (agent: sub:designer:a0aa5d99, by nicehugepark)

`[2026-08-28 15:18:12 KST - designer]`

세 건 모두 끝났고 전부 `review` 로 올렸다. 전체 스위트 **1379건 통과**(skipped 1). 커밋은 하지 않았다.

### REQ-20260828-017-62x6 — 비밀 바깥 경로 칸 → review

작업 중 코디네이터가 설계를 바꿨다("디렉토리가 없으면 만들면 되는 것 아닌가?"). **설명만 하고 남겨 두는 화면**에서 **저장하면 만드는 화면**으로 다시 만들었다.

- Settings → 내 계정 → 비밀 키 판 아래에 `바깥 폴더` 칸 + 판정 한 줄. 저장은 `POST /api/user/config`.
- **만드는 자리는 `do_user_config_set()` 한 곳뿐**이다 — CLI(`s9 user config`)도 대시보드도 이 함수를 지난다. 권한 0700, 새로 만들 때만(이미 있는 폴더 권한은 안 건드린다). `~` 는 펴서 절대 경로로 저장.
- **읽는 쪽 `external_secret_dir()` 은 그대로.** 커밋 가드·훅에서도 불리는 함수라 폴더 생성 부작용을 주지 않았다. 계약 테스트로 못을 박았다(`test_f1_reading_never_creates`).
- 못 만들면 저장을 성공으로 치지 않고 사유가 칸 옆에 뜬다. 저장소 안 경로는 거부.
- 판정 4상(`읽는 중`/`폴더 없음`/`쓰면 안 됩니다`/`정하지 않음`)은 **서버 `external_secret_state()` 한 곳**이 내고 `s9 secret ls` 와 화면이 같은 것을 읽는다. 낱말이 먼저 답하고 색은 글자에만.
- 곁가지 결함 하나를 고쳤다: 목록의 "둔 곳" 칸이 바깥 경로 **접두사**로 판정해서, 바깥 경로가 저장소 안(`users/`)을 가리키면 저장소 안 키까지 "저장소 밖"으로 뒤집혔다(캡처로 발견). 내부 폴더와 견주는 방식으로 바꿨다.
- `?extdbg` 로 실브라우저에서 셋 다 눌러 확인: 못 만드는 경로 → 사유 표시 / 없던 폴더 → 저장만으로 생성 후 "읽는 중" / 원래 값 복구.

새 파일 `/home/sjpark1/section9/tests/test_secret_external_path.py` (24건).

### REQ-20260828-009-62x6 — 읽는 동안 제목을 잃는다 → review

앞 세션이 이미 구현해 둔 것이었다. **손댄 코드 없음** — 내 몫은 눈으로 보는 것이었다.

- 900px 내린 화면에서 `제목 · REQ-20260828-009-62x6 · IN-PROGRESS` 한 줄이 판에 붙어 남는다.
- 목록 얼음은 한 장으로 증명이 안 돼서, **한 페이지를 45초 열어 둔 채** 18초째에 다른 문서(REQ-017)에 노트를 붙였다. 얼음이 없으면 그 문서가 그룹 맨 위로 튀어야 하는데, 0초·45초 캡처의 목록 순서가 글자 하나까지 같다.
- 앞 세션 노트가 걱정한 `test_docs_recent_order` 계약 어긋남은 이미 새 계약으로 옮겨져 있다.

### REQ-20260828-018-62x6 — 미답 대신 답한 비율 → review

**사용자 안 그대로** 갔다: `QUESTION 43/46` 한 덩이, 아직 남았을 때만 질문 잉크로 선다.

- "거슬린다"의 정체는 같은 한 종류를 두 칸이 나눠 세는 것이었고, 합치면 그 이음질이 사라진다.
- 읽는 규칙은 안 섞인다 — 분모(46)가 여전히 옆 칸들과 같은 뜻("질문이 몇 장인가")의 수다.
- 정확한 잔량(3장)은 뺄셈이 되므로 hover/`aria-label`("46장 중 43장 답함 · 3장 남음")로 옮겼다. 이 트레이드오프를 review 노트에 명시해 사용자가 되돌릴 수 있게 했다.
- **다 찼을 때(46/46)는 색을 거두고 회색으로 물러난다.** 표본을 좁혀 실제로 세워 봤다(`3/3`).
- 카드 오른쪽 `미답` 표는 **그대로 뒀다** — 한 장에 대한 판정이지 따로 뽑은 집계가 아니고, 없애면 답함/미답이 색으로만 갈린다. 이유를 문서에 적었다.
- 잉크 반전 스킨(terminal·calm)에서 분수가 배경에 묻히지 않는지 눌러서 확인.
- `test_question_type` V11 을 새 계약으로 옮겼다. 특히 **답한 장수를 "전체 빼기 미답"으로 어림잡지 말 것** — 판정이 3상(답함·미답·모름)이라 그 뺄셈은 틀린다.

### 리드가 알아야 할 것

- 변경 파일: `/home/sjpark1/section9/bin/s9`, `/home/sjpark1/section9/web/index.html`, `/home/sjpark1/section9/tests/test_question_type.py`, 신규 `/home/sjpark1/section9/tests/test_secret_external_path.py`.
- 검증 중 `users/nicehugepark/config/settings.json` 을 여러 번 바꿨다 **원래 상태(미설정)로 되돌려 놨고** 지금 diff 에 남은 `worker_worktree` 한 줄은 내 것이 아니다. 다만 `users/nicehugepark/profile.md` 의 config 감사 줄 6개는 그 검증의 흔적으로 남아 있다 — 손으로 지우는 것이 더 나쁘다고 판단해 두었다.
- 진단 손잡이 둘을 새로 뒀다: `?extdbg`(바깥 폴더 칸) · `?qdbg=<검색어>`/`?qdbg-on`(머리 띠 표본·활성 상태). 둘 다 진짜 컨트롤을 눌러 진짜 숫자를 그린다 — 화면을 속이지 않는다.

### 2026-08-28T15:03:58+09:00 subagent (agent: sub:designer:a0aa5d99, by nicehugepark)

`[2026-08-28 15:22:41 KST - designer]`

두 실패 알림은 **이미 지나간 일**이라 조치할 것이 없다. 확인했다.

- `bbhkj02in`("Update and run V11")·`bpsexgwu2`("Tidy stale comments")는 둘 다 exit 144 — 편집은 끝나고 뒤이은 테스트 실행이 120초 전경 제한에 걸려 백그라운드로 밀렸다가 껍데기 쉘이 정리된 것이다. 편집 자체는 파일에 그대로 들어가 있다: `test_v11_typebar_shows_answered_over_total` 1건, `web/index.html` 의 손본 주석 2곳(terminal 스킨 반전 설명 · `qAnswered` 를 따로 세는 이유) 모두 확인.
- 그 뒤에 돌린 전체 스위트가 **1379건 OK (skipped=1)** 이므로, 두 편집이 반영된 상태로 이미 초록이다.

앞선 보고에서 달라진 것 없다 — REQ-20260828-017-62x6 · 009 · 018 세 건 모두 `review`, 커밋은 리드 몫이다.

## History
- 2026-08-28T14:17:31+09:00 created by nicehugepark (status: open)
- 2026-08-28T14:17:37+09:00 status: open -> in-progress (by nicehugepark) — designer 에 위임 — 012 가 만든 같은 판에 칸 하나
- 2026-08-28T14:48:38+09:00 status: in-progress -> review (by nicehugepark) — 설정 → 내 계정을 열고 아래로 내려 "바깥 폴더" 칸을 봐 주세요. (1) 없는 폴더를 적어도 됩니다. 예를 들어 `~/.s9-secrets` 라고 적고 "경로 저장"을 누르면 그 폴더를 그 자리에서 만들고, 칸 아래 줄이 "읽는 중 — 이 폴더의 파일도 비밀로 함께 읽고 있습니다" 로 바뀝니다. 터미널로 나가서 폴더를 만들 필요가 없습니다. (2) 만들 수 없는 자리를 적으면(예: `/nope/aaa`) 저장되지 않고 그 이유가 칸 아래에 그대로 뜹니다. (3) 저장소 안(예: `~/section9/뭐든`)을 적으면 거부합니다 — 저장소는 다른 사람과 공유되니 밖에 두는 뜻이 없어서입니다. (4) 칸을 비우고 저장하면 설정이 지워지고 "정하지 않음" 으로 돌아갑니다. (5) 그 폴더에 파일을 하나 두면(파일 이름이 곧 키 이름) 위의 비밀 목록에 그 이름이 "저장소 밖" 으로 나타납니다. s9 는 그 폴더를 읽기만 합니다 — 화면의 ＋넣기는 언제나 저장소 안에 씁니다. 여쭐 것: (1)~(4) 의 말이 이해되는지, 그리고 칸 아래 한 줄이 "지금 이게 쓰이고 있나"에 바로 답이 되는지.
