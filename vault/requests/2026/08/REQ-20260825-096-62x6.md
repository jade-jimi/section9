---
id: REQ-20260825-096-62x6
type: request
title: 대시보드 자동 복구
summary: serve가 죽으면 사람이 알아채고 재기동해야 하는 구조 — 감시·자동 재기동과 사인 기록 필요
goal: serve 프로세스가 죽어도 사람 개입 없이 되살아나고, 죽은 사유가 로그에 남는다
status: done
size: M
user: sjpark1
machine: DESKTOP-TEHV1KR
session: cb49b2cd
sessions: ["d332535d", "cb49b2cd"]
project: section9
children: ["REQ-20260826-018-62x6"]
relates: ["REQ-20260826-011-62x6", "QST-20260826-001-62x6", "DOC-20260826-013-62x6"]
tags: ["dashboard", "session", "workflow"]
created: 2026-08-25T22:41:27+09:00
status_since: 2026-08-26T22:13:26+09:00
updated: 2026-08-26T22:13:26+09:00
---

## Original

사용자 지적(2026-08-25 22:19, 22:39): "그래서 대시보드는 절로 복구되는건가?", "대시보드는 왜 죽은거야."

실측: 22:19경 9909 리슨이 사라져 접속 거부 상태였고 리드가 수동으로 다시 띄웠다(22:20, pid 432001). state/serve.log 에는 크래시 트레이스가 없고 과거의 기동 실패(Address already in use) 기록만 있다 — **죽은 사유를 사후에 알 수 없다**는 것이 첫 번째 결함이고, **아무도 되살리지 않는다**는 것이 두 번째다.

범위: (1) serve 종료 사유를 남기는 경로(비정상 종료 시 stderr·시그널 기록), (2) 감시·자동 재기동(사용자 세션과 무관하게 동작해야 하므로 워처/래퍼 계층 결정 필요 — s9 code 래퍼, systemd/user 서비스, cron 중 택일), (3) 대시보드가 재연결 중임을 이미 표시하므로(REQ-032) 자동 복구 성공 시 그 표시가 자연스럽게 걷히는지 확인.

## Notes

### 2026-08-26T13:06:13+09:00 response (by sjpark1)

착수 전 제약 확정 (사용자 지시 2026-08-26 13:02: "이 하네스에서 systemd, cron을 사용 중인건가? 그런건 없기를 바라는데..").

**외부 스케줄러 도입 금지.** systemd(user) 유닛도, cron 항목도 만들지 않는다. 확인한 현재 상태도 그렇다 — 코드 전체에 crontab/systemctl 호출 0건, 이 계정 crontab 비어 있음. bin/s9-doctor 의 "systemd" 문자열은 *부모가 init/systemd/launchd 면 고아로 본다*는 판정용이지 사용이 아니다. (이 머신의 ~/.config/systemd/user 에 harness-tick·claude-oom-guard 유닛이 있으나 section9 것이 아니고 우리 코드가 건드리지 않는다.)

그래서 감시 계층은 **자기 감시 방식**으로 간다:
- `s9 serve` 가 감시자를 분리 기동(setsid + double-fork)해 사용자 세션과 무관하게 살아남는다. 감시자는 자식(serve)이 죽으면 종료 코드·시그널·마지막 stderr 를 남기고 되살린다 — 지금은 죽은 사유를 사후에 알 수 없다는 것이 이 문서의 첫 번째 결함이다.
- 감시자까지 전부 사라지는 경우(WSL 재시작)는 SessionStart 훅이 멱등하게 다시 세운다. 훅은 이미 설치돼 있고 새 의존이 아니다.
- 백오프를 둔다: 즉시 재기동을 무한 반복하면 포트 경합·자원 소모가 사고를 키운다(2026-08-25 망 단절이 그 교훈이다).

기각한 대안과 이유: systemd user 유닛 — 사용자가 명시적으로 배제했고, 이 셸에서 user 버스 연결도 안 된다. cron — 같은 이유로 배제. 둘 다 "의존성 0" 원칙과도 어긋난다.

### 2026-08-26T19:24:23+09:00 tdd (by sjpark1)

hello tdd probe

### 2026-08-26T19:24:49+09:00 tdd (by sjpark1)

(직전 "hello tdd probe" 노트는 CLI 확인용 흔적이다 — 무시하라. 실제 시나리오는 아래.)

### TDD 시나리오 (staff-engineer, 착수 전 작성)

검증 대상은 새 감시자(`s9 serve --supervise` 로 분리 기동되는 guard 루프)와 그 계약이다.
포트는 tests/portpool.free_port() 로만 잡는다 — 사용자 대시보드 9909 불가침.

- [ ] S1. 사유가 남는다: 자식(serve)이 SIGKILL 로 죽으면 state/serve-guard.log(JSONL)에
  rc/signal/ran_sec/tail(직전 출력) 이 남고, state/serve.log 에도 사람이 읽는 한 줄이 남는다.
- [ ] S2. 되살아난다: 비정상 종료 후 감시자가 자식을 다시 띄워 포트가 다시 HTTP 응답한다.
- [ ] S3. 백오프가 늘어난다: 연속 실패 n회의 대기가 단조 증가하고 상한에서 멈춘다.
  건강한 실행(60s 이상 생존) 뒤에는 카운터가 리셋된다. 순수 함수로 고정한다.
- [ ] S4. 정상 종료는 되살리지 않는다: 자식이 exit 0 이면 재기동하지 않고 감시자도 종료한다.
- [ ] S5. 감시자는 하나다: 같은 포트로 두 번 --supervise 해도 감시자는 하나뿐
  (state/serve-guard.<port>.lock 의 flock, 두 번째는 조용히 종료).
- [ ] S6. 살아 있는 서버는 건드리지 않는다: 포트를 이미 누가 점유 중이면 자식을 띄우지 않고
  지켜보기만 한다. 재기동은 오직 "이미 죽은 뒤"에만 일어나므로 진행 중 SSE 를 감시자가 끊지 않는다.
- [ ] S7. 코드 갱신 반영 수단이 아니다 (REQ-20260826-011-62x6 비충돌): 감시자는 살아 있는 프로세스를
  어떤 이유로도 죽이지 않고 소스 mtime/버전을 보지 않는다. 리드의 serve --restart 가 자식을
  SIGTERM 으로 내리면 사유를 기록하고 충분히 기다린 뒤 포트를 다시 재어, 새 서버가 잡았으면
  그냥 지켜본다(경합 수렴).

실행: python3 tests/ serve_guard

### 2026-08-26T19:44:59+09:00 response (by sjpark1)

결함 둘을 자기 감시로 닫았다 — 외부 스케줄러(systemd/cron) 0건 유지.

### 무엇을 만들었나

`s9 serve --supervise` — 감시자를 setsid + double fork 로 떼어 기동한다(사용자 세션·터미널과 무관하게 산다). 감시자는 `s9 serve` 를 자식으로 띄우고 `wait()` 로 지켜보다가,

- **사유를 남긴다**: 종료 코드·시그널 이름·생존 시간·연속 실패 횟수·다음 재기동까지의 대기, 그리고 **그 자식이 죽기 직전 serve.log 에 쓴 구간의 tail**(자식 기동 시점의 파일 offset 부터 읽는다 — 이전 기동의 로그가 섞이지 않는다). 기계용은 `state/serve-guard.log`(JSONL), 사람용은 `state/serve.log` 에 `[serve-guard] …` 한 줄. 사람이 보러 가는 곳에 흔적이 없던 것이 이 문서의 첫 결함이었다.
- **되살린다**: 백오프 1·2·5·15·30·60·120·300초(60초 이상 살았으면 카운터 리셋). 10회 연속으로 자리를 못 잡으면 "포기"를 기록하고 물러난다 — 되살리기로 풀리지 않는 문제(포트 권한·설정 오류)에 감시자가 영원히 도는 것을 막는다. 다음 세션 시작 훅이 다시 세우므로 복구 경로는 끊기지 않는다.

### 지킨 원칙: 살아 있는 서버는 건드리지 않는다

SSE 부작용에 대한 답이다. 재기동은 **자식이 이미 죽은 뒤에만** 일어난다.

- 포트를 누가 쥐고 있으면(내 자식이 아니어도) 죽이지 않고 5초 주기로 지켜보기만 한다.
- 소스 mtime/버전을 보지 않는다 — 자동 재기동이 코드 갱신 반영 수단이 되지 않는다(REQ-20260826-011-62x6 은 별도 판단이다). 테스트가 감시 루프 본문에 `os.kill`·`getmtime`·`terminate(`·`SIGKILL` 이 없음을 소스 수준에서 고정한다.
- `exit 0`(정상 종료)은 사용자 의도로 보고 되살리지 않고 감시도 끝낸다.
- SIGTERM/SIGINT 사망은 "누가 의도적으로 내렸다"로 보고 최소 5초 양보한다 — 리드의 `serve --restart` 가 그 사이 포트를 잡으면 감시자는 그걸 지켜보기만 한다(경합 수렴).

### 증명

- `python3 tests/ serve_guard` — **19 tests OK** (연속 6회 재실행 안정). 루프 판정 15개는 spawn/sleep/clock 주입으로 시간 없이 고정, 통합 4개는 실제로 감시자를 띄우고 자식을 SIGKILL 한다.
- 뮤테이션 확인: 사망 기록 제거·백오프 상수화·clean-exit 재기동 3곳을 일부러 망가뜨리니 5 failures + 1 error — 테스트가 실제로 계약을 잡고 있다.
- 실측 1(경합 수렴, 임시 루트/포트 18896): 감시자 기동 → `serve --restart` 로 자식 SIGTERM → 기록 `{"reason":"signal SIGTERM","retry_in":5}` → 12초 뒤 리스너 1개·serve 프로세스 1개. 서로 죽이지 않았다.
- 실측 2(운영 포트 9909): 사용자의 대시보드를 죽이지 않고 감시자만 붙였다(포트가 busy 라 자식을 띄우지 않는다). 그 뒤 리드가 `serve --restart` 로 서버를 갈았고(pid 474197 → 553848) 감시자는 방해하지 않았다 — 지금 리스너 1개, `GET /` 200. 감시자 pid 545741 가 계속 지켜본다.
- 다른 테스트 영향 확인: stdlib_only · session_attach · shot_hygiene · s9_code_args · server_keepalive · port_guard · liveness · session_restart 전부 OK, 스위트 종료 후 잔여 프로세스 0.

### 바꾼 곳

- `bin/s9`: `_guard_*` / `serve_guard_loop`(`_wait_port_free` 와 `cmd_serve` 사이 신규), `cmd_serve` 진입부의 `--supervise`/`--stop-guard` 분기, 이미 실행 중 분기에서 감시 하 자식만 exit 3 로 구분(감시자가 "정상 종료"로 오해하지 않게), `cmd_code` 의 서버 기동을 감시자 기동으로, 파서 2개 플래그. 리드·backend 가 같은 파일에서 고치는 영역(catalog·워처·통지·PDF)은 건드리지 않았다.
- `bin/s9-audit-session`: `ensure_serve` 가 서버 대신 **감시자**를 보장한다. 포트를 재보는 localhost 연결도 없앴다(감시자가 멱등이라 잴 필요가 없다 — 세션 시작마다 커넥션 하나를 아낀다).
- `bin/s9-install`: 설치 시에도 감시자로 기동.
- `docs/05-web-dashboard.md`: "자기 감시" 절 추가.
- `tests/test_serve_guard.py`: 신규.
- 커밋은 하지 않았다(리드 몫).

### 설계 결정과 기각한 대안

- **fork 대신 Popen 으로 감시자 기동**: `s9 code` 에서 `os.fork` 로 바로 갈라졌더니 argv 를 물려받아 `ps` 에 "s9 code" 로 보였다 — 사람도 s9-doctor 도 감시자를 알아볼 수 없다. 그래서 감시자는 항상 `s9 serve --supervise …` 라는 정직한 argv 로 뜬다.
- **파이프 대신 로그 offset**: 자식 출력을 파이프로 받아 tail 을 뜨면 감시자가 죽었을 때 자식이 파이프에 갇힌다. 자식은 serve.log 에 직접 쓰고, 감시자는 기동 시점 offset 이후를 읽는다.
- **감시자 단일성은 flock**: `state/serve-guard.<port>.lock`. 세션 시작 훅·`s9 code`·`s9-install` 이 매번 `--supervise` 를 던져도 하나만 남는다. (fcntl 없는 윈도우는 잠금 없이 진행 — 그 경우 감시자가 둘이어도 자식은 exit 3 로 물러나 서버는 하나로 수렴한다.)
- **자식 환경에서 S9_SESSION 제거**: 감시자는 세션보다 오래 산다. 죽은 세션 식별자를 물려주면 대시보드가 만드는 문서·전이가 그 세션에 귀속된다(`update_active_reqs` 는 대시보드를 "세션 없음"으로 보는 것이 전제).
- **권한상 열 수 없는 포트(<1024, 비root)는 맡지 않는다**: 재기동만 반복하는 감시자를 남기지 않는다(테스트가 무효 포트로 1을 쓰는 경로가 있다).

### 한계·미검증

- **s9-doctor --sweep 과의 상호작용**: 감시자는 일부러 고아가 되므로, `--sweep` 이 "테스트 풀 포트(18800~) + 고아"를 회수 대상으로 볼 때 걸린다. 운영 포트 9909 는 풀 밖이라 해당 없음. 통합 테스트는 이 경우를 skip 으로 낮춘다(실제로 다른 세션이 시작될 때 감시자가 거둬져 두 번 흔들렸다). 풀 포트에서 대시보드를 운영할 일이 생기면 s9-doctor 쪽 판정에 예외가 필요하다 — 지금은 필요 없다고 판단해 남의 파일을 건드리지 않았다.
- **화면 확인은 하지 않았다**: 자동 복구 성공 시 REQ-032 재연결 표시가 걷히는지는 브라우저 검증이 필요하고, 사용자가 쓰는 9909 를 죽여야 재현되므로 하지 않았다. 감시 기록의 화면 노출과 함께 REQ-20260826-018-62x6(자식 문서)로 분리했다.
- **WSL 재시작 경로**: 감시자까지 사라지는 경우는 세션 시작 훅이 다시 세우는 설계지만, 실제 WSL 재시작으로는 검증하지 못했다(사용자 환경을 내릴 수 없다). 훅 경로 자체는 session_attach·shot_hygiene 테스트가 지킨다.

### TDD 시나리오 커버리지

- [x] S1 사유가 남는다 (rc·signal·ran_sec·tail, serve.log 한 줄까지)
- [x] S2 되살아난다 (루프 + 실제 SIGKILL 통합)
- [x] S3 백오프 증가·상한·건강한 실행 리셋 / 추가: 10회 연속 실패 시 포기
- [x] S4 정상 종료는 되살리지 않는다
- [x] S5 감시자는 하나 (flock) + 분리(세션 분리·부모 소멸 후 생존)
- [x] S6 살아 있는 서버는 건드리지 않는다 (+ 자식 exit 3 는 실패로 세지 않는다)
- [x] S7 코드 갱신 반영 수단이 아니다 (소스 감시 없음 · SIGTERM 양보)
- 추가로 고정: spawn 실패(fork 고갈)에도 감시자는 살아남는다 / 중지 요청은 서버를 두고 감시만 끝낸다 / 잔재 중지 파일이 새 감시자를 즉사시키지 않는다 / 자식 환경에서 S9_SESSION 제거

### 2026-08-26T19:45:43+09:00 tdd (by sjpark1)

### TDD 시나리오 — 검증 완료 (python3 tests/ serve_guard = 19 tests OK, 6회 반복 안정)

- [x] S1. 사유가 남는다 — rc·signal 이름·ran_sec·직전 출력 tail 이 state/serve-guard.log(JSONL)에, 사람용 한 줄이 state/serve.log 에. tail 은 그 자식이 쓴 구간만(이전 기동 로그 불혼입) 확인.
- [x] S2. 되살아난다 — 루프 수준(죽을 때마다 재기동) + 통합(실제 감시자 기동 후 자식 SIGKILL → 새 pid 로 부활, 기록에 signal SIGKILL).
- [x] S3. 백오프가 늘어난다 — 단조 증가·상한 300s·60s 이상 생존 시 카운터 리셋(실패 카운트 [1,2,3,1,2] 와 대기 시퀀스 고정). 추가로 10회 연속 실패 시 "포기"로 물러난다.
- [x] S4. 정상 종료는 되살리지 않는다 — exit 0 이면 재기동 없이 감시 종료("clean-exit").
- [x] S5. 감시자는 하나 — 같은 포트 두 번 --supervise 시 두 번째는 "이미 감시 중"(flock). 분리 확인: 자기를 띄운 프로세스가 죽어도 살아 있고 그 세션에 남지 않는다.
- [x] S6. 살아 있는 서버는 건드리지 않는다 — 포트 점유 중이면 자식을 띄우지 않고 지켜보기만(SSE 보호). 자식이 exit 3(포트는 남의 것)으로 돌아오면 실패로 세지 않는다.
- [x] S7. 코드 갱신 반영 수단이 아니다 — 감시 루프 본문에 os.kill·getmtime·terminate(·SIGKILL 이 없음을 소스 수준에서 고정. SIGTERM 사망은 5초 이상 양보(serve --restart 와 경합 수렴 실측).
- [x] S8. spawn 실패(fork/exec 고갈)에도 감시자는 죽지 않고 백오프로 재시도한다.
- [x] S9. 중지 요청은 서버를 두고 감시만 끝낸다 / 감시 중이 아니면 중지 파일을 쓰지 않는다 / 잔재 중지 파일이 새 감시자를 즉사시키지 않는다.
- [x] S10. 자식 환경에서 S9_SESSION 을 제거한다 — 감시자가 죽은 세션의 식별자를 대시보드에 물려주지 않는다.
- [x] S11. 권한상 열 수 없는 포트(<1024, 비root)는 감시하지 않는다.

## History
- 2026-08-25T22:41:27+09:00 created by sjpark1 (status: open)
- 2026-08-26T19:19:34+09:00 status: open -> in-progress (by sjpark1) — drag 이동 [via dashboard]
- 2026-08-26T19:45:55+09:00 status: in-progress -> review (by sjpark1) — 확인 포인트 3가지. (1) 채택한 감시 방식: s9 serve --supervise 가 감시자를 분리 기동(setsid+double fork)하고, 자식이 죽으면 사유(시그널·종료코드·직전 출력 tail)를 state/serve-guard.log + state/serve.log 에 남기고 백오프(1~300s, 10회 연속 실패 시 포기)로 되살린다. 외부 스케줄러 0건 유지. (2) 재기동 정책이 의도와 맞는지: 살아 있는 서버는 절대 건드리지 않는다 — 재기동은 이미 죽은 뒤에만, exit 0 은 사용자 의도로 보고 안 살리고, SIGTERM 은 5초 양보한다(리드의 serve --restart 와 경합 수렴 실측). 코드 갱신 반영 수단으로 쓰지 않는다(REQ-20260826-011-62x6 비충돌 — 소스 감시 없음). (3) 운영 포트 9909 에 감시자를 이미 붙였다(pid 545741) — 사용자 대시보드를 죽이지 않았고 리드의 재기동도 방해하지 않았다(리스너 1개, GET / 200). 커밋은 하지 않았다(리드 몫). 화면 노출·재연결 표시 확인은 자식 문서 REQ-20260826-018-62x6 로 분리했다. 검증: python3 tests/ serve_guard = 19 OK(6회 반복 안정), 인접 8개 스위트 OK.
- 2026-08-26T22:13:26+09:00 status: review -> done (by sjpark1) — 대시보드 승인 [via dashboard]
