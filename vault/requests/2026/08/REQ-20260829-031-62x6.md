---
id: REQ-20260829-031-62x6
type: request
title: 없는 것을 말하는 자리
summary: 취소 열이 대부분의 날에 참인 문장 한 줄로 한 칸을 늘 차지한다
goal: 취소 열이 비어 있을 때 판에서 자리를 먹지 않고, 취소된 것이 있으면 그때 선다
status: review
size: S
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: 911949e0
project: section9
relates_why: {}
tags: ["index", "assets", "design", "attached", "png", "image"]
created: 2026-08-29T17:16:22+09:00
status_since: 2026-08-29T20:50:03+09:00
updated: 2026-08-29T20:50:03+09:00
priority: 50
---

## Original

사용자(17:15): "이걸 보여줄 필요가 있나?" — 판의 취소 열이 `하루 안에 취소된 요청 없음` 한 줄만 담은 채 한 칸을 통째로 쓰고 있다는 지적. 캡처 첨부.

`web/index.html:5373` 이 그 줄을 낸다.

**이 파일 자신이 이미 규칙을 적어 뒀다** (5384 근처):
"0건이면 아예 안 나온다: 멈춤 0 은 매번 참인 문장이라 곧 안 읽히고, 없는 것을 굳이 말하는 자리가 늘면 있는 것이 묻힌다."
취소 열이 그 규칙의 예외로 남아 있다.

**다른 열과 갈라야 하는 근거**: open·in-progress·review·blocked 는 **비어 있음이 정보**다 — "판정 대기 0" 은 사람이 확인하러 오는 값이다. done 은 "오늘 무엇을 끝냈나" 라 매일 보는 값이다. 취소는 예외적 사건이라 **비어 있는 것이 기본값**이고, 그 기본값을 매일 한 칸으로 말할 이유가 없다.

designer 몫. `web/index.html` 이 지금 다른 워크플로에 잡혀 있어 그 뒤에 잇는다.

## Notes

### 2026-08-29T20:41:21+09:00 designer (by nicehugepark)

### 무엇이 달라졌나

취소 열은 **하루 안에 취소된 것이 있을 때만** 판에 선다. 오늘 이 저장소의 취소
다섯 건은 전부 이틀이 넘어, 지금 판은 네 열(open · in-progress · review · done)이고
남은 열이 그만큼 넓어졌다. 취소된 것이 생긴 날에는 다섯째 열로 그대로 돌아온다 —
그 열의 하루 잣대·3건 접기·개수 표시는 done 과 똑같이 유지된다.

판정은 `colStanding` 한 곳에 있고, "이 열이 지금 담은 것"을 재는 자
(`colLive`)를 `colHTML` 과 나눠 쓴다. 잣대가 두 벌이면 열은 섰는데 안이 비거나,
안에 있는데 열이 안 서는 조합이 생긴다.

### 왜 취소만인가 (감출 것과 감추면 안 될 것)

- `open`·`in-progress`·`review` 는 **비어 있음 자체가 정보**다 — "판정 대기 0" 은
  사람이 확인하러 오는 값이라 비어도 선다.
- `done` 은 "오늘 무엇을 끝냈나"라 매일 보는 값이라 비어도 선다.
- **취소는 예외적 사건이라 비어 있는 것이 기본값**이고, 기본값을 매일 한 칸으로
  말할 이유가 없다. 이 파일이 이미 적어 둔 규칙(열 머리의 `멈춤 0`)의 예외로만
  남아 있던 자리다.
- `blocked`·`draft` 는 이 요청 전부터 "있을 때만" 서 있었다 — 건드리지 않았다.

### 기각한 대안

- **접어서 가는 띠로 남기기** — 기각. 폭이 3분의 1로 줄어도 매일 "없다"고 말하는
  것은 같고, 세로로 눕힌 글자는 이 화면의 어휘가 아니다.
- **드래그를 시작할 때만 열을 띄우기** — 기각. 집어 든 순간 판이 다시 흐르면
  커서 밑의 카드가 움직인다(움직이는 레이아웃은 결함이다).
- **다른 열 폭 고정** — 안 했다. 열은 `flex:1 1 252px` 라 한 칸이 빠지면 남은 열이
  고르게 넓어질 뿐, 빈 자리가 생기지 않는다.

### 딸려 오는 값 하나 (숨기지 않고 적는다)

열이 없는 날에는 **끌어다 취소하는 자리도 없다.** 취소는 문서 화면의
`→ cancelled` 로 늘 갈 수 있고(그 길에는 확인 창까지 있다) 되돌릴 수도 있어서,
매일 한 칸을 내주고 지킬 만큼의 지름길은 아니라고 봤다. 다르게 보시면 되돌리기
쉬운 판단이다 — `colStanding` 한 줄이다.

### 진단

`?cancelfresh[=N]` — 있는 취소 문서의 시각만 오늘로 당겨 **열이 서는 날**을 세운다.
안 보이는 쪽만 확인하고 마치면 감춘 것이 아니라 잃은 것일 수 있어서 둘 다 찍었다.

### 시험

`tests/test_board_cancel_column.py` **9건** 신규 — 오늘 취소가 없으면 안 선다 ·
있으면 선다 · 경계는 done 과 같은 하루 · done 은 비어도 선다 · 살아 있는 열은
그대로 · blocked/draft 규칙 유지 · 판정은 한 곳 · 잣대는 한 벌 · 취소만 예외.

잣대를 이름 있는 자리로 옮기면서 기존 시험 둘이 따라왔다
(`test_board_done_window` · `test_board_counts` — 계약은 그대로고 보는 자리만 옮겼다).

**전체 스위트 `Ran 2158 tests … OK (skipped=1)`, 410초. 회귀 0.**

[Image: assets/REQ-20260829-031-62x6/취소열-없는날-기본스킨.png]
[Image: assets/REQ-20260829-031-62x6/취소열-없는날.png]
[Image: assets/REQ-20260829-031-62x6/취소열-있는날.png]

### 2026-08-29T20:49:29+09:00 commit (by nicehugepark)

커밋 ffb5257 — 클레임이 시계를 되감지 않고, 자리가 사람에게 보인다 (REQ-033·034·030·031·035)

## History
- 2026-08-29T17:16:22+09:00 created by nicehugepark (status: open)
- 2026-08-29T20:08:25+09:00 status: open -> in-progress (by nicehugepark) — drag 이동 [via dashboard]
- 2026-08-29T20:50:03+09:00 status: in-progress -> review (by nicehugepark) — F5 후 Board 를 보세요. 취소 열이 사라져 네 열이 되고 남은 열이 넓어졌습니다 — 하루 안에 취소된 것이 있는 날에만 다섯째 열로 돌아옵니다. 지금 모습을 보려면 http://127.0.0.1:9909/?cancelfresh=2. 다만 열이 없는 날에는 카드를 끌어다 취소하는 자리도 없습니다(문서 화면에서는 늘 됩니다). 넓어진 판이 나으면 승인, 끌어서 취소하는 길이 필요하면 반려입니다.
