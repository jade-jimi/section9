---
id: REQ-20260825-072-62x6
type: request
title: 심미 방향 재정립
summary: 클로드류 틴트 색면 반대 + 무채색 미니멀도 식상 — 색을 면이 아닌 글자·형태로 쓰고 타이포·깊이로 예쁨을 만든다
goal: 보드가 색면 하이라이트 없이도 화사하고 정보가 한눈에 읽힌다
status: done
size: M
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: d332535d
agents: ["subagent", "designer"]
project: section9
tags: ["auto-audit", "design"]
created: 2026-08-25T20:21:38+09:00
status_since: 2026-08-25T21:39:57+09:00
updated: 2026-08-25T21:39:57+09:00
---

## Original

원래 취지는 예쁘고 보기 좋고 보기 쉬운거다. 클로드 순수 디자인은 안예쁘고 식상하다

## Notes

### 2026-08-25T20:37:30+09:00 designer (by nicehugepark)

calm 보드 재설계 착수 — 현행 진단(캡처 calm7-mist.png): ① 카드 첫 줄이 ID라 시선이 참조번호에 먼저 간다 ② 그림자 spread -18px라 깊이가 사실상 0, 카드가 캔버스 위 '흰 얼룩' ③ 메타 4개가 전부 회색 pill이라 제목과 무게 경쟁 ④ 컬럼 헤더가 약해 보드가 '카드 밭'으로 읽힘 ⑤ 색이 7px 점 5개뿐 = 무채색 일변도 ⑥ review 확인요청이 6줄 통짜라 컬럼 리듬 파괴. 설계 4축: (1) 읽기 순서를 flex order로 재배치 — 제목 최상단 → 메타 → 판정 → [ID·경과] 푸터 (2) 색은 면 아닌 잉크로: 컬럼 카운트 큰 색숫자·태그 색글자(hue)·아바타 hue 링·hover 상태색 테두리 (3) 깊이는 톤별 다른 수단(라이트=2단 그림자, 다크=링+면밝기) (4) 8px 리듬·컬럼 flex 268px. hue 색은 명도/채도를 톤별 변수(--hu-*/--av-*)로 빼 라이트·다크 모두 대비 확보.

### 2026-08-25T20:58:59+09:00 designer (by nicehugepark)

calm 보드 재설계 구현 완료 (web/index.html 1128~1320행 calm 블록 전면 교체, CSS 전용 · 마크업/JS 무변경).

[읽기 순서] 마크업은 id→제목→메타→판정이지만 카드에서 먼저 읽혀야 할 것은 제목이다. .card를 flex column으로 만들고 order로 [제목 → 메타 → 판정 → id·경과 푸터]로 재배치. 제목의 padding-right:56px(경과 회피용)도 제거해 줄바꿈이 줄었다.

[색의 위치 — 면이 아니라 잉크] ① 컬럼 카운트를 색 pill에서 19px 상태색 숫자로(오른쪽 정렬이라 컬럼마다 같은 x에 서서 보드 상단에 눈금 리듬) ② 태그는 배경·테두리 없이 해시 hue 글자(#포함) ③ 담당자는 채운 원이 아니라 hue 링 + hue 글자 ④ hover에서 카드 테두리가 상태색으로 ⑤ 활성 탭/필터는 색 틴트가 아니라 잉크 필. 면적을 가진 색은 캔버스의 파랑~청록 저알파 그라디언트뿐.

[깊이] base .board의 흰 패널/보더를 걷어냈다 — 흰 카드가 흰 판 위에 있어 그림자가 죽고 배경 색 공기가 안 보이던 것이 이번 밋밋함의 최대 원인. .docs도 동일 처리. 라이트=2단 그림자, 다크=링+면 밝기로 톤별 수단 분화.

[가독] 컬럼 폭 300→262px로 1440에서 5개 상태 컬럼이 모두 보인다. 확인요청/대기사유는 3줄 클램프(캡션 인라인 색글자)로 컬럼 리듬 유지. 규모(S/M/L)는 담당자 이니셜과 같은 낱글자라 혼동돼 각진 윤곽 칩으로 분리.

[대비] --hu-l 31%/72%, --av-l 32%/76%로 톤별 분리 — 전 hue에서 본문 4.8:1 이상. --faint(2.4:1)를 쓰던 id/경과는 --calm-quiet(muted 82% 혼합, 5.1:1)로 교체.

[부수 결함 2건 수정] (1) 이전 REQ-072 주석 안의 '(--hu-*/--av-*)' 문자열이 CSS 주석을 조기 종료시켜 토큰 블록 전체가 삼켜지고 있었다 — 태그·아바타 hsl()이 전부 무효가 돼 회색으로 떨어진 원인. (2) mist/graphite tone 블록에 --accent-soft/--accent-line이 없어 :root의 라이트 웜그레이(#f0f0ec)를 다크에서도 상속하고 있었다 — 톤별 값 추가.

색 하드코딩 0(컴포넌트 CSS는 전부 토큰/color-mix/hue 파생, hex는 tone 블록에만). prefers-reduced-motion 존중.

### 2026-08-25T20:59:19+09:00 designer (by nicehugepark)

검증(browser-verify): Windows Chrome headless로 라이트·다크 실캡처 + 상태 매트릭스(hover/focus-visible/빈 컬럼/판정 클램프/더보기/승인·반려 hover) 확인, 육안 검토 후 4회 수정 재캡처.
- /tmp/claude-1000/-home-sjpark1-section9/d332535d-290f-46b4-9591-96c2d7c6bddb/scratchpad/calm-final-mist.png (라이트 보드)
- .../calm-final-graphite.png (다크 보드)
- .../calm-final-states-mist.png, calm-final-states-graphite.png (상태 매트릭스)
- .../calm10-narrow.png (1024px 반응형), calm-final-docs.png (Docs 탭)
재캡처로 고친 것: 태그·아바타 색 무효(주석 조기종료), 규모 칩이 아바타와 같은 원이라 혼동, '더 보기' pill이 컬럼 폭으로 늘어남(.cards flex column · align-self 누락), 다크에서 규모 칩 테두리(hairline)가 안 보임, 보드 흰 패널 때문에 카드가 안 떠 보임.

[미해결·리드 확인 필요] 전체 python3 tests/ 는 238개 중 8 fail·68 error인데 전부 HTTP 서버 접속 ConnectionResetError 계열이고, 워킹트리에 다른 세션의 미완 작업(bin/s9 +101줄 — _port_busy/_wait_port_free 포트 프로브 추가, tests/test_instance_init.py 수정, tests/test_s9_sync.py 신규)이 섞여 있다. 같은 증상으로 s9 shot도 간헐 ERR_CONNECTION_RESET이 났다(재시도로 통과). 이번 변경은 CSS 전용이라 무관하며, 대시보드 관련 테스트(test_review_gate/test_term_conn/test_ux_agents/test_spinner_glyphs)는 개별 실행 시 전부 OK. 서버 접속 결함은 bin/s9 작업자 쪽에서 확인 필요.

### 2026-08-25T21:01:14+09:00 designer (by nicehugepark)

대안 비교(선택 근거 — 결정비용·되돌리기·접근성 기준).

[A. 카드 읽기 순서] A1 채택: order로 제목을 첫 줄로, id·경과를 푸터로. A2 기각: id를 그대로 첫 줄에 두고 크기만 줄이기 — 변경비용은 최소지만 시선이 매번 참조번호를 먼저 훑는 구조는 그대로라 '보기 쉬움' 요구를 못 푼다. A3 기각: cardHTML 마크업 자체를 재배열 — 다른 5개 스킨의 레이아웃까지 흔들려 되돌리기 비용이 크다. A1은 calm 블록의 order 선언 4줄이라 되돌리기가 한 줄 단위.

[B. 색을 어디에 둘 것인가] B1 채택: 컬럼 카운트 숫자 + 태그 글자 + 아바타 링 + hover 테두리. 면적 0, 색맹 사용자에겐 상태명 텍스트·모양이 병행돼 색이 유일 신호가 아니다. B2 기각: 카드 상단 4px 상태색 가로 바 — 면이 작아도 '띠'로 읽혀 금지 2에 저촉. B3 기각: 상태별 카드 배경 미세 틴트(3%) — 정확히 금지 1(클로드류 틴트). B4 부분채택: 캔버스 그라디언트는 남기되 파랑~청록 단일 계열로 한정 — 연보라/연주황이 섞이면 반려된 틴트 인상이 재현된다.

[C. 컬럼 폭] C1 채택 262px 고정 basis + flex-grow — 1440에서 5개 상태 컬럼이 모두 보인다. C2 기각: 완전 균등분할(min-width 0) — 과거 반려 이력대로 태그가 잘려 정보가 사라진다. C3 기각: 300px 유지 — done 컬럼이 잘려 워크플로 끝이 안 보인다.

[D. 판정 블록 길이] D1 채택 3줄 클램프 — 컬럼 리듬 유지, 전문은 카드 클릭 시 문서에서(되돌리기 비용 0, 정보 손실 없음). D2 기각: 전문 노출 — 카드 1장이 컬럼 1개 높이를 먹어 보드가 무너진다.

[E. 활성 탭] E1 채택 잉크 필(대비 최대, 색면 0). E2 기각: accent 틴트 pill(기존) — 헤더가 '클로드 디자인' 인상의 근원 중 하나였다.

### 2026-08-25T21:01:21+09:00 subagent (agent: subagent, by nicehugepark)

Appending design alternatives note to REQ-20260825-072-62x6

### 2026-08-25T21:01:52+09:00 subagent (agent: designer, by nicehugepark)

calm 스킨 보드 재설계를 구현하고 라이트·다크 실브라우저로 검증했습니다. 커밋은 하지 않았고 변경은 워킹트리에 있습니다.

### 무엇을 왜 바꿨나

**밋밋함의 진짜 원인은 색이 없어서가 아니라 세 가지 구조 결함이었습니다.**

1. **`.board`가 흰 패널이었다.** base의 `.board{background:var(--panel);border:1px solid var(--border)}`를 calm이 걷어내지 않아, 흰 카드가 흰 판 위에 놓여 그림자가 죽고 배경의 색 공기가 아예 안 보였습니다. 이걸 제거하니 카드가 실제로 뜨고 캔버스 색이 살아났습니다 — 이번 변경 중 시각 효과가 가장 큰 한 줄입니다. `.docs`도 같이 처리했습니다.
2. **카드 첫 줄이 참조번호였다.** 마크업은 `id → 제목 → 메타 → 판정` 순이라 시선이 매번 `REQ-2026...`를 먼저 훑었습니다. `.card`를 flex column으로 만들고 `order`로 **제목 → 메타 → 판정 → [id·경과] 푸터**로 재배치했습니다(마크업/JS 무변경).
3. **태그·아바타 색이 전부 무효였다.** 이전 REQ-072 주석 안의 `(--hu-*/--av-*)` 문자열이 `*/`로 CSS 주석을 조기 종료시켜 그 뒤 토큰 블록이 통째로 삼켜지고 있었습니다. 그래서 `hsl(var(--th) …)`가 전부 invalid → muted 회색으로 떨어졌습니다. 픽셀 샘플링으로 모든 태그가 `rgb(90,100,116)` 하나임을 확인해 잡았습니다.

**색은 면이 아니라 잉크로**: 컬럼 카운트를 색 pill에서 19px 상태색 숫자로(오른쪽 정렬이라 컬럼마다 같은 x에 서서 보드 상단에 눈금 리듬), 태그는 배경·테두리 없이 해시 hue 글자, 담당자는 채운 원이 아니라 hue **링**, hover 시 카드 테두리가 상태색, 활성 탭/필터는 틴트가 아니라 잉크 필. 면적을 가진 색은 캔버스의 파랑~청록 저알파 그라디언트뿐입니다.

**보기 쉬움**: 컬럼 폭 300→262px로 1440에서 5개 상태 컬럼이 모두 보입니다. 확인요청/대기사유는 3줄 클램프(캡션 인라인 색 글자)로 컬럼 리듬을 지키고 전문은 문서에서 봅니다. 규모(S/M/L)가 담당자 이니셜과 같은 낱글자라 혼동돼 각진 윤곽 칩으로 분리했습니다.

**대비**: `--hu-l` 31%/72%, `--av-l` 32%/76%를 톤별로 분리해 전 hue에서 4.8:1 이상을 계산으로 확보했습니다. `--faint`(2.4:1)를 쓰던 id·경과는 `--calm-quiet`(5.1:1)로 교체했습니다. 컴포넌트 CSS의 색 하드코딩은 0이고, hex는 tone 블록에만 있습니다. `prefers-reduced-motion`도 존중합니다.

부수로 mist/graphite tone 블록에 `--accent-soft`/`--accent-line`을 추가했습니다 — 없어서 다크에서도 `:root`의 라이트 웜그레이 `#f0f0ec`를 상속하던 버그입니다.

### 파일과 캡처

- `/home/sjpark1/section9/web/index.html` — calm 블록 1128~1320행 전면 교체, CSS 전용
- `/tmp/claude-1000/-home-sjpark1-section9/d332535d-290f-46b4-9591-96c2d7c6bddb/scratchpad/calm-final-mist.png`
- `.../calm-final-graphite.png`
- `.../calm-final-states-mist.png`, `.../calm-final-states-graphite.png` — hover·focus-visible·빈 컬럼·판정 클램프·더보기·승인/반려 hover
- `.../calm10-narrow.png` (1024px), `.../calm-final-docs.png`

육안 검토 후 5회 고쳐 재캡처했습니다: 태그·아바타 색 무효, 규모 칩이 아바타와 같은 원이라 혼동, `.more` pill이 컬럼 폭으로 늘어남(`.cards`가 flex column이라 `align-self` 누락), 다크에서 규모 칩 테두리가 hairline이라 안 보임, 보드 흰 패널.

### 리드가 봐야 할 것 두 가지

**1. 테스트 전체는 빨간불인데 제 변경과 무관합니다.** `python3 tests/` 238개 중 8 fail·68 error인데 전부 HTTP 접속 `ConnectionResetError` 계열입니다. 워킹트리에 **다른 세션의 미완 작업**이 섞여 있습니다 — `bin/s9` +101줄(`_port_busy`/`_wait_port_free` 포트 프로브 추가), `tests/test_instance_init.py` 수정, `tests/test_s9_sync.py` 신규. 같은 증상으로 `s9 shot`도 간헐 `ERR_CONNECTION_RESET`이 났습니다(재시도로 통과). 대시보드 관련 테스트(`test_review_gate`/`test_term_conn`/`test_ux_agents`/`test_spinner_glyphs`)는 개별 실행 시 전부 OK이고, 제 diff는 CSS 전용입니다. 서버 접속 결함은 `bin/s9` 작업자 쪽에서 확인이 필요합니다.

**2. 빈 컬럼 문구는 손대지 않았습니다.** `EMPTY_COL`의 "비어 있음"은 스킨 공용 JS 상수라 calm만의 문제가 아니고, ux-craft 기준으로는 빈 상태가 다음 행동을 줘야 합니다("여기로 끌어다 놓기" 등). 스킨 범위를 넘어서라 별건으로 남겨뒀습니다.

기록은 `REQ-20260825-072-62x6`에 진단·구현·검증·대안비교 4건을 `--label designer`로 남겼습니다. 대안 비교(읽기 순서 3안, 색 배치 4안, 컬럼 폭 3안, 판정 길이 2안, 활성 탭 2안)와 각 기각 사유는 그 문서에 있습니다.

### 2026-08-25T21:38:58+09:00 response (by nicehugepark)

리드 검증 완료: designer 에이전트 구현을 라이트(mist)·다크(graphite) 캡처로 직접 확인하고 커밋(647ad22). 에이전트가 찾은 핵심은 색이 아니라 구조 결함 3건이었다 — 보드가 흰 패널이라 카드 그림자·캔버스 색이 죽음, 카드 첫 줄이 참조번호라 시선이 번호부터, 이전 주석의 */가 CSS를 조기 종료시켜 태그·아바타 색 토큰이 전부 무효. 대비는 전 hue 4.8:1 이상 계산 확보. 부수로 mist/graphite에 누락돼 있던 --accent-soft/--accent-line도 채워졌다.

## History
- 2026-08-25T20:21:38+09:00 created by nicehugepark (status: open)
- 2026-08-25T20:22:53+09:00 status: open -> in-progress (by nicehugepark) — 색을 글자·형태로 쓰는 방향으로 재설계 착수
- 2026-08-25T21:38:58+09:00 status: in-progress -> review (by nicehugepark) — 확인 포인트(취향 판정, F5 후 calm+mist/graphite): (1) 카드가 캔버스 위에 떠 보이고 제목이 먼저 읽히는지, (2) 태그 색 글자·담당자 링 같은 "면 없는 색"이 원하던 화사함인지, (3) 다크(graphite)에서도 성립하는지. 캡처: scratchpad/calm-final-mist.png, calm-final-graphite.png, calm-final-states-*.png
- 2026-08-25T21:39:57+09:00 status: review -> done (by nicehugepark) — 대시보드 승인 [via dashboard]
