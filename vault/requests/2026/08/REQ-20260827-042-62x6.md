---
id: REQ-20260827-042-62x6
type: request
title: 스트림을 켜고 끌 수 있게
summary: transcript 미러를 사용자별 설정으로 on/off 하고 보관도 사용자별로 — 지금은 전원 강제이고 보존 기간이 없다
goal: 사용자가 설정에서 스트림 보관을 끄고 켤 수 있고, 켜 둔 경우 보관 위치·보존 기간이 사용자별로 정해진다
status: in-progress
size: M
user: sjpark1
machine: DESKTOP-TEHV1KR
session: 3da5b9de
project: section9
derived_from: REQ-20260827-036-62x6
relates_why: {}
tags: ["session"]
created: 2026-08-27T19:36:53+09:00
status_since: 2026-08-27T20:00:30+09:00
updated: 2026-08-27T20:27:03+09:00
priority: 50
---

## Original

사용자 제안: "stream은 개인 설정으로 선택사항으로 써도 되지 않을까? 개인별, 하네스 계정인 user 별로 관리되는게 어떤가? 개인설정에서 끌 수도 있고 켤 수도 있고."

## Notes

### 2026-08-27T19:37:27+09:00 analysis (by sjpark1)

**질문 셋에 먼저 답하고, 제안에 대한 판단을 붙인다.**

**① "모든 문서에서 저 스트림을 쓰는가"** — **아니다.** 코드에서 `streams/` 를 읽는
자리는 셋뿐이다.

    1. `_binding_activity_paths()`  live 초록점 판정의 **보조** 신호
                                    (주 신호는 바인딩의 transcript_path)
    2. `/api/streams` · `resolve_stream_path()`   대시보드 Stream 탭
    3. `cmd_resume()`               다른 머신·계정에서 **같은 대화를 되살리기**

digest·catalog·검색·상태 전이 등 **문서 쪽 기능은 스트림을 전혀 안 쓴다.** 즉
하네스의 본체(프롬프트 → REQ → 상태 → 보고)는 스트림 없이도 그대로 돈다.

**② "대화 기록이 있어야 계정·머신을 바꿔 이어서 작업이 되는 게 맞나"** — **맞다.
그리고 그게 스트림의 유일한 대체 불가 역할이다.** `s9 resume` 은 미러를
`~/.claude/projects/<cwd키>/<sid>.jsonl` 로 **복원한 뒤** `claude --resume <sid>` 를
띄운다. Claude Code 의 `--resume` 은 그 native 파일을 요구하므로, 문서로는 못 만든다.

**③ "문서만으로도 되면 존재 이유가 없다"** — 여기서 두 가지를 갈라야 한다.

    문서(vault)   무엇을 왜 했는가 → 새 세션이 읽고 **이어서 일한다**
                  (이 시스템의 전제: "컨텍스트는 사라지고 문서만 남는다")
    스트림        그 대화 자체     → **같은 대화를 그대로 되살린다**

**이어서 일하기**는 문서로 된다. **같은 대화를 되살리기**는 스트림이라야 한다.
둘은 다른 일이고, 후자가 필요 없는 사람에게는 스트림이 정말 선택사항이다.

**그래서 제안에 동의한다 — 다만 더 큰 지렛대는 on/off 가 아니라 보존 기간이다.**

    전체 50개 158MB  ·  최근 1일 11개 47MB  ·  3일 33개 136MB  ·  7일 50개 158MB
    가장 큰 것: 32MB(2일 전) · 27MB(1.7일 전) · 14MB(3.9일 전)

**일주일치가 전부다.** 즉 158MB 는 "오래 쌓여서"가 아니라 **하루 47MB씩 쌓이는데
아무도 지우지 않아서**다. 끄면 0이 되지만 켜 두면 한 달 뒤 700MB 다. on/off 만으로는
켜 둔 사람의 문제가 안 풀린다.

**제안하는 모양 셋**
1. `stream_mirror: on|off` — 사용자 설정. **기본 on.** 끄면 `s9 resume` 이 안 되는데,
   그 능력을 말없이 잃는 것이 디스크보다 나쁘다. 끌 때 그 사실을 화면에 적는다.
2. **보관을 사용자별로** — `users/<u>/streams/`. 이 시스템의 다른 것(가시성·설정·
   비밀)이 전부 사용자 단위이고, 공유 인스턴스에서 남의 대화 원문이 공용 자리에
   섞여 있는 지금 모양이 어색하다. 다만 `resolve_stream_path`·`cmd_resume` 이
   파일명으로 찾으므로 그 두 곳을 함께 옮겨야 한다.
3. **보존 기간** — 기본 N일(예: 14일) 지난 미러 정리. 진행 중 REQ 가 붙들고 있는
   세션은 예외. 이게 158MB 를 실제로 줄이는 유일한 손잡이다.

**036 과의 관계**: 이 논의와 무관하게 **오늘 157MB 를 공개 리포에 올리지 않는다**는
판단은 그대로다. 036 은 지금 판정해도 된다.

### 2026-08-27T20:00:29+09:00 decision (by sjpark1)

**확정 (사용자 판정 2026-08-27 19:57) — 이대로 구현한다.**

    기본        켜짐
    기본 보관   1주일 (설정으로 변경 가능)
    끄면        Stream 탭 안 보임 · 미러 안 함 · 깃헙에도 안 올라감
    켜면        보관일 말고는 지금과 같음

**"끄면 문서별 스트림 터미널도 숨기는 게 맞나" — 맞다. 다만 이유를 정확히 적어 둔다.**

`req_stream()` 은 `resolve_stream_path()` 를 타는데, 그건 **바인딩의 native
transcript 를 먼저 보고** 없을 때만 미러로 물러난다. 그래서 미러를 꺼도 **그 원본이
있는 머신에서는 문서별 스트림이 계속 열린다.**

그러면 스위치가 이렇게 된다: "껐는데 왜 보이지?" → 그리고 언젠가 Claude Code 가 제
기록을 지우는 날 **말없이 사라진다.** 이 저장소가 계속 싸워 온 실패 모양이다 —
**될 때까지는 되다가, 어느 날 조용히 안 된다.**

그래서 **끄면 스트림 관련 화면을 전부 내린다**: Stream 탭 · 문서별 스트림 터미널 ·
`s9 resume`. 스위치의 뜻을 하나로 만든다 — "나는 대화 기록을 쓰지 않는다."

**대가는 정직하게 적는다**: 내 머신에 원본이 남아 있어도 못 본다. 보이지 않는 상태에
따라 동작이 달라지는 스위치보다, 예측 가능한 쪽이 낫다고 봤다. 끌 때 화면에
**"끄면 `s9 resume` 으로 같은 대화를 되살릴 수 없습니다"** 를 함께 적는다.

**깃헙 부분은 저절로 성립한다**: 끄면 아무것도 안 쓰이므로 올라갈 것이 없다.
조건부 ignore 규칙보다 튼튼하다 — **없어서 안 올라가는 것**이다.

**남은 판단은 042 가 아니라 036 에 있다**: 켠 사람의 스트림을 **공개** 리포 이력에
넣을 것인가. git 은 지운 것도 기억하므로 보관 1주일이어도 이력은 영구 누적이다
(대략 한 달 0.3GB · 1년 4GB, 압축 기준). 036 의 risk 노트에 숫자와 선택지를 적었다.

### 2026-08-27T20:26:36+09:00 status (by sjpark1)

**솔직히: 착수만 해 놓고 손을 안 댔다.**

19:57 에 설계를 확정하고 in-progress 로 옮긴 뒤, 036(공개 판정)·034(푸시 막힘)·
043(review 오용 지적)·044(권한)·045(설정 미러)가 이어져 그쪽으로만 갔다. 그래서
보드에는 **아무도 안 하는데 in-progress 인 카드**로 남아 있었다 — 오늘 내가 고친
(REQ-20260827-027-62x6) 바로 그 모양을, 이번엔 표시가 아니라 **실제로** 만든 것이다.

지적이 맞다. 지금부터 실제로 만든다.

### 2026-08-27T20:27:03+09:00 tdd (by sjpark1)

TDD 시나리오 (실행: python3 tests/ stream_switch)

정상
  N1. 기본은 **켜짐** — 설정이 없으면 미러를 뜬다 (지금 동작 불변).
  N2. `stream_mirror` 를 off 로 두면 **미러를 쓰지 않는다.**
  N3. 보관 기간이 지난 미러는 정리된다 (기본 7일, `stream_keep_days` 로 변경).
  N4. `s9 stream` 이 지금 상태를 말한다 — 켜짐/꺼짐·보관일·파일 수·용량.

경계
  B1. **진행 중 REQ 가 붙들고 있는 세션의 미러는 보관 기간이 지나도 안 지운다.**
      일하는 도중에 그 대화 기록이 사라지면 되살릴 것이 없어진다.
  B2. `stream_keep_days` 가 0 이하이면 정리하지 않는다 — "무제한 보관"의 뜻.
  B3. 꺼도 **이미 있던 미러는 지우지 않는다.** 끄는 것과 지우는 것은 다른 결정이고,
      말없이 지우면 되돌릴 수 없다. 지우려면 따로 시킨다(`s9 stream prune --all`).

실패
  F1. 꺼져 있으면 `s9 resume` 이 **거부하고 이유를 말한다** — "미러가 꺼져 있어
      되살릴 기록이 없습니다". 조용히 실패하면 사용자는 왜 안 되는지 모른다.
  F2. 꺼져 있으면 대시보드가 스트림을 내주지 않는다 (`/api/streams` 빈 목록,
      문서별 스트림 없음). 화면이 "있는데 안 열리는" 상태로 남지 않게.

회귀
  R1. 켜져 있을 때는 전부 지금과 같다 — 이어쓰기 미러(REQ-20260827-039-62x6) 포함.
  R2. 설정을 읽지 못해도(파일 없음·깨짐) 켜진 것으로 본다 — 기록을 남기는 쪽이
      안전한 기본값이다.

**이번에 하지 않는 것과 이유**: 보관 위치를 `users/<u>/streams/` 로 옮기는 것은
미룬다. 방금 158MB 를 공개 리포에 올렸는데 곧바로 전 파일 경로를 바꾸면 이력이
두 배로 불고, `resolve_stream_path`·`cmd_resume`·`/api/streams` 세 곳을 동시에
옮겨야 한다. 사용자가 요청한 실효(끄기·보관 기간·화면에서 사라지기)는 이번 것으로
전부 충족된다. 옮기기는 별건으로 남긴다.

## History
- 2026-08-27T19:36:53+09:00 created by sjpark1 (status: open)
- 2026-08-27T20:00:30+09:00 status: open -> in-progress (by sjpark1) — 설계 확정 — 구현 착수
