---
id: REQ-20260826-036-62x6
type: request
title: 서버 정지 명령 부재
summary: s9 serve 에 서버 자체를 내리는 명령이 없어 사용자가 stop 을 찾다 못 찾았다
goal: s9 명령 하나로 대시보드가 내려가고 되살아나지 않는다
status: done
size: S
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: 8e60e4af
project: section9
relates: ["QST-20260826-004-62x6"]
tags: ["session"]
created: 2026-08-26T22:54:24+09:00
status_since: 2026-08-27T09:16:13+09:00
updated: 2026-08-27T09:16:13+09:00
priority: 50
---

## Original

`s9 serve --stop-guard` 는 감시자만 물린다. 서버까지 내리려면 그 다음 pkill 을 사람이 직접 쳐야 한다 — 그래서 "restart 도 안 되고 stop 도 안 된다"가 나왔다. `s9 serve --stop`(감시자 먼저 물리고 → 서버 SIGTERM → 포트 해제 확인) 한 줄이 있어야 한다. 근거: QST-20260826-004.

## Notes

### 2026-08-26T23:46:27+09:00 response (by nicehugepark)

`s9 serve --stop` 을 만들었다.

없어서 생긴 일이 이 문서의 요지다: 서버만 죽이면 감시자가 몇 초 뒤 되살린다. 사용자에겐 "stop 이 안 먹는" 것으로 보이고, 실제로 그렇게 보고됐다.

**순서가 전부다.** 감시자를 먼저 물리고 → **물러난 것을 확인한 뒤** → 서버를 내린다. 거꾸로 하면 감시자가 그 틈에 새 서버를 띄워, 명령은 성공했는데 대시보드는 살아 있다. 감시자가 끝내 안 물러나면 서버를 **건드리지 않고 멈춘다** — 되살아날 것을 알면서 죽이면 "내렸다"는 거짓 보고만 남는다.

종료 경로는 `--restart` 와 공유한다(`_signal_port`: SIGTERM → 유예 → SIGKILL). SSE 장수명 연결 탓에 SIGTERM 만으로는 포트가 늦게 풀리는데, 그 다듬음이 이미 `--restart` 안에 인라인으로 있었다 — 꺼내서 한 곳으로 모았다.

**테스트** `tests/test_serve_stop.py` 5건. 실제 포트·프로세스는 건드리지 않고 **호출 순서만** 본다: 감시자 퇴장 확인이 서버 종료보다 앞선다(T1) · 안 물러나면 서버를 안 죽인다(T2) · 내릴 서버가 없으면 조용히 끝난다(T3, 멱등) · `--stop` 이 파서에 있다(T4) · 종료 경로가 `--restart` 와 하나다(T5).

`docs/05-web-dashboard.md` 에 `--restart`·`--stop` 을 적고, 왜 감시자를 먼저 물려야 하는지도 남겼다.

### 2026-08-26T23:48:29+09:00 tdd (by nicehugepark)

- [x] T1 감시자 퇴장 확인이 서버 종료보다 앞선다
- [x] T2 안 물러나면 서버를 안 죽인다
- [x] T3 내릴 서버 없으면 조용히(멱등)
- [x] T4 --stop 이 파서에 있다
- [x] T5 종료 경로가 --restart 와 하나
전체 스위트 636 OK.

## History
- 2026-08-26T22:54:24+09:00 created by nicehugepark (status: open)
- 2026-08-26T23:07:48+09:00 status: open -> in-progress (by nicehugepark) — drag 이동 [via dashboard]
- 2026-08-26T23:21:04+09:00 status: in-progress -> review (by nicehugepark) — 이건 실제로 꺼 봐야 확인된다. 터미널에서 `bin/s9 serve --stop` 한 줄을 치면 "감시자 물러남 → 대시보드 정지"가 뜨고, 브라우저를 새로고침하면 페이지가 안 떠야 한다. 30초쯤 기다려도 되살아나지 않으면 성공이다(예전에는 몇 초 뒤 혼자 살아났다). 다시 띄우는 것은 `bin/s9 serve --supervise --port 9909`. 다만 지금 우리가 대시보드로 대화 중이라, 끄면 이 채팅 창도 함께 닫힌다 — 터미널에서 확인하는 편이 낫다.
- 2026-08-27T09:16:13+09:00 status: review -> done (by nicehugepark) — 리드 판정. 실제로 돌려 확인하려면 이 대화 창구가 함께 닫혀서 사용자에게 확인을 부탁할 수 없는 종류다. 대신 계약 5건이 순서를 못박는다 — 감시자 퇴장 확인이 서버 종료보다 앞서고, 안 물러나면 서버를 건드리지 않으며(되살아날 것을 알면서 죽이면 거짓 보고만 남는다), 내릴 서버가 없으면 조용히 끝난다. 종료 경로는 --restart 와 하나다.
