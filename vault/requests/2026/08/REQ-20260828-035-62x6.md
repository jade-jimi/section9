---
id: REQ-20260828-035-62x6
type: request
title: DAG 글자가 안개가 된다
summary: DAG 보기에서 노드 라벨이 겹쳐 흰 띠가 되어 사실상 읽을 수 없다
goal: DAG 보기에서 문서 561개 규모에도 글자가 서로 겹쳐 읽을 수 없게 되지 않고, 무엇을 보고 있는지 판단할 수 있다
status: review
size: M
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: b8fda8f9
agents: ["sub:subagent:ab56a87f", "sub:subagent:a1e6a636", "sub:designer:a9aa0303"]
contributions: [{"actor": "sub:subagent:ab56a87f", "item": "Appending response note to REQ-20260828-035-62x6", "started": "2026-08-28T18:33:50+09:00", "ended": "2026-08-28T18:33:50+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/subagents/agent-ab56a87f1c6f08169.jsonl"}, {"actor": "sub:subagent:a1e6a636", "item": "Verifying rendered note on REQ-20260828-035-62x6", "started": "2026-08-28T18:34:25+09:00", "ended": "2026-08-28T18:34:25+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/subagents/agent-a1e6a636f7137aa44.jsonl"}, {"actor": "sub:designer:a9aa0303", "item": "`[2026-08-28 18:41:22 KST - designer]`", "started": "2026-08-28T18:37:00+09:00", "ended": "2026-08-28T18:37:00+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/subagents/agent-a9aa0303dfc0d886a.jsonl"}]
project: section9
relates: ["DOC-20260828-002-62x6"]
relates_why: {"DOC-20260828-002-62x6": "이 요청에서 얻은 라벨 거르기 규칙을 다음 화면에서도 쓰려고 지식으로 뽑았다"}
tags: ["auto-audit", "assets", "dashboard", "terminal", "attached", "png", "image"]
created: 2026-08-28T17:43:38+09:00
status_since: 2026-08-28T18:35:10+09:00
updated: 2026-08-28T18:37:00+09:00
priority: 50
---

## Original

그래프 dag 화면인데 글자가 너무 겹쳐 포그가 되어 사실상 볼 수 없다. 디자이너, 아키텍트, 프론트엔드 개발자 다시 검토하고 개선 좀 해줘
[Image: assets/REQ-20260828-035-62x6/20260828T174237-image.png]

## Notes

### 2026-08-28T17:50:10+09:00 analysis (by nicehugepark)

architect 검토 (실측 · 요약)

**결론: 이 그래프의 병은 라벨 겹침이 아니라 스코프가 없다는 것이다.** 지금 그려지는 476개 노드 중 **살아 있는 요청은 11개**, 나머지 465개(98%)는 끝난 과거다. 그리고 이 탭이 답하라고 만들어진 물음("무엇이 앞의 요청 때문에 못 나가는가")의 데이터인 살아 있는 `blocked_by` 간선은 **오늘 0건**이다.

간선 실측: relates 278 · parent 124 · derived_from 34 · refs_docs 10 · **blocked_by 0**. 간선의 96%가 출처(계보) 축이고 상태 축은 0이다 — 화면의 잉크가 전부 과거에 쓰이고 있다.

안개의 산수 (스타일이 아니라 설계 결함)
- parent 기준 depth 분포: **layer 0 에 352개**(gap 3.1px) · layer 1 68 · 2 31 · 3 14 · 4 11.
- `web/index.html:8466` 의 `gap = Math.min(170, (W-120)/layer.length)` 가 **한 레이어를 반드시 한 줄에 우겨넣는다.** 352개면 3.1px 인데 라벨은 110~170px — 라벨 하나가 이웃 **35~55개**를 덮는다.
- `web/index.html:8766` 의 `showLabel` 이 dag 모드에서 줌·깊이 게이트를 **무조건 무시**한다(`|| graphLayout === "dag"`). force 에는 있는 컬링이 dag 에는 없다.
- 더 근본: 요청 388건 중 부모가 있는 것이 124건뿐이라 **이 vault 에는 볼 만한 계층이 거의 없다**(최대 깊이 4, 74% 가 고아). dag 는 없는 구조를 그리려다 실패하고 있다.

6개월 뒤 (하루 70건 가정 → 약 13,000 문서)
- dag layer 0: 9,500개 · **gap 0.11px** — 라벨을 고쳐도 점조차 구분 불가
- force 물리 O(N²): 113k → **85M 쌍/프레임** — 브라우저 정지
- graph_data() 가 요청마다 vault 전체를 파일로 읽는다: 562 → 13,000 read
- 판 1200×700 에서 겹치지 않는 라벨의 물리 상한은 **약 80개**
→ **라벨만 컬링하는 안은 6개월을 못 간다. 스코프를 자르는 안은 선다** — 살아 있는 요청 수는 문서 총량과 무관하게 동시 진행 능력에 묶인 상수(오늘 11)이기 때문이다.

이미 있는 것과의 중복: "무엇이 막혔나" 는 오른쪽 `선행 대기 현황` 레일이 이미 답하고(DOC-20260826-009-62x6 가 "노드 300개에서 엣지 하나를 눈으로 찾는 것은 현실적이지 않다" 며 만든 것이다), "지금 뭐가 진행 중인가" 는 Board 의 live 점이, "이걸 참조하는 문서" 는 문서 뷰어의 Linked mentions 가 답한다. **그래프만 답할 수 있는 것은 모양 하나다** — 사슬의 길이, 여러 갈래가 합류하는 지점, 그 합류점이 지금 살아 있는지.

권고: **기본 스코프를 "전체" 에서 "살아 있는 것 + 1홉" 으로 바꾸고 전역 지도는 라벨 없는 옵션으로 내린다.** DOC-20260826-001-62x6 규칙 4(선행이 끝나면 엣지는 사라진다 — 끝난 의존을 계속 그리면 그래프가 과거로 채워진다)를 **간선에서 노드로 확장**하는 것이다. 새 개념 0, 새 필드 0. 지금 화면이 정확히 그 문서가 금지한 상태다.

단계
1. **지혈(오늘, 렌더만)**: 8766 의 `|| graphLayout === "dag"` 제거해 dag 도 같은 라벨 게이트를 쓰게 하고, 8466 의 gap 에 하한(24px)+**줄바꿈**을 준다(352개면 9개씩 39줄). 모델 변경 없음.
2. **스코프 축**: 헤더에 `살아있음(기본) · 최근 7일 · 이 문서 주변 · 전체`. `/api/graph?scope=&focus=&hops=` 로 서버가 자른다 — 전부 이미 있는 필드로 계산되고 새 저장물이 없다. 비기능 목표를 숫자로: 라벨 있는 노드 ≤80, 그릴 노드 ≤300, 초기 렌더 ≤300ms.
3. **확장성**: graph_data() 가 walk_docs 대신 카탈로그를 읽게 하고, `전체` 모드는 노드 상한 + 라벨 off 로 "모양만 보는 우주" 로 정의한다 — 옵시디언 감성은 거기 산다. 읽을 수 없는 라벨을 붙여 두는 것보다 아예 없는 편이 그 감성에도 낫다.

**부수 발견 (내 습관 문제 · 별건 감)**: 요청 388건 중 부모가 있는 것이 124건뿐인데 relates 는 278건이다 — **리드가 계보를 걸어야 할 자리에 동급 연관을 걸고 있다.** dag 가 그릴 것이 없는 진짜 이유가 이것이다. 렌더를 아무리 고쳐도 이 습관이 안 바뀌면 계층 뷰는 영영 빈 뷰다.

반증 조건: ① 1단계만으로 사용자가 만족하고 계속 dag 를 쓴다면 원한 것은 국소 답이 아니라 지도 자체였다는 뜻이고 내 진단이 틀린 것 — **그래서 1단계를 먼저 배치해 이 반증을 싸게 얻는다.** ② 스코프를 좁힌 뒤에도 계속 Board 만 본다면 그래프에는 애초에 물음이 없었다는 뜻이다. ③ 살아 있는 blocked_by 가 2주간 계속 0이면 물음 A 는 실재하지 않는(혹은 기록되지 않는) 물음이다.

### 2026-08-28T17:55:17+09:00 analysis (by nicehugepark)

frontend-developer 검토 (CDP 실측 · 요약)

**결론: 라벨 거르는 장치는 있는데 DAG 에서 통째로 꺼져 있다.** `web/index.html:8858` 의 `|| graphLayout === "dag"` 한 조각이 줌·깊이 조건을 무효화해 476개 라벨을 매 프레임 전부 그린다. 그 위에 배치 결함이 겹친다. **성능은 원인이 아니다** — 겹침 검사를 히스테리시스까지 붙여 매 프레임 돌려도 0.26ms(프레임 예산의 1.5%)다.

측정 방법 (헤드리스 함정을 피한 방식): `s9 shot` 은 숫자를 못 주므로 Windows Chrome 을 `--remote-debugging-port` 로 띄우고 CDP 에 직접 붙었다(WebSocket 프레임을 손으로 짠 60줄, 외부 의존성 0). `?gsettle=2500` 으로 재운 뒤 `__gdbg.gen` 으로 **산 판인지 확인하고** 쟀다(frames=166, alpha=0.005 — 물리가 멎은 뒤). `ctx.fillText`/`ctx.arc` 를 가로채 프레임당 호출 수를 **추론이 아니라 계수**했다.

실측
- 라벨 그리기: DAG 프레임당 `fillText` **477회** = 노드 수와 정확히 같다. k=0.25(최대 축소)에서도 전부 그린다.
- force 모드도 겹침을 안 잰다 — k=1 에서 **81개**를 그리는데 겹치지 않고 들어가는 최대치는 29~34개다. 정도만 덜할 뿐 같은 안개다.
- 겹침 판정 코드는 **어디에도 없다.** 라벨 사각형을 재는 곳도, 공간 인덱스도 없다.
- 배치: 판 1110×612, 노드 476개. depth 0 에 **352개 · 이웃 간격 2.81px** · 1층 68(14.6px) · 2층 31 · 3층 14 · 4층 11. **계층 5개뿐, 74%가 맨 윗줄.** 노드 지름이 10~42px 이므로 **흰 띠는 라벨이 아니라 노드 자체가 3~15겹 포개진 것**이고 그 위에 173px 라벨이 얹힌다.
- 겹침: 라벨 사각형 476개 중 **472개(99.2%)가 겹친다. 겹치는 쌍 9,955개.**
- **배치만으로는 못 고친다**: 라벨 실측 폭 173px × 높이 13px × 476개 = 1,070,524 px². 판 넓이 679,320 px². **글자 면적이 판보다 1.58배 크다.** 어떤 배치를 쓰든 476개를 겹침 없이 놓을 자리가 물리적으로 없다 — **라벨을 거르는 일은 선택이 아니라 필수다.**

성능 (헤드리스 Chrome 151, 1400×900, DAG 476노드)
- 실 프레임 59.5fps, 프레임당 메인스레드 **5.81ms** (예산 16.7ms 의 35%)
- fillText 476회 = 0.635ms (전체의 11%) · 겹침 그리디 0.07~0.12ms · 히스테리시스 포함 0.26ms
- **매 프레임 돌려도 된다.** 캐시를 두면 "언제 무효화하나" 라는 결함 표면(팬·줌·hover·물리·15초 갱신)이 새로 생기는데 0.1ms 를 아끼자고 살 부채가 아니다.
- 다만 **매 프레임 그리디는 끓는다**(실측): 180px/s 팬에서 프레임당 라벨 교체 3.19개(최대 11). 10%가 깜빡이면 읽히지 않고 고장으로 보인다. 해법은 뜸이 아니라 **관성** — 직전 프레임에 보이던 라벨을 우선순위 맨 앞에 두면 교체 0.81개로 떨어지고 표시 손실은 1.2개(3.6%)뿐이다.

권고: **겹침을 재서 거르되(B′) 배치의 간격 하한을 함께 넣어라(E).** 하나만으로는 안 된다 — 그리디만 하면 k=4 최대 확대에서도 6개뿐이라 **확대해도 안 늘고**(한 줄에 몰려 1110/173≈6줄분이 천장), 배치만 고치면 74개가 여전히 겹친다.
간격 하한 26px + 줄바꿈을 얹은 시뮬레이션: k=1 라벨 29 → **74** · k=2 20 → 48 · k=4 6 → 15. **정원 2.5배**가 되고 확대가 비로소 보상을 준다.

순서: ① `|| graphLayout === "dag"` 삭제(한 조각, 즉시 안개 걷힘 — 이 상태를 캡처해 대조군으로) ② gap 에 하한 `Math.max(26, ...)` + 넘치면 줄바꿈, `n.y` 를 누적 오프셋으로(70+d*105 고정 버림) ③ showLabel 을 겹침 그리디로 교체(중요도 정렬 → 화면 밖 컬링 → 24×14 격자 점유 검사, measureText 캐시) ④ 히스테리시스 5줄 ⑤ hover 라벨은 채택 집합보다 나중에 맨 위에.
quadtree 는 **기각** — 격자 해싱으로 이미 0.1ms 라 복잡도 부채만 는다. 계층 알고리즘 교체(Sugiyama)도 기각 — depth 0 이 74%인 데이터에 정교한 계층 알고리즘을 써도 줄 하나는 줄 하나다.

지표(전부 지금 잴 수 있다): 겹치는 라벨 쌍 9,955 → **0** · 가려진 비율 99.2% → 0% · k=1 표시 476(전부 안개) → 30~74(전부 읽힘) · **확대 보상**: k=1→k=4 에서 표시 수가 늘어야 한다(지금 476→476 변화 없음) · 깜빡임 3.19 → 1.0 이하 · 프레임당 5.81 → 6.1ms 이하.
진단 손잡이 `?glab` 을 권함 — 판 위에 `표시 N/476 · 겹침쌍 M · 교체/프레임 C` 를 적으면 캡처 한 장이 곧 증거가 된다(`?gshape` 와 동형).

다음 결함 예고(범위 밖): `step()` 이 O(n²)(8682, 476²/2 = 113k쌍/프레임). DAG 는 물리가 꺼져 무관하지만 force 는 1000노드에서 500k쌍이 된다.

재현 캡처(자리 잡힌 상태): scratchpad/dag-fog.png — 사용자 캡처와 동일한 안개가 그대로 나온다.

### 2026-08-28T18:33:46+09:00 response (by nicehugepark)

`[2026-08-28 18:33 KST - designer]`

**안개가 걷혔다. 476개를 다 쓰던 이름을 40개만 남기고, 그 40개는 서로 겹치지 않는다** — 같은 판에서 잰 겹치는 이름 쌍이 11,515 → **0** 이다. 함께 실패하고 있던 FORCE 도 낫는다: 전체 보기를 누르면 이름이 **한 개도** 없던 화면에 이제 40개가 남는다.

세 관점이 모은 5단계를 그 순서대로 넣었다. 커밋은 하지 않았다(리드 몫).

### 무엇을 고쳤나

**① dag 예외 삭제** — `showLabel` 의 `|| graphLayout === "dag"` 한 조각이 줌·밀도 게이트를 통째로 무효화하고 있었다. 프레임당 글자 그리기 469회 = 노드 수. 지웠다.

**② 층을 접는다** — `gap = min(170, (W-120)/인원)` 에 하한이 없어 depth 0 의 352개가 2.8px 간격이었다. **글자를 다 꺼도 점 자체가 겹친다** — 사용자가 본 흰 띠의 절반은 글자가 아니라 뭉개진 점이었다. 가로 간격 하한 24px 을 두고, 그 하한으로 한 줄에 안 들어가면 다음 줄로 접는다. 층의 세로 위치를 고정값(`70 + d*105`)에서 **누적 오프셋**으로 바꿔 접힌 줄만큼 다음 층이 밀리게 했다. 판이 세로로 길어지므로 dag 첫 화면은 전체 보기로 시작한다.

**③ 이름은 자리로 정한다** — 매 프레임 한 번, 우선순위 순으로 훑어 이미 찬 자리와 겹치면 버린다(예산 40). 우선순위는 **지금 손이 가야 할 것** 순이다: 병목(미완료 파생) → 진행 중·막힘·검토 → 허브(연결 수) → 미완료. 전에는 문서 id 해시가 절반을 차지하는 깊이값이 정했다 — 병목이 우연히 원경이면 이름이 없었다. 글자 폭은 제목당 한 번만 잰다(프레임당 measureText 1 → **0**).

**④ 관성** — 직전 프레임에 있던 이름을 정렬 맨 앞에. 팬 중 깜빡임을 막는다.

**⑤ 이름은 점을 다 그린 뒤에** — 전에는 노드마다 자기 이름을 그려서 뒤에 그려진 이웃 점이 앞 사람 이름을 덮었다. 이제 채택 40개를 먼저, hover/이웃 이름을 그 위에 맨 마지막으로 그린다.

**덤으로 고친 둘.** (ㄱ) dag 에서 깊이 시차(`pfOf` 0.72~1.24)를 끈다 — 자리 자체가 뜻인 배치에 노드마다 다른 배율을 먹이면 곧은 줄이 휘어 계층이 안 읽혔다. dag 에서 깊이는 크기·농도가 계속 전한다. (ㄴ) hover 이름끼리 포개지는 것 — dag 는 이웃이 같은 줄에 나란히 서므로 이름이 그대로 쌓였다(진단으로 겹침쌍 3 을 봤다). 아래가 막히면 위로, 그다음 한 칸 아래로 자리를 옮겨 본다.

### 전후 (같은 판·같은 자로 잰 값)

| | 전 | 후 |
|---|---|---|
| 표시한 이름 | 468 / 479 | **40** / 479 |
| 겹치는 이름 쌍 | **11,515** | **0** |
| 프레임당 글자 그리기 | 469 | 40 (+윤곽 40) |
| 프레임당 글자 폭 재기 | 1 | **0** (제목당 1회 기억) |
| 이름 교체 / 프레임 | 19.13 | **0.00** |
| 팬 180px/s 중 교체 / 프레임 | — | **0.00** (관성 끄면 0.07) |
| 확대 보상 (화면 안 이름 비율) | 476→476 (변화 없음) | k0.62 40/468(9%) → **k4.0 15/26(58%)** |
| FORCE 전체 보기(k0.30) 이름 | **0개** | **40개** |

**진단 손잡이** `?glab` 을 넣었다(`?gshape` 와 동형) — 판 위에 `라벨 N/총 · 후보 M · 겹침쌍 P · 교체/프레임 C · k` 를 적는다. 대조군도 같은 자로 볼 수 있게 `?glabraw`(거르기 전) · `?gdagraw`(접기 전) · `?glabnohys`(관성 없이) · `?gpan`(끄는 동안 갈리는지) 를 함께 두었다. 위 표의 "전" 은 전부 그 대조군을 실제로 켜서 잰 값이다.

### 검증

실브라우저 헤드리스 캡처(Windows Chrome, `s9 shot`). `bin/s9 shot` 은 매번 새 프로필로 떠 `localStorage.s9glayout` 이 비어 항상 FORCE 로 찍히므로, 9909 앞에 주입 프록시를 세워 레이아웃/tone 을 쿼리로 심었다. DAG 는 물리가 꺼진 고정 배치라 첫 프레임이 곧 사용자 화면이고, FORCE 는 `?gsettle=400` 으로 수렴시켰다. 그리기 횟수는 추론이 아니라 `ctx.fillText`/`arc` 를 가로채 **센 값**이다.

실제로 해 본 것: DAG 기본·확대(휠 4·10·17번)·좁은 창(1024×768)·라이트 tone·노드 얹기(hover, 실제 pointer 이벤트)·끌기(팬 180px/s 60프레임)·FORCE 기본·FORCE 전체 보기·빈 상태(문서 0건).

전체 스위트 **1,511건 통과**. 이 계약을 지키는 테스트 14건을 `tests/test_graph_labels.py` 로 새로 두었다(안개의 원인이던 한 조각이 되돌아오면 바로 깨진다).

### 한계 · 남은 것

- 좁은 창(1024×768)에서는 판이 작아 이름이 20개로 준다. 안개는 없지만 배율이 0.34 라 점이 작다 — 넓은 창을 쓰거나 확대하면 된다.
- 프레임 시간(ms)은 못 쟀다. 헤드리스가 시계를 얼려 `performance.now()` 가 0 을 준다. 대신 그리기 횟수를 셌고, 글자 그리기가 469 → 40 이므로 방향은 줄어드는 쪽이다.
- architect 가 권한 "스코프를 살아 있는 것 + 1홉으로" 는 손대지 않았다. 이번 판이 그 반증 실험이다 — 라벨·배치만으로 충분한지 사용자가 판정한다.

[Image: assets/REQ-20260828-035-62x6/1-dag-before.png]
[Image: assets/REQ-20260828-035-62x6/2-dag-after.png]
[Image: assets/REQ-20260828-035-62x6/3-dag-user-view.png]
[Image: assets/REQ-20260828-035-62x6/4-force-fit.png]
[Image: assets/REQ-20260828-035-62x6/5-hover.png]
[Image: assets/REQ-20260828-035-62x6/6-light-tone.png]

### 2026-08-28T18:33:50+09:00 subagent (agent: sub:subagent:ab56a87f, by nicehugepark)

Appending response note to REQ-20260828-035-62x6

### 2026-08-28T18:34:25+09:00 subagent (agent: sub:subagent:a1e6a636, by nicehugepark)

Verifying rendered note on REQ-20260828-035-62x6

### 2026-08-28T18:37:00+09:00 subagent (agent: sub:designer:a9aa0303, by nicehugepark)

`[2026-08-28 18:41:22 KST - designer]`

**안개가 걷혔다.** 476개를 다 쓰던 이름을 40개만 남기고, 그 40개는 서로 겹치지 않는다 — 같은 판·같은 자로 잰 겹치는 이름 쌍이 **11,515 → 0**. 대칭으로 실패하던 FORCE 도 함께 낫는다: 전체 보기에서 이름이 **0개 → 40개**.

`REQ-20260828-035-62x6` 은 **review** 로 올렸고, 보고 노트에 캡처 6장을 그림으로 붙였다.

### 넣은 것 (지시한 5단계 그대로)

| 파일 | 내용 |
|---|---|
| `/home/sjpark1/section9/web/index.html` | ①~⑤ 전부 + 진단 손잡이 |
| `/home/sjpark1/section9/tests/test_graph_labels.py` | 계약 테스트 14건 (새 파일) |
| `/home/sjpark1/section9/vault/knowledge/2026/08/DOC-20260828-002-62x6.md` | 재사용 지식 "판에 놓을 수 있는 이름의 수" |

① `|| graphLayout === "dag"` 삭제 · ② `gap` 하한 24px + 층 줄바꿈, `n.y` 를 누적 오프셋으로 · ③ `showLabel` → 겹침 그리디(우선순위 = 병목 → 진행중·막힘·검토 → 허브 → 미완료, `measureText` 제목당 1회) · ④ 관성 · ⑤ 라벨은 점을 다 그린 뒤, hover 는 그 위에.

**덤으로 둘 더 고쳤다** (둘 다 이번 변경이 드러낸 결함):
- dag 에서 깊이 시차 `pfOf` 를 끈다 — 자리 자체가 뜻인 배치에 노드마다 다른 배율을 먹이니 곧은 줄이 휘어 층이 안 읽혔다. 끄니 계층이 그림으로 보인다.
- hover 이름끼리 포개지던 것 — dag 는 이웃이 같은 줄에 서므로 라벨이 쌓여 `?glab` 이 겹침쌍 3 을 찍었다. 아래→위→한 칸 아래로 자리를 옮겨 본다(다 막히면 버린다).

### 잰 값 (전부 대조군을 실제로 켜서 잰 것)

표시 468→**40** · 겹침쌍 11,515→**0** · 프레임당 fillText 469→**40** · measureText 1→**0** · 교체/프레임 19.13→**0.00**(팬 180px/s 중에도 0.00, 관성 끄면 0.07) · 확대 보상 k0.62 40/468(9%)→**k4.0 15/26(58%)** · FORCE 전체 보기 0→**40**.

`?glab` 을 `?gshape` 와 동형으로 넣었고, 같은 자로 전후를 재도록 `?glabraw`(거르기 전)·`?gdagraw`(접기 전)·`?glabnohys`(관성 없이)·`?gpan`(끄는 동안 갈리는지)를 함께 뒀다.

### 검증

실브라우저 헤드리스(Windows Chrome, `s9 shot`). 앞선 designer 의 주입 프록시 방식을 써 `s9glayout` 을 쿼리로 심었다. 그리기 횟수는 `ctx.fillText`/`arc` 를 가로채 **센 값**이다. 실제로 조작한 것: DAG 기본·확대 3단계·좁은 창 1024×768·라이트 tone·hover(실제 pointer 이벤트)·팬 60프레임·FORCE 기본·FORCE 전체 보기·빈 상태.

전체 스위트 **1,511건 통과**.

### 리드가 알아야 할 것 셋

1. **커밋은 안 했는데 이미 커밋돼 있다.** 작업 중 다른 세션이 `5f6bcb5 끝났는데 안 온 일이 보이게` 로 `web/index.html`·`tests/test_graph_labels.py`·지식 문서를 함께 쓸어 담았다. HEAD 에 내 변경이 온전히 들어 있음을 확인했다(`pickLabels`/`LAB_BUDGET`/`DAG_GAP_MIN` 있음, 안개 조각 없음). 별도 커밋 불필요.
2. **다른 세션 작업은 그대로다.** 내 백업 대비 diff 에 그 세션의 `?shortref=peek` 블록이 손상 없이 남아 있다. `git checkout`·`stash`·`reset` 은 쓰지 않았다.
3. **한계 둘.** 프레임 시간(ms)은 못 쟀다 — 헤드리스가 시계를 얼려 `performance.now()` 가 0 을 준다. 그리기 횟수(469→40)로 대신했고 방향은 줄어드는 쪽이다. 좁은 창(1024×768)에서는 판이 작아 이름이 20개, 배율 0.34 로 준다(안개는 없다).

architect 가 권한 "스코프를 살아 있는 것 + 1홉" 은 손대지 않았다 — 이번 판이 그 반증 실험이고, 라벨·배치만으로 충분한지가 이 판정의 핵심이다.

## History
- 2026-08-28T17:43:38+09:00 created by nicehugepark (status: open)
- 2026-08-28T17:43:58+09:00 status: open -> in-progress (by nicehugepark) — designer·architect·frontend-developer 세 관점 병렬 검토 착수 (읽기 전용) — 구현은 web/index.html 이 비는 대로
- 2026-08-28T18:35:10+09:00 status: in-progress -> review (by nicehugepark) — Graph 탭 → DAG 를 눌러 보세요 (F5 로 새로고침 먼저). 흰 띠가 사라지고 이름이 마흔 개만 남습니다. 1. **글자가 읽히나요.** 겹쳐 뭉개진 띠 대신 서로 떨어진 이름이 보여야 합니다. 붙어 있는 문서 그림 두 장(전/후)이 같은 자리를 찍은 것입니다. 2. **남은 이름이 옳은 이름인가요.** 지금 손이 가야 할 것 순으로 남깁니다 — 다른 요청을 붙잡고 있는 것(앰버 글자), 진행 중·막힘·검토, 연결이 많은 것. 이름이 있어야 할 문서가 빠져 있다면 그 순서가 틀린 것입니다. 3. **확대하면 이름이 늘어나나요.** 휠을 굴려 확대하면 화면에 남는 문서가 줄면서 그만큼 이름이 더 붙습니다. 전에는 아무리 확대해도 그대로였습니다. 4. **끌어 옮길 때 이름이 깜빡이나요.** 화면을 끌어 보세요. 이름이 나타났다 사라졌다 하면 안 됩니다. 5. **FORCE 에서 전체 보기를 눌러 보세요.** 전에는 이름이 하나도 안 남았습니다. 지금은 마흔 개가 남습니다. 6. **문서 하나에 마우스를 얹어 보세요.** 그 문서와 이웃만 이름이 남고 서로 포개지지 않아야 합니다. DAG 그림이 달라진 점 하나: 층이 판보다 넓으면 다음 줄로 접습니다(전에는 352건을 한 줄에 2.8px 간격으로 우겨넣어 점까지 겹쳤습니다). 판이 세로로 길어져 처음 들어가면 전체 보기 상태로 시작합니다. 아직 안 한 것: 그리는 문서 자체를 줄이는 일(살아 있는 것 + 1홉)은 손대지 않았습니다. 이번 것만으로 충분한지가 이 판정의 핵심입니다 — 부족하면 반려해 주세요.
