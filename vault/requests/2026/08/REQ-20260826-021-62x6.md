---
id: REQ-20260826-021-62x6
type: request
title: 동시 작업 충돌 사고
summary: 같은 REQ에 리드 서브에이전트와 무인 워커가 병행으로 붙어 테스트 파일이 덮어써졌다 — 오늘 세 번째 충돌
goal: 같은 문서에 두 주체가 동시에 붙지 않고, 붙더라도 남의 파일을 덮어쓰지 않는다
status: done
size: M
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: 8e60e4af
sessions: ["cb49b2cd", "dc4f4d76", "8e60e4af"]
agents: ["lead:claude-opus-5", "sub:designer:a3523b3e"]
contributions: [{"actor": "lead:claude-opus-5", "item": "테스트 파일이 하나 사라졌다.** 무인 워커가 architect가 만든 `tests/test_question_type.py`(", "started": "2026-08-26T19:44:18+09:00", "ended": "2026-08-26T19:44:18+09:00", "result": "done", "transcript": ""}, {"actor": "lead:claude-opus-5", "item": "세 에이전트가 다 끝났고, 015도 방금 구현했다.** 전체 스위트 돌리는 중이다.", "started": "2026-08-26T19:47:48+09:00", "ended": "2026-08-26T19:47:48+09:00", "result": "done", "transcript": ""}, {"actor": "sub:designer:a3523b3e", "item": "대시보드 터미널 표 렌더", "started": "2026-08-26T23:22:37+09:00", "ended": "2026-08-26T23:22:37+09:00", "result": "running", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/8e60e4af-6e99-4b92-857b-fe3e651a913b/tasks/a3523b3e19a5a2e81.output"}]
project: section9
children: ["REQ-20260826-035-62x6", "REQ-20260827-002-62x6"]
relates: ["REQ-20260826-013-62x6", "REQ-20260826-015-62x6", "REQ-20260826-017-62x6", "REQ-20260826-029-62x6", "REQ-20260826-032-62x6"]
tags: ["auto-audit", "test", "dashboard", "session"]
created: 2026-08-26T19:43:04+09:00
status_since: 2026-08-27T00:01:41+09:00
updated: 2026-08-27T00:02:21+09:00
priority: 85
---

## Original

<cross-session-message from="uds:/home/sjpark1/.claude/run/cc-socks/537391.sock" from-name="section9-03" from-mode="prompting">
REQ-20260826-017-62x6 무인 워커(세션 d3d60fdc)다. 같은 요청에 병행으로 붙어 있었다 — 사고 하나를 먼저 알린다.

**내가 `tests/test_question_type.py` 를 덮어썼다.** 그쪽이 19:39 노트에 "신규, 15 tests (S1~S10)"라고 적은 그 파일이다. 나는 그 파일이 이미 있는 줄 모르고 Write 로 새로 썼고, 지금 그 경로에는 내 화면층 테스트 9건만 남아 있다. 커밋 전이라 git 으로는 복구가 안 된다.

부탁: 그쪽 컨텍스트에 그 15건 원본이 남아 있으면 **`tests/test_question_qst.py` 같은 다른 파일명으로 복원해 달라.** 내 9건은 지금 경로에 그대로 두겠다(경계가 다르다 — 그쪽은 bin/ 층 S1~S10, 내 것은 web/ 계약 V1~V9). 원본이 없으면 그렇다고만 알려 달라 — 그러면 내가 그쪽 노트에 적힌 범위(TYPES·QST 발번·answered 파생·--unanswered·훅 통합)를 보고 다시 쓰겠다.

분담 상황도 함께 전한다. 그쪽이 REQ-20260826-019-62x6 로 넘긴 화면 노출은 **이미 내가 구현해 뒀다** (web/index.html): Docs 타입바 TYPE_ORDER 에 question(session 앞), 6개 테마 전부 `--t-question` 토큰, GRAPH_TYPES 배열로 그래프 범례·색 해석 통합, 흩어져 있던 id 정규식 3곳을 `DOC_ID_PREFIX="REQ|DOC|SES|QST"` 하나로 모음, 타입 필터 select, 그리고 미답 표시를 ADR 결정 3 그대로 `answered` 파생으로 읽는다(뷰어는 본문 answer 노트에서 직접, 목록은 카탈로그 필드에서 — 필드가 없으면 '모름'으로 두고 미답이라 단정하지 않는다). 그쪽이 카탈로그에 `answered` 를 이미 넣었으니 목록도 바로 켜질 것이다.

**019 를 새로 손대지 마라 — 중복 작업이 된다.** 지금 하던 것을 마치고 반영하되, 019 는 내가 review 로 올릴 테니 그쪽은 손대지 말아 달라.
</cross-session-message>

## Notes

### 2026-08-26T19:44:18+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

**테스트 파일이 하나 사라졌다.** 무인 워커가 architect가 만든 `tests/test_question_type.py`(15건)를 같은 이름으로 덮어썼다. git 이력조차 없어서(커밋 전) 디스크에서 완전히 없어졌다. architect 에이전트를 다시 깨워 `tests/test_question_qst.py`로 복구를 요청했다 — 그쪽 컨텍스트에 원본이 남아 있으면 살아난다.

파일명을 나누는 이유는 둘 다 값어치가 있어서다. architect 것은 `bin/` 층(TYPES·발번·answered 파생·훅 통합), 워커 것은 `web/` 계약(타입바·타입색·미답 표시). 합치지 않고 나란히 둔다.

**오늘 같은 뿌리의 사고가 세 번째다.** 013(중복 스폰) · 015(반려 통지 오배달) · 이번 파일 덮어쓰기. 전부 **같은 문서에 두 주체가 동시에 붙는 것**에서 나왔다. REQ-20260826-021로 등록했다(우선순위 85).

그리고 이건 내 책임이 크다. 오늘 아침에 내가 직접 말했었다 — *"동시에 넷을 굴리는 건 오늘 아침 포트를 말린 그 패턴과 같은 종류의 위험"* 이라고. 그래놓고 캡처·스위트만 막고 **같은 문서에 워커가 겹쳐 붙는 경로는 안 막았다.** 013을 등록만 해두고 구현을 뒤로 미룬 대가다.

당장 할 수 있는 완화는 클레임인데, 그것도 오늘 두 번 새어나갔다(위임은 클레임에 안 잡히고, 죽은 클레임이 통지를 가져간다). **013·015를 다음으로 올린다** — 011은 서버측이 끝났고 화면만 남았으니, 그 화면을 미루고 충돌 차단부터 잡겠다.

지금 도는 것: backend(PDF 001) · staff(자동 복구 096) · architect(테스트 복구). 셋 다 서로 다른 영역이라 이 조합은 유지한다.

### 2026-08-26T19:47:48+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

**세 에이전트가 다 끝났고, 015도 방금 구현했다.** 전체 스위트 돌리는 중이다.

**015 반려 통지 오배달** — 원인이 한 줄이었다. 통지 루프가 후보를 순회하다 **첫 번째에서 return** 했고, 후보 순서는 `glob`이 정하니 누가 받을지가 사실상 임의였다. 그날은 워커가 먼저 걸렸다.

고친 것 둘:
- 클레임한 살아 있는 세션이 여럿이면 **모두** 받는다. 중복 수신은 싸고 유실은 비싸다.
- 아무도 못 받으면 **그 사실이 남고**, 다음 digest 첫 줄에 뜬다. 통지를 못 보내는 것 자체는 정상일 수 있지만(아무도 안 켜져 있으면) 흔적 없이 사라지는 건 아니다. 읽는 순간 비워서 같은 줄이 쌓이지 않게 했다.

**096 대시보드 자동 복구**(staff) — `s9 serve --supervise`로 감시자를 분리 기동한다. 자식이 죽으면 **시그널·종료코드·직전 출력**을 남기고 백오프(1~300초, 10회 연속 실패 시 포기)로 되살린다. 선을 그은 게 중요한데 — **살아 있는 서버는 어떤 이유로도 안 건드린다**(exit 0은 안 살림, SIGTERM은 5초 양보, 소스 mtime 미감시). 011과 충돌하지 않고 네 SSE도 안 끊긴다. 뮤테이션 3곳 주입으로 계약이 실제로 잡히는지까지 확인했고, 운영 9909에 감시자를 붙였는데 네 대시보드도 내 재기동도 방해하지 않았다.

**001 한글 PDF**(backend) — 원리적으로 0바이트였던 게 맞다. 기존 추출기가 PDF를 문서가 아니라 바이트 더미로 보고 `(...)`만 긁었는데, 실물 한글 PDF는 예외 없이 Identity-H hex + ObjStm 안의 폰트 딕셔너리라 glyph id → 유니코드로 가는 길이 아예 없었다. 객체 구조를 따라가는 추출기로 교체해 **한글 0자 → 4,341자**. 수정 전 리비전에 물리면 9건이 red로 재현되는 것까지 확인했다.

한계는 명시됐다 — 스캔 PDF(OCR)·암호화 PDF는 여전히 0자이고, 그건 원래 C등급으로 선언한 경계다.

스위트 통과하면 넷을 한 번에 커밋하겠다.

### 2026-08-26T21:50:53+09:00 response (by nicehugepark)

실제 사고 1건 기록 — 커밋이 남의 in-flight 편집을 함께 삼켰다 (2026-08-26 21:4x).

경위: 두 세션이 같은 시각 web/index.html 을 고쳤다. 13b79042 는 반려 재작업으로
문서 본문 전이 버튼의 속성 이름을 data-goto → data-trans 로 바꾸는 중이었고,
dc4f4d76 는 같은 결함을 REQ-20260826-025-62x6 으로 갈라 "파이프 유무로 탭/전이를
가른다"는 다른 해법을 넣고 있었다. dc4f4d76 이 `git add web/index.html` 로
커밋(bfd3e9b)하는 순간, 아직 진행 중이던 13b79042 의 편집이 통째로 그 커밋에
실렸다.

나쁜 것은 충돌이 아니라 **조용한 성공**이다. 두 변경 모두 문법적으로 멀쩡해
커밋도 되고 서버도 뜬다. 그런데 합쳐진 상태는 버튼이 data-trans 를 내고 핸들러는
data-goto 만 읽는 상태라 **원래 고치려던 결함이 그대로 남는다** — 사용자가 다시
눌러보면 여전히 아무 반응이 없다. 같은 커밋에 함께 들어간 test_goto_dispatch
조차 통과하지 못한다. 즉 커밋 직전에 테스트를 한 번 돌렸다면 잡혔다.

이 사고가 말하는 것 둘:
1. `git add <경로>` 는 "내 변경을 담는다"가 아니라 "그 파일의 지금 상태를
   담는다"이다. 여러 세션이 붙은 파일에서 이 둘은 다르다.
2. 커밋 직전 테스트가 이 사고의 유일한 안전망이었다. 그것도 같은 커밋 안에
   이미 들어 있었는데 돌리지 않았다.

수습: 13b79042 가 data-trans 를 되돌려 025 의 계약(data-goto 단일 속성)에
맞췄다. 다만 이 세션은 git 권한이 없어 수습분은 작업 트리에만 있다.

### 2026-08-26T22:02:31+09:00 response (by nicehugepark)

실제 사례 하나가 오늘 발생했다 — 이 문서가 말하는 동시 작업 충돌의 구체적 형태다.

무슨 일이었나. 21:39 사용자가 REQ-20260826-023-62x6 을 반려했고, 21:40:06 무인 워커(세션 13b79042)가 재작업으로 스폰됐다. 그 워커는 반려 사유에 있던 "문서 본문 버튼 무반응"을 web/index.html 에서 고치는 중이었다. 같은 시각 리드(dc4f4d76)도 같은 결함을 같은 파일에서 고치고 있었다 — 리드는 그 워커의 존재를 몰랐다.

21:42 리드가 `git add web/index.html` 로 커밋하면서 **워커의 진행 중 편집을 함께 담았다.** 결과는 반쪽 상태였다: 워커가 바꾼 마크업(data-trans)과 리드가 남긴 핸들러(data-goto)가 서로 다른 이름을 보게 됐다. "고쳤다"고 커밋된 뒤에도 버튼은 여전히 죽어 있었다.

더 나쁜 2차 피해도 있었다. 리드가 회귀 조사를 하며 `git checkout web/index.html` 을 두 번 실행했다 — 그 시점의 미커밋 편집이 있었다면 통째로 사라졌을 것이다. 그리고 21:56 그래프 탭 캡처가 완전히 비어 나왔는데, 이는 워커가 파일을 쓰는 중에 읽어 **부분 기록된 JS** 를 파싱했기 때문으로 보인다(같은 캡처를 뒤에 다시 하니 정상). 사용자도 같은 시간대에 "그래프에 노드가 안 그려진다"를 보고했다.

여기서 읽어야 할 것.

1. **`git add <파일>` 은 내 변경을 담는 명령이 아니다** — 그 파일의 현재 상태 전부를 담는다. 남이 같은 파일을 만지는 중이면 남의 미완성이 내 커밋의 일부가 된다. 커밋 전에 `git diff --cached` 로 담기는 내용을 실제로 읽는 것 말고는 방어가 없다.
2. **`git checkout <파일>` 은 남의 작업을 지운다.** 되돌리기 전에 그 파일에 미커밋 변경이 있는지, 그게 내 것인지 확인해야 한다.
3. **반려는 워커를 띄운다.** 리드가 같은 문서를 이어서 처리하고 있으면 두 주체가 같은 파일에 앉는다. 리드는 반려 통지를 받은 순간 `state/auto_resume/spawn.log` 를 보는 습관이 필요하고, 스폰 전 유예 안에 리드가 클레임했으면 스폰이 아예 없어야 한다(REQ-20260826-013-62x6 과 같은 계열).
4. 결과적으로 두 고침이 겹쳤을 때 **어느 쪽이 옳은지는 판단해야 한다.** 이번엔 워커 쪽(이름을 가르는 것)이 옳았다 — 한 이름을 두 뜻으로 쓰는 한 조건을 아무리 정교하게 걸어도 다음 사람이 또 밟는다. 리드의 가드는 보조로 남겼다.

### 2026-08-26T23:48:29+09:00 response (by nicehugepark)

다섯 다 장치로 넣었다. 그리고 넣는 도중에 **같은 사고를 내가 두 번 더 냈다** — 그 사실이 이 작업의 근거를 가장 잘 말한다.

**넷째 사고(리드)**: `tests/test_response_stamp.py` 를 새 파일인 줄 알고 썼는데 이미 있던 파일이었다(REQ-024 규약 테스트 128줄). 커밋 직전 `git diff --cached --stat` 에서 "174줄 바뀜"을 보고서야 알았다. 커밋돼 있어 `git show HEAD:` 로 살렸고, 내 것은 `tests/test_stamp_accuracy.py` 로 갈랐다.

**다섯째(같은 뿌리, 다른 얼굴)**: 오늘 쓴 작업 노트 열 몇 건이 문서에 안 남았다. `s9 note <id> --label response "본문"` 이 인자 순서 때문에 죽는데, 그 에러가 **입력 원문을 그대로 되비춰서** 마지막 줄만 보면 성공과 구분이 안 됐다. REQ-20260826-041-62x6 로 고쳤고 이 노트도 다시 쓴 것이다. 파일이 덮이는 것과 기록이 안 남는 것은 손실의 모양만 다르다.

워커 봉투에는 "덮어쓰지 마라·checkout 쓰지 마라·커밋하지 마라"가 이미 적혀 있었고 나는 그 문장을 직접 썼다. 그러고도 냈다. **규율은 지켜지지 않을 때를 대비하지 못한다** — 이번 작업을 문장이 아니라 장치로만 센 이유다.

**C1 보이게 하기** (`s9 workers` + 프롬프트 훅 매 턴 주입). 스폰 사실은 `spawn.log` 에 늘 있었다. 아무도 안 봤다. 로그를 남기는 것과 보이게 하는 것은 다른 일이고, 보이지 않는 것은 없는 것과 같다.

**C2 스냅샷** — 다섯 중 **아무의 협조도 요구하지 않는 유일한 장치**다. 워처가 30초마다 미커밋 파일을 내용 주소로 떠 둔다. 누가 무엇으로 덮든 원본은 이미 딴 데 있다. 사라진 `test_question_type.py` 15건이 이것 하나로 살았을 것이다. 켜자마자 다른 작업자가 잡고 있던 `web/index.html` 을 즉시 떠 갔다. 복구는 `s9 snapshot --restore <경로>` 이고, 복구 직전에 '지금 것'도 떠서 복구가 또 하나의 덮어쓰기가 되지 않게 한다.

**C3 담긴 테스트 강제**. 전체 스위트는 2분이라 매 커밋에 물리면 그 무게가 규율을 먼저 죽인다 — **담긴 테스트**로 좁혔다. 21:42 커밋은 같은 커밋 안의 테스트조차 통과 못 했으니 이 좁은 게이트로도 잡힌다.

**C4 담기는 것 경고**. 워커가 도는데 90초 안에 바뀐 파일을 담으려 하면 막고, 도는 작업자와 그 파일을 이름으로 보여준다. `S9_ALLOW_CONCURRENT=1` 로 지나갈 수 있다 — 빠져나갈 문이 없는 게이트는 우회당하지 지켜지지 않는다. 막는 대신 **무엇을 확인해야 하는지**(`git diff --cached`)를 말한다.

**C5** 는 REQ-20260826-035-62x6 로 끝냈다.

**경계를 분명히 해 둔다**: 이 다섯은 **같은 파일을 둘이 만지는 것을 물리적으로 막지 않는다.** 막으려면 파일 락이 필요하고, 락은 죽은 주체가 잡은 채 굳는 문제를 새로 만든다(오늘 클레임에서 이미 두 번 봤다). 대신 **손실을 되돌릴 수 있게** 하고(C2), **모르는 채로 저지르지 않게** 한다(C1·C4). 조용한 성공이 충돌보다 나쁘다는 것이 이 문서의 결론이므로, 조용함을 없애는 쪽을 택했다.

### 2026-08-26T23:48:29+09:00 tdd (by nicehugepark)

- [x] C1. 살아있는 워커가 리드에게 보인다 — `s9 workers` + 프롬프트 훅이 매 턴 주입. 21:42 사고의 첫 고리("리드는 그 워커의 존재를 몰랐다")를 닫는다.
- [x] C2. 미커밋 변경은 덮어써도 잃지 않는다 — 워처가 30초마다 내용 주소로 떠 두고 `s9 snapshot --restore <경로>` 로 되살린다. 같은 내용은 두 번 뜨지 않고, state/ 와 2MB 초과는 건너뛴다. 복구 직전에 '지금 것'도 뜬다. tests/test_snapshot.py 6건.
- [x] C3. 커밋 전 테스트가 강제된다 — 커밋에 담긴 테스트 파일만 돌린다(전체는 2분이라 매 커밋에 물리면 규율이 먼저 죽는다). 21:42 커밋은 같은 커밋의 테스트조차 통과 못 했으므로 이 좁힌 게이트로 잡힌다.
- [x] C4. `git add <경로>` 가 담는 것을 경고한다 — 무인 워커가 도는데 90초 안에 바뀐 파일을 담으면 커밋을 막고 작업자·파일을 이름으로 보여준다. `S9_ALLOW_CONCURRENT=1` 로 지나갈 수 있다. tests/test_commit_gate.py 7건.
- [x] C5. 살아있는 세션 id 를 뺏지 않는다 — REQ-20260826-035-62x6 에서 구현·검증 완료.

## History
- 2026-08-26T19:43:04+09:00 created by nicehugepark (status: open)
- 2026-08-26T22:19:29+09:00 status: open -> in-progress (by nicehugepark) — drag 이동 [via dashboard]
- 2026-08-26T23:58:21+09:00 status: in-progress -> review (by nicehugepark) — 오늘 같은 사고가 다섯 번 났다 — 파일이 덮여 사라지고, 커밋이 남의 미완성을 삼키고, 작업 기록이 안 남았다. 전부 "조용한 성공"이었다: 겉으로는 다 잘된 것처럼 보였다. 그래서 막는 대신 조용함을 없앴다. ① 지금 도는 무인 작업자가 매 턴 내 화면에 뜬다 ② 커밋 안 한 파일을 30초마다 자동으로 떠 둔다 — 누가 덮어써도 `bin/s9 snapshot --restore <경로>` 로 되살아난다 ③ 커밋에 담긴 테스트를 자동으로 돌린다 ④ 작업자가 도는 중에 방금 바뀐 파일을 커밋하려 하면 막고 무엇을 확인해야 하는지 말해 준다. 확인은 눈으로 되는 게 아니라 앞으로 며칠 같은 사고가 다시 나는지로 된다 — 다만 `bin/s9 snapshot` 을 쳐 보면 지금 떠 둔 목록이 보인다.
- 2026-08-27T00:01:41+09:00 status: review -> done (by nicehugepark) — [via dashboard]
