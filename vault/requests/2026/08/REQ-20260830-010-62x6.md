---
id: REQ-20260830-010-62x6
type: request
title: 조각 하나가 통째로 죽는다
summary: 글자를 끌면 events.js 가 죽어 판 전체의 버튼이 멎는다 — e.target 이 요소가 아닐 때
goal: 판 위의 글자를 끌거나 눌러도 조각이 죽지 않고, 대상을 요소로 바꾸는 자리가 화면 전체에 하나뿐이다
status: done
size: S
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: d15c553b
project: section9
relates_why: {}
tags: ["assets", "dashboard", "terminal"]
created: 2026-08-30T12:39:20+09:00
status_since: 2026-08-30T12:52:40+09:00
updated: 2026-08-30T12:52:40+09:00
priority: 50
---

## Original

또 이런 오류가 발생했다.
[Image: /home/sjpark1/section9/state/terminal/uploads/nicehugepark/20260830T123703-image.png]

화면 조각 1개가 이 브라우저에서 죽었습니다. 그 조각이 맡던 자리는 그려지지 않습니다.
- app/events.js:5 — TypeError: Uncaught TypeError: e.target.closest is not a function
UA: Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/151.0.0.0 Safari/537.36

## Notes

### 2026-08-30T12:52:06+09:00 response (by nicehugepark)

### 문을 하나로 — 대상이 요소가 아닐 때

**무엇이었나.** `e.target` 은 요소라는 보장이 없습니다. 판 위의 글자를 끌면
dragstart 의 target 이 텍스트 노드이고, 텍스트 노드에는 `closest` 가 없습니다.
그 한 줄이 던진 TypeError 가 `events.js` 를 죽이면 그 조각이 맡은 자리 —
판의 거의 모든 버튼 — 이 한꺼번에 멎습니다. 화면을 조각으로 가른 뒤로
죽는 단위가 곧 조각 하나이기 때문입니다.

**처음이 아니었습니다.** REQ-20260830-006-62x6 에서 `tidy.js` 가 똑같이 죽었고,
그때는 그 조각 안에 방어를 하나 세워 막았습니다. 조각마다 자기 방어를 두면
조각 수만큼 구멍이 남고, 남은 구멍에서 그대로 다시 터졌습니다.

**고침.** 대상을 요소로 바꾸는 자리를 `evEl` 한 곳으로 모았습니다
(web/app/state.js). 요소면 그대로, 텍스트 노드면 감싼 요소로, 그것도 없으면
null 입니다 — 감싼 요소로 올리는 편이 낫습니다: 단추 안 글자를 눌렀을 때
null 이면 단추를 못 찾아 클릭이 통째로 무시됩니다. 56자리를 이 문으로
돌렸고, 손으로 덧대 두었던 `closest &&` 들과 `tidy.js` 의 자기 방어(`near`)도
같은 문으로 접었습니다. `userform.js` 에 남아 있던 `e.target.classList` 한
자리도 같은 위험이라 함께 걷었습니다.

**막는 장치.** `tests/test_event_target_gate.py` 5건 — 그중 G2 가 본체입니다:
어느 조각도 대상에게 직접 묻지 않는다(`closest`·`matches`·`classList`·`dataset`).
한 자리라도 문을 비껴가면 그 자리가 다음 사고입니다.

**실제로 눌러 봤습니다.** `web/verify-evel-drag.html` (DOC-20260830-001-62x6 의 방법):
카드 안 텍스트 노드에 dragstart·dragover·drop·click 을 던지고, 예외가 없는지와
**그러고도 탭이 실제로 켜지는지**를 함께 봅니다 — 죽은 조각의 증상은 예외가
아니라 침묵이라, 예외 없음만으로는 살아 있음의 증거가 못 됩니다. 10건 전부
통과, 지킴이가 알린 죽은 조각 없음.
캡처: /tmp/claude-1000/-home-sjpark1-section9/d15c553b-8362-411a-a4f7-ed7f1d8e2b4b/scratchpad/evel3.png

**곁가지 하나.** 그 캡처를 찍다 다른 세션이 12:46 에 넣은 `const rs` 이름
충돌을 발견해 함께 회수했습니다(문법 오류라 판이 통째로 멎어 있었습니다).
경위는 REQ-20260829-024-62x6 노트에 남겼습니다.

## History
- 2026-08-30T12:39:20+09:00 created by nicehugepark (status: open)
- 2026-08-30T12:39:36+09:00 status: open -> in-progress (by nicehugepark) — 판이 살아 있는 채로 버튼만 멎는 사고라 먼저 집는다. e.target 은 요소가 아닐 수 있다(글자를 끌면 텍스트 노드) — 한 자리에서 막는다
- 2026-08-30T12:52:40+09:00 status: in-progress -> done (by nicehugepark) — 실제 브라우저에서 확인해 닫습니다. 카드 안 글자에 드래그·클릭을 던져도 예외가 없고, 그러고도 탭이 정상적으로 켜집니다(10건 전부 통과, 죽은 조각 없음). 대상을 요소로 바꾸는 자리를 evEl 한 곳으로 모아 56자리를 그 문으로 돌렸고, 시험 G2 가 비껴가는 자리를 막습니다. 화면을 F5 하면 바로 적용됩니다 — 서버 재기동은 필요 없습니다(조각만 바뀌었습니다).
