---
id: REQ-20260829-024-62x6
type: request
title: 사람이 일을 세운다
summary: 깨우기의 반대편 — 진행 중인 작업을 화면에서 강제로 중단하고, 계정·모델 변경과 섞어 쓴다
status: review
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: 9bc92096
sessions: ["911949e0", "9bc92096"]
agents: ["lead:claude-opus-5", "sub:ux-writer:a69098f3", "sub:designer:abe4d716"]
contributions: [{"actor": "lead:claude-opus-5", "item": "`[2026-08-29 21:19:55 KST - lead]`", "started": "2026-08-29T21:19:56+09:00", "ended": "2026-08-29T21:19:56+09:00", "result": "done", "transcript": ""}, {"actor": "sub:ux-writer:a69098f3", "item": "Rename wake/stop labels", "started": "2026-08-30T12:38:17+09:00", "ended": "2026-08-30T12:44:24+09:00", "result": "done", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/9bc92096-ff05-427b-a7fd-0a4d7f8189d2/tasks/a69098f3c60bf58ed.output"}, {"actor": "sub:designer:abe4d716", "item": "Card handle layout review", "started": "2026-08-30T12:38:39+09:00", "ended": "2026-08-30T12:50:12+09:00", "result": "done", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/9bc92096-ff05-427b-a7fd-0a4d7f8189d2/tasks/abe4d716ee4ab86bf.output"}, {"actor": "sub:ux-writer:a69098f3", "item": "`[2026-08-30 12:38:21 KST - ux-writer]`", "started": "2026-08-30T12:44:24+09:00", "ended": "2026-08-30T12:44:24+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/9bc92096-ff05-427b-a7fd-0a4d7f8189d2/subagents/agent-a69098f3c60bf58ed.jsonl"}, {"actor": "sub:designer:abe4d716", "item": "`[2026-08-30 12:47:12 KST - designer]`", "started": "2026-08-30T12:50:12+09:00", "ended": "2026-08-30T12:50:12+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/9bc92096-ff05-427b-a7fd-0a4d7f8189d2/subagents/agent-abe4d716ee4ab86bf.jsonl"}]
project: section9
relates: ["REQ-20260828-041-62x6", "REQ-20260827-079-62x6"]
relates_why: {"REQ-20260828-041-62x6": "깨우기와 한 쌍이다 — 같은 카드에서 세우고 깨운다", "REQ-20260827-079-62x6": "계정·모델을 바꿀 때 하던 일을 멈추는 그 걸음을 이 요청이 손잡이로 꺼낸다"}
tags: ["auto-audit", "session"]
created: 2026-08-29T16:48:34+09:00
status_since: 2026-08-30T13:05:33+09:00
updated: 2026-08-30T13:05:33+09:00
priority: 50
---

## Original

진행 중인 작업을 깨우기 기능도 지금 진행 중인데, 의도하지 않게 멈춘 작업들을 깨우려고 하는건데, 반대로 진행 중인 작업들을 강제로 중단하는 기능도 만들어라. 그래야 계정을 변경하거나 모델을 바꿀 때 그 기능을 같이 섞어서 사용할 수 있다.

## Notes

### 2026-08-29T20:09:11+09:00 decision (by nicehugepark)

착수 지시 접수. 순서를 뒤에 둔다 — 서버 몫이 `bin/s9` 의 작업자 수명이라 지금 그 파일을 쓰고 있는 033·034(클레임 수명)와 같은 자리다. 겹쳐 붙이면 서로를 덮는다. 그 둘이 닫히는 대로 착수한다. 참고로 021 이 방금 낸 `worker_stop()` 이 이 요청의 절반을 이미 만들어 뒀다 — 남은 것은 화면 손잡이와 계정·모델 변경과의 결합이다.

### 2026-08-29T21:04:41+09:00 workspace (by nicehugepark)

본 저장소에서 이어받는다 — 자기 도구를 고치는 일이라 샌드박스가 사본에서 범위를 자른다. 커밋하면 다음 라운드부터 작업자가 다시 자기 워크트리를 받는다.

### 2026-08-29T21:07:35+09:00 tdd (by nicehugepark)

서버 몫 TDD 시나리오 (라운드2, tests/test_stop_reach.py). 021 의 worker_stop() 은 세션이 세션을 세우는 문이라 화면에서는 못 쓴다 — 대시보드에는 세션이 없다. 그 위에 얹을 것을 시험으로 먼저 못 박는다.

S1 화면이 읽는 계약: stop_request(id) 는 wake_request 와 같은 모양 {ok,id,action,message} 를 낸다. action: stopped·none·missing·not-request·error.
S2 두 번 눌러도 안전하다: 도는 작업자가 없으면 오류가 아니라 ok=True, action=none.
S3 죽이는 자리는 여전히 하나다: stop_request 는 자기 손으로 안 죽이고 worker_stop 을 지난다.
S4 사람의 권한은 클레임이 아니라 소유다: worker_stop(owner=True) 는 클레임 게이트를 지나되 이유는 여전히 필수 — 화면이 기본 사유를 실어 보낸다.
S5 세션끼리는 그대로다: owner=False 면 집지 않은 세션은 여전히 거부되고 신호를 보내지 않는다.
S6 여럿을 한 번에: stop_all_workers() 가 도는 작업자 전부를 세우고 센 수를 돌려준다 — 두 번째 kill 자리를 만들지 않는다.
S7 계정·모델과 섞인다: restart_session(stop_workers=True) 가 재기동 직전에 작업자를 세우고 그 수를 응답에 싣는다. 옛 계정으로 도는 작업자를 남긴 채 바꾸면 요금과 권한이 갈린다.
S8 손이 닿는다: POST /api/stop 이 stop_request·stop_all_workers 를 지나고, CLI 는 s9 workers --stop-all --why 를 받는다.
S9 감사: 누른 것도 거부도 _auto_log 한 줄로 남는다 (깨우기와 같은 이유 — 다음 반려 때 짐작하지 않기 위해).

화면 손잡이(web/index.html)는 이번 라운드 밖이다 — REQ-20260829-027-62x6 이 그 파일을 가르는 중이라 같은 자리를 열면 서로 덮는다.

### 2026-08-29T21:19:16+09:00 response (by nicehugepark)

라운드2 (세션 9bc92096) — 서버·CLI 몫을 적용하고 검증했다. 화면 손잡이는 남았다.

무엇을 했나 (bin/s9)
· worker_stop(owner=True): 사람이 화면에서 누른 것은 클레임이 아니라 소유가 근거다. 대시보드에는 세션이 없어 클레임 게이트를 그대로 두면 아무도 못 세운다. 이유는 여전히 필수고, 기록에 "사람 nicehugepark" / "세션 xxxxxxxx" 로 갈라 남는다. 세션끼리의 규칙(집지 않은 세션은 거부)은 글자 그대로 그대로다.
· stop_request(id, actor, why): 깨우기의 반대편. 반환이 wake_request 와 같은 모양 {ok,id,action,message} 라 화면은 같은 코드로 읽는다. action = stopped·none·missing·not-request·error. 도는 것이 없으면 오류가 아니라 none — 두 번 눌러도 안전하다. 자기 손으로는 아무것도 안 죽인다(worker_stop 한 문만 지난다).
· stop_all_workers(): 도는 작업자를 한 번에. 계정·모델을 바꾸기 전 판을 비우는 걸음이다.
· restart_session(stop_workers=True): 계정·모델 변경과 세우기를 한 걸음으로 묶었다. 세우는 자리는 **모든 거부 갈래를 지난 뒤**다 — 못 바꾸면서 돌던 일만 죽이면 사람이 잃기만 한다. 응답에 stopped(센 수)를 싣는다.
· POST /api/stop ({id} 한 건 / {all:true} 전부), POST /api/session/restart 에 stop_workers 전달, CLI `s9 workers --stop-all --why ...`.
· 감사: STOP-PRESS·STOP-ALL 한 줄이 거부 갈래까지 남는다(깨우기와 같은 이유 — 다음 반려 때 "버튼이 없던 것"인지 "눌렀는데 안 된 것"인지 짐작하지 않기 위해).

검증: python3 tests/ stop_reach worker_stop session_restart restart_reach session_wake → 68건 OK. 새 시험은 tests/test_stop_reach.py 16건(S1~S9).

남은 것: 화면 손잡이(카드의 세우기 버튼, 계정·모델 팝업의 "세우고 바꾸기" 갈래). 이번 라운드에 안 건드린 이유는 REQ-20260829-027-62x6 이 지금 web/index.html 을 css/·app/ 조각으로 가르는 중이기 때문이다 — 같은 자리를 열면 서로 덮는다. 서버는 이미 열려 있으니 그 위에 버튼만 얹으면 된다.

### 2026-08-29T21:19:27+09:00 note (by nicehugepark)

동시 편집 사실 기록 (충돌 아님 · 리드 확인용). 21:05 착수 시점 bin/s9 는 깨끗했는데, 21:12 께 다른 작업자가 같은 파일의 다른 자리(stall_verdict/stall_mins 재작성 = REQ-20260829-036-62x6, _claim_req·catalog_with_live·live_agents)를 고치고 있었다. 내 편집은 worker_stop 주변·wake_request 뒤·/api/stop·restart_session·cmd_workers 로 서로 겹치지 않는 자리라 문자열 치환으로만 넣었고 남의 줄은 건드리지 않았다. web/index.html·tests/*.py 다수도 REQ-027 이 지금 가르는 중이라 손대지 않았다.

주의: 그래서 지금 전체 스위트(python3 tests/)는 551건 실패다 — 화면 파일이 갈라지는 중간 상태(index.html 14778줄 삭제)와 036 의 stall 판정 재작성이 겹친 결과지 이 요청의 변경 탓이 아니다. 이 요청 범위의 68건은 통과한다.

### 2026-08-29T21:19:56+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-08-29 21:19:55 KST - lead]`

REQ-20260829-024를 이어받아 서버·CLI 몫을 적용하고 검증했습니다. 화면 손잡이는 남았습니다.

**적용한 것 (`bin/s9`)**
- `worker_stop(owner=True)` — 021이 낸 문은 *세션이 자기가 집은 문서의 작업자를 세우는* 문이라 화면에서는 열 수 없었습니다(대시보드에는 세션이 없습니다). 사람의 근거를 클레임이 아니라 소유로 갈랐고, 이유는 여전히 필수이며 기록이 `사람 …`/`세션 …`으로 갈립니다. 세션끼리의 규칙은 그대로입니다.
- `stop_request(id, actor, why)` — 깨우기의 반대편. 반환이 `wake_request`와 같은 모양 `{ok,id,action,message}`라 화면은 같은 코드로 읽습니다. 도는 것이 없으면 오류가 아니라 `none`(두 번 눌러도 안전). 죽이는 자리는 여전히 `worker_stop` 하나뿐입니다.
- `stop_all_workers()` + `restart_session(stop_workers=True)` — 계정·모델 변경과 세우기를 한 걸음으로 묶었습니다. 세우는 것은 **모든 거부 갈래를 지난 뒤**입니다(못 바꾸면서 돌던 일만 죽이면 잃기만 합니다).
- `POST /api/stop`(한 건/전부), `/api/session/restart`의 `stop_workers`, CLI `s9 workers --stop-all --why …`, 감사 로그 `STOP-PRESS`·`STOP-ALL`.

**검증**: `python3 tests/ stop_reach worker_stop session_restart restart_reach session_wake` → 68건 OK. 새 시험 `tests/test_stop_reach.py` 16건(S1~S9)을 먼저 빨갛게 세우고 채웠습니다.

**남긴 것과 이유**: 카드의 세우기 버튼과 계정·모델 팝업의 "세우고 바꾸기"는 손대지 않았습니다. REQ-20260829-027이 지금 `web/index.html`을 `css/`·`app/` 조각으로 가르는 중이라 같은 자리를 열면 서로 덮습니다. 서버는 열려 있으니 다음 라운드에 버튼만 얹으면 됩니다 — 그래서 review로 올리지 않고 in-progress로 뒀습니다(지금은 사람이 눌러 볼 것이 없습니다).

주의할 사실 하나: 전체 스위트는 지금 551건 실패인데, 화면 파일이 갈라지는 중간 상태와 036의 stall 판정 재작성이 겹친 결과지 이 변경 탓이 아닙니다. 이 사실도 문서에 노트로 남겼습니다.

### 2026-08-29T22:20:09+09:00 commit (by nicehugepark)

커밋 2faa0d2 — 화면을 조각으로 가르고, 멈춤이 일하는 손을 본다 (REQ-027·036·024)

### 2026-08-30T00:14:10+09:00 workspace (by nicehugepark)

본 저장소에서 이어받는다 — 화면 작업이라 살아 있는 서버가 읽는 자리에서 해야 한다. 커밋하면 다음 라운드부터 작업자가 다시 자기 워크트리를 받는다.

### 2026-08-30T00:26:47+09:00 response (by nicehugepark)

라운드3 (세션 9bc92096) — 화면 손잡이를 붙였다. 서버 몫은 라운드2 에서 커밋됐다(2faa0d2).

무엇을 했나
· bin/s9 catalog_with_live: 행에 `worker{pid,age}` 를 싣는다. 점(live_kind)으로는 알 수 없다 — 그 값은 클레임 **전**(spawned)만 말하고, 작업자가 문서를 집는 순간 direct 로 덮여 "지금 돌고 있다"는 사실이 행에서 사라진다. 판정은 새로 짓지 않고 중복 스폰을 막는 worker_running() 을 그대로 쓴다.
· web/app/card.js: workRowHTML() — 「작업 중 · 무인 작업자가 N분째 돌고 있다」 줄 + 「세우기」. stallHTML 안에서 짓는다: 카드와 문서 화면이 부르는 자리가 그 함수 하나라, 밖에 두면 같은 요청이 한 자리에선 세워지고 다른 자리에선 안 세워진다(이 화면이 이미 겪은 갈래).
· stopDoc(): **먼저 묻는다**. 깨우기에는 없던 걸음이다 — 깨우기는 아무 일도 안 하던 것을 굴리고, 세우기는 일하는 것을 끝낸다. 답은 서버의 message 를 그대로 옮기고 action 으로 문구를 가르지 않는다.
· web/app/restart.js·session.js: 계정·모델을 바꿀 때 도는 작업자가 있으면 「N건을 세우고 바꿉니다」를 한 번 묻고, 고른 것을 stop_workers 로 실어 보낸다(서버는 거부 갈래를 다 지난 뒤에 세운다). 물러날 자리는 그만두기 하나다 — 되돌릴 수 없는 일이 둘 붙은 창에서 Esc 가 그중 하나만 취소하면 사람이 예상할 수 없는 답이 된다.
· web/css/actions.css: .rvpt.work / .acts.stoprow — 새 층 없이 카드가 이미 쓰는 한 줄 문법과 행동 버튼을 그대로 입는다. 잉크는 멈춤 줄과 같은 in-progress 색(같은 상태의 두 얼굴이다).
· 진단 `?work=<분>[&workhold]`: 도는 작업자는 캡처하려는 그 순간에 없다. 깨우기가 두 번 고쳐 올려지는 동안 한 번도 눈으로 확인되지 못한 이유가 그것이라 같은 함정을 피했다.

검증: python3 tests/ stop_reach stall_pair wake_handle catalog_atomic board_counts board_done_order session_pick session_restart restart_reach account_and_model account_switch worker_stop workspace_chip → 173건 OK. 새 시험은 tests/test_stop_reach.py 23건(S1~S16). 캡처: scratchpad/stop-handle2.png (?work=12&workhold — 「세우기」와 눌린 「세우는 중…」이 한 화면에).

서버 재기동이 필요하다: 지금 9909 는 23:01 기동이라 /api/stop 도 worker 필드도 없다(대시보드 머리에 그 경고가 이미 서 있다). 재기동 전에는 손잡이가 실데이터로 서지 않는다.

### 2026-08-30T00:27:07+09:00 note (by nicehugepark)

이번 라운드에 만진 파일과 그 근거 (리드 확인용). 받은 목록은 bin/s9 · tests/test_stop_reach.py · web/index.html 이었는데, 027 이 화면을 조각으로 가른 뒤 index.html 은 215줄짜리 껍데기가 됐다 — 손잡이가 사는 자리가 web/app/card.js 로 옮겨갔다. 그래서 목록의 뜻(세우기의 화면 몫은 내 것)을 따르되 실제로 고친 것은 다음이고, 전부 착수 시점에 미커밋 변경이 없는(=남이 안 잡고 있는) 파일이다: web/app/card.js · web/app/events.js · web/app/restart.js · web/app/session.js · web/css/actions.css · tests/test_stall_pair.py(노드 하네스가 stallHTML 을 평가하는데 새 함수가 없어 ReferenceError 로 죽었다 — 함수 두 개를 명단에 더한 것이 전부다).

bin/s9 는 착수 시점에 남의 미커밋 변경이 있었다(REQ-014 등: 세션·터미널·resume 자리). 겹치지 않는 자리(catalog_with_live) 에 문자열 치환으로 열한 줄만 더했고 남의 줄은 건드리지 않았다. 만지지 않은 것: web/app/docs.js(문서 화면은 stallHTML 을 이미 부르므로 고칠 것이 없었다) · web/app/app.js(진단을 파이프라인에 새 줄로 붙이는 대신 이미 그 자리에 선 stallProbe 가 이웃을 부르게 했다).

캡처 중 관찰한 남의 일: 9909 첫 로드에서 app/state.js·docs.js·events.js 가 죽고 판이 비는 화면을 한 번 봤다(오류는 AUDIT_PAGE·$ 미정의 — 즉 const.js·state.js 가 안 실렸다는 뜻이고, 같은 시각 /app/const.js 단독 요청은 ERR_CONNECTION_RESET 이었다). 조각이 마흔 개로 늘면서 DOC-20260827-004-62x6(WSL2 루프백 동시 연결 벼랑)에 닿은 것으로 보인다 — 두 번째 로드는 정상이었다. 이 요청의 변경과는 무관하지만 사람이 첫 F5 에서 빈 판을 볼 수 있어 남긴다.

### 2026-08-30T12:40:24+09:00 tdd (by nicehugepark)

라운드4 TDD 시나리오 — 반려 ①「멈춰놓고선 다시 시작할 수 있는 기능이 없다」.

무엇이 실제로 막고 있었나 (코드를 읽어 확인한 것):
· 세우면 worker_stop 이 문서에 사유 노트를 남긴다 → updated 가 방금이 된다 → stall_verdict 는 moving 이라 stalled_mins 를 안 준다 → 15분 동안 「깨우기」가 아예 안 그려진다.
· 그 15분을 기다려도 per-REQ 쿨다운(600초)이 「방금 이 요청을 한 번 깨웠습니다」로 막는다. 즉 세운 사람은 자기가 세운 것을 되돌릴 수 없다.
· REQ-20260830-007-62x6 의 ux-writer 가 "세우기는 깨우기로 되살릴 수 있어 되돌릴 수 없는 일이 아니다"라고 적은 전제가 실제로는 거짓이었다.

R1 세운 사실이 남는다: worker_stop 성공 시 stop 표시(at·by·why)를 남긴다. 세울 것이 없었으면 남기지 않는다.
R2 사람이 누르는 길은 쿨다운을 지나간다: 세워 둔 요청이면 per-REQ 쿨다운이 「다시 시작」을 막지 않는다(하루·시간 예산은 그대로 든다). 쿨다운이 재는 것은 겹쳐 붙는 손인데, 세운 자리에는 붙어 있는 손이 없다.
R3 세운 것을 기계가 되살리지 않는다: 워처(reason≠wake)는 그 표시가 있는 동안 스폰하지 않는다 — 사람이 세운 것을 30초 뒤에 되살리면 세우기가 무의미하다.
R4 다시 시작하면 표시가 사라진다: 새 작업이 실제로 뜨면(_auto_mark_pid) 표시를 지운다.
R5 표시는 늙는다: 오래된 표시는 워처를 영원히 묶지 않는다.
R6 wake_request 가 세워 둔 요청을 moving 으로 거절하지 않는다 — 세운 그 자리가 곧 "조용하다"의 근거다.
R7 행이 그 사실을 나른다: catalog 행에 stopped{at,age,by} 가 실린다(작업이 도는 동안은 아니다).
R8 화면: 세워 둔 카드에 다시 시작 손잡이가 서고, 그 길은 깨우기와 **같은 길**(data-wake→wakeDoc)이다. 같은 일을 하는 손잡이를 한 카드에 둘 그리지 않는다 — 세워 둔 사실이 멈춤보다 구체적인 근거라 그쪽을 세운다.

반려 ②「깨우기·세우기 용어」는 ux-writer·designer 에게 검토를 맡겼다(사용자가 지목한 그 둘). REQ-20260830-007-62x6 이 팝업 문구를 이미 한 결로 맞춰 review 에 올라가 있어, 그 용어 사전과 어긋나지 않게 낱말만 다시 고른다.

### 2026-08-30T12:44:24+09:00 subagent (agent: sub:ux-writer:a69098f3, by nicehugepark)

`[2026-08-30 12:38:21 KST - ux-writer]`

### 결론 먼저

**① = ③ 은 같은 낱말 「이어가기」, ② 는 「중단하기」.** 그리고 상태 낱말과 행동 낱말을 품사가 아니라 **주체**로 갈랐다 — `멈춤`은 저절로 조용해진 **상태**, `중단`은 사람이 끝낸 **행동**. 이 한 줄이 REQ-007이 지키려던 제약(멈춤/멈추기 충돌)을 그대로 지키면서 「세우기」를 버리는 길이다.

| | 자리 | 낱말 | 왜 |
|---|---|---|---|
| ① | 멈춘 요청을 자동 작업에 맡김 | **이어가기** | Settings의 `자동 이어가기`(auto_resume)와 **같은 가족**이다. 사전에 이미 있는 말이라 새 낱말이 아니고, 은유가 아니다 |
| ② | 진행 중 자동 작업을 끝냄 | **중단하기** | `멈춤`(상태)과 글자가 겹치지 않고, `done`(완료)로 오해될 여지가 없다. 서버는 이미 "이유 없는 중단은 나중에 판정할 수 없다"고 이 낱말을 쓰고 있다 |
| ③ | 중단한 것을 다시 맡김 | **이어가기** (①과 동일) | 같은 일·같은 API·같은 결과에 두 번째 이름을 주지 않는다 |

**①과 ③을 같은 낱말로 둔 근거**: 처지가 다른 것은 사실이지만, 그 차이는 **버튼이 아니라 버튼 위의 줄**이 말한다(`멈춤 44분째 진전 없음` vs `중단 12분 전`). 이 화면의 문법이 이미 「캡션+사실+손잡이」다. 손잡이에 처지까지 실으면 사용자가 배울 낱말이 셋이 되고, "깨우기와 다시 시작이 같은 건가?"를 매번 맞춰 봐야 한다. 사용자의 반려문("멈춰놓고선, 다시 시작할 수 있는 기능이 없다")은 *다른 이름*을 요구한 게 아니라 **그 자리에 손잡이가 없다**는 말이다.

### 탈락시킨 후보

| 후보 | 자리 | 탈락 이유 |
|---|---|---|
| **「다시 시작」** (③ 가안) | ③ | `restart.js`가 세션 재기동 창의 눈썹으로 이미 `cap: "다시 시작"`을 쓴다. 계정을 바꾸며 자동 작업을 중단하는 그 흐름에서 두 창이 같은 낱말로 서로 다른 것을 가리키게 된다. 게다가 ①과 한 일을 둘로 나눈다 |
| **「멈추기」** | ② | REQ-007의 논거가 여전히 사실이다 — `멈춤 44분` 옆의 `멈추기`는 상태와 행동이 한 글자로 겹친다 |
| **「끝내기」** | ② | Board에 `done` 열이 있어 "이 요청을 완료 처리한다"로 읽힌다. 되돌릴 수 없다고 오해하면 아예 안 누른다 |
| **「그만두기」** | ② | `restart.js`에서 대화상자의 **취소** 라벨(`cancel: "그만두기"`)로 이미 쓰인다 |
| **「재개」·「계속」** | ①③ | 「계속」은 확인 버튼의 관용 라벨이라 창에서 뜻이 갈린다. 「재개」는 `자동 이어가기` 설정 이름과 붙지 않는다 |
| **「깨우기」 유지** | ① | 사용자가 직접 뒤집었다. 잠·기상 은유라 ③("내가 방금 중단한 것")에 얹으면 말이 안 된다 |

---

### 먼저 읽어야 할 것 — 낱말만으로는 ③이 서지 않는다

문구를 적용하기 전에 리드가 알아야 할 사실 둘이다. 코드를 읽어 확인했다.

**(A) 중단한 흔적이 카탈로그 행에 없다.** `worker_stop()`은 pidfile을 지우고(`bin/s9:2603`) 노트만 남긴다. 그래서 중단 직후 카드에는 `worker`도 `stalled_mins`도 없어 **아무 줄도 그려지지 않는다** — ③의 줄을 그리려면 행에 `stopped:{at,by,why}` 같은 필드가 실려야 한다.

**(B) 중단 직후의 「이어가기」를 서버가 거절한다.** `_stop_note()`(`bin/s9:2532`)가 `s9 note … --label stop`으로 문서를 건드려 `updated`가 방금이 된다 → `_wake_block`이 `moving`을 돌려준다: 「아직 멈추지 않았습니다 — 0분 전 이 요청이 움직였습니다. 15분 넘게 아무 움직임이 없을 때만 …」(`bin/s9:6209-6212`). **사람이 제 손으로 중단한 것은 "아직 안 멈춤"이 아니다** — 이 게이트(그리고 600초 쿨다운, `bin/s9:2021`)를 중단 직후에는 면제해야 한다. 이것이 사용자가 겪은 "다시 시작할 수 없다"의 기계적 뿌리이고, 버튼만 붙이면 같은 반려가 한 번 더 온다.

---

### 표 1 — 화면 (`web/app/`)

| 자리 | 지금 문자열 | 바꿀 문자열 | 파일:함수 |
|---|---|---|---|
| ① 손잡이 | `${going ? "깨우는 중…" : "깨우기"}` | `${going ? "이어가는 중…" : "이어가기"}` | `card.js:stallHTML` (L88) |
| ① 툴팁 | `자동 작업이 이 요청을 이어서 진행합니다` | (그대로 — 이미 새 낱말과 한 결이다) | `card.js:stallHTML` (L87) |
| ① 낙관적 도색 | `b.textContent = going ? "깨우는 중…" : "깨우기";` | `b.textContent = going ? "이어가는 중…" : "이어가기";` | `card.js:paintWake` (L413) |
| ① 연결 실패 | `서버가 깨우기를 알지 못합니다` | `서버가 이어가기를 알지 못합니다` | `card.js:wakeDoc` (L442) |
| ① 결과 눈썹 | `cap: d.ok ? "깨우기" : "깨우지 않음"` | `cap: d.ok ? "이어가기" : "이어가지 않음"` | `card.js:wakeDlg` (L468) |
| ② 줄 툴팁 | `자동 작업이 이 요청을 맡아 진행 중입니다 — 세우면 지금 하던 일이 거기서 끝나고, 세운 사유가 문서에 남습니다` | `자동 작업이 이 요청을 맡아 진행 중입니다 — 중단하면 지금 하던 일이 거기서 끝나고, 중단한 사유가 문서에 남습니다` | `card.js:workRowHTML` (L110-111) |
| ② 줄 본문 | `<span class="rvcap">진행 중</span>자동 작업 ${mins}` | (그대로) | `card.js:workRowHTML` (L114) |
| ② 손잡이 툴팁 | `진행 중인 자동 작업을 세웁니다 — 계정이나 모델을 바꾸기 전에 씁니다` | `진행 중인 자동 작업을 중단합니다 — 계정이나 모델을 바꾸기 전에 씁니다` | `card.js:workRowHTML` (L117) |
| ② 손잡이 | `${going ? "세우는 중…" : "세우기"}` | `${going ? "중단 중…" : "중단하기"}` | `card.js:workRowHTML` (L118) |
| ② 낙관적 도색 | `b.textContent = going ? "세우는 중…" : "세우기";` | `b.textContent = going ? "중단 중…" : "중단하기";` | `card.js:paintStop` (L476) |
| ② 확인 눈썹 | `cap: "세우기"` | `cap: "중단하기"` | `card.js:stopDoc` (L491) |
| ② 확인 제목 | `진행 중인 자동 작업을 세울까요?` | `진행 중인 자동 작업을 중단할까요?` | `card.js:stopDoc` (L494) |
| ② 확인 본문 | `지금 하던 일은 문서에 적힌 데까지만 남고, 그 뒤로 진행 중이던 것은 사라집니다. 세운 사실과 사유는 문서에 남습니다. 다시 진행하려면 카드가 멈춤으로 바뀐 뒤 깨우기를 누르면 됩니다.` | `지금 하던 일은 문서에 적힌 데까지만 남고, 그 뒤로 진행 중이던 것은 사라집니다. 중단한 사실과 사유는 문서에 남습니다. 다시 맡기려면 같은 자리에 생기는 「이어가기」를 누르면 됩니다.` | `card.js:stopDoc` (L495-497) |
| ② 확인 버튼 | `ok: "세우기", cancel: "그대로 두기"` | `ok: "중단하기", cancel: "그대로 두기"` | `card.js:stopDoc` (L498) |
| ② 연결 실패 | `서버가 세우기를 알지 못합니다` | `서버가 중단하기를 알지 못합니다` | `card.js:stopDoc` (L514) |
| ② 결과 눈썹 | `cap: d.ok ? "세우기" : "세우지 않음"` | `cap: d.ok ? "중단하기" : "중단하지 않음"` | `card.js:stopDoc` (L522) |
| **③ 상태 줄** (신규) | — | `<div class="rvpt stopped" title="${사용자} 가 12분 전 이 요청의 자동 작업을 중단했습니다 — 「이어가기」를 누르면 자동 작업이 이어서 진행합니다"><span class="rvcap">중단</span>${경과} 전${사유 ? " · " + 사유 : ""}</div>` + ①과 **같은** `data-wake` 손잡이 | `card.js` — `workRowHTML` 옆 `stopRowHTML`, 호출은 `stallHTML` 안에서 |
| ② 확인 본문 (진단) | `message: "이 요청을 깨웠습니다 — 자동 작업이 이어서 진행합니다. …"` | `message: "자동 작업이 이 요청을 이어서 진행합니다. …"` (서버와 한 글자까지 같게) | `diag.js` (L266) |

③ 줄의 본문을 「12분 전」으로 짧게 둔 이유: 캡션이 이미 `중단`을 말한다. 캡션이 말한 것을 본문이 되풀이하면 좁은 줄에서 같은 낱말이 두 번 선다 — `진행 중` 줄이 이미 세운 규칙이다. 사유는 서버가 지은 문장이라 길 수 있으니 20자에서 자르고 전문은 툴팁에 둔다.

### 표 2 — 계정·모델 창 (`web/app/restart.js`)

이 파일은 지금 **자동 작업 중단은 「세우고」, 세션 중단은 「멈추고」**로 두 낱말을 쓴다. 같은 흐름에서 창 두 개가 잇따라 뜨므로 **둘 다 「중단」으로 모아야** 낱말 하나=뜻 하나가 성립한다.

| 자리 | 지금 문자열 | 바꿀 문자열 | 함수 |
|---|---|---|---|
| 자동 작업 확인 제목 | `진행 중인 자동 작업 ${wk.length}건을 세우고 ${what} 바꿉니다` | `진행 중인 자동 작업 ${wk.length}건을 중단하고 ${what} 바꿉니다` | `sessionRestart` (L147) |
| 그 본문 | `이 재시작은 이 창만 바꿉니다 — 세우지 않으면 그 작업들은 옛 설정 그대로 계속 진행됩니다. 세우면 각 문서에 세운 사실과 사유가 남고, 나중에 그 카드의 깨우기로 다시 진행할 수 있습니다.` | `이 재시작은 이 창만 바꿉니다 — 중단하지 않으면 그 작업들은 옛 설정 그대로 계속 진행됩니다. 중단하면 각 문서에 중단한 사실과 사유가 남고, 나중에 그 카드의 「이어가기」로 다시 맡길 수 있습니다.` | `sessionRestart` (L148-150) |
| 그 버튼 | `ok: "세우고 바꾸기", cancel: "그만두기"` | `ok: "중단하고 바꾸기", cancel: "그만두기"` | `sessionRestart` (L151) |
| 세션 확인 본문 | `하던 일을 멈추고 바꿀까요? … 멈추지 않으면 지금 설정 그대로 둡니다.` | `하던 일을 중단하고 바꿀까요? … 중단하지 않으면 지금 설정 그대로 둡니다.` | `restartTell` (L79-80) |
| 세션 확인 버튼 | `ok: "멈추고 바꾸기", cancel: "그대로 두기"` | `ok: "중단하고 바꾸기", cancel: "그대로 두기"` | `restartTell` (L81) |
| 중단 실패 | `하던 일을 멈추지 못했습니다` | `하던 일을 중단하지 못했습니다` | `restartAfterStop` (L98) |
| 시간 초과 본문 | `멈춰 달라고는 했지만 15초 안에 끝나지 않았습니다` | `중단해 달라고는 했지만 15초 안에 끝나지 않았습니다` | `restartAfterStop` (L113) |
| 칩 사유 | `restartChip("fail", what, {reason: "안 멈춤"});` | `restartChip("fail", what, {reason: "안 끝남"});` | `restartAfterStop` (L111) — `멈춤`은 이제 상태 전용이다 |

「중단하고 바꾸기」는 7자다. 근거: 카드 안의 작은 손잡이가 아니라 **대화상자 확인 버튼**이고, 지금 자리에 이미 6자(「세우고 바꾸기」)가 서 있다.

### 표 3 — 서버·CLI (`bin/s9`) · 화면에서 고른 말이 그대로 실리는 자리

| 자리 | 지금 문자열 | 바꿀 문자열 | 함수 |
|---|---|---|---|
| 사유 없음 | `왜 세우는지 적어라 — ` | `왜 중단하는지 적어라 — ` | `worker_stop` (L2568) |
| 클레임 게이트 | `를 집지 않은 세션은 그 작업자를 세울 수 없다` | `를 집지 않은 세션은 그 자동 작업을 중단할 수 없다` | `worker_stop` (L2583-4) |
| 문서 노트 | `무인 작업자를 세웠다 (pid {pid}, {kind} ` | `자동 작업을 중단했다 (pid {pid}, {kind} ` | `worker_stop` (L2613) — 사전의 `무인 작업자`→`자동 작업`도 함께 |
| 성공 메시지 | `이 요청의 자동 작업을 세웠습니다. 멈춘 사실과 사유는 문서에 남겼습니다.` | `이 요청의 자동 작업을 중단했습니다. 중단한 사실과 사유는 문서에 남겼습니다.` | `worker_stop` (L2618-9) — `멈춘 사실`이 상태 낱말과 부딪친다 |
| 타입 거부 | `세울 수 있는 것은 요청 문서뿐입니다 ` | `중단할 수 있는 것은 요청 문서뿐입니다 ` | `stop_request` (L6365) |
| 기본 사유 | `가 화면에서 세웠다 (계정·모델 변경 등)` | `가 화면에서 중단했다 (계정·모델 변경 등)` | `stop_request` (L6369) |
| 실패 | `진행 중인 작업을 세우지 못했습니다.` | `진행 중인 자동 작업을 중단하지 못했습니다.` | `stop_request` (L6374) |
| 성공 | `이 요청의 자동 작업을 세웠습니다.` | `이 요청의 자동 작업을 중단했습니다.` | `stop_request` (L6381) |
| 일괄 사유 | `가 화면에서 전부 세웠다 (계정·모델 변경)` | `가 화면에서 전부 중단했다 (계정·모델 변경)` | `stop_all_workers` (L6395) |
| 일괄 빈 결과 | `진행 중인 자동 작업이 없어 세울 것이 없었습니다.` | `진행 중인 자동 작업이 없어 중단할 것이 없었습니다.` | `stop_all_workers` (L6409) |
| 일괄 성공 | `자동 작업 {len(done)}건을 세웠습니다.` | `자동 작업 {len(done)}건을 중단했습니다.` | `stop_all_workers` (L6411) |
| 일괄 실패 | `건은 세우지 못했습니다: ` | `건은 중단하지 못했습니다: ` | `stop_all_workers` (L6413) |
| CLI 도움말 | `help="세우는 이유 (--stop 에 필수, --stop-all 에 권장)"` | `help="중단하는 이유 (--stop 에 필수, --stop-all 에 권장)"` | argparse (L16786) |
| 쿨다운 | `방금 이 요청을 한 번 깨웠습니다 — ` | `방금 이 요청을 이어가기 시작했습니다 — ` | `_auto_gate` (L2021) |
| 일 한도 | `오늘 깨울 수 있는 횟수를 다 썼습니다 ` | `오늘 이어갈 수 있는 횟수를 다 썼습니다 ` | (L2028) |
| 시간 한도 | `이번 시간에 깨울 수 있는 횟수를 다 썼습니다 ` | `이번 시간에 이어갈 수 있는 횟수를 다 썼습니다 ` | (L2033) |
| 옵트인 꺼짐 | `을 켜야 깨울 수 있습니다` | `을 켜야 이어갈 수 있습니다` | (L5844) — 앞 문장의 `자동 이어가기`와 이제 같은 낱말이 된다 |
| 아직 안 멈춤 | `없을 때만 깨울 수 있습니다.` | `없을 때만 이어갈 수 있습니다.` | `_wake_block` (L6212) |
| 타입 거부 | `깨울 수 있는 것은 요청 문서뿐입니다 ` | `이어갈 수 있는 것은 요청 문서뿐입니다 ` | `wake_request` (L6272) |
| 상태 거부 | `깨울 수 있는 것은 진행 중` | `이어갈 수 있는 것은 진행 중` | `wake_request` (L6282) |
| 성공 | `이 요청을 깨웠습니다 — 자동 작업이 이어서 진행합니다. ` | `자동 작업이 이 요청을 이어서 진행합니다. ` | `wake_request` (L6314) — 「깨웠습니다」를 빼면 앞뒤가 겹치므로 결과부터 말한다 |
| 작업자 프롬프트 *(선택)* | `[section9 깨우기] REQ …` / `가 대시보드에서 직접 깨웠다.` | `[section9 이어가기] REQ …` / `가 대시보드에서 직접 이어가기를 눌렀다.` | `_spawn_wake` (L6091-2) — 사람에게 안 보이지만 로그에 남는다 |

**바꾸지 않는 것**: API 경로(`/api/wake`·`/api/stop`), CLI 하위명령(`s9 wake`·`s9 workers --stop-all`), DOM 속성(`data-wake`·`data-stop`), 진단 파라미터(`?dlg=stopask`), 설정 키(`auto_resume_wake_per_day`). 기술용어는 무리해서 번역하지 않는다는 이 저장소의 원칙이고, 사람이 읽는 자리가 아니다. 다만 `auto_resume_wake_per_day`는 이제 화면의 「이어가기」와 이름이 갈리므로, 한도 문구에 「이어갈 수 있는 횟수」라는 사람 말이 **먼저** 서고 키 이름이 뒤에 오는 지금 구조를 유지해야 한다.

### 지킬 시험 한 줄

`tests/test_dialog_voice.py`에 **V5**를 더한다 — 기존 V4(눈썹=누른 낱말)는 이 변경으로 깨지므로 어차피 손대야 한다.

> **V5** `web/app/*.js`와 `bin/s9`의 사용자 문자열(V1이 이미 걷는 그 자리)에 `깨우|세우|세웠|멈추기`가 하나도 없고, `이어가`와 `중단`이 각각 등장하며, 카드 손잡이 라벨은 정확히 `"이어가기"`·`"중단하기"` 둘뿐이다.

V4는 죽은 꼴 목록에 `'cap: d.ok ? "깨우기"'`·`'cap: d.ok ? "세우기"'`를 더하고 산 꼴을 `'cap: d.ok ? "이어가기" : "이어가지 않음"'`·`'cap: d.ok ? "중단하기" : "중단하지 않음"'`으로 바꾸면 된다. 함께 갱신될 시험: `test_stop_reach.py`·`test_wake*.py`·`test_stall_pair.py` 중 라벨 문자열을 찍는 자리.

---

파일은 하나도 고치지 않았다. 읽기만 한 파일: `/home/sjpark1/section9/web/app/card.js` · `/home/sjpark1/section9/web/app/restart.js` · `/home/sjpark1/section9/bin/s9` · `/home/sjpark1/section9/tests/test_dialog_voice.py` · `/home/sjpark1/section9/vault/requests/2026/08/REQ-20260829-024-62x6.md` · `/home/sjpark1/section9/vault/requests/2026/08/REQ-20260830-007-62x6.md`.

한계 하나: 실브라우저로 확인하지 않았다 — 「이어가기」·「중단하기」는 지금 라벨과 같은 4자라 `.acts` 줄의 폭이 흔들릴 자리는 없어 보이지만, 잠긴 얼굴 「이어가는 중…」(6자)이 「깨우는 중…」(5자)보다 한 자 길다. compact 밀도와 calm 스킨에서 한 번은 눈으로 봐야 한다. `?stall=20&stallhold` · `?work=12&workhold`가 그 두 얼굴을 한 화면에 세운다.

### 2026-08-30T12:50:12+09:00 subagent (agent: sub:designer:abe4d716, by nicehugepark)

`[2026-08-30 12:47:12 KST - designer]`

### 결론: 세 번째 줄을 만들지 마라 — 이미 줄은 하나뿐이다

카드에 손잡이 줄은 **언제나 최대 하나**다. 셋이 함께 서는 조합은 이 저장소가 이미 코드로 닫아 놨다. 그래서 판정 요청의 2번(밀도 붕괴)은 배치로 풀 문제가 아니고, 지금 실제로 깨져 있는 것은 **`.rvpt.held` 에 CSS가 없다는 것 하나**다. 캡션이 보라색(`--c-review`, 이 화면에서 "판정"의 잉크)으로 뜬다.

작업 중 발견: `web/app/card.js` 에 `stoppedRowHTML()` 이 **이미 구현돼 있다**(라운드4, 미커밋). 서버도 `stop_mark`·`r["stopped"]`·`wake_request` 의 stop-mark 갈래까지 들어가 있다. 그래서 이건 설계 제안이 아니라 **막 들어온 구현의 리뷰**다.

---

### 1. 세 상태가 한 카드에서 구분되는가 — 갈릴 필요가 없다, 함께 안 선다

배타성이 세 겹으로 걸려 있다.

| 조합 | 막는 자리 |
|---|---|
| 진행 중 + 멈춤 | `bin/s9` `stall_verdict` — `if r.get("live_kind")=="spawned" or r.get("worker"): return state="moving"` (REQ-20260830-009-62x6, 오늘 done). 도는 작업자가 있으면 `stalled_mins` 를 아예 안 싣는다 |
| 진행 중 + 세워 둠 | `catalog_with_live` 의 `if not wk:` 게이트 + `stoppedRowHTML` 의 `|| r.worker` |
| 세워 둠 + 멈춤 | `stallHTML` 의 `if (stopped) return work + stopped;` — 사람이 세운 근거가 더 구체적이라 그쪽이 이긴다 |

세 번째는 실제로 일어나는 조합이다(세운 뒤 15분이 지나면 `stopped` 와 `stalled_mins` 가 둘 다 실린다). 지금 코드가 그걸 맞게 처리하고 있다.

**그래서 잉크를 갈라 쓰지 마라.** 같은 화면에 나란히 서지 않는 둘에 색 대비를 주는 것은 없는 문제를 푸는 것이고, 대신 색 하나를 더 배워야 한다(`s9-design` 2절: 강조가 셋이면 강조가 없는 것과 같다). 구분은 색보다 강한 것이 이미 한다 — 캡션 낱말(멈춤/세워 둠/진행 중) + 버튼 낱말 + 점 모양. 흑백으로 인쇄해도 살아남는 구분이다.

`--muted` 안도 만들어 실제로 렌더해 봤다(「내가 세운 것 = 조용한 잉크」 논리). 버렸다. 세워 둔 카드의 점은 `.livedot.dot-stopped.mild` = `--c-inprogress` 잉크여야 하는데, 캡션만 회색으로 내리면 **같은 사실을 점과 글자가 두 색으로 말한다**. 이 파일이 스스로 세운 규칙("점·줄·손잡이가 셋 다 같은 문을 지난다")을 깨는 값이다.

### 바꿀 CSS — `web/css/actions.css`, 새 선언 0개

지금 있는 두 규칙에 셀렉터만 더한다. 46~47행과 68~69행이 같은 값을 두 벌 갖고 있으니 합치는 김에 셋으로 묶는다.

```css
/* 손잡이 줄 셋은 한 문법이다 — 한 줄로 자르고, 캡션은 상태 잉크로.
   셋이 한 카드에 함께 서는 일은 없다(서버 stall_verdict 가 worker 를 보면
   멈춤을 안 싣고, stoppedRowHTML 은 worker 가 있으면 서지 않는다). 그래서
   잉크를 갈라 쓸 이유가 없다 — 구분은 캡션 낱말과 버튼 낱말이 이미 한다. */
.rvpt.stall,.rvpt.work,.rvpt.held{white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
.rvpt.stall .rvcap,.rvpt.work .rvcap,.rvpt.held .rvcap{color:var(--c-inprogress)}
```

`.acts.wakerow{margin-top:5px}` 는 `stoppedRowHTML` 이 같은 클래스를 이미 쓰고 있어 손댈 것이 없다. 스킨 오버라이드도 필요 없다 — `skins.css`·`calm.css` 어디에도 `.rvcap` 재정의가 없어 열 몇 벌이 전부 이 한 줄을 따라온다.

---

### 2. 밀도 — 무너지지 않는다. 실측

`?work=12&stall=44` 로 둘을 억지로 세워 재 봤다(ledger, 1440px):

- 손잡이 없는 in-progress 카드: 약 75px
- 손잡이 한 줄(캡션·사실·버튼): **+62px**
- 두 줄: +125px → 카드의 63%가 손잡이
- 세 줄이었다면: +190px

한 줄이면 컬럼 리듬이 산다. **셋으로 늘지 않으므로 압축안은 전부 불필요하다** — 그래도 판정을 청했으니 답한다.

| 대안 | 판정 |
|---|---|
| 줄+버튼을 한 줄로 합치기 | ✗ 폭이 안 나온다. 250px 컬럼에서 `멈춤`(26px) + 사실(~200px) + `깨우기`(~52px) = 258px. 사실이 거의 다 잘린다. 지금 구조가 그 이유로 나뉜 것이고(actions.css 48~54행 주석) 그 판단은 여전히 맞다 |
| 버튼만 남기기 | ✗ 무엇을 세우는지 안 적힌다. 세워지는 것은 사람이 안 보는 곳에서 도는 프로세스라 글자로 한 번은 서야 한다 |
| 사실을 `title` 로 내리기 | ✗ 접근성 후퇴(`s9-design` 7절). title 은 얹어야 읽히고 터치·키보드에서는 못 읽는다. 이 화면이 `.livedot` 에서 이미 겪은 자리 — "얹어야 읽히는 툴팁" |
| **한 줄만 서게 두기(현행)** | ✓ 추천. 밀도를 배치가 아니라 상태 모델로 지킨다 |

한 가지만 줄이자면 캡션이 아니라 **사실 문장**이다. `세워 둠 22분째` 는 좋다. 길어지면(`1시간 22분째 · 12:02 에 세움`) ellipsis 로 잘리는데, 잘리는 쪽에 정작 중요한 "언제 세웠나"가 있다. 순서를 뒤집어 `12:02 에 세움 · 1시간 22분째` 로 두면 잘려도 앞이 남는다.

---

### 3. 「멈춤+깨우기」와 「진행 중+세우기」가 동시에 — **실데이터에는 이제 없다**

오늘 done 된 REQ-20260830-009-62x6(`멈춤과 진행 중이 같이 선다`)가 서버에서 닫았다. 지금 그 화면이 보이는 유일한 길은 진단 파라미터 `?work=` + `?stall=` 을 같이 주는 것이고, 그건 **존재할 수 없는 카드를 지어내고 있다**. 다음 사람이 그 화면을 보고 그것에 맞춰 디자인하게 된다.

그래도 화면이 서버의 말을 지우지 않는다는 원칙(card.js 68~69행)은 지킬 만하다. 방어 경로를 남긴다면 두 가지를 고쳐라.

**(가) 순서가 거꾸로다.** 지금 `stallHTML` 은 멈춤 줄 + 깨우기를 먼저 그리고 그 **뒤에** work 를 붙인다(95행 `+ work`). 카드가 "멈췄으니 깨워라"를 먼저 읽히고 그 아래에서 "돌고 있다"고 말한다. 지금의 사실이 이겨야 한다 — work 가 먼저다.

**(나) 그 깨우기는 서버가 반드시 거절한다.** 도는 작업자는 `_claim_req` 가 클레임으로 인정하고(`bin/s9` 2955행), 스폰 마커의 쿨다운도 걸려 있다. 누를 수 있게 그려 놓고 누르면 거절하는 손잡이다 — `stall_verdict` 주석(8812행)이 이미 이 병을 이름 붙여 놨다.

```js
// stallHTML — 사실은 지우지 않고, 못 누를 행동만 지운다
  const work = workRowHTML(r);          // 지금의 사실이 먼저다
  ...
  return work
    + `<div class="rvpt stall" title="…">`
    + `<span class="rvcap">멈춤</span>${fmtStall(st.mins)} 진전 없음`
    + (last ? ` · 마지막 ${esc(last)}` : "") + `</div>`
    // 도는 작업자가 있으면 깨우기는 서버가 반드시 거절한다(클레임·쿨다운).
    // 화면은 서버의 말을 지우지 않지만, 서버가 거절할 행동도 내밀지 않는다.
    + (work ? "" : `<div class="acts wakerow">…깨우기…</div>`);
```

겸해서 `workProbe` 가 `r.stalled_mins = null` 을 함께 넣으면 진단이 서버의 규칙을 그대로 흉내 내게 된다.

---

### 4. 제약 안에서 — 새 층은 하나도 안 만든다

색면·세로 띠 없음, 무채색 미니멀 아님, 색은 글자에만. 위 제안의 전부가 **기존 셀렉터에 클래스 이름 하나씩 더한 것**이고 새 declaration은 0개다. 새 컴포넌트도, 새 토큰도, 새 스킨 오버라이드도 없다.

### 다만 점 사다리에 갈래가 하나 빠졌다 (card.js `liveDot`)

세워 둔 카드는 `stalled_mins` 가 없으므로 `stallState()` 가 null → 사다리를 끝까지 내려가 **`.livedot.off`(속 빈 회색 원 = "in-progress지만 스트림 조용함, 모름")** 로 그려진다.

이건 REQ-20260828-041-62x6 2차 반려가 지운 바로 그 조합이다 — "손잡이는 있는데 점은 모름이라고 말하는 카드". 세워 둠 갈래로 되살아났다.

```js
  const liveDot = r.status === "in-progress"
    ? (st && st.face === "dead" ? …
       : st ? …
       /* 사람이 세워 둔 것은 **모름이 아니다** — 왜 조용한지 카드가 알고 있다.
          점이 서는 조건과 손잡이가 서는 조건을 다시 같은 집합으로 만든다. */
       : r.stopped
         ? `<span class="livedot dot-stopped mild" title="사람이 세워 뒀습니다 — …"></span>`
       : r.live ? … )
```

`.livedot.dot-stopped.mild` 는 이미 있는 규칙이다(속 빈 사각 + `--c-inprogress` 1.5px). CSS 추가 0줄.

---

### 인접 결함 둘 (내 범위 밖이지만 같은 손잡이를 물고 있다)

**낱말이 두 벌이라 지금 서로 다르다.** `stoppedRowHTML` 은 `다시 시작` 을 그리는데 `paintWake` 는 첫 붓질에 `이어가기` 로 덮는다. `stallHTML` 은 `깨우기`, `paintWake` 는 `이어가기`. `workRowHTML` 은 `세우기`, `paintStop` 은 `중단하기`. 처음 그려진 글자와 한 번 눌렀다 돌아온 글자가 다르다 — ux-writer 가 지금 이 파일에서 개명 중이라 과도기로 보이지만, 구조가 문제다: **낱말이 HTML 빌더와 페인터 두 곳에 있다.** 상수 하나를 두고 둘이 그것만 읽게 하라. 이 화면이 판정 버튼에서 세 번 배운 그 규칙이다.

**`다시 시작` 은 이미 임자가 있다.** `web/app/diag.js` 의 계정·모델 창 확인 버튼이 `ok:"다시 시작"` 이다(세션 재기동). 카드의 `다시 시작`(자동 작업 스폰)과 한 화면에서 같은 낱말이 다른 일을 한다. 사용자가 반려문에서 쓴 말이라 끌리지만, 그 낱말을 쓰려면 창머리도 따라와야 한다(`test_dialog_voice` V4: 창이 사람이 누른 낱말을 그대로 쓴다) — 지금 `wakeDoc` 은 `data-wlabel` 을 안 읽고 `wakeDlg` 에 넘기지도 않아서, `다시 시작` 을 눌러도 창은 `이어가기` 라고 뜬다. **낱말은 하나로 통일하는 쪽을 추천한다** — 하는 일이 같고(`/api/wake`), 길이 같고(`wakeDoc`), 예산도 같다.

---

### 라이트·다크·스킨에서 깨지는 자리

ledger 라이트 / ledger 다크(carbon) / calm 라이트에서 지금(NOW)과 제안(PROPOSED)을 나란히 렌더해 확인했다 — 캡처는 `/home/sjpark1/section9/scratchpad/dz-held4.png`, 실보드 캡처는 `/home/sjpark1/section9/scratchpad/dz-ledger.png` 와 `dz-both.png`.

| 자리 | 지금 | 원인 |
|---|---|---|
| **전 스킨 · 전 톤** | `세워 둠` 캡션이 보라 | `.rvpt.held .rvcap` 규칙이 없어 베이스 `.rvcap{color:var(--c-review)}` 가 그대로 산다. 라이트 `#6d28d9` · 다크 밝기보정본. 이 화면에서 보라는 판정의 잉크라, 세워 둔 in-progress 카드가 "판정 대기"라고 말한다 |
| **전 스킨** | 사실이 길면 두 줄로 감긴다 | `.rvpt.held` 에 nowrap/ellipsis 가 없다. 형제 줄(`.stall`·`.work`)만 갖고 있어 같은 카드 안에서 잘림 규칙이 갈린다 |
| **calm** | 캡션이 인라인으로 눕고 `.card>.rvpt` 에 헤어라인 구분선이 붙는다 | 의도된 override. 제안안 적용 후에도 그대로 성립한다(캡처로 확인) |
| **compact 밀도** | 버튼 9.5px/1px 패딩 | `.acts button` 만 줄어들고 줄은 그대로 — 문제 없음 |

대비는 잰 값으로 통과한다: `--c-inprogress` 라이트 `#b45309` on `#fff` = 5.0:1, 다크 `#d99537` on `#17181b` = 8.7:1. 캡션은 10px 볼드라 본문 기준 4.5:1을 적용했고 둘 다 넘는다.

미검증으로 남긴 것: **실데이터의 세워 둠 줄**. 9909 서버가 12:07 기동이라 `r.stopped` 를 아직 안 싣고, `.stopped` 마커 파일도 `state/auto_resume/` 에 하나도 없다. 위 캡처는 실 CSS를 그대로 불러온 하네스(`scratchpad/dz-held.html`)로 렌더한 것이지 실 카탈로그가 아니다. 서버를 `s9 serve --restart` 로 다시 띄우고 실제로 세워 본 뒤에 한 번 더 봐야 한다 — 그때 확인할 것은 점 색(회색 원인가, 앰버 사각인가)과 버튼 글자(`다시 시작` 인가 `이어가기` 인가) 둘이다.

파일은 하나도 고치지 않았다. `scratchpad/dz-held.html` 과 png 넷만 만들었다.

`s9 note` 로 이 결론을 REQ-20260829-024-62x6 에 남기려 했으나 권한이 막혔다 — 리드가 `--label designer` 로 옮겨 주면 좋겠다.

### 2026-08-30T12:51:43+09:00 response (by nicehugepark)

### 이름 충돌 한 줄 — 판이 통째로 멎어 있었다 (다른 세션에서 회수)

12:46 에 들어온 `data-restart` 손잡이의 변수명이 같은 함수 안의 기존
`const rs`(`data-resupply`, HEAD 부터 있던 것)와 겹쳤습니다. 같은 블록에
`const` 가 둘이면 **문법 오류**라 `events.js` 가 통째로 안 실행되고, 그
조각이 맡은 판의 버튼이 전부 멎습니다 — 헤드리스로 찍어 보고 잡았습니다:

    app/events.js:238 — SyntaxError: Identifier `rs` has already been declared

새로 들어온 쪽(170행)을 `rst` 로 바꿨습니다. 하는 일은 그대로입니다.
이 자리를 계속 만지신다면 `rs` 는 이미 아래에서 쓰고 있다는 것만 기억해
주세요. (REQ-20260830-010-62x6 에서 같은 파일을 만지다 발견)

### 2026-08-30T13:04:39+09:00 designer (by nicehugepark)

designer 판정 (라운드4, 리드가 옮겨 적음 — 에이전트는 문서 쓰기 권한이 막혀 있었다).

결론: 세 번째 줄을 새로 만들 필요가 없다. 카드에 손잡이 줄은 언제나 최대 하나이고, 셋이 함께 서는 조합은 이미 코드가 닫아 놨다 — ① 진행 중 + 멈춤: 서버 stall_verdict 가 도는 작업이 있으면 stalled_mins 를 안 싣는다(REQ-20260830-009-62x6). ② 진행 중 + 중단: catalog_with_live 의 if not wk 게이트와 화면의 r.worker 검사. ③ 중단 + 멈춤: stallHTML 이 중단 쪽을 세운다(사람이 한 일이 더 구체적인 근거다).

그래서 잉크를 갈라 쓰지 말라고 했다. 나란히 서지 않는 둘에 색 대비를 주는 것은 없는 문제를 푸는 대신 색 하나를 더 배우게 하는 일이고, 세워 둔 카드의 점은 --c-inprogress 사각이라 캡션만 회색으로 내리면 같은 사실을 점과 글자가 두 색으로 말한다. 구분은 색보다 강한 것이 이미 한다 — 캡션 낱말·버튼 낱말·점 모양(흑백 인쇄에서도 산다). 처음에 --muted 로 뒀던 것을 이 판정대로 --c-inprogress 로 되돌렸다.

밀도 실측(ledger, 1440px): 손잡이 없는 카드 75px · 한 줄 +62px · 두 줄 +125px(카드의 63%) · 세 줄이면 +190px. 한 줄이면 컬럼 리듬이 산다. 압축안 셋은 전부 탈락 — 줄+버튼 한 줄로 합치기는 250px 컬럼에서 258px 라 사실이 잘리고, 버튼만 남기면 무엇을 중단하는지 안 적히고, 사실을 title 로 내리면 얹어야 읽히는 글이 된다(터치·키보드에서 못 읽는다).

받아들여 함께 고친 지적 셋:
· 점 사다리에 중단 갈래가 없어 세워 둔 카드가 `.livedot.off`(모름)로 그려졌다 — REQ-20260828-041-62x6 2차 반려가 지운 "손잡이는 있는데 점은 모름이라 말하는 카드"가 되살아난 것이다. r.stopped 갈래를 넣어 이미 있는 dot-stopped mild 를 쓴다(CSS 0줄).
· 낱말이 HTML 빌더와 페인터 두 곳에 글자로 있었다. 그래서 개명 도중 처음 그려진 글자와 눌렀다 돌아온 글자가 실제로 달랐다. WAKE_LABEL·STOP_LABEL 상수 한 곳으로 모으고 창머리(cap)도 그 상수를 읽게 했다 — test_dialog_voice V4 가 그것을 지킨다.
· workProbe 가 서버가 만들 수 없는 카드(멈춤+진행 중)를 지어내고 있었다. 진단도 서버 규칙을 따르게 stalled_mins 를 비운다.

미룬 것 하나: 「멈춤+깨우기」와 「진행 중」이 동시에 설 때 깨우기를 감추자는 제안. 그 조합은 서버가 이미 막았고(009), 화면이 서버의 말을 지우지 않는다는 원칙과 부딪쳐 이번 범위 밖으로 둔다.

### 2026-08-30T13:05:11+09:00 response (by nicehugepark)

라운드4 — 반려 둘을 다 반영했다.

① "멈춰놓고선, 다시 시작할 수 있는 기능이 없다" — 맞는 지적이었고 뿌리가 둘이었다.
· 중단하면 그 사유가 문서에 적힌다 → 문서가 방금 움직인 것이 되어 멈춤 판정이 15분간 서지 않는다 → 손잡이가 아예 안 그려졌다.
· per-REQ 쿨다운(600초)도 같은 자리를 막고 있었다.
그래서 중단한 사실을 표시로 남긴다(state/auto_resume/<id>.stopped). 그 표시가 있는 동안 갈래가 정반대로 갈린다 — **기계는 되살리지 않고**(워처가 30초 뒤에 다시 띄우면 중단이라는 행동 자체가 무의미하다) **사람은 곧바로 되돌린다**(쿨다운이 재는 것은 겹쳐 붙는 손인데 중단한 자리에는 붙은 손이 없다. 하루·시간 예산은 그대로 든다). 표시는 두 시간이 지나면 늙는다 — 천장 없는 보호는 교착의 다른 이름이라 이 저장소가 delegated_running·worker_running 에서 두 번 배운 것이다. 다시 시작되면(_auto_mark_pid) 표시는 사라진다.
화면은 카드에 「중단 · N분 전」 줄과 「이어가기」를 세운다. 길은 멈춘 카드의 것과 **같은 길**(wakeDoc → /api/wake)이고, 중단해 둔 카드에서는 멈춤 줄 대신 이 줄이 선다 — 같은 일을 하는 손잡이를 한 카드에 둘 그리지 않는다.

② 용어 — 사용자가 지목한 ux-writer·designer 에 검토를 맡겼고, 그 결론대로 바꿨다.
· 「깨우기」·「다시 시작」 → **「이어가기」** (Settings 의 `자동 이어가기` 와 같은 가족이라 새로 배울 낱말이 아니다). 멈춘 것을 맡기는 일과 중단한 것을 다시 맡기는 일은 하는 일·API·결과가 같아 한 낱말을 쓴다 — 처지의 차이는 버튼이 아니라 그 위의 줄(「멈춤 44분째」 / 「중단 12분 전」)이 말한다.
· 「세우기」 → **「중단하기」**. 「멈추기」는 상태 낱말 `멈춤` 과 한 화면에서 부딪치고, 「끝내기」는 done 으로, 「그만두기」는 창의 취소 라벨로 이미 임자가 있다. `멈춤` 은 저절로 조용해진 **상태**, `중단` 은 사람이 끝낸 **행동** — 품사가 아니라 주체로 갈랐다.
· 세션 재기동 창의 「멈추고 바꾸기」도 「중단하고 바꾸기」로 모았다(한 흐름에서 창 둘이 잇따라 뜨는데 낱말이 갈려 있었다). 서버·CLI 문장도 같은 낱말로 맞췄다.
· REQ-20260830-007-62x6 의 ux-writer 는 이 둘을 유지하기로 판단했는데 사용자가 그 판단을 뒤집었다. 그 논거(멈춤/멈추기 충돌)는 여전히 사실이라 그 제약을 지키면서 다른 답을 골랐다. 007 의 용어 사전(무인 작업자→자동 작업 등)과는 어긋나지 않는다.

바꾸지 않은 것: API 경로(/api/wake·/api/stop)·CLI 하위명령·DOM 속성·설정 키. 기술 이름은 무리해서 옮기지 않는다.

검증: 전체 스위트 2387건 통과(python3 tests/). 새 시험 tests/test_stop_reach.py 는 35건(S1~S16 · R1~R8). 낱말이 바뀌며 계약 문장이 달라진 기존 시험 다섯 곳을 함께 고쳤다 — test_wake_handle·test_wake_reach·test_restart_reach·test_dialog_voice(V4 를 상수 검사로 세움)·test_dead_end(정지 마크가 딛는 서버 판정이 이제 둘이다)·test_stall_pair(노드 하네스 명단).
캡처: scratchpad/r4-held.png (중단 줄 + 이어가기 + 눌린 얼굴), scratchpad/r4-work-dark.png (calm 스킨의 진행 중 + 중단하기). 진단은 ?held=12[&heldhold] 와 ?work=12[&workhold].

서버 재기동이 필요하다: 지금 도는 9909 는 12:07 기동이라 stopped 필드도 새 문장도 없다.

## History
- 2026-08-29T16:48:34+09:00 created by nicehugepark (status: open)
- 2026-08-29T20:08:46+09:00 status: open -> in-progress (by nicehugepark) — drag 이동 [via dashboard]
- 2026-08-30T00:27:13+09:00 status: in-progress -> review (by nicehugepark) — 진행 중인 카드에 「세우기」가 생겼습니다 — 깨우기의 반대편입니다. 대시보드 머리의 「s9 serve --restart」 로 서버를 새로 띄우고 F5 한 뒤, 「작업 중 — 무인 작업자가 N분째 돌고 있다」 줄이 붙은 카드에서 그 아래 「세우기」를 눌러 보세요(도는 작업자가 없으면 그 줄도 없는 것이 정상입니다). 계정·모델을 바꿀 때는 「N건을 세우고 바꿉니다」 창이 먼저 뜹니다. 눌러서 그 작업자가 사라지고 문서에 세운 사유가 남으면 승인, 손잡이가 안 보이거나 눌러도 그대로면 반려입니다.
- 2026-08-30T12:09:14+09:00 status: review -> in-progress (by nicehugepark) — 반려: 멈춰놓고선, 다시 시작할 수 있는 기능이 없다. 그리고 깨우기, 세우기 라는 용어가 너무 어색한데 ux-writer, designer 다시 검토해줘 [via dashboard]
- 2026-08-30T13:05:33+09:00 status: in-progress -> review (by nicehugepark) — 「깨우기·세우기」가 「이어가기·중단하기」로 바뀌고, 중단한 카드에 다시 시작할 손잡이가 생겼습니다. 화면 위 「s9 serve --restart」 로 서버를 새로 띄우고 F5 한 뒤, 진행 중인 카드의 「중단하기」를 눌러 보세요 — 그 자리가 「중단 · N분 전」 줄과 「이어가기」로 바뀌고, 그것을 누르면 다시 진행됩니다. 두 번 다 눌리고 낱말이 어색하지 않으면 승인, 중단한 뒤 이어갈 손잡이가 없거나 낱말이 여전히 어색하면 반려입니다.
