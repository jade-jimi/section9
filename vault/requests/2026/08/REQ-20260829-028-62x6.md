---
id: REQ-20260829-028-62x6
type: request
title: 낡은 자리에 앉히지 않는다
summary: 워크트리 판단(026)을 스폰 경로가 강제하게 — 미커밋 상태에서 낡은 사본에 작업자를 앉히지 않는다
goal: 미커밋 diff 가 있는 동안 워크트리로 스폰되지 않고, 끝난 워크트리가 스스로 거둬져, 오늘 같은 일이 재발하지 않는다
status: in-progress
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: 911949e0
agents: ["sub:architect:a10a47b5", "sub:subagent:a80ed348", "sub:quality-assurance:acb77915"]
contributions: [{"actor": "sub:architect:a10a47b5", "item": "워크트리 스폰 가드 설계", "started": "2026-08-29T16:55:25+09:00", "ended": "2026-08-29T17:05:20+09:00", "result": "done", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/911949e0-5d1b-4cd2-b08d-70fd0415a19a/tasks/a10a47b5c85e54333.output"}, {"actor": "sub:subagent:a80ed348", "item": "Appending design note to REQ-20260829-028-62x6", "started": "2026-08-29T17:04:38+09:00", "ended": "2026-08-29T17:04:38+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/911949e0-5d1b-4cd2-b08d-70fd0415a19a/subagents/agent-a80ed3482f20e98cb.jsonl"}, {"actor": "sub:architect:a10a47b5", "item": "`[2026-08-29 17:0x KST - architect]`", "started": "2026-08-29T17:05:20+09:00", "ended": "2026-08-29T17:05:20+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/911949e0-5d1b-4cd2-b08d-70fd0415a19a/subagents/agent-a10a47b5c85e54333.jsonl"}, {"actor": "sub:quality-assurance:acb77915", "item": "019 그림 검증", "started": "2026-08-29T18:42:53+09:00", "ended": "2026-08-29T18:42:53+09:00", "result": "running", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/911949e0-5d1b-4cd2-b08d-70fd0415a19a/tasks/acb7791588d2950ef.output"}]
project: section9
children: ["REQ-20260829-030-62x6"]
derived_from: REQ-20260829-026-62x6
relates: ["DOC-20260829-001-62x6"]
relates_why: {"DOC-20260829-001-62x6": "이 구현이 강제해야 할 판단과 실측 근거가 그 문서다"}
tags: ["auto-audit"]
created: 2026-08-29T16:54:20+09:00
status_since: 2026-08-29T16:54:45+09:00
updated: 2026-08-29T18:42:53+09:00
priority: 50
---

## Original

026은 신중하게 판단하고 실제로 사용하면서 문제가 재발되지 않도록 구현해

## Notes

### 2026-08-29T17:04:19+09:00 architect (by nicehugepark)

### 낡은 자리에 앉히지 않는다 — 스폰 자리 판정 설계 (구현 지시서)

읽기 전용으로 스폰 경로 전체를 읽고 확정했다. `bin/s9` 는 손대지 않았다.

### 0. 먼저: 읽으면서 발견한 두 가지 (설계의 전제가 된다)

**(가) 지금 자동 커밋 하나가 30초마다 무한히 실패하고 있다.**
`state/auto_resume/spawn.log` 가 16:51부터 `WT-AUTOCOMMIT w-827-012-62x6-1787929230` 를
30초 간격으로 계속 찍는다. 그 워크트리는 `AD f` · `M bin/s9-install` ·
`M tests/test_commit_gate.py` 로 여전히 dirty 다. 원인은 `_worktree_autocommit`
(bin/s9:13205)이 `git commit` 을 `capture_output=True` 로 삼키고 반환코드를 안 보기
때문이다 — pre-commit 게이트(`bin/s9-guard`)가 거부하면 아무 소리 없이 실패하고,
sweep 이 다음 틱에 또 부른다. **실패가 안 보이는 자동 커밋은 장치가 아니라 거짓말이다.**
이것이 ③의 판단 근거 중 가장 무거운 실측이다.

**(나) 한 문서가 워크트리를 넷까지 만들었다.**
`state/worktree_owners/` 에 `w-829-011-62x6-` 이 타임스탬프만 다른 채 넷,
`w-829-015-62x6-` 이 둘 있다. `worker_worktree_name()`(13139)이 매 스폰마다
`int(time.time())` 을 붙이므로 재스폰이 곧 새 워크트리다. 아홉까지 늘어난 것은
`worktree_sweep` 이 게을러서가 아니라 **스폰이 무제한으로 만들기 때문**이다.

---

### ① 고칠 자리 (파일:줄 · 함수)

스폰은 이미 관문이 하나다 — 다행이다. 갈래를 새로 내지 말고 그 관문만 고친다.

| 자리 | 지금 하는 일 | 할 일 |
|---|---|---|
| `bin/s9:4510 _spawn_worker()` | **무인 스폰의 유일한 경로.** rework·followup·wake·resume-item 이 전부 여기로 온다 | 자리 판정·리스·대기를 여기 하나에 넣는다 |
| `bin/s9:4554` (그 안) | `cwd, wt_env, wt_name = worker_workspace(doc_id, cfg, cwd)` | `workspace_decision()` 결과를 넘기도록 바꾼다 |
| `bin/s9:4547 _auto_caps_ok()` | 통과하면 **카운터를 올린다** | ⚠ 대기 판정은 **반드시 이 줄 앞**에 둔다. 뒤에 두면 대기 한 번마다 일일 캡이 깎여 30분 뒤엔 띄울 수 없다 |
| `bin/s9:13144 worker_workspace()` | `cfg["worker_worktree"]` 불리언 하나로만 결정 | 판정을 `workspace_decision()` 에 위임하고, 여기서는 만들기만 |
| `bin/s9:13139 worker_worktree_name()` | 스폰마다 새 이름 | 문서당 하나로 (④ R1) |
| `bin/s9:13205 _worktree_autocommit()` | 실패를 삼키고 무한 재시도 | 반환코드·stderr 를 읽고 3회 실패면 포기 + 기록 |
| `bin/s9:13241 worktree_sweep()` | dirty/ahead 는 영구 보존 | 24시간 넘으면 **지우지 말고 사람 앞에 올린다** |
| `bin/s9:1585 _dirty_paths()` | 미커밋 전부 | 옆에 `code_dirty_paths()` 신설 |
| `bin/s9:7242 undelivered_requests()` | ahead/dirty 워크트리를 `s9 loose` 로 | 자동커밋 실패·묵은 워크트리 줄을 여기 얹는다 |
| `bin/s9:4797 wake_request()` | 성공/거부 메시지 | 자리와 대기를 사람 말로 (⑥) |
| `bin/s9:11311 catalog_with_live()` | 행에 live_kind | 행에 `workspace` 를 싣는다 (화면이 읽을 통로) |

세 스폰 경로가 각각 어느 길을 타는가 — **전부 같은 길이다**:

- `s9 worktree add|ls|rm|merge|sweep` → `cmd_worktree`(13290). 사람이 손으로 부르는 길.
  워커 스폰과 무관하다. 손대지 않는다(사람은 자기가 무엇을 하는지 안다).
- auto-resume 워커: `rework_watch_tick`(4863) / `maybe_auto_resume`(2070) →
  `_spawn_rework`(4660) · `_spawn_followup`(4693) → `_spawn_worker`(4510).
- `POST /api/wake`(12858) → `wake_request`(4797) → `_spawn_wake`(4715) → `_spawn_worker`.
- `s9 resume-item`(8557) → `_spawn_worker`.

즉 **판정을 `_spawn_worker` 한 곳에 넣으면 네 경로가 전부 덮인다.** 호출부에
옮겨 적지 마라 — 킬스위치가 옆문 났던 그 실수(4531 주석)의 반복이다.

---

### ② 판정 규칙

#### 핵심 통찰 하나: 워크트리가 놓치는 것은 **코드뿐이다**

`worker_workspace` 는 `S9_ROOT` 를 본 저장소로 못박는다(13165). 그래서
`vault/`·`state/`·`index/`·`users/` 의 미커밋은 워크트리 기준선과 **아무 상관이 없다** —
워커는 그것들을 본 저장소에서 읽는다. 지금 미커밋 101건 중 상당수가 그쪽이다.
"저장소가 dirty 면 거부" 가 부작용이 큰 이유가 여기 있다: 그 판정은 이 저장소에서
사실상 상수 `True` 라 무인 작업자가 영영 안 뜬다. **코드만 세면 판정이 다시 살아난다.**

#### 새 상수 (13054 `WORKTREE_MIN_AGE` 옆)

```python
DATA_PREFIXES  = ("vault/", "state/", "index/", "users/", ".git/")
SPINE          = ("bin/s9", "web/index.html")   # 이 저장소가 통째로 기대는 두 파일
LIVE_VERIFY    = ("web/",)                      # 9909 서버·tests 의 ../web 상대경로
WORKTREE_MAX        = 6         # 이보다 쌓이면 새로 만들지 않는다
WORKTREE_STALE_SEC  = 24*3600   # 주인 죽고 이만큼 지나면 사람 앞에 올린다
AUTOCOMMIT_MAX_TRIES = 3
LEASE_HOME          = os.path.join(ROOT, "state", "file_leases")
LEASE_WAIT_MAX_SEC  = 1800      # 기다림의 천장 — 넘으면 겹쳐도 띄운다
```

#### 새 함수 (의사코드)

```python
def code_dirty_paths():
    """워크트리 기준선이 놓치는 것 — 미커밋 **코드**만.
    데이터는 S9_ROOT 로 본 저장소를 함께 쓰므로 기준선과 무관하다.
    그것까지 세면 이 저장소는 늘 더럽고 판정은 상수가 된다."""
    return [p for p in _dirty_paths() if not p.startswith(DATA_PREFIXES)]


def declared_scope(doc_id):
    """이 요청이 만질 코드 파일 — **문서가 이름을 댄 것만**. 없으면 None(모름).
    추측하지 않는다: 모르면 모른다고 하고, 그때는 보수적으로 본 저장소를 고른다.
    틀리는 방향이 비대칭이다 — 본 저장소로 잘못 보내면 보이는 충돌 하나이고,
    워크트리로 잘못 보내면 오늘처럼 다섯 시간이 조용히 사라진다."""
    meta, body = read_doc(find_path(doc_id))
    text = " ".join([meta.get("title",""), meta.get("summary",""),
                     meta.get("goal",""), body])
    got = set()
    for m in re.finditer(r"(?<![\w/.-])((?:bin|web|tests|harness|docs)/"
                         r"[A-Za-z0-9_./-]+)", text):
        rel = m.group(1).rstrip(".,)`'\"")
        if os.path.isfile(os.path.join(ROOT, rel)):
            got.add(rel)
    return got or None


def workspace_decision(doc_id, cfg):
    """워크트리냐 본 저장소냐 — 판정은 여기 한 곳에만 있다.
    반환 {"kind","reason","scope","blocking"}"""
    if not cfg.get("worker_worktree"):        return M("off")
    if len(worktree_list()) >= WORKTREE_MAX:  return M("worktree-pile")
    if _doc_live_worktree(doc_id):            return M("worktree-exists")

    scope = declared_scope(doc_id)
    if scope and any(p.startswith(LIVE_VERIFY) for p in scope):
        return M("live-verify")     # 살아있는 서버가 못 보고, tests 가 ../web 를 읽는다
    if scope and any(p == "bin/s9" or p.startswith("bin/s9-") for p in scope):
        return M("self-edit")       # 샌드박스가 자기 도구를 자른다

    dirty = code_dirty_paths()
    if not dirty:                       return W("fresh")
    if set(dirty) & set(SPINE):         return M("dirty-spine", dirty)
    if scope is None:                   return M("dirty-unknown", dirty)
    if set(scope) & set(dirty):         return M("dirty-overlap", scope & dirty)
    return W("fresh-outside")
```

**`dirty-spine` 이 오늘의 사고를 정면으로 막는 줄이다.** `bin/s9` 나
`web/index.html` 이 미커밋이면, 무엇을 하든 워크트리는 낡은 자리다 — 시험이
`HERE/../bin/s9` 를 읽고 화면 검증이 9909 를 보기 때문에 스코프가 겹치지 않아도
검증이 거짓말을 한다. 지금 둘 다 dirty 다 → **오늘 뜨는 모든 워커는 본 저장소로 간다.**
이것이 의도한 결과다. 그리고 이 규칙은 좋은 압력을 만든다: **커밋하면 병렬 격리가
돌아온다.** 워크트리를 되찾는 값이 커밋이라는 것이 화면에 보인다(⑥ V6).

#### 본 저장소에서 여럿이 돌 때의 방벽 — 스폰 시점 파일 리스

워크트리를 안 쓰기로 하면 격리가 사라진다. 대체물 없이는 이 설계가 성립하지 않는다.
본 저장소에 이미 있는 것부터 세어 보면 방벽이 셋이다:

1. `snapshot_dirty()`(1610) — 30초마다 모든 미커밋 파일을 내용주소로 뜬다.
   덮어써도 원본이 `state/snapshots/` 에 있다. **복구는 된다.**
2. `bin/s9-guard concurrent_gate`(121) — 워커가 도는 중 900초 안에 만져진 파일을
   담으려는 커밋을 막는다. **반쪽 커밋은 안 난다.**
3. 봉투의 충돌 규율 문단(4596) — 이건 규율이지 장치가 아니다.

빠진 것은 **예방(같은 파일에 두 손이 동시에 붙는 것 자체)** 이다. 그것을 리스로 세운다.

```python
### state/file_leases/<safe(doc_id)>.json
### {"doc": ..., "pid": 0, "paths": [...], "since": ts, "reason": "spawn"}

def lease_take(doc_id, paths): ...          # 스폰 직전, pid 는 Popen 뒤에 채운다
def lease_release(doc_id): ...              # 워커 종료·문서 in-progress 이탈 시
def lease_conflict(doc_id, scope):
    """다른 문서가 잡고 있는 겹침 → [(path, holder_doc)]. 주인 pid 가 죽었으면
    그 리스는 없는 것으로 본다 — 죽은 손이 파일을 영원히 잡으면 교착이다."""
```

`_spawn_worker` 안, **`_auto_caps_ok` 앞**에 대기 판정을 넣는다:

```python
dec = workspace_decision(doc_id, cfg)
wait = None
if dec["kind"] == "main":
    inflight = [p for p in _worker_spawn_targets() if _worker_alive(p)]
    if len(inflight) >= int(cfg.get("auto_resume_max_inflight", 2)):
        wait = ("inflight", f"무인 작업자 {len(inflight)}이 이미 돌고 있다")
    elif dec["scope"]:
        hit = lease_conflict(doc_id, dec["scope"])
        if hit:
            wait = ("held", f"{hit[0][1]} 가 {hit[0][0]} 를 잡고 있다")
if wait and _wait_age(doc_id) < LEASE_WAIT_MAX_SEC:
    _wait_mark(doc_id, wait)
    _auto_log(f"DEFER {doc_id} reason={wait[0]} {wait[1]}")
    return block("waiting", f"{wait[1]} — 차례를 기다린다(30초마다 다시 본다).")
```

세 가지를 짚어 둔다:

- **거부가 아니라 대기다.** rework 워처가 30초마다 다시 부르므로 앞 작업자가 끝나면
  저절로 뜬다. 거부는 오늘의 병("멈춘 것이 계속 멈춰 있다")을 되살리지만 대기는 아니다.
- **기다림에 천장이 있다.** `LEASE_WAIT_MAX_SEC`(30분)를 넘기면 겹쳐도 띄우고 그
  사실을 크게 남긴다. 천장 없는 큐는 교착의 다른 이름이다.
- **스코프를 모르면(`None`) 붙잡지도 붙잡히지도 않는다.** 모르는 것으로 남을 막으면
  워커 하나가 전부를 세운다.
- 리스를 잡았으면 봉투(4596 else 가지)에 한 줄 더 준다:
  `[네 차례] 이 라운드에 네가 받은 코드 파일은 <paths> 다. 그 밖의 코드 파일은 만지지 마라 —
  다른 작업자가 잡고 있다.` 리스가 스케줄러 안에만 있으면 워커는 그것을 모른다.

---

### ③ 자동 커밋 — 쓰지 않는다

"스폰 전 스냅샷 커밋"은 기준선을 신선하게 만드는 가장 직접적인 수단이고,
그래서 진지하게 검토했다. **기각한다.** 근거 넷:

1. **이미 있는 자동 커밋이 지금 이 순간 조용히 실패 중이다.** (0-가) `_worktree_autocommit`
   은 자기 워크트리 안, 자기 파일만 담는 **가장 안전한 형태**의 자동 커밋인데도 게이트에
   막혀 30초마다 무한 반복하고 아무도 모른다. 그보다 훨씬 위험한 형태(본 저장소, 남의
   파일 포함)를 그 위에 얹는 것은 순서가 틀렸다. **먼저 고칠 것은 실패를 보이게 하는 일이다.**
2. **남의 진행 중 편집을 삼킨다.** 지금 22개 파일 2,702줄이 여러 주체의 손에 동시에
   있다. `git add -A` 는 "내 변경"이 아니라 "그 파일의 지금 상태"를 담는다 —
   2026-08-26 21:42 사고가 정확히 그것이었고, `s9-guard concurrent_gate` 는 그 사고를
   막으려고 존재한다. 스폰 경로의 자동 커밋은 그 게이트를 `S9_ALLOW_CONCURRENT` 로
   지나야만 가능하다. **분류기가 막은 일을 다른 문으로 통과시키는 것**이고,
   이 저장소가 이미 규율로 세운 '게이트에 옆문 내지 않기' 를 스스로 어긴다.
3. **되돌릴 수 없는 쪽을 자동화하는 셈이다.** 자리 선택은 틀려도 다음 스폰에 고쳐지고
   비용이 0이다. 커밋은 이력에 남고, 22개 파일을 뭉친 무의미한 커밋은 나중에 워크트리
   병합이 잘못됐을 때 되돌릴 지점을 없앤다.
4. **필요가 없다.** 기준선이 낡았다는 문제의 답은 두 가지다 — 기준선을 올리거나
   (커밋), 낡은 기준선을 안 쓰거나(본 저장소). 둘째가 공짜고 안전하다.

**대신 이렇게 한다:**

- (a) `_worktree_autocommit`(13205)을 **고친다** — 이건 유지한다. 워커 자기 가지,
  자기 파일이라 남을 삼키지 않고, 없으면 워커의 일이 사라진다(REQ-20260828-025-62x6).
  다만 `r = subprocess.run(...)` 의 반환코드와 stderr 를 읽고, 실패를 owner json 의
  `autocommit_tries`/`autocommit_error` 에 적고, `AUTOCOMMIT_MAX_TRIES` 회 실패하면
  그만두고 `s9 loose` 에 줄을 올린다.
- (b) 사람이 부르는 커밋은 손잡이로 준다: `s9 worktree add --commit-first` 또는
  `s9 spawn --commit-first`. **사람이 누르는 커밋은 괜찮다. 사람이 없을 때 나는 커밋이 문제다.**
- (c) 화면·CLI 가 "지금 워크트리를 못 쓰는 이유는 미커밋 `bin/s9`·`web/index.html`
  입니다" 를 말한다(⑥ V6). 커밋을 강제하지 말고 **이득을 보여서 유도한다.**

---

### ④ 워크트리를 거두는 규칙

원칙은 그대로다 — **남길 것이 있으면 지우지 않는다.** 바뀌는 것은 "지우지 않는다"가
"조용히 쌓인다"와 동의어이던 부분이다.

- **R1 문서당 하나.** `worker_workspace` 는 만들기 전에 `state/worktree_owners/` 에서
  같은 `doc` 을 찾는다.
  - 주인 pid 가 살아 있으면 → 애초에 스폰하지 않는다(겹쳐 띄우기 금지, 상류에서 막는다).
  - 죽었고 clean·not-ahead → 그 자리를 거두고 새로 만든다(잃을 것이 없다).
  - 죽었고 dirty 거나 ahead → **둘째를 만들지 않는다.** `worktree-exists` 로 본 저장소를
    고르고, 그 워크트리는 `s9 loose` 에 "합칠 것이 남았다" 로 올린다.
  오늘 `w-829-011` 이 넷이 된 것은 이 규칙 하나가 없어서다.
- **R2 총량 천장.** `worktree_list()` 가 `WORKTREE_MAX`(6) 이상이면 새 워크트리를 만들지
  않고 본 저장소로 간다(`worktree-pile`). 쌓임 자체가 신호다 — 신호를 무시하고 더
  쌓지 않는다.
- **R3 묵은 것은 지우지 말고 올린다.** sweep 에서 주인이 죽고 `WORKTREE_STALE_SEC`(24h)
  를 넘겼는데 dirty/ahead 인 워크트리는 `undelivered_requests()`(7242)에 이미 뜬다.
  거기에 **경과 시간**을 붙이고, 그 REQ 문서에 `--label worktree` 노트를 **한 번만**
  남긴다(중복 방지 표식을 owner json 에). 사람이 볼 자리는 문서다. 지우는 것은 사람이
  `s9 worktree merge` 또는 `rm` 으로 한다.
- **R4 자동커밋 포기.** (③-a) 3회 실패하면 재시도를 멈추고 `s9 loose` 에
  "자동 보존 실패 — 손으로 커밋해야 한다 (사유: …)" 로 올린다. 지금은 무한 루프라
  로그가 진짜 신호를 덮는다.
- **R5 성공 경로에서 즉시 거둔다.** `worktree_merge`(13170)는 이미 합치고 거둔다.
  더해서, 문서가 `done`/`blocked` 로 가고 그 가지가 이미 main 에 있으면(ahead==0,
  clean) sweep 이 `WORKTREE_MIN_AGE` 를 기다리지 않고 거둔다.

---

### ⑤ TDD 시나리오 — 구현 전에 실패해야 하는 계약

새 파일 `tests/test_spawn_workspace.py`. 실행: `python3 tests/ spawn_workspace`.
격리는 `tests/test_worker_worktree.py`(43행 `setUpClass`)의 방식을 그대로 쓴다 —
`S9_ROOT=mktemp` 에 git repo 를 세우고 `_load()` 로 `bin/s9` 를 모듈로 올린다.
docstring 첫 문단에 REQ-20260829-028-62x6 과 오늘의 실측(2,638줄·워크트리 아홉)을 적는다.

**A. 자리 판정 (`workspace_decision`)**

1. **W1(n) 깨끗하면 워크트리다.** code_dirty 가 비면 `kind=="worktree"`, `reason=="fresh"`.
2. **W2(n) 코드가 더러우면 본 저장소다.** `bin/s9` 를 미커밋으로 만든 뒤 판정하면
   `kind=="main"`. — *오늘의 사고 그 자체.*
3. **W3(b) 데이터만 더러운 것은 워크트리를 막지 않는다.** `vault/x.md`·`state/y.json`
   만 미커밋이면 여전히 `worktree`. (S9_ROOT 가 본 저장소를 가리키므로 기준선과 무관.)
4. **W4(b) 스코프를 모르고 코드가 더러우면 본 저장소다.** 문서가 아무 경로도 안
   대면 `reason=="dirty-unknown"`.
5. **W5(b) 등뼈가 더러우면 겹치지 않아도 본 저장소다.** 문서 스코프가 `tests/…` 뿐이고
   `web/index.html` 만 미커밋이어도 `reason=="dirty-spine"`.
6. **W6(b) 화면 작업은 깨끗해도 본 저장소다.** 스코프에 `web/` 가 있으면 코드가
   깨끗해도 `reason=="live-verify"`.
7. **W7(b) 자기 도구를 고치는 일은 본 저장소다.** 스코프에 `bin/s9` 가 있으면
   `reason=="self-edit"`.
8. **W8(b) 쌓이면 만들지 않는다.** 워크트리를 `WORKTREE_MAX` 개 세워 두면
   `reason=="worktree-pile"`.
9. **W9(r) 한 문서에 둘째 워크트리를 만들지 않는다.** 같은 doc 의 owner 가 이미 있고
   dirty 면 `reason=="worktree-exists"`, `worktree_list()` 길이가 안 는다.
   — *오늘 w-829-011 이 넷이 된 그 자리.*
10. **W10(r) 오늘의 재현.** HEAD 에는 없고 작업 트리에만 있는 함수를 고치라는 문서로
    `_spawn_worker` 를 (Popen 을 가짜로) 부르면, 워커의 cwd 가 `ROOT` 다.
    (지금 코드로 돌리면 워크트리 경로가 나와 실패한다.)

**B. 본 저장소 방벽 (리스)**

11. **L1(n) 스폰이 리스를 건다.** 스폰 후 `state/file_leases/<doc>.json` 에 스코프
    경로가 있고 `pid` 가 Popen 의 pid 다.
12. **L2(b) 겹치면 기다린다.** 다른 문서가 `web/index.html` 리스를 살아 있는 채로
    잡고 있으면 `_spawn_worker` 가 `False`, `out["blocked"]=="waiting"`, `out["why"]`
    에 잡고 있는 문서 id 가 들어간다.
13. **L3(b) 죽은 손은 잡지 못한다.** 리스 주인 pid 가 죽었으면 겹쳐도 스폰된다.
14. **L4(b) 기다림에 천장이 있다.** 대기 표식이 `LEASE_WAIT_MAX_SEC` 보다 오래됐으면
    겹쳐도 스폰되고, `_auto_log` 에 그 사실이 남는다. — *"멈춘 것이 계속 멈춰 있다"의 재발 금지.*
15. **L5(b) 동시 워커 수에 천장이 있다.** 살아 있는 무인 워커가
    `auto_resume_max_inflight` 이상이면 `waiting`.
16. **L6(r) 대기는 한도를 깎지 않는다.** 대기로 끝난 스폰 시도 뒤
    `state/auto_resume/_global.json` 의 `day_count`/`hour_count` 가 그대로다.
    — *`_auto_caps_ok`(4547) 앞에 판정을 두지 않으면 반드시 실패하는 계약.*
17. **L7(n) 워커가 자기 차례를 안다.** 본 저장소 스폰의 프롬프트에 리스로 받은
    경로들이 글자로 들어간다.

**C. 커밋 규율**

18. **C1(r) 스폰 경로는 본 저장소를 커밋하지 않는다.** 가짜 Popen 으로 스폰 전후
    `git rev-parse HEAD` 가 같고, `git status --porcelain` 의 스테이징 상태가 안 변한다.
19. **C2(r) 자동 커밋 실패가 보인다.** 커밋이 실패하도록 pre-commit 을 세우고
    `_worktree_autocommit` 를 3회 부르면, 4회째는 커밋을 시도하지 않고 owner json 에
    `autocommit_error` 가 있으며 `s9 loose` 결과에 그 워크트리 줄이 있다.
    — *지금 30초마다 무한 반복 중인 그 자리.*

**D. 거두기 (기존 회귀 유지 + 추가)**

20. **S1(r)** `tests/test_worker_worktree.py` 의 b1·b2·b4(dirty 보존·ahead 보존·
    살아있는 주인 보존)가 그대로 통과한다.
21. **S2(b)** 주인이 죽고 24시간 넘은 dirty 워크트리는 **지워지지 않고**
    `undelivered_requests()` 에 경과 시간과 함께 뜬다.

**E. 사람이 볼 자리**

22. **V1(n)** `state/auto_resume/spawn.log` 에 `WORKSPACE(main) <doc> reason=dirty-spine`
    형태의 줄이 남는다. (기존 `SPAWN(...)` 토큰은 건드리지 않는다 — 기존 시험과 운영
    grep 이 그 형태를 읽는다.)
23. **V2(n)** `state/auto_resume/<doc>.json` 에 `workspace.kind`·`workspace.reason` 이 있다.
24. **V3(n)** `wake_request` 의 성공 메시지에 '본 저장소' 또는 '워크트리' 가 들어간다.
25. **V4(b)** 대기일 때 `wake_request` 가 `action=="waiting"` 을 돌려주고 메시지가
    누가 무엇을 잡고 있는지 말한다 — 실패처럼 보이지 않는다.
26. **V5(n)** `catalog_with_live()` 의 in-progress 행에 `workspace` 키가 실린다.

---

### ⑥ 사람이 볼 자리

말없이 다르게 동작하면 다음 사람이 또 헤맨다. 여섯 자리에 낸다 — 통로는 전부 이미 있다.

- **V1 로그** `_auto_log`: `WORKSPACE(main|worktree) <doc> reason=<코드> [wt=<이름>]`,
  대기는 `DEFER <doc> reason=held holder=<doc> path=<경로>`.
- **V2 상태 파일** `state/auto_resume/<doc>.json` 의 `workspace{kind,reason,wt,at}`.
  `_worker_spawn_targets`(1915)와 `catalog_with_live`(11311)가 이미 이 파일을 읽으므로
  화면까지 새 통로가 필요 없다.
- **V3 문서** — 가장 중요한 자리. `_spawn_worker` 는 **자리 선택이 지난번과 달라졌을 때만**
  `s9 note <doc> --label workspace` 로 한 줄 남긴다:
  「본 저장소에서 이어받는다 — 미커밋 `bin/s9`·`web/index.html` 때문에 워크트리 기준선이
  다섯 시간 낡았다.」 매번 남기면 소음이고, 안 남기면 다음 사람이 또 문서를 뒤진다.
- **V4 손잡이의 답** `wake_request`(4797): 성공 메시지에 자리를 붙이고, 대기는
  `action="waiting"` 으로 "다른 작업자가 `web/index.html` 을 잡고 있어 차례를 기다린다 —
  30초마다 다시 본다" 를 돌려준다. `_wake_refusal`(4763)의 문장 톤을 따른다.
- **V5 화면** 카드에 작은 표시(`본 저장소` / `wt/…` / `대기 중`). 데이터는 V2 로 이미
  행에 실리므로 화면 몫은 표시뿐이다.
  ⚠ **이 REQ 에서는 `web/index.html` 을 만지지 마라** — 지금 그 파일은 다른 워크플로가
  잡고 있다(1,276줄 미커밋). 자식 REQ 로 갈라 designer/frontend 에 맡겨라.
  이 설계의 규칙을 이 설계의 구현이 스스로 지키는 것이 첫 증명이다.
- **V6 되찾는 값을 보인다** `s9 worktree ls` 와 `s9 doctor` 머리에 한 줄:
  「지금 워크트리를 못 쓴다 — 미커밋 코드: bin/s9, web/index.html (커밋하면 다시 쓴다)」.
  강제가 아니라 유인이다.

---

### ⑦ 기각한 대안

- **A. dirty 면 스폰 거부.** 기각. 이 저장소에서 미커밋은 상시라 무인 작업자가 영영
  안 뜨고, 그건 오늘의 병("멈춘 것이 계속 멈춰 있다")을 되살린다. 사용자가 명시적으로
  경고한 갈래다. — **채택한 것**: 거부가 아니라 **자리 바꾸기**(본 저장소), 그리고
  겹칠 때만 **천장 있는 대기**.
- **B. 스폰 전 본 저장소 스냅샷 커밋.** 기각. ③에 근거 넷. 요약하면 남의 편집을 삼키고,
  `s9-guard` 에 옆문을 내야 하며, 이미 있는 자동 커밋이 지금 조용히 실패 중이다.
- **C. `git stash` 로 잠깐 치우고 워크트리를 만든다.** 기각. 살아 있는 에이전트 셋이
  파일을 쥔 채로 stash 하면 그들의 다음 저장이 stash 뒤의 트리에 얹혀 뒤섞인다.
  이 저장소의 봉투가 `git stash` 를 금지하는 이유가 그것인데, 도구가 그걸 하면 규율이
  거짓말이 된다.
- **D. 워크트리를 만든 뒤 본 저장소의 미커밋 diff 를 이식한다.** 기각하되 **2단계로 남긴다.**
  이론상 신선함과 격리를 둘 다 얻는다. 그러나 지금 그대로 하면 워커의 가지가 **남의
  미완성 작업을 자기 기반으로 갖게 되고**, `s9 worktree merge` 가 그것을 워커의 산출물인
  양 본 저장소에 되돌려 넣는다 — 원저자가 그 사이에 더 고쳤다면 **고치는 것이 아니라
  지우는 일**이 된다. DOC-20260829-001-62x6 이 지목한 그 실패를 한 층 아래로 옮길 뿐이다.
  안전한 형태가 있긴 하다: **스코프에 해당하는 파일만** 이식하고, 워커가 그것을
  `기준선 이식` 이라는 **별도 첫 커밋**으로 못박게 하는 것. 그러면 병합이 이식분과
  작업분을 구분해 재생하고, 원본이 바뀌었으면 조용한 되돌림 대신 충돌이 뜬다.
  1단계(라우팅)가 돌고 나서, 워크트리를 다시 넓게 쓸 필요가 생기면 그때 만든다.
- **E. 워크트리를 아예 버린다.** 기각. `git add`/`checkout` 이 남의 작업을 삼키는 사고를
  막는 유일한 **구조적** 방벽이고(REQ-20260828-011-62x6, 네 번의 실사고), 깨끗한 기준선에서는
  대가가 0이다. 버리면 저장소가 커밋된 좋은 상태일 때조차 격리를 못 쓴다.
- **F. 워커의 파일 접근을 봉투로 제한해 충돌을 막는다.** 기각. `--allowedTools` 는
  도구 단위지 경로 단위가 아니고, 봉투는 "경계가 아니다"(4568 주석의 자백). 리스는
  스케줄러가 강제하고 봉투는 알리기만 한다 — 그게 이 도구로 할 수 있는 정직한 선이다.

---

### 단계 (현재 → 목표)

- **1단계 (이 REQ, `bin/s9` + `tests/` 만)**: `code_dirty_paths` · `declared_scope` ·
  `workspace_decision` · 리스 · 대기 · 로그/상태/문서 표출 · 거두기 R1~R5 ·
  `_worktree_autocommit` 실패 가시화. ⑤의 26개 계약.
  **선행 조건**: `bin/s9` 가 비어야 시작한다(지금 backend-developer 가 잡고 있다).
- **2단계 (자식 REQ)**: 카드 표시 — `web/index.html`, designer/frontend 몫.
- **3단계 (열어 두는 것)**: 대안 D 의 안전 변형(스코프 이식 + 기준선 이식 커밋).
  1단계가 돌고 나서, 워크트리를 넓게 쓸 필요가 실제로 생기면.

### 이 설계가 실패하는 조건 (미리 적어 둔다)

- `declared_scope` 가 문서에서 경로를 못 읽으면 판정이 늘 `dirty-unknown` 으로 몰려
  워크트리 lane 이 사실상 죽는다. 그래도 **안전한 방향의 죽음**이고, 되살리는 값은
  커밋이다. 다만 한 달 뒤 `WORKSPACE(worktree)` 가 로그에 한 줄도 없다면 그건
  워크트리를 버릴지 다시 물어야 한다는 신호다.
- 리스는 스폰 시점의 추정에 기댄다. 워커가 스코프 밖 파일을 고치면 리스가 못 막는다.
  그 잔여 위험은 `snapshot_dirty` 와 `s9-guard concurrent_gate` 가 받는다 —
  **예방은 새로, 복구와 커밋 게이트는 이미 있다.**

### 2026-08-29T17:04:38+09:00 subagent (agent: sub:subagent:a80ed348, by nicehugepark)

Appending design note to REQ-20260829-028-62x6

### 2026-08-29T17:05:20+09:00 subagent (agent: sub:architect:a10a47b5, by nicehugepark)

`[2026-08-29 17:0x KST - architect]`

`[2026-08-29 17:03:12 KST - architect]`

설계를 확정해 `REQ-20260829-028-62x6` 에 `--label architect` 노트로 남겼다(17,047자). `bin/s9` 는 한 글자도 고치지 않았고 git 쓰기 명령도 실행하지 않았다. 아래는 요지다.

### 읽으면서 나온 두 개의 실측 — 설계의 전제가 됐다

**자동 커밋 하나가 지금 이 순간 무한히 실패 중이다.** `spawn.log` 가 16:51부터 `WT-AUTOCOMMIT w-827-012-62x6-1787929230` 을 30초마다 계속 찍는다. `_worktree_autocommit`(`/home/sjpark1/section9/bin/s9:13205`)이 `git commit` 의 반환코드를 안 보고 `capture_output` 으로 stderr 를 버려서, pre-commit 게이트가 거부해도 아무 소리 없이 실패하고 sweep 이 다음 틱에 또 부른다.

**한 문서가 워크트리를 넷까지 만들었다.** `worker_worktree_name()`(13139)이 매 스폰에 `int(time.time())` 을 붙여 재스폰이 곧 새 워크트리다. `w-829-011-62x6-` 이 owner 파일 넷. 아홉까지 늘어난 건 sweep 이 게을러서가 아니라 스폰이 무제한이라서다.

### ① 고칠 자리

스폰 관문은 이미 하나다 — **`_spawn_worker`(bin/s9:4510)**. `s9 worktree` 하위명령만 다른 길(`cmd_worktree`, 13290, 사람이 부르는 길이라 손대지 않는다)이고, auto-resume(`rework_watch_tick` 4863 → `_spawn_rework` 4660), `/api/wake`(12858 → `wake_request` 4797 → `_spawn_wake` 4715), `resume-item`(8557)이 **전부 4510으로 모인다.** 판정을 여기 하나에 넣으면 네 경로가 덮인다.

핵심 함정 하나: 대기 판정은 **`_auto_caps_ok`(4547) 앞**에 둬야 한다. 그 함수는 통과 시 일일/시간 카운터를 올리므로, 뒤에 두면 대기 한 번마다 한도가 깎여 30분 뒤엔 못 띄운다. 계약 L6이 이걸 붙잡는다.

### ② 판정 규칙 — 핵심 통찰

**워크트리가 놓치는 것은 코드뿐이다.** `worker_workspace` 가 `S9_ROOT` 를 본 저장소로 못박으므로 `vault/`·`state/`·`index/`·`users/` 의 미커밋은 기준선과 무관하다. 그래서 "dirty 면 거부"는 이 저장소에서 상수 `True` 지만, **코드만 세면 판정이 다시 살아난다.**

```
if 등뼈(bin/s9 · web/index.html)가 미커밋:  본 저장소  ("dirty-spine")
if 스코프에 web/ 있음:                      본 저장소  ("live-verify")
if 스코프에 bin/s9 있음:                    본 저장소  ("self-edit")
if code_dirty 없음:                        워크트리   ("fresh")
if 스코프 모름:                             본 저장소  ("dirty-unknown")
if 스코프 ∩ code_dirty:                     본 저장소  ("dirty-overlap")
else:                                      워크트리   ("fresh-outside")
```

`dirty-spine` 이 오늘의 사고를 정면으로 막는 줄이다. 시험이 `HERE/../bin/s9` 를 읽고 화면 검증이 9909를 보므로, 등뼈가 더러우면 스코프가 안 겹쳐도 검증이 거짓말한다. **지금 둘 다 dirty → 오늘 뜨는 모든 워커는 본 저장소로 간다.** 의도한 결과이고, 좋은 압력을 만든다: 커밋하면 병렬 격리가 돌아온다.

**본 저장소 방벽**: 워크트리를 안 쓰면 격리가 사라지므로 대체물을 함께 설계했다. 이미 있는 것은 `snapshot_dirty`(복구)와 `s9-guard concurrent_gate`(반쪽 커밋 차단)뿐이고 **예방이 없다.** 그래서 스폰 시점 파일 리스(`state/file_leases/`)를 세운다 — 겹치면 **거부가 아니라 대기**(rework 워처가 30초마다 재시도), 대기에는 30분 천장(천장 없는 큐는 교착의 다른 이름), 스코프를 모르면 붙잡지도 붙잡히지도 않는다, 동시 워커 수 천장(`auto_resume_max_inflight`, 기본 2).

### ③ 자동 커밋 — 기각

근거 넷: (1) 가장 안전한 형태의 자동 커밋조차 지금 조용히 실패 중이다 — 그보다 위험한 걸 그 위에 얹는 건 순서가 틀렸다. (2) `git add -A` 는 "내 변경"이 아니라 "그 파일의 지금 상태"를 담는다(08-26 21:42 실사고). (3) `s9-guard` 를 `S9_ALLOW_CONCURRENT` 로 지나야만 가능하다 — 게이트에 옆문이다. (4) 필요 없다: 기준선이 낡았으면 안 쓰면 되고, 자리 선택은 비용 0에 되돌릴 수 있지만 커밋은 아니다.

대신 `_worktree_autocommit` 은 **유지하되 실패를 보이게** 고치고(3회 실패 후 포기 + `s9 loose` 에 올림), 커밋은 사람이 누르는 손잡이(`--commit-first`)로만 주고, 화면이 "지금 워크트리를 못 쓰는 이유는 미커밋 bin/s9·web/index.html 입니다" 를 말해 **강제 대신 유인**한다.

### ④ 거두기

문서당 하나(R1 — 오늘 넷이 된 자리) · 총량 천장 6(R2) · 24시간 넘은 것은 **지우지 말고 `s9 loose` 와 REQ 문서에 올린다**(R3) · 자동커밋 3회 실패면 포기하고 올린다(R4) · 합쳐진 가지는 즉시 거둔다(R5). "남길 것이 있으면 지우지 않는다"는 그대로, 다만 그게 "조용히 쌓인다"와 동의어이던 부분만 바꾼다.

### ⑤ TDD

`tests/test_spawn_workspace.py`(실행 `python3 tests/ spawn_workspace`), `tests/test_worker_worktree.py` 의 격리 방식(`S9_ROOT=mktemp` + `_load()`)을 그대로 따르는 **26개 번호 계약** — 자리 판정 10, 리스 7, 커밋 규율 2, 거두기 2, 표출 5. 오늘을 직접 재현하는 것이 W2·W10(HEAD 에 없는 함수가 작업 트리에만 있을 때 cwd 가 ROOT 여야 한다)·W9·C2·L6이다.

### ⑥ 사람이 볼 자리

로그 토큰 `WORKSPACE(main|worktree) … reason=` / `DEFER …`(기존 `SPAWN(...)` 은 건드리지 않는다 — 운영 grep 과 기존 시험이 읽는다) · `state/auto_resume/<doc>.json` 의 `workspace{}`(이미 `catalog_with_live` 가 읽는 파일이라 새 통로 불필요) · **자리가 지난번과 달라졌을 때만** REQ 문서에 `--label workspace` 한 줄 · `wake_request` 가 자리를 말하고 대기는 `action="waiting"` 으로 "누가 무엇을 잡고 있다" · 카드 표시 · `s9 worktree ls`/`doctor` 머리에 "커밋하면 다시 쓴다".

**중요**: 카드 표시는 `web/index.html` 이라 이 REQ 에서 만지면 안 된다(지금 다른 워크플로가 1,276줄 미커밋으로 잡고 있다). 자식 REQ 로 갈라 designer/frontend 에 맡겨라 — 이 설계의 규칙을 이 설계의 구현이 스스로 지키는 것이 첫 증명이다.

### ⑦ 기각한 대안

A) dirty→스폰 거부(멈춤 병 재발) B) 스폰 전 스냅샷 커밋(③) C) `git stash`(봉투가 금지한 걸 도구가 하면 규율이 거짓말이 된다) D) **미커밋 diff 이식** — 이론상 신선함+격리를 둘 다 얻지만 워커 가지가 남의 미완성을 기반으로 갖게 되고 병합이 그걸 워커 산출물인 양 되돌려 넣는다(원저자가 그 사이 고쳤으면 고치는 게 아니라 지우는 일 — DOC-001이 지목한 실패를 한 층 아래로 옮길 뿐). 다만 **스코프 파일만 이식 + `기준선 이식` 별도 첫 커밋** 형태는 안전하므로 3단계로 열어 뒀다. E) 워크트리 폐기(깨끗한 기준선에서 대가 0인 유일한 구조적 방벽) F) 봉투로 경로 제한(`--allowedTools` 는 도구 단위고, 봉투는 경계가 아니다 — 코드 자신이 4568에서 자백한다).

### 한계

`declared_scope` 가 문서에서 경로를 못 읽으면 판정이 `dirty-unknown` 으로 몰려 워크트리 lane 이 사실상 죽는다. 안전한 방향의 죽음이지만, 한 달 뒤 로그에 `WORKSPACE(worktree)` 가 한 줄도 없다면 워크트리를 버릴지 다시 물어야 한다는 신호다. 리스는 스폰 시점 추정이라 스코프 밖 편집은 못 막고, 그 잔여 위험은 `snapshot_dirty` 와 `concurrent_gate` 가 받는다 — 예방만 새로 세우고 복구와 커밋 게이트는 이미 있는 것을 쓴다.

구현 착수 선행 조건은 하나다: `bin/s9` 가 비어야 한다.

### 2026-08-29T17:06:22+09:00 response (by nicehugepark)

### 설계 중 드러난 두 가지 — 지금 이 순간 돌고 있는 결함이다 (리드 실측)

**① 자동 커밋이 30초마다 조용히 실패하며 무한히 되풀이된다.**

    state/auto_resume/spawn.log
    17:05:01 WT-AUTOCOMMIT w-827-012-62x6-1787929230 doc=REQ-20260827-012-62x6
    17:05:01 WT-AUTOCOMMIT w-829-021-62x6-1787989686 doc=REQ-20260829-021-62x6
    17:05:32 WT-AUTOCOMMIT w-827-012-62x6-1787929230 doc=REQ-20260827-012-62x6
    17:05:32 WT-AUTOCOMMIT w-829-021-62x6-1787989686 doc=REQ-20260829-021-62x6

두 워크트리가 계속 같은 줄을 찍는다. `_worktree_autocommit`(bin/s9:13205)이 `git commit` 의
반환코드를 안 보고 stderr 를 버려서, 게이트가 거부해도 성공으로 보이고 sweep 이 다음 틱에 또 부른다.
로그만 부풀고 아무것도 커밋되지 않는다. 그 워크트리에는 실제로 미커밋 넷이 남아 있다.

**② 오늘 일일 캡이 소진돼, 사람이 드래그한 카드에 작업자가 뜨지 않는다.**

    오늘 SPAWN(fresh) 8 · SPAWN(resume) 6 · SKIP(cap) 43

17:05 에도 `SKIP(cap) REQ-20260829-014-62x6 rework` 가 30초마다 찍힌다. 사용자가 16:47~16:52 에
014·016·021·023·026 을 in-progress 로 옮겼는데, 캡이 이미 차 있어 그중 상당수는 스폰되지 않는다.
**카드는 in-progress 인데 아무도 안 붙는 상태**가 바로 REQ-20260828-041-62x6 이 잡으려던 그 병이고,
사용자가 "깨우기가 안 된다"고 한 것의 한 갈래일 수 있다 — 눌러도 `capped` 로 거절되기 때문이다.

캡은 게이트다. 리드가 조용히 올리지 않는다(옆문 금지). 사용자에게 사실을 알리고 판단을 받는다.
①은 이 REQ 의 구현 범위(`bin/s9`)에 그대로 들어가고, ②는 REQ-20260828-041-62x6 의 검증에 걸린다.

## History
- 2026-08-29T16:54:20+09:00 created by nicehugepark (status: open)
- 2026-08-29T16:54:45+09:00 status: open -> in-progress (by nicehugepark) — 설계 먼저 — bin/s9 는 지금 079·041 워크플로의 backend 담당이 잡고 있어, 그 파일이 빌 때까지 읽기 전용 설계를 진행한다
