---
id: REQ-20260829-036-62x6
type: request
title: 멈춤이 일하는 손을 못 본다
summary: 리드가 붙인 서브에이전트가 문서에 등록되지 않아, 일하는 중인데 멈춤으로 그려지고 깨우기가 겹쳐 뜬다
goal: 누군가 실제로 붙어 있으면 멈춤으로 그리지 않고 깨우기도 겹쳐 띄우지 않는다
status: done
size: M
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: 911949e0
agents: ["sub:backend-developer:a740027e", "sub:subagent:ade33cad"]
contributions: [{"actor": "sub:backend-developer:a740027e", "item": "036 멈춤 판정 신뢰", "started": "2026-08-29T21:04:50+09:00", "ended": "2026-08-29T21:31:58+09:00", "result": "stalled", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/911949e0-5d1b-4cd2-b08d-70fd0415a19a/tasks/a740027ef12761cdd.output", "reason": "207초째 진전 없음 (기준 180초)"}, {"actor": "sub:subagent:ade33cad", "item": "Appending implementation note to REQ-20260829-036-62x6", "started": "2026-08-29T21:53:05+09:00", "ended": "2026-08-29T21:53:05+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/911949e0-5d1b-4cd2-b08d-70fd0415a19a/subagents/agent-ade33cad8e8bd5e7f.jsonl"}]
project: section9
relates_why: {}
tags: ["workflow", "index", "design"]
created: 2026-08-29T20:35:29+09:00
status_since: 2026-08-29T22:20:32+09:00
updated: 2026-08-29T22:20:32+09:00
priority: 50
---

## Original

사용자(20:33): "멈춤으로 나오는건 진짜인가? 신뢰경험이 아직인것같다"

**실측으로 확인했다 — 신뢰할 수 없는 것이 맞다. 그리고 두 갈래다.**

### ① 일하는 중인데 멈춤으로 그려진다 (거짓 멈춤)

20:34 현재 `s9 stalled` 가 REQ-20260829-030-62x6 을 "25분째 진전 없음"으로 올린다.
그런데 **바로 그 순간 designer 서브에이전트가 그 요청을 붙잡고 `web/index.html` 을 쓰고 있다.**

원인: 그 문서의 `agents`·`contributions` 가 **비어 있다.** 리드가 Agent 도구로 붙인
서브에이전트가 문서에 자기를 등록하지 않는다. 그래서
- `stall_mins` 의 시계(`updated`)는 25분 전에 멈춰 있고 — 에이전트는 코드를 고치지
  문서를 쓰지 않는다. **진전 = 문서 쓰기**라는 등식이 깨지는 자리다.
- `delegated_running` 도 볼 것이 없다.

REQ-20260829-034 가 고친 것의 **거울상**이다. 034 는 "한 일이 없는데 경보가 꺼진다"였고,
이것은 "일하는데 경보가 켜진다"이다. 같은 시계 하나에서 나온다.

### ② 그래서 깨우기가 겹쳐 띄운다 — 실제로 일으켰다

살아 있는 서버에 그대로 쳐 봤다:

    POST /api/wake {"id":"REQ-20260829-030-62x6"}
    → {"ok": true, "action": "spawned", "mins": 25,
       "message": "… 무인 작업자가 본 저장소에서 이어받는다"}

designer 가 그 파일을 쓰는 중에 무인 작업자가 하나 더 떴다(pid 450334). **일하는 손 위의
두 번째 손이다.** 27초 만에 `s9 workers --stop` 으로 세웠고(REQ-20260829-021 이 방금 낸
그 손잡이다) `web/index.html` 은 무손상이었다(마지막 수정 20:30:15, 워커는 20:34 에 떴다).

REQ-20260829-016 «리드가 붙은 일에 작업자가 또 뜬다» 가 done 으로 닫혀 있는데, **이 경로는
막지 못한다** — 016 은 워처 쪽 게이트를 봤고, 여기서 뚫린 것은 "리드의 서브에이전트가
애초에 문서에 안 보인다"는 앞단이다.

### ③ 그리고 참인 멈춤도 쓸모가 없다

`s9 stalled` 의 나머지(027·024·025·029)는 진짜로 아무도 안 붙어 있다. 다만 그것은 리드가
**일부러 줄 세운 것**이다 — 같은 파일(`web/index.html`·`bin/s9`)을 놓고 다투지 않게.
"멈춤 25분째"는 참이지만 사람에게는 "버려졌다"로 읽힌다. 대기와 멈춤이 구별되지 않는다.
이 절반은 REQ-20260829-030 이 맡고 있다.

### 고칠 방향

판정이 한 자리에서 나야 한다. 지금은 시계(`stall_mins`)와 겹침 판정(`delegated_running`)이
따로 살고, 둘 다 못 보는 세 번째 사실(리드의 서브에이전트가 붙어 있음)이 있다.
후보: 서브에이전트 스폰이 문서에 기여를 등록하게 하고(훅 또는 Agent 도구 경유),
`stall_mins` 가 그 신선한 기여를 진전으로 읽는다. 등록이 없는 경로가 남으면 같은 일이 또 난다.

## Notes

### 2026-08-29T21:11:10+09:00 tdd (by nicehugepark)

### TDD 시나리오 — 멈춤 판정 신뢰 (backend)

**먼저 실측으로 밝힌 것: "등록하지 않는다" 는 절반만 맞다. 더 나쁜 쪽은 엉뚱한
문서에 등록한다.** 세션 로그 SES-20260829-027-62x6 136행:

    20:09:58 위임 등록: REQ-20260829-029-62x6 ← sub:designer:a848238e (클레임)

그리고 REQ-029 의 기여 항목 이름이 그 증거다 — `"item": "030·031 화면 몫"`.
designer 는 030·031 을 하고 있었는데 기여는 029 에 심겼다. 이유:
`s9-audit-agent.named_reqs()` 는 정식 id 정규식만 본다. 리드가 description 에
적은 것은 `030·031` 이라 하나도 안 잡혔고, prompt 에 배경으로 실린 정식 id 는
여럿이라 "유일하지 않으면 고르지 않는다" 규칙에 걸려 클레임 경로로 물러났다.
그 세션의 active_reqs 는 [027,024,025,029] 였고 최근 클레임인 029 가 뽑혔다.

한 원인이 사고 둘을 낳았다 — 030 은 손이 안 보여 거짓 멈춤 + 겹친 스폰,
029 는 없는 손이 보여 진짜 멈춤이 가려졌다.

### 계약

- [ ] C1 (심장, ①의 재현). description 이 `030·031 화면 몫` 이면 위임 기여는
      030 **과** 031 둘 다에 등록된다. 세션이 잡고 있을 뿐인 029 에는 안 붙는다.
- [ ] C2. 지명이 안 풀려 세션 클레임으로 **추정**해 붙인 위임은 추정으로
      표시된다 — 바인딩의 `agent_req[transcript]` 가 비고 `unassigned_hands()`
      가 그 손을 낸다. 지명으로 붙인 것은 확정으로 표시돼 그 목록에 안 나온다.
- [ ] C3 (①의 결과). 미상의 손이 살아 있는 동안 in-progress 요청의 판정은
      `stalled` 이 아니라 `unknown` 이고 `stall_mins()` 는 None 이다 —
      `s9 stalled` 에도 안 실리고 화면도 멈춤으로 안 그린다.
- [ ] C4 (②). 그 상태에서 `wake_request()` 는 스폰하지 않고 `action="unknown"`
      으로 거부하며 이유를 사람 말로 낸다. 스폰 경로(`_spawn_worker`)에서도
      거부가 아니라 **대기**로 선다 — 워처가 30초마다 다시 부른다.
- [ ] C5 (③). `spawn_wait` 표식이 있는 요청은 행에 `stall_state="waiting"` ·
      `wait_kind` · `wait_why` · `wait_mins` 가 실리고 `stalled_mins` 는 None
      이다. 대기는 멈춤이 아니다.
- [ ] C6 (판정 한 자리). `stall_verdict()` 하나가 `catalog_with_live` ·
      `stalled_requests` · `wake_request` 셋의 근거다. `stall_mins()` 는 그
      함수의 얇은 껍데기로 남는다(기존 호출부 보존).
- [ ] C7 (회귀). 아무 손도 대기 표식도 없으면 여전히 `stalled` 이고 분이 나온다.
      살아 있는 위임 기여가 있으면 `attached`. 무인 워커가 돌면 `moving`.

### 행 계약 (화면 몫 — 여기서는 싣기만 하고 그리지 않는다)

`catalog_with_live()` 의 각 행에 추가:

    stall_state : "moving" | "attached" | "waiting" | "unknown" | "stalled"
    stall_why   : 사람이 읽는 한 문장 (근거)
    quiet_mins  : 문서가 조용한 시간(분) — 상태와 무관하게 늘 실린다
    wait_kind   : "inflight" | "held"        (대기 중일 때만)
    wait_why    : "REQ-… 가 bin/s9 를 잡고 있다" (대기 중일 때만)
    wait_mins   : 기다린 시간(분)             (대기 중일 때만)

`stalled_mins` 의 뜻은 좁아진다 — **`stall_state == "stalled"` 일 때만** 값이
있다. 대기·미상은 이제 멈춤이 아니다.

격리: S9_ROOT=mktemp. 실행: `python3 tests/ stall_trust delegation_target`

### 2026-08-29T21:14:05+09:00 conflict (by nicehugepark)

기준선이 깨끗하지 않다 — `bin/s9` 에 손이 둘이다.

착수 시점(21:05)에 무인 작업자 pid 506116 이 REQ-20260829-024-62x6 로 8분째
`bin/s9` 를 고치고 있었고, `state/file_leases/REQ-20260829-024-62x6.json` 이
그 파일을 잡고 있다(paths: ["bin/s9"]). `git diff bin/s9` 에 그쪽 변경이
189줄 들어와 있다(`stop_request`·`stop_all_workers`·`worker_stop(owner=)`·
`restart_session(stop_workers=)`·`cmd_workers --stop-all`).

이 요청이 바로 그 사고를 다루는데 그 사고의 한가운데서 작업하게 됐다.
물러나지 않고 계속하되 규율을 좁힌다: ① `bin/s9` 에는 Write 를 쓰지 않고
앵커 기반 부분 치환만 한다 ② 마칠 때 그쪽 5개 훅이 그대로 있는지 확인해
보고에 적는다 ③ `git add/checkout/stash` 는 쓰지 않는다.

리드에게: 커밋할 때 두 작업의 변경이 한 diff 에 섞여 있다.

### 2026-08-29T21:52:55+09:00 backend-developer (by nicehugepark)

### 구현 — 멈춤 판정을 한 자리로, 손을 보이게

### 먼저: 원인은 "등록을 안 한다" 가 아니라 **엉뚱한 문서에 등록한다** 였다

세션 로그 `SES-20260829-027-62x6` 136행이 증거다.

    20:09:58 위임 등록: REQ-20260829-029-62x6 ← sub:designer:a848238e (클레임)

그런데 그 기여의 항목 이름은 `"030·031 화면 몫"` 이다. designer 는 030·031 을
하고 있었고 기록만 029 로 갔다. 이유: `s9-audit-agent.named_reqs()` 가 **정식
id 정규식만** 봐서 description 의 `030·031` 이 하나도 안 잡혔고, prompt 에
배경으로 실린 정식 id 는 여럿이라 "유일하지 않으면 고르지 않는다"(REQ-028-041
D3)에 걸려 클레임 경로로 물러났다. 세션의 active_reqs 는 [027,024,025,029]
였고 최근 것인 029 가 뽑혔다.

**한 오귀속이 사고 둘을 낳는다.** 030 은 손이 안 보여 25분째 '멈춤' 이 됐고
20:34 의 깨우기가 두 번째 손을 붙였다. 029 는 **없는 손이 보여** 진짜 멈춤이
가려졌다. 요청서의 ①과, 요청서가 몰랐던 그 거울상이 같은 한 줄에서 나온다.

### 무엇을 진전으로 읽기로 했나

**"어느 문서인지 아는 손"만 진전이다.** 손의 존재(`live_agents` — 바인딩의
`agent_transcript_path` mtime)는 이미 있었지만 그건 **세션 단위**라 어느 요청
인지 말하지 못한다. 그것을 그대로 진전으로 읽으면 리드 세션이 살아 있는 동안
모든 요청의 경보가 꺼진다 — REQ-034 가 고친 병 그대로다. 그래서 손에 **행선지**
를 붙였다: 바인딩에 `agent_req = {transcript: REQ-id}`. 값이 셋이다 —
정식 id(확정) · `""`(추정) · 없음(옛 손). 그리고 판정을 다섯으로 나눴다.

    moving   무인 워커가 돌거나 문서가 방금 움직였다
    attached 살아 있는 위임 기여가 있다 (delegated_running)
    waiting  차례를 기다린다 (spawn_wait 표식)
    unknown  어느 문서인지 모르는 손이 붙어 있다 — 이 요청일 수 있다
    stalled  진짜 멈춤

`unknown` 은 **감추지 않는다**: 조용한 시간(`quiet_mins`)은 어느 상태든 그대로
싣고, `s9 stalled` 은 목록에서 빼되 "멈춤 아님: … n건" 한 줄로 센다. 넓게
잡히는 것은 알고 한 선택이다 — 거짓 멈춤의 대가는 일하는 파일 위의 두 번째
손이고, 거짓 미상의 대가는 2분(손의 신선도 창)의 유예다.

### 고친 것 (파일:함수)

- `bin/s9:delegation_targets()` **[신규]** — 귀속 판정의 유일한 자리. ① 정식
  id + **문서 번호 세 자리**(`030·031`)를 살아 있는 요청으로 푼다, 여럿이면
  **여럿 다** 낸다 ② prompt 는 유일할 때만 ③ 그래도 없으면 세션 클레임 —
  그러나 `src="클레임"` 으로 **추측임을 함께 낸다**. 날짜(2026-08-29)·포트
  (9909)·정식 id 안쪽 숫자는 번호로 안 센다. `cmd_delegate_target()` 로
  `s9 delegate-target` 노출.
- `bin/s9-audit-agent:target_reqs()` — 훅 안의 정규식·후보 조회를 **전부**
  위 명령 한 번으로 대체. 훅이 s9 를 대여섯 번 띄우던 것이 대상 하나면 4번.
  `main()` 이 지명된 **모든** 문서에 claim+contrib 를 남긴다.
- `bin/s9:_claim_req(agent_req_guess=)` · `cmd_claim(--guess)` — 추정 귀속을
  바인딩에 `""` 로 남긴다.
- `bin/s9:_bind_agent_hand()` · `s9 claim --agent-transcript P` (id 생략) —
  **붙일 문서를 못 골라도 손은 등록한다.** 종전엔 훅이 그냥 돌아서 바인딩에
  transcript 조차 안 남았고, `s9 workers` 도 못 보는 손이 됐다.
- `bin/s9:live_agents()` — 행에 `req` 를 싣는다(확정/추정/없음 3값).
- `bin/s9:unassigned_hands()` **[신규]** · `_attributed_transcripts()` —
  "어느 문서인지 모르는 살아 있는 손". 옛 손(기록 없음)은 **문서의 열린
  기여**로 되짚는다. 추정(`""`)은 문서로 되짚지 않는다 — 그러면 `--guess` 가
  아무 뜻도 없어진다.
- `bin/s9:stall_verdict()` **[신규]** — 판정이 사는 유일한 자리.
  `stall_mins()` 는 그 얇은 껍데기로 남았다(호출부 보존).
- `bin/s9:catalog_with_live()` — 행에 `stall_state`·`stall_why`·`quiet_mins`·
  `wait_kind`·`wait_why`·`wait_mins`. `unassigned_hands()` 는 한 번만 센다.
- `bin/s9:stalled_requests(states=)` · `cmd_stalled` — 색인 한 번 읽고
  멈춤/대기/미상을 가른다.
- `bin/s9:_wake_refusal()` — `waiting`·`unknown` 거부 문구. 문장은 판정이
  이미 낸 것을 그대로 쓴다(카드와 버튼이 다른 말을 하지 않게).
- `bin/s9:_spawn_worker()` — 미상의 손이 있으면 **거부가 아니라 대기**.
  워처·사람 손잡이가 함께 지나는 그 한 자리다.
- `bin/s9:_wait_info()` **[신규]** — `state/spawn_wait/` 표식을 행에 싣는 통로
  (③). 두 시계를 본다: `last`(워처가 30초마다 갱신) · `since`(기다림의 천장).

### 판정이 한 자리인가

`catalog_with_live` · `stalled_requests` · `wake_request` 셋 다
`stall_verdict()` 하나를 지난다. `test_stall_trust.C6` 이 그 사실을 소스에서
붙잡는다(`stall_mins` 본문에 `stall_verdict(` 가 있어야 한다).

### 등록 없이 남는 경로 — 세어 본 결과 **셋**

1. **하네스가 `agentId`/`output_file` 을 안 주는 형태로 바뀌면** —
   `parse_agent_result` 가 빈손이라 손도 문서도 못 남긴다. 완전 실명.
   이건 하네스 계약 의존이라 여기서 닫을 수 없다.
2. **`S9_AUDIT=off` 또는 훅 미설치** — 같은 이유로 실명.
3. **훅 타임아웃(20초)** — s9 호출 수를 줄여(6~7 → 4) 여지를 넓혔지만 남는다.

닫은 것: "대상 REQ 를 못 골랐을 때"(전에는 여기서 돌아섰다 → 이제 손만
등록) · "번호로 지명했을 때"(전에는 못 풀었다) · "둘을 맡겼을 때"(전에는
하나만 골랐다) · "추측을 확정처럼 적던 것".

### 화면 몫으로 넘길 계약 (`web/index.html` 은 안 건드렸다)

`GET /api/catalog` 의 각 in-progress 행:

    stall_state : "moving" | "attached" | "waiting" | "unknown" | "stalled"
    stall_why   : 사람이 읽는 근거 한 문장
    quiet_mins  : 문서가 조용한 시간(분) — 상태와 무관하게 늘 있다
    wait_kind   : "inflight" | "held" | "hands"   (waiting 일 때만)
    wait_why    : "REQ-… 가 bin/s9 를 잡고 있다"   (waiting 일 때만)
    wait_mins   : 기다린 시간(분)                  (waiting 일 때만)

`stalled_mins` 의 뜻이 **좁아졌다** — `stall_state == "stalled"` 일 때만 값이
있다. 대기·미상은 더 이상 멈춤이 아니다. 화면 쪽 `stallState(r)` 술어가 그
하나만 읽고 있으므로 지금도 안 깨지지만, 대기·미상을 **그리려면** 위 필드가
필요하다. 후속 REQ 로 내면 된다: 멈춤은 지금처럼, 대기는 "차례 25분째"로,
미상은 "확인 필요"로 — 셋을 한 이름으로 부르면 사람은 셋 다 못 믿는다.

`POST /api/wake` 의 `action` 에 값 둘이 늘었다: `waiting` · `unknown`.
화면은 여전히 `ok`·`action`·`message` 셋만 읽으면 된다.

### 시험

- **신규** `tests/test_stall_trust.py` — 13건 (C2 추정/확정/옛손 · C3 미상 ·
  C4 깨우기 거부 · C5 대기 · C6 단일 판정 · C7 회귀).
- **추가** `tests/test_delegation_target.py` — 8건 (D7 실사고 재현 ·
  D8 추정 표시 · D9 둘 다 등록 · D10 집 없는 손). 클래스 하나 신설.
- **고침** `tests/test_stalled_claim.py:test_b4` (판정 자리가 `stall_verdict`
  로 옮겨져 소스 검사 대상 변경) · `tests/test_wake_reach.py:setUp`
  (그 클래스는 점·손잡이 짝만 보므로 미상 축을 격리 — 안 그러면 결과가
  "지금 리드가 무엇을 시켜 놨나"에 따라 흔들린다).
- **전체** `python3 tests/` → **Ran 2214 tests in 468s — OK (skipped=1)**.

### 실물 확인

    $ s9 delegate-target --description "030·031 화면 몫" \
        --prompt "배경: REQ-…-027 와 REQ-…-024 도 보라"
    {"reqs": ["REQ-20260829-030-62x6", "REQ-20260829-031-62x6"], "src": "지명"}

사고 그대로의 입력이 이제 두 문서로 풀린다. 서버 재기동 뒤
`GET /api/catalog` 도 새 필드를 싣는 것을 확인했다(024 moving · 027·036
attached · 025·029 stalled).

### 못 한 것

- **위 세 경로(하네스 파싱·훅 off·타임아웃)** 는 닫지 못했다. 훅 바깥의 계약
  이라 여기서 닫으면 옆문이 된다.
- `unknown` 은 **모든** in-progress 요청에 걸린다 — 손의 행선지가 미상이라
  좁힐 근거가 없다. transcript 본문을 읽어 REQ 언급을 찾으면 좁힐 수 있지만,
  매 판정마다 큰 파일을 읽는 값이라 하지 않았다. 손의 신선도 창(2분)이 천장이다.
- `workspace_decision`·`lease_*` 는 읽기만 하고 안 고쳤다(요청서 지시).

### 2026-08-29T21:53:05+09:00 subagent (agent: sub:subagent:ade33cad, by nicehugepark)

Appending implementation note to REQ-20260829-036-62x6

### 2026-08-29T21:53:13+09:00 tdd (by nicehugepark)

### TDD 시나리오 — 결과

- [x] C1. description `030·031 화면 몫` → 030·031 **둘 다**에 등록, 029 에는
      안 붙는다. (`test_delegation_target` D7·D9, `s9 delegate-target` 실물)
- [x] C2. 추정 귀속은 `agent_req[transcript] = ""` 로 남고 `unassigned_hands()`
      가 그 손을 낸다. 지명은 확정으로 남아 안 세어진다. (D8·D8b, C2·C2b)
      옛 바인딩(기록 없음)은 문서의 열린 기여로 되짚는다 (C2d), 추정은
      문서가 있어도 추정으로 남는다 (C2e).
- [x] C3. 미상의 손이 있는 동안 판정은 `unknown`, `stall_mins()` 는 None,
      `s9 stalled` 목록에서 빠진다. `quiet_mins` 는 그대로 실린다. (C3·C3b)
- [x] C4. 그 상태의 `wake_request()` 는 `action="unknown"` 으로 거부하고
      이유를 낸다. 스폰 경로(`_spawn_worker`)에서도 거부가 아니라 대기다. (C4)
- [x] C5. `spawn_wait` 표식이 행에 `stall_state="waiting"`·`wait_kind`·
      `wait_why`·`wait_mins` 로 실리고 `stalled_mins` 는 None. (C5·C5b)
- [x] C6. `stall_verdict()` 하나를 화면·CLI·깨우기가 함께 먹는다. (C6)
- [x] C7. 진짜 멈춤은 여전히 멈춤, 살아 있는 위임은 `attached`. (C7·C7b)

**추가로 닫은 것 (계약에 없던 것)**
- [x] D10. 붙일 문서를 못 골라도 손은 바인딩에 남는다 —
      `s9 claim --agent-transcript P` (id 생략). 클레임은 안 건드린다.

전체: `python3 tests/` → Ran 2214 tests in 468s — OK (skipped=1).

### 2026-08-29T22:03:03+09:00 backend-developer (by nicehugepark)

### 라운드2 — 실물에서 하나 더 잡았다

배포 직후 살아 있는 저장소에서 확인하다 잡았다. `s9 stalled` 이 "누가 붙었는지
모르는 것 3건" 을 냈는데 `s9 workers` 에는 손이 하나뿐이었다.

원인: `_attributed_transcripts()` 가 **`result == "running"` 인 기여만** 셌다.
그런데 21:31 에 내(backend-developer) 기여가 `agent_health` 에 stalled 로
닫혔다 — 전체 시험 스위트(7분)를 도느라 transcript 가 180초 넘게 조용했기
때문이다. 손은 그대로 도는데 행선지 기록만 닫혔고, 그 순간 내 손이 '미상'이
되어 **다른 요청 셋이 전부 '미상'** 이 됐다.

고침: 결과를 안 본다. 여기서 묻는 것은 "이 손이 살아 있나"가 아니라 "이 손이
**어디** 있나" 뿐이고, 살아있음은 손의 신선도(`live_agents`)가 따로 답한다.
`bin/s9:_attributed_transcripts()` 한 줄.

**일하느라 바쁜 쪽이 손해 보는 판정**은 이 저장소가 `rework_claimed` 에서 이미
한 번 뒤집은 방향이다(2026-08-26: 긴 시험을 도는 세션일수록 클레임을 잃었다).
같은 함정에 다른 자리에서 다시 빠질 뻔했다.

확인: 고친 뒤 `unassigned_hands()` → `[]`, 024·025·029 는 다시 `stalled`,
027·036 은 `moving`. 전체 스위트 재실행 → **Ran 2214 tests in 450s — OK
(skipped=2)**.

### 2026-08-29T22:20:09+09:00 commit (by nicehugepark)

커밋 2faa0d2 — 화면을 조각으로 가르고, 멈춤이 일하는 손을 본다 (REQ-027·036·024)

## History
- 2026-08-29T20:35:29+09:00 created by nicehugepark (status: open)
- 2026-08-29T21:03:30+09:00 status: open -> in-progress (by nicehugepark) — drag 이동 [via dashboard]
- 2026-08-29T22:20:32+09:00 status: in-progress -> done (by nicehugepark) — 충족 근거: 커밋 2faa0d2, 전체 2214건 OK. 실사고 재현 계약(delegation_target D7)이 green 이고, 살아 있는 서버 실측으로 판정이 갈린다 — 024 stalled 44분 · 025 stalled 115분 · 027·029·036 moving. 보드 캡처로 멈춘 둘에만 멈춤 줄과 깨우기가 서는 것을 눈으로 확인했다. 닫지 못한 경로 셋(하네스 계약 의존)은 문서에 적었다.
