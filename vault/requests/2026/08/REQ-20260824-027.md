---
id: REQ-20260824-027
type: request
title: whoami 신원 파생 구현
summary: "025 승인 반영: me 셀렉터·s9me 폐기 → 서버가 OS계정·hostname으로 신원 파생(GET /api/whoami), 대시보드는 표시 라벨(user @ machine [role]), 쓰기 actor 서버 파생(클라이언트 user 무시), 로밍은 s9 user attach 1회. 가이드 해당 절·캡처 동반 갱신"
goal: 대시보드 신원이 선택 없이 서버에서 파생되고(라벨 표시), 쓰기 주체도 서버가 결정한다
status: done
size: M
user: sjpark1
machine: DESKTOP-TEHV1KR
session: 104b4fe3
agents: ["backend-developer", "subagent"]
project: section9
parent: REQ-20260824-025
tags: ["access", "session", "test"]
created: 2026-08-24T16:25:25+09:00
status_since: 2026-08-24T16:47:42+09:00
updated: 2026-08-24T16:47:42+09:00
---

## Original

whoami 신원 파생 구현

## Notes

### 2026-08-24T16:30:28+09:00 tdd (agent: backend-developer, by sjpark1)

### TDD 시나리오 (whoami 신원 파생 전환)
- [ ] W1. whoami 파생: 서버 S9_USER=alice(등록) → GET /api/whoami = {user:alice, registered:true, role}; S9_USER=stranger(미등록) → {user:stranger, registered:false, role:''}
- [ ] W1b. os_accounts 매칭: 프로필 이름 불일치라도 프로필 os_accounts에 OS계정이 있으면 그 프로필로 매칭 (직접 일치 우선)
- [ ] W2. attach 로밍: S9_USER=osacct9 미등록 상태에서 's9 user attach alice' → alice 프로필 os_accounts/machines에 추가, 이후 whoami가 alice/registered:true
- [ ] W2b. user add 시 현재 OS계정이 프로필 os_accounts에 자동 기록
- [ ] W3. 쓰기 actor 스푸핑 차단: POST /api/status 등에 user:'other' 를 보내도 무시되고 서버 whoami로 기록/인가 (member API: 클라이언트 user=owner 지정해도 whoami=contributor면 거부)
- [ ] W4. admin ?as= 미리보기/대리: admin 서버에서 ?as=bob GET → bob 시점 격리 적용, POST as:bob → actor=bob. 비admin의 as는 GET 무시(자기 시점 유지)·POST 400 거부. 미등록 as 거부
- [ ] W5. 격리·개인화 whoami 기준: 열람 격리(REQ-017 의미 보존 — 비멤버 비가시/멤버 가시/admin 전부/무소속 작성자만/audit 스코프)를 서버 신원(S9_USER)+admin as로 재검증
- [ ] W6. 회귀 8종: tests/ 스위트 전부 통과 (워처·auto_resume·live·TDD 카운터·stdlib 가드·CLI resolve_user 우선순위 불변)
- [ ] W7. UI(CDP): me 셀렉터 부재, whoami 라벨 'user @ machine [role]' 표기, 미등록 OS계정이면 경고 배너 + 's9 user add' 안내, '내 것만'/디스플레이 저장이 whoami 계정 기준 동작

### 2026-08-24T16:45:53+09:00 tdd (agent: backend-developer, by sjpark1)

### TDD 결과 (전 시나리오 초록)
- [x] W1. whoami 파생 — tests/test_whoami.py test_w1 (S9_USER=alice→registered:true/member, boss→admin, stranger77→registered:false/role '')
- [x] W1b. os_accounts 매칭 (직접 일치 우선) — test_w2/test_w2b (carolacct→carol 기록 확인)
- [x] W2. attach 로밍 — test_w2 (osacct9 attach 후 whoami=alice), 미등록 대상 attach 거부
- [x] W2b. user add 시 OS 계정 자동 기록 — test_w2b
- [x] W3. 쓰기 actor 스푸핑 차단 — test_w3 (user:boss 보내도 History '(by alice)'), test_project_member_api M6b (user:alice 위장해도 contributor 거부)
- [x] W4. admin as 미리보기/대리 — test_w4 (admin as=bob GET 격리 적용·POST actor 기록, 비admin as GET 무시·POST 400, 미등록 as 400, 구 ?me= 무시)
- [x] W5. 격리·개인화 whoami 기준 — test_w5 + test_access_isolation V2~V7 신모델 개정 통과 (의미 보존: 비멤버 비가시/멤버 가시/admin 전부/무소속 작성자만/audit 스코프, 미등록 whoami 비강제 유지)
- [x] W6. 회귀 — tests/ 9종 전부 OK (access_isolation·audit_prompt·auto_resume_gate·live_catalog·project_member_api·stdlib_only·subagent_attr·tdd_progress·whoami), CLI resolve_user 우선순위 불변(user current 3케이스 확인), done 가드(goal) 보존
- [x] W7. UI(CDP 실브라우저, Chrome 9225) — verify_whoami_ui.py 12/12 PASS: #me 셀렉터 부재, 라벨 'boss @ DESKTOP-TEHV1KR [admin]', admin as 셀렉터로 bob 시점(px 숨김·라벨 '→ @bob 시점'), skin 변경이 whoami 계정 config에 저장, 미등록 시 warn 라벨+'s9 user add stranger77' 안내+내것만 비활성

### 2026-08-24T16:46:03+09:00 tdd (agent: backend-developer, by sjpark1)

### TDD 결과 — 전 시나리오 통과
- [x] W1/W1b. whoami 파생 (tests/test_whoami.py test_w1) — 등록 매칭·미등록 registered:false·os_accounts 매칭
- [x] W2/W2b. attach 로밍·add 자동 기록 (test_w2, test_w2b)
- [x] W3. 쓰기 actor 스푸핑 차단 (test_w3 + test_project_member_api M6b) — user 파라미터 무시, History '(by whoami)' 기록
- [x] W4. admin ?as=/as 대리 (test_w4) — 비admin GET 무시·POST 400, 미등록 as 400, ?me= 무시
- [x] W5. 격리·개인화 whoami 기준 (test_w5 + test_access_isolation V1~V7 개정판, 검증 의미 보존)
- [x] W6. 회귀 9종 전부 OK (access_isolation·audit_prompt·auto_resume_gate·live_catalog·project_member_api·stdlib_only·subagent_attr·tdd_progress·whoami) + CLI resolve_user 우선순위 3케이스 수동 확인
- [x] W7. UI CDP ALL PASS (scratchpad/verify_whoami_ui.py) — 셀렉터 부재·라벨·미등록 경고·as 미리보기·skin 저장이 whoami 계정 기록. 스크린샷 whoami-label/as-bob/unregistered.png

### 2026-08-24T16:46:16+09:00 response (agent: backend-developer, by sjpark1)

### 구현 완료 보고 (whoami 신원 파생 전환)

### 변경 요지
- bin/s9 서버: user_profile()/os_identity()/whoami_info() 신설, GET /api/whoami={user,display,role,machine,registered}. 매칭 = 프로필 이름 직접 일치 → os_accounts 포함(로밍), 미매칭이면 OS계정명 그대로 registered:false.
- 쓰기 API(/api/status·/api/user/*·/api/project/member*): actor를 서버 whoami로 파생 — 클라이언트 user 파라미터 무시(주석으로 결정 명시). admin만 body 'as'로 대리 조작. admin 판정도 whoami 기준.
- 열람 격리 viewer_of: ?me= 무시 → whoami. admin만 ?as=<등록 사용자>로 시점 전환(비admin은 조용히 무시 — 상승 경로 없음).
- CLI: s9 user add가 현재 OS 계정을 프로필 os_accounts에 자동 기록, 신규 s9 user attach <name>(현 OS계정+머신을 os_accounts/machines에 1회 연결). resolve_user 우선순위 불변.
- web/index.html: me 셀렉터/localStorage s9me/onMeChanged 제거 → boot 시 /api/whoami 1회 + 표시 라벨(user @ machine [role], 미등록이면 warn 라벨+등록 안내). meQ()는 admin 미리보기 as만 전달. '내 것만'·mine·개인화(ui_*/pref) 전부 whoami 기준. Settings 사용자 관리에 admin 전용 '시점 미리보기/대리' 셀렉터 1개(DOC-20260823-001 F3 최소 구현, 비저장).
- web/guide.html: 설치 5단계·Settings 절·열람 격리 문단·FAQ를 라벨 모델로 개정(구조 유지), settings-display/settings-account 2컷만 데모 vault로 재촬영·교체.
- 테스트: test_whoami.py 신설(6), test_access_isolation.py(V2b 추가·as 모델 개정, 7)·test_project_member_api.py(M6b 스푸핑 무시 추가, 9) — 검증 의미 보존. 기동 직후 loopback RST 플레이크 재시도 가드.

### 검증
- tests/ 9종 전부 OK. CDP UI 12/12 PASS (스크린샷: scratchpad/whoami-label.png·whoami-as-bob.png·whoami-unregistered.png).
- 라이브 9909 재시작 완료, GET /api/whoami = {user:sjpark1, role:admin, machine:DESKTOP-TEHV1KR, registered:true}. index rebuild 142 docs. 샌드박스(s9id·9921/9922/9925·Chrome 9225) 정리 완료.

### 한계 (설계 명시)
- 한 OS 계정을 여러 사람이 공유하면 구분 불가 — admin 대리 모드가 유일한 예외 경로 (F3).
- 대시보드를 외부(0.0.0.0)에 열면 whoami 모델 무력 — 실인증(PIN/토큰) 필요 경계 유지 (F4).
- admin이 타인을 대신 등록하면 등록자 OS계정이 그 프로필 os_accounts에 남음 — 직접 일치 우선 매칭이라 오인 파생은 없으나 기록은 남는다.
- 기존 계정 소급은 attach 1회로 해결(자동 역추론 없음 — 단순성 결정).

### 2026-08-24T16:46:27+09:00 subagent (agent: subagent, by sjpark1)

Recording TDD results to REQ-20260824-027

### 2026-08-24T16:46:27+09:00 response (agent: backend-developer, by sjpark1)

### 구현 보고 — whoami 신원 파생 전환 (선택 → 파생)

### 서버 (bin/s9)
- whoami_info(): resolve_user() 폴백을 users/ 프로필과 매칭 (이름 직접 일치 → os_accounts 포함). 미매칭 = registered:false. GET /api/whoami = {user, display, role, machine, registered}.
- s9 user add: 현재 OS계정(S9_USER 폴백)을 os_accounts에 자동 기록. 신규 s9 user attach <name>: 현 OS계정+머신을 프로필 os_accounts/machines에 추가 (로밍/소급 1회 연결, 자동 역추론 없음).
- 쓰기 API 4종(status/user·config/member±): actor = 서버 whoami, 클라이언트 user 파라미터 무시(주석으로 결정 명시). admin만 body 'as'로 대리 조작.
- 열람 격리 viewer_of: ?me= 무시, whoami 기준. admin만 ?as=<등록자> 시점 전환 (비admin은 GET 조용히 무시·POST 400).
- 리드가 병행 반영한 done goal 가드(REQ-030) 보존 확인.

### 대시보드 (web/index.html)
- me 셀렉터·localStorage s9me·onMeChanged 제거 → boot 시 /api/whoami 1회, 라벨 'user @ machine [admin]'. 미등록이면 경고 라벨 + s9 user add 안내(내 것만 토글 비활성, 계정 섹션에도 안내).
- 신원 fetch 파라미터 전면 제거(meQ는 admin 미리보기 as 전용). mine·개인화(ui_*/pref)·admin 판정 전부 whoami 기준. 대리 모드 = Settings 사용자 관리 내 '시점 미리보기' 셀렉터 1개(asUser, 비저장) — postJSON/postStatus가 as 자동 첨부.

### 가이드 (web/guide.html)
- 설치 5단계(me 선택→자동 인식+attach), Settings 절, 'me 격리'→'열람 격리'(+시점 미리보기), FAQ 개정 + 미등록 경고 FAQ 신설. 구조 불변. 캡처 3컷(settings-display/account, project 패널)만 s9guide 데모 vault로 재촬영·재임베드 — me 박스가 크게 보이던 컷.

### 테스트
- 신규 tests/test_whoami.py (서버 4대: S9_USER=boss/alice/stranger77/osacct9). test_access_isolation·test_project_member_api를 서버 신원+as 모델로 개정(권한 거부·가시성 의미 보존, M6b 스푸핑 무시 케이스 추가). 9종 전부 OK.

### 배포
- 라이브 9909 재기동, /api/whoami = sjpark1 [admin] registered:true 실확인, ?me=hacker 무시 확인, index rebuild(143 docs).

### 한계
- 한 OS 계정을 여러 사람이 공유하면 구분 불가(설계 명시 한계 — admin 대리 모드로 예외 처리). 0.0.0.0 외부 노출 시 whoami 무력 — 실인증(PIN/토큰) 필요. attach는 신뢰 기반(인증 아님). admin이 타인을 대신 등록하면 등록자 OS계정이 그 프로필 os_accounts에 남지만 직접 일치 우선이라 오인 파생 없음.

### 2026-08-24T16:46:27+09:00 subagent (agent: subagent, by sjpark1)

Recording completion note to REQ-20260824-027

### 2026-08-24T16:46:38+09:00 subagent (agent: backend-developer, by sjpark1)

구현 완료. 상태 전이는 하지 않았다(리드 검수 대기, REQ는 in-progress 유지).

### 최종 보고 — REQ-20260824-027 whoami 신원 파생 전환

### 변경 요지 (선택 → 파생)
- **`/home/sjpark1/section9/bin/s9`**
  - `whoami_info()`/`user_profile()`/`os_identity()` 신설, `GET /api/whoami` → `{user, display, role, machine, registered}`. 매칭: 프로필 이름 직접 일치 → 프로필 `os_accounts` 포함(로밍). 미매칭이면 OS계정명 그대로 `registered:false`.
  - 쓰기 API(status·user/*·project/member*): **actor = 서버 whoami, 클라이언트 `user` 파라미터 무시**(결정을 주석으로 명시). admin만 body `as`로 대리 조작. admin 판정도 whoami 기준.
  - 열람 격리 `viewer_of`: `?me=` 무시, admin 전용 `?as=`만 인정(비admin은 조용히 무시 — 권한 상승 경로 없음).
  - CLI: `user add`가 현 OS계정을 `os_accounts`에 자동 기록, 신규 `s9 user attach <name>`(OS계정+머신 1회 연결). `resolve_user` 우선순위 불변(3케이스 실확인). 리드가 추가한 done 가드("goal 미기재") 보존 확인.
- **`/home/sjpark1/section9/web/index.html`**: me 셀렉터·`s9me`·`onMeChanged` 제거 → boot 시 whoami 1회 + 라벨 `sjpark1 @ DESKTOP-TEHV1KR [admin]`(미등록이면 warn 라벨 + `s9 user add` 안내). '내 것만'·mine·개인화(ui_*/pref) 전부 whoami 기준. Settings 사용자 관리에 admin 전용 시점 미리보기/대리 셀렉터 1개(비저장).
- **`/home/sjpark1/section9/web/guide.html`**: 설치 5단계·Settings 절·열람 격리 문단·FAQ를 라벨 모델로 개정(구조 대개편 없음). me 박스가 크게 보이던 settings-display/settings-account **2컷만** 데모 vault(hana)로 재촬영·교체.

### 시나리오 결과 (REQ 문서에 tdd 노트 2건 기록)
- W1~W5: `tests/test_whoami.py` 신설 6/6 OK — 파생 매칭, attach 로밍, **스푸핑 시도 시 History `(by alice)` 기록**, admin `?as=`(비admin POST 400·GET 무시, 구 `?me=` 무시), 격리·개인화 whoami 기준.
- W6: **tests/ 9종 전부 OK.** `test_access_isolation.py`(V2b 추가)·`test_project_member_api.py`(M6b 스푸핑 무시 추가)는 as 모델로 개정하되 권한 거부·가시성 의미 보존.
- W7: CDP 실브라우저 12/12 PASS — 셀렉터 부재, 라벨 표기, bob 시점 미리보기(px 숨김 + `→ @bob 시점` 표기), skin 저장이 whoami 계정 config 기록, 미등록 warn 라벨.

### 스크린샷
`/tmp/claude-1000/-home-sjpark1-section9/104b4fe3-7c7a-4a1e-9efe-72784dd8d94d/scratchpad/` 의 `whoami-label.png` · `whoami-as-bob.png` · `whoami-unregistered.png` (검증 스크립트 `verify_whoami_ui.py`)

### 마무리 상태
- 라이브 9909 재시작 완료 — `GET /api/whoami` = `{user:"sjpark1", role:"admin", machine:"DESKTOP-TEHV1KR", registered:true}`, `/`·`/guide.html` 200. `index rebuild` 142 docs.
- 샌드박스 정리: s9id vault·검증 서버 9921/9922/9925·Chrome 9225 종료/삭제 (s9guide 데모 vault는 가이드 재촬영용 공용이라 보존).

### 한계
- 한 OS 계정을 여러 사람이 공유하면 구분 불가(admin 대리 모드가 유일한 우회), 외부(0.0.0.0) 노출 시 whoami 모델 무력 — 실인증 필요 경계 유지.
- admin이 타인을 대신 등록하면 등록자 OS계정이 그 프로필 os_accounts에 기록됨 — 직접 일치 우선 매칭이라 오인 파생은 없으나 기록은 남는다. 기존 계정 소급은 attach 1회(자동 역추론 없음).

### 2026-08-24T16:46:55+09:00 subagent (agent: backend-developer, by sjpark1)

### 완료 보고 — REQ-20260824-027 whoami 신원 파생 전환

**결론**: 선택(me 셀렉터) → 파생(서버 whoami) 전환 완료. TDD 시나리오 W1~W7 전부 통과, 회귀 9종 OK, 라이브 9909 재기동·실확인 완료. 상태 전이는 하지 않음(리드 검수 대기, in-progress 유지).

### 변경 요지
- **서버** (`/home/sjpark1/section9/bin/s9`)
  - `whoami_info()`: resolve_user() 폴백(바인딩→$S9_USER→OS계정)을 프로필과 매칭 — 이름 직접 일치 우선, 다음 `os_accounts` 포함. 미매칭이면 `registered:false`. `GET /api/whoami` 신설.
  - `s9 user add`가 현재 OS계정을 `os_accounts`에 자동 기록, 신규 `s9 user attach <name>`(`do_user_attach`)이 현 OS계정+머신을 프로필에 1회 연결.
  - 쓰기 API 4종의 actor = 서버 whoami — **클라이언트 user 파라미터 무시**(주석으로 결정 명시). admin만 `as` 대리. 열람 격리 `viewer_of`: `?me=` 무시, admin만 `?as=`(비admin GET은 조용히 무시·POST 400).
  - 리드가 병행 추가한 done goal 가드(`grep "goal 미기재"` 확인) 보존, 테스트 request 생성에 `--goal` 반영.
- **대시보드** (`/home/sjpark1/section9/web/index.html`): 셀렉터·s9me·onMeChanged 제거 → boot 1회 whoami 로드, 라벨 `sjpark1 @ DESKTOP-TEHV1KR [admin]`, 미등록 시 경고 라벨+`s9 user add` 안내. mine·개인화·admin 판정 전부 whoami 기준. 대리 모드 = Settings 사용자 관리의 '시점 미리보기' 셀렉터 1개(비저장), postJSON/postStatus가 `as` 자동 첨부.
- **가이드** (`/home/sjpark1/section9/web/guide.html`): 설치 5단계·Settings 절·"me 격리"→"열람 격리"·FAQ 개정(+미등록 경고 FAQ), 구조 불변. me 박스가 크게 보이던 3컷(settings-display/account, project 패널)만 s9guide 데모 vault로 재촬영·재임베드.

### 시나리오 결과
- W1~W5: `/home/sjpark1/section9/tests/test_whoami.py` 6케이스 OK (서버 4대를 S9_USER로 통제 — 스푸핑 시 History `(by whoami)` 기록 검증 포함).
- 격리(REQ-017)·멤버 API(REQ-20260823-073) 테스트를 서버 신원+`as` 모델로 개정 — 권한 거부·가시성 의미 보존, M6b(user 파라미터 무시) 추가.
- W6: 9개 스위트 전부 OK + CLI resolve_user 우선순위 3케이스 수동 확인(불변).
- W7: CDP ALL PASS — 셀렉터 부재, 라벨 표기, `@bob 시점` 미리보기 격리 동작, 미등록 경고, skin 저장이 whoami 계정 config에 기록.
- 배포: 9909 재시작 → `/api/whoami` = sjpark1 [admin] registered:true, `?me=hacker` 무시 확인, `index rebuild` 143 docs.

### 스크린샷
`/tmp/claude-1000/-home-sjpark1-section9/104b4fe3-7c7a-4a1e-9efe-72784dd8d94d/scratchpad/` 의 `whoami-label.png`, `whoami-as-bob.png`, `whoami-unregistered.png`(UI 검증), `refig-6/7/8.png`(가이드 재촬영분).

### 한계
- 한 OS 계정을 여러 사람이 공유하면 구분 불가(설계 명시 한계 — admin 대리 모드가 예외 경로).
- 0.0.0.0 외부 노출 시 whoami 모델 무력 — 실인증(PIN/토큰) 필요 경계는 기존 결론 유지. attach도 신뢰 기반이지 인증이 아님.
- admin이 타인을 대신 등록하면 등록자 OS계정이 그 프로필 os_accounts에 남으나, 직접 일치 우선 매칭이라 오인 파생은 없음(response note에 명시).

## History
- 2026-08-24T16:25:25+09:00 created by sjpark1 (status: open)
- 2026-08-24T16:25:25+09:00 status: open -> in-progress (by sjpark1) — backend-developer 위임
- 2026-08-24T16:47:42+09:00 status: in-progress -> done (by sjpark1) — goal 충족: /api/whoami가 서버에서 신원 파생(sjpark1@머신 [admin]) — 셀렉터 없이 라벨 표시(스크린샷), 쓰기 actor도 서버 결정(user 파라미터 스푸핑 무시 실측·M6b), attach 로밍·admin as 대리·가이드 개정 포함. W1~W7+회귀 9종 통과, 라이브 반영 확인
