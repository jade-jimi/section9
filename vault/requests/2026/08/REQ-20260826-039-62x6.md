---
id: REQ-20260826-039-62x6
type: request
title: 그래프 빈 화면 무설명
summary: 필터·범례 조합으로 노드가 0이 되면 캔버스가 이유 없이 비어 보인다
goal: 그래프가 비면 왜 비었는지와 어떻게 되돌리는지를 화면이 말한다
status: done
user: sjpark1
machine: DESKTOP-TEHV1KR
session: 8e60e4af
agents: ["sub:subagent:ae8930b0"]
contributions: [{"actor": "sub:subagent:ae8930b0", "item": "Transitioning REQ-20260826-039-62x6 to review", "started": "2026-08-27T00:34:06+09:00", "ended": "2026-08-27T00:34:06+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/8e60e4af-6e99-4b92-857b-fe3e651a913b/subagents/agent-ae8930b05a59075e3.jsonl"}]
project: section9
children: ["REQ-20260827-007-62x6"]
relates: ["REQ-20260827-001-62x6", "REQ-20260827-003-62x6"]
tags: ["auto-audit", "assets", "docs", "terminal", "attached", "png", "image"]
created: 2026-08-26T23:28:03+09:00
status_since: 2026-08-27T00:37:08+09:00
updated: 2026-08-27T00:37:08+09:00
priority: 50
---

## Original

[선행 대화 — 직전 미문서화 메시지]
- (23:27) 038 요청의 연관성 요청으로 037이 등록되어있는데 무슨 연관성이 있는거지?? 전혀 관계없는것같은데

그래프 탭에 아무것도 보이지 않는다. 새로고침을 하면 보이긴 하던데...
[Image: assets/REQ-20260826-039-62x6/20260826T232750-image.png]

## Notes

### 2026-08-26T23:47:42+09:00 response (by sjpark1)

캡처를 읽으면 렌더 실패가 아니다 — **화면이 정직하게 0개를 그린 것**이고, 다만 이유를 말하지 않았다.

캡처 안에 근거가 다 있다:
- 헤더 타입 필터가 `question` → 대상이 `3 / 363 DOCUMENTS`
- 그래프 범례에서 `QUESTION` 이 **꺼져 있다**(KNOWLEDGE·SESSION 도 함께 흐림, REQUEST 만 켜짐)

필터는 질문 3건만 남겼는데 그래프는 질문 노드를 안 그린다. 교집합이 0이니 캔버스가 빈다. "새로고침하면 보인다"도 같은 얘기다 — 새로고침 때 필터가 풀리면서 조합이 풀린다.

**고칠 것은 그리기가 아니라 빈 화면의 침묵이다.** 지금은 사용자가 두 곳(헤더 필터·범례)을 스스로 대조해야 원인을 안다. 비었을 때 화면이 그걸 말해야 한다 — 무엇 때문에 0인지, 어디를 누르면 되돌아오는지. 원인이 여러 갈래이므로(필터가 0건 / 타입 토글로 0건 / 정말 문서가 없음) 구분해 각기 다른 말을 해야 한다.

designer 에게 중계했다 — "지금 하던 REQ-20260826-029-62x6 를 먼저 마치고 이어서 반영하라"는 규율을 함께 전달. 같은 파일(web/index.html)을 리드가 만지면 오늘 세 번 난 충돌이 네 번째가 된다.

### 2026-08-26T23:58:48+09:00 designer (by sjpark1)

**빈 캔버스가 이유를 말한다.** web/index.html 한 파일만 고쳤다(bin/s9 무수정).

### 진단 — 렌더 실패가 아니었다

첨부 캡처에 근거가 다 있었다. 헤더 종류 필터가 `question` 하나로 좁혀 3 / 363 건만
남겼는데, 그래프 범례에서는 그 `question` 이 꺼져 있었다(취소선). 교집합이 0이니
캔버스는 정직하게 아무것도 안 그렸다 — **다만 이유를 말하지 않았다.** 원인을 알려면
사용자가 위 필터와 범례 두 곳을 스스로 대조해야 했다.

"새로고침하면 보이던데"도 같은 얘기다. 헤더 필터는 새로고침에 풀리고 범례 설정은
브라우저에 남는다. 그래서 조합이 저절로 풀린다 — 사용자에게는 "가끔 되고 가끔
안 되는 화면"으로 읽힌다.

### 고친 것

`graphEmptyState(rows)` 가 **원인을 셋으로 가른다**. 셋을 한 문장으로 뭉뚱그리면
사용자는 여전히 어디를 눌러야 할지 모른다.

1. 기록 자체가 없음 → "요청을 하나 만들면 여기에 점으로 나타난다" (버튼 없음 —
   여기서 누를 것이 없다)
2. 검색·필터가 0건으로 좁힘 → 전체 건수를 말하고 **[검색·필터 지우기]**
3. 남은 문서의 종류를 범례에서 꺼 둠 → 몇 건이 무슨 종류인지 이름으로 말하고
   **[질문 다시 켜기]** (여러 종류면 "세션·질문 다시 켜기")
4. (드묾) 남은 것이 그래프가 그리지 않는 종류 → 필터 지우기로 안내

3번에는 새로고침 혼동을 푸는 한 줄을 붙였다: "범례 설정은 이 브라우저에 저장돼
새로고침해도 남는다 — 화면이 또 비면 여기를 보라."

**말만 하고 손이 없으면 절반이다.** `data-gfix` 버튼이 실제로 되돌린다. 필터
지우기는 검색어·본문검색·user·project·tag·type 에 더해 **'내 것만'까지 푼다** —
이것도 결과를 0건으로 만드는 조건이라, 안 풀면 버튼이 조용히 죽는다(테스트가 강제).
종류 켜기는 `data-gtypes` 로 켤 종류를 마크업이 들고 가므로 핸들러가 다시 계산하지
않는다.

안내는 **노드가 0일 때만** 뜬다(`nodes.length ? null : …`) — 그려진 그래프를 덮지 않는다.

### 시각 언어

- 색면 0, 판(배경) 0. 빈 캔버스가 이미 여백이라 여기에 색면을 얹으면 "또 하나의 창"이 된다.
- 색은 **꺼진 종류 이름 글자에만** 그 종류 색(--t-*)으로 — 위 범례 칩과 눈으로 이어진다.
- 버튼은 이 제품의 기존 인터랙션 언어(잉크 테두리 → hover/focus 시 반전). `.gmode.on` 과 같은 어휘다.
- **한국어 문장에 자간을 벌리지 않는다.** 초안에서 모노·대문자·letter-spacing 킥커를
  넣었다가 캡처에서 글자가 흩어져 보여 지웠다 — 그 어휘는 id·시각·카운트 같은
  라틴/숫자 메타데이터의 것이다. 지우고 나니 "한 문장 + 버튼 하나"로 더 단순해졌다.
- 캔버스 드래그를 막지 않는다(`pointer-events:none`, 버튼만 `auto`).
- `role="status"` — 동적으로 나타나는 안내라 상태 변화로 알린다.
- 색 하드코딩 0 (테스트가 `#rrggbb` 를 금지) → tone 6종 전부에서 성립.

### 검증

- 계약 테스트 `tests/test_graph_empty.py` 12개 (TDD — 12개 빨강 확인 후 구현).
- 전체 `python3 tests/` → 660 tests OK.
- 캡처:
  - `state/shots/039-tones.png` — **tone 6종(system/paper/carbon/phosphor/mist/graphite)
    × 원인 4갈래**. 실제 CSS·실제 문구 생성 함수를 그대로 써서 그렸다.
  - `state/shots/039-empty.png` — 실화면 #graph. 노드가 있을 때 안내가 뜨지 않는 것 확인.
    (headless 캡처가 처음엔 백지로 나왔는데, 물리 애니메이션 때문에 가상시간이
    모자란 것이었다 — DOM 덤프로 정상 렌더 확인 후 6초로 늘려 다시 찍었다.)

### 남긴 것

- 037↔038 연관성 의문(23:27 선행 대화)은 이 요청의 범위가 아니다 — 관계 데이터 문제라 별건이다.

### 2026-08-27T00:33:17+09:00 response (by sjpark1)

**클릭은 처음부터 멀쩡히 작동하고 있었다. 안 되던 것은 "어느 것을 누르라"는 안내였다.** web/index.html 만 고쳤다(bin/s9 무수정).

### 재현 — 추정으로 고치지 않고 실제로 눌러 봤다

헤드리스 스크린샷으로는 클릭을 만들 수 없어서, 같은 오리진에서 대시보드를 iframe 으로 띄우고 **실제 좌표의 hit-test(`elementFromPoint`)와 실제 `MouseEvent`** 로 눌렀다(`web/verify-gempty-click.html` — 저장소에 이미 있던 `verify-*.html` 하니스 선례를 따랐다).

반려 캡처와 같은 상태(헤더 종류=question · 범례에서 question/knowledge/session off)를 만들고 눌러 본 결과:

| 검사 | 결과 |
|---|---|
| 범례 위에 `.gempty` 가 덮여 있나 | **아니다.** 좌표의 최상위 요소 = `<span .gtype.off data-gtype=knowledge>` |
| 클릭이 핸들러까지 가나 | **간다.** `s9gtypes` 가 `["request"]` → `["request","knowledge"]` 로 바뀜 |
| JS 오류 | **0건** |
| 되돌리기 버튼은 되나 | **된다** |
| 범례 QUESTION 은 되나 | **된다** — 안내가 걷히고 점 3개가 그려짐(캔버스 잉크 픽셀 0 → 1732) |
| 범례 KNOWLEDGE 를 누르면 | **글자도 그림도 그대로** — 안내·버튼·캔버스 전부 동일 |

**리드 추정 셋은 모두 틀렸다** — ① 덮개 아님(hit-test 통과) ② render() 예외 아님(오류 0건) ③ 버튼도 범례도 죽지 않았음.

### 진짜 원인

범례에서 **취소선이 그어진 항목이 셋(KNOWLEDGE·QUESTION·SESSION)** 인데, 안내는 "위쪽 범례에서 **그 종류**를 꺼 두어"라고만 말했다. 사용자가 해야 할 일이 셋이나 됐다:

1. 한글 "질문"을 영문 대문자 `QUESTION` 으로 스스로 옮긴다
2. 흐려진 항목 셋 중에서 그걸 찾는다
3. 누른다

그리고 2번에서 어긋나 KNOWLEDGE·SESSION 을 누르면 — 조건에 맞는 3건이 전부 질문이므로 그릴 것이 여전히 0건이고, **안내는 같은 문장을 말없이 다시 그린다.** 11px 모노 라벨의 취소선 하나가 유일한 변화다. 사용자에게는 죽은 컨트롤이다.

거기에 안내의 주 문장이 **"위쪽"** 이라고 시선을 위로 보냈다. 바로 아래에 `질문 다시 켜기` 버튼이 있는데도 사용자를 범례로 올려보낸 것이 이 경로의 출발점이다.

즉 반려의 말("되돌리는 손이 작동하는지 검증되지 않았다")은 절반만 맞다. 손은 작동했다. **손이 어디 있는지 화면이 짚어 주지 않았다.**

### 고친 것 넷

1. **주 문장에서 위치 지시를 뺐다.** `위쪽 범례에서 그 종류를 꺼 두어 화면이 비었다` → `조건에 맞는 문서 3건이 전부 질문인데, 그 종류를 지금 꺼 두었다.` 시선이 바로 아래 버튼으로 간다. (계약: msg 에 "위쪽/아래/여기를" 금지)
2. **범례에 실제로 찍힌 글자를 그대로 준다.** 부연에 `범례의 QUESTION 을 직접 눌러도 된다` — 한글↔영문 번역을 사용자에게 맡기지 않는다.
3. **눌러야 할 범례 항목을 화면이 지목한다.** `.gtype.want` — 취소선과 흐림은 그대로 두고(꺼져 있다는 사실을 지우면 안 된다) 그 위에 **잉크 점선 아웃라인**만 얹는다. 색면이 아니라 점선인 것은 드래그 허용 컬럼(`.col.dropok`)과 같은 어휘라서다. 안내가 뜨는 순간부터 붙으므로 **오클릭 자체를 예방**한다.
4. **헛클릭에 침묵하지 않는다.** 켰는데도 화면이 그대로면 안내가 인정한다 — `KNOWLEDGE 를 켰지만 화면은 그대로다 — 지금 조건에 지식은 없다.` `role="status"` 안이라 스크린리더에도 "눌린 건 맞다"가 전달된다. 켠 것만 인정한다(끄는 건 의도적 축소다). 조건이 바뀌거나 되돌리기가 성공하면 무효가 된다 — 남으면 거짓말이 되므로.

지목과 버튼은 **같은 출처(`st.fix.types`) 하나**를 읽는다. 마크업과 핸들러가 서로 다른 이름을 보다 조용히 갈라지는 것이 REQ-20260826-025-62x6 의 결함이었다.

### 테스트 구멍도 막았다

기존 12개 계약은 **마크업과 핸들러의 존재만** 봤기 때문에 이 결함을 통과시켰다(025 와 같은 계열). 계약 8개를 더해 "눌렀을 때 화면이 사용자에게 무엇을 돌려주는가"를 박았다 — 위치 지시 금지 · 범례 라벨 축자 표기 · 지목 표시 존재 · 지목과 버튼의 출처 일치 · 지목이 꺼짐 신호를 지우지 않음 · 켠 종류 기록 · 헛클릭 인정 · 인정 줄의 수명. TDD 로 8건 빨강 확인 후 구현했다.

다만 정적 검사의 한계는 그대로다(파서는 픽셀도 클릭도 못 본다). 그래서 **실브라우저 하니스를 지우지 않고 남겼다** — 다음 사람이 같은 회귀를 5분에 재현할 수 있다.

### 검증

- `python3 tests/ graph_empty` → 20개 통과 (12 기존 + 8 신규)
- 전체 스위트 → 실패 0 (구현 전량 반영 상태에서 완주, 종료코드 0). 이후 한 줄
  (`#f-mine` 토글에서 `gLastOn = null`)을 더했고 대상 스위트 20건은 그대로 통과한다.
  같은 시각 다른 에이전트가 `bin/s9`·테스트 서버를 만지고 있어 재실행분은 서버
  의존 테스트에서 멈춰 판정을 못 냈다 — 이 변경과 무관한 경합이다.
- 실브라우저(Windows Chrome headless, iframe + 실제 MouseEvent):
  - `state/shots/039-diag-k.png` — **반려 상태 재현**: KNOWLEDGE 클릭 → "사용자가 보는 변화: 없음"
  - `state/shots/039-fix-k.png` — 같은 클릭 → 인정 줄·지목 점선이 뜬다. 이어서 검색어를 넣어 조건을 바꾸면 인정 줄이 걷힌다
  - `state/shots/039-diag-q.png` — QUESTION 클릭 경로(안내 걷히고 점이 그려짐)
  - `state/shots/039-tone-a.png` (system·paper·mist) · `039-tone-b.png` (carbon·graphite·phosphor) — **tone 6종 전부 실화면 확인**
- 색 하드코딩 0(전부 토큰) · 색면 0 · 좌측 세로 띠 0 · 캔버스 드래그 비간섭 유지

### 남긴 것 (리드 판단 필요)

범위가 `web/index.html` 이었는데 **검증 하니스 2개를 새로 두었다**: `web/verify-gempty-click.html`(클릭 재현 — 회귀 계약) · `web/verify-gempty-tones.html`(tone 6종). 저장소에 이미 `verify-contrast/docscroll/rvpt-hover.html` 세 개의 같은 선례가 있어 그 관행을 따랐다. 불필요하다고 보면 지워도 기능에 영향은 없다.

### 분리 등록한 것 — REQ-20260827-007-62x6 「범례 항목 키보드 접근」

안내가 "범례의 `QUESTION` 을 직접 눌러도 된다"고 그 컨트롤을 명시적으로 가리키게 되면서 드러났다: **범례 항목(`.gtype`)은 `<span>` 이라 Tab 으로 닿지 않는다.** 마우스 없이는 못 누른다.

039 에서 고치지 않은 이유는 둘이다. ① 안내의 **주 행동인 `<button class="gefix">` 는 키보드로 닿으므로 되돌리는 경로 자체는 끊기지 않는다.** ② 범례는 그래프 탭 전반이 쓰는 기존 컴포넌트라 여기를 고치면(tabindex·role·키 핸들러·포커스 링) 이 요청의 범위를 넘고, 같은 문제가 있는 다른 텍스트 컨트롤(`.gmode` 등)까지 함께 훑는 편이 낫다.

### 2026-08-27T00:34:06+09:00 subagent (agent: sub:subagent:ae8930b0, by sjpark1)

Transitioning REQ-20260826-039-62x6 to review

## History
- 2026-08-26T23:28:03+09:00 created by sjpark1 (status: open)
- 2026-08-26T23:31:30+09:00 status: open -> in-progress (by sjpark1) — designer 위임 — 같은 파일(web/index.html)을 잡고 있는 에이전트에 이어서 맡긴다
- 2026-08-26T23:59:11+09:00 status: in-progress -> review (by sjpark1) — 그래프가 비면 왜 비었는지와 어디를 누르면 되돌아오는지를 화면이 말하게 했다. 현상: 그래프가 텅 비어 있었다. 그런데 그림이 안 그려진 게 아니라, 위쪽에서 종류를 '질문'으로 좁혀 3건만 남겨 놓고 그래프 범례에서는 그 '질문'을 꺼 둔 상태였다. 둘이 겹치니 그릴 것이 0건이었다. 화면은 정직하게 아무것도 안 그렸을 뿐, 왜인지는 말하지 않았다. 비유: 서랍을 '빨간 것만' 으로 걸러 놓고, 동시에 빨간 것을 보지 않기로 설정해 둔 것이다. 서랍은 비어 보이지만 물건이 없어진 게 아니다. 새로고침하면 보이던 이유도 같다. 위쪽 검색·필터는 새로고침에 풀리고, 범례에서 끈 설정은 브라우저에 남는다. 그래서 우연히 겹침이 풀린다. 확인 절차 (F5 로 새로고침한 뒤 — 화면 코드가 바뀌었다): 1. Graph 를 연다. 평소처럼 점이 보이면 안내는 뜨지 않아야 맞다. 2. 그래프 위쪽 범례에서 QUESTION 을 눌러 끈다. 그다음 화면 맨 위 오른쪽 종류 칸을 question 으로 바꾼다. 3. 이제 화면 가운데에 한 문장이 뜬다 — 몇 건이 무슨 종류인지, 그걸 어디서 꺼 두었는지. 4. 그 아래 '질문 다시 켜기' 버튼을 누른다. 점이 바로 돌아오면 맞다. 5. 이번엔 위쪽 검색창에 아무 말이나(예: zzzz) 넣어 본다. 다른 문장이 뜨고 '검색·필터 지우기' 버튼이 나온다. 눌러 보면 조건이 전부 풀린다. 6. 아무것도 안 걸어 놓은 평소 상태에서는 이 안내가 절대 안 뜨는지도 같이 본다. 색을 칠하지 않았다. 배경을 깔거나 띠를 두르지 않고, 꺼 둔 종류의 이름 글자에만 그 종류 색을 입혔다 — 위 범례의 같은 색 칩과 눈으로 이어지도록. 여섯 가지 화면 색상(system/paper/carbon/phosphor/mist/graphite)에서 모두 확인했다: state/shots/039-tones.png · 실화면은 state/shots/039-empty.png 곁가지: 23:27 에 물으신 "038 요청에 037 이 왜 연관으로 걸려 있나"는 이 화면 문제와 다른 건이라 여기서 손대지 않았다.
- 2026-08-27T00:06:10+09:00 status: review -> in-progress (by sjpark1) — 채팅 반려: 범례를 눌러도 화면이 바뀌지 않는다
- 2026-08-27T00:33:56+09:00 status: in-progress -> review (by sjpark1) — 범례를 눌러도 아무 일이 없던 이유를 찾아 고쳤습니다. 클릭은 처음부터 멀쩡히 작동하고 있었습니다 — 문제는 "어느 것을 누르라"고 화면이 알려주지 않은 것입니다. 현상: 안내가 "위쪽 범례에서 그 종류를 꺼 두어 화면이 비었다"고만 말했는데, 그때 범례에 흐리게 그어져 있던 항목은 셋이었습니다(KNOWLEDGE·QUESTION·SESSION). 그중 QUESTION 이 아닌 것을 누르면 그릴 것이 여전히 없으니 화면이 글자 하나 안 바뀝니다. 누른 사람 눈에는 고장 난 버튼입니다. 비유: 서랍이 비어 보여서 "빨간 물건 보기를 꺼 두셨습니다"라고 쪽지가 붙어 있는데, 스위치 세 개가 나란히 다 내려가 있고 어느 게 빨강인지는 안 적혀 있는 상황입니다. 엉뚱한 스위치를 올리면 서랍은 그대로입니다. 브라우저에서 실제로 눌러 재현했습니다: 클릭은 범례에 정확히 닿았고, 자바스크립트 오류도 없었고, 설정도 바뀌었습니다. 안 바뀐 것은 화면뿐이었습니다. 고친 것 넷: 1. 눌러야 할 범례 항목에 점선 테두리를 둘러 짚어 줍니다. 안내가 뜨는 순간부터 표시되므로 애초에 엉뚱한 것을 누를 일이 줄어듭니다. 2. 안내 문구가 범례에 실제로 찍힌 글자(QUESTION)를 그대로 알려 줍니다. "질문"을 영문으로 옮겨 찾을 필요가 없습니다. 3. 안내의 큰 문장에서 "위쪽"이라는 말을 뺐습니다. 그 말이 시선을 위로 보내는 바람에 바로 아래 있는 버튼을 지나쳤던 것입니다. 4. 눌렀는데 화면이 안 바뀌면 그 사실을 화면이 말합니다 — "KNOWLEDGE 를 켰지만 화면은 그대로다 — 지금 조건에 지식은 없다." 확인 절차 (F5 로 새로고침한 뒤 — 화면 코드가 바뀌었습니다): 1. Graph 를 엽니다. 평소처럼 점이 보이면 안내는 안 뜨는 게 맞습니다. 2. 그래프 위쪽 범례에서 QUESTION 을 눌러 끕니다. 그다음 화면 맨 위 오른쪽 종류 칸을 question 으로 바꿉니다. 3. 화면 가운데 안내가 뜨는 동시에, 위 범례의 QUESTION 에 점선 네모가 둘러집니다. 이게 "여기를 누르세요" 표시입니다. 4. 일부러 엉뚱한 것을 눌러 봅니다 — 범례의 KNOWLEDGE 를 누르세요. 이전에는 아무 일도 안 일어났습니다. 지금은 안내 맨 위에 "KNOWLEDGE 를 켰지만 화면은 그대로다" 한 줄이 생겨야 맞습니다. 5. 이제 점선이 둘러진 QUESTION 을 누릅니다. 안내가 걷히고 점이 돌아오면 맞습니다. 가운데 '질문 다시 켜기' 버튼을 눌러도 결과는 같습니다. 6. 검색창에 아무 말이나(예: zzzz) 넣어 조건을 바꿔 봅니다. 방금 눌렀던 것에 대한 한 줄은 사라져야 합니다 — 조건이 달라졌으니 더 이상 맞는 말이 아니기 때문입니다. 색을 칠하지 않았습니다. 배경을 깔거나 띠를 두르지 않고, 잉크색 점선과 글자만 썼습니다. 꺼져 있다는 취소선도 지우지 않았습니다 — 지우면 "꺼져 있다"는 사실이 사라지니까요. 여섯 가지 화면 색상(system/paper/mist/carbon/graphite/phosphor)에서 모두 확인했습니다. 캡처: state/shots/039-diag-k.png (고치기 전 — 눌러도 변화 없음) · state/shots/039-fix-k.png (고친 뒤) · state/shots/039-diag-q.png · state/shots/039-tone-a.png · 039-tone-b.png · 039-tone-c.png 곁가지로 하나 발견해 따로 등록했습니다 (REQ-20260827-007-62x6): 범례 항목은 마우스로만 누를 수 있고 키보드로는 닿지 않습니다. 다만 안내 가운데 버튼은 키보드로 닿으므로 되돌리는 길 자체는 막혀 있지 않습니다.
- 2026-08-27T00:37:08+09:00 status: review -> done (by sjpark1) — 대시보드 승인 [via dashboard]
