---
id: REQ-20260827-040-62x6
type: request
title: 긴 붙여넣기를 접는다
summary: 터미널 입력줄에 대량 텍스트를 붙이면 그대로 다 보인다 — Claude Code 처럼 한 줄 칩으로 접고 다시 붙이면 펼치기
goal: 긴 텍스트를 붙이면 [Pasted text #N +M lines] 한 줄로 접히고, 전송 시에는 원문이 그대로 가며, 다시 붙이면 펼쳐진다
status: done
size: M
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: 3da5b9de
project: section9
relates_why: {}
tags: ["auto-audit", "access", "docs", "terminal", "attached", "png", "image"]
created: 2026-08-27T18:36:34+09:00
status_since: 2026-08-27T19:32:48+09:00
updated: 2026-08-27T19:32:48+09:00
priority: 50
---

## Original

[선행 대화 — 직전 미문서화 메시지]
- (18:29) 지금 우리 하네스 시스템에서 stream이 가치가 있나? 없어도 되는 수준인가?

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
❯ [Pasted text #5 +17 lines]
────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
  paste again to expand

이런식으로 로컬 터미널 클로드 코드에 대량의 문자열을 붙여넣기 하면 위 내용과 같이 접힌다.
여기는 그대로 다 보인다. 이 기능을 그대로 구현해줘.

## Notes

### 2026-08-27T18:42:58+09:00 tdd (by nicehugepark)

### TDD 시나리오 (붙여넣기 접기)

핵심 계약: **화면에만 접힌다. 서버로 가는 텍스트에는 늘 원문이 들어간다.**

- [ ] S1 정상: 20줄 텍스트를 붙이면 입력줄에 `[Pasted text #1 +20 lines]` 한 줄만 남는다.
- [ ] S2 정상(원문 보존): 접힌 채 전송하면 /api/chat 로 가는 text 에 원문 20줄이 그대로 들어간다.
- [ ] S3 경계(임계 미만): 5줄·799자 이하는 접지 않고 브라우저 기본 붙여넣기 그대로.
- [ ] S4 경계(한 줄 긴 토큰): 줄바꿈 없는 1200자는 `+1200 chars` 로 접는다 — 줄 수만으로 세면 wrap 되어 화면 밖으로 나가는 한 줄을 놓친다.
- [ ] S5 정상(다시 붙이면 펼침): 같은 내용을 한 번 더 붙이면 그 칩 자리가 원문으로 펼쳐지고 새 칩은 생기지 않는다.
- [ ] S6 번호: 두 번째 붙여넣기는 #2. 전송해도 초기화되지 않고 탭 안에서 계속 누적된다(같은 번호 재사용 금지 — 히스토리로 돌아온 옛 칩이 엉뚱한 원문으로 펼쳐지는 것을 막는다).
- [ ] S7 실패(유령 매핑 금지): 칩 글자를 한 글자라도 손으로 고치면 매핑이 끊긴다 — 그 자리는 글자 그대로 전송되고 원문은 새어나가지 않는다.
- [ ] S8 실패(중첩 주입 금지): 붙여넣은 원문 안에 다른 칩과 똑같은 문자열이 들어 있어도 그 안쪽은 다시 펼쳐지지 않는다(왼→오 1패스 치환).
- [ ] S9 회귀(초안 복원): 접힌 채 탭을 떠났다 돌아와도 칩↔원문 매핑이 살아 있어 전송 시 원문이 간다.
- [ ] S10 회귀(히스토리): ↑ 로 불러온 접힌 메시지도 전송하면 원문으로 나간다.
- [ ] S11 회귀(안 깨지는 것들): 이미지 붙여넣기·드래그&드롭 첨부 / `/` 명령 팔레트 / ↑↓ 히스토리 / Escape 지우기 / Ctrl+Enter·Shift+Enter 줄바꿈.
- [ ] S12 경계(선택 영역 위 붙여넣기·여러 칩): 선택을 덮어쓰며 칩이 들어가고, 칩 2개가 각각 제 원문으로 펼쳐진다.
- [ ] S13 경계(칩만 있는 입력): 칩 하나만 있는 입력도 빈 전송이 아니라 원문 전송이다.

실행: `python3 tests/ paste_fold`

### 2026-08-27T19:00:40+09:00 response (by nicehugepark)

### 구현 완료 — 접히는 것은 화면뿐, 나가는 것은 언제나 원문

**정한 것 (판정 대상)**

1. **접는 기준: 6줄 이상 또는 800자 이상.** 입력줄 최대 높이는 120px, 한 줄은
   12.5px×1.7 ≈ 21px 이라 **6줄째부터 붙인 글이 화면 밖으로 밀려 보이지 않는다**
   (textarea 가 overflow:hidden 이라 스크롤도 안 된다). 즉 "접어서 이득이 생기는
   지점"과 "안 보이기 시작하는 지점"이 같다. 두 번째 캡처가 그 잘림을 보여준다.
   줄 수만 세면 줄바꿈 없는 긴 한 줄(주소·로그 한 줄·base64)을 놓치므로 글자 수도
   함께 본다 — 좁은 창(1024px)에서 약 800자가 6줄이 되는 지점이다.
   **원본 Claude Code 의 정확한 값은 알 수 없다(추측)** — 위는 이 입력줄의 실제
   치수에서 뽑은 값이다.
2. **번호 #N = 이 탭에서 몇 번째 붙여넣기인가. 전송해도 초기화하지 않는다.**
   ↑ 히스토리로 옛 메시지의 접힌 자리가 되돌아오는데, 번호를 재사용하면 그 자리가
   **엉뚱한 원문으로 펼쳐진다** — 조용한 내용 바꿔치기다. 그래서 누적한다.
3. **라벨은 원본 그대로 `[Pasted text #N +M lines]`** (한 줄짜리는 `+M chars`).
   요청서의 목표 문장이 이 모양을 지목했고, 이 터미널은 첨부도 `[Image #1]` 로
   쓴다. 사람이 읽는 안내 문구만 한국어다 — `다시 붙여넣으면 펼쳐집니다`.

**원문이 사라지지 않게 한 장치 셋**

- 원문 보관(`termPastes`)은 **셸 DOM 보다 오래 산다** — 탭을 떠나면 입력줄이
  파괴되지만 보관은 남는다. 그래서 접힌 채 나갔다 와도 매핑이 살아 있다(캡처 없이
  실브라우저로 확인). 어떤 경로에서도 보관을 지우지 않는다 — 지운 뒤 남은 칩은
  원문을 잃은 껍데기가 되고, 그게 이 기능의 최악의 실패다.
- 치환은 **정확 일치**다. 칩 글자를 한 글자라도 손으로 고치면 매핑이 조용히 끊겨
  그 자리는 글자 그대로 나간다(유령 매핑 금지).
- 펼치기는 **왼→오 1패스**다. 치환해 넣은 원문을 다시 훑으면, 원문 안에 들어 있던
  다른 칩 문자열이 펼쳐져 **사용자가 붙이지 않은 내용이 전송된다**.

**검증** — `python3 tests/ paste_fold` 14건 OK, 전체 973건 OK(기준선 959 + 신규 14).
순수 로직은 index.html 에서 블록째 떼어 node 로 **실제 실행**한다(정규식으로
"그렇게 생겼다"를 보는 대신). 붙여넣기 자체는 헤드리스로 흉내내기 어려워
CDP 로 실브라우저를 몰아 진짜 paste 이벤트를 쐈다 — S1~S12 **20개 항목 전부 통과**:
접힘·원문 전송·다시 붙여 펼침·번호 누적·선택 덮어쓰기·유령 매핑 없음·탭 이탈 후
복원·짧은 것은 안 접힘·한 줄 긴 토큰·Escape·/팔레트·Ctrl+Enter·이미지 첨부·↑히스토리.

캡처: ① 20줄 붙여넣어 접힌 모습 + 안내 한 줄 ② 다시 붙여 펼친 모습(6줄째가
잘리는 것이 보인다 — 접는 기준의 근거) ③ 줄바꿈 없는 1200자 ④ 이미지 첨부 회귀

[Image: assets/REQ-20260827-040-62x6/paste-1-folded.png]
[Image: assets/REQ-20260827-040-62x6/paste-2-expanded.png]
[Image: assets/REQ-20260827-040-62x6/paste-3-oneline.png]
[Image: assets/REQ-20260827-040-62x6/paste-4-image-attach.png]

### 2026-08-27T19:01:08+09:00 tdd (by nicehugepark)

### TDD 시나리오 — 전부 통과

- [x] S1 정상: 20줄 → `[Pasted text #1 +20 lines]` 한 줄 (실브라우저)
- [x] S2 정상(원문 보존): 접힌 채 전송 → 서버로 가는 text 는 원문 20줄 (실브라우저에서 전송 payload 를 가로채 대조)
- [x] S3 경계: 5줄·799자 이하는 접지 않는다
- [x] S4 경계: 줄바꿈 없는 1200자 → `+1200 chars`
- [x] S5 정상: 같은 것을 다시 붙이면 그 자리가 원문으로 펼쳐진다
- [x] S6 번호: 전송해도 초기화되지 않고 #2, #3… 으로 누적
- [x] S7 실패(유령 매핑 금지): 칩을 한 글자 고치면 매핑이 끊기고 원문은 새지 않는다
- [x] S8 실패(중첩 주입 금지): 원문 안의 칩 문자열은 다시 펼치지 않는다(1패스)
- [x] S9 회귀(초안 복원): 접힌 채 탭 이탈 후 복귀 → 칩 유지, 전송 시 원문
- [x] S10 회귀(히스토리): ↑ 로 불러온 접힌 메시지도 전송 시 원문
- [x] S11 회귀: 이미지 붙여넣기 첨부 · / 팔레트 · ↑↓ 히스토리 · Escape · Ctrl+Enter 줄바꿈
- [x] S12 경계: 선택 영역 위 붙여넣기는 덮어쓰며 새로 접힌다 · 칩 2개 각각 제 원문으로
- [x] S13 경계: 칩 하나만 있는 입력도 빈 전송이 아니라 원문 전송

커버 못한 것: 없음. 다만 드래그로 **텍스트를 떨구는** 경우는 접지 않는다 —
드롭 핸들러는 파일만 받고 텍스트 드롭은 브라우저 기본 동작이다(기존 동작 유지).

## History
- 2026-08-27T18:36:34+09:00 created by nicehugepark (status: open)
- 2026-08-27T18:42:58+09:00 status: open -> in-progress (by nicehugepark) — designer 착수 — 접기 기준·번호 규칙을 먼저 정하고 TDD 시나리오부터 남긴다
- 2026-08-27T19:01:08+09:00 status: in-progress -> review (by nicehugepark) — 터미널 입력줄에 긴 글을 붙여넣으면 이제 한 줄로 접힙니다. 접히는 건 화면뿐이고, 보낼 때는 언제나 원문이 그대로 갑니다. 봐주실 것 셋 — 제가 정한 값들입니다. ① **몇 줄부터 접을지**: 6줄 이상, 또는 줄바꿈 없이 800자 이상. 입력줄이 딱 6줄 높이라 그때부터 붙인 글이 화면 밖으로 밀려 안 보입니다(두 번째 캡처에서 6줄째가 잘린 게 보입니다). 주소나 로그 한 줄처럼 줄바꿈 없이 긴 것도 접습니다. ② **접힌 자리 이름**: `[Pasted text #3 +20 lines]`. 번호는 이 화면에서 몇 번째로 붙여넣었는지이고, 메시지를 보내도 1번으로 돌아가지 않고 계속 늘어납니다 — 위 화살표로 옛 메시지를 불러왔을 때 그 안의 접힌 자리가 엉뚱한 글로 펼쳐지는 걸 막으려는 것입니다. ③ **안내 문구**: 접혀 있는 동안만 입력줄 아래 한 줄 — "다시 붙여넣으면 펼쳐집니다". 재현: Terminal 탭 → 6줄 넘는 글을 복사해 입력줄에 붙여넣기 → 한 줄로 접히는지 → 같은 것을 한 번 더 붙여넣어 원래대로 펼쳐지는지 → 접힌 채로 Enter 를 눌러 상대가 받은 내용이 원문 그대로인지. 접힌 채로 다른 탭에 갔다 와도, 위 화살표로 다시 불러와도 원문이 살아 있는 것까지 실제 브라우저로 확인했습니다. 접힌 글자를 손으로 지우면 그 글도 함께 사라집니다(내용 없는 빈 껍데기가 남지 않게). 하나 더 정해주실 것: **보내고 난 뒤 대화 기록**에는 접힌 한 줄이 아니라 원문이 전부 펼쳐져 보입니다(받은 쪽 기록을 그대로 그리기 때문). 기록에서도 접어 보이길 원하시면 별건으로 잡겠습니다.
- 2026-08-27T19:32:48+09:00 status: review -> done (by nicehugepark) — [via dashboard]
