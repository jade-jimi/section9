---
id: REQ-20260829-025-62x6
type: request
title: 문서를 치우는 길
summary: 문서 아카이브·퍼지·삭제 — 문서 한 건에서도, 여러 건을 골라서도
status: done
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: a5a0449c
sessions: ["911949e0", "ab1bb85d", "a5a0449c"]
project: section9
relates_why: {}
tags: ["auto-audit"]
created: 2026-08-29T16:50:01+09:00
status_since: 2026-08-30T12:35:39+09:00
updated: 2026-08-30T12:35:39+09:00
priority: 50
---

## Original

그리고 문서들을 아카이브하거나 퍼지, 삭제하는 기능을 추가해줘. 문서 자체에서 단건으로도 가능하고 여러개를 선택해서 삭제할 수 있도록 기능을 만들어라.

## Notes

### 2026-08-29T20:09:11+09:00 decision (by nicehugepark)

착수 지시 접수. 순서를 뒤에 둔다 — 서버 몫(문서 아카이브·퍼지·삭제)이 `bin/s9`, 화면 몫(단건·다건 고르기)이 `web/index.html` 이라 지금 두 파일을 쓰고 있는 작업들과 겹친다. **되돌릴 수 없는 기능이라 서두르지 않는다** — 삭제는 잘못 만들면 사고가 복구 불가다. 앞의 것들이 닫히는 대로 설계부터 착수한다.

### 2026-08-29T22:36:35+09:00 workspace (by nicehugepark)

본 저장소에서 이어받는다 — 화면 작업이라 살아 있는 서버가 읽는 자리에서 해야 한다. 커밋하면 다음 라운드부터 작업자가 다시 자기 워크트리를 받는다.

### 2026-08-29T22:40:14+09:00 tdd (by nicehugepark)

문서를 치우는 길 — 3단 모형(보관 / 삭제 / 소거)의 시나리오.

되돌릴 수 있는 순서로 층을 나눈다. **보관**은 파일을 그대로 두고 목록에서만 내린다. **삭제**는 같은 폴더의 .trash 로 옮긴다(번호는 계속 물고 있어 재발번이 없다). **소거**는 .trash 안의 것만 지우고, 그 자리에 번호만 남긴 묘비를 세운다 — 지운 번호가 다른 문서로 다시 나가는 사고를 막던 그 장치를 소거가 뚫으면 안 된다.

- [x] S1 s9 archive 문서 — frontmatter 에 archived 시각이 서고 History 에 한 줄이 남는다
- [x] S2 보관된 문서는 s9 ls 기본 목록에서 빠지고 s9 ls --archived 에서만 보인다 (카탈로그 행에는 archived 필드로 남는다)
- [x] S3 s9 unarchive 문서 — archived 가 지워지고 목록에 돌아온다
- [x] S4 s9 rm 문서 여러 건 — 한 번에 .trash 로, 카탈로그에서 사라진다
- [x] S5 s9 trash — 휴지통 목록에 id·제목·버린 시각이 나온다
- [x] S6 s9 restore 문서 — 원래 자리로 돌아오고 카탈로그에 다시 뜬다 (첨부도 함께)
- [x] S7 s9 purge 문서 --yes — .trash 의 파일이 실제로 사라진다. --yes 없으면 거부, 휴지통에 없는 살아 있는 문서도 거부
- [x] S8 소거 뒤에도 그 번호는 재발급되지 않는다 (.trash/purged 묘비)
- [x] S9 HTTP — POST /api/docs/archive·rm·restore·purge 와 GET /api/trash 가 CLI 와 같은 함수를 지난다
- [x] S10 화면 — 문서 뷰어에 보관/삭제 손잡이, 목록에 고르기(다중 선택)와 묶음 처리 띠, 휴지통·보관함 보기에서 복원/완전 삭제

### 2026-08-29T23:01:24+09:00 stop (by nicehugepark)

무인 작업자를 세웠다 (pid 675381, 세션 911949e0) — 리드의 서브에이전트 둘이 이미 같은 두 파일(web/·bin/s9)을 쓰고 있다 — 세 손이 겹쳐 사용자 화면이 깨졌다. 025 는 그 둘이 끝난 뒤 다시 붙인다 (REQ-20260829-036-62x6 의 미등록 경로)

### 2026-08-30T10:18:49+09:00 workspace (by nicehugepark)

본 저장소에서 이어받는다 — 화면 작업이라 살아 있는 서버가 읽는 자리에서 해야 한다. 커밋하면 다음 라운드부터 작업자가 다시 자기 워크트리를 받는다.

### 2026-08-30T10:30:17+09:00 response (by nicehugepark)

치우는 길 세 층을 끝까지 이어붙였다 — 서버(S1~S9)는 앞선 라운드에서 서 있었고, 이 라운드는 **화면(S10)** 을 눈으로 보고 고쳤다.

**실측으로 잡은 결함 넷** (모두 캡처로 확인 → 수정 → 재캡처):

1. **눈금이 문서 번호 위에 겹쳐 앉았다.** `.doclist.picking .row{padding-left:34px}` 가 눈금 자리를 내는데, 스킨(`calm.css`)과 밀도(`density.css`)가 **같은 무게로** `.doclist .row{padding}` 을 다시 쓴다. 무게가 같으면 순서가 이긴다 — `tidy.css` 가 그 둘보다 앞에 실려 있어서 졌고, `REQ-…` 의 첫 글자가 눈금에 먹혔다. `web/index.html` 의 선언 순서를 스킨·밀도 **뒤**로 옮겼다.
2. **한글에 영문 트래킹이 걸려 낱자가 흩어졌다.** 타입바 손잡이(`치 운 것`)·묶음 띠(`고 른 문 서`)·판 탭·문서 캡션이 모두 `.tb`/mono + `letter-spacing` 옷을 입고 있었다. 이 판이 이미 내린 결론(overlay.css `.dlgcap` 이 같은 이유로 자간 0)을 그대로 따라 **자간을 걷고 한 급 올렸다**.
3. **판 안의 버튼이 행위인데 이름의 옷을 입고 있었다.** `되돌리기`·`영구 삭제`·`닫기` 중 상태의 이름은 하나도 없다 — REQ-20260828-007-62x6 4차가 창 바닥(`.dlgfoot`)에서 이미 세운 "이름은 mono, 행위는 본문체" 규칙을 `.tidypanel .acts button` 에도 적용했다.
4. **붉음이 서른일곱 줄에서 흔해졌다.** tidy.css 스스로 "전부 붉으면 붉음이 아무 말도 하지 않는다" 고 적어 놓고, 휴지통 목록의 줄마다 붉은 테두리가 섰다. 줄에서는 **잉크만** 붉고 테두리는 보통으로, 면을 채우는 붉음은 짚었을 때와 바닥의 `전부 영구 삭제` 에만 남겼다.

**S10 을 시험으로 잠갔다.** 1번은 화면을 눌러 보지 않으면 안 보이고, 다음 스킨 한 벌이 또 뒤집을 수 있는 자리다 — `test_s10_screen_handles_and_load_order` 가 (ㄱ) `tidy.css` 가 `docs/skins/density/calm` 뒤에 실리는 순서와 (ㄴ) 세 자리(목록·문서 한 장·치운 것 판)의 손잡이 열다섯 개를 잡는다. `python3 tests/ doc_tidy` 10건 통과.

캡처: `scratchpad/tidy-pick.png`(수정 전, 겹침) → `tidy-pick2.png`(수정 후) · `tidy-trash.png`(수정 전, 붉은 목록) → `tidy-trash2.png`(수정 후) · `tidy-doc2.png`(문서 맨 아래 `이 문서 치우기` 줄).

만진 것은 `web/index.html`(선언 순서 한 줄) · `web/css/tidy.css` · `tests/test_doc_tidy.py` 뿐이다 — `bin/s9` 와 다른 조각은 지금 다른 세션이 쓰고 있어 손대지 않았다.

## History
- 2026-08-29T16:50:01+09:00 created by nicehugepark (status: open)
- 2026-08-29T20:08:48+09:00 status: open -> in-progress (by nicehugepark) — drag 이동 [via dashboard]
- 2026-08-30T10:30:34+09:00 status: in-progress -> review (by nicehugepark) — 문서를 치우고 되돌리는 손잡이가 Docs 화면에 생겼습니다. F5 후 Docs 목록 맨 윗줄 오른쪽 끝의 [고르기]를 눌러 문서 몇 개를 체크하고 [삭제]를, 옆의 [치운 것]에서 휴지통 탭을 열어 [되돌리기]를 눌러 보세요. 문서 한 장을 열면 맨 아래에도 [보관]·[삭제]가 있습니다. 골라 지운 것이 목록에서 사라졌다가 되돌리기로 제자리에 돌아오면 승인, 눈금이 글자를 가리거나 글자가 흩어져 읽히면 반려입니다.
- 2026-08-30T12:35:39+09:00 status: review -> done (by nicehugepark) — 승인 (메모 없음) [via dashboard]
