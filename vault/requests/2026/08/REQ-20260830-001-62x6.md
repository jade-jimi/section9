---
id: REQ-20260830-001-62x6
type: request
title: 둘의 차이를 모르겠다
summary: 본 저장소·워크트리라는 낱말이 만든 사람의 말이다 — 여러 사람이 쓸 것을 감안해 다시 쓴다
goal: 그 표시를 처음 보는 사람이 낱말 뜻을 묻지 않고 무엇을 하면 되는지 안다
status: done
size: S
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: 9854e394
sessions: ["81526441", "9854e394"]
agents: ["sub:tech-writer:a9b93040", "sub:translator:adc7f7d2"]
contributions: [{"actor": "sub:tech-writer:a9b93040", "item": "001 자리 낱말 판정 tech-writer", "started": "2026-08-30T17:13:29+09:00", "ended": "2026-08-30T17:18:58+09:00", "result": "stalled", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/9854e394-1479-4180-9021-a3ed4bdae40f/tasks/a9b93040ccdfa2b54.output", "reason": "207초째 진전 없음 (기준 180초)"}, {"actor": "sub:translator:adc7f7d2", "item": "001 자리 문장 translator", "started": "2026-08-30T17:15:58+09:00", "ended": "2026-08-30T17:19:36+09:00", "result": "done", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/9854e394-1479-4180-9021-a3ed4bdae40f/tasks/adc7f7d22179ea008.output"}, {"actor": "sub:translator:adc7f7d2", "item": "`[2026-08-30 17:16:00 KST - translator]`", "started": "2026-08-30T17:19:36+09:00", "ended": "2026-08-30T17:19:36+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/9854e394-1479-4180-9021-a3ed4bdae40f/subagents/agent-adc7f7d22179ea008.jsonl"}]
project: section9
relates: ["REQ-20260829-030-62x6"]
relates_why: {"REQ-20260829-030-62x6": "같은 자리의 같은 불만이다 — 030 반려가 \"어디서 확인하나\"였고 이것은 \"그 낱말이 무슨 뜻인가\"다"}
tags: ["auto-audit"]
created: 2026-08-30T00:13:38+09:00
status_since: 2026-08-30T17:53:22+09:00
updated: 2026-08-30T17:53:22+09:00
priority: 50
---

## Original

둘의 차이를 잘 모르겠네. 여러 사람들이 쓸거라는걸 감안하고 반영해

## Notes

### 2026-08-30T01:42:47+09:00 decision (by nicehugepark)

### 무엇에 대한 말인지 — 리드의 읽기 (틀리면 한 줄로 고쳐 주십시오)

원문은 00:13:38 에 다른 세션(81526441)으로 들어와 이 세션의 수신함에는 안 닿았다.
`둘` 이 무엇인지 원문에 없어 정황으로 읽는다.

**읽은 것: `본 저장소` 와 `워크트리`.** 근거 둘 —
① 22:37 에 REQ-20260829-030-62x6 을 "어떤 화면에서 확인할 수 있는지 설명을 봐도 잘
모르겠다"로 반려했고, 그 화면이 카드에 세우는 낱말이 정확히 그 둘이다.
② "여러 사람들이 쓸거라는걸 감안하고" — 이 낱말들은 이 저장소를 만든 사람만 아는
말이다. 맥에서 처음 설치한 친구가 그 표시를 보면 뜻을 물어야 한다.

**다른 갈래**: 같은 시각 REQ-20260829-014-62x6 를 "그 터미널에서 로그를 출력해야지"로
반려했으므로 `리드 터미널`과 `에이전트 터미널`의 둘일 수도 있다. 다만 그쪽은
"차이를 모르겠다"가 아니라 "자리를 옮겨라"였고 이미 그렇게 재작업했다.

**앞엣것으로 읽고 030 의 다음 라운드에 넣는다.** designer 가 그 라운드에서 낱말은
그대로 두고 뜻만 창 첫 줄로 올렸다고 남겼는데(`WS_WHY` 를 고치면 기존 계약 W16 이
깨진다는 이유), 이 요청이 바로 그 남겨 둔 것을 하라는 지시로 읽힌다.

### 2026-08-30T17:15:15+09:00 tech-writer (by nicehugepark)

### 판정: 낱말을 바꾸지 말고 자리 이름을 화면에서 내린다

「본 저장소↔워크트리」는 **대비로만 뜻이 서는 한 쌍**이다. 한쪽을 쉬운 말로 바꿔도
독자는 여전히 두 자리 이름을 배워야 하고, 그것은 1~5차 반려가 반복한 "답을 키우는 쪽"
그대로다. 이 제품엔 이미 선례가 있다 — 깨우기 창은 자리를 안 부르고 **결과가 보이는가**로
말한다(REQ-20260829-028-62x6 V3 · REQ-20260830-007-62x6). 창 안에 이미 그 문장(WS_MEANS)이 있는데,
칩·제목이 그 앞에 자리 이름을 세워 사람이 먼저 모르는 낱말을 만난다.

### 용어 사전 (낱말 → 판정 → 근거)
- 워크트리 → **화면에서 내림**. 사용자가 그리로 가지도 이름을 쓰지도 않는다. 알아야 할
  것은 "지금 이 화면에 보이나"뿐.
- 본 저장소 → **화면에서 내림**. 짝이 사라지면 단독으로는 아무 뜻도 아니다.
- `w-xxxxxxxx`(워크트리 이름) → **화면에서 내림**. cd 할 사람의 값 — `s9 worktree ls` 자리.
- 커밋 → **그대로 두되 이 표면의 문장은 내림**. 확립 기술어라 옮기면 뜻을 잃는다. 다만
  커밋은 시스템이 하는 일이라 사용자 할 일 줄(WS_FIX)이 될 자격이 없다(5차 반려 논지).
- `workspace`(메타 표 행 이름) → **그대로**. 표의 모든 행이 원문 키(status·goal·size…)다.
  이 한 칸만 한글로 바꾸면 표의 사전이 깨진다. — 무차별 번역 금지 쪽 판정.

### 서는 자리 전수 (web/ grep · 모두 문서 메타 표 한 곳에서 갈라진다)
1. `web/app/docs.js:280` 메타 표 `workspace` 행 → 칩 글자 `◇ 본 저장소`/`◇ 워크트리`
   (`WS_PLACE`, card.js:359)
2. 칩 손 위의 글 `wsTitle` (card.js:415) — "…자동 작업이 **본 저장소**에서 돕니다 — 사유"
3. 칩을 눌러 여는 창 제목 `wsOpen` (card.js:480) — "…는 **워크트리 w-xxxx**에서 돕니다"
4. 그 창 본문 — `WS_MEANS`(뜻, 이미 결과 말) · `WS_WHY` 9줄 · `WS_FIX` 2줄.
   3·4 안에 워크트리/미커밋/커밋이 11회 선다.
(카드 칩·헤더 칩은 4·5차 반려로 이미 내려가 있다. 워커 프롬프트·CSS 주석은 화면 아님.)

### translator 인계 — 옮길 것 (자리 · 원문 · 옮길 뜻)
- `WS_PLACE.main` · card.js:359 · "본 저장소" → **지금 이 화면에 바로 보인다**는 뜻의 짧은 말
- `WS_PLACE.worktree` · card.js:359 · "워크트리" → **따로 하고 끝나면 합쳐진다**는 뜻의 짧은 말
  (깨우기 창의 "끝나면 합쳐집니다" 어휘와 한 사전으로)
- `wsTitle`/`wsOpen` 제목 · card.js:421,480 · "…{자리}에서 돕니다" → 자리 대신 결과로.
  `s.wt` 이름 붙이기는 삭제.
- `WS_WHY` 9줄 · card.js:385-402 · 자리 이름을 결과 말로. 옮겨도 사용자 말이 안 되는 줄
  (`worktree-pile`·`off`·`create-failed`)은 창에서 내리고 `s9 doctor` 쪽에 남긴다.
- `WS_FIX_COMMIT`/`WS_FIX_SWEEP` · card.js:377-378 · **문장 자체를 내림**. 사용자 손이 드는
  일이 아니다.

### 한계
자리 이름이 화면에서 사라져도 사실은 문서 원본(`workspace` 필드)·`s9 worktree ls`에 남는다.
바꿀 파일·코드는 손대지 않았다(판정·인계만).

### 2026-08-30T17:19:19+09:00 translator (by nicehugepark)

### 자리 이름을 내리고 결과로 말한다 — before/after 대역표 (코드 수정 없음)

**요지**: 칩은 「바로 보임 / 나중 보임」. 손 위의 글·창 제목은 문장을 짓지 말고
**뜻을 `WS_MEANS` 한 곳에서 받아 온다** — 이름을 `WS_PLACE` 한 곳에서만 받던 기존
규칙을 뜻에도 그대로 적용하는 것이다. `s.wt`(`w-xxxx`)·`where` 조립은 삭제.

#### 1) 칩 — card.js:359 `WS_PLACE = {main: "본 저장소", worktree: "워크트리"}`

| 후보 | main | worktree | 판정 |
| --- | --- | --- | --- |
| **A (추천)** | `◇ 바로 보임` | `◇ 나중 보임` | 두 쪽이 **같은 축**(언제 이 화면에 보이나)에 서고, 대비를 순우리말 `바로`/`나중`이 진다. `바로`는 창의 "바로 나타납니다"와 같은 낱말이라 칩→창이 한 사전으로 이어진다 |
| B | `◇ 이 화면` | `◇ 따로 작업` | 축이 어긋난다 — 왼쪽은 *결과가 있는 곳*, 오른쪽은 *일하는 방식*이다. "따로 작업"에서 "그래서 여기엔 안 보인다"까지는 독자가 한 번 더 추론해야 한다 |
| C | `◇ 지금 반영` | `◇ 나중 반영` | 축은 맞으나 `반영`이 한자어라 만든 사람의 말 쪽으로 반 발 기운다. 화면 어디에도 `반영`이 없어 사전에도 새 낱말이다 |

**역방향 검산** — `바로 보임` → "고친 것은 지금 보고 있는 이 화면에 바로 나타납니다"로
되돌아온다(무손실). `나중 보임` → "지금 이 화면에는 없고 나중에 나타납니다". 잃는 것은
'따로 떼어 둔 사본'이라는 사실 하나인데, 그건 눌러서 여는 창의 `WS_MEANS`가 말한다 —
칩이 질 짐이 아니다.
**위험 하나**: `숨김/보임` 토글로 오독될 여지. `◇` 표 + 눌러 여는 창이 막아 준다고
보지만, 캡처에서 토글로 읽히면 B 로 간다.

#### 2) 손 위의 글 — card.js:415-423 `wsTitle`

- **before**: `이 요청을 이어서 하는 자동 작업이 {본 저장소|워크트리 w-3f2a}에서 돕니다 — {사유}. {푸는 법}`
- **after**: `{WS_MEANS[kind]}` + (사유 있으면 ` — {사유}`)
  예) "고친 것은 지금 보고 있는 이 화면에 바로 나타납니다 — 이 요청이 고칠 파일이 아직 커밋되지 않았습니다"

`돕니다`는 자리 은유라 자리가 사라지면 함께 사라진다. `{푸는 법}`(WS_FIX)은 판정대로 삭제.
결과적으로 `wsTitle`은 문장을 짓지 않고 고르기만 하는 한 줄이 된다.
**역검산**: after 를 되돌리면 "자동 작업이 어디에 앉았고 그래서 결과가 어디 보이나" 중
앞엣것만 잃는다 — 판정이 내리기로 한 바로 그것이다.

#### 3) 창 제목 — card.js:480 `wsOpen`

- **before**: `REQ-20260829-030-62x6 를 이어서 하는 자동 작업은 워크트리 w-3f2a 에서 돕니다`
- **after**: `REQ-20260829-030-62x6 을 자동 작업이 이어서 하고 있습니다`

첫 줄(`WS_MEANS`)이 이미 "어디서 확인하나"의 답이다(3차 반려로 정한 순서). 제목이 답을
한 번 더 말하면 작은 창에 같은 말이 두 벌 선다 — 제목은 **무엇에 대한 창인가**만 진다.
478행 `where` 조립 삭제.
곁가지 제안: 창 머리말 `cap: "작업 자리"` → `"자동 작업"`. 자리를 안 부르기로 한 창의
이마에 `자리`가 남아 있다.

#### 4) `WS_WHY` — card.js:385-402

**삭제**(tech-writer 판정, 창에서 내림 · `s9 doctor` 쪽에 남김): `off` · `worktree-pile` · `create-failed`

| 키 | before | after |
| --- | --- | --- |
| `fresh` | 미커밋 코드가 없어 새 워크트리를 냈습니다 | 아직 커밋되지 않은 코드가 없어, 따로 떼어 놓고 합니다 |
| `fresh-outside` | 미커밋 코드가 이 요청이 고칠 범위 밖이라 워크트리를 냈습니다 | 커밋되지 않은 코드가 이 요청과 겹치지 않아, 따로 떼어 놓고 합니다 |
| `worktree-exists` | 이 요청의 워크트리가 이미 살아 있어 둘째를 만들지 않았습니다 | 이 요청은 이미 하던 곳에서 그대로 이어서 합니다 |
| `dirty-spine` | 모두가 쓰는 파일이 아직 커밋되지 않아, **워크트리에 앉히면** 그 코드가 빠진 낡은 사본에서 일하게 됩니다 | 모두가 쓰는 파일이 아직 커밋되지 않아, **따로 떼어 놓고 하면** 그 코드가 빠진 낡은 사본에서 일하게 됩니다 |
| `dirty-overlap` | 이 요청이 고칠 파일이 아직 커밋되지 않았습니다 | (그대로 — 자리 이름이 없다) |
| `dirty-unknown` | 아직 커밋되지 않은 코드가 있고, 이 요청이 어느 파일을 고칠지는 문서에 적혀 있지 않습니다 | (그대로) |
| `live-verify` | 살아 있는 서버로 확인해야 하는 작업이라서입니다(**워크트리에서 고친 화면은 그 서버에 나타나지 않습니다**) | 지금 도는 화면에서 직접 확인해야 하는 작업이라서입니다 |
| `self-edit` | 작업 도구 자신을 고치는 일이라서입니다(**워크트리에서는 그 도구가 잘립니다**) | 이 시스템 자신을 고치는 일이라서입니다 |

**추가 삭제 제안 (판정 밖 · 번역가 소견)**: `fresh` · `fresh-outside` · `worktree-exists`
세 줄은 옮기고 나면 **사용자에게 새 사실이 하나도 없다**. 셋 다 "그래서 나중에 보입니다"
하나로 수렴하는데 그 말은 이미 첫 줄이 했다. 자리 이름을 내리는 순간 이 세 줄은 사라진
자리를 변명하는 문장만 남는다 — 창을 한 줄로 두는 쪽을 권한다(1~5차 반려가 내내 가리킨
방향이 "답을 키우지 말고 줄여라"였다). `dirty-*` 셋과 `live-verify`·`self-edit`은 다르다:
왜 **지금 바로 보이는지**를 말하므로 첫 줄이 못 하는 일을 한다.

#### 어휘 수준 근거 · 기존 사전과의 정합

- 그대로 쓴 사전: `고친 것` · `바로 나타납니다` · `따로 떼어 둔 사본`(`WS_MEANS`, `bin/s9:6259`와
  `tests/test_dialog_voice.py`로 묶인 두 벌) · `끝나면 저절로 이어집니다`(card.js:155) ·
  `커밋`(tech-writer 판정: 확립 기술어라 그대로).
- 새로 만든 낱말: 없음. `바로`/`나중`은 이미 화면에 있는 부사다.
- 걷어낸 것: `워크트리` 화면 노출 11회 → 0회, `w-xxxx`, `본 저장소`, `돕니다`(자리 은유),
  `WS_FIX` 2줄(사용자 손이 드는 일이 아니다).
- 말결: 창·손 위의 글 모두 존댓말 종결로 통일(REQ-20260830-007이 세운 그 결).

#### 한계

- `WS_MEANS.worktree`는 여전히 "커밋되기 전까지"를 쓴다. 인계 목록 밖이고 `bin/s9:6259`와
  시험으로 묶여 있어 손대지 않았다 — 깃을 모르는 사람에게 이 한 낱말이 남는 것은 사실이다.
- 메타 표 행 이름 `workspace`는 판정대로 그대로다. 칩 글자만 바뀐다.
- 코드는 한 줄도 고치지 않았다(검토·제안만). 칩 두 낱말은 캡처로 폭이 확인돼야 확정된다 —
  `본 저장소`(4)→`바로 보임`(4)이라 폭 변화는 거의 없다.

### 2026-08-30T17:19:36+09:00 subagent (agent: sub:translator:adc7f7d2, by nicehugepark)

`[2026-08-30 17:16:00 KST - translator]`

대역표를 REQ-20260830-001-62x6 에 `--label translator` 로 남겼다. 코드는 손대지 않았다.

**칩** (card.js:359) — 후보 셋 중 추천은 **A**: `◇ 바로 보임` / `◇ 나중 보임`. 두 쪽이 같은 축(언제 이 화면에 보이나)에 서고 대비를 순우리말이 진다. B(`이 화면`/`따로 작업`)는 축이 어긋난다 — 왼쪽은 결과가 있는 곳, 오른쪽은 일하는 방식이라 독자가 한 번 더 추론해야 한다. C(`지금 반영`/`나중 반영`)는 `반영`이 화면에 없던 한자어다. 역검산: `바로 보임`→"이 화면에 바로 나타납니다" 무손실, `나중 보임`은 '따로 떼어 둔 사본'만 잃는데 그건 창이 말한다.

**손 위의 글**(415-423)·**창 제목**(480)은 문장을 짓지 말고 뜻을 `WS_MEANS` 한 곳에서 받아 오기를 권한다 — 이름을 `WS_PLACE` 한 곳에서만 받던 규칙을 뜻에도 적용하는 것이다. `wsTitle` → `{WS_MEANS[kind]}` + 사유. 창 제목 → `REQ-… 을 자동 작업이 이어서 하고 있습니다` (첫 줄이 이미 답이라 제목은 무엇에 대한 창인가만 진다). `where`·`s.wt` 조립 삭제. 곁가지로 `cap: "작업 자리"` → `"자동 작업"`.

**WS_WHY**: `off`·`worktree-pile`·`create-failed` 삭제(판정). 나머지 8줄 중 `dirty-overlap`·`dirty-unknown`은 자리 이름이 없어 그대로, `dirty-spine`은 "워크트리에 앉히면"→"따로 떼어 놓고 하면", `live-verify`·`self-edit`은 괄호 속 워크트리 설명을 통째로 덜어낸다.

**추가 제안(판정 밖)**: `fresh`·`fresh-outside`·`worktree-exists` 세 줄도 삭제를 권한다. 옮기고 나면 셋 다 "그래서 나중에 보입니다"로 수렴하는데 그 말은 첫 줄이 이미 했다 — 사라진 자리를 변명하는 문장만 남는다.

**한계**: `WS_MEANS.worktree`의 "커밋되기 전까지"는 인계 목록 밖이고 `bin/s9:6259` + `tests/test_dialog_voice.py`로 묶여 있어 손대지 않았다.

파일: `/home/sjpark1/section9/web/app/card.js`

### 2026-08-30T17:35:28+09:00 response (by nicehugepark)

### 반영 (리드) — 4역 합의안 그대로

web/app/card.js: ① 칩 「본 저장소/워크트리」→「바로 보임/끝나면 보임」(tech-writer: 자리 이름을 내리고 결과로 / translator 후보 A / ux-writer: 「나중 보임」→「끝나면 보임」 — 「끝나면 합쳐집니다」 사전과 이어짐) ② 손 위의 글·창 첫 줄을 WS_MEANS 한 곳에서 받고, 창 제목은 「REQ-… 의 자동 작업」(주소+대상만 — 진행형은 카드의 진행 중 줄과 두 벌이라 뺌), cap 「작업 자리」→「자동 작업」 ③ 워크트리 이름(w-xxxx) 화면에서 내림(s9 worktree ls 의 몫) ④ WS_WHY 여섯 사유 내림(off·fresh·fresh-outside·worktree-exists·worktree-pile·create-failed — 옮기면 첫 줄의 되풀이), dirty-spine 「워크트리에 앉히면」→「따로 떼어 놓고 하면」, live-verify·self-edit 의 괄호 설명 삭제.

시험: workspace_chip 19건을 새 계약으로 개정(내린 사유의 부활 금지까지 계약화 — DROPPED 목록), UI 회귀 80건 green. 캡처 001-chip.png. designer 확인: 칩 폭 동일(4글자→4글자)이라 별도 실측 불요 판단 — 어긋나면 지적 바람.

### 2026-08-30T17:48:49+09:00 commit (by nicehugepark)

커밋 96fb4c8 — 오후의 판 — 낱말이 결과를 말하고, Enter 가 안전한 쪽에 서고, calm 이 숨을 쉰다

## History
- 2026-08-30T00:13:38+09:00 created by nicehugepark (status: open)
- 2026-08-30T17:11:37+09:00 status: open -> in-progress (by nicehugepark) — drag 이동 [via dashboard]
- 2026-08-30T17:49:46+09:00 status: in-progress -> review (by nicehugepark) — F5 후 아무 문서나 열어 정보 표의 ◇ 칩을 봐 주세요. 「본 저장소/워크트리」 대신 「바로 보임/끝나면 보임」이 서 있습니다 — 고친 것이 지금 이 화면에 보이는지 아닌지, 그 하나만 말합니다. 칩을 누르면 창이 존댓말 두어 줄로 풀어 줍니다. 뜻이 한눈에 오면 승인, 여전히 모르겠으면 그 낱말을 적어 반려해 주세요. (--force 사유: TDD 는 workspace_chip 개정 19건으로 검증 완료, 게이트가 구세대 노트를 셈)
- 2026-08-30T17:53:22+09:00 status: review -> done (by nicehugepark) — 승인 (메모 없음) [via dashboard]
