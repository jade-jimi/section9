---
id: REQ-20260825-095-62x6
type: request
title: 에이전트 지목 전송 구현
summary: 터미널에서 선택한 에이전트에게 프롬프트를 보내면 리드가 SendMessage로 중계하고 결과를 스트림에 되돌린다
goal: 터미널에서 에이전트를 지목해 보낸 메시지가 그 에이전트에게 전달되고, 전달 여부·대상 부재가 화면에서 즉시 확인된다
status: done
size: M
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: cb49b2cd
sessions: ["d332535d", "cb49b2cd"]
project: section9
derived_from: REQ-20260825-091-62x6
relates: ["REQ-20260826-010-62x6"]
tags: ["terminal"]
created: 2026-08-25T22:39:11+09:00
status_since: 2026-08-26T13:01:27+09:00
updated: 2026-08-26T13:01:27+09:00
---

## Original

사용자 지시(2026-08-25 22:36): "치명적인 단점은 없고, 위험이 있지만 방어 가능하고, 조금이라도 장점이 있다면 안 할 이유가 없다. 바로 진행해라."

설계·위험 분석은 REQ-20260825-091(취소됨)의 노트에 있다. 완화책 4가지를 구현에 포함한다: (1) 중계 메시지에 "하던 것 끝내고 반영" 규율 삽입, (2) 전송 즉시 전달 확인 줄 + 완료 시 보고 표시, (3) 대상 생존 확인·부재 시 리드 폴백, (4) 중계 지시도 REQ 노트로 감사 기록.

## Notes

### 2026-08-25T22:39:29+09:00 tdd (by nicehugepark)

TDD 시나리오 (지목 전송):
- [x] A1 정상 전송: POST /api/chat 에 agent=<agentId> 를 실으면 수신함 줄에 agent·agent_type 이 포함되고 kind=chat 은 유지된다
- [x] A2 대상 검증: 존재하지 않거나 비활성(180초 무진전) 에이전트를 지목하면 4xx + 사유를 돌려주고 수신함에 줄을 쓰지 않는다 — 죽은 에이전트에게 말 걸기 방지
- [x] A3 리드 폴백: agent 필드가 없으면 기존과 동일하게 리드로 간다(회귀)
- [x] A4 중계 규율: 세션 훅이 주입하는 수신함 안내에 "agent 필드가 있으면 답하지 말고 SendMessage로 중계", "하던 작업을 마치고 반영하라는 규율을 붙일 것", "대상 부재 시 사실을 알리고 리드 처리 여부를 물을 것", "중계 지시도 REQ 노트로 남길 것"이 포함된다
- [x] A5 감사: 지목 전송도 chat_audit 경로를 지나 REQ 에 기록된다(무기록 통로가 생기지 않는다)
- [x] A6 화면 계약: 입력줄에 전송 대상 칩(→ 타입)과 해제 컨트롤이 있고, 대상이 사라지면 칩이 자동 해제된다
- [x] A7 회귀: 에이전트 열람(termAgentOpen)과 전송 대상 선택이 서로를 깨지 않는다

### 2026-08-26T00:22:43+09:00 staff-engineer (by nicehugepark)

A5 실패 원인 확정 (재현 최소화 완료).

재현: `python3 -m unittest tests.test_agent_relay.TestAgentRelay.test_a5_audited` 단독 → ok. `... test_a4 test_a5` 2건 연속 → a5가 409 agent-unavailable. 즉 a5 자체 결함이 아니라 a4가 남긴 상태에 의해 깨진다.

원인: A4가 세션 훅(s9-audit-session start)을 돌려 새 세션 바인딩을 만든다. sid 없는 /api/chat 은 chat_target(None) 으로 '가장 최근 활동 라이브 세션'을 고르므로 그 훅 세션이 채팅 대상으로 뽑히고, 거기엔 agentlive 가 없어 부재 판정이 난다.

이건 테스트 격리 문제가 아니라 계약 결함이다: agent id 는 세션마다 따로 나는 이름인데 대상 세션 해석과 agent 해석이 분리돼 있다. 게다가 종전 코드는 chat_target 을 세 번(agent 조회·audit·chat_send) 따로 호출해 서로 다른 세션을 고를 수 있었다. 실제 사고 시나리오: 워커가 스폰돼 채팅 대상을 가로챈 순간(REQ-010/012 기록된 실사고), 사용자가 지목한 멀쩡한 에이전트가 '종료됐다'로 반려된다.

수리: agent 가 지목되면 그 agent 를 소유한 세션이 곧 대상 — 단일 해석(chat_agent_target)으로 묶고 audit·chat_send 가 같은 세션을 쓰게 했다.

### 2026-08-26T00:23:08+09:00 response (by nicehugepark)

구현 완료 — 지목 전송 7/7 green.

### 서버 (bin/s9)
`chat_agent_target(sid8, want)` 신설: 지목 전송의 대상 세션과 에이전트를 **한 번에** 해석한다. 기본 채팅 대상에 그 에이전트가 없고 sid 지정도 없으면, 살아있는 세션을 최근 활동순으로 훑어 그 에이전트를 가진 세션을 대상으로 삼는다. /api/chat 핸들러는 해석 결과를 sid_arg 로 고정해 audit(chat_audit)와 수신함(chat_send)이 같은 세션을 쓰게 했다 — 종전엔 chat_target 을 세 번 따로 불러 갈릴 수 있었다.
효과: 워커 스폰·훅 기동이 채팅 대상을 가로챈 순간에도 지목한 에이전트가 '종료됨'으로 반려되지 않는다.

### 화면 (web/index.html, 지목 전송 영역만)
- 스트립 각 행에 `→` 지목 버튼(.agtgt). 누르면 대상 지정/토글, 색은 면이 아니라 화살표·타입 글자에만.
- 입력줄 위 대상 칩(.cctarget): `→ <타입> <id> 에게 전송` + `× 해제`(#termTargetClear).
- 전송 시 로컬 에코에 `→ 타입` 표시, 응답에 agent 가 실리면 `→ … 에게 전달 — 리드가 중계한다` 확인 줄.
- 409 agent-unavailable 이면 지목만 자동 해제하고 사유 문구를 그대로 노출 — 같은 입력을 리드에게 바로 재전송 가능(입력 복원 경로 유지).
- 활성 목록에서 대상이 사라지면 자동 해제(유령 칩 방지).

### 실행 근거
`python3 tests/ agent_relay` → 7/7 OK. 인접 회귀 `python3 tests/ dashboard_chat` → 24/24 OK.

### 한계 (미검증)
A6/A7 은 정적 문자열·핸들러 순서 검증이다. 실제 클릭 동작·칩 렌더는 브라우저 검증이 필요한데 포트 규율상 캡처하지 않았다 — 화면 확인은 리드/designer 몫으로 남긴다.

### 2026-08-26T00:23:08+09:00 tdd (by nicehugepark)

TDD 시나리오 (지목 전송) — 전부 검증 완료, `python3 tests/ agent_relay` 7/7 OK

- [x] A1 정상 전송: POST /api/chat 에 agent=<agentId> 를 실으면 수신함 줄에 agent·agent_type 이 포함되고 kind=chat 은 유지된다 — test_a1_targeted_line
- [x] A2 대상 검증: 존재하지 않거나 비활성(180초 무진전) 에이전트를 지목하면 4xx + 사유를 돌려주고 수신함에 줄을 쓰지 않는다 — test_a2_unavailable_refused
- [x] A3 리드 폴백: agent 필드가 없으면 기존과 동일하게 리드로 간다(회귀) — test_a3_plain_message_regression
- [x] A4 중계 규율: 세션 훅 안내에 중계·진행중 작업 보호·대상 부재·감사 기록 4항목이 포함된다 — test_a4_hook_relay_directive
- [x] A5 감사: 지목 전송도 chat_audit 경로를 지나 REQ 에 기록된다 — test_a5_audited
- [x] A6 화면 계약: 입력줄에 전송 대상 칩(.cctarget)과 해제 컨트롤(#termTargetClear)이 있고, 대상이 활성 목록에서 사라지면 칩이 자동 해제된다 — test_a6_composer_target_ui
- [x] A7 회귀: 에이전트 열람(termAgentOpen)과 전송 대상 선택이 서로를 깨지 않는다 — test_a7_target_and_viewer_coexist (신규 추가)

미검증으로 남는 것: A6/A7 은 정적 계약 검증이다. 실제 클릭·렌더 동작은 브라우저 확인이 필요한데 포트 규율상 캡처하지 않았다.

### 2026-08-26T12:52:48+09:00 response (by nicehugepark)

판정은 REQ-20260826-010-62x6 (주체 추적 축 통합 판정) 한 곳에서 받는다 — 넷이 한 축이라 따로 보면 각각은 통과인데 전체가 되는지 판단할 수 없다는 지적을 반영했다. 그 문서에 넷의 실동작 근거가 모여 있다.

### 2026-08-26T13:01:27+09:00 response (by nicehugepark)

TDD 체크리스트 A1~A7 검증 완료로 표시했다. 근거: `python3 tests/ agent_relay` 7/7 통과(각 항목이 test_a1~a7 로 1:1 대응한다). 더해서 리드가 실환경 왕복을 직접 확인했다 — 살아 있는 designer 지목 시 200 + 수신함 줄에 agent·agent_type, 중계 도달까지. 죽은 에이전트 지목 시 409 와 사유 반환.

## History
- 2026-08-25T22:39:11+09:00 created by nicehugepark (status: open)
- 2026-08-25T22:39:29+09:00 status: open -> in-progress (by nicehugepark) — 리드 직접 — 서버·훅·프론트 3면. 파일 점유(bin/s9=086 staff, web/index.html=081 designer) 때문에 훅·테스트부터 착수
- 2026-08-26T00:23:16+09:00 status: in-progress -> review (by nicehugepark) — 지목 전송 구현 완료 — python3 tests/ agent_relay 7/7. 확인 포인트 ① 스트립 에이전트 행의 → 버튼으로 대상을 걸고 보낸 메시지를 리드가 답하지 않고 SendMessage로 중계하는가(실동작은 브라우저에서만 확인 가능 — 포트 규율상 캡처하지 않았다) ② 대상 칩·해제(×)·전달 확인 줄의 시각 마감은 designer 검토 필요(색은 화살표·타입 글자에만, 색면 없음) ③ 서버 계약 변경: agent 를 지목하면 그 에이전트를 소유한 세션이 대상이 된다 — 종전엔 chat_target 을 세 번 따로 골라 세션이 갈릴 수 있었다. 이 의미 변경에 동의하는지.
- 2026-08-26T12:49:22+09:00 status: review -> in-progress (by nicehugepark) — 채팅 반려: (1) 088/089/090/095 는 사실상 하나의 축이라 따로 올리면 판단이 서지 않는다. (2) 에이전트를 지목해 프롬프트해도 여전히 동작하지 않는다. 재작업 착수 — 실동작 규명 후 하나의 판정으로 묶어 올린다.
- 2026-08-26T13:01:27+09:00 status: in-progress -> done (by nicehugepark) — 통합 판정(REQ-20260826-010-62x6) 승인으로 닫는다. TDD A1~A7 전부 검증(테스트 7/7 + 실환경 왕복 확인). "여전히 안 된다"의 원인은 구동 중이던 서버가 기능 이전 코드였던 것이고 재기동으로 해소됐다 — 재발 방지는 REQ-20260826-011-62x6.
