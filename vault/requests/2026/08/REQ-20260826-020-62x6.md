---
id: REQ-20260826-020-62x6
type: request
title: 첨부 본문 보존
summary: 추출한 첨부 본문이 태그 3개 유도 후 버려진다 — 사이드카 캐시 + 카탈로그 색인으로 보존
status: done
size: M
user: sjpark1
machine: DESKTOP-TEHV1KR
session: 8e60e4af
sessions: ["cb49b2cd", "8e60e4af"]
agents: ["sub:subagent:a8786468", "sub:backend-developer:a31006d4", "sub:subagent:abd43816", "sub:subagent:a9e2efa7"]
contributions: [{"actor": "sub:subagent:a8786468", "item": "Locating REQ-20260826-020-62x6 in s9 ls", "started": "2026-08-26T19:40:42+09:00", "ended": "2026-08-26T19:40:42+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/cb49b2cd-601c-40bd-b9c4-d0afb0576d9f/subagents/agent-a8786468397198c66.jsonl"}, {"actor": "sub:backend-developer:a31006d4", "item": "원인** — 기존 `_pdf_text` 가 PDF를 문서가 아니라 바이트 더미로 봐서 모든 스트림을 풀어 `(...)` 만 긁", "started": "2026-08-26T19:44:35+09:00", "ended": "2026-08-26T19:44:35+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/cb49b2cd-601c-40bd-b9c4-d0afb0576d9f/subagents/agent-a31006d4e632699ee.jsonl"}, {"actor": "sub:subagent:abd43816", "item": "Appending conflict note to REQ-20260826-020-62x6", "started": "2026-08-27T00:10:20+09:00", "ended": "2026-08-27T00:10:20+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/8e60e4af-6e99-4b92-857b-fe3e651a913b/subagents/agent-abd43816815600b1b.jsonl"}, {"actor": "sub:subagent:a9e2efa7", "item": "Writing response note to REQ-20260826-020-62x6", "started": "2026-08-27T00:10:52+09:00", "ended": "2026-08-27T00:10:52+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/8e60e4af-6e99-4b92-857b-fe3e651a913b/subagents/agent-a9e2efa77f5c9d539.jsonl"}]
project: section9
parent: REQ-20260826-001-62x6
children: ["REQ-20260827-005-62x6"]
relates: ["DOC-20260827-001-62x6"]
tags: ["assets", "index", "dashboard"]
created: 2026-08-26T19:40:25+09:00
status_since: 2026-08-27T00:17:20+09:00
updated: 2026-08-27T00:22:26+09:00
priority: 50
---

## Original

읽기 결함(REQ-20260826-001)을 닫고 나니 추출한 본문이 여전히 버려진다 —
`attach_tags()` 가 태그 3개를 뽑고 나면 문자열은 그대로 소멸한다. 편집·인용·검색의
전제는 본문이 남는 것이므로, 보존 위치를 정해 구현한다.

## 권고안 (backend-developer, REQ-001 작업 중 판단)

**추출 본문은 문서가 아니라 파생물이다.** 원본(첨부 파일)에서 언제든 재생성
가능하므로 문서 본문/front-matter 에 넣으면 안 된다 (eng-principles: 파생물은
캐시/인덱스에, 진실은 원본에).

- **저장**: `assets/<문서ID>/.text/<파일명>.txt` 사이드카.
  인제스트 때 1회 쓰고, `s9 assets reindex` 로 전량 재생성 가능하게 한다.
  머신 간 동기화 대상에서 뺄지(재생성 가능하므로 .gitignore) 넣을지는
  "다른 머신에서 재추출 비용 vs 리포 크기" 로 판단 — 1MB PDF 추출이 0.32s 이므로
  **제외(재생성)** 를 권한다.
- **검색**: `catalog.jsonl` 행에 추출 본문 앞부분(예: 2000자)을 실어
  `s9 search --body` 가 첨부 내용까지 찾게 한다. 카탈로그는 이미 재생성 가능한
  인덱스라 스키마 확장 비용이 가장 싸다.
- **표시**: 대시보드 첨부 카드에 발췌 몇 줄. (별건)

## 하지 말 것
- 문서 본문에 추출 텍스트를 append — 사용자가 쓴 글과 기계가 뽑은 글이 섞이고,
  재추출할 때마다 diff 가 요동친다.
- front-matter 에 통째로 — 문서 헤더가 수 KB 로 부풀고 fm 파싱 비용이 는다.

## 완료 조건
- 첨부 인제스트 후 사이드카가 생기고, `s9 search <첨부 안 문구> --body` 로 문서가 잡힌다.
- 사이드카를 지우고 reindex 하면 동일 결과가 복구된다(재생성 가능성 증명).

## Notes

### 2026-08-26T19:40:42+09:00 subagent (agent: sub:subagent:a8786468, by sjpark1)

Locating REQ-20260826-020-62x6 in s9 ls

### 2026-08-26T19:44:35+09:00 subagent (agent: sub:backend-developer:a31006d4, by sjpark1)

**원인** — 기존 `_pdf_text` 가 PDF를 문서가 아니라 바이트 더미로 봐서 모든 스트림을 풀어 `(...)` 만 긁었고, 실물 한글 PDF는 예외 없이 Identity-H hex + ObjStm 안의 폰트 딕셔너리라 glyph id → 유니코드로 가는 길(페이지→리소스→폰트→ToUnicode)이 애초에 없었다 — 원리적으로 0바이트.

**수정** — `/home/sjpark1/section9/bin/s9` 의 추출 영역을 객체 구조를 따라가는 추출기로 교체(`_pdf_scan`·`_pdf_expand_objstm`·`_pdf_stream`(/Length 우선 + zlib 검증 절단)·`_pdf_tounicode`·`_pdf_ttf_reverse`·`_pdf_tokens`·`_pdf_page_text`), 8MB 하드코딩을 `ATTACH_MAX_BYTES` 로, 매핑 없는 CID는 버려 폰트·이미지 바이너리 유출을 차단 — stdlib(re·zlib·struct)만 사용.

**남은 한계** — 스캔 PDF(OCR)와 암호화 PDF는 여전히 0자이며 이는 DOC-20260826-003-62x6 C등급 경계 그대로다. 실물 스캔본 표본이 이 머신에 없어 합성 이미지 전용 PDF로 대체했고, 추가 요구인 추출 텍스트 보존은 REQ-20260826-020-62x6 으로 분리해 REQ 문서는 `review` 로 올렸다.

검증: `/home/sjpark1/section9/tests/test_pdf_text.py` 12/12 green(수정 전 리비전 `S9_PDF_BIN` 으로 물리면 9 fail로 red 재현), `tests/ assets` 28/28, 실물 한글 PDF 한글 0 → 4,341자 0.32s. 실물 픽스처는 `/home/sjpark1/section9/tests/fixtures/web_print_ko.pdf`.

### 2026-08-27T00:01:16+09:00 tdd (by sjpark1)

### TDD 시나리오 (첨부 본문 보존)

### 설계 결정 — 권고안에서 갈라지는 지점
권고안의 "카탈로그 행에 앞 2000자를 실어 검색 확장"은 **채택하지 않는다.**
근거: `index/catalog.jsonl` 은 대시보드가 `/api/catalog` 로 **전량**을 폴링마다
내려받는 인덱스다. 현재 366행 267KB(행당 ~730B). 첨부 문서는 13건이고 여기에
2000자(한글 UTF-8 최대 6KB)를 실으면 카탈로그가 +78KB(+29%) — 첨부가 늘수록 선형
증가한다. 검색은 가끔이고 보드 폴링은 상시인데, 상시 비용에 가끔 쓰는 데이터를
싣는 건 방향이 반대다. 게다가 2001자째 문구는 영원히 검색되지 않는다.

대신 **사이드카를 검색이 직접 읽는다**. `search --body` 는 이미 문서마다 파일을
여는 경로라, 첨부가 있는 문서에 한해 사이드카를 추가로 읽는 비용만 든다. 검색이
잘린 발췌가 아니라 **전문**을 본다. 카탈로그 스키마는 손대지 않으므로 재생성
경로도 무변경(회귀 위험 0).

권고안 중 채택: 사이드카 위치 `assets/<문서ID>/.text/<파일명>.txt`, 재생성 가능성,
git 제외. (git 제외는 이 리포에서 이미 충족 — `vault/` 자체가 .gitignore 대상이고
첨부는 `vault/**/assets/` 아래다. 추가 조치 불필요.)

### 시나리오
- [ ] S1. 인제스트 → 사이드카: 텍스트/PDF 첨부를 `s9 assets ingest <id>` 하면
      `assets/<id>/.text/<파일명>.txt` 가 생기고 내용이 추출 본문과 일치한다.
- [ ] S2. 검색 확장: 첨부 안에만 있는 문구로 `s9 search <문구> --body` 하면 그 문서가
      결과에 나오고, 매치 라인이 어느 첨부에서 나왔는지 파일명을 함께 보여준다.
- [ ] S3. 재생성: `.text/` 를 통째로 지우고 `s9 assets reindex` 하면 같은 사이드카가
      복구되고 S2 검색이 다시 성공한다 (파생물 증명).
- [ ] S4. 회귀 — 기존 검색 불변: 문서 본문 문구 검색 결과가 도입 전후 동일하고,
      첨부 없는 문서도 이전과 같이 잡힌다.
- [ ] S5. 회귀 — 카탈로그 불변: `s9 index rebuild` 후 catalog 행의 키 집합이
      변하지 않고 파일이 커지지 않는다.
- [ ] S6. 경계 — 0자 추출: 추출이 빈 문자열인 첨부(이미지/암호화 PDF·바이너리)는
      사이드카를 만들지 않는다 (빈 파일로 디렉토리를 더럽히지 않는다).
- [ ] S7. 경계 — 이름 충돌·유니코드: 한글 파일명이 보존되고, `a.pdf` 와 `a.txt` 가
      각각 `a.pdf.txt` / `a.txt.txt` 로 서로 덮어쓰지 않는다.
- [ ] S8. 경계 — 상한: 상한(ATTACH_TEXT_MAX)을 넘는 텍스트는 잘려 저장되고
      호출이 정상 종료한다.
- [ ] S9. 실패 격리: 첨부 하나의 추출이 실패해도 인제스트 전체(파일 이전·태깅)는
      완료된다.
- [ ] S10. 서버 동치: 대시보드 `/api/search` 도 CLI 와 같은 의미론으로 첨부 본문을
      찾는다 (검색 경로가 둘로 갈라지지 않는다).
- [ ] S11. 위생: `.text` 가 첨부 다운로드 경로로 새지 않는다
      (`/api/asset?f=.text` 는 404).

### 2026-08-27T00:09:52+09:00 conflict (by sjpark1)

권고안 3항 "표시: 대시보드 첨부 카드에 발췌 몇 줄" 은 이번 범위에서 **하지 않았다** —
`web/index.html` 은 designer 가 잡고 있어 손대지 말라는 지시를 받았다.

화면에 붙이려면 필요한 것(서버 쪽은 이미 준비됨):
1. `/api/search` 결과의 `matches[]` 에 첨부에서 나온 줄이면 `"file": "<첨부 파일명>"`
   키가 붙는다. 지금 화면은 이 키를 무시하므로, 첨부 줄에 파일명 배지를 붙이는 건
   프런트 한 줄이면 된다 (기존 렌더는 그대로 동작 — 추가 키라 회귀 없음).
2. 카드/문서 뷰에 발췌를 띄우려면 라우트가 하나 더 필요하다:
   `/api/asset-text?doc=<문서ID>&f=<첨부 파일명>` → 사이드카 텍스트.
   서버 쪽 읽기 함수 `asset_texts(doc_path, doc_id)` 가 이미 있어 라우트 추가는
   10줄 수준이지만, 문서 가시성(`doc_visible`) 게이트를 `/api/asset` 과 똑같이
   걸어야 한다 — 그러지 않으면 첨부 본문이 열람 권한을 우회한다.

리드 판단 요청: 위 2번 라우트를 지금 만들지(백엔드 몫), designer 작업이 끝난 뒤
별건 REQ로 뺄지.

### 2026-08-27T00:10:20+09:00 subagent (agent: sub:subagent:abd43816, by sjpark1)

Appending conflict note to REQ-20260826-020-62x6

### 2026-08-27T00:10:35+09:00 response (by sjpark1)

### 결론
첨부에서 뽑은 본문이 사이드카 `assets/<문서ID>/.text/<파일명>.txt` 로 남고,
`s9 search <문구> --body` 가 그 본문까지 찾는다. 사이드카는 파생물이라
`s9 assets reindex` 로 언제든 전량 재생성된다. **카탈로그 스키마·크기는 무변경**
(재생성 후 263,971 바이트 → 263,971 바이트, 동일).

### 권고안에서 갈라진 지점 — 카탈로그에 2000자를 싣지 않았다
권고안은 `catalog.jsonl` 행에 앞 2000자를 실어 검색을 확장하자고 했다. 채택하지
않았다.
- `catalog.jsonl` 은 대시보드가 `/api/catalog` 로 **폴링마다 전량**을 내려받는
  인덱스다(366행 264KB, 행당 ~730B). 여기에 2000자(한글이면 UTF-8 최대 6KB)를
  얹으면 상시 전송량이 첨부 수에 비례해 늘어난다. **검색은 가끔이고 폴링은
  상시** — 상시 비용에 가끔 쓰는 데이터를 싣는 건 방향이 반대다.
- 잘린 발췌는 2001자째 문구를 영원히 못 찾게 만든다. 사이드카를 직접 읽으면
  검색이 **전문**을 본다.
- 카탈로그를 안 건드리니 `catalog_row`·`rebuild_index` 회귀 위험이 0이다.
대신 `search --body` 가 문서 파일을 여는 그 자리에서 사이드카도 읽는다.
비용 측정: 문서 366건 전수 검색 0.121s (사이드카 없는 문서는 `listdir` 실패 한 번).

권고안 중 그대로 채택: 사이드카 위치, 재생성 가능성, git 제외.
git 제외는 **이 리포에서 이미 충족** — `vault/` 자체가 `.gitignore` 대상이고 첨부는
`vault/**/assets/` 아래다. `.gitignore` 추가 조치 불필요.

### 구현 (`bin/s9`)
- `ATTACH_TEXT_MAX = 200_000` 자 / `ATTACH_TEXT_DIR = ".text"`.
  태깅용 8000자와 분리한 이유: 태깅은 '주제 판별', 사이드카는 '보존'이라 목적이
  다르다. 200,000자는 산문 400쪽 남짓이고, 실물 PDF 픽스처 추출은 상한 8000이든
  200,000이든 0.004s 로 차이가 없다(추출 비용은 상한이 아니라 파일 크기가 정한다).
- `store_asset_text()` — 추출 1회 → tmp+`os.replace` 원자적 쓰기. **0자 추출이면
  파일을 만들지 않는다**(빈 사이드카는 '글자가 없다'와 '아직 안 했다'를 구별
  불가능하게 한다). 추출 예외는 삼키고 ""를 반환 — 첨부 하나가 인제스트 전체를
  세우면 안 된다.
- `asset_texts(doc_path, doc_id)` — 검색이 읽는 **단일 경로**. CLI 와 대시보드가
  같은 함수를 쓴다(같은 질의가 화면과 터미널에서 다른 답을 주지 않게).
- `reindex_asset_text()` + `s9 assets reindex [<문서ID>]` — 재생성. 원본이 사라진
  고아 사이드카는 지운다(파생물이 원본보다 오래 살면 검색이 없는 첨부를 찾아준다).
- `attach_tags(paths, text_of=None)` — **추출을 두 번 하던 것을 한 번으로.**
  예전엔 태그 3개만 뽑고 텍스트를 버렸고, 사이드카를 따로 추출하면 30MB PDF를
  두 번 파싱하며 태그와 사이드카가 서로 다른 세대를 볼 수 있었다. 태깅 표본은
  예전과 같이 8000자로 잘라 REQ-054 태깅 계약을 그대로 유지한다.
- `cmd_search` / 서버 `search_data` — [문서 본문] + [첨부별 본문] 세그먼트로 매치.
  첨부에서 나온 줄은 CLI 가 `REQ-...:web_print.pdf:1:` 로, API 는 `matches[].file`
  키로 출처를 밝힌다. 첨부 없는 문서는 세그먼트가 하나뿐이라 **동작이 이전과 동일**.

### TDD 시나리오 결과 — 12/12 green
- [x] S1 인제스트 → 사이드카   - [x] S1b 실물 한글 PDF 사이드카
- [x] S2 첨부 본문 검색(+출처 파일명)   - [x] S3 삭제 후 reindex 복구
- [x] S4 회귀: 문서 본문 검색 불변   - [x] S5 회귀: 카탈로그에 본문 미탑재
- [x] S6 0자 추출은 빈 파일 안 만듦   - [x] S7 이름 충돌·한글 파일명
- [x] S8 상한 절단   - [x] S9 첨부 하나 실패해도 인제스트 완주
- [x] S10 대시보드 검색 동치   - [x] S11 `.text` 가 첨부 다운로드로 안 샘(404)

### 실행 검증
- `python3 tests/ asset_text` → 12/12 OK (구현 전 8 fail 로 red 확인)
- `python3 tests/` **전체 672 tests OK** (skipped=1)
- 실물 vault: `s9 assets reindex` → 문서 1건·파일 1개(첨부 19개 중 18개가 PNG라
  추출 0자 = 정상). `s9 search 파이프라인 --body` 결과에
  `REQ-20260826-001-62x6:web_print.pdf:1: 배포 파이프라인 점검 보고서 …` 등장.
- 재생성 왕복(실물): `.text` 삭제 → 검색 히트 0 → `s9 assets reindex <id>` →
  히트 1. **완료 조건 두 개 모두 실물에서 충족.**

### 한계 / 미검증
- 스캔 PDF(OCR)·암호화 PDF는 여전히 추출 0자 → 사이드카 없음
  (DOC-20260826-003-62x6 C등급 경계 그대로. 이 REQ가 바꾸는 지점이 아니다).
- 첨부를 **문서 바깥에서** 교체하면(파일만 덮어쓰기) 사이드카가 낡는다.
  갱신 트리거는 인제스트와 `reindex` 둘뿐 — 자동 감시는 넣지 않았다(파일 감시
  데몬을 하나 더 다는 비용이 이득보다 크다). 첨부를 갈아끼웠으면 reindex 한 번.
- 대시보드 화면 표시(권고안 3항)는 범위 밖 — `conflict` 라벨 노트 참조.

### 2026-08-27T00:10:52+09:00 subagent (agent: sub:subagent:a9e2efa7, by sjpark1)

Writing response note to REQ-20260826-020-62x6

### 2026-08-27T00:11:37+09:00 tdd (by sjpark1)

### TDD 시나리오 — 전량 통과 (12/12)
검증: `python3 tests/ asset_text` 12/12 OK · 전체 `python3 tests/` 672 tests OK.
테스트 파일: `/home/sjpark1/section9/tests/test_asset_text.py`

- [x] S1. 인제스트 → 사이드카 (`assets/<id>/.text/<파일명>.txt`)
- [x] S1b. 실물 한글 PDF 픽스처의 추출본이 사이드카에 남고 검색된다
- [x] S2. 첨부 안에만 있는 문구로 `search --body` 히트 + 출처 파일명 표시
- [x] S3. `.text` 삭제 → 검색 0 → `s9 assets reindex` → 복구 (실물 vault 에서도 확인)
- [x] S4. 회귀: 문서 본문 검색 결과·라인번호 표기 불변
- [x] S5. 회귀: 카탈로그 행에 첨부 본문 미탑재 (재생성 후 263,971B → 263,971B 동일)
- [x] S6. 0자 추출(PNG 등)은 빈 사이드카를 만들지 않는다
- [x] S7. 한글 파일명 보존, `같은이름.txt`/`같은이름.md` 가 서로 덮어쓰지 않는다
- [x] S8. `ATTACH_TEXT_MAX`(200,000자) 상한에서 절단 후 정상 종료
- [x] S9. 첨부 하나의 추출 실패에도 파일 이전·태깅·문서 쓰기 완주
- [x] S10. 대시보드 `/api/search` 도 같은 사이드카를 읽는다 (경로 단일화)
- [x] S11. `.text` 가 `/api/asset` 다운로드 경로로 새지 않는다 (404)

### 2026-08-27T00:13:57+09:00 decision (by sjpark1)

리드 판단: 화면 노출(권고안 3항)은 REQ-20260827-005-62x6 로 갈랐다. 020 의 목표는 '본문이 버려지지 않는다'였고 충족됐다 — 검색이 첨부 본문을 전문으로 찾는다. 화면은 봉투가 다르고(designer), 지금 designer 는 039 재작업과 018 을 잡고 있다. 카탈로그에 발췌를 싣지 않은 판단도 지지한다: 상시 폴링 비용에 가끔 쓰는 데이터를 싣는 건 방향이 반대고, 잘린 발췌는 2001자째를 영영 못 찾게 만든다 — 전문을 그 자리에서 읽는 쪽이 옳다.

## History
- 2026-08-26T19:40:25+09:00 created by sjpark1 (status: open)
- 2026-08-26T23:49:24+09:00 status: open -> in-progress (by sjpark1) — drag 이동 [via dashboard]
- 2026-08-27T00:11:47+09:00 status: in-progress -> review (by sjpark1) — 첨부 파일 안의 문장을 s9 search 로 찾을 수 있게 됐다. 확인 절차: (1) 터미널에서 `s9 search 파이프라인 --body` 를 치면 결과에 `REQ-20260826-001-62x6:web_print.pdf:1: 배포 파이프라인 점검 보고서 …` 처럼 PDF 파일명과 그 안의 문장이 함께 나온다 — 예전에는 첨부 안 문장으로는 아무것도 안 나왔다. (2) 첨부한 파일의 글은 문서 옆 assets/<문서번호>/.text/ 아래 .txt 로 따로 보관된다. 이 폴더를 통째로 지운 뒤 `s9 assets reindex` 를 돌리면 원래대로 복구되고 (1)의 검색이 다시 된다 — 원본 첨부만 있으면 언제든 다시 만든다는 뜻. (3) 대시보드 검색창에 같은 낱말을 넣어도 같은 문서가 나온다(F5 후 확인). 화면 카드에 첨부 발췌를 띄우는 일은 이번에 하지 않았다 — 화면 파일을 디자이너가 작업 중이라, 서버 쪽 준비만 해 두고 conflict 노트에 남겼다.
- 2026-08-27T00:17:20+09:00 status: review -> done (by sjpark1) — [via dashboard]
