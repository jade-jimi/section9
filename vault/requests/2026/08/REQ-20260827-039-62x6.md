---
id: REQ-20260827-039-62x6
type: request
title: 미러가 매 턴 전부를 다시 쓴다
summary: Stop 훅이 transcript 전체를 매 턴 복사한다 — 이 세션은 턴당 7.7MB, 오늘 누적 7GB 규모
goal: transcript 미러가 새로 늘어난 부분만 쓰고, 압축·되감김 때는 안전하게 전체 복사로 물러난다
status: done
size: M
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: 3da5b9de
project: section9
relates_why: {}
tags: ["access", "audit", "session"]
created: 2026-08-27T18:30:27+09:00
status_since: 2026-08-27T18:37:05+09:00
updated: 2026-08-27T18:37:05+09:00
priority: 50
---

## Original

사용자 질문: "지금 우리 하네스 시스템에서 stream이 가치가 있나? 없어도 되는 수준인가?" — 측정하다 발견.

## Notes

### 2026-08-27T18:30:49+09:00 tdd (by nicehugepark)

TDD 시나리오 (실행: python3 tests/ mirror_append)

**발견**: `mirror_transcript()` 가 `shutil.copyfile` 로 **매 턴 전체를 다시 쓴다.**
이 세션은 지금 7.7MB 라 한 턴당 7.7MB 를 다시 쓰고, 세션이 자랄수록 턴당 비용이
커진다(누적은 대략 제곱). 오늘 이 세션만으로 7GB 규모다. 미러 50개 156MB 중
**원본이 사라져 미러만 남은 것은 1개(0MB)** — 즉 거의 전부가 옆에 있는 파일의 복사본이다.

미러 자체는 없앨 게 아니다. Claude Code 가 자기 기록을 지우면 그때 남는 건
미러뿐이고(이미 1건 있었다), 대시보드가 `~/.claude` 배치에 기대지 않게 해준다
(계정을 바꾸면 그 배치가 움직인다). 고칠 것은 **값을 치르는 방식**이다.

정상
  N1. 미러가 없으면 전체를 쓴다 (첫 턴).
  N2. 원본이 뒤에 자랐으면 **늘어난 부분만** 이어 쓴다 — 결과 파일 내용은
      전체 복사와 **같아야** 한다.
  N3. 늘어난 것이 없으면 아무것도 쓰지 않는다.

경계
  B1. 원본이 미러보다 **작아졌으면**(압축·되감김) 전체 복사로 물러난다 —
      이어 쓰면 미러가 원본과 다른 파일이 된다.
  B2. 앞부분이 달라졌으면(같은 길이라도 내용이 바뀜) 전체 복사로 물러난다.
      길이만 보면 되감김을 놓친다.

실패
  F1. 원본이 없거나 못 읽으면 조용히 물러난다 — 미러 실패가 응답 캡처를 막으면 안 된다.

회귀
  R1. 미러 경로·파일명은 그대로다(전체SID.jsonl) — resume 이 그 이름을 찾는다.

### 2026-08-27T18:37:05+09:00 response (by nicehugepark)

**보험은 유지하고, 보험료만 차액으로 바꿨다.**

**발견 경위**: "stream 이 가치가 있나"를 재다 나왔다. Stop 훅이 `shutil.copyfile` 로
**매 턴 transcript 전체를 다시 쓰고 있었다.**

    이 세션 7.7MB → 한 턴에 7.7MB 재기록 · 오늘 이 세션만 누적 7GB 규모
    미러 50개 156MB · 큰 것은 32MB·27MB

**그래도 없애지 않은 이유 셋** (측정으로 확인한 것)
1. Claude Code 는 자기 기록을 지운다 — 미러 50개 중 **원본이 사라져 미러만 남은 것이
   이미 1건** 있다. 원본은 영원하지 않다.
2. 대시보드가 `~/.claude` 배치에 기대지 않게 해준다 — 오늘 확인했듯 **계정을 바꾸면
   그 배치가 통째로 움직인다**(REQ-20260827-032-62x6).
3. 사설 리포가 생기면 다른 머신에서 기록을 보는 유일한 길이다(docs/08).

**고침**: 늘어난 만큼만 이어 쓴다. 결과 파일은 전체 복사와 **같고**, 안 늘었으면 아예
안 쓴다.

**함정과 대응**: transcript 는 **압축(compact)으로 줄거나 다시 쓰인다.** 그때 뒤에만
붙이면 미러는 원본과 다른 파일이 된다. 그래서 길이만 보지 않고 **앞머리 4KB 가
그대로인지도** 본다 — 아니면 전체 복사로 물러난다. 길이만 보면 되감김을 놓친다.

**테스트 설계 하나**: 판단을 `"full"/"append"/"skip"` 반환값으로 내놓았다.
**결과 파일만 보면 전체 복사와 이어쓰기를 구별할 수 없어서** 계약이 장식이 된다.

**남은 것 (별건 후보)**: 미러 보존 기간이 없다. 156MB 는 계속 는다. 오래된 미러를
줄이거나(도구 결과 본문 제외) 기간을 두는 것은 따로 판단할 일이다.

tests/test_mirror_append.py 7건 · 전체 959건 OK. 커밋 02589d9.

## History
- 2026-08-27T18:30:27+09:00 created by nicehugepark (status: open)
- 2026-08-27T18:30:49+09:00 status: open -> in-progress (by nicehugepark) — 리드 착수 — stream 가치 질문을 재다 발견한 실제 비용
- 2026-08-27T18:37:05+09:00 status: in-progress -> done (by nicehugepark) — goal 충족: 미러가 늘어난 부분만 쓰고, 압축·되감김이면 앞머리 4KB 비교로 알아채 전체 복사로 물러난다. 판단을 반환값으로 노출해 계약이 실제 동작을 본다(결과 파일만으로는 구별 불가). tests/test_mirror_append.py 7건 + 전체 959건 OK.
