---
id: REQ-20260827-027-62x6
type: request
title: 일 안 하는 요청이 초록이다
summary: 한 세션이 등록한 REQ 전부가 그 세션의 활동 하나로 함께 초록 점멸한다 — 아무도 붙어 있지 않은 요청까지 작업 중으로 보인다
goal: 한 세션의 활동은 그 세션이 가장 최근에 클레임한 요청 하나에만 직접 증거(초록)가 되고, 나머지는 간접 표시로 내려간다
status: done
size: M
user: sjpark1
machine: DESKTOP-TEHV1KR
session: 3da5b9de
project: section9
parent: REQ-20260827-023-62x6
relates: ["DOC-20260827-003-62x6"]
relates_why: {"DOC-20260827-003-62x6": "이 요청을 고치며 배운 것을 이 지식 문서로 남겼다"}
tags: ["session", "dashboard", "terminal"]
created: 2026-08-27T11:28:40+09:00
status_since: 2026-08-27T11:39:44+09:00
updated: 2026-08-27T16:51:59+09:00
priority: 50
---

## Original

모든 터미널에서 에이전트가 작업하지 않는데 대시보드는 여전히 active 하게 일하는 요청들이 있는 것으로 표시된다. 이 상태가 맞는건지 의도된건지 확인하고 각자 조치해달라.

## Notes

### 2026-08-27T11:30:47+09:00 tdd (by sjpark1)

TDD 시나리오 (실행: python3 tests/ live_worker_scope)

무엇이 일어났나 — 무인 워커는 **죽은 세션의 id 를 물려받아** 뜬다(`--resume`,
컨텍스트 승계가 목적). 그런데 그 세션의 바인딩에는 죽기 전 리드가 등록해 둔
REQ 가 통째로 남아 있다. 워커가 019 하나를 재작업하러 되살아나자, 그 바인딩의
활동 하나가 **019 와 022 를 함께 초록으로 점멸시켰다.** 022 에는 아무도 없었다.

판단 — 병행 REQ 를 다 초록으로 켜는 것 자체는 의도된 설계다(REQ-20260823-079):
리드는 실제로 여러 건을 오간다. 하지만 **워커는 정의상 한 건짜리다.** 스폰될 때
대상 REQ 하나를 받고 그 한 턴만 돌고 죽는다. 그러니 워커 바인딩의 활동은 그
한 건에만 직접 증거여야 한다.

누가 그 한 건인지는 추측하지 않는다 — 스폰 기록(`state/auto_resume/<REQ>.json`)에
그 워커의 pid 가 적혀 있고, 바인딩의 attach_pid 와 맞춰 보면 정확히 하나로 좁혀진다.

정상
  N1. 워커 바인딩(worker=1, attach_pid=P)이 살아 있고 스폰 기록이 P→REQ-X 이면
      REQ-X 만 live=true(직접).
  N2. 같은 바인딩에 남아 있던 다른 REQ-Y 는 live=false 이고 live_kind='session'
      — '담당 세션은 활동 중이나 이 요청의 직접 신호는 없음'.

경계
  B1. 워커가 아닌 보통 세션(worker 없음)은 예전 그대로 — 등록한 REQ 전부 직접.
  B2. 워커인데 pid 를 어느 스폰 기록과도 맞출 수 없으면(신규 세션으로 뜬 워커 등)
      좁히지 않는다 — 모르는 것으로 표시를 지우지 않는다.

실패
  F1. 워커가 죽은 뒤(pid 없음)에는 그 활동으로 아무것도 초록이 되지 않는다.

회귀
  R1. tests/test_live_catalog.py 의 병행 직접 증거(A·F·J·N)가 그대로 통과한다.
  R2. rework_claimed / chat_target 은 binding_req_ids 를 계속 예전 정의로 쓴다
      — 클레임 판정을 좁히면 워처가 중복 스폰한다.

### 2026-08-27T11:39:44+09:00 response (by sjpark1)

**아무도 안 붙어 있는 요청이 초록으로 점멸했다 — 맞는 상태가 아니었다.**

사용자가 본 것: 모든 터미널에서 에이전트가 놀고 있는데 보드의 022 가 초록 점멸.
확인해 보니 실제로 그랬다 — API 가 019·022 **둘 다** `live=direct, age=3` 로 냈다.
019 에는 워커가 붙어 있었지만 022 에는 아무도 없었다.

**왜**: 무인 워커는 죽은 세션의 id 를 물려받아 뜬다(`--resume` — 컨텍스트 승계가
목적이고, 그건 의도된 설계다). 그런데 그 세션의 바인딩에는 죽기 전 리드가 등록해
둔 REQ 가 통째로 남아 있다. 이 경우 바인딩 8e60e4af 의 `active_reqs` 가
`[019, 022]` 였고, 워커가 019 하나를 재작업하러 되살아나자 **그 바인딩의 활동
하나**가 둘을 함께 켰다.

**판단**: 병행 REQ 를 다 켜는 것 자체는 고치지 않는다 — 리드는 실제로 여러 건을
오가고, 그건 REQ-20260823-079 에서 의도해 만든 표시다. **워커만 다르다**: 정의상
대상 REQ 하나를 받고 한 턴 돌고 죽는다. 그러니 워커 바인딩의 활동은 그 한 건에만
직접 증거여야 한다.

누가 그 한 건인지는 추측하지 않는다. 스폰할 때 `_auto_mark_pid` 가
`state/auto_resume/<REQ>.json` 에 그 프로세스의 pid 를 적어 두므로, 바인딩의
`attach_pid` 와 맞추면 정확히 하나로 좁혀진다. **못 맞추면 좁히지 않는다** —
모르는 것으로 표시를 지우지 않는다.

밀려난 요청은 사라지지 않고 간접(`live_kind=session`, 속 빈 고리)으로 내려간다.
'담당 세션은 활동 중이나 이 요청의 직접 신호는 없음' — 화면에 이미 있던 어휘다.

**건드리지 않은 것**: `binding_req_ids` 자체는 그대로다. 클레임 판정
(`rework_claimed`)·채팅 대상 고르기가 같은 정의를 쓰는데, 거기서 좁히면 워처가
같은 REQ 에 워커를 중복 스폰한다.

**검증**: tests/test_live_worker_scope.py 4건 (N1 워커 대상만 direct · N2 남겨진
건은 session · B1 보통 세션 병행 표시 불변 · B2 pid 못 맞추면 안 좁힘). 기존
tests/test_live_catalog.py 병행 직접 증거 계약 전부 통과. 전체 847건 OK.

**실환경 확인**: 서버 재기동 후 022 는 `live=False` (회색 — '실제 동작 아닐 수
있음'), 이 세션이 실제로 잡고 있는 023·025·027 만 `direct` 로 남았다.

## History
- 2026-08-27T11:28:40+09:00 created by sjpark1 (status: open)
- 2026-08-27T11:30:26+09:00 status: open -> in-progress (by sjpark1) — 리드 착수 — 원인 확인: 무인 워커가 죽은 리드 세션 id 로 resume 하면서 그 바인딩의 active_reqs 전부(019·022)를 되살렸다
- 2026-08-27T11:39:44+09:00 status: in-progress -> done (by sjpark1) — goal(워커 활동은 한 건에만 직접 증거) 충족: 워커 바인딩을 스폰 기록 pid 로 대상 REQ 하나에 묶었다. 실환경에서 022 가 초록→회색으로 정정됨을 API 로 확인. tests/test_live_worker_scope.py 4건 + 기존 live_catalog 계약 + 전체 847건 OK.
