---
id: REQ-20260829-039-62x6
type: request
title: 조각 서른아홉이 벼랑을 만난다
summary: 화면을 조각으로 가르자 한 번의 F5 가 동시 요청 39개가 됐고, 루프백 벼랑에서 상당수가 잘려 화면이 깨진다
goal: F5 한 번에 화면이 온전히 서고, 조각이 잘려도 스스로 되찾는다
status: done
size: M
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: 1a1d44d9
sessions: ["911949e0", "1a1d44d9"]
agents: ["sub:backend-developer:ab8077dd", "lead:claude-opus-5"]
contributions: [{"actor": "sub:backend-developer:ab8077dd", "item": "037·039서버·002 서버 몫", "started": "2026-08-30T00:37:25+09:00", "ended": "2026-08-30T12:01:48+09:00", "result": "done", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/911949e0-5d1b-4cd2-b08d-70fd0415a19a/tasks/ab8077ddf58a99d32.output", "reason": "기록 자리가 사라졌다 (transcript 경로가 없다)"}, {"actor": "sub:backend-developer:ab8077dd", "item": "`[2026-08-30 01:35:12 KST - backend-developer]`", "started": "2026-08-30T01:30:14+09:00", "ended": "2026-08-30T01:30:14+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/911949e0-5d1b-4cd2-b08d-70fd0415a19a/subagents/agent-ab8077ddf58a99d32.jsonl"}, {"actor": "lead:claude-opus-5", "item": "`[2026-08-30 12:02:31 KST - lead]`", "started": "2026-08-30T12:02:31+09:00", "ended": "2026-08-30T12:02:31+09:00", "result": "done", "transcript": ""}]
project: section9
relates: ["REQ-20260829-027-62x6", "REQ-20260829-019-62x6"]
relates_why: {"REQ-20260829-027-62x6": "027 이 요청 1개를 39개로 만들면서 이 표면이 생겼다 — 가른 이득은 지키되 부른 횟수를 되돌린다", "REQ-20260829-019-62x6": "같은 벼랑을 그림에서 이미 갚았다 — 재시도·백오프의 수치와 근거를 그대로 쓴다"}
tags: ["dashboard", "assets", "design", "attached", "png", "image"]
created: 2026-08-29T23:02:13+09:00
status_since: 2026-08-30T13:24:38+09:00
updated: 2026-08-30T13:24:38+09:00
priority: 50
---

## Original

사용자(23:00): "이미지가 왜 자꾸 깨지지?" — 캡처를 보니 이미지가 아니라 **화면 전체가 깨져 있었다.** Docs 탭의 칩이 브라우저 기본 버튼 모양이고 카드 스타일이 없다. 스타일 조각이 안 온 것이다.

## 실측 (살아 있는 서버 9909, 동시 41개 요청 3회)

    회차1: 31/41 성공 — 실패 10 (ConnectionReset · URLError)
    회차2: 12/41 성공 — 실패 29
    회차3:  9/41 성공 — 실패 32

**연결이 잘린다.** 404 가 아니라 연결 자체가 안 선다. 이것이 DOC-20260827-004-62x6 이
규명한 WSL2 루프백 동시 도착 벼랑이고, REQ-20260829-019 가 그림에 대해 이미 갚은 그 빚이다.

## 왜 지금 터졌나 — 027 이 만든 새 표면

REQ-20260829-027 이 `web/index.html` 한 장을 껍데기 + 조각 38개로 갈랐다. 구조로는 옳은
결정이었지만, **한 번의 F5 가 요청 1개에서 39개가 됐다.** 그리고 그 39개는 브라우저가
직접 부르는 `<link>`·`<script>` 라 **실패해도 다시 걸지 않는다** — 019 가 `<img>` 에서
고친 것과 정확히 같은 구멍이, 이번엔 화면의 뼈대에서 열렸다.

한 장이 잘리면 증상이 갈린다: CSS 조각이면 **레이아웃이 통째로 무너지고**(사용자가 본 것),
JS 조각이면 그 기능만 조용히 죽는다.

**019 의 designer 가 이미 재 놓은 것**: 동시 상한(4·6·8)은 듣지 않는다 — 벼랑은 "몇 개가
떠 있나"가 아니라 "같은 순간에 몇 개가 도착하나"다. 간격을 벌리면 전부 통과한다.

## 고칠 방향 둘 (겹쳐 써야 한다)

**① 서버가 묶어 내준다 (뿌리).** `/css/all.css`·`/app/all.js` 한 갈래를 열어 껍데기가
선언한 **순서 그대로** 이어 붙여 낸다. 39개가 2개가 되면 벼랑에 닿을 일이 없다.
파일은 그대로 갈라져 있으니 사람이 고치는 자리는 안 바뀐다 — 가른 이득은 지키고
부른 횟수만 되돌린다. 순서가 곧 계약이므로 껍데기의 목록이 유일한 원천이어야 한다.

**② 그래도 잘리면 다시 건다 (안전망).** `<link onerror>`·`<script onerror>` 에서
019 와 같은 백오프(120·320·800ms + 지터)로 다시 걸고, 재시도 주소만 `?r=` 로 가른다.
껍데기의 지킴이는 이미 "무엇이 안 왔는지"를 말한다 — 말하기 전에 되찾게 한다.

## 주의

`bin/s9` 는 지금 REQ-20260829-037 이, `web/` 은 REQ-20260829-038 이 쓰고 있다.
①은 서버 몫, ②는 껍데기 몫이라 갈라 붙일 수 있다.

## Notes

### 2026-08-30T00:54:52+09:00 backend-developer (by nicehugepark)

### 서버 몫 — 42를 2로 (구현 완료, 시험 25/25)

`/css/all.css`·`/app/all.js` 두 갈래를 열었다. 껍데기가 선언한 **순서 그대로** 이어 붙여 한 응답으로 낸다.

**실측 (살아 있는 9909, 같은 순간 도착 5회)**

    낱개 42개 → 18/42 · 9/42 · 10/42 · 7/42 · 9/42
    묶음  3개 → 2/3 · 3/3 · 3/3 · 3/3 · 3/3

**고친 자리**
- `bin/s9`: `web_parts()`·`_part_bytes()`·`web_bundle()` 신설, `do_GET` 에 묶음 갈래(낱개 갈래보다 앞). 낱개는 그대로 열려 있다(진단·시험).
- `web/index.html`: 조각 목록이 `<template id="s9-parts-css">`·`<template id="s9-parts-app">` 안으로 들어갔다. template 내용은 **불활성**이라 브라우저가 안 가져간다 — 선언은 살아 있고 요청은 둘이다. 서버와 `tests/webasset.py` 가 **같은 한 벌**을 읽는다(목록이 두 벌이 되면 한 벌만 고쳐진다).

**묶으면서 잃은 것을 되찾아 뒀다.** 낱개일 때 사파리는 `ccrender.js` 의 lookbehind 한 줄을 문법 오류로 보고 그 파일만 버렸고 나머지는 멀쩡히 돌았다(REQ-038). 한 파일로 묶으면 같은 한 줄이 화면 전체를 죽인다. 그래서 묶음이 **도착했는데 한 줄도 못 돌았으면** 껍데기가 낱개로 되돌린다(`__S9_JS_CAME && !__S9_OOPS && !__S9_APP_READY`). 일부라도 돌았으면 되돌리지 않는다 — 같은 const 를 두 번 선언해 이번엔 낱개 쪽이 죽는다.

**조각 줄 번호**: 묶음 맨 앞에 `window.__S9_BUNDLE = [["app/card.js", 1234], …]` 표를 낸다. 조각마다 `/* --- app/card.js --- */` 주석도 붙는다. 콘솔이 말하는 줄을 조각의 줄로 되돌리는 자리다 — **화면 쪽 지킴이(app/oops.js)가 이 표를 쓰면** "app/all.js:1234" 가 "app/card.js:12" 가 된다. 지금은 표만 있고 쓰는 쪽이 없다.

**곁가지로 고친 것**: '조각을 못 받았습니다' 알림이 되찾기가 도는 중에 곧바로 body 를 지우고 있었다 — 200ms 뒤 돌아올 화면을 그 손으로 지운다. 그물이 멎을 때까지(또는 3초) 기다린 뒤 말하게 바꿨다.

시험: `python3 tests/ web_split` 25개 전부 green (s2c 목록 밖 태그 금지 · s5e 되돌리기 계약 · s7·s7b·s7c 묶음 서빙/글자 단위 동일/줄 번호표 · s8~s8e 고아 금지·줄바꿈·없는 조각·읽기 실패·옆문). `SHELL_MAX_LINES` 220→270 (이유는 시험 주석에).

[Image: assets/REQ-20260829-039-62x6/board1.png]

### 2026-08-30T01:28:47+09:00 backend-developer (by nicehugepark)

### 안전망을 **실제로 깨뜨려** 확인했다 + 전체 스위트

묶음의 유일한 대가는 '죽는 단위'다. 그것을 말로 두지 않고 재 봤다: 임시 뿌리에 web/ 을 복사하고 `app/card.js` 뒤에 문법 오류 한 줄(`var x = ;`)을 넣었다. 묶음은 200 으로 왔지만 파싱에서 통째로 죽는다.

결과 — **껍데기가 낱개로 되돌렸고 화면이 섰다.** 지킴이가 붉은 상자로 `app/card.js:1026 SyntaxError` 를 정확히 지목하고, 헤더·탭·필터·규칙은 그대로 그려졌다(첨부). 사파리에서 잃을 뻔한 것을 그대로 지킨 것이다.

되돌리기의 발동 조건은 셋이 모두 참일 때다: 묶음이 **도착했고**(`__S9_JS_CAME`, 묶음 태그의 onload) · 한 줄도 안 돌았고(`__S9_OOPS` 없음) · 아직 안 섰다(`__S9_APP_READY` 없음). 잘려서 안 온 것은 여기 안 걸린다 — 그건 재시도 몫이다. 일부라도 돌았으면 되돌리지 않는다(같은 const 를 두 번 선언하면 이번엔 낱개 쪽이 죽는다).

그 onload 가 인라인 스크립트보다 늦게 도는 브라우저가 있을 수 있어 **한 번 보고 포기하지 않는다** — 지킴이가 150ms마다 다시 묻고, 되돌린 뒤에는 시계를 다시 걸어 낱개가 오는 동안 '못 받았습니다' 를 말하지 않는다.

**전체 스위트**: `python3 tests/` → **Ran 2297 tests, FAILED (errors=1, skipped=1)**. 그 1건은 `test_code_read_gate.test_b2` 의 `ConnectionResetError` — 이 요청이 다루는 바로 그 루프백 벼랑이고, 단독으로 두 번 다시 돌리면 19/19 OK 다. 내 변경과 무관하다.

**화면 몫으로 넘길 계약 (`web/app/oops.js`, 다른 사람 자리)**: 묶음은 한 파일이라 콘솔이 말하는 줄도 하나뿐이다. `/app/all.js` 맨 앞의 `window.__S9_BUNDLE` = [[조각, 시작줄], …] 을 읽어 `base()`/`push()` 가 "app/all.js:1234" 를 "app/card.js:12" 로 되돌리면, 묶음이 죽었지만 되돌리기가 안 걸리는 경우(=일부만 돌다 죽음)에도 어느 조각인지 이름으로 말할 수 있다. 표는 서버가 이미 낸다.

[Image: assets/REQ-20260829-039-62x6/fallback.png]

### 2026-08-30T01:30:14+09:00 subagent (agent: sub:backend-developer:ab8077dd, by nicehugepark)

`[2026-08-30 01:35:12 KST - backend-developer]`

세 요청 모두 구현·검증했다. 전체 스위트 **2297건 · 실패 0 · 환경성 오류 1 · 스킵 1**.

---

### ① REQ-20260829-039-62x6 «조각 서른아홉이 벼랑을 만난다» — 서버 몫

**실측 (살아 있는 9909, 같은 순간 도착 5회)**

    낱개 42개 → 18/42 · 9/42 · 10/42 · 7/42 · 9/42
    묶음  3개 → 2/3 · 3/3 · 3/3 · 3/3 · 3/3   (1건은 재시도가 건진다)

**고친 자리**
- `/home/sjpark1/section9/bin/s9` — `web_parts()`·`_part_bytes()`·`web_bundle()` 신설, `do_GET` 에 `/css/all.css`·`/app/all.js` 갈래(낱개 갈래보다 앞). 낱개는 그대로 열려 있다.
- `/home/sjpark1/section9/web/index.html` — 조각 목록이 `<template id="s9-parts-css">`·`<template id="s9-parts-app">` 안으로. **template 내용은 불활성이라 브라우저가 가져가지 않는다** — 선언은 살아 있고 요청은 둘이다. 서버와 `tests/webasset.py` 가 **같은 한 벌**을 읽는다.
- 캐시 없음(`_send` no-store) · MIME 정확 · 조각마다 `/* --- app/card.js --- */` · 조각 끝 줄바꿈 보장 · 묶음 맨 앞에 `window.__S9_BUNDLE = [[조각, 시작줄], …]`.

**묶으면서 잃는 것을 되찾아 뒀다.** 낱개일 때 사파리는 `ccrender.js` 한 줄을 문법 오류로 보고 그 파일만 버렸다(REQ-038). 한 파일로 묶으면 같은 한 줄이 화면 전체를 죽인다. 그래서 묶음이 **도착했는데 한 줄도 못 돌았으면**(`__S9_JS_CAME && !__S9_OOPS && !__S9_APP_READY`) 껍데기가 낱개로 되돌린다. 재시도(`onerror` 캡처)는 그대로 살아 있다.

**확인한 것**: `bin/s9 shot` 으로 대시보드 정상 렌더 · 안전망을 **실제로 깨뜨려** 검증 — 임시 뿌리의 `card.js` 에 `var x = ;` 를 넣으니 묶음이 파싱에서 죽고, 되돌리기가 낱개를 다시 걸어 화면이 서고 지킴이가 `app/card.js:1026 SyntaxError` 를 지목했다.
**확인하지 못한 것**: 사파리·맥에서 직접 돌려 보지 못했다(브라우저는 HeadlessChrome).

**시험**: `python3 tests/ web_split` **25/25** (s2c 목록 밖 태그 금지 · s5e 되돌리기 계약 · s7·s7b·s7c 서빙/글자 단위 동일/줄번호표 · s8~s8e 고아 금지·줄바꿈·없는 조각·읽기 실패·옆문). `SHELL_MAX_LINES` 220→270 (이유를 시험 주석에 적었다).

**화면 몫으로 넘길 계약** (`web/app/oops.js`, 내 자리 아님): 묶음이 일부만 돌다 죽으면 콘솔은 `app/all.js:1234` 를 말한다. `window.__S9_BUNDLE` 표를 읽어 `app/card.js:12` 로 되돌리면 그때도 조각 이름이 나온다. 표는 서버가 이미 낸다.

---

### ② REQ-20260829-037-62x6 «맥에서 살아 있음을 못 본다»

앞선 작업자(af70d8ba)가 판정을 한 문으로 모아 놓았고(`proc_table`·`pid_alive`·`pid_cmdline`·`pid_comm`·`pid_ppid`·`pid_env`·`_port_owner_pid`), 나는 **전수 점검·끝에서 끝까지 검증·진단 강화**를 했다.

**친구가 맥에서 돌릴 한 줄 — 그대로 복사해 보내면 된다:**

```
cd ~/section9 && ./bin/s9 doctor --live
```

**확인한 것**
- `S9_PROC_BACKEND` 로 세 갈래(`proc`·`ps`·`none`)를 강제해 돌렸다. `ps` 갈래(맥 길)가 `proc` 갈래와 **같은 답**: 듣는 세션 2 · attach 2/2 · 포트 주인 찾음. `none` 은 전부 거짓으로 떨어지고 안 죽는다(rc 1).
- 끝에서 끝까지: 서버를 `S9_PROC_BACKEND=ps` 로 띄워 `/api/chat/target` → `{live: true, listening: true}`. 터미널 하단이 서는 그 값이다.
- 남은 `/proc` 사용처는 전부 갈래 뒤이거나 폴백이 있다(`bin/s9-doctor` `proc_cmd`→`ps`, `/proc/1/comm` 없으면 건너뜀, `os_platform`). 훅 둘은 복제본을 들지만 H1~H4 가 갈라짐을 못박는다.

**이번에 더한 것**: `live_diag()`/`print_live_diag()` 에 **증거 블록**. 듣는 것이 0일 때가 곧 친구의 화면인데 지금까지 "0개"만 말했다. 이제 수신함 파일이 있는지 · tail 이 도는지 · **그 명령줄이 어떤 모양인지**를 함께 낸다. `--json` 에도 `inboxes`·`tails` 로.

**확인하지 못한 것 (정직하게)**
- 맥에서 실제로 돌려 보지 못했다. 위는 "맥이라면 **어느 길로 가는가**"의 못박음이지 "맥에서 된다"의 증명이 아니다.
- 맥의 `ps -E`(환경 조회)는 SIP 이후 남의 프로세스 환경을 안 준다 — 계정 표시가 맥에서 빈칸일 수 있다.
- 맥에서 Monitor 가 띄우는 tail 의 명령줄 모양을 모른다. 위 진단의 마지막 블록이 그것을 보려는 것이다.

**시험**: `python3 tests/ platform_live` **23/23** (S8c 추가).

---

### ③ REQ-20260830-002-62x6 «끝난 일에 작업자가 뜬다»

**원인은 낡은 색인이 아니었다.** 증거: `state/sessions/approvals_seen.json` 이 **00:30 에** 쓰였고 그 안에 `REQ-20260829-019-62x6` 이 있다. `rework_watch_tick` 의 뒤 갈래(승인 후속, REQ-20260824-028)는 상태를 아예 안 본다 — `approvals_unseen()` 이 고르는 것이 애초에 `status == "done"` 인 문서다. **워처가 done 을 못 본 게 아니라 그 갈래는 done 을 보고 뜨도록 만들어져 있었다.**

그 대칭이 틀렸다. 반려는 in-progress 로 가지만 승인 메모는 언제나 done 으로 간다. 이 저장소는 `review→done` 에 근거 메모를 요구하므로 **승인마다 작업자가 떴다** — 2026-08-29 실측으로 그날 20슬롯 중 4건이 이 경로였고 저녁의 사람 깨우기는 전부 capped 였다(`test_wake.py` W4).

**고친 자리 (`bin/s9`)**
1. `_spawn_worker` — 스폰이 반드시 지나는 그 문 하나에 상태 게이트. done·cancelled → `blocked="closed"`. 판정은 새 `doc_status_live()` 로 **디스크의 문서**에서(카탈로그가 낡아도 안 열린다 — 그 가설도 함께 막았다). kill-switch 다음, 캡·자리 판정보다 앞.
2. `rework_watch_tick` — 승인 후속 무인 스폰 **제거**(`_spawn_followup`·`_approvals_mark` 삭제). 메모는 소비되지 않은 채 남아 훅이 다음 리드 세션 앞에 놓는다 — 그것이 028 의 본래 경로였고 무인 스폰은 폴백이었다.
3. `worker_stop` — **끝난 문서에는 클레임 게이트가 서지 않는다.** 끝난 문서는 `_claim_req` 가 등록을 막으므로 아무도 집을 수 없었고, 게이트가 시키는 명령이 성공할 수 없었다. 이유는 여전히 필수: `s9 workers --stop <문서id> --why '…'`
4. `s9 last --add` — **거절을 성공처럼 찍지 않는다.** 등록됐는지 확인하고 안 됐으면 왜인지 말하며 실패한다.
5. `lease_prune()` 이 워처 틱에서도 돈다. 종전엔 `lease_take` 안에서만 돌아 **다음 작업자가 뜰 때까지** 죽은 손의 리스가 남았다(014, pid 876195). 판정(`lease_conflict`)은 이미 죽은 pid 를 무시하므로 막지는 않았다.

**시험**: 새 파일 `/home/sjpark1/section9/tests/test_closed_no_worker.py` **11/11**. 회귀 둘 갱신 — `test_auto_resume_gate` W10(제거한 계약을 새 계약으로), `test_worker_stop` 의 글자 수 슬라이스를 함수 끝까지로(주석 몇 줄에 창이 밀려 제품은 멀쩡한데 시험만 빨개졌다).

**판정을 청한다 (리드 몫)**: 2번은 **기능 하나를 걷어낸 것**이다. "done 에는 어떤 경로로도 안 뜬다"를 글자 그대로 지키면 승인 후속 무인 작업자는 성립할 수 없다 — 그 경로는 언제나 done 으로만 간다. 되돌리려면 상태 게이트에 `reason == "followup"` 예외를 두면 되지만, 그러면 이 사고의 자리가 그대로 열린다. 문서 노트에 되돌리는 방법까지 적어 뒀다.

---

### 전체 스위트

```
Ran 2297 tests in 580.005s
FAILED (errors=1, skipped=1)
```

그 1건은 `test_code_read_gate.test_b2` 의 `ConnectionResetError` — ①이 다루는 바로 그 WSL2 루프백 벼랑이고, 단독으로 두 번 다시 돌리면 **19/19 OK**(두 번 다). 내 변경과 무관하다.

### 못 한 것 / 보고만

- 살아 있는 9909 는 여전히 **단발 요청도 20~40% 가 RST 로 잘린다**(브라우저 탭이 붙어 있을 때). 서버 코드로 옮길 수 없는 벼랑이라(DOC-20260827-004-62x6) 요청 수를 42→3 으로 줄이는 것이 할 수 있는 전부였고, 재시도가 나머지를 덮는다.
- `bin/s9 serve --restart` 를 셸에서 직접 띄우면 감시자(pid 800)의 자식이 아니게 된다 — 그 프로세스를 죽여 감시자가 새 자식을 띄우게 하는 편이 깨끗하다. 지금은 감시자 자식이 9909 를 물고 있다.
- git 쓰기는 하지 않았다. `web/app/*.js`·`web/css/*.css` 는 읽기만 했다.

### 2026-08-30T01:41:05+09:00 commit (by nicehugepark)

커밋 3c63e9c — 조각 마흔둘을 셋으로 묶고, 끝난 일에는 작업자가 뜨지 않는다 (REQ-039·038·037·030·002)

### 2026-08-30T11:58:03+09:00 workspace (by nicehugepark)

본 저장소에서 이어받는다 — 화면 작업이라 살아 있는 서버가 읽는 자리에서 해야 한다. 커밋하면 다음 라운드부터 작업자가 다시 자기 워크트리를 받는다.

### 2026-08-30T12:01:48+09:00 (agent: sub:backend-developer:ab8077dd, by nicehugepark)

끊긴 것은 일이 아니라 기록 자리였다 — 그 몫(서버 묶음·037·002)은 커밋 3c63e9c 에 그대로 들어가 있다. transcript 경로가 /tmp 로 잡혀 사라졌을 뿐이라 done 으로 닫는다.

### 2026-08-30T12:02:26+09:00 response (by nicehugepark)

### 화면 몫 — 묶음이 말하는 줄을 조각의 줄로 되돌린다

앞 라운드가 서버 몫(42→3 묶음)과 되찾기 그물까지 끝내고 `web/app/oops.js` 앞에 계약 하나를 놓고 갔다. 그 마지막 한 걸음을 마쳤다.

**무엇이 문제였나.** 묶은 뒤로 콘솔이 말하는 자리가 `app/ccrender.js:41` 에서 `app/all.js:1834` 가 됐다 — 사람이 열 파일이 없어진 것이다. 껍데기의 되돌리기(낱개로 다시 걸기)는 묶음이 **한 줄도 못 돌았을 때만** 발동하므로, 일부가 돌다 죽은 경우에는 이름을 되찾아 줄 사람이 지킴이뿐이었다.

**고친 자리** — `web/app/oops.js` 하나. 서버가 이미 내는 표(`window.__S9_BUNDLE` = [[조각, 시작 줄], …])를 읽는 `remap(file, line)` 을 더하고, 오류를 그리는 자리(`error` 리스너)가 그것을 지나가게 했다. 시작 줄은 그 조각의 이름 주석 줄이라 조각 안의 줄은 `줄 - 시작` 이다.

**지어내지 않는 자리 셋**: 표가 없거나 망가졌을 때(옛 서버가 내준 묶음) · 줄 번호가 없을 때(자원이 통째로 안 온 것이라 조각 탓이 아니다) · 표의 첫 조각보다 앞일 때(머리말과 표 자신). 셋 다 `app/all.js` 그대로 둔다 — 없는 조각을 지목하면 다음 사람이 엉뚱한 파일을 연다.

**시험** — 새 파일 `tests/test_bundle_lineno.py` **8/8**. 정규식으로 짐작하지 않고 `remap()` 을 떼어 node 로 **실행**한다(B1 되돌림 · B1b 마지막 조각 · B2 머리말 · B3 낱개는 그대로 · B4 표가 없거나 망가져도 안 죽음 · B5 줄 없으면 안 건드림 · B6 그 함수를 실제로 부르는가). B7 은 짐작한 표가 아니라 **서버가 지금 내는 표**로 되돌려, 조각마다 첫 줄·중간·끝을 골라 그 조각 원문의 그 줄과 **글자로** 맞춰 본다 — 표를 내는 쪽과 읽는 쪽이 한 줄씩 어긋나는 실패가 가장 찾기 어렵다.

`bin/s9`·`web/index.html` 은 이번에 안 건드렸다 — 다른 작업자의 미커밋 변경이 올라와 있다(index.html 은 tidy.css 순서, bin/s9 는 288줄). 화면 캡처: scratchpad/oops-039.png (판 정상, 붉은 상자 없음).

### 2026-08-30T12:02:31+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-08-30 12:02:31 KST - lead]`

남은 몫이었던 화면 쪽 계약(`web/app/oops.js` 의 줄 번호 되돌리기)을 구현하고 새 시험 8건을 통과시켰습니다. 전체 스위트가 백그라운드로 돌고 있어 결과를 받은 뒤 review 로 전이하겠습니다.

## History
- 2026-08-29T23:02:13+09:00 created by nicehugepark (status: open)
- 2026-08-29T23:02:25+09:00 status: open -> in-progress (by nicehugepark) — 사용자 화면이 지금 깨져 있다 — 최우선. 서버 몫은 037 담당(bin/s9)에게, 껍데기 몫은 038 담당(web/)에게 갈라 붙인다
- 2026-08-30T13:24:38+09:00 status: in-progress -> done (by nicehugepark) — 목표(묶은 뒤에도 콘솔이 조각의 파일·줄을 가리킨다) 충족 — web/app/oops.js 의 remap 이 서버가 내는 __S9_BUNDLE 표로 all.js 줄을 조각 줄로 되돌리고, tests/test_bundle_lineno.py 8/8 이 node 로 실제 실행해 확인한다(B7 은 서버가 지금 내는 표로 글자 대조). 커밋 3c63e9c 에 포함. 기계로 확인되는 건이라 review 를 거치지 않는다
