---
id: REQ-20260829-002-62x6
type: request
title: systemd 사용자 세션 기동 실패
summary: wsl 로그인 시 systemd --user 세션이 뜨지 않는다
status: blocked
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: a53ebee3
sessions: ["317e29e4", "a53ebee3"]
agents: ["lead:claude-opus-5"]
contributions: [{"actor": "lead:claude-opus-5", "item": "`[2026-08-29 00:51:08 KST - lead]`", "started": "2026-08-29T00:51:09+09:00", "ended": "2026-08-29T00:51:09+09:00", "result": "done", "transcript": ""}, {"actor": "lead:claude-opus-5", "item": "`[2026-08-29 01:18:37 KST - lead]`", "started": "2026-08-29T01:18:37+09:00", "ended": "2026-08-29T01:18:37+09:00", "result": "done", "transcript": ""}]
project: section9
relates: ["REQ-20260829-003-62x6"]
relates_why: {"REQ-20260829-003-62x6": "/tmp 누수가 부트 73초를 만들고, 그 73초가 WSL 의 사용자 세션 창을 놓치게 한다 — 같은 사슬의 앞뒤"}
tags: ["auto-audit", "session"]
created: 2026-08-29T00:46:56+09:00
status_since: 2026-08-29T01:17:29+09:00
updated: 2026-08-29T02:36:25+09:00
priority: 50
---

## Original

wsl: Failed to start the systemd user session for 'sjpark1'. See journalctl for more details.

이 문제를 해결해줘

## Notes

### 2026-08-29T00:50:14+09:00 ask (by nicehugepark)

wsl 에서 systemd가 돌아갈게 없다. 제거해 그냥.

### 2026-08-29T00:50:53+09:00 response (by nicehugepark)

원인 규명: WSL 부트에서 systemd-tmpfiles-setup.service 가 73초(직전 부트 46초) 걸려 sysinit.target 을 붙잡고, 그 뒤에 오는 systemd-logind 가 늦게 뜬다. WSL 은 uid=1000 사용자 세션을 10초만 기다리므로(CreateLoginSession:2758 Timed out waiting for user session for uid=1000) 그 창을 놓치고 'Failed to start the systemd user session' 을 출력한다. 그래서 /run/user/1000 도 없고 systemctl --user 도 버스에 못 붙는다.

사용자 판단: 이 WSL 에서 systemd 로 돌릴 것이 없으니 systemd 자체를 끈다(채팅 지시). 저장소 확인 결과 section9 는 systemd 에 의존하지 않는다(bin/core 전체에 systemctl 호출 없음). docker/containerd/snapd 만 systemd 아래 있었고 docker 는 /etc/init.d/docker 가 있어 'sudo service docker start' 로 뜬다.

조치: /etc/wsl.conf 의 [boot] systemd=true → false. 루트 권한이 필요해 리드가 직접 쓸 수 없어 사용자에게 한 줄로 넘긴다. 반영은 Windows 에서 'wsl --shutdown' 후 재접속.

### 2026-08-29T00:51:09+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-08-29 00:51:08 KST - lead]`

REQ-20260829-002-62x6 을 집었습니다. 원인은 규명됐고, 지시대로 systemd 를 끄는 것으로 방향을 잡았습니다.

**왜 그 메시지가 떴나**

부트 로그를 보면 이렇습니다.

```
00:46:15  systemd-tmpfiles-setup.service 시작
00:46:25  WSL: /sbin/init failed to start within 10000ms
00:46:35  WSL: Timed out waiting for user session for uid=1000
00:47:29  systemd-tmpfiles-setup.service 완료   ← 73초
00:47:29  systemd-logind 시작
```

`systemd-tmpfiles-setup` 이 73초(직전 부트는 46초) 동안 `sysinit.target` 을 붙잡고, 그 뒤에 오는 `systemd-logind` 가 그만큼 늦게 뜹니다. WSL 은 사용자 세션을 10초만 기다리다 포기하고 그 메시지를 냅니다. 그래서 `/run/user/1000` 도 없고 `systemctl --user` 도 "Failed to connect to bus" 가 납니다.

**끄기 전에 확인한 것**

- section9 은 systemd 를 쓰지 않습니다 (`bin/`, `core/` 전체에 `systemctl` 호출 0건).
- systemd 아래 있던 건 docker·containerd·snapd 뿐입니다. docker 는 `/etc/init.d/docker` 가 있어 `sudo service docker start` 로 뜹니다. snap 앱은 못 씁니다.

**적용 (루트가 필요해 직접 못 씁니다)**

`/etc/wsl.conf` 가 root 소유이고 sudo 가 비밀번호를 요구해서 제가 쓸 수 없습니다. 이 줄을 프롬프트에 붙여넣어 주세요.

```
! sudo sed -i 's/^systemd=true/systemd=false/' /etc/wsl.conf && cat /etc/wsl.conf
```

그다음 Windows PowerShell 에서 `wsl --shutdown` 하고 다시 여시면 반영됩니다 — 이때 지금 세션은 끊깁니다.

되돌리려면 같은 자리를 `systemd=true` 로 바꾸면 됩니다.

한 가지 덧붙입니다: systemd 를 끄면 메시지는 사라지지만 `systemd-tmpfiles` 가 왜 73초씩 걸렸는지는 미규명으로 남습니다. 다른 배포판이나 나중에 systemd 를 다시 켤 때 같은 자리에서 걸릴 수 있으니, 별건으로 남겨둘지는 말씀 주시면 문서로 올리겠습니다.

### 2026-08-29T01:18:37+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-08-29 01:18:37 KST - lead]`

포트 고갈이 아니었습니다. 다른 두 가지가 겹쳐 그 지문을 냈고, 그중 하나는 저장소를 못 쓰게 만들고 있었습니다.

**1. 고갈은 없습니다 (REQ-004, done)**

`s9 doctor` 실측: 윈도우 동적 포트 **35/16384 (0%)**, 신규 포트 공개 지연 0.25초, 고아 서버 0, 헤드리스 브라우저 0. 어느 지표로도 고갈이 아닙니다.

재부팅 직후 그렇게 보인 이유는 부트였습니다. `systemd-tmpfiles-setup` 이 `sysinit.target` 을 **73초** 붙잡아 그동안 아무것도 리슨하지 못했습니다. `s9 serve` 는 00:47:2x 에야 떴습니다. 그 74초 동안 "대시보드는 안 열리고 새 서버는 안 뜨는데 이미 뜬 것은 멀쩡한" — DOC-20260826-008-62x6 이 고갈의 지문이라고 못박은 그 비대칭과 겉모습이 똑같습니다. 지문이 겹치는 다른 원인이 하나 더 있었던 겁니다.

**2. `s9 doctor` 가 그때 아무 말도 못 한 이유** — 포트·캡처·서버 프로세스만 봤기 때문입니다. 축을 일곱으로 넓혔습니다: 부트 · /tmp · 디스크 · 서버 응답 · 저장소 · 훅 · 도구. 이제 네트워크가 정상이어도 걸린 게 있으면 "정상"이라 말하지 않고 나쁜 순서로 지목하고, critical 이면 종료코드 1 입니다.

지금 돌리면 이렇게 나옵니다:

```
✗ 부트: systemd 사용자 세션이 서지 못했다 (userspace 77.0초)
✗ 저장소: core.bare=true — status·add·commit 이 전부 거부된다
네트워크는 정상이다(공개 지연 0.25초). 걸린 것은 다른 데 있다:
```

**3. 그 두 번째 ✗ 가 진짜 사고였습니다 (REQ-005, done)**

`git status` 가 `fatal: this operation must be run in a work tree` 로 거부되고 있었습니다. `.git/config` 의 `core.bare` 가 true 로 뒤집혀 있었고, 되돌려도 **1분 안에 다시** 뒤집혔습니다.

경로를 끝까지 좇았습니다 — `s9 serve` 가 주인 떠난 워크트리를 자동 보존하며 `git commit` → git 이 pre-commit 훅에 `GIT_DIR=<repo>/.git/worktrees/<이름>` 을 넘김 → 훅이 스테이지 테스트를 띄움 → 테스트가 임시 리포에 `git init` → **`GIT_DIR` 이 `-C <임시경로>` 를 이겨** 그 워크트리 자리를 다시 init 하고 공용 `.git/config` 에 `core.bare=true` 를 박습니다.

격리 재현으로 고정했습니다: `GIT_DIR=<repo>/.git/worktrees/wt1 git -C <다른경로> init` → false → true.

막은 곳 셋: 훅 템플릿이 문 앞에서 `GIT_DIR` 을 벗기고(`GIT_INDEX_FILE` 은 남깁니다 — 가드가 지금 담기는 것을 보려면 필요합니다), `s9-guard` 가 자식 테스트에 스크럽한 환경을 넘기고, 테스트의 git 헬퍼도 같이 벗깁니다. 150초 감시 동안 자동 보존 커밋이 계속 도는데도 뒤집히지 않았고, 이 커밋 자체가 그 훅을 타고 들어갔습니다 (`76069b9`, `2fdc71a`).

**4. 73초의 정체 (REQ-003, done)**

`--remove` 가 붙은 tmpfiles 가 부트마다 `/tmp` 를 통째로 지웁니다. 증거 셋: `--remove` 없이 돌리면 6ms, systemd 가 잰 서비스 CPU 14.098초, `/tmp` 디렉터리 아이노드가 7.5MB(`/var/tmp` 는 4KB — ext4 디렉터리는 줄지 않으니 10만 개대를 담았던 자리입니다).

기여 요인은 우리 것입니다: `tests/*.py` 84개가 `mkdtemp` 로 S9_ROOT 를 만들고 한 번도 지우지 않습니다. 지금도 분당 1~2개씩 쌓입니다(doctor 의 `/tmp` 줄에 "우리 것 N개"로 보입니다). systemd 를 끄면 부트 비용은 사라지지만 이 누수는 남습니다 — 별건으로 잡을지는 말씀 주시면 하겠습니다.

**남은 하나 — REQ-002 는 blocked 입니다**

`/etc/wsl.conf` 가 root 소유이고 sudo 가 비밀번호를 요구해 제가 못 씁니다. 이 줄을 붙여넣어 주세요.

```
! sudo sed -i 's/^systemd=true/systemd=false/' /etc/wsl.conf && cat /etc/wsl.conf
```

그다음 Windows PowerShell 에서 `wsl --shutdown` 후 재접속하면 반영됩니다(지금 세션은 끊깁니다).

## History
- 2026-08-29T00:46:56+09:00 created by nicehugepark (status: open)
- 2026-08-29T00:47:45+09:00 status: open -> in-progress (by nicehugepark) — drag 이동 [via dashboard]
- 2026-08-29T01:17:29+09:00 status: in-progress -> blocked (by nicehugepark) — /etc/wsl.conf 는 root 소유이고 sudo 가 비밀번호를 요구해 리드가 쓸 수 없다. 사용자가 한 줄을 실행해야 진행된다: sudo sed -i "s/^systemd=true/systemd=false/" /etc/wsl.conf 그다음 Windows 에서 wsl --shutdown
