---
id: REQ-20260825-099-62x6
type: request
title: 네트워크 고갈 방지·복구
summary: 새 리스닝 포트가 먹통이 되는 WSL 자원 고갈을 유발하지 않도록 정리 규율을 넣고, 감지·즉시 복구 명령을 제공한다
goal: s9 doctor 로 상태를 즉시 판별할 수 있고, 캡처·테스트·서버가 자원을 남기지 않아 같은 증상이 재발하지 않는다
status: done
size: M
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: d332535d
project: section9
relates: ["REQ-20260825-100-62x6"]
tags: ["test", "docs", "dashboard"]
created: 2026-08-25T22:49:59+09:00
status_since: 2026-08-26T02:43:33+09:00
updated: 2026-08-26T09:32:16+09:00
---

## Original

사용자 지시: "ERR_NO_BUFFER_SPACE 오류는 너무 치명적이다. 잘 모르는 사람들은 os를 반드시 재시작해야하는 상황 아닌가. 이런 일이 발생하지 않도록 임시파일로 만들거나 프로세스를 제대로 닫으라는 요청이 있었을텐데 저런것까지도 당연히 포함해야한다. 신규 요청을 만들어서 해결하고 재발되지 않도록 조치해. 그리고 이런 상황에서 즉시 처리할 수 있는 명령어과 기능도 추가해둬."

실측(2026-08-25 22:5x, 리드): 이 머신이 지금 그 상태다.
- 새로 bind 한 임시 포트에 같은 프로세스 안에서 connect 해도 ECONNREFUSED. lo 인터페이스는 UP이고 127.0.0.1/8 정상.
- 반면 **이전부터 떠 있던** 9909(대시보드)에는 정상 연결된다. 즉 기존 포트는 살아 있고 **새 리스닝 포트만 먹통**이다.
- WSL 네트워킹 모드는 virtioproxy — 리스닝 포트를 호스트에 프록시/등록하는 구조라, 윈도우 측 자원이 마르면 새 포트 등록이 실패한다는 가설과 증상이 일치한다. 같은 시각 윈도우 Chrome은 ERR_NO_BUFFER_SPACE.
- 부수 피해: 서버를 새로 띄우는 테스트가 전부 실패(전체 스위트 29건 connection refused), s9 shot 캡처 실패, 새 검증 서버 기동 불가.

범위:
1. 감지: `s9 doctor` — 임시 포트 bind+connect 왕복으로 degraded 여부를 즉답, 리스너/프로세스/좀비 캡처 수, 윈도우 측 지표(가능하면), 판정과 다음 조치를 출력.
2. 즉시 복구: 덜 파괴적인 순서로 안내·실행 — 좀비 s9 serve/headless Chrome 정리 → 남은 임시 서버 회수 → (필요 시) wsl --shutdown 안내. 파괴적 단계는 실행 전 확인.
3. 재발 방지: (a) s9 shot 이 캡처마다 Chrome을 새로 띄우고 종료를 보장하는지 점검·수정, (b) 테스트가 서버를 반드시 정리하도록(실패 경로 포함) 보강, (c) 동시 캡처 수 제한, (d) 임시 파일은 scratchpad 하위로 통일하고 종료 시 정리.
4. 조사 결과(REQ-098, deep-diver)와 합류해 원인 확정 후 감지 임계값을 조정한다.

## Notes

### 2026-08-25T23:04:58+09:00 response (by nicehugepark)

원인·조치 요약은 REQ-20260825-100-62x6 노트 참조(윈도우 동적 포트 96% 고갈, DllHost WSL 중계가 15,709개 점유).
이번에 적용한 재발 방지:
- bin/s9 cmd_shot: 캡처마다 전용 user-data-dir(s9shot-<pid>) → 사용자 브라우저 세션에 붙지 않고, 종료 시 그 프로필을 가진 프로세스만 골라 회수(리눅스 pid + 윈도우 CIM 양쪽), 프로필 디렉토리 삭제. 성공·실패·타임아웃 어느 경로로 나가든 finally 에서 회수한다.
- 캡처 상한 SHOT_MAX_HEADLESS=8: 이미 좀비가 쌓여 있으면 캡처를 더 얹지 않고 s9-doctor --fix 를 안내하며 종료(4). 오늘 사고는 40~60회 캡처에 chrome.exe 165개가 쌓여 발생했다.
- 서버 테스트 기동 대기 5초→40초(14개 파일). WSL 포트 공개 지연이 평시 0.3~1초, 압박 시 수 초라 5초 대기는 반드시 깨진다.
- bin/s9-doctor 신설 + `s9 doctor` 서브커맨드 편입: 공개 지연을 초 단위로 측정(0=건강/3초↑=느림/12초 초과=degraded), 고아 테스트 서버·좀비 헤드리스 회수(--fix), 덜 파괴적인 순서의 조치 안내.

### 2026-08-25T23:47:05+09:00 response (by nicehugepark)

감지·복구 축이 완성됐다(REQ-20260825-100-62x6 노트·커밋 8a7bfd8 참조). 이 문서 범위 4항(원인 확정 후 임계값 조정)을 이번에 반영했다 — 원인이 호스트 동적 포트 고갈로 확정됐으므로, 임계값을 리눅스 쪽 지표가 아니라 호스트 소진도(60%/85%)로 잡고 doctor 가 매 실행마다 그 수치를 출력하게 했다. 1~3항(감지·즉시 복구·재발 방지)은 앞선 노트대로 적용 완료다.

### 2026-08-26T02:42:59+09:00 tdd (by nicehugepark)

TDD 시나리오 (반려 반영 — 리드가 직접 검증. tests/test_shot_hygiene.py 11건 + 전체 스위트 424건 OK):

- [x] H1 캡처마다 전용 프로필(`--user-data-dir=s9shot-<pid>`)이 붙고 표식이 프로세스마다 고유하다
- [x] H2 회수 대상이 그 표식과 일치한다 — 남의 브라우저를 죽이지 않는다
- [x] H3 타임아웃·크래시 경로에서도 finally 회수가 돈다(사고의 직접 원인이던 구멍)
- [x] H4 프로필 디렉토리도 남기지 않는다
- [x] H5 살아 있는 캡처가 상한(8) 이상이면 브라우저를 아예 띄우지 않고 종료 4 + 안내
- [x] H6 상한 바로 아래(7)에서는 정상 진행 — 경계에서 막히지 않는다
- [x] H7 상한을 보기 **전에** 회수를 먼저 돌린다(죽은 개체가 상한을 막으면 사람이 상한을 무시하게 된다)
- [x] H8 윈도우 개체수를 못 읽는 환경에서는 리눅스 ps 폴백으로 세어 상한이 무력화되지 않는다
- [x] H9 서버를 띄우는 테스트가 전부 공용 대기 헬퍼(portpool.wait_server)를 쓴다 — 촘촘한 사설 대기 루프가 다시 생기면 실패
- [x] H10 사흘 지난 캡처 산출물만 정리하고 최근 캡처(검증 근거)는 남긴다
- [x] H11 지울 게 없으면 조용히 0 — 매 --fix 마다 도는 경로라 부작용이 없어야 한다

실측 검증(코드 계약 외):
- 연속 캡처 3회 직후 `s9 doctor --json` → `headless_chrome: []`, `orphan_test_servers: []`, 공개 지연 0.27초, 윈도우 동적 포트 2168/16384(13%). 즉 캡처가 프로세스를 남기지 않는다.
- 윈도우 Chrome으로 대시보드 실캡처 성공(Board 렌더 확인) — 사고 당시 ERR_NO_BUFFER_SPACE 로 실패하던 경로가 복구됐다.
- 전체 스위트 424건 OK(skipped 1). 서버를 띄우는 14개 파일이 포트 풀 + 지수 백오프로 전부 통과.

### 2026-08-26T02:43:18+09:00 response (by nicehugepark)

반려 사유("이건 사용자가 확인할게 아니다 스스로 해라") 반영 — 리뷰로 넘겼던 확인 포인트 3건을 리드가 직접 검증하고 계약으로 고정했다.

1. **캡처마다 전용 프로필·회수** — 검증 완료. tests/test_shot_hygiene.py H1~H4가 전용 프로필 부여, 표식 일치 회수, **타임아웃·크래시 경로의 finally 회수**, 프로필 디렉토리 삭제를 고정한다. 실측으로도 연속 캡처 3회 뒤 `s9 doctor --json` 이 `headless_chrome: []` 를 보고했다.
2. **상한 거부** — 검증 완료. H5~H8이 상한 초과 시 브라우저 미기동+종료 4, 경계값 통과, **회수 선행 순서**, 윈도우 계수 불가 시 리눅스 폴백을 고정한다.
3. **서버 기동 대기** — 검증 완료. 옛 400회 루프는 이미 portpool.wait_server(지수 백오프, 40초/30회 이하)로 통합됐고, H9가 사설 대기 루프의 재등장을 막는다. 전체 스위트 424건 OK(skipped 1) — 서버를 띄우는 14개 파일 전부 통과.

추가로 넣은 것: 캡처 산출물 정리(`s9 doctor --fix` 가 사흘 지난 파일만 삭제, H10·H11). 프로세스만 거두고 파일을 남기면 디스크가 조용히 차는 구멍이었다.

현재 상태 실측: 공개 지연 0.27초, 윈도우 동적 포트 2168/16384(13%), 고아 테스트 서버 0, 좀비 캡처 0. 윈도우 Chrome으로 대시보드 실캡처가 정상 렌더된다(사고 당시 실패하던 경로).

재사용 지식은 DOC-20260826-008-62x6 "WSL 포트 고갈 사고 대응" 으로 분리했다 — 증상의 지문(기존 포트는 정상·신규 포트만 죽는 비대칭), 원인(호스트 동적 포트 16,384개를 WSL 중계가 독차지), 감지 임계값, 덜 파괴적인 순서의 복구(핵심은 `--recover`: 재부팅도 wsl 재시작도 필요 없다), 재발 방지 6종.

### 2026-08-26T02:45:03+09:00 response (by nicehugepark)

닫은 구멍 하나 더 — **serve 가 죽어 있는 동안의 무회수 구간**.

회수는 세 자리에서 돈다: 캡처 진입점(s9 shot 직전), serve 감시 틱(1분), 테스트 포트 풀 소진 시. 그런데 셋 다 "무언가 돌고 있을 때"만 작동한다. serve 가 내려가 있고 캡처도 안 하는 구간에는 아무도 거두지 않는다 — 2026-08-25 사고 직전이 정확히 그 구간이었다(serve 가 죽은 채 방치, 고아 브라우저 누적).

조치: 세션 시작 훅(bin/s9-audit-session)이 `s9-doctor --sweep` 을 비동기로 던진다. ensure_serve 가 서버를 되살리는 자리와 같은 체크포인트이고, 서버가 이미 떠 있어도(조기 반환 경로) 회수는 던진다. 세션 시작을 늦추지 않도록 결과를 기다리지 않고, 실패해도 무시한다.

계약: tests/test_shot_hygiene.py H12(서버가 살아 있어도 회수를 던진다·인자에 --sweep) · H13(회수 실패가 세션 시작을 막지 않는다).

이로써 누수 경로가 시간축에서도 닫힌다 — 캡처할 때, serve 가 돌 때, 세션이 시작될 때 각각 거둔다. 마지막 안전망(동적 포트 90% 도달 시 serve 가 자동 --recover)은 그대로 두되, 거기까지 가면 구조에 구멍이 있다는 신호로 기록에 남는다.

## History
- 2026-08-25T22:49:59+09:00 created by nicehugepark (status: open)
- 2026-08-25T22:49:59+09:00 status: open -> in-progress (by nicehugepark) — 리드 직접 착수 — s9 doctor 를 별도 실행파일(bin/s9-doctor)로 먼저 만든다(bin/s9 는 086 작업으로 점유 중)
- 2026-08-25T23:47:14+09:00 status: in-progress -> review (by nicehugepark) — 확인 포인트: 재발 방지 3종이 의도대로 걸리는지 — (1) s9 shot 이 캡처마다 전용 프로필을 쓰고 끝나면 회수하는지(연속 캡처 뒤 chrome.exe 가 쌓이지 않는지), (2) 헤드리스 8개 이상이면 캡처를 거부하고 안내하는지, (3) 서버 기동 대기 40초로 늘린 뒤 압박 상황에서도 테스트가 통과하는지. 감지·복구는 REQ-20260825-100-62x6 에서 확인.
- 2026-08-26T02:33:11+09:00 status: review -> in-progress (by nicehugepark) — 반려: 이건 사용자가 확인할게 아니다 스스로 해라. 스스로 할 수 있는 그리고 해야하는 리뷰는 모두 직접 처리해라. [via dashboard]
- 2026-08-26T02:43:33+09:00 status: in-progress -> done (by nicehugepark) — goal 충족 근거: (1) s9 doctor 가 공개 지연 초 단위 + 윈도우 동적 포트 소진도로 상태를 즉답하고 --fix/--recover 로 회수까지 한다, (2) 캡처·테스트·서버가 자원을 남기지 않는다 — 연속 캡처 3회 뒤 좀비 0·고아 서버 0 실측, 계약은 test_shot_hygiene 11건이 고정, (3) 전체 스위트 424건 OK. 재사용 지식은 DOC-20260826-008-62x6
