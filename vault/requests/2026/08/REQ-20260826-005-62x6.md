---
id: REQ-20260826-005-62x6
type: request
title: 요청 우선순위 가중치
summary: 요청에 가중치 형태의 우선순위를 두고, 진행·재개·조회가 그 순서를 따르도록 유도한다
status: done
size: M
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: 4449a560
sessions: ["cb49b2cd", "4449a560"]
agents: ["sub:designer:a6dfb3bd"]
contributions: [{"actor": "sub:designer:a6dfb3bd", "item": "Expose priority in dashboard", "started": "2026-08-26T09:25:41+09:00", "ended": "2026-08-26T09:25:41+09:00", "result": "running", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/4449a560-36b9-4b47-b137-50b9f72a4a6f/tasks/a6dfb3bd7e3816acb.output"}]
project: section9
tags: ["dashboard", "workflow", "session"]
created: 2026-08-26T02:28:06+09:00
status_since: 2026-08-26T12:42:41+09:00
updated: 2026-08-26T12:42:41+09:00
priority: 75
---

## Original

사용자 지시(2026-08-26 02:27): "요청에 우선 순위 개념을 가중치 형태로 추가하고 진행시 우선순위가 높은 항목 순서로 진행되게 유도해라"

## 범위
1. 스키마: request 프론트매터에 수치 가중치 필드. 기본값이 있어야 기존 문서 299건이 그대로 성립한다.
2. CLI: 생성·수정 시 지정(`s9 new --priority`, `s9 set --priority`), 조회 정렬(`s9 ls`), 검색 결과 정렬.
3. 유도: digest·SessionStart 컨텍스트가 높은 순으로 제시하고, 무인 재개(auto-resume)·항목 재개가 그 순서로 집는다. "유도"이지 강제가 아니다 — 사람이 뒤엣것을 집는 것을 막지 않는다.
4. 화면: 보드에서 우선순위가 읽히게(색면 금지 — 글자·마크로). designer 몫.

## 판단이 필요한 것 (착수 시 결정하고 근거를 남긴다)
- 척도: 연속 가중치(0~100) vs 등급(P0~P3). 사람이 매번 숫자를 고르게 하면 실제로는 안 쓰인다.
- 기본값: 명시하지 않은 문서를 어디에 둘지.
- 정렬 2차 키: 같은 우선순위면 무엇이 앞인가(생성 순? 갱신 순?).

## TDD 시나리오
- 우선순위 없는 기존 문서가 기본값으로 읽히고, 정렬에서 제자리에 놓인다.
- s9 ls 가 우선순위 내림차순으로 나온다(같으면 2차 키).
- digest 의 active 목록이 그 순서를 따른다.
- 무인 재개가 여러 후보 중 우선순위 높은 것을 먼저 집는다.
- 잘못된 값(범위 밖·비수치)은 거부되고 기존 값이 보존된다.

## Notes

### 2026-08-26T02:29:15+09:00 response (by nicehugepark)

착수 전 설계 결정 (근거 포함) — 물어보지 않고 정한다.

**척도: 정수 가중치 1~99, 기본 50. 별칭 4개를 함께 받는다(low=25 · normal=50 · high=75 · urgent=90).**
"가중치 형태로" 라는 지시를 그대로 따르되, 매번 숫자를 고르게 하면 실제로는 아무도 안 쓴다는 게 이런 필드의 흔한 결말이다. 별칭으로 대부분을 처리하고, 미세 조정이 필요할 때만 숫자를 쓴다. 저장은 언제나 숫자 하나라 정렬 규칙이 단순해진다.

**기본값 50인 이유.** 기존 문서 299건을 손대지 않아도 성립해야 한다. 중간값이면 새로 매긴 high 는 그 위로, low 는 아래로 자연히 갈린다. 0 이나 100 을 기본으로 두면 한쪽 방향이 죽는다.

**2차 정렬은 생성 순(오래된 것 먼저).** 갱신 순으로 두면 손이 자주 가는 항목이 계속 앞에 서서 뒤엣것이 굶는다. 우선순위가 같다면 오래 기다린 쪽이 먼저다.

**"유도"의 범위.** digest·SessionStart 목록과 무인 재개(auto-resume)·항목 재개가 높은 순으로 집는다. 사람이 뒤엣것을 집는 것은 막지 않는다 — 지시가 "유도"였다.

**잘못된 값은 거부하고 기존 값을 보존한다.** 범위 밖·비수치는 실패로 끝내고 문서를 건드리지 않는다. 조용히 기본값으로 되돌리면 사용자가 매긴 우선순위가 소리 없이 사라진다.

착수는 전체 스위트가 끝난 뒤에 한다 — 지금 스위트가 bin/s9 를 서브프로세스로 두드리고 있어 그 파일을 고치면 결과가 흔들린다. 화면 표기(보드에서 우선순위가 읽히게)는 designer 몫으로 분리한다.

### 2026-08-26T02:36:27+09:00 response (by nicehugepark)

구현 완료. 커밋 59229a9. 전체 스위트 413건 green.

들어간 것:
- 스키마: request 프론트매터 `priority` 정수(1~99, 기본 50). 값이 없거나 망가진 문서는 기본값으로 읽혀 제자리(중간)에 놓인다 — 기존 299건을 백필하지 않아도 성립한다.
- CLI: `s9 new --priority`, `s9 set <id> --priority`. 별칭 low/normal/high/urgent 를 받되 저장은 언제나 수치다.
- 순서: `work_order()` 한 곳에 규칙을 두고(가중치 내림차순, 동률이면 생성 순) ls·search 출력, digest active 목록, 무인 재개 워커의 후보 선택이 그것을 쓴다. 규칙이 여러 군데로 흩어지면 화면마다 순서가 달라진다.
- 표기: 기본값과 다를 때만 `!75`. 전부 `!50` 이 붙으면 정작 다른 것이 눈에 안 들어온다.

"유도"의 경계를 이렇게 그었다: ls·search·digest 는 **순서만 바꾸고 막지 않는다**. 반면 무인 재개(반려 재작업·승인 후속)는 사람이 순서를 고를 수 없는 자리라, 거기서는 이 순서가 실제로 집는 순서가 된다.

실환경 확인: 이 문서에 high 를 매기니 `s9 ls --status open` 과 digest 양쪽에서 맨 위로 올라오고 `!75` 가 붙는다.

테스트 12건(별칭·범위·기본값·정렬 2차 키·거부 시 기존 값 보존·표기 조건). 특히 거부 경로를 고정한 이유는, 조용히 기본값으로 되돌리면 사람이 매긴 우선순위가 사라졌다는 사실조차 안 남기 때문이다.

남긴 것: 보드에서 우선순위가 읽히게 하는 화면 작업은 designer 몫으로 분리한다(색면 금지 — 글자·마크로).

### 2026-08-26T09:14:20+09:00 response (by nicehugepark)

반려 수용 — 지적이 맞다. 내가 "기본값은 조용히"로 정한 탓에 **아무 데서도 보이지 않는 기능**이 됐다. 오늘 만든 문서는 전부 기본값 50이니 목록에 !N 이 하나도 안 붙고, 대시보드 카드에는 애초에 표기를 안 넣었다(designer 몫으로 미뤘다). 그러니 있는지 없는지조차 판단할 수 없는 게 당연하다.

판단 착오의 요지: 나는 "전부 !50 이 붙으면 시끄럽다"를 걱정했는데, 그건 이미 값이 쓰이고 있을 때의 문제다. 도입 직후에는 **보이지 않는 축은 없는 축**이고, 사용자가 값을 매길 계기조차 생기지 않는다. 시끄러움보다 부재가 나쁘다.

재작업 방향:
1. CLI 목록(ls·search·digest)에서 가중치를 **항상** 표기한다. 기본값도 보여야 축이 존재한다는 것이 읽히고, 무엇을 올릴지 판단할 수 있다.
2. 대시보드 카드에 우선순위를 노출한다 — designer 에 위임(색면 금지, 글자·마크로).
3. 문서 프론트매터에는 이미 priority 가 들어가 있다(`s9 show <id> --meta` 로 확인 가능) — 이건 유지한다.

### 2026-08-26T09:23:40+09:00 designer (by nicehugepark)

화면 표기 완료 (web/index.html). 반려 사유("우선순위 값이 하나도 보이지 않는다")의 직접 원인은 CLI가 채택한 "기본값과 다를 때만 표기" 규칙이다 — 도입 직후라 250건이 전부 50이어서 그 규칙 아래에서는 화면에 아무것도 안 뜬다. 대시보드는 반대로 간다: 항상 그린다.

어디에 붙였나 (request 문서에만 — knowledge/session에는 이 축이 없다)
- 보드 카드: id 줄 맨 앞. 3ch 우측정렬이라 컬럼 안에서 숫자가 한 줄로 선다(장부의 숫자 열).
- Docs 목록 행: 같은 자리, 같은 규칙.
- 문서 뷰어 메타: SIZE 다음에 PRIORITY 행 — `75 높음 · 높을수록 먼저 (기본 50)`. 목록과 달리 여기는 읽는 자리라 뜻을 함께 적는다. 사용자가 "숨겨져 있는 건가"라고 물은 곳이 문서 화면이므로 이 행이 반려에 대한 직접 답이다.

위계 (색면·띠 없이)
- 보통(38~62)·낮음(~37): mono 10px 400 var(--muted). 배경으로 물러나되 읽히기는 한다.
- 높음(63~82): mono 11.5px 700 var(--text). 잉크가 진해지고 커져서 50들 사이에서 먼저 잡힌다.
- 긴급(83~): 11.5px 700 var(--c-blocked) + 앞의 `!` 마크. 색맹·흑백에서도 `!`와 굵기로 갈린다(색만으로 구분하지 않는다).
띠 경계는 별칭 25/50/75/90의 중간값이고, 표기 위계와 이름(낮음/보통/높음/긴급)을 같은 띠에서 뽑는다 — 55 같은 미세 조정값이 "보통인데 굵게" 같은 식으로 따로 놀지 않는다. 값이 없거나 범위 밖이면 50으로 읽는다(s9 work_order와 같은 규칙).

접근성 수정: 처음엔 보통 티어를 var(--faint)로 잡았는데 10px에서 대비 3:1 미만이라 var(--muted)로 올렸다. 낮음도 같은 스타일이다 — 낮음과 보통을 가르는 것은 숫자 자체(25 vs 50)이고, 둘 다 "배경" 티어라는 사실이 더 중요하다.

대안 비교
(가) 채택 — 숫자 그대로 + 타이포 위계. 값을 읽을 수 있고(55와 75가 구분된다), 기본값도 보이며, 정렬된 열이라 훑기 쉽다. 새 컴포넌트 0개.
(나) 4단 눈금 게이지(막대 4칸) — 값의 크기는 빨리 읽히지만 정확한 수치를 잃고, 작은 사각형 무리가 결국 색면·띠로 읽힌다. 금지된 문법에 가깝다. 기각.
(다) 별칭 단어만("보통"/"높음") — 250건의 "보통"이 카드마다 한국어 두 글자로 앉아 제목과 경쟁한다. 미세 조정값도 표현할 수 없다. 기각. (뷰어에서는 읽는 자리라 숫자와 함께 쓴다.)
(라) 기본값 숨김(CLI와 동일) — 이번 반려의 원인 그 자체. 기각.

정렬은 건드리지 않았다 — 지시대로 서버가 준 순서를 그대로 쓴다. 다만 보드/Docs 목록의 클라이언트 정렬은 예전부터 최신순(sortRecent)이라, 화면 순서는 여전히 우선순위 순이 아니다. 표기와 순서가 어긋나는 셈인데 이건 화면 표기 범위 밖 결정이라 리드 판단으로 남긴다.

검증 (실브라우저 9909, 캡처 state/shots/req005/)
- 01-board-real.png — 실제 데이터. 250건 중 REQ-20260826-005의 `75`만 굵게 떠오르고 나머지 `50`은 배경으로 눕는다. 의도한 위계가 실데이터에서 성립.
- 02-board-tiers.png — 네 띠(25/50/75/!90)를 한 화면에 세운 표시 검증(클라이언트 catalog의 priority만 바꾼 렌더 테스트, 서버 데이터 무변경). 열 정렬 유지 확인.
- 03-docs-list.png — Docs 목록 행 표기.
- 04-viewer-meta.png — 문서 뷰어 PRIORITY 행.
- 05-board-tiers-carbon.png — ledger/carbon(다크)에서 잉크·긴급색·마크가 그대로 성립.
검증용 iframe 하네스는 캡처 후 삭제했다. 전체 스위트 428건 green.

### 2026-08-26T09:26:47+09:00 response (by nicehugepark)

재작업 완료 — 이제 화면에서 보인다. 커밋 3cccba9(CLI) + 949daef(화면). 전체 428건 green.

리드가 캡처를 직접 열어 확인했다(state/shots/req005/02-board-tiers.png): 보드 카드마다 가중치가 붙고, 위계가 색면이 아니라 크기·굵기·잉크 농도로 갈린다 — 25 흐리게, 50 배경으로, 75 진한 잉크, 90 은 ! 마크. 기본값 50 도 반드시 보인다(반려 사유의 핵심).

- CLI: ls·search·digest 가 항상 표기(`REQ-… !50 [open] …`).
- 화면: 보드 카드·Docs 행·문서 뷰어 메타.
- 테스트: 기본값이 보일 것·digest 에도 보일 것을 고정(13건).

이번 반려에서 얻은 판단 하나를 남긴다 — **도입 직후의 기본값은 숨기면 안 된다.** "전부 같은 값이면 시끄럽다"는 이미 값이 쓰이고 있을 때의 걱정이고, 축이 새로 생긴 시점에는 보이지 않는 축이 곧 없는 축이다. 사용자가 값을 매길 계기 자체가 생기지 않는다.

### 2026-08-26T09:34:58+09:00 designer (by nicehugepark)

designer 재작업 검증 — 표기안: 카드 메타 줄(.m) 맨 앞에 [게이지 마크 + 숫자]. 등급을 색면이 아니라 세 채널로 동시에 말한다(블록 글리프 높이 ▂▃▅▇ · 잉크색 · 글자 무게). 기본값 50도 항상 그린다. 배경 칠·세로 띠·pill 없음, 카드 높이 불변(카드 상단 좌표 226/303/378/453 변화 없음 확인). 검증: 10개 skin × light/dark 실브라우저 캡처 — ledger/paper, calm/carbon, soft/carbon, glass/mist, terminal/paper, grid/system, slate/graphite, cobalt/phosphor, field/paper, cork/graphite. 문서 뷰어 PRIORITY 행(size 위)과 Docs 목록 행도 확인. 캡처: scratchpad/final-*.png. 주의: 09:20~09:33 사이 다른 작업자가 같은 web/index.html을 동시에 편집했다 — 최종 파일은 .prio/prioHTML 이름의 그쪽 구현이며 설계는 동일(마크+숫자, 3채널)이다. 남은 한계는 designer 보고 참조.

### 2026-08-26T09:37:00+09:00 designer (by nicehugepark)

designer 재작업 완료 — 우선순위를 기본값 50까지 항상 표기. 보드 카드 메타 줄 맨 앞(size보다 앞)에 span.prio[data-prio][data-tier], 문서 뷰어 메타 표에 priority 행 추가. 등급 파생은 prioTier(p) 한 곳(90/75/50 경계)에서만 하고 카드·Docs 행·뷰어가 공유한다. 시각 처리는 색면 없이 글자색·무게·게이지 마크 높이 3채널: low 400/2-8, normal 500/3-8 muted, high 700/5-8 c-inprogress, urgent 700/7-8 c-blocked+자간. 10개 스킨 light/dark 캡처 확인(scratchpad/tier-*.png, prio-real-*.png, prio-viewer-*.png). 주의: ?theme=light|dark 는 유효 tone 이 아니다 — 다크는 theme=carbon|graphite.

### 2026-08-26T09:42:21+09:00 response (by nicehugepark)

재작업 마무리 — 남아 있던 결함 하나를 닫았다: **표기는 붙었는데 순서가 안 따라왔다.**

앞선 재작업(커밋 3cccba9·949daef)으로 카드·Docs 행·문서 뷰어에 가중치가 항상 보이게 됐지만, 보드의 클라이언트 정렬은 예전 그대로 최신 갱신순이었다. 축을 보여주고 순서는 그대로 두면 다음 지적은 "높게 매겼는데 왜 그 자리 그대로냐"가 된다. 표기와 순서가 어긋나면 사용자는 값을 매길 이유를 찾지 못한다.

**고친 것 (web/index.html)**
- 정렬 진입점을 `sortRecent` → `workOrder` 하나로 바꿨다. 1차 키는 우선순위 내림차순, 2차 키는 최근 갱신순. `filtered()` 한 곳에서만 걸어 Board·Docs·Graph 가 같은 순서를 쓴다 — 규칙이 흩어지면 탭마다 순서가 달라져 우선순위를 신뢰할 수 없다.
- 값 읽기는 표기와 같은 `prioOf()` 를 거친다. 정렬만 raw `r.priority` 를 보면 값이 없거나 망가진 문서에서 순서와 표기가 갈린다.

**2차 키를 CLI와 다르게 둔 판단.** CLI `work_order()` 는 생성 순(오래 기다린 것 먼저)이다 — 대기열이라 굶주림을 막아야 한다. 보드는 대기열이 아니라 지금 상태를 보는 화면이고, done 228건이 가장 오래된 것부터 쌓이면 그 컬럼이 쓸모없어진다. 같은 우선순위 안에서는 최근 갱신순을 유지했다. 두 순서가 다른 것은 두 화면의 용도가 다르기 때문이지 규칙이 흩어진 것이 아니다.

**표기 (designer 위임 결과, 현재 파일 상태)**
- 카드 메타 줄(.m) 맨 앞 — `size` 보다 앞이다. 무엇부터 집을지가 크기보다 먼저 읽혀야 한다.
- 등급을 세 채널이 동시에 말한다: 게이지 마크 높이(▂▃▅▇) · 잉크색 · 글자 무게. 색을 못 보는 조건에서도 마크 높이와 굵기로 갈린다. 배경 칠·세로 띠·pill 없음, 카드 높이 불변(카드 상단 좌표 변화 없음을 캡처로 확인).
- 등급 파생은 `prioTier()` 한 곳(90/75/50 경계)뿐 — 카드·Docs 행·뷰어가 같은 함수를 쓰므로 세 화면의 등급이 갈릴 수 없다.

**테스트 (tests/test_priority_visible.py)**
계약을 마크업 위치가 아니라 동작으로 고정했다 — 표기가 공용 함수 `prioHTML()` 로 묶여 있는데 "카드 안에 마크업 문자열이 있는가"를 검사하면 올바른 공용화가 회귀로 잡힌다. 검사하는 것: 카드가 조건 없이 그 함수를 부르는가(기본값 숨김 회귀 차단) · 그 함수가 data-prio/data-tier/숫자를 함께 내는가 · 세 화면이 같은 렌더러를 쓰는가 · 색면을 칠하지 않는가 · 전부 무채색은 아닌가 · 정렬 1차 키가 우선순위인가.

**검증**
- 전체 스위트 444건 green (skip 1).
- 실화면 캡처 `scratchpad/order-calm-carbon.png` — 카드마다 `▃50` 이 붙고, `▅75` 인 이 요청이 review 컬럼 맨 위에 선다. 정렬 변경이 실데이터에서 성립하는 것을 눈으로 확인했다.
- designer 가 10개 스킨 × light/dark 를 실브라우저로 확인 (`scratchpad/final-*.png`).
- 검증용 임시 하네스 `web/verify-prio.html` 은 지웠다.

**정리해 둘 사실 두 가지**
- 이 세션은 커밋 권한이 없다(allowlist 가 s9 로 시작하는 명령만 허용). 위 변경(`web/index.html` · `tests/test_priority_visible.py`)은 작업 트리에 있고 커밋되지 않았다. `s9 sync` 도 비활성이라 자동으로 실리지 않는다 — 커밋은 리드 몫으로 남긴다.
- 09:20~09:33 사이 이 요청에 작업자가 둘 붙었다(cb49b2cd · 4449a560). 같은 `web/index.html` 을 동시에 고쳐 한쪽 구현이 다른 쪽으로 덮였다. 최종 파일은 일관되고 잔재(`priHTML`·`.pri`·`PRI_BANDS`)가 없음을 확인했다. 중복 스폰 방지는 클레임(`s9 last --add --session`)에 달려 있는데 이번엔 클레임 이후에도 두 번째가 이미 붙어 있었다 — 워처가 스폰 직전에 클레임을 다시 확인해야 한다는 신호다. bin/ 범위라 제안으로만 남긴다.
- CLI 목록의 항상 표기(재작업 방향 1번)는 다른 작업자가 커밋 3cccba9 로 이미 반영했다. 이 세션은 bin/ 을 건드리지 않았다.

## History
- 2026-08-26T02:28:06+09:00 created by nicehugepark (status: open)
- 2026-08-26T02:36:36+09:00 status: open -> in-progress (by nicehugepark) — 착수(사후 기록) — 구현·검증을 마친 뒤 상태를 맞춘다.
- 2026-08-26T02:36:36+09:00 status: in-progress -> review (by nicehugepark) — 확인 포인트: (1) 척도 — 정수 1~99 + 별칭 4개(low 25/normal 50/high 75/urgent 90). 등급만 두는 편이 나은지, 지금처럼 숫자도 열어두는 게 맞는지. (2) 동률 2차 키를 생성 순(오래 기다린 것 먼저)으로 둔 판단 — 갱신 순은 자주 손대는 항목이 뒤엣것을 굶긴다고 봤다. (3) 유도의 경계 — 무인 재개만 강제 순서고 나머지는 순서 제시에 그친다. (4) 기존 299건은 전부 기본값 50이다: review 대기 18건에 내가 위험도 기준으로 초안을 매겨 올릴지, 앞으로 들어오는 것부터 쓸지 정해달라.
- 2026-08-26T09:13:50+09:00 status: review -> in-progress (by nicehugepark) — 반려: 오늘 새로 만든 요청 문서에서 우선순위 값이 하나도 보이지 않는다. 숨겨져있는건가? 판단할 수 없다. [via dashboard]
- 2026-08-26T09:26:47+09:00 status: in-progress -> review (by nicehugepark) — 재상신 — 확인 포인트: (1) 보드에서 우선순위가 이제 읽히는지, 그리고 50이 화면을 지배하지 않는지(위계가 크기·굵기·농도로만 갈린다). (2) 기존 299건은 전부 50이다 — review 대기 건들에 내가 위험도 기준으로 초안을 매겨 올릴지, 앞으로 들어오는 것부터 쓸지. (3) 척도(1~99 + 별칭 4개)와 동률 2차 키(오래 기다린 것 먼저) 판단. 캡처: state/shots/req005/
- 2026-08-26T09:42:41+09:00 status: review -> in-progress (by nicehugepark) — 정렬 보완 착수(반려 아님) — 표기는 붙었으나 보드 정렬이 여전히 최신순이라 우선순위 순서가 화면에 반영되지 않았다. 같은 세션(4449a560)이 이어서 처리한다.
- 2026-08-26T09:42:50+09:00 status: in-progress -> review (by nicehugepark) — 재상신(정렬 보완) — 확인 포인트: (1) 보드에서 우선순위가 읽히는가. 카드 메타 줄 맨 앞에 게이지 마크+숫자로 항상 뜬다(기본값 50도). 지금 실데이터는 이 요청만 75라 ▅75 하나가 ▃50 사이에서 떠오르는 모습이 전부다 — 그 대비가 충분한지, 아니면 50이 화면을 지배하는지 봐 달라. (2) 순서: 이제 보드·Docs·Graph 가 우선순위 내림차순으로 선다(review 컬럼 맨 위가 이 요청). 동률 2차 키는 보드만 최근 갱신순이고 CLI 는 생성 순이다 — 보드의 done 228건을 오래된 것부터 쌓지 않으려는 판단인데, 두 순서가 다른 것이 혼란스러운지. (3) 등급 경계를 별칭 값 그대로 뒀다(>=90 긴급 · >=75 높음 · >=50 보통 · 그 아래 낮음). 74는 보통, 89는 높음으로 읽힌다 — 중간값 경계(83/63/38)로 바꾸면 89가 긴급이 된다. 어느 쪽을 쓸지. (4) 기존 250건이 전부 50이다: 내가 위험도 기준으로 초안을 매겨 올릴지, 앞으로 들어오는 것부터 쓸지. (5) 이 세션은 커밋 권한이 없어 web/index.html · tests/test_priority_visible.py 변경이 작업 트리에만 있다 — 커밋이 필요하다. 전체 스위트 444건 green. 캡처: scratchpad/order-calm-carbon.png, scratchpad/final-*.png
- 2026-08-26T12:42:41+09:00 status: review -> done (by nicehugepark) — [via dashboard]
