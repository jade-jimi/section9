---
id: REQ-20260829-013-62x6
type: request
title: 터미널이 살아 있는지 안 보인다
summary: 대시보드 터미널에 에이전트의 진행 문장·경과시간·대기 표시가 오지 않아 도는 중인지 멈췄는지 알 수 없다
goal: 대시보드 터미널만 보고도 지금 무엇을 하는 중인지·얼마나 됐는지를 알 수 있다
status: in-progress
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: b0cce452
agents: ["sub:deep-diver:a4eb5479", "sub:subagent:af4d03bb"]
contributions: [{"actor": "sub:deep-diver:a4eb5479", "item": "Trace dashboard terminal stream gap", "started": "2026-08-29T10:31:40+09:00", "ended": "2026-08-29T10:38:03+09:00", "result": "done", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/b0cce452-7815-44d0-8bb7-ce1e7b28c6da/tasks/a4eb54796e9796266.output"}, {"actor": "sub:subagent:af4d03bb", "item": "Appending findings note to REQ-20260829-013-62x6", "started": "2026-08-29T10:37:58+09:00", "ended": "2026-08-29T10:37:58+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/b0cce452-7815-44d0-8bb7-ce1e7b28c6da/subagents/agent-af4d03bbc5490e183.jsonl"}, {"actor": "sub:deep-diver:a4eb5479", "item": "`[2026-08-29 10:42:11 KST - deep-diver]`", "started": "2026-08-29T10:38:03+09:00", "ended": "2026-08-29T10:38:03+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/b0cce452-7815-44d0-8bb7-ce1e7b28c6da/subagents/agent-a4eb54796e9796266.jsonl"}]
project: section9
relates_why: {}
tags: ["auto-audit", "terminal", "assets", "dashboard", "attached", "png", "image"]
created: 2026-08-29T10:25:18+09:00
status_since: 2026-08-29T10:31:07+09:00
updated: 2026-08-29T10:38:03+09:00
priority: 50
---

## Original

[선행 대화 — 직전 미문서화 메시지]
- (10:10) 또 지금 멈춤이라도 뜬 요청이 3개나 있는데, 진짜 멈춘건가???

로컬 터미널에서 실행 중인 클로드 코드 화면의 디자이너 에이전트는 응답하는 내용이 훨씬 풍부하다. 같은 디자이너 에이전트가 대시보드 터미널에서는 일부만 출력된다.
[Image: assets/REQ-20260829-013-62x6/20260829T102439-image.png]

## Notes

### 2026-08-29T10:26:11+09:00 ask (by nicehugepark)

그래서 대시보드 터미널에서는 시간의 흐림이나 로그의 흐름, 웨이팅 스크립트 같은 장치가 없어서 작동 중인지 아닌지 즉각 판단이 어렵다.

### 2026-08-29T10:32:28+09:00 ask (by nicehugepark)

리드 에이전트가 아니라 서브에이전트의 상황을 말한거다.

### 2026-08-29T10:37:30+09:00 deep-diver (by nicehugepark)

[규명] 서브에이전트의 말이 대시보드 터미널에 안 오는 이유 — 층별 확정 (고치지 않음)

### 원천은 두 파일이고, 대시보드 메인 판은 그중 하나만 읽는다

대시보드 터미널 메인 타임라인의 원천 = 리드 세션의 네이티브 transcript
(`state/sessions/<machine>__<sid8>.json` 의 `transcript_path` →
`~/.claude/projects/-home-sjpark1-section9/<sessionUUID>.jsonl`).
`resolve_stream_path` bin/s9:11395. 없으면 미러 `streams/<sid>.jsonl`.
(`state/terminal/inbox-*.jsonl` 은 대시보드→세션 **채팅 수신함**이지 표시 원천이 아니다 — bin/s9:2206.)

서브에이전트의 말은 이 파일에 **한 줄도 없다**. 전수 확인:
이 머신의 `~/.claude/projects/**/*.jsonl` 중 `"isSidechain":true` 를 가진 파일은
**예외 없이 `<sessionUUID>/subagents/agent-<agentId>.jsonl`** 뿐이고 최상위 세션
transcript 에는 0건이다. 이번 세션 b0cce452 리드 transcript 864줄 중 isSidechain=0.
`/tmp/claude-1000/.../tasks/<agentId>.output` 은 그 subagents 파일로의 **심볼릭 링크**다
— 원천은 하나고 이름이 둘이다.

### 원천에 있는 것 / 없는 것 (이 구분이 핵심)

있다(그대로 흘리면 되는 것):
- 서브에이전트 진행 문장 — `agent-a546747613cc0a1ef.jsonl`(designer) 에 assistant/text 로
  timestamp 와 함께 실재. 실측 발췌: "TDD: 계약을 먼저 잠근다." / "Green. 이제 실브라우저로
  본다." / "Red(5). 이제 고친다."  ← 사용자가 캡처에서 본 바로 그 문장들.
- 도구 호출 하나하나(tool_use). Bash 면 `input.command` 원문 — 캡처의 대기 스크립트
  `until grep -qE "^(OK|FAILED)" …; sleep 10; done` 명령줄도 원천에 그대로 있다.
- 매 줄 `timestamp`, `message.usage.output_tokens`.

없다(대시보드가 다른 재료로 만들어야 하는 것):
- `Searched for 2 patterns, read 1 file, ran 7 shell commands` — 연속 tool_use 를 센 요약.
- `Reading 5 files, running 5 shell commands · 2m 16s…` — tool_result 가 아직 안 붙은
  tool_use 들 + 마지막 timestamp→현재 로 파생.
- 개별 도구 경과 `(2m 16s)`, 헤더 `Spelunking… (53m 34s · ↓ 325.5k tokens)` 의 동사/누적.
즉 **재료는 다 있고 셈만 없다.**

### 층별 판정

L1 원천: 있다. 단, 리드 파일이 아니라 서브에이전트 파일에.
L2 서버: `/api/stream`·`/api/stream/sse` → `stream_events`(bin/s9:11435) → `parse_stream_file`
  (11442) 가 **리드 transcript 한 파일만** 연다. 서브에이전트 내용은 이 API 로 절대 안 나온다.
  이 함수가 붙이는 `agent = bool(o.get("isSidechain"))` (bin/s9:11464) 은 지금 구조에서
  **영원히 False** — 죽은 플래그다(옛 CC 레이아웃 유물).
  반면 `/api/agents`(11929)는 서브에이전트를 안다: 리드 transcript 의 Agent tool_use ↔
  tool_result 의 `agentId:`/`output_file:` 정규식으로 매핑(`session_agents_map` 11506) 후
  output 파일 증분 스캔(`agent_file_stats` 11612)으로 label·tokens·first_ts 를 낸다.
  `/api/agentstream`(11961)은 그 파일을 통째로 내줄 수 있다 — **에이전트를 지목해야만.**
L3 화면: 메인 판 `#ccout` 은 `/api/stream` 만 그린다. `ccEvent()` 의 사이드체인 분기
  `if (e.agent){ … ⚙ agent · … }` (web/index.html:7478) 는 **도달 불가능한 죽은 코드**다.
  스폰은 `e.name==="Agent"` 분기(7515)가 `designer(desc)` + `⎿ Backgrounded agent` 두 줄만
  찍고 끝 — 완료 통지까지 메인 판에 한 글자도 안 온다. 내용을 보려면 스트립 행 클릭/← 로
  `termAgentOpen`(8715)이 메인 판을 **숨기고** `#cc-agview` 로 갈아끼워야 한다(동시 관람 불가).

### 캡처의 세 조각은 어디서 오는가

- `designer`(이름) ← `/api/agents.type` = Agent tool_use 의 `subagent_type`. 있다.
- `Checking pinhead render in terminal skin`(지금 하는 일) ← `/api/agents.label` =
  `_agent_label`(bin/s9:11564). **있지만 망가져 있다** — text 경로가 걸리면 첫 줄을 쓰는데,
  이 저장소 규약상 모든 응답이 `` `[시각 - 역할명]` `` 로 시작한다. 실측 응답:
  designer → "`[2026-08-29 10:20:37 KST - designer]`", ux-writer → "`[2026-08-29 09:41:38 KST - ux-writer]`".
  "지금 무엇을 하는가" 자리에 **시각 도장**이 찍힌다.
- `53m 33s · ↓ 325.5k tokens` ← `/api/agents.elapsed`(first_ts→now)·`.tokens`(usage 합). 있다.
  다만 화면은 10초 폴 때만 갱신하고 그 사이 초가 흐르지 않는다(`fmtDur(a.elapsed)` 정적 문자열,
  web/index.html:8207). 카드의 1초 갱신(`tickElapsed` 3411)에 해당하는 것이 스트립엔 없다.

### 화면이 이미 가진 것, 그런데 왜 안 도는가

가진 것: 에이전트 스트립 `#cc-agents`(DOM 7642, `termAgentsRender` 8187, 10초 폴 7699-7714),
대기 스피너 `#cc-wait`(`termSpinnerEval` 8135 — 회전 동사·1초 경과·"응답까지 Ns" 마감),
압축 표시(`termCompactSync` 7728), 에이전트 뷰어(8715), `Ran N shell commands` 묶음(7583).
즉 **없어서 못 보는 게 아니라, 있는 것이 서브에이전트에는 안 걸린다.**

안 도는 이유 둘(실측):
1) 스트립이 `active` 만 그린다 — `filter(a => a.active)`(web/index.html:8192).
   `active` = `actor_alive("sub:…", age=now-mtime)`(bin/s9:11957) → `HEALTH_WIN["sub"]=180`(7474).
   서브에이전트가 한 도구에 3분 넘게 머물면(캡처의 `until … sleep 10` 대기 루프, 브라우저 캡처,
   긴 테스트) output 파일 mtime 이 안 움직여 `active:false` → **행이 화면에서 사라진다.**
   지금 `s9 agents health` 실측: designer 347초째·ux-writer 3204초째 stalled.
   같은 뿌리가 Board 의 "멈춤" 3건(사용자 10:10 발언)도 만들었다.
2) 메인 판 스피너가 리드 기준이다 — `const on = !!T.sid && (T.lastRole === "user" || "tool")`
   (web/index.html:8138). 리드가 async 에이전트를 띄우고 조용해지면 마지막 이벤트가
   `assistant`/`result` 라 스피너가 꺼진다. 서브에이전트가 한 시간을 일해도 메인 판은 정지 화면.
   실측: 지금 b0cce452 스트림 마지막 이벤트 = `assistant`.

### 고치려면 볼 자리 (판정은 리드)

- 합류: `stream_events`/`parse_stream_file` bin/s9:11435-11504 — 리드 파일 하나만 여는 구조.
  `session_agents_map` 이 이미 아는 output 경로를 함께 읽어 timestamp 로 머지해야 한다.
  `offset` 이 단일 파일 바이트값이라 다중 파일 커서로 API 계약이 바뀐다(가장 무거운 지점).
- 라벨: `_agent_label` bin/s9:11564 (시각 도장 회피).
- 생존 판정: `HEALTH_WIN` bin/s9:7474 · `judge_health` 7703 · `/api/agents` active 11957 ·
  `termAgentsRender` filter web/index.html:8192 (사라지는 대신 "대기 중"으로 남기기).
- 메인 스피너: `termSpinnerEval` web/index.html:8135 (서브에이전트 활동도 근거로).
- 스트립 초 갱신: `termAgentsRender` 8187 + `tickElapsed` 3411 패턴.
- 죽은 코드: `ccEvent` 사이드체인 분기 web/index.html:7478, `parse_stream_file` 의 agent 필드 bin/s9:11464.

증거계층: 전부 1차(실파일·실 API 응답·실행 출력). 확신도 높음.
남은 미지수: CC 가 언제 사이드체인을 별도 파일로 옮겼는지(공개 코드 이후 상류 변경이라
git 이력으로는 추적 불가 — 이 저장소의 첫 커밋에 이미 옛 구조 전제 코드가 들어 있다).

### 2026-08-29T10:37:58+09:00 subagent (agent: sub:subagent:af4d03bb, by nicehugepark)

Appending findings note to REQ-20260829-013-62x6

### 2026-08-29T10:38:03+09:00 subagent (agent: sub:deep-diver:a4eb5479, by nicehugepark)

`[2026-08-29 10:42:11 KST - deep-diver]`

규명을 마쳤습니다. 기록은 `REQ-20260829-013-62x6` 에 `--label deep-diver` 로 남겼습니다.

**층별 결론.** 원천은 두 파일인데 대시보드 메인 판은 그중 하나만 읽습니다 — 서브에이전트의 말은 리드 세션 transcript(`~/.claude/projects/-home-sjpark1-section9/<sessionUUID>.jsonl`)에 한 줄도 없고, 전부 `<sessionUUID>/subagents/agent-<agentId>.jsonl` 이라는 별도 파일에 `isSidechain:true` 로 들어갑니다(리드가 받는 `/tmp/.../tasks/<agentId>.output` 은 그 파일로의 심볼릭 링크일 뿐 별개 파일이 아닙니다). 이 머신 전체를 훑어 확인했습니다: `isSidechain:true` 를 가진 파일은 예외 없이 `subagents/` 안이고 최상위 세션 transcript 에는 0건, 이번 세션도 864줄 중 0건입니다. 그러니 **사라지는 지점은 서버 층**입니다 — `/api/stream` 의 `parse_stream_file`(bin/s9:11442)이 리드 파일 하나만 열고, 거기에 붙는 `agent = bool(o.get("isSidechain"))`(bin/s9:11464)과 그것을 받는 화면의 사이드체인 렌더 분기(web/index.html:7478)는 옛 Claude Code 레이아웃 유물로 지금은 **도달 불가능한 죽은 코드**입니다. 메인 판에 남는 건 스폰 두 줄(`designer(desc)` + `⎿ Backgrounded agent`)과 완료 통지뿐이고, 그 사이는 통째로 0입니다(일부만 취하는 게 아닙니다).

**경과시간·대기 표시는 대부분 원천에 있습니다.** designer 의 진행 문장("Green. 이제 실브라우저로 본다.", "Red(5). 이제 고친다.")도, 캡처의 대기 스크립트 명령줄(`until … sleep 10; done`)도, 줄마다의 `timestamp` 와 `usage.output_tokens` 도 서브에이전트 파일에 그대로 있습니다. 원천에 **없는** 것은 CLI 가 화면에서 셈해 만드는 것들뿐입니다 — `Ran 6 shell commands` 같은 묶음 요약, `Reading 5 files … · 2m 16s…` 진행 표시, `Spelunking… (53m 34s)` 헤더. 재료는 다 있고 셈만 없습니다.

**화면은 이미 상당수를 갖고 있는데 서브에이전트에는 안 걸립니다.** 캡처 하단의 세 조각(이름·하는 일·경과/토큰)은 `/api/agents` 가 이미 내주고 있고 실제 응답으로 확인했습니다. 다만 두 군데가 고장 나 있습니다. 하나, 라벨(`_agent_label`, bin/s9:11564)이 응답 첫 줄을 쓰는데 이 저장소 규약상 모든 응답이 `` `[시각 - 역할명]` `` 로 시작해서 "지금 하는 일" 자리에 **시각 도장**만 찍힙니다(실측: designer 의 label 이 `` `[2026-08-29 10:20:37 KST - designer]` ``). 둘, 스트립이 `active` 인 행만 그리는데(web/index.html:8192) `active` 판정 창이 180초(`HEALTH_WIN["sub"]`, bin/s9:7474)라 **한 도구에 3분 넘게 머무는 서브에이전트는 행 자체가 화면에서 사라집니다** — 캡처의 `2m 16s` 짜리 대기 루프가 바로 그 경우이고, 지금 실측으로도 designer 347초·ux-writer 3204초째 stalled 입니다. 사용자가 10:10 에 말한 "멈춤 3건"도 같은 뿌리입니다. 여기에 메인 판 스피너가 리드의 마지막 이벤트만 보는 탓에(web/index.html:8138) 리드가 async 에이전트를 띄우고 조용해지면 스피너까지 꺼져, 서브에이전트가 한 시간을 일해도 화면은 완전 정지입니다.

**고치려면 볼 자리**(설계는 하지 않았습니다): 합류는 `stream_events`/`parse_stream_file`(bin/s9:11435-11504) — `session_agents_map` 이 이미 아는 output 경로를 함께 읽어 timestamp 로 머지해야 하고, `offset` 이 단일 파일 바이트값이라 API 계약이 바뀌는 것이 가장 무거운 지점입니다. 라벨은 `_agent_label`(bin/s9:11564), 사라짐은 `HEALTH_WIN`(bin/s9:7474)·`/api/agents` 의 active(bin/s9:11957)·스트립 필터(web/index.html:8192), 스피너는 `termSpinnerEval`(web/index.html:8135), 스트립 초 갱신은 `termAgentsRender`(8187)에 카드의 `tickElapsed`(3411) 패턴, 죽은 코드는 web/index.html:7478 과 bin/s9:11464 입니다. 증거는 전부 1차(실파일·실 API 응답·`s9 agents health` 출력)이며 확신도는 높습니다. 남은 미지수는 상류 Claude Code 가 언제 사이드체인을 별도 파일로 옮겼는지로, 이 저장소 첫 커밋에 이미 옛 구조 전제 코드가 있어 git 이력으로는 추적되지 않습니다.

## History
- 2026-08-29T10:25:18+09:00 created by nicehugepark (status: open)
- 2026-08-29T10:31:07+09:00 status: open -> in-progress (by nicehugepark) — 로컬 CLI 는 진행 문장·경과시간·대기 스크립트를 흘리는데 대시보드에는 그 층이 오지 않는다. 무엇이 어디서 끊기는지부터 규명한다.
