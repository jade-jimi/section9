---
id: REQ-20260823-073
type: request
title: 프로젝트 멤버관리 UI
summary: 대시보드에서 프로젝트 멤버 추가/역할·직무·만료 변경/제거를 할 수 있는 관리 UI — 현재 CLI 전용인 member add/rm을 웹으로. 사용자 지시로 다른 세션에서 착수 예정
status: done
size: M
user: sjpark1
machine: DESKTOP-TEHV1KR
session: b6556c6d
sessions: ["b443a84e", "b6556c6d"]
agents: ["lead:claude-fable-5", "subagent"]
project: section9
refs_docs: ["DOC-20260823-005", "DOC-20260823-006"]
tags: ["access", "session", "project"]
created: 2026-08-23T22:06:28+09:00
status_since: 2026-08-23T22:24:48+09:00
updated: 2026-08-24T17:29:27+09:00
---

## Original

프로젝트 멤버관리를 할 수 있는 대시보드를 만들어야 하는데 다른 세션으로 이어서 하겠다. 기다려라.

## Notes

### 2026-08-23T22:06:47+09:00 brief (agent: lead:claude-fable-5, by sjpark1)

### 다음 세션용 착수 브리프
과제: 대시보드 #proj-info 패널에 프로젝트 멤버 관리(추가/역할·직무·만료 변경/제거) UI.
- 서버: bin/s9 serve의 do_POST에 /api/project/member 계열 추가. 반드시 CLI와 동일한 인가 경로 재사용 — project_can(slug, actor, 'manage'), owner 조작은 'own', 마지막 활성 owner 강등/제거 차단(project_owner_count), 시스템 admin 우회. actor는 기존 자기신고 모델(req.user=me, registered_users 검사, [via dashboard] audit 관례) 그대로.
- 데이터: upsert 의미론 유지(명시 필드만 갱신: role/position/until). role=owner|maintainer|contributor|viewer, until=YYYY-MM-DD(빈값 무기한), position 자유형식.
- UI: s9-design 스킬 준수(헤어라인·모노·플랫, pill 금지). #proj-info 멤버 테이블에 인라인 편집/추가 행 방식 권장 — me가 manage 권한 없으면 편집 컨트롤 숨김(조회 전용 유지). 만료 임박 amber 관례(DOC-20260823-006)와 · ME 마커 유지. 멤버 변경 후 refreshProjects()로 mine 스코프·스트립 즉시 갱신.
- 검증: tdd 스킬(시나리오 선작성: 권한별 허용/거부, 마지막 owner 가드, upsert 부분 갱신, 미등록 사용자 거부) + browser-verify(s9 shot: 권한 있는/없는 me, 다크, 좁은 폭).
- 착수 절차: 새 세션에서 S9_SESSION=<앞8자> s9 last REQ-20260823-073 → status in-progress (세션 승계가 문서 session을 자동 갱신, REQ-20260823-069 참조).

### 2026-08-23T22:13:17+09:00 tdd (by sjpark1)

### TDD 시나리오 (서버 API)
- [x] M1. 정상 추가: maintainer가 POST /api/project/member (slug,user,role) → 멤버 추가됨
- [x] M2. upsert 부분 갱신: position만 보내면 role/until 불변
- [x] M3. 권한 거부: contributor의 멤버 추가 시도 → 400 (maintainer 이상 필요)
- [x] M4. owner 지정은 own 권한: maintainer가 role=owner 지정 → 400
- [x] M5. 마지막 활성 owner 강등/제거 차단 → 400
- [x] M6. 미등록 사용자(me) → 400 거부
- [x] M7. 정상 제거: maintainer가 POST /api/project/member/rm → 제거됨
- [x] M8. 회귀: 추출 후에도 CLI s9 project member add/rm/ls 동작 불변
### 검증 (UI — browser-verify)
- [x] U1. manage 권한 me: #proj-info 멤버 테이블에 추가/편집/제거 컨트롤 노출, 실제 변경 동작
- [x] U2. 권한 없는 me: 조회 전용 (컨트롤 숨김)
- [x] U3. 다크 모드·좁은 폭 렌더 확인 (s9 shot)

### 2026-08-23T22:24:48+09:00 response (by sjpark1)

### 구현 완료 보고
### 서버 (bin/s9)
- 멤버 upsert/제거를 do_member_set / do_member_rm 공용 함수로 추출 — CLI(project member add/rm)와 웹 서버가 동일 코드 경로·동일 인가(project_can manage/own, 마지막 활성 owner 강등·제거 차단, upsert 부분 갱신 의미론) 공유. 거부는 ValueError → CLI die / 서버 400.
- POST /api/project/member (upsert: slug·member·role·until·position), /api/project/member/rm 추가. actor는 기존 자기신고 모델(req.user, 등록 사용자 검사, '(by X via dashboard)' audit) 그대로.
### UI (web/index.html #proj-info)
- manage 권한 me(프로젝트 maintainer+ 또는 시스템 admin): 멤버 테이블 인라인 편집 — role select(own 없으면 owner 옵션 제외·owner 행 잠금), 직무 input, 만료 date input(비우면 무기한), '− 제거'(owner 행은 own 필요), 하단 추가 행(미참여 등록 사용자 후보 select). 변경 즉시 POST → refreshProjects()로 mine 스코프·스트립·패널 갱신. 서버 거부는 재렌더(원복) 후 .pi-err로 사유 표시.
- 권한 없는 me는 기존 조회 전용 그대로. s9-design 준수(라운드 0·모노·헤어라인, pill 없음).
### 검증
- API: tests/test_project_member_api.py M1~M8 (S9_ROOT 격리, 실서버 기동) 전부 통과.
- 실브라우저(s9 shot, Windows Chrome headless): maintainer 편집 뷰 / contributor 조회 전용 / carbon 다크·480px 좁은 폭(가로 스크롤 정상) 스크린샷 확인. same-origin iframe 하네스(uitest.html)로 실클릭 T1 역할변경·T2 추가·T3 제거·T4 거부메시지 표면화 PASS.
### 부수 수정
- 작업 중 발견/보고된 문서뷰 클릭 리로드 버그는 REQ-20260823-076으로 분리 수정 완료.

### 2026-08-24T17:29:27+09:00 subagent (agent: subagent, by sjpark1)

<analysis>
This is a long-running session managing the section9 project (~/section9) — an external-memory/audit system for Claude Code with a CLI (bin/s9), dashboard (web/index.html), hooks (bin/s9-audit-*), and auto-rework infrastructure. The session spans 2026-08-23 to 2026-08-24 and covers ~35 REQ documents.

Chronological work covered (major arcs):
1. Fragment classification fix (REQ-20260823-075) — "logout" false REQ cards
2. Project member management UI (REQ-20260823-073) — server API + dashboard UI
3. Doc viewer click-reload bug (REQ-20260823-076) — data-doc collision
4. Design skins saga (REQ-20260823-077, 5 rejections) — glass revival → costume skins rejected → subtle skins rejected → bold palette (field/cobalt/relief/grid/cockpit) → Jira/Trello-style (atlas→slate, tray→cork renamed) — finally approved by user
5. Graph depth saga (REQ-20260823-078, 6 rejections) — glossy spheres rejected → starfield → node z-axis with motion parallax → idle camera drift → starfield removed → transparency strengthened, bottleneck arc gauge — finally approved by user
6. Live-dot semantics (REQ-20260823-079/080/082) — claim-based (last_req/active_reqs) direct evidence only
7. Claim-based watcher redesign (REQ-20260823-083) — rejection auto-progress without user utterance
8. Worker permission envelope (REQ-005) — allowlist spawns, patches via notes
9. Opt-in unattended apply (REQ-012) — auto_resume_apply=on for sjpark1
10. User preferences (REQ-006/007/015/016) — pref_* storage, hook injection, Settings UI, absence injection
11. Access isolation (REQ-017) — machine-scoped accounts, me-based visibility
12. whoami identity derivation (REQ-027) — me selector removed, server-derived identity
13. User guide (REQ-018) — web/guide.html, still in review
14. Goal mandate (REQ-030) — goal field required, done-guard
15. Approval memo follow-up loop (REQ-028) — symmetric to rejection loop
16. Dashboard auto-start (REQ-020), dual-attach warning (REQ-031)
17. Dashboard-Claude chat (REQ-032) — user rejected my design, proposed their own: canonical terminal session + dashboard messaging via RC/SendMessage; spike initially failed (no reachable agents), then user opened new RC-connected session (vast-quail), second spike just launched and completed

The very last state: background courier spike (byvlkx4jq) completed — I have NOT yet read its output (/tmp/.../tasks/byvlkx4jq.output or courier-spike2.log). The immediate next step is reading that result and reporting success/failure to the user, then proceeding to implementation (RC auto-connect in s9 code + chat panel + instant transition notifications) or Plan B.

Key standing user instructions to preserve:
- Personal pref active: "말투: 칭찬, 아부성 발언 절대 금지" (no praise/flattery)
- PROTOCOL 14: detailed step-by-step guides when requesting user action (harness-wide, NOT a personal pref — user explicitly corrected this)
- goal mandate, review guards, all the process rules built during session
- S9_SESSION=104b4fe3 prefix on all s9 calls
- Delegation claims via `s9 last <id> --add`; bind agent_transcript_path when delegating

Technical details to capture: file paths, key function names (do_transition, catalog_with_live, rework_watch_tick, _spawn_rework, _spawn_followup, approvals_unseen, doc_visible, whoami_info, ensure_serve, cmd_code, _discover_transcript, _tdd_progress, prefs_context), test files (9+ suites), the CDP client (scratchpad/cdp.py), server restart pattern, PROTOCOL.md items 9-14.

Errors and fixes were numerous — capture the significant ones with user feedback.

All user messages must be listed — there are ~45 of them. I'll list them all compactly but completely.
</analysis>

<summary>

1. Primary Request and Intent:
The user (sjpark1, Korean-speaking, admin) iteratively develops "section9" (~/section9) — an external-memory/audit/control system for Claude Code. Over this session they requested, in order: fix spurious REQ card creation from meaningless prompts; continue project member management UI (REQ-20260823-073); make design skins diverse (glass revival, then multiple rejections shaping taste rules); make graph 3D/spatial (6 rejection iterations); make live/green-dot indicators truthful (only actually-executing REQs blink green); redesign rejection auto-progress so it never depends on user utterance (claim-based watcher); opt into unattended apply (option 2: workers apply+verify+review autonomously); per-user preferences saved by prompt and auto-injected (later: absence must also be explicit instruction); Settings UI to view/edit prefs; account/document access isolation (machine-registered accounts only, member-project visibility); then deeper: identity should be derived not selected (whoami model — me selector removed); a friendly end-user guide with screenshots (web/guide.html — includes "run claude from ~/section9" per rejection); project asset system review (approved: projects/<slug>/ with CONTEXT.md+assets/, slug keys with id resolution, project slug indexing); dashboard server must auto-start on install and on every Claude session start; every REQ must have a goal and goal-satisfaction verdict (done-guard); approval/rejection memos must be documented AND auto-acted-upon (approval follow-up loop symmetric to rejection loop); stream terminal sections must be identifiable (highlight own REQ mentions); subagent report attribution must be accurate; TDD counters must count only current-generation scenarios and reset on rejection; review entry requires complete TDD; blocked reasons must be visible with resolution guides; session dual-attach must warn; and finally REQ-032: dashboard-Claude chat — user rejected my dedicated-session design and specified their own architecture: a single entry command (s9 code, name flexible) that ensures the dashboard and launches interactive claude in the terminal, with the dashboard messaging that same canonical session via Remote Control cross-session SendMessage, enabling instant review-approval/rejection feedback to the live lead session. Standing meta-instructions: no praise/flattery in responses (active pref); when requesting user action, always provide detailed step-by-step guides (harness-wide PROTOCOL rule 14, explicitly NOT a personal preference); record guides in documents not just terminal output.

2. Key Technical Concepts:
- section9 architecture: stdlib-only Python (pyproject.toml declares dependencies=[]; tests/test_stdlib_only.py guard), single-file CLI bin/s9 with embedded HTTP dashboard server (port 9909, 127.0.0.1 only), single-file web/index.html dashboard, hooks (UserPromptSubmit=s9-audit-prompt, SessionStart/End=s9-audit-session, Stop=s9-audit-response, SubagentStop=s9-audit-subagent)
- Vault documents: REQ/DOC/SES/PRJ markdown with frontmatter; catalog.jsonl index; do_transition as single write path for status transitions with guards
- Live-dot semantics: green blink = session execution registration (last_req/active_reqs) + fresh activity (<120s); hollow ring = session-indirect; amber blink = spawned-awaiting-claim; grey = quiet. Claims auto-added on in-progress transition, pruned on leaving; `s9 last <id> --add` for parallel/delegated claims (last_req rotates per prompt)
- Claim-based rework watcher: rejection → PENDING → grace (default 30s, auto_resume_grace_sec) → unclaimed → spawn worker (--resume if session quiet, fresh if active); cooldown 600s default; caps; kill-switches S9_AUTO_RESUME_DISABLE, S9_REWORK_WATCH=off
- Worker envelopes: default restricted (--allowedTools Read Glob Grep Bash(s9bin:*)), patches delivered via `s9 note --label patch` + blocked("패치 적용 대기(리드)"); opt-in apply mode (auto_resume_apply=on, active for sjpark1): --permission-mode acceptEdits + Bash(python3 tests/:*) etc., scope limited to web/·vault/·tests/
- Approval follow-up loop (REQ-028): approvals_unseen()/cmd_approvals (consume-once via state/approvals_seen.json), hook injects 🆗 memos, watcher _spawn_followup for unconsumed memos
- whoami identity model (REQ-027): GET /api/whoami derives user from resolve_user() matched against profiles (os_accounts field); write actor server-derived (client user param ignored); admin-only ?as= preview; me selector replaced by label
- Goal mandate (REQ-030): done transition rejected without goal (--force exception); hook instructs --goal at intake; done notes must contain satisfaction evidence
- TDD counter: counts only last tdd-labeled note section (generation handling); rejection (review→in-progress) auto-unchecks all `- [x]`; review entry guard requires N/N complete
- CDP verification infrastructure: scratchpad/cdp.py (stdlib WebSocket+CDP client), Windows Chrome --headless=new --remote-debugging-port, sandbox vaults (S9_ROOT=scratch dirs), s9 shot for screenshots
- Cross-session messaging: ListAgents/SendMessage; reachability depends on Remote Control connection; headless courier pattern (claude -p with --allowedTools "ListAgents" "SendMessage")
- s9 code command: ensures dashboard (idempotent port check) then os.execvp claude in ROOT
- Project assets (REQ-019 approved, phase 1 implemented by follow-up worker): projects/<slug>/CONTEXT.md + assets/; hook announces CONTEXT.md path on --project

3. Files and Code Sections:
- **bin/s9** (~3000 lines, heavily modified): 
  - `do_transition`: added rejection TDD-reset (`if old == "review" and new_status == "in-progress": body = re.sub(r"(?m)^- \[[xX]\]", "- [ ]", body)`), review-entry TDD guard, done goal-guard (`if new_status == "done" and meta.get("type") == "request" and not force and not (meta.get("goal") or "").strip(): raise ValueError("goal 미기재 — ...")`)
  - `catalog_with_live`: live judgment (pointer/active_reqs + fresh session paths only; live_kind direct/session/spawned), `_binding_activity_paths` (transcript+agent_transcript_path+streams)
  - `update_active_reqs(doc_id, new_status)`: claim registry maintenance
  - `rework_watch_tick(grace=None)`, `_spawn_rework`, `_spawn_followup`, `_last_transition`, `rework_claimed`, `approvals_unseen`, `_approvals_mark`, `cmd_approvals`, `cmd_blocked`/`blocked_requests`/`_block_reason`
  - `maybe_auto_resume`: now PENDING-log only
  - `_tdd_progress`: last-tdd-section only, label regex `([A-Z]\d+)\b`
  - `_discover_transcript(sid8)`: S9_CLAUDE_PROJECTS glob for claim liveness
  - `cmd_last`: --add flag (active_reqs + transcript discovery)
  - `cmd_code` (newest): `s9 code` = ensure dashboard + `os.execvp("claude", ["claude"] + args.claude_args)`; --no-claude for tests
  - whoami_info/api (agent-written), doc_visible/viewer_of isolation, /api/users?scope=machine, member API do_member_set/do_member_rm, catalog slug field, config key validation `r"[A-Za-z0-9_.가-힣-][A-Za-z0-9_.가-힣 -]{0,63}"` with strip
- **bin/s9-audit-prompt**: Fragment classification (≤20 chars, no imperative/question); S9_AUTO_RESUME turn: no card + sid injection "`s9 last {target} --add --session {session}`"; system-notification guard (<task-notification> etc.); prefs_context (pref_* injection; absence → "◈ 개인 설정: 없음 — 기본 말투·방식으로 응답하라(관성 금지)"); approvals/blocked warnings injection; goal instruction in rules
- **bin/s9-audit-session**: ensure_serve (port check → spawn serve, logs to state/serve.log); dual-attach detection (attach_pid binding, /proc check, warning injection merged with digest)
- **bin/s9-audit-subagent**: attribution fix (mentioned REQ ∩ active_reqs∪last_req; generic+unmentioned → log only)
- **bin/s9-install**: start_dashboard() at end
- **web/index.html**: member management UI (#proj-info inline editing), livedot 4-state, blocked ⛔ reason display, pref Settings section (#pf-table/#pf-save), graph: node z-axis (zf/near, sxOf/syOf/scOf projections, pfOf parallax 0.72-1.24), idle camera drift (st.dx/dy, RM reduced-motion, lastTouch), bottleneck arc gauge + capsule (halo removed), starfield removed (6차), window.__gdbg test seam (snap/cam), graph type toggles (gtypes Set, default request+knowledge, legend click, localStorage s9gtypes), 10 skins (ledger/soft/terminal/glass/field/cobalt/relief/grid/cockpit... slate/cork), loadDoc retry (try/catch + 0.8s retry + data-ret

…(truncated)

## History
- 2026-08-23T22:06:28+09:00 created by sjpark1 (status: open)
- 2026-08-23T22:12:16+09:00 status: open -> in-progress (by sjpark1) — 브리프대로 이 세션(b6556c6d)에서 이어받아 착수
- 2026-08-23T22:24:48+09:00 status: in-progress -> done (by sjpark1) — TDD M1~M8 + 실브라우저 U1~U3·기능 T1~T4 검증 완료 — 객관 검증을 마쳐 done (문제 시 반려)
