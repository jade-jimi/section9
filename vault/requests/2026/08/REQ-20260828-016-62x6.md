---
id: REQ-20260828-016-62x6
type: request
title: 멈춘 미완 즉시 마무리
summary: 재부팅으로 방치된 in-progress 4건과 blocked 1건을 이번 세션에서 확인·마무리한다
goal: 078·011·012·013 이 각각 실제 상태에 맞게 done/blocked/review 로 전이되고, 083 의 패치 두 개가 적용돼 graph_fit 테스트가 통과한다
status: done
size: M
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: b8fda8f9
agents: ["sub:designer:a20a4928", "lead:claude-opus-5", "sub:designer:a0aa5d99", "sub:designer:a49bfbcb", "sub:designer:a2d809d2", "sub:ux-writer:ac53d5c8", "sub:architect:a63e1df3", "sub:designer:afd44bf7", "sub:designer:a52356ae", "sub:ux-writer:a5c042ff", "sub:designer:adc50661", "sub:architect:a75477ec", "sub:designer:a4488cd8", "sub:architect:adc8f490", "sub:frontend-developer:a0219ca5", "sub:designer:a9aa0303", "sub:architect:a54c5c1a", "sub:designer:a992b2d5", "sub:backend-developer:a0b7582c", "sub:architect:aeaee0ab"]
contributions: [{"actor": "lead:claude-opus-5", "item": "`[2026-08-28 15:34:18 KST - lead]`", "started": "2026-08-28T15:34:18+09:00", "ended": "2026-08-28T15:34:18+09:00", "result": "done", "transcript": ""}, {"actor": "lead:claude-opus-5", "item": "`[2026-08-28 15:35:23 KST - lead]`", "started": "2026-08-28T15:35:23+09:00", "ended": "2026-08-28T15:35:23+09:00", "result": "done", "transcript": ""}, {"actor": "lead:claude-opus-5", "item": "`[2026-08-28 15:43:50 KST - lead]`", "started": "2026-08-28T15:43:51+09:00", "ended": "2026-08-28T15:43:51+09:00", "result": "done", "transcript": ""}, {"actor": "lead:claude-opus-5", "item": "`[2026-08-28 15:55:03 KST - lead]`", "started": "2026-08-28T15:55:03+09:00", "ended": "2026-08-28T15:55:03+09:00", "result": "done", "transcript": ""}, {"actor": "lead:claude-opus-5", "item": "`[2026-08-28 15:56:39 KST - lead]`", "started": "2026-08-28T15:56:39+09:00", "ended": "2026-08-28T15:56:39+09:00", "result": "done", "transcript": ""}, {"actor": "sub:designer:afd44bf7", "item": "대답 속 코드 표시 안 검토", "started": "2026-08-28T15:58:08+09:00", "ended": "2026-08-28T16:06:10+09:00", "result": "done", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/tasks/afd44bf72c3b813d2.output"}, {"actor": "sub:designer:a52356ae", "item": "판정 버튼과 언급 링크", "started": "2026-08-28T16:03:49+09:00", "ended": "2026-08-28T17:19:01+09:00", "result": "done", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/tasks/a52356aea4666d57f.output"}, {"actor": "lead:claude-opus-5", "item": "`[2026-08-28 16:04:09 KST - lead]`", "started": "2026-08-28T16:04:09+09:00", "ended": "2026-08-28T16:04:09+09:00", "result": "done", "transcript": ""}, {"actor": "sub:designer:afd44bf7", "item": "`[2026-08-28 16:04:32 KST - designer]`", "started": "2026-08-28T16:06:10+09:00", "ended": "2026-08-28T16:06:10+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/subagents/agent-afd44bf72c3b813d2.jsonl"}, {"actor": "lead:claude-opus-5", "item": "`[2026-08-28 16:07:37 KST - lead]`", "started": "2026-08-28T16:07:37+09:00", "ended": "2026-08-28T16:07:37+09:00", "result": "done", "transcript": ""}, {"actor": "sub:designer:a49bfbcb", "item": "`[2026-08-28 16:16:21 KST - designer]`", "started": "2026-08-28T16:16:32+09:00", "ended": "2026-08-28T16:16:32+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/subagents/agent-a49bfbcb21b323f53.jsonl"}, {"actor": "sub:ux-writer:a5c042ff", "item": "타임존 고르는 자리 문구 설계", "started": "2026-08-28T16:59:22+09:00", "ended": "2026-08-28T16:59:22+09:00", "result": "running", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/tasks/a5c042ffa83ffb659.output"}, {"actor": "lead:claude-opus-5", "item": "`[2026-08-28 16:59:40 KST - lead]`", "started": "2026-08-28T16:59:40+09:00", "ended": "2026-08-28T16:59:40+09:00", "result": "done", "transcript": ""}, {"actor": "sub:designer:adc50661", "item": "타임존 고르는 자리 구현", "started": "2026-08-28T17:09:16+09:00", "ended": "2026-08-28T17:09:16+09:00", "result": "running", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/tasks/adc506610e24f7e7d.output"}, {"actor": "lead:claude-opus-5", "item": "`[2026-08-28 17:09:39 KST - lead]`", "started": "2026-08-28T17:09:39+09:00", "ended": "2026-08-28T17:09:39+09:00", "result": "done", "transcript": ""}, {"actor": "sub:designer:a52356ae", "item": "`[2026-08-28 17:19 KST - designer]`", "started": "2026-08-28T17:19:01+09:00", "ended": "2026-08-28T17:19:01+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/subagents/agent-a52356aea4666d57f.jsonl"}, {"actor": "lead:claude-opus-5", "item": "`[2026-08-28 17:19:28 KST - lead]`", "started": "2026-08-28T17:19:29+09:00", "ended": "2026-08-28T17:19:29+09:00", "result": "done", "transcript": ""}, {"actor": "sub:architect:a75477ec", "item": "축약 표기 존폐 검토", "started": "2026-08-28T17:28:16+09:00", "ended": "2026-08-28T17:28:16+09:00", "result": "running", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/tasks/a75477ec8a7319e01.output"}, {"actor": "lead:claude-opus-5", "item": "`[2026-08-28 17:28:48 KST - lead]`", "started": "2026-08-28T17:28:48+09:00", "ended": "2026-08-28T17:28:48+09:00", "result": "done", "transcript": ""}, {"actor": "lead:claude-opus-5", "item": "`[2026-08-28 17:36:44 KST - lead]`", "started": "2026-08-28T17:36:44+09:00", "ended": "2026-08-28T17:36:44+09:00", "result": "done", "transcript": ""}, {"actor": "sub:designer:a4488cd8", "item": "DAG 안개 - 디자인 관점", "started": "2026-08-28T17:44:32+09:00", "ended": "2026-08-28T17:57:06+09:00", "result": "done", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/tasks/a4488cd8a10d92393.output"}, {"actor": "sub:architect:adc8f490", "item": "DAG 안개 - 구조 관점", "started": "2026-08-28T17:44:50+09:00", "ended": "2026-08-28T17:49:23+09:00", "result": "done", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/tasks/adc8f490f5d01adba.output"}, {"actor": "sub:frontend-developer:a0219ca5", "item": "DAG 안개 - 구현 관점", "started": "2026-08-28T17:45:07+09:00", "ended": "2026-08-28T17:54:29+09:00", "result": "done", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/tasks/a0219ca580cd2d55c.output"}, {"actor": "lead:claude-opus-5", "item": "`[2026-08-28 17:45:26 KST - lead]`", "started": "2026-08-28T17:45:27+09:00", "ended": "2026-08-28T17:45:27+09:00", "result": "done", "transcript": ""}, {"actor": "lead:claude-opus-5", "item": "`[2026-08-28 17:47:44 KST - lead]`", "started": "2026-08-28T17:47:44+09:00", "ended": "2026-08-28T17:47:44+09:00", "result": "done", "transcript": ""}, {"actor": "sub:architect:adc8f490", "item": "`[2026-08-28 17:52:41 KST - architect]`", "started": "2026-08-28T17:49:23+09:00", "ended": "2026-08-28T17:49:23+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/subagents/agent-adc8f490f5d01adba.jsonl"}, {"actor": "lead:claude-opus-5", "item": "`[2026-08-28 17:50:31 KST - lead]`", "started": "2026-08-28T17:50:31+09:00", "ended": "2026-08-28T17:50:31+09:00", "result": "done", "transcript": ""}, {"actor": "sub:frontend-developer:a0219ca5", "item": "`[2026-08-28 17:58:20 KST - frontend-developer]`", "started": "2026-08-28T17:54:29+09:00", "ended": "2026-08-28T17:54:29+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/subagents/agent-a0219ca580cd2d55c.jsonl"}, {"actor": "lead:claude-opus-5", "item": "`[2026-08-28 17:55:40 KST - lead]`", "started": "2026-08-28T17:55:40+09:00", "ended": "2026-08-28T17:55:40+09:00", "result": "done", "transcript": ""}, {"actor": "sub:designer:a4488cd8", "item": "`[2026-08-28 17:56:12 KST - designer]`", "started": "2026-08-28T17:57:06+09:00", "ended": "2026-08-28T17:57:06+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/subagents/agent-a4488cd8a10d92393.jsonl"}, {"actor": "sub:designer:a9aa0303", "item": "DAG 안개 구현", "started": "2026-08-28T18:03:39+09:00", "ended": "2026-08-28T18:03:39+09:00", "result": "running", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/tasks/a9aa0303dfc0d886a.output"}, {"actor": "sub:architect:a54c5c1a", "item": "상태 자가점검 - 구조", "started": "2026-08-28T18:11:41+09:00", "ended": "2026-08-28T18:11:41+09:00", "result": "running", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/tasks/a54c5c1a6c388985a.output"}, {"actor": "sub:designer:a992b2d5", "item": "상태 자가점검 - 화면", "started": "2026-08-28T18:12:02+09:00", "ended": "2026-08-28T18:23:19+09:00", "result": "done", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/tasks/a992b2d5e2c875907.output"}, {"actor": "sub:backend-developer:a0b7582c", "item": "상태 자가점검 - 구현", "started": "2026-08-28T18:12:19+09:00", "ended": "2026-08-28T18:20:05+09:00", "result": "done", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/tasks/a0b7582cce6179f49.output"}, {"actor": "sub:architect:aeaee0ab", "item": "상태 자가점검 - 구조 재시도", "started": "2026-08-28T18:17:16+09:00", "ended": "2026-08-28T18:21:32+09:00", "result": "done", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/tasks/aeaee0ab6dcaf1ba2.output"}, {"actor": "lead:claude-opus-5", "item": "`[2026-08-28 18:19:03 KST - lead]`", "started": "2026-08-28T18:19:04+09:00", "ended": "2026-08-28T18:19:04+09:00", "result": "done", "transcript": ""}, {"actor": "sub:backend-developer:a0b7582c", "item": "`[2026-08-28 18:17:37 KST - backend-developer]`", "started": "2026-08-28T18:20:05+09:00", "ended": "2026-08-28T18:20:05+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/subagents/agent-a0b7582cce6179f49.jsonl"}, {"actor": "sub:architect:aeaee0ab", "item": "`[2026-08-28 18:20:23 KST - architect]`", "started": "2026-08-28T18:21:32+09:00", "ended": "2026-08-28T18:21:32+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/subagents/agent-aeaee0ab6dcaf1ba2.jsonl"}, {"actor": "sub:designer:a992b2d5", "item": "`[2026-08-28 18:22:04 KST - designer]`", "started": "2026-08-28T18:23:19+09:00", "ended": "2026-08-28T18:23:19+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/subagents/agent-a992b2d5e2c875907.jsonl"}, {"actor": "lead:claude-opus-5", "item": "`[2026-08-28 18:35:31 KST - lead]`", "started": "2026-08-28T18:35:31+09:00", "ended": "2026-08-28T18:35:31+09:00", "result": "done", "transcript": ""}]
project: section9
relates_why: {}
tags: ["auto-audit", "session"]
created: 2026-08-28T12:40:03+09:00
status_since: 2026-08-28T13:35:53+09:00
updated: 2026-08-28T18:35:31+09:00
priority: 50
---

## Original

아니 왜 또 다음세션까지 기다리는거야 어서 마무리해

## Notes

### 2026-08-28T12:53:40+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-08-28 12:53:40 KST - lead]`

`[2026-08-28 13:10 KST - lead]`

078·011 닫았습니다. 남은 셋(083·012·013)은 designer 가 `web/index.html` 하나에서 순차로 작업 중입니다.

- **REQ-20260827-078-62x6** done — 스트림 66개가 `users/nicehugepark/streams/` 로 이동 완료, 공용 `streams/` 비었고 Stream 탭 정상, 관련 테스트 52건 통과. 07:57 커밋 뒤 전이만 누락돼 있었습니다.
- **REQ-20260828-011-62x6** done — 2·3단계 구현. 워커가 전용 워크트리에서 돌고(`S9_ROOT` 는 본 저장소로 못박음), 자기 가지에 커밋하면 리드가 `s9 worktree merge` 로 합칩니다. 거두는 자리(`worktree_sweep`)도 함께 — 미커밋 변경이나 안 합친 커밋이 있으면 남깁니다. **스위치를 켰습니다** (`worker_worktree on`); 끄려면 `s9 user config nicehugepark worker_worktree ""`.

커밋 게이트가 한 번 막아섰는데 옳은 지적이었습니다 — 커밋 훅이 넘긴 `GIT_DIR` 이 임시 저장소용 테스트로 새어 들어, 테스트가 본 저장소의 git 을 만지고 있었습니다. 봉하고 코드에도 빈 이름 방어를 넣었습니다. 전체 스위트 1338건 통과.

designer 결과가 오면 이어서 보고하겠습니다.

### 2026-08-28T13:14:41+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-08-28 13:14:40 KST - lead]`

083 담당 designer 에게 중계했습니다. 지금 하던 작업을 마치고 반영합니다.

전달한 내용: 줌아웃 시 무리가 등방적 덩어리여야 하는데 ↖→↘ 로 눌린 타원이다. 배치(힘 시뮬레이션의 축 대칭)와 화면 변환(fit transform) 중 어디서 비등방이 생기는지 갈라서 확인하도록 했습니다. 캡처를 보니 짚이는 것이 하나 더 있어 함께 넘겼습니다 — 오른쪽에 떨어진 작은 무리와 왼쪽 아래 외톨이 노드가 fit 의 경계 상자를 키워 본 무리를 한쪽으로 밀고 눌러 보이게 만들 수 있습니다.

보내신 캡처는 서버가 083 문서 첨부로 들여놓았습니다(`assets/REQ-20260827-083-62x6/20260828T131128-image.png`). 처음에 유실된 줄 알고 그렇게 적었는데 아니었고, 문서 노트에 정정해 두었습니다.

검증 방식도 함께 못박았습니다: 헤드리스 캡처가 물리를 몇 프레임만 돌려 자리가 안 잡힌 그래프를 찍는 탓에 지금까지 이 결함을 놓쳐 왔습니다. `?gsettle=N` 으로 충분히 수렴시킨 뒤 눈으로 보고 고치도록 했습니다.

승인해도 될 것 같다고 하신 review 항목들은 제가 임의로 옮기지 않고 그대로 두겠습니다 — 대시보드에서 눌러 주시면 됩니다.

### 2026-08-28T13:36:10+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-08-28 13:36:10 KST - lead]`

`[2026-08-28 13:33 KST - lead]`

멈춰 있던 것 전부 정리했습니다. **in-progress 0건.**

닫은 것
- **078** done — 스트림 66개가 사용자 자리로 이동 완료, 커밋 뒤 전이만 누락돼 있었습니다
- **011** done — 워크트리 2·3단계 구현, 스위치 켬
- **016** done — 이 마무리 작업 자체

판정 대기로 올린 것 (designer 작업, 커밋 `d2f1b14`)
- **083** 그래프 — 노드 사라짐의 진짜 원인은 수렴이 아니라 그 아래였습니다. 초반에 밀려난 노드 몇 개 탓에 월드 상자가 31,791×11,185 로 벌어져 필요한 배율(0.035)이 하한(0.25)에 걸렸고, 배율은 못 내리면서 이동량만 커져 무리를 화면 밖으로 밀었습니다. 맞출 상자를 몸통 기준으로 재고, 배율은 화면에 실제로 남는 노드가 가장 많은 쪽을 고르게 했습니다. 손잡이 클릭 0% → 97%.
  말씀하신 **대각선 찌그러짐**은 깊이 시차를 팬 오프셋에 곱하고 있던 탓이었습니다 — 시차가 아니라 팬 방향으로 미는 전단이었습니다. 카메라를 화면 가운데 두고 거리에 깊이를 먹이도록 바꿨습니다. **전체 보기 긴축/짧은축 1.24 → 1.05** (1.00이 원). 제가 직접 캡처해 눈으로도 확인했습니다.
- **012** 비밀 키 — 개인설정에 자리. 목록은 키 이름과 둔 곳뿐, 값은 저장 뒤 즉시 비웁니다
- **013** 스트림 스위치 — 같은 판에 켜기/끄기와 보관 기간, 저장 버튼 없이 바로 반영

전체 스위트 **1356건 통과**. 대시보드는 새 코드로 재기동했습니다 — F5 후 봐 주세요.

designer 가 남긴 한계 하나: 깊게 확대(k≈2.5)하면 화면에 남는 노드가 25% 아래로 떨어져 "⤢ 전체 보기" 손잡이가 뜹니다. 이번 지적과는 무관한 기존 판정 기준의 결과라 손대지 않았습니다 — 거슬리시면 별건으로 열겠습니다.

### 2026-08-28T13:47:16+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-08-28 13:47:16 KST - lead]`

REQ-20260827-083-62x6 승인되어 done 으로 닫혔습니다. 메모에 후속 지시는 없어 추가 작업은 하지 않습니다.

남은 판정 대기는 012(비밀 키)·013(스트림 스위치)와 그 전부터 올라가 있던 건들입니다. 진행 중인 작업은 없습니다.

### 2026-08-28T13:47:47+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-08-28 13:47:47 KST - lead]`

REQ-20260828-013-62x6(스트림 스위치) 승인되어 done 으로 닫혔습니다. 후속 지시는 없습니다.

### 2026-08-28T13:51:04+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-08-28 13:51:04 KST - lead]`

**REQ-20260827-035-62x6**(어제 18:36 done)에서 정했고, 그때 하신 말이 그대로 설계가 됐습니다. 답을 012 문서에 노트로도 남겼습니다.

키를 어떻게 두기로 했나
- **키 하나 = 파일 하나.** 파일명이 키 이름, 내용이 값입니다. 한 파일에 몰면 부분 유출이 전체 유출이 되고 권한·삭제·감사가 거칠어집니다.
- 자리는 둘 — internal `users/<이름>/secrets/`, external `external_secrets_path` 가 가리키는 **리포 밖** 디렉토리.
- 같은 이름이 양쪽에 있으면 **internal 이 이깁니다.** 리포 안이 기본이고, 바깥은 "리포에 두기 싫은 것"의 자리입니다.
- external 이 없거나 못 읽으면 **조용히 internal 만으로 동작합니다** — 바깥 경로가 없다고 안쪽 비밀 사용이 막히면 안 됩니다.

구현 (`bin/s9`)
- `external_secret_dir()` 2544 — 설정값을 펴서 **실제 디렉토리일 때만** 인정
- `secret_keys()` 2552 — external 을 훑고 internal 로 덮음(= internal 우선)
- `secret_leak()` 2575 — 커밋 가드. 경로만이 아니라 **값 내용도** 봅니다. 값만 다른 파일에 붙여 넣는 것이 실제 유출 경로라서요. 8자 미만은 오탐이 더 해로워 내용 검사에서 뺐습니다
- 2946 — `s9 config export --with-permissions` 가 권한 파일을 리포 밖으로만 내보낼 때 이 경로를 기본 자리로 씁니다. 자리를 못 정하면 아예 안 씁니다
- 값 비노출: `ls` 는 키만 · `set` 은 stdin(argv 에 안 남음) · `run` 은 출력에서 가림. 다만 **"모델이 값을 못 본다"는 이 하네스에서 보장 못 합니다** — 모델이 같은 OS 계정으로 셸을 돕니다. 막는 것은 사고로 새는 길이고, 이 한계는 코드 주석과 문서에 적어 뒀습니다

지금 상태 — 계정에 `external_secrets_path` 는 **미설정**입니다(`s9 secret ls` 도 그렇게 말합니다). 키는 internal 의 `test_key` 하나뿐입니다.

한 가지 남은 것: 이번 화면은 목록에 "저장소 안 / 저장소 밖"만 보이는데, **경로를 정하는 칸은 아직 화면에 없습니다.** CLI 로만 됩니다 — `s9 user config nicehugepark external_secrets_path <리포 밖 경로>`. 012가 제기한 문제("CLI 에만 있으면 없는 기능")가 이 한 칸에는 그대로 남아 있습니다. 화면에 넣을까요?

### 2026-08-28T13:56:14+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-08-28 13:56:14 KST - lead]`

아닙니다. **`external_secrets_path` 는 비밀이 아니라 계정 설정값입니다** — `ui_skin`·`auto_resume` 과 같은 선반에 있습니다. 시크릿 목록에 그 이름의 키를 만드는 게 아닙니다.

설정은 두 단계입니다.

```
mkdir -p ~/.section9-secrets && chmod 700 ~/.section9-secrets
s9 user config nicehugepark external_secrets_path ~/.section9-secrets
```

디렉토리가 **실제로 있어야** 합니다. 경로만 적고 폴더가 없으면 아무 말 없이 무시됩니다(`isdir` 이 참일 때만 인정). 확인은 `s9 secret ls` 끝줄이 "external 미설정" 대신 그 경로를 보이는지로 합니다.

값을 그 자리에 넣는 법 — **s9 는 external 을 읽기만 합니다.** `s9 secret set` 도, 화면의 넣기 버튼도 언제나 internal 에 씁니다. 그래서 external 에 두려면 파일을 직접 놓습니다. 파일 이름이 키, 내용이 값입니다:

```
printf %s "<값>" > ~/.section9-secrets/MY_KEY && chmod 600 ~/.section9-secrets/MY_KEY
```

지우기는 양쪽 다 됩니다(CLI·화면 모두 internal·external 을 훑습니다).

**지금은 굳이 필요 없습니다.** 커밋 가드가 경로와 값 내용 양쪽을 막고 있어서 리포 안(internal)에 둬도 커밋에 섞이지 않습니다. external 이 필요한 경우는 하나입니다 — 이 저장소 자체를 남과 공유·공개하는데 값이 디스크에서조차 그 트리 밖에 있어야 할 때.

원하시면 위 두 줄을 제가 지금 실행하고, 화면에도 경로 칸을 내겠습니다. 경로를 `~/.section9-secrets` 말고 다른 데 두고 싶으시면 그것만 알려주세요.

### 2026-08-28T14:27:52+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-08-28 14:27:52 KST - lead]`

REQ-20260827-072-62x6(구간에 메모 달기) 승인되어 done 으로 닫혔습니다. 후속 지시는 없습니다. designer 는 017·009 작업 중입니다.

### 2026-08-28T14:31:44+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-08-28 14:31:44 KST - lead]`

맞습니다. 제가 지시를 잘못 냈습니다 — "폴더가 없으면 무시된다는 걸 화면에서 읽히게 하라"는 문제를 설명만 하고 그대로 남겨 두는 것이었습니다. 경로를 적어 저장하면 그 자리에서 만들도록 designer 에게 바꿔 전달했습니다.

다만 **만드는 자리는 저장할 때 한 곳으로 못박았습니다.** 읽는 쪽(`external_secret_dir()`)은 그대로 둡니다 — 비밀을 읽을 때마다 불리고 커밋 가드와 훅에서도 불리는 함수라, 읽기가 폴더를 만드는 부작용을 가지면 오타 하나가 엉뚱한 자리에 폴더를 만들고 아무도 모릅니다.

함께 붙인 조건들:
- 새로 만들 때만 권한 0700, 이미 있는 폴더의 권한은 덮지 않음
- 못 만들면(권한 없음·상위 경로 없음) **저장을 성공으로 처리하지 않고** 사유를 그대로 표시 — 여기서 조용히 넘어가면 처음 물으신 그 상황("설정했는데 안 먹는다")이 다시 생깁니다
- 리포 안 경로는 거부(리포는 공유·공개되므로 바깥에 두려는 뜻이 사라집니다), `~` 는 펴서 저장
- "지금 이 경로가 쓰이고 있는지" 표시는 그래도 남깁니다 — 저장한 뒤 폴더가 지워지거나 마운트가 빠지는 경우가 있습니다

### 2026-08-28T14:34:01+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-08-28 14:34:01 KST - lead]`

**REQ-20260828-018-62x6 (미답 대신 답한 비율)** 로 접수하고 designer 에게 세 번째 건으로 붙였습니다. 최종 형태는 디자이너 판단에 맡기라고 하신 그대로 전달했습니다 — 사용자 안이 그대로 최선이면 그대로, 더 나은 형태가 있으면 그것으로 하되 이유를 문서에 남기고 판정하실 수 있게 올리도록 했습니다.

판단에 넣도록 함께 넘긴 것:
- 옆 칸들(`SESSION 78`, `PROJECT 1`)은 단일 수인데 한 칸만 분수가 되면 읽는 규칙이 섞이지 않는지
- 강조는 색면이 아니라 글자·굵기·크기·깊이로
- 카드 오른쪽에 붙는 `미답` 표는 머리 띠와 다른 자리입니다 — 거슬린다고 하신 건 머리 띠의 따로 뽑은 칸이라, 카드까지 손댈지는 디자이너 판단으로 두되 손대면 이유를 남기게 했습니다
- 다 찼을 때(46/46) 어떻게 보이는지도 정하도록

designer 는 지금 017(비밀 바깥 경로 칸) → 009(제목 앵커) → 018 순으로 갑니다.

### 2026-08-28T15:25:14+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-08-28 15:25:14 KST - lead]`

`[2026-08-28 15:22 KST - lead]`

세 건을 처리했습니다.

**007 반려 원인 — 고침은 있었는데 화면에 닿지 않았습니다.** 14:47 재작업은 실제로 이뤄졌지만, 그 무인 작업자가 **워크트리 안에서** 돌았고 봉투에 git 이 없어 **커밋을 못 했습니다.** 변경이 그 워크트리에만 남아 본 저장소에는 한 줄도 오지 않았습니다. 그 세션이 상신 노트에 "커밋이 필요합니다"라고 적어 뒀는데 제가 읽지 않고 review 로 둔 것이 직접 원인입니다. 꺼내서 합쳤고(`9fce8bb` → `864f08b`) 다시 올렸습니다. 이제 문서 화면의 `→ done` 도 카드의 `✓ 승인` 과 같은 함수를 지나 같은 창·같은 문구가 뜹니다.

**같은 사고가 반복되지 않게 두 곳을 막았습니다** (`78fe985`):
- 워크트리 워커 봉투에 `add`·`commit`·`status`·`diff`·`log` 를 넣었습니다. `push`·`checkout`·`reset`·`stash` 는 주지 않습니다 — 앞은 바깥으로 나가는 일이고 뒤는 지우는 손입니다
- git 훅 넷이 `--show-toplevel` 로 자리를 정하고 있어서, 워크트리에서 커밋하면 post-commit 이 남기는 커밋 노트가 **워크트리 사본의 vault** 에 쓰였습니다(이번에 실제로 그랬습니다). 이제 스크립트는 제 자리 것을 부르되 `S9_ROOT` 는 본 저장소로 못박습니다

**017 재작업 착수** — 바깥 폴더에 키를 넣을 수 없다는 지적, designer 에 맡겼습니다. 쓰는 자리를 하나로 두고 CLI 도 같이 맞추게 했습니다. 바깥 폴더가 없으면 조용히 안쪽으로 떨어뜨리지 말고 왜 못 고르는지 말하게 했고, 같은 이름이 양쪽에 있으면 안쪽이 이기니 그 사실도 알리게 했습니다.

**019 (자동 pull/push) — 먼저 사실 하나.** 지금 이 저장소에서는 **자동 pull/push 가 돌지 않습니다.** `.s9-sync` 마커가 있을 때만 켜지고 여기엔 없습니다. 오늘 커밋은 전부 제가 손으로 했고 push 는 한 번도 없었습니다. 켜지는 자리는 `s9 instance init` 하나인데, 거기서 **묻지 않고** 마커를 씁니다.

의견입니다:
- **기본 꺼짐에 동의합니다.** push 는 이 기계 밖으로 나가는 일이고, 사람이 고르지 않은 기본값이 남의 리모트로 글을 내보내는 건 되돌릴 수 없는 쪽으로 기운 기본값입니다.
- **다만 진짜 문제는 셋을 한 스위치로 묶은 것입니다.** 지금 마커 하나가 commit·pull·push 를 함께 켭니다. 로컬 커밋은 이 저장소의 영속성 그 자체이고(문서는 커밋돼야 남습니다), 바깥과 오가는 건 pull·push 뿐입니다. "혼자 쓰니 끈다"를 고르면 로컬 커밋까지 꺼져서, 문서가 미커밋으로 쌓이다 누군가의 git 한 줄에 사라지는 길이 열립니다 — 오늘 그 사고를 두 번 봤습니다(워크트리에 갇힌 007, 미커밋으로 남아 있던 화면 작업 575줄).
- 물음은 대화형일 때만 하고, 답이 없거나 무인 설치면 **꺼진 채로** 끝내야 합니다. 물음이 막혀 설치가 멈추는 게 더 나쁩니다.
- 이미 만들어진 인스턴스도 `s9 sync --on/--off` 로 껐다 켤 수 있어야 합니다. 지금은 "파일을 지우세요"를 외워야 합니다.

판단만 주시면 바로 만들겠습니다: **한 스위치 그대로 두고 기본만 끔**으로 갈지, **로컬 커밋(기본 켬)과 pull·push(기본 끔)를 나눌지**. 저는 나누는 쪽을 권합니다.

### 2026-08-28T15:29:54+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-08-28 15:29:54 KST - lead]`

007을 다시 열고 세 관점을 동시에 돌렸습니다 — designer(시각·정보 위계) · ux-writer(낱말·읽는 사람) · architect(개념 모델·용어의 단일 출처). 셋 다 **파일은 못 만지게** 했습니다. 지금 017 작업으로 `web/index.html` 이 다른 손에 있어서, 의견만 받고 구현은 하나로 모아서 하겠습니다.

각자에게 던진 물음의 초점이 다릅니다:
- **designer** — 한 줄에 이름(`→ done`)과 행위(`✓ 승인`) 두 종류가 섞인 게 실제로 읽기를 방해하는지, 캡처를 떠서 눈으로 보고 판단
- **ux-writer** — `deny` 가 이 자리에 맞는 낱말인지(여기서 일어나는 일은 거부가 아니라 되돌려 보내 다시 하게 함입니다), 그리고 버튼을 바꾸면 판정 창 문장·History·CLI 출력까지 어디까지 따라가야 말이 안 갈라지는지
- **architect** — "판정"이 상태 전이의 부분집합에 붙은 별도 개념인지 화면에서만 만들어 낸 것인지, 낱말의 단일 출처를 둘 값이 지금 규모에서 나는지

제 사전 판단도 적어 둡니다. 이 갈라짐은 사고가 아니라 의도였습니다 — `→ done` 은 **상태의 이름**이라 문서 앞머리·CLI·커밋 메시지에 같은 글자로 박히고, `✓ 승인` 은 **사람이 하는 일**이라 글꼴로 갈랐습니다. 다만 그 의도가 화면에서 안 읽히면 의도가 아니라 그냥 불일치입니다. 그게 지금 상황으로 보입니다.

셋의 답이 오면 모아서 한 안으로 정리해 올리겠습니다.

### 2026-08-28T15:34:18+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-08-28 15:34:18 KST - lead]`

architect 답이 먼저 왔습니다. designer·ux-writer 는 아직입니다.

**architect 결론: `approve`/`deny` 에는 반대, 대신 문법을 하나로 통일 — `✓ 승인 → done` · `↺ 반려 → in-progress`.**

핵심 논지 셋입니다.

1. **판정은 화면이 만든 각색이 아니라 서버가 이미 믿고 있는 사실입니다.** review 에서 나가는 두 간선을 서버가 열 자리 남짓에서 다르게 취급합니다 — 승인에만 goal/TDD 게이트 면제, 반려는 사유 필수에 워커 재스폰까지. 다만 지금은 그게 속성이 아니라 `if old == "review"` 열 개로 흩어져 있습니다.

2. **`approve`/`deny` 는 문제를 못 고칩니다.** 다른 버튼은 목적지 명사(`→ done`)인데 이것만 행위 동사(`approve`)라, 언어만 맞추고 문법은 여전히 둘입니다. 2차에서 서체로 한 번 고쳤는데 또 물으신 것이 그 증거라는 지적입니다. 게다가 소문자 영문 동사는 이 화면에서 곧 상태 식별자의 서체라, `s9 status <id> approve` 를 치게 되는데 그런 전이는 없습니다.

3. **전부 상태 이름으로 통일하는 안(B)도 대가가 있습니다.** `→ in-progress` 가 반려라는 사실이 버튼에서 사라집니다 — 사유 필수·재스폰이 달린 가장 무거운 간선이 "그냥 뒤로 옮기기"로 보입니다.

그리고 **라벨 논의와 별개로 지금 살아 있는 결함 하나**를 짚었는데, 제가 실측해 확인했습니다:

```
web/index.html:4016   postStatus(id, "done", memo ? "승인: " + memo : ...)
bin/s9:6535           if not memo.startswith("승인:"): continue
```

화면이 문자열에 의미를 실어 보내고 서버가 그 한글 두 글자를 파싱합니다. 계약 테스트도 없어서, 접두사를 건드리면 `s9 approvals` 가 **에러 없이 빈 목록**이 되고 승인 메모 후속 작업이 소리 없이 죽습니다. 더구나 그 바로 윗줄이 이미 `(review, done)` 을 확인하므로 이 검사 자체가 불필요합니다. 낱말을 어떻게 정하든 이건 따로 끊어야 합니다.

designer·ux-writer 답이 오면 셋을 견줘 한 안으로 정리해 올리겠습니다.

### 2026-08-28T15:35:23+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-08-28 15:35:23 KST - lead]`

ux-writer 도 왔습니다. **architect 와 같은 결론에 다른 길로 도달했습니다** — `approve`/`deny` 반대, `✓ 승인 → done` · `↺ 반려 → in-progress`.

ux-writer 가 더한 것 셋입니다.

**1. 화면의 영문은 번역이 아니라 인용입니다.** `→ done` 은 사용자가 문서 앞머리·CLI·커밋·History 에서 **대조할 원본이 있는** 글자입니다. `approve` 에는 그 원본이 없습니다 — 상태머신에 그런 전이가 없고 `s9 approve` 도 없고 History 에도 안 남습니다. 화면에만 있는 영어는 인용이 아니라 새로 만든 외래어입니다. 상태 이름을 한글로 안 바꾼 그 논리를 그대로 뒤집으면 행위를 영문으로 바꾸는 것도 같은 잘못이라는 지적입니다.

**2. `deny` 는 낱말 자체가 틀렸습니다.** 여기서 일어나는 일은 거절이 아니라 되돌려 보내 다시 하게 하는 것입니다 — 문서는 살아 있고, 사유가 필수이고, 담당 세션에 통지가 가고, 체크된 TDD 시나리오가 초기화되고, 작업자가 재개됩니다. deny 다음에 오는 건 없는데 실제로는 문서가 다시 굴러가니 버튼이 거짓말을 합니다. 게다가 이 화면엔 이미 `거부` 가 **다른 뜻**으로 있습니다(서버가 전이를 못 받았을 때의 알림).

**3. 진짜 원인 진단이 architect 와 정확히 같습니다** — `→ done` 에는 도착지 이름이 있는데 `✓ 승인` 에는 없다. 판정 창은 이미 "승인해 done 상태로 넘깁니다"라고 제대로 말하는데 **버튼이 그 말을 안 합니다.** 사용자는 버튼만 보고 창을 열지 말지 정하는데도요.

곁들여 짚은 것들: 지금 창 머리가 넷 다 `판정` 인데 승인·반려 아닌 이동까지 그렇게 부르는 건 3차에서 세운 원칙과 어긋난다 · `→ 이어 말하기` 의 `→` 가 "이 상태로 옮김"과 "저기로 감" 두 뜻으로 갈린다 · `「제목」 을(를)` 은 이 화면에 남은 유일한 서식 편지투인데 받침으로 조사를 계산할 수 있다.

그리고 **바꿀 때 따라가야 할 자리를 13개 목록으로 냈는데, 그 목록 자체가 바꾸지 말라는 근거**입니다. 특히 `bin/s9:6535` 의 `startswith("승인:")` — architect 가 짚은 그 결함을 독립적으로 같이 찾았습니다. 그리고 되돌릴 수 없는 것 하나: `— 승인: …` 이 이미 vault 전체 History 에 박혀 있어, 낱말을 바꾸면 옛 기록과 새 기록이 다른 말이 됩니다.

designer 답만 남았습니다. 폭 문제(`✓ 승인 → done` 이 보드 카드에서 접히는지)는 둘 다 designer 판단으로 넘겼습니다.

### 2026-08-28T15:43:51+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-08-28 15:43:50 KST - lead]`

**019 — 나눴습니다. 그리고 "끌 일이 없잖아"가 제 제안보다 낫습니다.** 끌 일이 없는 것에는 스위치를 달지 않는 게 맞습니다. 스위치는 설명할 것을 하나 늘리고, 아무도 안 끄는 스위치는 그 값을 못 갚습니다. 그래서 **스위치는 하나만** 뒀습니다:

```
로컬 커밋    언제나        스위치 없음
pull·push    기본 끔       instance init 이 묻고, s9 sync --on/--off 로 나중에도
```

파일도 새로 만들지 않았습니다 — `.s9-sync` 하나의 **내용이 모드**입니다(`local`/`remote`). 두 파일이면 조합이 넷이 되고 그중 둘은 뜻이 없습니다. 옛 마커(`on — …`)는 `remote` 로 읽습니다. 이미 여럿이 쓰던 인스턴스가 업그레이드 한 번에 조용히 끊기는 게 이 변경으로 생길 수 있는 제일 나쁜 일이라서요. 설치 물음은 대화형일 때만 하고 답이 없으면 `local` 입니다. TDD 7건, 전체 스위트 1417건 통과. 커밋은 같은 파일에 017 작업이 아직 붙어 있어 그 세션이 끝나면 함께 하겠습니다.

**007 — 세 관점이 모두 왔고, 셋 다 `approve`/`deny` 에 반대했습니다.** 서로 다른 길로 같은 결론에 닿았습니다.

- **architect**: 판정은 별도 개념이 아니라 전이 그래프의 두 간선에 붙은 속성이다. `approve` 는 없는 상태 이름을 발명하는 것이고, 결국 `s9 approve` alias 를 만들고 싶어져 쓰기 경로가 둘이 된다
- **ux-writer**: 이 화면의 영문은 번역이 아니라 **인용**이다. `→ done` 은 문서·CLI·커밋에서 대조할 원본이 있지만 `approve` 에는 없다. 그리고 `deny` 는 뜻이 틀렸다 — 여기서 일어나는 일은 거절이 아니라 되돌려 보내 다시 하게 하는 것이다
- **designer** (유일하게 실측): **코드 주석의 전제가 사실이 아니었다.** "글꼴이 다르니 헷갈리지 않는다"고 적혀 있는데, review 문서의 버튼 줄에는 **mono 상태 이름이 한 개도 없다** — `RVLABEL` 이 review 에서 나가는 두 간선을 **둘 다** 덮기 때문입니다. 대비할 상대가 없으니 글꼴 구분은 애초에 작동한 적이 없고, 사용자에게는 "리뷰만 한국어"만 남습니다

**셋의 진단이 같습니다: `→ done` 에는 도착지 이름이 있는데 `✓ 승인` 에는 없다.** 판정 창은 이미 "승인해 done 상태로 넘깁니다"라고 제대로 말하는데 버튼이 그 말을 안 합니다.

권고안은 **`승인 done` · `반려 in-progress`** — 행위는 본문체, 이름은 그 뒤에 mono 로. designer 가 폭을 실측했는데 `✓ 승인 done` 은 보드 카드 215px 에서 두 줄로 감기고, **글리프(✓/↺)를 빼면 한 줄에 들어갑니다.** 글리프는 두 버튼을 가르려고 있던 것인데 이제 `done`/`in-progress` 가 그보다 강하게 가릅니다.

곁들여 나온 것 둘: `→ 이어 말하기` 의 `→` 가 목적지 화살표로 위장하고 있어 떼야 하고(비용 0), `bin/s9:6535` 가 화면이 만든 `"승인:"` 접두사를 파싱하고 있어 낱말을 건드리면 승인 후속 작업이 소리 없이 죽습니다.

designer 가 5개 안을 나란히 그린 대조 캡처를 007 문서에 붙여 뒀습니다(`cmp-5안-대조.png`). **그대로 진행할까요, 아니면 그래도 `approve`/`deny` 로 갈까요.** 지금 `web/index.html` 은 017 작업 중이라, 답 주시면 그게 끝나는 대로 붙이겠습니다.

### 2026-08-28T15:55:03+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-08-28 15:55:03 KST - lead]`

007 확정안을 문서에 못박았고, 021 로 새 요청을 접수했습니다. 둘 다 `web/index.html` 이라 지금 017 작업 중인 세션이 끝나는 대로 붙입니다.

**007 확정안**
- 판정 버튼 `승인 done` · `반려 in-progress` — 행위는 본문체, 상태 이름은 뒤에 mono
- **글리프(✓/↺)는 뺍니다.** 실측상 `✓ 승인 done` 은 보드 카드 215px 에서 두 줄로 감기고, 빼면 한 줄에 듭니다. 글리프는 두 버튼을 가르려고 있던 건데 이제 `done`/`in-progress` 가 그보다 강하게 가릅니다
- 보드 카드와 문서 화면이 **반드시 같은 글자** — 한쪽만 고치면 3차 반려가 되풀이됩니다
- `→ 이어 말하기` 의 `→` 제거. 지금 그 화살표가 "다섯 번째 목적지"로 위장합니다
- 창 머리는 승인·반려만 `판정`, 나머지는 `상태 옮기기`. 전이 실패 알림의 `거부` 는 `실패` 로(사람이 거절한 게 아니라 서버가 못 받은 겁니다)
- `을(를)` 은 받침으로 조사를 계산
- 낱말 자체(`승인`/`반려`)는 안 바꿉니다 — 이미 vault 전체 History 에 박힌 기록 어휘입니다
- 그리고 `"승인:"` 접두사 결합을 끊습니다: 화면은 메모 원문만 보내고 서버가 접두어를 짓게, 계약 테스트도 겁니다

**021 — 언급한 것을 눌러서 연다.** 착수 전에 짚어 둔 것이 하나 있습니다. 문서 id 는 이미 있는 문서 링크와 같이 가면 되는데, **파일 경로는 다릅니다.** 지금 저장소 파일을 화면에 내주는 길은 문서 첨부 전용(`/api/asset`, 문서 가시성 상속)뿐이고 코드 파일에는 없습니다. 새로 내려면 **무엇을 내줄지 경계를 먼저 정해야 합니다** — 비밀 경로(`users/*/secrets`)와 `state/` 제외 같은 규칙이 필요하고, 그 판정은 이미 있는 게이트와 **같은 함수**여야 합니다. 새로 만들면 옆문이 됩니다.

`web/index.html:4016` 처럼 줄 번호까지 붙은 형태를 저희가 보고에 늘 쓰니, 그 언저리만 보이는 미리보기가 새 탭보다 나을 것 같습니다. 이 판단도 designer 에 함께 넘기겠습니다.

017 이 끝나면 007 → 021 순으로 붙이고 보고하겠습니다.

### 2026-08-28T15:56:39+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-08-28 15:56:39 KST - lead]`

**REQ-20260828-022-62x6 (거기서 바로 이어 말하기)** 로 접수했고, 021 과 `relates` 로 묶었습니다. 같은 자리에 붙는 요구라 함께 설계해야 합니다 — 021 은 언급된 것을 **읽게** 하고, 022 는 그것을 **집게** 합니다. 따로 만들면 같은 글자 옆에 버튼이 둘 서거나 하나가 다른 하나를 가립니다.

기제 자체는 이미 있습니다. 문서 화면과 보드 카드의 `→ 이어 말하기` 가 그 문서를 집어 입력줄을 그 문서에 붙입니다. 없는 건 **터미널 대답 안에서** 그 손잡이가 나오는 것뿐입니다 — 지금은 대답에 적힌 id 를 보고 Docs 탭으로 건너가 문서를 찾아 눌러야 하고, 그 순간 읽던 자리를 잃습니다.

기존 규율 둘도 함께 지키도록 적어 뒀습니다: 집기와 아티클 스위치(✎)는 서로를 끈다, 그리고 문서 안에서 자리를 옮기는 손짓이 화면을 통째로 갈아치우면 안 된다(072가 세운 것 — 이번 요구는 그 규율의 터미널 판입니다).

대기 줄은 이렇습니다: **017**(진행 중) → **007**(판정 버튼) → **021 · 022**(언급한 것 읽기·집기, 한 묶음). 017 이 끝나는 대로 순서대로 붙이고 보고하겠습니다.

### 2026-08-28T16:04:09+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-08-28 16:04:09 KST - lead]`

**023 (대답 속 코드 접어 두기)** 는 말씀대로 진행하지 않고 designer 검토만 돌렸습니다. 파일은 안 건드리게 하고, 안을 두세 개 나란히 그려 캡처로 보여 주도록 했습니다 — 눈으로 고르실 수 있게요. 검토에 넣은 물음은 접는 게 맞는지·기본이 접힘인지 펴짐인지·짧은 코드와 200줄 로그에 같은 규칙을 쓸 수 있는지·접힌 머리에 무엇을 적어야 펼지 말지 정할 수 있는지, 그리고 **021·022 와의 자리 다툼**입니다. 한 줄의 글자 하나에 손잡이가 셋(열기·집기·펴기) 붙을 수 있어서, 무엇을 버려야 하는지가 이 검토의 핵심입니다.

그 사이 **017 이 끝났습니다** (커밋 `1812236`, 스위트 1417건 통과). 넣기 폼은 한 벌 두고 그 아래 목적지 두 칸을 좌우로 세웠습니다 — 값은 한 번만 치고 목적지만 고릅니다. 폼을 두 벌로 안 만든 이유도 문서에 적혀 있습니다. 쓰는 자리는 `secret_write()` 하나, 지우는 자리는 `secret_remove()` 하나로 모아 CLI 와 화면이 같은 함수를 지납니다.

작업 중 결함 둘을 캡처로 잡았습니다: 서버가 "이 값은 가려져 쓰이지 않습니다"라고 답하는데 화면이 그 응답을 버리고 있었고, `s9 secret set K --external` 이 argparse 에 먹혀 조용히 무시돼 저장소 안에 쓸 뻔했습니다 — 이 요청이 고치려던 바로 그 실패 유형입니다.

**019 도 done 으로 닫았습니다.** 무인 설치가 `local` 로 끝나는 것, local 모드가 실제 원격에 아무것도 안 보내는 것, 옛 마커가 `remote` 로 읽히는 것까지 실제 bare 원격으로 검증했습니다.

이제 **007 확정안 → 021·022** 를 한 세션에 붙였습니다. 021·022 는 같은 글자에 손잡이가 둘 붙는 자리라 함께 설계하게 했고, 파일 경로 링크는 경계를 못 정하겠으면 억지로 열지 말고 문서 id 부터 내라고 못박았습니다.

### 2026-08-28T16:06:10+09:00 subagent (agent: sub:designer:afd44bf7, by nicehugepark)

`[2026-08-28 16:04:32 KST - designer]`

**결론: 기본 접힘은 반대다. 실측하니 이 판의 대답 속 코드 블록은 "곁다리 코드"가 아니라 대답의 결론 그 자체였다. 접어야 할 것(도구 입·출력)은 이미 접혀 있다. 대신 문턱 하나 — 12줄 넘는 블록만 "머리 8줄 + `+ N줄 더 보기`" 로 자르고, 그보다 먼저 고칠 것은 접기가 아니라 지금 블록이 입고 있는 회색 색면 + 세로 띠다.** 파일은 건드리지 않았다.

캡처 (경로 그대로):
- 지금 화면: `/tmp/claude-1000/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/scratchpad/term-now.png`
- 세 안 대조(짧은 블록 / 30줄 블록 두 줄로): `/tmp/claude-1000/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/scratchpad/cmp-fold.png`
- 대조 페이지 원본(실제 `<style>` 을 뽑아 재현): `/tmp/claude-1000/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/scratchpad/fold-mock.html`

---

### 0. 먼저 — 실측이 전제를 뒤집었다

최근 세션 40개의 transcript에서 **리드/에이전트 대답 본문의 fenced 블록 43개**를 전수로 셌다.

| 길이 | 개수 |
|---|---|
| 1~3줄 | 25 |
| 4~10줄 | 18 |
| 11줄 이상 | **0** |

그리고 **43개 전부 언어 태그가 없다**(` ```js ` 같은 것이 하나도 없음). 내용을 열어 보니 소스 코드가 아니었다:

- 붙여 실행할 명령 절차 (`bin/s9 serve --stop-guard` … 4줄)
- 실측 수치 표 (`사건 없음 169px / 복구 성공 170px / 둘 다 접으면 132px`)
- before/after 대응 (`"이 로그 왜 이래?\n[File: …]" → 이전 request ✕ / 지금 question ○`)
- 결정 요약 (`로컬 커밋 언제나 스위치 없음 / pull·push 기본 끔 …`)

즉 **이 판에서 코드 블록은 "대답에 섞인 부산물"이 아니라 대답이 도달한 결론을 고정폭으로 세워 둔 자리**다. 진짜 긴 코드는 어디 있나 — 도구 입력(Edit/Write)과 도구 출력(Bash/Read)에 있고, 거기는 이미 접혀 있다(`web/index.html` 의 `ccEvent`: 도구 입력 130자 초과 접힘, 결과 200자 초과 접힘, 연속 Bash 2개 이상은 `Ran N shell commands` 한 줄로 묶음).

---

### 1. 지금 무엇이 문제인가 — "길다"가 아니다

캡처(`term-now.png`)와 대조 A열을 보고 셋을 찾았다.

**(1) 면이 문장 사이에 끼어든다 — 길이가 아니라 재질 문제.**
`.ccblk` 은 `background:rgba(255,255,255,.06)` + `border-left:2px solid` 다. 회색 색면 + 세로 띠 — 이 제품이 명시적으로 금지한 두 가지가 정확히 그 모양으로 있다. 게다가 인라인 `.cccode` 도 `background:rgba(255,255,255,.09)` 색면 칩이라, 실제 캡처의 15:56 대답 한 개에만 회색 조각이 **여섯 개** 흩어져 있다(`relates`, `→ 이어 말하기`, `id`, `Docs`, `072` …). 문장이 얼룩덜룩해서 **무엇이 강조인지 사라진다.** 잃는 것은 "읽는 속도"가 아니라 **강조의 신호값**이다.

**(2) 블록이 앞 문장과 뒤 문장을 갈라 놓는다.**
`.ccblk` 은 `margin:5px 0` 에 색면 padding 이 붙어 실제 3줄짜리가 5줄어치 자리를 먹는다. "그래서 스위치는 하나만 뒀습니다:" → 블록 → "파일도 새로 만들지 않았습니다" 이 세 덩어리가 한 눈에 안 들어온다. 잃는 것은 **한 문단이 하나의 생각이라는 감각**이다.

**(3) 그런데 지금 실제로 판을 먹고 있는 건 코드 블록이 아니다.**
캡처를 보면 화면의 세로를 차지하는 것은 `Bash(...)` 도구 줄, `⎿` 결과 줄, `agent(...)` 줄이다. 대답 본문의 코드 블록은 이번 세션 569개 이벤트 중 **5개** 뿐이다. 코드 블록을 접어도 화면은 별로 안 조용해진다.

---

### 2. 접는 것이 맞는가

**기본은 펴짐이다.** 근거 셋:

- **접으면 대답이 결론을 잃는다.** 대조 B열을 보라 — "스위치는 하나만 뒀습니다:" 로 끝나고 그 *하나*가 안 보인다. 실행할 명령 네 줄도 한 번 더 눌러야 한다. 43개 중 43개가 이 꼴이 된다: 클릭 세금 100%, 이득 0.
- **접힌 글자는 브라우저 찾기(Ctrl+F)에 안 걸린다.** `<details>` 안은 검색 대상이 아니다. 이 터미널은 이미 도구 출력 접힘으로 그 값을 치르고 있는데, 대답 본문까지 접으면 **화면에서 글자로 찾을 수 있는 것이 산문 문장뿐**이 된다. 문서 id·경로가 코드 블록에 들어 있으면 021(눌러 열기)도 그 순간 무력해진다.
- **되돌리기 비용이 비대칭이다.** 펴진 것을 안 읽는 비용 = 스크롤 한 번. 접힌 것을 놓치는 비용 = 결론을 못 본 채 다음 지시를 내림.

**길이에 따라 달라야 하나 — 그렇다. 단 문턱은 하나만.**
기준은 줄 수가 아니라 **"블록 앞 문장과 뒤 문장이 블록과 한 화면에 같이 서는가"** 다. 실측: `.ccterm` 은 12.5px/line-height 1.7 = 줄당 약 21px, 1080 높이 창에서 출력 판(`.ccout`)이 약 700px = **약 33줄**. 블록 하나가 판의 1/3(≈11줄)을 넘으면 앞뒤 문맥이 같이 못 선다. 그래서 **12줄**을 문턱으로 잡는다. 43개 중 0개가 걸린다 — 지금은 아무것도 안 바뀌고, 에이전트가 코드를 붙이기 시작하면 그때부터 작동한다. **바로 그것이 이 문턱의 미덕이다: 오늘 있는 것을 망가뜨리지 않고, 사용자가 걱정한 미래만 막는다.**

**긴 것도 접지 말고 자른다(권고, 대조 C열 아래).** `<details>` 로 통째로 감추는 대신 **머리 8줄은 늘 보이고 나머지만 `+ N줄 더 보기`** 로 접는다.
- 첫 줄들이 **스스로 머리**가 된다 — 요약을 지어낼 필요가 없다.
- `+ N개 더 보기` 는 이 제품이 보드 컬럼·Docs 그룹에서 이미 쓰는 낱말이다. 새 문법 0.
- 잘린 줄은 DOM 에 그대로 남겨 Ctrl+F 가 계속 걸리게 한다(`<details>` 는 안 된다).

---

### 3. 접힌 머리에 무엇을 적나

권고안에서는 **머리를 안 적는다** — 그게 이 질문의 답이다. 그래도 순수 접힘(B안)을 고를 경우를 위해 순위를 매기면:

1. **첫 줄, 손대지 않고 그대로.** 실측상 첫 줄이 곧 정체다 — `$ bin/s9 next`, `web/index.html:4016`, `로컬 커밋 언제나 스위치 없음`. 요약보다 원문이 강하다.
2. **줄 수.** 펼 값을 가늠할 유일한 수. `· 30줄`.
3. **언어 — 쓸 수 없다.** 43개 중 0개가 태그를 달았다. 추정해서 적으면 틀린 정보를 화면에 세우는 것이다.
4. **파일 경로 — 대답 블록엔 거의 없다.** 있으면 이미 첫 줄에 들어 있다(`web/index.html:4016`).

즉 `첫 줄 그대로… · N줄` 하나면 끝이고, 그마저도 "첫 8줄을 그냥 보여주기"보다 못하다.

---

### 4. 021·022 와의 자리 다툼

**손잡이 셋이 한 낱말에 서는 일은 사실 없다 — 붙는 대상이 다르다.**

| 요구 | 손잡이 | 붙는 대상 |
|---|---|---|
| 021 | 열기 | **낱말** (문서 id · 파일 경로) |
| 022 | 집기 | **낱말** (문서 id) |
| 023 | 펴기 | **블록** (블록 끝 한 줄) |

**펴기는 다툼에 안 낀다.** 블록 하단 왼쪽의 독립된 한 줄이고, 낱말 위에 겹칠 일이 없다. 진짜 충돌은 **021 vs 022 — 문서 id 한 낱말에 손잡이 둘**이고, 이건 023 이 만든 문제가 아니다.

**제안(021·022 몫이지만 자리 다툼 답으로):** 낱말 자체 = **열기** 하나만(기존 `a.doclink` 점선 밑줄 어휘 그대로). **집기는 낱말 옆에 버튼을 세우지 말고, 열어서 뜨는 미리보기(hovercard) 바닥줄에 `이어 말하기` 로 둔다.** 이유 — 한 낱말에 손잡이는 하나, 미리보기는 이 제품이 문서 뷰어에서 이미 쓰는 패턴, 그리고 터미널 줄에 버튼이 늘어나면 고정폭 판의 줄 정렬이 무너진다.

**코드 블록 안의 경로도 링크가 되어야 하나 — 아니오. 그리고 지금 코드가 이미 그렇게 정해 놓았다.**
`ccText` 의 인라인 파이프라인은 fenced 블록과 인라인 `` ` `` 코드를 **먼저** placeholder 로 빼내고(`keep()`), 그 다음에야 경로·해시 강조 정규식을 돌린다(`web/index.html` 6024·6031 → 6035 이후). 그래서 **블록 안 경로는 지금도 `.ccval` 이 안 붙는다.** 이건 버그가 아니라 규율이다:

- 블록의 내용은 **읽을 것이 아니라 붙일 것**이다. 링크를 걸면 드래그 선택이 링크 드래그가 되고, 복사본에 마크업이 섞일 위험이 생기며, 한 판에서 "눌러야 할 것"과 "긁어야 할 것"이 뒤섞인다.
- 대신 값이 훨씬 큰 손잡이가 따로 있다: 블록 우상단 hover 시 **`⧉ 복사`** 하나(대조 C열). 실측상 이 블록들의 절반은 명령 절차다.
- 예외는 **인라인** `` ` `` 코드 — 이미 대답 문장 안이므로 문서 id·경로면 021 링크를 걸어도 된다. 다만 지금 인라인 코드도 placeholder 로 보호되므로, 021 을 붙이려면 그 순서를 의도적으로 바꿔야 한다. **모르고 순서를 바꾸면 블록 안까지 링크가 번진다** — 021 구현자에게 반드시 전달할 것.

**접힌/잘린 상태에서 문서 id 는?** 잘린 부분에 있으면 링크도 검색도 사라진다. 그래서 12줄 문턱을 높게 잡았고, `<details>` 대신 클리핑을 골랐다.

---

### 5. 이 판의 어휘로 어떻게 생겨야 하나

대조 C열이 그 답이다. 재료는 **선·여백·글자색**뿐:

- **색면 제거, 세로 띠 제거.** 대신 **위아래 헤어라인 + 좌측 14px 들여쓰기**. 이 어휘는 새 것이 아니다 — 이 판의 표(`.cctbl`)가 `border-top/bottom` 헤어라인으로 이미 판을 나눈다.
- **색은 면이 아니라 글자에 남긴다** — 블록 글자는 지금과 같은 청록(`--cc-cyan`). 색 신호는 유지되고 면만 사라진다.
- **펴는 손잡이 = `+ N줄 더 보기` 텍스트 버튼**(mono, dim, hover 밑줄). 보드·Docs 의 `+ N개 더 보기` 와 같은 손이다. 아이콘·pill·화살표 금지.
- **복사 = `⧉ 복사`**, dim 상주 없이 hover/focus 에서만. (구현 주의: 블록에 `padding-right:6ch` 를 예약해야 한다 — 캡처 C열 첫 블록에서 손잡이가 주석 글자 위에 앉았다.)
- 인라인 `.cccode` 도 같은 규칙으로 색면을 걷고 **청록 글자 + 좌우 얇은 여백**만 남기는 것을 권한다. 블록만 고치면 문장 속 얼룩은 그대로다.

---

### 6. 하지 말아야 할 것 (내 안에서 스스로 잘라낸 것)

- **문법 강조(syntax highlighting) 금지.** 43개 전부 언어 태그가 없어 무엇으로 칠할지 알 수도 없고, 하이라이터는 색을 대여섯 개 쓰게 되어 "색은 신호만" 을 정면으로 깬다. 외부 CDN 금지·의존성 0 규칙과도 충돌한다.
- **줄 번호 gutter 금지.** 3줄짜리에 번호는 잡음이고, 긁어 복사할 때 번호가 딸려 온다.
- **언어/줄수 배지 pill 금지.** 색면 배지 금지 규칙 그대로.
- **블록별 접힘 상태 기억 금지.** 스트림 이벤트라 블록에 안정된 id 가 없다. 폴링 재렌더마다 엉뚱한 블록이 펴진다.
- **"코드 전부 접기/펴기" 전역 스위치 금지.** 끌 일이 없는 것에는 스위치를 안 단다 — 이 세션이 019 에서 방금 세운 규칙이다.
- **블록 내부 스크롤(max-height + overflow) 금지.** 스크롤 판(`.ccout`) 안의 스크롤 판은 휠을 가로챈다.
- **범위를 더 줄여야 한다면 잘라낼 순서:** ① `⧉ 복사` 손잡이(별도 요구로 떼도 됨) → ② 12줄 문턱과 `+ N줄 더 보기`(오늘 걸리는 블록이 0개다) → ③ **색면·세로 띠 제거는 마지막까지 남긴다.** 이 하나만 해도 사용자가 느낀 불편의 대부분이 사라지고, 접기 없이 즉시 효과가 난다.

---

### 진행하려면 필요한 판정

사용자가 `cmp-fold.png` 를 보고 **A(지금) / B(전부 접힘) / C(권고)** 중 하나를 고르면 된다. 내 권고는 **C**이고, 그중에서도 **색면·세로 띠 제거 먼저, 12줄 문턱은 그다음**이다.

### 2026-08-28T16:07:37+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-08-28 16:07:37 KST - lead]`

023 검토가 왔습니다. 구현은 안 했고 판정 대기로 올렸습니다 — **결론은 "기본 접힘 반대"** 입니다.

**실측이 전제를 뒤집었습니다.** 최근 세션 40개에서 대답에 들어간 코드 블록 43개를 전수로 셌는데 **11줄 넘는 것이 하나도 없었고**, 43개 전부 언어 태그가 없었습니다. 열어 보니 소스 코드가 아니라 붙여 실행할 명령 절차 · 실측 수치 표 · 결정 요약이었습니다. 즉 이 판에서 코드 블록은 곁다리가 아니라 **대답이 도달한 결론을 고정폭으로 세워 둔 자리**입니다. 접으면 "스위치는 하나만 뒀습니다:" 로 끝나고 정작 그 하나가 안 보입니다. 접힌 글자는 Ctrl+F 에도 안 걸리고요. 진짜 긴 코드는 도구 입·출력에 있는데 거기는 이미 접혀 있습니다.

**대신 진짜 원인을 찾았고 제가 코드로 확인했습니다:**

```
web/index.html:730  .ccblk{background:rgba(255,255,255,.06); border-left:2px solid …}
web/index.html:729  .cccode{background:rgba(255,255,255,.09); …}
```

**색면 하이라이트와 세로 띠** — 저희가 쓰지 않기로 한 바로 그 두 가지가 정확히 그 모양으로 있습니다. 인라인 칩까지 합치면 대답 하나에 회색 조각이 예닐곱 개 흩어져서, 길이 문제가 아니라 **무엇이 강조인지가 사라지는** 문제였습니다.

권고(C안)는 ① 색면·세로 띠를 걷고 위아래 헤어라인 + 들여쓰기로 ② 12줄 넘는 블록만 머리 8줄 남기고 `+ N줄 더 보기` 로 **접는 게 아니라 잘라서**(Ctrl+F 유지) ③ hover 시 `⧉ 복사` 하나. 오늘 12줄 걸리는 블록이 0개라 화면은 그대로이고, 앞으로 긴 코드가 붙을 때만 작동합니다.

**021·022 와의 자리 다툼은 없다**는 것도 확인됐습니다 — 열기·집기는 낱말에 붙고 펴기는 블록 끝 줄에 붙습니다. 진짜 충돌은 021 대 022(문서 id 한 낱말에 손잡이 둘)이고, 그쪽 제안은 **낱말은 열기 하나만, 집기는 미리보기 바닥줄에** 두는 것입니다. 지금 021·022 를 만들고 있는 세션에 이 제안과 함께, 인라인 코드에 링크를 걸 때 순서를 잘못 바꾸면 블록 안까지 링크가 번진다는 주의도 전달하겠습니다.

**A(그대로) · B(전부 접기) · C(권고)** 중 골라 주세요. 캡처 두 장을 023 문서에 붙여 뒀습니다. designer 권고는 C 이고, C 안에서도 **색면·세로 띠 제거를 먼저**입니다.

### 2026-08-28T16:16:32+09:00 subagent (agent: sub:designer:a49bfbcb, by nicehugepark)

`[2026-08-28 16:16:21 KST - designer]`

방금 뜬 알림은 아까 백그라운드로 밀렸던 `s9 serve --restart` 가 정상 종료(exit 0)한 것이라 조치할 것이 없다. 확인했다.

- 그 작업의 출력 끝에 이미 새 응답이 찍혀 있었다 — `/api/secrets` 가 `shadowed` 를 실어 보내고 `external_state: ok` 를 준다. 즉 **캡처를 뜨던 내내 서버는 새 코드로 돌고 있었다.**
- 지금 다시 물어도 같다: 남은 키는 사용자의 `test` 하나뿐이고, 검증에 쓴 `S9DBG_` 키는 자취가 없다.
- 중간의 `port-guard` 줄은 이 요청과 무관한 상시 감시 로그다.

앞선 보고에서 달라진 것 없다 — REQ-20260828-017-62x6 은 `review`, 전체 스위트 1417건 통과, 커밋은 리드 몫이다. `bin/s9` 에 다른 세션의 sync/instance 변경이 섞여 있다는 점만 커밋 전에 다시 확인하라.

### 2026-08-28T16:59:40+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-08-28 16:59:40 KST - lead]`

**REQ-20260828-029-62x6 (타임존을 골라서 고른다)** 로 접수하고 024 와 묶었습니다 — 024 가 시각을 이 설정에 물렸으니, 이제 사람이 그걸 고를 수 있어야 짝이 맞습니다.

두 역할을 겹치지 않게 나눴습니다. `web/index.html` 은 지금 007·021·022 designer 가 쥐고 있어서, **ux-writer 를 먼저 읽기 전용으로 돌렸습니다** — 그 결과물이 designer 의 입력이 됩니다.

ux-writer 에 넘긴 물음:
- 칸 이름이 `timezone` 인가 `시간대` 인가 (이 제품은 다른 데 박힌 글자만 영문으로 둡니다 — 007 에서 정한 그 규칙에 이게 속하는지)
- 목록에 IANA 이름(`Asia/Seoul`)을 적을지 한국어 도시명을 적을지, 지금 시각이나 `+09:00` 을 함께 보이는 게 도움인지 잡음인지. **498개**를 다 보여줄 수 없으니 처음에 무엇이 보일지도
- 검색어를 "서울"·"seoul"·"korea"·"kst"·"+9" 중 어디까지 받을지 — 안 되는 건 안 된다고 분명히 적어서 designer 가 구현할 수 있게
- 고르는 즉시 저장인지(같은 판의 스트림 스위치가 그렇습니다), 그리고 **바뀐 걸 그 자리에서 확인할 방법** — 화면 시각이 바로 따라가는지
- 치다가 위아래로 옮기고 Enter·Esc 하는 흐름의 각 자리 문구

이 설정은 이제 실제 쓰임이 있습니다 — 매 턴 주입되는 시각과 대시보드 시각이 여기를 따르니, 잘못 고르면 화면의 모든 시각이 틀어집니다. 그 무게를 문구가 감당해야 한다고 함께 넘겼습니다.

ux-writer 안이 나오면 designer 에 넘겨 붙이겠습니다. 지금 대기 줄은 **007 · 021 · 022**(작업 중) → **026**(첨부 폴더, 제 몫) → **029** 입니다.

### 2026-08-28T17:09:39+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-08-28 17:09:39 KST - lead]`

세 건이 한꺼번에 끝났고, 029 는 방금 구현에 들어갔습니다.

**007 · 021 · 022 — 커밋 `87ead56`, 스위트 1449건 통과.** 007 은 방금 승인해 주셨습니다.

007 에서 designer 가 제 확정안을 한 번 더 고쳤는데, 지적하신 그대로였습니다. `승인 done`(이름 9.5px 흐림)이 `open` 문서의 `→ blocked`(이름 10.5px 잉크)와 **같은 것을 두 얼굴로** 그리고 있었습니다. 그래서 규칙을 바꿨습니다 — **한 줄, 한 문법 `[행위][도착지]`**. 도착지 얼굴은 하나이고 무게 차이는 행위 칸이 맡습니다: 기호 `→` 는 그냥 이동, 낱말 `승인`·`반려` 는 판정. 제가 낸 안보다 낫습니다.

**021·022** 는 문서 id 글자 자체가 손잡이가 됐습니다. 얹으면 카드, 누르면 그 줄 **바로 아래** 미리보기가 펴지고 거기서 `이어 말하기`·`문서 열기`·`닫기` 로 갈립니다. 화면은 갈아치우지 않습니다.

**코드 파일 경로는 안 냈습니다.** "git 추적 파일만"이라는 경계를 떠올렸다가 버렸다고 합니다 — 이 저장소는 `state/sessions`·`users/profile` 을 추적하는데, 어제 012 가 "비밀은 값도 **경로도** 주지 않는다"고 정했고 계정 설정에 바깥 비밀 폴더 경로가 들어 있습니다. 하루 전 결정을 조용히 뒤집는 규칙이라 혼자 정하지 않고 **REQ-028** 로 뗐습니다. 그 판단이 맞습니다.

**029 (타임존 고르기)** 는 ux-writer 설계가 나와서 designer 에 넘겼습니다. 핵심 둘:
- 목록 한 행에 **IANA 이름 + 지금 시각** 둘만. 오프셋은 화면에서 빼고 검색어로만 받습니다 — 손목시계에 `+09:00` 은 안 적혀 있으니까요
- 시각을 보이는 결정적 이유: **`Etc/GMT+9` 는 실제로 UTC−9 입니다.** 이름만 보면 한국인은 백이면 백 그걸 고릅니다. 시각이 옆에 있으면 고르기 전에 틀린 게 보입니다

그리고 실측으로 잡은 것 둘 — 지금 `설정 저장` 버튼이 렌더 시점의 낡은 `timezone` 값으로 방금 저장한 값을 덮습니다(같이 고칩니다). 그리고 `display_tz()` 가 서버 기동 시 1회만 계산돼 타임존을 바꿔도 대화 기록 시각이 재기동 전까지 안 따라옵니다 — **REQ-030** 으로 뗐습니다. 문구로 덮지 말라는 게 ux-writer 판단이고 동의합니다.

이제 제 몫인 **026(첨부 폴더 두 벌)** 을 잡겠습니다.

### 2026-08-28T17:19:01+09:00 subagent (agent: sub:designer:a52356ae, by nicehugepark)

`[2026-08-28 17:19 KST - designer]`

배경 작업 종료 통지였고, 그때 대시보드 서버가 함께 내려가 있었습니다. **다시 올려 뒀습니다** — 감시자(pid 839)가 붙어 있어 이후 죽어도 되살아납니다.

- `http://127.0.0.1:9909/` → 200, `/api/transitions` 응답 정상
- 서빙 중인 화면에 이번 작업분(`actLabel`·`ccDocLink`)이 들어 있는 것까지 확인했습니다

작업 내용 자체에는 변동이 없습니다. REQ-20260828-007-62x6 · -021 · -022 셋 다 review 에 올라가 있고, 사용자가 F5 후 확인할 수 있는 상태입니다.

### 2026-08-28T17:19:29+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-08-28 17:19:28 KST - lead]`

맞습니다. **제가 규칙을 엉뚱한 자리에서 끌어다 썼습니다.** 뒤집었습니다.

`id_alias` 의 "후보가 둘 이상이면 매핑하지 않는다"는 **클레임과 워커 스폰**의 규칙입니다. 거기서 잘못 집으면 남의 작업을 가로채고 워커가 겹쳐 뜨는데, 그게 **조용히** 일어납니다. 그래서 안 집는 편이 낫습니다.

링크는 그 자리가 아닙니다. 잘못 열려도 **미리보기에 제목이 뜨는 순간 사람이 알아채고**, 되돌리는 비용은 닫기 한 번입니다. 위험의 크기가 다른데 같은 규칙을 갖다 댔습니다.

그리고 말씀대로 **축약 규칙은 이미 있습니다** — 우리가 계속 쓰던 "가장 최근 날짜의 N번째". 없는 규칙을 새로 만들자는 게 아니라 쓰고 있던 규칙을 화면이 모르고 있을 뿐입니다.

designer 에 넘긴 범위:
- `REQ-028` `DOC-12` 처럼 날짜 없는 번호를 카탈로그에서 풉니다 — 같은 종류·같은 번호 중 **날짜가 가장 최근인 것**
- 링크에 **풀린 전체 id 를 함께 보입니다**(귀띔과 미리보기 머리). 짐작이 틀렸을 때 그 자리에서 보여야 하고, 그 완화가 있어서 짐작이 허용됩니다
- 못 찾으면 맨 글자 — 없는 문서에 밑줄을 긋던 죽은 링크 문제와 같은 규칙입니다

**한 가지만 선을 그었습니다: 터미널에서만입니다. 문서 본문에는 안 겁니다.** 문서는 기록이고, 기록은 시간이 지나도 같은 걸 가리켜야 합니다. 본문에 `REQ-028` 을 "최근"으로 풀어 두면 내일 `REQ-20260829-028` 이 생기는 순간 **같은 글자가 다른 문서를 가리킵니다** — 어제 쓴 노트가 오늘 거짓말을 하게 됩니다. 대화 판은 지금 읽는 자리라 "지금 최근"이 맞지만 기록은 아닙니다. 이 구분은 주석과 테스트로 못박게 했습니다.

개인 설정도 그에 맞게 고쳤습니다 — 축약을 쓰지 말라가 아니라, 축약은 "가장 최근 날짜의 그 번호"를 뜻하고 화면도 그 규칙으로 링크한다로.

한편 대시보드 서버가 잠깐 내려갔다가 다시 올라와 있습니다. 007·021·022 작업분은 화면에 반영돼 있습니다.

### 2026-08-28T17:28:48+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-08-28 17:28:48 KST - lead]`

맞습니다. `pref_문서언급` 지웠습니다.

두 가지를 잘못했습니다. **자리를 잘못 잡았고** — 축약은 이 시스템이 실제로 쓰는 식별 방식이니 그걸 아는 건 화면의 몫이지 사람이 설정으로 들고 다닐 게 아닙니다. 개인 설정에 넣으면 계정마다 링크가 걸리고 안 걸리고가 갈립니다. 그럴 이유가 없습니다. 그리고 **책임을 사람 쪽으로 돌렸습니다** — "내 글쓰기 습관이 문제"라고 적고 설정으로 밀어 넣은 건 결함을 규율로 덮은 것입니다.

축약 해석은 코드의 계약으로 문서에 남겼습니다.

**"아예 안 쓰는 것도 방법"** — architect 에 검토를 걸었습니다(읽기 전용, 파일 안 건드립니다). 물은 것:

- 축약이 **id 의 별칭인지, 말버릇인지, 사실상 두 번째 식별자 체계인지**. `id_alias()` 가 이미 한 겹의 축약(접미사 없는 형태)을 공식화해 뒀는데 날짜 없는 축약이 그것과 같은 종류인지
- "안 쓴다"를 택하면 **규율로 강제되는지 장치로 강제되는지**. 이 저장소는 "표식만으로는 약하고 주입해야 실제 장치가 된다"를 여러 번 배웠습니다. 오늘 실제로 축약이 몇 번 쓰였는지도 세어 보라고 했습니다
- "최근 우선으로 푼다"가 틀리는 경우가 구체적으로 뭐고, 그 틀림이 **링크에서 끝나는지 아니면 어딘가 저장되거나 다른 판정에 쓰이는지**
- 제3의 안 — 응답 훅이 **글을 쓰는 쪽에서** 전체 id 로 펴 주거나, 축약을 정식 별칭으로 승격해 `s9 show REQ-028` 도 되게 하는 것(그러면 화면·CLI·문서가 한 규칙이 됩니다)

designer 는 지금 029 구현 중이라 그 세션이 비는 대로 같은 물음을 붙이겠습니다. 두 답이 모이면 한 안으로 정리해 구현하겠습니다.

### 2026-08-28T17:36:44+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-08-28 17:36:44 KST - lead]`

architect 답이 왔고, **제 결정을 실측으로 뒤집었습니다.**

**"최근 우선"은 지금 저장소의 축약 언급 327건 중 243건(74%)을 다른 문서로 풉니다.** 가설이 아니라 센 숫자입니다:

```
SES-20260823-006 본문 "REQ-078"   당시 뜻 20260823-078 → 최근우선 20260827-078
QST-20260827-005-62x6 본문 "REQ-019"   당시 뜻 20260827-019 → 최근우선 20260828-019
```

부패 속도가 **하루**입니다(일일 발번 29~91건). 제가 "대화 판은 지금 읽는 자리라 지금 최근이 맞다"고 한 전제는 스크롤백이 하루도 못 버틴다는 사실 앞에서 무너집니다.

그리고 제가 못 본 게 하나 더 있습니다 — **틀림이 링크에서 안 멈춥니다.** 미리보기의 `이어 말하기` 가 `/api/chat → s9 note` 로 이어져 **그 문서에 영구 기록**됩니다. `/api/note` 에 모호성 가드가 있지만, 화면이 이미 축약을 전체 id 로 접어 보내니 **애매함이 가드 앞에서 이미 사라진 채 도착합니다.** "잘못 열면 닫기 한 번"이라던 제 근거는 미리보기에만 참이었습니다.

**"아예 안 쓴다"도 실행 불가입니다.** vault 본문에 이미 327건이 굳어 있고, `normalize_ids()` 라는 장치가 쓰기 시점에 잡으려 했는데도 애매해서 놓아준 것들입니다. 게다가 실제 지배적 형태는 `REQ-028` 보다 더 짧은 **맨 숫자**입니다 — 오늘 이 세션만 123건("078 · 011 · 012 · 013"). 그건 어떤 정규식으로도 안전하게 못 잡습니다.

**권고안: 기준시각을 "렌더하는 지금"이 아니라 "그 줄이 쓰인 때"로 옮긴다.**

- `(종류, 날짜, 번호)` 중복이 카탈로그 561건에 **0건** — 임의 시점에 대해 후보 최신은 언제나 유일합니다
- **시간에 대해 고정됩니다.** 내일 새 문서가 생겨도 오늘 쓴 줄의 해석이 안 바뀝니다
- 그래서 제가 세운 "문서 본문에는 걸지 마라"는 금지가 **필요 없어지고**, 굳어 있던 327건이 되살아납니다
- 짐작으로 푼 id 는 **읽기 전용** — 미리보기·문서 열기는 되고 `이어 말하기` 는 안 뜹니다. 전체 id 를 확인하고 확정한 뒤에야 집습니다

검증 방법까지 같이 왔습니다: 축약 327건을 고정 픽스처로 박고 "당시 뜻" 대비 정확도를 재는 것 — 지금 제 방식은 **26%**, 작성시각 방식이 95% 아래면 전제가 틀린 겁니다.

designer 는 029 를 끝내는 대로 "짐작으로 푼 것을 화면에서 어떻게 보일지"를 맡기겠습니다. 구조는 architect 안대로 가겠습니다 — 이견 있으시면 말씀해 주세요.

**REQ-20260828-022-62x6 승인 확인했습니다.**

### 2026-08-28T17:45:27+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-08-28 17:45:26 KST - lead]`

캡처 봤습니다. 라벨이 가로 띠로 겹쳐 흰 안개가 됐고, 아래쪽도 글자끼리 파고들어 뭉개져 있습니다. **REQ-20260828-035-62x6** 로 접수하고 세 관점을 동시에 돌렸습니다.

각자에게 다른 물음을 줬습니다:

- **designer** — 문제를 갈라라: ① 라벨을 밀도와 무관하게 **언제나 다 그리는** 것인지 ② DAG 배치가 세로로 눌려 한 띠에 몰리는 것인지 ③ 둘 다인지. 어느 쪽이 지배적인지 **숫자로**. 그리고 판 크기와 글자 크기로 **읽을 수 있는 라벨 수의 상한**을 계산해, 그 상한을 넘으면 라벨을 버릴지 노드를 버릴지 배치를 바꿀지 정하라
- **architect** — 화면 손질 말고 **이 그래프가 무슨 물음에 답하는 도구인지**부터. 562개를 한 화면에 그리는 게 그 물음에 답하는지, 아니라면 무엇을 기준으로 줄일지(상태·기간·관계 깊이·지금 보는 문서 주변), 그 줄이는 축이 **모델에 이미 있는지**. 그리고 **하루 30~90건씩 느는 것을 전제로** 6개월 뒤에도 서는 안인지
- **frontend-developer** — 코드를 읽고 라벨 거르는 장치가 있는지, 있다면 왜 안 듣는지. DAG 가 몇 층으로 나오고 한 층에 몇 개가 몰리는지 **실제로 재서**. 외부 의존성 없이(이 저장소는 `web/index.html` 한 파일로 돕니다) 가능한 안과 각각의 비용, 그리고 팬·줌 중 매 프레임 겹침 검사를 돌려도 되는지 프레임 시간으로

셋 다 **파일은 안 건드립니다** — 지금 designer 하나가 029(타임존 고르기)를 구현 중이라 같은 파일입니다. 안이 모이면 한 세션에서 붙이겠습니다.

헤드리스 캡처가 물리를 몇 프레임만 돌려 자리 안 잡힌 그래프를 찍는 함정도 셋 다에게 못박았습니다 — 이 저장소가 그것 때문에 그래프 결함을 이미 한 번 놓쳤습니다.

### 2026-08-28T17:47:44+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-08-28 17:47:44 KST - lead]`

**REQ-20260828-029-62x6(타임존 고르기) 승인 확인했습니다.** 후속 지시는 없습니다.

같은 designer 세션이 이어서 REQ-20260828-021-62x6 을 하고 있어서, **방금 정정을 급히 보냈습니다.** 제가 어제 준 "가장 최근 날짜로 푼다"를 그대로 만들고 있었을 수 있는데, architect 실측이 그 안을 뒤집었기 때문입니다 — 그 규칙은 기존 축약 327건 중 **243건을 다른 문서로** 풉니다.

바뀐 규칙은 **기준시각을 "렌더하는 지금"이 아니라 "그 줄이 쓰인 때"로 잡는 것**입니다. `(종류, 날짜, 번호)` 중복이 562건 중 0건이라 임의 시점에 대해 후보 최신은 언제나 유일합니다 — 짐작이 아니라 결정적입니다. 그리고 시간이 지나도 해석이 안 바뀝니다.

함께 못박은 것 둘:
- **짐작으로 푼 id 는 읽기 전용** — 미리보기·문서 열기는 되고 `이어 말하기` 는 안 뜹니다. 그 경로가 `/api/chat → s9 note` 로 **그 문서에 영구 기록**까지 이어지는데, 화면이 축약을 이미 전체 id 로 접어 보내서 서버의 모호성 가드가 무력해집니다
- **해석기는 한 곳에.** 이 저장소에 같은 물음에 답하는 코드가 이미 여섯 자리 있습니다 — 일곱 번째를 화면에 따로 만드는 게 최악입니다

지금 도는 것은 넷입니다: designer 하나가 021 구현, 그리고 DAG 안개 건으로 designer·architect·frontend-developer 셋이 읽기 전용 검토 중입니다. 029 작업분은 아직 커밋 전인데, 같은 파일을 그 designer 가 계속 쥐고 있어 끝나면 함께 커밋하겠습니다.

### 2026-08-28T17:49:23+09:00 subagent (agent: sub:architect:adc8f490, by nicehugepark)

`[2026-08-28 17:52:41 KST - architect]`

### 결론

**이 그래프의 병은 라벨 겹침이 아니라 스코프가 없다는 것이다.** 오늘 화면에 그려지는 476개 노드 중 **살아 있는 요청은 11개**이고, 나머지 465개(98%)는 끝난 과거다. 그리고 이 탭이 답하라고 만들어진 물음 — "무엇이 앞의 요청 때문에 못 나가는가" — 의 데이터인 살아 있는 `blocked_by` 간선은 **오늘 0건**이다. 안개는 증상이고, 원인은 **"전부 그린다"가 기본값**인 것이다. 권고는 **그래프를 전역 지도에서 초점+반경 뷰로 바꾸는 것**이고, 줄이는 축은 이미 모델에 다 있다(status의 종결성 · `blocked_by`의 수명 · 초점으로부터의 홉 수).

---

### 1. 지금 무엇을 노드로, 무엇을 간선으로 그리는가

`bin/s9:10460 graph_data()` 는 `walk_docs()` 로 **vault 전체를 매 요청마다 파일로 읽어** 노드/간선을 만든다.

간선은 5종이고, 실측 개수는 이렇다(562행 카탈로그 + frontmatter 직접 집계):

| 간선 | 건수 | 성격 |
|---|---|---|
| `parent` | 124 | 계보(주) |
| `derived_from` | 34 | 계보(부차) |
| `relates` | **278** | 계보(동급·무방향) |
| `refs_docs` | 10 | 참조 |
| `blocked_by` | **0** | 상태(수명 있음) |

**여기서 이미 결론의 절반이 나온다.** 간선의 96%(436/446)가 `DOC-20260825-002-62x6`가 정의한 **출처(provenance) 축** — "이 문서가 어디서 왔는가" — 이고, `DOC-20260826-001-62x6`이 "사용자가 그래프에서 보고 싶어한 것"이라고 명시한 **상태 축은 0건**이다. 지금 화면의 잉크는 전부 과거에 쓰이고 있다.

레이아웃(`web/index.html:8445`)은 `parent` **하나만** 써서 depth를 계산한다. `relates` 278개는 깊이에 기여하지 않고 그려지기만 한다 — 캡처의 가로 띠를 관통하는 흰 선 다발이 그것이다.

### 2. 안개의 산수 (설계 결함이지 스타일 문제가 아니다)

기본 표시 종류(request/article/knowledge/question, session은 off) = **476 노드**. `parent` 기준 depth 분포:

```
layer 0: 352개  gap 3.1px   ← 여기가 안개
layer 1:  68개  gap 15.9px
layer 2:  31개  gap 34.8px
layer 3:  14개  gap 77.1px
layer 4:  11개  gap 98.2px
```

`web/index.html:8466` 의 `gap = Math.min(170, (W-120)/layer.length)` 는 **한 레이어를 반드시 한 줄에 우겨넣는다.** 352개면 3.1px. 라벨은 최대 16자 × 11px ≈ **110~170px**. 즉 라벨 하나가 이웃 **35~55개**를 덮는다. 게다가 `:8766` 의 `showLabel` 은 dag 모드에서 줌·깊이 게이트를 **무조건 무시**한다(`|| graphLayout === "dag"`) — force 모드에는 있는 컬링이 dag에는 없다. 이 둘이 만나면 결과는 안개일 수밖에 없다.

**핵심은 352라는 숫자다.** 요청 388건 중 부모가 있는 것은 124건뿐. dag 모드는 계층을 보여주는 뷰인데, **이 vault에는 볼 만한 계층이 거의 없다**(최대 깊이 4, 74%가 고아). dag는 없는 구조를 그리려다 실패하고 있다.

### 3. 562개를 다 그리는 것이 물음에 답하는가 — 아니다

사용자가 이 탭을 열 때 무엇을 알고 싶어했는지는 문서에 원문으로 남아 있다.

- **REQ-20260826-009-62x6**: *"요청들이 진행이 안되는 이유 중 몇개가 앞의 요청 때문에 진행이 안된다는게 그래프에서 확인이 되는지 보려고 했다."* → 물음 A: **지금 무엇이 무엇에 막혀 있나**
- **REQ-20260826-039-62x6 선행 대화**: *"038 요청의 연관성 요청으로 037이 등록되어있는데 무슨 연관성이 있는거지?"* → 물음 B: **이 문서 하나의 이웃이 누구고 왜 연결됐나**
- **REQ-20260823-008 / 078**: *"진짜 옵시디언 같은"*, *"입체적인 느낌"* → 물음 C: **미학** (물음이 아니라 취향 요구다. 뒤에서 따로 다룬다.)

A와 B는 둘 다 **국소적**이다. A의 답은 오늘 11개 노드 안에 있고, B의 답은 노드 1개 + 이웃 몇이다. **476개를 그려야 답이 나오는 물음은 셋 중 하나도 없다.** 그리고 A는 이미 `DOC-20260826-009-62x6`가 결론을 내려 뒀다: *"노드 300개에서 엣지 하나를 눈으로 찾는 것은 현실적이지 않다."* 그 문서가 만든 것이 오른쪽 `선행 대기 현황` 레일이다 — 캡처에서 그 레일은 "없음"이다.

**줄이는 축은 새로 만들 필요가 없다. 모델에 이미 셋 다 있다:**

1. **종결성(status)** — `DEP_DEAD`(done/cancelled)는 코드에 이미 있고, `DOC-20260826-001-62x6` 규칙 4가 *"선행이 끝나면 엣지는 사라진다 — 끝난 의존을 계속 그리면 그래프가 과거로 채워진다"* 를 이미 못박았다. **이 규칙이 간선에만 적용되고 노드에는 적용된 적이 없다.** 지금 화면이 정확히 그 문서가 금지한 상태 — "과거로 채워진 그래프" — 다. 새 결정이 필요한 게 아니라, 이미 내린 결정을 노드로 확장하면 된다.
2. **홉 수(초점 반경)** — 3층 계보가 그대로 거리 함수다. 물음 B는 반경 1~2홉이면 끝난다.
3. **기간/사용자/프로젝트** — 이미 헤더 필터에 있다.

### 4. 하루 30~90건 전제에서 6개월 뒤 무너지는 것

실측 증가: 8/23=91, 8/24=83, 8/25=114, 8/26=66, 8/27=140, 8/28=56. 보수적으로 **70/일 × 180일 ≈ +12,600 → 약 13,000 문서.**

| 지점 | 오늘 | 6개월 뒤 | 판정 |
|---|---|---|---|
| dag layer 0 | 352개 / gap 3.1px | ~9,500개 / **gap 0.11px** | 라벨을 고쳐도 점조차 구분 불가 |
| force 물리 `:8592` | 476² /2 = 113k 쌍/프레임 | 13,000² /2 = **85M 쌍/프레임** | 브라우저 정지 |
| `graph_data()` | 요청당 562 파일 read | 요청당 **13,000 파일 read** | 탭 전환마다 수 초 |
| 라벨 가독 상한 | — | 판 1200×700에서 겹치지 않는 라벨은 **약 80개** | 물리 상한 |

**"라벨만 컬링"하는 안은 6개월을 못 간다** — 점 자체가 이미 서브픽셀이다. **스코프를 자르는 안은 선다**: 살아 있는 요청 수는 문서 총량과 무관하게 **동시 진행 능력에 묶인 상수**(오늘 11)이기 때문이다. 이것이 두 안을 가르는 유일한 기준이다.

### 5. 이미 있는 것과의 중복

- **물음 A(무엇이 막혔나)** → `선행 대기 현황` 레일이 이미 답한다. 정렬 기준이 "몇 건을 붙잡고 있나"라 우선순위까지 준다. 그래프는 같은 답을 더 못하게 한다.
- **"지금 뭐가 진행 중인가"** → Board(칸반 + live 점). `QST-20260828-008-62x6` 에서 이미 *"그걸 볼 수 있는 자리는 그래프가 아니라 보드의 live 점"* 이라고 답변했다.
- **"이걸 참조하는 문서"** → 문서 뷰어의 `Linked mentions`(`:8788 renderBacklinks`). 다만 **역방향 1홉만** 보여준다.
- **"이 단어가 든 문서"** → 검색.

**그래프만 답할 수 있는 것은 하나다: 모양.** 사슬의 길이, 여러 갈래가 한 점으로 합류하는 지점, 그 합류점이 지금 살아 있는지. 목록은 이걸 못 그린다. 그런데 지금 화면은 그 모양을 그리기에 **노드가 43배 많고, 그릴 계층은 오히려 없다.**

### 6. 권고 — 하나

> **그래프의 기본 스코프를 "전체"에서 "살아 있는 것 + 1홉"으로 바꾼다. 전역 지도는 라벨 없는 옵션으로 내린다.**

`DOC-20260826-001-62x6` 규칙 4(수명)를 **간선에서 노드로 확장**하는 것이다 — 새 개념 0개, 새 필드 0개.

**단계별 마이그레이션** (되돌리기 쉬운 순서):

- **1단계 — 지혈 (오늘, 렌더만).** `:8766` 의 `|| graphLayout === "dag"` 를 없애 dag도 force와 같은 라벨 게이트를 쓰게 하고, `:8466` 의 gap을 하한(예 24px) + **줄바꿈**으로 바꾼다(352개면 9개씩 39줄). 안개는 사라지고 사용자는 즉시 화면을 되찾는다. 모델 변경 없음.
- **2단계 — 스코프 축 (구조).** 헤더에 범위 하나: `살아있음(기본) · 최근 7일 · 이 문서 주변 · 전체`. 서버는 `/api/graph?scope=live|recent|focus&focus=<id>&hops=2` 로 자른다 — 전부 이미 있는 필드(`status`, `parent`/`derived_from`/`relates`, `created`)로만 계산되고 새 저장물이 없다. **비기능 목표를 숫자로 고정**: 기본 스코프에서 라벨 있는 노드 ≤ 80, 그릴 노드 ≤ 300, 초기 렌더 ≤ 300ms. 문서 뷰어의 `Linked mentions` 옆에 "관계 보기" 하나를 두어 물음 B를 그래프로 넘긴다(양방향 ±2홉 — 지금 백링크는 역방향 1홉뿐이라 여기서 실제 이득이 생긴다).
- **3단계 — 확장성 (6개월용).** `graph_data()` 가 `walk_docs()` 대신 카탈로그를 읽게 하고, `relates`/`derived_from`/`refs_docs` 를 카탈로그 행에 싣는다(원본에서 재생성되는 파생물이므로 두 번째 진실이 아니다). `전체` 모드는 노드 상한 + 라벨 off로 **"모양만 보는 우주"** 로 정의한다 — 물음 C(옵시디언 감성)는 여기서 산다. 읽을 수 없는 라벨을 붙여 두는 것보다 아예 없는 편이 그 감성에도 낫다.

**부수 발견 하나(별도 REQ 감).** 요청 388건 중 부모가 있는 것이 124건뿐이고 `relates` 가 278건이다 — 리드가 계보를 걸어야 할 자리에 동급 연관을 걸고 있다. dag가 그릴 것이 없는 진짜 이유가 이것이다. 렌더를 아무리 고쳐도 이 습관이 안 바뀌면 계층 뷰는 영영 빈 뷰다. `QST-20260828-008-62x6` 에서 이미 제안된 "파일 충돌로 줄 세울 때 `blocked_by` 로 걸기"도 같은 뿌리다.

### 7. 내가 틀렸다는 것을 어떻게 알아채는가

세 가지를 보면 된다.

1. **1단계만으로 사용자가 만족하고 계속 dag를 쓴다면** — 그가 원한 것은 국소 답이 아니라 **지도 자체**(물음 C)였다는 뜻이고, 내 진단("스코프가 문제")은 틀렸다. 그때 정답은 스코프가 아니라 LOD/클러스터링이다. **1단계를 먼저 배치하는 이유가 이 반증을 싸게 얻기 위해서다** — 2단계를 지금 짓지 않는다.
2. **스코프를 좁힌 뒤에도 그래프 탭 재방문이 늘지 않고 사용자가 계속 Board만 본다면** — 그래프에는 애초에 물음이 없었다는 뜻이다. 그때는 탭을 없애고 문서 뷰어의 관계 패널로 흡수하는 것이 정직하다.
3. **살아 있는 `blocked_by` 간선이 앞으로 2주간 계속 0이라면** — 물음 A는 실재하지 않는(혹은 기록되지 않는) 물음이다. 그러면 그래프의 존재 근거는 물음 B 하나로 줄고, "탭"이 아니라 "문서 옆 패널"이 맞는 형태다. 반대로 이 수가 꾸준히 >0이 되면 내 권고가 확증된다.

계측은 새로 만들 것이 없다 — 1·3은 카탈로그에서 바로 세어지고, 2는 세션 로그에 이미 남는다.

---

**관련 파일**
- `/home/sjpark1/section9/web/index.html` — `:8445` dag 배치, `:8466` gap 계산, `:8766` showLabel, `:8592` O(N²) 물리, `:2947` depBoard, `:8788` renderBacklinks
- `/home/sjpark1/section9/bin/s9` — `:10460` graph_data(walk_docs 전체 스캔), `:4354` dep_edges
- `/home/sjpark1/section9/vault/knowledge/2026/08/DOC-20260826-001-62x6.md` — 규칙 4(수명). 이번 권고의 근거
- `/home/sjpark1/section9/vault/knowledge/2026/08/DOC-20260826-009-62x6.md` — "노드 300개에서 엣지 하나를 눈으로 찾는 것은 현실적이지 않다"
- `/home/sjpark1/section9/vault/knowledge/2026/08/DOC-20260825-002-62x6.md` — 계보 3층

**하지 않은 것**: 파일 편집·커밋·`s9 note` 전부 없음. 읽기와 집계만 했다. 1·2단계 산수는 W=1200 기준 추정이고 실제 창 폭에 따라 달라진다 — 브라우저 실측은 하지 않았다.

### 2026-08-28T17:50:31+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-08-28 17:50:31 KST - lead]`

architect 답이 먼저 왔고, 진단이 제 예상과 다릅니다.

**"병은 라벨 겹침이 아니라 스코프가 없다는 것"** — 지금 그려지는 476개 노드 중 **살아 있는 요청은 11개**입니다. 나머지 465개(98%)는 끝난 과거입니다.

안개의 산수는 이렇습니다:
- `parent` 기준 **layer 0 에 352개**가 몰려 있고 간격이 **3.1px** 입니다. 라벨은 110~170px 이니 **라벨 하나가 이웃 35~55개를 덮습니다**
- `web/index.html:8466` 이 한 레이어를 **반드시 한 줄에** 우겨넣습니다
- `web/index.html:8766` 의 라벨 게이트가 **dag 모드에서만 무조건 우회**됩니다 — force 에는 있는 컬링이 dag 에는 없습니다

그리고 더 아픈 것 둘:

**이 탭이 답하라고 만든 물음의 데이터가 0건입니다.** 살아 있는 `blocked_by` 간선이 오늘 0개입니다. 간선 446개 중 96%가 계보(relates 278 · parent 124)이고 상태 축은 비어 있습니다. 화면의 잉크가 전부 과거에 쓰이고 있습니다.

**6개월 뒤(약 13,000 문서) 라벨만 고치는 안은 못 버팁니다** — layer 0 간격이 **0.11px**, force 물리가 프레임당 **85M 쌍**이 됩니다. 반면 스코프를 자르는 안은 섭니다. 살아 있는 요청 수는 문서 총량과 무관하게 동시 진행 능력에 묶인 상수라서요.

권고는 **기본 스코프를 "전체"에서 "살아 있는 것 + 1홉"으로** 바꾸고 전역 지도는 라벨 없는 옵션으로 내리는 것입니다. 이미 있는 규칙을 확장하는 것뿐입니다 — `DOC-20260826-001-62x6` 이 "선행이 끝나면 엣지는 사라진다, 끝난 의존을 계속 그리면 그래프가 과거로 채워진다"고 정해 뒀는데 그게 **간선에만 적용되고 노드에는 적용된 적이 없습니다.** 지금 화면이 정확히 그 문서가 금지한 상태입니다.

**그리고 제 습관 문제가 하나 드러났습니다.** 요청 388건 중 부모가 있는 건 124건뿐인데 `relates` 는 278건입니다 — 계보를 걸어야 할 자리에 동급 연관을 걸어 왔습니다. **dag 가 그릴 계층이 없는 진짜 이유가 이것입니다.** 렌더를 아무리 고쳐도 이 습관이 안 바뀌면 계층 뷰는 영영 빈 뷰입니다.

designer·frontend-developer 답이 오면 셋을 견줘 단계를 정하겠습니다. architect 는 **지혈(라벨 게이트 + 줄바꿈)을 먼저** 하자고 했는데, 그게 싸고 되돌리기 쉬울 뿐 아니라 "사용자가 원한 게 국소 답인가 지도 자체인가"를 가르는 반증 실험이기도 하다는 이유입니다.

### 2026-08-28T17:54:29+09:00 subagent (agent: sub:frontend-developer:a0219ca5, by nicehugepark)

`[2026-08-28 17:58:20 KST - frontend-developer]`

### 결론

**라벨을 거르는 장치는 있는데 DAG 모드에서 통째로 꺼져 있다** — `web/index.html:8858-8859` 의 `|| graphLayout === "dag"` 한 조각이 줌·깊이 조건을 무효화해서 476개 라벨을 매 프레임 전부 그린다. 그 위에 배치 결함이 겹친다: 476개 중 **352개가 depth 0 한 줄**에 **2.81px 간격**으로 놓인다(`8558`). 성능은 원인이 아니다 — 겹침 검사를 히스테리시스까지 붙여 매 프레임 돌려도 **0.26ms**(프레임 예산의 1.5%)다. 고칠 것은 두 줄짜리 게이트와 한 줄짜리 배치식이다.

파일은 건드리지 않았다. 아래 줄 번호는 지금(11,103줄) 기준인데, 작업 중 다른 세션이 파일을 91줄 밀었으므로 **앵커 문자열로 다시 잡아라**.

---

### 1. 지금 코드가 하는 일

- **캔버스 2D 한 장.** `#gcanvas`, `getContext("2d")`. SVG/DOM 노드 없음. `loop()`(`9108-9114`)이 매 프레임 `step()` → `gFitStep()` → `draw()` → `gAwaySync()` 를 돌고 `requestAnimationFrame` 으로 무조건 재예약한다. **정지 상태에서도 계속 전체를 다시 그린다.**
- **force 모드**: `step()`(`8682`)이 O(n²) 반발력 + 엣지 스프링. `alpha` 가 0.006 아래로 식으면 멎는다.
- **DAG 모드**(`8537-8563`): 물리를 끄고(`alpha: 0.005`, `8603`) 계층 배치를 한 번만 계산한다.
  - depth 는 **`rel === "parent"` 체인만** 본다(`8539` `parentOf`). derived_from·relates·blocked_by 는 깊이에 기여하지 않는다.
  - 좌표: `n.x = W/2 + (i - (layer.length-1)/2) * gap`, `n.y = 70 + d*105` (`8560-8561`)
  - **`const gap = Math.min(170, (W - 120) / Math.max(layer.length, 1));`** (`8558`) — 간격에 **상한만 있고 하한이 없다**. 한 계층이 커질수록 간격이 0으로 수렴한다. 이 한 줄이 가로 띠의 원인이다.
- **라벨**(`8858-8865`): `11px "Noto Sans KR"`, 제목을 16자로 자르고 `fillText(t, px, py + rr + 13)`.
- 줌 범위는 `0.25 ~ 4`(`gClampK`, `8952`).

### 2. 라벨 필터 — 있는데 DAG에서 꺼진다

```js
const showLabel = !faded && ((st.tf.k > 0.75 && n.near > 0.45) ||
  (hov && (n.id === hov.id || nb.has(n.id))) || graphLayout === "dag");
```

세 가지가 동시에 틀렸다.

1. **`|| graphLayout === "dag"` 가 앞의 모든 조건을 무력화한다.** DAG 에서는 확대 수준(`st.tf.k`)도, 깊이(`n.near`)도 보지 않는다. `k=0.25`(최대 축소)에서도 476개를 전부 그린다. **실측: `ctx.fillText` 를 가로채 세니 DAG 프레임당 477회** — 노드 수와 정확히 같다.
2. **force 쪽 게이트도 겹침을 재지 않는다.** `k>0.75 && near>0.45` 는 깊이 대리 지표일 뿐 위치를 안 본다. 실측: force 모드 k=1 에서 **프레임당 81개** — 화면에 겹치지 않고 들어가는 최대치(29~34개)의 2.4배다. force 도 정도만 덜할 뿐 같은 안개다.
3. **겹침 판정 자체가 코드 어디에도 없다.** 라벨 사각형을 재는 곳도, 공간 인덱스도 없다.

### 3. 배치 실측 — 462개가 상위 두 층에

CDP 로 붙어 `?gsettle=2500` 으로 재운 뒤(§6) `__gdbg.snap()` 의 화면 좌표를 깊이 투영(`pfOf = 0.72 + 0.52*near`)의 역으로 풀어 월드 좌표로 되돌려 쟀다. 판 **1110×612**, 노드 **476개**(카탈로그 562건 중 `session` 종류가 기본 꺼짐).

| depth | 노드 | x 범위 폭 | 이웃 간격(중앙값) |
|---|---|---|---|
| 0 | **352** | 987px | **2.81px** |
| 1 | 68 | 975px | 14.56px |
| 2 | 31 | 958px | 31.94px |
| 3 | 14 | 919px | 70.71px |
| 4 | 11 | 900px | 90.00px |

**계층은 5개뿐이고 74%가 맨 윗줄에 있다.** 노드 반지름은 최소 5px(`r = 5 + min(9, 2.2√deg)`, 병목이면 +7)이므로 지름 10~42px 짜리 원 352개가 2.81px 간격으로 놓인다 — 캡처의 흰 띠는 **라벨이 아니라 노드 자체가 3~15겹 포개진 것**이다. 그 위에 173px 짜리 라벨이 얹힌다.

**겹침 실측(k=1)**: 라벨 사각형 476개 중 **472개(99.2%)가 최소 한 개 이상과 겹친다. 겹치는 쌍 9,955개.**

**그리고 이건 배치만으로는 못 고친다.** 라벨 한 개의 실측 폭은 `measureText` 로 **173px**(16자 + …), 높이 13px. 476개 = **1,070,524 px²**. 판 넓이 1110×612 = **679,320 px²**. **글자 면적이 판보다 1.58배 크다.** 어떤 배치를 쓰든 476개를 동시에 겹침 없이 놓을 자리가 물리적으로 없다. **라벨을 거르는 일은 선택이 아니라 필수다.**

### 4. 후보와 비용 (전부 순수 구현, 새 의존성 0)

| 후보 | 비용/프레임 | 얻는 것 | 한계 |
|---|---|---|---|
| **A. 게이트 삭제**(`|| graphLayout === "dag"` 제거) | 0 | 즉시 안개 제거 | force 게이트를 그대로 물려받는다 — 여전히 81개(정원 34개)라 덜한 안개 |
| **B. 겹침 그리디 + 점유 격자** | **0.07~0.12ms** | 겹침 0 보장, 확대할수록 자동으로 더 보인다(LOD 공짜) | 팬 중 라벨이 끓는다(아래) |
| **B′. B + 히스테리시스** | **0.26ms** (snap 재구성 포함, 실제 코드는 더 쌈) | 끓음 제거 | 상태 1개(직전 프레임 집합) |
| C. 중요도 상위 N개 | ~0.05ms | 단순 | 겹침을 안 재니 몰린 곳은 그대로 겹침 |
| D. quadtree | 구축 O(n log n) | 그리디보다 정확 | **불필요** — 격자 해싱으로 이미 0.1ms. 의존성 아닌 복잡도 부채 |
| E. 계층 줄바꿈(`gap` 하한) | 배치 1회, 0 | 노드 포개짐 해소 + 라벨 정원 2.5배 | 부모 barycenter 정렬이 약해져 교차선이 는다 |
| F. 배치 알고리즘 교체(Sugiyama 등) | 크다 | — | **기각.** depth 0 이 74%인 데이터에 계층 알고리즘을 더 정교하게 해봐야 줄 하나는 줄 하나다 |

**B 의 실측 수확량**(그리디, 중요도=화면 반지름 순):

| k | 화면 안 노드 | 겹침 없이 남는 라벨 | 비용 |
|---|---|---|---|
| 0.3 | 468 | 13 | 0.122ms |
| 0.6 | 474 | 24 | 0.122ms |
| 1.0 | 476 | **34** | 0.102ms |
| 2.0 | 247 | 21 | 0.084ms |
| 4.0 | 95 | **6** | 0.074ms |

k=4(최대 확대)에서도 6개뿐인 것이 배치 결함의 증거다 — 한 줄에 몰려 있으니 1110/173 ≈ 6줄분이 천장이다. **확대해도 안 늘어난다.**

**E 를 얹었을 때**(간격 하한 26px, 줄 높이 30px 로 계층을 줄바꿈 — 오프라인 시뮬레이션):

| 배치 | 월드 높이 | k=1 라벨 | k=2 | k=4 |
|---|---|---|---|---|
| 지금 | 490px | 29 | 20 | 6 |
| 줄바꿈 | 750px | **74** | **48** | **15** |

전체 보기 배율(k≈0.67)에서도 52개. **정원이 2.5배**가 되고, 확대가 비로소 보상을 준다.

### 5. 성능 — 라벨은 병목이 아니다

헤드리스 Chrome 151, 1400×900, DAG, 476노드:

- **실 프레임 59.5fps** (3.01초에 179장) — vsync 에 붙어 있다.
- **프레임당 메인스레드 5.81ms** (스크립트 4.02ms) — Performance 도메인 TaskDuration 실측. 16.7ms 예산의 35%.
- `fillText` 476회 = **0.635ms** (전체의 11%). 나머지는 엣지 획과 노드 원/링/게이지.
- 겹침 그리디 = **0.07~0.12ms** (2%). 히스테리시스 포함 0.26ms.

**답: 매 프레임 돌려도 된다.** 캐시를 두면 "언제 무효화하나"라는 결함 표면(팬·줌·hover·물리·15초 목록 갱신)이 새로 생기는데, 0.1ms 를 아끼자고 살 부채가 아니다.

**다만 매 프레임 그리디는 끓는다** — 이건 재서 확인했다. 180px/s 로 팬하며 60프레임 동안 채택 집합의 변화를 세니:

| 방식 | 평균 표시 | **프레임당 교체** | 최대 교체 |
|---|---|---|---|
| 그리디만 | 33.3개 | **3.19개** | 11개 |
| **+ 히스테리시스** | 32.1개 | **0.81개** | 3개 |

프레임당 3개(전체의 10%, 최악 3분의 1)가 깜빡이면 읽히지 않고 고장으로 보인다 — 이 저장소가 `gAwaySync` 손잡이에서 이미 배운 그 함정이다(`400ms` 뜸 들이기 주석). 해법은 뜸이 아니라 **관성**: 직전 프레임에 보이던 라벨을 우선순위 맨 앞에 두고 자리를 먼저 잡게 한다. 표시 개수 손실은 1.2개(3.6%)뿐이다.

**성장 전망**: 라벨 그리디는 O(n × 라벨당 격자칸 ≈ 12)로 선형이라 1000노드에서도 ~0.25ms다. 진짜 벽은 다른 데 있다 — **`step()` 이 O(n²)** (`8682`, 476² /2 = 113k쌍/프레임). DAG 는 물리가 꺼져 있어 무관하지만 force 는 1000노드에서 500k쌍이 된다. 이번 건의 범위 밖이지만 같은 화면의 다음 결함이다.

### 6. 헤드리스 함정 — 어떻게 피했는지

`s9 shot` 은 스크린샷만 주므로 숫자를 못 얻는다. **Windows Chrome 을 `--remote-debugging-port` 로 띄우고 CDP 에 붙었다** — WebSocket 프레임을 직접 짠 60줄짜리 클라이언트(외부 의존성 0). `?gsettle=2500` 으로 재운 뒤 `__gdbg.gen` 으로 **산 판인지 확인하고** 쟀다(REQ-083 이 죽은 판을 재고 통과시킨 그 함정). 잰 시점 `frames=166, alpha=0.005` — 물리가 멎은 뒤다. `ctx.fillText`/`ctx.arc` 를 가로채 프레임당 호출 수를 직접 셌다(추론이 아니라 계수).

재현 캡처(현재 판, 자리 잡힌 상태): `/tmp/claude-1000/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/scratchpad/dag-fog.png` — 사용자 캡처와 동일한 안개가 그대로 나온다. 측정 스크립트는 같은 디렉터리의 `cdp.py`·`lib.py`·`m3~m10.py`·`sim.py`. 띄운 브라우저는 `Browser.close` 로 회수했다(개체수 상한 위생).

### 7. 권고

**겹침을 재서 거르되(B′), 배치의 간격 하한을 함께 넣어라(E).** 둘 중 하나만으로는 안 된다 — B′ 만 하면 확대해도 6개에서 안 늘고, E 만 하면 74개가 여전히 겹친다.

구현 순서(각 단계가 독립적으로 화면에 보인다):

1. **`|| graphLayout === "dag"` 를 지운다** (`8859`). 한 조각. 즉시 안개가 걷힌다. 이 상태를 캡처해 두면 이후 단계의 대조군이 된다.
2. **`gap` 에 하한을 넣는다** (`8558`): `Math.max(26, Math.min(170, ...))`. 넘치면 계층을 줄바꿈하고 `n.y` 는 누적 오프셋으로 바꾼다(`70 + d*105` 고정을 버린다). 노드 포개짐이 사라지고 전체 보기(`gFit`)가 이미 있으므로 높이가 늘어도 화면에 맞는다.
3. **`showLabel` 을 겹침 그리디로 교체한다.** `draw()` 앞에서 한 번: 중요도(waiting → deg → status 활성 → 최신) 정렬 → 화면 밖 컬링 → 24×14 격자 점유 검사 → 채택 집합. `drawNode` 는 그 집합만 본다. 라벨 폭은 `measureText` 캐시(제목당 1회, 폰트 크기 고정이라 줌과 무관).
4. **히스테리시스**: `st.labelKeep` 에 직전 집합을 두고 정렬 키 맨 앞에 `keep.has(id) ? 0 : 1`. 5줄.
5. hover 라벨은 지금처럼 무조건 통과시키되 **채택 집합보다 나중에, 맨 위에** 그린다(현재도 hov 를 마지막에 그린다 — 순서 유지).

**나아졌다고 말할 수 있는 지표** — 전부 지금 재는 방법이 있다:

- **겹치는 라벨 쌍**: 9,955 → **0** (사각형 충돌 계수)
- **가려진 라벨 비율**: 99.2% → **0%**
- **k=1 표시 라벨 수**: 476개(전부 안개) → 30~74개(전부 읽힘)
- **확대 보상**: k=1 → k=4 로 갈 때 표시 수가 **늘어야 한다**. 지금은 476 → 476(변화 없음), 그리디만 하면 34 → 6(줄어듦, 배치 결함), 배치까지 고치면 74 → 15에서 화면 안 밀도 기준으로 증가.
- **깜빡임**: 팬 180px/s 에서 프레임당 라벨 교체 3.19 → **1.0 이하**
- **프레임당 메인스레드**: 5.81ms → **6.1ms 이하** (겹침 검사 추가분 0.3ms 이내)
- **자리 잡힘 확인**: 모든 측정은 `?gsettle=2500` + `__gdbg.gen` 확인 후

기존 진단 손잡이 계보를 따라 **`?glab`** 하나를 더하길 권한다 — 판 위에 `표시 N/476 · 겹침쌍 M · 교체/프레임 C` 를 적으면 캡처 한 장이 곧 증거가 된다(`?gshape` 와 동형).

한계: 측정은 헤드리스 Chrome 151 · 1400×900 · 476노드 한 판이다. 실제 사용자 창 크기·DPR·GPU 합성 환경에서 프레임 시간은 다를 수 있다(방향은 헤드리스가 더 느린 쪽). 라벨 폭 173px 은 16자 한글 대표값이며 실제 제목 길이 분포는 재지 않았다 — 그리디 구현 시 `measureText` 로 제목별 실폭을 써야 한다.

### 2026-08-28T17:55:40+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-08-28 17:55:40 KST - lead]`

frontend-developer 가 CDP 로 브라우저에 직접 붙어 숫자를 재 왔습니다. designer 만 남았습니다.

**결정적인 숫자 하나: 라벨 글자 면적이 판보다 1.58배 큽니다.** 라벨 실측 폭 173px × 476개 = 1,070,524 px², 판은 679,320 px². **어떤 배치를 쓰든 476개를 겹침 없이 놓을 자리가 물리적으로 없습니다.** 라벨을 거르는 건 선택이 아니라 필수입니다.

무엇이 잘못돼 있는지:
- `web/index.html:8858` 의 `|| graphLayout === "dag"` 한 조각이 줌·깊이 게이트를 무효화합니다. **프레임당 `fillText` 477회** — 노드 수와 정확히 같습니다. 최대 축소에서도 전부 그립니다
- **force 모드도 겹침을 안 잽니다** — k=1 에서 81개를 그리는데 겹치지 않고 들어가는 최대치는 34개입니다. 정도만 덜할 뿐 같은 안개입니다
- 겹침 판정 코드가 **어디에도 없습니다**
- 흰 띠의 정체: depth 0 에 352개가 **간격 2.81px** 로 놓이는데 노드 지름이 10~42px 입니다. **라벨이 아니라 노드 자체가 3~15겹 포개진 것**이고 그 위에 라벨이 얹힙니다
- 라벨 사각형 476개 중 **472개(99.2%)가 겹칩니다. 겹치는 쌍 9,955개**

**성능은 원인이 아닙니다.** 겹침 검사를 매 프레임 돌려도 0.26ms 로 프레임 예산의 1.5% 입니다(현재 프레임당 5.81ms / 예산 16.7ms). 캐시를 두면 "언제 무효화하나"라는 결함 표면이 새로 생기는데 0.1ms 를 아끼자고 살 부채가 아니라는 판단입니다.

다만 매 프레임 그리디는 **끓습니다** — 팬 중 프레임당 라벨 3.19개가 교체되는 걸 재서 확인했습니다. 10%가 깜빡이면 읽히지 않고 고장으로 보입니다. 해법은 뜸 들이기가 아니라 **관성**(직전 프레임 라벨을 우선순위 맨 앞에) — 교체가 0.81개로 떨어지고 표시 손실은 3.6%뿐입니다.

**둘 다 해야 한다**는 게 결론입니다. 겹침 필터만 하면 최대 확대에서도 6개뿐이라 **확대해도 안 늘고**(한 줄에 몰려 있어 천장이 6줄분), 배치만 고치면 74개가 여전히 겹칩니다. 간격 하한 26px + 줄바꿈을 얹으면 k=1 에서 29 → **74개**, 정원이 2.5배가 되고 확대가 비로소 보상을 줍니다.

architect 와 결이 맞습니다 — architect 는 "스코프를 줄여라", frontend 는 "라벨을 걸러라"인데, 둘 다 **1단계로 `|| graphLayout === "dag"` 삭제**를 먼저 두자고 합니다. 한 조각이고 즉시 안개가 걷히며 이후 단계의 대조군이 됩니다.

designer 답이 오면 세 안을 한 순서로 묶어 붙이겠습니다.

### 2026-08-28T17:57:06+09:00 subagent (agent: sub:designer:a4488cd8, by nicehugepark)

`[2026-08-28 17:56:12 KST - designer]`

### 결론

**글자를 줄이는 문제가 아니라 개수 문제다. 이 판에 겹치지 않게 들어가는 이름은 실측 45~57개인데 DAG 는 476개를 전부 그린다 — 10배 초과다.** 그리고 DAG 배치는 라벨을 다 꺼도 깨져 있다: depth 0 한 층에 352건이 **2.84px 간격**으로 놓여 점 자체가 3.5겹이다. 둘 다 고쳐야 하고, **라벨 규칙(안 A)이 먼저**다 — 배치만 고쳐도 안개는 남는다.

파일은 하나도 건드리지 않았다. `web/index.html` 의 `M` 은 내가 들어오기 전부터 있던 다른 세션 것이다.

---

### 1. 재현 — 사용자 캡처 그대로 나왔다

`bin/s9 shot` 은 매번 새 프로필로 뜨므로 `localStorage.s9glayout` 이 비어 항상 FORCE 로 찍힌다. 9909 앞에 주입 프록시(9910)를 세워 레이아웃/타입/스킨을 쿼리로 심고 실제 대시보드를 그대로 태웠다. DAG 는 물리가 꺼진 고정 배치(`alpha 0.005`)라 `?gsettle` 이 필요 없다 — 첫 프레임이 곧 사용자가 보는 그림이다. FORCE 쪽만 `?gsettle=400` 으로 수렴시켰다.

| 캡처 | 내용 |
|---|---|
| `/tmp/claude-1000/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/scratchpad/a1-dag-now.png` | **DAG 지금** — 사용자 캡처 그대로 재현 |
| `…/scratchpad/a2-force-now.png` | FORCE 기본 배율(k=1) |
| `…/scratchpad/a3-force-fit.png` | FORCE + 전체 보기(k=0.362) — **라벨 0개** |
| `…/scratchpad/b-options.png` | **지금 / 안 A / 안 B / 안 C 나란히** |
| `…/scratchpad/c-saturate.png` | 포화 실측(예산 무제한) |

---

### 2. 무엇이 문제인가 — ③ 둘 다. 숫자로 가른다

판 실측 **1120×520**, 표시 문서 476건(session 꺼짐 기본), 라벨은 16자 절단 후 평균 폭 **116px**(11px Noto Sans KR).

**② DAG 배치 — 층이 아니라 막대다**

`gap = min(170, (W-120)/층 인원)` 이라 인원이 많을수록 간격이 0 으로 수렴한다.

| 층 | 인원 | 간격 | 라벨 겹침 | 줌 상한 4배에서도 |
|---|---|---|---|---|
| depth 0 | **352** | **2.84px** | **41겹** | 11.4px → 10겹 (영영 못 읽음) |
| depth 1 | 68 | 14.7px | 7.9겹 | 58.8px → 2겹 |
| depth 2 | 31 | 32.3px | 3.6겹 | 129px → 겨우 읽힘 |
| depth 3 | 14 | 71.4px | 1.6겹 | 읽힘 |
| depth 4 | 11 | 90.9px | 1.3겹 | 읽힘 |

**모든 층이 겹친다.** 줌 상한(4배)까지 올려도 읽히는 것은 depth 3·4 의 **25건뿐**이다. 그리고 라벨을 전부 꺼도 depth 0 은 지름 10px 이상인 점이 2.84px 간격이라 **3.5겹** — 캡처의 굵은 띠는 글자를 지워도 남는다.

원인은 데이터 모양이다. parent 간선이 하나라도 붙은 문서는 **166건뿐**이고 나머지 310건은 계보가 없다. 즉 depth 0 은 "루트 층"이 아니라 **고아 더미**이고, DAG 는 그 더미를 한 줄에 눌러 넣고 있다.

**① 라벨 규칙 — 밀도를 보지 않는다**

`showLabel = … || graphLayout === "dag"` (web/index.html 8767행). DAG 면 배율·밀도와 무관하게 **476개 전부**. 이것이 안개의 흰색이다 — 캡처의 띠가 파란 점 색이 아니라 흰색인 이유가 이것이다.

**어느 쪽이 지배적인가.** ①이다. 배치를 접어 간격을 18px 로 벌려도 116px 라벨은 여전히 6겹이다(안 B 실측: 예산 없이 넣으면 57개에서 포화). ②는 최악의 띠(41겹)를 만들고 점까지 뭉개므로 독립 결함이지만, ①을 두고 ②만 고치면 안개가 옅어질 뿐 사라지지 않는다.

---

### 3. 562개에서 몇 개가 읽히나 — 실측 상한 45~57

판 크기와 글자 크기로 계산한 이상적 상한은 9열(1120/124) × 37행(520/14) = 333칸이지만, 라벨은 빈 칸이 아니라 **노드에 붙는다**. 실제 노드 좌표 위에서 겹침 없이 채워 넣어 포화시킨 실측값(`c-saturate.png`):

- 지금 배치: **45개**
- 접은 배치: **57개** (자리가 고르게 퍼져 더 들어간다)

판 높이가 실제 520px 이면 대략 **70~80개**. 요구된 476개는 상한의 **7~10배**다.

**타이포로는 못 푼다.** 11px → 9px 로 줄이면 면적 이득 1.5배(≈110개) — 여전히 4배 부족한데다 한글 9px 은 본문 대비 4.5:1 기준을 지켜도 읽히지 않는다. **버릴 것은 라벨의 개수**다. 노드는 지도이고 라벨은 범례다 — 노드를 버리면 그래프의 존재 이유가 사라지고, 배치만 바꿔서는 상한을 두 배 이상 못 올린다.

---

### 4. 안 세 가지 (`b-options.png` 를 눈으로 고르면 된다)

### 안 A — 이름은 늘 마흔 개만, 급한 것부터 · **권고**
배치는 그대로. 그릴 때 우선순위 높은 순으로 **줄마다 이미 찬 구간을 확인하고**, 겹치면 그 이름은 버린다. 예산 40개.

- 순서: **병목(미완료 파생 수) → 진행 중·막힘·검토 → 허브(연결 수) → 미완료**. "지금 손이 가야 할 것"이 이름을 갖는다. 지금은 해시·허브·상태로 만든 깊이값(`n.near`)이 정하는데, 그건 임의값이다.
- **줌이 저절로 LOD 가 된다.** 확대하면 화면에 남는 노드가 줄고 같은 예산 안에서 자연히 더 많은 이름이 뜬다. `k > 0.75` 라는 절벽을 없앤다.
- 얹거나 고른 노드는 예산과 무관하게 이웃까지 이름을 준다(지금 동작 유지).
- 색은 면이 아니라 글자로: 병목=앰버 글자, 진행 중=잉크 600, 나머지=흐린 잉크. 색면·세로 띠 없음.
- 비용: 코드 한 함수. 배치 손 안 댐. 되돌리기 쉬움.
- 한계: depth 0 의 점 막대는 그대로 남는다.

### 안 B — 층을 접는다 (안 A 위에 얹는다)
한 층이 판보다 넓으면 다음 줄로 접고, **점 간격을 18px 아래로 내리지 않는다.** depth 0 은 6줄(높이 156px)이 되고 점 겹침이 사라진다. 라벨 상한도 45 → 57 로 오른다.

- 비용: `layoutFlat` 을 접기 배치로 교체. 판이 세로로 길어져 첫 화면이 전체 보기로 시작해야 한다.
- 변형: 계보 없는 310건을 판 아래 "계보 없음" 밭으로 접어 두면 계층이 훨씬 또렷해진다. 다만 "다 보인다"는 약속을 깨므로 사용자 판정 사안이다.

### 안 C — 그림에는 형태만, 이름은 장부로
판에 글자를 두지 않고 오른쪽 레일이 급한 순으로 이름을 세운다. 노드에 얹으면 레일 줄이 밝아지고, 레일 줄에 얹으면 노드가 밝아진다.

- 이 판의 언어(장부·계기판)와 가장 잘 맞고, 지금 비어 있는 선행/대기 레일 자리를 그대로 쓴다.
- 비용: 시선 이동이 두 배. 그래프만 보고는 무엇이 무엇인지 모른다.
- **단독으로는 부족하다** — 안 A 와 겹쳐 쓸 때 가장 좋다(판에 40개, 레일에 급한 12줄).

**추천: 안 A 를 먼저 넣고, 같은 판에서 안 B 를 얹는다.** A 는 두 배치(FORCE·DAG) 공통 규칙이라 한 번 고치면 두 화면이 같이 낫는다. C 는 다음 판.

---

### 5. FORCE 는 지금 괜찮은가 — 아니다. 조용히 실패한다

라벨 조건이 `k > 0.75 && near > 0.45` 다. `near > 0.45` 를 만족하는 노드가 **81개(17%)** 뿐이라 기본 배율에서는 화면에 든 30~40개만 떠서 읽힌다(`a2-force-now.png`). 안개는 안 난다.

문제는 반대편이다.

- **전체 보기를 누르면 배율이 0.362 로 떨어지고 이름이 한 개도 남지 않는다**(`a3-force-fit.png` — 427개 점, 라벨 0). "다 보고 싶어서" 누른 결과가 "무엇이 무엇인지 하나도 모름"이다. DAG 의 안개와 정확히 대칭인 실패다.
- 어떤 이름이 남는지가 **급한 순이 아니다.** `near` 는 문서 ID 해시 50% + 허브 근접 42% + 상태 보정으로 만든 깊이값이다. 병목 노드가 우연히 원경이면 이름이 없다.
- 배율 0.75 를 갓 넘긴 구간에서는 81개가 한 화면에 다 뜨는데 판 상한이 ~70개라 서로 파고든다(`a2` 에서 "크로스머신 resume 검증", "문서 이벤트 동기화" 부근 확인).

**안 A 의 예산 규칙 하나로 세 가지가 같이 풀린다** — 절벽이 사라지고, 무엇이 남을지가 급한 순으로 정해지고, 상한이 지켜진다. FORCE 만 따로 고칠 것은 없다.

---

### 검증 수단과 미검증

- 실브라우저 헤드리스 캡처(Windows Chrome, `s9 shot`) · 판 1440×900. DAG 4장, FORCE 2장, 안 비교 2장.
- 안 A·B·C 는 **같은 실데이터(476건)** 로 그린 목업이다(`…/scratchpad/mockup.html`, `measure.html`). 대시보드 코드가 아니라 별도 파일이므로 실제 적용 시 숫자는 ±10% 달라질 수 있다.
- 미검증: 마우스 얹기/드래그 상호작용(정적 캡처만), 라이트 tone·다른 skin 에서의 라벨 대비, 좁은 창(1000px 이하)에서 접은 배치의 세로 길이.
- 목업을 다시 보려면: `cd <scratchpad> && python3 -m http.server 9911` 후 `http://127.0.0.1:9911/mockup.html`.

### 2026-08-28T18:19:04+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-08-28 18:19:03 KST - lead]`

036 검토 셋 중 architect 가 API 오류로 끊겨 다시 걸었습니다. 그 사이 **025 의 재발 방지 장치를 붙였습니다.**

`worktree_sweep()` 이 주인 떠난 워크트리에 미커밋이 남아 있으면 **거두지 않고 그 가지에 대신 커밋합니다.** 박은 뒤에는 "안 합친 커밋"이 되어 sweep 이 손대지 않고, 제가 `s9 worktree ls` 에서 보고 합칩니다. 일하는 중인 워커의 것은 건드리지 않습니다.

이걸 만든 이유가 중요합니다 — **오전에 이미 워커 봉투에 `git add`·`commit` 을 넣어 뒀는데도**(커밋 `78fe985`, 15:20) 그 뒤 16:16 에 뜬 워커가 커밋하지 않았습니다. 손을 쥐여 주는 것만으로는 안 됩니다. 이 저장소가 여러 번 배운 그것입니다 — 표식만으로는 약하고 장치여야 합니다.

TDD 10건 통과. 025 는 워크트리 가지를 합치면 닫을 수 있습니다.

지금 상태:
- **026** 구현·적용 완료, 커밋 대기
- **025** 워커 구현이 가지에 보존됨 + 재발 장치 붙임, 합치기 대기
- **035** designer 구현 중
- **036** 검토 셋 진행 중 (architect 재시작)
- 워크트리 가지 4개 보존됨 — 035 끝나면 하나씩 합칩니다

### 2026-08-28T18:20:05+09:00 subagent (agent: sub:backend-developer:a0b7582c, by nicehugepark)

`[2026-08-28 18:17:37 KST - backend-developer]`

### 결론 먼저

**"상태체크"를 새로 만들 필요가 없다. 이미 여섯 벌이 있고, 지금 이 순간 서로 다른 답을 내고 있다.** 그리고 그 불일치의 뿌리는 판정 알고리즘이 아니라 **입력 데이터가 오염돼 있다는 것**이다 — 지금 이 세션의 위임 4건(나를 포함)이 전부 **13:35 에 done 된 REQ-20260828-016-62x6** 에 `running` 으로 기록돼 있다. 실제로 일하는 REQ-035 는 "아무도 안 붙었다"로, 끝난 016 은 "에이전트 4명 작업 중"으로 보인다.

따라서 권고는 ① 오귀속 두 개를 먼저 막고 ② 판정을 **한 함수**로 합쳐 `/api/catalog` 에 필드 3개로 내주고 ③ 자동 교정은 스폰 확대가 아니라 **상태 되돌리기(in-progress → open)** 로 한다. 무인 스폰 확대는 지금 하면 안 된다 — **일일 캡이 이미 소진(20/20)돼 워처가 30초마다 헛돌고 있고, 오늘 spawn.log 1052줄 중 1016줄(96.6%)이 `SKIP(cap)`** 이다.

---

### 1. 지금 무엇을 무슨 증거로 판정하는가

전부 `/home/sjpark1/section9/bin/s9` (12,664줄). 읽은 자리:

| 함수 (줄) | 묻는 것 | 증거 | 창 |
|---|---|---|---|
| `catalog_with_live()` **10377** | 이 카드가 초록인가 | 세션 바인딩 `last_req`+`active_reqs`(**616** `binding_req_ids`) ↔ 활동 파일 **mtime**(**1980** `_binding_activity_paths`: 부모 transcript · agent transcript · 스트림 미러) | `LIVE_WIN=120` (10386) |
| ↳ `direct` | 이 REQ를 등록한 세션이 방금 움직였나 | 위 mtime. **워커 바인딩만 `_worker_spawn_targets()` 로 1건으로 좁힘**(10437) | 120s |
| ↳ `session` | 문서 `session` 필드의 세션이 살아있나 | 같은 mtime | 120s |
| ↳ `spawned` / `spawn_failed` | 무인 워커를 띄웠나 / 즉사했나 | `state/auto_resume/<id>.json` 의 `last` + `pid` → `_worker_alive`(**1831**, `/proc` cmdline 에 claude 확인) | `SPAWN_WIN=600` (1770) |
| `_worker_spawn_targets()` **1797** | pid → 이 워커가 맡은 REQ | 스폰 마커 파일명 + `pid`, 동일 pid 는 `last` 최신 승 | — |
| `_binding_activity_paths()` **1980** | 활동 경로 목록 | 파일 존재 여부 + `streams_glob` | — |
| `delegated_live()` **2001** | 서브에이전트가 붙어 있나 | **문서 frontmatter `contributions[].result=="running"`** 의 `started/ended` 나이 | `DELEGATE_WIN=3600` (1999) |
| `rework_claimed()` **2044** | 살아있는 세션이 등록해 뒀나 | `delegated_live` **또는** 바인딩에 id 있고 `chat_live`(**2272**: inbox tail `/proc` 스캔 → `attach_pid` `/proc` 존재 → 활동 mtime 300s) | 120/300s |
| `stalled_requests()` **6557** | 잡아 놓고 멈췄나 | `catalog_with_live` 결과에서 `spawned` 제외 + `delegated_live` 제외 + **문서 `updated` 나이** | `STALLED_WIN=900` (6553) |
| `loose_requests()` **6612** | 한 줄이면 닫히나 | **독자 스캔**: `state/*.json` 직접 glob + `claim_dead`(**582**) + 본문에 `" commit ("` 문자열 | `CLAIM_GRACE=1800` (577) |
| `next_pickup()` **6687** | 다음에 뭘 집나 | `stalled_requests(win=0)` ∖ `rework_claimed` → `work_order` 1위 | — |
| `rework_watch_tick()` **4337** | 무인 스폰할까 | **마지막 전이 모양**(`review→in-progress` 또는 `open→in-progress [via dashboard]`, 4365-4370) + 유예(기본 30s) + `not rework_claimed` (4376) | — |
| `agent_health()` **7302** / `judge_health()` **7240** | 기여자 한 명이 살아있나 | `contributions[].transcript` mtime + 워커 pid + 워커 로그 마지막 줄. **범위: in-progress 문서만** | kind별 `HEALTH_WIN` |
| `live_agents()` **1843** / `live_workers()` **1884** | 지금 도는 에이전트/워커 | agent transcript mtime(120s) / 마커 pid | 120s |

증거 종류를 세면 **6가지**다: 프로세스 생존(`/proc`), 활동 파일 mtime, 문서 `updated`, 문서 `contributions`, 스폰 마커, 전이 이력 모양. 어느 함수도 이 6개를 다 보지 않고, 각자 3~4개씩 **다른 조합**으로 본다.

---

### 2. 판정이 몇 벌인가 — 실측 불일치

같은 물음에 답하는 코드가 **최소 6벌**(`catalog_with_live` · `stalled_requests` · `loose_requests` · `rework_claimed` · `agent_health` · `rework_watch_tick` 의 전이-모양 필터). 여기에 "이 세션이 지금 무슨 REQ를 하나"에 답하는 코드가 **또 2벌** — `_capture_target`(**1443**, `active_reqs` **역순**)과 `bin/s9-audit-agent:83 target_req`(`last_req` 먼저, `active_reqs` **정순**). 픽 순서가 반대다.

18:16 실측 (`in-progress` 4건 + 관련 2건):

```
id                         status       board live     stalled  loose  deleg  claimed  agent_health
REQ-20260828-016-62x6      done         -              -        -      True   True     []
REQ-20260828-021-62x6      in-progress  direct/1s      멈춤      -      False  True     []
REQ-20260828-025-62x6      in-progress  idle           -        -      False  False    []
REQ-20260828-026-62x6      in-progress  direct/1s      -        -      False  True     []
REQ-20260828-035-62x6      in-progress  direct/1s      멈춤      -      False  True     []
```

- **REQ-035 한 건에서 세 판정이 전부 갈린다**: 보드=초록(1초 전 진전), `stalled`=18분째 멈춤, `delegated_live`=아무도 없음. 사실은 designer 서브에이전트가 18:03부터 붙어 있다.
- **`loose_requests` 는 4건 모두 침묵**한다 — 독자 스캔이라 `live` 를 아예 안 본다. 훅이 매 턴 주입하는 "한 줄이면 닫히는 것"(bin/s9-audit-prompt:614)이 오늘 이 4건에 대해 한 번도 울리지 않았다.
- **`agent_health` 는 전부 빈 배열**이다 — 살아있는 `running` 기여가 done 문서(016)에 달려 있는데, 이 함수는 in-progress 문서만 훑는다(7305). 그래서 그 4건은 영원히 재판정되지 않는다.

**원인 A — 위임 기여의 오귀속 (치명적).** `bin/s9-audit-agent:83 target_req()` 는 `[last_req, *active_reqs]` 중 **소유 확인을 통과한 첫 번째**를 고른다. `_session_owns`(**1420**)는 소유만 보고 **상태를 안 본다**. 리드 바인딩은 `last_req = REQ-20260828-016-62x6`(13:35 done)을 그대로 들고 있다. 결과:

```
REQ-20260828-016-62x6 (status: done, 13:35 종결)
  running sub:designer:a9aa0303   18:03:39  "DAG 안개 구현"        ← 실제로는 REQ-035
  running sub:architect:a54c5c1a  18:11:41  "상태 자가점검 - 구조"  ← 실제로는 REQ-036
  running sub:designer:a992b2d5   18:12:02  "상태 자가점검 - 화면"  ← 실제로는 REQ-036
  running sub:backend-developer:a0b7582c 18:12:19 "상태 자가점검 - 구현" ← 나
```

`s9 claim`(`_claim_req` **7617**)에도 상태 가드가 없어 done 된 016 이 `active_reqs` 에 **다시 들어간다**. `update_active_reqs`(**1152**)가 done 전이 때 지웠는데 훅이 되살린 것이다. 되살아난 done REQ 는 그 뒤 모든 위임을 빨아들이는 블랙홀이 된다. vault 전체에 `result: running` 이 **28건** 떠 있는 것이 이 누적이다(done 193, stalled 2, failed 1).

**원인 B — 클레임이 한 번 움직이면 영원히 안 죽는다.** `claim_dead`(**582**)는 `updated <= claim_at` 일 때만 참이다. REQ-021 은 17:12 클레임 → 17:46 노트 한 번 → 그 뒤 30분 무진전인데 `claim_dead=False` 다. "클레임 이후 한 번이라도 움직였나"는 판정이지 "**최근에** 움직였나"가 아니다. 노트 한 줄이 클레임을 영구 면역시킨다.

---

### 3. 거짓 양성 / 거짓 음성

| 자리 | 종류 | 실측 |
|---|---|---|
| 보드 `direct` | **거짓 양성** | 021·026·035 셋 다 `live_age=1s`. 근거는 **리드 transcript 한 개의 mtime** 이다 — 리드가 036 을 하고 있어도 `active_reqs` 에 든 4건이 전부 초록. `stalled_requests` 는 이 결함을 알고 우회했지만(6591 주석, REQ-20260827-074-62x6) **보드는 안 고쳤다.** 사용자가 보는 화면이 거짓말하는 쪽이다. |
| `stalled` | **거짓 양성** | 035 를 "18분째 멈춤"으로 싣는다. designer 가 18:03부터 붙어 있다. `delegated_live` 가 못 잡는 이유는 §2 원인 A. |
| `delegated_live` | **거짓 음성** | 035·036 False. **거짓 양성**은 016(done)에 True. |
| `rework_claimed` | **거짓 양성** | 021·035 True — 리드 세션이 살아있다는 이유만으로. 그래서 워처가 021(17:12 채팅 반려 → `PENDING` 로그까지 찍힘)에 **1시간째 스폰을 못 한다.** 반려 자동재개의 유일한 목적이 이 자리인데 리드의 죽은 클레임이 막고 있다. |
| `rework_watch_tick` | **거짓 음성(구조적)** | REQ-025 는 55분째 무주였는데 후보조차 아니다. 마지막 전이가 `open→in-progress` 인데 `[via dashboard]` 가 없어서다(4368). 워커 세션 `adf77fd2`(pid 226984)가 16:17 에 CLI 로 착수하고 죽었고, 그 뒤 아무 장치도 이 문서를 보지 않았다. **사용자가 18:02 에 지적한 그 케이스가 정확히 이것이다.** |
| `agent_health` | **거짓 음성** | in-progress 만 훑어 done 문서의 열린 `running` 4건을 못 본다. `health_apply` 도 못 되쓴다 → 28건의 영구 좀비. |
| 반대로 잘 맞은 것 | — | 025 의 `idle` 판정은 정확했다. 워커 pid 226984 가 죽었고 `chat_live` 3경로 모두 실패 → `rework_claimed=False`. `_worker_spawn_targets` 의 1건 좁히기(10437)도 제대로 동작했다. |

---

### 4. "상태체크"를 서버가 어떻게 내줄 것인가

**새 API를 만들지 마라. `/api/catalog` 에 필드 3개를 더해라.** 근거는 비용이 아니라 **판정 개수**다 — 엔드포인트를 하나 더 내면 7벌째가 된다.

측정치 (565행 카탈로그, 세션 바인딩 90개, `/proc` 51):

```
load_catalog            2.8 ms
catalog_with_live      57.4 ms   ← /api/catalog 이 매번 부름
stalled_requests       74.6 ms
loose_requests        110.8 ms
next_pickup           223.7 ms
agent_health           34.9 ms
rework_claimed(1건)    24.1 ms   ← 대부분 chat_live 의 /proc 스캔
approvals_unseen      550.3 ms   ← 워처가 30초마다 부름
```

`catalog_with_live` 가 무엇을 훑는가 (cProfile, 10회):
- `state/sessions/*.json` **90개** 열기 + 파싱
- 바인딩마다 `streams_glob` → glob **179회/호출**
- **`load_catalog` 를 12회 재호출** = `json.loads` **7,360회/호출**. `claim_dead`(**582**)가 클레임마다 카탈로그 전체를 다시 읽는다.
- `/proc` 스캔은 여기 없다(0.6ms 짜리 `_proc_cmdlines` 는 `rework_claimed` 경로).

**즉 `catalog_with_live` 시간의 60%가 카탈로그 재파싱이다.** 요청 스코프 캐시 하나로:

```
catalog_with_live  as-is       71.9 ms
catalog_with_live  캐시 적용    21.1 ms   (3.4x)
stalled_requests   캐시 적용    17.7 ms
```

폴링 비용: `web/index.html:3720` 이 `setInterval(refreshCatalog, 15000)` — **`document.hidden` 가드가 없다**(3724·3729 의 다른 폴러들과 달리). 탭 1개당 67ms/15s = **코어의 0.45%**. 캐시 후 0.14%. 여기에 판정 필드를 얹어도 `stalled`/`delegated` 는 이미 같은 데이터를 보므로 **증분은 사실상 0** 이다.

진짜 낭비는 폴링이 아니라 워처다: `_rework_loop`(**11758**)가 30초마다 `approvals_unseen` 550ms + `agent_health` 35ms + REQ별 `rework_claimed` 24ms 를 돌린다. **한 코어의 약 2%가 상시** 소모되는데, 그 550ms 는 **절대 소진되지 않는 큐 4건**을 매번 다시 계산하는 것이다(§5).

**계약 제안** — `/api/catalog` 각 행에 (파생 필드, 원본에서 언제든 재계산 가능):

```
attention: "working" | "delegated" | "spawned" | "unattended" | "spawn_failed"
attention_since: <ISO>        # 이 상태가 된 때 (서버 시각 기준)
attention_why: "designer 18:03 착수" | "클레임 후 34분 무진전" | ...
```

원칙 둘은 이 파일이 이미 지키는 것을 그대로 따른다(11141 주석): **서버는 사실만, 화면이 정책을**(빨강/노랑은 화면이 정한다), **응답에 서버 시각을 담는다**.

---

### 5. 자동 교정을 어디까지

**무인 스폰 확대는 지금 하면 안 된다.** 실측:

```
state/auto_resume/_global.json → {"day_count": 20, "hour_count": 4}
기본 캡 (bin/s9:1634 _auto_caps_ok): per-REQ 쿨다운 600s · 시간당 6 · 일일 20
오늘 spawn.log 1052줄 중 SKIP(cap) 1016줄 (96.6%)
```

16:16 에 followup 3건이 한꺼번에 뜨면서 일일 캡이 찼고, **그 뒤 2시간 동안 워처는 30초마다 같은 4건에 대해 `SKIP(cap)` 만 찍고 있다.** 큐가 안 빠지는 이유: `_approvals_mark` 는 **스폰 성공 시에만** 호출된다(4402) → 캡에 막히면 영원히 재시도. 게다가 `_auto_caps_ok` 는 스폰 성공을 확인하기 **전에** 카운터를 올린다(1663-1666) — 즉사한 워커도 캡을 태운다(이미 REQ-20260826-021-62x6 에서 겪은 실패다).

여기에 "무주 in-progress 전체"를 얹으면:
- **후보 폭증**: 지금은 전이 모양 필터가 후보를 2종으로 좁힌다. 풀면 in-progress 전량이 후보다.
- **중복 스폰**: `rework_claimed` 가 거짓 양성/음성 둘 다 있는 상태라(§3), 리드나 서브에이전트가 붙어 있는 문서에 워커가 겹쳐 뜬다 — `web/index.html` 을 두 주체가 동시에 고친 REQ-20260828-003-62x6 사고의 재현. **지금 `web/index.html` 은 다른 세션이 쥐고 있다.**
- **캡 고갈**: 반려 자동재개(진짜로 필요한 자리)가 무주 순찰에 밀려 굶는다. 이미 021 이 1시간째 못 뜨고 있다.
- **로그 오염**: 후보가 늘면 `SKIP(cap)` 이 30초 × N건으로 늘어 spawn.log 가 판독 불가가 된다.

**상태만 되돌리는 안(in-progress → open)이 옳다.** 이유:
- **멱등**하다. 이미 open 이면 무동작. 재시도가 부작용을 낳지 않는다.
- **되돌릴 수 있다.** 잘못 되돌려도 사람이나 다음 세션이 다시 집으면 끝이다. 잘못 스폰하면 파일이 깨진다.
- **캡이 필요 없다.** 프로세스도 토큰도 안 쓴다.
- **아무도 안 붙었다는 사실이 대기열에 반영된다** — open 으로 돌아가면 `work_order` 에 다시 들어가고, `s9 next`(6687)와 세션 기동 훅이 정상 경로로 집는다. 지금은 in-progress 라서 "이미 누가 한다"로 보여 아무도 안 집는다.

되돌리기 조건은 보수적으로: `attention == "unattended"` 이고 무진전 30분 이상(`STALLED_WIN` 의 2배)이고 커밋 흔적 없음. 전이 노트에 **되돌린 근거를 반드시 적어** 다음 사람이 판정할 수 있게 한다(`s9 status <id> open --note '무주 34분 — 자동 반환(세션 adf77fd2 종료)'`). 되돌림은 문서 History 에 남으므로 사후 감사도 된다.

---

### 6. 권고 하나와 구현 순서

**권고: `req_attention()` 한 함수를 진실로 삼고, 나머지 여섯 벌은 그것을 읽기만 하게 바꾼다. 자동 교정은 스폰이 아니라 상태 반환으로 한다.**

### 순서

**0단계 — 데이터 오염부터 막는다 (이걸 안 하면 나머지가 전부 헛수고다).** TDD 3건:
- `_claim_req`(7617)·`update_active_reqs`(1152): **종결 상태(done/cancelled) 문서는 `active_reqs`/`last_req` 에 넣지 않는다.** 회귀: done 전이 후 훅이 claim 해도 되살아나지 않는다.
- `bin/s9-audit-agent:83 target_req()`: 소유 + **`status == in-progress`** 인 것만 후보. `_capture_target`(1443)과 **픽 순서를 통일**한다(최근 클레임 우선).
- `agent_health`(7302) 범위를 "in-progress 문서" → "**열린 `running` 기여가 있는 문서**"로. 지금 vault 에 떠 있는 28건의 좀비를 `health_apply` 가 stalled 로 마감한다.

**1단계 — 판정 단일화.** `req_attention(rows=None, now=None)` 를 `catalog_with_live` 바로 위에 두고, 요청 스코프 카탈로그 캐시를 인자로 흘린다(측정: 71.9 → 21.1ms). 반환은 §4의 3필드. 그 안에서만 6가지 증거를 본다. `catalog_with_live` 의 `live_kind` 는 이 함수의 얇은 표면이 되고, `stalled_requests`·`loose_requests`·`next_pickup`·`rework_claimed` 는 **필터로만** 남는다. 동시에 두 결함을 고친다:
- `claim_dead`(582): "클레임 후 한 번이라도" → "**최근 N분 내**" 로. `updated` 가 `now - CLAIM_GRACE` 보다 오래면 클레임 해제.
- `direct` 판정: 리드 바인딩은 transcript **한 개**를 여러 REQ 가 공유한다. 워커에 이미 적용한 1건 좁히기(10437)의 논리를 리드에도 적용 — **그 REQ에 귀속된 agent transcript 또는 그 REQ 문서의 `updated`** 를 증거로 요구한다.

**2단계 — 내주기.** `/api/catalog`(11149)에 3필드 추가. 새 엔드포인트 없음. `/api/agents`(11239)는 세션 스코프라 그대로 둔다. 화면은 다른 세션 몫.

**3단계 — 자동 교정.** `_rework_loop`(11758)에 `unattended_sweep()` 을 얹는다 — 스폰이 아니라 상태 반환. 같은 틱에 `approvals_unseen` 을 캡 소진 시 건너뛰게 해서 550ms 헛계산을 없앤다(캡이 찼으면 계산 자체가 무의미하다).

### 나아졌다고 말할 지표 (전부 지금 값이 측정돼 있다)

| 지표 | 지금 | 목표 |
|---|---|---|
| 열린 `running` 기여 중 종결 문서에 달린 비율 | **4/4 = 100%** | 0% |
| vault 전체 좀비 `running` 수 | **28** | 0 (마감되거나 실제 진행) |
| 한 REQ에서 board/stalled/claimed 가 갈리는 건수 | **3/4 in-progress** | 0 |
| in-progress 무주 체류 시간 p95 | **84분** (사용자 실측, 18:02) | < 30분 |
| spawn.log `SKIP(cap)` 비율 | **1016/1052 = 96.6%** | < 5% |
| 사용자가 "진행 중인 거 맞나" 를 먼저 묻는 횟수 | **오늘 3회** (16:24·17:56·18:02) | 0 |
| `/api/catalog` p50 | **57~72ms** | < 25ms |
| 워처 1틱 비용 | **~620ms / 30s (코어 2%)** | < 100ms |

---

### 한계·미검증

- 나는 **읽기만** 했다. 위 수치는 전부 `/home/sjpark1/section9/bin/s9` 를 모듈로 import 해 실행한 결과이고, 파일은 

…(truncated)

### 2026-08-28T18:21:32+09:00 subagent (agent: sub:architect:aeaee0ab, by nicehugepark)

`[2026-08-28 18:20:23 KST - architect]`

**결론 먼저: 판정 축이 두 개뿐이라 못 잡는다 — "누가 붙어 있나"와 "문서가 움직였나"는 있는데 "산출물이 도착했나"가 없다. 그런데 그 사실은 이미 계산되고 있다(`worktree_state().ahead`). 필요한 것은 새 판정이 아니라 연결이다. 그러니 상태체크는 버튼도 표시도 아닌 인도(delivery) 자동화여야 한다.**

---

### 1. 판정 능력 — "모른다"와 "안 물어본다"가 갈린다

다섯 함수의 증거 원천을 보면 전부 같은 두 축이다.

| 함수 | 증거 | 답하는 질문 |
|---|---|---|
| `catalog_with_live` | 세션 바인딩 mtime · 스폰 마커 pid | 프로세스가 붙어 있나 |
| `delegated_live` | 문서의 `contributions.result == "running"` (1h 한도) | 서브에이전트가 붙어 있나 |
| `rework_claimed` | 살아 있는 바인딩 + 위 | 누가 잡고 있나 |
| `stalled_requests` | 위 셋 + 문서 `updated` 나이 | 붙은 이도 없고 문서도 안 움직였나 |
| `loose_requests` | **문서 본문에 `" commit ("` 문자열** · 죽은 클레임 | 한 줄로 닫히나 |

**다섯 중 어느 것도 저장소를 보지 않는다.** 질문에서 확인하라 한 `loose_requests` 의 `committed` 판정은 본 저장소 커밋을 보는 게 아니라 **문서 본문 문자열**이다 (`bin/s9:6654` — `has_commit = " commit (" in f.read()`). 그 문자열은 post-commit 훅이 남긴 노트 헤딩이므로, 결국 여기서도 증거는 git 이 아니라 문서다. 즉 산출물 도착은 **애초에 모른다**.

그런데 시스템 전체로는 안다. 방금 읽기만으로 확인했다.

```
$ ./bin/s9 loose          → (출력 없음)
$ ./bin/s9 stalled        → 021, 035 두 건 (워크트리 넷은 언급 없음)
$ ./bin/s9 worktree ls    → 넷 전부 "← 안 합친 커밋 1"
```

`worktree_state()` 가 `ahead`/`dirty` 를 정확히 계산하고 `s9 worktree ls` 가 그대로 찍는다. **아무 판정 함수도 그 함수를 부르지 않을 뿐이다.** 능력의 부재가 아니라 배선의 부재다 — 이게 뒤의 권고를 싸게 만든다.

**추가로 찾은 결함 (권고에 직결).** `cmd_commit_note` (`bin/s9:7472`) 는 `git log -1` 을 `cwd=ROOT` 로 돈다. 그런데 post-commit 훅은 워크트리에서 발화하고 `S9_ROOT` 는 본 저장소로 못박혀 있다(`bin/s9-install:98-116`). 결과: **워커가 자기 가지에 커밋하면 문서에 적히는 것은 본 저장소의 마지막 커밋 — 남의 것이다.** 자기 REQ 에는 아무 흔적도 안 남고(→ `loose` 놓침), 본 HEAD 메시지에 적힌 REQ 에는 "커밋됐다 → `s9 status done`" 이 붙는다(→ 거짓 완료). `cmd_commit_note` 가 docstring 에서 "근거 없는 자동 완료는 거짓 진행보다 나쁜 거짓 완료를 쌓는다"고 명시적으로 피하려 한 바로 그것이 워크트리에서는 발생 가능하다.

### 2. 왜 경고가 있는데 84분이 흘렀나 — 세 가지 구조

**(a) 경고가 도착하는 순간이 언제나 "사용자가 방금 새 일을 시킨" 순간이다.** `stalled` 은 UserPromptSubmit 훅에서 새 REQ 문서 생성과 함께 주입된다(`bin/s9-audit-prompt:619`). 문구는 "**다른 일을 시작하기 전에** 이어서 하라". 사용자 지시와 정면 충돌하고, 충돌은 항상 사용자 쪽으로 풀린다. 이 장치는 **한가한 턴이 존재하지 않는 주체**에게 붙어 있다. 게으름이 아니라 설계된 우선순위 역전이다.

**(b) 상시 참인 신호는 배경이 된다.** 같은 자리에 9종(untitled·approvals·reopened·loose·stalled·serve-stale·blocked·review·workers)이 나란히 오고 그 뒤에 20줄 규약이 붙는다. `loose_requests` 의 docstring 이 이미 자백했다 — "실제로 나는 매 턴 그 경고를 받으면서 넘겼다". 그래서 `loose` 를 좁혔는데, 좁힌 `loose` 는 지금 **0건**을 내고 `stalled` 은 넓은 채로 남았다. 좁히기가 반만 적용됐다.

**(c) 원인 단정이 틀리면 처방이 실행 불가가 되고, 경고 자체의 신뢰를 깎는다.** `stalled` 문구는 "**내가 잡아 놓고 손을 뗀 것이다**", 처방은 "이어서 하거나 blocked/review". REQ-025 의 실제 상태는 "끝났는데 도착 안 함"이었고 문서에는 TDD 11개 체크 + 완료 보고가 있었다. 리드가 그 문서를 열면 경고가 **오탐으로 보인다.** 오탐으로 보인 경고는 다음번에도 넘긴다. 84분은 경고가 울리지 않아서가 아니라 **경고가 틀린 이름을 불러서**다.

### 3. "상태체크"는 무엇이어야 하나 — 스스로 고치는 장치, 단 고칠 대상은 상태가 아니라 도착

- **버튼(눌러 확인)** — 기각. 이미 있다: 대시보드 회색 점, `s9 stalled`, `s9 next`. 사용자가 오늘 세 번 물은 것 자체가 "버튼이 있어도 사람이 눌러야 한다"의 증거다. 사용자의 요구는 "물어볼 자리를 달라"가 아니라 "묻지 않아도 되게 하라"였다.
- **표시(늘 말하는)** — 기각. 이미 있고, 그 상태에서 84분이 났다. 같은 축에 하나 더 놓으면 배경 소음만 늘어난다(2b).
- **스스로 고치는 장치** — 채택. 단 **자동 `done` 전이는 금지**다. `cmd_commit_note` 가 이미 옳게 거부했고 그 판단을 뒤집을 근거가 없다. 자동화해도 안전한 것은 판정이 아니라 기계적 인도다: 워커의 결과물을 본 저장소로 옮기는 일.

**결과물 도착까지 봐야 하는가 — 그렇다, 그것이 빠진 유일한 축이다.**

```
축1 붙어 있나        catalog_with_live · delegated_live · rework_claimed   [있음]
축2 문서가 움직였나   stalled_requests · loose_requests(문서 문자열)        [있음]
축3 산출물이 도착했나  ─────────────────────────────────────── 없음
```

축3 없이는 1·2를 아무리 정밀하게 만들어도 REQ-025 를 못 잡는다. 반대로 축3만 있으면 **워커가 죽는 그 순간에** 잡힌다 — 15분 유예조차 필요 없다. 기존 장치와 겹치지도 않는다: `s9 next`·`rework_watch_tick`·보드 live 점은 전부 축1·2다.

보드에는 새 수동 플래그가 아니라 축3에서 파생된 상태 하나가 필요하다 — **미인도(undelivered)**. "아무도 안 붙었다"와 "끝났는데 안 왔다"가 같은 회색으로 보이는 것이 지금의 결함이다.

### 4. 권고 하나

**워커의 종료가 곧 인도가 되게 하라.** 세 조각을 순서대로, 앞의 것이 뒤의 것 없이도 값어치가 있다.

1. **(반나절) `loose_requests` 에 세 번째 종류 `undelivered` 를 넣는다.** `worktree_list()` → `worktree_owner_read(name)["doc"]` 로 REQ 를 얻고 `worktree_state()` 의 `ahead`/`dirty` 가 있으면 실었다. fix 는 `s9 worktree merge <이름>`. 새 표도 새 판정도 없다 — **이미 계산된 사실을 이미 있는 통로에 꽂는 것**. 지금 즉시 4건이 리드 앞에 뜬다. 그리고 이건 2b 의 좁히기 원칙에 맞는다: 붙여 넣을 명령이 하나로 정해져 있다.
2. **워커 종료 시 자동 커밋.** `worktree_sweep()` 이 30초마다 돌며 "주인 pid 죽음 + dirty" 를 만나면 지금은 그냥 남긴다. 대신 그 자리에서 `git add -A && git commit -m "워커 미커밋 보존: <REQ>"` 한다. 워크트리는 그 REQ 전용이라 남의 편집을 담을 위험이 **구조적으로** 없다 — 그게 `worker_worktree` 를 켠 이유다. 이러면 "커밋 못 하고 죽은 워커"라는 소실 경로가 사라지고 남는 문제는 합치기 하나로 줄어든다. 봉투에 git 을 넣어 준 15:20 조치가 규율에 기댄 것이었다면 이건 장치다.
3. **`cmd_commit_note` 의 워크트리 결함을 고친다.** 커밋을 읽는 cwd 는 훅이 발화한 저장소여야 하고, 문서를 쓰는 ROOT 만 본 저장소여야 한다. 지금은 둘 다 본 저장소다. 안 고치면 2단계가 오히려 거짓 완료를 양산한다 — 자동 커밋이 늘수록 잘못된 REQ 에 "커밋됐다" 노트가 쌓인다.

**반증 조건** (이 권고가 틀렸다고 인정할 지점):

- **워크트리가 원인이라면** 장치가 아니라 설정을 끄는 게 맞다. 확인법: `worker_worktree` 도입 이전 로그에서 "워커가 구현했는데 본 저장소에 안 왔다"를 찾는다. REQ-20260826-021-62x6(미커밋 작업 트리 소실)이 이미 그 형태이므로 워크트리는 **원인이 아니라 드러낸 것**이라고 보지만, 반례가 없으면 워크트리 자체를 재검토해야 한다.
- **1단계 투입 후에도 `undelivered` 가 몇 시간 방치되면** 문제는 "모른다"가 아니라 "알아도 안 한다"이고, 주입 계열 전부가 이 주체에게 안 통한다는 뜻이다. 지표: undelivered 등장 → merge 까지의 중앙값. 30분을 넘으면 4단계(테스트 통과 시 자동 merge)로 가야 한다.
- **2단계 자동 커밋이 반쪽 상태를 들이면** — 충돌이 아니라 "문법은 맞는데 반만 된 코드"가 본 저장소에 오면. 이건 커밋이 아니라 무검증 merge 탓이므로 권고를 뒤집지 않고 merge 전 테스트 게이트라는 조건을 추가한다.

---

관련 파일: `/home/sjpark1/section9/bin/s9` (`stalled_requests` 6557 · `loose_requests` 6612 · `cmd_commit_note` 7456 · `worktree_state` 11927 · `worker_workspace` 11979 · `worktree_sweep` 12049), `/home/sjpark1/section9/bin/s9-audit-prompt` (596-690 주입 블록), `/home/sjpark1/section9/bin/s9-install` (85-120 훅 설치).

파일은 하나도 고치지 않았고 `s9 note` 도 남기지 않았다. 실행한 것은 읽기 전용 조회 셋(`stalled`·`loose`·`worktree ls`)뿐이다.

### 2026-08-28T18:23:19+09:00 subagent (agent: sub:designer:a992b2d5, by nicehugepark)

`[2026-08-28 18:22:04 KST - designer]`

### 결론

**화면은 구별하지 못한다. 못 하는 게 아니라 반대로 말한다** — 지금 초록 점 3개 중 2개가 거짓이다. 점이 재는 것이 "이 요청의 진전"이 아니라 **"이 요청을 잡고 있는 세션의 맥박"** 이기 때문이다. 리드 세션이 살아서 다른 일을 하고 있으면, 그 세션이 잡아 둔 요청은 손 뗀 지 한 시간이 지나도 전부 초록으로 뛴다. 사용자가 세 번 물은 이유는 회색 점이 작아서가 아니라 **화면이 초록이라고 답했기 때문**이다.

고칠 것은 셋. ① 점의 근거를 세션 맥박 → 요청 진전으로 바꾼다(데이터), ② 멈춘 카드에만 **글자로 정도를 적는다**(카드에 이미 있는 `선행 대기` 줄과 같은 컴포넌트), ③ 오래 멈춘 것을 열 위로 올린다. **새 경고 띠는 만들지 않는다.**

모양: `/tmp/claude-1000/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/scratchpad/proposal.png` (원본 `proposal.html`, `http://127.0.0.1:9977/proposal.html` 로 띄워 둠)

---

### 1. 지금 화면이 무엇을 말하고 있나 — 실측

18:13 캡처 `board-now.png`(현행 skin) / `ledger-board.png`(기본 ledger·paper), 4배 확대 `zoom1.png`·`zoom2.png`.

in-progress 4건의 화면 표시 vs 실제:

| 카드 | 화면의 점 | `s9 stalled` | 마지막 진전 |
|---|---|---|---|
| 026 첨부 폴더가 두 벌 | 🟢 초록 | (없음) | 18:11 — **진짜 도는 중** |
| 035 DAG 글자가 안개 | 🟢 초록 | 16분째 진전 없음 | 17:55 — **거짓** |
| 021 언급한 것을 눌러서 | 🟢 초록 | 25분째 진전 없음 | 17:46 — **거짓** |
| 025 배너가 못 보는 훅 | ⚪ 회색 | 54분째 진전 없음 | 17:18 |

`/api/catalog` 원본에서 021·035 둘 다 `live:true, live_kind:"direct", live_age:0`. 즉 화면은 "0초 전에 갱신됨"이라고 말하고 있었다.

**왜 그런가 — 판정이 두 벌이고 서로 반대다.**
- `bin/s9:10440` `catalog_with_live()` → 세션의 transcript 파일 mtime이 120초 안이면 그 세션의 `active_reqs` **전부** `live=true`. 화면은 이것을 그린다.
- `bin/s9:6592` `stalled_requests()` → 주석에 이렇게 쓰여 있다: *"세션이 살아 있다는 것은 **이 요청의** 증거가 아니다 (REQ-20260827-074-62x6). 리드 세션은 늘 살아 있고 여러 요청을 한꺼번에 클레임한다."* 그래서 CLI는 요청 문서의 `updated` 를 본다.

**REQ-20260827-074에서 CLI만 고치고 화면을 두고 갔다.** 사용자가 보는 쪽이 안 고쳐진 쪽이다. 이게 오늘의 근인이고, 아래 시각 개선은 전부 이걸 먼저 고쳐야 의미가 있다 — 안 고치면 더 크고 정확해 보이는 거짓말이 된다.

### 2. 회색 점이 왜 안 들리는가 — 잰 값

캡처 픽셀에서 직접 측정(순수 python PNG 디코드).

**대비 — 기준 미달.**
| | 값 | 판판 대비 |
|---|---|---|
| off 점 (라이트 `#9096a0` on `#fff`) | (144,150,160) | **2.98:1** |
| off 점 (다크 `#5f646d` on `#17181b`) | (95,100,109) | **2.98:1** |
| on 점 `#16a34a` | (22,163,74) | 3.29:1 (라이트) |

비텍스트 UI 요소 기준 3:1(WCAG 1.4.11) **미달**. 라이트·다크 양쪽에서 우연히 같은 값인 이유는 off 가 `--faint` 를 쓰기 때문 — 토큰 세트에서 가장 흐린 색이다.

**색만으로 구분하고, 그 색조차 명도가 같다.**
- 라이트: 초록 L=0.269 vs 회색 L=0.303 → 둘 사이 대비 **1.11:1**. 흑백으로 보면 **회색 점이 초록보다 오히려 밝다.** "일하는 중"이 "버려짐"보다 어둡다.
- 다크: 1.81:1. 역시 3:1 미만.
- s9-design 7절 "색만으로 상태를 구분하지 않는다(모양·문구 병행)" 위반. 적록색약이면 두 점은 같은 점이다.

**크기·위치·경쟁.**
- 6px 원. 카드 안 요소 중 가장 작고, 가장 흐린 색(`.id` 줄, mono 10px `--faint`)에 실려 있다.
- 현행 skin에서는 카드 **맨 아래** — 제목·태그·아바타를 다 읽은 뒤에 온다.
- 같은 카드에 더 크고 더 초록인 것들이 있다: 사용자 아바타 링이 `#2f9f42` 초록 지름 18px(면적 약 9배)이고, 025 카드에는 초록 `TDD 11/11` pill까지 있다. **아무도 안 붙은 카드가 그 열에서 가장 초록인 카드다**(`zoom2.png`).

**진실은 화면에 있지만 닿을 수 없다.** off 점의 `title` 은 이미 정직하다 — *"in-progress지만 스트림 조용함 — 실제 동작 아닐 수 있음"*. 그런데 그 문장은 6px 타깃의 hover 툴팁 안에 있다: 터치에서 안 뜨고, 포커스가 안 잡혀 키보드로 못 가고, 최소 터치 타깃 44×44 기준의 1/50이다. 유일하게 사실을 말하는 자리가 도달 불가다.

**시간 정보 — "84분"은 화면에 없다. 있는 숫자는 오해를 만든다.**
카드 우상단 `⏱ 1h 55m` 은 `status_since` = **"in-progress 상태로 있은 시간"**(`web/index.html:4208`)이지 침묵 시간이 아니다. 025는 `⏱ 2h 1m` 인데 침묵은 60분, 035는 `⏱ 34m` 인데 침묵은 23분 — 상관이 없다. 게다가 이 숫자는 초록 점 옆에서 **일한 시간처럼 읽힌다.** 화면에 있는 유일한 시간이 정반대 방향으로 안심시킨다.
(덤: `.card:hover .elapsed{visibility:hidden}` — 마우스를 얹으면 이 슬롯은 "이어 말하기" 버튼에 자리를 내주고 사라진다. 중요한 정보를 여기 두면 안 된다는 뜻이기도 하다.)

### 3. 무엇이 보여야 안 묻게 되나 — 세 물음은 한 표시로 못 답한다

세 질문의 답이 다르고, **사용자가 이어서 할 행동이 다르다**:

| 물음 | 답의 성격 | 사용자가 할 일 |
|---|---|---|
| 진짜 도는 중인가 | 이진 + 최신성 | 아무것도 — 기다린다 |
| 줄 서 있는 건가 | **무엇을** 기다리는지(대상) | 선행을 풀거나 판정을 누른다 |
| 그냥 멈춘 건가 | **얼마나** 조용한지(정도) | 이어받거나 놓는다 |

한 점으로는 불가능하다. 점은 이진값 하나만 나르는데, 2번과 3번은 **점이 안 켜진 상태를 공유한다** — 지금 화면에서 "대기 중"과 "버려짐"이 똑같이 회색인 이유다. 그리고 정도(60분)는 애초에 이진값에 담기지 않는다.

**나누되, 자리는 늘리지 않는다.** 카드에는 이미 "이 카드가 왜 안 나아가나"를 적는 줄이 있다 — `.rvpt` (선행 대기 / 대기 사유, `index.html:4168`). 여기서 낱말만 갈리게 한다:

```
(줄 없음)                                    → 도는 중
선행 대기  서버 시각이 설정을 늦게 안다        → 줄 서 있다   (이미 있음)
멈춤      60분째 진전 없음 · 마지막 17:18      → 멈췄다      (새로, 같은 컴포넌트)
```

**부재가 신호다.** 이유 줄은 조용한 카드에만 생기므로, 열을 훑을 때 **줄 달린 카드 = 손봐야 할 카드**. 새 배지도, 새 색면도, 새 컴포넌트도 없다.

점은 보조로 남기되 **모양을 가른다**: 켜짐 = 채운 초록(+펄스), 멈춤 = **속 빈 링** `--muted` 1.5px. 이미 `.livedot.sess` 가 쓰는 문법이다. 대비 라이트 5.98:1 / 다크 6.75:1 — 토큰 하나 바꾸면 3:1 미달이 해소된다.

### 4. 어디에 두나

- **Board 카드 = 1차 자리.** 사용자가 실제로 보는 곳이고, 세 번의 질문이 전부 보드를 보다 나왔다.
- **in-progress 열 머리 = 보조.** `IN-PROGRESS  3 멈춤  4` — mono, `--c-inprogress` 글자색(면 아님). 훑을 때 한 번에 들어온다. 썸네일 28% 축소 확인(`thumb.png`): 현행 열은 균일한 회색 덩어리, 제안 열은 앰버 표식 3개 + 머리 수가 남는다.
- **정렬 = 표시를 안 늘리는 답.** 오래 멈춘 순 → 도는 중 순. 오늘 025(60분)가 맨 위인 건 우연이고 021(32분)은 맨 아래였다. 규칙으로 만들면 **열의 첫 줄만 봐도 답이 있다.**
- **`선행 대기 현황` 레일은 쓰지 마라.** 축이 다르다 — 레일은 "다른 요청 때문에 막혔다"(정상적 대기), 우리는 "아무도 안 붙었다"(방치)를 말한다. 한 판에 섞으면 사용자가 세 번 물은 그 혼동을 화면이 제도화한다. 그리고 실증: 오늘 "없음"인 건 막힌 게 없어서가 아니라 **전체 문서 300여 건 중 `blocked_by` 가 걸린 문서가 0건, `blocked` 상태도 0건**이기 때문이다. 잘 만든 판인데 데이터가 한 번도 들어온 적이 없다 — 판을 하나 더 세우자는 안에 대한 경고로 읽어야 한다. (다만 **형식의 선례**로는 좋다: in-progress 가 10건을 넘어가면 같은 `.dsitem` 문법으로 Board 우측 `멈춘 작업` 레일을 만들고, 0건일 때 판을 버리는 `.empty` 규칙까지 물려받는다. 지금 규모에선 불필요.)
- **문서 화면**: History 위에 한 줄 — `마지막 진전 17:18 (60분 전)`. 카드가 요약, 문서가 근거.
- **Docs**: in-progress 행에 같은 컴포넌트 재사용. **Terminal**: 아니다 — 지금 이 세션의 흐름이지 전체 현황이 아니다.

### 5. 하지 말아야 할 것

- **경고 띠를 하나 더 얹지 마라.** 오늘 `s9 stalled` 이 매 턴 리드 앞에 주입되고도 84분이 흘렀다. 장치는 울렸고 안 들렸다. 헤더에는 이미 `▲ 서버가 옛 코드로 돌고 있습니다` 띠가 하루 종일 떠 있다 — 같은 층위를 하나 더 만들면 둘 다 죽는다. `loose_requests()` 주석이 이미 이 교훈을 적어 뒀다: *"길어진 목록은 훑고 넘긴다."* 그래서 이번 답은 **띠가 아니라 자리·낱말·순서**다.
- 카드 배경 앰버 칠하기, 좌측 컬러 바, 세로 띠 — 금지(디자인 언어).
- 깜빡임을 늘리지 마라. 멈춘 카드가 펄스하면 진짜 도는 초록 펄스의 뜻이 죽는다. 움직임은 "지금 돌고 있다" 하나에만.
- 새 pill 배지 금지 — `.rvpt` 로 해결된다.
- **모르는 것을 초록으로 말하지 마라.** 지금 초록의 뜻은 "세션이 살아 있다"인데 사용자에겐 "이 일이 돌아간다"로 읽힌다. 확신 없는 초록보다 정직한 침묵 표기가 낫다.
- 무채색 미니멀 금지 — 여기서 앰버(`--c-inprogress #b45309`)는 **글자색**으로만 쓴다. 면 없음.

### 6. 모양

- **제안(권장)**: `/tmp/claude-1000/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/scratchpad/proposal.png` — 왼쪽 "지금(초록3/회색1)", 가운데 "제안(도는 것1/멈춘 것3)", 오른쪽 "세 물음 세 답". 하단에 대비 실측 근거. 원본 `proposal.html` 동일 디렉터리, 현재 `http://127.0.0.1:9977/proposal.html` 로 서빙 중(python http.server, 필요 없으면 `pkill -f "http.server 9977"`).
- **현재 실화면**: `board-now.png` / `ledger-board.png`
- **4배 확대 비교**: `zoom1.png`(026·035 초록), `zoom2.png`(021 초록 vs 025 회색 — 025 카드의 초록 TDD pill이 함께 보인다)
- **썸네일 스캔 검증**: `thumb.png`

### 구현 순서 (다른 세션이 index.html 을 놓은 뒤)

1. **서버**: `catalog_with_live()` 가 `stalled_requests()` 와 같은 판정을 쓰게 하고, 행마다 `stalled_mins` 를 실어 준다. 판정을 두 벌 만들지 말 것 — 이번 사고의 원인이 그거였다. (`updated` 는 이미 `/api/catalog` 에 있으므로 화면에서 계산도 가능하지만, 두 벌이 다시 생긴다.)
2. **CSS**: `.livedot.off` → 링(`background:transparent;border:1.5px solid var(--muted)`). 토큰 한 줄.
3. **cardHTML**: 멈춘 in-progress 에 `.rvpt.stop` 한 줄 추가(`선행 대기` 줄과 형제, 선행이 있으면 선행 줄이 이긴다 — 같은 사실을 두 줄로 말하지 않는 기존 규칙 유지).
4. **열 머리 + 정렬**: `3 멈춤` 카운트, 멈춘 시간 내림차순 우선.

### 한계

- `?skin=` 파라미터가 있어 ledger/paper 는 확인했으나, glass·tray·atlas 등에서 `.rvpt.stop` 이 어떻게 앉는지는 안 봤다. `.rvpt` 는 이미 전 skin에서 쓰이는 컴포넌트라 위험은 낮지만, 구현 후 skin 4종 캡처 필요.
- 실제 상호작용(hover 시 `.elapsed` 가리기와 새 줄의 간섭)은 목업이 아니라 실화면에서 확인해야 한다.
- 대비 수치는 캡처 픽셀 기준이라 안티에일리어싱 가장자리는 제외하고 점 중심값으로 쟀다.

### 2026-08-28T18:35:31+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-08-28 18:35:31 KST - lead]`

`[2026-08-28 18:38 KST - lead]`

개선했습니다. 커밋 `5f6bcb5`, 전체 스위트 **1511건 통과**.

**036 검토 셋이 화면의 거짓말을 잡아냈습니다.** 화면이 못 보는 게 아니라 **반대로 말하고 있었습니다** — 초록 점 3개 중 2개가 거짓이었습니다. 점이 재는 게 "이 요청의 진전"이 아니라 **"이 요청을 잡고 있는 세션의 맥박"** 이라, 제가 다른 일을 하고 있어도 잡아 둔 요청이 전부 초록으로 뜁니다. 작년에 `REQ-20260827-074-62x6` 가 CLI 만 고치고 **화면을 두고 갔습니다.**

그리고 판정 축이 둘뿐이었습니다 — "누가 붙어 있나", "문서가 움직였나". 셋째 축 **"산출물이 도착했나"** 가 없어서, 워커가 구현을 끝내고 완료 보고까지 적었는데 코드가 워크트리 안에만 있는 경우를 아무도 못 잡았습니다.

이번에 닫은 것:

- **`s9 loose` 에 셋째 축을 꽂았습니다.** `worktree_state().ahead` 는 이미 계산되고 있었는데 아무도 안 불렀을 뿐입니다. 지금 **4건이 매 턴 제 앞에 뜹니다** — 붙여 넣을 명령까지 함께
- **끝난 문서가 클레임으로 되살아나지 않습니다.** 13:35 에 done 된 문서가 훅의 클레임으로 살아나 그 뒤 위임 **네 건을 전부 빨아들였습니다.** 화면은 끝난 문서를 "에이전트 4명 작업 중"으로, 정작 일하던 문서를 "아무도 없음"으로 그렸습니다. vault 에 좀비 `running` 이 28건 쌓여 있던 이유입니다
- **`cmd_commit_note` 가 남의 커밋을 읽고 있었습니다.** 워크트리에서 커밋하면 본 저장소의 마지막 커밋을 읽어 **엉뚱한 REQ 에 "커밋됐다"를 붙입니다** — 그 함수가 주석에서 피하려 한 "거짓 완료" 그 자체입니다
- **워커 미커밋을 sweep 이 대신 박습니다.** 오전에 봉투에 git 을 넣어 줬는데도 안 했으니, 규율이 아니라 장치로

함께 들어간 것: **026**(첨부 폴더 정식 id, 3개 합침 — done), **029**(시간대 고르기 — `Etc/GMT+9` 가 실제로 UTC−9 라 이름만으로는 백이면 백 틀리게 고릅니다), **021**(축약 해석을 작성 시각 기준으로 — 렌더 시각 기준은 298건 중 208건을 다른 문서로 풀었습니다).

남은 것: **035**(그래프, designer 구현 중) · **025**(워크트리 가지 합치면 닫힘) · **036 화면 몫**(점의 근거를 세션 맥박에서 요청 진전으로, 멈춘 카드에 정도를 글자로). 워크트리 4개는 035 가 끝나는 대로 하나씩 합치겠습니다.

## History
- 2026-08-28T12:40:03+09:00 created by nicehugepark (status: open)
- 2026-08-28T12:40:28+09:00 status: open -> in-progress (by nicehugepark) — 다음 세션을 기다리지 않고 이번 턴에서 처리
- 2026-08-28T13:35:53+09:00 status: in-progress -> done (by nicehugepark) — 목표 충족 근거: - REQ-20260827-078-62x6 done (스트림 자리 이동 검증 — users/nicehugepark/streams 66개, 공용 0개, 관련 테스트 52건) - REQ-20260828-011-62x6 done (워크트리 2·3단계 구현·스위치 on, TDD 7건) - REQ-20260827-083-62x6 blocked → review (designer: 판 높이 실측·몸통 기준 fit·깊이 시차를 팬이 아니라 거리에. 손잡이 클릭 0%→97%, 전체 보기 긴축/짧은축 1.24→1.05) - REQ-20260828-012-62x6 → review (비밀 키 화면), REQ-20260828-013-62x6 → review (스트림 스위치 화면) - 커밋 d2f1b14. 전체 스위트 1356건 통과. 리드가 전체 보기 화면을 직접 캡처해 1.05 확인. 부수: 앞선 세션들이 커밋하지 못하고 남긴 화면 작업(review 대기 건들)도 이 커밋에 함께 담아 미커밋 상태를 해소했다 — 그대로 두면 누군가의 git 한 줄에 사라진다.
