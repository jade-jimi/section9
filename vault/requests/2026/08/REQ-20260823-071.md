---
id: REQ-20260823-071
type: request
title: 문서뷰 스트림 접힘 버그
summary: docs 뷰에서 15초 catalog 폴링이 변화 감지 시 뷰 전체를 재구축해 열려 있던 reqstream 터미널이 접히고 깜박임 — 열림 상태/스크롤 보존 필요
status: done
size: S
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: b443a84e
agents: ["frontend-developer"]
project: section9
relates: ["REQ-20260823-069", "REQ-20260823-076"]
tags: ["dashboard", "test", "terminal"]
created: 2026-08-23T21:48:07+09:00
status_since: 2026-08-23T21:59:16+09:00
updated: 2026-08-25T17:22:16+09:00
---

## Original

http://127.0.0.1:9909/#docs/REQ-20260823-070 스트림 로그를 보는 중 화면이 자꾸 전체 리프레시되어 깜박이며 접힘. 원인: refreshCatalog(true)→render()→renderDocs()가 #view.innerHTML 전체 재구축, details.reqstream이 닫힌 채 재생성되고 폴링 타이머도 소멸.

## Notes

### 2026-08-23T21:53:34+09:00 tdd (agent: frontend-developer, by nicehugepark)

### TDD 시나리오 (문서뷰 스트림 접힘 버그)
- [ ] S1. 정상(보존): reqstream 터미널을 열고 위로 스크롤한 상태에서 catalog 변화 → 15초 백그라운드 재렌더 후에도 details 열림 유지 + 터미널 스크롤 위치 유지 + live follow 폴링 지속(이벤트 카운트 계속 증가)
- [ ] S2. 정상(갱신 유지): 문서에 노트 추가 → doclist·메타·본문에 새 내용이 반영된다 (갱신을 끄는 방식 금지)
- [ ] S3. 경계(무변화): 선택 문서의 updated가 안 바뀐 재렌더 → 뷰어 재로드 스킵(깜빡임·스크롤 튐 없음), doclist만 갱신
- [ ] S4. 경계(뷰어 스크롤): 본문을 스크롤해 읽는 중 백그라운드 갱신 → 뷰어 스크롤 위치 보존 (기존엔 매번 top으로 튐)
- [ ] S5. 회귀(정리/기존 동작): 다른 문서 선택·탭 이탈 시 기존 details 제거로 폴링 타이머 자멸(누수 없음), 사용자가 직접 문서를 클릭해 여는 경로는 기존처럼 상단부터·접힘 기본

### 2026-08-23T21:57:21+09:00 tdd (agent: frontend-developer, by nicehugepark)

### TDD 결과 — 전 시나리오 통과 (하니스 12/12 PASS, s071-harness.png)
- [x] S1 (details 열림 유지 + 터미널 스크롤(상단 읽기 위치) 보존 + live 폴링 지속으로 이벤트 증가 — 15s catalog 틱 경과 후 확인)
- [x] S2 (노트 tick 증가가 뷰어 본문에 반영, doclist 갱신 유지 — 갱신 끄지 않음)
- [x] S3 (catalog 동결 + 강제 재렌더 → h1 DOM 노드 보존 = 뷰어 재로드 스킵, details 생존)
- [x] S4 (뷰어 본문 스크롤 위치 보존)
- [x] S5 (다른 문서 클릭 → details 제거로 폴링 타이머 자멸 경로 확인, 새 문서는 상단부터·접힘 기본)
### 구현 (web/index.html)
1) renderDocs(): #view에 .docs+#viewer 구조가 이미 있으면 doclist.innerHTML만 교체 — 뷰어 DOM(터미널·타이머) 파괴 안 함. loadDoc은 bg=true로 호출.
2) loadDoc(id, bg): viewer.dataset.doc/updated 기록. bg+같은 문서+updated 무변화 → 재로드 스킵. bg+같은 문서 재로드 시 viewer.scrollTop과 열린 reqstream의 {scrollTop, atBottom} 캡처 → 재구축 후 det.__restore 전달 + det.open=true(toggle 핸들러가 로드·폴링 재개). 404 시 dataset 리셋.
3) reqstream toggle 핸들러 firstLoad 스크롤: __restore가 있고 하단이 아니었으면 저장 위치 복원, 아니면 기존대로 하단.
사용자 클릭 경로는 bg 미전달 → 기존 동작(상단부터·접힘) 보존. 검증: 스크래치패드 스텁(catalog updated 자동 bump + live reqstream, 실제 web/index.html 서빙) + s9 shot 실브라우저 하니스(가상시간으로 15s 틱 2회 경과 관찰). 실서버 :9909 Docs 뷰 회귀 스크린샷(s071-real-docs.png) 정상.

### 2026-08-23T21:57:56+09:00 response (agent: frontend-developer, by nicehugepark)

### 수정 완료 보고 (frontend-developer)
원인(15s refreshCatalog → renderDocs 전체 재구축 → details 접힘·타이머 소멸)을 뷰어 보존 방식으로 해소. web/index.html 3개 지점: (1) renderDocs가 기존 .docs 구조 존재 시 doclist만 innerHTML 교체 — 뷰어·터미널·폴링 타이머 비파괴, (2) loadDoc(id, bg) — 백그라운드 경로에서 updated 무변화면 재로드 스킵, 같은 문서 재로드면 뷰어 스크롤+reqstream {열림, 스크롤, atBottom} 캡처 후 det.__restore로 복원·det.open=true로 폴링 재개, (3) toggle 핸들러 firstLoad가 복원 위치 존중. 사용자 클릭 경로는 bg 미전달로 기존 동작(상단·접힘) 보존, 문서 전환·탭 이탈 시 타이머 자멸 경로 유지(누수 없음). 실브라우저 하니스 12/12 PASS(s071-harness.png, 가상시간으로 15s 틱 경과+live 폴링 관찰), 실서버 Docs 회귀 정상(s071-real-docs.png). AC 1~4 전부 충족.

## History
- 2026-08-23T21:48:07+09:00 created by nicehugepark (status: open)
- 2026-08-23T21:48:15+09:00 status: open -> in-progress (by nicehugepark) — 070 구현 에이전트(frontend-developer)가 web/index.html 작업 중이라 파일 충돌 방지 위해 동일 에이전트에 추가 위임
- 2026-08-23T21:59:16+09:00 status: in-progress -> done (by nicehugepark) — 리드 검수 통과 — doclist만 교체·updated 무변화 스킵·터미널 열림/스크롤/atBottom 복원, 하니스 12/12(가상시간 15s 틱) + 실서버 회귀 확인
