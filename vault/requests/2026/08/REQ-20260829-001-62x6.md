---
id: REQ-20260829-001-62x6
type: request
title: 게이트 시험이 조용히 건너뛴다
summary: 워크트리에서 .git 이 파일이라 훅 설치 확인이 skip 된다 — 무인 워커가 커밋하는 바로 그 자리
goal: 워크트리에서 python3 tests/ commit_gate 를 돌렸을 때, pre-commit 훅이 없으면 skip 이 아니라 실패한다
status: done
size: S
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: b8b2d515
sessions: ["bf1593d1", "a53ebee3", "f30ed2c4", "b0cce452", "b8b2d515"]
agents: ["lead:claude-opus-5"]
contributions: [{"actor": "lead:claude-opus-5", "item": "`[2026-08-29 09:12:14 KST - lead]` 전체 스위트 결과를 기다리는 중입니다.", "started": "2026-08-29T09:12:15+09:00", "ended": "2026-08-29T09:12:15+09:00", "result": "done", "transcript": ""}, {"actor": "lead:claude-opus-5", "item": "`[2026-08-29 09:36:44 KST - lead]`", "started": "2026-08-29T09:36:44+09:00", "ended": "2026-08-29T09:36:44+09:00", "result": "done", "transcript": ""}]
project: section9
parent: REQ-20260827-012-62x6
relates: ["REQ-20260829-005-62x6"]
relates_why: {"REQ-20260829-005-62x6": "둘 다 워크트리의 git 자리를 지어내면 틀리는 함정 — 하나는 .git 이 파일이라 훅 확인이 skip 되고, 하나는 GIT_DIR 이 -C 를 이겨 공용 config 를 뒤집는다"}
tags: ["sync", "audit", "test"]
created: 2026-08-29T00:02:37+09:00
status_since: 2026-08-29T09:46:00+09:00
updated: 2026-08-29T09:46:00+09:00
priority: 50
---

## Original

승인 메모의 실동작 확인을 재현하다 발견. 워크트리에서 python3 tests/ commit_gate 를 돌리면 G7(pre-commit 이 s9-guard 를 부르는가)이 "git repo 아님" 으로 skip 된다 — 워크트리의 .git 은 디렉토리가 아니라 파일이기 때문이다. 훅 자체는 공용 git 디렉토리에 있어 실제로는 동작하지만, 훅이 사라져도 워크트리 세션의 테스트는 OK 를 낸다. 무인 워커가 커밋하는 자리가 바로 워크트리다. 021 이 없애려던 조용한 성공이 게이트의 시험 쪽에 그대로 남아 있었다. 같은 가정을 쓰는 자리: tests/test_streams_untracked.py(클래스 전체 skip), bin/s9-install(워크트리에서 git repo 아님 이라며 훅 설치 생략).

## Notes

### 2026-08-29T00:03:25+09:00 tdd (by nicehugepark)

- [x] G7. 워크트리에서도 훅 설치 확인이 산다 — .git 이 파일이어도 skip 하지 않고, 저장소인데 pre-commit 이 없으면 **실패**한다
- [x] G11. 훅 자리는 git 에게 묻는다 — 임시 저장소 + 링크된 워크트리에서, 손으로 이은 <워크트리>/.git/hooks 는 없고 git_hooks_dir 은 공용 자리를 찾는다
- [x] N. streams 미러 검사가 워크트리에서 통째로 사라지지 않는다
- [x] 회귀: 기존 G1~G10 그대로 통과

### 2026-08-29T09:11:52+09:00 proposal (by nicehugepark)

범위 밖(bin/) 제안 — 같은 가정이 남은 자리 셋. 이 세션은 web·vault·tests 만 고치므로 손대지 않고 적어 둔다.

① bin/s9-install:88 install_git_hooks() 가 hooks_dir 을 os.path.join(ROOT, '.git', 'hooks') 로 짓는다. 지금은 조용히 넘어간다 — ROOT 가 main_worktree_root() 로 본 저장소에 되맞춰지기 때문이다(REQ-20260828-014-62x6 이 다른 이유로 넣은 방어). 즉 원 보고의 '워크트리에서 훅 설치를 생략한다'는 지금 코드에서는 재현되지 않는다. 다만 자리를 지어내는 것 자체는 남아 있어, core.hooksPath 를 쓰거나 S9_ROOT 가 워크트리를 가리키면 다시 '저장소 아님'으로 읽힌다. 고칠 때는 s9-doctor 의 git_hooks_dir() 을 그대로 쓰면 된다(git rev-parse --git-path hooks).

② bin/s9:4805 sync_mode() 가 os.path.isdir(ROOT/'.git') 로 저장소 여부를 본다. 워크트리를 S9_ROOT 로 쓰는 인스턴스에서는 동기화가 조용히 꺼진다 — 꺼졌다는 말이 어디에도 안 나온다. git rev-parse --is-inside-work-tree 로 물어야 한다.

③ bin/s9-doctor 의 git_hooks_dir() 은 물려받은 GIT_DIR 을 벗기지 않고 subprocess 를 부른다. 훅 아래에서 부르면 cwd 가 무시된다(REQ-20260829-005-62x6). 지금은 GIT_DIR 이 워크트리 것이어도 hooks 는 공용 자리로 풀려 답이 같지만, 임시 저장소를 물을 때는 틀린다 — 그래서 tests 쪽에서는 벗기고 부른다.

### 2026-08-29T09:12:15+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-08-29 09:12:14 KST - lead]` 전체 스위트 결과를 기다리는 중입니다.

### 2026-08-29T09:17:12+09:00 commit (by nicehugepark)

커밋 fc94af1 — 워커 작업 자동 보존: REQ-20260829-001-62x6

### 2026-08-29T09:36:44+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-08-29 09:36:44 KST - lead]`

전체 스위트가 아직 도는 중입니다(브라우저 검증 테스트가 포함돼 느립니다). 그동안 확인한 것:

- 커밋 `fc94af1`(→ `27f5855`)에 이번 REQ의 구현이 이미 들어가 있습니다 — 훅 자리를 `<ROOT>/.git/hooks` 로 지어내던 것을 `bin/s9-doctor` 의 `git_hooks_dir()`(= `git rev-parse --git-path hooks`)에 묻도록 바꾸고, 건너뛰는 경우를 "저장소가 아닐 때" 하나로 좁혔습니다. G11(`HooksDirIsAskedOfGit`)은 실제 임시 저장소 + 링크된 워크트리를 만들어 검사합니다.
- 대상 모듈 25건 전부 통과했고, 특히 `StreamsUntracked` 가 이 워크트리에서 **더 이상 통째로 skip되지 않고** 실제로 돕니다(이전에는 `.git` 이 파일이라 클래스 전체가 사라졌습니다).
- `tests/test_commit_gate.py` 에 쓰이지도 않으면서 매 실행 `/tmp/s9gate-*` 를 하나씩 남기던 `mkdtemp` 한 줄을 제거했습니다(REQ-20260829-003-62x6 이 세운 규칙과 충돌하던 자리).

전체 스위트 결과가 나오는 대로 커밋하고 review로 올리겠습니다.

### 2026-08-29T09:45:50+09:00 commit (by nicehugepark)

커밋 7e68de1 — 워커 작업 자동 보존: REQ-20260829-001-62x6

### 2026-08-29T09:46:00+09:00 response (by nicehugepark)

TDD 검증: G7·G11·N·회귀 전부 초록. 워크트리에서 python3 tests/ commit_gate 를 돌리면 훅 없는 링크 워크트리가 no-repo 가 아니라 missing 으로 읽혀 실패한다. 본 저장소 전체 스위트 1748건 초록(test_short_ref 등 워크트리에서 나던 실패는 그 자리에 vault 가 없어 나던 환경 몫이었고 본 저장소에서는 나지 않는다). 병합 27f5855.

## History
- 2026-08-29T00:02:37+09:00 created by nicehugepark (status: open)
- 2026-08-29T00:02:41+09:00 status: open -> in-progress (by nicehugepark) — 워크트리에서 게이트 설치 확인이 조용히 skip 되는 것을 재현. .git 이 파일인 자리를 git 에게 물어 해결한다.
- 2026-08-29T09:46:00+09:00 status: in-progress -> done (by nicehugepark) — 워크트리에서도 게이트 시험이 산다 — 훅 자리를 지어내지 않고 git 에게 묻고, 건너뛰는 경우는 저장소가 아닐 때 하나로 좁혔다. 링크된 워크트리를 실제로 만들어 훅 없는 상태가 missing 으로 읽히는 것을 확인했다. streams 미러 검사도 같은 가정이라 함께 고쳤다. 전체 1748건 초록.
