---
id: REQ-20260826-027-62x6
type: request
title: 판정 화면 배경 먼저
summary: 판정을 요구하는 자리가 무엇에 대한 건인지 먼저 말하도록 — 요약을 확인 포인트 위로
status: done
size: S
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: dc4f4d76
agents: ["sub:designer:abda90fd"]
contributions: [{"actor": "sub:designer:abda90fd", "item": "판정 화면이 이제 배경을 먼저 말한다. `/home/sjpark1/section9/web/index.html` 만 수정했고 커", "started": "2026-08-26T22:17:35+09:00", "ended": "2026-08-26T22:17:35+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/dc4f4d76-3d54-4136-8c5d-a99d692ba5a8/subagents/agent-abda90fd15bd439d3.jsonl"}]
project: section9
derived_from: REQ-20260826-023-62x6
relates: ["DOC-20260826-015-62x6"]
tags: ["test", "workflow", "dashboard"]
created: 2026-08-26T22:03:31+09:00
status_since: 2026-08-26T22:46:38+09:00
updated: 2026-08-26T22:46:38+09:00
priority: 50
---

## Original

반려 사유에서 갈라져 나온 화면 작업 — "이 건은 무슨 내용에 대한 리뷰인지 모르겠고, 리뷰 확인 포인트도 뭔말인지 모르겠다"(REQ-20260826-023 반려, 2026-08-26 21:39).

설계는 DOC-20260826-015 에 확정돼 있고 계약은 tests/test_review_context.py 가 잡는다(무인 워커 세션 13b79042 가 남긴 것). 그 워커의 web/index.html 구현은 리드의 `git checkout web/index.html` 로 유실됐다 — 동시 작업 충돌의 2차 피해(REQ-20260826-021 에 사례 기록). 설계와 테스트가 남았으므로 그것을 규격으로 다시 구현한다.

## Notes

### 2026-08-26T22:16:51+09:00 designer (by nicehugepark)

판정 화면 배경 먼저 — 구현 완료 (web/index.html, 미커밋).

구조 (DOC-20260826-015-62x6 규칙 그대로, tests/test_review_context.py 16건 전부 통과)
1) Board 판정 카드: .judge 안에 .rvpt.what(무엇을 = r.summary) 을 확인 요청 위에. 판정 카드에만 — cardHTML 본문 템플릿에는 summary 를 넣지 않았다.
2) 문서 뷰어 .gate: .gate-w(무엇을) 를 .gate-b 앞에. m.summary 가 없으면 what='' 이라 빈 줄조차 남지 않는다(캡처로 확인).
3) gateNote(note) 신설: esc → 분할 → linkifyIds 순. 원문 치환 없이 ①-⑳ 와 ' (n) ' 앞에서만 자르고, 번호가 1,2,3… 순서일 때만 갈래로 본다. lookbehind 미사용. 현재 회차 gateNote(cur.note) · 이전 회차 gateNote(r.note) 로 한 함수를 공유.

잉크·간격 선택 근거
- .rvpt.what .rvcap = --muted. --c-review 는 '확인 요청' 캡션이 이미 쓰고 있어 두 번 쓰면 강조가 둘이 된다(판정 블록의 가장 큰 소리는 하나). --faint(2.9:1)는 캡션 잉크로 이미 반려된 값(REQ-20260825-081-62x6). --muted 는 종이톤 6:1대 — 조용하되 읽힌다. 위계는 색이 아니라 순서 + 캡션 잉크 한 단 차이로 만든다.
- .rvpt.what .wtx = line-clamp 2. 카드는 '무슨 건인가'만 집어주면 되고 전문은 문서 뷰어가 편다. 3줄이면 확인 포인트와 높이가 같아져 어느 쪽이 배경인지 사라진다.
- .gate-w = --muted 12px + 하단 hairline 1px + 8px 패딩. .gate-b(--text 12.5px)보다 한 단 낮아 배경으로 읽힌다. 면·라운드·그림자 없이 선 하나로 배경과 판단 요구를 가른다(계기판 언어).
- .gate-p 갈래 간격 8px. 갈래 안 줄간이 약 19px 이라 7px 로는 붙어 보이고, 8px 이면 베이스라인 간격 약 27px 로 확실히 갈라진다. 불릿·번호 치환은 없다 — 원문의 ①/(1) 이 그대로 마커다.
- calm skin 보정 2건: calm 은 .rvpt margin 을 0 으로 눕혀 배경 줄과 확인 요청이 한 덩어리 문장 벽이 됐다 → .judge .rvpt.what{margin-bottom:10px}. 또 calm 의 .judge .rvcap{--c-review} 가 무엇을 캡션까지 물들여 → .judge .rvpt.what .rvcap{--muted} 로 복원. 베이스는 건드리지 않고 override 로만.

검증 (포트 9909 실캡처, scratchpad/rvctx/)
board-ledger-paper.png · board-ledger-carbon.png · board-calm-paper.png · doc023-ledger-paper.png · doc023-ledger-carbon.png · doc013-blocked-paper.png · gatenote-probe.png(갈래 표본 8건 실브라우저 실행) · gate-empty-probe.png(요약 없는 문서 = 빈 줄 없음).
REQ-20260826-023-62x6 문서 뷰어에서 확인 포인트가 머리말 + (1) + (2) 세 갈래로 갈라지는 것, '테스트 (3) 건'·번호 역순·평문은 끊기지 않는 것, script 태그 주입이 문자로 escape 되는 것, 갈래 안 문서 id 링크가 유지되는 것 모두 실브라우저에서 확인.
인접 스위트 goto_dispatch·oldcode_banner·dep_arrow·priority·review_gate 통과.

한계
- 카드의 확인 포인트(.rvpt) 자체는 ledger 스킨에서 클램프가 없어 긴 note 가 컬럼을 늘린다(REQ-20260826-009-62x6 카드). 기존 동작이고 이번 계약 밖이라 손대지 않았다 — 판단 요구를 자르는 것은 별건으로 다뤄야 한다.
- gateNote 는 갈래 표기를 ①-⑳ 와 (1)-(99) 두 가지만 본다. '1.' '가.' 표기는 갈래로 보지 않는다(문장 끝 마침표와 구분 불가).
- 커밋하지 않았다. web/index.html 외 변경 없음.

### 2026-08-26T22:17:35+09:00 subagent (agent: sub:designer:abda90fd, by nicehugepark)

판정 화면이 이제 배경을 먼저 말한다. `/home/sjpark1/section9/web/index.html` 만 수정했고 커밋하지 않았다. 작업 시작 직전·저장 직전 `git status --short web/index.html` 확인 — 남의 미커밋 변경 없었다.

### 무엇을 바꿨나

**1) Board 판정 카드** — `cardHTML` 의 `acts` 블록에 `.rvpt.what` 을 확인 요청 **위에** 넣었다. `무엇을` 캡션 + `r.summary`. 카드 본문 템플릿(`return \`<div class="card"...\``)에는 summary 를 넣지 않았다 — 판정 카드에만 붙는다.

**2) 문서 뷰어 `.gate`** — `const what = m.summary ? ... : ""` 을 `.gate-b` 앞에 삽입. 요약이 없으면 문자열 자체가 비어 빈 줄조차 남지 않는다(캡처로 확인).

**3) `gateNote(note)` 신설** (linkifyIds 바로 뒤) — `esc(note)` → 분할 → 갈래별 `linkifyIds`. 원문 치환 없이 `①-⑳` 와 ` (n) ` 앞에서만 자르고, **번호가 1,2,3… 순서일 때만** 갈래로 본다. lookbehind 미사용. `gateNote(cur.note)`(현재 회차)와 `gateNote(r.note)`(이력) 이 같은 함수를 쓴다.

### 잉크·간격을 왜 그렇게 골랐나

- **`.rvpt.what .rvcap` = `--muted`.** `--c-review` 는 '확인 요청' 캡션이 이미 쓰고 있다 — 두 번 쓰면 강조가 둘이 되고, 강조가 둘이면 없는 것과 같다. `--faint`(2.9:1)는 캡션 잉크로 이미 반려된 값(REQ-20260825-081-62x6). `--muted` 는 종이톤 6:1대. 위계는 색이 아니라 **순서 + 캡션 잉크 한 단 차이**로 만들었다.
- **`.rvpt.what .wtx` = line-clamp 2.** 3줄이면 확인 포인트와 높이가 같아져 어느 쪽이 배경인지 사라진다. 카드는 "무슨 건인가"만 집어주고 전문은 문서 뷰어가 편다.
- **`.gate-w` = `--muted` 12px + 하단 hairline + 8px 패딩.** `.gate-b`(`--text` 12.5px)보다 한 단 낮아 배경으로 읽힌다. 면·라운드·그림자 없이 선 하나로 가른다.
- **`.gate-p` 갈래 간격 8px.** 갈래 안 줄간이 ≈19px 이라 7px 로는 붙어 보이고, 8px 이면 베이스라인 ≈27px 로 확실히 갈라진다. 불릿·번호 치환 없음 — 원문의 `①`/`(1)` 이 그대로 마커다.
- **calm skin 보정 2건 (override만, 베이스 무수정).** calm 이 `.rvpt{margin:0}` 으로 눕혀 배경 줄과 확인 요청이 한 덩어리 문장 벽이 됐다 → `margin-bottom:10px`. calm 의 `.judge .rvcap{--c-review}` 가 `무엇을` 캡션까지 물들여 → `--muted` 복원.

### 검증

- `python3 tests/ review_context` **16/16 통과**. 인접 스위트 `goto_dispatch`·`oldcode_banner`·`dep_arrow`·`priority`·`review_gate` 통과. **전체 569건 OK (skipped=1)**.
- 포트 9909 실캡처 (`/home/sjpark1/section9/scratchpad/rvctx/`):
  - `board-ledger-paper.png` · `board-ledger-carbon.png` · `board-calm-paper.png` — 판정 카드의 `무엇을`(muted) → `확인 요청`(review 잉크) 순서와 2줄 클램프
  - `doc023-ledger-paper.png` · `doc023-ledger-carbon.png` — REQ-20260826-023-62x6 의 확인 포인트가 머리말 + (1) + (2) 세 갈래로 갈라짐
  - `doc013-blocked-paper.png` — blocked 문서의 대기 사유 gate
  - `gatenote-probe.png` — 실브라우저 JS 엔진에서 표본 8건: ①②③ 분할 ✓, (1)(2)(3) 분할 ✓, "테스트 (3) 건" 안 끊김 ✓, 번호 역순 안 끊김 ✓, 평문 안 끊김 ✓, `(1)` 하나뿐 안 끊김 ✓, `<script>` 주입 escape ✓, 갈래 안 문서 id 링크 유지 ✓
  - `gate-empty-probe.png` — 요약 있음/없음 대조: **없을 때 빈 줄 없음** ✓
- 근거는 `s9 note REQ-20260826-027-62x6 --label designer` 로 남겼다.

### 한계

- **카드의 확인 포인트(.rvpt) 자체는 ledger 스킨에서 클램프가 없다.** REQ-20260826-009-62x6 카드의 긴 note 가 컬럼을 20줄 넘게 늘린다(`board-ledger-paper.png` 에서 보인다). 기존 동작이고 이번 계약 밖이라 손대지 않았다 — 판단 요구를 자르는 것은 배경을 자르는 것과 다른 판단이라 별건으로 다뤄야 한다.
- `gateNote` 는 `①-⑳` 와 `(1)-(99)` 두 표기만 본다. `1.` `가.` 는 문장 끝 마침표와 구분이 안 돼 갈래로 보지 않는다.
- **테스트 결함은 발견하지 못했다.** `test_gate_note_behaviour` 의 파이썬 참조 구현과 내 JS 는 표본 8건에서 동일하게 동작했다. 다만 계약 하나는 짚어둘 만하다 — `test_card_judge_shows_summary` 의 정규식이 `"review"\s*\?` 로 되어 있어 `const acts = ...` 와 `?` 사이에 주석을 넣을 수 없다. 주석을 `const acts` **위**로 올려 해결했고, 다음 사람이 같은 곳에서 걸릴 수 있다.

## History
- 2026-08-26T22:03:31+09:00 created by nicehugepark (status: open)
- 2026-08-26T22:03:39+09:00 status: open -> in-progress (by nicehugepark) — designer 위임 — 설계(DOC-20260826-015-62x6)와 계약(tests/test_review_context.py)이 이미 있으므로 구현과 잉크 선택만 남았다.
- 2026-08-26T22:23:52+09:00 status: in-progress -> review (by nicehugepark) — 확인해 주실 것 (F5 후): (1) Board 의 review 카드에서 `무엇을` 한 줄이 `확인 요청` 위에 있는지 — 무슨 건인지 먼저 읽히는지. (2) 문서를 열었을 때 확인 포인트가 갈래별로 줄이 나뉘어 몇 개를 판단해야 하는지 눈에 잡히는지. 이 문서(REQ-20260826-023-62x6)가 (1)(2) 두 갈래라 보기 좋습니다. (3) 배경 줄이 확인 요청보다 조용한지 — 너무 조용해 안 읽히면 그것도 실패입니다.
- 2026-08-26T22:46:38+09:00 status: review -> done (by nicehugepark) — 대시보드 승인 [via dashboard]
