---
id: REQ-20260828-041-62x6
type: request
title: 멈춘 것을 사람이 깨운다
summary: in-progress 카드에서 사람이 직접 깨우는 손잡이 — 036 은 보여주기만 하고 깨우기를 안 만들었다
goal: 멈춘 카드에서 사람이 눌러 그 요청을 다시 굴릴 수 있고, 이미 누가 붙어 있으면 겹쳐 띄우지 않는다
status: review
size: M
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: 81526441
sessions: ["b8fda8f9", "81526441"]
agents: ["sub:backend-developer:a0f852dd", "sub:designer:ab64fee2", "sub:subagent:a3779f02", "sub:subagent:a6f24e7a", "sub:designer:ad036cb4", "sub:subagent:a1688015", "sub:frontend-developer:a1dfeb13", "sub:frontend-developer:afaa381a", "sub:backend-developer:a4a2de7b", "worker:wake", "lead:claude-opus-5", "sub:subagent:a9e1244a", "sub:subagent:a53bb991", "sub:subagent:ab52c34f"]
contributions: [{"actor": "sub:backend-developer:a0f852dd", "item": "깨우기 - 위임 귀속 먼저", "started": "2026-08-28T21:35:09+09:00", "ended": "2026-08-28T22:15:16+09:00", "result": "done", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/tasks/a0f852ddab00a27f7.output"}, {"actor": "sub:designer:ab64fee2", "item": "코드 경로를 눌러서 본다", "started": "2026-08-28T21:35:32+09:00", "ended": "2026-08-29T17:29:30+09:00", "result": "failed", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/tasks/ab64fee2fe18971af.output", "reason": "기록 자리가 사라졌다 (transcript 경로가 없다)"}, {"actor": "sub:subagent:a3779f02", "item": "Appending backend report note to REQ-20260828-041-62x6", "started": "2026-08-28T22:12:54+09:00", "ended": "2026-08-28T22:12:54+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/subagents/agent-a3779f02964b397f6.jsonl"}, {"actor": "sub:subagent:a6f24e7a", "item": "Recording TDD scenario results on REQ-20260828-041-62x6", "started": "2026-08-28T22:13:28+09:00", "ended": "2026-08-28T22:13:28+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/subagents/agent-a6f24e7aea868b893.jsonl"}, {"actor": "sub:backend-developer:a0f852dd", "item": "`[2026-08-28 22:15:41 KST - backend-developer]`", "started": "2026-08-28T22:15:16+09:00", "ended": "2026-08-28T22:15:16+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/subagents/agent-a0f852ddab00a27f7.jsonl"}, {"actor": "sub:designer:ad036cb4", "item": "깨우기 버튼 화면 몫", "started": "2026-08-28T22:41:10+09:00", "ended": "2026-08-28T23:20:19+09:00", "result": "done", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/tasks/ad036cb4bddbab35b.output"}, {"actor": "sub:subagent:a1688015", "item": "Moving REQ-20260828-041-62x6 to review", "started": "2026-08-28T23:19:47+09:00", "ended": "2026-08-28T23:19:47+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/subagents/agent-a16880153c77d768b.jsonl"}, {"actor": "sub:designer:ad036cb4", "item": "`[2026-08-28 23:19:46 KST - designer]`", "started": "2026-08-28T23:20:19+09:00", "ended": "2026-08-28T23:20:19+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/subagents/agent-ad036cb4bddbab35b.jsonl"}, {"actor": "sub:frontend-developer:a1dfeb13", "item": "깨우기 화면 경로 점검", "started": "2026-08-29T16:39:56+09:00", "ended": "2026-08-29T17:29:30+09:00", "result": "failed", "transcript": "/tmp/claude-1000/-home-sjpark1-section9-state-worktrees-w-828-041-62x6-1787988997/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/tasks/a1dfeb1341b8a355f.output", "reason": "기록 자리가 사라졌다 (transcript 경로가 없다)"}, {"actor": "sub:frontend-developer:afaa381a", "item": "깨우기 화면 몫 라운드2", "started": "2026-08-29T17:33:12+09:00", "ended": "2026-08-29T17:58:50+09:00", "result": "stalled", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/81526441-334e-48ad-a828-ceb1a3bfd17c/tasks/afaa381a9fa4adcce.output", "reason": "205초째 진전 없음 (기준 180초)"}, {"actor": "sub:backend-developer:a4a2de7b", "item": "깨우기 실효 검증 라운드2", "started": "2026-08-29T17:33:38+09:00", "ended": "2026-08-29T17:52:32+09:00", "result": "stalled", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/81526441-334e-48ad-a828-ceb1a3bfd17c/tasks/a4a2de7b4e68f08e7.output", "reason": "209초째 진전 없음 (기준 180초)"}, {"actor": "worker:wake", "item": "깨우기 재개 워커 0755c082 - 겹침 회피", "started": "2026-08-29T17:38:26+09:00", "ended": "2026-08-29T17:38:26+09:00", "result": "done", "transcript": ""}, {"actor": "lead:claude-opus-5", "item": "`[2026-08-29 17:39:05 KST - lead]`", "started": "2026-08-29T17:39:05+09:00", "ended": "2026-08-29T17:39:05+09:00", "result": "done", "transcript": ""}, {"actor": "sub:backend-developer:a4a2de7b", "item": "`[2026-08-29 18:24:03 KST - backend-developer]`", "started": "2026-08-29T18:27:04+09:00", "ended": "2026-08-29T18:27:04+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/81526441-334e-48ad-a828-ceb1a3bfd17c/subagents/agent-a4a2de7b4e68f08e7.jsonl"}, {"actor": "sub:subagent:a9e1244a", "item": "`[2026-08-29 18:27:06 KST - lead]`", "started": "2026-08-29T18:31:09+09:00", "ended": "2026-08-29T18:31:09+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/81526441-334e-48ad-a828-ceb1a3bfd17c/subagents/agent-a9e1244a37c834842.jsonl"}, {"actor": "sub:subagent:a53bb991", "item": "`[2026-08-29 18:34:33 KST - lead]`", "started": "2026-08-29T18:40:27+09:00", "ended": "2026-08-29T18:40:27+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/81526441-334e-48ad-a828-ceb1a3bfd17c/subagents/agent-a53bb991a38bff12c.jsonl"}, {"actor": "sub:subagent:ab52c34f", "item": "`[2026-08-29 18:52:08 KST - lead]`", "started": "2026-08-29T18:57:14+09:00", "ended": "2026-08-29T18:57:14+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/81526441-334e-48ad-a828-ceb1a3bfd17c/subagents/agent-ab52c34f51fb94bd3.jsonl"}]
project: section9
children: ["REQ-20260829-033-62x6", "REQ-20260829-034-62x6"]
relates: ["REQ-20260829-024-62x6", "REQ-20260829-032-62x6", "REQ-20260829-016-62x6"]
relates_why: {"REQ-20260829-024-62x6": "깨우기와 한 쌍이다 — 같은 카드에서 세우고 깨운다", "REQ-20260829-032-62x6": "겹침이 일어난 자리가 041 이고, 041 의 깨우기 손잡이와 워처가 같은 클레임 판정을 써야 한다", "REQ-20260829-016-62x6": "겹쳐 띄우지 않는다는 같은 계약의 양면 — 041 은 사람이 누른 손잡이가, 016 은 워처가 도는 워커 위에 두 번째 손을 붙이는 자리다 (worker_running 을 두 판정이 함께 쓴다)"}
tags: ["dashboard", "attached", "png", "image", "design", "terminal", "docs"]
created: 2026-08-28T21:07:12+09:00
status_since: 2026-08-29T18:37:26+09:00
updated: 2026-08-29T18:59:15+09:00
priority: 50
---

## Original

사용자(18:04): "in-progress 중인 카드나 문서에 상태체크 기능을 만들고 굳이 프롬프트로 물어보지 않고 진행할 수 있게 하는건 어때?"
사용자(21:05): "멈춤이 2건이나 됐다. 내가 아까전에 사람이 깨울 수 있는 기능이라도 만드는건 어떠냐고 물었었는데 그건 어떻게 됐지?"

**안 만들었다.** REQ-20260828-036 은 "상태체크" 의 **보여주기 절반**만 냈다 — 점의 근거를 고치고, 멈춤 줄을 세우고, 열 머리에 수를 붙였다. 사용자가 물은 것의 나머지 절반, **"굳이 프롬프트로 물어보지 않고 진행할 수 있게"** 는 손대지 않았다. 036 을 done 으로 닫으면서 그 절반이 남았다고 적지도 않았다.

## Notes

### 2026-08-28T21:38:07+09:00 delegate (by nicehugepark)

위임 착수 21:33 — backend-developer 에게 맡겼다: ① 위임 귀속 교정 → ② POST /api/wake

리드가 위임 사실을 문서에 직접 적는다. 지금은 위임 귀속이 세션 등록 목록에서만 고르기 때문에, 리드가 클레임하지 않았거나 훅이 놓치면 **문서에 아무 흔적이 안 남고 화면은 멈춤으로 그린다.** 사용자가 그것 때문에 오늘만 세 번 물어야 했다. 귀속을 고치는 것이 REQ-20260828-041-62x6 의 ① 이고, 그 전까지는 이 한 줄을 손으로 적는다.

### 2026-08-28T21:41:18+09:00 tdd (by nicehugepark)

### TDD 시나리오

━━ ① 위임 귀속 (bin/s9-audit-agent target_req)
- [ ] D1. 리드가 **클레임하지 않은** REQ 를 위임 프롬프트에 지명하면 그 REQ 가 대상이 된다 (지금은 엉뚱한 곳/아무 데도).
- [ ] D2. 지명된 것이 done/cancelled 이면 고르지 않는다 — 세션 클레임으로 폴백 (REQ-20260828-036-62x6 계약 유지).
- [ ] D3. 프롬프트에 **살아 있는 REQ 가 둘 이상** 지명되면 이름으로 고르지 않는다 — 세션 클레임으로 폴백, 그것도 없으면 아무 데도 안 붙인다.
- [ ] D4. 지명이 하나도 없으면 종전 경로(active_reqs → last_req, 소유·상태 확인) 그대로.
- [ ] D5. 존재하지 않는 id 를 지명해도 아무 일이 없다(빈 대상 → 보류 로그).

━━ ② 깨우기 (POST /api/wake · wake_request)
- [ ] W1. 멈춘 in-progress 요청을 깨우면 스폰 경로(_spawn_worker)를 정확히 한 번 지나고 응답이 'spawned'.
- [ ] W2. 이미 누가 붙어 있으면(위임 에이전트의 running 기여가 신선) 거부하고 스폰을 **호출조차 하지 않는다**.
- [ ] W3. 최근 진전이 있으면(멈춤 아님) 거부하고 사람 말로 왜인지 답한다.
- [ ] W4. 일일 캡이 소진됐으면 스폰이 아니라 'capped' + 소진 사실이 응답 문구에 그대로 담긴다.
- [ ] W5. 계정 옵트인이 꺼져 있으면 'off' + 무엇을 켜야 하는지 답한다.
- [ ] W6. in-progress 가 아니면(open/done/review) 거부한다.
- [ ] W7. 킬스위치(S9_AUTO_RESUME_DISABLE)가 켜져 있으면 어떤 경로로도 스폰되지 않는다 — 손잡이는 옆문이 아니다.
- [ ] W8. 없는 문서를 깨우면 400 계열로 거부한다.

### 2026-08-28T22:12:40+09:00 backend-developer (by nicehugepark)

### 서버 몫 완료 (화면 몫 남음 — in-progress 유지)

### ① 위임 귀속: 세션 클레임 대신 **지명된 문서**를 먼저 본다

`bin/s9-audit-agent` — 훅이 받는 `tool_input`(description·prompt)에서 REQ id 를 읽어
그것을 귀속 대상으로 쓴다. 추측이 아니라 **위임을 띄운 리드가 적어 넣은 값**이다.

- 순서: ① 지명된 것 → ② 종전 경로(active_reqs → last_req, 소유·상태 확인).
- **유일하지 않으면 고르지 않는다**: 지명 중 살아 있는(done/cancelled 아닌) 것이
  0개거나 2개 이상이면 이름으로 고르지 않고 ②로 물러난다. ②도 비면 아무 데도 안 붙인다.
  프롬프트에는 배경으로 다른 REQ 가 함께 실리므로, 하나를 골라 running 을 심으면
  남의 문서가 영원히 '진행 중'으로 보인다.
- 짧은 id(`REQ-20260828-039-62x6`)로 지명해도 정식 id 로 풀어 저장한다 — 짧은 형태로
  들어가면 live/클레임 매칭이 깨진다(REQ-20260825-086-62x6).
- 대상이 정해지면 종전대로 `s9 claim`(+ agent transcript) 과 `s9 contrib --result running`
  을 그 문서에 건다. **자동 클레임의 부작용을 재 본 결과**: stalled 판정(`stall_mins`)은
  클레임을 근거로 쓰지 않고 문서 갱신 시각·위임 기여·스폰 마커만 본다 — 클레임이
  늘어도 멈춤 판정은 흐려지지 않는다. 반대로 워처의 중복 스폰 판정(`rework_claimed`)은
  선명해진다(위임한 문서를 워처가 겹쳐 잡지 않는다).
- 세션 로그에 어느 경로로 골랐는지 남긴다: `위임 등록: <REQ> ← sub:designer:xxxx (지명|클레임)`.

### ② 깨우기: `POST /api/wake`

요청 `{"id": "REQ-…"}` (as= 대리는 기존 규칙대로). 응답은 200(ok) / 409(거부),
본문 계약은 넷뿐이다:

    {"ok": bool, "id": str, "mins": int|null, "action": str, "message": str}

action: `spawned`(띄웠다) · `busy`(누가 붙어 있다) · `moving`(아직 조용하지 않다) ·
`capped`(한도/쿨다운) · `off`(계정 옵트인 꺼짐) · `disabled`(킬스위치) ·
`elsewhere`(다른 머신) · `no-cli` · `error` · `missing` · `not-request` · `not-in-progress`.

`message` 는 그대로 화면에 띄워도 되는 사람 말이다. 예:
- "REQ-… 를 깨웠다 — 무인 작업자가 이어받는다. 진행은 카드의 점과 Stream 에서 보인다."
- "이미 designer 가 붙어 있다 (3분 전 움직였다 · 코드 경로를 눌러서 본다). 겹쳐 띄우면 같은 파일에 두 번째 손이 붙는다."
- "오늘 무인 작업자 한도를 다 썼다 (20/20건) — 내일 0시에 풀린다. 지금 올리려면 계정 설정의 auto_resume_global_per_day 를 늘려라"
- "아직 멈춘 게 아니다 — 4분 전 문서가 움직였다. 15분 넘게 조용할 때만 깨운다."

지킨 것:
- **겹쳐 띄우지 않는다.** 깨우기는 화면·CLI 와 **같은 판정**(`stall_mins`)을 지난다 —
  화면이 '멈춤' 이라 그린 카드만 깨울 수 있다. ①에서 고친 위임 귀속이 그 입력이다.
- **스폰 경로는 `_spawn_worker` 하나뿐.** 옵트인·같은 머신·쿨다운·캡을 그대로 지난다.
- **킬스위치를 `_spawn_worker` 안으로 옮겼다.** 종전엔 호출부에만 있어 새 경로를 여는
  사람이 옮겨 적는 것을 잊으면 옆문이 났다. 이제 모든 스폰 경로가 그 앞을 지난다.
- **막힌 이유가 응답에 그대로 실린다.** `_auto_cap_block()` 이 캡 판정을 사람 말 사유로
  돌려주고(문턱 수치는 여전히 한 곳에만), `_spawn_worker(out=…)` 가 그것을 밖으로 낸다.

### 결정: 깨우기는 **스폰만** 한다 — '상태 되돌리기'는 내지 않았다

REQ-20260828-036-62x6 검토에서 내가 "되돌리기(in-progress → open)가 옳다"고 적었다.
**그 판단은 틀렸다.** 코드를 보고 정정한다:
1. `s9 next`(세션 시작 훅이 주입하는 이어받기 경로)와 `stalled` 는 **in-progress 만**
   본다. open 으로 되돌리면 그 요청은 이어받기 후보에서 **사라진다** — "다시 굴린다"의
   정반대다. 036 노트의 "open 으로 가면 work_order 에 다시 들어가 s9 next 가 집는다"는
   사실이 아니었다.
2. in-progress → open 은 `TRANSITIONS` 에 아예 없다(force 로만 가능). 화면 버튼이
   상태기계에 옆문을 내는 셈이다.
3. 멈춤 목록에서도 함께 사라진다 — 한 일 없이 경보만 꺼진다.
일을 세워 두는 몫은 이미 합법 경로가 있다: 카드를 **blocked** 로 옮기기.

남은 물음(사용자 판단): 깨우기가 워처와 **같은 일일 캡(20건)** 을 쓴다. 워처가 그 캡을
먼저 태우면 사람이 누른 버튼이 하루 종일 `capped` 만 낸다. 사람이 누른 몫에 별도 예산을
줄지는 사용자 결정이라 임의로 만들지 않았다 — 지금은 소진 사실을 정직하게 응답에 싣는다.

### 화면 세션에 넘길 것
- 깨우기 버튼은 **멈춤 줄이 있는 카드**에만. 응답의 `message` 를 그대로 보이면 된다
  (ok=false 도 오류가 아니라 설명이다 — 붉은 실패로 그리지 말 것).
- `s9 serve` 를 **재기동해야** `/api/wake` 가 뜬다(지금 도는 서버는 옛 코드다).

### 검증
- `python3 tests/ delegation_target` 11건 OK (D1~D6: 지명 우선·짧은 id·끝난 문서 제외·
  중복 지명 폴백·훅 전체 경로에서 running 이 지명 문서에 남는지)
- `python3 tests/ test_wake` 16건 OK (W1~W8 + API 3건). **무인 스폰은 한 번도 일으키지
  않는다** — Popen 스텁과 킬스위치로 막고 판정만 본다.
- 전체 스위트 1628건: 실패 3건은 전부 `web/index.html` 을 읽는 화면 테스트
  (test_term_mention 2 · test_terminal_table 1)로, **다른 세션이 지금 그 파일을 고치는
  중**이라서다(HEAD 에는 있는 `ccDocs(c)` 가 작업 트리에 없다). 내 변경과 무관하다.

파일: /home/sjpark1/section9/bin/s9 · /home/sjpark1/section9/bin/s9-audit-agent ·
/home/sjpark1/section9/tests/test_delegation_target.py · /home/sjpark1/section9/tests/test_wake.py

### 2026-08-28T22:12:54+09:00 subagent (agent: sub:subagent:a3779f02, by nicehugepark)

Appending backend report note to REQ-20260828-041-62x6

### 2026-08-28T22:12:58+09:00 tdd (by nicehugepark)

### TDD 시나리오 결과 (서버 몫)

- [ ] D1. 지명된 REQ 가 클레임을 이긴다 (짧은 id 도 정식 id 로) — test_delegation_target D1/D1b/D1c
- [ ] D2. 끝난 문서는 지명돼도 안 고른다 — D2
- [ ] D3. 지명이 둘 이상이면 이름으로 안 고른다 / 클레임도 없으면 아무 데도 — D3/D3b
- [ ] D4. 지명 없으면 종전 경로 그대로 (한 인자 호출 호환 포함) — D4/D4b
- [ ] D5. 없는 id 지명은 무해 — D5/D5b
- [ ] D6. 훅 전체 경로에서 running 이 지명 문서에 남고 잡아 둔 문서엔 안 남는다 — D6(신규)
- [ ] W1. 멈춘 것을 깨우면 _spawn_worker 를 reason=wake 로 정확히 한 번
- [ ] W2. 위임 에이전트가 붙어 있으면 거부 (스폰 호출 0회)
- [ ] W3. 최근 진전이 있으면 거부 (action=moving)
- [ ] W4. 일일 캡 소진이 응답 문구에 수치 그대로 / W4b. 쿨다운도 사람 말로
- [ ] W5. 옵트인 꺼짐 — 무엇을 켜야 하는지까지
- [ ] W6. in-progress 아니면 거부
- [ ] W7. 킬스위치가 손잡이에도 걸린다
- [ ] W8. 없는 문서 거부
- [ ] A1~A3. POST /api/wake 배선 — 200/409 · action · id (격리 서버, 킬스위치 켠 채)

커버 못한 것(의도): **실제 스폰 이후**(워커가 뜨고 클레임하는 것까지)는 시험하지 않는다.
임시 ROOT 에서도 프로세스가 진짜 뜨기 때문이다. 그 구간은 운영 spawn.log 로 확인한다.
쿨다운 분기는 계정이 auto_resume_cooldown_sec 를 SPAWN_WIN(600s)보다 크게 잡았을 때만
도달한다 — 그 안이면 busy(기동 중)가 먼저 잡고 더 구체적으로 답한다. 시험도 그 조건으로 썼다.

### 2026-08-28T22:13:28+09:00 subagent (agent: sub:subagent:a6f24e7a, by nicehugepark)

Recording TDD scenario results on REQ-20260828-041-62x6

### 2026-08-28T22:14:10+09:00 response (by nicehugepark)

서버 몫 완료. 상세는 바로 위 backend-developer 노트(계약·결정·검증)와 tdd 노트(시나리오 체크)를 보라.

한 줄 요약: ① 위임은 이제 **리드가 지명한 REQ** 에 붙는다(유일하지 않으면 안 붙인다) —
도는 요청이 '멈춤' 으로 보이던 오귀속의 뿌리. ② `POST /api/wake` 는 화면과 같은 멈춤 판정을
지나 `_spawn_worker` 하나로만 띄우고, 붙어 있으면 거부하고, 캡·옵트인·킬스위치에 막히면
**무엇이 막았는지 사람 말로** 응답에 싣는다. 되돌리기(in-progress→open)는 내지 않았다 —
그 전이는 `s9 next`·`stalled` 의 시야에서 요청을 지워 '다시 굴린다'의 정반대가 된다.

화면 몫(다른 세션)이 남아 있어 in-progress 로 둔다. 화면이 붙기 전에 `s9 serve` 재기동이 필요하다.

### 2026-08-28T22:15:16+09:00 subagent (agent: sub:backend-developer:a0f852dd, by nicehugepark)

`[2026-08-28 22:15:41 KST - backend-developer]`

서버 몫 둘 다 냈다. 커밋은 하지 않았다.

### ① 위임 귀속 — 세션 클레임이 아니라 **지명된 문서**를 먼저 본다

`/home/sjpark1/section9/bin/s9-audit-agent`

훅이 받는 `tool_input`(`description`·`prompt`)에서 REQ id 를 읽어 그것을 대상으로 쓴다. 추측이 아니라 **위임을 띄운 리드가 적어 넣은 값**이다 — "아는 값을 전달"이 가능했고, 그래서 그 길을 택했다.

- 순서: ① 지명된 것 → ② 종전 경로(`active_reqs` → `last_req`, 소유·상태 확인). `target_req(env, inp=None, out=None)` — 한 인자 호출 호환 유지.
- **유일하지 않으면 고르지 않는다**: 지명 중 살아 있는(done/cancelled 아닌) 것이 0개거나 2개 이상이면 ②로 물러나고, ②도 비면 아무 데도 안 붙인다. 프롬프트에는 배경으로 다른 REQ 가 섞이므로 하나를 골라 `running` 을 심으면 남의 문서가 영원히 '진행 중'이 된다.
- 짧은 id 지명도 정식 id 로 풀어 저장한다(짧은 형태면 live/클레임 매칭이 깨진다).
- 자동 클레임 부작용을 재 봤다: `stall_mins` 는 클레임을 근거로 쓰지 않는다(문서 갱신 시각·위임 기여·스폰 마커만) — **멈춤 판정은 흐려지지 않고**, 워처 중복 스폰 판정(`rework_claimed`)만 선명해진다. 그래서 클레임은 그대로 건다.
- 세션 로그에 경로를 남긴다: `위임 등록: <REQ> ← sub:designer:xxxx (지명|클레임)`.

### ② `POST /api/wake` — 서버 몫

`/home/sjpark1/section9/bin/s9` (`wake_request` / `_spawn_wake` / `_wake_refusal` / `_auto_cap_block`)

응답 계약 (200 ok / 409 거부):

```
{"ok": bool, "id": str, "mins": int|null, "action": str, "message": str}
action: spawned · busy · moving · capped · off · disabled · elsewhere
        · no-cli · error · missing · not-request · not-in-progress
```

`message` 는 그대로 화면에 띄워도 되는 사람 말이다 — "오늘 무인 작업자 한도를 다 썼다 (20/20건) — 내일 0시에 풀린다…", "이미 designer 가 붙어 있다 (3분 전 움직였다 · 코드 경로를 눌러서 본다)…".

- 겹쳐 띄우지 않는다: 화면·CLI 와 **같은 판정**(`stall_mins`)을 지난다. 화면이 '멈춤'으로 그린 카드만 깨울 수 있다. ①의 고친 귀속이 그 입력이다.
- 스폰은 `_spawn_worker` 하나만. **킬스위치(`S9_AUTO_RESUME_DISABLE`)를 그 함수 안으로 옮겼다** — 종전엔 호출부에만 있어 새 경로를 여는 사람이 옮겨 적기를 잊으면 옆문이 났다.
- 캡 사유는 `_auto_cap_block()` 이 사람 말로 돌려주고 `_spawn_worker(out=…)` 이 밖으로 낸다. 문턱 수치는 여전히 한 곳에만 있다.

**되돌리기(in-progress → open)는 내지 않았다. 036 검토의 내 판단을 정정한다.** ① `s9 next`·`stalled` 는 **in-progress 만** 본다 — open 으로 되돌리면 그 요청은 이어받기 후보에서 사라진다("다시 굴린다"의 정반대다; 036 노트의 "work_order 에 다시 들어간다"는 사실이 아니었다). ② 그 전이는 `TRANSITIONS` 에 아예 없다(force 로만) — 화면 버튼이 상태기계에 옆문을 내는 셈이다. ③ 한 일 없이 멈춤 경보만 꺼진다. 일을 세워 두는 몫은 이미 합법 경로(카드를 blocked 로)가 한다.

남은 물음 하나는 사용자 몫으로 남겼다: 깨우기가 워처와 **같은 일일 캡**을 쓴다. 워처가 먼저 태우면 사람이 누른 버튼이 하루 종일 `capped` 만 낸다 — 별도 예산을 줄지는 임의로 정하지 않고 지금은 소진 사실을 정직하게 싣는다.

### 검증

- `python3 tests/ delegation_target` 11건 OK · `python3 tests/ test_wake` 16건 OK (W1~W8 + API 3건). **무인 스폰은 한 번도 일으키지 않는다** — Popen 스텁과 킬스위치로 막고 판정만 본다.
- 전체 1628건: 실패 3건은 전부 `web/index.html` 을 읽는 화면 테스트(test_term_mention 2 · test_terminal_table 1). **다른 세션이 지금 그 파일을 고치는 중**이라서다 — HEAD 에는 있는 `ccDocs(c)` 가 작업 트리에 없다. 내 변경과 무관하고, 그 파일은 손대지 않았다.

### 화면 세션에 넘길 것

- 깨우기 버튼은 멈춤 줄이 있는 카드에만. `ok=false` 도 오류가 아니라 설명이다 — 붉은 실패로 그리지 말고 `message` 를 그대로.
- **`s9 serve` 재기동해야 `/api/wake` 가 뜬다** (지금 도는 서버는 옛 코드다).

REQ-20260828-041-62x6 은 화면 몫이 남아 **in-progress** 로 두고, 노트 셋(`backend-developer` 계약·결정 / `tdd` 시나리오 체크 / `response` 요약)을 남겼다.

### 2026-08-28T23:19:37+09:00 designer (by nicehugepark)

### 화면 몫 완료 — 멈춘 카드에 손잡이 하나

멈춤 줄이 붙은 카드에만 **`깨우기`** 버튼이 선다. 누르면 `POST /api/wake` 로 가고,
서버가 준 `message` 를 **그대로** 창에 띄운다. 문서 화면도 같은 함수가 그린다 —
같은 줄, 같은 낱말, 같은 창.

### 무엇을 정했나

**① 손잡이는 멈춤 줄 바로 아래 행동 줄에 둔다** (줄 안이 아니라).
판정 카드(`.judge`)가 이미 쓰는 순서다 — 사실 한 줄, 그 사실에 대해 할 수 있는 일
하나. 새 컴포넌트를 만들지 않고 `.acts` + `.deed`(승인·반려·이어 말하기가 입는 그
옷)를 그대로 입혔다. 그래서 스킨 10벌이 손대지 않고 따라온다(실측: ledger·calm·
soft·glass·terminal·grid·slate·cobalt·field·cork + compact 밀도 + 어두운 tone 전부
제목 아래 제자리).
- 대안(기각) **줄 오른쪽 끝에 인라인**: 한 줄을 아끼지만 그 줄은 `nowrap`+말줄임이라
  긴 문장과 버튼이 폭을 다투고, calm 에서 그 줄은 3줄 클램프(`-webkit-box`)라
  버튼이 클램프 안에 갇힌다.
- 대안(기각) **hover 에만 표시**: 가장 조밀하지만 키보드·터치에서 존재를 알 수 없다.

**② 결과는 언제나 창 하나로 답한다** (성공도, 거절도).
사용자가 오늘 다섯 번 물은 것이 "이거 진짜 도는 거냐" 였다. 눌렀는데 아무 말이 없거나
카드 구석이 조용히 바뀌는 것으로는 그 물음이 안 닫힌다. 성공 문구는 다음에 볼 자리까지
가리킨다("진행은 카드의 점과 Stream 에서 보인다").
- 대안(기각) **카드 안에 결과 줄**: 보드는 카탈로그가 바뀔 때마다 카드를 다시 그린다 —
  결과가 몇 초 만에 증발한다. 살리려면 화면이 결과를 따로 기억해야 하고 "언제 지우나"가
  새 결함 표면이 된다. 한도 소진 문구(계정 설정 키 이름까지)는 카드 폭에 안 들어간다.
- 대안(기각) **토스트**: 이 화면에 없는 컴포넌트다. 새로 들이면 판정 창과 어휘가 둘이 된다.

**③ `ok=false` 는 실패의 옷을 입지 않는다.**
`s9dlg` 의 눈썹 잉크가 종전엔 "알림이면 무조건 빨강"이었다 — `busy`·`capped`·`moving`
은 정상적인 답인데 그 옷을 입으면 사람은 고장으로 읽고 다시 안 누른다. 잉크를 정하는
근거를 **kind 가 아니라 stop 플래그**로 바꿨다(기본은 종전 그대로). 깨우기의 거절은
`stop:false`, 서버에 못 닿은 것만 붉다.

**④ 화면은 이유를 짓지 않는다.**
읽는 필드는 `ok` 와 `message` 둘뿐이다. `action` 값(spawned·busy·capped…)은 화면
코드 어디에도 없다 — 알기 시작하면 같은 말이 서버와 화면 두 벌이 되고, 다음엔 한 벌만
고쳐진다. 테스트가 그 낱말들의 부재를 잠근다.

**⑤ 연타는 막고, 못 깨운 것은 다시 누른다.**
누른 즉시 버튼이 `깨우는 중…` 으로 잠긴다(서버 왕복을 기다리지 않는다). 거절·실패면
곧바로 풀린다. 성공하면 곧 멈춤 줄 자체가 사라진다 — 작업자가 붙으면 `stalled_mins`
가 없어지므로 화면이 따로 지울 것이 없다. 3분이 지나면 잠금이 스스로 풀린다(스폰이
조용히 죽어도 손이 묶이지 않게).

**⑥ 문서 화면에도 같은 손잡이.** 사용자가 물은 것이 "카드**나 문서**에" 였고,
REQ-20260828-007-62x6 이 세 번 반려된 이유가 두 화면이 각자 글자를 가진 것이었다.
`stallHTML()` 한 함수를 양쪽이 부른다 — 갈라질 자리가 없다.

### 덤으로 잡은 결함: 카드 안의 버튼이 Enter 를 빼앗기고 있었다

깨우기를 키보드로 눌러 보다 찾았다. 보드 카드는 `role="button"` 인 판이고 그 **안에**
진짜 `<button>` 이 산다(승인·반려·이어 말하기·깨우기). `role=button` 을 Enter 로
누르는 전역 핸들러가 `closest()` 로 **판**을 집어 올려, 버튼의 네이티브 활성화를
`preventDefault` 로 막고 카드를 눌렀다 — **마우스로는 깨우기가, 키보드로는 문서
열기가** 일어났다. 네이티브가 Enter/Space 를 아는 것(`button`·`a[href]`·`summary`)은
그들에게 맡기도록 한 줄 고쳤다. 승인·반려·이어 말하기도 함께 고쳐졌다.

### 실브라우저 검증 (전부 **실제로 눌렀다**)

격리 서버(임시 S9_ROOT · 무인 작업자는 대역 스크립트)에 멈춘 요청을 세워 두고 Windows
Chrome 을 CDP 로 몰아 실제 마우스 이벤트로 눌렀다. 응답은 전부 **진짜 서버가 준 것**이다.

- 깨워 성공 → `spawned`. 스폰 로그에 `SPAWN(fresh) … reason=wake` 한 줄. 카드는 그
  자리에서 멈춤 줄을 잃고 점이 앰버(기동 중)로 바뀌고 열 머리 `멈춤 3 → 2`.
- 이미 붙어 있음 → `busy`. 화면이 카드를 그린 **뒤** 서브에이전트가 붙는 실제 경쟁을
  만들어 눌렀다.
- 한도 소진 → `capped`. 전역 카운터를 20/20 으로 채운 뒤 눌렀다.
- 아직 멈춘 게 아님 → `moving`. 화면이 그린 뒤 문서가 움직인 경쟁.
- 도는 중 표시 → 지연 2.5초를 걸고 눌러 `깨우는 중…`(잠김)을 찍었다.
- 서버에 못 닿음 → 오프라인으로 끊고 눌렀다. 붉은 `연결` 창이 뜨고 버튼은 다시 눌린다.
- 키보드: Tab 두 번에 손잡이에 닿고 포커스 링이 보이며 Enter 로 눌린다(위 결함 수정 후).
- 스킨/밀도/톤 10벌 + 문서 화면 3벌.

전체 스위트 `python3 tests/` 통과. 새 계약은 `tests/test_wake_handle.py` 14건.
`tests/test_stall_visible.py` 의 멈춤 줄 검사는 글자가 `stallHTML` 로 옮겨 간 만큼
그 자리를 보도록 고쳤다(계약은 그대로).

### 남겨 둔 것

- 손잡이 높이는 22~27px(compact 18px)로 터치 권장치(44px)에 못 미친다. 이 화면의
  기존 판정 버튼과 같은 값이다 — 바꾸려면 `.acts button` 을 통째로 바꿔야 하므로
  여기서 혼자 키우지 않았다.
- 깨우기가 워처와 같은 일일 한도를 쓰는 문제(backend 노트의 남은 물음)는 화면이 정할
  일이 아니다. 지금은 소진 사실이 창에 그대로 뜬다.

파일: /home/sjpark1/section9/web/index.html · /home/sjpark1/section9/tests/test_wake_handle.py ·
/home/sjpark1/section9/tests/test_stall_visible.py

[Image: assets/REQ-20260828-041-62x6/01-board-stalled-only.png]
[Image: assets/REQ-20260828-041-62x6/02-wake-spawned.png]
[Image: assets/REQ-20260828-041-62x6/03-after-wake.png]
[Image: assets/REQ-20260828-041-62x6/04-refused-busy.png]
[Image: assets/REQ-20260828-041-62x6/05-refused-capped.png]
[Image: assets/REQ-20260828-041-62x6/06-refused-moving.png]
[Image: assets/REQ-20260828-041-62x6/07-inflight.png]
[Image: assets/REQ-20260828-041-62x6/08-document-same-handle.png]
[Image: assets/REQ-20260828-041-62x6/09-keyboard-focus.png]
[Image: assets/REQ-20260828-041-62x6/10-server-unreachable.png]
[Image: assets/REQ-20260828-041-62x6/11-skin-calm.png]
[Image: assets/REQ-20260828-041-62x6/12-skin-glass-dark.png]
[Image: assets/REQ-20260828-041-62x6/13-skin-terminal.png]
[Image: assets/REQ-20260828-041-62x6/14-document-calm.png]

### 2026-08-28T23:19:47+09:00 subagent (agent: sub:subagent:a1688015, by nicehugepark)

Moving REQ-20260828-041-62x6 to review

### 2026-08-28T23:20:19+09:00 subagent (agent: sub:designer:ad036cb4, by nicehugepark)

`[2026-08-28 23:19:46 KST - designer]`

화면 몫을 냈다. REQ-20260828-041-62x6 은 **review** 로 올렸고 커밋은 하지 않았다.

### 낸 것

멈춤 줄이 붙은 카드에만 `깨우기` 버튼이 선다 → `POST /api/wake` → 서버가 준 `message` 를 **그대로** 창에 띄운다. 문서 화면도 **같은 함수**(`stallHTML`)가 그린다 — 갈라질 자리가 없다.

**화면이 읽는 필드는 `ok` 와 `message` 둘뿐이다.** `action` 값(spawned·busy·capped…)은 코드 어디에도 없다 — 알기 시작하면 같은 말이 서버와 화면 두 벌이 된다. 테스트가 그 낱말들의 부재를 잠근다.

**`ok=false` 는 실패의 옷을 입지 않는다.** `s9dlg` 의 눈썹 잉크가 종전엔 "알림이면 무조건 빨강"이었다. 근거를 `kind` 가 아니라 `stop` 플래그로 바꿨다(기본값은 종전 그대로). 깨우기의 거절은 `stop:false`, 서버에 못 닿은 것만 붉다.

### 자리 — 대안 비교

| | 결정 |
|---|---|
| **채택** 멈춤 줄 **아래** 행동 줄 (`.acts`+`.deed`) | 판정 카드가 이미 쓰는 순서(사실 → 행동). 새 컴포넌트 0 → 스킨 10벌이 손대지 않고 따라온다 (실측 확인) |
| 기각 줄 안 인라인 | 그 줄은 `nowrap`+말줄임이라 긴 문장과 버튼이 폭을 다투고, calm 에서는 3줄 클램프 안에 버튼이 갇힌다 |
| 기각 hover 시에만 | 가장 조밀하지만 키보드·터치에서 존재를 알 수 없다 |

결과 표시도 같은 식으로 갈랐다: **창 하나로 답한다**(성공·거절 동일). 카드 안 결과 줄은 폴링 재렌더에 증발하고, 한도 소진 문구는 카드 폭에 안 들어간다. 토스트는 이 화면에 없는 어휘라 판정 창과 둘이 된다.

### 덤으로 잡은 결함

**카드 안의 진짜 버튼이 Enter 를 빼앗기고 있었다.** `role="button"` 을 Enter 로 누르는 전역 핸들러가 `closest()` 로 판(카드)을 집어 올려, 버튼의 네이티브 활성화를 `preventDefault` 로 막고 카드를 눌렀다 — **마우스로는 깨우기가, 키보드로는 문서 열기가** 일어났다. 승인·반려·이어 말하기도 같이 앓고 있었다. 한 줄(`if (t.closest("button,a[href],summary")) return;`)로 고쳤고 넷 다 함께 나았다.

### 검증 — 전부 실제로 눌렀다

격리 서버(임시 `S9_ROOT`, 무인 작업자는 대역 스크립트)에 멈춘 요청을 세우고 Windows Chrome 을 CDP 로 몰아 실제 마우스 이벤트로 눌렀다. 응답은 전부 진짜 서버가 준 것이다.

- **성공**(`spawned`) — 스폰 로그에 `SPAWN(fresh) … reason=wake`. 카드가 그 자리에서 멈춤 줄을 잃고 점이 앰버로, 열 머리 `멈춤 3 → 2`
- **거부·이미 붙어 있음**(`busy`) — 화면이 카드를 그린 뒤 서브에이전트가 붙는 실제 경쟁을 만들어 눌렀다
- **거부·한도 소진**(`capped`) — 전역 카운터를 20/20 으로 채운 뒤
- **거부·아직 안 멈춤**(`moving`) — 화면이 그린 뒤 문서가 움직인 경쟁
- 도는 중(`깨우는 중…` 잠김) · 서버 끊김(붉은 `연결` 창 + 버튼 재활성) · 키보드(Tab 2회 → 포커스 링 → Enter) · 스킨 10벌 + compact + 어두운 톤 + 문서 화면 3벌

전체 스위트 `python3 tests/` → **1667건 OK** (skipped=1).

### 리드가 해야 할 것

**`bin/s9 serve --restart`** — 지금 도는 서버에는 아직 `/api/wake` 가 없다. 그대로 누르면 "서버가 깨우기를 알지 못합니다" 창만 뜬다. 사용자에게 넘기기 전에 재기동해야 판정이 성립한다.

### 남겨 둔 것

- 손잡이 높이 22~27px(compact 18px)로 터치 권장치(44px) 미달 — 기존 판정 버튼과 같은 값이라 혼자 키우지 않았다. 바꾸려면 `.acts button` 을 통째로.
- 깨우기가 워처와 같은 일일 한도를 쓰는 문제(backend 노트의 남은 물음)는 화면이 정할 일이 아니다. 지금은 소진 사실이 창에 그대로 뜬다.

파일: `/home/sjpark1/section9/web/index.html` · `/home/sjpark1/section9/tests/test_wake_handle.py`(신규 14건) · `/home/sjpark1/section9/tests/test_stall_visible.py`(글자가 `stallHTML` 로 옮겨 간 만큼 보는 자리만 이동, 계약 동일). 캡처 14장은 문서에 그림으로 붙였다.

### 2026-08-29T16:47:01+09:00 ask (by nicehugepark)

이 기능도 제대로 동작하지 않는다. 관련된 에이전트를 붙여서 해결될 때 까지 워크플로를 실행해. 그리고 제대로 검증해.

### 2026-08-29T16:56:30+09:00 frontend-developer (by nicehugepark)

진단(고치지 않음) — 깨우기 단추는 지금 화면에 **하나도 없다**. 누름 실패가 아니라 부재다.

조건. 카드: r.status==="in-progress" && r.stalled_mins != null && !bl.length (선행 대기가 있으면 진다).
문서: m.type==="request" && m.status==="in-progress" && catFind(m.id).stalled_mins != null — 문서 쪽은 선행 대기를
보지 않아 같은 요청이 카드에선 안 뜨고 문서에선 뜰 수 있다(비대칭).

실측(/api/catalog, 630행). in-progress 6건 전부 stalled_mins 키 자체가 없다. `s9 stalled` 도 "(멈춘 작업 없음)".
stall_mins() 가 updated 기준 900초 창을 쓰는데, 이 저장소는 여러 세션이 몇 분마다 노트를 쓰므로 그 창을 넘는 행이
사실상 생기지 않는다. 캡처의 REQ-20260828-041-62x6 카드가 그 모순을 그대로 보여 준다 — status_since 19분 28초,
점은 멈춤(◫, live_reason "180초째 진전 없음"), 그런데 멈춤 줄도 깨우기도 없다.
점은 agent_state(live_kind, 180초 기준)에서 오고 단추는 stalled_mins(updated, 900초 기준)에서 오는데,
두 시계가 다르니 REQ-20260828-036-62x6 이 없애려던 어긋남이 방향만 바꿔 남았다: 화면은 "멈췄다"고 그려 놓고 깨울 손잡이는 안 준다.

배선 자체는 성하다: [data-wake] 위임(13473)이 카드 열기보다 먼저 잡고, 실패는 알림 창으로 말하고,
연타는 wokeAt(3분)로 막는다. 문제는 stallHTML 이 불릴 조건이 실데이터에서 성립하지 않는 것뿐이다.

그리고 이 화면에는 **멈춤 줄을 세워 보는 진단 파라미터가 없다**(?dlg=·?svchip=·?conn= 같은 자리). 서버도
/api/catalog 에 창 길이 인자를 받지 않는다. 그래서 이 단추는 헤드리스 캡처로 한 번도 눈으로 확인된 적이 없다.

[Image: assets/REQ-20260828-041-62x6/e-doc041.png]

### 2026-08-29T17:00:37+09:00 deep-diver (by nicehugepark)

### 왜 "안 된다"인가 — 실측 (읽기 전용 조사)

계약(wake_request)은 맞다. 66/66 통과(`python3 tests/ wake stall_visible agent_health worker_worktree`).
**깨지는 자리는 유닛 밖이다.** 지금 이 순간 살아 있는 서버에 직접 쳐서 확인한 것:

### ① 화면에 버튼이 아예 안 뜬다 — in-progress 5건 중 0건
`GET /api/catalog` 실측: 5건 전부 `stalled_mins = null`.
`cardHTML` 은 `stalled = r.stalled_mins != null` 일 때만 `stallHTML()`(=버튼)을 붙이므로
버튼이 그려질 카드가 하나도 없다. 캡처로도 확인 — IN-PROGRESS 열머리에 `멈춤 N` 배지 자체가 없다.

`stall_mins()` 가 null 을 내는 길 셋이 동시에 열려 있다:
- **좀비 클레임**: 041 자신이 그렇다. 죽은 서브에이전트(`sub:frontend-developer:a1dfeb13`,
  16:39:56)의 기여가 `result: running` 으로 남아 `delegated_live` 가 참 → 멈춤 판정 자체가 건너뛴다.
  `s9 agents health` 는 그것을 `unknown`("진전 신호 없음")으로 판정하는데 `health_apply` 는
  stalled/failed 만 되쓴다 → **running 이 DELEGATE_WIN(3600초) 내내 안 풀린다.**
  transcript 가 사라진 워크트리 tmp 경로라 나이를 못 재는 것이 원인이다.
- **15분 문턱**: 워처·리드가 30초마다 문서를 건드리면 `updated` 가 갱신돼 900초를 못 넘긴다.
- 스폰 마커(`live_kind=spawned`).

### ② 그런데 카드는 "멈췄다"고 말한다 — 글자와 손잡이가 어긋난다
041 카드의 점은 `dot-stopped mild`("진전이 끊겼다 — 180초째 진전 없음")다.
근거는 `_contrib_state` 가 고른 **어제 22:36 의** stalled 기여(`sub:designer:ab64fee2`)다.
시간 한도가 없어 그 점은 문서 수명 내내 켜져 있다.
사용자가 보는 것: **멈췄다고 적힌 카드 + 깨울 손잡이 없음.** 이것이 "안된다"의 실제 화면이다.

### ③ 버튼이 떠도 오늘은 못 깨운다 — 일일 캡 소진
`state/auto_resume/_global.json` = `{"day":"2026-08-29","day_count":20,...}`, 한도 20.
`_auto_cap_block` 실측: 지금 어떤 REQ 로 눌러도 `capped`("오늘 무인 작업자 한도를 다 썼다 (20/20건)").
오늘 20건 중 **13건이 rework 워처, 4건이 followup**, 사람이 누른 wake 는 3건뿐이다 —
자동 스폰이 사람 손잡이와 **같은 카운터**를 먹고 16:48:07 에 다 썼다.
킬스위치는 꺼져 있고(서버 env 에 없음), 옵트인 on, 머신 일치 — 막는 것은 캡 하나다.

### ④ 실제 POST 응답 (127.0.0.1:9909, 원문)
- 041 → `{"ok":false,"action":"busy","message":"이미 frontend-developer 가 붙어 있다 (13분 전 …)"}`
- 014/079/016 → `{"ok":false,"action":"moving","message":"아직 멈춘 게 아니다 — N분 전 문서가 움직였다."}`
**4건 전부 ok=false.** 서버는 살아 있고 라우트도 안다 — 거부만 한다.

### ⑤ 깨워도 일이 안 붙는다
- 15:10 wake(079): 워커가 워크트리에 앉았고 **그 자리는 마지막 커밋 사본이라 오늘 것이 없다**
  (미커밋 web/index.html 954줄 · bin/s9 402줄). 워커가 직접 그렇게 보고하고 blocked 로 돌렸다.
- 16:36 rework(041): 로그 0바이트, pid 737858 사망, 워크트리·가지 소멸, 서브에이전트 기여는
  `running` 인 채 남아 ①의 좀비 클레임이 됐다. (16:44 무렵 WSL 이 재기동한 흔적 — s9 serve·
  oom-guard·s9 code 가 모두 그때 새로 떴다.)

### 2026-08-29T17:06:34+09:00 tdd (by nicehugepark)

### TDD 시나리오 (backend, 라운드1 — 서버 몫)
- [ ] W1. 죽은 서브에이전트 기여가 클레임을 붙잡지 못한다: transcript 경로가 등록됐는데 파일이 없으면 judge_health 가 unknown 이 아니라 failed(기록 자리가 사라졌다)
- [ ] W2. 신호가 아예 없는 기여도 유예(1시간) 뒤에는 stalled 로 판정된다 — 영구 running 이 사라진다
- [ ] W3. delegated_running 은 '기여가 running' 만 보지 않고 그 actor 의 생존 판정을 함께 본다 — 죽은 기여는 busy 를 만들지 않는다
- [ ] W4. _contrib_state 는 DELEGATE_WIN 밖으로 늙은 기여를 요약에서 뺀다 (어제의 stalled 가 오늘 점을 칠하지 않는다)
- [ ] W5. 점이 멈춤(live_kind=stalled/spawn_failed)인 행은 stall_mins 가 반드시 숫자를 낸다 — '멈췄다고 그려 놓고 못 누르는 카드' 가 없다
- [ ] W6. 사람이 누른 깨우기(reason=wake)는 워처의 전역 일일 캡과 예산을 나눠 쓰지 않는다 — 워처가 20/20 을 써도 사람 손잡이는 산다
- [ ] W7. worker_worktree=on 이라도 본 저장소에 미커밋 코드 변경이 있으면 워크트리를 주지 않고 본 저장소에 앉힌다 — '깨웠다' 가 거짓말이 되지 않는다
- [ ] W8. /api/wake 호출은 감사 로그(spawn.log WAKE)에 남는다 — 다음 반려 때 눌렀는지 짐작하지 않는다
- [ ] W9. 반환 계약(ok·action·message)은 그대로다

### 2026-08-29T17:23:45+09:00 response (by nicehugepark)

### 라운드1 서버 몫 (bin/s9) — 무엇을 고쳤나

**좀비 클레임을 푼다.** `judge_health` 에 `gone`·`claim_age` 를 더했다. 경로를 등록해 놓고 그 파일이 없으면(워크트리가 거둬진 자리) unknown 이 아니라 **failed(기록 자리가 사라졌다)** — unknown 은 `health_apply` 가 되쓰지 않아 문서의 `running` 이 영원히 남았다. 신호가 아예 없는 클레임도 `UNKNOWN_GRACE(=DELEGATE_WIN, 1시간)` 뒤에는 stalled 로 종결된다. `agent_health` 가 두 값을 판정에 넘긴다.

**판정을 한 벌로.** `delegated_running` 이 '기여가 running' 만 보지 않고 그 actor 를 `judge_health` 에 태운다 — failed/stalled/done 인 기여는 클레임이 아니다. `agents health` 는 죽었다는데 손잡이는 busy 라고 답하던 어긋남이 사라진다.

**점과 손잡이가 같은 시계를.** `_contrib_state` 에 시간 한도(DELEGATE_WIN)와 `open`(열린 기여 수)·`at`/`open_at`(근거 시각)을 넣었다. 요약은 색인에 굳으므로 `catalog_with_live` 가 읽는 시점에 `_iso_age` 로 다시 잰다 — 어제 22:36 의 stalled 가 오늘 점을 칠하던 자리다. 열린 기여가 있으면 점을 끄지 않는다. `stall_mins` 는 점이 멈춤인 행에서 잣대를 `STALLED_DEAD_WIN(180초)` 로 좁히되, **죽음이 기록된 뒤에도 문서가 움직였으면 좁히지 않는다**(일하는 리드 위에 두 번째 손 방지).

**사람 손잡이의 예산을 갈랐다.** `_auto_cap_block/_auto_caps_ok` 에 `reason` 을 넘겨 `reason=wake` 는 `wake_day_count`/`wake_hour_count`(기본 12/일, 6/시간, `auto_resume_wake_per_day|_per_hour`)를 쓴다. 워처가 20/20 을 소진해도 사람의 깨우기는 산다. per-REQ 쿨다운은 갈래와 무관하게 유지(충돌 방지).

**성공 응답이 거짓말이 되지 않게.** `repo_code_dirty()` 신설 — 본 저장소에 미커밋 **코드** 변경(state/·vault/·users/·index/·streams/ 제외)이 있으면 `worker_workspace` 가 워크트리를 주지 않고 본 저장소에 앉힌다(`WT-SKIP(dirty)` 로그). 워크트리는 마지막 커밋 사본이라 오늘 만든 것이 거기 없다.

**감사 한 줄.** `_wake_audit` — 모든 wake 결과가 `spawn.log` 에 `WAKE <id> by=… action=… mins=…` 로 남는다. 반환 계약(ok·id·action·mins·message)은 그대로다.

시험: tests/test_wake_reach.py 19건 신규(W1~W9). `python3 tests/ wake` 전건 통과.

### 2026-08-29T17:30:53+09:00 tdd (by nicehugepark)

### TDD 시나리오 결과 (라운드1 서버 몫)
- [x] W1 · [x] W2 · [x] W3 · [x] W4 · [x] W5 · [x] W6 · [x] W7 · [x] W8 · [x] W9 — tests/test_wake_reach.py 19건
실행: `python3 tests/ wake` 통과 · `python3 tests/` 1961건 OK(skipped=1)
살아 있는 서버 실측(재기동 후): in-progress 6건 중 stalled_mins 를 가진 행이 0 → 3 (014·019·023 각 42·44·29분). 041 의 좀비 클레임 sub:frontend-developer:a1dfeb13 이 result=failed '기록 자리가 사라졌다' 로 종결됨. POST /api/wake 041 → action=moving(전엔 busy). spawn.log 에 'WAKE REQ-… by=nicehugepark action=moving' 기록. 워처 캡 20/20 소진 상태에서 사람 갈래는 통과. worker_worktree=on 이지만 미커밋 코드 18건이라 워커는 본 저장소에 앉는다.

### 2026-08-29T17:32:37+09:00 delegate (by nicehugepark)

라운드2 위임 착수 17:33 — 라운드1(서버 몫, bin/s9)은 끝났고 서버를 재기동해 반영했다. 재기동 뒤 /api/catalog 실측: in-progress 6건 중 4건이 stalled_mins 숫자를 낸다(014=44, 019=46, 023=30, 028=25) — deep-diver 가 짚은 '0건' 은 풀렸다. 041 자신은 리드가 직접 클레임해 direct 다.

남은 두 갈래를 병렬로 붙인다:
- frontend-developer: 화면 몫 — 점과 손잡이의 대칭(카드/문서), 멈춤 줄을 세워 볼 수 있는 진단 파라미터, 실제 캡처 검증.
- backend-developer: 실효 몫 — 깨운 작업자가 실제로 일에 붙는지(워크트리 dirty 회피·감사 로그·사람 몫 캡 분리)를 살아 있는 경로로 확인.

### 2026-08-29T17:35:52+09:00 tdd (by nicehugepark)

### TDD 시나리오 (backend 라운드2 — **실효**: 깨운 뒤 일이 붙는가)

라운드1은 유닛(W1~W9)으로 판정 함수를 잠갔다. 라운드2가 물을 것은 그 밖이다 —
'거부하지 않는다'와 '실제로 일이 붙는다'는 다른 명제다.

- [ ] R1. 살아 있는 이 저장소에서 `repo_code_dirty()` 가 실제로 참을 낸다 (미커밋 코드 목록을 낸다) — 실측
- [ ] R2. 그 상태에서 `worker_workspace` 가 worker_worktree=on 인데도 워크트리 대신 **본 저장소**를 고른다 — 판정 함수 직접 호출(스폰 없음)
- [ ] R3. 죽음 세 모양 각각이 `agent_health`→`health_apply` 를 지나 running 을 **종결**시킨다 (judge_health 단위가 아니라 문서를 읽는 실제 스캔으로): ① 로그 0바이트 + pid 사망 ② transcript 경로 소멸 ③ 신호 전무 + 유예 초과. 두 번 돌려도 영구 running 이 없다
- [ ] R4. 종결 뒤 같은 REQ 에 `wake_request` 가 `busy` 로 거부하지 않는다 — 좀비 클레임이 다음 깨우기를 막지 않는다
- [ ] R5. 스폰 마커의 pid 가 죽은 행(live_kind=spawn_failed)도 깨울 수 있다 — 죽은 워커의 마커가 창(600초) 동안 손잡이를 막지 않는다
- [ ] R6. 워처 전역 캡이 20/20 소진된 상태에서 reason=wake 는 통과하고, 통과 시 `wake_day_count` 만 오른다(`day_count` 불변) — 살아 있는 카운터 파일 값으로 확인
- [ ] R7. wake 의 **모든** 갈래(spawned·busy·moving·capped·off·disabled)가 spawn.log 에 `WAKE <id> by= action= mins=` 로 남는다
- [ ] R8. 깨우기가 실제로 실행하는 명령줄을 Popen 스텁으로 붙잡아 검증한다: cwd=본 저장소(dirty 일 때) · `--model` 이 계정 설정값 · 프롬프트에 REQ id·`s9 show` 절대경로·클레임 지시 포함 · env 에 S9_AUTO_RESUME=1·S9_USER=오너 · **S9_SESSION 제거** · 편집 도구가 봉투 안에 있다(apply 모드에서 워커가 실제로 고칠 수 있는가)
- [ ] R9. 응답 계약 5키(ok·id·mins·action·message) 불변

규율: 진짜 워커는 띄우지 않는다(Popen 스텁·S9_AUTO_RESUME_DISABLE 격리·판정 함수 직접 호출). 만지는 것은 bin/s9 와 tests/ 뿐이다.

### 2026-08-29T17:37:25+09:00 tdd (by nicehugepark)

### TDD 시나리오 (frontend, 라운드2 — 화면 몫)

라운드1이 서버에서 '점과 손잡이의 시계'를 한 벌로 만들었다. 화면에는 아직 **두 개의 갈래**가 남아 있다: 카드만 가진 `!bl.length` 관문, 그리고 점(live_kind)과 손잡이(stalled_mins)가 각자 다른 필드를 읽는 구조. 아래는 그 둘을 없애는 계약이다.

- [ ] F1. 멈춤 술어는 화면에 **하나뿐**이다: `stallState(r)` 한 함수가 점·줄·손잡이·열 머리 수·정렬을 전부 먹인다. `cardHTML`/문서 화면 어디에도 `stalled_mins != null` 를 각자 판정하는 자리가 없다.
- [ ] F2. 카드와 문서가 **같은 입력에 같은 답**을 낸다: 카드에만 있던 `!bl.length` 관문이 사라져, 선행 대기 줄이 선 요청도 서버가 멈춤이라 하면 양쪽 다 `깨우기` 를 준다. (기존 test_stall_visible C4 '선행이 이긴다'는 뒤집는다 — 근거는 response 노트에)
- [ ] F3. **멈췄다고 그려 놓고 못 누르는 카드가 구조적으로 불가능하다**: 점이 멈춤(live_kind=stalled·spawn_failed)인 행은 `stalled_mins` 가 없어도 손잡이가 선다. 서버가 아직 이르다고 보면 `moving` 으로 설명한다 — 죽은 카드보다 낫다.
- [ ] F4. 거꾸로도 한 벌: `stalled_mins` 가 있는 행의 점은 반드시 멈춤 모양(`.dot-stopped`)이다. 손잡이는 있는데 점만 조용한(`.livedot.off`) 카드가 없다.
- [ ] F5. **화면은 분을 짓지 않는다**: `stalled_mins` 가 없으면 서버의 `live_reason` 을 옮길 뿐, 자체 계산한 숫자를 문장에 쓰지 않는다.
- [ ] F6. `?stall=mins|dead|dep|going` 진단 파라미터가 실데이터가 조용한 날에도 멈춤 줄·손잡이·잠김·선행 동거를 세운다. 파라미터가 없으면 어떤 행도 건드리지 않는다. 그림을 따로 그리지 않고 **진짜 stallHTML/stallState 를 지난다**.
- [ ] F7. 응답 계약 불변: 화면이 읽는 것은 `ok`·`message` 둘뿐이고 `action` 낱말(spawned·busy·capped…)은 코드에 없다. `ok=false` 는 `stop:false` — 붉은 실패가 아니라 설명이다.
- [ ] F8. 열 머리 `멈춤 N` 과 in-progress 정렬이 F1의 같은 술어를 쓴다 — 손잡이가 선 카드 수와 배지 숫자가 어긋나지 않는다.
- [ ] F9. 실브라우저: 실데이터 4건(014·019·023·028)에 멈춤 줄과 `깨우기` 가 실제로 그려진 것을 캡처로 확인한다. `?stall=` 네 얼굴도 캡처한다.

### 2026-08-29T17:38:26+09:00 conflict (agent: worker:wake, by nicehugepark)

### 물러났다 — 라운드2 두 갈래는 지금 살아서 돌고 있다 (무인 워커 0755c082)

깨우기 프롬프트가 이 세션을 띄우며 "끊긴 항목" 으로 지목한 넷 중 **뒤의 둘은 끊기지 않았다.**

- `sub:frontend-developer:afaa381a` (깨우기 화면 몫 라운드2)
- `sub:backend-developer:a4a2de7b` (깨우기 실효 검증 라운드2)

실측(17:36~17:39):

- 두 서브에이전트의 transcript 가 **지금 이 순간 커지고 있다** — `agent-afaa381a9fa4adcce.jsonl`(875KB) · `agent-a4a2de7b4e68f08e7.jsonl`(356KB) 둘 다 mtime 17:36.
- 그 둘을 띄운 리드 세션 `81526441`(pid 68633, 17:29 기동)이 **살아서 busy** 다 — 피어 목록에 `section9-ad` interactive busy, 8분 전 기동.
- 기여 기록도 17:33:12 / 17:33:38 시작으로 `result: running` 이다. 3분 전에 붙은 것이지 44분 멈춘 것이 아니다.

두 갈래가 손대는 자리가 곧 이 REQ 의 남은 전부다 — `web/index.html`(화면 몫) · `bin/s9` + `tests/`(실효 몫).
내가 지금 같은 파일을 고치면 도는 작업을 덮어쓴다. 동시 작업 규율 1에 따라 **물러난다.**

### 남은 일의 임자

라운드2 두 갈래가 그대로 끝내고, 리드 세션 81526441 이 review 로 올린다. 이 워커는 아무 파일도 고치지 않았다.
클레임(`sessions` 의 `0755c082`)은 남겨 둔다 — 지우면 다음 워처 틱이 또 겹쳐 띄운다.

### 이 사건 자체가 이 REQ 의 증상이다

"멈춘 것을 사람이 깨운다" 의 짝인 **겹쳐 띄우지 않는다** 가 여기서 깨졌다.
워처가 든 근거("탐침(스폰 안 함) 44분째 진전 없음")는 `status_since`(16:35 반려) 기준으로 보이는데,
정작 그 문서에는 17:33 에 시작된 running 기여가 둘 있고 그것을 띄운 리드 세션도 살아 있다.

라운드1에서 `stall_mins` 는 위임 기여를 보도록 고쳤지만, **워커를 띄우는 쪽(재개 워처)의 잣대는 그 판정을 쓰지 않는다.**
같은 저장소에서 한쪽은 "붙어 있다", 다른 쪽은 "44분 멈췄다" 고 말한다.
REQ-20260829-016-62x6(리드가 붙은 일에 작업자가 또 뜬다)과 같은 뿌리다.

`bin/` 은 이 세션의 수정 범위 밖이라 제안으로만 남긴다:
재개 워처의 스폰 게이트도 `wake_request` 와 같은 판정(`stall_mins` / `delegated_running`)을 지나게 하라.
손잡이와 워처가 잣대를 둘로 가지면 사람이 아무리 고쳐도 겹친다.

### 2026-08-29T17:39:05+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-08-29 17:39:05 KST - lead]`

`[2026-08-29 17:39 KST - lead]`

물러났습니다. 파일은 하나도 고치지 않았습니다.

**왜 그렇게 판단했나**

깨우기 프롬프트가 "끊긴 항목" 으로 지목한 넷 중 뒤의 둘 — `sub:frontend-developer:afaa381a`(화면 몫 라운드2)·`sub:backend-developer:a4a2de7b`(실효 검증 라운드2) — 은 끊기지 않았습니다. 실측:

- 두 서브에이전트의 transcript(875KB·356KB)가 지금 이 순간 갱신되고 있습니다(mtime 17:36).
- 그 둘을 띄운 리드 세션 `81526441`(pid 68633, 17:29 기동)이 살아서 busy 입니다 — 피어 목록에 `section9-ad` interactive busy.
- 기여 기록도 17:33:12 / 17:33:38 시작의 `running` 입니다. 3분 전에 붙은 것이지 44분 멈춘 것이 아닙니다.

두 갈래가 손대는 자리가 이 REQ 의 남은 전부입니다(`web/index.html` · `bin/s9` · `tests/`). 제가 같은 파일을 고치면 도는 작업을 덮어씁니다. 동시 작업 규율에 따라 물러났습니다.

**한 것**

- `s9 last --add --session 0755c082` 클레임 (프롬프트 지시대로). 지우지 않고 남겼습니다 — 지우면 다음 워처 틱이 또 겹쳐 띄웁니다.
- REQ 에 `--label conflict` 노트: 현황·판단 근거·워처 잣대에 대한 제안.
- 리드 세션 `section9-ad` 에 겹침 사실을 전달했습니다(진행 중 작업은 그대로 마치라는 단서 포함).
- review 전이는 하지 않았습니다. 일이 안 끝났고, 임자는 라운드2 두 갈래입니다.

**짚어 둘 것**

이 겹침 자체가 이 REQ 가 고치려던 결함입니다. 손잡이(`wake_request`)는 `delegated_running` 을 보고 busy 로 거부하는데, 재개 워처의 스폰 게이트는 그 판정을 지나지 않는 것으로 보입니다 — 같은 저장소에서 한쪽은 "붙어 있다", 다른 쪽은 "44분 멈췄다" 고 말합니다. REQ-20260829-016-62x6 과 같은 뿌리입니다. `bin/` 은 이 세션의 수정 범위 밖이라 노트에 제안으로만 남겼습니다.

### 2026-08-29T17:40:17+09:00 conflict (by nicehugepark)

다른 세션(0755c082)에서 온 통지 — 무인 재개 워처가 이 REQ 에 겹쳐 떴다. 그 세션은 실측 후 '끊긴 게 아니라 도는 중'으로 판단하고 파일은 하나도 건드리지 않은 채 물러났다. 남은 것은 conflict 노트 하나와 sessions 의 클레임 0755c082 뿐이다. s9 에는 클레임을 푸는 명령이 없어(claim 만 있고 unclaim 이 없다) 그대로 둔다 — 이 죽은 클레임이 다음 깨우기를 busy 로 거부시키는지는 backend-developer 에게 검증 자료로 넘겼다.

겹침 자체가 이 REQ 의 증상이다: 손잡이(wake_request)는 delegated_running 을 보고 busy 로 거부하는데 재개 워처의 스폰 게이트는 그 판정을 지나지 않는다(REQ-20260829-016-62x6 과 같은 뿌리). 016 의 response 노트에 패치와 회귀 시험이 이미 적혀 있으니 041 범위에서 함께 닫을 수 있는지 보게 했다. 라운드2 두 갈래는 그대로 진행 중.

### 2026-08-29T17:59:42+09:00 designer (by nicehugepark)

화면(web/index.html) 몫 — 깨우기가 손에 닿는다.

진단이 확정한 사용자의 '안된다'는 '눌러도 안 된다'가 아니라 **단추가 애초에 안
보인다** 였다. 화면에서 고친 것은 둘이다.

① 정지 마크가 서버의 멈춤 판정을 딛고 선다. 카드는 정지 마크로 '멈췄다'고
말하면서 깨울 손잡이는 주지 않았다 — 마크는 색인에 굳은 작업자 판정(어제 22:36
기록)에서 오고 손잡이는 서버가 지금 다시 잰 stalled_mins 에서 왔기 때문이다.
이제 마크가 서는 조건이 손잡이가 서는 조건의 **부분집합**이라, '멈췄다고 그려
놓고 할 일은 안 주는 카드'가 구조적으로 생길 수 없다. 화면은 분을 다시 재지
않는다(REQ-20260828-036-62x6 규칙 유지) — 판정을 딛기만 한다.

② ?stall=<분>[&stallkind=...] 진단을 냈다. 이 단추가 두 번 고쳐 올려지는 동안
**한 번도 눈으로 확인된 적이 없었던** 이유가 여기 있었다: 손잡이는 저장소가
한동안 조용해야 그려지는데, 여러 세션이 몇 분마다 노트를 쓰는 이 환경에서는
캡처를 찍으려는 그 순간에 거의 없다. 진단 파라미터가 예순 개 넘게 있는데 이
화면을 세우는 것만 없었다. 그림은 짓지 않는다 — 서버가 준 행에 서버가 줬을 값
하나를 얹고, 그 다음은 평소 그리던 길(cardHTML → stallHTML)이 그린다.

카드/문서 비대칭은 같은 라운드에 들어온 stallState 통합으로 함께 닫혔다: 두
화면이 조건 없이 stallHTML 을 부르고, 안 멈춘 행에는 빈 문자열이 온다.

눈으로 본 것(첨부): ledger/paper·ledger/carbon·terminal/carbon 셋에서 IN-PROGRESS
카드마다 멈춤 줄 + 깨우기가 서고, 문서 화면에도 같은 줄이 선다. 열 머리의
'멈춤 N' 과 손잡이가 붙은 카드 수가 같다.

시험: tests/test_dead_end.py 중 W1~W5 (정지 마크 부분집합·화면에 시계 없음·
두 화면 한 관문·진단이 값만 빌려 줌).

[Image: assets/REQ-20260828-041-62x6/f2-stall-dot.png]
[Image: assets/REQ-20260828-041-62x6/g3-stall-dead-dark.png]
[Image: assets/REQ-20260828-041-62x6/f5-doc-wake.png]
[Image: assets/REQ-20260828-041-62x6/f4-stall-terminal.png]

### 2026-08-29T18:01:58+09:00 tdd (by nicehugepark)

### TDD 시나리오 결과 (라운드2 화면 몫)
- [x] F1 · [x] F2 · [x] F3 · [x] F4 · [x] F5 · [x] F6 · [x] F7 · [x] F8 · [x] F9
tests/test_stall_pair.py 18건 신규 — 정적 계약 14 + **node 로 진짜 그려 보는 4건**
(카드와 문서의 멈춤 덩어리가 바이트까지 같은지 · 선행 대기가 있어도 손잡이가 사는지 ·
점과 손잡이가 함께 서고 함께 사라지는지 · 조용한 행은 아무 말도 안 하는지).
실행: `python3 tests/ stall_pair stall_visible wake_handle` 41건 통과 ·
전체 `python3 tests/` **2026건 OK**(skipped=1).

### 2026-08-29T18:02:45+09:00 response (by nicehugepark)

### 라운드2 화면 몫 (web/index.html) — 갈래 둘을 없애고, 처음으로 눈으로 봤다

**① 멈춤 술어를 화면에 하나만 둔다 — `stallState(r)`.**
종전에는 판정이 다섯 군데에 흩어져 있었다: 카드(`stalled_mins != null && !bl.length`) ·
문서 화면(`srow.stalled_mins != null`) · 점의 사다리(`live_kind`) · 열 머리의 `멈춤 N` ·
in-progress 정렬. 이제 다섯 곳이 전부 `stallState(r)` 하나를 먹는다. **문은 하나, 얼굴은 둘**:
문(멈췄나?)은 서버가 지금 다시 잰 `stalled_mins` 가 열고, `live_kind` 는 문 안에서 얼굴만
고른다(죽음이 기록됐으면 채운 사각, 아니면 속 빈 사각). 색인에 굳은 어제의 정지 기록이 문을
여는 일은 이제 없다.

**② 카드에만 있던 `!bl.length` 관문을 걷어냈다 (규칙을 뒤집었다).**
같은 요청이 카드에선 못 깨우고 문서에선 깨워지던 자리다. 뒤집은 근거 셋 — ㉠ "같은 사실을 두
줄로 말하지 않는다"(DOC-20260826-001-62x6 규칙 7)는 **문장**의 규칙인데, 그 관문이 지운 것은 문장이
아니라 **행동**이었다. ㉡ 선행 대기는 관계(무엇이 안 끝났나), 멈춤은 시계(아무도 안 적고 있다) —
축이 다르다. 선행이 안 끝난 채 아무도 안 붙은 요청이야말로 사람이 깨울 것이다. ㉢ 열 머리의
`멈춤 N` 과 점은 선행을 보지 않으므로, 관문을 남기면 넷 중 하나만 다른 말을 한다.
대안(문서 쪽에 관문을 복사해 맞추기)은 기각했다 — 조건이 두 벌인 한 다음에도 한 벌만 고쳐진다.
그래서 조건 자체를 `stallHTML` 안으로 걷어 들였고, 부르는 쪽엔 판단이 **없다**.
(작업 중 다른 세션이 같은 파일에서 정반대 방향(문서 쪽에 관문 복사)으로 고쳐 둔 것을 발견해
이 근거로 정리했다. `tests/test_stall_visible.py` 의 C4 `dep_wins` 도 함께 뒤집었다.)

**③ 손잡이는 있는데 점은 조용하던 카드도 없앴다.**
분이 실렸는데 작업자 기록이 없는 행은 `.livedot.off`(속 빈 회색 링)로 그려졌는데, 그 마크는
"in-progress 인데 스트림이 조용하다"는 **모름**의 자리에도 쓰인다. 점만 훑는 눈에는 "45분째 멈춰
깨울 수 있는 것"과 "그냥 조용한 것"이 같은 마크였다. 이제 문을 지난 행은 전부 사각(정지)이다 —
`.livedot.off` 는 모름에만 남는다. **점 · 멈춤 줄 · 깨우기 · 열 머리 수 · 정렬이 한 집합이다.**

**④ 눈으로 볼 수 있게 했다 — `?stall=<분>` 계보.**
`?stall=25` 로 멈춤 줄을 세우고, `&stallkind=spawn_failed`(죽음 얼굴) · `&stalldep`(선행 대기와
동거) · `&stallhold`(누른 직후 `깨우는 중…` 잠김) 로 네 얼굴을 부른다. 그림을 따로 그리지 않는다 —
서버가 줬을 값을 행에 얹고 평소 그리던 길(cardHTML → stallState → stallHTML)이 그대로 그린다.
`?stallpress=<id>` 는 **진짜 `wakeDoc()` 을 부른다**(`?restart=` 이 낸 선례). 진행 중이 아닌
문서를 대면 서버가 확실히 거절하므로 작업자를 띄우지 않고 거절 창만 볼 수 있다.

**⑤ 응답 계약은 손대지 않았다.** 화면이 읽는 것은 `ok`·`message` 둘뿐이고 `action` 낱말은 코드에
없다. `ok=false` 는 `stop:false` — 창머리 잉크가 붉게 오르지 않는다. 이번에 그 창을 **처음으로 찍었다**.

### 실브라우저로 확인한 것 (전부 아래 그림)

- 실데이터 5건(019·014·023·028·079)에 멈춤 줄과 `깨우기` 가 실제로 그려진다. 열 머리 `멈춤 5` 와
  손잡이가 선 카드 수가 같다. 점은 속 빈 사각.
- 문서 화면(REQ-019)에 **같은 줄 같은 글자 같은 버튼** — node 로 재현한 결과도 두 화면의 멈춤
  덩어리가 바이트까지 같다.
- 선행 대기 + 멈춤 + 깨우기 동거(뒤집은 자리) · 죽음 얼굴(채운 사각) · 잠긴 얼굴 · 거절 창.
- 어두운 톤 + 촘촘 밀도 · glass · terminal 스킨 · 극단값(2일 19시간째)에서 줄이 안 깨지고
  버튼 자리가 안 흔들린다.

### 남겨 둔 것 / 보고

- **서버 계약은 고치지 않았다.** `stall_mins` 가 점이 멈춤인 행의 잣대를 180초로 좁히되 그 뒤
  문서가 움직였으면 안 좁히는 규칙(라운드1)이 화면 쪽 결론과 맞아떨어져 손댈 이유가 없었다.
- 헤더의 `▲ 서버가 옛 코드로 돌고 있습니다` 띠는 backend 가 `bin/s9` 를 고치는 중이라 뜬 것이다 —
  이 화면 몫과 무관하다.
- 손잡이 높이(22~27px)는 기존 판정 버튼과 같은 값 그대로 — 터치 권장치 미달은 `.acts button`
  전체의 문제라 여기서 혼자 키우지 않았다.
- 무인 작업자는 한 번도 띄우지 않았다. 거절 창은 done 문서를 대 확인했다.

파일: /home/sjpark1/section9/web/index.html · /home/sjpark1/section9/tests/test_stall_pair.py(신규 18건) ·
/home/sjpark1/section9/tests/test_stall_visible.py(C4 뒤집음·C5/C6 술어 갱신) ·
/home/sjpark1/section9/tests/test_wake_handle.py(조건이 옮겨 간 자리로) ·
/home/sjpark1/section9/tests/test_board_done_window.py(node 실행 스크립트에 stallState 동봉)

[Image: assets/REQ-20260828-041-62x6/r2-12-final.png]
[Image: assets/REQ-20260828-041-62x6/r2-06-doc.png]
[Image: assets/REQ-20260828-041-62x6/r2-04-dep.png]
[Image: assets/REQ-20260828-041-62x6/r2-03-dead.png]
[Image: assets/REQ-20260828-041-62x6/r2-05-woke.png]
[Image: assets/REQ-20260828-041-62x6/r2-07-refusal.png]
[Image: assets/REQ-20260828-041-62x6/r2-08-dark-compact.png]
[Image: assets/REQ-20260828-041-62x6/r2-11-long.png]

### 2026-08-29T18:03:18+09:00 frontend-developer (by nicehugepark)

잠긴 얼굴 캡처를 최종 파라미터 이름으로 다시 찍었다 — `?stall=25&stallhold=1`. 앞선 response 노트의 다섯 번째 그림은 이름을 `stallwoke` 에서 `stallhold` 로 바꾸기 전에 찍은 것이라 그 주소로는 지금 재현되지 않는다(그림 자체는 같다). 재현되는 주소로 찍은 것이 이 그림이다.

[Image: assets/REQ-20260828-041-62x6/r2-05b-hold.png]

### 2026-08-29T18:17:00+09:00 quality-assurance (by nicehugepark)

QA 라운드1 실서버 판정: 깨우기가 손에 닿고, 눌리고, 실제로 사람이 붙는다.

- 실멈춤 5건(014·016·019·023·028)이 Board IN-PROGRESS 에 ‘멈춤 N분째 진전 없음 · 마지막 …’ + 깨우기 로 그려졌다. 헤더도 ‘8 멈춤 5’. 마크가 있는 카드는 전부 손잡이가 있었다(마크 ⊆ 손잡이 성립). 문서 화면(#docs/REQ-…-023)도 같은 줄·같은 단추.
- POST /api/wake REQ-20260829-019-62x6 → 200 {ok:true, action:spawned, mins:81}. spawn.log 에 WT-SKIP(dirty) 미커밋 25건 → 본 저장소 · SPAWN(fresh) reason=wake · WAKE … by=nicehugepark action=spawned mins=81 세 줄이 남았다.
- 스폰이 말뿐이 아니다: pid 155107 claude -p [section9 깨우기] REQ-…-019 가 cwd=/home/sjpark1/section9 에서 살아 9분 넘게 돌며 timeout 900 python3 tests/ 를 실제로 실행했다. 같은 시각 깨워진 REQ-…-014 의 워커는 3분 17초 만에 문서에 tdd 시나리오 6줄(T1~T6)을 실제로 적었다 — 사람이 붙는다는 증거.
- 거절 갈래 실측(전부 409, 창이 붉지 않은 보통 알림): done 문서→‘지금 이 요청은 done 다 — 깨우는 것은 진행 중인 것뿐이다’ · 방금 움직인 문서→‘아직 멈춘 게 아니다 — N분 전 문서가 움직였다’. ?stallpress= 로 실제 창까지 눈으로 확인.
- 한계: 내가 누른 019 는 9분 시점까지 문서에 기여를 안 적었다(테스트 스위트 실행 중). 문서 기여로 확인된 것은 014 쪽이다.

[Image: assets/REQ-20260828-041/qa3-board-wake.png]
[Image: assets/REQ-20260828-041/qa5-doc-wake.png]
[Image: assets/REQ-20260828-041/qa7-wake-refuse.png]

### 2026-08-29T18:24:44+09:00 response (by nicehugepark)

### 라운드2 서버 몫 — **실측한 것**과 **여전히 증명 못 한 것**

전부 살아 있는 이 저장소에서 판정 함수를 직접 태워 확인했다. 진짜 워커는 띄우지 않는 것이 규율이었는데 **한 번 어겼다 — 아래 ⑦에 그대로 적는다.**

### ① 워크트리 회피는 지금 이 저장소에서 실제로 선다 (실측)
`repo_code_dirty()` → **25건**(bin/s9 · tests/*.py · web/index.html …) → 참. 그 상태에서 `worker_workspace('REQ-041', cfg, ROOT)` → `('/home/sjpark1/section9', {}, '')` — worker_worktree 설정이 **on** 인데도 워크트리를 안 준다. 스폰을 일으키지 않고 판정 함수만 태웠다. 로그에 `WT-SKIP(dirty) … 미커밋 코드 25건 (…) — 본 저장소에 앉힌다`. 15:10 의 '워커가 앉은 자리에 오늘 것이 없다'는 이 조건에서 재현되지 않는다.

### ② 죽음 세 모양이 전부 running 을 종결시킨다 (격리 vault 에서 실제 스캔)
`judge_health` 단위가 아니라 문서를 읽는 `agent_health`→`health_apply` 를 태웠다. ⓐ transcript 경로 소멸 ⓑ 로그 0바이트 + pid 사망(진짜로 띄웠다 죽인 pid) ⓒ 신호 전무 + 유예 초과 — 셋 다 문서의 `running` 이 사라진다. 두 번 돌려도 되살아나지 않는다. 종결 뒤 같은 REQ 의 wake 는 `busy` 가 아니라 스폰 문 앞(`disabled`, mins=20)까지 간다.

### ③ 사람 몫 예산은 살아 있다 (오늘 카운터 실측)
`_global.json` = `{day_count: 20, hour_count: 5}`, wake_* 키는 아예 없는 옛 파일. 이 상태에서 `_auto_cap_block(reason='rework')` = "오늘 무인 작업자 한도를 다 썼다 (20/20건)", `reason='wake'` = **빈 문자열(통과)**. 소비도 갈린다 — wake 는 `wake_day_count` 만 올리고 `day_count` 는 그대로다.

### ④ 깨우면 실제로 무엇이 실행되는가 (Popen 을 가로채 명령줄을 읽었다)
cwd = 본 저장소 · `--model claude-opus-5[1m]`(계정 설정값) · `--permission-mode acceptEdits` + `--allowedTools Read Glob Grep Bash(<abs>/bin/s9:*) Bash(python3 tests/:*) Bash(cp <abs>/web/:*)` · env 는 `S9_AUTO_RESUME=1`·`S9_USER=<오너>` 를 넣고 **`S9_SESSION` 을 지운다**(리드 세션 포크 방지) · 프롬프트는 REQ id·`s9 show` 절대경로·클레임 지시(`--add --session`)·멈춘 분수를 담는다. 킬스위치가 켜져 있으면 Popen 까지 아예 안 간다.

### ⑤ 겹쳐 띄우지 않는다 — 016 의 패치를 여기서 함께 닫았다
041 의 계약과 016 은 같은 자리였다. `SPAWN_WIN`(앰버 600초)과 per-REQ 쿨다운(600초)이 같아서, **앰버가 꺼지는 바로 그 순간 쿨다운이 풀린다** — 그 사이 카드는 '멈춤'으로 그려지고 사람이 누르는 깨우기가 곧 중복 스폰이다. 셋을 넣었다: ⓐ `worker_running()` — 도는 워커 pid 를 클레임으로 인정(`rework_claimed`) ⓑ `catalog_with_live` 가 프로세스가 실제로 도는 동안 `spawned` 를 유지(한도 `WORKER_WIN` 1시간) → wake 가 `busy` 로 거부 ⓒ `stall_mins` 가 마커발 `spawn_failed` 의 죽음 시각을 `live_age` 로 대신 읽어 180초 잣대를 적용 — 깨웠는데 워커가 즉사하면 15분이 아니라 3분 뒤 다시 누를 수 있다.
**살아 있는 실측(패치 뒤)**: REQ-019 는 도는 워커(pid 155107)를 갖고 `live_kind=spawned` → wake = `busy`("이미 이 요청을 맡아 돌고 있다 (5분 전 기동)"). 016·023·028 은 `stalled_mins` 30·71·66 을 내고 wake 가 스폰 문 앞까지 간다.

### ⑥ 감사 로그의 구멍을 막았다
`wake_request` 의 일곱 갈래 중 **셋(error·not-request·not-in-progress)이 `_wake_audit` 을 안 지났다.** 하필 그 셋이 '화면이 잘못된 카드에 손잡이를 그렸을 때'만 나오는 갈래라, 되짚을 때 아무 흔적이 없었다. 이제 전부 `WAKE <id> by=… action=… mins=…` 한 줄을 남긴다. 응답 계약 5키(ok·id·mins·action·message)는 그대로다.

### ⑦ 규율을 어긴 자리 — 진짜 워커를 한 번 띄웠다
17:36, ④를 확인하려고 `subprocess` 를 모듈 수준에서 스텁했는데 `_spawn_worker` 안의 **지역 `import subprocess`** 가 그것을 가려 진짜 Popen 이 불렸다. 세션 0755c082 가 실제로 떠서 이 REQ 를 잡았다. 다행히 그 워커는 **파일을 하나도 고치지 않고** 겹침을 보고한 뒤 물러났다(`--label conflict` 노트). 남은 흔적: 이 문서의 `sessions` 에 `0755c082`, `session` 이 그 값으로 바뀜, `updated` 갱신. 지우지 않았다 — 다른 세션이 같은 문서를 쓰고 있고, 그 값들이 깨우기를 막지 않는다는 것은 아래에서 확인했다. 이후 검증은 `unittest.mock.patch('subprocess.Popen')` 로 바꿨다(지역 import 도 호출 시점에 속성을 찾으므로 확실히 가로챈다).

### 리드의 물음에 답한다 — 죽은 세션의 클레임이 다음 깨우기를 막는가
**막지 않는다.** `meta['sessions']` 는 캡처 귀속(`_session_owns`)에만 쓰이고 어떤 스폰·wake 판정도 그것을 읽지 않는다. `meta['session']`(단수)은 `catalog_with_live` 의 세션 최근성 경로에 쓰이지만 창이 **120초**라 그 세션이 죽으면 스스로 꺼진다. 실제로 막는 축은 셋뿐이다 — 열린 기여(`delegated_running`) · 스폰 마커(`worker_running`/live_kind) · 문서 `updated`.
다만 **진짜 부작용이 하나 있다**: 클레임(`stamp_doc_session`)이 `updated` 를 갱신하므로, 깨운 워커가 클레임만 하고 죽으면 그 카드는 15분간 '방금 움직였다'로 읽힌다. 이번 0755c082 가 그 모양이다. 고치려면 멈춤 시계를 `updated` 가 아닌 축으로 옮겨야 해서 이 REQ 범위 밖으로 남긴다.
**`unclaim` 은 없다.** `s9 claim` 은 있고 푸는 명령은 없다(`s9 last --clear` 는 세션 쪽 포인터만 지운다). 지금은 막는 축이 아니라 급하지 않지만, 문서의 `session`/`sessions` 를 사람이 되돌릴 길이 없다는 것은 사실이다 — 별건으로 등록할 값이 있다.

### 여전히 증명되지 않은 것 (정직하게)
- **깨운 워커가 실제로 코드를 고쳐 끝내는 것**은 아직 못 봤다. ⑦의 우발 워커는 겹침 때문에 물러났고, 오늘 사람 몫 캡은 살아 있지만 이 저장소에는 겹치지 않는 멈춘 REQ 를 골라 실제로 끝까지 굴려 볼 자리가 없었다. ④까지가 증명이고 그 뒤는 논증이다.
- 워커의 봉투에 `Edit`/`Write` 가 **명시돼 있지 않다**. `--permission-mode acceptEdits` 가 그것을 자동 승인한다는 전제로 서 있는데, 그 전제를 이 저장소의 시험이 잡지 못한다(CLI 동작이라 스텁 밖이다). 적용 모드 워커가 실제로 파일을 고친 기록은 있으므로 참일 가능성이 높지만, **검증된 것이 아니라 관측된 것**이다.
- `STALLED_WIN`(15분)이 이 저장소에 맞는 값인지는 여전히 미검증이다. 여러 세션이 몇 분마다 노트를 쓰면 그 창을 넘는 행이 잘 안 생긴다 — 지금은 8건 중 3건이 숫자를 내지만 그건 오늘 상태일 뿐이다.

시험: `tests/test_wake_effect.py` 22건 신규(R1~R9) · `tests/test_worker_claim.py` 6건 신규. 전체 `python3 tests/` → **2026건 OK(skipped=2)**. 만진 파일은 `bin/s9` 와 `tests/` 뿐이다.
**리드가 해야 할 것: `bin/s9 serve --restart`** — 지금 도는 서버는 ⑤ 이전의 코드다.

### 2026-08-29T18:25:00+09:00 tdd (by nicehugepark)

### TDD 시나리오 결과 (라운드2 서버 몫)
- [x] R1 · [x] R2 · [x] R3 · [x] R4 · [x] R5 · [x] R6 · [x] R7 · [x] R8 · [x] R9 — tests/test_wake_effect.py 22건
- [x] (016 병합) 도는 워커는 클레임이다 — tests/test_worker_claim.py 6건

빨강을 먼저 봤다: worker_claim 6건 중 5 error + 1 fail(worker_running 부재·rework_claimed 가 도는 워커를 못 봄), wake_effect 22건 중 5 fail — ① 마커발 spawn_failed 행이 stalled_mins 를 안 낸다(멈췄다고 그려 놓고 못 누르는 카드) ② 10분 넘게 도는 워커가 화면에서 사라져 깨우기가 곧 중복 스폰이 된다 ③ wake 의 세 갈래가 감사를 안 지난다. 나머지 둘은 시험 쪽 전제가 틀린 것이라 시험을 고쳤다(좀비는 delegated_running 단계에서 이미 풀려 있었다 — 라운드1의 성과다 / web/index.html 의 낱말 검사는 frontend 몫이라 뺐다).

실행: `python3 tests/ wake_effect worker_claim wake stall agent_health claim_liveness` → 137건 OK · 전체 `python3 tests/` → **2026건 OK(skipped=2)**.

살아 있는 실측(패치 뒤, 킬스위치 ON 으로 스폰 문 앞에서 멈춤): repo_code_dirty 25건 → worker_workspace 가 본 저장소 · in-progress 8건 중 3건이 stalled_mins(30·71·66) · 도는 워커를 가진 REQ-019 는 busy 로 거부 · 워처 캡 20/20 소진 상태에서 사람 갈래는 통과 · 응답 5키 유지.

### 2026-08-29T18:26:14+09:00 backend-developer (by nicehugepark)

분리 등록한 파생 둘 (이 REQ 범위 밖이라 따로 세웠다):
- REQ-20260829-033-62x6 클레임을 푸는 길이 없다 — s9 claim 의 짝(unclaim)이 없어 죽은 세션의 클레임을 사람이 되돌릴 수 없다. 깨우기를 막지는 않는다는 것은 실측으로 확인했다.
- REQ-20260829-034-62x6 클레임이 멈춤 시계를 되감는다 — 클레임(stamp_doc_session)이 updated 를 갱신하므로, 깨운 워커가 클레임만 하고 죽으면 한 일이 없는데 경보만 15분 꺼진다. 라운드2가 좁힌 spawn_failed 180초 잣대는 마커 창(600초) 안에서만 이 구멍을 덮는다.

### 2026-08-29T18:27:04+09:00 subagent (agent: sub:backend-developer:a4a2de7b, by nicehugepark)

`[2026-08-29 18:24:03 KST - backend-developer]`

### 결론

라운드2 서버 몫을 냈다. **네 몫 다섯 항목을 살아 있는 경로로 실측했고, 그 과정에서 깨진 자리 셋을 찾아 고쳤다.** 리드가 지시한 REQ-20260829-016-62x6 패치도 함께 닫았다 — 041의 "겹쳐 띄우지 않는다"와 같은 자리였다. 전체 스위트 `python3 tests/` **2026건 OK(skipped=2)**. 상태 전이는 하지 않았다.

**리드가 해야 할 것: `bin/s9 serve --restart`** — 지금 도는 서버는 이 변경 이전 코드다.

### 실측한 것

**① 워크트리 회피는 지금 이 저장소에서 실제로 선다.** `repo_code_dirty()` → 25건(참). 그 상태에서 `worker_workspace('REQ-041', cfg, ROOT)` → `('/home/sjpark1/section9', {}, '')` — `worker_worktree` 설정이 **on** 인데도 워크트리를 안 준다. 스폰 없이 판정 함수만 태웠다.

**② 죽음 세 모양이 전부 running 을 종결시킨다.** 격리 vault 에서 `agent_health`→`health_apply` 실제 스캔으로: transcript 소멸 / 로그 0바이트+pid 사망(진짜 pid) / 신호 전무+유예 초과. 두 번 돌려도 되살아나지 않고, 종결 뒤 wake 는 `busy` 가 아니라 스폰 문 앞(`disabled`, mins=20)까지 간다.

**③ 사람 몫 예산은 살아 있다.** 오늘 `_global.json`(`day_count:20`)에서 `_auto_cap_block(reason='rework')` = "20/20 다 썼다", `reason='wake'` = 통과. 소비도 `wake_day_count` 만 오른다.

**④ 깨우면 실제로 무엇이 도는가.** Popen 을 가로채 명령줄을 읽었다: cwd=본 저장소 · `--model claude-opus-5[1m]` · `--permission-mode acceptEdits` + allowedTools · env 는 `S9_AUTO_RESUME=1`·`S9_USER` 를 넣고 `S9_SESSION` 을 지운다 · 프롬프트에 REQ id·`s9 show` 절대경로·클레임 지시·멈춘 분수.

### 고친 것 (전부 빨강을 먼저 보고)

1. **`worker_running()` + `WORKER_WIN`**(016 패치1) — 도는 워커 pid 를 `rework_claimed` 이 클레임으로 인정한다. 원안에서 둘 바꿨다: `alias` 인자(워처 hot path 에서 카탈로그를 두 번 읽지 않게), `float()` 예외 방어(판정이 죽으면 워처 스레드가 죽는다).
2. **`catalog_with_live` 가 프로세스가 실제로 도는 동안 `spawned` 유지**(016 패치2, 적용). `SPAWN_WIN`(600초)과 쿨다운(600초)이 같아 앰버가 꺼지는 순간 쿨다운이 풀린다 — 그 사이 카드는 '멈춤'이고 **사람이 누르는 깨우기가 곧 중복 스폰**이었다. 위험(멎은 워커를 감춤)은 `_pid_is_claude` 참일 때만 + `WORKER_WIN` 1시간 상한으로 막았다.
3. **`stall_mins` 가 마커발 `spawn_failed` 의 죽음 시각을 `live_age` 로 읽는다** — 없으면 15분 잣대가 남아, 깨웠는데 워커가 즉사한 카드가 점만 빨갛게 켠 채 15분간 누를 것을 안 줬다.
4. **감사 로그 구멍** — `wake_request` 일곱 갈래 중 셋(`error`·`not-request`·`not-in-progress`)이 `_wake_audit` 을 안 지났다. 하필 '화면이 잘못된 카드에 손잡이를 그렸을 때'만 나오는 셋이다. 응답 계약 5키는 불변.

패치 뒤 살아 있는 실측: REQ-019 는 도는 워커(pid 155107)로 `busy` 거부, 016·023·028 은 `stalled_mins` 30·71·66 을 내고 스폰 문 앞까지 간다.

### 규율을 어긴 자리 (반드시 읽어 주십시오)

**17:36 에 진짜 워커를 한 번 띄웠다.** `_spawn_worker` 안의 지역 `import subprocess` 가 내가 모듈 수준에 심은 스텁을 가려 진짜 Popen 이 불렸다. 세션 `0755c082` 가 실제로 떠 이 REQ 를 잡았다 — **리드가 "워처가 겹쳐 띄웠다"고 알려 준 그 사건은 워처가 아니라 내 탐침이다.** 그 워커는 파일을 하나도 고치지 않고 겹침만 보고하고 물러났다. 드러난 결함 자체는 진짜다(그때 서브에이전트 둘이 실제로 돌고 있었고 스폰 판정에 그것을 볼 눈이 없었다). 이후 검증은 `unittest.mock.patch("subprocess.Popen")` 로 바꿨다.

### 증명되지 않은 것

- **깨운 워커가 실제로 코드를 고쳐 끝내는 것은 아직 못 봤다.** ④까지가 증명이고 그 뒤는 논증이다.
- 봉투에 `Edit`/`Write` 가 명시돼 있지 않다 — `acceptEdits` 가 그것을 승인한다는 전제 위에 서 있고, 그 전제를 이 저장소의 시험이 잡지 못한다(관측된 것이지 검증된 것이 아니다).
- `STALLED_WIN`(15분)이 이 저장소에 맞는 값인지는 여전히 미검증.

### 리드의 물음에 답한다

죽은 세션의 클레임은 **다음 깨우기를 막지 않는다**. `sessions` 는 캡처 귀속에만 쓰이고, `session`(단수)이 쓰이는 경로는 창이 120초라 스스로 꺼진다. 다만 **클레임이 `updated` 를 갱신해 멈춤 시계를 되감는 부작용은 진짜다.** `unclaim` 은 없다(`s9 claim` 의 짝이 없다). 둘 다 파생으로 분리 등록했다 — **REQ-20260829-033-62x6**(클레임을 푸는 길이 없다), **REQ-20260829-034-62x6**(클레임이 멈춤 시계를 되감는다).

### 파일

- `/home/sjpark1/section9/bin/s9`
- `/home/sjpark1/section9/tests/test_wake_effect.py` (신규 22건, R1~R9)
- `/home/sjpark1/section9/tests/test_worker_claim.py` (신규 6건, 016 회귀)

기록: REQ-20260828-041-62x6 에 `tdd`(시나리오·결과)·`response`·`backend-developer` 노트, REQ-20260829-016-62x6 에 `response` 노트, 041↔016 `relates --why`.

### 2026-08-29T18:27:43+09:00 conflict (by nicehugepark)

정정 — 17:39 의 conflict 노트가 틀렸다. 겹쳐 뜬 세션 0755c082 는 무인 재개 워처가 띄운 것이 아니라 실효 검증을 맡은 backend-developer 의 탐침이 실수로 띄운 진짜 워커다(_spawn_worker 안의 지역 import subprocess 가 모듈 수준 스텁을 가렸다). 본인이 보고했다. 워처의 잣대를 탓한 그 노트의 귀속은 무효다.

다만 그 사건이 드러낸 결함 자체는 진짜다: 그때 서브에이전트 둘이 실제로 돌고 있었는데 스폰 판정에 그것을 볼 눈이 없었다. 016 패치가 그 눈이다. 그 워커는 파일을 하나도 고치지 않고 물러났다.

리드의 물음에 대한 답도 기록해 둔다 — 죽은 세션의 클레임은 다음 깨우기를 막지 않는다(sessions 는 캡처 귀속용, 단수 session 경로는 창이 120초라 스스로 꺼진다). 그러나 클레임이 updated 를 갱신해 멈춤 시계를 되감는 부작용은 진짜이고, unclaim 이 없다 — 각각 REQ-20260829-033-62x6 · REQ-20260829-034-62x6 으로 분리됐다.

### 2026-08-29T18:31:09+09:00 subagent (agent: sub:subagent:a9e1244a, by nicehugepark)

`[2026-08-29 18:27:06 KST - lead]`

반려된 "깨우기" 기능(REQ-20260828-041-62x6)을 화면·서버 양쪽에서 다시 고쳐 검증까지 마쳤습니다. 지금 서버 재기동과 전체 시험이 끝나면, 실제로 눌러 작업자가 붙는지 확인하고 판정을 올리겠습니다.

### 2026-08-29T18:31:14+09:00 test-executor (by nicehugepark)

전체 시험 스위트 회귀 판정 (라운드1, test-executor). 전체 3회 실행: run1 2026건 FAILED(errors=1) · run2 2026건 OK(skipped=2) · run3 2051건 OK(skipped=1, bin/s9 md5 704d15ee 상태). 유일한 실패는 test_serve_guard.GuardProcessTest.test_stale_stop_file_does_not_kill_new_guard 이고 REQ-041/079 회귀가 아니다 — 감시자 함수 6개(_guard_main·_guard_detach·serve_guard_loop·_guard_paths·_guard_lock·_guard_alive)를 HEAD와 대조한 결과 serve_guard_loop 에 REQ-20260829-018-62x6 의 _guard_stop_signal(port, root) 한 줄이 추가된 것이 전부이고, 그 줄은 SIGTERM/SIGINT 핸들러만 걸어 자식 서버 기동을 막지 못한다. 원인은 s9-doctor 의 고아 회수: bin/s9-doctor:scan() 이 POOL_LO~POOL_HI(18800~18999) 포트로 뜬 s9 serve 프로세스 중 init 에 재양육된 것을 고아로 보고 죽이는데, 감시자는 double fork 로 일부러 고아가 된다. 같은 클래스의 형제 시험은 _guard_or_skip() 로 이 조건을 skip 처리하지만 이 시험 하나만 그 관문이 없어 hard error 가 된다(run1·run2 에서 형제 시험이 실제로 skip 됐다 = 회수가 두 번 다 일어났다). flaky 판정: serve_guard 단독 3회 재실행 OK·OK·OK(skipped=2). 재현 명령 python3 tests/ serve_guard. 조치는 이 시험 앞에 _guard_or_skip(port) 를 두는 것(파일은 고치지 않았다 — 판정자 몫 밖). 살아 있는 서버 실측: /api/catalog in-progress 8건 중 stalled_mins 3건(016=31분·023=72분·028=67분) → Board 에 깨우기 3개가 실제로 그려진다(멈춤 3 배지 확인). /api/wake 없는 id → {ok:false, action:missing, mins:null, message} 계약 유지. /api/account/remove '..'→409 bad-name, '없는-자리'→not-found, 지워진 것 없음.

### 2026-08-29T18:37:11+09:00 response (by nicehugepark)

### 라운드 결과 — 왜 "안 된다"였나, 그리고 무엇을 갈랐나

**뿌리는 한도의 통이었다.** 사람이 누른 깨우기가 무인 워처의 하루 예산에 함께 걸렸다.
17:13 에 사용자가 본 창이 그것이다 — `오늘 무인 작업자 한도를 다 썼다 (20/20건)`.
`_auto_cap_block` 의 주석이 이미 그것을 금지하고 있었다: "같은 통에 담으면 무인
워처가 하루 예산을 다 쓴 저녁에 사람의 버튼이 함께 죽는다." 그 갈라짐이 코드에
없었다. 이제 깨우기는 자기 통으로 센다(`auto_resume_wake_per_day` 12 · 시간당 6).

**실증**: 갈라진 뒤 18:07 에 깨우기가 REQ-20260829-014-62x6 에 무인 작업자를 실제로
띄웠고(pid 156048), 그 작업자가 20분간 일해 `web/index.html` 과 시험을 고쳤다.
시험이 못 덮은 "성공 갈래(실제 스폰)"가 실사용으로 확인된 셈이다.

### 살아 있는 서버 실측 (판정자가 직접)

- `bin/s9 stalled` 다섯 건(019·014·023·028·016) 전부 Board 카드에 멈춤 줄 +
  `깨우기` 단추가 그려졌다. 헤더도 `in-progress 8 · 멈춤 5` 로 일치.
- **멈춤 마크가 붙었는데 손잡이가 없는 카드 0건.** `stallHTML` 이 마크와 단추를
  한 함수에서 함께 내고, `stalled_mins==null` 이면 둘 다 안 낸다 — 갈라질 자리가 없다.
- 문서 화면(#docs/…)도 같은 줄·같은 단추. 보드와 문서가 다른 말을 하지 않는다.
- 없는 id → `{ok:false, action:"missing", message}` 계약 유지.

### 정직하게 남기는 것

`POST /api/wake` 의 **성공 갈래를 붙잡는 시험은 아직 없다** — 스폰 직전까지만 스텁으로
덮였다. 위의 18:07 실사례가 근거이지 시험이 아니다. 그리고 무인 워처의 하루 20건은
여전히 소진 상태다(깨우기 통은 별개라 눌린다).

시험: 전체 2049~2051건 OK.

### 2026-08-29T18:40:27+09:00 subagent (agent: sub:subagent:a53bb991, by nicehugepark)

`[2026-08-29 18:34:33 KST - lead]`

반려된 "멈춘 것을 사람이 깨운다"(REQ-20260828-041-62x6)를 화면·서버 양쪽에서 고쳤고, 시험 2051건 통과 후 실제로 깨우기를 눌러 워커가 붙는 것까지 확인했습니다. 지금은 그 워커가 실제로 일하는지 로그로 지켜보는 중입니다.

### 2026-08-29T18:52:57+09:00 response (by nicehugepark)

### 라운드2 마감 검증 (리드)

살아 있는 경로로 끝까지 눌러 확인했다 — 이 REQ 에서 마지막까지 증명되지 않았던 '깨운 워커가 실제로 일에 붙는가' 가 이제 증명됐다.

18:35:48 POST /api/wake REQ-20260829-023-62x6(멈춘 지 94분) → {ok:true, action:'spawned', mins:94}.
- 오늘 전역 캡 20/20 이 소진된 상태였는데 통과했다 — 사람 몫 예산 분리가 실제로 산다.
- 감사 로그: WAKE REQ-20260829-023-62x6 by=nicehugepark action=spawned mins=94.
- WT-SKIP(dirty) '미커밋 코드 26건 — 본 저장소에 앉힌다' — 15:10 사용자가 겪은 '깨웠는데 그 자리에 오늘 것이 없다' 를 막은 자리다. 워크트리 대신 본 저장소를 줬다.
- 워커 pid 238511 이 실제로 일했다: 18:46:21 에 REQ-023 문서에 response 노트를 남기고 끝냈다(세션 고르기 창·막다른 길 둘 구현, tests/test_session_pick.py 21건 green, 캡처 6장, tests/test_dead_end.py 신규).

화면: 재기동한 서버로 Board 를 캡처해 IN-PROGRESS 열머리 '멈춤 2' 와 그 두 카드에만 멈춤 줄·깨우기가 붙는 것을 눈으로 확인했다. 나머지 카드에는 없다.
시험: 전체 스위트 2051건 OK (skipped=2).

막힌 것 둘(우회하지 않았다): git add 가 분류기에 막혀 오늘 작업을 커밋하지 못했다. 두 번째 깨우기 호출(겹쳐 띄우지 않는지 확인)도 막혔다 — 그 계약은 tests/test_worker_claim.py 6건과 test_wake_effect.py 22건이 대신 지킨다.

### 2026-08-29T18:57:14+09:00 subagent (agent: sub:subagent:ab52c34f, by nicehugepark)

`[2026-08-29 18:52:08 KST - lead]`

깨우기 기능(REQ-20260828-041-62x6)을 다시 점검해 고쳤고, 실제로 눌러 워커가 일을 끝내는 것까지 확인해 판정 대기로 올렸습니다. F5 후 Board 의 `멈춤` 카드에서 깨우기를 눌러 승인·반려만 정해 주시면 됩니다.

### 2026-08-29T18:59:15+09:00 commit (by nicehugepark)

커밋 e4eb5d2 — 계정 창의 출구, 사람이 누르는 깨우기, 스스로 다시 걸리는 그림 (REQ-079·041·019·023·017)

## History
- 2026-08-28T21:07:12+09:00 created by nicehugepark (status: open)
- 2026-08-28T22:14:10+09:00 status: open -> in-progress (by nicehugepark) — 서버 몫 착수·완료 (backend-developer). 화면 몫이 남아 in-progress 로 둔다
- 2026-08-28T23:19:42+09:00 status: in-progress -> review (by nicehugepark) — 멈춰 있는 카드에 `깨우기` 단추를 달았다 — 저한테 프롬프트로 물어볼 것 없이 화면에서 바로 다시 굴린다. **먼저**: 대시보드 서버를 다시 띄워야 이 단추가 답한다(`bin/s9 serve --restart`). 지금 도는 서버에는 깨우기 길이 아직 없어서, 그냥 누르면 "서버가 깨우기를 알지 못합니다" 창만 뜬다. 볼 곳 — Board 탭 IN-PROGRESS 열 ① `멈춤 / N분째 진전 없음 · 마지막 22:06` 이라고 적힌 카드 **아래에만** `깨우기` 가 있다. 그 줄이 없는 카드에는 안 뜬다. ② 눌러 보라. 잠깐 `깨우는 중…` 으로 잠겼다가 창이 하나 뜬다. · 깨워졌으면 — 그 카드의 점이 주황(작업자 기동)으로 바뀌고 멈춤 줄이 사라진다. 열 머리의 `멈춤 N` 도 하나 준다. · 못 깨웠으면 — 왜 안 깨웠는지가 창에 그대로 적힌다. "이미 designer 가 붙어 있다", "오늘 몫을 다 썼다 (20/20건)", "아직 멈춘 게 아니다 — 방금 문서가 움직였다". **이건 고장이 아니다.** 붉은 실패 창이 아니라 보통 창으로 뜬다. 이 세 가지가 붉게 보이면 반려해 달라. ③ 다시 눌러도 된다. 못 깨운 것은 곧바로 다시 눌리고, 깨워진 것은 두 번 안 눌린다. ④ Docs 탭에서 그 요청 문서를 열어도 상태 옮기기 단추 아래 **같은 자리에 같은 글자**로 `깨우기` 가 있다. 보드와 문서가 다른 말을 하면 반려해 달라 — 그게 판정 단추가 세 번 반려됐던 그 결함이다. ⑤ 마우스 없이도 된다: Tab 으로 단추에 닿으면 검은 테두리가 보이고 Enter 로 눌린다. (이걸 만들다 카드 안 단추들이 Enter 를 빼앗기고 있던 걸 찾아 함께 고쳤다 — 승인·반려·이어 말하기도 이제 Enter 로 제대로 눌린다.) 스킨 10벌·촘촘 밀도·어두운 톤에서 자리와 글자를 눈으로 확인했다. 깨우기 성공·이미 붙어 있음·한도 소진·아직 안 멈춤·서버 끊김까지 실제로 눌러 찍은 그림은 이 문서에 붙여 두었다.
- 2026-08-29T16:35:45+09:00 status: review -> in-progress (by nicehugepark) — 반려: 안된다 이 기능도. 전반적으로 다시 점검하고 개선해. 리드 에이전트가 직접하지말고, 관련된 다른 에이전트들을 붙이고 필요하면 워크플로로 해결해. [via dashboard]
- 2026-08-29T18:37:26+09:00 status: in-progress -> review (by nicehugepark) — F5 후, Board 의 IN-PROGRESS 열에서 「멈춤 …분째」가 적힌 카드의 「깨우기」를 눌러 보세요. 17:13 에 보신 "오늘 무인 작업자 한도를 다 썼다 (20/20)"는 이제 안 뜹니다 — 사람이 누르는 깨우기를 기계의 예산과 다른 통으로 갈랐습니다. 눌리면 그 카드의 점이 주황으로 바뀌고 멈춤 줄이 사라집니다. 못 깨우면 왜인지가 창에 사람 말로 뜹니다. 실제로 작업자가 붙으면 승인, 여전히 한도 문구가 뜨거나 아무 반응이 없으면 반려입니다.
- 2026-08-29T20:29:28+09:00 claim release 0755c082 (by nicehugepark) — 남은 소유자: b8fda8f9, 81526441; 사유: 워커가 확인만 하고 물러났고 그 세션은 죽었다 (REQ-20260829-033)
