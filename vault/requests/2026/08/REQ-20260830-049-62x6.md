---
id: REQ-20260830-049-62x6
type: request
title: 이어가기 창은 예외만
summary: 정상(바로 보임) 갈래는 카드의 ▶→⏸ 변화가 이미 답이라 창을 세우지 않고, 예외(끝나면 보임)일 때만 창이 선다 — 본문 2슬롯 위계(message+note 서버 계약)와 함께
goal: 이어가기 성공 시 main 갈래는 창 없이 카드 상태 변화로 답하고, worktree 갈래만 한 절 제목+부가 한 줄의 창이 서는 것이 실브라우저로 확인된다
status: review
size: M
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: 38ad98c8
sessions: ["982e7476", "38ad98c8"]
agents: ["sub:frontend-developer:ab663a6a", "sub:frontend-developer", "lead:claude-fable-5"]
contributions: [{"actor": "sub:frontend-developer:ab663a6a", "item": "049 이어가기 창은 예외만", "started": "2026-08-31T13:39:24+09:00", "ended": "2026-08-31T14:00:43+09:00", "result": "stalled", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/38ad98c8-51fd-4b50-a26a-495b58404817/tasks/ab663a6a68a95cb78.output", "reason": "188초째 진전 없음 (기준 180초)"}, {"actor": "sub:frontend-developer", "item": "TDD 시나리오 (이어가기 창은 예외만)", "started": "2026-08-31T13:43:07+09:00", "ended": "2026-08-31T13:43:07+09:00", "result": "done", "transcript": ""}, {"actor": "lead:claude-fable-5", "item": "리드 판정: card.js 6줄 수정 승인 — wakeDoc→wakeDlg 사이에 성공+무예외면 창을 안 세우는 판정(wake", "started": "2026-08-31T13:43:16+09:00", "ended": "2026-08-31T13:43:16+09:00", "result": "done", "transcript": ""}, {"actor": "sub:frontend-developer", "item": "049 구현 — 성공 경로의 창을 없애고, 답을 두 칸으로", "started": "2026-08-31T14:28:53+09:00", "ended": "2026-08-31T14:28:53+09:00", "result": "done", "transcript": ""}, {"actor": "sub:frontend-developer", "item": "049 실브라우저 캡처 — main 갈래(창 없음) · 워크트리 갈래(2슬롯 창) · 대기 갈래(창 유지) · 실제 키보드 E", "started": "2026-08-31T14:29:05+09:00", "ended": "2026-08-31T14:29:05+09:00", "result": "done", "transcript": ""}, {"actor": "sub:frontend-developer", "item": "TDD 시나리오 (개정 — 구현 후 판정)", "started": "2026-08-31T14:29:28+09:00", "ended": "2026-08-31T14:29:28+09:00", "result": "done", "transcript": ""}, {"actor": "sub:frontend-developer:ab663a6a", "item": "`[2026-08-31 13:39:26 KST - frontend-developer]`", "started": "2026-08-31T15:33:30+09:00", "ended": "2026-08-31T15:38:26+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/38ad98c8-51fd-4b50-a26a-495b58404817/subagents/agent-ab663a6a68a95cb78.jsonl"}]
project: section9
parent: REQ-20260830-048-62x6
children: ["REQ-20260831-009-62x6"]
relates_why: {}
tags: ["dashboard", "design", "attached", "png", "image"]
created: 2026-08-30T22:21:52+09:00
status_since: 2026-08-31T15:07:46+09:00
updated: 2026-08-31T15:53:36+09:00
priority: 50
---

## Original

REQ-20260830-048 designer 판정 A안 + PO 파생 분리 판정에서 갈라져 나온 작업. 근거는 두 판정 노트 참조: 이어가기는 비파괴·자동·되돌림 가능(⏸)이라 성공 경로 모달은 이 화면의 창 규율(물음 아니면 거절)과 어긋나고, 창이 자기가 가리키는 카드를 가린다. 실패/대기 갈래(waiting·busy·capped)의 창은 유지. 서버 응답 계약을 message+note 두 칸으로 늘리는 조정 포함(wakeDlg 단일 창 규칙과의 조정 비용 있음).

## Notes

### 2026-08-31T13:43:07+09:00 tdd (agent: sub:frontend-developer, by nicehugepark)

### TDD 시나리오 (이어가기 창은 예외만)

계약 판정: 성공 응답의 **`note` 유무 하나로** 창이 갈린다. 화면이 워크스페이스 갈래를 다시 판정하면 그리기와 답이 두 벌이 되므로(card.js 가 세운 「화면은 이유를 짓지 않는다」 규칙), 창을 세울지도 서버가 할 말을 가졌는지로만 정한다.

- [ ] S1 정상(main·창 없음): main 갈래 요청을 깨우면 응답이 `ok=true` · `message="멈춰 있던 작업이 다시 이어집니다."` · `note` 칸 **없음** → 화면은 창을 세우지 않고 카드의 ▶→⏸ 만 남는다
- [ ] S2 예외(worktree·창 있음): worktree 갈래는 `note="고친 내용은 작업이 끝난 뒤에 이 화면에 보입니다."` 가 함께 오고, 창이 2슬롯으로 선다 — `.dlgt` 한 절 제목 + `.dlgs` 부가 한 줄
- [ ] S3 실패·대기 갈래 유지: `ok=false`(waiting·busy·capped·moving·not-in-progress)는 `note` 유무와 무관하게 창이 서고, 여전히 붉지 않다(stop:false, 눈썹 「이어가지 않음」)
- [ ] S4 갈래를 화면이 짓지 않는다: card.js 에 workspace/kind 로 창을 가르는 분기가 없다 — 판정은 `d.ok && !d.note` 한 줄
- [ ] S5 제목에 갈래 문장이 없다: `WAKE_SPAWNED_KO` 는 한 절이고 `{means}` 자리표가 사라졌다 — 두 급의 말이 한 슬롯에 겹치지 않는다 (designer 판정 §2)
- [ ] S6 두 벌 동기화 회귀: `WS_MEANS_KO` ↔ card.js `WS_MEANS` 일치 유지(test_dialog_voice V3), 진단 답본(diag.js `?dlg=wakespawn`)이 서버 실문장과 일치(V3b 를 message+note 두 칸으로 개정)
- [ ] S7 진단이 실제 경로를 지난다: `?dlg=wakespawn`(main)·`?dlg=wakespawnws`(worktree) 둘 다 `wakeDlg` 를 직접 부르지 않고 창 판정 함수를 지난다 — 진단이 규칙을 건너뛰면 캡처가 사람이 보는 창을 비추지 못한다
- [ ] S8 실브라우저: main 갈래에서 ▶ 를 눌렀을 때 창이 뜨지 않고, worktree 갈래에서만 두 줄 창이 서는 것을 캡처로 확인

### 2026-08-31T13:43:16+09:00 response (agent: lead:claude-fable-5, by nicehugepark)

리드 판정: card.js 6줄 수정 승인 — wakeDoc→wakeDlg 사이에 성공+무예외면 창을 안 세우는 판정(wakeAnswer)이 서야 하고, 서버 message 비우기(오류 창 오인)나 wake.js 두 벌 경로(옆문)는 부적격이라는 frontend-developer 분석 타당. designer 미커밋 hunk(176-220·633-663·669·1173-1176·1262-1268)와 수정 구간(1030·1048-1055) 비겹침 실측 확인. 조건: Edit 치환만, 실브라우저 3갈래 검증, 노트 기록

### 2026-08-31T14:28:53+09:00 response (agent: sub:frontend-developer, by nicehugepark)

### 049 구현 — 성공 경로의 창을 없애고, 답을 두 칸으로

**계약**: 깨우기 성공의 답이 `message`(한 절, 결과 그 자체) + `note`(예외 사실 한 줄) **두 칸**이 됐고, 화면은 **`note` 유무 하나로** 창을 가른다 — `d.ok && !d.note` 면 창이 서지 않는다. 갈래(워크스페이스)를 화면이 다시 판정하지 않는 것이 핵심이다: 말은 여전히 서버 한 곳에서 오고(`_wake_note`), 화면은 "서버가 할 말을 가졌나"만 읽는다. 별도 `quiet` 플래그를 두는 안은 기각했다 — 같은 말을 두 필드가 하면 곧 한 벌만 고쳐진다.

**고친 자리**
- `bin/s9` — `WAKE_SPAWNED_KO` 에서 `{means}` 자리표를 걷어 한 절로. `_workspace_means` → `_wake_note`(워크트리에만 한 줄, main 은 빈 문자열). 응답 조립부(6581행대)는 note 가 있을 때만 칸을 싣는다. staff-engineer 가 만지던 `stall_verdict`(9280행대)는 건드리지 않았다.
- `web/app/card.js` — 리드 승인 범위 그대로 3곳: `wakeDoc` 의 호출 한 줄, 창이 설지를 가르는 `wakeAnswer` 신설, `wakeDlg` 에 `desc: d.note || ""`(designer 2슬롯: `.dlgt` 제목 + `.dlgs` 부가). designer 의 미커밋 hunk 와 구간이 떨어져 있고 Edit(정확 문자열 치환)만 썼다.
- `web/app/diag.js` — `?dlg=wakespawn`(main·창 없음)과 `?dlg=wakespawnws`(워크트리·창 섬) 둘로. 진단이 `wakeDlg` 를 직접 부르던 것을 `wakeAnswer` 로 바꿨다 — 진단이 판정을 건너뛰면 캡처로 고친 것이 사람이 보는 화면이 아니게 된다(048 이 문안에서 겪은 그 병).

**시험** (`python3 tests/ wake dialog workspace screen_lexicon ws_findable` → 192건 통과, echo $? 0)
- 신설 `tests/test_wake_handle.py::TheWindowStandsOnlyForExceptions` 7건 — 조용한 성공·판정을 지나는 두 길·거절의 창 유지·화면이 갈래를 안 캠·2슬롯·문장 안 쪼갬 금지
- 개정: `test_spawn_workspace` V3(제목이 한 절이고 main 은 note 칸이 없다) + V3b 신설(워크트리는 실제 워크트리 경로를 지나 부가 한 줄을 싣는다 — 갈래를 손으로 심지 않았다), `test_dialog_voice` V3b(진단 답본이 두 칸을 비춘다), `test_workspace_chip` W11/W12
- 전체 스위트: 무관한 선존 실패 8건(`test_doc_pin_release` 7 · `test_doc_title_anchor` 1)이 있다. `class="pinoff"` 가 web/ 어디에도 없고 `web/app/docs.js` 는 미수정이라 이번 작업과 무관한 별건이다.

**실브라우저** (검증용 9919 포트에 별도 기동 — 라이브 9909 는 손대지 않음)
- 정적 캡처 3갈래 + 톤/스킨 4벌(carbon·terminal·glass·soft)에서 2슬롯 위계가 선다
- CDP 실측(Windows Chrome `--remote-debugging-port`, 진짜 함수 호출): main 성공 → 창 안 섬 / 워크트리 → `.dlgt`·`.dlgs` 두 줄, 눈썹 「이어가기」, 붉지 않음 / 거절 → 창 서고 눈썹 「이어가지 않음」, 붉지 않음. 콘솔 오류 0
- 실제 키보드 경로(Tab 포커스 + 진짜 Enter 키 이벤트)에서도 `windowOpen: False` — 성공 경로에 창이 서지 않는 것이 진단이 아니라 사람이 누르는 그 길에서 확인됐다

**곁가지로 드러난 것**: designer 판정 A안의 접근성 근거("포커스가 ▶ 에 남는다")가 지금 구현에서 성립하지 않았다 — 눌린 단추가 `disabled` 가 되며 포커스가 body 로 떨어진다(CDP 실측 `activeAfter=BODY`). 049 가 만든 결함은 아니지만(종전에도 창이 열릴 때 activeElement 는 이미 body 였다) 창이 사라진 지금은 유일한 경로라, 파생 `REQ-20260831-009-62x6` 으로 분리해 이어서 처리했다.

### 2026-08-31T14:29:05+09:00 response (agent: sub:frontend-developer, by nicehugepark)

049 실브라우저 캡처 — main 갈래(창 없음) · 워크트리 갈래(2슬롯 창) · 대기 갈래(창 유지) · 실제 키보드 Enter 로 누른 성공 경로(창 없음).

[Image: assets/REQ-20260830-049-62x6/049-main-nodlg.png]
[Image: assets/REQ-20260830-049-62x6/049-worktree-dlg.png]
[Image: assets/REQ-20260830-049-62x6/049-wait-dlg.png]
[Image: assets/REQ-20260830-049-62x6/049-ws-carbon.png]
[Image: assets/REQ-20260830-049-62x6/009-cdp-pressed.png]

### 2026-08-31T14:29:28+09:00 tdd (agent: sub:frontend-developer, by nicehugepark)

### TDD 시나리오 (개정 — 구현 후 판정)

- [x] S1 정상(main·창 없음): `ok=true` · `message="멈춰 있던 작업이 다시 이어집니다."` · `note` 칸 없음 — `test_spawn_workspace` V3 + CDP 실측(창 안 섬)
- [x] S2 예외(worktree·창 있음): `note` 가 실리고 창이 `.dlgt`+`.dlgs` 2슬롯으로 선다 — V3b(실제 워크트리 경로) + 캡처
- [x] S3 실패·대기 갈래 유지: `ok=false` 는 note 없이도 창이 서고 붉지 않다 — `TheWindowStandsOnlyForExceptions` + `?dlg=wakewait` 캡처 + CDP
- [x] S4 갈래를 화면이 짓지 않는다: `wakeAnswer` 안에 workspace·worktree·WS_MEANS 어느 낱말도 없다
- [x] S5 제목에 갈래 문장이 없다: `WAKE_SPAWNED_KO` 에서 `{means}` 자리표 제거 — `test_dialog_voice` V3b 가 붙잡는다
- [x] S6 두 벌 동기화 회귀: `WS_MEANS_KO` ↔ `WS_MEANS` 일치(V3), 진단 답본이 두 칸을 그대로 비춤(V3b)
- [x] S7 진단이 실제 경로를 지난다: `?dlg=wakespawn`·`?dlg=wakespawnws` 둘 다 `wakeAnswer` 를 부른다 — `test_workspace_chip` W12 가 지킨다
- [x] S8 실브라우저: 정적 3갈래 + 톤/스킨 4벌 + CDP(진짜 Enter 키)로 확인, 콘솔 오류 0

### 2026-08-31T15:07:08+09:00 commit (by nicehugepark)

커밋 c00dd2d — 일하는 손은 멈춤이 아니고, 성공한 이어가기는 창이 없다 (REQ-20260831-005-62x6 서버 · REQ-20260830-049-62x6 계약)

### 2026-08-31T15:33:30+09:00 subagent (agent: sub:frontend-developer:ab663a6a, by nicehugepark)

`[2026-08-31 13:39:26 KST - frontend-developer]`

낡은 대기 작업이 정리된 알림이다 — 조치할 것은 없다. 스위트 판정은 앞서 보고한 대로이고(748건 중 실패 0, 부하성 오류 1건은 단독 재실행 26/26 통과), 작업은 종료 상태다.

두 REQ 모두 문서에 근거가 남아 있다:
- `REQ-20260830-049-62x6` — 계약·수정 자리·시험·캡처 5장, TDD 8/8
- `REQ-20260831-009-62x6` — 뿌리 진단·CDP 실측표·한계 한 줄, TDD 8/8

전이와 커밋은 리드 몫으로 남긴다.

### 2026-08-31T15:38:26+09:00 subagent (agent: sub:frontend-developer:ab663a6a, by nicehugepark)

`[2026-08-31 13:39:26 KST - frontend-developer]`

정리 알림이다 — 조치 불필요. 작업은 완료 상태이고 더 보고할 것이 없다.

**REQ-20260830-049-62x6 — 이어가기 창은 예외만**

깨우기 성공 응답을 `message`(한 절 제목) + `note`(예외 사실 한 줄) 두 칸으로 나누고, 화면은 `note` 유무 하나로 창을 가른다 — `d.ok && !d.note`면 창이 서지 않는다. 갈래(워크스페이스)를 화면이 다시 판정하지 않아 말은 여전히 서버 한 곳에서 온다.

- `/home/sjpark1/section9/bin/s9` — `WAKE_SPAWNED_KO`에서 `{means}` 제거, `_workspace_means` → `_wake_note`(워크트리에만)
- `/home/sjpark1/section9/web/app/card.js` — `wakeAnswer` 신설(`if (d.ok && !d.note) return null;`), `wakeDlg`에 `desc: d.note || ""`
- `/home/sjpark1/section9/web/app/diag.js` — `?dlg=wakespawn`(창 없음)·`?dlg=wakespawnws`(창 섬), 둘 다 판정을 지난다

**REQ-20260831-009-62x6 — 이어간 뒤 키보드 손이 없다** (049 실측에서 발견해 파생 등록·처리)

`faceDeed`의 `b.disabled = going`이 눌린 단추에서 포커스를 걷어 body로 보내고 있었다. 잠금은 원래 `wokePending`/`stopPending`이 지고 있었으므로 `aria-disabled`로 옮겼고, 재그리기를 건너는 `keepDeedFocus`를 더했다.

- `/home/sjpark1/section9/web/app/card.js` — `DEED_BUSY` 상수, `keepDeedFocus`·`deedHandle`
- `/home/sjpark1/section9/web/css/actions.css` — 잠금 규칙을 `[aria-disabled="true"]`로, `.dacts` 갈래 명시

실측(진짜 Tab + 진짜 Enter): 눌린 직후 `active=SAME HANDLE`·`domDisabled=false`, 연타 2회에 요청 1건, 재그리기 뒤 `landedOnHandle=true`, 남의 자리는 `stayedPut=true`. 049에서 잰 `activeAfter=BODY`가 뒤집혔다.

**검증**: 전체 스위트 748건 실패 0(부하성 소켓 오류 1건은 `subagent_flow` 단독 26/26 통과로 무관 확인), 신설 시험 14건 포함. 실브라우저 캡처는 두 REQ에 첨부돼 있다. 9919 검증 서버 정리 완료, 라이브 9909는 손대지 않았다.

### 2026-08-31T15:53:36+09:00 commit (by nicehugepark)

커밋 572b7ac — 점은 한 무대에 선다 — 3층 좌표계·화소 격자 글리프·성공은 창 없이 (REQ-20260831-005-62x6/006/009/012/016 · REQ-20260830-049-62x6 화면 몫)

## History
- 2026-08-30T22:21:52+09:00 created by nicehugepark (status: open)
- 2026-08-31T13:39:01+09:00 status: open -> in-progress (by nicehugepark) — 채팅 지시로 착수(동시 처리 가능 판단) — frontend-developer 위임: 성공 경로 창 제거·예외 갈래만 창, 서버 message+note 계약. 카드 영역(card.js)은 designer 진행 중이라 경계 지정. 부모 048 이 review 중이나 049 는 그 판정(A안)의 파생 구현이라 진행 — 048 반려 시 문구만 재조정될 수 있음
- 2026-08-31T15:07:46+09:00 status: in-progress -> review (by nicehugepark) — 멈춘 카드의 ▶ 를 눌러 이어갈 때 뜨던 안내 창을 없앴습니다 — 손 아래에서 ▶ 가 ⏸ 로 바뀌는 것이 답입니다. F5 후 멈춤 줄이 붙은 카드의 ▶ 를 눌러 보세요. 창 없이 카드만 바뀌면 승인, 무슨 일이 일어났는지 알 수 없으면 반려해 주세요. 못 깨운 경우와 결과가 나중에 보이는 경우의 한 줄 창은 그대로 뜹니다. 이 판정은 REQ-20260830-048-62x6(안내문 정비)을 함께 닫습니다.
