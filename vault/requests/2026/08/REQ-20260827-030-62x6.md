---
id: REQ-20260827-030-62x6
type: request
title: 관계 전수 검사와 재발 방지
summary: 연관을 왜 걸었는지가 어디에도 안 남아 사후 판정이 불가능하다 — 이유를 간선에 붙이고 전수 점검한다
goal: 지금 걸린 모든 relates 를 한 번 판정해 잘못 걸린 것을 거두고, 앞으로 이유 없는 연관이 새로 생기지 않는다
status: done
size: M
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: 3da5b9de
project: section9
tags: ["index"]
created: 2026-08-27T16:46:25+09:00
status_since: 2026-08-27T16:58:12+09:00
updated: 2026-08-27T16:58:12+09:00
priority: 50
---

## Original

사용자 지적: "REQ-20260827-013 문서에 연관 문서로 REQ-20260827-029 가 있는데 연관되는게 맞나? 문서 연관관계에 대해서 전부 검사를 한번 하는거 어떻게 생각하나? 근본적으로 해결되고 재발이 안됐으면 좋겠는데 말이지."

지적이 맞았다. 그 연관은 리드가 걸었고, 이유는 "둘 다 web/index.html 을 고치니 순서를 나눠야 한다" 였다 — 작업 순서에 관한 사실이지 문서 사이의 관계가 아니다.

## Notes

### 2026-08-27T16:47:42+09:00 analysis (by nicehugepark)

**전수를 세어 봤다 — 검사만으로는 재발이 안 막힌다.**

    relates 간선 119개 (문서 410건 중) · 끊어진 간선 0
      양쪽 본문이 서로를 부름  24
      한쪽만 부름            55
      **아무도 안 부름        40**   ← 근거가 문서 어디에도 없다

그런데 그 40개를 훑어보면 **잘못 걸린 것과 제대로 걸린 것이 섞여 있다.**
예: `DOC-20260827-002-62x6 ✕ REQ-20260827-025-62x6` 는 그 요청을 하며 쓴 지식 문서라
제대로 걸린 것이다. 본문이 서로를 안 부를 뿐이다. 반대로 사용자가 짚은
`REQ-013 ✕ REQ-029` 는 내용상 무관한데도 본문 언급이 있어 어떤 자동 검사에도
안 걸린다(내가 순서 조정 노트에서 013 을 언급했기 때문이다).

**즉 기계가 가릴 수 있는 문제가 아니다.** 이미 있는 탐지기
(`linkcheck --prune-proximity`)는 '둘 다 채팅 audit 산이고 15분 안에 생겼고
서로를 안 부른다'는 아주 좁은 자국만 잡는다 — 지금 1건 걸린다.

**빠진 것은 탐지가 아니라 이유다.** 간선에 남는 것이 상대 id 하나뿐이라, 나중에
누가 봐도 '진짜 관계'와 '그때 사정'을 구별할 수 없다. 사용자가 물은 것이 정확히
그것이다 — 이게 맞는 관계냐. 지금은 그 질문에 문서가 답을 못 한다.

**그래서 이유를 간선에 붙인다.** 걸 때 한 줄을 받고, 그 한 줄을 문서에 남긴다.
그러면 전수 검사가 추측이 아니라 읽기가 된다. 119개를 지금 사람이 다시 추측해서
판정하는 것은 사용자가 다시 안 하고 싶다고 한 바로 그 일이라, 그 방향으로 가지
않는다.

**감수하는 것 (숨기지 않는다)**: 이유를 강제하면 '관련 있음' 같은 빈 말이 들어올
수 있다. 그건 막을 수 없다 — 다만 빈 말은 감사에서 **보인다.** 지금은 아무것도
안 보인다.

### 2026-08-27T16:48:00+09:00 tdd (by nicehugepark)

TDD 시나리오 (실행: python3 tests/ relates_why)

정상
  N1. `s9 link A --relates B --why '...'` → 양쪽 문서의 `relates_why` 에 그 한 줄이
      상대 id 로 들어간다 (관계가 양방향이니 이유도 양방향이다).
  N2. `s9 linkcheck --relates-audit` 가 모든 간선을 이유와 함께 낸다 —
      이유가 있는 것과 없는 것이 한눈에 갈린다.
  N3. `s9 new ... --relates B --why '...'` 도 같은 규칙을 탄다 — 입구가 둘이니
      규칙도 둘 다에 건다. 한 입구만 막으면 다른 입구로 샌다.

경계
  B1. `--unrelate` 로 거두면 이유도 함께 사라진다 — 양쪽에서. 남은 이유는
      없는 관계를 설명하는 유령이 된다.
  B2. 옛 문서에는 이유가 없다(119개 전부). 그건 결함이 아니라 **미기재**로
      보고된다 — 없는 것을 있는 것처럼 채우지 않는다.
  B3. 이유가 공백뿐이면 안 준 것과 같다.

실패
  F1. `--why` 없이 `--relates` 하면 **죽는다.** 지금 고치려는 것이 정확히
      '이유 없이 걸린 관계'라, 조용히 통과시키면 아무것도 안 바뀐다.
      (`--parent`·`--derived-from` 은 방향과 뜻이 정해진 관계라 강제하지 않는다.)

회귀
  R1. 관계 무결성 검사(대칭·끊어진 간선)는 예전 그대로 통과한다.
  R2. `--unrelate`·`--parent`·`--blocked-by` 는 `--why` 를 요구하지 않는다.

### 2026-08-27T16:58:12+09:00 response (by nicehugepark)

**연관에 이유를 붙였다. 전수 검사는 이제 추측이 아니라 읽기다.**

**한 일**
- `s9 link <id> --relates <상대> --why '한 줄'` — 이유 없이 걸면 **죽는다.**
  양쪽 문서에 대칭으로 남고(`relates_why`), 거두면 양쪽에서 함께 사라진다.
- `s9 new ... --relates` 도 같은 규칙. 입구가 둘인데 한쪽만 막으면 다른 입구로 샌다.
- 자동으로 거는 경로(채팅이 판정 대기 문서를 이름으로 지목할 때)도 이유를 남긴다 —
  기계가 거는 간선이야말로 나중에 아무도 이유를 모른다.
- `s9 linkcheck --relates-audit` — 전 간선을 이유와 함께 낸다. 미기재 개수와
  양쪽 이유 불일치를 세어 준다.
- `parent`·`derived_from` 은 강제하지 않는다. 방향과 뜻이 이름에 이미 있다.

**실제로 거둔 것**
- `REQ-013 ✕ REQ-029` — 사용자가 짚은 그것. 내가 순서 조정을 관계로 적은 것이다.
- `REQ-20260827-003-62x6 ✕ REQ-20260827-006-62x6` — 12분 차로 자동 연결된 무관한 둘
  ('039 반려: 범례 무반응' ✕ '검증 방법 확인 문의'). 근접 탐지가 잡아 뒀던 1건.
- 오늘 내가 건 지식 간선 셋에는 이유를 적었다.

**하지 않은 것 — 그리고 왜**
옛 간선 115개에 이유를 소급해 채우지 않았다. 그건 **추측을 기록으로 굳히는 짓**이고,
사용자가 "다시 안 하고 싶다"고 한 바로 그 일이다. `미기재` 로 정직하게 보고되고,
그 문서를 다음에 만질 때 덮어 적힌다.

**감수하는 위험**: 이유를 강제하면 '관련 있음' 같은 빈 말이 들어올 수 있다. 막을 수
없다 — 다만 빈 말은 감사에서 **보인다.** 지금까지는 아무것도 안 보였다.

**검증**: tests/test_relates_why.py 7건 (양쪽 기재 · 감사 리포트 · new 입구 · 거두기
정리 · 공백 거부 · 이유 없으면 즉사 · 다른 관계 불변). 기존 테스트 넷이 픽스처에서
이유 없이 `--relates` 를 불러 걸렸는데, **계약을 되돌리지 않고 호출부에 이유를
적었다** — 그 넷이 보는 것은 대칭·거두기·백필이지 이유가 아니다. 전체 913건 OK.
커밋 8b10ecd.

## History
- 2026-08-27T16:46:25+09:00 created by nicehugepark (status: open)
- 2026-08-27T16:46:46+09:00 status: open -> in-progress (by nicehugepark) — 리드 착수 — 먼저 지금 걸린 relates 전수와 근거 유무를 센다
- 2026-08-27T16:58:12+09:00 status: in-progress -> done (by nicehugepark) — goal 충족: (1) 전수 판정 — linkcheck --relates-audit 로 118간선을 이유와 함께 읽게 했고, 잘못 걸린 2건(013✕029, 003✕006)을 실제로 거뒀다. (2) 재발 방지 — --why 없는 relates 는 CLI 두 입구(link·new)와 자동 경로 모두에서 거부/기재된다. 옛 간선은 추측으로 채우지 않고 미기재로 보고한다. tests/test_relates_why.py 7건 + 전체 913건 OK. 규약(CLAUDE.md·s9-protocol)에 반영.
