---
id: REQ-20260828-021-62x6
type: request
title: 언급한 것을 눌러서 연다
summary: 터미널·문서에서 언급되는 문서 id 와 파일 경로를 실제 링크로 만들어 새 탭이나 미리보기로 연다
goal: 터미널 대화와 문서 본문에 나오는 REQ/DOC id 와 저장소 파일 경로를 눌러 그 자리에서 열 수 있고, 찾아가지 않아도 무엇을 말하는지 확인된다
status: review
size: M
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: b8fda8f9
agents: ["worker:rework", "sub:subagent:adbb1b6f", "sub:designer", "sub:subagent:a677386a", "sub:subagent:a6ae2936"]
contributions: [{"actor": "worker:rework", "item": "023(대답 속 코드 보이는 모양) 재작업에서 넘어온 것 — 021 이 받아야 할 두 가지.", "started": "2026-08-28T16:24:18+09:00", "ended": "2026-08-28T16:24:18+09:00", "result": "done", "transcript": ""}, {"actor": "sub:subagent:adbb1b6f", "item": "Reading row-REQ-20260828-021-62x6 screenshot", "started": "2026-08-28T16:35:00+09:00", "ended": "2026-08-28T16:35:00+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/subagents/agent-adbb1b6fb019c9c85.jsonl"}, {"actor": "sub:designer", "item": "문서는 냈고, 코드 파일은 내지 않았습니다 — 경계를 혼자 정하지 않기로 했습니다.", "started": "2026-08-28T16:59:19+09:00", "ended": "2026-08-28T16:59:19+09:00", "result": "done", "transcript": ""}, {"actor": "sub:subagent:a677386a", "item": "Attaching peek captures to REQ-20260828-021-62x6", "started": "2026-08-28T16:59:40+09:00", "ended": "2026-08-28T16:59:40+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/subagents/agent-a677386acd14bc1f9.jsonl"}, {"actor": "sub:subagent:a6ae2936", "item": "Transitioning REQ-20260828-021-62x6 to review", "started": "2026-08-28T17:00:17+09:00", "ended": "2026-08-28T17:00:17+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/subagents/agent-a6ae29367320eab8f.jsonl"}]
project: section9
children: ["REQ-20260828-028-62x6"]
relates: ["REQ-20260827-072-62x6", "REQ-20260828-022-62x6", "REQ-20260828-023-62x6"]
relates_why: {"REQ-20260827-072-62x6": "둘 다 읽는 자리에서 벗어나지 않고 가리킨 것을 확인하게 하는 문제다", "REQ-20260828-022-62x6": "같은 줄에서 나온 요구다 — 언급된 것을 읽는 것(021)과 그것을 집어 말하는 것(022)", "REQ-20260828-023-62x6": "같은 자리(터미널 대답)에 무엇을 어떻게 실을지의 문제다 — 링크·집기와 한 줄에서 부딪힌다"}
tags: ["auto-audit", "audit", "terminal", "attached", "png", "image"]
created: 2026-08-28T15:53:32+09:00
status_since: 2026-08-28T16:59:42+09:00
updated: 2026-08-28T17:00:17+09:00
priority: 50
---

## Original

안을 종합해서 진행하고, 이어 말하기도 뭔가 수정할게 있으면 같이 변경해.
추가로 이 터미널에서도 참조하는 파일을 언급하는데, 요청 문서든, 생성된 파일이든, 참조된 파일이든 실제 링크를 달아서 별도 탭으로 열던가 아니면 미리보기 팝업을 보이던가 했으면 좋겠다. 문서와 파일을 언급시키는데 찾아가서 확인하기 너무 불편하고 어려워.

## Notes

### 2026-08-28T15:54:43+09:00 plan (by nicehugepark)

사용자(15:53): "이 터미널에서도 참조하는 파일을 언급하는데, 요청 문서든, 생성된 파일이든, 참조된 파일이든 실제 링크를 달아서 별도 탭으로 열던가 아니면 미리보기 팝업을 보이던가 했으면 좋겠다. 문서와 파일을 언급시키는데 찾아가서 확인하기 너무 불편하고 어려워."

지금 무엇이 링크가 아닌가 (착수 전 실측 필요)
- 터미널 대화에 찍히는 `REQ-20260828-007-62x6` 같은 문서 id — 문서 화면 본문에는 `a.doclink` 로 링크가 걸리는데(web/index.html) 터미널 판에서는 맨 글자다.
- `web/index.html:4016` 같은 **파일 경로 + 줄 번호** — 리드와 에이전트가 보고에 늘 쓰는 형태인데 눌리지 않는다. 사용자가 그 줄을 보려면 에디터로 찾아가야 한다.
- `vault/requests/2026/08/assets/…/x.png` 같은 첨부 경로 — `/api/asset` 로 열 수 있는데 글자로만 나온다.
- 커밋 해시.

가늠할 것
- 문서 id 는 이미 있는 문서 링크와 같은 동작이면 된다(그 자리에서 열기 · REQ-20260827-072-62x6 가 만든 "화면을 갈아치우지 않는다" 규율을 따른다).
- **파일 경로는 다르다.** 저장소 파일을 화면에 내주는 길(`/api/asset` 은 문서 첨부 전용, 가시성 상속)이 코드 파일에는 없다. 새로 내려면 **무엇을 내줄지 경계를 먼저 정해야 한다** — 리포 안 텍스트 파일만·비밀 경로(users/*/secrets)와 state/ 제외 같은 규칙이 필요하고, 그 판정은 이미 있는 게이트와 같은 함수여야 한다. 게이트를 새로 만들면 옆문이 된다(DOC-20260827-007-62x6).
- 미리보기 팝업이냐 새 탭이냐: 사용자는 둘 중 하나를 원했다. 읽는 자리를 잃지 않는 쪽(팝업)이 이 제품의 다른 결정과 결이 맞지만, 코드 파일 전체를 팝업에 담는 것은 무리다 — 줄 번호가 있으면 그 언저리만 보이는 편이 낫다.

### 2026-08-28T16:24:18+09:00 handoff (agent: worker:rework, by nicehugepark)

023(대답 속 코드 보이는 모양) 재작업에서 넘어온 것 — 021 이 받아야 할 두 가지.

**① 사용자가 023 을 되돌리며 남긴 말이 021 을 가리킨다.**
"대답을 접는 행위에 집중이 된 것 같은데, **참조 파일을 쉽게 보는게 본질이다.** 당장은
안접어도 된다. 소스코드 파일을 어떻게 보여줄거냐는 고민이 있는 것 같길래 의견을 말한
것뿐." — 즉 접기는 021 의 본류가 아니고, 021 이 여는 **파일을 어떻게 보여주느냐**가
본류다. 023 은 접기를 버리고 코드 블록의 재질(색면·세로 띠 제거)만 고쳤다.

**② 023 designer 가 남긴 경고 — 모르고 건드리면 블록 안까지 링크가 번진다.**
`ccText` 는 fenced 블록과 인라인 백틱을 **먼저** placeholder 로 빼낸 뒤 경로·해시
강조를 돌린다(web/index.html 의 md() 안, 블록 → 첨부 → 인라인 코드 → 굵게 → 경로 →
해시 순). 그래서 블록 안 경로에는 지금도 링크가 안 붙는데 이건 버그가 아니라 규율이다:
블록 내용은 읽을 것이 아니라 **붙일 것**이라, 링크를 걸면 드래그 선택이 링크 드래그가
되고 복사본에 마크업이 섞인다. 인라인 백틱에 021 링크를 걸려면 그 순서를 의도적으로
바꿔야 하고, 순서만 바꾸면 블록 안까지 링크가 번진다.

**③ 023 이 방금 블록 안에 넣은 것 — 복사 손잡이(`button.ccbcp`).**
`<span class="ccblk">` 의 첫 자식으로 버튼이 하나 들어간다(절대 위치, 평소 opacity:0).
블록 텍스트를 읽어 쓰는 코드가 021 쪽에 생기면 `.ccbcp` 를 먼저 걷어내라 —
`ccBlockCopy()` 가 하는 그대로다. 안 걷으면 첫 줄이 "⧉ 복사" 로 시작한다.

**④ 파일 미리보기의 시각 언어는 이미 정해졌다.**
023 이 코드 블록을 색면·세로 띠 없이 위아래 헤어라인 + 왼쪽 14px 들여쓰기로 바꿨고
`.ccterm` 과 `.term` 양쪽에 같은 규칙이 걸린다. 021 이 파일 본문을 화면에 낼 때 새
스타일을 만들지 말고 이 `.ccblk` 를 그대로 써라. 다만 **긴 파일은 그대로 쏟으면 안
된다** — designer 권고는 머리 몇 줄만 늘 보이고 나머지는 `+ N줄 더 보기` 로 **자르는**
것(details 로 감추지 않는다. DOM 에 남겨야 Ctrl+F 가 계속 걸린다). 대답 본문에는
지금 걸릴 블록이 0개라 023 에서는 구현하지 않았다 — 실제로 필요한 곳은 021 의 파일
미리보기다.

### 2026-08-28T16:35:00+09:00 subagent (agent: sub:subagent:adbb1b6f, by nicehugepark)

Reading row-REQ-20260828-021-62x6 screenshot

### 2026-08-28T16:59:19+09:00 response (agent: sub:designer, by nicehugepark)

문서는 냈고, 코드 파일은 내지 않았습니다 — 경계를 혼자 정하지 않기로 했습니다.

### 낸 것

**터미널 대답의 문서 id 가 손잡이가 됐습니다.** 맨 글자였던 `REQ-20260828-007-62x6`
가 점선 밑줄 링크입니다. 인라인 백틱 안에 있어도 걸립니다 — 이 저장소의 대답은
문서 id 를 거의 늘 백틱에 넣기 때문입니다.

- **얹으면** 지금까지 쓰던 그 얹기 카드(제목·요약·상태)가 뜹니다. 새 어휘를
  만들지 않았습니다.
- **누르면** 그 줄 **바로 아래**에 미리보기가 펴집니다. 화면을 갈아치우지
  않습니다 — REQ-20260827-072-62x6 가 세운 규율의 터미널 판입니다.
- **Ctrl+클릭·가운데클릭** 은 새 탭입니다. 앵커에 진짜 `href` 를 그대로 두어
  REQ-20260827-013-62x6 이 세운 것(탭 이동·새 탭에서 열기)이 전부 따라옵니다.

미리보기 카드는 문서 주소·제목·상태·요약과 **본문 앞 8줄**입니다. 제목 줄
(`## Original`·`### 2026-…-… response (by …)`)은 뺐습니다 — 이 저장소 문서는
앞머리 여덟 줄 중 셋이 제목이라 그대로 자르면 미리보기가 목차가 됩니다.
나머지는 접지 않고 **자릅니다**(`+ N줄 더 보기`) — 접힌 글은 Ctrl+F 에 걸리지
않아 화면에 있는데도 없는 것처럼 보입니다.

**문서 첨부 경로**(`…/assets/REQ-…/그림.png`)도 링크가 됐습니다. 새 탭으로
열리고, **이미 있는 게이트**(`/api/asset`, 문서 가시성을 그대로 상속)를 그대로
씁니다.

색면도 세로 띠도 없습니다. 카드는 위아래 규칙선과 글리프 칸만큼의 들여쓰기로
"이 줄에 딸린 것"을 말합니다. 색은 터미널 자신의 팔레트(`--cc-*`)만 씁니다 —
문서 화면의 상태 잉크는 tone 을 따르는데 이 판은 tone 무관 상시 다크라, 종이
톤에서 짙은 초록이 검은 판에 앉아 안 읽힙니다.

### 내지 않은 것 — 코드 파일 경로 (REQ-20260828-028-62x6 로 냈습니다)

`web/index.html:4016` 같은 것은 손대지 않았습니다. 지금 저장소 파일을 화면에
내주는 길은 `/api/asset` 하나뿐이고 그것은 첨부 전용입니다. 코드 파일에는
대응하는 판정이 없고, 새로 만들면 그것이 옆문입니다(DOC-20260827-007-62x6).

경계를 하나 떠올렸다가 버렸습니다 — "git 이 추적하는 파일만". 이 저장소는
`state/sessions/*.json` 과 `users/*/profile.md` 를 추적합니다. 그리고 어제
REQ-20260828-012-62x6 는 비밀을 다루며 **값도 경로도 주지 않는다**고 정했는데,
`users/*/config.json` 에는 바깥 비밀 폴더 경로가 들어 있습니다. 하루 전 결정을
조용히 뒤집는 규칙이라면 제가 혼자 정할 것이 아닙니다.

028 에 정해야 할 것 다섯(판정 함수의 자리·막을 경로·상한·대상 사용자·테스트)과
화면 몫(줄 언저리만 보이는 미리보기, 이번에 만든 `.ccpeek` 재사용)을 적어
뒀습니다. 커밋 해시도 같은 이유로 손대지 않았습니다 — 열 곳이 없습니다.

### 아직 남은 자리

Stream 탭과 문서 안의 대화 기록(`.term`)은 터미널 탭과 **다른 렌더러**를 씁니다.
거기 문서 id 는 아직 맨 글자입니다. 같은 규칙을 옮기는 일이고 이번 범위 밖이라
건드리지 않았습니다.

### 검증

실브라우저 캡처 3장 첨부. 헤드리스로 누른 화면을 보려고 `?peek=<문서 id>` 진단
파라미터를 하나 뒀습니다 — `?dlg=`·`?pick=`·`?conn=` 이 낸 선례와 같은 자리이고,
다음 회차에도 이 카드를 눈으로 볼 수 있습니다.
전체 스위트 1449건 통과. 계약 테스트는 `tests/test_term_mention.py` 13건.

[Image: assets/REQ-20260828-021-62x6/peek2.png]
[Image: assets/REQ-20260828-021-62x6/peek-dark.png]
[Image: assets/REQ-20260828-021-62x6/term1.png]

### 2026-08-28T16:59:40+09:00 subagent (agent: sub:subagent:a677386a, by nicehugepark)

Attaching peek captures to REQ-20260828-021-62x6

### 2026-08-28T17:00:17+09:00 subagent (agent: sub:subagent:a6ae2936, by nicehugepark)

Transitioning REQ-20260828-021-62x6 to review

## History
- 2026-08-28T15:53:32+09:00 created by nicehugepark (status: open)
- 2026-08-28T15:54:42+09:00 status: open -> in-progress (by nicehugepark) — 007 화면 작업과 같은 파일이라 순번 대기 — 017 세션이 끝나면 함께 착수
- 2026-08-28T16:59:42+09:00 status: in-progress -> review (by nicehugepark) — **F5 로 새로고침한 뒤** Terminal 탭에서 봐 주세요. 네 가지입니다. 1. **대답 속 문서 번호가 눌리는가** — 클로드가 `REQ-20260828-007-62x6` 같은 문서 번호를 말한 자리를 찾아 보세요. 이제 점선 밑줄이 그어져 있습니다. 마우스를 올리면 지금까지 쓰시던 그 미리보기 쪽지(제목·요약·상태)가 뜹니다. 2. **눌러도 화면이 안 바뀌는가** — 그 번호를 눌러 보세요. **Docs 탭으로 건너뛰지 않고** 그 줄 바로 아래에 카드가 펴져야 합니다: 문서 번호와 작성자·날짜, 제목, 상태, 한 줄 요약, 그리고 본문 앞부분 여덟 줄. 읽던 자리가 그대로 있어야 합니다. 같은 번호를 다시 누르면 접히고, 다른 번호를 누르면 앞의 카드가 닫힙니다. Esc 로도 닫힙니다. 3. **거기서 바로 이어 말할 수 있는가** — 카드 아래 `이어 말하기` 를 눌러 보세요. 카드가 닫히고 입력줄 위에 그 문서가 집힌 표시가 서야 합니다. 곧바로 그 문서에 대해 말할 수 있습니다. 옆의 `문서 열기` 는 예전처럼 Docs 탭으로 갑니다 — 이건 누르셨을 때만 갑니다. (Ctrl+클릭이나 가운데 클릭은 예전처럼 새 탭입니다.) 4. **첨부 그림 경로** — 대답에 `…/assets/REQ-…/무엇.png` 같은 경로가 나오면 그것도 눌립니다. 새 탭에서 그림이 열립니다. **못 한 것 하나** — `web/index.html:4016` 같은 **코드 파일 경로는 아직 안 눌립니다.** 저장소의 코드 파일을 화면에 내주려면 "무엇까지 내줄 것인가" 를 먼저 정해야 하는데(비밀 파일·설정 파일이 같이 새면 안 됩니다), 그건 제가 혼자 정할 일이 아니라고 판단해 REQ-20260828-028-62x6 로 따로 냈습니다. 그 문서에 정할 것과 화면 몫을 적어 뒀습니다. 지금 바로 열어야 한다고 보시면 반려해 주시고, 순서가 맞다고 보시면 승인해 주세요. 또 하나 — Stream 탭과 문서 안의 대화 기록은 터미널 탭과 다른 방식으로 그려져서 거기 문서 번호는 아직 맨 글자입니다. 같은 것을 옮기는 일이라 이번에는 손대지 않았습니다.
