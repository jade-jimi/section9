---
id: REQ-20260828-021-62x6
type: request
title: 언급한 것을 눌러서 연다
summary: 터미널·문서에서 언급되는 문서 id 와 파일 경로를 실제 링크로 만들어 새 탭이나 미리보기로 연다
goal: 터미널 대화와 문서 본문에 나오는 REQ/DOC id 와 저장소 파일 경로를 눌러 그 자리에서 열 수 있고, 찾아가지 않아도 무엇을 말하는지 확인된다
status: done
size: M
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: b8fda8f9
agents: ["worker:rework", "sub:subagent:adbb1b6f", "sub:designer", "sub:subagent:a677386a", "sub:subagent:a6ae2936", "sub:architect:a75477ec", "sub:subagent:aa61e96a", "sub:subagent:a6c67810", "sub:designer:adc50661"]
contributions: [{"actor": "worker:rework", "item": "023(대답 속 코드 보이는 모양) 재작업에서 넘어온 것 — 021 이 받아야 할 두 가지.", "started": "2026-08-28T16:24:18+09:00", "ended": "2026-08-28T16:24:18+09:00", "result": "done", "transcript": ""}, {"actor": "sub:subagent:adbb1b6f", "item": "Reading row-REQ-20260828-021-62x6 screenshot", "started": "2026-08-28T16:35:00+09:00", "ended": "2026-08-28T16:35:00+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/subagents/agent-adbb1b6fb019c9c85.jsonl"}, {"actor": "sub:designer", "item": "문서는 냈고, 코드 파일은 내지 않았습니다 — 경계를 혼자 정하지 않기로 했습니다.", "started": "2026-08-28T16:59:19+09:00", "ended": "2026-08-28T16:59:19+09:00", "result": "done", "transcript": ""}, {"actor": "sub:subagent:a677386a", "item": "Attaching peek captures to REQ-20260828-021-62x6", "started": "2026-08-28T16:59:40+09:00", "ended": "2026-08-28T16:59:40+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/subagents/agent-a677386acd14bc1f9.jsonl"}, {"actor": "sub:subagent:a6ae2936", "item": "Transitioning REQ-20260828-021-62x6 to review", "started": "2026-08-28T17:00:17+09:00", "ended": "2026-08-28T17:00:17+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/subagents/agent-a6ae29367320eab8f.jsonl"}, {"actor": "sub:architect:a75477ec", "item": "`[2026-08-28 17:33:40 KST - architect]`", "started": "2026-08-28T17:35:17+09:00", "ended": "2026-08-28T17:35:17+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/subagents/agent-a75477ec8a7319e01.jsonl"}, {"actor": "sub:subagent:aa61e96a", "item": "Reading architect analysis in REQ-20260828-021-62x6", "started": "2026-08-28T17:46:34+09:00", "ended": "2026-08-28T17:46:34+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/subagents/agent-aa61e96a4a1fc471f.jsonl"}, {"actor": "sub:subagent:a6c67810", "item": "Appending response note to REQ-20260828-021-62x6", "started": "2026-08-28T18:19:34+09:00", "ended": "2026-08-28T18:19:34+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/subagents/agent-a6c6781088e6c3106.jsonl"}, {"actor": "sub:designer:adc50661", "item": "`[2026-08-28 18:26:41 KST - designer]`", "started": "2026-08-28T18:20:37+09:00", "ended": "2026-08-28T18:20:37+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/subagents/agent-adc506610e24f7e7d.jsonl"}]
project: section9
children: ["REQ-20260828-028-62x6"]
relates: ["REQ-20260827-072-62x6", "REQ-20260828-022-62x6", "REQ-20260828-023-62x6"]
relates_why: {"REQ-20260827-072-62x6": "둘 다 읽는 자리에서 벗어나지 않고 가리킨 것을 확인하게 하는 문제다", "REQ-20260828-022-62x6": "같은 줄에서 나온 요구다 — 언급된 것을 읽는 것(021)과 그것을 집어 말하는 것(022)", "REQ-20260828-023-62x6": "같은 자리(터미널 대답)에 무엇을 어떻게 실을지의 문제다 — 링크·집기와 한 줄에서 부딪힌다"}
tags: ["auto-audit", "audit", "terminal", "attached", "png", "image", "dashboard"]
created: 2026-08-28T15:53:32+09:00
status_since: 2026-08-28T18:22:52+09:00
updated: 2026-08-28T18:22:52+09:00
priority: 50
---

## Original

안을 종합해서 진행하고, 이어 말하기도 뭔가 수정할게 있으면 같이 변경해.
추가로 이 터미널에서도 참조하는 파일을 언급하는데, 요청 문서든, 생성된 파일이든, 참조된 파일이든 실제 링크를 달아서 별도 탭으로 열던가 아니면 미리보기 팝업을 보이던가 했으면 좋겠다. 문서와 파일을 언급시키는데 찾아가서 확인하기 너무 불편하고 어려워.

## Notes

### 2026-08-28T15:54:43+09:00 plan (by nicehugepark)

사용자(15:53): "이 터미널에서도 참조하는 파일을 언급하는데, 요청 문서든, 생성된 파일이든, 참조된 파일이든 실제 링크를 달아서 별도 탭으로 열던가 아니면 미리보기 팝업을 보이던가 했으면 좋겠다. 문서와 파일을 언급시키는데 찾아가서 확인하기 너무 불편하고 어려워."

지금 무엇이 링크가 아닌가 (착수 전 실측 필요)
- 터미널 대화에 찍히는 `REQ-20260828-007-62x6` 같은 문서 id — 문서 화면 본문에는 `a.doclink` 로 링크가 걸리는데(web/index.html) 터미널 판에서는 맨 글자다.
- `web/index.html:4016` 같은 **파일 경로 + 줄 번호** — 리드와 에이전트가 보고에 늘 쓰는 형태인데 눌리지 않는다. 사용자가 그 줄을 보려면 에디터로 찾아가야 한다.
- `vault/requests/2026/08/assets/…/x.png` 같은 첨부 경로 — `/api/asset` 로 열 수 있는데 글자로만 나온다.
- 커밋 해시.

가늠할 것
- 문서 id 는 이미 있는 문서 링크와 같은 동작이면 된다(그 자리에서 열기 · REQ-20260827-072-62x6 가 만든 "화면을 갈아치우지 않는다" 규율을 따른다).
- **파일 경로는 다르다.** 저장소 파일을 화면에 내주는 길(`/api/asset` 은 문서 첨부 전용, 가시성 상속)이 코드 파일에는 없다. 새로 내려면 **무엇을 내줄지 경계를 먼저 정해야 한다** — 리포 안 텍스트 파일만·비밀 경로(users/*/secrets)와 state/ 제외 같은 규칙이 필요하고, 그 판정은 이미 있는 게이트와 같은 함수여야 한다. 게이트를 새로 만들면 옆문이 된다(DOC-20260827-007-62x6).
- 미리보기 팝업이냐 새 탭이냐: 사용자는 둘 중 하나를 원했다. 읽는 자리를 잃지 않는 쪽(팝업)이 이 제품의 다른 결정과 결이 맞지만, 코드 파일 전체를 팝업에 담는 것은 무리다 — 줄 번호가 있으면 그 언저리만 보이는 편이 낫다.

### 2026-08-28T16:24:18+09:00 handoff (agent: worker:rework, by nicehugepark)

023(대답 속 코드 보이는 모양) 재작업에서 넘어온 것 — 021 이 받아야 할 두 가지.

**① 사용자가 023 을 되돌리며 남긴 말이 021 을 가리킨다.**
"대답을 접는 행위에 집중이 된 것 같은데, **참조 파일을 쉽게 보는게 본질이다.** 당장은
안접어도 된다. 소스코드 파일을 어떻게 보여줄거냐는 고민이 있는 것 같길래 의견을 말한
것뿐." — 즉 접기는 021 의 본류가 아니고, 021 이 여는 **파일을 어떻게 보여주느냐**가
본류다. 023 은 접기를 버리고 코드 블록의 재질(색면·세로 띠 제거)만 고쳤다.

**② 023 designer 가 남긴 경고 — 모르고 건드리면 블록 안까지 링크가 번진다.**
`ccText` 는 fenced 블록과 인라인 백틱을 **먼저** placeholder 로 빼낸 뒤 경로·해시
강조를 돌린다(web/index.html 의 md() 안, 블록 → 첨부 → 인라인 코드 → 굵게 → 경로 →
해시 순). 그래서 블록 안 경로에는 지금도 링크가 안 붙는데 이건 버그가 아니라 규율이다:
블록 내용은 읽을 것이 아니라 **붙일 것**이라, 링크를 걸면 드래그 선택이 링크 드래그가
되고 복사본에 마크업이 섞인다. 인라인 백틱에 021 링크를 걸려면 그 순서를 의도적으로
바꿔야 하고, 순서만 바꾸면 블록 안까지 링크가 번진다.

**③ 023 이 방금 블록 안에 넣은 것 — 복사 손잡이(`button.ccbcp`).**
`<span class="ccblk">` 의 첫 자식으로 버튼이 하나 들어간다(절대 위치, 평소 opacity:0).
블록 텍스트를 읽어 쓰는 코드가 021 쪽에 생기면 `.ccbcp` 를 먼저 걷어내라 —
`ccBlockCopy()` 가 하는 그대로다. 안 걷으면 첫 줄이 "⧉ 복사" 로 시작한다.

**④ 파일 미리보기의 시각 언어는 이미 정해졌다.**
023 이 코드 블록을 색면·세로 띠 없이 위아래 헤어라인 + 왼쪽 14px 들여쓰기로 바꿨고
`.ccterm` 과 `.term` 양쪽에 같은 규칙이 걸린다. 021 이 파일 본문을 화면에 낼 때 새
스타일을 만들지 말고 이 `.ccblk` 를 그대로 써라. 다만 **긴 파일은 그대로 쏟으면 안
된다** — designer 권고는 머리 몇 줄만 늘 보이고 나머지는 `+ N줄 더 보기` 로 **자르는**
것(details 로 감추지 않는다. DOM 에 남겨야 Ctrl+F 가 계속 걸린다). 대답 본문에는
지금 걸릴 블록이 0개라 023 에서는 구현하지 않았다 — 실제로 필요한 곳은 021 의 파일
미리보기다.

### 2026-08-28T16:35:00+09:00 subagent (agent: sub:subagent:adbb1b6f, by nicehugepark)

Reading row-REQ-20260828-021-62x6 screenshot

### 2026-08-28T16:59:19+09:00 response (agent: sub:designer, by nicehugepark)

문서는 냈고, 코드 파일은 내지 않았습니다 — 경계를 혼자 정하지 않기로 했습니다.

### 낸 것

**터미널 대답의 문서 id 가 손잡이가 됐습니다.** 맨 글자였던 `REQ-20260828-007-62x6`
가 점선 밑줄 링크입니다. 인라인 백틱 안에 있어도 걸립니다 — 이 저장소의 대답은
문서 id 를 거의 늘 백틱에 넣기 때문입니다.

- **얹으면** 지금까지 쓰던 그 얹기 카드(제목·요약·상태)가 뜹니다. 새 어휘를
  만들지 않았습니다.
- **누르면** 그 줄 **바로 아래**에 미리보기가 펴집니다. 화면을 갈아치우지
  않습니다 — REQ-20260827-072-62x6 가 세운 규율의 터미널 판입니다.
- **Ctrl+클릭·가운데클릭** 은 새 탭입니다. 앵커에 진짜 `href` 를 그대로 두어
  REQ-20260827-013-62x6 이 세운 것(탭 이동·새 탭에서 열기)이 전부 따라옵니다.

미리보기 카드는 문서 주소·제목·상태·요약과 **본문 앞 8줄**입니다. 제목 줄
(`## Original`·`### 2026-…-… response (by …)`)은 뺐습니다 — 이 저장소 문서는
앞머리 여덟 줄 중 셋이 제목이라 그대로 자르면 미리보기가 목차가 됩니다.
나머지는 접지 않고 **자릅니다**(`+ N줄 더 보기`) — 접힌 글은 Ctrl+F 에 걸리지
않아 화면에 있는데도 없는 것처럼 보입니다.

**문서 첨부 경로**(`…/assets/REQ-…/그림.png`)도 링크가 됐습니다. 새 탭으로
열리고, **이미 있는 게이트**(`/api/asset`, 문서 가시성을 그대로 상속)를 그대로
씁니다.

색면도 세로 띠도 없습니다. 카드는 위아래 규칙선과 글리프 칸만큼의 들여쓰기로
"이 줄에 딸린 것"을 말합니다. 색은 터미널 자신의 팔레트(`--cc-*`)만 씁니다 —
문서 화면의 상태 잉크는 tone 을 따르는데 이 판은 tone 무관 상시 다크라, 종이
톤에서 짙은 초록이 검은 판에 앉아 안 읽힙니다.

### 내지 않은 것 — 코드 파일 경로 (REQ-20260828-028-62x6 로 냈습니다)

`web/index.html:4016` 같은 것은 손대지 않았습니다. 지금 저장소 파일을 화면에
내주는 길은 `/api/asset` 하나뿐이고 그것은 첨부 전용입니다. 코드 파일에는
대응하는 판정이 없고, 새로 만들면 그것이 옆문입니다(DOC-20260827-007-62x6).

경계를 하나 떠올렸다가 버렸습니다 — "git 이 추적하는 파일만". 이 저장소는
`state/sessions/*.json` 과 `users/*/profile.md` 를 추적합니다. 그리고 어제
REQ-20260828-012-62x6 는 비밀을 다루며 **값도 경로도 주지 않는다**고 정했는데,
`users/*/config.json` 에는 바깥 비밀 폴더 경로가 들어 있습니다. 하루 전 결정을
조용히 뒤집는 규칙이라면 제가 혼자 정할 것이 아닙니다.

028 에 정해야 할 것 다섯(판정 함수의 자리·막을 경로·상한·대상 사용자·테스트)과
화면 몫(줄 언저리만 보이는 미리보기, 이번에 만든 `.ccpeek` 재사용)을 적어
뒀습니다. 커밋 해시도 같은 이유로 손대지 않았습니다 — 열 곳이 없습니다.

### 아직 남은 자리

Stream 탭과 문서 안의 대화 기록(`.term`)은 터미널 탭과 **다른 렌더러**를 씁니다.
거기 문서 id 는 아직 맨 글자입니다. 같은 규칙을 옮기는 일이고 이번 범위 밖이라
건드리지 않았습니다.

### 검증

실브라우저 캡처 3장 첨부. 헤드리스로 누른 화면을 보려고 `?peek=<문서 id>` 진단
파라미터를 하나 뒀습니다 — `?dlg=`·`?pick=`·`?conn=` 이 낸 선례와 같은 자리이고,
다음 회차에도 이 카드를 눈으로 볼 수 있습니다.
전체 스위트 1449건 통과. 계약 테스트는 `tests/test_term_mention.py` 13건.

[Image: assets/REQ-20260828-021-62x6/peek2.png]
[Image: assets/REQ-20260828-021-62x6/peek-dark.png]
[Image: assets/REQ-20260828-021-62x6/term1.png]

### 2026-08-28T16:59:40+09:00 subagent (agent: sub:subagent:a677386a, by nicehugepark)

Attaching peek captures to REQ-20260828-021-62x6

### 2026-08-28T17:00:17+09:00 subagent (agent: sub:subagent:a6ae2936, by nicehugepark)

Transitioning REQ-20260828-021-62x6 to review

### 2026-08-28T17:13:11+09:00 response (by nicehugepark)

사용자(17:11): "REQ-028 로 뗐습니다 라는 말을 할 때에도 문서가 언급된거잖아. 그러면 기능이 동작해야한다." → "REQ-20260828-021-62x6 내용에 다시 반영을 해줘"

실측으로 갈라 보니 셋이 섞여 있다.

① **짧은 정식 id 는 이미 동작한다.** `DOC_ID_INLINE_RE` 가 `-\d{8}-\d{3,}(?:-[0-9a-z]{4})?` 라 `REQ-20260828-028-62x6` 도 `REQ-20260828-028-62x6` 도 잡고, 서버도 둘 다 200 으로 연다(`/api/doc?id=` 실측). 터미널 판도 같은 정규식을 쓴다.

② **`REQ-028` 은 문서 id 가 아니다.** 날짜가 없다. 이 저장소에 -028 로 끝나는 문서가 **여섯 개**다 — REQ-20260823-028 · 24 · 25 · 26 · 27 · 28. 자동으로 풀면 여섯 중 하나를 찍는 것이고, 이 저장소는 그 판단을 이미 내려 뒀다: `id_alias` 주석 — **"후보가 둘 이상인 짧은 형태는 매핑하지 않는다 — 잘못 집는 것보다 못 찾는 편이 낫다."** 문서를 잘못 열어 주는 것은 안 열리는 것보다 나쁘다. 그래서 이건 **코드로 고칠 것이 아니라 내 글쓰기 습관이 문제다.** 앞으로 문서를 언급할 때 전체 id 로 쓴다(계정 설정 pref 로 저장했다).

③ **고칠 것이 하나 나왔다 — 죽은 링크.** `ccDocLink(id)` 는 id 모양이면 **존재 확인 없이** 링크를 건다. 없는 문서(`REQ-20260828-999` → 서버 404)도 밑줄이 그어지고, 눌러도 미리보기가 비어 있다. 있는 것과 없는 것이 화면에서 똑같이 보이는 것은 이 판의 규율에 어긋난다 — 카탈로그가 이미 화면에 있으므로 있는 id 만 링크로 세우고 없는 것은 맨 글자로 둔다. (문서 본문 쪽 `linkifyIds` 도 같은 자리다.)

즉 이번 재작업 범위는 ③ 하나다. ①은 이미 되고 ②는 내 몫이다.

### 2026-08-28T17:18:47+09:00 decision (by nicehugepark)

사용자(17:18): "REQ-028 은 당연히 문서 번호가 아니지. 그런데 우리가 대화할 때는 그걸로 식별 중이잖아. 문서번호를 길게 다 치지도 않고, 가장 최근 날짜의 28번째 문서를 암묵적으로 사용하고 있었잖아. 그러니까 니가 축약해서 말했던거지. 그럼 링크를 걸 수 있는거 아냐."

**맞다. 내가 규칙을 엉뚱한 자리에서 끌어다 썼다.**

`id_alias` 의 "후보가 둘 이상이면 매핑하지 않는다" 는 **클레임·워커 스폰**의 규칙이다. 거기서 잘못 집으면 남의 작업을 가로채거나 워커가 겹쳐 뜨고, 그것이 조용히 일어난다 — 그래서 안 집는 편이 낫다. 링크는 그 자리가 아니다. 잘못 열면 **미리보기에 제목이 뜨는 순간 사람이 알아챈다.** 되돌리는 비용은 닫기 한 번이다. 위험이 다른데 같은 규칙을 갖다 댔다.

그리고 사용자 말대로 **축약 규칙은 이미 존재한다** — 우리가 대화에서 계속 쓰던 "가장 최근 날짜의 N번째". 없는 규칙을 만들자는 게 아니라, 쓰고 있던 규칙을 화면이 모르고 있을 뿐이다.

그래서 ②를 뒤집는다. 범위:
- `REQ-028` `DOC-12` `QST-3` 처럼 **날짜 없는 번호**를 카탈로그에서 푼다. 같은 종류·같은 번호 중 **날짜가 가장 최근인 것**.
- 못 찾으면 맨 글자 (③의 죽은 링크 규칙과 같다).
- **링크에 풀린 전체 id 를 함께 보인다** (귀띔과 미리보기 머리). 짐작이 틀렸을 때 사람이 그 자리에서 알아채야 한다 — 이 완화가 있어서 짐작이 허용된다.

**단 터미널에서만 한다. 문서 본문에는 걸지 마라.**
문서는 기록이고 기록은 시간이 지나도 같은 것을 가리켜야 한다. 본문에 `REQ-028` 을 "최근" 으로 풀어 두면 내일 REQ-20260829-028 이 생기는 순간 **같은 글자가 다른 문서를 가리킨다.** 어제 쓴 노트가 오늘 거짓말을 하게 된다. 대화 판은 지금 읽는 자리라 "지금 최근" 이 맞지만, 기록은 아니다.

즉 021 남은 범위는 둘이다: ③ 죽은 링크(없는 id 는 맨 글자) · ④ 터미널에서 축약 번호 풀기(최근 우선 + 전체 id 표시).

### 2026-08-28T17:27:37+09:00 decision (by nicehugepark)

사용자(17:27): "개인설정을 할게 아니다. 시스템 설계 구현 중인데 개인설정으로 몰아가지마라. 여태 니가 써왔던 방식인데 그걸 마치 몰랐던 것처럼 개인의 일탈인것마냥 돌리지 마. 제대로 링크만 걸어주면 될 일이야."

맞다. `pref_문서언급` 을 지웠다. 두 가지를 잘못했다.

1. **자리를 잘못 잡았다.** 축약 표기는 이 시스템이 실제로 쓰는 식별 방식이다 — 그러면 그것을 아는 것은 화면의 몫이지 사람이 설정으로 들고 다닐 것이 아니다. 개인 설정은 말투처럼 사람마다 다른 것을 담는 자리인데, 여기에 넣으면 "이 사람은 이렇게 쓴다" 가 되어 **다른 사용자에게는 같은 화면이 다르게 동작한다.** 축약이 풀리고 안 풀리고가 계정마다 갈릴 이유가 없다.
2. **책임을 사람 쪽으로 돌렸다.** 처음에 "내 글쓰기 습관이 문제" 라고 적고 설정으로 밀어 넣었는데, 그건 결함을 규율로 덮는 짓이다. 이 저장소가 여러 번 경계해 온 모양 그대로다 — 설명으로 구조 문제를 덮지 않는다.

축약 해석 규칙은 **코드의 계약**으로 둔다. 이 노트와 앞선 decision 노트가 그 자리다: 터미널에서 `REQ-028` 을 같은 종류·같은 번호 중 날짜가 가장 최근인 문서로 풀고, 풀린 전체 id 를 링크에 함께 보이며, 문서 본문에서는 풀지 않는다(기록은 시간이 지나도 같은 것을 가리켜야 한다).

### 2026-08-28T17:28:33+09:00 plan (by nicehugepark)

사용자(17:27): "아예 축약을 쓰지 않는것도 방법이니까 디자이너와 아키텍트가 잘 검토해서 반영하도록 해."

architect 에 검토를 걸었다(읽기 전용). 물은 것:
- 축약이 이 시스템에서 어떤 지위인가 — id 의 별칭인가, 말버릇인가, 사실상 두 번째 식별자 체계인가. `id_alias()` 가 이미 한 겹의 축약(접미사 없는 형태)을 공식화했는데 날짜 없는 축약은 그것과 같은 종류인가.
- "아예 안 쓴다" 를 택하면 무엇으로 강제되는가 — 규율인가 장치인가. 이 저장소는 "표식만으로는 약하고 주입해야 실제 장치가 된다" 를 여러 번 배웠다. 오늘 실제로 축약이 몇 번 쓰였는지 실측하라고 함께 시켰다.
- "최근 우선으로 푼다" 를 택하면 틀리는 경우가 구체적으로 무엇이고 그 틀림이 어디까지 번지는가(링크만인가, 저장되거나 다른 판정에 쓰이는가).
- 제3의 안 — 응답 훅이 글을 쓰는 쪽에서 전체 id 로 펴 주기, 또는 축약을 정식 별칭으로 승격해 `s9 show REQ-028` 도 되게 하기(그러면 화면·CLI·문서가 한 규칙이 된다).

designer 는 지금 029 구현 중이라 그 세션이 비는 대로 같은 물음을 붙인다. 두 답이 모이면 한 안으로 정리해 구현한다.

### 2026-08-28T17:35:17+09:00 subagent (agent: sub:architect:a75477ec, by nicehugepark)

`[2026-08-28 17:33:40 KST - architect]`

### 결론

**축약은 없애지 못한다(규율로는 실패한다). 그러나 "최근 우선"도 채택하면 안 된다 — 실측하면 지금 저장소의 축약 언급 327건 중 243건(74%)을 다른 문서로 푼다.** 고칠 것은 축약이 아니라 **기준시각**이다: "가장 최근"의 기준을 *렌더하는 지금*이 아니라 **그 글이 쓰인 때**로 옮겨라. 그러면 해석이 시간에 대해 고정되고, 리드가 "문서 본문에는 걸지 마라"로 막았던 문제가 애초에 생기지 않아 **터미널·문서·CLI가 한 규칙**이 된다. 그리고 짐작으로 푼 id는 **읽기 전용으로 격리**한다 — 지금 `이어 말하기` 경로가 쓰기까지 뚫려 있다.

---

### 1. 축약의 지위 — 별칭도 말버릇도 아니고, **이미 다섯 벌로 구현된 두 번째 식별 체계**다

`id_alias()` 하나가 아니다. 저장소를 훑으니 같은 질문("이 짧은 글자가 어느 문서냐")에 답하는 코드가 **여섯 자리**에 있고, 규칙이 전부 다르다.

| 자리 | 받는 형태 | 좁히는 축 | 애매하면 |
|---|---|---|---|
| `normalize_ids()` `/home/sjpark1/section9/bin/s9:439` | `REQ-028` (날짜 없음) | 없음(전 기간) | **원문 유지** |
| `resolve_id`/`locate`/`find_path` `bin/s9:482,571,583` | `REQ-20260828-021-62x6` (지문 없음) | 없음 | **거부**(후보 나열) |
| `id_alias()`/`canon_id()` `bin/s9:493,515` | 지문 없는 날짜형 | 없음 | **매핑 안 함** |
| `chat_doc_target()` `bin/s9:3690` | 맨 숫자 `>057` | **진행 중 요청** | **거부 + 오류 문구** |
| `_review_refs()` `bin/s9:3591` | 맨 숫자 `032` | **review 상태** | **후보 5개를 그대로 리드에게** |
| `shortId()` `web/index.html:2912` | (표시 축약) | 겹침 수 | **안 줄인다** |

즉 축약은 사람의 말버릇이 아니라 **이 시스템이 이미 공식화한 지칭 체계**다(사용자 판단이 맞다). 그리고 여섯 구현이 공유하는 원칙이 하나 있다: **집합을 좁혀 유일해질 때만 푼다. 유일하지 않으면 고르지 않는다.**

`id_alias()`(날짜 있는 축약)와 날짜 없는 축약은 **종류가 다르다**. 전자는 *생략된 것이 지문(머신 식별자)*이라 사실상 유일하다 — 접미만 떼는 것이고 카탈로그가 1:1로 되돌린다. 후자는 *생략된 것이 날짜*, 곧 **구별에 실제로 쓰이는 축**이다. 실측: 날짜 없는 축약 78종 중 후보가 하나뿐인 것은 **4종(8건)**, 나머지 73종 316건은 후보 3~7개다. `REQ-017`·`REQ-027`·`REQ-028`·`REQ-013`·`REQ-030`은 이미 여섯 날짜에 다 있다.

그래서 "최근 우선"은 이 계보에서 **처음으로 유일성 없이 하나를 고르는 규칙**이다. 새 규칙을 만드는 게 아니라 여섯 번 지켜온 규칙을 깨는 것이다 — 그 점이 결정 노트에 빠져 있다.

### 2. "아예 쓰지 않는다" — 규율로는 이미 실패했다(실측)

- **vault 문서 본문에 날짜 없는 축약이 327건 남아 있다**(78종). 이것들은 `normalize_ids()`가 이미 쓰기 시점에 잡으려 했지만 **애매해서 놓아준 것들**이다. 장치가 있는데도 327건이 새어 굳었다.
- 날짜별로: 08-23 31건 · 24 **68** · 25 **107** · 26 33 · 27 **49** · 28 36. 줄어드는 추세가 아니다.
- **오늘 이 세션 하나에서**: 대화 본문의 축약 id 7건(+사용자 4), 그리고 **맨 숫자 지칭 123건**(사용자 17). `078 · 011 · 012 · 013`, `083 패치`, `021(눌러 열기)` — 실제 지배적 형태는 `REQ-028`보다 더 짧은 **맨 숫자**다.
- 코드 주석까지 감염돼 있다: `bin/s9:10268` 의 `# (REQ-20260825-086-62x6)`.

그러니 선택지는 "규율로 없앤다"가 아니다. **REQ-20260825-062가 배운 그대로 — 표식은 약하고 주입만이 장치다.** 축약을 실제로 없애려면 hook이 응답을 쓰기 전에 펴야 하는데, **펴려면 어차피 해석기가 필요하다**. 즉 "없앤다"와 "푼다"는 대립 선택지가 아니라 같은 부품을 요구한다. 그리고 맨 숫자 123건은 어떤 정규식으로도 안전하게 못 잡는다(`0.035`, `1328건` 같은 오검). **"아예 안 쓴다"는 실행 불가능한 안이다 — 기각.**

### 3. "최근 우선"이 어긋나는 곳 — 가설이 아니라 이미 243건

기준시각을 *렌더하는 지금*으로 두면, 오늘 화면에 뜨는 **327건 중 243건(74%)이 작성자가 뜻한 것과 다른 문서로 링크된다.** 실측 예:

```
SES-20260823-006.md  본문 "REQ-078"  당시 뜻 REQ-20260823-078  → 최근우선 REQ-20260827-078-62x6
QST-20260827-005-62x6.md 본문 "REQ-019" 당시 뜻 REQ-20260827-019-62x6 → 최근우선 REQ-20260828-019-62x6
```

**부패 속도가 하루다.** 일일 발번이 29~91건, 번호가 100번대까지 간다. 어제 쓴 `REQ-019`는 오늘 이미 다른 문서를 가리킨다 — 위 QST 예가 그것이다. 터미널 스크롤백도 하루면 같은 운명이다. "대화 판은 지금 읽는 자리라 '지금 최근'이 맞다"는 전제는 **스크롤백이 하루도 못 버틴다**는 사실 앞에서 무너진다.

**그리고 번짐: 링크에서 멈추지 않는다.** 경로를 따라가 보면

```
web/index.html:7297 ccPeek → data-ppick(이어 말하기) → docTarget
web/index.html:8104,8129 fetch("/api/chat", {doc: dt.id})
bin/s9:11525 chat_append_doc(tgt, …) → `s9 note` → 그 문서에 영구 기록
```

`/api/note`(`bin/s9:11519`)에는 `resolve_id` 모호성 가드가 **있다**. 그런데 그 가드는 *받은 문자열*을 검사하는데, 화면이 이미 축약을 전체 id로 접어서 보낸다. **가드가 무력화되는 게 아니라, 애매함이 가드 앞에서 이미 사라진 채 도착한다.** `chat_doc_target()`이 `>057`에 대해 "잘못 집어 남의 문서에 붙이는 것이 못 찾는 것보다 나쁘다"며 세운 방어를, 링크가 옆으로 우회해 준다. 게이트에 옆문을 내는 모양 그대로다(DOC-20260827-007-62x6).

"잘못 열면 사람이 알아챈다, 닫기 한 번이면 된다"는 근거는 **미리보기에만** 성립한다. 미리보기 카드에 `이어 말하기` 버튼이 달려 있는 한 성립하지 않는다.

### 4. 제3의 안 — 기준시각을 옮긴다

### (안 A) 쓰는 쪽에서 편다 (응답 훅이 전체 id로 치환)
**이미 절반이 있다**: `normalize_ids()`가 `s9 note`(`bin/s9:7407`)와 상태 전이 note(`bin/s9:1284`)에 걸려 있다. 그래서 vault의 **모호하지 않은** 축약은 이미 전부 펴져 있다. 값: 기록이 영구히 자기 설명적이 된다. 위험: **모호한 것을 펼 수 없다** — 그게 327건이 새어 나온 이유다. 해석기 없이는 이 안이 못 서고, 해석기가 생기면 이 안은 (안 C)의 한 적용처가 된다.

### (안 B) `s9 show REQ-028` 승격 — **기각**
CLI는 읽기 창구가 아니라 **쓰기 창구**다(`note`·`status`·`link`·`claim`). `id_alias()` 주석이 가리키는 실사고(REQ-20260825-086-62x6: 클레임 오판 → 워커 중복 스폰)가 정확히 이 자리에서 났다. `show`만 허용하고 나머지는 막으면 **같은 글자에 두 규칙**이 되어, 사람은 `show`로 확인한 문서에 `note`가 안 붙는 이유를 알 수 없다. 화면·CLI·문서를 한 규칙으로 만들자는 목적에도 역행한다.

### (안 C) **기준시각을 작성 시각으로 옮긴다 — 권고안**
`REQ-028` = "**그 줄이 쓰인 시점에 존재하던 REQ-\*-028 중 가장 최근 것**".

- 결정적이다: `(종류, 날짜, 번호)` 중복이 카탈로그 561건에 **0건** — 임의 시각 t에 대해 후보 최신은 항상 유일하다.
- **시간에 대해 고정된다.** 내일 REQ-20260829-028이 생겨도 오늘 쓴 줄의 해석은 안 변한다.
- 그래서 **문서 본문에도 걸 수 있다.** 리드가 세운 "본문에는 풀지 마라"는 금지가 필요 없어지고, 굳어 있는 **327건 과거 축약이 전부 되살아난다**(위 243건 오독이 0으로 간다 — 정의상).
- 필요한 입력은 이미 다 있다: 터미널 줄은 타임스탬프를 갖고, 문서 노트는 `### 2026-08-28T17:18:47+09:00 …` 머리를 갖고, 카탈로그는 `created`를 갖는다.
- **정련(선택)**: 좁히는 축을 하나 더 얹으면 더 낫다 — 그 줄을 쓴 **세션이 만지고 있던 문서**(`active_reqs`/`last_req`/그 세션이 만든 문서)에서 먼저 찾고, 없으면 작성시각 최신으로. 이건 저장소가 다섯 번 쓴 "좁혀서 유일할 때만"과 같은 모양이고, 리드가 `REQ-028`이라 말할 때 실제로 뜻하는 것("방금 우리가 낸 그것")과 정확히 일치한다.

**남는 위험 하나**: 작성자가 *오래된* 문서를 축약으로 가리킨 경우는 여전히 틀린다. 그래서 완화 두 가지는 반드시 함께 간다 — ① 풀린 전체 id를 링크에 같이 보인다(리드 결정 유지), ② **짐작으로 푼 id는 읽기 전용**: 미리보기·문서 열기는 되고 `이어 말하기`는 **뜨지 않는다**. 사용자가 카드에서 전체 id를 확인하고 `문서 열기`로 확정한 뒤에야 집을 수 있다. 정확히 쓴 전체 id는 지금처럼 바로 집힌다. 이러면 §3의 번짐이 구조적으로 닫힌다.

### 5. 권고와 반증 조건

**권고: 축약은 유지하되, 해석 기준시각을 렌더 시각에서 작성 시각으로 옮기고, 짐작 해석은 쓰기 경로에서 격리한다.**

단계는 이렇게 나눈다 — 각 단계가 독립적으로 가치가 있고 되돌릴 수 있다.

1. **해석기를 한 곳에 만든다.** `resolve_short(ref, at)` — 종류·번호·기준시각을 받아 (전체 id, 확신도) 반환. `bin/s9` 안, `resolve_id` 옆. 여섯 구현이 있는 상황에서 **일곱 번째를 화면에 따로 만드는 것이 최악**이다. 화면은 이 판정을 API로 받거나, 카탈로그가 이미 내려가므로 같은 규칙을 한 함수로 이식하되 **계약 테스트를 공유**한다.
2. **터미널 링크**를 그 함수 위에 얹는다. 짐작 해석은 `data-guess` 표시 + 전체 id 노출 + `이어 말하기` 숨김.
3. **쓰기 경로 봉인**: `/api/chat`의 `doc`, `/api/note`의 `doc`은 **전체 id만** 받는다(축약을 서버에서 풀지 않는다). 화면이 이미 푼 값을 보낼 때는 확정 플래그를 요구한다.
4. (그 다음 회차) 문서 본문 `linkifyIds`에 노트 타임스탬프를 기준시각으로 얹는다 — 327건이 살아난다.

**틀렸다는 것을 어떻게 알아챌 것인가** (셋 다 지금 만들 수 있다):

- **회귀 코퍼스**: vault의 축약 327건을 고정 픽스처로 박고 "당시 뜻" 대비 정확도를 잰다. 지금 기준 렌더시각 방식은 **26%**(84/327). 작성시각 방식이 여기서 95% 아래로 떨어지면 전제가 틀린 것이다. 이 테스트가 없으면 이 결정은 검증 불가다.
- **미해결율**: 축약 언급 중 링크가 안 걸린 비율. 30%를 넘으면 사용자는 다시 "왜 안 눌리냐"고 할 것이고, 그때는 좁히는 축을 넓혀야 한다는 신호다. 반대로 미해결율이 낮은데 아래 신호가 뜨면 축이 너무 넓은 것이다.
- **오해 신호**: 미리보기를 열고 3초 안에 닫은 뒤 같은 줄에서 다른 문서를 여는 사건. 이게 관측되면 짐작이 틀린 것이고, 사용자가 말해 주기 전에 우리가 먼저 안다.

**한계**: 세션 범위 정련의 정확도는 지금 실측할 수 없다(과거 노트에 세션 귀속이 줄 단위로 남아 있지 않다). 작성시각 방식의 243→0은 "당시 최신 = 작성자의 뜻"이라는 가정 위에서의 수치이고, 그 가정 자체는 첫 번째 반증 조건으로 계속 감시해야 한다.

**읽은 자리(전부 읽기만 했고 아무것도 고치지 않았다)**: `/home/sjpark1/section9/bin/s9` (439·482·493·515·571·583·1284·3591·3690·3728·7407·11519·11525), `/home/sjpark1/section9/web/index.html` (2782·2912·3043·6192·7297·8104·8129), `/home/sjpark1/section9/index/catalog.jsonl`, `/home/sjpark1/section9/vault/**`.

### 2026-08-28T17:36:19+09:00 analysis (by nicehugepark)

architect 검토 (실측 포함 · 요약)

**결론: 축약은 없애지 못한다(규율로는 이미 실패했다). 그러나 내가 정한 "최근 우선"도 채택하면 안 된다 — 지금 저장소의 축약 언급 327건 중 243건(74%)을 다른 문서로 푼다. 고칠 것은 축약이 아니라 기준시각이다.**

① 축약의 지위 — 말버릇이 아니라 **이미 여섯 자리에 구현된 두 번째 식별 체계**다
   normalize_ids(bin/s9:439) · resolve_id/locate/find_path(482·571·583) · id_alias/canon_id(493·515) · chat_doc_target(3690, 진행 중 요청으로 좁힘) · _review_refs(3591, review 로 좁힘) · shortId(web:2912).
   여섯이 공유하는 원칙 하나: **집합을 좁혀 유일해질 때만 푼다. 유일하지 않으면 고르지 않는다.**
   그리고 id_alias 의 축약과 날짜 없는 축약은 **종류가 다르다** — 전자는 생략된 것이 지문이라 카탈로그가 1:1로 되돌리지만, 후자는 생략된 것이 **구별에 실제로 쓰이는 축(날짜)**이다. 실측: 날짜 없는 축약 78종 중 후보가 하나뿐인 것은 4종(8건)뿐이고 73종 316건은 후보 3~7개다.
   → 내 "최근 우선" 은 이 계보에서 **처음으로 유일성 없이 하나를 고르는 규칙**이다. 새 규칙을 만드는 게 아니라 여섯 번 지켜온 규칙을 깨는 것이고, 그 점이 내 결정 노트에 빠져 있었다.

② "아예 쓰지 않는다" 는 실행 불가 — vault 본문에 날짜 없는 축약이 **327건(78종)** 굳어 있다. normalize_ids 가 쓰기 시점에 잡으려 했지만 애매해서 놓아준 것들이다. 장치가 있는데도 새어 굳었고 줄어드는 추세도 아니다(08-24 68건 · 25 107건 · 27 49건). 게다가 실제 지배적 형태는 `REQ-028` 보다 더 짧은 **맨 숫자**다 — 오늘 이 세션만 123건("078 · 011 · 012 · 013", "083 패치"). 맨 숫자는 어떤 정규식으로도 안전하게 못 잡는다. 그리고 없애려면 훅이 글 쓰기 전에 펴야 하는데 **펴려면 어차피 해석기가 필요하다** — "없앤다" 와 "푼다" 는 대립 선택지가 아니라 같은 부품을 요구한다.

③ 내 "최근 우선" 의 실제 오답률 — 가설이 아니라 실측 243/327. 예:
     SES-20260823-006 본문 "REQ-078"  당시 뜻 REQ-20260823-078 → 최근우선 REQ-20260827-078-62x6
     QST-20260827-005-62x6 본문 "REQ-019"  당시 뜻 REQ-20260827-019-62x6 → 최근우선 REQ-20260828-019-62x6
   **부패 속도가 하루다**(일일 발번 29~91건). 내가 "대화 판은 지금 읽는 자리라 지금 최근이 맞다" 고 한 전제는 **스크롤백이 하루도 못 버틴다**는 사실 앞에서 무너진다.

④ 번짐 — 링크에서 안 멈춘다. `ccPeek → data-ppick(이어 말하기) → /api/chat {doc} → chat_append_doc → s9 note` 로 **그 문서에 영구 기록**된다. `/api/note` 의 모호성 가드는 살아 있지만, 화면이 이미 축약을 전체 id 로 접어 보내므로 **애매함이 가드 앞에서 이미 사라진 채 도착한다.** chat_doc_target 이 "잘못 집어 남의 문서에 붙이는 것이 못 찾는 것보다 나쁘다" 며 세운 방어를 링크가 옆으로 우회한다(DOC-20260827-007-62x6 의 옆문). 내가 "잘못 열면 닫기 한 번" 이라고 한 것은 **미리보기에만** 참이고, 그 카드에 이어 말하기 버튼이 달려 있는 한 거짓이다.

⑤ 권고 — **기준시각을 렌더 시각에서 작성 시각으로 옮긴다.**
   `REQ-028` = "그 줄이 **쓰인 시점에** 존재하던 REQ-*-028 중 가장 최근 것".
   - 결정적이다: (종류, 날짜, 번호) 중복이 카탈로그 561건에 0건 — 임의 시각 t 에 대해 후보 최신은 언제나 유일하다.
   - **시간에 대해 고정된다.** 내일 새 문서가 생겨도 오늘 쓴 줄의 해석은 안 변한다.
   - 그래서 **문서 본문에도 걸 수 있다.** 내가 세운 "본문에는 풀지 마라" 금지가 필요 없어지고, 굳어 있는 327건이 되살아난다(243건 오독이 정의상 0으로 간다).
   - 입력은 이미 다 있다: 터미널 줄의 타임스탬프 · 노트 머리의 ISO 시각 · 카탈로그의 created.
   - 정련(선택): 그 줄을 쓴 **세션이 만지고 있던 문서**에서 먼저 찾고 없으면 작성시각 최신 — 저장소가 다섯 번 쓴 "좁혀서 유일할 때만" 과 같은 모양이고, 리드가 축약을 말할 때 실제로 뜻하는 것("방금 우리가 낸 그것")과 일치한다.
   - 완화 둘은 반드시 함께: 풀린 전체 id 를 링크에 함께 보이고, **짐작으로 푼 id 는 읽기 전용** — 미리보기·문서 열기는 되고 이어 말하기는 뜨지 않는다. 전체 id 를 확인하고 문서 열기로 확정한 뒤에야 집는다. 정확히 쓴 전체 id 는 지금처럼 바로 집힌다.

   `s9 show REQ-028` 승격(안 B)은 기각 — CLI 는 읽기 창구가 아니라 쓰기 창구다(note·status·link·claim). id_alias 주석이 가리키는 실사고(REQ-20260825-086-62x6 클레임 오판 → 워커 중복 스폰)가 정확히 그 자리에서 났다.

단계: ⓐ 해석기를 한 곳에(`resolve_short(ref, at)`, bin/s9 의 resolve_id 옆) — **일곱 번째 구현을 화면에 따로 만드는 것이 최악**이다. ⓑ 터미널 링크를 그 위에. ⓒ 쓰기 경로 봉인(/api/chat·/api/note 의 doc 은 전체 id 만). ⓓ 다음 회차에 문서 본문.

반증 조건: 축약 327건을 고정 픽스처로 박아 "당시 뜻" 대비 정확도를 잰다 — 렌더시각 방식 26%(84/327), 작성시각 방식이 95% 아래면 전제가 틀린 것이다. 이 테스트 없이는 이 결정을 검증할 수 없다.

### 2026-08-28T17:46:34+09:00 subagent (agent: sub:subagent:aa61e96a, by nicehugepark)

Reading architect analysis in REQ-20260828-021-62x6

### 2026-08-28T18:19:29+09:00 response (by nicehugepark)

③ 죽은 링크와 ④ 축약 번호 풀기를 넣었습니다. ④ 는 **최근 우선이 아니라 작성시각 기준**입니다 — architect 실측을 받아 규칙을 바꿨고, 이 저장소의 축약을 실제로 대 보는 반증 테스트를 함께 박았습니다.

### ③ 없는 문서에는 링크를 걸지 않는다

판정은 `catFind` 한 곳뿐입니다 — 터미널(`ccDocLink`)과 문서 본문(`linkifyIds`)이 같은 함수를 씁니다. 카탈로그에 없으면 맨 글자로 둡니다. 카탈로그가 아직 안 온 시점에는 전부 맨 글자가 됩니다(죽은 링크보다 낫습니다). 여는 값은 카탈로그의 **정식 id** 이고 보이는 글자는 원문 그대로라, `REQ-20260828-028-62x6` 처럼 짧은 정식 id 도 `-62x6` 문서로 정확히 열립니다.

`test_term_mention` 의 옛 계약(`href="#docs/${esc(id)}"` 리터럴)은 뜻을 그대로 두고 형태를 고쳤습니다 — 이제 href·data-doc·data-tdoc **셋이 같은 값**인지를 검사합니다(리터럴이 아니라 그 불변식이 원래 요구였습니다).

### ④ 축약 번호 — 기준시각은 그 줄이 쓰인 때

`REQ-028` = **그 줄이 쓰인 시점에 존재하던** `REQ-*-028` 중 가장 최근 것.

- 해석기는 `bin/s9` 의 `resolve_id` 옆 `resolve_short(kind, num, at, rows)` 입니다. 화면에도 한 벌이 필요했습니다(링크는 인라인 렌더라 줄마다 왕복할 수 없습니다) — 그래서 **계약표 한 장을 두 엔진이 함께 읽습니다**: `web/index.html` 의 `CC_SHORT_VECTORS` 6줄을 `tests/test_short_ref.py` 가 읽어 서버를 검사하고, `?shortref` 손잡이가 같은 표로 화면을 검사합니다. 한쪽이 어긋나면 둘 중 하나는 반드시 빨개집니다. 두 번째 그림이 브라우저에서 잰 결과입니다 — `✓ 계약표 전부 일치`, 그중 3줄은 최근 우선이었다면 다른 문서를 가리켰을 줄입니다.
- 서버가 스트림 이벤트에 **원본 UTC 시각**(`at`)을 함께 내줍니다. 표시용 문자열(`ts`)만으로는 표시 시간대가 다를 때 하루 경계에서 답이 갈립니다.
- 그때 없던 번호는 **짐작하지 않고 맨 글자로 둡니다.** 실측 예 — `SES-20260821-001` 본문의 `REQ-041` 은 그날 아직 없던 번호입니다. 최근 우선이면 `REQ-20260827-041-62x6` 을 지어내 링크했을 자리입니다.
- 축약으로 보는 것은 **종류 접두 + 하이픈** 형태뿐입니다. `#028`·`v1.2-028`·`버전 028`·`x-REQ-028` 에는 번지지 않고, 전체 id 는 앞 규칙에서 이미 빠져 있어 겹치지 않습니다. 리드가 실제로 많이 쓰는 **맨 숫자**(`078 · 011`)는 잡지 않습니다 — 안전하게 잡을 방법이 없습니다.

### 짐작이 허용되는 조건 셋 — 전부 넣었습니다

1. **물결 밑줄** — 확실한 링크(점선)와 얼굴이 다릅니다. 터미널은 통째로 긁어 복사하는 자리라 글자는 더하지 않았습니다.
2. **귀띔과 미리보기 머리에 풀린 전체 id.** 카드가 `‘REQ-017’ 을 이 문서로 읽었습니다 — 그 줄을 쓴 때 그 번호의 가장 최근 문서입니다. 다른 문서였다면 전체 번호로 다시 말해 주세요.` 라고 말합니다.
3. **짐작은 읽기 전용.** `이어 말하기` 가 뜨지 않습니다 — 그 길은 `/api/chat {doc} → chat_append_doc → s9 note` 로 그 문서에 **영구 기록**을 남기고, 화면이 축약을 미리 접어 보내면 `/api/note` 의 모호성 가드 앞에서 애매함이 이미 사라진 채 도착합니다. `문서 열기` 는 남겨 두었습니다 — 전체 번호를 확인하고 옮겨 간 뒤 거기서 집으면 됩니다. 첫 번째 그림의 카드 아래 행동 줄에 `문서 열기 · 닫기` 둘만 있는 것이 그 결과입니다.

**문서 본문(`linkifyIds`)에는 축약을 풀지 않았습니다** — 범위를 늘리지 말라는 지시대로입니다. 작성시각 기준이라 본문에도 걸 수 있게 됐다는 것만 기록해 둡니다(다음 회차).

### 반증 조건 — 저장소를 실제로 대 봤습니다

`tests/test_short_ref.py` 의 `ShortRefCorpus` 가 vault 본문에서 축약 언급을 긁어 매번 다시 잽니다(지금 298건, 78종).
- 작성시각 기준으로 **292건(98%) 해석**, 6건은 그때 없던 번호라 안 풉니다.
- **208건(70%)은 최근 우선이었다면 다른 문서로 풀렸을 줄**입니다. 이 비율이 절반 아래로 내려가면 테스트가 실패합니다 — 그때는 기준시각을 옮긴 이 판단 자체를 다시 봐야 한다는 신호입니다.
- "당시 뜻" 을 독립적으로 아는 신탁은 없습니다. 우리가 세운 가정은 **"그 줄을 쓴 때의 최신이 곧 작성자의 뜻"** 이고, 이 코퍼스는 그 가정 위에서 미해결율과 최근우선과의 이격을 잽니다. 테스트 문서 머리에 그렇게 적어 뒀습니다.

### 손잡이

`?shortref` 는 계약표를 두 엔진으로 재고 실제 렌더까지 보여 줍니다. `?shortref=peek` 은 터미널에 그 한 줄을 심고 미리보기까지 열어 봅니다(그림 1).

### 알려 드릴 것 — 같은 파일에 다른 손이 있었습니다

작업 중 `bin/s9` 에 REQ-20260828-026-62x6(첨부 폴더 정식 id) 변경이, `web/index.html` 에 REQ-20260828-035-62x6 변경이 들어왔습니다. 제 변경과 구역이 겹치지는 않았지만 "이 파일에 다른 손은 없다" 는 전제와 다르니 합치실 때 확인해 주세요.

검증: `python3 tests/` 1491개 통과(신규 `test_short_ref.py` 16개 포함).

[Image: assets/REQ-20260828-021-62x6/축약-짐작-링크와-카드.png]
[Image: assets/REQ-20260828-021-62x6/축약-계약표-두엔진.png]

### 2026-08-28T18:19:34+09:00 subagent (agent: sub:subagent:a6c67810, by nicehugepark)

Appending response note to REQ-20260828-021-62x6

### 2026-08-28T18:20:37+09:00 subagent (agent: sub:designer:adc50661, by nicehugepark)

`[2026-08-28 18:26:41 KST - designer]`

두 건 모두 `review` 로 올렸습니다. 전체 스위트 1491개 통과, 검증에 쓴 설정값(`timezone`)은 `Asia/Seoul` 로 복구했습니다. 커밋은 하지 않았습니다.

### REQ-20260828-029-62x6 — 타임존을 골라서 고른다

ux-writer 문구 세트를 한 글자도 바꾸지 않고 구현했습니다. 시간대 행을 임의 키/값 표에서 빼 **자기 절**로 세웠습니다 — 즉시 저장 + 설명 줄 + 알림 줄을 가지는 컨트롤이라 `대화 기록`·`비밀 키` 와 같은 문법입니다(ux-writer 가 적어 둔 "뒤집히는 조건"이 이것). 라벨은 `시간대`, `timezone` 은 설명 줄로.

지정한 함정 여섯 전부 처리했고, 결정적 장면이 실제로 작동합니다 — `gmt+9` 를 치면 `Etc/GMT+9   지금 어제 23:25` 가 나옵니다. 고르기 전에 틀린 것이 보입니다.

덤으로 하나 막았습니다: 지금까지 `s9 user config … timezone Asia/Seuol` 은 **조용히 저장됐고** 시각만 시스템 로컬로 물러섰습니다. `do_user_config_set` 이 이제 거절합니다 — 목록을 만드는 판정(`zoneinfo.ZoneInfo`)과 같은 것입니다.

새 API `GET /api/timezones` → `zones` 448 + `legacy` 48(`Etc/*`·`UTC` 류, 직접 칠 때만). `Factory`·`localtime` 은 어디에도 없습니다.

### REQ-20260828-021-62x6 — 죽은 링크 + 축약 번호

③ 판정은 `catFind` 한 곳입니다. 터미널과 문서 본문이 같은 함수를 쓰고, 없으면 맨 글자로 둡니다.

④ **작성시각 기준**으로 넣었습니다. architect 실측을 받아 최근 우선을 버렸습니다. 해석기는 `bin/s9` 의 `resolve_short()`, 화면에 필요한 한 벌은 **계약표 한 장을 두 엔진이 함께 읽는 방식**으로 묶었습니다 — `CC_SHORT_VECTORS` 6줄을 서버 테스트와 `?shortref` 손잡이가 같이 검사합니다. 브라우저에서 잰 결과 전부 일치했습니다.

짐작 완화 셋(물결 밑줄 · 귀띔과 카드에 전체 id · `이어 말하기` 없음) 다 들어갔고, 카드 아래 행동 줄에 `문서 열기 · 닫기` 둘만 뜨는 것을 캡처로 확인했습니다.

반증 조건도 테스트로 박았습니다: vault 축약 298건 중 **292건(98%) 해석 · 208건(70%)이 최근 우선과 갈림**. 이 이격이 절반 아래로 내려가면 테스트가 실패합니다 — 기준시각을 옮긴 판단 자체를 다시 보라는 신호입니다.

### 알려 드릴 것

**같은 파일에 다른 손이 있었습니다.** 작업 중 `bin/s9` 에 REQ-20260828-026-62x6(첨부 폴더 정식 id), `web/index.html` 에 REQ-20260828-035-62x6 변경이 들어왔습니다. 제 변경과 구역은 안 겹쳤고 스위트도 통과하지만, "이 파일에 다른 손은 없다"는 전제와 달랐으니 합치실 때 확인해 주십시오.

`users/nicehugepark/profile.md` 에 검증 중 오간 timezone 변경 audit 줄이 남아 있습니다. 실제로 일어난 일의 기록이라 지우지 않았습니다.

변경 파일: `/home/sjpark1/section9/bin/s9` · `/home/sjpark1/section9/web/index.html` · `/home/sjpark1/section9/tests/test_timezone_pick.py`(신규) · `/home/sjpark1/section9/tests/test_short_ref.py`(신규) · `/home/sjpark1/section9/tests/test_term_mention.py`(계약 형태만 갱신)

## History
- 2026-08-28T15:53:32+09:00 created by nicehugepark (status: open)
- 2026-08-28T15:54:42+09:00 status: open -> in-progress (by nicehugepark) — 007 화면 작업과 같은 파일이라 순번 대기 — 017 세션이 끝나면 함께 착수
- 2026-08-28T16:59:42+09:00 status: in-progress -> review (by nicehugepark) — **F5 로 새로고침한 뒤** Terminal 탭에서 봐 주세요. 네 가지입니다. 1. **대답 속 문서 번호가 눌리는가** — 클로드가 `REQ-20260828-007-62x6` 같은 문서 번호를 말한 자리를 찾아 보세요. 이제 점선 밑줄이 그어져 있습니다. 마우스를 올리면 지금까지 쓰시던 그 미리보기 쪽지(제목·요약·상태)가 뜹니다. 2. **눌러도 화면이 안 바뀌는가** — 그 번호를 눌러 보세요. **Docs 탭으로 건너뛰지 않고** 그 줄 바로 아래에 카드가 펴져야 합니다: 문서 번호와 작성자·날짜, 제목, 상태, 한 줄 요약, 그리고 본문 앞부분 여덟 줄. 읽던 자리가 그대로 있어야 합니다. 같은 번호를 다시 누르면 접히고, 다른 번호를 누르면 앞의 카드가 닫힙니다. Esc 로도 닫힙니다. 3. **거기서 바로 이어 말할 수 있는가** — 카드 아래 `이어 말하기` 를 눌러 보세요. 카드가 닫히고 입력줄 위에 그 문서가 집힌 표시가 서야 합니다. 곧바로 그 문서에 대해 말할 수 있습니다. 옆의 `문서 열기` 는 예전처럼 Docs 탭으로 갑니다 — 이건 누르셨을 때만 갑니다. (Ctrl+클릭이나 가운데 클릭은 예전처럼 새 탭입니다.) 4. **첨부 그림 경로** — 대답에 `…/assets/REQ-…/무엇.png` 같은 경로가 나오면 그것도 눌립니다. 새 탭에서 그림이 열립니다. **못 한 것 하나** — `web/index.html:4016` 같은 **코드 파일 경로는 아직 안 눌립니다.** 저장소의 코드 파일을 화면에 내주려면 "무엇까지 내줄 것인가" 를 먼저 정해야 하는데(비밀 파일·설정 파일이 같이 새면 안 됩니다), 그건 제가 혼자 정할 일이 아니라고 판단해 REQ-20260828-028-62x6 로 따로 냈습니다. 그 문서에 정할 것과 화면 몫을 적어 뒀습니다. 지금 바로 열어야 한다고 보시면 반려해 주시고, 순서가 맞다고 보시면 승인해 주세요. 또 하나 — Stream 탭과 문서 안의 대화 기록은 터미널 탭과 다른 방식으로 그려져서 거기 문서 번호는 아직 맨 글자입니다. 같은 것을 옮기는 일이라 이번에는 손대지 않았습니다.
- 2026-08-28T17:12:25+09:00 status: review -> in-progress (by nicehugepark) — 채팅 반려: 짧게 쓴 문서 번호(REQ-028)도 링크가 동작해야 한다
- 2026-08-28T18:19:53+09:00 status: in-progress -> review (by nicehugepark) — F5 로 새로고침한 뒤 Terminal 탭에서 봐 주세요. 두 가지입니다. 1. **없는 문서 번호는 이제 안 눌립니다.** 전에는 번호 모양이기만 하면 밑줄이 그어졌고, 눌러도 빈 쪽지가 떴습니다. 이제 저장소에 없는 번호는 그냥 글자로 남습니다. 문서 본문 보기에서도 같습니다. 2. **`REQ-028` 처럼 짧게 쓴 번호도 눌립니다 — 다만 "그때" 기준으로 풉니다.** 말씀하신 "가장 최근 날짜의 28번째" 를 그대로 옮기되, "가장 최근" 의 기준을 **화면을 보는 지금**이 아니라 **그 줄을 쓴 때**로 잡았습니다. 지금 기준으로 하면 어제 쓴 줄이 오늘 다른 문서를 가리킵니다 — 저장소에 굳어 있는 축약 298건에 대 보니 그중 208건이 그렇게 어긋났습니다. 하루에 번호가 30~90개씩 나가니 스크롤백이 하루를 못 버팁니다. 그래서 지금은 이렇게 동작합니다: 사흘 전 대화의 `REQ-017` 은 **사흘 전의 017** 을 엽니다. 오늘 새로 생긴 017 이 아닙니다. 짐작이라서 셋을 함께 뒀습니다. - 밑줄이 **물결**입니다. 확실한 번호(점선)와 눈으로 구분됩니다. - 마우스를 올리면 귀띔에, 눌러서 나온 쪽지 안에 **어떤 문서로 읽었는지 전체 번호**가 적힙니다. - 쪽지에 **`이어 말하기` 가 없습니다.** 짐작이 틀렸는데 이어 말하면 남의 문서에 글이 영구히 남고, 그건 닫기 한 번으로 못 지웁니다. 대신 `문서 열기` 로 옮겨 가서 번호를 확인한 뒤 거기서 집으시면 됩니다. 확인하실 곳: 대화 중에 `REQ-` 로 시작하는 짧은 번호를 쓰신 줄을 찾아 마우스를 올려 보세요. 없으면 주소창에 `http://127.0.0.1:9909/?shortref=peek#terminal` 을 치시면 화면 맨 아래에 예시 한 줄을 심고 쪽지를 열어 보여 드립니다. **안 한 것**: 문서 본문에서는 짧은 번호를 아직 안 풉니다(터미널만 하라고 하셔서). 그리고 `078` 처럼 **접두 없는 맨 숫자**는 못 잡습니다 — 일반 숫자와 구별할 방법이 없습니다.
- 2026-08-28T18:22:52+09:00 status: review -> done (by nicehugepark) — 승인 (메모 없음) [via dashboard]
