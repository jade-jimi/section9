---
id: REQ-20260829-027-62x6
type: request
title: 비대한 화면 파일을 가른다
summary: web/index.html 이 한 파일에 다 들어 있어 자주 고칠수록 서로 부딪힌다 — 모듈로 가른다
goal: web/index.html 이 의미 단위 모듈로 갈라져, 한 기능을 고칠 때 남의 자리를 열지 않아도 된다
status: done
size: L
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: 911949e0
agents: ["sub:frontend-developer:a5ae3d65", "sub:subagent:ac8f9b0c"]
contributions: [{"actor": "sub:frontend-developer:a5ae3d65", "item": "027 화면 파일 모듈화", "started": "2026-08-29T20:59:47+09:00", "ended": "2026-08-29T21:37:41+09:00", "result": "done", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/911949e0-5d1b-4cd2-b08d-70fd0415a19a/tasks/a5ae3d6579050fc7d.output"}, {"actor": "sub:frontend-developer:a5ae3d65", "item": "`[2026-08-29 21:36:49 KST - frontend-developer]`", "started": "2026-08-29T21:37:41+09:00", "ended": "2026-08-29T21:37:41+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/911949e0-5d1b-4cd2-b08d-70fd0415a19a/subagents/agent-a5ae3d6579050fc7d.jsonl"}, {"actor": "sub:subagent:ac8f9b0c", "item": "Appending backend note to REQ-20260829-027-62x6", "started": "2026-08-29T21:52:02+09:00", "ended": "2026-08-29T21:52:02+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/911949e0-5d1b-4cd2-b08d-70fd0415a19a/subagents/agent-ac8f9b0c9cf97a3c9.jsonl"}]
project: section9
children: ["REQ-20260829-029-62x6"]
relates: ["REQ-20260829-039-62x6"]
relates_why: {"REQ-20260829-039-62x6": "027 이 요청 1개를 39개로 만들면서 이 표면이 생겼다 — 가른 이득은 지키되 부른 횟수를 되돌린다"}
tags: ["auto-audit", "dashboard", "index", "attached", "png", "image", "docs", "terminal"]
created: 2026-08-29T16:51:55+09:00
status_since: 2026-08-29T22:28:17+09:00
updated: 2026-08-29T23:02:25+09:00
priority: 50
---

## Original

그리고 지금 자주 수정을 하는 web/index 파일이 너무 비대한데, 모듈화를 해라. 그리고 우선순위 기능을 화면에서 조절할 수 있게 하고, 우선순위가 바뀌면 제일 최우선으로 실행될 수 있게 설계를 제대로 하고, 구현해줘.

## Notes

### 2026-08-29T17:00:18+09:00 ask (by nicehugepark)

한 프롬프트에 여러개의 요청이 있으면 요청 문서를 나눠서 생성했던것같은데 그 기능은 사라진건가?

### 2026-08-29T20:08:49+09:00 decision (by nicehugepark)

착수 지시를 받았으나 **순서를 뒤로 둔다.** 이 요청은 `web/index.html` 을 통째로 다시 짜는 일이라 그 파일을 혼자 써야 한다. 지금 같은 파일에 030(자리를 사람에게 말한다)·031(없는 것을 말하는 자리)이 붙어 있고, 그 둘은 작은 변경이라 먼저 끝난다. 모듈화를 지금 겹쳐 붙이면 셋이 서로를 덮고, 그 사고를 막으려고 오늘 만든 규칙(한 파일에 한 사람)을 만든 날 어긴다. 030·031 이 닫히는 대로 착수한다.

### 2026-08-29T21:20:12+09:00 tdd (by nicehugepark)

### TDD 시나리오 — 가르기가 만드는 새 실패 모드

가르기의 위험은 "기능이 안 된다"가 아니라 **화면이 멀쩡해 보이면서 무너지는 것**이다.
그래서 시나리오도 그 셋을 겨눈다 (`tests/test_web_split.py`).

- [x] S1. 껍데기가 부르는 조각이 전부 실재한다 — 없으면 그 기능만 조용히 죽는다
- [x] S2. 아무도 안 부르는 고아 조각이 없다 · 같은 조각을 두 번 부르지 않는다
      (고아를 고치면 화면이 안 바뀐다 — 오늘 워크트리에서 이미 겪은 종류)
- [x] S3. 이어 붙인 한 장이 가르기 전의 그 화면이다 (`"use strict"` · `:root{` ·
      `boot();` · `#view` · `</html>` 가 다 있고, 가장 큰 스크립트 덩어리가 30만 자 이상)
- [x] S4. 껍데기가 다시 붓지 않는다 — `web/index.html` 220줄 이하, `<style>` 블록 없음
- [x] S5. 조각을 못 받았을 때의 알림은 **껍데기 안 인라인**이다 (조각으로 빼면 자기도
      못 와서 다시 흰 화면이 된다) · 마지막 조각이 `__S9_APP_READY` 표식을 세운다
- [ ] S6. 서버가 조각을 실제로 내준다 — `/css/*.css` 는 `text/css`, `/app/*.js` 는
      `text/javascript`, 전량 200, `web/` 밖으로 새는 길 없음.
      **지금 빨강이다.** `bin/s9` 정적 서빙 패치가 들어가야 초록이 된다 (S6d 만 통과).

되돌릴 수 없는 것을 검사한 방법: 자른 조각을 원래 순서로 이어 붙여 **가르기 전
원본과 글자 단위로 대조**했다 — 687,990자 전량 일치(배너 주석 38개·표식 1개·지킴이
1블록을 뺀 뒤). 내용이 사라지거나 순서가 바뀌지 않았다는 증거가 이것이다.

### 2026-08-29T21:23:28+09:00 frontend-developer (by nicehugepark)

### 가른 기준과 결과 (1차)

`web/index.html` 14,792줄 · 917KB 를 **껍데기 137줄 + CSS 12조각(3,067줄) + JS 26조각(11,722줄)** 으로 갈랐다.

### 어떻게 가르지 "않았"나 — 기각한 대안

- **ES 모듈(`type="module"`) 로의 전환 — 기각.** 이 화면의 JS 11,667줄은 하나의 전역
  이름공간을 공유한다(함수 400여 개가 서로를 이름으로 부른다). 모듈로 바꾸려면 그
  참조 전부에 export/import 를 달아야 하는데, 그것은 가르기가 아니라 **다시 쓰기**다.
  "모습과 동작을 바꾸지 마라"와 정면으로 부딪힌다. 대신 **순서가 있는 classic script**
  로 갔다 — 브라우저가 그대로 읽고, 의미론이 이어 붙이기와 같다(전역 스코프 하나,
  위에서 아래로 실행). 번들러는 어느 쪽이든 안 들인다.
- **줄 수로 자르기 — 기각.** 같이 바뀌는 것끼리 모아야 다음 사람이 한 파일만 연다.
  경계는 오늘 실제로 부딪힌 자리에서 가져왔다: 판·카드·문서·터미널·판정 창·계정/모델
  창·첨부·스킨·스트림.
- **조각을 `.html` 로 위장해 서버를 안 고치기 — 성립하지 않음.** 지금 정적 서빙은
  `/[\w.-]+\.html` 하나뿐이라 확장자를 맞추면 서버를 안 고쳐도 되나 확인했는데,
  **스타일시트는 안 된다**: 표준 모드 브라우저는 `text/html` 로 온 stylesheet 를
  거부한다. 그래서 서버에 한 갈래를 더해야 한다 (아래 패치).

### 내용이 안 바뀌었다는 증거

자른 조각을 원래 순서로 이어 붙여 가르기 전 원본과 **글자 단위로 대조**했다 —
687,990자 전량 일치. 사라진 줄도, 순서가 바뀐 줄도 없다. 그 이어 붙이기를
`tests/webasset.py` 가 맡고, 화면 계약 시험 81개가 그것을 읽는다.

### 곁들여 고친 것 둘 (가르기가 만든 구멍)

- **엄격 모드는 파일 단위다.** 원본은 `"use strict"` 한 줄로 11,667줄 전체가 엄격
  모드였다. 그대로 자르면 첫 조각만 엄격해지고 나머지 25개가 느슨해진다(선언 안 한
  변수 대입이 조용히 전역이 된다). 조각마다 선언을 세웠다.
- **조각을 못 받으면 흰 화면이다.** 옛 서버에 붙으면 아무 말 없는 흰 판이 남는다.
  이 저장소가 낡은 코드 알림에서 이미 값을 치른 종류라, 껍데기 끝에 지킴이를 넣었다 —
  무엇이 안 왔고 무엇을 하면 되는지 세 문장으로 적는다 (캡처 첨부).

### 2026-08-29T21:23:56+09:00 frontend-developer (by nicehugepark)

### `bin/s9` 에 필요한 것 — 리드가 적용 (한 갈래 삽입, 기존 줄 무수정)

화면이 뜨려면 서버가 `web/css/*.css` 와 `web/app/*.js` 를 내줘야 한다. 지금은 404 다.
**기존 줄을 하나도 고치지 않는 순수 삽입**이라 다른 작업과 글자로 부딪히지 않는다.

자리: `do_GET` 안, 정적 html 갈래(`elif re.fullmatch(r"/[\w.-]+\.html", ...)`) 의
`else: self._send(404, "text/plain", b"not found")` **바로 다음 줄**
(그 아래가 `# 가시성 필터는 응답 직전에만` 주석 + `/api/serveinfo` 갈래다).

```python
            elif re.fullmatch(r"/(?:app|css)/[\w.-]+\.(?:js|css)",
                              parsed.path or ""):
                # 화면 조각 (REQ-20260829-027-62x6). web/index.html 이 갈라져
                # web/css/*.css 와 web/app/*.js 를 부른다. 위 html 길과 **같은
                # realpath 규율**이다 — 접두 검사만으로는 심링크가 새 나간다.
                #
                # MIME 은 정확해야 한다: 스타일시트를 text/html 로 내주면 표준
                # 모드 브라우저가 그것을 **거부한다**. 조각을 .html 로 위장해
                # 이 갈래를 아예 만들지 않는 우회가 성립하지 않는 이유다.
                sub, _, base = parsed.path.strip("/").partition("/")
                home = os.path.realpath(os.path.join(ROOT, "web", sub))
                fp = os.path.realpath(
                    os.path.join(home, os.path.basename(base)))
                if os.path.dirname(fp) == home and os.path.isfile(fp):
                    ctype = ("text/javascript" if fp.endswith(".js")
                             else "text/css")
                    with open(fp, "rb") as f:
                        self._send(200, ctype + "; charset=utf-8", f.read())
                else:
                    self._send(404, "text/plain", b"not found")
```

이 갈래가 들어가면 `tests/test_web_split.py` 의 `Served` 4개(S6·S6b·S6c·S6d)가 초록이
된다. 지금은 S6d(옆문 차단)만 통과하고 셋이 빨강이다 — **그 빨강이 이 패치의 인수
조건**이다. 캐시 헤더는 `_send` 가 이미 `no-store` 라 화면이 즉시 바뀐다(정적 파일은
매 요청 디스크에서 읽으므로 `running_code_stamp` 에 더할 것이 없다).

### 곁들여 권함 (선택, 같은 파일)

`SPINE` 이 등뼈로 세는 것이 `web/index.html` 하나인데, 로직이 이제 `web/app/` 에 있다.
등뼈가 미커밋일 때 워크트리를 피하는 이유("검증 자체가 거짓말을 한다")가 새 조각에도
글자 그대로 적용된다.

```python
SPINE = ("bin/s9", "web/index.html")    # 이 저장소가 통째로 기대는 두 파일
SPINE_UNDER = ("web/css/", "web/app/")  # 그 아래 전부가 등뼈인 자리 (화면 조각)
...
    spine = [p for p in dirty if p in SPINE or p.startswith(SPINE_UNDER)]
```

### 문서 한 줄 (harness/claude/skills/s9-design/SKILL.md:8)

`대시보드는 단일 파일(web/index.html), 외부 CDN 금지(오프라인), 의존성 0.`
→ `대시보드는 web/index.html(껍데기) + web/css/ + web/app/ 조각들. 외부 CDN 금지(오프라인), 의존성 0.
새 스킨은 web/css/skins.css 에, 토큰은 web/css/tokens.css 에 — 순서가 곧 계약이라 껍데기의 부르는 차례를 지킨다.`

[Image: assets/REQ-20260829-027-62x6/guard-oldserver.png]

### 2026-08-29T21:29:05+09:00 frontend-developer (by nicehugepark)

### 새 파일 목록 — 각각이 맡은 것

**껍데기** `web/index.html` 137줄 (14,792줄에서). 마크업 + 조각 부르는 차례 +
"조각을 못 받았을 때"의 지킴이. 여기 규칙·로직을 다시 넣으면 시험이 막는다.

**web/css/ — 12조각 3,067줄** (뒤에 오는 것이 앞을 덮는다, 순서가 계약)
`tokens.css`(52) 색·간격·글꼴의 단일 소유자 · `header.css`(245) 헤더 두 줄·서버 알림
띠·사용량 칩 · `board.css`(187) 계기줄·컬럼·카드 · `docs.css`(293) 목록·뷰어·판정 근거·
본문 표·첨부·Audit · `terminal.css`(375) Stream/Terminal 판 · `graph.css`(113) ·
`responsive.css`(63) 좁은 폭 보정 · `actions.css`(199) 판정 버튼·드래그 ·
`skins.css`(533) skin 여덟 · `density.css`(197) compact 축 · `calm.css`(398) tone
mist·graphite + skin calm(한 벌로 설계된 세트) · `overlay.css`(399) hovercard·아티클·
구간 메모·판정 창·고르는 창·사용량 카드 — **스킨 뒤에 온다**(원본에서 그 자리였고
동순위 규칙의 승패가 그 순서에 달렸다).

**web/app/ — 26조각 11,722줄** (위에서 아래로 실행)
`const`(143) · `state`(133) 화면 상태와 정렬 · `refs`(144) 축약 참조·선행 대기 ·
`format`(161) 표기·표시 설정 · `notice`(545) 신원·낡은 코드·자동 복구·상태 칩 ·
`boot`(381) 첫 그림의 보급선 · `app`(412) 라우팅·필터·render · `card`(842) 멈춤·작업
자리·깨우기·판정 전이·컬럼 · `docs`(546) 목록·뷰어·backlinks · `dialog`(593)
hovercard·s9dlg·s9choose · `attach`(238) 첨부 그림·md2html · `anchor`(500) 문서 구간·
구간 메모 · `audit`(199) Audit·Stream · `usage`(173) TERM 상태·사용량 칩 ·
`ccrender`(582) cc 줄 렌더·서브에이전트 병합 · `terminal`(805) 터미널 판·SSE·에이전트
띠 · `target`(532) 지목 문서·코드 창 · `input`(485) 붙여넣기·키보드·팔레트 ·
`session`(640) 세션·모델·계정 창 · `restart`(279) 재시작·sendChat · `graph`(1,356)
Graph 탭 · `settings`(236) · `timezone`(464) · `userform`(670) · `events`(314) 전역
배선 + `boot()` · `diag`(324) 진단 재현자 + 조각 도착 표식.

### 시험은 어떻게 옮겼나 — 계약은 한 글자도 안 바꿨다

`tests/webasset.py` 하나가 조각을 **브라우저가 받는 한 장**으로 되돌린다
(`<link>` 묶음 → `<style>…</style>`, `<script src>` 묶음 → `<script>…</script>`).
시험 81개에서 바뀐 것은 **경로를 얻는 줄 하나뿐**이다:
`INDEX = os.path.join(HERE, "..", "web", "index.html")` → `INDEX = index_path()`.
`open(INDEX)` 도, 그 뒤의 정규식도, 단언도 그대로다. 이어 붙이는 규칙이 두 벌이
되지 않게 도우미는 하나로 두었다 — 갈라지면 시험들이 서로 다른 화면을 본다.

### 남은 것 (후속 REQ 감)

1. **의존 경계는 아직 없다.** 이번은 *파일* 경계다 — 26조각이 여전히 전역 이름공간
   하나를 공유한다. 누가 누구를 부르는지는 파일이 말해 주지 않는다. 다음 단계는
   조각마다 밖으로 내보내는 이름을 정하는 것(그다음이 ES 모듈).
2. **`graph.js` 1,356줄이 아직 한 덩어리다** — `renderGraph` 함수 하나가 1,200줄이라
   더 자르려면 함수를 갈라야 한다(구조가 아니라 코드를 만지는 일이라 분리했다).
3. `card.js`(842)·`terminal.js`(805)·`userform.js`(670) 도 다음 차례.
4. 곁가지: `CODE_DIAG` 의 예시 주소 `web/index.html:4016` 이 이제 없는 줄을 가리킨다
   (`?codepeek` 진단은 링크가 서는지만 보므로 통과하지만, 눌러 보면 빈 창이다).
   test_code_peek_ui 가 이 글자를 계약으로 잡고 있어 여기서 손대지 않았다.

### 2026-08-29T21:29:32+09:00 frontend-developer (by nicehugepark)

### 가르기 전후 — 같은 주소, 같은 스킨

`bin/s9` 를 못 고치는 동안 검증이 막히지 않게, **제안한 패치와 같은 로직**을 쓰는
대역 서버를 임시로 세우고(정적은 직접, `/api/…` 는 9909 로 넘김) 그 주소를 찍었다.
그러니 아래 캡처는 가르기뿐 아니라 **패치가 들어간 뒤의 화면**이기도 하다.

찍은 것: Board(ledger·glass·terminal·calm × 라이트/다크) · Docs 목록 · 문서 뷰어(다크) ·
Terminal(SSE·에이전트 띠·스피너 살아 있음) · Graph(물리 도는 캔버스) · Settings ·
판정 창(`?dlg=judge`). 콘솔 오류 없음, 죽은 판 없음.

**차이는 없다.** 카드 순서가 미세하게 다른 것은 경과 시각이 흐른 탓이고(라이브 데이터),
한 캡처의 머리에 붙은 「서버가 옛 코드로 돌고 있습니다」 띠는 그때 다른 작업자가
`bin/s9` 를 만지고 있었기 때문이다 — 둘 다 이 변경과 무관하다.

첫 장이 **가르기 전**, 둘째가 **가른 뒤**다 (같은 ledger·paper·Board).
셋째는 calm 스킨 — 스킨 override 와 overlay 의 얹히는 차례가 살아 있음을 본다.
넷째는 문서 뷰어(다크), 다섯째는 Terminal.

[Image: assets/REQ-20260829-027-62x6/board-ledger-paper.png]
[Image: assets/REQ-20260829-027-62x6/board-ledger.png]
[Image: assets/REQ-20260829-027-62x6/board-calm.png]
[Image: assets/REQ-20260829-027-62x6/doc.png]
[Image: assets/REQ-20260829-027-62x6/terminal-ledger-paper.png]

### 2026-08-29T21:36:25+09:00 frontend-developer (by nicehugepark)

### 전체 스위트 (숫자 그대로)

`python3 tests/` 두 번 돌렸다 (조각을 만든 뒤 한 번, 엄격 모드 선언과 껍데기 주석을
더한 뒤 한 번). 파일로 받아 grep 했다 — `| tail` 로 요약 줄을 자르지 않았다.

- 1차: **Ran 2203 tests in 462.9s · FAILED (failures=4, skipped=1)**
  넷 중 셋은 아래 S6, 하나는 `test_stalled_checkclaim.test_b4_live_session_is_not_evidence`
  로 **이 변경과 무관**하다(`bin/s9` 의 `stall_mins` 를 읽는 시험 — 그때 다른 작업자가
  그 함수를 재작업 중이었다).
- 2차: **Ran 2212 tests in 516.2s · FAILED (failures=3, skipped=1)**
  남은 셋이 전부 S6 다(1차의 무관한 하나는 그 사이 고쳐져 사라졌다). 포트 누수 없음.

```
FAIL: test_s6_style_parts_are_served_as_css   (/css/tokens.css 404)
FAIL: test_s6b_script_parts_are_served_as_js  (/app/const.js 404)
FAIL: test_s6c_every_part_is_reachable        (조각 38개 전부 404)
```

**이 셋이 `bin/s9` 정적 서빙 패치의 인수 조건이다.** 패치가 들어가면 초록이 되고,
그때 회귀는 0 이다. 나머지 2,209개는 화면 계약 시험 81개를 포함해 전부 통과한다.

### 값어치 (재는 자리)

요청이 1 → 39 로 늘었다. 브라우저는 같은 출처에 동시 6개까지만 여니 이 환경의
루프백 동시 연결 벼랑(DOC-20260827-004-62x6, 열 개쯤에서 자른다)에는 닿지 않는다.
실측 순차 39요청 904KB, 렌더는 가르기 전과 같은 대기(3초) 안에 끝났다. 조각은
`no-store` 로 둔다 — 고친 화면이 F5 로 바로 보여야 하는 저장소라 캐시가 답이 아니다.
조각이 크게 더 늘면 다시 잴 자리다.

### 2026-08-29T21:36:45+09:00 frontend-developer (by nicehugepark)

바로 앞 노트의 시험 이름을 바로잡는다: 1차 실행에서 이 변경과 무관하게 빨갛던 하나는
`test_stalled_claim.StalledClaim.test_b4_live_session_is_not_evidence` 다
(`bin/s9` 의 `stall_mins` 재작업 중이던 자리 — 2차 실행에서는 이미 초록이었다).

### 2026-08-29T21:37:41+09:00 subagent (agent: sub:frontend-developer:a5ae3d65, by nicehugepark)

`[2026-08-29 21:36:49 KST - frontend-developer]`

### 결론

`web/index.html` **14,792줄 → 껍데기 137줄 + `web/css/` 12조각(3,067줄) + `web/app/` 26조각(11,722줄)**. 화면의 모습과 동작은 그대로다(캡처 대조). 전체 스위트 **2,212개 중 3개 실패**이고, 그 셋은 전부 내가 일부러 남긴 빨강 — **`bin/s9` 정적 서빙 패치의 인수 조건**이다. 그 패치가 없으면 화면이 안 뜬다.

### 가른 기준과 기각한 대안

**기준**: 줄 수가 아니라 오늘 실제로 부딪힌 경계 — 판·카드·문서·터미널·판정 창·계정/모델 창·첨부·스킨·스트림. 순서는 한 줄도 바꾸지 않았다(CSS 캐스케이드와 JS 실행 순서가 곧 계약이라).

- **ES 모듈(`type="module"`) — 기각.** JS 11,667줄이 전역 이름공간 하나를 공유한다(함수 400여 개가 서로를 이름으로 부른다). export/import 를 다는 것은 가르기가 아니라 다시 쓰기고, "동작을 바꾸지 마라"와 정면 충돌한다. 대신 **순서 있는 classic script** — 브라우저가 그대로 읽고 의미론이 이어 붙이기와 같다. 번들러는 어느 쪽이든 안 들였다.
- **조각을 `.html` 로 위장해 서버를 안 고치기 — 성립하지 않는다.** 지시대로 먼저 확인했다: 지금 정적 서빙은 `re.fullmatch(r"/[\w.-]+\.html", …)` 하나뿐이라 확장자만 맞추면 서버 수정이 필요 없어 보이지만, **스타일시트는 안 된다** — 표준 모드 브라우저는 `text/html` 로 온 stylesheet 를 거부한다. 그래서 서버에 한 갈래가 필요하다.
- **줄 수로 자르기 — 기각.** 같이 바뀌는 것끼리 모여야 다음 사람이 한 파일만 연다.

**내용이 안 바뀌었다는 증거**: 조각을 원래 순서로 이어 붙여 가르기 전 원본과 글자 단위 대조 — **687,990자 전량 일치**.

### `bin/s9` — 손대지 않았다. 패치는 이것 (기존 줄 무수정, 순수 삽입)

자리: `do_GET` 안, 정적 html 갈래의 `else: self._send(404, "text/plain", b"not found")` **바로 다음 줄**(그 아래가 `# 가시성 필터는 응답 직전에만` 주석 + `/api/serveinfo`).

```python
            elif re.fullmatch(r"/(?:app|css)/[\w.-]+\.(?:js|css)",
                              parsed.path or ""):
                # 화면 조각 (REQ-20260829-027-62x6). 위 html 길과 같은 realpath 규율 —
                # 접두 검사만으로는 심링크가 새 나간다. MIME 은 정확해야 한다:
                # 스타일시트를 text/html 로 내주면 브라우저가 거부한다.
                sub, _, base = parsed.path.strip("/").partition("/")
                home = os.path.realpath(os.path.join(ROOT, "web", sub))
                fp = os.path.realpath(
                    os.path.join(home, os.path.basename(base)))
                if os.path.dirname(fp) == home and os.path.isfile(fp):
                    ctype = ("text/javascript" if fp.endswith(".js")
                             else "text/css")
                    with open(fp, "rb") as f:
                        self._send(200, ctype + "; charset=utf-8", f.read())
                else:
                    self._send(404, "text/plain", b"not found")
```

곁들여 권함(선택): `SPINE` 이 등뼈로 세는 것이 `web/index.html` 하나인데 로직이 이제 `web/app/` 에 있다 — `SPINE_UNDER = ("web/css/", "web/app/")` 를 더하고 `spine = [p for p in dirty if p in SPINE or p.startswith(SPINE_UNDER)]`. 그리고 `harness/claude/skills/s9-design/SKILL.md:8` 의 "대시보드는 단일 파일(web/index.html)" 한 줄(자세한 문안은 REQ 노트에).

**주의**: 패치 전까지 9909 는 조각을 404 로 돌려준다. 흰 화면은 안 나오게 껍데기 끝에 지킴이를 넣었다 — 무엇이 안 왔고 무엇을 하면 되는지 세 문장으로 적는다(캡처 REQ 첨부).

### 시험을 어떻게 옮겼나 — 계약은 한 글자도 안 바꿨다

`/home/sjpark1/section9/tests/webasset.py` 하나가 조각을 브라우저가 받는 한 장으로 되돌린다(`<link>` 묶음 → `<style>…</style>`, `<script src>` 묶음 → `<script>…</script>`). 시험 81개에서 바뀐 것은 **두 줄뿐**이다 — import 한 줄과 `INDEX = os.path.join(HERE, "..", "web", "index.html")` → `INDEX = index_path()`. `open(INDEX)` 도 그 뒤의 정규식·단언도 그대로다. 이어 붙이는 규칙이 두 벌이 되지 않게 도우미는 하나만 두었다.

새 계약 시험 `/home/sjpark1/section9/tests/test_web_split.py` — 조각 실재·고아 금지·중복 호출 금지·한 장의 완전성·껍데기 220줄 상한·`<style>` 재유입 금지·지킴이 인라인 유지·서버 서빙(S6).

### 곁들여 고친 것 둘 (가르기가 낸 구멍)

1. **엄격 모드는 파일 단위다.** 원본은 `"use strict"` 한 줄로 전체가 엄격이었는데 자르면 첫 조각만 엄격해진다 — 조각 26개 전부에 선언을 세웠다.
2. **조각을 못 받으면 흰 화면** — 껍데기 끝의 지킴이(마지막 조각이 세우는 `__S9_APP_READY` + `--bg` 토큰 둘 다 본다).

### 캡처 (가르기 전후, 같은 주소·같은 스킨)

`bin/s9` 를 못 고치는 동안 검증이 막히지 않게 **제안한 패치와 같은 로직**의 대역 서버를 임시로 세워 찍었다(정적 직접 + `/api/…` 는 9909 프록시). 그러니 이 캡처는 패치가 들어간 뒤의 화면이기도 하다. 다 찍고 정리했다.

- 전: `/tmp/claude-1000/-home-sjpark1-section9/911949e0-5d1b-4cd2-b08d-70fd0415a19a/scratchpad/before/` (board ledger·glass·terminal × paper·carbon, docs, terminal, graph)
- 후: 같은 경로의 `after/`, `after2/` (+ `?dlg=judge` 판정 창, Settings, 문서 뷰어 다크, 지킴이 화면)
- **REQ 문서에 전/후/calm/문서뷰어/터미널 5장 + 지킴이 1장을 그림으로 붙였다.**

차이는 없다. 카드 순서가 미세하게 다른 것은 경과 시각(라이브 데이터), 한 캡처의 「서버가 옛 코드로 돌고 있습니다」 띠는 그때 다른 작업자가 `bin/s9` 를 만지던 탓 — 둘 다 무관하다.

### 전체 스위트 (숫자 그대로)

- 1차: `Ran 2203 tests in 462.9s · FAILED (failures=4, skipped=1)` — 셋은 S6, 하나는 무관(`test_stalled_claim.StalledClaim.test_b4_live_session_is_not_evidence`, 그때 `bin/s9 stall_mins` 재작업 중)
- 2차: `Ran 2212 tests in 516.2s · FAILED (failures=3, skipped=1)` — **셋이 전부 S6.** 포트 누수 없음

### 남은 것 (후속 REQ 감)

1. **의존 경계는 아직 없다.** 이번은 *파일* 경계다 — 26조각이 여전히 전역 하나를 공유하고, 누가 누구를 부르는지는 파일이 말해 주지 않는다. 다음 단계가 조각별 공개 이름 정하기(그다음이 ES 모듈).
2. `web/app/graph.js` 1,356줄이 아직 한 덩어리 — `renderGraph` 함수 하나가 1,200줄이라 더 자르려면 함수를 갈라야 한다(구조가 아니라 코드를 만지는 일이라 이번 범위에서 뺐다). 이어서 `card.js`(842)·`terminal.js`(805)·`userform.js`(670).
3. 곁가지: `CODE_DIAG` 의 예시 주소 `web/index.html:4016` 이 이제 없는 줄을 가리킨다(`?codepeek` 진단은 통과하지만 눌러 보면 빈 창). `test_code_peek_ui` 가 그 글자를 계약으로 잡고 있어 손대지 않았다.

상태 전이는 하지 않았다 — 판정은 리드 몫이다. 기록은 REQ-20260829-027-62x6 에 `tdd`·`frontend-developer` 라벨로 남겼다.

### 2026-08-29T21:51:40+09:00 backend-developer (by nicehugepark)

### 서버가 화면 조각을 내준다 (리드가 대신 넣어 달라 한 패치)

REQ-036 작업 중 `bin/s9` 를 쥐고 있어 대신 넣었다. 두 줄기다.

**① `/css/*.css` · `/app/*.js` 라우트** — `do_GET` 의 정적 html 갈래 바로
다음에 삽입했다. 기존 줄은 하나도 안 고친 순수 삽입이다.

    elif re.fullmatch(r"/(?:app|css)/[\w.-]+\.(?:js|css)", parsed.path or ""):

- **경로 규율은 위 html 갈래와 같다** — `realpath` 로 풀고 `dirname(fp) ==
  home` 으로 잠근다. 접두 검사만으로는 심링크가 새 나간다(REQ-20260828-028-62x6
  에서 배운 것). 정규식이 이미 슬래시를 막지만 `basename()` 도 함께 건다.
- **MIME 은 정확히** `text/javascript` / `text/css` (+charset). 스타일시트를
  text/html 로 내주면 브라우저가 아예 거부한다.
- **캐시는 안 준다.** `_send()` 가 이미 `Cache-Control: no-store` 를 붙인다 —
  첨부(`_send_asset`)처럼 ETag 로 물리지 않은 것은 판단이다: 조각은 자주
  바뀌고 여기는 루프백이라 대역폭이 문제가 아니다. 물려 두면 "고쳤는데 F5
  해도 옛 화면"이 되고, 그 자막은 결함이다.

**② `SPINE_UNDER = ("web/css/", "web/app/")`** — 넣었다. 리드의 판단이 맞다.
등뼈 판정(`workspace_decision` 의 `dirty-spine`)이 `web/index.html` 한 파일만
셌는데 로직이 조각으로 옮겨 갔다. 그대로 두면 조각이 미커밋인 채 워크트리가
열리고, 그 자리의 검증(9909 서버·tests 의 `../web`)이 과거를 본다 — 오늘
워크트리 아홉을 만든 그 사고와 같은 모양이다. `p in SPINE or
p.startswith(SPINE_UNDER)` 한 줄.

**검증**

- `python3 tests/ web_split` → 13 tests, OK (S6 셋 포함 — 전에는 404 로 빨강).
- `web/index.html` 껍데기가 참조하는 조각 **38개 전부** 200. (curl 이 간혹
  `000` 을 내는데 루프백 동시 연결 상한이지 라우트가 아니다 — `--retry` 로
  전부 200. DOC-20260827-004-62x6 에 적힌 그 현상이다.)
- MIME 확인: `/app/app.js` → `200 text/javascript; charset=utf-8`,
  `/css/tokens.css` → `200 text/css; charset=utf-8`.
- 탈출 시도 `/app/../../bin/s9` → 404, 없는 조각 `/css/nope.css` → 404.
- 전체 스위트 `python3 tests/` → **Ran 2214 tests … OK (skipped=1)**.
- `s9 shot` 으로 9909 를 직접 찍었다 — 보드·카드·필터·열이 모두 그려진다.

**넘길 것 하나 (화면 몫, 내가 안 고쳤다)**: 캡처를 보면 in-progress 의
REQ-029·025 카드에 `멈춤 …` 줄이 없다. 서버는 그 두 행에 `stalled_mins`
(75·76)를 그대로 싣고 있다(`curl /api/catalog` 로 확인). 조각으로 가르면서
그 줄을 그리는 자리가 빠졌는지 확인해 달라. 그리고 REQ-036 이 행에 새 필드
넷(`stall_state`·`quiet_mins`·`wait_*`)을 실었으니 그 계약도 함께 보라.

### 2026-08-29T21:52:02+09:00 subagent (agent: sub:subagent:ac8f9b0c, by nicehugepark)

Appending backend note to REQ-20260829-027-62x6

### 2026-08-29T22:20:08+09:00 commit (by nicehugepark)

커밋 2faa0d2 — 화면을 조각으로 가르고, 멈춤이 일하는 손을 본다 (REQ-027·036·024)

### 2026-08-29T22:20:56+09:00 tdd (by nicehugepark)

### TDD S1~S6 — 전부 검증 완료 (체크 갱신)

S6 이 마지막까지 빨강이었던 것은 **의도한 것**이다 — `bin/s9` 정적 서빙 패치의 인수
조건이었고, 그 패치는 다른 사람이 쓰던 파일이라 리드가 036 담당에게 넘겨 넣었다.

- [x] S1. 껍데기가 부르는 조각이 전부 실재한다
- [x] S2. 고아 조각 없음 · 같은 조각 두 번 부르지 않음
- [x] S3. 이어 붙인 한 장이 가르기 전의 그 화면이다 (687,990자 전량 일치)
- [x] S4. 껍데기가 다시 붓지 않는다 (137줄, `<style>` 없음)
- [x] S5. 조각을 못 받았을 때의 알림은 껍데기 안 인라인
- [x] S6. **서버가 조각을 실제로 내준다** — `/css/*.css` → `text/css`,
      `/app/*.js` → `text/javascript`, 껍데기가 참조하는 38개 전부 200.
      실측: `curl /app/anchor.js` → `200 text/javascript; charset=utf-8`.

`python3 tests/ web_split` 13건 OK. 전체 2214건 OK. 커밋 2faa0d2.

**게이트가 이 시험에서 결함 하나를 잡았다.** S6 의 서버 시험이 `os.environ` 을 그대로
물려받아, 다른 모듈이 전역에 갈아 끼운 `S9_ROOT`(임시 자리)를 뿌리로 삼고 조각을
404 로 돌려줬다 — **혼자 돌리면 통과하고 스위트에서만 깨지는** 실패다. `S9_ROOT` 를
저장소 뿌리로 못박고 경위를 주석에 적었다. 커밋 게이트가 없었으면 이 상태로 밀렸다.

## History
- 2026-08-29T16:51:55+09:00 created by nicehugepark (status: open)
- 2026-08-29T20:08:32+09:00 status: open -> in-progress (by nicehugepark) — drag 이동 [via dashboard]
- 2026-08-29T22:20:56+09:00 status: in-progress -> review (by nicehugepark) — F5 후 대시보드가 평소와 똑같이 떠야 합니다 — 화면 파일을 조각으로 갈랐지만 모습과 동작은 그대로입니다. 판·문서·터미널·판정 창을 한 번씩 눌러 보세요. 이상이 없으면 승인, 어딘가 깨지거나 안 뜨면 반려입니다. 흰 화면 대신 "무엇이 안 왔는지" 적힌 안내가 뜨면 그것도 반려해 주세요.
- 2026-08-29T22:28:17+09:00 status: review -> done (by nicehugepark) — 승인 (메모 없음) [via dashboard]
