---
id: REQ-20260824-048
type: request
title: 깃 동기화 주기 검토
summary: 가이드 완료 시 자동 푸시 + 푸시/풀 주기 설계 + 다른 사용자와의 준실시간 vault 동기화 방안 검토(코드/데이터 리모트 분리, 이벤트 기반 push, serve 폴링 pull)
goal: 동기화 설계(주기·리모트 구성·충돌 전략·실시간성 한계)가 지식 문서로 정리되어 사용자 방향 승인을 받는다
status: done
size: M
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: d332535d
sessions: ["104b4fe3", "d332535d"]
agents: ["lead:claude-fable-5", "Explore", "sub:architect"]
contributions: [{"actor": "sub:architect", "item": "결론: 설계를 as-built 기준으로 갱신하고 방향 5개를 확정했다 (DOC-20260826-002가 DOC-20260824", "started": "2026-08-26T00:19:23+09:00", "ended": "2026-08-26T00:19:23+09:00", "result": "done", "transcript": ""}, {"actor": "sub:architect", "item": "TDD 시나리오 (개정 — 이 요청의 완료 기준은 '설계 승인'이다)", "started": "2026-08-26T00:19:23+09:00", "ended": "2026-08-26T00:19:23+09:00", "result": "done", "transcript": ""}]
project: section9
children: ["REQ-20260826-003-62x6"]
derived_from: DOC-20260826-002-62x6
relates: ["REQ-20260824-053", "REQ-20260825-013", "REQ-20260825-066-62x6"]
tags: ["auto-audit", "sync", "docs", "dashboard"]
created: 2026-08-24T20:23:17+09:00
status_since: 2026-08-26T23:28:34+09:00
updated: 2026-08-26T23:28:34+09:00
---

## Original

완료되면 깃헙에 푸시도 알아서 하고, 푸시하고 풀 하는 주기에 대해도 고민해볼 필요가 있다. 다른 사용자와 실시간으로 싱크를 하기 위해서 어떻게 할지도 검토해줘.

## Notes

### 2026-08-24T20:25:50+09:00 response (agent: lead:claude-fable-5, by nicehugepark)

**REQ-048(동기화 설계) 검토를 마치고 리뷰로 올렸습니다** — 설계 전문은 DOC-20260824-002에 있습니다. 판정해 주실 갈림길은 두 가지입니다:

1. **리모트 구성**: 말씀하신 대로 곧 private으로 전환하시면 **단일 리포에 데이터(vault/users/projects/streams)까지 track**하는 원안(docs/08)이 다시 성립합니다 — 가장 단순합니다. 반면 나중에 코드만 다시 공개할 가능성이 있으면 **dual-repo**(코드 리포 + 사설 데이터 리포를 같은 디렉토리에 이중 git으로)가 안전합니다.
2. **주기**: push는 문서 쓰기 이벤트 기반 + 30초 디바운스 배치(serve 스레드), pull은 serve 60초(유휴 시 5분) + 세션 시작 1회 — 다른 사용자 변경이 보이는 지연은 통상 1분 미만, 최악 ~90초입니다. 초 단위 실시간이 필요해지면 활동 감지 시 pull을 10초로 좁히는 가변 주기를 먼저 쓰고, 그래도 부족할 때만 대시보드 허브(SSE) 방식을 검토하는 순서를 권합니다. 충돌은 rebase 실패 시 자동 해결을 시도하지 않고 대시보드 ⚠ 배지로 사람에게 넘깁니다.

승인하시면 `s9 sync` 명령과 serve 자동 push/pull 스레드(1·2단계)를 구현합니다.

중간 지시 두 건도 반영해 푸시했습니다(커밋 2cc31e6): README 초반부에 **사용자 가이드 안내 블록**(가이드 → docs 순 진입 동선)을 배치했고, private 전환 전제에 따라 guide.html의 gitignore 제외를 해제했습니다 — 지금 워커가 프로젝트 챕터를 작업 중이라, **완료·검증되는 대로 guide.html을 커밋·푸시**하고 018을 재리뷰로 올리겠습니다.

### 2026-08-24T21:19:32+09:00 response (by nicehugepark)

구조 판정 해소(사용자 확정, 2026-08-24): 리모트 구성은 업스트림-인스턴스 플로우로 확정 — 데이터는 인스턴스 리포가 track(REQ-053 구현·docs/08). 남은 판정: 주기 자동화(s9 sync: push 디바운스 30s/pull 60s, 기본 off 옵트인) 구현 여부만.

### 2026-08-25T17:59:25+09:00 response (agent: lead:claude-fable-5, by nicehugepark)

### 재작업 구현 (반려 확정 방향 — 이벤트마다 순차 commit→pull→push)
적용 완료(bin/s9): sync_enabled(.s9-sync 마커+git 리포, S9_SYNC=off 킬스위치) /
sync_run(락 직렬화 → 데이터 경로만 add → 변경 시 commit → pull --rebase(실패 시
abort+로그) → push, 전 단계 fail-soft·state/sync.log 기록, 오프라인 백오프 60s —
백오프 중엔 로컬 커밋만 하고 다음 성공 때 일괄 push) / maybe_sync 호출 지점:
do_transition(대시보드 전이 포함)·new·note·set·link·rm / cmd_sync(수동·--status) /
instance init이 .s9-sync를 생성·track — 팀원 클론에도 자동 적용, section9 코드
리포는 마커 없음=완전 비활성(REQ-051 제약 유지). 실측: s9 sync --status 정상
로드·비활성 확인.

### TDD 시나리오 (tests/test_s9_sync.py 작성済 — 실행 대기)
- [ ] Y1. 생성 이벤트가 origin(bare)까지 push
- [ ] Y2. 전이 이벤트 추가 커밋 push
- [ ] Y3. 마커 없으면 git 동작 0 (051)
- [ ] Y4. 원격 불능: 작업 성공+로컬 커밋 보존+⚠로그+백오프(후속 이벤트 빠름)
- [ ] Y5. 협업: 타 클론 선push → pull --rebase 후 push, 동료 변경 워킹트리 반영
- [ ] Y6. S9_SYNC=off 킬스위치

### 리드 인계 (이 봉투는 python3 테스트 실행 권한이 없음)
1) python3 tests/test_s9_sync.py → Y1~Y6 확인 후 tdd 노트 체크 개정
2) 전체 스위트 회귀 → 커밋·업스트림 푸시 → example-org merge 배포
3) example-org 루트에 .s9-sync 생성·커밋(기존 인스턴스 소급) → 실이벤트 1회로 origin 반영 실측
4) REQ-20260825-006의 sync 안전망(pull-merge 중복 id 리네임)은 후속 통합 지점
5) 봉투 범위 고지: bin/ 수정을 패치 노트가 아니라 직접 적용했다 — 리드 세션 재개형 워커였기 때문. 검증만 위 절차로 완결할 것

### 2026-08-25T18:15:46+09:00 response (by nicehugepark)

봉투 권한 재실측(리드 세션 703a52d5): blocked 사유였던 'python3 테스트 실행 불가'는 오판 — 디렉토리 실행 형태(python3 tests/ s9_sync)는 allowlist에 허용된다. 실측 결과: Y1~Y6 전부 통과, 전체 스위트 247건 OK (instance init 테스트 2건은 자동 sync 커밋/무인 env 상속에 맞게 계약 갱신). 남은 인계는 git 쓰기 권한이 필요한 커밋·업스트림 푸시·인스턴스 배포·마커 소급뿐 — 이 봉투에선 git이 막혀 있어 리드 턴 몫.

### 2026-08-25T22:21:32+09:00 subagent (agent: Explore, by nicehugepark)

조사 결과를 보고한다. 파일은 `/home/sjpark1/section9/bin/s9` (현재 6240행, 다른 세션이 실시간으로 편집 중인 활성 저장소라 인용 행번호는 이번 조사 시점 기준). 읽기 전용으로만 조사했다.

### 0. 실제 재현 증거 (이 저장소 자체에서)

`/home/sjpark1/section9/state/sessions/DESKTOP-TEHV1KR__d332535d.json` (이 세션 자신의 바인딩)의 실제 값:

```json
"active_reqs": [
 "REQ-20260824-048",
 "REQ-20260825-080-62x6",
 "REQ-20260825-083-62x6",
 "REQ-20260825-081-62x6",
 "REQ-20260825-086-62x6",
 "REQ-20260825-082-62x6"
],
"last_req": "REQ-20260825-073-62x6"
```

`/home/sjpark1/section9/index/catalog.jsonl`의 실제 id는 `"REQ-20260825-080-62x6"`, `"REQ-20260825-083-62x6"` — 즉 **같은 바인딩 안에서도 섞여 있다**: 081/086/082는 uid 접미(`-62x6`) 포함, 080/083은 접미 없이 저장돼 있어 카탈로그와 매칭되지 않는다. `REQ-20260824-048`은 레거시(uid 스킴 도입 전) 문서라 짧은 형태가 곧 전체 id라 우연히 일치한다. 세 문서(048/080/083) 모두 `status=in-progress`, `session=d332535d`로 확인했다 — 즉 지금 이 순간 대시보드 카드 3장 중 080/083 두 장이 "직접 증거(direct)" 판정을 못 받고 있을 것으로 추정된다.

### 1. `resolve_id()` / `locate()` / `find_path()` / `normalize_ids()`

`bin/s9:387-395`
```python
def resolve_id(doc_id):
    """id → 후보 uid 목록 (REQ-20260825-031, git 짧은 해시 원리).
    정확 일치(레거시 포함)가 최우선 — 있으면 그 1건만. 아니면 짧은 지칭
    (지문 없는 date형)을 uid prefix로 해석해 후보 전부를 돌려준다."""
    rows = load_catalog()
    exact = [r for r in rows if r["id"] == doc_id]
    if exact:
        return exact[:1]
    return [r for r in rows if r["id"].startswith(doc_id + "-")]
```

- **반환 자료구조**: 문자열이 아니라 `load_catalog()`가 읽은 **카탈로그 행(dict) 리스트**(`index/catalog.jsonl`의 각 줄, `id`/`path`/`title`/`status`/`session`… 키 포함). 항상 리스트이며, 원소 개수는 0/1/N.
- **짧은 id 입력 시 동작**: 먼저 `doc_id`가 카탈로그 `id`와 정확히 일치하는 행이 있으면(레거시 uid-없는 문서는 짧은 형태 자체가 전체 id라 여기서 잡힘) 그 1건만 반환. 없으면 `doc_id + "-"` 접두 매치를 **전부** 반환 — `REQ-20260825-080-62x6`을 넣으면 `REQ-20260825-080-62x6`가 이 조건에 걸려 후보로 나온다(git 짧은 해시 방식). 후보가 2건 이상(동일 날짜·순번을 서로 다른 머신이 발번)이면 모호 상태로 전체 후보가 반환된다.
- **매칭 실패/모호 시**: `resolve_id` 자체는 판단하지 않고 리스트만 돌려준다 — 판단은 호출부(`locate`/`find_path`) 몫.

`bin/s9:398-410`
```python
def locate(doc_id):
    cands = resolve_id(doc_id)
    if len(cands) == 1:
        p = os.path.join(ROOT, cands[0]["path"])
        if os.path.exists(p):
            return p
    if cands:
        return None  # 모호(2건+) — 잘못 집지 않는다. find_path가 후보를 보여준다
    for p in walk_docs():  # catalog가 낡았을 때 fallback
        bn = os.path.basename(p)
        if bn == doc_id + ".md" or bn.startswith(doc_id + "-"):
            return p
    return None
```
후보가 정확히 1건일 때만 실제 파일 경로(str)를 반환. 0건이면 파일시스템 직접 스캔 폴백. 2건 이상이면 **조용히 `None`** — 잘못 집지 않는다는 방어이지, 사용자에게 즉시 에러를 주지 않는다(호출부에 따라 "문서 없음"으로 오인될 수 있음).

`bin/s9:413-422`
```python
def find_path(doc_id):
    cands = resolve_id(doc_id)
    if len(cands) > 1:
        die(f"모호한 id: {doc_id} — 후보 {len(cands)}건: "
            + ", ".join(f"{r['id']}({r.get('title', '')[:16]})" for r in cands)
            + " — 지문 포함 전체 id로 다시 지정하라")
    p = locate(doc_id)
    if not p:
        die(f"document not found: {doc_id}")
    return p
```
`find_path`만 모호 시 사용자에게 명시적으로 `die()`(프로세스 종료 + 메시지)한다. `locate`는 CLI가 아닌 내부 조회(대시보드 API, `do_transition`, `active_session_for` 등)에서 쓰이므로 모호할 때 그냥 실패를 삼킨다.

`bin/s9:345-384`
```python
def normalize_ids(text, known=None):
    """본문 속 short id(REQ-044, DOC-3 등)를 full id(REQ-20260823-044)로 확장.
    - 이미 full(PREFIX-YYYYMMDD-NNN)인 것은 안 건드린다.
    - 확장 후보가 정확히 1개일 때만 확장(0개·2개+ 애매하면 원본 유지).
    반환: (정규화된 text, [(원본, 확장)] 변경 목록)."""
    if not text:
        return text, []
    if known is None:
        known = _all_doc_ids()
    # 접미 3자리 → 후보 full id들 (같은 prefix). uid(지문 접미)도 수용 (REQ-031)
    by_suffix = {}
    for fid in known:
        m = re.match(r"^(REQ|DOC|SES)-\d{8}-(\d{3,})(?:-[0-9a-z]{4})?$", fid)
        if m:
            by_suffix.setdefault((m.group(1), m.group(2)[-3:].zfill(3)),
                                 []).append(fid)
    changes = []
    # short 패턴: PREFIX-<1~3자리>, 단 뒤에 -나 숫자가 이어지면(=full) 제외
    def repl(mo):
        prefix, num = mo.group(1), mo.group(2)
        suf = num.zfill(3)
        cands = by_suffix.get((prefix, suf), [])
        if len(cands) == 1:
            changes.append((mo.group(0), cands[0]))
            return cands[0]
        return mo.group(0)  # 0개·2개+ = 애매 → 유지
    out = re.sub(r"\b(REQ|DOC|SES)-(\d{1,3})(?![\d-])", repl, text)
    # 지문 없는 date형(레거시 표기·짧은 지칭)이 유일한 uid를 가리키면 전문으로
    def repl_date(mo):
        base = mo.group(0)
        if base in known:
            return base  # 레거시 정확 일치 — 그대로
        cands = [fid for fid in known if fid.startswith(base + "-")]
        if len(cands) == 1:
            changes.append((base, cands[0]))
            return cands[0]
        return base
    out = re.sub(r"\b(?:REQ|DOC|SES)-\d{8}-\d{3,}\b(?!-[0-9a-z])", repl_date,
                 out)
    return out, changes
```
**관계**: `normalize_ids`는 `resolve_id`와 별개의 (텍스트, 변경목록) 반환형이며, **자유 텍스트(History 노트 등)** 안에 섞인 짧은 REQ 언급을 확장하는 용도다. `resolve_id`/`locate`/`find_path`는 **구조적 id 인자**(CLI의 `id` 위치 인자)를 다루는 용도다. 이 둘은 서로 호출하지 않는다 — `normalize_ids`는 `_all_doc_ids()`(카탈로그 id 집합 또는 파일 스캔)를 쓰고, `resolve_id`는 `load_catalog()`를 직접 쓴다.

**핵심**: `normalize_ids`는 `do_transition()`에서 **`note` 파라미터에만** 적용된다(`bin/s9:937` `note, _ = normalize_ids(note)`). **`doc_id` 자체, 즉 `active_reqs`/`last_req`에 최종적으로 저장되는 문자열에는 어디서도 적용되지 않는다.** 짧은 id를 정규화하는 기계장치는 이미 존재하지만, 하필 이 값에는 배선이 안 돼 있다.

### 2. `active_reqs`를 쓰는 모든 지점

**(a) `update_active_reqs()` — `bin/s9:803-838`**
```python
def update_active_reqs(doc_id, new_status):
    """세션 바인딩의 active_reqs 유지 (REQ-20260823-080) — 대시보드 live 판정의
    직접 증거. in-progress 전이면 현 세션(S9_SESSION) 바인딩에 등록하고,
    다른 상태로 떠나면 모든 바인딩에서 제거한다. do_transition 락 밖에서 호출할 것.
    실패해도 전이를 막지 않는다 — 호출부에서 예외를 삼켜라."""
    import glob as _glob
    if new_status == "in-progress":
        sess = os.environ.get("S9_SESSION", "")
        if not sess:
            return  # 실행 세션을 모름(대시보드/외부) — 등록하지 않는다
        acquire_lock()
        try:
            machine = current_machine()
            b = read_binding(machine, sess) or {
                "machine": machine, "session": sess, "user": "", "history": []}
            ar = b.get("active_reqs", [])
            if doc_id not in ar:
                b["active_reqs"] = ar + [doc_id]
                write_binding(b)
        finally:
            release_lock()
        return
    acquire_lock()
    try:
        for bp in _glob.glob(os.path.join(STATE, "*.json")):
            try:
                with open(bp, encoding="utf-8") as f:
                    b = json.load(f)
            except (OSError, ValueError):
                continue
            ar = b.get("active_reqs", [])
            if doc_id in ar:
                b["active_reqs"] = [x for x in ar if x != doc_id]
                write_binding(b)
    finally:
        release_lock()
```
`doc_id`는 **인자로 받은 그대로** `ar + [doc_id]`로 저장된다 — `resolve_id`/`locate` 등으로 정규화하지 않는다. 제거 경로(`x != doc_id`)도 문자열 그대로 비교하므로, 등록 시 짧은 형태였다면 제거도 짧은 형태로만 매치된다(우연히 일관되긴 함).

호출부:
- `bin/s9:2321` (`cmd_status`, CLI `s9 status`) — `update_active_reqs(args.id, args.new_status)`. **`args.id`는 argparse 위치 인자 그대로, CLI 사용자가 타이핑한 문자열**(짧은 id일 수 있음). `do_transition(args.id, ...)`은 내부적으로 `locate(doc_id)`로 파일을 찾긴 하지만, 그 결과(전체 id)를 `args.id`에 되돌려쓰지 않는다. `cmd_status`는 `bin/s9:2312-2313`에서 `st = sub.add_parser("status", ...); st.add_argument("id")`로 정의되어 어떤 정규화도 argparse 단계에 없다.
- `bin/s9:5818` (dashboard `POST /api/status` 핸들러) — `update_active_reqs(tid, tto)`, 여기서 `tid, tto = str(req.get("id", "")), str(req.get("to", ""))`. 다만 대시보드 프런트(`web/index.html:1980,1992,4488`)는 카드의 `data-doc` 속성(=카탈로그 `r.id`, 항상 전체 uid — 주석 `web/index.html:1426` "참조(data-doc)는 항상 전문 uid")을 그대로 보내므로 **이 경로는 현재 안전**하다. 위험은 CLI 경로(짧은 id를 직접 타이핑하는 에이전트/사람)에 있다.

**(b) `cmd_last()`의 `--add` 분기 — `bin/s9:3409-3435`**
```python
    if getattr(args, "add", False):
        # --add: last_req는 두고 active_reqs에 병행 REQ를 실행 등록 (live 직접 증거,
        # REQ-20260823-082). 떠나는 상태 전이가 자동 제거한다.
        args.id or die("usage: s9 last <REQ-id> --add")
        acquire_lock()
        try:
            b = read_binding(machine, session) or {
                "machine": machine, "session": session, "user": "", "history": []}
            ar = [x for x in (b.get("active_reqs") or [])]
            changed = False
            if args.id not in ar:
                b["active_reqs"] = ar + [args.id]
                changed = True
            if not b.get("transcript_path"):  # 클레임 즉시 live 증거 확보
                tp = _discover_transcript(session)
                if tp:
                    b["transcript_path"] = tp
                    changed = True
            if changed:
                write_binding(b)
        finally:
            release_lock()
        # --add도 문서에 승계를 스탬프한다 — set 경로만 스탬프해서 --add로 잡은
        # REQ가 소유로 인정되지 않던 구멍 (REQ-20260825-066-62x6)
        stamp_doc_session(args.id, session)
        print(f"active_reqs += {args.id}")
        return
```
`args.id`가 검증·정규화 없이 그대로 `b["active_reqs"]`에 append된다. `stamp_doc_session(args.id, session)`(`bin/s9:978-995`)은 내부적으로 `locate(doc_id)`를 호출해 문서 프론트매터에는 정상적으로 전체 경로 문서를 찾아 스탬프하지만, **바인딩 JSON의 `active_reqs` 문자열 자체는 짧은 형태로 남는다** — 문서 쪽(정합)과 바인딩 쪽(비정합)이 갈라지는 지점.

**(c) `cmd_last()`의 `last_req` set/clear 분기 — `bin/s9:3436-3453`**
```python
    if args.id or args.clear or args.pause:
        acquire_lock()
        try:
            b = read_binding(machine, session) or {
                "machine": machine, "session": session, "user": "", "history": []}
            if args.pause and not args.id and not args.clear:
                b["capture_paused"] = True
            else:
                b["last_req"] = "" if args.clear else args.id
                b["capture_paused"] = False  # set/clear는 캡처 재개
                b.pop("capture_reject", None)  # 새 클레임 — 거부 기록 초기화
            if args.id and not b.get("transcript_path"):
                tp = _discover_transcript(session)  # 클레임 즉시 live 증거 (REQ-013)
                if tp:
                    b["transcript_path"] = tp
            write_binding(b)
        finally:
            release_lock()
```
`b["last_req"] = "" if args.clear else args.id` — 마찬가지로 원문 그대로 저장. `s9 last REQ-20260825-080-62x6`처럼 짧은 인자를 주면 `last_req`도 짧게 저장된다(실측 데이터의 `last_req: REQ-20260823-070`이 바로 이 경로의 산물로 추정).

이 외에 `active_reqs`/`last_req`를 직접 쓰는 지점은 없다 — `cmd_bind`(`bin/s9:3014-3031`)는 임의 키 write지만 실제로 `active_reqs`/`last_req` 키로 호출되는 사례는 코드 안에 없다(임의 확장 표면일 뿐).

### 3. `active_reqs` / `last_req`를 읽어서 문서 id와 매칭하는 모든 지점

**(a) `catalog_with_live()` — 대시보드가 직접 소비하는 함수, `bin/s9:5052-5120`**
```python
    def catalog_with_live():
        """catalog + 실제 LLM 동작 신호(live). 문서 상태 in-progress와 별개로,
        live(녹색 점멸)는 **세션의 명시 실행 등록**이 있을 때만 (REQ-20260823-080/082):
        어떤 세션이 last_req 또는 active_reqs(in-progress 전이 시 자동 등록)로
        붙잡고 있고 그 세션의 스트림/transcript가 2분 내 갱신.
        스트림에 id가 언급만 되거나(대화 ≠ 작업) 문서 파일이 갱신된 것(반려 전이 등)은
        직접 증거로 치지 않는다 — 반려 직후 가짜 점멸의 원인이었다 (REQ-20260823-082).
        세션만 활발하고 실행 등록이 없는 in-progress는 live_kind='session'(간접) —
        병행 작업의 존재는 알리되 실동작과 구분한다 (REQ-20260823-079).
        둘 다 아니면 idle(좀비 in-progress 의심)."""
        import glob as _glob, time as _time
        rows = load_catalog()
        LIVE_WIN = 120
        # 세션 바인딩에서 last_req/active_reqs/세션 → 최근성(초) 수집
        active = {}    # req_id -> 최근성(초) — 실행 등록(포인터/active_reqs) 경로
        sess_age = {}  # sid8   -> 최근성(초) — 문서 session 매칭 경로
        now = _time.time()
        for bp in _glob.glob(os.path.join(STATE, "*.json")):
            try:
                with open(bp, encoding="utf-8") as f:
                    b = json.load(f)
            except (OSError, ValueError):
                continue
            # 활동 경로: 부모 transcript + 스트림 미러 + 위임 에이전트 transcript
            # (병렬 위임 시 리드가 `s9 bind agent_transcript_path <path>` 로 등록)
            atp = b.get("agent_transcript_path", "")
            paths = [p for p in
                     ([b.get("transcript_path", "")] +
                      (atp if isinstance(atp, list) else [atp]))
                     if p and os.path.exists(p)]
            sid8 = b.get("session", "")
            if sid8:
                paths += _glob.glob(os.path.join(STREAMS, safe_name(sid8) + "*.jsonl"))
  

…(truncated)

### 2026-08-25T22:28:40+09:00 response (by nicehugepark)

진행 상황 정리(사용자 질의): 구현·검증은 끝났고 **남은 것은 git 쓰기 4단계뿐**이다.
- 완료: 이벤트 순차 commit→pull→push 구현(sync_enabled/.s9-sync 마커, sync_run 락 직렬화, 오프라인 백오프 60s), maybe_sync 호출 지점(do_transition·new·note·set·link·rm), cmd_sync(--status), instance init이 마커 생성·track. TDD Y1~Y6 전부 통과·전체 스위트 통과(리드 재실측 2026-08-25 18:15).
- 남음: (1) 커밋 (2) 업스트림 푸시 (3) 인스턴스 배포 (4) 인스턴스 루트에 .s9-sync 생성·커밋 후 실이벤트 1회로 origin 반영 실측.
- 지금 멈춰 있는 이유: 워킹트리에 위임 에이전트 2개의 미완 변경(bin/s9=086, web/index.html=081)이 섞여 있어 지금 커밋하면 반쪽 상태가 박힌다. 두 작업이 끝나 전체 스위트가 초록인 것을 확인한 직후 커밋·푸시한다.

### 2026-08-26T00:19:23+09:00 response (agent: sub:architect, by nicehugepark)

### 결론: 설계를 as-built 기준으로 갱신하고 방향 5개를 확정했다 (DOC-20260826-002가 DOC-20260824-002를 대체). **0단계 차단 수정 전에는 인스턴스에 sync를 켜지 마라.**

DOC-20260824-002는 구현과 어긋나 있었다. 6축 대조: 일치 2 · 대체 1(dual-repo → 업스트림/인스턴스, REQ-053) · **미구현 3(pull 주기, 실패 표면화, config 옵트인)**.

### A. push는 '이벤트마다 즉시'가 맞다 — 디바운스 설계는 폐기한다
문서 이벤트는 초당이 아니라 분당 단위다. 디바운스 30초는 협업 지연을 줄이지 못하면서 커밋 1개=변경 1개라는 감사 가치를 깎고, 배치 스레드를 serve 수명에 묶어 CLI만 쓰는 사용자를 놓친다. 현행 순차 방식을 확정한다.

### B. streams를 이벤트 동기화에서 뺀다 — 지금 최대 리스크
실측: 동기화 대상 106MB 중 **streams가 102MB(96%)**, 최대 파일 33MB. 미러는 append가 아니라 **매 Stop 훅마다 전체 파일 복사**(bin/s9-audit-response)라 문서 이벤트마다 33MB 새 blob이 히스토리에 쌓인다. 하루 50이벤트면 GB 단위다. → 이벤트 커밋은 vault·users·projects·state/sessions만. streams는 세션 종료 1회 또는 `s9 sync --streams` 배치로 따로 싣는다. 90일 아카이브 정책은 '후속'이 아니라 **streams 동기화의 선행 조건**이다. 대가(타 머신에서 진행 중 세션 transcript 실시간 열람 불가)는 받아들인다 — transcript는 사후 감사용이고 진행 상황은 문서와 state/sessions로 읽힌다.

### C. commit에 pathspec을 준다 — 결함 두 개가 한 수정으로 닫힌다 (격리 재현 완료)
현행 sync_run은 데이터 경로만 add한 뒤 **pathspec 없이 commit** 한다.
- **결함 A**: admin이 WIP 코드를 stage 해둔 채 문서를 하나 만들면 **미완성 코드가 origin에 푸시된다**(재현: bin/fake_code.py가 sync 커밋에 실려 나감).
- **결함 B**: non-admin이 보호경로를 stage 하면 pre-commit(s9-guard)이 커밋을 거부해 **sync가 전면 정지**한다 — `s9 new`는 rc=0으로 성공을 보고하고 문서는 로컬에도 커밋되지 않는다. **무증상 데이터 미동기화**다.
`git commit --only -- <데이터경로>` 로 바꾸면 커밋 범위가 데이터로 한정돼 A가 막히고, guard가 볼 파일에 보호경로가 없어 B도 사라진다. **--no-verify로 훅을 우회하지 않는다** — s9-guard는 인가 경계지 장애물이 아니다.

### D. pull은 세 지점에 둔다 — 쓰지 않는 사람도 최신을 본다
현재 pull은 sync_run 안에만 있다. 즉 **문서를 쓰지 않으면 로컬 vault가 영원히 낡는다** — 대시보드로 읽기만 하는 팀원은 남의 변경을 전혀 못 본다. 전수 조사 결과 SessionStart 훅·serve 스레드·digest·cron 어디에도 pull이 없다. DOC-002가 약속한 '최악 90초'는 **성립한 적이 없다**. 새 데몬·새 스레드 없이 세 지점: (1) SessionStart 훅 1회 (2) serve의 기존 30초 루프에 pull 틱(활동 중 60초/유휴 5분) (3) `s9 digest` TTL 120초. **목표 수치 고정: push=이벤트 즉시(수 초), pull ≤ 60초, 체감 최악 ~60초.** 초 단위 실시간(SSE 허브)은 하지 않는다 — 로컬 파일 원칙을 깨고 단일 장애점을 만든다. 필요해지면 pull 주기를 10초로 좁히는 것이 먼저다.

### E. 실패를 사람에게 보인다 — 자동 해결은 계속 금지
rebase 충돌은 지금 '60초 백오프 → 다음 이벤트 재시도 → 같은 실패'를 **아무 신호 없이** 무한 반복한다. sync.log를 읽는 코드는 `s9 sync --status` 하나뿐이다. → 연속 실패 카운터를 두고 `s9 digest` 첫 줄·대시보드 배지·SessionStart 컨텍스트에 ⚠ 한 줄(사유 + 다음 행동)을 올린다.

### 재판정 불필요로 확정한 것
- 리모트: 업스트림↔인스턴스(dual-repo 폐기) · 옵트인: `.s9-sync` 마커 유지 — '클론만 한 사용자 환경에서 git 쓰기 0'이라는 확정 취지는 마커로 충족된다(마커는 인스턴스에만 있다), 킬스위치 `S9_SYNC=off` 유지. 단 **docs/08의 '이 시스템은 스스로 commit/push 하지 않는다'는 문장은 인스턴스에서 거짓이 됐다 — 개정 필요**
- **id 충돌 안전망 불필요**: uid 지문 접미(REQ-20260825-031)로 다머신 동시 발번이 파일명 수준에서 겹치지 않는다 → 인계 항목 4(REQ-20260825-006 §3) close
- state 범위 현행 유지(state/sessions만 공유, 파일명에 머신 포함) · 첨부는 vault 하위라 동기화 대상 — 단 대시보드 ingest가 `s9 new` 뒤에 실행돼 그 사이클을 놓친다(순서 수정 필요)

### 마이그레이션 (현재 → 목표)
0단계(차단 수정, REQ-20260826-003으로 분리): commit pathspec + 첨부 ingest 순서 + 회귀 테스트 → 1단계 streams 분리·아카이브 → 2단계 pull 3지점 → 3단계 실패 표면화·docs/08 개정 → 4단계 **실환경 1회 실측**. 지금까지 sync는 **단위 테스트에서만** 돌았다 — 이 저장소는 마커도 sync.log도 없는 순수 업스트림이라, 인스턴스에서 실이벤트 1건이 origin에 반영되는 것을 봐야 '동작한다'고 말할 수 있다.

부수 발견: tests/test_s9_sync.py에 tearDownClass가 없어 실행마다 /tmp에 임시 디렉토리를 누출한다(REQ-20260826-003에 동봉).

### 2026-08-26T00:19:23+09:00 tdd (agent: sub:architect, by nicehugepark)

### TDD 시나리오 (개정 — 이 요청의 완료 기준은 '설계 승인'이다)
구현 시나리오 Y1~Y6은 리드 세션 703a52d5가 이미 실측 통과시켰다(2026-08-25T18:15 노트: Y1~Y6 전부 통과, 전체 스위트 247건 OK). 아래는 그 결과를 현행 세대로 옮겨 적은 것이다.
- [x] Y1. 생성 이벤트가 origin(bare)까지 push
- [x] Y2. 전이 이벤트 추가 커밋 push
- [x] Y3. 마커 없으면 git 동작 0 (REQ-20260825-051-62x6 제약)
- [x] Y4. 원격 불능: 작업 성공 + 로컬 커밋 보존 + 경고 로그 + 백오프
- [x] Y5. 협업: 타 클론 선push → pull --rebase 후 push
- [x] Y6. S9_SYNC=off 킬스위치
- [x] Y7. 설계 문서화: 주기·리모트 구성·충돌 전략·실시간성 한계가 지식 문서로 정리됨 (DOC-20260826-002가 DOC-20260824-002를 대체)

0단계 차단 수정(commit pathspec · 첨부 ingest 순서 · 테스트 임시 디렉토리 누출)의 시나리오는 이 요청이 아니라 **REQ-20260826-003-62x6(동기화 커밋 범위 결함)** 에 속한다 — 이 요청의 산출물은 설계 판정이다.

### 2026-08-26T14:51:06+09:00 response (by nicehugepark)

선행 조건 해소 — REQ-20260826-003-62x6(동기화 커밋 범위 결함)이 방금 승인·완료됐다. 이 문서의 확인 포인트 (1)에서 "0단계 차단 수정 전에는 인스턴스 sync 를 켜지 않는다"고 걸어둔 조건이 풀렸다는 뜻이다.

닫힌 결함 넷: 스테이징된 코드가 sync 커밋에 실려 나가던 것 / 보호경로가 stage 되면 문서 동기화가 무증상 정지하던 것(커밋 범위 + pull --autostash 양쪽) / 첨부가 동기화 사이클을 놓치던 것 / 테스트가 임시 디렉토리를 누출하던 것.

이제 남은 판단은 이 문서 본래의 것이다 — 인스턴스에 sync 를 켤지, 그리고 설계(DOC-20260826-002-62x6)의 나머지 항목(streams 제외·pull 3지점·실패 표면화·옵트인 마커)에 동의하는지.

## History
- 2026-08-24T20:23:17+09:00 created by nicehugepark (status: open)
- 2026-08-24T20:24:05+09:00 status: open -> in-progress (by nicehugepark) — 리드 직접 — 설계 검토 후 review로
- 2026-08-24T20:25:35+09:00 status: in-progress -> review (by nicehugepark) — 확인 포인트(방향 판정 2건): (1) 리모트 구성 — private 전환 후 단일 리포에 데이터 track(docs/08 원안, 단순) vs dual-repo(코드 공개 유지 대비, 격리) 중 택일. (2) 주기 승인 — push 이벤트+디바운스 30s / pull 60s(유휴 5분), 체감 지연 1분 미만·실시간 필요 시 가변 10s 강화. 설계 전문: DOC-20260824-002. 승인 시 s9 sync 1·2단계 구현 착수
- 2026-08-25T17:51:32+09:00 status: review -> in-progress (by nicehugepark) — 반려: 우선 개인이 혼자 작업하는 상황을 기본부터 시작하면, 요청이 생성될 때 그리고 상태가 전이될 때 마다 커밋과 pull, push가 순차적으로 되어야 할 것 같다. 그러면 자연스럽게 여러 사람들이 같이 사용해도 실시간 처럼 코드가 반영이 되지 않을까? private이냐 public냐는 중요하지 않은 것 같다. [via dashboard]
- 2026-08-25T17:59:31+09:00 status: in-progress -> blocked (by nicehugepark) — 검증 대기(리드): 구현 적용済(이벤트 순차 commit→pull→push, .s9-sync 마커 활성) — 이 봉투는 python3 테스트 권한이 없어 Y1~Y6 실행 불가. 해제 절차는 response 노트의 리드 인계 1~3(테스트→회귀→커밋·푸시→인스턴스 배포·마커 소급) — 다음 리드 턴이 자동으로 집는다
- 2026-08-25T22:00:47+09:00 status: blocked -> in-progress (by nicehugepark) — blocked 사유 소멸: 워커가 python3 테스트 권한 없음으로 오판해 걸어둔 것이고, 리드 재실측에서 Y1~Y6·전체 스위트 통과로 확인됨. 남은 일은 git 쓰기(커밋·푸시·인스턴스 배포·.s9-sync 소급)뿐 — 이번 리드 턴이 집는다
- 2026-08-26T00:19:30+09:00 status: in-progress -> review (by nicehugepark) — 확인 포인트: (1) 0단계 차단 수정(REQ-20260826-003-62x6) 전에는 인스턴스 sync를 켜지 않는다는 판단 (2) streams를 이벤트 동기화에서 빼는 것 — 대가는 타 머신 transcript 실시간 열람 불가 (3) pull 3지점과 목표 수치(체감 최악 60초) (4) 실패 표면화 위치(digest 첫 줄·대시보드 배지) (5) 옵트인은 마커 방식 유지 — 설계 전문 DOC-20260826-002-62x6
- 2026-08-26T23:28:34+09:00 status: review -> done (by nicehugepark) — 대시보드 승인 [via dashboard]
