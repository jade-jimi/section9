---
id: REQ-20260824-017
type: request
title: 계정·문서 접근 격리
summary: me 후보를 이 머신 등록 계정으로 한정 + 열람을 멤버 프로젝트 문서·본인 문서로 필터(admin 전체, 무소속=작성자만). 자기신고 한계 명시 — 실인증은 후속 단계
status: done
size: M
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: 104b4fe3
project: section9
children: ["REQ-20260824-022"]
tags: ["access", "project", "session"]
created: 2026-08-24T14:36:05+09:00
status_since: 2026-08-24T14:59:20+09:00
updated: 2026-08-24T14:59:20+09:00
---

## Original

최초 설치 시 등록했던 계정만 노출하고, 자기 계정에 포함된 프로젝트 그리고 프로젝트에 포함된 문서만 접근하게 할 수는 없나?

## Notes

### 2026-08-24T14:57:21+09:00 tdd (by nicehugepark)

TDD 결과: V1~V5·V7 전부 [x] (tests/test_access_isolation.py 6/6 OK), V6 [x] (기존 6종 스위트 각각 python3 실행 전부 OK), UI [x] (CDP: me 후보 remote 제외, alice→bob 전환 시 px 문서·프로젝트 사라짐 — 스크린샷 iso-me-alice.png / iso-me-bob.png). 미커버: /api/stream·/api/streams(Stream 탭 세션 원문)는 범위 밖으로 무필터 유지.

### 2026-08-24T14:57:39+09:00 backend (by nicehugepark)

구현 완료 (backend). 변경: [1] bin/s9 — doc_visible(row, me) 가드레일 헬퍼 신설(admin=전부, 프로젝트 문서(PRJ 포함)=활성 멤버만, 무소속·미등록 프로젝트=작성자만, 미등록 뷰어=비강제(부트스트랩, project_can의 비강제 원칙과 동일)); GET /api/catalog·graph(노드+엣지)·search·doc(404)·reqstream(404)·audit(SES 문서 가시성)·projects(비멤버 제외, admin 예외)에 ?me= 필터를 HTTP 응답 직전에만 적용 — catalog_with_live/audit_data 등 내부 반환은 무필터 원본 유지(워처·live 판정 불변); /api/users?scope=machine → registered_on==current_machine() 계정만 + 응답에 machine 포함. [2] web/index.html — me 셀렉터는 /api/users?scope=machine, 모든 조회 fetch에 meQ()(&me=) 부여, onMeChanged가 graph/audit 캐시 무효화 후 refreshProjects→refreshCatalog→render 재조회. Settings 사용자 관리는 전체 /api/users 유지. 한계(명시): me는 localStorage 자기신고 — 이 격리는 인증이 아니라 가드레일이며 curl로 me 변조 시 우회 가능(DOC-20260823-001 정책 연장). Stream 탭(/api/stream·streams)은 이번 범위 밖(무필터). 라이브 9909 재시작·검증 완료(root 200, scope=machine=[nicehugepark,user1,user2], admin 폴백 130행, me=user1은 0행). 기존 단독 사용자 nicehugepark=admin → 체감 변화 없음 확인.

### 2026-08-24T14:57:54+09:00 tdd (by nicehugepark)

### TDD 시나리오 (최종 상태)
- [x] V1. me 후보 머신 필터 (?scope=machine)
- [x] V2. 비멤버 격리 (catalog/graph/search/doc 404/reqstream 404/projects)
- [x] V3. 멤버 가시
- [x] V4. admin 전체
- [x] V5. 무소속 문서 = 작성자만
- [x] V6. 회귀 — 기존 6종 스위트 전부 OK
- [x] V7. audit 세션 이벤트 격리
- [x] UI — CDP 실브라우저 (me 후보 제한 + 전환 시 목록 변화, 스크린샷 2장)

### 2026-08-24T14:58:23+09:00 subagent (agent: backend-developer, by nicehugepark)

### 최종 보고 — REQ-20260824-017 대시보드 접근 격리

### 결론
요구 A(me 후보 머신 제한)·B(me 기준 열람 격리) 구현 완료. 신규 테스트 6/6, 기존 6종 스위트 전부 통과, CDP 실브라우저 검증 통과, 라이브 9909 재시작·확인 완료. 상태 전이는 하지 않았다(리드 검수 대기).

### 변경 요지
**`/home/sjpark1/section9/bin/s9`**
- `doc_visible(row, me)` 신설 (project_can 옆, ~line 1923): admin=전부 / 프로젝트 문서(PRJ 문서 자신 포함)=활성 멤버만 / project 비었거나 미등록 프로젝트=작성자만 / **미등록 뷰어=비강제** — `project_can`의 "정책 부재 → 강제 안 함" 원칙과 동일(부트스트랩·샌드박스에서 빈 화면 방지, 라이브에선 폴백이 nicehugepark=admin이라 무관).
- `viewer_of(qs)`: `?me=`(등록 사용자만 인정) > `resolve_user()` 폴백.
- 필터는 **HTTP 응답 직전에만** 적용: `/api/catalog`, `/api/graph`(노드 필터 후 엣지 재필터), `/api/search`, `/api/doc`(비가시=404, 존재 누설 없음), `/api/reqstream`(비가시=동일 404), `/api/audit`(SES 문서 가시성 기준), `/api/projects`(비멤버 제외, admin 예외). `catalog_with_live`/`audit_data` 등 내부 함수 반환은 무필터 원본 그대로 — 워처·auto_resume·live 판정·TDD 카운터 불변.
- `/api/users?scope=machine`: `registered_on == current_machine()` 계정만 + 응답에 `machine` 포함. 무지정은 전체(Settings 사용자 관리용).

**`/home/sjpark1/section9/web/index.html`**
- `meQ()` 헬퍼로 catalog/projects/search/doc/reqstream/graph(2곳)/audit fetch에 `&me=` 부여. me 셀렉터는 `?scope=machine`, Settings는 전체 목록 유지.
- `onMeChanged()`: graph/audit 캐시 무효화 → refreshProjects → refreshCatalog → render 전면 재조회.

### 시나리오 결과 (REQ 문서에 기록됨)
- V1~V5, V7: `python3 tests/test_access_isolation.py` → **6/6 OK** (Red 4건 확인 후 Green)
- V6 회귀: 기존 6종(`test_audit_prompt`, `test_auto_resume_gate`, `test_live_catalog`, `test_project_member_api`, `test_stdlib_only`, `test_tdd_progress`) 각각 실행 **전부 OK**
- UI(CDP, Windows Chrome headless + 샌드박스 9918): me 후보에 타머신 계정(remote) 미노출, alice→bob 실전환 시 px 문서·프로젝트 셀렉터에서 소멸, 전 탭(Docs/Graph/Audit/Settings/문서뷰) JS 예외 없음
- 라이브: 9909 재시작 후 root 200, `scope=machine`=[nicehugepark, user1, user2], me 미지정(admin 폴백) 130행 전체 유지, `me=user1` 0행 — 기존 단독 사용자 체감 변화 없음

### 스크린샷
- `/tmp/claude-1000/-home-sjpark1-section9/104b4fe3-7c7a-4a1e-9efe-72784dd8d94d/scratchpad/iso-me-alice.png`
- `/tmp/claude-1000/-home-sjpark1-section9/104b4fe3-7c7a-4a1e-9efe-72784dd8d94d/scratchpad/iso-me-bob.png`

### 한계 (명시)
- **me는 localStorage 자기신고** — 이 격리는 인증이 아니라 가드레일. `curl ?me=<타인>` 또는 me 미지정(서버 계정=admin 폴백)으로 우회 가능. 실인증은 후속 단계(DOC-20260823-001 정책 연장, 코드 주석·REQ note에 명시).
- Stream 탭(`/api/stream`, `/api/streams`)은 요구 범위 밖으로 무필터 유지 — 세션 원문이 노출되는 우회 열람 경로이므로 후속 REQ 후보.
- 샌드박스 serve(9918)·Chrome은 종료했고, 정리 파일: `tests/test_access_isolation.py`(신규), 검증 스크립트 `scratchpad/verify_isolation.py`.

## History
- 2026-08-24T14:36:05+09:00 created by nicehugepark (status: open)
- 2026-08-24T14:36:05+09:00 status: open -> in-progress (by nicehugepark) — backend-developer 위임 착수
- 2026-08-24T14:59:20+09:00 status: in-progress -> done (by nicehugepark) — 리드 검수: 테스트 6/6+회귀 6종, 실서버(user1 0행·404, machine 계정 3) 및 스크린샷 확인. me 자기신고 한계는 문서·주석 명시
