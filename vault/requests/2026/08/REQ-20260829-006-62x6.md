---
id: REQ-20260829-006-62x6
type: request
title: 게이트가 담긴 테스트를 하나만 돌린다
summary: s9-guard 는 이름을 여럿 넘기는데 러너는 첫 번째만 쓴다 — 나머지는 조용히 안 돌아간다
goal: 게이트가 넘긴 테스트가 하나도 빠짐없이 실제로 돌고, 안 돌면 커밋이 멈춘다
status: in-progress
size: S
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: a53ebee3
project: section9
parent: REQ-20260829-003-62x6
relates_why: {}
tags: ["test", "sync"]
created: 2026-08-29T01:44:57+09:00
status_since: 2026-08-29T01:45:12+09:00
updated: 2026-08-29T02:35:42+09:00
priority: 50
---

## Original

커밋 게이트(bin/s9-guard)의 staged_tests_gate 는 스테이지된 테스트 파일 이름을 전부 모아 `python3 tests/ a b c` 로 넘긴다. 그런데 tests/__main__.py 는 sys.argv[1] 하나만 패턴으로 쓴다. 두 번째부터는 아무 말 없이 선택되지 않고, 게이트는 "담긴 테스트가 통과했다"고 판정한다. REQ-20260829-003 의 /tmp 조사 중 발견 — 실제로 `python3 tests/ commit_gate streams_untracked` 가 commit_gate 만 돌고 있었다.

## Notes

### 2026-08-29T01:45:13+09:00 response (by nicehugepark)

### TDD 시나리오 — 러너가 패턴을 전부 받는다

조용한 성공을 없애는 것이 요점이다. '안 돌았다' 가 '통과했다' 로 보이면 게이트는 장식이다.
실행: python3 tests/ runner_patterns

- [x] R1. 패턴을 여럿 주면 전부 선택된다 — 지금은 sys.argv[1] 하나만 쓴다.
- [x] R2. 같은 파일이 두 패턴에 걸려도 한 번만 돈다(테스트 id 로 중복 제거).
- [x] R3. 아무것도 못 고른 패턴이 있으면 그 패턴 이름을 대고 **종료코드 1** 로 끝낸다 — 나머지가 다 통과해도 그렇다. 이게 이 건의 핵심이다.
- [x] R4. 인자가 없으면 전체(test_*.py)를 돈다.
- [x] R5. 인자 형태 셋을 그대로 받는다: 'x' · 'test_x' · 'tests/test_x.py'.
- [x] R6. s9-guard 가 넘기는 이름 목록이 러너에서 전부 선택된다 — 게이트가 담긴 테스트 전부를 실제로 돌린다(끝에서 끝까지).
- [x] 회귀: 기존 한 개 인자 호출과 인자 없는 호출이 그대로 동작(tmp_hygiene 의 러너 시험 포함).

### 2026-08-29T01:47:35+09:00 response (by nicehugepark)

구현 — 러너가 인자를 전부 패턴으로 받는다 (tests/__main__.py).

patterns(argv): 인자마다 'tests/' 접두·'.py' 접미·'test_' 접두를 벗겨 test_*<조각>*.py 로 만든다. 인자가 없으면 test_*.py 하나.
discover(pats): 패턴마다 discovery 를 돌려 합치되 테스트 id 로 중복을 제거한다(두 번 도는 것은 낭비이고, 상태를 쓰는 테스트는 두 번째가 첫 번째의 잔재를 본다). 반환에 '아무것도 못 고른 패턴' 목록을 함께 준다.
main(): 못 고른 패턴이 있으면 이름을 대고 종료코드 1 — 고른 것이 전부 통과해도 그렇다. 이게 이 건의 핵심이다.

실측 대조 — 게이트가 실제로 쓰는 호출 형태:
  전: python3 tests/ commit_gate streams_untracked → Ran 10 (commit_gate 만)
  후: python3 tests/ commit_gate streams_untracked → Ran 19 (둘 다)
  python3 tests/ uid 없는것zzz → 'no tests matched: test_*없는것zzz*.py' + rc=1
  python3 tests/ uid → rc=0

시험: tests/test_runner_patterns.py 10개 — 인자 세 형태, 여러 패턴 수집, 중복 제거, 못 고른 패턴 지목, 프로세스 경계에서의 종료코드, 그리고 s9-guard 가 만드는 이름 목록이 러너에서 전부 선택되는지(끝에서 끝까지).

### 2026-08-29T02:35:42+09:00 response (by nicehugepark)

검증: 전체 스위트 1737 tests OK (434초, rc=0) 에 포함돼 돌았다. 스위트 총계가 1723 → 1737 로 늘어난 것이 이 건의 시험 10개와 REQ-003 의 시험이 실제로 선택돼 돈다는 증거다.

부수 발견 하나 — 이 시험(test_runner_patterns)이 스위트 안에서 러너를 또 띄우자, 안쪽 실행이 시작할 때 돌리는 포트 회수가 바깥 스위트의 서버를 죽여 전체가 멈췄다. S9_TESTS_NESTED=1 로 안쪽 실행은 제 임시 루트만 챙기고 바깥 세계에 손대지 않게 했다(REQ-003 노트에 함께 기록).

## History
- 2026-08-29T01:44:57+09:00 created by nicehugepark (status: open)
- 2026-08-29T01:45:12+09:00 status: open -> in-progress (by nicehugepark) — 러너가 패턴을 전부 받게 하고, 못 고른 패턴은 실패로 끝낸다
