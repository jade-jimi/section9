---
id: REQ-20260824-040
type: request
title: Terminal 탭 완전 재현
summary: 마우스·키보드 동작 터미널 동일, 에이전트/워크플로 실행 표시·/ 명령 재현(팔레트+스킬 패스스루), 이미지 첨부, 입력 레이턴시 최소화(SSE 푸시), 단축키
goal: 키 입력이 즉각 반영되고(로컬 에코·비재빌드), 새 이벤트가 1초 미만으로 표시되며(SSE), / 팔레트·이미지 첨부·단축키가 동작하고 실브라우저 검증을 통과한다
status: done
size: L
user: sjpark1
machine: DESKTOP-TEHV1KR
session: 104b4fe3
agents: ["lead:claude-fable-5", "frontend-developer"]
project: section9
parent: REQ-20260824-037
children: ["REQ-20260824-041", "REQ-20260824-044"]
tags: ["auto-audit", "assets", "terminal", "dashboard"]
created: 2026-08-24T19:09:44+09:00
status_since: 2026-08-24T20:04:23+09:00
updated: 2026-08-24T20:04:54+09:00
---

## Original

[Image #7] 대시보드 터미널이 아까는 잘 됐던거같은데 지금은 이 상태다. 그리고 마우스와 키보드 기능을 터미널과 완전히 동일하게 해주고, 에이전트 실행과 워크플로 실행 시 그리고 / 명령어를 모두 재현시켜줘. 그리고 이미지 첨부하기도 기능도 터미널과 동일하게 해줘. 무엇보다 레이턴시가 튀지 않게, 빠르게 반영되게 해줘. 특히 키보드 입력 레이전시는 실제 터미널 기능과 비슷한 수준으로 맞춰줘. 단축키도 해줘.

## Notes

### 2026-08-24T19:13:28+09:00 tdd (by sjpark1)

### TDD 시나리오
- [ ] L0. 입력 보존(치명, 사용자 실측 결함): 라이브 갱신·폴링·상태 갱신 중 입력줄 DOM이 절대 재생성되지 않는다 — 타이핑 중 어떤 갱신이 와도 입력 내용·포커스·커서 유지
- [ ] L1. SSE 푸시: /api/stream/sse가 증분 이벤트를 250ms 감시로 내려보내고(EventSource), 새 이벤트 표시 지연 1초 미만. 폴링은 폴백
- [ ] L2. 업로드: POST /api/chat/upload(base64) → state/chat/uploads/ 저장·절대경로 반환, 10MB 초과/비base64 거부
- [ ] L3. 명령 목록: /api/chat/commands가 사용자 스킬(~/.claude/skills)·프로젝트 커맨드를 반환
- [ ] L4. / 팔레트: 입력 '/'로 팔레트(필터·↑↓·Enter/Tab 선택), 스킬은 메시지로 패스스루, CLI 전용 빌트인은 구분 표시
- [ ] L5. 이미지 첨부: 붙여넣기/드래그/버튼 → 업로드 → 메시지에 [Image: 경로] 삽입, 전송 줄에 첨부 표시
- [ ] L6. 웨이팅 상태줄: 진행 중이면 스피너+동사+경과초(예: ✻ Doing… (30s)) 애니메이션, 완료 시 소요시간 확정 표기
- [ ] L7. 단축키: ↑/↓ 입력 히스토리, Ctrl+L 화면 클리어, Ctrl+O 마지막 접힘 토글, Esc 입력 클리어 — 어디를 클릭해도 타이핑하면 입력줄 포커스
- [ ] L8. 에이전트/워크플로 표시: transcript의 서브에이전트 이벤트를 CC처럼(⚙/○ 트리·라벨) 렌더
- [ ] L9. 회귀: Stream/Board 무영향, JS 예외 0, 서버 테스트 전체 OK

### 2026-08-24T19:16:26+09:00 tdd (by sjpark1)

### 시나리오 추가 (사용자 실측 보고 2건)
- [ ] L10. 출력 내용 일치: 같은 시점의 CC 터미널 화면과 Terminal 탭이 같은 내용을 보여야 한다 — CC 표시 규칙을 따를 것: assistant 본문은 그대로, thinking은 기본 접힘('✻ ~' 한 줄), tool은 이름+핵심 인자 요약 한 줄+결과 접힘, 시스템 잡음(훅 주입·system-reminder·meta 이벤트)은 CC가 안 보여주듯 숨김, 이벤트 순서 동일. transcript에 없는 데이터(토큰 수 등)만 예외로 허용
(참고: L0 입력 소실 결함은 기존 항목에 이미 반영)

### 2026-08-24T19:16:55+09:00 response (agent: lead:claude-fable-5, by sjpark1)

중간 보고입니다.

**"no live session" 오검출은 수정 완료 — 지금 Terminal 탭을 새로고침하면 복구되어 있습니다** (REQ-041 done). 원인은 세션 시작 때 기록한 attach pid가 낡은 상태에서 5분 이상 유휴하면 활동 신선도까지 만료돼 대상에서 빠지는 것이었고, 이제 매 프롬프트마다 pid를 재바인딩해 자가 치유됩니다.

나머지 요구는 전부 **REQ-20260824-040(Terminal 탭 완전 재현)** 으로 진행 중입니다. 방금 주신 추가 보고 두 건도 반영했습니다:
- **타이핑 중 입력 소실(치명)** → 최우선 시나리오 L0: 입력줄 DOM을 절대 재생성하지 않는 구조로 재작업, CDP 자동화로 "타이핑→갱신 3주기→입력·포커스·커서 보존"을 증명해야 통과
- **터미널과 출력 내용 불일치** → L10: CC 표시 규칙(thinking 접힘, tool 요약+결과 접힘, 훅 주입·시스템 잡음 숨김, 순서 동일)로 내용 일치, 실스트림을 참조 스크린샷과 나란히 비교 보고

서버측은 구현·검증을 마쳤습니다(테스트 17종 전체 통과, 라이브 반영):
- **SSE 푸시**(`/api/stream/sse`, 250ms 감시) — 폴링 왕복 없이 1초 미만 반영, 레이턴시 요구의 핵심
- **첨부 업로드**(`/api/chat/upload`) — 붙여넣기/드래그한 이미지를 저장하고 CC 관례대로 `[Image: 경로]`로 세션에 전달
- **명령 목록**(`/api/chat/commands`) — `/` 팔레트용 스킬·커맨드 열거

UI 워커에는 웨이팅 스피너(✻ 애니메이션 + 동사 + 경과초 + 완료 시 총 소요시간), `/` 팔레트(CLI 전용 빌트인은 구분 표시), 이미지 첨부 칩, 단축키(↑/↓ 히스토리, Ctrl+L, Ctrl+O, Esc), 에이전트 트리 표시까지 포함해 위임했습니다. 완료 보고가 오면 L0·L10을 직접 재검증한 뒤 올리겠습니다.

### 2026-08-24T19:20:38+09:00 frontend-developer (by sjpark1)

frontend 착수: Terminal 탭 재작업 — L0(입력줄 DOM 1회 생성·비재빌드 아키텍처)부터. 검증은 목 서버+CDP 자동화(타이핑 중 3주기 갱신 후 입력/포커스/커서 보존 assert) + 실서버 9909 렌더 비교.

### 2026-08-24T19:54:18+09:00 tdd (by sjpark1)

### TDD 시나리오 (frontend 검증 개정 — CDP 자동화 52/52 + 서버 테스트 108 OK)
- [x] L0. 입력 보존: 목 CDP — 타이핑 중 갱신 3주기(SSE 증분+refreshCatalog(true)→render 강제+로그 폴) 후 입력값·포커스·커서·노드 동일성 보존, sid 교체(대격변)에도 입력줄 DOM 불변. 실서버 9909 동일 증명(전송 없음). 원인이던 15s catalog render의 터미널 재빌드는 render() 조기 반환 가드로 차단, 셸 DOM은 탭 진입 시 1회만 생성
- [x] L1. SSE 푸시: EventSource 연결(목·실서버), 증분 rAF 배칭 append, onerror 시 마지막 offset 재접속, 4연속 실패 시 2.5s 폴링 폴백(30s마다 SSE 복귀). 키 입력·로컬 에코는 네트워크와 분리. 서버 test_l1_sse_incremental_push OK
- [x] L2. 업로드: 서버 test_l2_upload OK + 클라이언트 목 검증(붙여넣기→POST /api/chat/upload→경로 수신)
- [x] L3. 명령 목록: 서버 test_l3_commands OK + 실서버 /api/chat/commands 응답 확인
- [x] L4. / 팔레트: 이름+설명 2열, 타이핑 필터(/td→/tdd 단일), ↑↓ 이동, Enter/Tab 완성('/tdd '), Esc 닫기, CLI 빌트인 dim '(CLI 전용)' 구분. fetch 실패는 비캐시(다음 열림 재시도)
- [x] L5. 이미지 첨부: 붙여넣기→업로드→칩([Image #1] 파일명 ×)→전송 메시지 끝 '[Image: 절대경로]' 삽입(서버 수신 확인), 전송 줄 📎N. 드롭·📎 버튼은 동일 핸들러 공유(드롭 원생 이벤트는 목 자동화 미실행)
- [x] L6. 웨이팅 상태줄: 마지막 user/tool 시 ✻✳✶ 점멸(CSS content 애니메이션, reduced-motion 존중)+Thinking…/Running…/Doing…+(Ns) 1s 갱신, 다음 이벤트 도착 시 그 자리에 dim '✻ ~ (총 Ns)' 확정
- [x] L7. 단축키: ↑/↓ 히스토리(빈 입력일 때만), Ctrl+L 로컬 클리어(이후 수신 계속), Ctrl+O 마지막 접힘 토글, Esc 첨부→입력 순 클리어, 타임라인 클릭 후 타이핑→입력줄 포커스+문자, 텍스트 선택 중 포커스 이동 안 함
- [x] L8. 에이전트: Agent tool_use → '● type(설명)' 초록+'⎿ Backgrounded agent' dim, 사이드체인 진행 ⚙agent dim 들여쓰기·result 접힘·thinking 숨김, 상태 스트립 '○ type desc running'(task-notification 시 해제 근사). 실스트림엔 사이드체인 이벤트 부재(백그라운드는 별도 세션)라 목 검증
- [x] L9. 회귀: Board 15카드·Stream 18행 정상, 탭 왕복 재진입 정상, JS 예외 0(목 2종+실서버), 서버 테스트 108 전체 OK
- [x] L10. 출력 일치: system-reminder·구조화 주입·사이드체인 thinking 숨김, thinking 기본 접힘 '✻ 요약…', tool 도구명+핵심 인자 1줄+긴 결과 접힘, 연속 Bash≥2 'Ran N shell commands', ✗ 분홍, task-notification dim 접힘, 순서=transcript. 허용 예외(transcript 부재): 토큰 카운트·실측 소요시간('Cogitated for Ns')·CLI 힌트 문구

### 2026-08-24T19:54:18+09:00 frontend-developer (by sjpark1)

Terminal 탭 재작업 완료 (web/index.html 단독 수정). L0 원인 = boot()의 15s refreshCatalog(true)→render()→renderTerminal() innerHTML 전체 재빌드로 textarea 파괴. 수정 = render() 조기 반환 가드 + 비재빌드 아키텍처(셸 1회 생성, 갱신은 타임라인 append/텍스트 노드 patch만, sid 교체도 타임라인만). SSE(EventSource)+rAF 배칭, 폴링 폴백·SSE 자동 복귀, 로컬 에코 즉시·전송 중 입력 비활성화 제거. / 팔레트·이미지 첨부·CC 스피너·단축키·에이전트 표시·L10 필터 구현. 검증: CDP 목 52/52 PASS, 실서버 9909 L0 증명+예외 0, 서버 테스트 108 OK. 스크린샷 scratchpad/t40-*.png. 미검증: OS 원생 클립보드/드래그(합성 이벤트 대체), reduced-motion 실기기, 실세션 전송 클릭(금지 준수)

### 2026-08-24T19:55:01+09:00 subagent (agent: frontend-developer, by sjpark1)

### 완료 보고 — REQ-20260824-040 Terminal 탭 재작업

**수정 파일: `/home/sjpark1/section9/web/index.html` 단독** (bin/s9 무변경, 서버 테스트 108개 전체 OK). 최종 상태: 실서버 9909 `#terminal` 정상 렌더(2,339줄, SSE 연결), JS 예외 0.

### L0 — 증명 방법과 결과 (치명 결함 해결)
**근본 원인 규명**: `boot()`의 15초 주기 `refreshCatalog(true)`가 catalog 변경(라이브 세션 중 상시 발생) 시 `render()` → `renderTerminal()` → `#view` innerHTML 전체 재빌드 → textarea 파괴. TDD Red 런에서 현행 코드로 재현했다(입력 노드 동일성 FAIL).

**수정**: ① `render()`에 조기 반환 가드(살아있는 터미널 셸은 절대 재빌드 금지), ② 비재빌드 아키텍처 재작성 — 셸 DOM(타임라인·팔레트·첨부·입력줄·상태 스트립)은 탭 진입 시 1회만 생성, 이후 모든 갱신은 타임라인 append(스피너 줄 앞 삽입)와 텍스트 노드 patch만. sid 교체(대격변)도 `termAttach()`가 타임라인만 교체.

**자동 증명(CDP)**: 타이핑 → 갱신 3주기 이상(SSE 증분 프레임 + `refreshCatalog(true)` 강제 render + 로그 폴) 통과 → **입력값·포커스·커서 위치·노드 동일성(dataset 마커) 전부 보존** assert PASS. sid 교체 목(8초 후 target 교체)에서도 타임라인은 교체되고 입력은 보존. **실서버 9909에서도 동일 증명 통과**(전송 없음).

### 검증 결과
- **목 서버 CDP 스위트: 52/52 PASS** (L0 8건, L1 SSE, L4 팔레트 6건, L5 첨부 5건, L6 스피너, L7 단축키 8건, L8 에이전트, L10 필터 6건, 400 에러 경로, JS 예외 0×2)
- 재현→해결: 입력 소실(L0), 훅 주입 노이즈 노출(L10), 사이드체인 원문 노출(L10)
- 실서버 화면을 CC 참조(6.png)와 비교 — 일치: `❯`/`●`/`⎿`/`✻` 글리프 체계, tool 이름+인자 1줄+접힘 결과, "Ran N shell commands", ✗ 분홍 에러, 에이전트 스폰 초록+"⎿ Backgrounded agent", 하단 상태 스트립. **차이(허용 예외 — transcript에 없는 데이터)**: 토큰 카운트(`↓ 3.3k tokens`), 실측 사고 시간(`Cogitated for 17s` — 대신 `✻ ~ (총 Ns)` 확정줄), CLI 힌트 문구(`↓ to manage`)

### 스크린샷 (scratchpad)
`/tmp/claude-1000/-home-sjpark1-section9/104b4fe3-7c7a-4a1e-9efe-72784dd8d94d/scratchpad/` — `t40-mock-main.png`(타임라인 전형), `t40-palette.png`(/ 팔레트), `t40-attach.png`(첨부 칩), `t40-err.png`(400), `t40-swap.png`(sid 교체), `t40-real-top.png`·`t40-real-bottom.png`(실서버). 검증 스크립트: `verify_t40.py`, 목: `term40-mock.py`.

### 미검증 항목 (정직 보고)
- OS 원생 클립보드 붙여넣기/파일 드래그(합성 이벤트로 핸들러 검증 — 드롭·📎 버튼은 동일 업로드 경로 공유)
- `prefers-reduced-motion` 실기기 동작(CSS 미디어쿼리 코드 확인만)
- 실세션으로의 실제 전송 클릭(지시대로 금지 준수 — 전송 경로는 목으로 성공/400/복원까지 검증)
- SSE 5분 자연 종료 후 재접속(코드 경로는 onerror 재접속으로 구현, 5분 대기 테스트는 미실행)

### 기록
REQ-20260824-040에 tdd 노트 개정(L0~L10 전 항목 `[x]`, 근거 명시) + `--label frontend-developer` 요약 note 완료. 상태 전이는 리드 판단대로 하지 않았다.

중간에 라이브 파일이 잠시 깨져 보인 점은 스플라이스 직후 즉시 복구·확인했고, 이후 소요 시간은 플레이크(이전 런의 CDP 페이지 잔존이 목 서버를 포화시킨 테스트 인프라 문제) 해소에 쓰였다 — 제품 코드의 실결함 1건(`/api/chat/commands` 실패 영구 캐시)도 이 과정에서 발견·수정했다.

### 2026-08-24T20:04:54+09:00 response (by sjpark1)

승인 메모 '대시보드 승인' 접수 — 후속 지시 없음, 종결. (037 동시 승인)

## History
- 2026-08-24T19:09:44+09:00 created by sjpark1 (status: open)
- 2026-08-24T19:11:08+09:00 status: open -> in-progress (by sjpark1) — 서버측(업로드·SSE·명령 목록·라이브 오검출 수정) 리드, UI는 frontend 위임
- 2026-08-24T20:02:06+09:00 status: in-progress -> review (by sjpark1) — 확인 포인트: 실사용 체감 — (1) 라이브 중 타이핑해도 입력이 절대 사라지지 않는지(L0), (2) 메시지 반영 속도(SSE 1초 미만), (3) 터미널과 내용 일치(훅 잡음 숨김·thinking 접힘), (4) / 팔레트·이미지 첨부·단축키(↑히스토리·Ctrl+L·Ctrl+O)·스피너 경과시간. 미재현(데이터 한계): 세션 자체 토큰 카운터·실측 사고시간
- 2026-08-24T20:04:23+09:00 status: review -> done (by sjpark1) — 대시보드 승인 [via dashboard]
