---
id: REQ-20260826-009-62x6
type: request
title: 의존 화살표 그래프 렌더
summary: blocked_by 축을 Graph 에서 방향 있는 엣지로, 카드·문서 뷰에는 "무엇을 기다리는가" 한 줄로 표시
status: done
size: S
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: cb49b2cd
sessions: ["cb49b2cd", "320b5f74"]
agents: ["sub:designer:a19164dd", "sub:subagent:ace08502", "lead:claude-opus-5", "sub:subagent:a48f2665", "sub:designer:a166a747", "sub:subagent:a777fb99", "sub:subagent:a98e5b5e", "sub:subagent:a20c1adb", "sub:subagent:a2182592", "sub:designer:ae99c70f"]
contributions: [{"actor": "sub:designer:a19164dd", "item": "의존 화살표 그래프 렌더", "started": "2026-08-26T12:47:44+09:00", "ended": "2026-08-26T13:09:48+09:00", "result": "stalled", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/cb49b2cd-601c-40bd-b9c4-d0afb0576d9f/tasks/a19164dd9d6fbc062.output", "reason": "271초째 진전 없음 (기준 180초)"}, {"actor": "sub:subagent:ace08502", "item": "Appending designer note to REQ-20260826-009-62x6", "started": "2026-08-26T13:05:06+09:00", "ended": "2026-08-26T13:05:06+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/cb49b2cd-601c-40bd-b9c4-d0afb0576d9f/subagents/agent-ace0850215e52af29.jsonl"}, {"actor": "lead:claude-opus-5", "item": "REQ-20260826-009-62x6 재작업에 착수했다가 **중복 스폰임을 발견해 편집을 양보했다.** 상태는 in-prog", "started": "2026-08-26T13:22:32+09:00", "ended": "2026-08-26T13:22:32+09:00", "result": "done", "transcript": ""}, {"actor": "sub:subagent:a48f2665", "item": "Appending designer note to REQ-20260826-009-62x6", "started": "2026-08-26T13:30:04+09:00", "ended": "2026-08-26T13:30:04+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/cb49b2cd-601c-40bd-b9c4-d0afb0576d9f/subagents/agent-a48f2665a86a503ce.jsonl"}, {"actor": "lead:claude-opus-5", "item": "전체 스위트가 아직 도는 중이다. 그동안 이번 턴 결과를 정리한다.", "started": "2026-08-26T13:30:45+09:00", "ended": "2026-08-26T13:30:45+09:00", "result": "done", "transcript": ""}, {"actor": "sub:designer:a166a747", "item": "원인**: 촉이 안 보인 게 아니라 그릴 선이 0개였다 — vault 전수에서 `blocked_by` 를 가진 문서는 REQ-", "started": "2026-08-26T13:31:11+09:00", "ended": "2026-08-26T13:31:11+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/cb49b2cd-601c-40bd-b9c4-d0afb0576d9f/subagents/agent-a166a747f8c202902.jsonl"}, {"actor": "lead:claude-opus-5", "item": "레일 이동 구현과 캡처 검증을 마쳤고, 전체 테스트 스위트 결과만 기다리는 중이다.", "started": "2026-08-26T13:56:04+09:00", "ended": "2026-08-26T13:56:04+09:00", "result": "done", "transcript": ""}, {"actor": "lead:claude-opus-5", "item": "전체 스위트 재실행을 기다리는 중이다. 첫 실행에서 나온 2건은 원인을 특정했다.", "started": "2026-08-26T13:57:36+09:00", "ended": "2026-08-26T13:57:36+09:00", "result": "done", "transcript": ""}, {"actor": "lead:claude-opus-5", "item": "전체 스위트를 다시 돌리는 중이다. 그동안 `test_shot_hygiene` 플레이크는 고쳤다 (`shot_hygiene` ", "started": "2026-08-26T14:00:24+09:00", "ended": "2026-08-26T14:00:24+09:00", "result": "done", "transcript": ""}, {"actor": "sub:subagent:a777fb99", "item": "Checking REQ-20260826-009-62x6 status metadata", "started": "2026-08-26T14:24:15+09:00", "ended": "2026-08-26T14:24:15+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/cb49b2cd-601c-40bd-b9c4-d0afb0576d9f/subagents/agent-a777fb99550659b71.jsonl"}, {"actor": "sub:subagent:a98e5b5e", "item": "Retrying review transition on REQ-20260826-009-62x6", "started": "2026-08-26T14:24:47+09:00", "ended": "2026-08-26T14:24:47+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/cb49b2cd-601c-40bd-b9c4-d0afb0576d9f/subagents/agent-a98e5b5e746e0a2e3.jsonl"}, {"actor": "sub:subagent:a20c1adb", "item": "Appending review checkpoints to REQ-20260826-009-62x6", "started": "2026-08-26T14:25:20+09:00", "ended": "2026-08-26T14:25:20+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/cb49b2cd-601c-40bd-b9c4-d0afb0576d9f/subagents/agent-a20c1adbaa1b5177e.jsonl"}, {"actor": "sub:subagent:a2182592", "item": "Linking DOC-20260826-010-62x6 to REQ-20260826-009-62x6", "started": "2026-08-26T14:25:52+09:00", "ended": "2026-08-26T14:25:52+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/cb49b2cd-601c-40bd-b9c4-d0afb0576d9f/subagents/agent-a2182592fa8b10c15.jsonl"}, {"actor": "sub:designer:ae99c70f", "item": "현황판을 우측 레일로 세우고, **비어 있을 때는 판(창)을 버리고 여백 주석이 되어 폭을 캔버스에 돌려주도록** 마저 고쳤다", "started": "2026-08-26T14:25:55+09:00", "ended": "2026-08-26T14:25:55+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/cb49b2cd-601c-40bd-b9c4-d0afb0576d9f/subagents/agent-ae99c70f6c7c0ac25.jsonl"}]
project: section9
derived_from: REQ-20260825-097-62x6
relates: ["REQ-20260826-013-62x6", "DOC-20260826-009-62x6", "REQ-20260826-015-62x6", "DOC-20260826-010-62x6"]
tags: ["dashboard", "design", "docs"]
created: 2026-08-26T12:47:15+09:00
status_since: 2026-08-26T22:13:59+09:00
updated: 2026-08-26T22:13:59+09:00
priority: 60
---

## Original

REQ-20260825-097 승인 시 남은 몫. 그 문서에서 데이터 축(blocked_by 스키마·CLI·자동 채집·linkcheck·그래프 데이터)은 구현·승인됐고, **화면 표시만 남았다** — architect 가 web/index.html 을 건드리지 않고 요구사항만 넘겼기 때문이다.

원래 요구(REQ-097 범위 3·4):
(3) Graph 에서 방향 있는 엣지(화살표)로 렌더 — 색면 금지 규칙을 지키며 잉크/점선/화살촉으로. 화살촉 방향은 선행 → 후행.
(4) 카드·문서 뷰에도 "무엇을 기다리는가" 한 줄.

사용자 원문 맥락: "요청들이 진행이 안되는 이유 중 몇개가 앞의 요청 때문에 진행이 안된다는게 그래프에서 확인이 되는지 보려고 했다."

수명 규칙(설계 DOC-20260826-001): 선행이 끝나면 엣지는 사라지고 이력만 History 에 남는다 — 화면도 그 규칙을 따라야 한다(끝난 의존을 계속 그리면 그래프가 과거로 채워진다).

## Notes

### 2026-08-26T12:53:28+09:00 response (by nicehugepark)

지목 중계(리드) — 사용자가 대시보드에서 이 에이전트를 지목해 보낸 질문을 SendMessage 로 전달했다: "어떤 요청을 진행 중인거지?" 함께 전달한 규율: 하던 작업을 마치고 반영할 것, 답은 채팅이 아니라 이 문서에 남길 것(맡은 REQ·현재 단계·남은 것).

### 2026-08-26T12:53:42+09:00 response (by nicehugepark)

진행 상황(designer): REQ-20260826-009-62x6 "의존 화살표 그래프 렌더"를 맡고 있다.

단계 = 구현 완료 → 캡처 검증 중.
- 완료: web/index.html 에 (1) Graph 의 blocked_by 방향 엣지(잉크 + 9/5 긴 대시 + 채운 화살촉, 화살촉은 막힌 문서 쪽 = 선행→후행), 굵기·농도는 그 선행이 붙잡은 건수에 비례, (2) 카드의 "선행 대기" 한 줄, (3) 문서 뷰 blocked_by 행(상태 바로 아래, 각 선행으로 doclink 이동), (4) 수명 규칙 — 양끝 중 하나라도 done/cancelled 면 엣지·줄을 그리지 않는다.
- 남은 것: 라이트/다크 톤 캡처로 화살촉 가시성 확인, 임시로 건 blocked_by 5건 원복(REQ-011/096/090 ← 001, 088 ← 096, 089 ← 095), review 상신.
- 검증용 임시 관계를 지금 vault 에 걸어둔 상태다. 캡처가 끝나면 전부 --unblocked-by 로 되돌린다.

### 2026-08-26T13:04:49+09:00 designer (by nicehugepark)

### 결과 (designer) — web/index.html 한 파일, 서버·bin/s9 무수정

세 화면에 "무엇을 기다리는가"를 같은 규칙으로 넣었다.

**1) Graph — 세 번째 획**
blocked_by 를 잉크(--text 토큰 파생) + 긴 대시 리듬 9/5 + 채운 화살촉으로 그린다.
기존 계보와 확실히 갈린다: parent=회색 실선, derived_from/relates/ref=회색 가는 점선(4/4),
선행 의존=잉크 굵은 대시+화살촉. 색면·후광 없음, 색은 획에만.
- 화살촉 방향 = 선행 → 후행. 촉은 **막힌 문서**의 원 경계 바로 밖에 놓아 노드를 덮지 않는다.
  촉 크기는 화면 px 기준(√k)이라 줌아웃해도 방향이 남는다.
- **병목은 굵기·농도로**: 그 선행이 지금 붙잡은 건수(1~4)에 비례해 선이 굵고 진해진다.
  노드 후광을 더하지 않았다 — 앰버 호(미완료 파생)와 신호가 겹치면 둘 다 안 읽힌다.
- hover 시 나머지 의존선은 물러나고(α .16) 짚은 것만 하이라이트 — 기존 이웃 강조와 동일 문법.
- 범례에 획 자체를 그려 넣었다(인라인 SVG 대시+삼각형, currentColor=잉크): "선행 → 후행 대기".

**2) 카드 — 한 줄**
`선행 대기 | <선행 제목>[ 외 N건]` 한 줄. blocked 전용이 아니다 — open/in-progress 카드도
안 끝난 선행을 가질 수 있고 사용자가 알고 싶었던 게 바로 그 경우다. 캡션만 blocked 잉크로
칠해 "이건 상태가 아니라 제약"임을 말한다(면·띠 없음). 한 줄로 잘라 보드 밀도를 지킨다.
선행이 잡히면 기존 자유문 "대기 사유"보다 이 줄이 이긴다 — 같은 사실을 두 줄로 말하지 않는다.
관계가 없는 과거 문서만 자유문으로 폴백(DOC-20260826-001-62x6 규칙 7).

**3) 문서 뷰 — 상태 바로 아래**
`blocked_by` 행을 status 바로 밑에 두고 `이 문서가 / 이 N건이 모두 끝나야 풀린다` 캡션 +
선행마다 [상태 점 · doclink · 제목 · 상태] 한 줄. doclink 라 그 문서로 바로 건너뛴다.

**4) 수명 규칙을 화면이 지킨다**
`liveBlockers()` 한 곳에서 판정하고 카드·문서·그래프가 같이 쓴다. **양끝 중 하나라도
done/cancelled 면 그리지 않는다.** 선행이 끝나면 서버(trigger_dependents)가 지우지만,
후행이 먼저 끝난 경우는 서버가 지우지 않는다 — 그 유령 화살표를 화면에서 막는 것이 이 필터다.

### 대안과 선택 근거
- (기각) 화살표에 마칭 대시 애니메이션 — 방향이 더 또렷하지만, 그래프에 이미 레이더 핑·
  유휴 드리프트가 돈다. 움직이는 신호가 셋이면 강조가 없는 것과 같다. 화살촉은 화면 px
  고정이라 정지 상태로도 읽힌다.
- (기각) 색상 구분(빨간 엣지) — 색면·장식색 금지 규칙 위반이고, tone 4종에서 대비가 무너진다.
  잉크는 어느 tone에서든 배경 반대쪽에 선다.
- (기각) 병목을 노드 후광으로 — 앰버 호(미완료 파생 병목)와 인지 충돌. 설계 문서도 엣지
  굵기/농도를 지정했다.
- (기각) 카드에 선행 전부 나열 — 컬럼 높이가 무너진다. 카드 1건+외 N건, 전부는 문서 뷰.

### 검증 (실브라우저 캡처, Windows Chrome headless)
격리 vault(S9_ROOT=…/scratchpad/depvault, 포트 9931)에 더미 9+26건으로 병목 1→3,
합류 2→1, 끝난 선행, 끝난 후행 케이스를 만들어 확인했다. 9909(사용자 보드)는 무수정.
- .../scratchpad/iso-graph2-light.png — 병목 1건이 3건에 굵은 화살표, 합류 2건은 가는 화살표
- .../scratchpad/iso-graph2-dark.png — carbon(#111214)에서 화살촉·대시 모두 또렷
- .../scratchpad/iso-board-dark.png — "선행 대기 / 인증 토큰 스키마", "로그인 화면 외 1건"
- .../scratchpad/iso-doc-dark.png — "이 2건이 모두 끝나야 풀린다" + 선행 2건 doclink
- .../scratchpad/iso-doc-done.png — done 문서: 파일엔 blocked_by 가 남아 있어도 행이 없다(수명 규칙)
- .../scratchpad/dep-graph-light.png / dep-graph-dark.png — glass·ledger 실데이터 화면
- .../scratchpad/final-board.png — 원복 후 사용자 보드(대기 줄 0건, 회귀 없음)
전체 테스트 444건 통과.

### 임시 관계 원복 확인
검증용으로 실제 vault 에 걸었던 6건을 전부 `--unblocked-by` 로 되돌렸다(REQ-011←001,
096←001, 090←001, 090←095, 088←096, 089←095). 확인:
`grep -l "^blocked_by: \[\"" vault/requests/2026/08/*.md` → **REQ-20260825-056-62x6 한 건만**.
이건 내가 만든 것이 아니라 작업 착수 전부터 있던 데이터다(첫 조사 grep에 이미 잡혔고,
내 첫 s9 link 실행보다 앞선다). final-board.png 에도 대기 줄이 하나도 없다.
잘못된 방식이었다 — 이후 검증은 격리 vault 에서만 했다.

### 리드에게 보고 (bin/s9 무수정 원칙에 따라 고치지 않음)
- **끝난 후행에 남는 유령 blocked_by**: REQ-20260825-056-62x6(done)이 REQ-20260826-001-62x6(open)을
  선행으로 들고 있다. trigger_dependents 는 "선행이 끝날 때" 도는 경로라 이 방향은 안 지운다.
  linkcheck --fix 도 "종료된 선행"만 청소한다 — 후행이 종료된 경우는 대상이 아니다.
  지금은 화면이 가려서 판단에는 영향이 없지만, 데이터에는 계속 남는다.
  link_audit 에 "후행이 done/cancelled 면 blocked_by 비움" 한 줄이 있으면 깨끗해진다.
- 서버 데이터는 부족한 것이 없었다 — catalog_row 의 blocked_by 로 카드·문서·그래프가 전부
  렌더된다. 추가 필드 요청 없음.

### 2026-08-26T13:05:06+09:00 subagent (agent: sub:subagent:ace08502, by nicehugepark)

Appending designer note to REQ-20260826-009-62x6

### 2026-08-26T13:21:02+09:00 response (by nicehugepark)

무인 재작업 세션(320b5f74) 보고 — 중복 스폰이라 편집을 양보했다.

**상황.** 반려(13:09:18) 직후 워처가 나를 스폰했는데(state/auto_resume/spawn.log 13:09:48 SPAWN fresh),
같은 REQ를 리드 세션 cb49b2cd 의 designer 서브에이전트(agentId a166a747f8c202902)가 이미
2차 재작업 중이었다. 내가 CSS를 한 덩어리 얹은 뒤에야 web/index.html 에 그쪽 미커밋 변경
150여 줄(.depsum 선행 대기 현황판·보드 병목 한 줄·범례 엣지 수)이 이미 있는 것을 발견했다.
같은 5천 줄 파일을 둘이 동시에 고치면 서로를 덮어쓴다 — **내가 넣은 블록을 즉시 원복하고
web/ 편집에서 손을 뗐다.** 클레임(active_reqs)은 유지한다: 지금 풀면 워처가 3번째 워커를
스폰한다. 이 REQ 는 designer 가 끝낸다.

**반려 사유(화살촉 안 보임)의 원인 — 코드를 읽어 특정했다 (인계용).**
현재 web/index.html 의 그리기 순서에 원인이 네 겹으로 쌓여 있다. 촉 크기만 키워선 안 낫는다.

1. **획이 촉을 관통한다.** 엣지 루프가 moveTo(p중심) → lineTo(q중심) 로 중심에서 중심까지
   긋고, 그 위에 삼각형을 얹는다. 획이 촉을 지나 노드 중심까지 계속되므로 실루엣이
   '선 + 살짝 두꺼워진 끝'으로만 읽힌다 — 촉의 밑변이 배경과 만나지 않으면 삼각형은
   삼각형으로 안 보인다. **획을 촉의 밑변에서 끊어야 한다** (그 지점까지만 lineTo).
2. **촉이 획과 같은 알파다.** fillStyle = col 인데 col 은 rgba(잉크, 0.60~0.92)이고,
   다른 노드에 hover 중이면 0.16 까지 내려간다. 5~14px 삼각형은 반투명이면 배경에 녹는다.
   촉은 **불투명**으로, 획보다 한 단계 진하게 칠해야 한다 — 방향이 이 엣지에서 가장 먼저
   읽혀야 할 정보다.
3. **분리선이 없다.** 노드 상태 링·교차하는 계보 선과 겹치면 윤곽이 사라진다.
   strokeStyle = bgColor / lineWidth 약 1.4 / lineJoin round 로 배경색 테두리를 두르면
   무엇과 겹쳐도 삼각형 윤곽이 남는다.
4. **밑변이 너무 좁다.** B = L * 0.44 는 폭이 길이의 0.88배인 가는 다트다. gap 클램프로
   L 이 5px 까지 떨어지면 폭 4.4px — 9/5 대시 한 칸(9px)과 구별되지 않는다.
   B 를 L 의 0.5~0.55 로 벌리고 L 하한을 대시 길이보다 확실히 크게(11~12px 이상) 잡아야
   '끝이 뭉친 선'이 아니라 '화살표'로 읽힌다.

**사용자 보드에는 애초에 화살표가 한 개도 안 그려진다.**
vault 전수: blocked_by 가 비어 있지 않은 문서는 REQ-20260825-056-62x6 한 건뿐이고
그건 done 이라 수명 규칙(DOC-20260826-001-62x6 규칙 4)으로 화면에서 지워진다.
즉 지금 9909 Graph 에는 의존 엣지가 0개다 — 사용자가 '촉이 안 보인다'고 한 화면이
이것일 가능성이 크다. 촉 기하를 고쳐도 **빈 상태가 스스로 말하지 않으면 같은 반려가 다시
난다.** designer 가 넣은 .depsum 빈 상태 문구가 그 자리를 맡는 것이 맞고, 범례의 엣지 수
표기(0)도 같은 몫이다. 이 축은 '없음'을 명시해야 하는 축이다.

**내가 실제로 고친 것 (tests/ — 충돌 없는 범위).**
tests/test_dep_edges.py 의 환경 격리. 무인 워커 세션에서 스위트를 돌리면 상속된
S9_AUTO_RESUME=1 이 cmd_status 의 trigger_dependents 호출을 통째로 건너뛰어
D5(선행 done → 후행 해제)·D6(부분 해제)가 코드와 무관하게 실패했다.
env.pop 한 줄 추가 — test_audit_prompt·test_instance_init 이 이미 쓰는 같은 패턴이다.
적용 후 dep_edges 8/8 통과(적용 전 6/8).

**제안 (bin/ 는 이 세션 범위 밖이라 손대지 않았다).**
- 워처 클레임 창이 서브에이전트 위임을 못 본다. 리드가 designer 에 위임한 30분 동안
  active_reqs 는 비어 있어 워처가 '미클레임'으로 판정하고 스폰했다. 위임 시점의
  contributions/agents 항목도 클레임으로 쳐야 중복 스폰이 막힌다.
- 끝난 후행에 남는 유령 blocked_by(REQ-20260825-056-62x6, 1차 노트에서 이미 보고)는
  여전히 linkcheck --fix 대상이 아니다.

### 2026-08-26T13:22:32+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

REQ-20260826-009-62x6 재작업에 착수했다가 **중복 스폰임을 발견해 편집을 양보했다.** 상태는 in-progress 그대로 두었다 — review로 올리지 않았다.

**무슨 일이 있었나**

워처가 반려(13:09:18) 직후 나를 스폰했는데(`spawn.log` 13:09:48), 같은 REQ를 리드 세션 cb49b2cd의 `designer` 서브에이전트(a166a747f8c202902)가 이미 2차 재작업 중이었다. 내가 CSS 한 덩어리를 얹은 뒤에야 `web/index.html`에 그쪽 미커밋 변경(`.depsum` 선행 대기 현황판, 보드 병목 한 줄, 범례 엣지 수)이 이미 들어 있는 걸 봤다. 그 에이전트는 지금도 살아 있다(transcript mtime이 계속 갱신, 격리 vault 만들어 캡처 준비 중). 같은 5천 줄 파일을 둘이 고치면 서로를 덮어쓰므로 **내 블록을 원복하고 web/에서 손을 뗐다.** 클레임(active_reqs)만 유지한다 — 풀면 워처가 세 번째 워커를 띄운다.

**반려 사유의 원인은 특정했다** (코드 읽기로, 인계 완료)

"화살촉이 안 보인다"는 촉 크기 문제가 아니라 네 겹이다:
1. 엣지 루프가 `moveTo(p중심)→lineTo(q중심)`로 **중심에서 중심까지** 긋고 그 위에 삼각형을 얹는다 — 획이 촉을 관통해 실루엣이 "선 + 두꺼워진 끝"으로만 읽힌다. 획을 촉 밑변에서 끊어야 한다.
2. `fillStyle = col`이라 촉이 획과 같은 알파(0.60~0.92, 다른 노드 hover 시 0.16)다. 촉은 불투명이어야 한다.
3. 배경색 테두리가 없어 노드 링·교차 계보선과 겹치면 윤곽이 사라진다.
4. `B = L*0.44` + gap 클램프로 L이 5px까지 떨어지면 폭 4.4px — 9/5 대시 한 칸과 구별되지 않는다.

여기에 더해 **사용자 보드에는 애초에 의존 엣지가 0개**다. vault 전수에서 `blocked_by`가 빈 배열이 아닌 문서는 `REQ-20260825-056-62x6` 한 건뿐이고 그건 done이라 수명 규칙으로 지워진다. 사용자가 본 화면이 이것일 가능성이 크고, 촉 기하만 고치면 같은 반려가 다시 난다 — 빈 상태가 스스로 말해야 한다.

**내가 실제로 적용한 것** (충돌 없는 범위)

`tests/test_dep_edges.py` 환경 격리 한 곳. 무인 세션이 상속하는 `S9_AUTO_RESUME=1`이 `cmd_status`의 `trigger_dependents` 호출을 통째로 건너뛰어 D5·D6이 코드와 무관하게 실패하고 있었다(`test_audit_prompt`·`test_instance_init`이 이미 막는 같은 누수). `env.pop` 추가 후 **dep_edges 8/8 통과** (적용 전 6/8). 전체 스위트는 designer가 동시에 돌리고 있어 포트 충돌로 오탐이 날 수 있어 돌리지 않았다 — 이 변경은 해당 테스트 자신의 env만 건드린다.

진단 전문은 REQ 노트(`response`)에 남겼고, `section9-11` 세션에도 SendMessage로 인계했다. 그쪽 designer가 이 REQ를 끝낼 것이다.

### 2026-08-26T13:29:53+09:00 designer (by nicehugepark)

### 반려 원인 규명 (designer 2차) — 촉이 안 보인 게 아니라 **선이 하나도 없었다**

**결론 먼저.** 사용자 화면(9909)의 Graph 에는 의존 화살표가 **0개**다. 촉 가시성 문제가
아니라 그릴 엣지가 없었다.

근거 (실데이터, 짐작 아님):
- vault 전수에서 `blocked_by` 가 빈 배열이 아닌 문서는 **REQ-20260825-056-62x6 한 건뿐**.
- `curl http://127.0.0.1:9909/api/graph` → `blocked_by` 엣지 1개.
  `REQ-20260825-056-62x6(후행, status=done) <- REQ-20260826-001-62x6(선행, status=open)`
- 후행이 done 이므로 수명 규칙(양끝 중 하나라도 done/cancelled 면 안 그린다)에 걸려
  화면에서 지워진다 → 캔버스에 남는 의존 획 0개.
- 1차 작업의 캡처는 격리 vault(더미 관계)라 촉이 보였다. 실데이터와 격리 데이터의 차이가
  그대로 반려 사유가 됐다. 실화면을 먼저 보지 않은 것이 1차의 절차 결함이다.

그런데 화면은 그 사실을 **말하지 않았다**. 범례는 "선행 → 후행 대기"라는 획을 계속
약속했고, 캔버스에는 그 획이 없었다. 사용자가 본 것은 "촉이 안 보인다"가 맞다 —
찾을 것이 있다고 화면이 말했는데 없었으니까.

### 고친 것 1 — 촉 기하 (리드 중계 지적 4건, 전부 수용)

실데이터가 0건이어도 촉 자체가 약했던 것은 사실이다. 코드 근거가 붙은 4가지를 다 고쳤다:
1. **획이 촉을 관통했다** — 엣지 루프가 중심→중심으로 긋고 그 위에 삼각형을 얹어
   실루엣이 "끝이 좀 두꺼운 선"으로 읽혔다. `depGeom()` 이 밑변 좌표(bx,by)를 돌려주고
   획은 **거기서 끊는다**.
2. **촉이 획과 같은 알파였다**(0.16~0.92) — 촉은 평시 **불투명**, hover 감쇠 중에도
   획보다 +0.30 진하게.
3. **분리선이 없었다** — 촉에 배경색 1.4px `lineJoin:round` 테두리. 노드 링·교차 계보선과
   겹쳐도 윤곽이 남는다.
4. **밑변이 좁았다**(L*0.44, 최소 5px = 9/5 대시 한 칸과 구별 불가) — 밑변 **L*0.52**,
   길이 하한 **11px**(두 원이 붙어 자리가 없을 때만 7px까지). 크기는 여전히 화면 px(√k).

### 고친 것 2 — 표·글 (사용자 제안, 합당하다고 판단해 구현)

**판단 근거: 그래프는 구조를 보여주고, 표는 현황을 답한다.** 사용자가 원래 알고 싶었던
것은 "지금 무엇 때문에 무엇이 막혀 있나"이고, 노드 322개 중 엣지 하나를 눈으로 찾는 것은
현실적이지 않다. 그래프를 표로 대체하지 않고 **같은 데이터를 두 형태로 나란히** 둔다.

**(1) Graph 캔버스 바로 아래 `선행 대기 현황` 표** — 왜 여기인가: 사용자가 이 질문을 들고
가는 곳이 Graph 탭이고, 그래프가 답하지 못하는 순간 바로 아래에 답이 있어야 한다.
Docs 는 검색하는 곳이라 이 질문의 자리가 아니다.
- 열: `막힌 요청 | 기다리는 선행 | 선행 상태 | 멈춘 지`(= 선행이 현재 상태로 머문 시간).
  블록이 언제 걸렸는지는 데이터에 없으므로 **없는 값을 지어내지 않고** 선행의
  status_since 를 쓰고 열 이름·title 로 그 뜻을 정확히 밝혔다.
- 정렬: ① 그 선행이 붙잡은 건수 ② 막힌 요청이 오래 멈춘 순 — 밀어야 할 것이 맨 위로.
- 한 요청이 선행 2건을 기다리면 두 줄로 펴고 둘째 줄 첫 칸은 `〃`(장부 관용). 같은 요청의
  줄은 반드시 붙어 있게 그룹 단위로 정렬한다.
- 병목 표기 `N건 붙잡음`은 그 선행이 **처음 나온 줄에만** — 반복하면 신호가 죽는다.
- 기본 8건 + `N개 더 보기`(밀도 규칙). 필터가 걸려 있으면 헤더에 `현재 필터 기준`.
- 캔버스 높이를 표 높이만큼 양보해 둘이 한 화면에 선다.

**(2) 빈 상태 — 지금이 그 상태다. 여기가 이번 반려의 진짜 수리 지점이다.**
`선행 대기 현황 · 없음` + "지금 다른 요청 때문에 멈춘 요청이 없다. … 선행을 걸면 여기에
줄이 쌓이고 위 그래프에 화살표가 그려진다." + `s9 link <막힌 요청> --blocked-by <선행>`.
그리고 각주: **"양끝 중 하나가 이미 끝난 의존 1건은 세지 않았다 — 끝난 의존은 그래프에도
이 표에도 남지 않고, 근거는 각 문서 History 에 남는다."** 화면이 스스로 "왜 아무것도
없는가"에 답한다.

**(3) 범례가 없는 것을 약속하지 않는다** — `선행 → 후행 대기 <N>` 으로 살아 있는 획 수를
적고, 0이면 범례 항목이 흐려진다(.gedge.none).

**(4) Board 상단 병목 한 줄** — 카드에는 이미 "선행 대기" 줄이 있다. 카드가 말할 수 없는
것, 즉 **한 선행이 2건 이상을 붙잡을 때만** 뜬다: `병목 REQ-… <제목> 이(가) 9건을 붙잡고
있다 · 전체 11건이 선행을 기다린다  선행 대기 현황 →`. 그 외에는 보드를 그대로 둔다
(같은 사실을 두 번 말하지 않는다).

### 대안과 기각 근거
- (기각) **표로 그래프를 대체** — 사용자 원문은 "그래프로 표현도 있지만"이다. 구조(사슬·
  합류)는 표가 못 보여준다. 대체가 아니라 병치.
- (기각) **선행 기준 그룹 표**("REQ-A 가 붙잡은 것들") — 병목은 잘 보이지만 "내 요청이 왜
  안 나가나"를 못 찾는다. 주 질문은 막힌 쪽이라 행을 막힌 요청 기준으로 두고, 병목은
  정렬과 한 줄 요약이 맡는다.
- (기각) **Docs 탭에 배치** — 검색의 자리다. 현황 질문은 Graph/Board 에서 난다.
- (기각) **막힌 건수를 stats 계기줄의 카운터로** — stats 는 상태축(open/done…)이고
  선행 대기는 상태가 아니라 제약이다. 축이 섞인다.
- (기각) **표 행에 색면·좌측 컬러 바** — 금지. 색은 상태 점(7px)·글자색·잉크에만.

### 검증 (실브라우저 캡처, Windows Chrome headless)
검증 데이터는 **격리 vault**(실vault 사본 322건 + 더미 요청 6건, `S9_ROOT=…/scratchpad/iv`,
포트 9931)에서만 만들었다. **실 vault 는 무수정** — blocked_by 를 한 건도 걸지 않았다.
`grep -rn '^blocked_by:' vault/ | grep -v '\[\]'` → 착수 전과 같은 REQ-20260825-056-62x6 한 건.
- .../scratchpad/v3-real-empty-glass.png — **9909 실화면(glass·라이트)**: 빈 상태 + 각주 +
  범례 `대기 0`. 사용자가 지금 보게 될 화면.
- .../scratchpad/v3-real-empty-dark.png — 같은 화면 carbon(다크).
- .../scratchpad/v3-graph-light.png — 322노드 실축척에서 촉: 획이 밑변에서 끊기고
  삼각형이 또렷하다. 병목 1건이 9건을 붙잡은 모양.
- .../scratchpad/v3-graph-dark-all.png — carbon + 표 전량 펼침(`?depall=1`) + `접기`.
- .../scratchpad/v3-graph-narrow.png — 900px 폭: 가로 스크롤 없음, 표는 자체 컨테이너.
- .../scratchpad/v2-board-light2.png — Board 병목 한 줄(한 줄에 정렬, 점프 링크).
- 대비: 표 캡션·각주를 --faint → --muted 로 올렸다(#9096a0 2.9:1 → #5c6470 5.9:1).
- 전체 테스트 444건 통과.
- 클릭 불가 환경용 진단 파라미터 `?depall=1` 추가(기존 `?mpanel`·`?nosse` 와 동형).

### 남은 사실 (리드 판단 요청, bin/s9 무수정)
- 유령 blocked_by 는 그대로다: REQ-20260825-056-62x6(done)이 REQ-20260826-001-62x6(open)을 선행으로
  들고 있다. `trigger_dependents` 는 "선행이 끝날 때" 도는 경로라 이 방향을 안 지우고,
  `linkcheck --fix` 도 "종료된 선행"만 청소한다. 화면은 가리고 각주로 세지만, **데이터는
  계속 남는다.** link_audit 에 "후행이 done/cancelled 면 blocked_by 비움" 한 줄이면 깨끗해진다.
- 검증용 격리 서버를 포트 9931 에 띄워 뒀다(실 vault 아님, 더미 관계 포함).
  촉을 직접 눈으로 보려면 http://127.0.0.1:9931/#graph — 확인 후
  `pkill -f "s9 serve --port 9931"` 로 정리하면 된다.

### 2026-08-26T13:30:04+09:00 subagent (agent: sub:subagent:a48f2665, by nicehugepark)

Appending designer note to REQ-20260826-009-62x6

### 2026-08-26T13:30:09+09:00 response (by nicehugepark)

무인 워커(320b5f74) 2차 보고 — 화살촉 회귀 계약을 tests/ 에 박았다.

**인계 결과.** 앞 노트에서 넘긴 촉 기하 원인 4겹을 designer 가 전부 반영했다(현재 워킹트리
depGeom/depArrow): 획을 촉 밑변에서 끊고(g.bx/g.by), 촉을 불투명으로 올리고(headCol 분리),
배경색 1.4px 테두리를 두르고, L 하한 11px·밑변 L*0.52 로 넓혔다. web/ 은 그대로 designer
몫이라 이번에도 손대지 않았다.

**새로 추가한 것: tests/test_dep_arrow.py (10건).**
캔버스는 단위 테스트로 픽셀을 볼 수 없다. 그래서 '무엇이 보이는가'가 아니라 '보이지 않게
만들던 그리기 방식으로 되돌아갔는가' 를 계약으로 박았다 — test_priority_visible 과 같은
정적 계약 계보다. 이 반려는 두 번 났고(촉 없음 → 촉 안 보임), 원인이 전부 캔버스 그리기
순서라 코드 리뷰로는 다시 놓치기 쉽다.
- a2/a3 의존 획이 노드 중심에서 중심으로 그어지지 않는다 (끝점은 촉 밑변)
- b2 촉에 배경색 분리 테두리가 있다 / b3 촉 색이 획 색 변수와 다르다(반투명 금지)
- c2 촉 길이 하한 11px 이상 (의존 획 대시 한 칸 9px 보다 크다) / c3 밑변 반폭 L*0.5 이상
- d1 수명 규칙: 엣지 필터가 양끝을 둘 다 검사한다(서버는 후행이 먼저 끝난 경우를 안 지운다)

**이 계약이 실제로 반려를 잡는지 검증했다.** 임시 프로브로 반려 당시 커밋(6d0cd72)의
web/index.html 을 꺼내 같은 술어를 돌렸다. 여섯 계약이 전부 FAIL 했다 —
a2 중심에서 중심 획 검출, a3 끝점 sxOf(q)/syOf(q), b2 테두리 없음, b3 촉색과 획색이 같음,
c2 하한 7px(대시 9px 보다 작다), c3 밑변 0.44. 지금 코드에서는 10/10 통과.
프로브는 실행 후 삭제했다(tests/ 에 남기지 않음).

설계 주의 하나 남긴다: 처음엔 blocked_by 분기 본문만 잘라 검사했는데
**반려 당시 코드는 경로를 분기 바깥 루프 머리에서 미리 만들고 분기 안에서 stroke 만 했다.**
그대로 뒀으면 계약이 통과해 버리는 헛도는 테스트였다. 지금은 엣지 루프 머리부터
continue 까지를 본다 — 나중에 리팩터링할 때 이 구간 설정을 좁히지 마라.

**검증.** dep_arrow 10/10, dep_edges 8/8. 전체 스위트는 designer 의 격리 vault 캡처와
포트가 겹칠 수 있어 결과에 잡음이 낄 수 있다 — 결과는 별도로 확인해 보고한다.

전이는 하지 않는다. 이 REQ 의 화면 검증(캡처)과 review 상신은 designer/리드 몫이다.

### 2026-08-26T13:30:45+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

전체 스위트가 아직 도는 중이다. 그동안 이번 턴 결과를 정리한다.

**재스폰됐지만 상황은 그대로다** — 리드 세션의 `designer` 서브에이전트가 아직 살아서(transcript 13:24 갱신) 같은 REQ를 마무리 중이다. `web/`은 이번에도 건드리지 않았다.

**인계가 먹혔다.** 지난 턴에 넘긴 촉 기하 원인 4겹을 designer가 전부 반영했다 — 획을 촉 밑변에서 끊고(`g.bx/g.by`), 촉을 불투명으로 분리하고(`headCol`), 배경색 1.4px 테두리를 두르고, L 하한 11px·밑변 `L*0.52`로 넓혔다. 코드 주석에 ①②③④로 그대로 적혀 있다. 지난 턴의 `tests/test_dep_edges.py` 환경 격리 수정도 리드가 커밋했다(`52a7bd4`, 세션 320b5f74 크레딧).

**이번 턴에 한 일: `tests/test_dep_arrow.py` 추가 (10건 통과).**

이 반려는 두 번 났고(촉 없음 → 촉 안 보임) 원인이 전부 캔버스 그리기 순서라 코드 리뷰로 다시 놓치기 쉽다. 캔버스는 단위 테스트로 픽셀을 못 보니 "무엇이 보이는가"가 아니라 **"보이지 않게 만들던 그리기 방식으로 되돌아갔는가"**를 정적 계약으로 박았다(`test_priority_visible`과 같은 계보).

**중요한 건 이게 헛도는 테스트가 아님을 증명한 부분이다.** 처음 쓴 버전은 `blocked_by` 분기 본문만 검사했는데, 반려 당시 코드는 경로를 **분기 바깥 루프 머리**에서 미리 만들고 분기 안에서 `stroke`만 했다 — 그대로 뒀으면 반려 코드가 그냥 통과하는 장식용 테스트였다. 검사 구간을 엣지 루프 머리까지 넓힌 뒤, 임시 프로브로 반려 당시 커밋(`6d0cd72`)의 `web/index.html`을 꺼내 같은 술어를 돌려 확인했다:

```
a2 중심→중심 획 검출: FAIL(=잡음)      b3 촉색=획색:   FAIL(=잡음)
a3 끝점이 노드중심:   FAIL(=잡음)      c2 길이 하한 7px: FAIL(=잡음)
b2 배경 테두리 없음:  FAIL(=잡음)      c3 밑변 0.44:    FAIL(=잡음)
```

여섯 계약 전부 반려 코드에서 실패하고 현재 코드에서 통과한다. 프로브는 실행 후 삭제했다.

`dep_arrow` 10/10, `dep_edges` 8/8. **전이는 하지 않았다** — 화면 캡처 검증과 review 상신은 designer/리드 몫이다. 상세는 REQ 노트에 남기고 `section9-11`에도 알렸다.

### 2026-08-26T13:31:11+09:00 subagent (agent: sub:designer:a166a747, by nicehugepark)

**원인**: 촉이 안 보인 게 아니라 그릴 선이 0개였다 — vault 전수에서 `blocked_by` 를 가진 문서는 REQ-20260825-056-62x6 한 건뿐이고 후행인 그 문서가 done 이라 수명 규칙이 유일한 엣지를 지운다(`/api/graph` 응답으로 확인). 리드가 중계한 촉 기하 4결함(획 관통·동일 알파·분리선 없음·좁은 밑변)은 별개로 전부 사실이라 함께 고쳤다.

**더한 것 1**: Graph 캔버스 바로 아래 `선행 대기 현황` 표(막힌 요청 | 기다리는 선행 | 선행 상태 | 멈춘 지, 병목 순 정렬, 8건+더 보기)와 Board 상단 병목 한 줄(한 선행이 2건 이상 붙잡을 때만) — 그래프는 구조를, 표는 현황을 답하게 병치했다.

**더한 것 2**: 빈 상태가 자기 이유를 말하게 했다 — "없음" + `양끝 중 하나가 이미 끝난 의존 1건은 세지 않았다` 각주 + 범례에 살아 있는 획 수 `대기 0`(0이면 흐림). 지금 사용자 화면이 바로 이 상태다.

주요 경로: `/home/sjpark1/section9/web/index.html` (유일 변경) · 캡처 `/tmp/claude-1000/-home-sjpark1-section9/cb49b2cd-601c-40bd-b9c4-d0afb0576d9f/scratchpad/v3-real-empty-glass.png`, `.../v3-real-empty-dark.png`, `.../v3-graph-light.png`, `.../v3-graph-dark-all.png`, `.../v3-graph-narrow.png`, `.../v2-board-light2.png` · 지식 `DOC-20260826-009-62x6` · REQ-20260826-009-62x6 review 상신, 테스트 444건 통과, 실 vault 무수정(검증은 격리 vault 포트 9931).

### 2026-08-26T13:31:31+09:00 response (by nicehugepark)

리드 판단 — 커밋 시점에 대해 (무인 워커 320b5f74 가 물어온 건). 워커에게 직접 회신할 경로가 없어 여기 남긴다.

`tests/test_dep_arrow.py` 확인했다: 10/10 통과, 반려 당시 커밋(6d0cd72)의 web/index.html 로 역검증해 여섯 계약이 전부 FAIL 하는 것까지 확인된 상태다 — 헛도는 테스트가 아니다. 특히 "처음엔 blocked_by 분기 본문만 검사해서 반려 당시 코드가 통과해 버렸을 것"을 스스로 잡고 검사 구간을 엣지 루프 머리까지 넓힌 것, 정적 계약이 픽셀 가시성을 대신하지 못한다고 파일 머리에 명시한 것 — 둘 다 유지한다.

**커밋은 지금 하지 않는다.** 이 테스트가 검사하는 구현(web/index.html)이 designer 작업 중이라 아직 미커밋이다. 테스트만 먼저 커밋하면 그 시점 트리가 스스로 성립하지 않는다. 오늘 정확히 그 실수를 한 번 했다 — 커밋 6d0cd72 에 test_shot_hygiene 만 들어가고 구현이 빠져 779c312 로 보충해야 했다. 반복하지 않는다.

designer 완료 후 **web/index.html + test_dep_arrow.py 를 한 커밋으로** 리드가 묶는다. 전체 스위트도 그때 리드가 한 번만 돌린다(지금 격리 vault 캡처와 포트가 겹치면 잡음이 낀다).

워커가 이미 적용한 tests/test_dep_edges.py 환경 누수 수정은 충돌 범위 밖이라 먼저 커밋했다(52a7bd4).

### 2026-08-26T13:56:04+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

레일 이동 구현과 캡처 검증을 마쳤고, 전체 테스트 스위트 결과만 기다리는 중이다.

**한 일 — 현황판을 하단에서 오른쪽 레일로**

`.gsplit` 그리드(`1fr` + `clamp(292px, 25vw, 348px)`)로 캔버스와 현황판을 나란히 세웠다. 캔버스 높이에서 빼던 `depH` 보정을 없애 **그래프가 세로를 온전히 돌려받았다**.

단순 이동이 아니라 다시 짜야 했다 — 기존 4열 표(`min-width:620px`)를 300px대 레일에 넣으면 가로 스크롤이 생기고, 가로 스크롤은 안 보이는 열이라 없는 것과 같다. 그래서 표를 세로 목록으로 바꿨다: 막힌 요청이 위, 기다리는 선행이 `↳`로 들여쓰기, 상태·경과는 그 아래 한 줄. 사라진 열 이름은 `막힌 요청 ↳ 기다리는 선행` 한 줄이 대신한다. 1180px 아래로는 레일이 아래로 접히되 `max-height:44vh`로 캔버스 높이를 지킨다.

레일 폭에서 드러난 것 셋도 같이 잡았다:
- `<code>` 예시가 인라인이라 앞 문장 끝 글자와 뒤엉켜 끊겼다 → 제 줄로
- 한국어가 아무 데서나 끊겼다("그려진 / 다.") → `word-break:keep-all`
- 병목 문장 전체가 경고색이라 레일에서 제목보다 크게 외쳤다 → 잉크는 겨눈 한 단어(`4건`)에만

**검증 (실브라우저 캡처)**

실 vault에 살아 있는 `blocked_by`가 없어 채워진 레일을 볼 수 없었다. `web/railprobe.html`(index.html 복사본)에 `fetch` 응답만 가로채 가짜 의존을 얹어 확인하고 파일은 삭제했다 — **서버·vault는 건드리지 않았다.**

- `scratchpad/rail-full-light.png` / `rail-full-dark.png` — 채워진 레일 + 병목 1건이 3건을 붙잡은 모양. 화살촉이 또렷하게 보인다
- `scratchpad/rail-real-light.png` / `rail-real-dark.png` — 사용자 실보드(빈 상태), 캔버스 전체 높이
- `scratchpad/rail-narrow.png` — 1080px에서 아래로 접힘
- `scratchpad/rail-glass.png` — glass 스킨(레일이 기존 스킨 규칙을 그대로 물려받는다)

`dep_arrow` 10/10, `assets` 28/28 통과. 전체 스위트가 끝나면 결과와 함께 review로 올린다.

### 2026-08-26T13:57:36+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

전체 스위트 재실행을 기다리는 중이다. 첫 실행에서 나온 2건은 원인을 특정했다.

`test_shot_hygiene` 실패는 내 변경과 무관한 **잠재 플레이크**다. 그 테스트는 `tempfile.mkdtemp(prefix="s9shot-")`로 `/tmp/s9shot-XXXX`를 만들어 `S9_ROOT`로 쓰는데, `bin/s9-doctor`의 `sweep_stale_shots`가 `TMPDIR`에서 **`s9shot-`로 시작하는 디렉터리를 rmtree**한다(`SHOT_MARKERS`). 소유자 판별 정규식이 `s9shot-(\d+)`라 랜덤 접미사가 숫자로 시작하면(이번엔 `9j2anjm6` → pid 9) 죽은 pid로 읽혀 즉시 삭제 대상이 된다. 캡처가 돌 때마다 sweep이 선행되므로 테스트의 임시 루트가 실행 중에 사라진다.

`test_shot_hygiene.py`는 `web/`을 전혀 읽지 않으므로 이번 레이아웃 변경이 원인일 수 없다. 재실행 결과와 나머지 error 2건을 확인한 뒤 접두사를 sweep 대상이 아닌 것으로 바꾸고(테스트 범위), `bin/` 쪽 날카로운 모서리는 제안으로 남기겠다.

### 2026-08-26T14:00:24+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

전체 스위트를 다시 돌리는 중이다. 그동안 `test_shot_hygiene` 플레이크는 고쳤다 (`shot_hygiene` 단독 실행 통과).

원인은 그 테스트가 자기 임시 루트를 `s9shot-` 접두사로 만든 것이었다 — 하필 그게 `s9-doctor`의 `sweep_stale_shots`가 `TMPDIR`에서 rmtree하는 표식이고, 소유자 판정 정규식 `s9shot-(\d+)`가 랜덤 접미사의 앞 숫자를 pid로 읽어(`s9shot-9j2anjm6` → pid 9) 죽은 소유자로 판정한다. 캡처는 매번 sweep을 선행하므로, 스위트 중 캡처가 한 번이라도 돌면 테스트의 `S9_ROOT`가 통째로 사라진다. 접미사 뽑기에 따라 되기도 안 되기도 하던 플레이크다. 접두사를 `s9-shothyg-`로 바꿔 회수 대상에서 뺐다.

남은 error 2건은 이번 실행 결과에서 확인하겠다.

### 2026-08-26T14:11:06+09:00 response (by nicehugepark)

리드 재착수 — 13:47 반려("선행 대기 현황 창이 하단이라 그래프 화면이 너무 좁다, 우측으로 옮겨달라")를 이제야 받았다. 그 통지가 리드가 아니라 이미 물러난 무인 워커 세션(320b5f74) 수신함으로 갔기 때문이다(근거·재발 방지는 REQ-20260826-015-62x6). 38분 늦었다.

이 세션이 클레임을 잡았으니(active_reqs) 다음 전이부터는 여기로 온다. designer 에 레이아웃 재구성을 넘긴다.

### 2026-08-26T14:12:21+09:00 response (by nicehugepark)

현황판을 하단에서 오른쪽 레일로 (3차 반려 반영, 무인 워커 320b5f74).

반려: '그래프 탭에서 선행 대기 현황 없음 창이 하단에 있으니까 그래프 화면이 너무 좁다.
우측으로 옮기는 디자인을 다시 구성해줘.'

**왜 좁았나.** 직전 구현은 캔버스 높이를 현황판 높이만큼 깎았다(H = innerHeight - 210 - depH).
세로는 화면에서 가장 귀한 축인데 그래프도 표도 세로를 쓴다 — 아래에 세우면 둘이 같은 축을
두고 다툰다. 옆에 세우면 캔버스는 높이를 온전히 돌려받고 현황판은 남아 도는 가로를 쓴다.

**옮기기만 해선 안 됐다.** 기존 표는 4열에 min-width 620px 이라 300px대 레일에 넣으면
가로 스크롤이 생긴다. 가로 스크롤 안의 열은 안 보이는 열이고, 안 보이면 없는 것과 같다 —
그래서 표를 버리고 세로 목록으로 다시 짰다.
- .gsplit 그리드 = 캔버스 minmax(0,1fr) + 레일 clamp(292px, 25vw, 348px), align-items:start
- 목록 위계: 막힌 요청이 위, 기다리는 선행이 화살표로 들여쓰기(캔버스 화살촉과 같은 방향 기호),
  상태·경과는 선행 아래 한 줄. 사라진 열 이름은 '막힌 요청 / 기다리는 선행' 한 줄이 대신한다
- 캔버스는 H = innerHeight - 210 으로 복원(depH 보정 삭제)
- 1180px 아래에서는 레일이 아래로 접히되 max-height 44vh + 내부 스크롤 — 좁은 화면에서도
  캔버스 높이가 먼저다
- 컨테이너 클래스는 .depsum 그대로라 스킨 8종의 패널 처리(라운드·글래스·그림자)를 그대로 물려받는다

**레일 폭에서만 드러난 것 셋도 같이 잡았다.**
- s9 link 예시 code 가 인라인이라 앞 문장 끝 글자와 뒤엉켜 끊겼다 → 제 줄로(display:block)
- 한국어가 아무 데서나 끊겼다('그려진 / 다.') → word-break:keep-all + overflow-wrap:break-word
- 병목 문장 전체가 경고색이라 레일에서 제목보다 크게 외쳤다 → 잉크는 겨눈 한 단어(건수)에만,
  이름은 본문 잉크로
- dsnote 문구의 '이 표에도' → '이 목록에도' (더 이상 표가 아니다)

**검증 — 실브라우저 캡처.**
실 vault 에 살아 있는 blocked_by 가 없어 채워진 레일을 볼 수 없다. web/railprobe.html
(index.html 복사본)에 fetch 응답만 가로채 가짜 의존을 얹어 확인하고 파일은 삭제했다.
서버·vault 는 건드리지 않았다.
- scratchpad/rail-full-light.png · rail-full-dark.png — 채워진 레일. 병목 1건이 3건을 붙잡은
  모양이 문장·목록·화살표 세 곳에서 같이 읽힌다. 화살촉도 또렷하다
- scratchpad/rail-real-light.png · rail-real-dark.png — 사용자 실보드(빈 상태), 캔버스 전체 높이
- scratchpad/rail-narrow.png — 1080px 접힘
- scratchpad/rail-glass.png — glass 스킨

**테스트: 전체 454건 통과(OK, skipped=1).**
그 과정에서 test_shot_hygiene 의 잠재 플레이크를 하나 고쳤다(tests/ 범위). 그 테스트는
자기 임시 루트를 s9shot- 접두사로 만드는데, 그건 바로 그 테스트가 검증하는 회수 장치가
지우는 표식이다 — s9-doctor 의 sweep_stale_shots 가 TMPDIR 에서 SHOT_MARKERS 로 시작하는
디렉터리를 rmtree 하고, 소유자 판정 정규식 s9shot-(숫자) 가 mkdtemp 랜덤 접미사의 앞 숫자를
pid 로 읽는다(s9shot-9j2anjm6 → pid 9 → 죽은 소유자 → 삭제). 캡처는 매번 sweep 을 선행하므로
스위트 중 캡처가 한 번이라도 돌면 이 테스트의 S9_ROOT 가 통째로 사라진다.
접미사 뽑기에 따라 되기도 안 되기도 하던 플레이크다 — 접두사를 s9-shothyg- 로 바꿨다.

**제안 (bin/ 는 이 세션 범위 밖).**
sweep_stale_shots 의 OWNER_RE 가 s9shot-(숫자) 를 search 로 찾는다. 표식이 아닌 이름에도
부분 매치가 나 남의 디렉터리를 pid 로 오인할 수 있다 — fullmatch 나 앵커를 걸면 이런
오삭제 경로가 닫힌다.

### 2026-08-26T14:23:50+09:00 designer (by nicehugepark)

### 3차 재작업 (designer) — 현황판을 우측 레일로, 빈 상태는 폭을 양보한다

**결론 먼저.** 현황판은 캔버스 오른쪽 레일에 선다. 그리고 **비어 있을 때는 판(窓)이 아니라
여백 주석이 되어 폭을 캔버스에 돌려준다** — 반려 원문이 지목한 것이 바로 그 '창'이라,
자리만 옮기고 크기를 그대로 두면 같은 불만이 옆으로 옮겨갈 뿐이다.
실화면(9909·막힌 요청 0건) 기준 캔버스 가로가 **1030px → 1150px** 로 넓어졌고, 세로는
아래 패널을 없앤 시점에 이미 온전히 돌려받았다.

### 판단 넷 (요구받은 항목)

**1. 폭과 위계 — 그래프가 주인공, 레일은 답이 있을 때만 대등해진다.**
막힌 요청이 있을 때 레일 `clamp(292px, 25vw, 348px)`. 이 폭은 장식이 아니라 최소치다 —
한 줄에 [상태 점 · id · 제목]이 들어가고 그 아래 [선행 상태 · 경과 · N건 붙잡음]이
줄바꿈 없이 붙는 폭이 292px 부터다. 그보다 좁히면 목록이 다시 표처럼 접혀 읽기 비용이
올라간다. 캔버스는 나머지 전부(`minmax(0,1fr)`).

**2. 빈 상태 — 접는 대신 '무게'를 버린다.**
0건이면 훑을 목록이 없다. 그런데 스캔용 폭과 판의 무게(보더·배경·그림자)를 그대로 쥐면
사용자에게는 "빈 창이 그래프를 밀어냈다"로 읽힌다. 그래서 두 가지만 내준다 —
- **폭**: `clamp(196px, 16vw, 236px)`. 목록을 훑을 폭이 아니라 문장 한 단의 폭.
- **무게**: 보더·배경·그림자·backdrop-filter 제거 → 장부 여백에 적은 주석이 된다.
  (`.gsplit .depsum.empty` 특이도 0,3,0 — 스킨 9종의 `[data-skin] .depsum` 판 스타일을
  스킨마다 끄지 않고 한 번에 덮는다. calm·soft·glass 캡처로 확인)
**문구는 한 글자도 줄이지 않았다.** 빈 상태는 자기 이유를 말해야 한다(DOC-20260826-009-62x6) —
줄인 것은 자리와 무게지 설명이 아니다. 각주("끝난 의존 1건은 세지 않았다")도 그대로다.

**3. 좁은 창 — 두 상태의 접힘 폭이 다르다.**
- 목록이 있는 레일: **1180px** 아래에서 아래로 접히고 `max-height:44vh` 로 캔버스를 지킨다.
- 여백 주석: 220px 뿐이라 노트북 폭에서도 캔버스를 크게 해치지 않는다 → **900px** 까지
  옆에 남는다(1080px 캡처: 캔버스 825px, 가로 스크롤 없음). 900px 아래에서만 내려온다.
같은 컴포넌트라도 차지하는 폭이 다르면 접는 시점도 달라야 한다.

**4. 스크롤 — 레일 안에서만, 그래프는 고정.**
`.dsbody{overflow:auto}` 가 목록만 굴리고 머리글·병목 한 줄·`더 보기`·각주는 제자리에
고정된다. 캔버스 크기는 목록 길이와 무관하다(전량 펼침 `?depall=1` 캡처로 확인 —
11건 전부 펼쳐도 그래프 기하가 그대로다).
덧붙여 **레일 바닥을 그래프 판 바닥에 맞췄다**: 범례가 한 줄이냐 두 줄이냐에 따라 판 높이가
달라져 고정 계산(`100vh - 218px`)은 매번 어긋났다. `syncRailH()` 가 그릴 때마다 실제 판
높이를 재서 넣는다(빈 상태·접힌 폭에서는 CSS 가 맡는다).

### 함께 고친 것
- **접근성**: 스크롤 영역이 키보드로 닿지 않았다(WCAG 2.1.1). `.dsbody` 에 tabindex·
  aria-label·포커스 링을 줬다 — 목록이 길 때 휠 없이 갇히지 않는다.
- **빈 상태 코드 예시 줄바꿈**: 220px 폭에서 브라우저가 `<선행>` 한가운데를 끊었다.
  플래그 앞에서 우리가 끊는다(`white-space:pre-wrap` + 본문 줄바꿈).

### 대안과 기각 근거
- (기각) **빈 상태를 캔버스 아래 한 줄로** — 폭을 100% 돌려주는 최선책이지만, 사용자가
  지목해 옮기라고 한 것이 바로 그 아래 자리다. 높이는 24px 뿐이라도 "다시 아래로 갔다"로
  읽힌다. 폭만 양보하고 자리는 지킨다.
- (기각) **손으로 접는 레일(접기 버튼 + 범례에서 복귀)** — 캔버스 폭이 바뀌면 그래프를
  다시 그려야 하고, 그때 물리·카메라가 리셋된다. 되돌리는 비용이 얻는 것보다 크다.
  "고르게 하지 말고 정해주라"에 따라 **자동**으로 정했다(0건이면 양보, 1건 이상이면 확보).
- (기각) **빈 상태에서 레일을 아예 지우기** — 화면이 왜 비었는지 말할 자리가 사라진다.
  2차 반려의 수리 지점이 그 설명이었다.
- (기각) **레일을 그래프 높이에 stretch 로 붙이기** — 2건짜리 목록이 690px 판이 된다.
  판은 내용 높이로 서고, 길어질 때만 잰 높이로 잘린다.

### 검증 (실브라우저 캡처, Windows Chrome headless)
실 vault 는 **무수정** — `blocked_by` 를 한 건도 걸지 않았다.
`grep -rn '^blocked_by:' vault/ | grep -v '\[\]'` → 착수 전과 같은 REQ-20260825-056-62x6 한 건.
목록이 찬 화면은 격리 vault(`S9_ROOT=.../scratchpad/iv`, 포트 9931, 의존 13건)에서만 만들었다.
- `.../scratchpad/d4-real-empty.png` — **9909 실화면**. 사용자가 지금 보게 될 화면.
  여백 주석 + 캔버스 1150px. (변경 전 대조: `.../scratchpad/d4-base-light.png` — 같은 화면에서
  348px 짜리 빈 판이 캔버스를 1030px 로 밀고 있다)
- `.../scratchpad/d4-empty-dark.png` — carbon(다크) 여백 주석.
- `.../scratchpad/d4-empty-calm.png` / `d4-empty-soft.png` — 스킨이 판 스타일을 얹어도
  여백 주석이 유지된다(그림자·라운드 카드 안 생김).
- `.../scratchpad/d4-full-final.png` (1600px) / `d4-full-light2.png` / `d4-full-dark.png` —
  목록이 찬 레일. 바닥이 그래프 판 바닥과 맞고, 목록만 안에서 스크롤된다.
- `.../scratchpad/d4-full-all.png` — 11건 전량 펼침(`?depall=1`): 그래프 기하 불변.
- `.../scratchpad/d4-empty-1080.png` — 1080px: 여백 주석이 옆에 남는다(캔버스 825px).
- `.../scratchpad/d4-empty-860.png` — 860px: 아래로 내려온다. 가로 스크롤 없음.
- `.../scratchpad/d4-full-1100.png` — 목록이 찬 레일은 1100px 에서 아래로(44vh).
전체 테스트 **454건 통과**(skip 1). `tests/test_dep_arrow.py` 10/10 — 레이아웃만 바꿨고
촉 기하 계약은 그대로다.

변경 파일은 `web/index.html` 하나뿐이다(`bin/s9`·서버 무수정).

### 2026-08-26T14:24:15+09:00 subagent (agent: sub:subagent:a777fb99, by nicehugepark)

Checking REQ-20260826-009-62x6 status metadata

### 2026-08-26T14:24:46+09:00 response (by nicehugepark)

### 확인 포인트 (F5 후 Graph 탭) — 14:12 상신 이후 추가분

문서는 이미 review 라 전이하지 않았다(review→review 불가). 14:12 상신 뒤에 **빈 상태의 폭**을
마저 손봤으니 그 축만 더 봐 달라.

1. **빈 상태가 폭을 양보하는가** — 지금 실데이터가 그 상태다. 0건이면 레일이 판(창)을 버리고
   여백 주석이 된다(보더·배경·그림자 제거, 348px → 220px). 캔버스 가로 **1030 → 1150px**.
   대조: `scratchpad/d4-base-light.png`(빈 판이 캔버스를 미는 모습) ↔ `scratchpad/d4-real-empty.png`(지금).
   문구·각주는 한 글자도 줄이지 않았다 — 줄인 것은 자리와 무게다.
2. **목록이 찼을 때** — `d4-full-final.png`(1600px) · `d4-full-light2.png` · `d4-full-dark.png`.
   레일 바닥이 그래프 판 바닥과 맞는다(범례 줄 수로 판 높이가 달라져 어긋나던 것을 실측해 넣는다).
3. **스크롤 격리** — 목록만 레일 안에서 굴고 머리글·병목 줄·더 보기·각주는 고정. 캔버스 기하는
   목록 길이와 무관하다(11건 전량 펼침 `d4-full-all.png`).
4. **좁은 창 — 두 상태의 접힘 폭이 다르다** — 여백 주석은 220px 뿐이라 900px 까지 옆에 남고
   (`d4-empty-1080.png`, 캔버스 825px), 목록이 찬 레일은 1180px 에서 아래로 접힌다
   (`d4-full-1100.png`, 44vh). 860px 에서 주석도 내려온다(`d4-empty-860.png`). 가로 스크롤 없음.
5. **스킨 물림** — calm·soft 처럼 패널에 그림자·라운드를 얹는 스킨에서도 빈 상태가 카드로
   되살아나지 않는다(`d4-empty-calm.png`, `d4-empty-soft.png`). 다크는 `d4-empty-dark.png`.
6. **접근성** — 스크롤 영역이 키보드로 닿지 않던 것(WCAG 2.1.1)을 tabindex·aria-label·포커스
   링으로 고쳤다.

검증: 실 vault 무수정(`blocked_by` 는 착수 전과 같은 1건), 목록이 찬 화면은 격리 vault
(포트 9931)에서만. 전체 테스트 **454건 통과**(skip 1), `test_dep_arrow` 10/10 — 레이아웃만
바꿔 촉 기하 계약은 그대로다. 변경 파일은 `web/index.html` 하나.

### 2026-08-26T14:24:47+09:00 subagent (agent: sub:subagent:a98e5b5e, by nicehugepark)

Retrying review transition on REQ-20260826-009-62x6

### 2026-08-26T14:25:20+09:00 subagent (agent: sub:subagent:a20c1adb, by nicehugepark)

Appending review checkpoints to REQ-20260826-009-62x6

### 2026-08-26T14:25:52+09:00 subagent (agent: sub:subagent:a2182592, by nicehugepark)

Linking DOC-20260826-010-62x6 to REQ-20260826-009-62x6

### 2026-08-26T14:25:55+09:00 subagent (agent: sub:designer:ae99c70f, by nicehugepark)

현황판을 우측 레일로 세우고, **비어 있을 때는 판(창)을 버리고 여백 주석이 되어 폭을 캔버스에 돌려주도록** 마저 고쳤다 — 실화면 캔버스 가로 1030→1150px, 문구·각주는 한 글자도 줄이지 않았다(줄인 것은 자리와 무게). 목록이 찬 레일 348px·목록만 내부 스크롤·바닥 실측 정렬, 접힘 분기점은 상태별로 다르게(주석 900px, 목록 1180px), 스킨 9종·다크·1080/860px 캡처로 확인했고 실 vault는 무수정(목록 찬 화면은 격리 vault 9931), 전체 테스트 454건 통과.

변경은 `/home/sjpark1/section9/web/index.html` 한 파일이며 근거·대안·캡처 경로는 `REQ-20260826-009-62x6` 노트(designer/response)와 지식 `DOC-20260826-010-62x6`에 남겼다 — 문서가 이미 review 상태라 전이는 하지 않았고(review→review 불가) 확인 포인트는 노트로 붙였다. 핵심 캡처: `.../scratchpad/d4-base-light.png`(변경 전) ↔ `.../scratchpad/d4-real-empty.png`(지금 화면), `.../scratchpad/d4-full-final.png`(목록이 찬 레일).

## History
- 2026-08-26T12:47:15+09:00 created by nicehugepark (status: open)
- 2026-08-26T12:47:27+09:00 status: open -> in-progress (by nicehugepark) — designer 에 위임 — 097 승인으로 데이터 축은 닫혔고 화면만 남았다.
- 2026-08-26T13:05:04+09:00 status: in-progress -> review (by nicehugepark) — 확인 포인트: (1) Graph 에서 화살촉이 "무엇이 끝나야 이게 풀리는가"로 읽히는가 — 선행에서 후행으로 뻗고, 촉은 막힌 문서에 붙는다(iso-graph2-light.png: 인증 토큰 스키마 1건이 3건을 붙잡은 모양). (2) 계보 엣지와 구별되는가 — 계보는 회색 가는 선, 의존은 잉크 굵은 대시+화살촉. 색면 없이 이 대비로 충분한가. (3) 병목을 엣지 굵기·농도로만 표현했다(노드 후광 추가 안 함) — 앰버 호와 겹치지 않게 한 판단이 맞는가. (4) 카드 한 줄 "선행 대기 | 제목 외 N건" 을 blocked 가 아닌 open/in-progress 카드에도 띄운다 — 사용자 원문 사례가 그것이라 그렇게 했다. (5) 수명 규칙: 양끝 중 하나라도 done/cancelled 면 안 그린다(iso-doc-done.png). (6) 다크(carbon)에서 화살촉 가시성 — iso-graph2-dark.png. 검증용 임시 관계는 전량 원복했고, 이후 캡처는 격리 vault(포트 9931)에서만 했다. 남은 blocked_by 는 착수 전부터 있던 REQ-20260825-056-62x6 한 건뿐(final-board.png 로 확인). 리드 확인 필요: 끝난 후행에 남는 유령 blocked_by 는 linkcheck --fix 대상이 아니다 — 데이터 청소가 필요한지 판단 요청(노트 마지막 절).
- 2026-08-26T13:09:18+09:00 status: review -> in-progress (by nicehugepark) — 반려: 화살표 촉이 보이지 않는다. 그리고 의견을 주자면, 그래프로 표현도 있지만, 테이블이나 정리된 글로 표현하는 방법도 검토를 해주고, 합당하다 싶으면 구현해줘. [via dashboard]
- 2026-08-26T13:30:52+09:00 status: in-progress -> review (by nicehugepark) — 확인 포인트: (1) **원인** — 촉이 안 보인 게 아니라 그릴 선이 0개였다. vault 전수에서 blocked_by 를 가진 문서는 REQ-20260825-056-62x6 한 건이고 그건 done 이라 수명 규칙으로 지워진다. /api/graph 응답으로 확인(노트 첫 절). (2) **9909 실화면**을 먼저 봐 달라 — v3-real-empty-glass.png / v3-real-empty-dark.png: "선행 대기 현황 · 없음" + 각주 "양끝 중 하나가 이미 끝난 의존 1건은 세지 않았다" + 범례 "대기 0"(흐림). 화면이 왜 비었는지 스스로 말하는가. (3) **촉 기하** — 리드가 넘긴 4건(획 관통·동일 알파·분리선 없음·좁은 밑변)을 전부 고쳤다. 322노드 실축척 캡처 v3-graph-light.png / v3-graph-dark-all.png 에서 촉이 읽히는가. (4) **표** — 그래프 아래 "선행 대기 현황"(막힌 요청 | 기다리는 선행 | 선행 상태 | 멈춘 지). 이 표가 "지금 무엇 때문에 무엇이 막혀 있나"에 답하는가. 열 이름 "멈춘 지"는 선행이 현재 상태로 머문 시간이다 — 블록이 걸린 시각은 데이터에 없어 지어내지 않았다. (5) **Board 병목 한 줄**은 한 선행이 2건 이상을 붙잡을 때만 뜬다(v2-board-light2.png) — 카드의 "선행 대기" 줄과 겹치지 않게 한 판단이 맞는가. (6) 실 vault 무수정: blocked_by 를 한 건도 걸지 않았고 검증은 격리 vault(포트 9931)에서만 했다. 촉을 직접 보려면 http://127.0.0.1:9931/#graph. 테스트 444건 통과. 리드 판단 요청: 유령 blocked_by(후행이 done)는 linkcheck --fix 대상이 아니라 데이터에 계속 남는다 — bin/s9 는 손대지 않았다.
- 2026-08-26T13:47:33+09:00 status: review -> in-progress (by nicehugepark) — 반려: 그래프 탭에서 '선행 대기 현황 없음' 창이 하단에 있으니까 그래프 화면이 너무 좁다. 우측으로 옮기는 디자인을 다시 구성해줘. [via dashboard]
- 2026-08-26T14:12:46+09:00 status: in-progress -> review (by nicehugepark) — 확인 포인트 (F5 새로고침 후 Graph 탭): (1) 그래프가 세로를 온전히 쓰는가 — 현황판이 오른쪽 레일로 갔고 캔버스 높이 보정(depH)을 없앴다. scratchpad/rail-real-light.png, rail-real-dark.png. (2) 레일 폭에서 목록이 읽히는가 — 4열 표는 300px대에서 가로 스크롤이 생겨 버렸고, 막힌 요청 위 / 기다리는 선행을 화살표로 들여쓰기 / 상태·경과는 그 아래 한 줄로 다시 짰다. 열 이름 자리는 '막힌 요청 - 기다리는 선행' 한 줄이 대신한다. 채워진 모습은 scratchpad/rail-full-light.png, rail-full-dark.png (실 vault 에 살아 있는 blocked_by 가 없어 index.html 복사본에서 fetch 응답만 가로채 확인했고 그 파일은 삭제했다 — 서버·vault 무수정). (3) 레일 폭 clamp(292px, 25vw, 348px) 가 적당한가 — 더 좁히면 제목이 접히고 더 넓히면 캔버스를 먹는다. (4) 1180px 아래에서 레일이 아래로 접히되 44vh 로 잘린다 — 좁은 화면에선 캔버스 높이를 먼저 지키는 판단이 맞는가. scratchpad/rail-narrow.png. (5) 병목 문장에서 경고색을 건수 한 단어에만 얹었다 — 문장 전체가 붉으면 레일에서 제목보다 크게 외쳤다. (6) 스킨 물림: 컨테이너 클래스를 .depsum 그대로 두어 스킨 8종 패널 처리를 물려받는다. scratchpad/rail-glass.png. 검증: 전체 테스트 454건 통과(OK, skipped=1). 그 과정에서 test_shot_hygiene 의 잠재 플레이크를 고쳤다 — 임시 루트 접두사가 s9shot- 이라 s9-doctor sweep 의 삭제 대상과 겹쳤다(노트 참조). bin/ 쪽 OWNER_RE 부분매치 문제는 제안으로만 남겼다.
- 2026-08-26T22:13:59+09:00 status: review -> done (by nicehugepark) — 대시보드 승인 [via dashboard]
