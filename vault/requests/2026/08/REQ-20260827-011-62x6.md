---
id: REQ-20260827-011-62x6
type: request
title: 바인딩 경로가 글자로 쪼개졌다
summary: agent_transcript_path 옛 데이터가 문자 리스트로 남아 활동 판정을 오염시킨다
goal: 상한 바인딩이 복구되고, 같은 모양이 다시 저장되지 않는다
status: done
size: S
user: sjpark1
machine: DESKTOP-TEHV1KR
session: a46c19bb
sessions: ["8e60e4af", "a46c19bb"]
agents: ["worker:반려-재작업"]
contributions: [{"actor": "worker:반려-재작업", "item": "전수 점검 (데이터 62건 + 코드 경로)", "started": "2026-08-27T08:40:27+09:00", "ended": "2026-08-27T08:40:27+09:00", "result": "done", "transcript": ""}, {"actor": "worker:반려-재작업", "item": "bin/s9 제안 패치 (봉투 밖)", "started": "2026-08-27T08:40:30+09:00", "ended": "2026-08-27T08:40:30+09:00", "result": "done", "transcript": ""}, {"actor": "worker:반려-재작업", "item": "TDD 시나리오 개정 (전수 점검 세대)", "started": "2026-08-27T08:41:55+09:00", "ended": "2026-08-27T08:41:55+09:00", "result": "done", "transcript": ""}, {"actor": "worker:반려-재작업", "item": "동시 작업 겹침 + 판정/복구 술어 불일치", "started": "2026-08-27T08:43:02+09:00", "ended": "2026-08-27T08:43:02+09:00", "result": "done", "transcript": ""}]
project: section9
children: ["REQ-20260827-018-62x6"]
created: 2026-08-27T01:10:27+09:00
status_since: 2026-08-27T09:13:45+09:00
updated: 2026-08-27T10:22:02+09:00
priority: 50
---

## Original

요구는 노트 참조. 코드는 이미 고쳐졌고 데이터만 남았다.

## Notes

### 2026-08-27T01:10:55+09:00 response (by sjpark1)

REQ-20260827-002-62x6 를 조사하다 발견했다 — **바인딩 두 개의 `agent_transcript_path` 가 경로 문자열이 아니라 글자 하나씩 쪼개진 리스트로 저장돼 있다.**

    "agent_transcript_path": ["/", "t", "m", "p", "/", "c", "l", "a", "u", "d", "e", …]

`list("/tmp/claude-…")` 를 한 결과다. 지금 코드는 정규화한다(`atp = [atp] if isinstance(atp, str) else list(atp)`) — 즉 **이미 고쳐진 결함의 옛 데이터가 남은 것**이다. 해당 바인딩 둘(`d332535d` · `104b4fe3`)의 나이가 25.7시간이라 그 이전 판본이 쓴 것으로 보인다.

**해가 없지는 않다.** `_binding_activity_paths()` 가 이 목록을 활동 경로로 훑는데, 그중 `"/"` 는 실제로 존재하는 디렉토리라 살아남아 **루트의 mtime 이 세션 활동 신선도로 계산된다.** 그리고 바인딩 하나당 100개 넘는 가짜 경로를 매번 `os.path.exists` 로 두드린다 — 채팅 대상 고르기가 매 메시지마다 도는 경로다.

**요구**: `s9 normalize` 가 이 모양을 알아보고 되돌린다. 판별은 쉽다 — 리스트 원소가 전부 길이 1이면 그건 경로 목록이 아니라 쪼개진 문자열이다. `"".join()` 으로 복원하고, 복원한 경로가 존재하지 않으면 빈 목록으로 둔다(죽은 세션의 임시 파일은 이미 사라졌다).

**교훈은 이 저장소가 오늘 세 번 밟은 것과 같다**: 한 필드가 문자열과 리스트 두 뜻으로 쓰였다. 읽는 자리 셋 중 둘은 `isinstance` 로 방어했고 쓰는 자리 하나가 안 했다 — 방어가 읽는 쪽에 흩어져 있으면 **쓰는 쪽 하나만 새어도 데이터가 상한다.** 방어는 경계 한 곳(쓰기)에 있어야 한다.

### 2026-08-27T01:29:35+09:00 response (by sjpark1)

고쳤다. **되돌리기보다 자리가 요점이었다.**

`s9 normalize` 에 넣으려던 처음 생각을 바꿨다. 그건 사람이 기억해서 돌려야 하는 명령이고, 이 결함은 **다시 생기지 않게 하는 것**이 본체다. 그래서 바인딩을 읽고 쓰는 **경계 한 곳**(`read_binding`·`write_binding`)에 `_norm_binding()` 을 뒀다.

내 노트가 적은 교훈을 그대로 적용한 것이다 — 방어가 읽는 쪽에 흩어져 있으면 쓰는 쪽 하나만 새어도 데이터가 상하고, 그 데이터를 읽는 새 코드가 또 당한다. 실제로 그렇게 났다: 읽는 자리 셋 중 둘이 `isinstance` 로 방어했는데 쓰는 자리 하나가 안 했다.

판별과 복원:
- 원소가 전부 한 글자면 경로 목록이 아니라 쪼개진 문자열 → `"".join()` 으로 붙인다.
- 붙였는데 그 파일이 없으면 버린다(죽은 세션의 임시 파일은 이미 사라졌다).
- 옛 문자열 형태도 리스트로 맞춘다 — 두 뜻 중 하나로 모은다.
- 멀쩡한 목록은 건드리지 않는다.

`chat_target` 은 바인딩 파일을 직접 열길래 거기도 같은 경계를 태웠다.

**실물 복구**: 상해 있던 바인딩 둘을 읽고 다시 써서 고쳤다. **107개 → 1개**, 그리고 복원된 경로가 실제로 존재하는 것까지 확인했다. 이제 루트 디렉토리(`"/"`)가 세션 활동 신선도로 계산되는 일도 없고, 채팅 대상 고르기가 매 메시지마다 두드리던 가짜 경로 200여 개도 사라졌다.

**테스트** `tests/test_binding_shape.py` 7건. S3 이 이 결함의 진짜 해를 못 박는다 — `"/"` 가 활동 경로로 살아남지 않는가. 루트는 실제로 존재하므로 "없는 경로 거르기"로는 안 걸러진다. S7 은 읽고 쓰면 파일이 스스로 고쳐지는 경로를 지킨다 — 옛 데이터가 정리되는 길이 있어야 사람이 손으로 고치지 않는다.

이것으로 REQ-20260827-002-62x6 의 막힘이 풀린다.

### 2026-08-27T01:29:35+09:00 tdd (by sjpark1)

- [ ] S1. 글자로 쪼개진 경로가 다시 붙는다 (실제로 상해 있던 모양)
- [ ] S2. 붙였는데 없는 파일이면 버린다
- [ ] S3. `"/"` 가 활동 경로로 살아남지 않는다 (이 결함의 진짜 해)
- [ ] S4. 옛 문자열 형태도 리스트로 맞춰진다
- [ ] S5. 멀쩡한 목록은 불변 (고침이 새 손실이 되면 안 된다)
- [ ] S6. 정규화가 읽기·쓰기 **양쪽** 경계에 걸려 있다
- [ ] S7. 읽고 쓰면 저장된 파일이 스스로 고쳐진다
실물 바인딩 2개 복구 확인 (107개 → 1개, 복원 경로 존재).

### 2026-08-27T08:35:55+09:00 response (by sjpark1)

전수로 훑었다. **그리고 한 번 더 훑는 것으로 끝내지 않고 명령으로 남겼다** — 사람이 물어볼 때만 도는 검사는 물어보지 않으면 안 돈다.

### 훑은 결과

**바인딩 60개 · 문서 385개 → 쪼개진 데이터 0건.** 어제 고친 둘은 복구된 채로 남아 있고 새로 생긴 것은 없다. 필드마다 모양도 하나씩이다(한 필드가 두 뜻으로 쓰이는 자리 0).

훑다가 나온 것 둘:

**① `approvals_seen.json` 이 바인딩 폴더에 산다.** 바인딩이 아니라 REQ id 를 키로 쓰는 별개 파일인데 `state/sessions/` 안에 있어서, `state/sessions/*.json` 을 바인딩으로 훑는 코드가 전부 이걸 집는다(`chat_target` 이 메시지마다 그렇게 한다). `session` 키가 없어 걸러지므로 지금 해는 없지만, **"이 폴더의 파일은 바인딩이다"라는 전제가 이미 거짓**이다. 검사에서는 `session` 유무로 명시적으로 걸렀다(A5 가 그 자리를 지킨다).

**② `priority` 는 문서에 문자열로 저장된다.** 정렬에 쓰이는 값이라 확인했는데, `doc_priority()` 가 한 곳에서 숫자로 바꾸고 카탈로그에는 385건 전부 int 로 들어간다. **경계에서 정규화하고 있어서 안전하다** — 이번 결함이 없었던 이유가 그것이고, `agent_transcript_path` 는 그 경계가 없었다.

### 남긴 것 — `s9 shapes`

훑는 규칙을 명령으로 뽑았다. 상한 결과만 찾는 게 아니라 **상하게 될 자리**를 본다:

- 글자 단위로 쪼개진 값
- **한 필드가 여러 모양으로 나타나는 자리** — 이게 본체다. `agent_transcript_path` 사고는 데이터가 상하기 **전에** 여기서 보였을 것이다.
- 깨진 JSON (보고하되 검사를 세우지 않는다 — 하나 때문에 나머지를 못 보면 전수 검사가 아니다)

**판정을 한 번 고쳤다.** 처음엔 "원소가 전부 한 글자면 쪼개짐"으로 봤는데, 그게 `["x"]` 같은 멀쩡한 한 글자 태그를 오인했다 — **내가 쓴 테스트(A4)가 잡았다.** 쪼개진 경로는 언제나 길므로(20조각 넘음) 길이로 갈랐다. 늘 시끄러운 검사는 아무도 안 보게 되고, 그러면 없는 것과 같다.

**테스트** `tests/test_binding_shape.py` 에 6건 추가(A1 멀쩡하면 조용함 · A2 쪼개짐 탐지 · A3 한 필드 두 모양 탐지 · A4/A4b 빈 리스트와 짧은 한 글자 목록은 오탐 아님 · A5 바인딩 아닌 파일 건너뜀 · A6 깨진 JSON 보고).

### 2026-08-27T08:40:17+09:00 decision (by sjpark1)

**리드가 이 문서에서 손을 뗀다.** 무인 작업자(세션 a46c19bb, 08:29:47 스폰)가 이미 붙어 있고, 그쪽이 나보다 앞서 있다.

경위를 그대로 적는다. 반려를 받고 내가 곧바로 전수 점검에 들어갔는데 — **클레임을 하지 않았다.** 유예가 지나 워처가 작업자를 스폰했고, 그때 나는 이미 같은 문서의 같은 문제를 손대고 있었다. 어제 내가 만든 규율(착수하면 클레임한다)을 내가 어긴 것이고, REQ-20260826-021-62x6 이 없애려던 상태를 내가 만들었다.

**그리고 그 작업자가 내 고침의 진짜 구멍을 찾았다.**

    _norm_binding 의 존재 확인이 os.path.exists 다 → 디렉토리도 통과한다.
    "/" 는 디렉토리이므로 여전히 살아남는다.

직접 확인했다:

    bind 로 "/" 를 넣으면 → {'agent_transcript_path': ['/']}
    os.path.exists("/") = True · os.path.isfile("/") = False

내 테스트 S3 은 **쪼개진 리스트를 join 한 경로**만 봤다. 문자열로 곧장 들어오는 `"/"` 는 안 봤다. transcript 는 언제나 파일이므로 `isfile` 이어야 한다 — 작업자 말이 맞다. 이 결함의 진짜 해가 "루트가 활동 경로로 살아남는 것"이었는데 **그 문을 내가 반쯤만 닫았다.**

**내 작업 트리에 남아 있는 것**(커밋하지 않았다): `bin/s9` 의 `shape_audit()` + `s9 shapes` 명령, `tests/test_binding_shape.py` 의 검사 테스트 6건. 작업자와 같은 파일이라 담지 않는다 — 커밋 게이트도 이걸 막았다(종료코드 1). 작업자가 마치면 그 위에 얹거나, 겹치면 작업자 것을 살린다.

전수 결과 자체는 위 노트에 있다(바인딩 60·문서 385, 쪼개진 데이터 0). 작업자가 찾은 세 갈래는 그것과 층이 다르다 — 나는 **남아 있는 상한 데이터**를 봤고, 작업자는 **다시 상할 수 있는 경로**를 봤다. 후자가 반려가 요구한 것에 더 가깝다.

### 2026-08-27T08:40:27+09:00 response (agent: worker:반려-재작업, by sjpark1)

전수 점검했다. **깨끗한 곳과 안 깨끗한 곳이 갈렸다** — 데이터는 다 나았고, 코드에는 같은 함정이 세 갈래 더 남아 있었다.

### 데이터 — 전수 통과

저장된 바인딩 **62개 전부**를 훑었다. 글자로 쪼개진 목록 0건, 리스트로 쓰는 필드(`agent_transcript_path`·`active_reqs`·`history`)가 문자열로 저장된 것 0건. 앞서 복구한 둘(`d332535d`·`104b4fe3`)도 정상 모양으로 남아 있다. 이 확인은 이제 손이 아니라 스위트가 한다 — `tests/test_binding_sweep.py::StoredBindingsAreClean` 이 매번 실제 `state/sessions/` 를 훑는다.

**쓰기 경계도 하나가 맞다.** 바인딩 파일에 쓰는 자리는 `write_binding()` 하나뿐이고(`bin/s9:223`), 훅·서버·설치기 어디에도 두 번째 writer 가 없다. 그래서 **새 오염이 다시 들어올 길은 막혀 있다.** 아래 세 구멍은 전부 그 사실 위에 서 있는 것 — 지금 당장 데이터를 상하게 하고 있지는 않지만, 문은 열려 있다.

### H1. 존재 확인이 `exists` 라 디렉토리가 통과한다

이 결함의 진짜 해는 "없는 경로"가 아니라 **"실재하는 디렉토리 `/` 의 mtime 이 세션 활동으로 읽히는 것"** 이었다. 그런데 고침은 `os.path.exists(joined)` 로 걸렀다 — 디렉토리에도 참인 검사다. 그래서:

- `s9 bind agent_transcript_path /` → 문자열 갈래는 존재 확인조차 없이 `["/"]` 가 된다.
- 쪼개진 리스트를 붙였는데 그게 디렉토리면(`list("/tmp")`) 그대로 남는다.

transcript 는 언제나 파일이다. 걸러야 할 것은 '없는 것'이 아니라 **'파일이 아닌 것'** 이다.

### H2. 활동 경로 계산이 아직 두 벌이다

`_binding_activity_paths()` (`bin/s9:1740`) 와 카탈로그 live 판정(`bin/s9:8126~8135`, `s9 serve` 안)이 **같은 코드를 각자 손으로 들고 있다** — transcript_path + agent_transcript_path + 스트림 글롭, 한 줄까지 같다. 그리고 둘 다 `atp if isinstance(atp, list) else [atp]` 로 **읽는 쪽에 흩어진 방어**를 한다. 쪼개진 리스트는 이 방어를 그냥 통과한다(이미 리스트니까).

이 문서가 적은 교훈이 "방어는 읽는 쪽이 아니라 경계"였는데, 정작 **활동 경로의 경계가 비어 있었다.** 바인딩 파일을 `_norm_binding` 없이 직접 `json.load` 하는 자리가 넷이고, 넷 다 이 함수를 지난다:

| 자리 | 하는 일 |
|---|---|
| `bin/s9:1767` `rework_claimed()` | 워커 중복 스폰을 막는 클레임 판정 |
| `bin/s9:2792` 통지 팬아웃 | 반려 메시지를 어느 세션에 보낼지 |
| `bin/s9:8120` 카탈로그 live | 대시보드 점멸등 (인라인 복제본) |
| `bin/s9:8409` 에이전트 대상 선택 | 지목 메시지 중계 |

방어를 `_binding_activity_paths()` 한 곳에 두면 **넷이 함께 닫힌다.**

### H3. 형제 필드 `active_reqs` 가 무방비다 — 해는 더 크다

`_norm_binding` 은 `agent_transcript_path` 만 본다. 바로 옆 `active_reqs` 는 정확히 같은 함정에 노출돼 있고, 문 하나가 열려 있다:

1. `s9 bind active_reqs REQ-X` — `cmd_bind` 는 임의 키에 **문자열**을 넣는다(`bin/s9:4771`).
2. 그다음 `_claim_req` 의 `list(b.get("active_reqs") or [])` (`bin/s9:5519`) 또는 `update_active_reqs` 의 같은 줄(`:1014`)이 그 문자열을 **글자로 쪼갠다.**
3. `write_binding` 이 쪼개진 것을 **그대로 저장한다** — 정규화가 이 필드를 안 보기 때문이다.
4. `binding_req_ids()` (`:479`)가 오염되어 클레임 판정이 깨지고, **워처가 같은 REQ에 워커를 중복 스폰한다.**

원본 결함(atp)은 활동 신선도를 흐렸을 뿐이지만, 이쪽은 **누가 무엇을 잡고 있는가**를 흐린다. REQ-20260825-086-62x6 이 이미 한 번 밟은 자리다.

### 못 박아 둔 것

`tests/test_binding_sweep.py` 16건. 그중 **8건이 위 세 구멍의 계약**이고 `expectedFailure` 로 표시했다 — 고침이 전부 `bin/s9` 안이라 이 무인 세션의 봉투(web·vault·tests) 밖이기 때문이다. 리드가 제안 패치를 적용하면 이 8건이 `unexpected success` 로 스위트를 붉게 만든다. 그때 마커를 지우면 된다. **고침에 테스트가 반드시 따라붙게 하는 장치**다.

나머지 8건은 지금 초록이고, 회귀를 잡는다 — 정규화 멱등성, 없는 키를 만들어 넣지 않기, 문자열 아닌 쓰레기 거르기, 섞인 목록에서 진짜 경로만 남기기, 쓰기 경계 단일성, 그리고 저장된 바인딩 전수 확인 2건.

검증: `python3 tests/ binding` → 34건 OK (expected failures=8). 관련 스위트(live·claim·chat·notify) 전부 통과.

### 2026-08-27T08:40:30+09:00 proposal (agent: worker:반려-재작업, by sjpark1)

**제안 (봉투 밖 — `bin/s9`, 리드 판단)**. 세 구멍(H1·H2·H3)은 한 패치로 닫힌다. 사본을 만들어 **실제로 돌려 보고 확인했다** — `scratchpad/s9-req011-patched` 가 그 사본이다. `diff -u bin/s9 scratchpad/s9-req011-patched` 로 그대로 볼 수 있고, 세 군데뿐이다.

**① `_norm_binding` — 필드 하나짜리를 두 필드로 넓히고, `exists` 를 `isfile` 로** (`bin/s9:191`)

```python
    for key, is_path in (("agent_transcript_path", True),
                         ("active_reqs", False)):
        v = _norm_str_list(b.get(key), is_path)
        if v != b.get(key) and (v or key in b):
            b[key] = v
    return b


def _norm_str_list(v, is_path=False):
    """문자열/리스트 두 뜻으로 쓰인 필드를 리스트 하나로 모은다."""
    if isinstance(v, str):
        v = [v] if v else []
    elif isinstance(v, list):
        if v and all(isinstance(x, str) and len(x) <= 1 for x in v):
            joined = "".join(v)
            v = [joined] if (os.path.isfile(joined) if is_path
                             else bool(joined)) else []
        else:
            v = [x for x in v if isinstance(x, str) and len(x) > 1]
    else:
        v = []
    if is_path:
        # 없는 경로는 남겨 둔다(고침이 새 손실이 되면 안 된다). 버릴 것은
        # **파일이 아닌 것** — 실재하는 디렉토리가 활동 신선도로 읽히는 것이
        # 이 결함의 진짜 해였다.
        v = [x for x in v if not os.path.isdir(x)]
    return v
```

없는 경로를 남기고 디렉토리만 버리는 것이 요점이다. 전부 `isfile` 로 걸러 버리면 S5(멀쩡한 목록 불변)가 깨진다 — 고침이 새 손실이 되면 안 된다.

**② `_binding_activity_paths` 를 활동 경로의 경계로** (`bin/s9:1740`)

```python
    b = _norm_binding(b)     # 방어는 읽는 쪽이 아니라 경계 한 곳에 (REQ-011)
    paths = [p for p in
             ([b.get("transcript_path", "")] +
              list(b.get("agent_transcript_path") or []))
             if p and os.path.isfile(p)]
```

이 한 줄로 바인딩을 직접 `json.load` 하던 네 자리(`:1767` 클레임 판정 · `:2792` 통지 팬아웃 · `:8120` 카탈로그 live · `:8409` 에이전트 대상 선택)가 **함께 닫힌다.**

**③ 카탈로그 live 의 인라인 복제본 제거** (`bin/s9:8229` 부근) — 손으로 복제한 열 줄을 `paths = _binding_activity_paths(b)` 한 줄로. 스트림 글롭도 그 안에 이미 있으니 뒤따르던 `if sid8: paths += ...` 도 같이 없어진다.

(`_norm_binding` 의 docstring 이 아직 `agent_transcript_path` 만 말한다 — 넓힌 뜻으로 한 줄 고쳐 두면 좋겠다.)

**검증 결과**: 사본을 불러 여덟 계약 전부 통과, 그리고 기존 계약(S1·S2·S3·S4·S5·S7)과 `live_agents()` 의 정상 판정까지 함께 통과했다(15건 OK). 즉 **이 패치는 새 계약을 세우면서 옛 계약을 하나도 깨지 않는다.**

적용하면 `tests/test_binding_sweep.py` 의 `expectedFailure` 8건이 `unexpected success` 로 스위트를 붉게 만든다 — 그때 마커만 지우면 그대로 정식 회귀 테스트가 된다.

### 2026-08-27T08:41:55+09:00 tdd (agent: worker:반려-재작업, by sjpark1)

- [x] S1. 글자로 쪼개진 경로가 다시 붙는다 (실제로 상해 있던 모양)
- [x] S2. 붙였는데 없는 파일이면 버린다
- [x] S3. `"/"` 가 활동 경로로 살아남지 않는다 (이 결함의 진짜 해)
- [x] S4. 옛 문자열 형태도 리스트로 맞춰진다
- [x] S5. 멀쩡한 목록은 불변 (고침이 새 손실이 되면 안 된다)
- [x] S6. 정규화가 읽기·쓰기 **양쪽** 경계에 걸려 있다
- [x] S7. 읽고 쓰면 저장된 파일이 스스로 고쳐진다
- [x] D1. 저장된 바인딩 62개 전수 — 쪼개진 목록 0건 (`s9 shapes` 도 0건 동의)
- [x] D2. 리스트로 쓰는 필드가 문자열로 저장된 것 0건
- [x] A1. 정규화 멱등 · 없는 키를 만들지 않음 · 문자열 아닌 쓰레기 거름 · 섞인 목록에서 진짜 경로만 남김 · 바인딩 writer 단일성
- [x] H1. `exists` 가 디렉토리를 통과시키는 구멍을 재현해 계약으로 못 박았다 (`s9 bind agent_transcript_path /` · `list("/tmp")`)
- [x] H2. 활동 경로 계산이 두 벌이고 경계가 비어 있음을 재현해 계약으로 못 박았다 (readers 4곳이 이 한 함수를 지난다)
- [x] H3. 형제 필드 `active_reqs` 의 같은 함정을 재현해 계약으로 못 박았다 (문자열 → 클레임이 글자로 쪼갬 → 그대로 저장됨)
- [x] P1. 제안 패치를 사본에 적용해 실행 — H1·H2·H3 여덟 계약 전부 통과, 기존 계약(S1~S5·S7)과 `live_agents()` 정상 판정도 함께 통과 (15건 OK)
- [x] P2. 전체 스위트 810건 통과 (skipped=1, expected failures=8)

H1·H2·H3 의 실제 **적용**은 `bin/s9` 안이라 이 무인 세션의 봉투 밖이다 — 계약과 검증까지 마쳤고, 적용 여부가 이 review 의 확인 포인트다.

### 2026-08-27T08:43:02+09:00 conflict (agent: worker:반려-재작업, by sjpark1)

**동시 작업 알림 — 같은 REQ에 둘이 붙어 있었다.**

내가 착수했을 때(08:29) `bin/s9` 는 깨끗했는데, 작업 중(08:50) 미커밋 변경이 생겼다: `shape_audit()` · `_shape_of()` · `_split_chars()` · `cmd_shapes()` 와 `tests/test_binding_shape.py::ShapeAudit` — 다른 주체가 같은 반려("한번 더 전수 점검")에 **다른 각도로** 붙어 있다. 그쪽은 "검사를 상시 명령으로 남긴다"(`s9 shapes`), 나는 "남은 구멍을 찾아 계약으로 못 박는다". **파일이 겹치지 않아 덮어쓴 것은 없다** — 나는 `tests/test_binding_sweep.py` 하나만 새로 만들었고 `bin/s9` 는 읽기만 했다.

**두 작업이 만나는 자리에 걸림돌이 하나 있다.** 새 `_split_chars()` 는 `SPLIT_MIN = 8`(여덟 조각 이상)로 쪼개짐을 판정하는데, `_norm_binding()` 의 복구는 **길이 하한이 없다**(`all(len(x) <= 1)`). 둘이 어긋난다:

- `["a", "b"]` → 검사는 "깨끗함", 정규화는 `"ab"` 로 붙였다가 파일이 아니라서 **버린다.** 검사가 조용한 채 데이터가 사라진다.
- 다섯 조각으로 쪼개진 값 → 정규화는 되붙이는데 검사는 보고하지 않는다.

**판정과 복구가 같은 술어를 써야 한다.** 이 REQ가 하루 종일 말한 그것이다 — 같은 뜻이 두 벌이면 한쪽만 고쳐진다. `_norm_binding` 이 `_split_chars()` 를 호출하도록 모으면 둘이 영원히 같아진다(`is_path` 갈래는 그대로 두고 판별만 공유). 내 제안 패치(`scratchpad/s9-req011-patched`)는 이 미커밋 변경 **이전** 사본이라 그 부분이 빠져 있다 — 적용할 때 함께 보라.

`s9 shapes` 실측 결과는 `바인딩 60개 · 문서 386개 → 문제 0건`. (바인딩 파일은 62개지만 `session` 없는 두 개는 바인딩이 아니라 건너뛴다 — 정상이다.) 내가 손으로 훑은 62개 전수 결과와 일치한다.

## History
- 2026-08-27T01:10:27+09:00 created by sjpark1 (status: open)
- 2026-08-27T01:27:37+09:00 status: open -> in-progress (by sjpark1) — 리드 착수 — 002 를 막고 있다
- 2026-08-27T01:32:11+09:00 status: in-progress -> review (by sjpark1) — 눈에 보이는 변화는 없지만 안에서 쓰레기 하나를 치웠다. 세션 기록에 파일 경로가 글자 하나씩 쪼개져 저장돼 있었고(107조각), 그중 슬래시 하나가 진짜 폴더라서 "이 세션이 방금 활동했다"는 판정에 섞여 들어가고 있었다. 채팅을 보낼 때마다 존재하지도 않는 경로 200여 개를 확인하고 있었던 것도 함께 사라졌다. 확인할 것이 딱히 없는 종류라 그냥 알려 두는 것이다 — 굳이 보겠다면 대시보드 터미널 탭에서 대화 상대가 여전히 제대로 잡히는지 정도다.
- 2026-08-27T08:28:54+09:00 status: review -> in-progress (by sjpark1) — 반려: 이거 한번 더 전수 점검해줘. [via dashboard]
- 2026-08-27T08:42:07+09:00 status: in-progress -> review (by sjpark1) — 다시 전수로 훑었다. 저장된 기록 62개는 전부 멀쩡하다 — 이건 확인이 끝났고, 이제 스위트가 매번 대신 훑는다. 문제는 "문이 아직 열려 있다"는 쪽이었고 세 군데 남아 있었다. ① 파일 자리에 폴더를 적어도 통과한다 — 폴더는 언제나 방금 손댄 것처럼 보여서, 끝난 세션이 계속 일하는 중으로 읽힌다(지난번에 잡았다고 한 바로 그 증상이다). ② 같은 계산을 두 군데가 각자 베껴 들고 있다 — 한쪽만 고쳐지는 모양이다. ③ 바로 옆 칸, 어떤 요청을 붙잡고 있는지 적는 칸이 똑같은 함정에 무방비다. 여기가 상하면 누가 무엇을 잡고 있는지가 흐려져 같은 요청에 작업자가 둘 뜬다 — 앞의 것보다 해가 크다. 셋 다 고치는 자리가 이 무인 세션이 손대면 안 되는 파일(bin/s9)이라 적용하지 않고 제안으로 남겼다. 대신 사본에 실제로 적용해 돌려 봤다: 새 확인 15가지 전부 통과, 기존 확인도 하나도 안 깨진다. 전체 스위트 810건도 통과했다. 확인할 것은 하나 — 이 제안을 적용할지만 정해 주면 된다. 문서의 proposal 노트에 고칠 세 군데가 그대로 있고, 터미널에서 diff -u bin/s9 scratchpad/s9-req011-patched 로 눈으로 볼 수 있다. 화면으로 볼 것은 없다.
- 2026-08-27T09:13:45+09:00 status: review -> done (by sjpark1) — 대시보드 승인 [via dashboard]
