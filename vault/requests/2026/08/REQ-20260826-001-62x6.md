---
id: REQ-20260826-001-62x6
type: request
title: PDF 텍스트 추출 결함
summary: 한글 PDF가 0바이트로 추출된다 — hex 문자열·ToUnicode CMap 미처리, endstream 매칭·8MB 컷 결함
goal: 한글 PDF 첨부의 본문이 추출된다
status: done
size: M
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: ebfb947f
sessions: ["cb49b2cd", "ebfb947f"]
agents: ["sub:subagent:a170bf65", "sub:subagent:af2afdd7", "sub:backend-developer", "lead:claude-opus-5"]
contributions: [{"actor": "sub:subagent:a170bf65", "item": "Checking REQ-20260826-001-62x6 blocker status", "started": "2026-08-26T12:50:21+09:00", "ended": "2026-08-26T12:50:21+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/cb49b2cd-601c-40bd-b9c4-d0afb0576d9f/subagents/agent-a170bf652a672be20.jsonl"}, {"actor": "sub:subagent:af2afdd7", "item": "Writing TDD scenarios to REQ-20260826-001-62x6", "started": "2026-08-26T19:25:45+09:00", "ended": "2026-08-26T19:25:45+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/cb49b2cd-601c-40bd-b9c4-d0afb0576d9f/subagents/agent-af2afdd7c294513b2.jsonl"}, {"actor": "sub:backend-developer", "item": "결론: 원인은 \"스트림을 전부 풀어 (...) 만 긁는\" 방식 자체였다 — 객체 구조를 따라가는 추출기로 교체했고, 실물 한글", "started": "2026-08-26T19:41:50+09:00", "ended": "2026-08-26T19:41:50+09:00", "result": "done", "transcript": ""}, {"actor": "sub:backend-developer", "item": "재현 자산을 테스트에 물렸다 (보강)", "started": "2026-08-26T19:42:26+09:00", "ended": "2026-08-26T19:42:26+09:00", "result": "done", "transcript": ""}, {"actor": "sub:backend-developer", "item": "정정: 재현 자산은 `tests/fixtures/` 에 커밋한다 (vault/ 는 .gitignore 대상)", "started": "2026-08-26T19:43:26+09:00", "ended": "2026-08-26T19:43:26+09:00", "result": "done", "transcript": ""}, {"actor": "sub:backend-developer", "item": "TDD 시나리오 — 결과 (전부 green, `python3 tests/ pdf_text` 12/12)", "started": "2026-08-26T19:43:57+09:00", "ended": "2026-08-26T19:43:57+09:00", "result": "done", "transcript": ""}, {"actor": "lead:claude-opus-5", "item": "`[2026-08-28 16:17:07 KST - lead]`", "started": "2026-08-28T16:17:07+09:00", "ended": "2026-08-28T16:17:07+09:00", "result": "done", "transcript": ""}]
project: section9
parent: REQ-20260825-056-62x6
children: ["REQ-20260826-020-62x6"]
relates: ["REQ-20260827-008-62x6"]
tags: ["docs", "attached", "pdf", "document", "dashboard"]
created: 2026-08-26T00:13:21+09:00
status_since: 2026-08-27T09:16:13+09:00
updated: 2026-08-28T16:17:07+09:00
---

## Original

아키텍트 조사(2026-08-26)에서 실증된 읽기 경로 결함. REQ-054/-055가 done이지만 실제 한글 PDF는 한 글자도 읽히지 않는다 — 테스트가 직접 만든 장난감 PDF만 검증했기 때문.

결함 4건 (bin/s9 _pdf_text):
1. hex 문자열 <...> 미처리 — 정규식이 (...) 리터럴만 본다. 한글 PDF는 사실상 전부 Identity-H + hex라 원리적으로 0바이트 추출.
2. /ToUnicode CMap 미적용 — 서브셋 폰트 glyph id → 유니코드 역매핑이 없다.
3. stream…endstream 비탐욕 매칭 — 바이너리에 endstream 바이트열이 있으면 그 뒤 본문이 통째로 잘린다. /Length 기반으로 교체해야 한다.
4. 첫 8MB만 읽는다 — 첨부 상한은 30MB. 상한과 어긋난다.

부수 결함: TJ 배열(커닝)에서 단어가 쪼개져 나온다, 임베드 폰트 테이블 이름(glyf/head/hmtx)이 본문으로 오탐돼 태그를 오염시킨다.

추가 요구: 추출한 텍스트가 지금은 태그 3개 유도에만 쓰이고 버려진다(attach_tags가 유일한 소비자). 편집·인용의 전제는 본문이 남는 것 — 보존 위치를 함께 정하라.

검증 조건: 장난감 PDF가 아니라 실물 3종(한글 워드 출력 / 스캔본 / 웹 인쇄본)을 첨부해 통과시킬 것.

## Notes

### 2026-08-26T12:50:21+09:00 subagent (agent: sub:subagent:a170bf65, by nicehugepark)

Checking REQ-20260826-001-62x6 blocker status

### 2026-08-26T19:25:45+09:00 subagent (agent: sub:subagent:af2afdd7, by nicehugepark)

Writing TDD scenarios to REQ-20260826-001-62x6

### 2026-08-26T19:26:34+09:00 tdd (by nicehugepark)

### TDD 시나리오 (착수 전 작성 — backend-developer)

### Red 실증 (수정 전, 실물)
`/mnt/c/User_guide/User_guide_korean.pdf` (삼성 한국어 사용설명서, 1,094,152 B,
PDF-1.4 + **XRef 스트림 + ObjStm 22개**) 를 현재 `_pdf_text` 로 추출:
```
len: 8000  hangul: 0
'ko www .s amsung . c om/ sec ko ko ko ko 2 ko ko ??\x00\t ko ...'
tags: ['attached', 'pdf', 'document', 'docs', 'index']
```
**1MB 한글 PDF에서 한글 0자.** 8000자 상한이 쓰레기로 다 차서 태그까지 오염된다.
REQ 본문의 "0바이트"가 실물로 재현됐다.

### 재현 자산 (장난감 PDF 금지)
- **A. 실물 한글 문서**: 위 삼성 사용설명서. 머신 종속 경로라 테스트는 있으면
  검증하고 없으면 skip. 이 머신의 결과는 노트에 남긴다.
- **B. 실물 웹 인쇄본**: `chrome.exe --headless --print-to-pdf` 로 한글 HTML을
  인쇄해 생성(Chrome 실물 출력물 — Identity-H + hex + ToUnicode + XRef 스트림).
- **C. 실물동형 생성기(포터블 픽스처)**: 테스트 안에서 Word/Chrome과 **같은 구조**를
  stdlib로 합성 — Identity-H CID 폰트 · hex 문자열 `<...>` · TJ 커닝 배열 ·
  ToUnicode CMap(bfchar/bfrange) · FlateDecode · **ObjStm + XRef 스트림** ·
  임베드 FontFile2. 장난감 PDF와의 차이는 구조를 실물과 같게 만든다는 것.
- **D. 스캔본**: 텍스트 0, 이미지 스트림만 있는 PDF를 합성. OCR은 비지원
  (DOC-20260826-003-62x6 C등급)이라 기대값은 "본문 없음"이고, 검증 포인트는
  **이미지 바이너리가 본문/태그로 새지 않는 것**이다.

### 시나리오
- [ ] **S1 실물 한글 PDF**: A를 `attach_text` -> 한글 200자 이상, 사용설명서 어휘가
      실제로 등장. (지금 0자)
- [ ] **S2 hex 문자열**: `<D55CAD6D> Tj` 만 있는 페이지 -> "한글" 복원. 리터럴
      `(...)` 이 한 개도 없는 PDF에서 본문이 나온다.
- [ ] **S3 ToUnicode CMap**: bfchar 단건 · bfrange 구간형 · bfrange 배열형 ·
      surrogate pair(4바이트 UTF-16BE) 4종 모두 올바르게 복원.
- [ ] **S4 ToUnicode 부재 폴백**: ToUnicode 없는 Identity-H PDF(우리 자체 생성기
      산출물 = DOC-20260826-003-62x6 의 "자기 폐루프 실패") -> 임베드 FontFile2 의 cmap
      역매핑으로 한글 복원. 폴백 불가여도 쓰레기를 뱉지 않는다.
- [ ] **S5 endstream 오절단**: 스트림 내부 바이너리에 `endstream` 바이트열을 심은
      PDF -> `/Length` 기반 절단으로 뒤 본문까지 온전히 추출.
- [ ] **S6 8MB 컷**: 9MB PDF의 8MB 이후에 놓인 본문이 추출된다 — 상한은
      `ATTACH_MAX_BYTES`(30MB)와 일치.
- [ ] **S7 TJ 커닝**: `[(sec) -3 (tion9 ) 12 (dep) 5 (loy)] TJ` -> `section9 deploy`
      로 이어 붙는다. 큰 음수 조정값만 공백으로 복원.
- [ ] **S8 폰트 바이너리 오탐**: 임베드 폰트가 있는 PDF의 본문·태그에
      `glyf`/`head`/`hmtx`/`gasp`/`loca` 가 하나도 없다.
- [ ] **S9 스캔본**: D -> 본문이 비고, 이미지 바이너리 문자열이 태그에 섞이지 않는다.
- [ ] **S10 회귀**: REQ-054/-055 가 검증하던 평문 `(...) Tj` PDF와 zip·strings 경로가
      그대로 통과한다.
- [ ] **S11 방어**: 잘린 PDF · `/Encrypt` 암호화 PDF · 0바이트 파일 -> 예외 없이
      `""` 또는 부분 결과. 어떤 입력에도 attach 경로가 죽지 않는다.
- [ ] **S12 비용 상한**: 1MB 실물 PDF 추출이 초 단위로 끝난다(첨부 인제스트가 동기
      경로라 지연이 사용자에게 그대로 보인다). 측정치를 노트에 남긴다.

### 범위 밖 (별건 분리)
"추출 텍스트 보존 위치"(REQ 본문 추가 요구)는 첨부 메타 스키마 + 인제스트 쓰기 경로
변경이라 `attach_text` 인근 수정과 분리한다. S1~S12 는 **읽기 정확도**만 닫고,
보존은 결론 노트에서 설계안으로 제시한다.

### 2026-08-26T19:41:50+09:00 response (agent: sub:backend-developer, by nicehugepark)

### 결론: 원인은 "스트림을 전부 풀어 (...) 만 긁는" 방식 자체였다 — 객체 구조를 따라가는 추출기로 교체했고, 실물 한글 PDF에서 한글 0자 -> 4,341자가 나온다

### 원인 (실물로 재현)
지목된 결함 4건은 모두 실재했고, 뿌리는 하나였다. 기존 `_pdf_text` 는 **PDF를
문서가 아니라 바이트 더미로 봤다** — 모든 `stream…endstream` 을 풀어 `(...)` 만
정규식으로 긁었다. 실물 한글 PDF는 예외 없이 Identity-H CID 폰트 + hex 문자열이고,
PDF 1.5+(Word·Chrome·Acrobat 기본)는 폰트/페이지 딕셔너리를 **압축 객체스트림
(ObjStm)** 에 숨긴다. 그래서 hex 를 읽어도 glyph id 일 뿐이고, glyph id를 글자로
되돌릴 ToUnicode 로 가는 길(페이지 -> 리소스 -> 폰트 -> ToUnicode)이 애초에 없었다.
**원리적으로 0바이트**였다는 아키텍트 진단이 그대로 맞다.

수정 전 실측 (`/mnt/c/User_guide/User_guide_korean.pdf`, 삼성 한국어 사용설명서
1,094,152 B, PDF-1.4 + XRef 스트림 + ObjStm 22개):
```
len: 8000  hangul: 0
'ko www .s amsung . c om/ sec ko ko ko ko 2 ko ko ??\x00\t ko ...'
tags: ['attached','pdf','document','docs','index']     <- 쓰레기가 8000자 상한을 다 먹는다
```

### 수정 (bin/s9, attach_text 인근 / 의존성 0 유지 — stdlib re·zlib·struct 만)
객체 표를 만들고 **페이지 -> 리소스 -> 폰트 -> ToUnicode** 를 실제로 따라간다.
- `_pdf_scan` : `N 0 obj` 스캔으로 객체 표 (xref 를 신뢰하지 않는 복구 방식 —
  XRef 스트림의 PNG predictor 역필터를 자작하지 않아도 되고 손상 PDF에서 더 건진다)
- `_pdf_expand_objstm` : ObjStm 을 펼친다 (**이게 없으면 1.5+ 에서 폰트에 닿지 못한다**)
- `_pdf_stream` : `/Length`(간접 참조 포함) 우선 절단, 못 믿으면 endstream 후보를
  **zlib 해제 성공 여부로 검증**해 고른다 — 결함 3(바이너리 속 endstream 오절단) 해소
- `_pdf_tounicode` : bfchar · bfrange(구간형/배열형) · surrogate pair — 결함 2 해소
- `_pdf_ttf_reverse` : ToUnicode 가 없으면 임베드 FontFile2 의 cmap(4/12)을 역매핑.
  **DOC-20260826-003-62x6 이 기록한 "자기 폐루프 실패"(우리가 만든 한글 PDF를 우리가 못
  읽는다)가 이걸로 닫혔다**
- `_pdf_tokens` : 컨텐츠 스트림 토크나이저 — hex `<...>` 를 dict `<<` 와 구분(결함 1),
  8진 이스케이프·중첩 괄호 처리
- `_pdf_page_text` : **y 좌표가 실제로 바뀔 때만** 줄바꿈. Td/Tm 마다 끊으면
  "T oUnicode" 처럼 단어가 쪼개진다. TJ 조각은 이어 붙이고 큰 음수 조정값(<= -150)만
  공백 — 부수 결함(커닝 단어 분리) 해소
- 매핑 없는 CID 는 **버린다** — 폰트/이미지 바이너리를 본문으로 흘리지 않는다(태그 오염 해소)
- 읽기 상한 8MB 하드코딩 -> `ATTACH_MAX_BYTES`(30MB) — 결함 4 해소
- `_pdf_loose_text` : 페이지 트리를 못 찾은 파일(비정상/조각 PDF)만 타는 최후 수단.
  폰트·이미지 스트림은 건너뛴다

### 검증
**실물 3종** (`python3 tests/ pdf_text` 11/11 green, `tests/ assets` 28/28 green,
`tests/ project_assets` 7/7, `tests/ stdlib` 2/2)
| 표본 | 성격 | 결과 |
|---|---|---|
| 삼성 한국어 사용설명서 1.09MB | 실물, XRef 스트림 + ObjStm, ToUnicode | 한글 **0 -> 4,341자**, 0.32s |
| Chrome `--headless --print-to-pdf` 한글 인쇄본 87KB | 실물 웹 인쇄본, 고전 xref + 서브셋 폰트 | 한글 104자, 문장 온전, 0.01s |
| shared-mime-info-spec.pdf 148KB | 실물 ASCII, ObjStm | 본문 정상(회귀 없음), 0.01s |
| 자체 생성 한글 PDF(ToUnicode 없음) 7.4MB | DOC-003 의 폐루프 실패 표본 | 한글 복원 성공, 0.15s |
| 합성 스캔본(이미지 전용) | OCR 비지원 경계 | 본문 0 + 이미지 바이너리 유출 없음 |

**red 재현으로 테스트의 값을 증명했다** — 같은 스위트를 수정 전 리비전에 물리면
(`S9_PDF_BIN=<HEAD의 s9> python3 tests/ pdf_text`) **11개 중 8개 fail**. REQ-054/-055 가
done 을 통과한 이유가 이 red 가 없었기 때문이다.

E2E: 격리 vault(`S9_ROOT=mktemp`)에 실물 1MB 한글 PDF를 첨부 -> `ingest_assets`
0.32s, 태그가 **실제 한글 본문**에서 유도됨(폰트 테이블 이름 오염 0).

### TDD 시나리오 결과
- [x] S1 실물 한글 PDF (4,341자) · [x] S2 hex · [x] S3 ToUnicode 4종 · [x] S4 폰트 역매핑
- [x] S4b 매핑 없으면 침묵 · [x] S5 endstream 함정 · [x] S6 8MB 너머 · [x] S7 TJ 커닝
- [x] S8 폰트 테이블 이름 0 · [x] S9 스캔본 · [x] S10 회귀 · [x] S11 방어(잘림/암호화/빈 파일)
- [x] S12 비용 1.09MB -> 0.32s (수정 중 O(n^2) 누적 2건을 찾아 제거: 8.6s -> 0.33s)

### 남은 한계 (열어둔 채로 명시)
1. **스캔 PDF는 여전히 0자다** — OCR 은 DOC-20260826-003-62x6 C등급(비지원). 결함이 아니라 경계다.
2. **암호화(/Encrypt) PDF도 0자** — 같은 이유로 비지원.
3. **실물 스캔본 표본을 이 머신에서 구하지 못해** 합성 이미지 전용 PDF로 대체했다
   (검증 포인트는 "이미지 바이너리가 태그로 새지 않는가"이므로 합성으로 충분하다고 판단).
4. 같은 y 좌표의 표 셀은 공백 없이 붙을 수 있다(BT/ET 경계는 공백 처리). 키워드
   추출 목적에는 무해.
5. `/Differences` 커스텀 인코딩 단순 폰트는 latin-1 근사다 — 실물 3종에서 문제 없었다.
6. **후속 관찰**: 한글 본문이 살아났는데도 태그는 `derive_tags` 어휘에 걸리는 것만
   나온다(사용설명서에서 dashboard/session). 태그 어휘의 한국어 대응은 별건 감이다.

### 추가 요구(추출 텍스트 보존)는 분리 등록
**REQ-20260826-020-62x6 (첨부 본문 보존)** 으로 등록하고 이 문서를 parent 로 걸었다.
권고: 추출 본문은 **파생물**이므로 문서 본문/front-matter 가 아니라
`assets/<문서ID>/.text/<파일명>.txt` 사이드카 + `catalog.jsonl` 발췌로 두고
언제든 재추출로 재생성한다(1MB에 0.32s 이므로 동기화 대상에서 제외 권고).

### 재현 자산 첨부 (실물 웹 인쇄본)
[File: assets/REQ-20260826-001-62x6/web_print.pdf]

### 2026-08-26T19:42:26+09:00 response (agent: sub:backend-developer, by nicehugepark)

### 재현 자산을 테스트에 물렸다 (보강)
첨부한 Chrome 한글 인쇄본을 `tests/test_pdf_text.py` 가 직접 읽는다
(`vault/requests/2026/08/assets/REQ-20260826-001-62x6/web_print.pdf`) — **실물
PDF가 스위트 안에 상주**하므로 다른 머신·다음 사람에게도 같은 red/green 이 재현된다.
"장난감 PDF만 봐서 done 이 됐다"는 이 결함의 재발 경로를 구조적으로 막는 조치다.

최종: `python3 tests/ pdf_text` **12/12 green**, 수정 전 리비전에 물리면
**12개 중 9개 fail**. 변경 파일은 `bin/s9`(추출 영역만)과 신규
`tests/test_pdf_text.py` 둘뿐이다.

### 2026-08-26T19:43:26+09:00 response (agent: sub:backend-developer, by nicehugepark)

### 정정: 재현 자산은 `tests/fixtures/` 에 커밋한다 (vault/ 는 .gitignore 대상)
문서 첨부(vault/)만으로는 다른 머신에 자산이 가지 않는다 — `.gitignore` 가
`vault/` 를 통째로 막는다(로컬 인스턴스 데이터, REQ-20260824-046). 그대로 두면
다른 머신에서는 합성 픽스처만 돌고, **이 결함을 done 으로 통과시킨 "장난감 PDF만
검증" 상태로 되돌아간다.** 그래서 Chrome 한글 인쇄본을
`tests/fixtures/web_print_ko.pdf`(87KB, 우리가 생성 — 저작권 무관)로 커밋하고
테스트가 이걸 읽는다. 문서 첨부본은 사람이 보는 증거로 그대로 둔다.

변경 파일 3개: `bin/s9`(추출 영역), `tests/test_pdf_text.py`(신규),
`tests/fixtures/web_print_ko.pdf`(신규 실물 픽스처).

### 2026-08-26T19:43:57+09:00 tdd (agent: sub:backend-developer, by nicehugepark)

### TDD 시나리오 — 결과 (전부 green, `python3 tests/ pdf_text` 12/12)
수정 전 리비전(`S9_PDF_BIN=<HEAD의 s9>`)에 같은 스위트를 물리면 **12개 중 9개 fail** —
red 를 재현해 테스트가 결함을 실제로 잡는다는 것을 증명했다.

- [x] S1. 실물 웹 인쇄본(Chrome 한글 출력, tests/fixtures/web_print_ko.pdf): 문장 온전 복원
- [x] S1b. 실물 한글 사용설명서 1.09MB(XRef 스트림+ObjStm): 한글 0 -> 4,341자, 0.32s
- [x] S2. hex 문자열 `<...>` 만 있는 PDF에서 본문 복원 (리터럴 `(...)` 0개)
- [x] S3. ToUnicode CMap: bfchar · bfrange 구간형 · bfrange 배열형 · surrogate pair
- [x] S4. ToUnicode 부재 -> 임베드 FontFile2 cmap 역매핑으로 복원 (DOC-003 폐루프 해소)
- [x] S4b. 매핑이 아예 없으면 쓰레기 대신 빈 문자열
- [x] S5. 스트림 바이너리 속 `endstream` 함정: /Length 기반 절단으로 뒤 본문까지 생존
- [x] S6. 8MB 하드코딩 제거 — 9MB PDF의 8MB 너머 본문 추출, 상한은 ATTACH_MAX_BYTES(30MB)
- [x] S7. TJ 커닝 배열에서 단어가 쪼개지지 않는다 (`sec tion9` -> `section9 deploy`)
- [x] S8. 임베드 폰트 테이블 이름(glyf/head/hmtx/gasp/loca)이 본문·태그에 0건
- [x] S9. 스캔본(이미지 전용): 본문 0 + 이미지 바이너리가 태그로 새지 않음 (OCR 비지원 경계)
- [x] S10. 회귀 — REQ-054/-055 가 보던 평문 `(...) Tj` 경로 유지 (tests/ assets 28/28)
- [x] S11. 방어 — 잘린 PDF · /Encrypt · 0바이트 파일에서 예외 없이 ""/부분 결과
- [x] S12. 비용 — 1.09MB 추출 0.32s (작업 중 O(n^2) 누적 2건 제거: 8.6s -> 0.33s)

미구현으로 남긴 시나리오 없음. 범위 밖으로 분리한 "추출 텍스트 보존"은
REQ-20260826-020-62x6 으로 등록했다.

### 2026-08-28T16:16:53+09:00 response (by nicehugepark)

### 승인 후속 감사 — 질문 없음 · 후속 작업 없음 (재확인만)

승인 메모는 리드의 판정 근거 기록이지 질문도 지시도 아니었다. 세 문장 각각을 현재
상태와 맞춰 확인했고, 새로 열 일이 없다고 판정한다.

- **'첨부 사이드카에 한글 104자 포함 312자'** — 사이드카 보존은 REQ-20260826-020-62x6
  (첨부 본문 보존, done)이 닫았다. 이 문서가 parent 다.
- **'화면 노출은 REQ-20260827-005-62x6 에서 이어졌다'** — 그 REQ 는 -020 의 child 로
  걸려 있다. 계보가 이미 이어져 있어 추가 등록이 불필요하다.
- **'한계(스캔·암호화 PDF 는 0자)는 원래 선언된 경계'** — DOC-20260826-003-62x6 C등급
  그대로. 결함이 아니므로 후속 REQ 로 만들지 않는다.

**회귀 재확인(이 워크트리)**: `python3 tests/ pdf_text` **12/12 green** (0.551s).
실물 픽스처 `tests/fixtures/web_print_ko.pdf` 를 읽는 S1 과 머신 종속 실물
사용설명서를 읽는 S1b 를 포함해 전부 통과 — 승인 시점의 결과가 유지되고 있다.

코드 변경 없음. 상태는 done 유지.

### 2026-08-28T16:17:07+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-08-28 16:17:07 KST - lead]`

승인 메모를 확인한 결과 **질문도 후속 지시도 아니었습니다** — 리드가 승인하며 남긴 판정 근거 기록입니다. 메모의 세 문장을 현재 상태와 대조했습니다:

- **"첨부 사이드카에 한글 104자 포함 312자"** — 사이드카 보존은 이미 자식 REQ-20260826-020-62x6(첨부 본문 보존)이 done으로 닫았습니다.
- **"화면 노출은 REQ-20260827-005-62x6 에서 이어졌다"** — 그 REQ는 -020의 child로 계보에 이미 걸려 있습니다. 추가 등록할 것이 없습니다.
- **"한계(스캔·암호화 PDF는 0자)는 원래 선언된 경계"** — DOC-20260826-003-62x6 C등급 그대로 수용. 결함이 아니므로 REQ화하지 않습니다.

따라서 새 REQ를 만들지 않았고, `--label answer` 노트도 남기지 않았습니다(답할 질문이 없음).

**회귀만 재확인했습니다**: 이 워크트리에서 `python3 tests/ pdf_text` → **12/12 green** (0.551s). 실물 픽스처 `tests/fixtures/web_print_ko.pdf`를 읽는 S1과 머신 종속 실물 사용설명서를 읽는 S1b 포함 전부 통과 — 승인 시점 결과가 유지되고 있습니다.

감사 공백이 남지 않도록 위 판정을 `s9 note REQ-20260826-001-62x6 --label response`로 문서에 기록했습니다. 코드 변경이 없어 워크트리는 clean이고, 커밋할 것이 없습니다. 상태는 done 유지 — 사용자가 눌러 판정할 것이 없으므로 review로 되돌리지 않았습니다.

## History
- 2026-08-26T00:13:21+09:00 created by nicehugepark (status: open)
- 2026-08-26T19:19:37+09:00 status: open -> in-progress (by nicehugepark) — drag 이동 [via dashboard]
- 2026-08-26T19:44:08+09:00 status: in-progress -> review (by nicehugepark) — 읽기 결함 4건 모두 닫힘 — 실물 한글 PDF 0자 -> 4,341자(0.32s), tests/ pdf_text 12/12 green(수정 전 리비전에 물리면 9 fail로 red 재현). 확인 포인트 ① 실물 3종 중 스캔본만 이 머신에 표본이 없어 합성 이미지 전용 PDF로 대체한 것(검증 포인트가 '이미지 바이너리가 태그로 새지 않는가'라 합성으로 충분하다 판단) ② OCR·암호화 PDF는 여전히 0자 — DOC-20260826-003-62x6 C등급 경계를 그대로 두는 것 ③ 추가 요구(추출 텍스트 보존)를 REQ-20260826-020-62x6으로 분리하고 '문서가 아니라 파생물(사이드카+카탈로그)'로 권고한 것 ④ 실물 픽스처 87KB를 tests/fixtures/ 에 커밋하는 것 — vault/는 gitignore라 문서 첨부만으로는 다른 머신에서 사라진다
- 2026-08-27T09:16:13+09:00 status: review -> done (by nicehugepark) — 리드 판정. 실물 확인: 첨부 사이드카에 한글 104자 포함 312자가 실제로 들어 있고, 그 글자가 s9 search --body 로 찾힌다. 화면 노출은 REQ-20260827-005-62x6 에서 이어졌다. 한계(스캔·암호화 PDF 는 0자)는 원래 선언된 경계 그대로다.
