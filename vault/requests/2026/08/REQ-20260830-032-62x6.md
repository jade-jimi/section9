---
id: REQ-20260830-032-62x6
type: request
title: 단추를 아이콘으로 줄이기
summary: 이어가기·중단하기 글자 단추를 play/pause 아이콘(▶·⏸꼴)으로 바꾸는 안 — 카드가 뚱뚱해져 한 화면 카드 수가 준다는 문제의식. designer 합당성 검토
goal: designer 검토로 아이콘 채택/기각/절충이 근거와 함께 판정되고, 채택 시 4역 검토를 거쳐 반영된다
status: review
size: S
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: 9854e394
sessions: ["9854e394", "21804fd6"]
agents: ["sub:designer:ac5b4851", "sub:subagent:a712b42d", "lead:claude-fable-5"]
contributions: [{"actor": "sub:designer:ac5b4851", "item": "032 아이콘 단추 검토", "started": "2026-08-30T17:11:12+09:00", "ended": "2026-08-30T17:39:37+09:00", "result": "done", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/9854e394-1479-4180-9021-a3ed4bdae40f/tasks/ac5b48517a74428c2.output"}, {"actor": "sub:subagent:a712b42d", "item": "Appending designer note to REQ-20260830-032-62x6", "started": "2026-08-30T17:19:51+09:00", "ended": "2026-08-30T17:19:51+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/9854e394-1479-4180-9021-a3ed4bdae40f/subagents/agent-a712b42d6def10846.jsonl"}, {"actor": "sub:designer:ac5b4851", "item": "`[2026-08-30 17:45:38 KST - designer]`", "started": "2026-08-30T17:39:37+09:00", "ended": "2026-08-30T17:39:37+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/9854e394-1479-4180-9021-a3ed4bdae40f/subagents/agent-ac5b48517a74428c2.jsonl"}, {"actor": "lead:claude-fable-5", "item": "재작업 검증(리드, 무인 적용 모드): 아이콘 구현이 본 저장소에 적용돼 있음을 확인 — web/app/card.js(face", "started": "2026-08-30T17:59:23+09:00", "ended": "2026-08-30T17:59:23+09:00", "result": "done", "transcript": ""}]
project: section9
relates_why: {}
tags: ["auto-audit", "dashboard", "design", "attached", "png", "image"]
created: 2026-08-30T17:10:38+09:00
status_since: 2026-08-30T17:59:29+09:00
updated: 2026-08-30T17:59:29+09:00
priority: 50
---

## Original

그리고 세우고 깨우고 관련 디자인을 일반적인 play, pause 버튼 (영어 글자 버튼이 아니고, 세모와 이퀄사인을 세워놓은것)을 사용해보는건 어때? 너무 카드 크기가 뚱뚱해져서 한 화면에 많은 카드를 보여주기 어려워지는 것 같아서 하는 말이다. 디자이너 다시 검토 해봐. 합당한지 아닌지.

## Notes

### 2026-08-30T17:19:34+09:00 designer (by nicehugepark)

### 판정: 아이콘 단독 교체는 기각. 절충 — 밀도 결함 수정이 먼저, 아이콘은 2단계에서만.

### ① 실측 (1152×900, skin=calm, density=normal, 열 폭 224px, 가시 영역 560px)

멈춤 카드 197.1px의 내역 — padding 27 / id줄 22 / 제목 21.8 / 메타줄 45 /
멈춤 줄 42.4 / **손잡이 줄 37 (27px + margin 10px)** / elapsed 14.
같은 열의 손잡이 없는 카드는 119.8px. 멈춤 상태가 얹는 77px 중 손잡이 줄이 37px(48%).

네 시안을 같은 마크업·같은 CSS로 세워 재었다(첨부 variants.png):

| 시안 | 카드 높이 | pitch | 단추 | 560px에 몇 장 |
|---|---|---|---|---|
| A 현재(글자·별도 줄) | 205.1 | 216.1 | 64.6×27 | 2 |
| B 아이콘 단독·별도 줄 | **205.1** | 216.1 | 36×27 | **2** |
| C 아이콘·줄 합침 | 171.8 | 182.8 | 31×22 | 3 |
| D 아이콘+글자·별도 줄 | 205.1 | 216.1 | 79.6×27 | 2 |

**B가 A와 1px도 다르지 않다.** 단추 높이는 낱말 길이가 아니라 padding(3px)+라인박스가
정하고, 손잡이는 자기 줄을 통째로 차지한다 — 아이콘화는 폭만 줄인다(64.6→36).
사용자가 지목한 "뚱뚱함"의 원인은 단추의 **글자**가 아니라 **줄 하나의 존재**다.

### ② 진짜 원인은 따로 있다 — density=compact 가 calm 스킨에서 죽어 있다

같은 카드를 skin=ledger·density=compact 로 세우면 205.1 → **134.1 (-35%), 2장 → 4장**.
아이콘화(0%)·줄 합침(-16%) 어느 쪽보다 크다. 그런데 calm 에서는 compact 를 켜도
카드가 205.1 그대로다 — `web/css/calm.css:176` 의
`[data-skin="calm"] .card{padding:14px 16px 13px}` 이 `density.css:9` 의
`[data-density="compact"] .card{padding:5px 10px}` 와 특이도 동급인데 뒤에 실려 덮는다
(t/m 글자 크기도 같은 자리에서 덮인다: calm.css:187,189).
사용자가 이미 가지고 있는 밀도 손잡이가 지금 스킨에서 작동하지 않는 것이 실원인이다.
설계 문서의 "밀도 변형은 skin이 아니라 density 축" 규칙이 스킨 CSS에 깨져 있다.

### ③ 아이콘 단독을 기각하는 근거 (HIG·이 화면의 일관성)

1. **높이 절감 0** — 위 실측. 얻는 것 없이 뜻만 잃는다.
2. **세 번째 얼굴을 전송 은유가 못 감당한다.** 손잡이는 세 낱말이다 —
   「이어가기」·「중단하기」·「끝났는지 확인」(commit drift, card.js:128). 앞의 둘만
   ▶/⏸ 로 바꾸면 같은 ▶ 가 "이어가기"와 "끝났는지 확인" 두 뜻이 되고, 그 구분은
   같은 줄의 「고친 것 있음」이 지는데 그 줄은 ellipsis 라 **뒤부터 잘린다**
   (card.js:115~119 이 이미 경고해 둔 자리다). 근거가 잘린 채 뜻이 바뀌는 손잡이가 된다.
3. **⏸ 는 없는 약속을 한다.** 「중단하기」의 툴팁은 "지금 하던 일이 거기서 끝나고,
   중단한 사유가 문서에 남습니다" — 이건 pause 가 아니라 stop 이다. 전송 문법에서
   pause 는 같은 지점에서 이어짐을 뜻하는데, 여기서 이어가기는 작업자를 새로 띄운다.
   ⏸ 를 쓰면 사용자가 "잠깐 멈췄다 그 자리에서 재개"로 읽는다.
4. **같은 줄의 다른 단추와 종이 갈린다.** `.acts` 에는 「승인 done」·「반려 in-progress」·
   「→ done」이 선다. REQ-20260828-007-62x6 이 다섯 라운드에 걸쳐 "이름은 mono, 행위는
   본문체, 한 줄 한 문법"으로 맞춘 자리다. 여기만 글리프를 들이면 같은 슬롯에 종이 셋이 된다.
5. **타깃이 더 작아진다.** 27px → 22px(합침 시). 44px 권고에서 멀어지고, 카드 전체가
   문서로 가는 클릭 대상이라 오조작이 곧 원치 않는 이동이다. 「중단하기」는 도는
   작업을 죽이는 반쯤 파괴적 행동이라 라벨 없는 글리프 뒤에 두지 않는다(HIG).
6. **낱말은 사용자가 직접 고른 것이다** (REQ-20260829-024-62x6 라운드4).

아이콘이 값을 하는 자리는 딱 하나 — **줄을 합칠 때의 폭**이다. 합친 줄에서 글자 단추는
71px, 아이콘은 31px 을 먹고, 그 40px 차이만큼 사실 줄("· 마지막 17:10", "· 고친 것 있음")이
더 살아남는다(첨부 v3.png C 대 E). 높이가 아니라 폭의 논거다.

### ④ 권고 순서

1. **calm 스킨의 density 무력화를 고친다** — `[data-skin="calm"]` 블록이 padding·font-size 를
   못박은 자리를 density 축이 이길 수 있게(`[data-skin="calm"][data-density="compact"]`
   대응 값 추가). 한 줄도 안 지우고 -35%, 열당 2장 → 4장.
2. 그러고도 모자라면 **손잡이 줄을 멈춤/진행 중 줄에 합친다**(-16%, 2장 → 3장). 이때만
   아이콘을 쓰되 title + aria-label 필수, 「끝났는지 확인」은 글리프로 옮기지 말고
   글자를 유지한다(세 번째 뜻은 전송 문법 밖이다).
3. 아이콘 단독으로 세 손잡이를 전부 갈아엎는 안은 채택하지 않는다.

검증: Windows Chrome headless 1152×900/1000×760, 실제 DOM(`?stall=40`)을 뜯어 같은
CSS 번들(/css/all.css)로 다시 세워 getBoundingClientRect 로 실측. 코드는 고치지 않았다.

[Image: assets/REQ-20260830-032-62x6/v3.png]
[Image: assets/REQ-20260830-032-62x6/variants.png]

### 2026-08-30T17:19:51+09:00 subagent (agent: sub:subagent:a712b42d, by nicehugepark)

Appending designer note to REQ-20260830-032-62x6

### 2026-08-30T17:30:41+09:00 designer (by nicehugepark)

### 구현: calm 이 삼킨 density 축을 되돌렸다 (권고 1 실행, 권고 2는 손대지 않음)

### 고친 것 — web/css/calm.css 한 파일, +56줄, 지운 줄 0

파일 끝에 `[data-skin="calm"][data-density="compact"]` 블록을 얹었다. 특이도 0,3,0
이라 calm 의 기존 선언(0,2,0)을 순서와 무관하게 이긴다 — density.css 를 뒤로 옮기는
길은 택하지 않았다(그러면 다른 스킨의 normal 값까지 compact 가 넘본다).
calm 의 언어(라운드·그림자·카드 문법)는 건드리지 않았다. **여백과 글자만** 줄인다 —
밀도는 재질을 바꾸는 축이 아니다.

덮은 자리: `.board` gap · `.col` 폭 288→212 · `.cards` gap · `.card` padding
14/16/13→7/10/6 · `.t` 15→12.5px · `.m`·알약(태그·크기·담당자·우선순위) 11→9.5px ·
`.id` 10→9px · `.rvpt`·`.rvcap` · `.acts`·`.acts button` · `.judge` · `.colempty` ·
`.elapsed`/`.pickdoc`(카드 아래 모서리에 절대 배치라 여백이 줄면 함께 따라와야 한다) ·
보드 밖의 `main`·`.doclist .row`·`.metatbl td`(카드만 고치면 Docs 탭은 여전히
compact 를 모르는 화면이 된다).

알약들은 자기 font-size 를 따로 들고 있어 `.m` 만 줄여서는 따라오지 않았다 — 함께
눌러야 줄 높이가 실제로 준다. 9.5px 는 density.css 가 이미 쓰는 compact 값이라
새 눈금을 만들지 않았다.

### 실측 (1152×900, skin=calm, ?stall=40, 같은 DOM·같은 CSS 번들, getBoundingClientRect)

| | normal | compact | |
|---|---|---|---|
| 멈춤 카드 | 197.1px | **137.6px** | **-30.2%** |
| 보통 카드 | 119.8px | 88.1px | -26.5% |
| in-progress 열에 온전히 보이는 카드 | **2장** | **4장** | 캡처로 확인 |

normal 은 197.1px 그대로다 — 고치기 전과 1px도 다르지 않다(회귀 없음). 다른 스킨은
선택자가 `[data-skin="calm"]` 로 묶여 있어 닿지 않는다.

첨부 셋: normal 보드(2장) · compact 보드(4장) · compact Docs 목록(보드 밖도 따라온다).
잘림·겹침·모서리 이탈 없음, 콘솔 오류 없음.

### 회귀 시험 — tests/test_calm_density.py (새로 씀)

낱값을 박지 않고 **계약**을 지킨다: density.css 가 compact 에서 어떤 선택자의 어떤
속성을 줄여 놓았는데 어떤 skin 이 같은 선택자의 같은 속성을 덮었다면, 그 skin 안에
`[data-skin="X"][data-density="compact"]` 짝이 있어야 한다. 낱값이 바뀌어도 시험은
안 바뀌고, 새 skin·새 override 가 같은 사고를 내면 그 자리에서 빨개진다.

시험이 진짜로 무는지 확인했다 — calm 의 compact `.card` 한 줄을 지우자
`{('calm', '.card'): ['padding']} != {}` 로 실패, 되돌리자 green.

    python3 tests/ calm_density                                  2 tests OK
    python3 tests/ card_edge_clip review_clamp review_hover calm_density   28 OK
    python3 tests/ shot_blank doc_tidy wake_handle               35 OK
    python3 tests/ card board_counts                              9 OK

### 남은 빚 (시험의 KNOWN 목록에 이름으로 적어 뒀다 — 줄어들기만 하는 목록)

같은 사고가 다섯 스킨에 더 있다: cork(.card·.col h2) · grid(.card·.t·.col·.col h2·
.doclist .row·.viewer·main) · slate(.card·.t·body) · field(main) · terminal(body).
이번 요청의 범위(calm)를 넘으므로 고치지 않고 목록에 남겼다 — 별도 REQ 감이다.

권고 2(손잡이 줄을 멈춤 줄에 합치기)는 지시대로 손대지 않았다.

[Image: assets/REQ-20260830-032-62x6/before_normal.png]
[Image: assets/REQ-20260830-032-62x6/after_compact.png]
[Image: assets/REQ-20260830-032-62x6/docs_compact.png]

### 2026-08-30T17:39:24+09:00 designer (by nicehugepark)

### 마무리 — 캡션 되돌림 한 건, 전체 스위트는 리드 실행에 넘김

구현 뒤 스스로 한 자리를 되돌렸다. compact 에서 `.rvcap` 을 8.5px 로 줄였던 것을
**9.5px 그대로 둔다.** calm 의 그 글자는 이미 `--faint` 인데, 그 잉크는 캡션으로
쓰기엔 옅다고 한 번 반려된 값이다(REQ-20260825-081-62x6). 게다가 인라인이라 줄
높이(10px × 1.45) 안에 들어가므로 줄여도 카드가 1px 도 안 준다 — 얻는 것 없이
읽기만 나빠지는 교환이었다. 좁힌 것은 글자가 아니라 캡션과 사실 사이의 간격이다.

최종 실측(1152×900, skin=calm, ?stall=40):
멈춤 카드 197.1px → **137.9px (-30.0%)** · normal 은 197.1px 그대로(회귀 없음) ·
in-progress 열에 온전히 보이는 카드 2장 → 4장.

표적 시험: `python3 tests/ calm_density card_edge_clip review_clamp` **24 OK**
(그 밖에 shot_blank·doc_tidy·wake_handle 35 OK, card·board_counts 9 OK).

전체 스위트는 돌리지 않았다. `tests/ --changed` 는 `bin/s9` 가 바뀌어 있어 전체로
폴백하는데, 리드가 오후 변경 전체를 덮는 전체 병렬 실행을 이미 돌리고 있어 같은
시간에 둘이 돌면 포트·부하 경합으로 간헐 실패가 늘어난다. 내가 띄운 실행은 세웠고
(프로세스 확인), 앞서 본 `FAILED (failures=1)` 한 건의 정체는 **리드 실행의 실패
목록으로 가른다** — 내 변경은 `[data-skin="calm"][data-density="compact"]` 로만
묶여 있어 다른 스킨·다른 화면에 닿지 않는다.

바뀐 파일은 둘뿐이다: `web/css/calm.css`(+56줄, 지운 줄 0) · `tests/test_calm_density.py`(새 파일).
권고 2(손잡이 줄 합치기)는 지시대로 손대지 않았다.

### 2026-08-30T17:39:37+09:00 subagent (agent: sub:designer:ac5b4851, by nicehugepark)

`[2026-08-30 17:45:38 KST - designer]`

전체 스위트 실행 세웠다 — 프로세스 없음 확인, 대기 루프도 풀었다. 추가 작업 없이 마무리한다.

**결과**: calm 이 삼킨 density 축을 되돌렸다. 멈춤 카드 197.1 → **137.9px (-30.0%)**, in-progress 열에 온전히 보이는 카드 **2장 → 4장**. normal 은 197.1px 그대로 — 회귀 없음. 다른 스킨은 선택자가 `[data-skin="calm"]` 로 묶여 있어 닿지 않는다.

**마지막 자체 반려 한 건**: compact 에서 `.rvcap` 을 8.5px 로 줄였던 것을 9.5px 로 되돌렸다. calm 의 그 글자는 이미 `--faint`(캡션 잉크로 한 번 반려된 옅기)이고, 인라인이라 줄여도 카드가 1px도 안 준다 — 얻는 것 없이 읽기만 나빠지는 교환이었다.

**검증**: `calm_density card_edge_clip review_clamp` 24 OK / `shot_blank doc_tidy wake_handle` 35 OK / `card board_counts` 9 OK. 새 시험은 낱값이 아니라 계약을 문다("skin 이 덮은 density 선언에는 compact 짝이 있어야 한다"), 짝을 지워 실패하는 것까지 확인했다. 남은 빚(cork·grid·slate·field·terminal 의 같은 사고)은 시험의 KNOWN 목록에 이름으로 적어 뒀다 — 별도 REQ 감.

**바뀐 파일 둘**
- `/home/sjpark1/section9/web/css/calm.css` (+56줄, 지운 줄 0)
- `/home/sjpark1/section9/tests/test_calm_density.py` (새 파일)

기록·캡처는 `s9 note REQ-20260830-032-62x6 --label designer` 로 문서에 붙였다. 권고 2(손잡이 줄 합치기)는 손대지 않았다. 상태 전이는 리드 몫으로 남긴다.

### 2026-08-30T17:48:49+09:00 commit (by nicehugepark)

커밋 96fb4c8 — 오후의 판 — 낱말이 결과를 말하고, Enter 가 안전한 쪽에 서고, calm 이 숨을 쉰다

### 2026-08-30T17:56:34+09:00 designer (by nicehugepark)

### 재작업: 손잡이를 글리프로 (오너 판정 채택 — 내 기각 근거는 완화로 바꿔 넣었다)

오너: "밀도 관련 요청이라기보다는 디자인 자체에 대한 건의였는데, 아이콘으로 지정해줘."
방향은 오너 것이다. 내가 기각 사유로 든 위험 넷은 지우지 않고 **각각 막아 두었다.**

### 얼굴과 이름을 갈랐다

`▶`(세모)·`⏸`(세워 놓은 두 획)는 인라인 `<svg>` 다 — 이모지는 폰트마다 다른 그림이
나오고 색이 잉크를 안 따른다. `fill:currentColor` 라 hover 반전에서 배경과 함께
뒤집히고 열 몇 벌의 스킨·톤이 자기 잉크를 그대로 물려준다. 색면 없음.

**낱말은 죽이지 않았다** — 사용자가 고른 「이어가기」·「중단하기」가 `aria-label` 로
낭독기에, `title` 로 얹은 손에 그대로 간다. 상수(WAKE_LABEL·STOP_LABEL)는 그대로라
대화창의 「그대로 두기 / 중단하기」도 한 낱말을 계속 쓴다.

### 위험 넷 → 완화 넷

1. **과녁**: `.deed.ico` 에 `min-width/min-height:27px`. 실측 — calm·ledger ×
   normal·compact 네 조합 모두 **29~31 × 27px**. compact 에서 낱말 단추가 18px 로
   주는 동안에도 27px 을 지킨다(특이도 0,3,1 이 density 를 이긴다): 밀도 축은 여백을
   줄이는 축이지 과녁을 깎는 축이 아니다.
2. **셋째 손잡이**: 「끝났는지 확인」은 **낱말로 남겼다.** ▶ 는 "이어간다"를 말하는데
   그 카드가 청하는 일은 "끝났는지 보라"다 — 같은 그림에 두 뜻을 실으면 둘을 가르는
   것은 바로 위 줄의 「고친 것 있음」뿐인데 그 줄은 ellipsis 라 뒤부터 잘린다.
   ✓ 로 가르는 길도 막혀 있다: 같은 카드의 「승인 done」이 이미 승인의 그림이라
   ✓ 하나에 "승인"과 "확인하러 가기"가 겹친다. 그래서 규칙을 하나로 세웠다 —
   **전송 문법에 있는 것만 글리프, 나머지는 낱말.**
3. **다른 `.acts` 단추와의 문법 충돌**: 규칙이 위계를 만든다 — **판정(사람이 뜻을
   정하는 일) = 낱말**(「승인 done」·「반려 in-progress」), **조작(작업자를 켜고 끄는
   일) = 글리프.** 둘은 서로 다른 줄에 서고(판정은 `.judge` 아래), 이제 줄이 다른
   이유가 모양으로도 읽힌다. 새 컴포넌트는 없다 — `.acts .deed` 를 그대로 입었다.
4. **도는 중**: 낱말이 없으니 숨으로 말한다. 옅히기만 하면 꺼진 단추(=못 누른다)와
   구별이 안 되므로 1.4s 호흡을 준다. `prefers-reduced-motion` 이면 정지 + opacity.
   `aria-label`·`title` 은 「이어가는 중…」·「중단 중…」으로 바뀐다.

### 곁가지로 잡은 버그 하나

`paintWake` 가 눌린 뒤 얼굴을 `WAKE_LABEL` 로 박고 있었다 — 드리프트 카드에서
「끝났는지 확인」을 눌렀다 거절당하면 「이어가기」로 바뀌어 돌아왔다. 이름을 그리는
자리와 되돌리는 자리가 갈라진 탓이다. 이름을 **요소가 들고 다니게** 했다(`data-name`),
칠하는 붓은 `faceDeed` 하나로 합쳤다. `textContent` 로 칠하면 `<svg>` 가 지워지므로
글리프 단추는 이름을 글자가 아니라 `aria-label` 로 갈아 끼운다.

### 시험 — 라벨 문자열을 물던 계약을 옮겼다 (개정 3건, 전부 더 세게)

- `test_wake_handle` / `test_stop_reach`: `class="deed wake"` 완전일치 → `class="deed
  wake[ `$]` 정규식. 무는 것은 낱말이 아니라 **입은 옷**이다(상태 갈래 ico·busy 허용).
- `test_stop_reach` R8: `assertIn("이어가기", row)` 는 이제 **주석 한 줄에도 통과한다** —
  상수(`WAKE_LABEL`)와 `aria-label="${…WAKE_LABEL` 을 물게 바꿨다.
- `test_stall_pair` 의 node 하네스: 낱말·글리프 상수를 베껴 두지 않고 원문에서 떠
  오도록 `_const()` 를 뒀다(베끼면 두 벌이 되고 한 벌만 고쳐진다).

    python3 tests/ wake_handle stall_pair stop_reach commit_drift dialog_safe
                   dialog_voice wake_reach calm_density card_edge_clip   115 OK

### 캡처 넷

calm/normal(▶) · calm/compact(▶, 과녁 유지) · ledger/compact(「끝났는지 확인」 낱말과
⏸ 글리프가 한 화면에) · ledger/normal ?workhold(도는 중 ⏸ 흐려짐).
잘림·겹침 없음, 콘솔 오류 없음. 카드와 문서 화면이 같은 그림을 그리는 것은
stall_pair 의 node 실행이 판정한다.

바뀐 파일: `web/app/card.js` · `web/css/actions.css` · 시험 3건 개정.

[Image: assets/REQ-20260830-032-62x6/ico_calm_normal.png]
[Image: assets/REQ-20260830-032-62x6/ico_calm_compact.png]
[Image: assets/REQ-20260830-032-62x6/ico_drift.png]
[Image: assets/REQ-20260830-032-62x6/ico_busy.png]

### 2026-08-30T17:57:52+09:00 workspace (by nicehugepark)

본 저장소에서 이어받는다 — 화면 작업이라 살아 있는 서버가 읽는 자리에서 해야 한다. 커밋하면 다음 라운드부터 작업자가 다시 자기 워크트리를 받는다.

### 2026-08-30T17:59:23+09:00 response (agent: lead:claude-fable-5, by nicehugepark)

재작업 검증(리드, 무인 적용 모드): 아이콘 구현이 본 저장소에 적용돼 있음을 확인 — web/app/card.js(faceDeed·data-name·인라인 svg ▶/⏸), web/css/actions.css(.deed.ico min 27px 과녁). 표적 스위트 wake_handle stall_pair stop_reach commit_drift dialog_safe dialog_voice wake_reach calm_density card_edge_clip = 115 tests OK (직접 실행, 종료코드 실측). 캡처 4장(ico_calm_normal/ico_calm_compact/ico_drift/ico_busy) 직접 열어 확인: 멈춘 카드 ▶, 도는 카드 ⏸ 흐림, 「끝났는지 확인」은 낱말 유지, 잘림·겹침 없음. 참고: 캡처의 상단 배너대로 서버가 16:01 코드로 돌고 있으나 이 변경은 web/ 정적 파일이라 F5 만으로 보인다.

## History
- 2026-08-30T17:10:38+09:00 created by nicehugepark (status: open)
- 2026-08-30T17:10:58+09:00 status: open -> in-progress (by nicehugepark) — designer 합당성 검토 위임
- 2026-08-30T17:31:16+09:00 status: in-progress -> review (by nicehugepark) — 아이콘(▶·⏸) 제안을 실측으로 검토한 결과: 높이 절감이 0 이라(단추 줄 자체가 높이를 정함) 기각하고, 대신 카드가 뚱뚱했던 진짜 원인 — calm 스킨에서 밀도(간격 좁게) 설정이 안 먹던 결함 — 을 고쳤습니다. F5 후 Settings 에서 스킨 calm + 밀도 compact 로 두면 한 열에 카드가 2장→4장 섭니다. 그리 되면 승인, 아이콘을 그래도 원하시면 그 뜻을 적어 반려해 주세요.
- 2026-08-30T17:48:20+09:00 status: review -> in-progress (by nicehugepark) — 반려: 밀도 관련 요청이라기 보다는 디자인 자체에 대한 건의였는데, 아이콘으로 지정해줘 [via dashboard]
- 2026-08-30T17:59:29+09:00 status: in-progress -> review (by nicehugepark) — 요청하신 대로 이어가기·중단하기 단추가 글자 대신 ▶·⏸ 아이콘으로 바뀌었습니다. 아이콘에 손을 얹으면 원래 낱말이 말풍선으로 보이고, 「끝났는지 확인」만은 ▶ 와 뜻이 겹쳐 글자로 남겼습니다. F5 후 보드에서 멈춘 카드의 ▶, 진행 중 카드의 흐려지는 ⏸ 를 눌러 보세요. 모양과 쓰임이 뜻대로면 승인, 크기·모양이 다르게 보이거나 셋째 단추도 아이콘을 원하시면 그 뜻을 적어 반려해 주세요.
