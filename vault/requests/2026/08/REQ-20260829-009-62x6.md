---
id: REQ-20260829-009-62x6
type: request
title: 확인 메시지가 너무 길다
summary: 카드의 review 확인 포인트가 통째로 실려 잘리고, 결국 본문으로 들어가 읽게 된다
goal: 보드 카드의 확인 요청이 세 줄을 넘지 않고, 전문은 문서에서 읽는다
status: review
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: b0cce452
agents: ["sub:designer:a5467476", "sub:ux-writer:a505f2c6", "sub:subagent:a6ce21f1", "sub:ux-writer"]
contributions: [{"actor": "sub:designer:a5467476", "item": "Review card review-note length", "started": "2026-08-29T09:30:42+09:00", "ended": "2026-08-29T10:12:26+09:00", "result": "stalled", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/b0cce452-7815-44d0-8bb7-ce1e7b28c6da/tasks/a546747613cc0a1ef.output", "reason": "202초째 진전 없음 (기준 180초)"}, {"actor": "sub:ux-writer:a505f2c6", "item": "Rewrite review-note copy rules", "started": "2026-08-29T09:31:06+09:00", "ended": "2026-08-29T09:45:18+09:00", "result": "stalled", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/b0cce452-7815-44d0-8bb7-ce1e7b28c6da/tasks/a505f2c6cf677e27d.output", "reason": "200초째 진전 없음 (기준 180초)"}, {"actor": "sub:subagent:a6ce21f1", "item": "Checking REQ-20260829-009-62x6 metadata", "started": "2026-08-29T09:40:37+09:00", "ended": "2026-08-29T09:40:37+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/b0cce452-7815-44d0-8bb7-ce1e7b28c6da/subagents/agent-a6ce21f15d7125957.jsonl"}, {"actor": "sub:ux-writer", "item": "확인 포인트의 길이 규율을 정하고 게이트로 잠갔다. 글 쪽만 맡았다 — 화면이 얼마나 보여줄 것인가는 designer 몫이다.", "started": "2026-08-29T09:41:21+09:00", "ended": "2026-08-29T09:41:21+09:00", "result": "done", "transcript": ""}, {"actor": "sub:designer:a5467476", "item": "`[2026-08-29 10:20:37 KST - designer]`", "started": "2026-08-29T10:29:35+09:00", "ended": "2026-08-29T10:29:35+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/b0cce452-7815-44d0-8bb7-ce1e7b28c6da/subagents/agent-a546747613cc0a1ef.jsonl"}]
project: section9
relates: ["REQ-20260829-008-62x6", "REQ-20260829-011-62x6"]
relates_why: {"REQ-20260829-008-62x6": "둘 다 문서의 내용을 화면이 어떻게 내주는가의 문제 — 하나는 이미지를 글자로만 내주고, 하나는 글을 통째로 내줘 못 읽게 한다", "REQ-20260829-011-62x6": "같은 카드의 아래쪽 — 하나는 확인 포인트가 카드를 넘치게 하고, 하나는 손을 뗀 뒤 표시가 남고 자리가 밀린다"}
tags: ["auto-audit", "assets", "terminal", "attached", "png", "image", "dashboard"]
created: 2026-08-29T09:26:27+09:00
status_since: 2026-08-29T10:37:56+09:00
updated: 2026-08-29T10:37:56+09:00
priority: 50
---

## Original

확인 메시지가 너무 기니까 잘린다. 잘리는게 맞나? 그리고 이렇게까지 길게 보여주는게 맞을까? 너무 길면 결국 가독성이 떨어져서 본문으로 들어가서 보게 되는데 말이야. 디자이너, ux-writer 다시 검토해줘.
[Image: assets/REQ-20260829-009-62x6/20260829T092450-image.png]

## Notes

### 2026-08-29T09:40:37+09:00 subagent (agent: sub:subagent:a6ce21f1, by nicehugepark)

Checking REQ-20260829-009-62x6 metadata

### 2026-08-29T09:41:21+09:00 response (agent: sub:ux-writer, by nicehugepark)

확인 포인트의 길이 규율을 정하고 게이트로 잠갔다. 글 쪽만 맡았다 — 화면이 얼마나 보여줄 것인가는 designer 몫이다.

### 규칙

**300자 · 갈래 3개.** `s9 status <id> review --note` 가 이 선을 넘으면 do_transition 이 거부한다(예외는 `--force`). 상수는 bin/s9 의 `REVIEW_POINT_MAX = 300` · `REVIEW_POINT_BRANCHES = 3`.

**네 문장이면 된다** — ① 무엇이 달라졌나 ② 무엇에 빗댈 수 있나(생략 가능) ③ 어디서 무엇을 눌러 보나(F5·서버 재시작 같은 선행 조작 포함) ④ 무엇이 보이면 승인이고 무엇이 보이면 반려인가.

**들어가지 않는 것** — 원인·경위·고친 방법·파일 이름·곁가지 수정·스킨 전수 확인·캡처 파일 목록·무관한 실패 테스트. 전부 `s9 note <id> '...' --label response` 로 **옮긴다**. 지우라는 뜻이 아니라 자리를 옮기라는 뜻이다: 판정 요청은 판정에 필요한 것만 지고, 근거는 문서가 진다.

**배경도 쓰지 않는다** — 카드는 요약(무엇에 대한 건인가)을 확인 포인트 **위에** 이미 붙여 준다(REQ-20260826-023-62x6). 확인 포인트가 배경으로 시작하면 같은 말이 두 번이다.

### 숫자의 근거

우리가 쓴 것에서 나왔다. 지금까지 쌓인 `-> review` 전이 260건: 중앙값 231자, 평균 344자, 최장 1805자. 300자 이하가 60%(157건), 250자 이하가 54%. 즉 **300자는 새 규율이 아니라 다수가 이미 지키던 선**이다. 반면 카드를 잘리게 한 사고 사례는 전부 그 밖이다(REQ-20260826-039-62x6 1805자·1026자, REQ-20260827-017-62x6 1417자, REQ-20260828-041-62x6 982자). 상한을 중앙값 근처(250자)로 두면 절반이 걸려 --force 우회를 부르고, 400자로 두면 사고의 일부만 거른다.

갈래 3개는 같은 자리에서 나왔다. 260건 중 갈래 4개 이상이 56건이다. 한 판정 요청은 한 결정을 청한다 — ①②③④⑤ 를 한 문단에 욱여넣는 순간 그것은 판정 요청이 아니라 점검 목록이다.

### 선행 규율과의 관계 (대체 아님, 층)

세 겹으로 정리했다. ① **올릴 것을 고른다** — ✓승인/↺반려로 답할 수 있는 것만 review, 사람 손이 필요하면 blocked, 기계로 확인되는 것은 리드가 직접 닫는다. ② **사용자 언어로 쓴다** — 현상 → (낯설면) 비유 → 재현 절차 순, 내부 용어 금지. ③ **길이를 지킨다** — 300자·갈래 3개. 길이는 ①②를 덮지 않고 그 위에 얹힌다. ②의 '현상→비유→절차'는 순서 규칙이지 분량 규칙이 아니었고, 이번에 각 한 문장으로 못을 박았다. ①②는 그동안 리드의 기억에만 있었는데 이번에 PROTOCOL.md 로 옮겨 적어 세 규율이 한자리에서 읽힌다.

### 다시 쓴 예시 (전 → 후)

**REQ-20260828-041-62x6 (982자 · 갈래 5 → 232자 · 갈래 0)**
> 멈춰 있던 카드에 `깨우기` 단추가 생겼습니다. 먼저 `bin/s9 serve --restart` 로 서버를 다시 띄우고, Board 탭 IN-PROGRESS 열에서 `멈춤 …` 줄이 붙은 카드의 `깨우기` 를 눌러 보세요. 깨워지면 점이 주황으로 바뀌고 멈춤 줄이 사라집니다. 못 깨운 이유(이미 붙어 있음·한도 소진·아직 안 멈춤)가 붉은 실패 창으로 뜨면 반려해 주세요 — 고장이 아니라 보통 창이어야 합니다.

덜어낸 것: Docs 탭에도 같은 자리에 같은 글자로 있는지, Tab 으로 닿아 Enter 로 눌리는지, 카드 안 단추들이 Enter 를 빼앗기던 것을 함께 고친 일, 스킨 10벌·촘촘 밀도·어두운 톤 전수 확인. 앞의 둘은 **기계로 확인되는 것이라 리드가 검증해 닫을 몫**이고, 뒤의 둘은 response 노트가 맡을 근거다.

**REQ-20260828-028-62x6 (약 700자 · 갈래 5 → 239자 · 갈래 0)**
> 터미널 대답 속 `web/index.html:4016` 같은 경로가 눌러서 볼 수 있는 글자가 됐습니다. Terminal 탭에서 F5 한 뒤 점선 밑줄이 그어진 경로를 눌러 보세요 — 그 자리에 카드가 펴지고 해당 줄이 밝게 나옵니다. 열리지 않을 경로(`vault/…`·`state/…`)에 밑줄이 서 있거나, 눌러 실패한 뒤에도 밑줄이 남아 있으면 반려해 주세요. 직접 세워 보려면 주소 뒤에 `?codepeek=peek`.

덜어낸 것: 붙여넣기용 코드 상자 안의 경로에 밑줄이 없다는 항목, `+ 앞뒤 더 보기`·`닫기` 설명, `?codepeek=fail`. 하나로 합칠 수 있는 판정 기준("밑줄이 서 있으면 안 될 자리에 서 있는가")으로 묶었다.

**REQ-20260827-079-62x6 (약 460자 · 갈래 2 → 212자 · 갈래 0)**
> 클로드 계정과 모델을 화면에서 갈아 끼웁니다. 맨 위 사용량이 적힌 계정 칩과 터미널 아래 모델 이름을 각각 눌러 보세요 — 창이 뜨고, 줄을 고르면 그 계정/모델로 같은 대화가 다시 시작됩니다. 고른 뒤 대화가 이어지지 않거나 지금 쓰는 줄에 표시가 없으면 반려해 주세요. 세션 없이 창만 보려면 주소 뒤에 `?dlg=account` · `?dlg=model`. F5 후 확인.

덜어낸 것: 창머리에 로그인 메일이 적힌다, 모델 라벨에서 계정 항목이 빠졌다, 위아래 화살표·Enter·Esc 동작, `?dlg=empty`. 키보드 동작은 기계로 확인되는 것이라 사람에게 물을 일이 아니다.

**REQ-20260826-039-62x6 (1805자 · 갈래 6 → 241자 · 갈래 0)** — 현상→비유→절차 순서를 지키면서도 한 문장씩으로 줄인 본보기
> 그래프가 비었을 때 어느 범례를 눌러야 돌아오는지를 화면이 짚어 줍니다. 스위치 세 개가 다 내려가 있는데 어느 것이 빨강인지 안 적힌 서랍이던 것을 고쳤습니다. F5 뒤 Graph 에서 범례 QUESTION 을 끄고 위 종류 칸을 question 으로 바꿔 보세요. 눌러야 할 QUESTION 에 점선 네모가 둘러지고, 엉뚱한 KNOWLEDGE 를 누르면 "켰지만 화면은 그대로" 한 줄이 떠야 합니다. 안 그러면 반려해 주세요.

다시 쓴 넷이 212~241자로, 260건의 중앙값(231자)에 그대로 떨어진다. 상한이 조이는 선이 아니라 원래의 선임을 뜻한다.

### 적은 자리

- `bin/s9` — `REVIEW_POINT_MAX`/`REVIEW_POINT_BRANCHES`/`review_point_faults()`/`REVIEW_POINT_HELP` 신설, `do_transition` 의 note 정규화 직후에 review 진입 가드. 거부 문구가 몇 자를 덜어낼지·어디로 옮길지(`--label response`)·예외(`--force`)를 말한다. 사람 판정 전이(judge)는 막지 않는다 — 승인/반려 메모는 확인 포인트가 아니다.
- `bin/s9` 무인 재작업 프롬프트 2곳 — "확인 포인트 필수" 뒤에 300자·갈래 3개와 네 문장 얼개를 붙였다. 무인 워커가 게이트에 눈감고 부딪히지 않게.
- `CLAUDE.md` 4항 — 리드가 매 세션 읽는 자리.
- `harness/common/PROTOCOL.md` 12항 — "확인 포인트 쓰는 법" 소절, 세 겹을 한자리에.
- `harness/claude/skills/s9-protocol/SKILL.md` — 규칙 + 232자 실예시.
- `harness/claude/skills/browser-verify/SKILL.md` — 화면 작업의 확인 포인트가 가장 길어지므로 designer 가 읽는 자리에도.
- `tests/test_review_point_len.py` — 14건 전부 통과. 통과(232자·갈래 3), 경계(정확히 300자 통과 / 301자 거부 / 개행으로 우회 불가), 실사고 재현(982자 5갈래·짧아도 4갈래·`1) 2) 3) 4)`), 문구 계약(거부가 '옮길 자리'와 '--force'를 말하는가), 예외(--force·판정 메모 면제), 문서-코드 일치(CLAUDE.md·PROTOCOL.md·SKILL.md 에 300자가 있고 코드 상수와 같은가), 선행 규율 잔존(사용자 언어·현상·✓승인/↺반려).

### 남은 것

- `python3 tests/ review` 에서 `test_review_hover` 2건이 실패한다 — skin `calm` 에 `.card:hover` 클램프 해제 규칙이 없다는 것으로, 지금 designer 가 편집 중인 `web/index.html` 몫이다. 이 작업과 무관하고 손대지 않았다.
- 빈 확인 포인트(`--note` 없이 review)는 이번에 막지 않았다. 무인 경로에 미치는 영향이 있어 길이 규율과 분리했다 — 필요하면 별건으로.

### 2026-08-29T09:50:41+09:00 response (by nicehugepark)

카드 몫을 **세 줄**로 정하고, 전문은 문서가 맡게 했다 (표시 쪽 결정 — 문구는 ux-writer 몫).

**잘리는 게 맞다.** 보드 열에서 내리는 결정은 "어느 건을 지금 볼 것인가"이지 판정 자체가 아니다. 판정은 근거 전문을 읽고 내리는 일이고, 그 자리는 문서 뷰어의 확인 요청 callout 이다 — 거기엔 갈래(①②③) 분리·문서 링크·첨부 그림이 이미 다 있다. 폭 210~250px 짜리 열에 스무 줄 문단을 넣는 것은 읽으라고 준 것이 아니라 **있다고 보여준 것**이고, 본문으로 들어가 읽게 되었다는 말씀이 그 증거다.

**원인은 호버였다.** 올려 주신 캡처의 카드는 「이어 말하기」가 떠 있다 — 마우스가 얹힌 상태다. calm 스킨에 "카드에 마우스를 얹으면 3줄 클램프를 풀고 높이를 60em 까지 연다"는 규칙이 있었고(REQ-20260825-071-62x6 때 "호버라도 내용을 보고 싶다"는 요청에 답한 것), 그것이 스무 줄 카드를 만들어 위아래가 화면 밖으로 나갔다. 그 규칙을 걷어냈다: 카드 높이가 포인터 위치에 따라 변하면 아래 카드가 통째로 밀리고, 마우스를 떼면 읽던 글이 사라진다.
그리고 클램프 자체가 **calm 한 벌에만** 있었다 — ledger·glass·terminal·field·cork 등에서는 확인 포인트가 통째로 실려 review 열이 글자 벽이었다.

고친 것 넷:
① 확인 요청·대기 사유 본문을 **세 줄**에서 자른다. 베이스 CSS 라 스킨 열 벌 전부에 적용된다. 배경 줄(무엇을) 2줄 + 확인 요청 3줄 = 판정 블록 다섯 줄 상한 → 카드 높이에 천장이 생겨 열의 리듬이 서고, 승인·반려 버튼이 긴 글에 밀려나지 않는다.
② 잘렸을 때만 그 아래 한 줄 손잡이가 뜬다. 누르면 문서로 간다(카드 클릭과 같은 목적지). 말줄임표는 "끊겼다"만 말하고 나머지가 어디 있는지는 말하지 않는다. 색면·테두리 없는 mono 글자 + hover 밑줄 — 보드가 이미 쓰는 `+ N개 더 보기` 와 같은 문법이다.
③ 잘렸는지는 **재서** 안다. 글자 수로 짐작하면 스킨·밀도·열 폭마다 틀린다. 문턱은 px 상수가 아니라 줄 높이(반 줄)로 잡았다 — 클램프가 자르면 최소 한 줄이 남으므로 그보다 작은 차이는 반올림이지 잘림이 아니다.
④ calm 의 blocked 카드에서 글자 한 줄이 말줄임표도 없이 **가로로 반 잘려** 있던 것을 함께 고쳤다. 호버 전개를 여닫으려고 있던 max-height 가 padding 과 겹쳐 클램프와 싸우고 있었다.

**택하지 않은 안 둘.**
· 카드 안에서 펼치기(더 보기 → 카드 안 전개): 되돌리기는 쉽지만 펼친 결과가 정확히 이번에 신고하신 화면이고, 210px 폭에서 스무 줄을 읽는 일 자체가 나쁘다.
· 호버 미리보기 카드: 마우스를 유지해야 하고 키보드·터치로는 닿지 않는다. 오래 읽고 판정하는 글에 맞지 않는다.
채택안의 비용은 클릭 한 번이고, 그 클릭은 이미 하고 계시던 동작이다.

**눈으로 확인한 것** (첨부 순서대로): ① ledger 1440px — review 열이 글자 벽에서 카드로 돌아왔고 승인·반려가 모두 카드 안에 있다 ② calm 1440px — blocked 카드의 반 잘린 줄이 없어지고 세 줄 + 말줄임표로 끝난다 ③ ledger 2560px 넓은 창 — 열이 넓어져 세 줄에 다 들어가는 카드에는 손잡이가 붙지 않는다(폭에 따라 다시 잰다) ④ ledger 900px + 촘촘 밀도 — 좁고 촘촘해도 세 줄과 버튼이 유지된다. 그 밖에 terminal·glass·field·cork·paper(밝은 톤)에서도 같은 모양을 확인했다.

계약은 tests/test_review_clamp.py 에 잠갔다(10건). 호버 전개를 요구하던 옛 계약(tests/test_review_hover.py)은 "접었으면 펼 수단을 짝으로 준다"는 살아 있는 요구만 남기고 수단을 손잡이로 바꿔 다시 썼다 — 두 요청의 이력이 그 파일 머리에 순서대로 적혀 있다. board/card/review/judge/skin 관련 164건 회귀 통과.

**곁가지 한 건**(이번 건과 무관, 리드 판단용): 문서 화면에서 "서버가 옛 코드로 돌고 있습니다" 띠가 뜬 동안 문서 판 높이가 그 띠만큼 짧게 잘린다 — 판 높이를 재는 시점(sizeDocs)이 띠보다 빨라서다.

[Image: assets/REQ-20260829-009-62x6/fix-1-board-ledger.png]
[Image: assets/REQ-20260829-009-62x6/fix-2-board-calm.png]
[Image: assets/REQ-20260829-009-62x6/fix-3-wide-nohandle.png]
[Image: assets/REQ-20260829-009-62x6/fix-5-compact.png]

### 2026-08-29T10:29:35+09:00 subagent (agent: sub:designer:a5467476, by nicehugepark)

`[2026-08-29 10:20:37 KST - designer]`

세 건 다 고쳤다. 커밋은 안 했다.

**REQ-20260829-009-62x6 「확인 메시지가 너무 길다」** — 잘리는 게 맞다고 판정했다. 보드 열에서 내리는 결정은 "어느 건을 지금 볼 것인가"이지 판정 자체가 아니고, 전문을 펴는 자리는 문서 뷰어의 확인 요청 callout(갈래 분리·문서 링크·첨부 그림이 이미 다 있다)이다. 정작 사용자가 본 스무 줄 카드의 원인은 **calm 스킨의 호버 전개**였다 — 캡처의 카드는 「이어 말하기」가 떠 있어 마우스가 얹힌 상태였고, "얹으면 3줄 클램프를 풀고 60em 까지 연다"는 규칙(REQ-20260825-071-62x6 때 넣은 것)이 그 화면을 만들었다. 그 규칙을 걷어내고 확인 요청·대기 사유를 **베이스 CSS 에서 3줄**로 잘랐다(전에는 calm 한 벌에만 클램프가 있어 ledger·glass·terminal 등에서는 문단이 통째로 실렸다). 잘렸을 때만 그 아래 한 줄 손잡이가 뜨고 누르면 문서로 간다 — 잘림 여부는 글자 수로 짐작하지 않고 실측하되 문턱을 줄 높이로 잡았다(1px 상수로는 반올림을 못 거른다). calm blocked 카드에서 글자 한 줄이 말줄임표도 없이 가로로 반 잘려 있던 것도 함께 잡았다.

**REQ-20260829-011-62x6 「카드가 손을 뗀 뒤에도 그대로다」** — **멈춘 hover 가 아니다.** `?pick=` 으로 세워 사용자 캡처를 그대로 재현했다: 「이어 말하기」로 집어 둔 카드의 모습이고, 그렇게 읽힌 이유는 집힘의 손잡이 규칙이 hover 규칙과 글자 그대로 같았기 때문이다(`color:var(--text); text-decoration:underline`). 지속 상태를 찰나 상태의 재료로 그린 것. 이제 집힌 카드도 손잡이는 얹거나 포커스했을 때만 뜨고, 집힘은 **낱말**(`대상`, 잉크)로 말하며, **경과시간이 사라지지 않는다**. 리드가 물은 ②는 실측으로 답했다 — 셋이 한 줄에 설 폭이 없어(id 97 + 손잡이 56 + 시각 57 ≈ 226px > 199px) 나눌 축은 픽셀이 아니라 **시간**이다: 찰나의 교대는 남기고 지속 점유만 없앴다. 키보드 포커스 쪽은 리드 말씀대로 남겼다. 곁가지로, 시각 자리를 비우려다 좁은 창에서 식별자가 `REQ-20260829-` / `010` 으로 쪼개진 것을 flex + nowrap 으로 잡았다.

**REQ-20260829-012-62x6 「문서 선택이 내 뜻과 다르다」** — REQ-20260828-009-62x6 의 못 박기는 유지하고 경계만 그었다: **못 박기는 "내가 고른 것"이 아니라 "지금 오른쪽에 열려 있는 것"의 표시다.** 그 줄이 REQUEST 머리글 위에 이름 없이 서 있어 목록의 1번으로 읽혔던 것이라, 이 목록이 이미 쓰는 머리글 문법으로 `지금 보는 문서` 한 줄을 앞세우고 그 안에 `닫기` 를 넣었다. 되살아난 선택도 여전히 맨 위에 선다(오른쪽 판이 그것을 그리고 있는데 목록이 숨기면 두 판이 다른 말을 한다 — 009 가 고친 결함). 닫으면 주소에서도 빠져 새로고침에 되살아나지 않는다. **Esc 는 쓰지 않기로 했다** — 이 화면에서 Esc 는 떠 있는 것(호버카드·미리보기·대화상자)을 닫는 키이고, 문서 선택은 판의 상태다. 같은 키에 두 층을 얹으면 호버카드를 닫으려던 Esc 가 읽던 문서까지 되돌리기 없이 지운다.

시험은 셋 다 빨간 것을 먼저 확인하고 고쳤다: `/home/sjpark1/section9/tests/test_review_clamp.py`(10), `/home/sjpark1/section9/tests/test_pick_state_look.py`(9), `/home/sjpark1/section9/tests/test_doc_pin_release.py`(10). 옛 결정을 담고 있던 시험 둘은 이력을 적어 다시 썼다 — `test_review_hover.py`(호버 전개 요구 → "접었으면 펼 수단을 짝으로 준다"로), `test_doc_title_anchor.py` 한 줄(글자 그대로의 못 박기 → "그룹보다 먼저 선다"로). 전체 1798건 중 실패는 리드가 진행 중인 `BARE_ASSET_RE`(REQ-008) 하나뿐이다.

캡처는 세 REQ 문서에 각각 4장씩 붙여 뒀다(ledger·calm·grid·glass·terminal·cork, 밝은 톤과 어두운 톤, 좁은 창과 넓은 창). 못 본 것 둘은 노트에도 적었다: 진짜 마우스 hover 와 `닫기` 실제 클릭은 헤드리스에 손이 없어 확인하지 못했다(둘 다 배선은 기존 손잡이와 같은 규칙).

한 가지 알려 둔다 — 이번 건과 무관한 결함을 봤다. **"서버가 옛 코드로 돌고 있습니다" 띠가 뜬 동안 Docs 의 문서 판 높이가 그 띠만큼 짧게 잘린다.** 판 높이를 재는 `sizeDocs` 시점이 띠보다 빨라서다(첨부 캡처들에서도 아래가 잘려 있다). 리드가 판정할 자리로 남긴다.

## History
- 2026-08-29T09:26:27+09:00 created by nicehugepark (status: open)
- 2026-08-29T09:28:21+09:00 status: open -> in-progress (by nicehugepark) — 카드에 실린 확인 포인트가 길어 잘리고, 결국 본문으로 들어가 읽게 된다. 디자이너·ux-writer 에 검토를 맡긴다 — 얼마나 보여줄 것인가와 무엇을 쓸 것인가 두 몫.
- 2026-08-29T10:37:56+09:00 status: in-progress -> review (by nicehugepark) — 판정 대기 카드에 확인 요청이 통째로 실려 스무 줄까지 늘어나던 것을 세 줄로 잘랐습니다. 마우스를 얹어도 더 펼쳐지지 않습니다. F5 후 Board 의 review 열 카드를 보세요. 세 줄로 접히고 그 아래 문서로 가는 손잡이가 있으면 승인, 여전히 문단이 통째로 실리거나 손잡이 없이 잘리기만 하면 반려입니다.
