---
id: REQ-20260827-065-62x6
type: request
title: 압축 중을 화면에도
summary: 로컬 터미널이 컨텍스트를 압축하는 동안 대시보드 터미널도 같은 상태를 보인다
goal: 압축이 시작되면 대시보드 터미널에 그 상태가 보이고 끝나면 사라진다
status: done
size: S
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: 3da5b9de
project: section9
relates_why: {}
tags: ["dashboard", "terminal", "attached", "png", "image"]
created: 2026-08-27T21:55:32+09:00
status_since: 2026-08-27T23:31:50+09:00
updated: 2026-08-27T23:31:50+09:00
priority: 50
---

## Original

사용자: "로컬 터미널에서 컨텍스트가 컴팩션 되는 중을 대시보드 터미널에서도 인지하고 똑같이 보여줄 수 없나?" (QST-20260827-030)

압축 중에는 응답이 한동안 멈춘다. 대시보드만 보는 사람에게는 그것이 "멎었다"와 구분되지 않는다 — 침묵의 이유를 말해 주지 않으면 고장으로 읽힌다.

## Notes

### 2026-08-27T23:29:57+09:00 tdd (by nicehugepark)

### TDD 시나리오 — 계약 8건 전부 통과

- [x] ① 서버가 주는 `compacting` 이 화면까지 온다 — "컨텍스트 압축 중".
- [x] ② `live` 를 덮지 않는다. 압축 중에도 세션은 살아 있으므로 상태줄에서
      live 자리를 빼앗지 않고 그 옆에 선다.
- [x] ③ 침묵이 생기는 자리(출력 판 끝)에 응답 대기 줄과 **같은 어휘**로 선다 —
      스피너 · 하는 일 · 경과. 새 어휘를 만들지 않는다.
- [x] ④ 색으로만 가르지 않는다 — 낱말을 함께 적는다. 색 하드코딩 없이 터미널
      팔레트(--cc-*) 토큰만.
- [x] ⑤ 경과가 **초 단위로** 흐른다. 대상 감시는 5초 박자라 그 박자로 적으면
      5초씩 건너뛰어 오히려 굳은 것처럼 보인다.
- [x] ⑥ 끝나면 줄도 표시도 타이머도 남지 않는다. 대상이 사라져도 마찬가지.
- [x] ⑦ 타임라인을 다시 붙이는 동안에도 살아남는다 — `termClearOut` 이 압축
      줄까지 쓸어 버리면 압축이 도는 내내 화면이 다시 침묵한다. (실제로 이
      결함이 있었다: 붙박이 줄 예외에 압축 줄이 빠져 있어 첫 캡처에서 안 나왔다.)
- [x] ⑧ 위로 올려 읽는 중이면 화면을 끌어내리지 않는다 — 자동 따라가기와 같은
      잣대(`termAtBottom`)를 쓴다. 진단 파라미터 `?compacting`.

실행: `python3 tests/ compacting_visible` 8 OK.

### 2026-08-27T23:29:57+09:00 designer (by nicehugepark)

두 자리에 세웠다. 하나면 부족하다.

**침묵이 생기는 자리 — 출력 판 끝.** `✻ 컨텍스트 압축 중 (2s)`. 응답 대기 줄과 **같은 어휘**다(스피너 · 하는 일 · 경과). 압축이 도는 동안 세션은 한동안 말이 없는데, 그 침묵이 생기는 바로 그 자리에 기다림의 표시가 서야 "멎었다"가 아니라 "무언가 하고 있다"로 읽힌다. 경과를 함께 보이는 이유는 정적인 문구가 멈춘 것처럼 읽히기 때문이다 — 그리고 초는 초 단위로 센다(대상 감시 박자인 5초로 적으면 숫자가 5초씩 건너뛰어 오히려 굳어 보인다).

**늘 보이는 자리 — 상태줄.** `▶▶ live · 압축 중 · target …`. 출력을 위로 올려 읽는 중이면 판 끝의 줄은 화면 밖이라 안 보인다. 여기서 **live 를 덮지 않는다** — 압축 중에도 세션은 살아 있고, live 를 압축으로 바꿔 쓰면 "죽었나"로 읽힌다. 초록 live 는 그대로 두고 노랑 "압축 중"이 그 옆에 붙는다. 색으로만 가르지 않도록 낱말을 함께 적었다.

끝나면 둘 다 흔적 없이 사라진다.

고치면서 잡은 결함 하나: 타임라인을 다시 붙일 때 `termClearOut` 이 출력 판의 자식을 응답 대기 줄만 빼고 전부 지운다 — 압축 줄도 함께 쓸려 나갔다. 첫 캡처에서 표시가 안 나와 알았다. 붙박이 줄 예외에 압축 줄을 넣었다.

검증: 진단 파라미터 `?compacting` 으로 헤드리스에서 직접 봤다.

[Image: assets/REQ-20260827-065-62x6/compact4.png]
[Image: assets/REQ-20260827-065-62x6/compact-status.png]

## History
- 2026-08-27T21:55:32+09:00 created by nicehugepark (status: open)
- 2026-08-27T21:57:47+09:00 status: open -> in-progress (by nicehugepark) — 뒤쪽(훅·판정·API) 완료, 화면 표시는 디자이너 큐
- 2026-08-27T23:29:58+09:00 status: in-progress -> review (by nicehugepark) — 확인 포인트: 로컬 터미널에서 컨텍스트 압축이 돌 때 Terminal 탭을 보세요. 출력 맨 아래에 노란 "컨텍스트 압축 중 (경과시간)" 한 줄이 스피너와 함께 뜨고, 아래 상태줄에도 "live · 압축 중"이 나란히 붙습니다. live 는 초록 그대로입니다 — 압축 중에도 세션은 살아 있다는 뜻입니다. 압축이 끝나면 둘 다 사라집니다. 지금 압축이 안 돌아도 http://127.0.0.1:9909/?compacting#terminal 로 그 화면을 볼 수 있습니다. F5 후 확인.
- 2026-08-27T23:31:50+09:00 status: review -> done (by nicehugepark) — 대시보드 승인 [via dashboard]
