---
id: REQ-20260828-009-62x6
type: request
title: 읽는 동안 제목을 잃는다
summary: 문서 본문을 스크롤하면 제목이 사라지고, 왼쪽 목록도 실시간 갱신돼 지금 보는 문서를 못 짚는다
goal: 문서를 아무리 내려도 무엇을 보고 있는지 화면에서 알 수 있다
status: done
size: M
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: e94da575
sessions: ["3da5b9de", "b8fda8f9", "e94da575"]
agents: ["sub:designer", "sub:subagent:a9181943", "sub:designer:a2d809d2"]
contributions: [{"actor": "sub:designer", "item": "화면 몫", "started": "2026-08-28T08:23:39+09:00", "ended": "2026-08-28T08:23:39+09:00", "result": "running", "transcript": ""}, {"actor": "sub:subagent:a9181943", "item": "Appending anchor verification note to REQ-20260828-009-62x6", "started": "2026-08-28T14:49:08+09:00", "ended": "2026-08-28T14:49:08+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/subagents/agent-a9181943d8cd6ab4d.jsonl"}, {"actor": "sub:designer:a2d809d2", "item": "`[2026-08-28 15:33:59 KST - designer]`", "started": "2026-08-28T15:35:05+09:00", "ended": "2026-08-28T15:35:05+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/subagents/agent-a2d809d2c7ae83607.jsonl"}]
project: section9
relates_why: {}
tags: ["dashboard", "audit", "attached", "png", "image"]
created: 2026-08-28T08:20:46+09:00
status_since: 2026-08-28T16:13:28+09:00
updated: 2026-08-28T16:13:28+09:00
priority: 50
---

## Original

사용자(08:20): "문서 본문 보기를 할 때 화면 스크롤을 해서 내용을 보다보면, 본문의 제목을 잊어버린다. 무슨 내용을 보고 있었는지. 스크롤할 때 본문 제목은 앵커를 거는게 어때? 좌측 문서 목록에서 현재 보고 있는 문서를 고정하거나 강제를 해도 되는데 지금 왼쪽 문서 목록이 거의 실시간으로 목록이 갱신이 되어버리니 본문 제목을 캐치하기 어렵다."

캡처로 확인: 메타 표까지 내려가면 제목이 화면 어디에도 없다. 왼쪽 목록에도 지금 보는 문서가 표시돼 있지 않다.

두 갈래다.
1. 본문 제목을 스크롤에 붙인다.
2. 왼쪽 목록에서 지금 보는 문서를 표시하고, 실시간 갱신에 그 자리가 밀리지 않게 한다.

둘 중 하나만 해도 "무엇을 보고 있나"는 풀리지만, 2번은 그 자체로 따로 문제다 — 목록이 발밑에서 움직이면 읽던 자리를 잃는다.

## Notes

### 2026-08-28T08:23:39+09:00 (agent: sub:designer, by nicehugepark)

리드가 designer 서브에이전트에 맡겼다. 같은 web/index.html 을 다른 주체가 함께 고치면 남의 미커밋 작업이 사라진다(REQ-20260826-021-62x6) — 무인 작업자를 겹쳐 띄우지 마라.

### 2026-08-28T09:39:57+09:00 (by nicehugepark)

**전체 스위트가 이 변경 때문에 하나 빨갛다** (REQ-20260827-072-62x6 작업 중 발견, 그쪽과 무관하다).

`python3 tests/` → 1309건 중 `test_docs_recent_order.test_n2_docs_uses_it` 1건 실패. Docs 목록이 `recentOrder(rows)` 대신 `stableOrder(rows, okey, refreeze)` 를 쓰도록 바뀌었는데(이 요청의 '얼음을 깨는 두 조건'), 그 계약 테스트는 아직 옛 이름을 그대로 찾는다:

    tests/test_docs_recent_order.py:47
    assertIn("recentOrder(rows)", …) — 'Docs 목록이 최근순 정렬을 쓰지 않는다'

정렬을 바꿨으면 그 계약도 같이 옮겨야 한다. 지금은 '최근순을 쓰는가'가 아니라 '무엇을 언제 얼리는가'가 계약이다. 나는 이 요청에 손이 붙어 있지 않아 고치지 않는다 — web/index.html 도 다른 주체가 쥐고 있었다.

### 2026-08-28T14:49:02+09:00 response (by nicehugepark)

**두 갈래 다 화면에서 동작한다 — 앞 세션이 만든 것을 실브라우저로 확인했다.**

이 요청에는 이미 `stableOrder(rows, okey, refreeze)` 와 `tests/test_doc_title_anchor.py` 가 들어와 있었다. 내 몫은 코드가 아니라 **눈으로 보는 것**이었다: 코드가 맞아 보여도 렌더 결과는 다르다.

### ① 내려도 제목이 남는다
문서를 900px 내려 메타 표를 지나 NOTES·HISTORY 까지 간 화면(첫 캡처)에서, 판 맨 위에 `읽는 동안 제목을 잃는다 · REQ-20260828-009-62x6 · IN-PROGRESS` 한 줄이 붙어 있다. 붙는 것은 이 한 줄뿐이고(경로 줄은 함께 올라가 사라진다) 판 끝까지 늘어난다.

### ② 왼쪽 목록이 발밑에서 움직이지 않는다
이것은 한 장으로는 증명되지 않는다 — 새로고침할 때마다 순서가 다시 얼기 때문이다. 그래서 **한 페이지를 45초 열어 둔 채** 18초째에 다른 문서(REQ-20260828-017-62x6)에 노트를 붙였다. 목록은 `updated` 내림차순이므로 얼음이 없으면 그 문서가 그룹 맨 위로 튀어 오른다.

두 캡처(0초 · 45초)의 목록 순서가 **글자 하나까지 같다.** 017 은 3번째 자리를 지켰다. 읽던 자리가 그대로 남는다는 뜻이다.

### 지금 보는 문서
목록 맨 위, 그룹 **밖**에 못 박혀 ● 표식과 굵은 제목으로 선다. 그룹 밖인 것이 중요하다 — 조건에 걸러져 목록에 없는 문서를 열어 두었을 때 "이것도 조건에 맞는다"는 거짓말을 하지 않는다. 색면도 세로 띠도 아니고 잉크 표식이다.

### 남은 것 없음
앞 세션 노트가 걱정한 계약 테스트 어긋남(`test_docs_recent_order.test_n2_docs_uses_it` 이 옛 이름 `recentOrder(rows)` 를 찾던 문제)은 이미 새 계약으로 옮겨져 있다. `python3 tests/ doc_title_anchor` 8건 · `docs_recent_order` 5건 모두 통과, 전체 스위트도 통과한다. 손댄 곳 없음.

[Image: assets/REQ-20260828-009-62x6/anchor-scrolled.png]
[Image: assets/REQ-20260828-009-62x6/anchor-frozen.png]

### 2026-08-28T14:49:08+09:00 subagent (agent: sub:subagent:a9181943, by nicehugepark)

Appending anchor verification note to REQ-20260828-009-62x6

### 2026-08-28T14:57:41+09:00 response (by nicehugepark)

**중복 스폰 — 재작업 워커가 한 번 더 떴고, 고칠 것이 없었다.**

14:17 사용자가 보드에서 이 카드를 open → in-progress 로 끌어 옮겼다(사유 텍스트는 "drag 이동"). 워처는 그것을 재작업 신호로 읽고 무인 워커(세션 e94da575)를 띄웠다. 그런데 같은 시각 다른 세션(b8fda8f9)이 이미 이 요청을 붙들고 있었고, 14:49 에 검증을 마치고 review 로 올렸다. 두 세션이 2분 차로 겹쳤다.

**독립 검증 결과 — 앞 세션의 판단과 같다.**

- 계약 테스트: `python3 tests/ doc_title_anchor` 8건 · `python3 tests/ docs_recent_order` 5건 모두 통과. 전체 스위트도 통과(exit 0).
- 실화면(ledger · 기본 톤, 1440x900, 문서를 1600px 내린 상태): 판 맨 위에 `읽는 동안 제목을 잃는다 · REQ-20260828-009-62x6 · REVIEW` 한 줄이 붙어 남는다. 경로 줄은 함께 올라가 사라진다. 왼쪽 목록 맨 위, 그룹 밖에 지금 보는 문서가 ● 표식과 굵은 제목으로 못 박혀 있다.
  캡처: `scratchpad/009-ledger-deep2.png`
- glass 스킨에서도 붙박이 줄이 본문을 가린다(비쳐 보이지 않는다): `scratchpad/009-glass-deep.png`

**검증하지 못한 한 갈래 — soft 스킨.**

soft 는 판이 아니라 쪽 전체가 구르는 셸이다(`[data-skin="soft"] .viewer{max-height:none}`). headless 캡처에서는 쪽 스크롤 상태의 sticky/fixed 요소가 문서 좌표로 그려져 나와, 붙박이 줄이 실제로 창 위에 남는지를 캡처로 판정할 수 없었다. CSS 상으로는 `.viewer` 가 스크롤 컨테이너가 아니게 되어 sticky 의 기준이 뷰포트가 되고, soft 의 헤더는 위쪽 띠가 아니라 왼쪽 세로 사이드바라 `top:0` 이 가려질 일은 없다 — 그러나 이것은 추론이고 눈으로 본 것이 아니다. **사람이 확인할 때 skin 을 soft 로 두고 한 번 더 내려 봐 주면 이 한 갈래가 닫힌다.**

손댄 파일 없음(워크트리 clean, 커밋 없음).

참고: `s9 shot --size` 는 `1440,900` 처럼 쉼표다. `1440x900` 을 주면 Chrome 이 그 값을 버리고 기본 창 크기로 떨어져, `--docsh` 실측이 바닥값 240px 로 내려앉은 화면(목록이 세 줄만 보이는 화면)을 찍는다 — 결함으로 오독하기 쉽다.

### 2026-08-28T15:35:05+09:00 subagent (agent: sub:designer:a2d809d2, by nicehugepark)

`[2026-08-28 15:33:59 KST - designer]`

### 결론

**`approve`/`deny` 로 가지 마라. 지금의 갈라짐(이름 vs 행위)은 **분류로서는 옳다**. 그런데 그 갈라짐을 **글꼴로 말한 것이 틀렸다** — 두 종류는 한 줄에 같이 선 적이 **한 번도 없다**. 글꼴 대비를 볼 기회 자체가 없으니 사용자에게는 "리뷰만 한국어"라는 사실만 남는다. 고칠 것은 낱말이 아니라 **목적지를 버튼 안으로 들이는 것**이다: `승인 done` · `반려 in-progress`.**

---

### 1. 캡처로 본 것 — 코드 주석의 전제가 사실이 아니다

`web/index.html:1137` 의 주석은 이렇게 적혀 있다:

```
같은 줄에 서는 두 종류를 한 글꼴로 그렸다 … 생김새가 다르면 같은 줄에 있어도 헷갈리지 않는다.
```

`GET /api/transitions` 는 `"review": ["done", "in-progress"]` 를 준다. `RVLABEL` 이 그 **둘 다** 덮는다. 즉 **review 문서의 버튼 줄에는 mono 상태 이름이 한 개도 없다.** 캡처로 확인했다:

- review 문서(REQ-20260828-009-62x6): `✓ 승인` `↺ 반려` │ `→ 이어 말하기` — 셋 다 본문체. 대비할 상대가 없다.
- 그 밖의 문서(REQ-20260828-007-62x6): `→ blocked` `→ cancelled` `→ done` `→ review` │ `→ 이어 말하기` — 여기서만 두 글꼴이 만난다.

**글꼴 구분은 같은 시야 안에서만 작동하는 신호다.** 갈라 놓은 두 종류가 서로 다른 화면에 있으면, 사용자는 기억으로 대조해야 한다. 사람은 그러지 않는다 — 그래서 "왜 여기만 한글이지"가 다시 올라온 것이다. 이건 취향 문제가 아니라 **신호가 닿지 않는 자리에 놓인 것**이다.

부수 발견 하나: 두 어휘가 실제로 만나는 유일한 줄(비-review 문서)에서 `→ 이어 말하기` 가 전이 버튼들과 **`→` 글리프를 공유한다.** 글꼴은 달라도 화살표가 같아서 "다섯 번째 목적지 이름"처럼 읽힌다. 헤어라인(`.adiv`)은 1px `--border` 라 그 줄에서 거의 보이지 않는다.

### 2. 전부 영문 상태 이름(`→ done` / `→ in-progress`)으로 통일하면 — 기각

세 가지를 잃는다.

**하나, 무게를 잃는다.** review 에서 나가는 것만 판정이다 — 반려는 사유가 **필수**고, History 에 판정으로 남고, 사람이 문서를 열어 읽고 내린 결정이다. `→ blocked` 옆에 `→ done` 을 나란히 세우면 네 개 중 하나가 된다. s9-design 1절(한 화면 한 결정)이 말하는 그 하나가 사라진다.

**둘, `→ in-progress` 는 반려를 말하지 못한다.** "돌려보낸다"가 아니라 "진행 중으로 옮긴다"로 읽힌다. 게다가 누르면 **사유를 요구하는 창이 뜬다** — 상태를 옮긴다고 적힌 버튼이 글을 쓰라고 요구하는 것은 약속 위반이다.

**셋, 보드와 창이 따라오지 못한다.** 보드 판정 카드(`data-approve`)와 판정 창 문장("…을(를) 승인해 done 상태로 넘깁니다")은 한국어 행위로 서 있다. 문서만 이름으로 바꾸면 **REQ-20260828-007-62x6 3차 반려와 같은 형태의 갈라짐**이 다시 생긴다 — 같은 행동을 화면마다 다른 말로 부르는 것.

### 3. `approve`/`deny` 로 가면 — 기각 (더 나쁘다)

**존재하지 않는 어휘를 새로 만드는 것이다.** 상태머신에 `approve` 는 없고, History 는 `review -> done` 으로 적히고, CLI 도 `s9 status <id> done` 이다. `→ done` 이 영어인 이유는 **그 글자가 다른 데 그대로 박혀 있어서**지 영어라서가 아니다. `approve` 는 어디에도 박혀 있지 않다 — 화면에만 있는 영어가 되고, 그건 지금 한국어가 화면에만 있는 것과 정확히 같은 문제를 영어로 옮긴 것뿐이다.

그리고 **3갈래가 된다**: 보드 카드 `✓ 승인` (한국어) / 문서 `approve` (영어) / 판정 창 "승인하기" (한국어). 셋 다 고치면? 창 안 문장이 `「제목」 을(를) approve 해 done 상태로 넘깁니다` 가 된다 — 조사가 붙지 않는 낱말을 문장 한가운데 끼우는 것이라, 007 이 "상태로"를 끼워 넣어 해결했던 그 문제가 되돌아온다.

정책으로 정리하면 이 제품의 규칙은 **"영어 vs 한국어"가 아니라 "다른 데 박힌 글자 vs 사람이 하는 말"** 이다. `approve` 는 어느 쪽도 아니다.

### 4. 제3안 (권고) — 목적지를 버튼 안으로

두 어휘를 **갈라 놓지 말고 한 버튼에서 만나게 한다.** 판정은 실제로 "행위이면서 동시에 어느 이름으로 착지하는 것"이니, 화면이 그렇게 생기면 된다.

```
승인 done        반려 in-progress        │  → 이어 말하기
```

- 행위는 본문체 600, 이름은 그 뒤에 mono 9.5px `--muted` — 지금의 두 글꼴 규칙을 **버리지 않고 한 자리에 붙인 것**이다.
- "왜 여기만 한글이지"가 **버튼 안에서 해소된다.** 영어 이름이 바로 옆에 있다.
- 지금은 목적지가 **툴팁에만** 있다. 터치·키보드에는 툴팁이 없고, s9-design 1절은 "설명이 필요하면 화면을 고칠 신호"라고 못박는다.
- 판정 창 문장("승인해 done 상태로 넘깁니다")·보드·History 와 **같은 낱말 한 벌**이 된다.

**폭 검증(1x 실측, ledger/paper).** 보드 카드 폭 215px 에서:

| 안 | 215px 보드 카드 | 문서 패널 |
|---|---|---|
| `✓ 승인 done` (글리프 유지) | **두 줄로 감김** (normal density) | 한 줄 OK |
| `승인 done` (글리프 제거) | **한 줄 OK** | 한 줄 OK |
| `✓ 승인` / `done` 두 줄 버튼 | 한 줄 OK, 높이 +14px | OK |
| `in-prog` 축약 | 한 줄 OK | — |

→ **글리프(✓/↺)를 뺀 `승인 done` 을 권한다.** 글리프는 두 버튼을 가르려고 있던 것인데, 이제 `done`/`in-progress` 가 그보다 강하게 가른다. 축약(`in-prog`)은 기각 — 어디에도 없는 글자를 만드는 순간 "같은 이름"이라는 전제가 무너진다.

**같이 고쳐야 할 것 하나.** `→ 이어 말하기` 에서 `→` 를 뗀다. 두 어휘가 실제로 만나는 유일한 줄에서 그 화살표가 "다섯 번째 목적지"로 위장하고 있다. 이건 위 권고와 무관하게 **비용 0으로 지금 참인 결함**이다.

### 5. 대안 비교

| | 되돌리기 | 결정 비용 | 접근성 | 판정 |
|---|---|---|---|---|
| **A. 그대로 두기** | — | 0 | 목적지가 툴팁에만 (터치·키보드 없음) | 사용자 질문이 또 온다 |
| **B. 전부 `→ done`** | 쉬움 | 낮음 | 반려의 무게·사유 요구가 예고되지 않음 | 기각 |
| **C. `approve`/`deny`** | 쉬움 | 중간 | 3갈래 또는 창 문장 붕괴 | 기각 |
| **D. `승인 done` (권고)** | 쉬움(문자열) | 낮음 | 목적지가 화면에 상시 | **권고** |
| **E. `→` 만 떼기** | 매우 쉬움 | 0 | 변화 없음 | D 와 함께, 또는 최소안 |

D 를 택하지 않겠다면 **A + E**(그대로 두고 화살표만 정리)가 차선이다. B·C 는 어느 쪽도 지금보다 낫지 않다.

### 6. 남는 것 (이번 건 아님)

`.acts button` 은 `padding:3px 10px` + 10.5px 라 실높이 ~22px — 터치 타깃 44×44 기준에 한참 못 미친다. 데스크톱 대시보드라 지금 급한 결함은 아니지만, 판정은 **되돌리기 어려운 행동**이라(승인은 done 으로 가고 done 에서 나가는 전이가 없다) 오조작 비용이 가장 큰 버튼이 가장 작다는 점은 기록해 둔다.

---

**검증 수단**: `bin/s9 shot` (WSL→Windows Chrome headless), 포트 9909 실서버. review 문서·비-review 문서 실화면 각 1장, 그리고 `web/index.html` 의 실제 `<style>` 을 그대로 뽑아 재현한 대안 비교 2장(보드 카드 215px / 문서 패널 430px, normal·compact 밀도).

**캡처 경로** (전부 scratchpad, 프로젝트 미변경):
- `/tmp/claude-1000/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/scratchpad/rv2.png` — review 문서 실화면
- `/tmp/claude-1000/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/scratchpad/ip2.png` — in-progress 문서 실화면
- `/tmp/claude-1000/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/scratchpad/cmp.png` — 5개 안 확대 대조
- `/tmp/claude-1000/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/scratchpad/cmp2.png`, `cmp3.png` — 폭 실측

**파일은 하나도 고치지 않았다.** `web/index.html` 은 읽기만 했다.

## History
- 2026-08-28T08:20:46+09:00 created by nicehugepark (status: open)
- 2026-08-28T14:17:00+09:00 status: open -> in-progress (by nicehugepark) — drag 이동 [via dashboard]
- 2026-08-28T14:49:14+09:00 status: in-progress -> review (by nicehugepark) — Docs 탭에서 아무 문서나 열고 아래로 쭉 내려 봐 주세요 (F5 한 번 하신 뒤). (1) 아무리 내려도 판 맨 위에 제목·문서번호·상태 한 줄이 붙어 남습니다. 파일 경로 줄은 함께 올라가 사라집니다 — 읽을 자리를 먹지 않으려고 한 줄만 남깁니다. (2) 왼쪽 목록 맨 위에 지금 보고 있는 문서가 ● 표식과 굵은 제목으로 못 박혀 있습니다. (3) 그 상태로 1분쯤 그냥 두어 보세요. 목록이 15초마다 새로 받아오지만 순서가 다시 섞이지 않습니다. 읽던 자리가 그대로 있어야 합니다. 그 사이에 새로 생긴 문서만 맨 위로 올라옵니다. (4) 검색어나 필터를 바꾸면 그때는 순서가 다시 정해집니다 — 사람이 조건을 바꾼 것이니까요. 여쭐 것: (3)에서 정말 발밑이 안 흔들리는지, 그리고 (1)의 붙박이 줄이 본문을 읽는 데 방해가 되지는 않는지.
- 2026-08-28T16:13:28+09:00 status: review -> done (by nicehugepark) — [via dashboard]
