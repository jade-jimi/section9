---
id: REQ-20260827-001-62x6
type: request
title: 캡처 도구가 화면을 못 맞춘다
summary: s9 shot 이 터미널·그래프에서 무한대기 또는 백지를 성공으로 돌려준다
goal: 캡처 도구가 백지를 성공으로 돌려주지 않는다
status: done
size: M
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: 8e60e4af
project: section9
relates: ["REQ-20260826-029-62x6", "REQ-20260826-039-62x6"]
tags: ["dashboard", "design", "terminal"]
created: 2026-08-27T00:00:43+09:00
status_since: 2026-08-27T09:16:13+09:00
updated: 2026-08-27T09:16:13+09:00
priority: 50
---

## Original

실행: s9 shot 이 대상 화면에 맞춰 SSE 차단·레이아웃 안정 대기를 스스로 하고, 백지에 가까운 캡처는 성공으로 반환하지 않는다. 근거는 노트 참조.

## Notes

### 2026-08-27T00:00:53+09:00 response (by nicehugepark)

designer 가 REQ-20260826-029-62x6·039 작업 중에 발견해 보고한 것이다 — 화면 검증이 규율인데(REQ-20260825-065-62x6) 그 규율을 지키는 도구가 두 화면에서 사람 손을 요구한다.

1. **`#terminal` 은 headless 캡처가 끝나지 않는다.** SSE 연결 때문에 가상 시간이 소진되지 않아 무한 대기한다. `?nosse` 를 붙이면 찍힌다 — 즉 도구가 알아서 붙일 수 있는 것을 사람이 매번 기억해야 한다.
2. **`#graph` 는 기본 대기로 백지가 나온다.** 물리 애니메이션이 아직 자리를 못 잡아서다. `--wait 6000` 이상이면 정상. 여기서 나쁜 것은 실패가 아니라 **백지가 성공처럼 나오는 것**이다 — 오늘 하루 종일 나온 그 패턴("조용한 성공")이고, 실제로 21:56 에 빈 그래프 캡처를 한 번 진짜 결함으로 오인했다.

**요구**: `s9 shot` 이 대상 화면을 보고 알아서 맞춘다. 터미널이면 SSE 를 끄고, 그래프면 레이아웃이 안정될 때까지 기다린다(고정 대기보다, 캔버스가 더 이상 안 바뀔 때까지 재는 편이 낫다). **그리고 백지에 가까운 캡처는 성공으로 반환하지 않는다** — 규율을 지키는 도구가 조용히 거짓을 돌려주면 규율 자체가 무의미해진다.

서버측(`bin/s9`)이라 designer 봉투 밖이었고, 그래서 손대지 않고 보고만 했다 — 옳은 판단이다.

별건 관찰: 전체 스위트 한 번에서 `errors=1` 이 났고 재실행 두 번은 OK 였다. 포트/타이밍 환경성으로 보이나 원인을 특정하지 못했다. 재현되면 별도 문서로 가른다.

### 2026-08-27T00:53:44+09:00 response (by nicehugepark)

셋 다 고쳤다. **핵심은 백지를 성공으로 돌려주지 않는 것**이다.

**1. 대상 화면을 보고 조건을 맞춘다** (`_shot_tune`). `#terminal` 이면 `?nosse` 를 붙이고(SSE 상시 연결이 virtual-time 을 소진시키지 않아 캡처가 끝나지 않는다), `#graph` 면 최소 대기를 6000ms 로 올린다. 이미 붙어 있는 질의 문자열(`?skin=`·`?theme=`)은 보존한다. 나머지 화면은 건드리지 않는다 — 조건을 전부에 걸면 캡처가 느려지고, 느린 도구는 안 쓰이게 된다.

**2. 비어 보이면 한 번 더 기다린다.** 원인은 대개 '덜 그려짐'이므로 사람을 부르기 전에 도구가 한 번 더 해 보는 것이 맞다(대기 ×3, 상한 15000ms). 실제로 그래프는 designer 가 말한 6000ms 로도 여전히 백지였고 재시도에서 그려졌다 — 고정값을 믿을 수 없다는 뜻이다.

**3. 그래도 비어 있으면 실패로 끝낸다** (exit 5). 메시지가 무엇을 하지 말아야 하는지 말한다: *"이 캡처를 '확인했다'의 근거로 쓰지 마라."*

**판정 방법에서 두 번 갈아엎었다.**

처음엔 파일 크기를 봤다. 안 된다 — 어두운 테마의 정상 캡처가 작게 압축돼 백지와 붙는다(바이트 균일도 정상 0.983 vs 백지 0.996).

그래서 줄 단위 균일도로 바꿨는데, 그것도 **인코딩된 바이트를 그대로 봤다.** 이 파일의 B1 테스트가 그걸 잡아냈다 — 합성 PNG(필터 없음)에서 같은 백지가 0.0 으로 읽혔다. **판정이 인코더의 필터 선택에 딸려 있었던 것이다.** 검증이 거짓말하지 않게 만드는 도구가 그런 것에 딸리면 안 된다.

지금은 **PNG 필터를 실제로 되돌린 뒤 픽셀을 본다**(Sub/Up/Average/Paeth 전부). 되돌린 뒤 실측:

    정상  g.png 0.094 · t.png 0.000 · 026-graph 0.121 · 029-real 0.202 · 039-empty 0.089
    백지  blank.png 0.798
    문턱  0.75

바이트만 볼 때는 정상 최악이 0.648 이라 문턱이 아슬아슬했는데, 되돌리고 나니 0.20 대 0.80 으로 벌어졌다. **정확하게 하니까 여유도 함께 생겼다.**

판정하지 못하는 파일(8비트 아닌 것·인터레이스·깨진 PNG)은 `None` 을 돌려 **막지 않는다.** 도구가 길을 막으면 사람이 도구를 끄고, 그 순간 규율도 함께 꺼진다.

**테스트** `tests/test_shot_blank.py` 10건: 백지 판정 4(단색=1.0 · 변화 있으면 문턱 아래 · 문턱이 정상 캡처를 잡아먹지 않음 · 못 읽으면 막지 않음) + 조건 맞춤 6(터미널 nosse · 그래프 대기 · 나머지 불변 · 문자열 wait 강제변환 · 기존 질의 보존 · 백지는 비-0 종료).

문자열 `--wait` 는 실제로 빠뜨려 `'<' not supported between str and int` 로 죽었다 — T4 가 그 자리를 지킨다.

**남은 것**: designer 가 보고한 "전체 스위트 한 번에서 errors=1, 재실행 두 번 OK" 는 재현되지 않아 손대지 않았다. 재현되면 별도 문서로 가른다.

### 2026-08-27T00:53:44+09:00 tdd (by nicehugepark)

- [x] B1. 단색 캡처는 균일행 1.0 으로 읽힌다
- [x] B2. 변화 있는 캡처는 문턱 아래
- [x] B3. 문턱이 정상 캡처를 잡아먹지 않는다 (실측 정상 0.20 · 백지 0.80)
- [x] B4. 판정 못 하면 막지 않는다
- [x] T1. 터미널은 SSE 를 끄고 찍는다
- [x] T2. 그래프는 레이아웃 시간을 받는다
- [x] T3. 나머지 화면은 불변
- [x] T4. 문자열 --wait 가 숫자로 맞춰진다 (실제로 죽었던 자리)
- [x] T5. 기존 질의 문자열 보존 (skin·theme)
- [x] T6. 다시 찍어도 비면 비-0 종료 + "확인했다의 근거로 쓰지 마라"
실화면 확인: #graph(6000ms 백지 → 재시도 15000ms 성공) · #terminal(무한대기 없이 캡처).

## History
- 2026-08-27T00:00:43+09:00 created by nicehugepark (status: open)
- 2026-08-27T00:48:27+09:00 status: open -> in-progress (by nicehugepark) — 리드 착수 — bin/s9 가 비었다
- 2026-08-27T01:06:58+09:00 status: in-progress -> review (by nicehugepark) — 캡처 도구가 이제 거짓말을 하지 않는다. 예전에는 그래프 화면을 찍으면 아무것도 안 그려진 백지가 나오는데도 "성공"으로 끝나서, 그 백지를 보고 진짜 고장이라고 오해한 적이 있다(21:56). 지금은 화면마다 필요한 조건을 도구가 알아서 맞추고(터미널은 실시간 연결을 끄고 찍고, 그래프는 그림이 자리 잡을 시간을 준다), 그래도 비어 보이면 한 번 더 기다렸다가, 끝내 비면 실패로 끝낸다 — "이 캡처를 확인했다의 근거로 쓰지 마라"고 말하면서. 확인은 터미널에서 `bin/s9 shot http://127.0.0.1:9909/#graph --out /tmp/x.png --wait 800` 을 쳐 보면 된다. 대기를 일부러 짧게 줬는데도 조건을 맞추고 재시도해서 제대로 된 그림이 나와야 한다.
- 2026-08-27T09:16:13+09:00 status: review -> done (by nicehugepark) — 리드 판정. 사용자가 쓰는 도구가 아니라 에이전트가 화면을 검증할 때 쓰는 도구다. 실측: 백지 0.798 · 정상 0.094, 문턱 0.75 로 양쪽에 넉넉하다. 실화면에서 그래프가 6000ms 로도 백지였고 재시도에서 그려지는 것, 터미널이 무한대기 없이 찍히는 것까지 확인했다.
