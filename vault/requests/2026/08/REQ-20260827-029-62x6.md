---
id: REQ-20260827-029-62x6
type: request
title: 우선순위 표시가 안 읽힌다
summary: 카드·문서 본문의 우선순위 표시가 보기 불편하고 모르는 사람은 의미를 알 수 없다 — 디자인 재검토
goal: 우선순위 표시를 처음 보는 사람도 무슨 뜻인지 알고, 카드 안에서 눈에 거슬리지 않는다
status: done
size: M
user: sjpark1
machine: DESKTOP-TEHV1KR
session: 3da5b9de
agents: ["sub:subagent:a087b9a8", "sub:designer:a5d9347d"]
contributions: [{"actor": "sub:subagent:a087b9a8", "item": "Attaching captures to REQ-20260827-029-62x6", "started": "2026-08-27T14:04:29+09:00", "ended": "2026-08-27T14:04:29+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/3da5b9de-895e-4435-9e4c-262ae0d58d32/subagents/agent-a087b9a8caee7521a.jsonl"}, {"actor": "sub:designer:a5d9347d", "item": "`[2026-08-27 14:12:31 KST - designer]`", "started": "2026-08-27T14:06:19+09:00", "ended": "2026-08-27T14:06:19+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/3da5b9de-895e-4435-9e4c-262ae0d58d32/subagents/agent-a5d9347de2d7fa5d8.jsonl"}]
project: section9
tags: ["auto-audit", "dashboard", "design", "attached", "png", "image", "docs", "terminal"]
created: 2026-08-27T13:13:48+09:00
status_since: 2026-08-27T16:41:59+09:00
updated: 2026-08-27T16:46:07+09:00
priority: 50
---

## Original

카드나 문서 본문의 우선순위 표시하는 디자인에 대해서 불만이다. 예쁘지 않고, 직관적이라면 직관적이지만, 보기 불편하고 어떤 의미인지 모르는 사람 입장에서는 알수가 없다.

## Notes

### 2026-08-27T13:33:07+09:00 queue (by sjpark1)

designer 가 지금 REQ-20260827-013-62x6(문서 링크 키보드 접근)을 붙들고 있다. 둘 다 `web/index.html` 을 고치므로 동시에 붙이면 서로의 변경을 덮는다 — 013 이 끝나는 대로 같은 에이전트에 넘긴다.

넘길 때 함께 줄 것: 지금 우선순위는 문서 화면에서 `■ 50 보통(normal) · 높을수록 먼저 집는다 (1~99, 기본 50)` 처럼 **설명문까지 한 줄에 붙어** 나오고, 카드에서는 `■ 50` 만 나온다. 사용자 지적 그대로다 — 예쁘지 않고, 처음 보는 사람은 50 이 무슨 뜻인지 알 수 없다.

### 2026-08-27T13:46:08+09:00 tdd (by sjpark1)

### TDD 시나리오 (우선순위 표기 재작업)

세 갈래 결정을 계약으로 고정한다. 기존 test_priority_visible.py 의 계약 두 개가
이번 사용자 불만과 정면으로 부딪히므로(카드에 숫자를 그리라 / 우선순위를 크기보다
앞에 두라) 그 두 건은 사유를 적어 갱신한다 — 나머지 회귀 방지(조건 없는 표기·
단일 파생·단일 정렬·색면 금지·무채색 금지)는 그대로 지킨다.

- [ ] S1 정상(카드): prioHTML(r) 이 등급 낱말(긴급/높음/보통/낮음)을 그리고,
      카드에는 원시 숫자 글자가 없다. data-prio·data-tier 는 그대로 남는다
      (기계·정렬·테스트가 읽는 값은 사라지지 않는다).
- [ ] S2 정상(문서): prioHTML(m, true) 만 사람이 읽는 값을 함께 낸다 —
      `보통 50/99` 형태. 항상 떠 있던 설명문(높을수록 먼저 집는다 / 1~99 /
      기본 50)이 화면에서 사라진다.
- [ ] S3 정상(배움): 문서 뷰어의 우선순위는 hovercard 로 척도를 연다 —
      새 컴포넌트를 만들지 않고 doclink 미리보기와 같은 카드를 쓰고,
      마우스(mouseover)와 키보드(focusin) 두 손 모두에서 열린다.
- [ ] S4 경계(등급 경계): 90/89/75/74/50/49 가 urgent/high/high/normal/normal/low
      로 갈린다 — 파생 함수는 여전히 prioTier() 하나뿐이다.
- [ ] S5 경계(값 없음·범위 밖): priority 필드가 없거나 0·100·"abc" 인 문서도
      기본 50(보통)으로 그려진다. 조건부로 감추지 않는다(REQ-20260826-005-62x6 반려 회귀).
- [ ] S6 자리(카드): 우선순위가 카드 메타 줄의 첫 칸이 아니다 — 담당자(@user)
      뒤에 온다. 크기(size)보다는 여전히 앞이다(무엇부터 집을지가 먼저).
- [ ] S7 자리(목록): Docs 목록 행에서 우선순위가 문서 ID보다 앞서지 않는다.
- [ ] S8 실패(어휘): .prio 계열 CSS 에 색면(background)·세로 띠가 없고,
      규칙이 전부 무채색도 아니다. 게이지 마크(▂▃▅▇)는 어느 글꼴에서 두부로
      떨어지므로 표기에서 제거한다 — 이번 불만의 절반이 그 글자였다.
- [ ] S9 회귀(정렬): 표기를 접어도 정렬·필터는 원래 가중치로 돈다 —
      workOrder 진입점 하나, 1차 키는 prioOf().
- [ ] S10 회귀(스킨): 스킨 10종에서 등급 낱말이 배경에 먹히지 않는다.
      terminal 의 인버스(hover/선택) 위에서도 읽힌다 — 실브라우저 캡처로 확인.

### 2026-08-27T14:04:26+09:00 response (by sjpark1)

### 우선순위 표기 재작업 — 정한 것 셋

**① 숫자를 등급 낱말로 접었다. 값은 잃지 않았다.**
카드·목록이 그리는 글자는 이제 `보통`·`높음`·`긴급`·`낮음` 넷뿐이다. `50` 은 처음
보는 사람에게 아무 뜻이 없었고, 306건이 전부 그 숫자를 달고 있어 눈에 남지도
않았다. 정확한 값은 세 곳에 그대로 있다 — 정렬·필터가 읽는 축(`data-prio`,
workOrder 는 손대지 않았다) · 훑을 때 뜨는 툴팁 한 문장 · 문서를 열었을 때의
`높음 85/99`. 목록은 이미 이 값 순으로 서 있어서, 첫 칸의 숫자 열이 없어도
순위는 자리로 읽힌다.
게이지 마크 `▂▃▅▇` 도 뺐다. 10px 모노 글꼴에서 두부(■)로 떨어져 순위가 아니라
얼룩으로 읽혔다 — "예쁘지 않다"의 절반이 그 글자였다. 색을 못 보는 조건에서
등급을 가르는 채널은 이제 마크 높이가 아니라 낱말 자체다("긴급"≠"보통").

**② 설명문을 지우고, 필요할 때 열리게 했다.**
문서 메타 행에 상주하던 `보통(normal) · 높을수록 먼저 집는다 (1~99, 기본 50)` 을
없앴다. 그 자리는 이제 `높음 85/99` 두 낱말이다 — 등급·값·척도를 한 번에 말하고,
`높음`이 85/99 라는 사실만으로 큰 수가 먼저라는 방향이 읽힌다.
남은 설명(4등급 경계·기본값·바꾸는 명령)은 등급 낱말에 손을 얹거나 Tab 으로 닿으면
카드로 열린다. **새 컴포넌트를 만들지 않았다** — 문서 링크 미리보기와 같은
hovercard 이고, 점선 밑줄도 이 화면이 이미 쓰는 "여기 더 있다"는 약속 그대로다.
배울 것이 하나도 늘지 않는다.

**③ 카드 첫 칸을 담당자에게 내줬다.**
메타 줄이 `@담당자 → 우선순위 → 크기 → TDD → 태그` 순이 됐다. 크기보다는 여전히
앞이다(무엇부터 집을지가 먼저). 대부분인 `보통`·`낮음`은 이웃 메타(크기·태그)와
같은 계조로 물러나고, 눈이 먼저 가야 하는 `높음`(앰버)·`긴급`(레드)만 잉크가
올라온다. Docs 목록 행에서도 문서 ID 뒤로 옮겼다 — 왼쪽 첫 자리를 비웠다.
색면·세로 띠·pill 없음. 잡은 결함 하나: `긴급`에 남아 있던 자간(.07em)이 한글에서
"긴 급" 두 낱말로 끊겨 보여 0으로 되돌렸다(첫 캡처에서 발견).

### 검증
- 테스트: 새 계약 14건(tests/test_priority_legible.py) + 기존 계약 갱신 2군데.
  전체 스위트 896건 OK(기준선 882 + 14).
- 실브라우저: 스킨 10종 × 문서 화면 전수 캡처, 4등급을 한 화면에 세운 램프 캡처,
  키보드 Tab 으로 척도 카드가 열리는 것까지 확인. terminal 스킨의 반전(선택 행)
  위에서도 등급 낱말이 읽히고 포커스 링이 보인다.

[Image: assets/REQ-20260827-029-62x6/n1-board.png]
[Image: assets/REQ-20260827-029-62x6/n2-doc.png]
[Image: assets/REQ-20260827-029-62x6/n3-scale.png]
[Image: assets/REQ-20260827-029-62x6/n4-terminal.png]
[Image: assets/REQ-20260827-029-62x6/f-ramp.png]
[Image: assets/REQ-20260827-029-62x6/before-board.png]

### 2026-08-27T14:04:29+09:00 subagent (agent: sub:subagent:a087b9a8, by sjpark1)

Attaching captures to REQ-20260827-029-62x6

### 2026-08-27T14:04:43+09:00 tdd (by sjpark1)

### TDD 시나리오 — 통과 현황

- [x] S1 카드가 등급 낱말을 그린다(원시 숫자 글자 없음, data-prio 유지)
- [x] S2 문서 뷰어만 `보통 50/99` 로 값을 함께 낸다 (prioHTML(m, true))
- [x] S3 척도는 기존 hovercard 로 열린다 — mouseover·focusin 같은 선택자
- [x] S4 등급 경계 90/75/50 은 prioTier() 한 곳에서만 갈린다
- [x] S5 값이 없거나 범위 밖이면 기본 50(보통)으로 그려진다 — 조건부 표기 없음
- [x] S6 카드 메타 줄 첫 칸은 담당자, 우선순위는 그 뒤(크기보다는 앞)
- [x] S7 Docs 목록 행은 문서 ID 가 먼저
- [x] S8 .prio 계열에 색면 없음 · 전부 무채색도 아님 · 게이지 마크 제거
- [x] S9 정렬은 여전히 원래 가중치로 — workOrder 진입점 하나, 1차 키 prioOf()
- [x] S10 스킨 10종 실브라우저 확인 (terminal 인버스 위 포함)

실행: `python3 tests/ priority` → 43건 OK. 전체 `python3 tests/` → 896건 OK(skipped 2).
기존 계약 갱신 2군데(사유는 각 테스트 주석에):
  · test_card_renders_prio_span — 클래스 문자열이 pfull 로 갈려 여는 따옴표까지만 본다
  · test_priority_visible 모듈 docstring — DOM 계약과 후속 REQ 를 적어 두었다
갱신하지 않은 것: 조건 없는 표기·단일 파생·단일 정렬·색면 금지·무채색 금지 —
REQ-20260826-005-62x6 반려가 세운 계약은 그대로 살아 있다.

### 2026-08-27T14:06:19+09:00 subagent (agent: sub:designer:a5d9347d, by sjpark1)

`[2026-08-27 14:12:31 KST - designer]`

**정했다 (①②③).** ① 카드·목록은 등급 낱말(`긴급/높음/보통/낮음`)로 접고, 원래 가중치는 정렬 축(`data-prio`·workOrder 무변경)·툴팁·문서 뷰어의 `높음 85/99` 세 곳에 남겼다 — `높음`이 85/99라는 사실 하나로 "큰 수가 먼저"가 설명 없이 읽힌다. ② 메타 행에 상주하던 설명문을 지우고, 등급 낱말에 점선 밑줄(doclink 어휘) + **기존 hovercard**로 4등급 경계·기본값·`s9 set --priority` 를 필요할 때만 연다 — 새 컴포넌트 0개, 마우스·키보드 두 손 모두. ③ 카드 메타 줄 첫 칸을 담당자에게 내주고 `@user → 우선순위 → 크기` 순으로, Docs 목록 행도 문서 ID 뒤로 옮겼다. 기본 등급은 이웃 메타와 같은 계조로 물러나고 높음(앰버)·긴급(레드)만 잉크가 올라온다.

**고친 것.** `/home/sjpark1/section9/web/index.html` — `PRIO_TIERS` 를 마크 없는 이름표로, `prioHTML(r, full)` 로 훑는 표기/읽는 표기 분리, `showPrioHover()` + `placeHover()` 로 hovercard 재사용, `.prio` CSS 재작성(색면·세로 띠 없음), calm 스킨 override 정리. 게이지 마크 `▂▃▅▇` 제거 — 10px 모노에서 두부(■)로 깨져 나온 게 "예쁘지 않다"의 절반이었다. 캡처에서 잡은 결함 하나 추가 수정: `긴급`의 자간 .07em 이 한글을 "긴 급"으로 끊어 0으로 되돌렸다.

**검증.** 새 계약 14건(`/home/sjpark1/section9/tests/test_priority_legible.py`) Red→Green, 기존 `test_priority_visible.py` 는 충돌 계약 2군데만 사유를 적어 갱신(반려가 세운 계약 — 조건 없는 표기·단일 파생·단일 정렬·색면 금지 — 은 그대로 유지). 전체 스위트 **896건 OK**(기준선 882 + 신규 14). 실브라우저로 스킨 10종 전수, 4등급을 한 화면에 세운 램프, terminal 인버스 행 위 가독성, Tab 포커스로 척도 카드가 열리는 것까지 눈으로 확인했다.

**상태.** `REQ-20260827-029-62x6` → **review**. 캡처 6장(현재 3화면 + terminal + 램프 + 바뀌기 전)은 `s9 note --attach` 로 문서에 그림으로 붙였고, 판정 노트에는 확인 포인트만 적었다. 재사용 결론은 `DOC-20260827-005-62x6 수치 축의 화면 표기` 로 남기고 이 REQ에 `derived_from` 으로 걸었다. **커밋하지 않았다.**

## History
- 2026-08-27T13:13:48+09:00 created by sjpark1 (status: open)
- 2026-08-27T13:45:42+09:00 status: open -> in-progress (by sjpark1) — designer 착수. 캡처로 현상 확인: 카드 306장·Docs 목록 306행이 전부 메타 줄 첫 자리에 ▃ 50 을 달고 있고, 그 마크는 10px 모노에서 두부(■)로 떨어진다. 문서 메타 행은 "■ 50 보통(normal) · 높을수록 먼저 집는다 (1~99, 기본 50)" 로 값·범례·설명문이 한 줄에 다 붙는다. 세 갈래(①숫자↔등급 ②설명 전달 방식 ③카드 내 자리)를 정해 재작업한다.
- 2026-08-27T14:05:52+09:00 status: in-progress -> review (by sjpark1) — 우선순위 표시를 숫자에서 낱말로 바꿨습니다. 대시보드에서 F5 를 한 번 누른 뒤 봐 주세요. 지금까지: 카드마다 왼쪽 첫 칸에 작은 네모와 50 이 붙어 있었습니다. 50 이 무슨 뜻인지, 큰 쪽이 급한 건지 알 길이 없었고 306장이 전부 같은 숫자라 눈에 남지도 않았습니다. 그 네모는 사실 막대그래프 모양 글자였는데 화면 글꼴에 없어서 네모로 깨져 나왔습니다. 바꾼 것: 이제 그 자리에 보통 · 높음 · 긴급 · 낮음 네 낱말 중 하나가 뜹니다. 숫자는 지웠지만 사라진 게 아니라, 문서를 열면 "높음 85/99" 처럼 등급과 값이 함께 나옵니다 — 85 가 99 중 85 라는 걸 같이 보여 주니 큰 수가 먼저라는 게 그냥 읽힙니다. 순서를 매기는 기준은 그대로 원래 숫자입니다. 봐 주실 것 세 가지입니다. ① 카드에서 눈에 거슬리는지 — Board 탭. 우선순위를 카드 첫 칸에서 담당자 뒤로 옮겼습니다(@sjpark1 보통 M 순서). 대부분인 "보통" 은 크기·태그와 같은 회색으로 물러나고, "높음"(주황) 과 "긴급"(빨강) 만 색이 올라옵니다. 지금 열려 있는 요청은 거의 다 보통이라 화면이 조용할 겁니다 — 그게 의도한 모습이 맞는지 봐 주세요. ② 처음 보는 사람이 알 수 있는지 — Docs 탭에서 아무 요청이나 하나 열고 PRIORITY 줄을 보세요. 늘 붙어 있던 설명문("높을수록 먼저 집는다 (1~99, 기본 50)")을 지웠습니다. 대신 "높음" 글자에 점선 밑줄이 있고, 거기 마우스를 얹으면(또는 Tab 으로 닿으면) 작은 카드가 떠서 네 등급의 경계와 값을 바꾸는 명령까지 알려 줍니다. 문서 링크에 마우스를 얹었을 때 뜨는 그 미리보기 카드와 같은 물건입니다. 설명을 늘 띄워 두는 게 나은지, 이렇게 필요할 때만 여는 게 나은지가 판단해 주실 지점입니다. ③ 목록에서 — Docs 탭 왼쪽 목록도 문서 번호가 먼저 오고 등급이 그 뒤로 갔습니다. 맨 위 몇 건이 주황 "높음" 으로 뜨는지 봐 주세요. 스킨 10종을 전부 띄워 확인했고, 글자가 배경에 먹히는 곳은 없었습니다. 캡처는 이 문서 본문에 그림으로 붙여 뒀습니다(바뀌기 전 모습도 맨 뒤에 함께). 마음에 걸리는 것 하나를 미리 적어 둡니다: 카드에서 "보통" 바로 뒤에 크기 "M" 이 와서 "보통 크기" 로 읽힐 여지가 있습니다. 거슬리면 순서를 바꾸거나 크기를 앞으로 빼겠습니다.
- 2026-08-27T16:41:59+09:00 status: review -> done (by sjpark1) — 대시보드 승인 [via dashboard]
