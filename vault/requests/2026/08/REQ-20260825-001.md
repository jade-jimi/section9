---
id: REQ-20260825-001
type: request
title: 터미널 탭 전송 불능
summary: "대시보드 터미널 탭: 프롬프트 전송해도 세션에 도달하지 않아 REQ가 생성되지 않음. 헤더/컴포저의 \"@?\" 표기 의심. 부수: 탭 이탈 후 복귀 시 작성 중 메시지 소실, esc로 전송 취소 불가."
goal: 대시보드 터미널 탭에서 보낸 메시지가 세션에 전달·응답되고 draft/첨부·상태 표시가 유지된다
status: done
size: M
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: d332535d
agents: ["subagent"]
project: section9
children: ["REQ-20260825-002"]
tags: ["terminal", "assets", "audit"]
created: 2026-08-25T08:28:58+09:00
status_since: 2026-08-25T11:44:40+09:00
updated: 2026-08-25T21:55:18+09:00
---

## Original

대시보드 터미널이 동작하지 않는 것 같다. 하단부의 @? 이런 문구도 의심스럽고, 정작 프롬프팅을 했는데 요청도 생기지 않는다. (스크린샷: live · target d332535d @?, 전송 메시지 미반영, esc 취소 불가, 탭 이동 시 메시지 소실)

## Notes

### 2026-08-25T08:30:10+09:00 attachment (by nicehugepark)

원 요청에 스크린샷 첨부 있음: /home/sjpark1/.claude/image-cache/d332535d-290f-46b4-9591-96c2d7c6bddb/1.png (터미널 탭 @? 표기·미응답 화면). 추가 지적: 프롬프트 첨부 이미지가 요청 문서에 기록되지 않는 결함도 본 건 범위에 포함.

### 2026-08-25T08:37:35+09:00 tdd (by nicehugepark)

TDD 시나리오 —
[정상] (1) POST /api/chat 에 명령형 텍스트 → 응답에 req id, vault에 REQ 생성(user=발신자, tag=auto-audit), inbox 줄에 req 필드. (2) /api/chat/target 응답에 listening(tail 가동 여부)·user(바인딩 비면 whoami 폴백) 포함. (3) SessionStart 훅: seen 오프셋 이후 미처리 inbox 줄을 additionalContext로 주입 + tail -c +<EOF+1> -f 형태 arm 지시 + seen 파일 갱신. (4) UserPromptSubmit 훅: tail 미가동 시 미처리 줄 주입(가동 시 주입 없이 seen만 전진). (5) kind=interrupt POST → inbox에 kind=interrupt 줄, REQ 미생성.
[경계] 질문/파편 텍스트는 REQ 미생성(응답 req=null). /로 시작하는 커맨드 텍스트 audit 제외. 빈 inbox에서 arm 오프셋 +1. seen>size(파일 교체)면 0으로 클램프.
[실패] audit 실패(분류 예외)해도 전송은 성공해야 한다. 대상 세션 없으면 기존 오류 유지.
[회귀] 기존 c1~c9 대시보드 채팅 테스트 전부 통과. auto-resume 턴은 pending 주입 제외.
[프런트, 실브라우저 확인] 탭 이탈→복귀 시 draft·첨부·히스토리 보존. Esc: 첨부→입력→(비었으면) interrupt 전송. live/idle/stale 3상태 표시. 전송 응답의 req id를 시스템 줄로 표시.

### 2026-08-25T08:50:53+09:00 response (by nicehugepark)

구현 보고 — 원인: (1) 대시보드 채팅은 UserPromptSubmit 훅을 타지 않아 REQ 자동생성이 없었고, 수신 세션의 Monitor(tail)는 첫 턴 실행 후에만 arm되므로 유휴 세션(턴 0)은 메시지를 소비하지 못했다. chat_live()가 attach_pid 생존만으로 live 판정 → 대시보드는 live 표시+전송 성공 처리(침묵 유실). (2) @? = 바인딩 user 빈 값 그대로 노출. (3) 탭 전환 시 #view innerHTML 교체로 컴포저 파괴. (4) Esc는 로컬 클리어뿐.
수복: 서버측 chat audit(/api/chat에서 분류·REQ 즉시 생성, inbox 줄에 req 동봉) · 훅의 미처리 수신함 주입+오프셋 arm(tail -c +EOF+1, seen 파일) · target API에 listening/user 폴백 · 프런트 live/idle/stale 3상태 · Esc→kind=interrupt · 컴포저 보존. 전체 스위트 134 통과. 커밋 aae0907.
한계: (a) Esc 중단은 협조적 — 세션이 다음 도구 경계에서 멈춘다. CC 프로세스에 외부에서 키를 넣을 수는 없다. (b) s9 shot이 터미널 탭(#terminal)은 SSE 상시 연결 탓에 virtual-time이 안 끝나 캡처 불가(기존 한계) — 보드 렌더 정상으로 JS 부팅은 확인, 컴포저 보존·Esc는 실브라우저 확인 필요. (c) 가이드(docs/guide.md)에 Terminal 탭 절 부재 — REQ-018 개편에 포함 권고.

### 2026-08-25T21:55:18+09:00 subagent (agent: subagent, by nicehugepark)

<analysis>
Let me chronologically work through this very long conversation to capture the essential technical and interaction details.

**Overall context**: This is the `~/section9` project — a Korean-language "external memory" system for Claude Code sessions. Prompts get audited by hooks into REQ documents in a vault, with a local dashboard (single-file `web/index.html` served by `bin/s9 serve`) showing a board/docs/graph/terminal. The user (nicehugepark) interacts largely through the dashboard's Terminal tab chat, which appends to an inbox JSONL file that my session tails via a Monitor.

**Phase 1 — Dashboard terminal not working (REQ-20260825-001/-002)**:
User reported dashboard terminal didn't work, "@?" text was suspicious, prompts didn't create requests. I found: chat messages only reached sessions via Monitor tail (armed only after first turn), so idle sessions never consumed them; no REQ creation on the dashboard path; `chat_live()` judged live by attach_pid alone. Fixes: server-side `chat_audit()` creating REQs at `/api/chat`, `_inbox_pending()` + seen-offset in both hooks, `listening` field, composer preservation, Esc interrupt (cooperative), attachment path preservation.

**Phase 2 — Many rapid dashboard-chat requests**: selection visibility (REQ-003), waiting elapsed reset (004), task-notification render (005), uid numbering (006), original message loss (007), Esc SIGINT with guards (008), etc. Key patterns established: TDD notes before code, `python3 tests/` full suite, `bin/s9 shot` browser captures, commit per REQ, `s9 status` transitions with `--note`.

**Phase 3 — uid numbering (REQ-006 → 031)**: User rejected A(machine band)/B/C, wanted D refined. Final: `REQ-YYYYMMDD-NNN-<machinefp4>`, display shortening (git short-hash principle), tombstone rm (`.trash`), resolve with ambiguity refusal.

**Phase 4 — Attachments (REQ-013 → 050/023/053/054/055)**: Store attachments beside docs at `assets/<docid>/`, serving route `/api/asset` inheriting doc visibility, rm moves assets to `.trash/assets-<id>`, migration, inline `<img>` render, generic file support, 30MB, tag extraction from PDF/OOXML/legacy MS/parquet via stdlib only.

**Phase 5 — Governance/gates**: goal gate, TDD gate, raw-title gate (REQ-042), judge exemption for dashboard approvals (REQ-030), review_refs (REQ-041), tag auto-derivation (052), link integrity (`s9 linkcheck`, REQ-059/061/063), relation 3-layer model (DOC-20260825-002-62x6), untitled pressure injection (replacing a decorative ⚠ badge after user said badges are meaningless).

**Phase 6 — Model/effort control (REQ-037)**: CC can't change model in-flight; `claude --resume <sid> --model --effort` is supported; `s9 code` became a restart-loop wrapper; `/api/session/restart` with guards; model label in status bar; ultracode explanation panel; fable added to model list after user couldn't return to fable.

**Phase 7 — Design work (REQ-057/062/068/069/070/071/072)**: This is where the most user friction occurred:
- I created `ux-craft` skill (Apple HIG + Toss principles) and wired it into designer/ux-writer/frontend-developer agents.
- User rejected my "mechanical" board tweaks (3-line clamp, removing elapsed on done cards, verbose empty-column text) and demanded a NEW SKIN instead of editing existing design.
- I created skin `calm` + tones `mist`/`graphite`.
- User: "카드 하이라이트가 전형적인 클로드 디자인이다 절대 반대다" → removed tinted callouts.
- User: "좌측의 띠 역시 같은 맥락으로 반대" → removed left rules.
- User: "단순히 없애라는 피드백만 받지말고 원래 취지에 맞게 검토하고 반영해" → I re-solved intent (status is columns; judge block with caption; etc.).
- User: "원래 취지는 예쁘고 보기 좋고 보기 쉬운거다. 클로드 순수 디자인은 안예쁘고 식상하다" → color via ink/marks not fills.
- User: "지금 디자인은 새로 개선한 에이전트의 능력을 검증하는거야" → I delegated to the designer agent, which found real structural bugs (board white panel killing shadows, card reading order starting with ID, a `*/` inside a CSS comment prematurely terminating and voiding tag/avatar color tokens).

**Phase 8 — Infrastructure problems**:
- Duplicate REQ cards: `~/.claude/settings.json` had 25 duplicate hook entries all pointing at `/home/sjpark1/section9-work/`. I fixed paths + deduped. User then ordered: delete the example-org directory and kill its processes → I backed it up (9.7MB tgz in scratchpad), deleted, killed leftovers.
- User: "왜 다음세션부터 이중생성이 멈추는거야 당장 멈춰" → implemented `_recent_duplicate()` idempotency guard in `cmd_new` (later scoped to auto-audit-tagged cards only after it broke 14 tests).
- Server connection failures: my `_port_busy()` using `connect()` mis-detected free ports as busy under WSL2 (connect succeeds to non-listening ports), causing the "이미 실행 중" early-return to kill test servers → 62 test errors. Fixed by bind-based probe + SO_REUSEADDR (TIME_WAIT false positives).
- User: "검증전용 서버가 기존의 example-org 디렉토라에서 쓰던 포트네... 9910을 쓰지말고 다른걸 써" → killed 9910, agreed to use another port.

**Phase 9 — Most recent**: User approved 062/057/068/067/071/072. Follow-ups: (a) 057 approval memo: "앞으로 화면 디자인은 기본적으로 새로 만든 에이전트를 사용하도록 해라" → I added CLAUDE.md rule #3 delegating visual work to the designer agent; (b) 068 approval memo: "개선된 디자이너로 모든 스킨을 점검해라. 디자인이 깨진것들이 많다" → I registered REQ-20260825-081-62x6 and spawned a designer agent for a full skin audit.

**Phase 10 — Fable limit crisis (current)**: User sent a screenshot showing "You've reached your Fable 5 limit" repeatedly, the dashboard terminal target switched to session `0116660e` with model `<synthetic>`, and said: "멋대로 fable-5로 변경되더니 결국 리밋이 걸리고 아무것도 못하는 상태가 됐다. 모델 변경도 못하고. 해결해."

My diagnosis so far:
- Agent definitions (`harness/claude/agents/*.md`) had no `model:` frontmatter → spawned subagents used the account default (Fable 5), whose quota was already at 94% and got exhausted.
- An auto-resume worker session `0116660e` (entry=code, active_reqs REQ-068/081) was spawned by the watcher and ran on Fable; it appeared as the dashboard target with `<synthetic>` model.
- Actions taken: TaskStop on the skin-audit designer agent; killed worker pid 408543 (confirmed gone); pinned `model: opus` into designer/ux-writer/frontend-developer frontmatter (harness files; `~/.claude/agents/*` are symlinks so they inherited).
- I was mid-investigation of the auto-resume spawn command lines in `bin/s9` (lines 1921, 1926, 1981 use `["claude", "-p", prompt, ...]`; line 3423 `cmd = ["claude"]` in cmd_code) to add an explicit model so workers stop defaulting to Fable.

**Final user message (mid-turn)**: "082 요청 같은 경우는 ux-craft 를 s9-design 을 덮어쓰는건 안되나? 괜히 2개가 있으면 분명히 문제가 되는 순간이 올거같은데..." — asking whether ux-craft should absorb/override s9-design because having two skills will cause conflicts. Note: REQ-082 is a card I have not yet seen the content of (it must have been auto-created from a recent chat message about skill precedence). I have not yet answered this.

Key user preferences/constraints to preserve:
- 말투: 칭찬, 아부성 발언 절대 금지 (no praise/flattery).
- Design: no Claude-style tinted color-fill highlights, no vertical color strips/rules, no all-monochrome minimalism; color via text/outline/marks/typography/depth.
- When told to remove something, first identify what it was doing and replace that function.
- Judgment requests go into review documents, not chat.
- Titles must be 20-char noun phrases; goal required for done.
- Visual work must be delegated to the designer agent (CLAUDE.md rule 3).
- Use port 9909 for the dashboard; do not use 9910.
- s9 commands must be run with `< /dev/null` to avoid stdin hangs (learned when a background `s9 new` hung).
</analysis>

<summary>
1. Primary Request and Intent:
   The user (nicehugepark) is building `~/section9`, a Korean-language external-memory/audit system for Claude Code (CLI `bin/s9`, single-file dashboard `web/index.html`, hooks `bin/s9-audit-*`, vault of markdown REQ/DOC/SES documents). Over this session the user made a rapid series of requests via the dashboard Terminal chat:
   - Fix the dashboard terminal: prompts sent from the dashboard produced no REQ and no response; "@?" label; Esc could not cancel; composer text lost on tab switch (REQ-001/002).
   - Fix visual/interaction defects: selection highlight invisible in terminal, waiting-spinner elapsed reset, spurious "background task 완료 통지 / ~ (총 5s)" lines, spinner glyph jitter, waiting-text variety and gradient wave, elapsed unit expansion (003/004/005/017/019/027/058/077).
   - Solve document id collision across users/machines "발번 방식 전체 공통사항" — user rejected machine-band (A), demanded D (uid + display shortening) designed "촘촘하고 구체적으로" (006 → 031); later asked for retroactive backfill "최대한" (063).
   - Preserve original messages/attachments in documents (007/002/009), and redesign attachment storage because `state/terminal/uploads` is gitignored, single-directory, and absolute-path based (013 → 050/023), extend to generic files, 30MB, tags from file type/extension/content, PDF/xlsx/docx/ppt/keynote/parquet/orc/json/xml extraction with zero dependencies (053/054/055), and register PDF editing as a future request (056).
   - Make regressions structurally impossible rather than relying on discipline: goal/TDD/raw-title gates, judge exemption so dashboard approvals never get blocked (030), review_refs so chat rejections transition documents (041), tag auto-derivation + backfill (052), link integrity checker `s9 linkcheck` with auto-repair at serve startup, 3-layer relation model (059/061/063), duplicate-card idempotency guard (073).
   - Model/effort/account control from the dashboard (037), ultracode discoverability (045), session wake button when "no session" (067).
   - Design: strengthen designer/ux-writer/frontend-developer agents with Apple HIG + Toss references (057); prove it by actually improving the design (062); then, after rejecting "mechanical" tweaks, provide a NEW skin and tone rather than editing the existing one; then reject Claude-style tinted highlights and vertical strips; then demand that removals be replaced by fulfilling the original intent; then clarify the goal is "예쁘고 보기 좋고 보기 쉬운" and that "클로드 순수 디자인은 안예쁘고 식상하다"; then require that design work be done by the improved agent (verification of the agent), and finally that all skins be audited with it (081).
   - Infrastructure: delete `~/section9-work` and kill its processes; stop duplicate card creation immediately (not next session); fix repeated server connection failures as a new request (075); don't use port 9910 for the verification server.
   - Most recent crisis: subagents silently ran on Fable 5, exhausted its quota ("You've reached your Fable 5 limit"), the dashboard target switched to worker session `0116660e` showing model `<synthetic>`, and the user could not change models — "해결해."
   - Final (mid-turn) question, still unanswered: "082 요청 같은 경우는 ux-craft 를 s9-design 을 덮어쓰는건 안되나? 괜히 2개가 있으면 분명히 문제가 되는 순간이 올거같은데..."

2. Key Technical Concepts:
   - section9 protocol: REQ/DOC/SES/PRJ documents, statuses (open/in-progress/blocked/review/done/cancelled), `s9 status` transitions with `--note`, goal gate, TDD gate, raw-title gate, judge (human) exemption.
   - Hook-based audit: `s9-audit-prompt` (UserPromptSubmit classify → request/question/fragment/nothing), `s9-audit-session` (SessionStart/End; injects digest, chat inbox arming instructions, pending inbox lines), `s9-audit-response/subagent`.
   - Dashboard chat transport: `state/terminal/inbox-<sid8>.jsonl` append + Monitor `tail -c +<offset> -f`; `.seen` offset files; `kind=chat|event|interrupt`; `req` and `review_refs` fields embedded in inbox lines.
   - uid scheme: `PREFIX-YYYYMMDD-NNN-<machine fingerprint base36×4>` (`machine_fp()`, `S9_ORIGIN` override), `resolve_id()` prefix resolution with ambiguity refusal, display shortening via `shortId()`/`idBaseCount`, tombstone `.trash` deletion so numbers are never reissued.
   - Attachments: `assets/<docid>/` beside documents, `ASSET_RE` for `[Image:|File: …]`, `ingest_assets()`, `attach_text()` dispatch (TEXTISH / ZIPPED / SCAN / PDF via zlib / rtf), `attach_tags()

…(truncated)

## History
- 2026-08-25T08:28:58+09:00 created by nicehugepark (status: open)
- 2026-08-25T08:29:05+09:00 status: open -> in-progress (by nicehugepark) — 터미널 탭 전송 파이프라인 조사 착수
- 2026-08-25T08:50:53+09:00 status: in-progress -> review (by nicehugepark) — 확인 포인트(실브라우저 3건): (1) 터미널 탭에서 쓰다 만 메시지 입력 → 다른 탭 → 복귀 시 draft/첨부 보존되는지. (2) 전송 직후 "REQ-… 로 기록됨" 줄과 상단 상태 live/idle 구분 표시. (3) 응답 대기(Thinking) 중 빈 입력에서 Esc → 중단 요청 줄 출력·세션이 곧 멈추는지. 서버·훅 경로는 TDD 134건 통과로 검증됨
- 2026-08-25T11:44:40+09:00 status: review -> done (by nicehugepark) — [via dashboard]
