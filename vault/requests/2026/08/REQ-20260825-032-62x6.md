---
id: REQ-20260825-032-62x6
type: request
title: 터미널 연결 끊김 표시
summary: 같은 질문이 30초에 3회 재전송된 실사고 — 서버 재기동 등으로 수신 스트림이 끊겨도 화면에 아무 표시가 없어 무응답으로 오인·재전송하게 됨. SSE 단절/폴백/재접속 상태를 상태줄에 가시화
goal: 수신 스트림 단절 시 사용자가 "연결 문제"임을 화면에서 즉시 알 수 있고, 복구 시 표시가 사라진다
status: done
size: S
user: sjpark1
machine: DESKTOP-TEHV1KR
session: d332535d
project: section9
tags: ["test", "dashboard", "session"]
created: 2026-08-25T12:28:51+09:00
status_since: 2026-08-25T14:50:52+09:00
updated: 2026-08-25T14:50:52+09:00
---

## Original

12:27 동일 메시지 3회 재전송 정황에서 파생

## Notes

### 2026-08-25T13:59:02+09:00 tdd (by sjpark1)

TDD 시나리오 (수신 연결 상태 가시화):
- [x] S1. SSE 단절(onerror) 시 상태줄 cc-conn에 '재접속 중' 경고(ccwarn)가 나타난다 — termConnSet(T,'retry') 배선
- [x] S2. 폴링 폴백 진입 시 cc-conn에 폴백 수신(지연) 표시가 나타난다 — termConnSet(T,'poll') 배선
- [x] S3. 폴백마저 연속 실패(서버 재기동 등) 시 '서버 응답 없음' 경고로 승격된다 — pollFails 누적 → termConnSet(T,'down')
- [x] S4. 복구(SSE onopen/onmessage 또는 폴 성공) 시 경고가 사라진다(hidden) — termConnSet(T,'sse'/'poll') + pollFails 리셋
- [x] S5. 하단 meta(cc-meta)도 conn 상태를 반영한다(sse/poll/재접속/불통)
검증: python3 tests/ term_conn (index.html 계약 테스트) + s9 shot 실화면

### 2026-08-25T14:07:39+09:00 response (by sjpark1)

구현 완료 (수신 연결 상태 가시화) — TDD 시나리오 전부 통과:
- [x] S1. SSE 단절(onerror) → 상태줄 cc-conn "✗ 수신 끊김 — 재접속 중…" (ccwarn, 점멸·reduced-motion 존중)
- [x] S2. 폴링 폴백 진입 → "폴백 수신 (~2.5s 지연)" (dim — 경고 아님, 수신은 되는 상태)
- [x] S3. 폴백 fetch 2회 연속 실패(서버 재기동 등) → "✗ 서버 응답 없음 — 수신 중단" 승격. 툴팁에 "이미 보낸 메시지는 다시 보내지 않아도 됩니다" — 재전송 오인 실사고 직접 대응
- [x] S4. 복구(SSE onopen/onmessage·폴 성공) 시 표시 소거 + pollFails 리셋. 세션 재부착 시에도 소거
- [x] S5. 하단 meta(cc-meta)도 연동: sse / poll 2.5s / "sse 재접속" / "수신 불통"

구현: web/index.html — termConnSet(단일 전이 경로) + TERM_CONN 라벨표 + ccs1 중앙 cc-conn 요소 + CSS(.ccconn, 점멸 keyframes). 글리프는 비이모지 ✗(U+2717) — 스피너 이모지 오염 정책 준수. 진단 플래그 ?conn=retry|down|poll 추가(?nosse·?mpanel 선례) — 실단절은 실시간이라 virtual-time 캡처로 재현 불가, 상태 고정으로 캡처 검증.

검증:
- python3 tests/ → 186 tests OK (신규 계약 테스트 tests/test_term_conn.py 7건 포함, Red→Green 확인)
- s9 shot 실화면: scratchpad/shot-032-poll.png(폴백 dim 표시) · shot-032-down2.png(불통 경고 적색) · shot-032-retry.png(재접속 경고 적색)

미커밋: 무인 봉투 allowlist(s9/python3 tests/ 한정)로 git 커밋 불가 — 워킹트리에 적용된 상태. 커밋은 오너 확인 후 부탁.

### 2026-08-25T14:07:57+09:00 tdd (by sjpark1)

TDD 시나리오 (수신 연결 상태 가시화) — 검증 완료 갱신:
- [x] S1. SSE 단절(onerror) 시 상태줄 cc-conn에 '재접속 중' 경고(ccwarn) — termConnSet(T,'retry') 배선 (test_sse_error_marks_retry + shot-032-retry.png)
- [x] S2. 폴링 폴백 진입 시 cc-conn에 폴백 수신(지연) 표시 — termConnSet(T,'poll') (test_poll_fallback_states + shot-032-poll.png)
- [x] S3. 폴백 연속 실패(서버 재기동 등) 시 '서버 응답 없음' 경고 승격 — pollFails≥2 → termConnSet(T,'down') (test_poll_fallback_states + shot-032-down2.png)
- [x] S4. 복구(SSE onopen/onmessage·폴 성공) 시 경고 소거(hidden) + pollFails 리셋, 재부착 시에도 소거 (test_sse_recovery_clears·test_attach_resets_conn)
- [x] S5. 하단 meta(cc-meta)도 conn 상태 반영(sse/poll/재접속/불통) (test_meta_reflects_conn)
검증 실행: python3 tests/ → Ran 186 tests, OK (계약 테스트 tests/test_term_conn.py 7건 포함, Red 7실패 → Green 확인)

### 2026-08-25T14:10:49+09:00 tdd (by sjpark1)

TDD 시나리오 검증 완료 (수신 연결 상태 가시화):
- [x] S1. SSE 단절(onerror) 시 상태줄 cc-conn에 '재접속 중' 경고(ccwarn) — termConnSet(T,'retry') 배선
- [x] S2. 폴링 폴백 진입 시 cc-conn에 폴백 수신(지연) 표시 — termConnSet(T,'poll') 배선
- [x] S3. 폴백 연속 실패(서버 재기동 등) 시 '서버 응답 없음' 경고 승격 — pollFails 2회 누적 → termConnSet(T,'down')
- [x] S4. 복구(SSE onopen/onmessage·폴 성공) 시 경고 소거(hidden) + pollFails 리셋, 세션 재부착 시에도 소거
- [x] S5. 하단 meta(cc-meta)도 conn 상태 반영: sse / poll 2.5s / "sse 재접속" / "수신 불통"
검증: python3 tests/ term_conn 7건 OK + 전체 스위트 OK. s9 shot 실화면 3종(scratchpad/shot-032-{retry,down,poll}-v2.png) — 세션 9fc6adfc 재검증.

### 2026-08-25T14:10:49+09:00 response (by sjpark1)

재작업 완료 보고 (세션 9fc6adfc — drag 이동으로 재개된 카드, 이전 세션 d332535d 구현분 검증·마감):

구현 (web/index.html, 워킹트리 적용됨):
- termConnSet(단일 전이 경로) + TERM_CONN 라벨표 + 상태줄 중앙 cc-conn 요소(기본 hidden) + CSS(.ccconn, 점멸 keyframes, reduced-motion 존중)
- SSE onerror → "✗ 수신 끊김 — 재접속 중…"(적색 점멸) / 폴백 진입 → "폴백 수신 (~2.5s 지연)"(dim) / 폴백 fetch 2연속 실패 → "✗ 서버 응답 없음 — 수신 중단" 승격(툴팁: "이미 보낸 메시지는 다시 보내지 않아도 됩니다" — 재전송 실사고 직접 대응)
- 복구(onopen/onmessage/폴 성공)·세션 재부착 시 표시 소거 + pollFails 리셋. 하단 cc-meta도 sse/poll 2.5s/"sse 재접속"/"수신 불통" 연동
- 진단 플래그 ?conn=retry|down|poll (?nosse 선례) — 실단절은 실시간이라 virtual-time 캡처 불가, 상태 고정용

검증 (이 세션에서 재실행):
- python3 tests/ term_conn → 7 tests OK (tests/test_term_conn.py 계약 테스트)
- python3 tests/ 전체 스위트 → OK
- s9 shot 실화면 3종 재캡처: scratchpad/shot-032-retry-v2.png(적색 경고+meta "sse 재접속") · shot-032-down-v2.png(적색 "수신 중단"+meta "수신 불통") · shot-032-poll-v2.png(dim 폴백+meta "poll 2.5s")

확인 포인트 (F5 새로고침 후 — SPA라 구 CSS 오판 방지):
1. Terminal 탭에서 `?nosse&conn=retry#terminal` 접속 시 상태줄 중앙에 적색 "✗ 수신 끊김 — 재접속 중…" 점멸 표시
2. `?conn=down`은 "✗ 서버 응답 없음 — 수신 중단", `?conn=poll`은 회색 "폴백 수신 (~2.5s 지연)"
3. 플래그 없는 정상 접속에서는 cc-conn 표시가 없고(hidden), 하단 meta가 "sse · ↓ N events"
4. 실단절 재현: 접속 중 서버를 재기동하면 재접속→폴백→불통 순으로 승격 표시, 복구 시 자동 소거

미커밋: 무인 봉투 allowlist(s9·python3 tests/ 한정)로 git 커밋 불가 — 워킹트리 적용 상태. 커밋은 오너 확인 후 요청.

### 2026-08-25T14:50:33+09:00 response (by sjpark1)

리드 검증(사용자 지적 수용 — 셀프 검증으로 종결): 워커 구현을 독립 검증했다. ①실브라우저 캡처: ?conn=down에서 상태줄 적색 "✗ 서버 응답 없음 — 수신 중단"+meta "수신 불통", ?conn=retry에서 "✗ 수신 끊김 — 재접속 중…"+meta "sse 재접속" 정상 렌더(캡처 2매). ②테스트: term_conn 7건 + 전체 186건 통과. ③워커의 allowlist 제약으로 미커밋이던 변경을 리드가 커밋. 이 건은 시각 취향이 아니라 상태 표시의 정확성 문제라 사용자 판정이 불필요한 유형 — 앞으로 이런 유형은 review에 올리지 않고 리드가 검증해 바로 done 한다.

## History
- 2026-08-25T12:28:51+09:00 created by sjpark1 (status: open)
- 2026-08-25T13:55:40+09:00 status: open -> in-progress (by sjpark1) — drag 이동 [via dashboard]
- 2026-08-25T14:08:02+09:00 status: in-progress -> review (by sjpark1) — 확인 포인트: 대시보드 새로고침(F5) 후 Terminal 탭에서 — ① ?conn=down 붙여 열면 상태줄 중앙에 적색 '✗ 서버 응답 없음 — 수신 중단', ?conn=retry면 '✗ 수신 끊김 — 재접속 중…' 점멸 표시 ② ?nosse면 dim '폴백 수신 (~2.5s 지연)' ③ 평상시(SSE 정상)엔 아무 표시 없음 ④ 하단 meta도 sse/poll/재접속/불통 연동. 실단절 재현은 s9 serve 재기동으로도 가능(폴백 2회 실패 후 경고 승격, 복구 시 자동 소거). 스크린샷: scratchpad/shot-032-{poll,down2,retry}.png. 변경은 워킹트리 미커밋(무인 allowlist 제약).
- 2026-08-25T14:48:20+09:00 status: review -> in-progress (by sjpark1) — 사용자 채팅 반려 접수("저 정도는 스스로 검증 가능") — 리드가 직접 객관 검증 후 종결한다. 채팅 반려가 상태에 자동 반영되지 않는 공백은 별도 논의
- 2026-08-25T14:50:52+09:00 status: in-progress -> done (by sjpark1) — goal 충족: 단절(?conn=down)·재접속(?conn=retry)·폴백(nosse) 상태 표시와 meta 연동·복구 소거를 실브라우저 캡처 2매+term_conn 7건+전체 186건으로 리드가 직접 검증(TDD 5항목 체크 완료). 커밋 e449858
