---
id: REQ-20260828-023-62x6
type: request
title: 대답 속 코드 보이는 모양
summary: 대답 속 코드 블록에서 회색 색면·세로 띠를 걷어내고 헤어라인+들여쓰기로 — 접지 않는다, 복사 손잡이만 붙인다
goal: 터미널과 문서 스트림의 코드 블록이 문장 흐름을 끊지 않고 읽히고, 붙여 쓸 명령을 한 번에 복사할 수 있다
status: review
size: S
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: b2a0d62a
sessions: ["b8fda8f9", "b2a0d62a"]
agents: ["worker:rework"]
contributions: [{"actor": "worker:rework", "item": "재작업 TDD — 계약 먼저 쓰고(11개 전부 실패 확인) 고쳤다. tests/test_code_block_look.py", "started": "2026-08-28T16:30:17+09:00", "ended": "2026-08-28T16:30:17+09:00", "result": "done", "transcript": ""}, {"actor": "worker:rework", "item": "반려를 받아 방향을 바꿨다. **접기는 버렸다.** 사용자가 말한 본질(참조 파일을 쉽게", "started": "2026-08-28T16:30:20+09:00", "ended": "2026-08-28T16:30:20+09:00", "result": "done", "transcript": ""}, {"actor": "worker:rework", "item": "전체 스위트: 1428개 중 실패 2개 — 둘 다 test_instance_init(새 인스턴스를 만들어 원격에 푸시하는 절차", "started": "2026-08-28T16:30:28+09:00", "ended": "2026-08-28T16:30:28+09:00", "result": "done", "transcript": ""}]
project: section9
relates: ["REQ-20260828-021-62x6"]
relates_why: {"REQ-20260828-021-62x6": "같은 자리(터미널 대답)에 무엇을 어떻게 실을지의 문제다 — 링크·집기와 한 줄에서 부딪힌다"}
tags: ["auto-audit", "attached", "png", "image"]
created: 2026-08-28T15:57:20+09:00
status_since: 2026-08-28T16:42:59+09:00
updated: 2026-08-28T16:42:59+09:00
priority: 50
---

## Original

소스 코드 스니펫 같은 경우는 코드 스니펫 블럭을 접어서 대답에 추가해주는것도 괜찮을거같고.. 이 부분도 디자이너가 또 고민해줘. 바로 진행하지말고

## Notes

### 2026-08-28T16:07:15+09:00 analysis (by nicehugepark)

designer 검토 (요청대로 구현 없이 안만)

**결론: 기본 접힘은 반대. 실측하니 이 판의 대답 속 코드 블록은 곁다리가 아니라 대답의 결론 그 자체였다. 접어야 할 것(도구 입·출력)은 이미 접혀 있다.**

실측이 전제를 뒤집었다
- 최근 세션 40개 transcript 에서 대답 본문의 fenced 블록 43개 전수: 1~3줄 25개 · 4~10줄 18개 · **11줄 이상 0개**. 그리고 **43개 전부 언어 태그가 없다**.
- 내용은 소스 코드가 아니었다 — 붙여 실행할 명령 절차 · 실측 수치 표 · before/after 대응 · 결정 요약. 즉 **결론을 고정폭으로 세워 둔 자리**다.
- 진짜 긴 코드는 도구 입력(Edit/Write)·출력(Bash/Read)에 있고 거기는 이미 접혀 있다(도구 입력 130자·결과 200자 초과 접힘, 연속 Bash 2개 이상은 한 줄로 묶음).
- 이번 세션 569개 이벤트 중 대답 본문 코드 블록은 5개. **코드 블록을 접어도 화면은 별로 안 조용해진다.**

접으면 잃는 것
- 대답이 결론을 잃는다("스위치는 하나만 뒀습니다:" 로 끝나고 그 하나가 안 보인다). 43개 중 43개가 이 꼴 — 클릭 세금 100%, 이득 0.
- **접힌 글자는 Ctrl+F 에 안 걸린다.** 대답 본문까지 접으면 화면에서 글자로 찾을 수 있는 것이 산문뿐이 되고, 문서 id·경로가 블록 안에 있으면 REQ-021(눌러 열기)도 그 순간 무력해진다.
- 되돌리기 비용이 비대칭이다: 펴진 것을 안 읽는 비용은 스크롤 한 번, 접힌 것을 놓치는 비용은 결론을 못 본 채 다음 지시를 내리는 것.

진짜 문제는 길이가 아니라 재질이었다 (리드 실측 확인)
    web/index.html:730  .ccblk{background:rgba(255,255,255,.06); border-left:2px solid …}
    web/index.html:729  .cccode{background:rgba(255,255,255,.09); …}
**색면 하이라이트와 세로 띠 — 이 제품이 명시적으로 금지한 두 가지가 정확히 그 모양으로 있다.** 인라인 칩까지 합쳐 15:56 대답 하나에만 회색 조각이 여섯 개 흩어져 있어, 무엇이 강조인지 사라진다. 잃는 것은 읽는 속도가 아니라 **강조의 신호값**이다.

권고 (C안)
- 색면·세로 띠 제거 → 위아래 헤어라인 + 좌측 14px 들여쓰기(이 판의 표가 이미 쓰는 어휘). 색은 면이 아니라 글자에 남긴다.
- **12줄 문턱**: 넘는 블록만 머리 8줄을 늘 보이고 나머지를 `+ N줄 더 보기` 로 자른다. details 로 감추지 않고 **클리핑** — 잘린 줄을 DOM 에 남겨 Ctrl+F 가 계속 걸린다. 기준은 줄 수가 아니라 "앞뒤 문장이 블록과 한 화면에 같이 서는가"(출력 판 약 33줄, 1/3 이 11줄). 오늘 43개 중 0개가 걸린다 — **오늘 있는 것을 망가뜨리지 않고 걱정한 미래만 막는다.**
- 블록 우상단 hover 시 `⧉ 복사` (이 블록들의 절반이 명령 절차다).
- `+ N줄 더 보기` 는 보드·Docs 가 이미 쓰는 낱말 — 새 문법 0.

021·022 와의 자리 다툼: **펴기는 다툼에 안 낀다.** 열기·집기는 낱말에 붙고 펴기는 블록 끝 한 줄에 붙는다. 진짜 충돌은 021 vs 022(문서 id 한 낱말에 손잡이 둘)이고 023 이 만든 문제가 아니다. designer 제안: 낱말 자체는 **열기 하나만**, 집기는 열어서 뜨는 미리보기 바닥줄에 `이어 말하기` 로 — 한 낱말에 손잡이는 하나, 고정폭 판의 줄 정렬도 지킨다.

**021 구현자에게 반드시 전달할 것**: `ccText` 는 fenced 블록과 인라인 백틱을 **먼저** placeholder 로 빼낸 뒤 경로·해시 강조를 돌린다(web/index.html 6024·6031 → 6035). 그래서 블록 안 경로에는 지금도 링크가 안 붙는데 이건 버그가 아니라 규율이다 — 블록 내용은 읽을 것이 아니라 **붙일 것**이라, 링크를 걸면 드래그 선택이 링크 드래그가 되고 복사본에 마크업이 섞인다. 인라인 백틱에 021 링크를 걸려면 그 순서를 의도적으로 바꿔야 하고, **모르고 바꾸면 블록 안까지 링크가 번진다.**

하지 말 것 (designer 가 스스로 잘라낸 것): 문법 강조(언어 태그가 0개라 무엇으로 칠할지 알 수 없고 색을 대여섯 개 쓰게 된다) · 줄 번호 gutter(복사에 딸려 온다) · 언어/줄수 배지 pill · 블록별 접힘 상태 기억(스트림 이벤트라 안정된 id 가 없어 재렌더마다 엉뚱한 블록이 펴진다) · 전역 접기 스위치(끌 일 없는 것에 스위치를 안 단다 — 019 에서 방금 세운 규칙) · 블록 내부 스크롤(스크롤 판 안의 스크롤 판은 휠을 가로챈다).

범위를 줄여야 하면 잘라낼 순서: ① 복사 손잡이 → ② 12줄 문턱 → ③ **색면·세로 띠 제거는 마지막까지 남긴다.** 이 하나만 해도 불편의 대부분이 사라지고 접기 없이 즉시 효과가 난다.

캡처: assets/REQ-20260828-023-62x6/지금-화면.png · cmp-3안-대조.png (A 지금 / B 전부 접힘 / C 권고)

### 2026-08-28T16:30:17+09:00 tdd (agent: worker:rework, by nicehugepark)

재작업 TDD — 계약 먼저 쓰고(11개 전부 실패 확인) 고쳤다. tests/test_code_block_look.py
실행: `python3 tests/ code_block_look` → 11/11 통과.

- [x] t1. 코드 블록에 색면(background)이 없다
- [x] t2. 코드 블록에 세로 띠(border-left)가 없다
- [x] t3. 구분은 위아래 헤어라인 + 왼쪽 들여쓰기(10px 이상)로 — 표·헤딩과 같은 어휘
- [x] t4. 인라인 코드도 색면이 아니라 글자색으로 말한다
- [x] t5. 같은 규칙이 터미널(.ccterm)과 문서 스트림(.term) 양쪽에 걸린다
- [x] t6. .term 용 규칙의 var() 에는 fallback 이 있다(그쪽엔 --cc-* 가 없다)
- [x] t7. 블록 마크업이 복사 손잡이를 함께 낸다
- [x] t8. 손잡이는 평소 숨고 hover·focus-visible 에서만 선다(WCAG 2.1.1)
- [x] t9. 손잡이는 선택에 끼지 않는다(user-select:none)
- [x] t10. 복사본에서 손잡이 글자를 걷어낸다 + 성공/실패를 말한다
- [x] t11. 리스너는 위임 하나 — 손잡이마다 붙이면 재렌더마다 죽는다

- [x] s1. 화면 실캡처: DOC-20260826-005-62x6 규약대로 web/index.html 의 <style> 전문과
      ccText/ccBlocks/mdTable 를 그대로 추출한 정적 하네스를 만들어 3판 대조 캡처
      (이전 / 지금 터미널 / 지금 문서 스트림). 노트에 붙였다.
- [x] s2. 전체 스위트 회귀 확인.

미검증(사람 확인 몫): 실제 클릭 동작 — 손잡이를 눌렀을 때의 클립보드 쓰기와
"복사됨" 피드백은 정적 하네스로 검증되지 않는다. 대시보드 터미널 탭은 SSE 때문에
headless 캡처가 정착하지 않는다(DOC-20260826-005-62x6).

### 2026-08-28T16:30:20+09:00 response (agent: worker:rework, by nicehugepark)

반려를 받아 방향을 바꿨다. **접기는 버렸다.** 사용자가 말한 본질(참조 파일을 쉽게
보기)은 021 의 몫이라 그쪽으로 넘겼고, 이 문서에는 접지 않고도 되는 것만 남겼다.
제목도 바꿨다(대답 속 코드 접어 두기 → 대답 속 코드 보이는 모양) — 접기를 안 하는데
제목이 접기라고 말하고 있으면 나중에 아무도 판정할 수 없다.

### 고친 것 (web/index.html 한 파일)

**① 코드 블록의 재질 — 회색 색면과 왼쪽 세로 띠를 걷어냈다.**
이 판이 쓰지 않기로 한 정확히 그 두 가지가 그 모양으로 있었다. 대신 위아래 헤어라인 +
왼쪽 14px 들여쓰기 — 표와 헤딩이 이미 쓰는 어휘다. 색은 면이 아니라 글자에 남겼다.
인라인 코드(백틱)의 회색 조각도 같이 걷었다: 한 대답에 회색 조각이 여섯 개씩 흩어지면
무엇이 강조인지가 사라진다.

**② 문서 스트림(.term)에도 같은 규칙을 걸었다 — 없던 것을 채운 자리다.**
터미널과 문서 스트림은 같은 `ccText` 출력을 그리는데, 스타일은 `.ccterm` 에만 걸려
있었다. 그래서 문서에서 스트림을 펴 보면 코드 블록이 맨 글자로 흘렀다. 규칙을 두 자리에
같이 걸고, `.term` 에는 `--cc-*` 토큰이 없으므로 var() 마다 fallback 을 붙였다
(표가 REQ-20260826-029-62x6 에서 세운 규율 그대로).

**③ 블록에 복사 손잡이(⧉ 복사).**
실측한 43개 블록의 절반이 붙여 실행할 명령이었다. 평소엔 숨고 포인터를 올리거나 키보드
초점이 닿을 때만 블록 위쪽 선에 뜬다(글자를 가리지 않는 자리). 복사본에서는 손잡이
글자를 걷어내고 준다 — 안 걷으면 붙여넣은 명령 첫 줄이 "⧉ 복사" 로 시작한다.
손잡이는 드래그 선택에도 끼지 않는다. 리스너는 위임 하나다(스트림은 통째로 다시
그려지므로 손잡이마다 붙이면 재렌더마다 죽는다).

### 안 한 것과 이유

- **접기·클리핑**: 사용자 지시대로 뺐다. 실측으로도 근거가 없다 — 최근 40개 대화의
  대답 본문 fenced 블록 43개 중 11줄을 넘는 것이 0개였고, 그 43개는 소스 코드가 아니라
  결론 그 자체였다.
- **문법 강조·줄 번호·언어 배지**: designer 가 스스로 잘라낸 것(43개 전부 언어 태그가
  없어 무엇으로 칠할지 알 수 없다). 그대로 두었다.
- **파일 미리보기 자체**: 021 의 몫이다. 021 에 handoff 노트를 남겼다 —
  사용자가 말한 본질, `ccText` 의 placeholder 순서 경고(모르고 바꾸면 블록 안까지
  링크가 번진다), 새로 생긴 `.ccbcp` 를 텍스트로 읽을 때 걷어낼 것, 그리고 긴 파일
  본문에는 `+ N줄 더 보기` 클리핑이 필요하다는 것(details 로 감추면 Ctrl+F 가 죽는다).

### 검증

- `python3 tests/ code_block_look` — 새 계약 11개 전부 통과(고치기 전엔 11개 전부 실패).
- 전체 스위트 회귀 확인.
- 캡처(붙임): 왼쪽 = 이전(회색 색면 + 세로 띠) · 가운데 = 지금 터미널 · 오른쪽 = 지금
  문서 스트림. 손잡이는 hover 상태를 강제로 켜서 찍었다. 대시보드 터미널 탭은 SSE 때문에
  headless 캡처가 정착하지 않아(DOC-20260826-005-62x6) 규약대로 실제 CSS·함수를 그대로
  추출한 정적 하네스로 찍었다 — 실제 클릭 동작은 사람 확인 몫이다.

[Image: assets/REQ-20260828-023-62x6/shot-4.png]

### 2026-08-28T16:30:28+09:00 test (agent: worker:rework, by nicehugepark)

전체 스위트: 1428개 중 실패 2개 — 둘 다 test_instance_init(새 인스턴스를 만들어 원격에 푸시하는 절차)이고 "푸시 실패 — 원격 리포가 아직 없을 가능성이 크다" 로 끝난다. 이 워크트리에 원격이 없어 나는 환경 실패이고, 이번 변경(web/index.html 의 CSS·ccText)과 닿는 지점이 없다. 새 계약 tests/test_code_block_look.py 11개는 전부 통과.

### 2026-08-28T16:42:28+09:00 commit (by nicehugepark)

커밋 d880247 — 코드는 접지 않고, 회색 덩어리만 걷어낸다 (REQ-20260828-023-62x6)

## History
- 2026-08-28T15:57:20+09:00 created by nicehugepark (status: open)
- 2026-08-28T15:57:36+09:00 status: open -> in-progress (by nicehugepark) — 사용자 지시: 바로 진행하지 말고 designer 검토 먼저 — 검토는 읽기 전용이라 지금 착수
- 2026-08-28T16:07:15+09:00 status: in-progress -> review (by nicehugepark) — 진행 여부를 정해 주세요 — 구현은 아직 하지 않았습니다. 문서에 붙인 캡처 두 장을 봐 주세요: `지금-화면.png` 과 `cmp-3안-대조.png` (A 지금 / B 전부 접힘 / C 권고). designer 가 세어 보니 최근 대화 40개에서 대답에 들어간 코드 블록이 43개인데 **11줄 넘는 것이 하나도 없었습니다.** 그리고 그 43개는 소스 코드가 아니라 붙여 실행할 명령 · 실측 수치 · 결정 요약이었습니다. 즉 접으면 대답이 결론을 잃습니다("스위치는 하나만 뒀습니다:" 로 끝나고 정작 그 하나가 안 보이는 식). 접힌 글자는 브라우저 찾기(Ctrl+F)에도 안 걸립니다. 대신 진짜 원인을 하나 찾았습니다. 지금 코드 블록은 **회색 색면 + 왼쪽 세로 띠** 로 그려져 있는데, 이 제품이 쓰지 않기로 한 바로 그 두 가지입니다. 문장 사이에 회색 덩어리가 끼어들어 무엇이 강조인지 흐려집니다. 권고(C)는 이렇습니다: 1. 색면과 세로 띠를 걷어내고 위아래 가는 선 + 왼쪽 들여쓰기로 — **이것만 해도 대부분 해소됩니다** 2. 12줄 넘는 블록만 머리 8줄을 보이고 나머지를 "+ N줄 더 보기" 로 접습니다. 지금 걸리는 블록은 0개라 오늘 화면은 그대로이고, 앞으로 긴 코드가 붙을 때만 작동합니다. 접어 숨기는 게 아니라 잘라 두는 방식이라 Ctrl+F 는 계속 걸립니다 3. 블록에 마우스를 올리면 오른쪽 위에 "복사" 하나 A(그대로) · B(전부 접기) · C(권고) 중 무엇으로 갈지, 또는 C 에서 무엇을 빼고 갈지 말씀해 주세요.
- 2026-08-28T16:12:35+09:00 status: review -> in-progress (by nicehugepark) — 대답을 접는 행위에 집중이 된 것 같은데, 참조 파일을 쉽게 보는게 본질이다. 당장은 안접어도 된다. 소스코드 파일을 어떻게 보여줄거냐는 고민이 있는 것 같길래 의견을 말한것뿐. [via dashboard]
- 2026-08-28T16:42:59+09:00 status: in-progress -> review (by nicehugepark) — 접기는 만들지 않았습니다 — 말씀대로 뺐습니다. 대신 코드 조각이 화면에서 어떻게 보이는지만 고쳤습니다. **대시보드를 F5 로 새로고침한 뒤** 봐 주세요(안 하면 예전 화면이 그대로 보입니다). 확인 포인트 넷입니다. ① 터미널 탭에서 제가 낸 대답 중 코드가 들어간 것을 하나 보세요. 전에는 코드 조각이 회색 판때기에 얹혀 있고 왼쪽에 세로 막대가 있었습니다 — 문장 사이에 회색 덩어리가 끼어 있는 모양이었죠. 지금은 판때기와 막대를 걷어내고 위아래 가는 선과 왼쪽 들여쓰기만 남겼습니다. 문장 → 코드 → 문장이 한 흐름으로 읽히는지, 그래도 코드가 코드로 보이는지 봐 주세요. 문장 속 백틱 글자(`이런 것`)의 회색 조각도 같이 걷었습니다. ② 코드 덩어리에 마우스를 올리면 오른쪽 위 선 위에 "⧉ 복사" 가 뜹니다. 눌러 보시고 "복사됨" 이 뜨는지, 아무 데나 붙여넣었을 때 첫 줄이 "⧉ 복사" 로 시작하지 않고 코드만 들어오는지 확인해 주세요(마우스로 긁어서 복사할 때도 마찬가지여야 합니다). 이 손잡이만은 실제 클릭까지는 제가 확인하지 못했습니다 — 터미널 화면은 자동 캡처가 되지 않습니다. ③ Docs 에서 문서를 하나 열어 "이 요청의 스트림" 을 펴 보세요. 여기는 전에 코드가 아무 표시 없이 맨 글자로 흘렀는데, 이제 터미널과 같은 모양으로 나옵니다. ④ 제목을 "대답 속 코드 접어 두기" 에서 "대답 속 코드 보이는 모양" 으로 바꿨습니다 — 접기를 안 하는데 제목이 접기라고 말하면 나중에 이 카드를 아무도 알아볼 수 없어서입니다. 말씀하신 본질(참조 파일을 쉽게 보기)은 REQ-20260828-021-62x6 로 넘겼습니다 — 그 문서에 파일 미리보기가 이 코드 모양을 그대로 쓰도록, 그리고 긴 파일은 잘라서 보여 주도록 인계 노트를 남겼습니다. 붙임 캡처: 왼쪽 이전 · 가운데 지금 터미널 · 오른쪽 지금 문서 스트림.
