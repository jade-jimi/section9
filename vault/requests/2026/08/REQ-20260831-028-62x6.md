---
id: REQ-20260831-028-62x6
type: request
title: 프로젝트 화면
summary: Docs 좌측 PROJECT 진입점·새 프로젝트 다이얼로그·PRJ 문서 뷰 통합(멤버·고객·설정 인라인)·Board 패널 한 줄 띠 축소 — 신규 web/app/project.js·web/css/project.css 격리, B1 골격(mock)→B2 실연동
goal: Docs 에서 프로젝트 목록·생성·상세(멤버·고객·설정)를 처리하고 Board 패널이 한 줄 띠가 된 것이 실브라우저(스킨 전수·배율 1/1.25/1.5 육안)로 확인된다
status: in-progress
size: L
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: 38ad98c8
agents: ["sub:frontend-developer:aea99b3d", "sub:frontend-developer"]
contributions: [{"actor": "sub:frontend-developer:aea99b3d", "item": "028 프로젝트 화면 골격", "started": "2026-08-31T22:53:17+09:00", "ended": "2026-09-01T00:04:43+09:00", "result": "done", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/38ad98c8-51fd-4b50-a26a-495b58404817/tasks/aea99b3da0560bcd2.output"}, {"actor": "sub:frontend-developer", "item": "TDD 시나리오 — B1 프로젝트 화면 골격 (mock)", "started": "2026-08-31T22:58:31+09:00", "ended": "2026-08-31T22:58:31+09:00", "result": "done", "transcript": ""}, {"actor": "sub:frontend-developer", "item": "B1 골격 완료 — 신규 두 파일에 격리, 검증 자 하나로 상태 전부", "started": "2026-08-31T23:32:58+09:00", "ended": "2026-08-31T23:32:58+09:00", "result": "done", "transcript": ""}, {"actor": "sub:frontend-developer", "item": "보고 이후 고친 넷 (전부 눈으로 보거나 전체 실행에서 드러난 것)", "started": "2026-08-31T23:38:24+09:00", "ended": "2026-08-31T23:38:24+09:00", "result": "done", "transcript": ""}, {"actor": "sub:frontend-developer", "item": "전체 실행이 잡은 것 — 저장소 게이트 셋을 어긴 채였다", "started": "2026-08-31T23:48:09+09:00", "ended": "2026-08-31T23:48:09+09:00", "result": "done", "transcript": ""}, {"actor": "sub:frontend-developer", "item": "TDD 시나리오 — B1 골격 (개정 · 통과 표시)", "started": "2026-08-31T23:52:00+09:00", "ended": "2026-08-31T23:52:00+09:00", "result": "done", "transcript": ""}, {"actor": "sub:frontend-developer", "item": "캡처를 믿을 수 있게 만든 것 (B2 가 같은 자를 쓴다)", "started": "2026-09-01T00:03:49+09:00", "ended": "2026-09-01T00:03:49+09:00", "result": "done", "transcript": ""}, {"actor": "sub:frontend-developer:aea99b3d", "item": "`[2026-09-01 00:04:12 KST - frontend-developer]`", "started": "2026-09-01T00:04:43+09:00", "ended": "2026-09-01T00:04:43+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/38ad98c8-51fd-4b50-a26a-495b58404817/subagents/agent-aea99b3da0560bcd2.jsonl"}, {"actor": "sub:frontend-developer:aea99b3d", "item": "`[2026-09-01 00:04:38 KST - frontend-developer]`", "started": "2026-09-01T00:05:07+09:00", "ended": "2026-09-01T00:05:07+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/38ad98c8-51fd-4b50-a26a-495b58404817/subagents/agent-aea99b3da0560bcd2.jsonl"}]
project: section9
parent: REQ-20260831-026-62x6
relates_why: {}
tags: ["project", "access", "dashboard", "attached", "png", "image", "terminal"]
created: 2026-08-31T22:52:31+09:00
status_since: 2026-08-31T22:52:38+09:00
updated: 2026-09-01T00:05:07+09:00
priority: 50
---

## Original

프로젝트 화면

## Notes

### 2026-08-31T22:58:31+09:00 tdd (agent: sub:frontend-developer, by nicehugepark)

### TDD 시나리오 — B1 프로젝트 화면 골격 (mock)

검증 자: `web/verify-project.html`(진짜 렌더 함수 + mock 행) · 회귀: `tests/test_project_screen.py`(신규, stdlib only, 서버 미기동)

### 렌더 계약 — Docs 좌측 PROJECT 목록
- [ ] S1 N개: 최근 활동순 정렬 · 20 + 「N개 더 보기」 · 행마다 세 줄(제목+상태 / ID·slug / 멤버·열린 요청·마지막 활동)
- [ ] S2 0개: 안내가 아니라 행동 — 빈 문구 한 줄 + 「새 프로젝트」 단추 하나
- [ ] S3 1개: 0→1→N 에서 구조가 바뀌지 않는다(머리·행·단추 자리 동일)
- [ ] S4 보관됨: 「보관됨 N개」로 접힘, 펼치면 잉크 --c-cancelled + 오른쪽 ARCHIVED 글자(색 없이도 읽힌다)
- [ ] S5 만들 권한 없음: 「새 프로젝트」를 **아예 안 그린다**(회색 단추 금지)

### 렌더 계약 — PRJ 문서 뷰 프로젝트 패널
- [ ] S6 멤버 0 + 등록 사용자 있음: 추가 행이 표 자리에 바로 선다(막다른 안내 아님)
- [ ] S7 멤버 0 + 등록 사용자도 0: 다른 문구 + Settings›사용자 관리 링크, 추가 폼 없음
- [ ] S8 뷰어 권한: 편집 컨트롤(select/input/제거) 0개 + 표 아래 권한 한 줄
- [ ] S9 manage 이나 own 아님: owner 행 컨트롤만 disabled + 이유(title) — 여기만 회색 허용
- [ ] S10 마지막 owner: 「제거」 disabled + 이유
- [ ] S11 만료 임박(≤14일): 만료일 잉크만 --c-inprogress + 툴팁
- [ ] S12 만료됨: 상태 「만료」 + 행 잉크 **--muted**(--c-blocked 아님 — 만료는 고장이 아니다)
- [ ] S13 관리 요약: 상태별 요청 수·마지막 활동은 catalog 파생으로 계산(별도 저장 없음)

### 저장 규칙 하나 (인라인)
- [ ] S14 설정 인라인 편집과 멤버 표 변경이 **같은 관문 함수**를 지난다(post 경로 한 곳)
- [ ] S15 변경 1회 = 요청 1회, 값이 안 바뀌면 요청 0회
- [ ] S16 실패: 컨트롤 원복 + 실패 줄(붉은 잉크, 색면 아님) + 다시 시도 가능
- [ ] S17 status 변경만 확인 창을 거친다(보관은 전 문서가 접히는 사건)

### 생성 다이얼로그 — 기존 dlg 문법 재사용
- [ ] S18 필드는 넷만(표시명·slug·개요·고객) — 담당자 4필드 없음
- [ ] S19 기존 다이얼로그 클래스 문법(.dlghead/.dlgbody/.dlgfoot/.acts)만 쓴다 — 새 창 부품 발명 0
- [ ] S20 표시명 비면 진행 불가 · slug 정규식 위반/중복은 그 자리에서 사유 표시(서버 왕복 전 화면 선검사, 판정은 서버가 최종)

### Board 문맥 띠
- [ ] S21 한 줄(32px) · 표 없음 · 아래 hairline 하나 · 「프로젝트 문서 열기」는 doclink 문법
- [ ] S22 프로젝트 미선택이면 자리도 없다

### 시각 언어 · 배율 · 스킨 전수
- [ ] S23 project.css 는 색 리터럴 0(토큰만) · 라운드 0 · 그림자 0 · 좌측 세로 띠 0 · 색면 배지 0
- [ ] S24 배율 불변: 새 홀수 px 치수·소수 px 테두리 없음(점은 확정된 7px .cdot 재사용)
- [ ] S25 CSS 복제 금지: project.css 에 .proj-info 셀렉터 0 — 멤버 표 규칙은 .pmem 한 곳
- [ ] S26 문구는 PRJ_TEXT 한 곳에 모인다(C 판정이 W2 에서 통째로 갈아 끼울 수 있게)
- [ ] S27 스킨 10종 × 배율 1·1.25·1.5 캡처를 확대 육안 판정 — 잘림·겹침·대비 이상 없음
- [ ] S28 hover/focus 는 CDP 실포인터로 건 화소로 판정(정적 캡처 금지) — 칠이 이웃 글자를 파고들지 않고, 반전 스킨(terminal)에서 사라지지 않는다

### 2026-08-31T23:32:58+09:00 response (agent: sub:frontend-developer, by nicehugepark)

### B1 골격 완료 — 신규 두 파일에 격리, 검증 자 하나로 상태 전부

### 무엇을 지었나 (파일)
- `web/app/project.js` (신규) — 짓는 함수 · 문구 표 · 저장 관문
- `web/css/project.css` (신규) — 배치와 밀도만. 색 리터럴 0, 라운드 0, 그림자 0, 좌측 세로 띠 0
- `web/verify-project.html` (신규) — 진짜 렌더 함수 + mock 행으로 상태 13갈래를 한 장에
- `tests/test_project_screen.py` (신규) — 31건 (읽어서 아는 계약 16 + 실브라우저 15)
- `web/index.html` — **2줄만** (아래 「불가피한 등록」 참조)

### 컴포넌트 명세 (부르는 쪽 계약)
| 함수 | 무엇을 돌려주나 | 비고 |
|---|---|---|
| `prjListHTML(list, o)` | Docs›PROJECT 목록 한 장(머리 + 행 + 보관 접힘) | o = {canCreate, statsBy, selected, expanded, arcOpen, limit=20} |
| `prjRowHTML(p, o)` | 목록 행 하나 — 문서 목록의 `.row/.st/.id` 문법 그대로 + `.pjmeta` 셋째 줄 | |
| `prjSort(list, statsBy)` | 최근 활동순 | 이름순 아님(설계 판정) |
| `prjStats(rows, slug, now)` | {open, total, last, when} | **catalog 파생 — 저장소 신설 없음** |
| `prjFormHTML(v)` / `prjFormCheck(v, taken)` / `prjFormWire(root, v, o)` | 생성 창의 네 줄 · 순수 검사 · 배선 | 창 껍데기는 `s9dlg` 것 |
| `prjCreateDlg(o)` | 창을 열고 `/api/project/add` 까지 | |
| `prjPanelHTML(p, o)` | PRJ 문서 뷰 패널(설정 격자 + 멤버 표 + 관리 요약 + 실패 줄) | 격자는 `.metatbl` 재사용 |
| `prjStripHTML(p, o)` | Board 문맥 띠 한 줄(32px) | 프로젝트 없으면 빈 문자열 |
| `prjWire(root, p, o)` | 인라인 편집 · status 확인 창 · 멤버 추가/변경/제거 배선 | 전부 `prjPost` 한 문을 지난다 |
| `prjPost(path, payload, o)` | **저장 관문 하나** | o.post 로 갈아 끼울 수 있어 시험이 서버 없이 돈다 |

### mock API 가정 (A · REQ-20260831-027-62x6 와 B2 에서 맞출 것)
- `POST /api/project/add {user,slug,name,summary,customer}` → `{ok:true,id,slug}` | `{ok:false,error}`
- `POST /api/project/set {user,slug, name?|summary?|customer?|status?|contact_*?}` → `{ok:true}` | `{ok:false,error}`
- `POST /api/project/member`·`/member/rm` — **기존 그대로** (화면이 새 필드를 요구하지 않는다)
- `GET /api/projects` — 기존 모양 그대로. **화면은 `history_tail` 을 쓰지 않는다**(관리 요약은 catalog 집계) — A 가 넣더라도 B1 은 안 읽는다
- 계약 셋: ① 거부 사유는 서버 문장을 **그대로** 그린다(화면이 짓지 않는다) ② 변이 성공 뒤 한 번 다시 읽는다(폴링 벨트 밖) ③ 인가 판정은 서버 단일 경로 — 화면의 권한 계산은 무엇을 그릴지만 정한다

### C 판정 대상 문자열 (PRJ_TEXT 한 곳 · 전량 초안)
목록: head/create/none/fold/rowMeta/never/agoNone/more/agoNow/agoMin/agoHour/agoYesterday/agoDay
창: dlgCap/dlgTitle/dlgDesc/dlgOk/dlgCancel/fName/fSlug/fSummary/fCustomer/phName/phSlug/phSummary/phCustomer/hintSlug/errName/errSlugEmpty/errSlugBad/errSlugTaken
패널: 
띠: stripMeta/stripOpen
→ 표 밖에 화면 글자가 있으면 시험이 잡는다(`TheWordsLiveInOnePlace`). 같은 이름 두 번 적는 것도 잡는다 — 실제로 `none` 을 두 번 적어 빈 상태 문구가 통째로 「없음」이 된 사고가 있었고, 그때 시험은 전부 초록이었다(캡처를 눈으로 봐서 잡았다).

### 캡처가 잡아 고친 결함 넷 (전부 코드로는 안 보이던 것)
1. 마지막 owner 의 「제거」가 **살아 있는 단추로 보였다** — `.acts button` 이 잉크색을 못박아 브라우저 기본 회색을 덮고, hover 반전이 disabled 를 가리지 않았다 → `.acts.pjcell button:disabled(:hover)` 로 회색 고정 (회귀: `test_a_dead_button_looks_dead`)
2. 만료된 줄에서 **글자만 물러나고 컨트롤은 또렷했다** — 컨트롤은 색을 물려받지 않는다 → `.pmem tr.exp .pjc{color:var(--muted)}`
3. 문맥 띠의 상태가 목록과 **다른 얼굴**(소문자)이었다 → `.pjs-st` 로 대문자 mono, slug 는 그대로(SECTION9 가 되면 없는 이름이 된다)
4. 관리 요약이 「마지막 활동 **활동 없음**」으로 겹쳐 읽혔다 → 자리에 따라 다른 낱말(agoNone)

### 검증
- `python3 tests/ project_screen` → **31/31 ok** (실브라우저 15건 포함: 정렬·보관 접힘·0/1/N 구조 불변·권한 셋·멤버 0 두 갈래·마지막 owner·만료색·띠 32px·네 필드·한글 slug 후보 없음·중복 이름·저장 1회·거부 원복)
- `python3 tests/ web_split` → 25/25 ok
- 스킨 **10종 × 배율 1·1.25·1.5 = 30장** 전수 캡처 후 확대 육안. 실포인터(CDP `Input.dispatchMouseEvent`) hover/focus 를 ledger·terminal 두 맥락 × 세 배율에서 잼:

      대상        x1     x1.25   x1.5   판정
      새 프로젝트  251px   337     387   밑줄 — 이웃 침범 없음
      보관됨 N개   283     338     407   같음
      값 자리      85      105     126   점선 밑줄 한 줄, 글자와 안 겹침
      권한 셀렉트  258      324     388   테두리 잉크
      제거         880     1344    1920  반전 — 칠이 단추 상자 안(43.6px)에 정확히
      제거(잠김)   0        0       0    **바뀌지 않음** = 못 누르는 단추는 안 뒤집힌다
      문서 열기    0        0       0    → 한계 참조

### 불가피한 등록 (지시와 다른 한 곳)
`web/index.html` 에 2줄(css/project.css · app/project.js)을 넣었다. 지시는 「등록 diff 는 B2 에서」였으나 이 저장소에는 **등록되지 않은 조각이라는 것이 없다** — `tests/test_web_split.py` 의 `test_s2_no_orphan_part` 가 `web/app/`·`web/css/` 의 파일 목록과 껍데기의 호출 목록이 **정확히 같기를** 요구한다(안 부르는 조각은 고쳐도 화면이 안 바뀌므로). 넣지 않으면 그 게이트가 붉은 채로 B2 까지 간다. 두 파일은 **불활성**이다: project.js 는 부를 때만 도는 함수와 상수만 선언하고(로드 시 아무것도 실행하지 않는다), project.css 는 기존 셀렉터를 하나도 건드리지 않는다 — 지금 대시보드의 화면은 한 화소도 달라지지 않는다. B2 는 이 2줄을 다시 넣을 필요가 없다.

### B2 로 넘기는 것
1. **`.proj-info` 블록 삭제가 B2 의 첫 손이다** — `web/css/header.css` 163~236행. 지금 규칙이 두 벌인 것은 이번 물결이 기존 css 를 안 만지기 때문이고, 두 벌인 채 끝나면 관문이 둘이 된다. 마크업은 `.pmem` 을 입히면 된다(이름 매핑: table→`.pmem` · `.pi-x`→`.acts.pjcell button` · `.m-act/.m-exp`→`.m-on/.m-off` · select/input→`.pjc` · `.pi-err`→`.pjerr`)
2. **프로젝트 행의 관문은 하나여야 한다** — 권고: Docs 가 `curType==="project"` 일 때 목록을 `prjListHTML` 로 통째 그린다(docs.js rowHTML 에 project 분기를 더하면 행 문법이 두 벌이 된다)
3. `data-prjnew`·`data-prjmore`·`data-prjarc`·`data-pjset` 등의 손잡이를 events.js 위임에 연결. `prjWire` 는 부르는 쪽이 `reload`(다시 읽고 다시 그리기)를 주어야 원복이 완성된다
4. density=compact 규칙을 project.css 에 임시로 두었다 — density.css 로 옮길지는 그 축 주인이 판정
5. 문맥 띠를 `#proj-info` 자리에 앉히고 244px 표를 걷어낸다

### designer 판정을 청하는 셋
1. **「+ 새 프로젝트」가 화면에서 가장 흐린 잉크다**(`.more` = `--faint`, 대비 ~2.8:1). 설계가 「N개 더 보기와 같은 문법」이라 했고 그대로 따랐지만, 이 REQ 가 푸는 문제가 「문이 없다」인데 그 문을 가장 옅게 그린 셈이다. hover 에서는 `--text` 로 올라온다
2. **active 점의 잉크를 `--text`(잉크)로 했다** — 설계는 「프로젝트 타입 잉크」라 했으나 `--t-project` 토큰이 없다. 토큰 신설은 tokens.css(단일 소유자)를 여는 일이라 B1 에서 하지 않았다. archived 는 `--c-cancelled`
3. **표 안 컨트롤 과녁을 28px 로 못박았다**(44px 아님). 44px 면 행이 44px 이 되어 밀도 규칙과 부딪히고, 이 저장소의 확정 과녁은 벨트 글리프 27px 이다. compact 에서도 안 줄어든다

### 한계 (미검증으로 남긴 것)
- **「프로젝트 문서 열기」의 hover 얼굴은 검증되지 않았다** — `a.doclink` 는 base CSS 에 `:hover` 규칙이 없고, 실제 피드백은 JS hovercard 다(골격에는 없다). 여기에만 새 hover 를 더하면 다른 모든 doclink 와 얼굴이 갈리므로 더하지 않았다 — B2 에서 실화면으로 다시 볼 것
- 좁은 판(<520px)에서 멤버 표는 제 상자 안에서 가로로 구른다(`.pmemwrap`). 대조표의 두 칸 배치(572px)에서는 머리글이 두 줄로 접히는데, 실제 문서 판은 그보다 넓다 — 실화면 폭에서 다시 볼 것
- 서버 실연동은 하지 않았다(mock). `/api/project/add`·`set` 은 아직 없다

[Image: assets/REQ-20260831-028-62x6/ledger-x1.png]
[Image: assets/REQ-20260831-028-62x6/terminal-x1.25.png]
[Image: assets/REQ-20260831-028-62x6/h-ledger-rm-x1.25-hover.png]
[Image: assets/REQ-20260831-028-62x6/h-ledger-val-x1.5-hover.png]
[Image: assets/REQ-20260831-028-62x6/h-ledger-new-x1.5-focus.png]

### 2026-08-31T23:38:24+09:00 response (agent: sub:frontend-developer, by nicehugepark)

### 보고 이후 고친 넷 (전부 눈으로 보거나 전체 실행에서 드러난 것)

1. **담당자를 한 줄에서 네 줄로 폈다.** 설계 스케치의 `CONTACT — (이름 · 소속 · 메일 · 전화)` 는 값을 **읽는** 모습인데, 인라인 편집이 붙는 순간 그 한 칸에 네 필드가 든다 — 이어 붙인 글자를 그대로 저장하면 `contact_name` 에 전화번호가 들어간다. 서버도 CLI도 네 필드로 저장하므로 화면도 넷으로 폈다(`contact · 이름/소속/메일/전화`). 한 줄로 보이고 그 자리에서 넷을 고치는 형태가 필요하면 designer 판정.
2. **빈 칸은 채울 수 있는 사람에게만 선다.** 넷으로 펴자 담당자 없는 프로젝트에서 「—」 넉 줄이 읽기 전용 사용자에게도 자리를 먹었다. 문서 뷰의 메타 표는 값 없는 줄을 아예 안 그리는 문법이라(docs.js `fields.filter`) 그 문법을 따랐다 — maintainer 이상에게만 빈 줄이 선다(그 사람에게는 그게 채울 자리다).
3. **빈 날짜 상자의 귀띔을 갈랐다** — 읽는 자리는 「무기한」, 고치는 자리는 「비우면 무기한」.
4. **전체 실행(병렬)에서만 나던 ERROR 14건을 고쳤다.** 원인은 화면이 아니라 시험이었다: `document.readyState === "complete"` 는 **about:blank 에서도 참**이라, `--jobs 4` 로 붙는 순간의 탭이 아직 이동 전이면 빈 판을 재고 그 위의 모든 물음이 `null.disabled` 로 죽었다. 이제 판이 실제로 섰는지(`.case` 수)를 묻고 기다리며, 못 잰 값이 하나라도 있으면 그 자리에서 말한다 — 단독 실행에서는 나지 않던 종류라 남겨 둔다.

검증: `python3 tests/ project_screen` **32/32 ok** (`test_an_empty_slot_is_a_place_only_for_who_can_fill_it` 추가).

### 2026-08-31T23:48:09+09:00 response (agent: sub:frontend-developer, by nicehugepark)

### 전체 실행이 잡은 것 — 저장소 게이트 셋을 어긴 채였다

단독 실행(project_screen 32/32)에서는 안 보이고 `python3 tests/ --jobs 4` 에서 드러났다. 셋 다 **화면 규율이지 내 시험이 아니라서** 내가 안 짠 시험이 잡았다 — 그게 이 게이트들이 있는 이유다.

1. **`을(를)` 를 썼다** (`test_judge_dialog.test_the_josa_is_computed_not_hedged`). 「멤버를 뺍니다」의 조사를 서식 편지투로 얼버무렸다. 이 저장소는 받침으로 계산한다 — `josa()`(card.js) 한 곳이 그 규칙이고, 창 문안 셋(멤버 빼기·보관·되열기)이 이제 그것을 부른다.
2. **확인 창 셋이 대장에 없었다** (`test_dialog_safe` F1·F2). 이 화면의 모든 확인 창은 「맨 Enter 가 어디에 닿아야 하는가」를 `CENSUS` 에 적어야 한다. 셋 다 `safe:false`(물러설 이유가 없다)로 등록했다 — 생성은 만들고 보관하면 그만, 멤버 빼기는 다시 넣으면 원상복구(app.js 의 같은 창과 같은 판정), 보관은 「다시 열기」가 같은 셀렉트에 있다.
   - 부수 판정: 문안이 `PRJ_TEXT` 표에서 오면 대장이 창을 가려낼 이름이 없다 → 두 낱말을 **이름으로** 넘긴다(`ok: rmOk`). card.js 의 `stopAsk` 선례 그대로다.
3. **검증 자가 창 껍데기를 베끼고 있었다.** 위 둘을 고치다 보니 드러났다 — 흉내 낸 `.dlghead/.dlgfoot` 는 실제 캐스케이드와 다를 수 있어 검증이 거짓이 된다. 이제 `dialog.js` 를 그대로 불러 **진짜 창**을 띄우고 그 자리로 옮겨 놓는다(`card.js` 도 함께 — `josa` 가 거기 산다). 베낀 마크업 금지는 시험으로 못박았다(`test_the_fixture_calls_the_real_builders`).

그러고 나서 **창의 손이 첫 칸에 놓이지 않는 것**도 보였다: s9dlg 는 확인 단추에 초점을 준다(물음이 하나인 창의 규칙). 적을 것이 넷인 창에서는 Tab 부터 눌러야 했다 → `prjFormWire` 가 표시명 칸에 손을 놓는다.

검증: `python3 tests/ project_screen dialog_safe judge_dialog web_split` → **93/93 ok**.
전체 실행 첫 회(635건)는 내 시험 14건 외에 **다른 회귀 0** 이었고, 그 14건은 시험 자신의 병렬 취약점(about:blank 를 재고 있었다)이라 고쳤다.

### 2026-08-31T23:52:00+09:00 tdd (agent: sub:frontend-developer, by nicehugepark)

### TDD 시나리오 — B1 골격 (개정 · 통과 표시)

검증 자 `web/verify-project.html`(진짜 렌더 함수 + 진짜 창) · 회귀 `tests/test_project_screen.py` 34건 (읽어서 아는 계약 19 + 실브라우저 15). 실행: `python3 tests/ project_screen`

### 렌더 계약 — Docs 좌측 PROJECT 목록
- [x] S1 N개: 최근 활동순 · 20 + 「N개 더 보기」 · 행마다 세 줄
- [x] S2 0개: 안내가 아니라 문장 한 줄 + 머리의 「새 프로젝트」(단추가 자리를 옮기지 않는다)
- [x] S3 1개: 0→1→N 구조 불변
- [x] S4 보관됨: 「보관됨 N개」 접힘 · ARCHIVED 글자가 색 없이도 말한다
- [x] S5 만들 권한 없음: 아예 안 그린다

### 렌더 계약 — PRJ 문서 뷰 패널
- [x] S6 멤버 0 + 넣을 사람 있음: 추가 행이 표 자리에
- [x] S7 멤버 0 + 등록 사용자 0: 다른 문구 + Settings›사용자 관리 링크, 추가 폼 없음
- [x] S8 뷰어: 편집 컨트롤 0개 + 권한 한 줄 (빈 설정 칸도 안 그린다 — 채울 수 있는 사람의 자리다)
- [x] S9 manage≠own: owner 행만 잠김 + 이유
- [x] S10 마지막 owner: 「제거」 잠김 + 이유 + **잠긴 얼굴**(회색 · hover 반전 없음)
- [x] S11 만료 임박(≤14일): 만료일 잉크만 `--c-inprogress`
- [x] S12 만료됨: `--muted` (붉은 잉크 아님) · 컨트롤까지 함께 물러난다
- [x] S13 관리 요약: catalog 파생 계산(별도 저장 없음)

### 저장 규칙 하나 (인라인)
- [x] S14 설정·멤버가 같은 관문(`prjPost`)을 지난다 — `fetch` 는 파일에 하나뿐
- [x] S15 변경 1회 = 요청 1회 · 값이 그대로면 요청 0회
- [x] S16 실패: 원복 + **다시 그린 뒤의 자리**에 서버가 준 문장 그대로
- [x] S17 status 만 확인 창 · 값 편집은 묻지 않는다

### 생성 다이얼로그
- [x] S18 필드 넷만(담당자 4필드 없음)
- [x] S19 창은 `s9dlg` 것 — 껍데기를 스스로 짓지 않는다 (검증 자도 진짜 창을 띄운다)
- [x] S20 빈 표시명·한글 slug·중복 이름을 그 자리에서, 확인 단추는 통과할 때만 눌린다
- [x] S20b 손은 첫 칸에 놓인다(창의 기본 초점은 확인 단추 — 적을 것이 넷인 창은 다르다)

### Board 문맥 띠
- [x] S21 32px 한 줄 · 표 없음 · 좌측 띠·색면 없음 · doclink
- [x] S22 프로젝트 미선택이면 자리도 없다

### 시각 언어 · 배율 · 스킨 전수
- [x] S23 색 리터럴 0 · 라운드 0 · 그림자 0 · 좌측 세로 띠 0
- [x] S24 배율 불변: 새 홀수 px·소수 테두리 없음 (7px 점은 확정 무대 재사용)
- [x] S25 `.proj-info` 복제 0 — 멤버 표 규칙은 `.pmem` 한 곳
- [x] S26 문구는 `PRJ_TEXT` 한 곳 · 같은 이름 두 번 금지
- [x] S27 스킨 10종 × 배율 1·1.25·1.5 캡처 확대 육안
- [x] S28 hover/focus 실포인터(CDP) — ledger·terminal 두 맥락 × 세 배율
  - 예외 하나: 「프로젝트 문서 열기」(doclink)는 base CSS 에 hover 규칙이 없고 실제
    피드백이 JS hovercard 라 골격에서는 검증 불가 — B2 실화면 몫

### 저장소 게이트 (내가 안 짠 시험이 잡은 것 — 전부 초록)
- [x] G1 `을(를)` 금지 → `josa()` 계산 (test_judge_dialog)
- [x] G2 확인 창 셋이 대장(CENSUS)에 · `safe` 판정 (test_dialog_safe)
- [x] G3 조각 등록 (test_web_split — 등록 안 된 조각이라는 것은 이 저장소에 없다)

### 2026-09-01T00:03:49+09:00 response (agent: sub:frontend-developer, by nicehugepark)

### 캡처를 믿을 수 있게 만든 것 (B2 가 같은 자를 쓴다)

1.5배 캡처 한 장이 **민얼굴**로 찍혔다 — 규칙이 하나도 안 온 채 그려진 그림이었다. 원인은 화면이 아니라 검증 절차다: `web/verify-project.html` 에는 껍데기(`web/index.html`)가 가진 **되찾기 그물이 없다**(조각이 잘려 와도 다시 걸어 주는 그 코드). 조각을 한 탭에서 열 번 갈아 끼우면 그중 한 번은 규칙이 덜 온 채로 찍힌다.

고친 방식은 우회가 아니라 **세는 것**이다: 찍기 전에 `document.styleSheets.length`(15)와 `.case` 수를 확인하고, 안 오면 그 자리에서 멈춘다("캡처를 믿을 수 없다"). 그 뒤 배율 1·1.25·1.5 를 다시 찍어 **셋이 같은 모양**임을 눈으로 확인했다.

B2 에서 이 검증 자로 캡처할 때 같은 함정이 있다 — 스킨을 갈아 끼우며 연속 촬영하면 반드시 「규칙이 다 왔나」를 세고 찍어라. (덤: 촬영 도구를 남이 재료로 부를 수 있게 `if __name__ == "__main__"` 로 감쌌다 — 안 감싸면 import 하는 순간 전수 촬영이 통째로 한 번 더 돈다. 이것 때문에 1.5배가 실패한 것처럼 보였다.)

### 전체 실행 최종 결과

`python3 tests/ --jobs 4` — 실패 **1건**, 그마저 회귀가 아니다: `test_port_pool.BoundedReuse.test_allocated_port_is_usable` 의 `Address already in use`. 스위트가 도는 동안 내가 진단용 브라우저를 같은 포트 풀에서 띄운 탓이다(그 시험이 검사하려던 바로 그 자원을 내가 잡았다). **그 파일 단독 재실행 29/29 ok** — 그리고 이 사고 자체가 포트 풀 규율이 왜 있는지 보여 준다: 캡처 도구도 풀을 쓰되 스위트와 동시에 돌리지 마라.

[Image: assets/REQ-20260831-028-62x6/ledger-x1.5.png]
[Image: assets/REQ-20260831-028-62x6/terminal-x1.25-2.png]

### 2026-09-01T00:04:43+09:00 subagent (agent: sub:frontend-developer:aea99b3d, by nicehugepark)

`[2026-09-01 00:04:12 KST - frontend-developer]`

### 결론 — B1 골격 완료. 신규 세 파일에 격리, 상태 전부를 한 검증 자에 세웠고 실브라우저로 판정했다

### 지은 것
- `/home/sjpark1/section9/web/app/project.js` (589줄) — 짓는 함수 · 문구 표 · **저장 관문 하나**
- `/home/sjpark1/section9/web/css/project.css` (159줄) — 배치와 밀도만. 색 리터럴 0, 라운드 0, 그림자 0, 좌측 세로 띠 0
- `/home/sjpark1/section9/web/verify-project.html` (209줄) — 진짜 렌더 함수 + **진짜 창**(dialog.js) + mock 행으로 상태 14갈래
- `/home/sjpark1/section9/tests/test_project_screen.py` (573줄) — 34건 (읽어서 아는 계약 19 + 실브라우저 15)
- `/home/sjpark1/section9/tests/test_dialog_safe.py` — 확인 창 대장에 셋 등록(+9줄)
- `/home/sjpark1/section9/web/index.html` — **2줄만** (아래)

### 지시와 다른 한 곳 (먼저 보고합니다)
「등록 diff 는 B2 에서」였으나 index.html 에 2줄(css/project.css · app/project.js)을 넣었습니다. 이 저장소에는 **등록되지 않은 조각이라는 것이 없습니다** — `tests/test_web_split.py`의 `test_s2_no_orphan_part`가 `web/app/`·`web/css/` 파일 목록과 껍데기 호출 목록이 정확히 같기를 요구하고, 넣지 않으면 그 게이트가 B2까지 붉은 채로 갑니다. 두 파일은 **불활성**입니다(project.js는 로드 시 아무것도 실행하지 않고, project.css는 기존 셀렉터를 하나도 건드리지 않음) — 지금 대시보드는 한 화소도 달라지지 않습니다. B2는 이 2줄을 다시 넣을 필요가 없습니다.

### 검증
- `python3 tests/ project_screen` **34/34 ok** · `project_screen dialog_safe judge_dialog web_split` **93/93 ok**
- 전체 `python3 tests/ --jobs 4` — 실패 1건이나 회귀 아님: `test_port_pool` 포트 경합(스위트가 도는 동안 내가 진단 브라우저를 같은 풀에서 띄운 탓). 그 파일 단독 **29/29 ok**
- **스킨 10종 × 배율 1·1.25·1.5 = 30장** 확대 육안 + 실포인터(CDP) hover/focus 를 ledger·terminal 두 맥락 × 세 배율에서 측정. 잠긴 「제거」는 세 배율 모두 **diff=0**(안 뒤집힘), 칠은 단추 상자(43.6px) 안에 정확히, 값 자리 점선 밑줄은 글자·이웃 줄과 안 겹침

### 눈과 게이트가 잡은 결함 (코드로는 안 보이던 것)
1. 마지막 owner 「제거」가 **살아 있는 단추로** 보였다 → `.acts.pjcell button:disabled(:hover)`
2. 만료 줄에서 글자만 물러나고 **컨트롤은 또렷**했다 → `.pmem tr.exp .pjc`
3. 띠의 상태가 목록과 다른 얼굴(소문자)이었다 → `.pjs-st`
4. `PRJ_TEXT`에 같은 이름을 두 번 적어 **빈 상태 문구가 통째로 「없음」**이 됐다(시험은 전부 초록이었다) → 중복 키 금지 시험 추가
5. `을(를)` 서식 편지투 → `josa()` (test_judge_dialog)
6. 확인 창 셋이 **대장(CENSUS)에 없었다** → 등록 + 문안을 이름으로 넘김 (test_dialog_safe)
7. 검증 자가 **창 껍데기를 베끼고** 있었다 → 진짜 창을 띄우고, 베끼기 금지를 시험으로
8. 네 칸짜리 창인데 손이 **확인 단추에** 놓였다 → 첫 칸으로

### designer 판정을 청합니다 (셋)
1. 「+ 새 프로젝트」가 화면에서 **가장 흐린 잉크**(`.more` = `--faint`, ~2.8:1). 설계가 「N개 더 보기와 같은 문법」이라 했고 그대로 따랐지만, 이 REQ가 푸는 문제가 「문이 없다」인데 그 문을 가장 옅게 그린 셈입니다
2. active 점 잉크를 `--text`로 했습니다 — `--t-project` 토큰이 없고, 토큰 신설은 tokens.css(단일 소유자)를 여는 일이라 B1에서 하지 않았습니다
3. 담당자를 **네 줄로 폈습니다**(설계 스케치는 한 줄). 한 칸에 네 필드를 이어 붙여 저장하면 이름 칸에 전화번호가 들어갑니다. 빈 칸은 고칠 수 있는 사람에게만 섭니다

### B2 인계 (순서대로)
1. **첫 손은 `web/css/header.css` 163~236행 삭제** — `.proj-info` 규칙이 지금 두 벌입니다(이번 물결이 기존 css를 안 만져서). 매핑: table→`.pmem` · `.pi-x`→`.acts.pjcell button` · `.m-act/.m-exp`→`.m-on/.m-off` · select/input→`.pjc` · `.pi-err`→`.pjerr`
2. 프로젝트 행의 관문은 하나로 — Docs가 `curType==="project"`일 때 `prjListHTML`로 통째 그리기 권고(docs.js rowHTML에 분기를 더하면 행 문법이 두 벌)
3. `prjWire`는 부르는 쪽이 **`reload`(다시 읽고 다시 그리기)를 주어야** 원복이 완성됩니다 — 낙관적 갱신 금지
4. mock API 가정은 REQ 노트에 표로 적었습니다(A/027과 맞출 것). 화면은 `history_tail`을 쓰지 않습니다

### 한계
- 「프로젝트 문서 열기」의 hover 얼굴은 **검증되지 않았습니다** — `a.doclink`는 base CSS에 hover 규칙이 없고 실제 피드백이 JS hovercard라 골격에는 없습니다. 여기에만 새 hover를 더하면 다른 모든 doclink와 얼굴이 갈리므로 더하지 않았습니다(B2 실화면 몫)
- 서버 실연동 없음(mock) — `/api/project/add`·`set`은 아직 없습니다
- 검증 자에는 껍데기의 되찾기 그물이 없어 연속 촬영 시 규칙이 덜 온 채 찍히는 일이 있습니다(실제로 1.5배 한 장이 민얼굴로 찍혔고, 그래서 촬영 도구가 「규칙이 다 왔나」를 세고 멈추게 했습니다) — B2도 같은 함정

기록은 전부 `REQ-20260831-028-62x6`에 남겼습니다(tdd 개정 + response 4건, 캡처 7장 첨부). git 조작은 하지 않았습니다.

### 2026-09-01T00:05:07+09:00 subagent (agent: sub:frontend-developer:aea99b3d, by nicehugepark)

`[2026-09-01 00:04:38 KST - frontend-developer]`

배경 작업(1.5배 캡처 대기) 종료 알림이며, 새 지시가 아닙니다. 앞선 보고 이후 달라진 것은 없습니다 — B1 골격은 완료 상태이고, 검증·기록은 모두 끝났습니다.

최종 상태 재확인:
- 산출물 `/home/sjpark1/section9/web/app/project.js` · `/home/sjpark1/section9/web/css/project.css` · `/home/sjpark1/section9/web/verify-project.html` · `/home/sjpark1/section9/tests/test_project_screen.py`, 그리고 `web/index.html` 2줄 · `tests/test_dialog_safe.py` 대장 등록
- 시험: project_screen 34/34, 관련 넷 합쳐 93/93, 전체 스위트는 회귀 0(유일한 실패는 내가 동시에 띄운 진단 브라우저와의 포트 경합, 단독 재실행 29/29)
- 캡처: 스킨 10종 × 배율 1·1.25·1.5 전수 + 실포인터 hover/focus, 마지막 세 스킨(cobalt·field·cork)의 1.5배까지 확보해 육안 판정 완료
- 기록: `REQ-20260831-028-62x6` 에 tdd 개정 1건 + response 4건, 캡처 7장 첨부

다음 손(B2)이 가장 먼저 할 일은 `web/css/header.css` 163~236행의 `.proj-info` 블록 삭제입니다 — 지금 규칙이 두 벌인 유일한 자리이고, 그대로 두면 관문이 둘이 됩니다.

## History
- 2026-08-31T22:52:31+09:00 created by nicehugepark (status: open)
- 2026-08-31T22:52:38+09:00 status: open -> in-progress (by nicehugepark) — W1 착수
