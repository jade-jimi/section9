---
id: REQ-20260831-028-62x6
type: request
title: 프로젝트 화면
summary: Docs 좌측 PROJECT 진입점·새 프로젝트 다이얼로그·PRJ 문서 뷰 통합(멤버·고객·설정 인라인)·Board 패널 한 줄 띠 축소 — 신규 web/app/project.js·web/css/project.css 격리, B1 골격(mock)→B2 실연동
goal: Docs 에서 프로젝트 목록·생성·상세(멤버·고객·설정)를 처리하고 Board 패널이 한 줄 띠가 된 것이 실브라우저(스킨 전수·배율 1/1.25/1.5 육안)로 확인된다
status: in-progress
size: L
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: 38ad98c8
agents: ["sub:frontend-developer:aea99b3d", "sub:frontend-developer", "sub:frontend-developer:ad4a83f9", "lead:claude-fable-5"]
contributions: [{"actor": "sub:frontend-developer:aea99b3d", "item": "028 프로젝트 화면 골격", "started": "2026-08-31T22:53:17+09:00", "ended": "2026-09-01T00:04:43+09:00", "result": "done", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/38ad98c8-51fd-4b50-a26a-495b58404817/tasks/aea99b3da0560bcd2.output"}, {"actor": "sub:frontend-developer", "item": "TDD 시나리오 — B1 프로젝트 화면 골격 (mock)", "started": "2026-08-31T22:58:31+09:00", "ended": "2026-08-31T22:58:31+09:00", "result": "done", "transcript": ""}, {"actor": "sub:frontend-developer", "item": "B1 골격 완료 — 신규 두 파일에 격리, 검증 자 하나로 상태 전부", "started": "2026-08-31T23:32:58+09:00", "ended": "2026-08-31T23:32:58+09:00", "result": "done", "transcript": ""}, {"actor": "sub:frontend-developer", "item": "보고 이후 고친 넷 (전부 눈으로 보거나 전체 실행에서 드러난 것)", "started": "2026-08-31T23:38:24+09:00", "ended": "2026-08-31T23:38:24+09:00", "result": "done", "transcript": ""}, {"actor": "sub:frontend-developer", "item": "전체 실행이 잡은 것 — 저장소 게이트 셋을 어긴 채였다", "started": "2026-08-31T23:48:09+09:00", "ended": "2026-08-31T23:48:09+09:00", "result": "done", "transcript": ""}, {"actor": "sub:frontend-developer", "item": "TDD 시나리오 — B1 골격 (개정 · 통과 표시)", "started": "2026-08-31T23:52:00+09:00", "ended": "2026-08-31T23:52:00+09:00", "result": "done", "transcript": ""}, {"actor": "sub:frontend-developer", "item": "캡처를 믿을 수 있게 만든 것 (B2 가 같은 자를 쓴다)", "started": "2026-09-01T00:03:49+09:00", "ended": "2026-09-01T00:03:49+09:00", "result": "done", "transcript": ""}, {"actor": "sub:frontend-developer:aea99b3d", "item": "`[2026-09-01 00:04:12 KST - frontend-developer]`", "started": "2026-09-01T00:04:43+09:00", "ended": "2026-09-01T00:04:43+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/38ad98c8-51fd-4b50-a26a-495b58404817/subagents/agent-aea99b3da0560bcd2.jsonl"}, {"actor": "sub:frontend-developer:aea99b3d", "item": "`[2026-09-01 00:04:38 KST - frontend-developer]`", "started": "2026-09-01T00:05:07+09:00", "ended": "2026-09-01T00:05:07+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/38ad98c8-51fd-4b50-a26a-495b58404817/subagents/agent-aea99b3da0560bcd2.jsonl"}, {"actor": "sub:frontend-developer:aea99b3d", "item": "`[2026-09-01 00:04:52 KST - frontend-developer]`", "started": "2026-09-01T00:06:47+09:00", "ended": "2026-09-01T00:06:47+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/38ad98c8-51fd-4b50-a26a-495b58404817/subagents/agent-aea99b3da0560bcd2.jsonl"}, {"actor": "sub:frontend-developer:ad4a83f9", "item": "028 B2 프로젝트 화면 실연동", "started": "2026-09-01T00:07:18+09:00", "ended": "2026-09-01T00:20:42+09:00", "result": "stalled", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/38ad98c8-51fd-4b50-a26a-495b58404817/tasks/ad4a83f9e63c01211.output", "reason": "194초째 진전 없음 (기준 180초)"}, {"actor": "lead:claude-fable-5", "item": "B1 리드 검수: 재실측 6스위트 green(web_split 일시 경합 1회 후 재실행 OK), 커밋 e8e7587 푸시. ", "started": "2026-09-01T00:07:29+09:00", "ended": "2026-09-01T00:07:29+09:00", "result": "done", "transcript": ""}, {"actor": "sub:frontend-developer", "item": "TDD 시나리오 — B2 실연동 (개정 · B1 세대를 대체한다)", "started": "2026-09-01T00:16:06+09:00", "ended": "2026-09-01T00:16:06+09:00", "result": "done", "transcript": ""}, {"actor": "sub:frontend-developer", "item": "TDD 시나리오 — B2 실연동 (개정 · 통과 표시)", "started": "2026-09-01T01:14:56+09:00", "ended": "2026-09-01T01:14:56+09:00", "result": "done", "transcript": ""}, {"actor": "sub:frontend-developer", "item": "B2 실연동 완료 — 관문이 하나가 됐고, 화면은 실제로 서버를 지난다", "started": "2026-09-01T01:21:11+09:00", "ended": "2026-09-01T01:21:11+09:00", "result": "done", "transcript": ""}, {"actor": "sub:frontend-developer", "item": "전체 실행의 붉은 넷 — 하나도 이 물결의 것이 아니다 (앞 노트 보완)", "started": "2026-09-01T01:22:14+09:00", "ended": "2026-09-01T01:22:14+09:00", "result": "done", "transcript": ""}, {"actor": "sub:frontend-developer", "item": "지시와 다르게 판정한 셋 (먼저 보고합니다 — 리드가 뒤집을 수 있게)", "started": "2026-09-01T01:22:50+09:00", "ended": "2026-09-01T01:22:50+09:00", "result": "done", "transcript": ""}, {"actor": "sub:frontend-developer:ad4a83f9", "item": "`[2026-09-01 01:22:56 KST - frontend-developer]`", "started": "2026-09-01T01:23:32+09:00", "ended": "2026-09-01T01:23:32+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/38ad98c8-51fd-4b50-a26a-495b58404817/subagents/agent-ad4a83f9e63c01211.jsonl"}, {"actor": "sub:frontend-developer:ad4a83f9", "item": "`[2026-09-01 01:23:0x KST - frontend-developer]`", "started": "2026-09-01T01:23:59+09:00", "ended": "2026-09-01T01:23:59+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/38ad98c8-51fd-4b50-a26a-495b58404817/subagents/agent-ad4a83f9e63c01211.jsonl"}, {"actor": "sub:frontend-developer:ad4a83f9", "item": "`[2026-09-01 01:25:42 KST - frontend-developer]`", "started": "2026-09-01T01:25:57+09:00", "ended": "2026-09-01T01:25:57+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/38ad98c8-51fd-4b50-a26a-495b58404817/subagents/agent-ad4a83f9e63c01211.jsonl"}]
project: section9
parent: REQ-20260831-026-62x6
relates_why: {}
tags: ["project", "access", "dashboard", "attached", "png", "image", "terminal"]
created: 2026-08-31T22:52:31+09:00
status_since: 2026-08-31T22:52:38+09:00
updated: 2026-09-01T01:25:57+09:00
priority: 50
---

## Original

프로젝트 화면

## Notes

### 2026-08-31T22:58:31+09:00 tdd (agent: sub:frontend-developer, by nicehugepark)

### TDD 시나리오 — B1 프로젝트 화면 골격 (mock)

검증 자: `web/verify-project.html`(진짜 렌더 함수 + mock 행) · 회귀: `tests/test_project_screen.py`(신규, stdlib only, 서버 미기동)

### 렌더 계약 — Docs 좌측 PROJECT 목록
- [ ] S1 N개: 최근 활동순 정렬 · 20 + 「N개 더 보기」 · 행마다 세 줄(제목+상태 / ID·slug / 멤버·열린 요청·마지막 활동)
- [ ] S2 0개: 안내가 아니라 행동 — 빈 문구 한 줄 + 「새 프로젝트」 단추 하나
- [ ] S3 1개: 0→1→N 에서 구조가 바뀌지 않는다(머리·행·단추 자리 동일)
- [ ] S4 보관됨: 「보관됨 N개」로 접힘, 펼치면 잉크 --c-cancelled + 오른쪽 ARCHIVED 글자(색 없이도 읽힌다)
- [ ] S5 만들 권한 없음: 「새 프로젝트」를 **아예 안 그린다**(회색 단추 금지)

### 렌더 계약 — PRJ 문서 뷰 프로젝트 패널
- [ ] S6 멤버 0 + 등록 사용자 있음: 추가 행이 표 자리에 바로 선다(막다른 안내 아님)
- [ ] S7 멤버 0 + 등록 사용자도 0: 다른 문구 + Settings›사용자 관리 링크, 추가 폼 없음
- [ ] S8 뷰어 권한: 편집 컨트롤(select/input/제거) 0개 + 표 아래 권한 한 줄
- [ ] S9 manage 이나 own 아님: owner 행 컨트롤만 disabled + 이유(title) — 여기만 회색 허용
- [ ] S10 마지막 owner: 「제거」 disabled + 이유
- [ ] S11 만료 임박(≤14일): 만료일 잉크만 --c-inprogress + 툴팁
- [ ] S12 만료됨: 상태 「만료」 + 행 잉크 **--muted**(--c-blocked 아님 — 만료는 고장이 아니다)
- [ ] S13 관리 요약: 상태별 요청 수·마지막 활동은 catalog 파생으로 계산(별도 저장 없음)

### 저장 규칙 하나 (인라인)
- [ ] S14 설정 인라인 편집과 멤버 표 변경이 **같은 관문 함수**를 지난다(post 경로 한 곳)
- [ ] S15 변경 1회 = 요청 1회, 값이 안 바뀌면 요청 0회
- [ ] S16 실패: 컨트롤 원복 + 실패 줄(붉은 잉크, 색면 아님) + 다시 시도 가능
- [ ] S17 status 변경만 확인 창을 거친다(보관은 전 문서가 접히는 사건)

### 생성 다이얼로그 — 기존 dlg 문법 재사용
- [ ] S18 필드는 넷만(표시명·slug·개요·고객) — 담당자 4필드 없음
- [ ] S19 기존 다이얼로그 클래스 문법(.dlghead/.dlgbody/.dlgfoot/.acts)만 쓴다 — 새 창 부품 발명 0
- [ ] S20 표시명 비면 진행 불가 · slug 정규식 위반/중복은 그 자리에서 사유 표시(서버 왕복 전 화면 선검사, 판정은 서버가 최종)

### Board 문맥 띠
- [ ] S21 한 줄(32px) · 표 없음 · 아래 hairline 하나 · 「프로젝트 문서 열기」는 doclink 문법
- [ ] S22 프로젝트 미선택이면 자리도 없다

### 시각 언어 · 배율 · 스킨 전수
- [ ] S23 project.css 는 색 리터럴 0(토큰만) · 라운드 0 · 그림자 0 · 좌측 세로 띠 0 · 색면 배지 0
- [ ] S24 배율 불변: 새 홀수 px 치수·소수 px 테두리 없음(점은 확정된 7px .cdot 재사용)
- [ ] S25 CSS 복제 금지: project.css 에 .proj-info 셀렉터 0 — 멤버 표 규칙은 .pmem 한 곳
- [ ] S26 문구는 PRJ_TEXT 한 곳에 모인다(C 판정이 W2 에서 통째로 갈아 끼울 수 있게)
- [ ] S27 스킨 10종 × 배율 1·1.25·1.5 캡처를 확대 육안 판정 — 잘림·겹침·대비 이상 없음
- [ ] S28 hover/focus 는 CDP 실포인터로 건 화소로 판정(정적 캡처 금지) — 칠이 이웃 글자를 파고들지 않고, 반전 스킨(terminal)에서 사라지지 않는다

### 2026-08-31T23:32:58+09:00 response (agent: sub:frontend-developer, by nicehugepark)

### B1 골격 완료 — 신규 두 파일에 격리, 검증 자 하나로 상태 전부

### 무엇을 지었나 (파일)
- `web/app/project.js` (신규) — 짓는 함수 · 문구 표 · 저장 관문
- `web/css/project.css` (신규) — 배치와 밀도만. 색 리터럴 0, 라운드 0, 그림자 0, 좌측 세로 띠 0
- `web/verify-project.html` (신규) — 진짜 렌더 함수 + mock 행으로 상태 13갈래를 한 장에
- `tests/test_project_screen.py` (신규) — 31건 (읽어서 아는 계약 16 + 실브라우저 15)
- `web/index.html` — **2줄만** (아래 「불가피한 등록」 참조)

### 컴포넌트 명세 (부르는 쪽 계약)
| 함수 | 무엇을 돌려주나 | 비고 |
|---|---|---|
| `prjListHTML(list, o)` | Docs›PROJECT 목록 한 장(머리 + 행 + 보관 접힘) | o = {canCreate, statsBy, selected, expanded, arcOpen, limit=20} |
| `prjRowHTML(p, o)` | 목록 행 하나 — 문서 목록의 `.row/.st/.id` 문법 그대로 + `.pjmeta` 셋째 줄 | |
| `prjSort(list, statsBy)` | 최근 활동순 | 이름순 아님(설계 판정) |
| `prjStats(rows, slug, now)` | {open, total, last, when} | **catalog 파생 — 저장소 신설 없음** |
| `prjFormHTML(v)` / `prjFormCheck(v, taken)` / `prjFormWire(root, v, o)` | 생성 창의 네 줄 · 순수 검사 · 배선 | 창 껍데기는 `s9dlg` 것 |
| `prjCreateDlg(o)` | 창을 열고 `/api/project/add` 까지 | |
| `prjPanelHTML(p, o)` | PRJ 문서 뷰 패널(설정 격자 + 멤버 표 + 관리 요약 + 실패 줄) | 격자는 `.metatbl` 재사용 |
| `prjStripHTML(p, o)` | Board 문맥 띠 한 줄(32px) | 프로젝트 없으면 빈 문자열 |
| `prjWire(root, p, o)` | 인라인 편집 · status 확인 창 · 멤버 추가/변경/제거 배선 | 전부 `prjPost` 한 문을 지난다 |
| `prjPost(path, payload, o)` | **저장 관문 하나** | o.post 로 갈아 끼울 수 있어 시험이 서버 없이 돈다 |

### mock API 가정 (A · REQ-20260831-027-62x6 와 B2 에서 맞출 것)
- `POST /api/project/add {user,slug,name,summary,customer}` → `{ok:true,id,slug}` | `{ok:false,error}`
- `POST /api/project/set {user,slug, name?|summary?|customer?|status?|contact_*?}` → `{ok:true}` | `{ok:false,error}`
- `POST /api/project/member`·`/member/rm` — **기존 그대로** (화면이 새 필드를 요구하지 않는다)
- `GET /api/projects` — 기존 모양 그대로. **화면은 `history_tail` 을 쓰지 않는다**(관리 요약은 catalog 집계) — A 가 넣더라도 B1 은 안 읽는다
- 계약 셋: ① 거부 사유는 서버 문장을 **그대로** 그린다(화면이 짓지 않는다) ② 변이 성공 뒤 한 번 다시 읽는다(폴링 벨트 밖) ③ 인가 판정은 서버 단일 경로 — 화면의 권한 계산은 무엇을 그릴지만 정한다

### C 판정 대상 문자열 (PRJ_TEXT 한 곳 · 전량 초안)
목록: head/create/none/fold/rowMeta/never/agoNone/more/agoNow/agoMin/agoHour/agoYesterday/agoDay
창: dlgCap/dlgTitle/dlgDesc/dlgOk/dlgCancel/fName/fSlug/fSummary/fCustomer/phName/phSlug/phSummary/phCustomer/hintSlug/errName/errSlugEmpty/errSlugBad/errSlugTaken
패널: 
띠: stripMeta/stripOpen
→ 표 밖에 화면 글자가 있으면 시험이 잡는다(`TheWordsLiveInOnePlace`). 같은 이름 두 번 적는 것도 잡는다 — 실제로 `none` 을 두 번 적어 빈 상태 문구가 통째로 「없음」이 된 사고가 있었고, 그때 시험은 전부 초록이었다(캡처를 눈으로 봐서 잡았다).

### 캡처가 잡아 고친 결함 넷 (전부 코드로는 안 보이던 것)
1. 마지막 owner 의 「제거」가 **살아 있는 단추로 보였다** — `.acts button` 이 잉크색을 못박아 브라우저 기본 회색을 덮고, hover 반전이 disabled 를 가리지 않았다 → `.acts.pjcell button:disabled(:hover)` 로 회색 고정 (회귀: `test_a_dead_button_looks_dead`)
2. 만료된 줄에서 **글자만 물러나고 컨트롤은 또렷했다** — 컨트롤은 색을 물려받지 않는다 → `.pmem tr.exp .pjc{color:var(--muted)}`
3. 문맥 띠의 상태가 목록과 **다른 얼굴**(소문자)이었다 → `.pjs-st` 로 대문자 mono, slug 는 그대로(SECTION9 가 되면 없는 이름이 된다)
4. 관리 요약이 「마지막 활동 **활동 없음**」으로 겹쳐 읽혔다 → 자리에 따라 다른 낱말(agoNone)

### 검증
- `python3 tests/ project_screen` → **31/31 ok** (실브라우저 15건 포함: 정렬·보관 접힘·0/1/N 구조 불변·권한 셋·멤버 0 두 갈래·마지막 owner·만료색·띠 32px·네 필드·한글 slug 후보 없음·중복 이름·저장 1회·거부 원복)
- `python3 tests/ web_split` → 25/25 ok
- 스킨 **10종 × 배율 1·1.25·1.5 = 30장** 전수 캡처 후 확대 육안. 실포인터(CDP `Input.dispatchMouseEvent`) hover/focus 를 ledger·terminal 두 맥락 × 세 배율에서 잼:

      대상        x1     x1.25   x1.5   판정
      새 프로젝트  251px   337     387   밑줄 — 이웃 침범 없음
      보관됨 N개   283     338     407   같음
      값 자리      85      105     126   점선 밑줄 한 줄, 글자와 안 겹침
      권한 셀렉트  258      324     388   테두리 잉크
      제거         880     1344    1920  반전 — 칠이 단추 상자 안(43.6px)에 정확히
      제거(잠김)   0        0       0    **바뀌지 않음** = 못 누르는 단추는 안 뒤집힌다
      문서 열기    0        0       0    → 한계 참조

### 불가피한 등록 (지시와 다른 한 곳)
`web/index.html` 에 2줄(css/project.css · app/project.js)을 넣었다. 지시는 「등록 diff 는 B2 에서」였으나 이 저장소에는 **등록되지 않은 조각이라는 것이 없다** — `tests/test_web_split.py` 의 `test_s2_no_orphan_part` 가 `web/app/`·`web/css/` 의 파일 목록과 껍데기의 호출 목록이 **정확히 같기를** 요구한다(안 부르는 조각은 고쳐도 화면이 안 바뀌므로). 넣지 않으면 그 게이트가 붉은 채로 B2 까지 간다. 두 파일은 **불활성**이다: project.js 는 부를 때만 도는 함수와 상수만 선언하고(로드 시 아무것도 실행하지 않는다), project.css 는 기존 셀렉터를 하나도 건드리지 않는다 — 지금 대시보드의 화면은 한 화소도 달라지지 않는다. B2 는 이 2줄을 다시 넣을 필요가 없다.

### B2 로 넘기는 것
1. **`.proj-info` 블록 삭제가 B2 의 첫 손이다** — `web/css/header.css` 163~236행. 지금 규칙이 두 벌인 것은 이번 물결이 기존 css 를 안 만지기 때문이고, 두 벌인 채 끝나면 관문이 둘이 된다. 마크업은 `.pmem` 을 입히면 된다(이름 매핑: table→`.pmem` · `.pi-x`→`.acts.pjcell button` · `.m-act/.m-exp`→`.m-on/.m-off` · select/input→`.pjc` · `.pi-err`→`.pjerr`)
2. **프로젝트 행의 관문은 하나여야 한다** — 권고: Docs 가 `curType==="project"` 일 때 목록을 `prjListHTML` 로 통째 그린다(docs.js rowHTML 에 project 분기를 더하면 행 문법이 두 벌이 된다)
3. `data-prjnew`·`data-prjmore`·`data-prjarc`·`data-pjset` 등의 손잡이를 events.js 위임에 연결. `prjWire` 는 부르는 쪽이 `reload`(다시 읽고 다시 그리기)를 주어야 원복이 완성된다
4. density=compact 규칙을 project.css 에 임시로 두었다 — density.css 로 옮길지는 그 축 주인이 판정
5. 문맥 띠를 `#proj-info` 자리에 앉히고 244px 표를 걷어낸다

### designer 판정을 청하는 셋
1. **「+ 새 프로젝트」가 화면에서 가장 흐린 잉크다**(`.more` = `--faint`, 대비 ~2.8:1). 설계가 「N개 더 보기와 같은 문법」이라 했고 그대로 따랐지만, 이 REQ 가 푸는 문제가 「문이 없다」인데 그 문을 가장 옅게 그린 셈이다. hover 에서는 `--text` 로 올라온다
2. **active 점의 잉크를 `--text`(잉크)로 했다** — 설계는 「프로젝트 타입 잉크」라 했으나 `--t-project` 토큰이 없다. 토큰 신설은 tokens.css(단일 소유자)를 여는 일이라 B1 에서 하지 않았다. archived 는 `--c-cancelled`
3. **표 안 컨트롤 과녁을 28px 로 못박았다**(44px 아님). 44px 면 행이 44px 이 되어 밀도 규칙과 부딪히고, 이 저장소의 확정 과녁은 벨트 글리프 27px 이다. compact 에서도 안 줄어든다

### 한계 (미검증으로 남긴 것)
- **「프로젝트 문서 열기」의 hover 얼굴은 검증되지 않았다** — `a.doclink` 는 base CSS 에 `:hover` 규칙이 없고, 실제 피드백은 JS hovercard 다(골격에는 없다). 여기에만 새 hover 를 더하면 다른 모든 doclink 와 얼굴이 갈리므로 더하지 않았다 — B2 에서 실화면으로 다시 볼 것
- 좁은 판(<520px)에서 멤버 표는 제 상자 안에서 가로로 구른다(`.pmemwrap`). 대조표의 두 칸 배치(572px)에서는 머리글이 두 줄로 접히는데, 실제 문서 판은 그보다 넓다 — 실화면 폭에서 다시 볼 것
- 서버 실연동은 하지 않았다(mock). `/api/project/add`·`set` 은 아직 없다

[Image: assets/REQ-20260831-028-62x6/ledger-x1.png]
[Image: assets/REQ-20260831-028-62x6/terminal-x1.25.png]
[Image: assets/REQ-20260831-028-62x6/h-ledger-rm-x1.25-hover.png]
[Image: assets/REQ-20260831-028-62x6/h-ledger-val-x1.5-hover.png]
[Image: assets/REQ-20260831-028-62x6/h-ledger-new-x1.5-focus.png]

### 2026-08-31T23:38:24+09:00 response (agent: sub:frontend-developer, by nicehugepark)

### 보고 이후 고친 넷 (전부 눈으로 보거나 전체 실행에서 드러난 것)

1. **담당자를 한 줄에서 네 줄로 폈다.** 설계 스케치의 `CONTACT — (이름 · 소속 · 메일 · 전화)` 는 값을 **읽는** 모습인데, 인라인 편집이 붙는 순간 그 한 칸에 네 필드가 든다 — 이어 붙인 글자를 그대로 저장하면 `contact_name` 에 전화번호가 들어간다. 서버도 CLI도 네 필드로 저장하므로 화면도 넷으로 폈다(`contact · 이름/소속/메일/전화`). 한 줄로 보이고 그 자리에서 넷을 고치는 형태가 필요하면 designer 판정.
2. **빈 칸은 채울 수 있는 사람에게만 선다.** 넷으로 펴자 담당자 없는 프로젝트에서 「—」 넉 줄이 읽기 전용 사용자에게도 자리를 먹었다. 문서 뷰의 메타 표는 값 없는 줄을 아예 안 그리는 문법이라(docs.js `fields.filter`) 그 문법을 따랐다 — maintainer 이상에게만 빈 줄이 선다(그 사람에게는 그게 채울 자리다).
3. **빈 날짜 상자의 귀띔을 갈랐다** — 읽는 자리는 「무기한」, 고치는 자리는 「비우면 무기한」.
4. **전체 실행(병렬)에서만 나던 ERROR 14건을 고쳤다.** 원인은 화면이 아니라 시험이었다: `document.readyState === "complete"` 는 **about:blank 에서도 참**이라, `--jobs 4` 로 붙는 순간의 탭이 아직 이동 전이면 빈 판을 재고 그 위의 모든 물음이 `null.disabled` 로 죽었다. 이제 판이 실제로 섰는지(`.case` 수)를 묻고 기다리며, 못 잰 값이 하나라도 있으면 그 자리에서 말한다 — 단독 실행에서는 나지 않던 종류라 남겨 둔다.

검증: `python3 tests/ project_screen` **32/32 ok** (`test_an_empty_slot_is_a_place_only_for_who_can_fill_it` 추가).

### 2026-08-31T23:48:09+09:00 response (agent: sub:frontend-developer, by nicehugepark)

### 전체 실행이 잡은 것 — 저장소 게이트 셋을 어긴 채였다

단독 실행(project_screen 32/32)에서는 안 보이고 `python3 tests/ --jobs 4` 에서 드러났다. 셋 다 **화면 규율이지 내 시험이 아니라서** 내가 안 짠 시험이 잡았다 — 그게 이 게이트들이 있는 이유다.

1. **`을(를)` 를 썼다** (`test_judge_dialog.test_the_josa_is_computed_not_hedged`). 「멤버를 뺍니다」의 조사를 서식 편지투로 얼버무렸다. 이 저장소는 받침으로 계산한다 — `josa()`(card.js) 한 곳이 그 규칙이고, 창 문안 셋(멤버 빼기·보관·되열기)이 이제 그것을 부른다.
2. **확인 창 셋이 대장에 없었다** (`test_dialog_safe` F1·F2). 이 화면의 모든 확인 창은 「맨 Enter 가 어디에 닿아야 하는가」를 `CENSUS` 에 적어야 한다. 셋 다 `safe:false`(물러설 이유가 없다)로 등록했다 — 생성은 만들고 보관하면 그만, 멤버 빼기는 다시 넣으면 원상복구(app.js 의 같은 창과 같은 판정), 보관은 「다시 열기」가 같은 셀렉트에 있다.
   - 부수 판정: 문안이 `PRJ_TEXT` 표에서 오면 대장이 창을 가려낼 이름이 없다 → 두 낱말을 **이름으로** 넘긴다(`ok: rmOk`). card.js 의 `stopAsk` 선례 그대로다.
3. **검증 자가 창 껍데기를 베끼고 있었다.** 위 둘을 고치다 보니 드러났다 — 흉내 낸 `.dlghead/.dlgfoot` 는 실제 캐스케이드와 다를 수 있어 검증이 거짓이 된다. 이제 `dialog.js` 를 그대로 불러 **진짜 창**을 띄우고 그 자리로 옮겨 놓는다(`card.js` 도 함께 — `josa` 가 거기 산다). 베낀 마크업 금지는 시험으로 못박았다(`test_the_fixture_calls_the_real_builders`).

그러고 나서 **창의 손이 첫 칸에 놓이지 않는 것**도 보였다: s9dlg 는 확인 단추에 초점을 준다(물음이 하나인 창의 규칙). 적을 것이 넷인 창에서는 Tab 부터 눌러야 했다 → `prjFormWire` 가 표시명 칸에 손을 놓는다.

검증: `python3 tests/ project_screen dialog_safe judge_dialog web_split` → **93/93 ok**.
전체 실행 첫 회(635건)는 내 시험 14건 외에 **다른 회귀 0** 이었고, 그 14건은 시험 자신의 병렬 취약점(about:blank 를 재고 있었다)이라 고쳤다.

### 2026-08-31T23:52:00+09:00 tdd (agent: sub:frontend-developer, by nicehugepark)

### TDD 시나리오 — B1 골격 (개정 · 통과 표시)

검증 자 `web/verify-project.html`(진짜 렌더 함수 + 진짜 창) · 회귀 `tests/test_project_screen.py` 34건 (읽어서 아는 계약 19 + 실브라우저 15). 실행: `python3 tests/ project_screen`

### 렌더 계약 — Docs 좌측 PROJECT 목록
- [x] S1 N개: 최근 활동순 · 20 + 「N개 더 보기」 · 행마다 세 줄
- [x] S2 0개: 안내가 아니라 문장 한 줄 + 머리의 「새 프로젝트」(단추가 자리를 옮기지 않는다)
- [x] S3 1개: 0→1→N 구조 불변
- [x] S4 보관됨: 「보관됨 N개」 접힘 · ARCHIVED 글자가 색 없이도 말한다
- [x] S5 만들 권한 없음: 아예 안 그린다

### 렌더 계약 — PRJ 문서 뷰 패널
- [x] S6 멤버 0 + 넣을 사람 있음: 추가 행이 표 자리에
- [x] S7 멤버 0 + 등록 사용자 0: 다른 문구 + Settings›사용자 관리 링크, 추가 폼 없음
- [x] S8 뷰어: 편집 컨트롤 0개 + 권한 한 줄 (빈 설정 칸도 안 그린다 — 채울 수 있는 사람의 자리다)
- [x] S9 manage≠own: owner 행만 잠김 + 이유
- [x] S10 마지막 owner: 「제거」 잠김 + 이유 + **잠긴 얼굴**(회색 · hover 반전 없음)
- [x] S11 만료 임박(≤14일): 만료일 잉크만 `--c-inprogress`
- [x] S12 만료됨: `--muted` (붉은 잉크 아님) · 컨트롤까지 함께 물러난다
- [x] S13 관리 요약: catalog 파생 계산(별도 저장 없음)

### 저장 규칙 하나 (인라인)
- [x] S14 설정·멤버가 같은 관문(`prjPost`)을 지난다 — `fetch` 는 파일에 하나뿐
- [x] S15 변경 1회 = 요청 1회 · 값이 그대로면 요청 0회
- [x] S16 실패: 원복 + **다시 그린 뒤의 자리**에 서버가 준 문장 그대로
- [x] S17 status 만 확인 창 · 값 편집은 묻지 않는다

### 생성 다이얼로그
- [x] S18 필드 넷만(담당자 4필드 없음)
- [x] S19 창은 `s9dlg` 것 — 껍데기를 스스로 짓지 않는다 (검증 자도 진짜 창을 띄운다)
- [x] S20 빈 표시명·한글 slug·중복 이름을 그 자리에서, 확인 단추는 통과할 때만 눌린다
- [x] S20b 손은 첫 칸에 놓인다(창의 기본 초점은 확인 단추 — 적을 것이 넷인 창은 다르다)

### Board 문맥 띠
- [x] S21 32px 한 줄 · 표 없음 · 좌측 띠·색면 없음 · doclink
- [x] S22 프로젝트 미선택이면 자리도 없다

### 시각 언어 · 배율 · 스킨 전수
- [x] S23 색 리터럴 0 · 라운드 0 · 그림자 0 · 좌측 세로 띠 0
- [x] S24 배율 불변: 새 홀수 px·소수 테두리 없음 (7px 점은 확정 무대 재사용)
- [x] S25 `.proj-info` 복제 0 — 멤버 표 규칙은 `.pmem` 한 곳
- [x] S26 문구는 `PRJ_TEXT` 한 곳 · 같은 이름 두 번 금지
- [x] S27 스킨 10종 × 배율 1·1.25·1.5 캡처 확대 육안
- [x] S28 hover/focus 실포인터(CDP) — ledger·terminal 두 맥락 × 세 배율
  - 예외 하나: 「프로젝트 문서 열기」(doclink)는 base CSS 에 hover 규칙이 없고 실제
    피드백이 JS hovercard 라 골격에서는 검증 불가 — B2 실화면 몫

### 저장소 게이트 (내가 안 짠 시험이 잡은 것 — 전부 초록)
- [x] G1 `을(를)` 금지 → `josa()` 계산 (test_judge_dialog)
- [x] G2 확인 창 셋이 대장(CENSUS)에 · `safe` 판정 (test_dialog_safe)
- [x] G3 조각 등록 (test_web_split — 등록 안 된 조각이라는 것은 이 저장소에 없다)

### 2026-09-01T00:03:49+09:00 response (agent: sub:frontend-developer, by nicehugepark)

### 캡처를 믿을 수 있게 만든 것 (B2 가 같은 자를 쓴다)

1.5배 캡처 한 장이 **민얼굴**로 찍혔다 — 규칙이 하나도 안 온 채 그려진 그림이었다. 원인은 화면이 아니라 검증 절차다: `web/verify-project.html` 에는 껍데기(`web/index.html`)가 가진 **되찾기 그물이 없다**(조각이 잘려 와도 다시 걸어 주는 그 코드). 조각을 한 탭에서 열 번 갈아 끼우면 그중 한 번은 규칙이 덜 온 채로 찍힌다.

고친 방식은 우회가 아니라 **세는 것**이다: 찍기 전에 `document.styleSheets.length`(15)와 `.case` 수를 확인하고, 안 오면 그 자리에서 멈춘다("캡처를 믿을 수 없다"). 그 뒤 배율 1·1.25·1.5 를 다시 찍어 **셋이 같은 모양**임을 눈으로 확인했다.

B2 에서 이 검증 자로 캡처할 때 같은 함정이 있다 — 스킨을 갈아 끼우며 연속 촬영하면 반드시 「규칙이 다 왔나」를 세고 찍어라. (덤: 촬영 도구를 남이 재료로 부를 수 있게 `if __name__ == "__main__"` 로 감쌌다 — 안 감싸면 import 하는 순간 전수 촬영이 통째로 한 번 더 돈다. 이것 때문에 1.5배가 실패한 것처럼 보였다.)

### 전체 실행 최종 결과

`python3 tests/ --jobs 4` — 실패 **1건**, 그마저 회귀가 아니다: `test_port_pool.BoundedReuse.test_allocated_port_is_usable` 의 `Address already in use`. 스위트가 도는 동안 내가 진단용 브라우저를 같은 포트 풀에서 띄운 탓이다(그 시험이 검사하려던 바로 그 자원을 내가 잡았다). **그 파일 단독 재실행 29/29 ok** — 그리고 이 사고 자체가 포트 풀 규율이 왜 있는지 보여 준다: 캡처 도구도 풀을 쓰되 스위트와 동시에 돌리지 마라.

[Image: assets/REQ-20260831-028-62x6/ledger-x1.5.png]
[Image: assets/REQ-20260831-028-62x6/terminal-x1.25-2.png]

### 2026-09-01T00:04:43+09:00 subagent (agent: sub:frontend-developer:aea99b3d, by nicehugepark)

`[2026-09-01 00:04:12 KST - frontend-developer]`

### 결론 — B1 골격 완료. 신규 세 파일에 격리, 상태 전부를 한 검증 자에 세웠고 실브라우저로 판정했다

### 지은 것
- `/home/sjpark1/section9/web/app/project.js` (589줄) — 짓는 함수 · 문구 표 · **저장 관문 하나**
- `/home/sjpark1/section9/web/css/project.css` (159줄) — 배치와 밀도만. 색 리터럴 0, 라운드 0, 그림자 0, 좌측 세로 띠 0
- `/home/sjpark1/section9/web/verify-project.html` (209줄) — 진짜 렌더 함수 + **진짜 창**(dialog.js) + mock 행으로 상태 14갈래
- `/home/sjpark1/section9/tests/test_project_screen.py` (573줄) — 34건 (읽어서 아는 계약 19 + 실브라우저 15)
- `/home/sjpark1/section9/tests/test_dialog_safe.py` — 확인 창 대장에 셋 등록(+9줄)
- `/home/sjpark1/section9/web/index.html` — **2줄만** (아래)

### 지시와 다른 한 곳 (먼저 보고합니다)
「등록 diff 는 B2 에서」였으나 index.html 에 2줄(css/project.css · app/project.js)을 넣었습니다. 이 저장소에는 **등록되지 않은 조각이라는 것이 없습니다** — `tests/test_web_split.py`의 `test_s2_no_orphan_part`가 `web/app/`·`web/css/` 파일 목록과 껍데기 호출 목록이 정확히 같기를 요구하고, 넣지 않으면 그 게이트가 B2까지 붉은 채로 갑니다. 두 파일은 **불활성**입니다(project.js는 로드 시 아무것도 실행하지 않고, project.css는 기존 셀렉터를 하나도 건드리지 않음) — 지금 대시보드는 한 화소도 달라지지 않습니다. B2는 이 2줄을 다시 넣을 필요가 없습니다.

### 검증
- `python3 tests/ project_screen` **34/34 ok** · `project_screen dialog_safe judge_dialog web_split` **93/93 ok**
- 전체 `python3 tests/ --jobs 4` — 실패 1건이나 회귀 아님: `test_port_pool` 포트 경합(스위트가 도는 동안 내가 진단 브라우저를 같은 풀에서 띄운 탓). 그 파일 단독 **29/29 ok**
- **스킨 10종 × 배율 1·1.25·1.5 = 30장** 확대 육안 + 실포인터(CDP) hover/focus 를 ledger·terminal 두 맥락 × 세 배율에서 측정. 잠긴 「제거」는 세 배율 모두 **diff=0**(안 뒤집힘), 칠은 단추 상자(43.6px) 안에 정확히, 값 자리 점선 밑줄은 글자·이웃 줄과 안 겹침

### 눈과 게이트가 잡은 결함 (코드로는 안 보이던 것)
1. 마지막 owner 「제거」가 **살아 있는 단추로** 보였다 → `.acts.pjcell button:disabled(:hover)`
2. 만료 줄에서 글자만 물러나고 **컨트롤은 또렷**했다 → `.pmem tr.exp .pjc`
3. 띠의 상태가 목록과 다른 얼굴(소문자)이었다 → `.pjs-st`
4. `PRJ_TEXT`에 같은 이름을 두 번 적어 **빈 상태 문구가 통째로 「없음」**이 됐다(시험은 전부 초록이었다) → 중복 키 금지 시험 추가
5. `을(를)` 서식 편지투 → `josa()` (test_judge_dialog)
6. 확인 창 셋이 **대장(CENSUS)에 없었다** → 등록 + 문안을 이름으로 넘김 (test_dialog_safe)
7. 검증 자가 **창 껍데기를 베끼고** 있었다 → 진짜 창을 띄우고, 베끼기 금지를 시험으로
8. 네 칸짜리 창인데 손이 **확인 단추에** 놓였다 → 첫 칸으로

### designer 판정을 청합니다 (셋)
1. 「+ 새 프로젝트」가 화면에서 **가장 흐린 잉크**(`.more` = `--faint`, ~2.8:1). 설계가 「N개 더 보기와 같은 문법」이라 했고 그대로 따랐지만, 이 REQ가 푸는 문제가 「문이 없다」인데 그 문을 가장 옅게 그린 셈입니다
2. active 점 잉크를 `--text`로 했습니다 — `--t-project` 토큰이 없고, 토큰 신설은 tokens.css(단일 소유자)를 여는 일이라 B1에서 하지 않았습니다
3. 담당자를 **네 줄로 폈습니다**(설계 스케치는 한 줄). 한 칸에 네 필드를 이어 붙여 저장하면 이름 칸에 전화번호가 들어갑니다. 빈 칸은 고칠 수 있는 사람에게만 섭니다

### B2 인계 (순서대로)
1. **첫 손은 `web/css/header.css` 163~236행 삭제** — `.proj-info` 규칙이 지금 두 벌입니다(이번 물결이 기존 css를 안 만져서). 매핑: table→`.pmem` · `.pi-x`→`.acts.pjcell button` · `.m-act/.m-exp`→`.m-on/.m-off` · select/input→`.pjc` · `.pi-err`→`.pjerr`
2. 프로젝트 행의 관문은 하나로 — Docs가 `curType==="project"`일 때 `prjListHTML`로 통째 그리기 권고(docs.js rowHTML에 분기를 더하면 행 문법이 두 벌)
3. `prjWire`는 부르는 쪽이 **`reload`(다시 읽고 다시 그리기)를 주어야** 원복이 완성됩니다 — 낙관적 갱신 금지
4. mock API 가정은 REQ 노트에 표로 적었습니다(A/027과 맞출 것). 화면은 `history_tail`을 쓰지 않습니다

### 한계
- 「프로젝트 문서 열기」의 hover 얼굴은 **검증되지 않았습니다** — `a.doclink`는 base CSS에 hover 규칙이 없고 실제 피드백이 JS hovercard라 골격에는 없습니다. 여기에만 새 hover를 더하면 다른 모든 doclink와 얼굴이 갈리므로 더하지 않았습니다(B2 실화면 몫)
- 서버 실연동 없음(mock) — `/api/project/add`·`set`은 아직 없습니다
- 검증 자에는 껍데기의 되찾기 그물이 없어 연속 촬영 시 규칙이 덜 온 채 찍히는 일이 있습니다(실제로 1.5배 한 장이 민얼굴로 찍혔고, 그래서 촬영 도구가 「규칙이 다 왔나」를 세고 멈추게 했습니다) — B2도 같은 함정

기록은 전부 `REQ-20260831-028-62x6`에 남겼습니다(tdd 개정 + response 4건, 캡처 7장 첨부). git 조작은 하지 않았습니다.

### 2026-09-01T00:05:07+09:00 subagent (agent: sub:frontend-developer:aea99b3d, by nicehugepark)

`[2026-09-01 00:04:38 KST - frontend-developer]`

배경 작업(1.5배 캡처 대기) 종료 알림이며, 새 지시가 아닙니다. 앞선 보고 이후 달라진 것은 없습니다 — B1 골격은 완료 상태이고, 검증·기록은 모두 끝났습니다.

최종 상태 재확인:
- 산출물 `/home/sjpark1/section9/web/app/project.js` · `/home/sjpark1/section9/web/css/project.css` · `/home/sjpark1/section9/web/verify-project.html` · `/home/sjpark1/section9/tests/test_project_screen.py`, 그리고 `web/index.html` 2줄 · `tests/test_dialog_safe.py` 대장 등록
- 시험: project_screen 34/34, 관련 넷 합쳐 93/93, 전체 스위트는 회귀 0(유일한 실패는 내가 동시에 띄운 진단 브라우저와의 포트 경합, 단독 재실행 29/29)
- 캡처: 스킨 10종 × 배율 1·1.25·1.5 전수 + 실포인터 hover/focus, 마지막 세 스킨(cobalt·field·cork)의 1.5배까지 확보해 육안 판정 완료
- 기록: `REQ-20260831-028-62x6` 에 tdd 개정 1건 + response 4건, 캡처 7장 첨부

다음 손(B2)이 가장 먼저 할 일은 `web/css/header.css` 163~236행의 `.proj-info` 블록 삭제입니다 — 지금 규칙이 두 벌인 유일한 자리이고, 그대로 두면 관문이 둘이 됩니다.

### 2026-09-01T00:06:43+09:00 commit (by nicehugepark)

커밋 e8e7587 — 프로젝트 화면 골격 — 상태 14갈래가 한 검증 자에 선다 (REQ-20260831-028-62x6 B1)

### 2026-09-01T00:06:47+09:00 subagent (agent: sub:frontend-developer:aea99b3d, by nicehugepark)

`[2026-09-01 00:04:52 KST - frontend-developer]`

또 다른 배경 작업 종료 알림입니다 — 사용자 입력이 아니고, 새로 할 일도 없습니다. 대기하던 캡처 작업들이 모두 끝나 남은 백그라운드 작업이 없습니다.

작업은 앞선 보고에서 완료된 상태 그대로입니다:

- **산출물**: `/home/sjpark1/section9/web/app/project.js` · `/home/sjpark1/section9/web/css/project.css` · `/home/sjpark1/section9/web/verify-project.html` · `/home/sjpark1/section9/tests/test_project_screen.py` (+ `web/index.html` 2줄, `tests/test_dialog_safe.py` 대장 등록)
- **검증**: project_screen 34/34 · 관련 넷 93/93 · 전체 스위트 회귀 0 · 스킨 10종 × 배율 1·1.25·1.5 전수 캡처 육안 판정 · 실포인터 hover/focus 측정
- **기록**: `REQ-20260831-028-62x6` 에 tdd 개정 1건 + response 4건, 캡처 7장 첨부

미해결로 남긴 것은 보고에 적은 셋뿐입니다 — designer 판정 3건(생성 진입점 잉크 농도 · active 점 토큰 · 담당자 네 줄), doclink hover 얼굴 미검증(B2 실화면 몫), 서버 실연동 없음(mock).

### 2026-09-01T00:07:29+09:00 response (agent: lead:claude-fable-5, by nicehugepark)

B1 리드 검수: 재실측 6스위트 green(web_split 일시 경합 1회 후 재실행 OK), 커밋 e8e7587 푸시. B1 의 designer 판정 요청 3건 리드 확정 — ① 진입점 잉크 --muted(문이 보여야 한다) ② active 점은 기존 project 타입색 원천 준수(신규 하드코딩 금지) ③ 담당자 4줄 유지. B2(실연동) 착수 — 같은 REQ 에서 계속

### 2026-09-01T00:16:06+09:00 tdd (agent: sub:frontend-developer, by nicehugepark)

### TDD 시나리오 — B2 실연동 (개정 · B1 세대를 대체한다)

검증 자 `web/verify-project.html`(mock 상태 대조표, 유지) · 회귀 `tests/test_project_screen.py`(개정) ·
실연동은 **실브라우저 + 실서버**(별도 포트, 라이브 vault 무오염)로 밟는다. 실행 `python3 tests/ project_screen`

### 관문 하나 — 규칙이 두 벌이던 자리를 없앤다
- [ ] G1 `web/css/header.css` 에 `.proj-info` 셀렉터가 **0개**다 (멤버 표 규칙은 `.pmem` 한 곳)
- [ ] G2 `web/app/app.js` 에 멤버 표를 짓는 마크업·배선(`wireMemberControls`·`pi-*`)이 없다 — 저장 관문은 `prjPost` 하나
- [ ] G3 Docs 목록에서 프로젝트 줄을 짓는 함수는 `prjRowHTML` 하나다(`curType==="project"` 목록도 mixed 목록도)

### 실 API 배선 (REQ-20260831-027-62x6 명세)
- [ ] A1 생성: 창 → `POST /api/project/add {slug,name,summary,customer}` → 200 이면 다시 읽고 **그 문서로 이동**
- [ ] A2 설정: 값 하나를 고치면 `POST /api/project/set` 에 **바뀐 필드 하나만** 실린다(미포함=미변경)
- [ ] A3 같은 값 재입력·재선택이면 요청 0회 (400 「변경할 필드 없음」을 부르지 않는다)
- [ ] A4 status 는 확인 창 없이 즉시 저장(보관·보관 해제 양방향)
- [ ] A5 거부 400 의 `error` 가 화면 문장으로 선다 — 아는 사유 셋은 존댓말 화면 문장으로 치환, 모르는 사유는 서버 문장 그대로
- [ ] A6 `/api/projects` 는 폴링 벨트 밖 — 15초 catalog 폴이 이 요청을 **끌고 다니지 않는다**. 부트 1회 + 탭 진입 + 변이 성공 후에만

### 폴링이 손을 파괴하지 않는다 (PM 리스크 R2 · REQ-055 E7 준용)
- [ ] R1 값 자리를 열어 둔(입력 상자가 서 있는) 동안 배경 재로드가 그 판을 갈아 끼우지 않는다
- [ ] R2 멤버 추가 행에 적어 둔 값도 같은 가드에 들어간다
- [ ] R3 가드는 **편집 중일 때만** 문다 — 편집이 끝나면 다음 폴이 정상 갱신한다

### 문구 = 029 확정본 전량 (PRJ_TEXT 를 통째로 갈아 끼운다)
- [ ] W1 「+ 프로젝트 만들기」(명사구 아님) · aria-label 있음 · 잉크 `--muted`
- [ ] W2 보관: **확인 창 없음** · 접힘 줄은 「+ 보관된 프로젝트 N개」 · 펼치면 「− 접기」
- [ ] W3 ACTIVE 비표기 — active 는 글자를 안 받고 archived 만 받는다(값은 영문 원문, tech-writer)
- [ ] W4 멤버 동사 「제거」 통일 · 자기 자신만 「나가기」(창도 따로, safe:true)
- [ ] W5 캡션 「멤버 N명」 / 만료 있으면 「멤버 N명 — 활성 a · 만료 b」
- [ ] W6 빈 상태 **세 벌**(멤버 0·넣을 사람 있음 / 등록 사용자 0 / 모두 이미 멤버)
- [ ] W7 role 뜻 범례 한 줄이 maintainer 이상에게만 선다
- [ ] W8 열 이름 「참여 상태」(한 화면에 상태 축이 둘이므로) · slug 는 읽기 전용 + 까닭 한 줄
- [ ] W9 세 자리(목록 줄 · 문맥 띠 · 관리 요약)가 **같은 낱말**로 같은 수를 센다
- [ ] W10 화면 글자는 여전히 `PRJ_TEXT` 한 곳 · 같은 이름 두 번 없음 · `을(를)` 없음(런타임에도)
- [ ] W11 PRJ 문서 하단 문서 치우기 「보관」 단추 비표시(삭제는 유지) — 한 화면에 보관 둘 금지

### 자리 (설계 C안)
- [ ] P1 Docs `curType==="project"` 목록이 `prjListHTML` 한 장 — 머리 낱말은 타입바와 겹치지 않는다
- [ ] P2 PRJ 문서 뷰에 프로젝트 패널(설정 격자 · 멤버 표 · 관리 요약)이 선다 — 격자는 **한 표**(사전 둘 금지)
- [ ] P3 Board 위 244px 표가 32px 문맥 띠로 바뀐다 · 프로젝트 미선택이면 자리도 없다
- [ ] P4 active 점의 잉크는 새 하드코딩 없이 기존 색 원천을 따른다

### 시각 언어 · 배율 · 얼굴
- [ ] V1 색 리터럴 0 · 라운드 0 · 그림자 0 · 좌측 세로 띠 0 · 색면 배지 0
- [ ] V2 새 홀수 px·소수 px 테두리 없음
- [ ] V3 스킨 10종 × 배율 1·1.25·1.5 확대 육안
- [ ] V4 hover/focus 는 CDP 실포인터 — 「프로젝트 문서 열기」(doclink)의 손 얹은 얼굴을 **실화면에서** 판정(B1 미검증분)

### 실연동 시나리오 (실서버 · 실브라우저로 한 번에 밟는다)
- [ ] E1 생성 → 문서 이동 → 멤버 추가 → 권한 변경 → 제거 → 설정 인라인 → 보관 → 접힘 → 보관 해제
- [ ] E2 거부(권한 미달)에서 컨트롤 원복 + 화면 문장
- [ ] E3 콘솔 오류 0 · 4xx/5xx 0(의도한 거부 제외)

### 2026-09-01T01:14:56+09:00 tdd (agent: sub:frontend-developer, by nicehugepark)

### TDD 시나리오 — B2 실연동 (개정 · 통과 표시)

시험 자 셋: 계약 `tests/test_project_screen.py` 45건(읽어서 아는 것 27 + 검증 자 실브라우저 18) ·
mock 상태 대조표 `web/verify-project.html` · **실서버+실브라우저 시나리오**(격리 S9_ROOT 에
서버를 세우고 15갈래를 끝까지 밟는다 — 라이브 9909·라이브 vault 무접촉).
실행: `python3 tests/ project_screen dialog_safe judge_dialog web_split` → **106/106 ok** (exit 0 실측)

### 관문 하나 — 규칙이 두 벌이던 자리를 없앤다
- [x] G1 `header.css`·`skins.css` 에 `.proj-info`·`.pi-x` 셀렉터 **0개** (시험이 web/css 전수를 훑는다)
- [x] G2 `app.js` 에 멤버 표를 짓는 마크업·배선 0 (`wireMemberControls`·`pi-*` 제거)
- [x] G3 프로젝트 줄을 짓는 함수는 `prjRowHTML` 하나 — `curType==="project"` 목록도, 타입 섞인 목록도

### 실 API 배선 (REQ-20260831-027-62x6 명세)
- [x] A1 생성 → 200 이면 다시 읽고 **그 문서로 이동**(실측: 만든 뒤 `#docs/PRJ-…` 로 갔고 생성자가 owner)
- [x] A2 설정: 값 하나를 고치면 `set` 에 **바뀐 필드 하나만** (시험이 요청 몸통의 키 개수를 센다)
- [x] A3 같은 값이면 요청 0회 — 실브라우저에서 실패 응답 계수가 그대로임을 확인
- [x] A4 status 는 확인 창 없이 즉시 저장 · 보관/보관 해제 양방향
- [x] A5 거부 400 의 사유가 화면 문장으로 — 아는 사유 다섯은 존댓말로 옮기고, 모르는 사유는 그대로
- [x] A6 `/api/projects` 는 폴링 벨트 밖 (시험이 refreshCatalog 본문에 그 호출이 없음을 단언)

### 폴링이 손을 파괴하지 않는다
- [x] R1 값 자리를 열어 둔 동안 배경 재렌더가 그 판을 갈아 끼우지 않는다(실브라우저에서 render() 를 직접 불러 확인)
- [x] R2 **고르는 중**(select 에 초점)도 같은 가드에 들어간다 — 글자만 보던 1차를 실측으로 넓혔다
- [x] R3 가드는 배경 갱신만 문다 — 변이 뒤 되읽기는 이 문을 지나지 않는다(시험이 prjWire 본문을 확인)

### 문구 = 029 확정본 전량
- [x] W1 「+ 프로젝트 만들기」 · aria-label · 잉크 `--muted`(--faint 아님을 화면에서 잰다)
- [x] W2 보관 **확인 창 없음** · 「+ 보관된 프로젝트 N개」 · 펼치면 「− 접기」(aria-expanded)
- [x] W3 ACTIVE 비표기 — 목록 행·문맥 띠는 archived 만 글자를 받는다(값은 영문 원문)
- [x] W4 「제거」 통일 + 자기 자신만 「나가기」(창 따로 · safe:true · 초점이 「그만두기」에)
- [x] W5 캡션 「멤버 5명 — 활성 4 · 만료 1」 / 만료 0이면 「멤버 1명」
- [x] W6 빈 상태 세 벌
- [x] W7 role 뜻 범례 한 줄 — maintainer 이상에게만
- [x] W8 열 이름 「참여 상태」 · slug 읽기 전용 + 까닭 한 줄
- [x] W9 같은 것을 같은 낱말로 — 목록·띠·요약이 「열린 요청 N · 마지막 활동 X」 한 벌
- [x] W10 화면 글자는 `PRJ_TEXT` 한 곳 · 중복 키 0 · `을(를)` 0 (**런타임에도** — 사람 이름은 받침 계산이 안 서므로 「님을」로 받는다)
- [x] W11 PRJ 문서 하단 문서 치우기 「보관」 비표시(삭제는 유지)

### 자리 (설계 C안)
- [x] P1 Docs `curType==="project"` 목록이 한 장 — 머리 낱말은 타입바와 겹치지 않는다
- [x] P2 PRJ 문서 뷰 패널 — 격자는 **한 표**(프로젝트 여덟 + 문서 공통 줄이 이어 붙는다)
- [x] P3 Board 위 244px 표 → 32px 띠 · 미선택이면 자리도 없다 (실측: 컬럼 머리 y=413 → **167**)
- [x] P4 점의 잉크는 새 하드코딩 0 — 기존 색 원천(TCOLOR/SCOLOR)을 읽는다

### 시각 언어 · 배율 · 얼굴
- [x] V1 색 리터럴 0 · 라운드 0 · 그림자 0 · 좌측 세로 띠 0 · 색면 0
- [x] V2 새 홀수 px·소수 테두리 0
- [x] V3 **스킨 10종 × 배율 1·1.25·1.5 = 30조합** 캡처 후 확대 육안 — 띠 높이 30/30 모두 32px
- [x] V4 hover/focus 를 CDP 실포인터로 — ledger·terminal × 세 배율 6조합. 잠긴 「제거」는 여섯 번 모두 **화소가 한 점도 안 바뀌었다**(못 누르는 단추는 안 뒤집힌다). B1 이 미검증으로 남긴 「프로젝트 문서 열기」는 **hovercard 가 뜨는 것**이 그 얼굴임을 실화면에서 확인

### 실연동 시나리오 (15갈래 · 실서버 · 실브라우저)
- [x] E1 목록 → 보관 펴고 접기 → 생성(빈·한글 slug·중복·통과) → 문서 이동 → 멤버 추가 → 권한 변경 →
      설정 인라인 → 보관 → 목록 접힘 → 보관 해제 → 멤버 제거
- [x] E2 거부 둘: ① 창이 떠 있는 사이 같은 slug 가 생김 → 알림 한 장 ② 낡은 화면이 마지막 owner 를
      빼려 함 → **원복 + 「멤버를 바꾸지 못했습니다 — 마지막 owner 는 뺄 수 없습니다 …」**
- [x] E3 콘솔 오류 0 · 뜻밖의 실패 응답 0 (일부러 부른 거부 둘만)

### 2026-09-01T01:21:11+09:00 response (agent: sub:frontend-developer, by nicehugepark)

### B2 실연동 완료 — 관문이 하나가 됐고, 화면은 실제로 서버를 지난다

### 1. 첫 손: 규칙이 두 벌이던 자리를 없앴다
- `web/css/header.css` 의 `.proj-info` 블록 **전량 삭제**(패널·표·컨트롤·`.pi-x`·`.m-act/.m-exp`).
- 거기서 끝나지 않았다 — **`web/css/skins.css` 의 스킨 여섯 벌**(glass·field·cobalt·grid·slate·cork)이
  같은 셀렉터를 저마다 다시 그리고 있었다. 상자가 사라졌으므로 그 override 도 함께 걷어냈다.
  시험이 이제 `web/css` 전수를 훑어 `.proj-info`·`.pi-x`·`.pi-err`·`.pi-grid` 가 **0개**임을 지킨다.
- `web/app/app.js` 의 `renderProjectInfo`(244px 표)와 `wireMemberControls`(두 번째 저장 경로)를
  걷어내고 32px 문맥 띠 한 줄로 바꿨다. 저장은 이제 `prjPost` 한 문뿐이다.

### 2. 세 자리에 얹었다 (설계 C안)
- **Docs › PROJECT**: `curType==="project"` 이면 목록을 `prjListHTML` 한 장으로. 타입 섞인 목록의
  프로젝트 줄도 같은 `prjRowHTML` 을 부른다 — 행 문법이 갈릴 데가 없다. 머리 낱말은 바로 위
  타입바가 이미 말하므로 접고, 만드는 손잡이만 남긴다.
- **PRJ 문서 뷰**: 설정 격자(프로젝트 여덟 + 문서 공통 줄이 **한 표**로 이어 붙는다) · 멤버 표 ·
  role 범례 · 관리 요약. 멤버를 바꾸면 같은 화면 아래 History 에 줄이 남는다(designer 의 근거).
- **Board/Graph 위**: 244px 표 → 32px 띠. 실측으로 컬럼 머리가 **y=413 → y=167**.

### 3. 실 API 배선 (REQ-20260831-027-62x6 명세 그대로)
`add`·`set`·`member`·`member/rm` 넷 다 `prjPost` 를 지난다. set 은 **바뀐 필드 하나만** 싣고,
값이 그대로면 요청이 아예 안 나간다(400 「변경할 필드 없음」을 부르지 않는다). 쓰는 사람은
서버가 정하므로 화면은 admin 대리(`as`)만 싣는다. 되읽기는 부르는 쪽이 준 `reload` 한 손.
`/api/projects` 는 **폴링 벨트에서 내렸다** — 부트 1회 · 탭 진입 · 변이 성공 직후.

### 4. 실화면이 잡아 고친 결함 여섯 (코드로는 안 보이던 것)
1. **멤버를 넣었는데 표가 안 바뀌었다.** 문서에는 들어갔고 목록도 새 값을 들고 있는데 화면만
   옛 표였다. `bg` 한 인자가 두 가지를 뜻하고 있었다 — "안 바뀌었으면 건너뛴다"와 "읽던 자리를
   지킨다". 변이 뒤 되읽기는 뒤엣것만 원한다 → `loadDoc(id, bg, force)` 로 갈랐다(회귀 시험 있음).
2. **프로젝트 목록보다 문서를 먼저 열면 패널이 영영 안 그려졌다** — 재료가 없어 맨 메타 표만
   서고, 그 뒤로는 「안 바뀌었으니 건너뛴다」에 걸렸다. 목록이 도착하는 자리에서 채운다.
3. **고르는 손도 파괴됐다.** 편집 가드가 글자(input)만 보고 있어, 후보를 고르는 중(select 초점)에
   폴이 오면 고른 값이 사라졌다. 초점이 패널 안이면 쓰고 있는 것으로 본다.
4. **고르는 칸이 옛 이름을 들고 있었다**(`portal ·미등록`). 폴링 벨트가 우연히 고쳐 주던 것이
   벨트에서 내리자 드러났다 — 목록이 바뀌면 그 파생 뷰(fillProjects)를 그 자리에서 다시 짓는다.
5. **「멤버」가 한 화면에서 두 수를 말했다** — 캡션 「멤버 5명 — 활성 4 · 만료 1」, 바로 아래
   요약 「멤버 4」. 표가 이미 말한 자리이므로 요약에서 멤버를 뺐다(`열린 요청 N · 마지막 활동 X`).
6. **「멤버」가 두 번 읽혔다** — 머리글 「멤버」와 캡션 「멤버 5명 …」. 표의 이름은 caption 이 진다.

곁가지 하나: 컬러 지면 스킨(field)에서 띠의 가운뎃점만 사라져 한 덩이로 읽혔다 — `--faint` 로
못박은 잉크를 제 낱말(currentColor)로 되돌렸다.

### 5. 문구는 029 확정본 전량 (PRJ_TEXT 를 통째로 갈아 끼움)
「+ 프로젝트 만들기」(--muted 잉크·aria) · 보관 **확인 창 없음** + 「+ 보관된 프로젝트 N개」/「− 접기」 ·
ACTIVE 비표기(값은 영문 원문) · 「제거」 통일 + 자기 자신만 「나가기」(창 따로·safe) ·
캡션 「멤버 N명 — 활성 a · 만료 b」 · 빈 상태 세 벌 · role 뜻 범례 한 줄 · 「참여 상태」 열 ·
slug 읽기 전용 + 까닭 한 줄 · PRJ 문서 하단 문서 「보관」 비표시.
서버 거부 사유는 아는 다섯을 화면 어투로 옮기고 모르는 것은 그대로 둔다.

### 6. 실측
- `python3 tests/ project_screen dialog_safe judge_dialog web_split` → **106/106 ok** (exit 0)
- `python3 tests/ --jobs 4` → **615건 중 실패 3** — 전부 **오늘 자정에 넘어간 날짜** 탓이고 이
  물결과 무관하다: `tests/test_note_attach.py:55` 와 `tests/test_article_type.py:69` 가 자산 경로에
  `"2026","08"` 을 **못박아 두었다**. 오늘이 2026-09-01 이라 새 문서가 `2026/09` 로 가면서
  어제까지 초록이던 셋이 오늘 붉어졌다(단독 실행에서도 같다 — 병렬 탓이 아니다).
  지시가 `tests/` 의 서버 시험 수정을 금해 손대지 않았다 — 리드 판정 몫이다.
- **실서버 + 실브라우저 시나리오 15/15** — 격리 S9_ROOT 에 서버를 세워 라이브 9909·라이브
  vault 무접촉. 생성(빈·한글 slug·중복·통과) → 문서 이동 → 멤버 추가 → 권한 변경 → 설정 인라인 →
  보관 → 목록 접힘 → 보관 해제 → 제거 → 거부 둘 → 띠 → 폴링 가드 → 콘솔 오류 0.
- **스킨 10종 × 배율 1·1.25·1.5 = 30조합** 캡처 확대 육안. 문맥 띠 높이 **30/30 모두 32px**.
- **CDP 실포인터** hover/focus 를 ledger·terminal × 세 배율에서. 잠긴 「제거」는 **여섯 번 모두
  화소가 한 점도 안 바뀌었다**(못 누르는 단추는 안 뒤집힌다). 반전 스킨에서도 hover 가 배경과
  같은 색으로 사라지지 않는다. B1 이 미검증으로 남긴 「프로젝트 문서 열기」는 **hovercard 가
  뜨는 것**이 그 얼굴임을 실화면에서 확인했다(다른 모든 doclink 와 같다).

### 7. 고친 파일
`web/app/project.js`(문구 표 전면 교체 + 구조) · `web/app/app.js` · `web/app/docs.js` ·
`web/app/events.js` · `web/app/tidy.js` · `web/css/header.css` · `web/css/skins.css` ·
`web/css/project.css` · `web/index.html` · `tests/test_project_screen.py`(45건) ·
`tests/test_dialog_safe.py`(대장 갱신). git 조작 없음.

[Image: assets/REQ-20260831-028-62x6/list-final.png]
[Image: assets/REQ-20260831-028-62x6/12-strip-board.png]
[Image: assets/REQ-20260831-028-62x6/04-dlg-ok.png]
[Image: assets/REQ-20260831-028-62x6/10-rm-dialog.png]
[Image: assets/REQ-20260831-028-62x6/12-leave-dialog.png]
[Image: assets/REQ-20260831-028-62x6/z-ledger-x15-mem.png]
[Image: assets/REQ-20260831-028-62x6/h-terminal-x15-rm-hover.png]
[Image: assets/REQ-20260831-028-62x6/h-ledger-x10-rmoff-hover.png]

### 2026-09-01T01:22:14+09:00 response (agent: sub:frontend-developer, by nicehugepark)

### 전체 실행의 붉은 넷 — 하나도 이 물결의 것이 아니다 (앞 노트 보완)

`python3 tests/ --jobs 4` 이 실패 3 + 에러 1 을 냈다. 넷 다 짚어 본 결과:

1. **실패 셋 — 오늘 자정에 넘어간 날짜.** `tests/test_note_attach.py:55` 와
   `tests/test_article_type.py:69` 가 자산·문서 경로에 `"2026","08"` 을 **못박아 두었다.**
   오늘이 2026-09-01 이라 새 문서가 `2026/09` 로 가면서 어제까지 초록이던 셋이 오늘
   붉어졌다 — **단독 실행에서도 같다**(병렬 탓이 아니다). 이 물결은 `bin/s9` 를 한 줄도
   안 만졌고 그 경로와 무관하다. 지시가 `tests/` 서버 시험 수정을 금해 손대지 않았다.
   고칠 곳은 그 두 줄이 달을 오늘 날짜에서 파생하게 하는 것이다.
2. **에러 하나 — 병렬 부하에서의 연결 끊김.** `test_dashboard_chat.test_c4_transition_event`
   가 `/api/status` 응답을 읽다 소켓이 끊겼다(HTTP 상태 줄을 못 읽음). **그 파일 단독
   재실행 25/25 ok** — B1 이 겪은 포트 경합과 같은 갈래다.

즉 이 물결이 만든 회귀는 **0**이다. 내 시험(project_screen 45 · dialog_safe · judge_dialog ·
web_split)은 전체 실행에서도 전부 초록이었다.

### 2026-09-01T01:22:50+09:00 response (agent: sub:frontend-developer, by nicehugepark)

### 지시와 다르게 판정한 셋 (먼저 보고합니다 — 리드가 뒤집을 수 있게)

1. **멤버 제거 창 desc 를 translator 교정본 그대로 쓰지 않았다.**
   중재는 「접근≠읽기 차단」이 사실 오류라며 「요청을 올리거나 설정을 고칠 수 없게 됩니다」로
   고치라 했다. 근거는 `project_can("read")` 가 누구나 True 라는 것(bin/s9:12645)인데, **화면이
   지나는 문은 그것이 아니다**: HTTP 응답 직전에 `doc_visible`(bin/s9:12748-12768)이 프로젝트
   문서를 **활성 멤버에게만** 내준다. 그래서 제거하면 그 사람은 대시보드에서 그 프로젝트의
   요청·문서를 실제로 못 본다 — 「접근만 끊깁니다」 쪽이 참이었다. 사실인 문장으로 썼다:
   「이 사람은 더 이상 이 프로젝트의 요청·문서를 볼 수 없습니다. 문서는 그대로 남고, 나중에
   다시 추가할 수 있습니다.」 나가기 창도 같은 잣대로 맞췄다. **화면이 거짓을 말하지 않는 것이
   이 교정의 본래 목적**이라 그 목적 쪽으로 판정했다 — 낱말 판정을 뒤집자는 뜻이 아니다.
2. **active 점의 잉크는 `TCOLOR.project || var(--text)`.** 「기존 색 원천(TCOLOR)을 따르고 새
   하드코딩 금지」를 그대로 지키려면 `--t-project` 토큰이 있어야 하는데 없다. 토큰을 새로
   지으면 그게 곧 새 색 하드코딩이라, **원천을 읽되 없으면 잉크색으로 물러난다** — 누가
   `--t-project` 를 tokens.css 에 더하는 날 이 자리가 저절로 따라온다.
3. **관리 요약에서 「멤버 N」을 뺐다.** ux-writer 판정 7 은 세 자리가 「멤버 N · 열린 요청 N ·
   마지막 활동」 한 벌이라 했는데, 그 「문서 상단」은 표가 **없던** 자리였다. 통합 뒤 그 줄은
   멤버 표 **아래**로 왔고, 캡션이 「멤버 5명 — 활성 4 · 만료 1」이라 말한 바로 밑에서 요약이
   「멤버 4」를 세워 한 화면에 같은 낱말이 두 수를 말했다(실캡처). 표가 있는 자리에서는 표가
   센다 — 목록·띠(표가 없는 자리)는 한 벌 그대로다.

### 판정을 청합니다 (하나)

**제목 줄의 상태 배지(`ACTIVE`)를 그대로 두었다.** 「ACTIVE 비표기」 확정은 목록 행과 문맥
띠(ux-writer 표 1·6)의 판정이었고, 제목 줄 배지(`.dst`)는 **모든 문서 종류가 갖는 문법**이다
(요청은 `open`/`done`, 질문은 `답함`). 프로젝트만 배지를 빼면 그 한 종류만 다른 얼굴이 되고,
같은 값이 배지와 격자에 함께 서는 것도 이 화면이 모든 문서에서 이미 하는 일이다. 그래서
건드리지 않았으나, 「보관 아닌 것은 어디서도 글자를 받지 않는다」로 판정이 넓어진다면
`statusLabel` 자리에서 project 만 archived 일 때 그리게 하면 된다(한 줄).

### 남은 한계

- 좁은 판(<520px)에서 멤버 표는 제 상자 안에서 가로로 구른다(`.pmemwrap`). 실제 문서 판 폭
  (1500px)에서는 접히지 않음을 스킨 10종에서 확인했지만, 그보다 좁은 창은 안 봤다.
- 실연동 검증은 **격리 vault**에서 했다(라이브 9909·라이브 vault 무접촉). 라이브 화면에서는
  F5 후 Docs › PROJECT 로 확인이 필요하다 — 지금 라이브 서버는 027 커밋 이전 프로세스라
  `/api/projects` 에 history_tail 이 없다(화면은 그 필드를 쓰지 않으므로 무관하나, 생성·설정
  API 를 실제로 눌러 보려면 서버 재기동이 필요하다 — 지시가 금해 하지 않았다).
- density=compact 규칙은 아직 project.css 에 있다(B1 이 남긴 자리). 그 축의 주인이 density.css
  로 옮길지 판정할 몫이다.

### 2026-09-01T01:23:32+09:00 subagent (agent: sub:frontend-developer:ad4a83f9, by nicehugepark)

`[2026-09-01 01:22:56 KST - frontend-developer]`

### 결론 — B2 실연동 완료. 관문이 하나가 됐고, 화면이 실제로 서버를 지나는 것을 실브라우저로 끝까지 밟았다

### 첫 손 — 규칙이 두 벌이던 자리를 없앴다
`web/css/header.css` 의 `.proj-info` 블록 전량 삭제에서 끝나지 않았다. **스킨 여섯 벌**(glass·field·cobalt·grid·slate·cork)이 `skins.css` 에서 같은 셀렉터를 저마다 다시 그리고 있었고, 상자가 사라졌으니 그 override 도 함께 걷어냈다. `app.js` 의 `wireMemberControls`(두 번째 저장 경로)도 제거 — 저장은 `prjPost` 한 문뿐이다. 되살아나는 것은 시험이 `web/css` 전수를 훑어 막는다.

### 실화면이 잡아 고친 결함 여섯 (코드로는 안 보이던 것)
1. **멤버를 넣었는데 표가 안 바뀌었다** — `bg` 한 인자가 "안 바뀌었으면 건너뛴다"와 "읽던 자리를 지킨다" 둘을 뜻하고 있었다. 되읽기는 뒤엣것만 원한다 → `loadDoc(id, bg, force)` 로 갈랐다(회귀 시험 포함).
2. 목록보다 문서를 먼저 열면 패널이 **영영** 안 그려졌다 → 목록이 도착하는 자리에서 채운다.
3. 편집 가드가 글자만 봐서 **고르는 중**(select 초점)에 폴이 오면 고른 값이 사라졌다.
4. 폴링 벨트에서 내리자 고르는 칸이 옛 이름(`portal ·미등록`)을 들고 있었다 → 파생 뷰를 그 자리에서 다시 짓는다.
5. 「멤버」가 한 화면에서 두 수를 말했다(캡션 5명 / 요약 4) → 표가 있는 자리에서는 표가 센다.
6. 머리글 「멤버」와 캡션 「멤버 5명 …」 겹말 → 표의 이름은 caption 이 진다.

### 실측
- **실서버+실브라우저 시나리오 15/15** — 격리 S9_ROOT(라이브 9909·라이브 vault 무접촉). 생성(빈·한글 slug·중복·통과)→문서 이동→멤버 추가→권한 변경→설정 인라인→보관→목록 접힘→보관 해제→제거→거부 둘→띠→폴링 가드→콘솔 오류 0.
- 표적 시험 **106/106**, 전체 `--jobs 4` 에서 **이 물결발 회귀 0**.
- **스킨 10종 × 배율 1·1.25·1.5 = 30조합** 확대 육안. 문맥 띠 **30/30 모두 32px**. Board 컬럼 머리 y=413 → **167**.
- **CDP 실포인터** hover/focus 6조합 — 잠긴 「제거」는 여섯 번 모두 **화소 불변**, 반전 스킨에서도 hover 가 사라지지 않는다. B1 미검증분(「프로젝트 문서 열기」)은 **hovercard 가 그 얼굴**임을 실화면에서 확인.

### 리드가 봐야 할 것 넷
1. **전체 스위트의 붉은 넷은 내 것이 아니다** — 실패 3은 `tests/test_note_attach.py:55`·`tests/test_article_type.py:69` 가 경로에 `"2026","08"` 을 못박아 둔 **날짜 넘김**(오늘부터 붉다, 단독 실행도 동일). 에러 1은 병렬 부하의 연결 끊김(단독 25/25 ok). 지시가 `tests/` 서버 시험 수정을 금해 손대지 않았다.
2. **멤버 제거 창 문안을 중재본 그대로 쓰지 않았다** — translator 근거는 `project_can("read")`인데 화면이 지나는 문은 `doc_visible`(bin/s9:12748-12768)이고, 그것은 프로젝트 문서를 **활성 멤버에게만** 내준다. 「접근만 끊긴다」 쪽이 참이었다. 교정의 목적(화면이 거짓을 말하지 않기) 쪽으로 판정했다.
3. **active 점 잉크는 `TCOLOR.project || var(--text)`** — `--t-project` 토큰이 없고, 새로 지으면 그게 곧 새 색 하드코딩이라 원천을 읽되 없으면 잉크색으로 물러난다.
4. **제목 줄 상태 배지(`ACTIVE`)는 그대로 뒀다** — 「ACTIVE 비표기」는 목록 행·문맥 띠의 판정이었고 `.dst` 는 모든 문서 종류가 갖는 문법이다. 판정이 넓어지면 한 줄로 바꾼다.

### 고친 파일
`/home/sjpark1/section9/web/app/project.js` · `web/app/app.js` · `web/app/docs.js` · `web/app/events.js` · `web/app/tidy.js` · `web/css/header.css` · `web/css/skins.css` · `web/css/project.css` · `web/index.html` · `tests/test_project_screen.py`(45건) · `tests/test_dialog_safe.py`. git 조작 없음.

기록은 `REQ-20260831-028-62x6` 에 tdd 개정 1건 + response 3건, 캡처 8장 첨부.

### 2026-09-01T01:23:59+09:00 subagent (agent: sub:frontend-developer:ad4a83f9, by nicehugepark)

`[2026-09-01 01:23:0x KST - frontend-developer]`

배경 작업(전체 스위트 대기) 종료 알림이며 새 지시가 아닙니다. 앞선 보고 이후 달라진 것은 없습니다 — B2 는 완료 상태이고, 검증·기록도 끝났습니다.

최종 상태 재확인:
- 산출물: `/home/sjpark1/section9/web/app/project.js` · `web/app/app.js` · `web/app/docs.js` · `web/app/events.js` · `web/app/tidy.js` · `web/css/header.css` · `web/css/skins.css` · `web/css/project.css` · `web/index.html` · `tests/test_project_screen.py` · `tests/test_dialog_safe.py`
- 검증: 실서버·실브라우저 시나리오 15/15 · 표적 시험 106/106 · 전체 스위트에서 이 물결발 회귀 0(붉은 넷은 날짜 넘김 3 + 병렬 연결 끊김 1) · 스킨 10종 × 배율 3 = 30조합 육안 · CDP 실포인터 hover/focus 6조합
- 기록: `REQ-20260831-028-62x6` 에 tdd 개정 1건 + response 3건, 캡처 8장 첨부
- 남은 판정: 제목 줄 상태 배지(`ACTIVE`) 존치 여부 하나 — 넓히기로 하면 `statusLabel` 한 줄
- 임시물 정리 완료(격리 vault 의 web 심링크 제거). git 조작 없음.

### 2026-09-01T01:25:57+09:00 subagent (agent: sub:frontend-developer:ad4a83f9, by nicehugepark)

`[2026-09-01 01:25:42 KST - frontend-developer]`

또 다른 배경 작업(전체 스위트 대기) 종료 알림입니다 — 사용자 입력이 아니고, 새로 할 일도 없습니다. 남은 백그라운드 작업이 모두 끝났고 작업은 앞선 보고 그대로 완료 상태입니다.

- **산출물**: `/home/sjpark1/section9/web/app/project.js` · `web/app/app.js` · `web/app/docs.js` · `web/app/events.js` · `web/app/tidy.js` · `web/css/header.css` · `web/css/skins.css` · `web/css/project.css` · `web/index.html` · `tests/test_project_screen.py`(45건) · `tests/test_dialog_safe.py`
- **검증**: 실서버·실브라우저 시나리오 15/15 · 표적 시험 106/106 · 전체 스위트 이 물결발 회귀 0 · 스킨 10종 × 배율 1·1.25·1.5 = 30조합 확대 육안(띠 30/30 모두 32px) · CDP 실포인터 hover/focus 6조합(잠긴 「제거」 화소 불변)
- **기록**: `REQ-20260831-028-62x6` 에 tdd 개정 1건 + response 3건, 캡처 8장 첨부
- **리드 몫으로 남긴 것**: ① 전체 스위트의 붉은 넷 — 날짜 넘김 3(`tests/test_note_attach.py:55`·`tests/test_article_type.py:69` 가 `"2026","08"` 을 못박음) + 병렬 연결 끊김 1(단독 25/25 ok), 지시가 `tests/` 서버 시험 수정을 금해 손대지 않음 ② 제목 줄 상태 배지(`ACTIVE`) 존치 여부 ③ 멤버 제거 창 문안을 `doc_visible` 실측에 맞춰 중재본과 다르게 쓴 판정

git 조작 없음, 임시물 정리 완료.

## History
- 2026-08-31T22:52:31+09:00 created by nicehugepark (status: open)
- 2026-08-31T22:52:38+09:00 status: open -> in-progress (by nicehugepark) — W1 착수
