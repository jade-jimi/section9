---
id: REQ-20260828-025-62x6
type: request
title: 배너가 못 보는 훅 파일
summary: 낡은 코드 배너가 bin/s9 지문만 떠서, 서버가 캐시해 도는 bin/s9-audit-prompt 를 고치면 침묵한다
status: in-progress
size: S
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: adf77fd2
agents: ["lead:claude-opus-5"]
contributions: [{"actor": "lead:claude-opus-5", "item": "`[2026-08-28 16:33:49 KST - lead]`", "started": "2026-08-28T16:33:49+09:00", "ended": "2026-08-28T16:33:49+09:00", "result": "done", "transcript": ""}]
project: section9
parent: REQ-20260826-034-62x6
relates: ["REQ-20260826-011", "REQ-20260827-059-62x6"]
relates_why: {"REQ-20260826-011": "이 배너를 만든 요청 — 지문이 bin/s9 하나였던 자리를 넓힌다", "REQ-20260827-059-62x6": "훅이 매 턴 serve-stale 을 주입하게 한 요청 — 그 판정의 재료를 넓힌다"}
tags: ["audit", "terminal", "assets"]
created: 2026-08-28T16:17:47+09:00
status_since: 2026-08-28T16:17:53+09:00
updated: 2026-08-28T16:33:49+09:00
priority: 50
---

## Original

서버는 기동 시점 코드를 메모리에 들고 돈다. 그래서 낡은 코드 배너(REQ-20260826-011)가 있는데, 그 지문은 SERVE_CODE_STAMP = code_stamp() 로 bin/s9 하나만 뜬다.

그런데 채팅 판정자(classify·is_durable_question)는 bin/s9-audit-prompt 에 있고, 서버는 첫 채팅 때 한 번 로드해 _chat_classifier 로 캐시한다. 즉 훅만 고치면 서버는 옛 판정자를 계속 들고 도는데 배너는 침묵한다.

이 결함이 실제로 물린 자리가 부모 REQ-20260826-034-62x6 이다: 첨부 분류 고침이 훅에 들어갔는데 서버는 1시간 반 낡은 판정자로 채팅을 받았고, 그 결과 질문이 요청 카드가 되어 반려가 났다. 그때 배너가 뜬 것은 우연히 bin/s9 도 같이 만져지던 덕이었다.

서버가 메모리에 들고 도는 것이 그 둘이니, 지문도 그 둘이어야 한다.

## Notes

### 2026-08-28T16:20:04+09:00 tdd (by nicehugepark)

- [x] H1. 지문이 서버가 들고 도는 것 둘(bin/s9 · bin/s9-audit-prompt)을 담는다
- [x] H2. **훅만** 바뀌어도 낡음으로 잡힌다 — 이 결함이 놓치던 바로 그 경우
- [x] H3. bin/s9 만 바뀌어도 여전히 낡음 (기존 행동 보존)
- [x] H4. 둘 다 그대로면 조용하다 — 상시 경고는 곧 안 읽힌다
- [x] H5. 옛 단일 지문(이 고침 이전 서버가 남긴 것)도 깨지지 않는다 — 비교 가능한 bin/s9 만 본다
- [x] H6. 읽을 수 없으면 낡았다고 단정하지 않는다 (한쪽 파일만 못 읽은 경우 포함)
- [x] H7. serve_stale 메시지가 어떤 파일이 바뀌었는지 이름을 말한다
- [x] H8. 훅만 바뀐 서버에 대해 serve_stale 이 침묵하지 않는다
- [x] H9. serve 기동이 둘 다를 지문으로 남긴다
- [x] H10. /api/serveinfo 도 같은 지문·같은 판정을 쓴다 (화면과 CLI 가 갈리지 않는다)
- [x] H11. 지문의 훅 경로가 채팅 판정자를 실제로 로드하는 그 경로다

### 2026-08-28T16:32:21+09:00 response (by nicehugepark)

**무엇이 문제였나**

서버는 기동 시점 코드를 메모리에 들고 돈다. 그걸 알리라고 낡은 코드 배너(REQ-20260826-011-62x6)가 있는데, **지문을 `bin/s9` 하나만 떴다.** 채팅 판정자(`classify`·`is_durable_question`)는 `bin/s9-audit-prompt` 에 있고 서버가 첫 채팅 때 한 번 로드해 `_chat_classifier` 로 캐시한다 — 즉 **훅만 고치면 배너는 침묵한다.**

부모 REQ-20260826-034-62x6 의 반려가 정확히 그 자리다. 첨부 분류 고침은 훅에 들어갔는데 채팅을 받던 서버는 1시간 반 낡은 판정자를 들고 있었고, 질문이 요청 카드가 됐다. 그때 배너가 뜬 것은 **우연히 `bin/s9` 도 같이 만져지고 있던 덕**이었다. 다음번에 훅만 고치면 아무도 울지 않는다.

**고침 (bin/s9)**

- `running_code_stamp()` — 서버가 들고 도는 것 전부의 지문: `{s9, hook}`. 기동 시 `SERVE_CODE_STAMP` 가 이걸 뜨고 `state/serve-code.json` 에 그대로 남는다.
- `stale_parts(started, current)` — 어긋난 파일의 이름 목록. 참/거짓이 아니라 이름을 돌려주는 이유는 사람이 무엇이 안 도는지를 알아야 재기동을 결정하기 때문이다. `stamp_is_stale` 은 그것의 참/거짓.
- **한쪽에만 있는 이름은 판정하지 않는다.** 이 고침 이전에 뜬 서버는 훅 지문을 뜬 적이 없다 — 뜬 적 없는 것으로 낡음을 단정하면 근거 없는 경고가 되고, 근거 없는 경고는 곧 무시된다(`code_is_stale` 과 같은 규율). 대신 옛 형식(`{mtime,size}`)은 `{s9: ...}` 로 맞춰 비교할 수 있는 것은 비교한다 — 그 서버를 통째로 판정 불가로 떨어뜨리면 정작 낡은 쪽이 조용해진다.
- `serve_stale()` · `/api/serveinfo` · 대시보드 배너가 모두 같은 지문·같은 판정을 쓰고, 바뀐 파일 이름을 그대로 말한다. 배너 문구는 `bin/s9` 로 못 박혀 있었는데(훅만 바뀐 경우엔 거짓말이 된다) 이제 서버가 준 `changed` 를 그린다.
- `chat_audit` 의 훅 로드도 `hook_path()` 하나로 모았다 — 지문과 로드가 다른 파일을 가리키면 배너는 또 엉뚱한 것을 본다.

**검증**

- `python3 tests/ hook_code_stamp` — 12건 통과. 핵심은 H2(훅만 바뀌어도 낡음)와 H5(옛 형식 지문도 안 깨진다).
- `python3 tests/ serve_stale · stale_serve · oldcode_banner · attachment_classify` — 전부 통과(각 10·5·5·11건).
- 전체 스위트 1429건, 실패 2. 둘 다 `test_instance_init` 이고 이 변경과 무관하다: i1 은 원격 리포가 없어 `git push` 가 실패한 것, i6 은 스위트가 도는 동안 vault 문서(이 REQ 문서 자신 포함)가 새로 쓰여 임시 클론의 `git merge` 가 충돌한 것이다. 둘 다 환경이지 코드가 아니다.
- 화면: `?oldcode=1` 로 두 파일이 다 바뀐 가장 긴 줄을 강제해 캡처했다 — 한 줄에 들어가고 헤더가 밀리지 않는다. `state/shots/req025-oldcode-two-files.png`

**한 줄**

서버가 메모리에 들고 도는 것이 둘이면 지문도 둘이어야 한다. 하나만 뜨는 지문은 고쳐도 안 도는 상태를 정확히 그 순간에만 못 본다.

### 2026-08-28T16:33:49+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-08-28 16:33:49 KST - lead]`

승인 메모는 질문이 아니라 리드 판정이었습니다. 다만 그 문서에 닫히지 않은 채 남아 있던 제안 하나가 있어 후속으로 등록하고 구현했습니다.

**REQ-20260828-025-62x6 「배너가 못 보는 훅 파일」** (부모: REQ-20260826-034-62x6)

낡은 코드 배너는 `bin/s9` 하나만 지문을 떴는데, 채팅 판정자는 `bin/s9-audit-prompt` 에 있고 서버가 첫 채팅 때 로드해 캐시합니다 — **훅만 고치면 배너가 침묵**합니다. 부모 REQ의 반려를 만든 상황이 바로 그것이었고, 그때 배너가 떴던 건 우연히 `bin/s9` 도 같이 만져지던 덕이었습니다.

- `running_code_stamp()` 이 두 파일의 지문을 뜨고, `stale_parts()` 가 **어긋난 파일 이름**을 돌려줍니다. `serve_stale` · `/api/serveinfo` · 대시보드 배너가 모두 그 이름을 그대로 말합니다.
- 옛 형식 지문(이 고침 이전에 뜬 서버)은 비교 가능한 `bin/s9` 만 봅니다 — 뜬 적 없는 것으로 낡음을 단정하지 않는 기존 규율 유지.
- `chat_audit` 의 훅 로드도 `hook_path()` 로 모아 지문과 갈리지 않게 했습니다.

검증: `tests/test_hook_code_stamp.py` 12건 통과, 관련 스위트(serve_stale·stale_serve·oldcode_banner·attachment_classify) 전부 통과. 전체 1429건 중 실패 2건은 `test_instance_init` 으로 이 변경과 무관합니다(원격 리포 없음으로 push 실패 / 스위트 실행 중 vault 문서가 쓰여 임시 클론 merge 충돌). 배너 화면은 두 파일이 다 바뀐 가장 긴 줄을 강제해 캡처했고 한 줄에 들어갑니다.

커밋은 아직 못 했습니다. `s9-guard` 의 동시작업 게이트가 걸렸는데(워크트리 사본이라 실제 충돌은 없고, `git diff --cached` 로 담기는 넷이 전부 제 편집임을 확인했습니다), 가드가 안내하는 `S9_ALLOW_CONCURRENT=1 git commit` 은 환경변수 접두라 이 세션의 허용 목록이 거부합니다. 우회하지 않고 게이트 자체의 조건(파일 수정 후 15분)이 지나기를 기다리는 중이며, 16:47경 커밋하고 결과를 보고하겠습니다.

## History
- 2026-08-28T16:17:47+09:00 created by nicehugepark (status: open)
- 2026-08-28T16:17:53+09:00 status: open -> in-progress (by nicehugepark) — 부모 REQ-20260826-034-62x6 승인 종결 후속 — 문서에 남아 있던 제안 둘(낡은 코드 배너가 bin/s9-audit-prompt 를 안 본다)을 닫는다.
