---
id: REQ-20260831-001-62x6
type: request
title: catalog 폭주가 서버를 세운다
summary: /api/catalog 한 요청이 load_catalog 39회·json.loads 3만회(736문서, 1.75s CPU)를 부르고, 폴링이 겹치면 스레드가 쌓여 GIL 정체로 응답이 분 단위(실측 72~91s→120s+ 타임아웃)가 된다
goal: /api/catalog 1회가 부하 없이 0.3초 이내, 동시 폴링 하에서도 5초 이내로 응답하는 것이 실측되고 회귀 시험이 선다
status: in-progress
size: M
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: 982e7476
agents: ["sub:backend-developer:aa5afd93", "sub:backend-developer"]
contributions: [{"actor": "sub:backend-developer:aa5afd93", "item": "001 catalog 성능 결함 수리", "started": "2026-08-31T00:33:00+09:00", "ended": "2026-08-31T00:53:30+09:00", "result": "stalled", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/982e7476-7fbe-4344-b885-ee3b1f3c495c/tasks/aa5afd93347c3b5c9.output", "reason": "212초째 진전 없음 (기준 180초)"}, {"actor": "sub:backend-developer", "item": "TDD 시나리오 (catalog 프로세스 캐시 — 관문 load_catalog 한 곳)", "started": "2026-08-31T00:35:47+09:00", "ended": "2026-08-31T00:35:47+09:00", "result": "done", "transcript": ""}, {"actor": "sub:backend-developer", "item": "TDD 시나리오 (2차 — 1차 캐시 후 실측 0.75~0.84s, goal 0.3s/동시 5s 미달로 확장. 1차 S1~S6", "started": "2026-08-31T00:41:12+09:00", "ended": "2026-08-31T00:41:12+09:00", "result": "done", "transcript": ""}, {"actor": "sub:backend-developer", "item": "TDD 시나리오 (최종 — 전부 통과, tests/test_catalog_cache.py 11개 + 회귀)", "started": "2026-08-31T01:26:41+09:00", "ended": "2026-08-31T01:26:41+09:00", "result": "done", "transcript": ""}, {"actor": "sub:backend-developer", "item": "catalog 폭주 수리 — 재파싱 관문 캐시 + 동시 폴링 계산 공유 (bin/s9, HEAD e99f296 대비 +111/", "started": "2026-08-31T01:27:07+09:00", "ended": "2026-08-31T01:27:07+09:00", "result": "done", "transcript": ""}, {"actor": "sub:backend-developer:aa5afd93", "item": "`[2026-08-31 01:14:52 KST - backend-developer]`", "started": "2026-08-31T01:27:29+09:00", "ended": "2026-08-31T01:27:29+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/982e7476-7fbe-4344-b885-ee3b1f3c495c/subagents/agent-aa5afd93347c3b5c9.jsonl"}]
project: section9
relates_why: {}
tags: ["index"]
created: 2026-08-31T00:32:20+09:00
status_since: 2026-08-31T00:32:34+09:00
updated: 2026-08-31T01:27:29+09:00
priority: 50
---

## Original

실측: 새 프로세스(00:26 기동)도 3분 만에 CPU 159%. cProfile: catalog_with_live 1.752s 중 load_catalog 39회 1.028s(30,404 json.loads), claim_dead 0.555s, _stop_claim_map 0.440s. 서버 스레드 다수가 각자 같은 계산을 반복(폴링 적체). 루트(정적)는 0.3s — 무거운 엔드포인트만 기아.

## Notes

### 2026-08-31T00:35:47+09:00 tdd (agent: sub:backend-developer, by nicehugepark)

### TDD 시나리오 (catalog 프로세스 캐시 — 관문 load_catalog 한 곳)
- [ ] S1 정상(1회 파싱): 같은 프로세스에서 load_catalog 를 연속 2회 부르면 json 파싱은 첫 호출에만 일어난다 (두 번째는 캐시 적중, 행 내용은 동일)
- [ ] S2 경계(무효화): 인덱스 파일이 갱신되면(rebuild_index / 직접 재작성) 다음 load_catalog 가 재파싱해 새 행을 돌려준다 — 낡은 캐시가 새 문서를 감추지 않는다
- [ ] S3 실패(캐시 오염 없음): 반환 행에 호출자가 r['live']=True 등 최상위 키를 덧써도 다음 load_catalog 반환에는 그 키가 없다 (catalog_with_live 의 장식이 캐시에 스미지 않음)
- [ ] S4 경계(파일 없음): 인덱스 파일이 없으면 기존과 같이 [] — 캐시가 옛 행을 유령으로 돌려주지 않는다
- [ ] S5 회귀(한 패스 1회 파싱): catalog_with_live 한 번에 인덱스 파싱이 정확히 1회다 (종전 39회 — claim_dead·_stop_claim_map·canon_id 경유 재파싱 제거)
- [ ] S6 회귀(기존 시험): 기존 catalog 사용 시험(test_stall_visible·test_claim_clock·test_claim_release·test_jobfile·test_stop_branch 등) 전부 통과
검증 실측: cProfile 전후 비교(load_catalog 내 json.loads 호출수·catalog_with_live 총 시간)를 response 노트에 첨부

### 2026-08-31T00:41:12+09:00 tdd (agent: sub:backend-developer, by nicehugepark)

### TDD 시나리오 (2차 — 1차 캐시 후 실측 0.75~0.84s, goal 0.3s/동시 5s 미달로 확장. 1차 S1~S6 유지)
- [ ] S1 정상(1회 파싱): load_catalog 연속 2회 → 파싱은 첫 호출만
- [ ] S2 경계(무효화): rebuild_index / 직접 재작성 후 다음 호출이 새 행을 본다
- [ ] S3 실패(오염 없음): 반환 행 최상위 키 덧쓰기가 캐시에 안 스민다
- [ ] S4 경계(파일 없음): 기존과 같이 [] — 유령 캐시 금지
- [ ] S5 회귀(한 패스 1회 파싱): catalog_with_live 1회 = 인덱스 파싱 1회 (종전 44회)
- [ ] S6 회귀(기존 시험): catalog 사용 기존 스위트 전부 통과
- [ ] S7 파생 캐시(id_alias): 인덱스가 안 바뀌면 alias 맵을 재구축하지 않고, 인덱스 갱신 시 재구축한다 (패스당 18회 → 1회)
- [ ] S8 동시 공유: 동시에 들어온 catalog_with_live 기본 호출 여럿이 계산 1회를 공유한다 — 계산 중이면 결과를 기다린다 (폴링 적체 → GIL 정체 나선 차단)
- [ ] S9 동시 공유 격리: stall_win 을 명시한 호출(CLI 경로)은 공유를 타지 않고 제 값을 받는다

### 2026-08-31T01:26:41+09:00 tdd (agent: sub:backend-developer, by nicehugepark)

### TDD 시나리오 (최종 — 전부 통과, tests/test_catalog_cache.py 11개 + 회귀)
- [x] S1 정상(1회 파싱): load_catalog 연속 2회 → 파싱은 첫 호출만 (gen 불변)
- [x] S2 경계(무효화): rebuild_index(os.replace)·직접 재작성 모두 다음 호출이 새 행을 본다
- [x] S3 실패(오염 없음): 반환 행 최상위 키 덧쓰기(r['live'] 등)가 캐시에 안 스민다
- [x] S4 경계(파일 없음): [] 반환 — 유령 캐시 금지, 복귀 시 재파싱
- [x] S5 회귀(한 패스 1회 파싱): catalog_with_live 1회 = 인덱스 파싱 1회 (종전 44회)
- [x] S6 회귀(기존 시험): stop_branch·stop_reach·wake_reach·stall_visible 78개 + claim/jobfile 계열 + 전체 스위트 (--jobs 4 flake 6파일은 직렬 재실행 64개 전부 green — HTTP 타임아웃, 라이브 서버 158% CPU 부하 겹침으로 판명)
- [x] S7 파생 캐시(id_alias): 인덱스 불변 시 재구축 없음·갱신 시 1회 재구축·명시 rows 는 캐시 우회·반환 복사본
- [x] S8 동시 공유: 동시 기본 호출 4개가 계산 1회 공유, 응답 간 dict 비공유
- [x] S9 공유 격리: stall_win 명시 호출(CLI·wake)은 공유를 타지 않는다 (tombstone 배제 계약 시험 포함)

### 2026-08-31T01:27:07+09:00 response (agent: sub:backend-developer, by nicehugepark)

### catalog 폭주 수리 — 재파싱 관문 캐시 + 동시 폴링 계산 공유 (bin/s9, HEAD e99f296 대비 +111/-14)

### 뿌리 (재검증)
한 catalog 패스가 load_catalog 를 **44회** 불러 json.loads 34,133회(737문서). 호출 그래프: catalog_with_live 본체 1회 + claim_dead(유예 경과 클레임마다 문서 조회) + canon_id 118회가 alias 미지참 시 id_alias 를 **18회 재구축** + _stop_claim_map(id_alias+binding_req_ids 경유) + resolve_id/locate. 수정 전 실측(모듈 직호출, 라이브 vault): 단독 2.41~2.50s, **동시 8요청 85.6s** — 리드가 본 72~91s 나선과 일치(중복 계산이 GIL 을 나눠 먹는 정체).

### 고침 (관문 한 곳, 옆문 없음)
1. **load_catalog 프로세스 캐시**: 인덱스 파일 stat(mtime_ns·size·ino) 키. rebuild_index 는 os.replace 라 무효화 보장(fstat 으로 연 fd 의 stat 을 읽어 교체 경합 제거). 반환은 행별 얕은 복사 — 호출자의 최상위 덧쓰기(r['live'] 등)가 캐시에 안 스민다(중첩값 제자리 변형은 전수 grep 로 부재 확인, 주석으로 금지 명시). Lock 이 동시 스레드의 겹침 파싱도 1회로 모은다.
2. **id_alias 파생 캐시**: 카탈로그 캐시 키 종속(_ALIAS_CACHE). 명시 rows(부분집합) 호출은 우회.
3. **catalog_with_live 동시 공유 게이트**: 기본 호출(stall_win=None, /api/catalog 폴링)이 계산 중이면 결과를 기다려 받는다(낡음 상한=계산 1회 길이). stall_win 명시 호출(CLI·wake)은 다른 창의 판정이라 우회. 데코레이터(_share_default_pass)로 얹어 판정 본문이 def catalog_with_live 블록에 남는다 — stop_branch/stop_reach 의 구조 계약 시험 4곳 무수정 통과. compute 내부의 catalog_with_live 재호출 2곳(6380·9320)은 모두 stall_win 명시라 재진입 데드락 없음.

### 실측 (전후, 737문서)
- cProfile: catalog_with_live 3.556s → 1.998s, 인덱스 파싱 44회 → **1회**, json.loads 34,133 → 1,705(비카탈로그 몫)
- 벽시계: 단독 2.41~2.50s → **0.80~0.89s** · 동시 8요청 85.6s → **0.70s**(계산 1회 공유, max 0.68s)
- goal 대비: '동시 폴링 5초 이내' **충족**(0.70s, 122배). '부하 없이 0.3초'는 0.8s 로 **미달** — 남은 절반은 다른 뿌리다: 바인딩 활동 신선도 스캔(streams_glob 181회·resolve_user 177회/패스)과 /proc 전수 조회 43회/패스(무캐시는 test_dashboard_chat C9/C18 이 붙잡은 신선도 계약). 필요하면 파생 REQ 로 분리 권고.

### 시험
tests/test_catalog_cache.py 신규 11개 green(격리 tmp 루트, 병렬 안전 — SERIAL 불요). 전체 스위트 --jobs 4: 18건 실패는 전부 HTTP 5s 타임아웃 계열로 라이브 서버(pid 404411, CPU 158%) + 병렬 부하의 자원 기아 flake — 해당 6파일 직렬 재실행 64개 전부 green. 서버 404411 미접촉 — 신판 적용은 재기동(리드 몫) 필요.

### 2026-08-31T01:27:29+09:00 subagent (agent: sub:backend-developer:aa5afd93, by nicehugepark)

`[2026-08-31 01:14:52 KST - backend-developer]`

**REQ-20260831-001-62x6 완료 보고 — catalog 재파싱 관문 캐시 + 동시 폴링 계산 공유. 결론: 동시 8폴링 85.6s → 0.70s(122배), 단독 2.5s → 0.8s. goal 의 "동시 5초 이내"는 충족, "부하 없이 0.3초"는 0.8s 로 미달(남은 절반은 다른 뿌리 — 아래).**

### 뿌리 (재검증 — 리드 실측과 일치)
한 catalog 패스 = load_catalog **44회**, json.loads 34,133회(737문서). 경로: `claim_dead`(유예 경과 클레임마다 문서 재조회) · `canon_id` 118회 → `id_alias` **18회 재구축** · `_stop_claim_map` · `resolve_id`. 수정 전 HEAD 판 실측: 단독 2.41~2.50s, **동시 8요청 85.6s** — 보고된 72~91s 나선이 정확히 이 중복 계산의 GIL 정체다.

### 고침 (`/home/sjpark1/section9/bin/s9`, +111/-14)
1. **load_catalog 프로세스 캐시** (관문 한 곳): 인덱스 파일 stat(mtime_ns·size·ino) 키 — `rebuild_index` 가 os.replace 라 무효화 보장, fstat 으로 교체 경합 제거. 반환은 행별 얕은 복사(호출자의 `r["live"]=…` 덧쓰기가 캐시에 안 스밈). Lock 이 동시 파싱도 1회로 모은다.
2. **id_alias 파생 캐시**: 카탈로그 키 종속. 명시 rows 호출은 우회.
3. **catalog_with_live 동시 공유 게이트**: 기본 호출(폴링)은 계산 중이면 결과 대기(낡음 상한 = 계산 1회). `stall_win` 명시 호출(CLI·wake)은 우회. **데코레이터**로 얹어 `def catalog_with_live` 블록 앵커를 검사하는 기존 구조 시험(stop_branch·stop_reach 4곳)이 무수정 통과. compute 내부 재호출 2곳은 모두 stall_win 명시라 재진입 데드락 없음.

### 실측 (cProfile + 벽시계, 전후)
| | 전 | 후 |
|---|---|---|
| 인덱스 파싱/패스 | 44회 | **1회** |
| cProfile catalog_with_live | 3.556s | 1.998s |
| 벽시계 단독 | 2.41~2.50s | **0.80~0.89s** |
| 벽시계 동시 8요청 | **85.6s** | **0.70s** |

### 한계 — 0.3s 미달의 남은 뿌리 (범위 밖, 판단 요청)
남은 0.8s 의 절반은 재파싱이 아니라 **바인딩 활동 신선도 스캔**이다: streams_glob 181회·resolve_user 177회/패스, `/proc` 전수 조회 43회/패스. `/proc` 무캐시는 test_dashboard_chat C9/C18 이 붙잡은 신선도 계약이라 손대지 않았다. 0.3s 까지 밀려면 파생 REQ 분리를 권한다.

### 시험
- 신규 `/home/sjpark1/section9/tests/test_catalog_cache.py` 11개 green (S1~S9, 격리 tmp·병렬 안전, SERIAL 불요). TDD 체크 9/9 문서 갱신됨.
- 전체 스위트 `--jobs 4`: 18건 실패는 전부 HTTP 5s 타임아웃 — 라이브 서버(404411, CPU 158%) + 병렬 부하의 자원 기아 flake로 판명(해당 6파일 직렬 재실행 **64개 전부 green**). 소스 앵커 스위트 78개 포함 통과.

기록: tdd·response 노트 REQ 문서에 기재 완료. **서버 404411 은 미접촉** — 구판으로 계속 돌고 있으니 재기동해야 신판이 적용된다. 커밋·상태 전이는 리드 몫.

## History
- 2026-08-31T00:32:20+09:00 created by nicehugepark (status: open)
- 2026-08-31T00:32:34+09:00 status: open -> in-progress (by nicehugepark) — backend-developer 위임 — 한 패스 내 메모이즈 + 인덱스 mtime 기반 캐시 방향 검토
