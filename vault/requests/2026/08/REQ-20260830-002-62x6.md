---
id: REQ-20260830-002-62x6
type: request
title: 끝난 일에 작업자가 뜬다
summary: done 으로 닫힌 문서에 무인 작업자가 스폰돼 등뼈 파일을 잡고 다른 일을 막는다
goal: done·cancelled 문서에는 어떤 경로로도 작업자가 뜨지 않는다
status: open
size: S
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: 911949e0
agents: ["sub:backend-developer:ab8077dd", "sub:designer:a8222a3d", "sub:subagent:a9c776bb", "sub:subagent:a0dc00c0"]
contributions: [{"actor": "sub:backend-developer:ab8077dd", "item": "037·039서버·002 서버 몫", "started": "2026-08-30T00:37:26+09:00", "ended": "2026-08-30T00:37:26+09:00", "result": "running", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/911949e0-5d1b-4cd2-b08d-70fd0415a19a/tasks/ab8077ddf58a99d32.output"}, {"actor": "sub:designer:a8222a3d", "item": "`[2026-08-30 01:07:25 KST - designer]`", "started": "2026-08-30T01:08:12+09:00", "ended": "2026-08-30T01:08:12+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/911949e0-5d1b-4cd2-b08d-70fd0415a19a/subagents/agent-a8222a3d67a8e02d4.jsonl"}, {"actor": "sub:subagent:a9c776bb", "item": "Drafting note3.txt for REQ-20260830-002-62x6", "started": "2026-08-30T01:11:30+09:00", "ended": "2026-08-30T01:11:30+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/911949e0-5d1b-4cd2-b08d-70fd0415a19a/subagents/agent-a9c776bb97dda844f.jsonl"}, {"actor": "sub:subagent:a0dc00c0", "item": "Appending backend-developer note to REQ-20260830-002-62x6", "started": "2026-08-30T01:28:42+09:00", "ended": "2026-08-30T01:28:42+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/911949e0-5d1b-4cd2-b08d-70fd0415a19a/subagents/agent-a0dc00c09ce47ab62.jsonl"}]
project: section9
relates_why: {}
tags: ["workflow", "index"]
created: 2026-08-30T00:34:39+09:00
updated: 2026-08-30T01:30:53+09:00
priority: 50
---

## Original

실사고 2026-08-30 00:30. `REQ-20260829-019-62x6` 은 18:56 에 done 으로 닫혔는데, 00:30 에 무인 작업자(pid 930298)가 그 문서로 떴다. 리스로 `bin/s9`·`web/index.html`·시험 둘을 잡아 **대기 중이던 다른 작업들을 그만큼 더 막았다.**

`wake_request` 는 in-progress 가 아니면 거부한다(계약 W6, `not-in-progress`). 그러니 이 경로는 사람이 누른 깨우기가 아니라 **재개 워처의 스폰**이다 — 그쪽 게이트가 상태를 다시 확인하지 않거나, 낡은 색인을 본다.

REQ-20260829-016 «리드가 붙은 일에 작업자가 또 뜬다» 와 같은 뿌리다: **손잡이 쪽 게이트와 워처 쪽 게이트가 두 벌**이고 워처 쪽이 성기다. 016 은 겹침을, 이것은 상태를 놓친다.

곁가지로 확인된 것: 물러난 작업자(014, pid 876195)의 리스 파일이 그대로 남아 있었다. `lease_prune` 이 있는데도 남았으므로, 언제 도는지·무엇을 기준으로 거두는지 확인이 필요하다.

## Notes

### 2026-08-30T00:36:00+09:00 response (by nicehugepark)

### 곁가지로 드러난 막다른 길 — 끝난 문서의 작업자는 세울 수 없다 (리드 실측)

작업자를 세우려 했더니 이렇게 답한다:

    REQ-20260829-019-62x6 를 집지 않은 세션은 그 작업자를 세울 수 없다
      — 먼저 `s9 last REQ-20260829-019-62x6 --add` 로 집어라.

시킨 대로 했다. `active_reqs += REQ-20260829-019-62x6` 이 출력됐다. 그런데 다시 세우려니
같은 거부가 나온다. 바인딩을 직접 읽어 보니 그 id 가 목록에 없다:

    binding_req_ids(b) → [025, 029, 037, 038, 039]   ← 019 없음

**`binding_req_ids` 가 살아 있는 요청만 낸다.** 019 는 `done` 이므로 아무리 집어도 그 목록에
안 들어가고, `_session_claims` 는 영원히 거짓이다. 즉 **끝난 문서에 뜬 작업자는 클레임
경로로는 세울 수 없다.** 게다가 거부 문구가 시키는 명령은 **성공할 수 없는 명령**이다 —
사용자를 막다른 길에 세우는 그 형태(REQ-20260827-079-62x6 가 계정 창에서 겪은 것과 같다).

이 요청이 막아야 할 것이 둘로 갈린다:
1. **애초에 안 뜨게 한다** (본래 goal) — done·cancelled 에는 어떤 경로로도 스폰되지 않는다.
2. **떠 버렸으면 세울 수 있어야 한다** — 상태와 무관하게, 그 문서를 집었던 세션이나 사람이
   이유를 대고 세울 수 있어야 한다. 1이 완벽해도 2가 없으면 다음 구멍에서 또 갇힌다.

지금 자리는 `s9 workers --stop-all` 뿐인데, 그것은 **도는 것을 전부** 세우므로 옆에서
정당하게 일하던 작업자까지 함께 죽는다(이번에 실제로 그랬다).

### 2026-08-30T01:08:12+09:00 subagent (agent: sub:designer:a8222a3d, by nicehugepark)

`[2026-08-30 01:07:25 KST - designer]`

### 실측 — 이 표시는 오늘 몇 개나 서는가

`/api/catalog` 실측: **진행 중 7건 중 `workspace` 가 실린 것 2건** (REQ-029·REQ-030, 둘 다 `main`/`live-verify`). **반려 시각 22:37 에는 0건이었다** — 029 에 값이 붙은 것이 23:07, 030 이 00:36 이다. 사용자는 안내를 못 읽은 게 아니라 **볼 것이 없는 판을 봤다.**

다만 "거의 모든 날에 안 보이는 기능"은 아니다. 서버는 스폰마다 그 문서에 값을 싣는다 — 기능이 산 지 두 시간이라 2건이고, 스폰이 쌓이면 진행 중 카드 대부분이 갖는다.

### 결정: 없는 값을 지어내지 않고, **있는 표시를 찾을 수 있게** 만든다

기각한 대안 셋 —
- **화면이 저장소 사정으로 대신 말한다**: 자리는 서버 `workspace_decision` 하나가 정한다. 화면이 미커밋 여부로 추측하면 판정이 두 벌이 되고 한 벌만 고쳐진다(이 저장소가 세 번 밟은 실패).
- **진단으로만 두고 카드에서 뺀다**: 못 보는 기능은 없는 기능이다. 실데이터 2건이 이미 서 있다.
- **헤더 칩을 늘 세운다**: `live-verify` 에는 사람이 할 일이 없다. 할 일 없는 상시 칩은 진짜 손이 필요한 날 안 읽힌다. 헤더의 침묵은 유지.

### 고친 것 — 표시가 아니라 **재질**이 문제였다

2차는 표(◇)를 붙였지만 재질은 메타 줄 어휘 그대로였다. mono 10px 흐린 글자는 `#태그`·`보통`·`S` 와 같은 급이라, 560px 에서 태그 줄로 밀려 `#index` 옆에 붙어 **태그로 읽혔다**(캡처 확인).

1. `.wsat` 에 **점선 밑줄** — 이 화면이 이미 쓰는 "누르면 더 있다" 문법(doclink, `.prio.pfull .pname`)의 셋째. 메타 줄에서 밑줄은 이 칩뿐이라 태그와 한눈에 갈린다. `border-bottom` 대신 `text-decoration` 인 이유: 재질은 같고 줄바꿈에도 따라가며, 2차 계약(색면·테두리 금지)을 안 건드린다.
2. 밑줄 색을 **글자색에서 파생**(`color-mix currentColor 60%`) — `--hairline` 을 빌렸더니 glass/carbon 에서 배경에 잠겼다(실측).
3. **과녁 22px** — 인라인 세로 패딩은 줄 상자를 안 넓히고, 가로는 음수 여백으로 되돌려 옆 태그가 한 픽셀도 안 밀린다.
4. `wsOpen` 창의 **첫 줄을 질문의 답으로** — 반려문이 곧 "어느 화면에서 확인하나"였다. 이제 `WS_MEANS` 가 잉크로 먼저 서고 사유·푸는 법이 뒤따른다. 강조는 하나, 나머지를 흐리게 내리지는 않았다(본문 대비).

### 리드가 그대로 쓸 확인 포인트 (210자, 2갈래)

**주소: `http://127.0.0.1:9909/?ws`**

> 진행 중인 카드의 이름 아래 줄에 점선 밑줄 그은 「◇ 본 저장소」가 섰습니다. F5 한 뒤 http://127.0.0.1:9909/?ws 를 열어 IN-PROGRESS 열 아무 카드의 그 글자를 눌러 보세요. 창 첫 줄에 「고친 것은 지금 보고 있는 이 화면에 바로 나타납니다」(워크트리면 「나타나지 않습니다」)가 뜨면 승인, 여전히 어디를 봐야 할지 모르겠으면 반려입니다.

`?ws` 는 서버가 줬을 값을 진행 중 행에 얹을 뿐, 그림은 평소 길(`cardHTML → wsChip`)이 그린다. 진단 없이 그냥 `/` 를 열어도 `우선순위가 곧 순서다`·`자리를 사람에게 말한다` 두 장에 실데이터로 서 있다 — 다만 030 을 review 로 옮기면 그 카드의 칩은 사라지므로(진행 중일 때만 그린다), 확인 포인트에는 `?ws` 를 쓰는 편이 안전하다.

### 캡처

`/tmp/claude-1000/-home-sjpark1-section9/911949e0-5d1b-4cd2-b08d-70fd0415a19a/scratchpad/shots/` — `24-실데이터-보드.png` · `25-실데이터-누른창.png` · `20-자리칩-보드.png` · `22-자리칩-좁은폭.png` · `21-자리칩-누른창.png` · `13b-glass-dark.png` · `23-자리칩-터미널다크.png` (전부 문서에 그림으로 첨부됨)

### 시험

`/home/sjpark1/section9/tests/test_ws_findable.py` **9건 신규** — 누를 수 있다는 재질이 있다 · 빌려 온 문법의 원래 자리가 살아 있다 · 태그는 그 밑줄을 안 쓴다 · 색면·테두리 여전히 없다 · 밑줄 색이 글자에서 파생한다 · 과녁이 글자보다 크다 · 판을 안 민다 · 창 첫 줄이 답이다 · 답이 두 벌이 아니다. 기존 `test_workspace_chip.py` 19건은 손대지 않고 그대로 통과.

**전체 스위트 `Ran 2285 tests in 487.380s`, `FAILED (failures=2, skipped=1)` — 둘 다 `bin/s9`, web/ 는 0.** 화면 관련 117건 전부 OK.

### 리드가 알아야 할 것

- **작업자가 겹쳐 떴다.** 00:36:25 에 자동 재작업 워커(pid 938747)가 스폰돼 `card.js`·`diag.js`·`test_workspace_chip.py` 를 고치고 스위트를 돌리는 중에, 00:38:09 에 내가 붙었다. 겹친 동안엔 파일을 안 건드리고 화면 확인만 했고, 워커가 죽은 뒤(00:45) 그 결과 위에 이어 고쳤다 — **워커가 남긴 것은 하나도 지우지 않았다.** REQ-20260829-016-62x6 의 구멍이 또 열렸다.
- **`bin/s9` 가 몇 분간 문법이 깨져 있었다** — 스위트 중 `s9 user add` 가 `SyntaxError: unterminated string literal (line 14206)`. 지금은 파싱된다. 남은 실패 `test_worker_stop.test_nothing_else_grew_a_kill` 도 `worker_stop` 본문이 아직 쓰이는 중(REQ-20260830-002-62x6)이라 나는 것으로, 커밋 전에 그쪽이 끝났는지 확인이 필요하다.
- **남긴 것**: 창 안 말투가 섞여 있다 — 제목·답은 합쇼체인데 사유·푸는 법은 반말이다(손 위 귀띔과 문자열을 나눠 쓰는 탓). 통일하려면 `WS_WHY` 를 고쳐야 하고 그러면 `test_workspace_chip` W16(`"커밋되지 않았다"`)이 깨진다. 그 파일은 이번 라운드 내 몫이 아니라 다음 라운드로 남겼다. `본 저장소`·`워크트리` 낱말도 같은 이유로 그대로다 — 대신 사람이 알아야 할 **뜻**을 창 첫 줄로 올렸다.

고친 파일: `/home/sjpark1/section9/web/css/board.css` · `/home/sjpark1/section9/web/css/overlay.css` · `/home/sjpark1/section9/web/app/card.js` · `/home/sjpark1/section9/tests/test_ws_findable.py`(신규). 상태 전이는 하지 않았다.

### 2026-08-30T01:11:30+09:00 subagent (agent: sub:subagent:a9c776bb, by nicehugepark)

Drafting note3.txt for REQ-20260830-002-62x6

### 2026-08-30T01:28:23+09:00 backend-developer (by nicehugepark)

### 원인은 낡은 색인이 아니었다 — 승인 후속 경로다 (구현·시험 완료)

**되짚은 자리** (증거: `state/sessions/approvals_seen.json` 이 **00:30 에** 쓰였고 그 안에 `REQ-20260829-019-62x6` 이 있다)

`rework_watch_tick` 은 두 갈래다. 앞 갈래(반려 재작업)는 `status != "in-progress"` 를 거르지만, **뒤 갈래(승인 후속, REQ-20260824-028)는 상태를 아예 안 본다** — `approvals_unseen()` 이 고르는 것이 애초에 `status == "done"` 인 문서이기 때문이다. 019 는 18:56 에 `review→done` 으로 닫히며 근거 메모를 남겼고, 그 메모가 미소비로 남아 30초마다 후보였다가 00:30 에 스폰됐다.

즉 **워처가 done 을 못 본 게 아니라, 그 갈래는 done 을 보고 뜨도록 만들어져 있었다.** 낡은 색인은 아니었다(그래도 그 구멍은 함께 막았다 — 아래 ①).

**그 대칭이 틀렸다.** 반려는 아직 할 일이 남은 in-progress 로 가지만 승인 메모는 언제나 done 으로 간다. 게다가 이 저장소는 `review→done` 에 충족 근거를 요구하므로 **승인마다 메모가 있고, 그러니 승인마다 작업자가 떴다.** 값은 두 번 치렀다: ① 리스 — 이번 사고 ② 예산 — 2026-08-29 실측으로 그날 20슬롯 중 4건이 이 경로였고 저녁의 사람 깨우기가 전부 capped 였다(tests/test_wake.py W4 가 그 실측을 들고 있다).

**고친 것 (`bin/s9`)**
1. `_spawn_worker` — 스폰이 반드시 지나는 **그 문 하나**에 상태 게이트를 세웠다. done·cancelled 면 `blocked="closed"` 로 거부한다(kill-switch 다음, 캡·자리 판정보다 **앞**: 어차피 안 띄울 후보에 git 을 부르고 예산을 셀 이유가 없다). 판정은 새 `doc_status_live()` 로 **디스크의 문서**에서 읽는다 — 카탈로그는 파생물이라 낡을 수 있고, 그 가설도 이 참에 함께 막힌다.
2. `rework_watch_tick` — 승인 후속 무인 스폰을 **걷어냈다**(`_spawn_followup`·`_approvals_mark` 제거). **잃는 것은 없다**: 메모는 소비되지 않은 채 남고 훅이 다음 리드 세션 앞에 그대로 놓는다(`s9 approvals --quiet`) — 그것이 REQ-20260824-028 의 본래 경로였고 무인 스폰은 폴백이었다. 물음이면 사람이 붙은 자리에서 답하고, 후속 작업이면 새 요청으로 연다.
3. `worker_stop` — **끝난 문서에는 클레임 게이트가 서지 않는다.** 그 게이트의 뜻은 "지나가는 세션이 남의 작업자를 끄지 못하게" 인데, 끝난 문서는 `_claim_req` 가 등록을 막으므로(REQ-20260828-036-62x6, 그것대로 옳다) 아무도 집을 수 없다 — 게이트가 시키는 명령이 성공할 수 없었다. 이유는 여전히 필수다. 이제 이 한 줄로 세운다:

       s9 workers --stop <문서id> --why '끝난 문서에 떴다'

4. `s9 last --add` — **거절을 성공처럼 찍지 않는다.** `_claim_req` 가 끝난 문서를 등록하지 않는데도 언제나 `active_reqs += <id>` 를 찍었다. 이제 등록됐는지 확인하고, 안 됐으면 왜인지 말하며 실패한다.
5. `lease_prune()` 이 워처 틱에서도 돈다. 종전엔 `lease_take` 안에서만 돌아 **다음 작업자가 뜰 때까지** 죽은 손의 리스 파일이 남았다(014, pid 876195 가 그것). 판정(`lease_conflict`)은 이미 죽은 pid 를 무시하므로 남을 막지는 않았지만, 쌓인 파일은 조용하고 조용한 것은 다음 사람을 헷갈리게 한다.

**시험**: `tests/test_closed_no_worker.py` 11건 green — done·cancelled 가 네 경로(wake·rework·followup·resume) 모두에서 거부 · **낡은 색인을 일부러 만들어도** 문이 안 열림 · in-progress 는 그대로 통과(회귀) · 승인 문서에 워처가 아무것도 안 띄우고 메모는 남음 · 끝난 문서 작업자를 클레임 없이 세움 · 이유 없으면 여전히 거부 · 진행 중 문서는 여전히 클레임 필요(회귀) · `last --add` 가 끝난 문서에 실패하고 산 문서엔 성공 · 스폰 없이도 죽은 리스가 거둬짐.

**판정을 청한다 (리드 몫)**: 2번은 **기능 하나를 걷어낸 것**이다. "done 에는 어떤 경로로도 안 뜬다" 를 글자 그대로 지키면 승인 후속 무인 작업자는 성립할 수 없다 — 그 경로는 언제나 done 으로만 가기 때문이다. 되돌리려면 `_spawn_worker` 의 상태 게이트에 `reason == "followup"` 예외를 두면 되지만, 그러면 이 사고의 자리가 그대로 열린다.

### 2026-08-30T01:28:42+09:00 subagent (agent: sub:subagent:a0dc00c0, by nicehugepark)

Appending backend-developer note to REQ-20260830-002-62x6

### 2026-08-30T01:30:53+09:00 decision (by nicehugepark)

### 판정: 승인 후속 무인 스폰을 걷어낸 것이 맞다 (리드)

담당이 판정을 청한 자리에 답한다. **걷어낸다.**

**근거 셋.**

① **그 갈래는 상태를 못 본 것이 아니라 done 을 보고 뜨도록 만들어져 있었다.**
`approvals_unseen()` 이 고르는 것이 애초에 `status == "done"` 인 문서다. 즉 이것은
구멍이 아니라 설계였고, 설계가 틀렸다. 반려는 in-progress 로 가지만 승인 메모는
**언제나** done 으로 간다 — 그러니 "승인할 때마다 작업자가 뜬다"가 규칙이 된다.

② **값이 실측으로 잡혔다.** 2026-08-29 하루 20슬롯 중 4건이 이 경로였고, 저녁에
사람이 누른 깨우기는 전부 `capped` 였다. 사람의 손잡이가 기계의 승인 후속에 밀려
죽은 것이다. 오늘 17:13 에 사용자가 본 `20/20` 창이 그 결과의 일부다.

③ **본래 경로가 살아 있다.** REQ-20260824-028 의 설계에서 승인 메모는 훅이 다음 리드
세션 앞에 놓는 것이었고 무인 스폰은 폴백이었다. 메모는 소비되지 않은 채 남으므로
그 경로는 그대로 돈다 — 걷어낸 것은 폴백이지 기능이 아니다.

**되돌리는 방법도 문서에 남아 있다** — 상태 게이트에 `reason == "followup"` 예외를
두면 된다. 다만 그러면 이 사고의 자리가 그대로 열리므로, 되돌릴 이유가 생기면
**그 예외가 아니라 "승인 후속은 done 이 아닌 자리에서 돈다"** 로 설계를 고치는 편이 맞다.

이 판정은 사람이 눌러 확인할 것이 없어 review 로 올리지 않고 리드가 닫는다.

## History
- 2026-08-30T00:34:39+09:00 created by nicehugepark (status: open)
