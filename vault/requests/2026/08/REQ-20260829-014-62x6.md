---
id: REQ-20260829-014-62x6
type: request
title: 서브에이전트의 말이 안 흐른다
summary: 서브에이전트 대화가 별도 파일에 있는데 스트림 API 가 리드 파일만 읽어 진행 문장이 통째로 빠진다
status: done
size: M
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: e6f62b91
project: section9
parent: REQ-20260829-013-62x6
relates: ["REQ-20260830-016-62x6"]
relates_why: {"REQ-20260830-016-62x6": "이 메시지가 판정 대기 문서를 이름으로 지목했다 (대시보드 채팅 audit)"}
tags: ["session", "design", "index"]
created: 2026-08-29T10:38:32+09:00
status_since: 2026-08-30T16:25:38+09:00
updated: 2026-08-30T16:25:38+09:00
priority: 50
---

## Original

deep-diver 규명(REQ-20260829-013)의 후반부. 서브에이전트의 말은 리드 세션 transcript 에 한 줄도 없고 <sessionUUID>/subagents/agent-<agentId>.jsonl 에 isSidechain:true 로 들어간다. /api/stream 의 parse_stream_file(bin/s9:11442)이 리드 파일 하나만 열어 스폰 두 줄과 완료 통지 사이가 통째로 0이다. isSidechain 분기(bin/s9:11464)와 화면의 사이드체인 렌더(web/index.html:7478)는 옛 레이아웃 유물로 지금은 도달 불가능한 죽은 코드다.

가장 무거운 지점: offset 이 단일 파일 바이트값이라 두 파일을 timestamp 로 머지하면 API 계약이 바뀐다. session_agents_map 이 이미 output 경로를 안다.

013 본체는 '살아 있는지 보이는가'(라벨·사라짐·스피너·초 갱신)를 맡고, 이 문서가 '무슨 말을 하는지 보이는가'를 맡는다.

## Notes

### 2026-08-29T18:11:01+09:00 tdd (by nicehugepark)

서브에이전트의 말이 리드의 말과 한 줄기로 흐르는가 — 화면이 두 원천을 겹친다(서버 offset 계약은 파일당 한 개 그대로).

- [ ] T1 스폰과 완료 사이가 0이 아니다: 에이전트 파일의 줄이 main 판에 ⚙ 요약선으로 들어온다
- [ ] T2 순서가 시각이다: 리드 줄과 에이전트 줄을 섞어도 쓰인 시각 순으로 정렬되고, 시각이 같거나 없으면 받은 순서가 유지된다
- [ ] T3 offset 은 원천마다 따로다: 에이전트마다 자기 offset 으로 증분만 받아 같은 줄을 두 번 그리지 않는다
- [ ] T4 이미 지나간 에이전트는 새로 따라잡지 않는다: 스트립에서 사라진(show=false) 에이전트는 추적을 시작하지 않되, 이미 따라가던 것은 남은 줄을 마저 받는다
- [ ] T5 누구의 말인지 보인다: 요약선이 에이전트 종류(designer 등)를 말한다 — agent 라는 익명이 아니라
- [ ] T6 붙일 때의 과거도 섞인다: 세션을 붙이면 에이전트 과거 줄이 리드 이력과 시각 순으로 병합돼 한 번에 그려진다(에이전트당 상한 있음)

### 2026-08-29T18:32:42+09:00 response (by nicehugepark)

### 적용 완료 (무인 작업자, 세션 e6f62b91)

**원인.** 서브에이전트의 말은 리드 transcript 에 한 줄도 없다 — `<sessionUUID>/subagents/agent-<id>.jsonl` 에 따로 쌓인다. 터미널 판은 `/api/stream` 하나만 읽으므로 스폰 두 줄과 완료 통지 사이가 통째로 0이었다. `isSidechain` 분기(bin/s9)와 화면의 사이드체인 렌더는 옛 레이아웃 유물이라 도달 불가능한 죽은 코드였다.

**고친 자리 — 화면(web/index.html)뿐이다.** 서버 offset 은 파일 하나의 바이트값이라 두 파일을 서버에서 머지하면 /api/stream 계약이 바뀐다(문서가 짚은 그 지점). 그래서 원천마다 자기 offset 을 들고 **화면이 시각으로만 겹친다** — 읽을 길은 이미 있었다(`/api/agents`·`/api/agentstream`).

- 순수 블록 `subagent merge core (pure)` 신설: `subFollowPlan`(틱마다 물어볼 목록·증분 offset·내려간 뒤 tail 한 번), `subBackfillPlan`(붙는 순간엔 끝난 에이전트도 포함), `subTag`(agent·atype·aid), `subCap`(에이전트당 200줄 상한), `subOrder`(안정 정렬).
- `termAttach`: `/api/agents` 를 함께 부르고 각 에이전트 파일을 읽어 리드 이력·수신함 로그와 한 벌로 정렬해 그린다. `T.subs` 는 세션 교체 때 비운다.
- 2.5초 `subTick`: 스트립이 아는 에이전트마다 자기 offset 으로 증분만 받아 같은 버퍼에 넣는다. `termScheduleFlush` 가 배치를 시각 순으로 겹친다.
- 죽은 사이드체인 렌더를 되살려 씀: 요약선이 `agent` 익명 대신 종류(backend-developer 등)를 말하고, 시각 도장은 미리보기에서 뺐으며(REQ-013 과 같은 규칙), 도구 줄은 리드와 같은 `ccToolSummary` 를 쓴다.

**정렬 열쇠 주의.** 열쇠는 표시 시각 `ts` 다. 섞이는 원천이 셋인데(수신함 채팅·리드·에이전트) 채팅 줄에는 원본 UTC(`at`)가 없어, UTC 를 우선 열쇠로 삼으면 채팅 줄이 9시간 어긋난 자리로 끌려간다. 시험 `test_the_chat_line_is_not_dragged_nine_hours` 가 이걸 잠근다.

**검증.** `tests/test_subagent_flow.py` 신설 27건 통과(순수 로직 node 실행 · 화면 소스 계약 · 스크립트 전체 파싱 · 실서버 통합 — 픽스처로 10분 침묵 구간을 만들고 두 API 로 그 구간이 메워지는지, 두 번째 물음엔 새 줄이 없는지 확인). 전체 스위트 2051건 OK(skipped 3). 실화면: `scratchpad/term-014d.png` (18:04 리드 메시지와 18:27 리드 메시지 사이가 ⚙ backend-developer 줄로 채워짐, 이전엔 그 사이가 빈칸), `scratchpad/term-014e.png`.

**남은 것 — bin/s9 범위라 제안만 남긴다(이 세션의 수정 범위는 web/·vault/·tests/).**
1. `session_agents_map` 은 Agent 도구의 tool_result 에서 `agentId`·`output_file` 을 읽는다. 그래서 **워크플로(Workflow) 안에서 도는 에이전트**(`subagents/workflows/wf_*/agent-*.jsonl`)와 tool_result 가 아직 안 온 포어그라운드 에이전트는 목록에 없고, 따라서 이 판에도 안 흐른다. 실측: 세션 911949e0 이 18:14~18:17 에 워크플로 에이전트 둘을 돌리는 동안 `/api/agents` 는 architect 하나만 답했다. 서버가 세션 디렉토리의 `subagents/**` 를 훑어 보완하면 그 구멍이 닫힌다.
2. `/api/agentstream` 은 폴링뿐이다(SSE 없음). 에이전트 줄만 2.5초 늦게 오는데, 리드 줄과 같은 즉시성을 원하면 SSE 를 열거나 `/api/stream/sse` 가 에이전트 델타를 함께 실어야 한다.

### 2026-08-30T00:00:30+09:00 workspace (by nicehugepark)

본 저장소에서 이어받는다 — 화면 작업이라 살아 있는 서버가 읽는 자리에서 해야 한다. 커밋하면 다음 라운드부터 작업자가 다시 자기 워크트리를 받는다.

### 2026-08-30T00:24:41+09:00 response (by nicehugepark)

### 2차(반려 반영) — 말은 그 에이전트의 판으로, main 은 리드의 판으로

**반려 사유가 옳다.** 1차는 서브에이전트의 줄을 main 판에 시각 순으로 섞었다. 에이전트 하나가 도구를 백 번 부르면 리드의 문장 사이가 남의 로그 수백 줄로 밀린다 — 침묵을 없애려다 읽을 수 없는 판을 만들었다.

**자리를 셋으로 갈랐다.**
1. **main 판은 리드의 판이다.** `termAttach` 의 에이전트 파일 되돌려 읽기(subBackfillPlan·subTag)와 2.5초 주입 틱(subTick)을 걷어냈고, `termScheduleFlush` 의 두 원천 정렬(subOrder)도 되돌렸다. main 에는 서브에이전트의 줄이 한 줄도 오지 않는다.
2. **말은 그 에이전트의 판에서 흐른다.** 하단 스트립 행 클릭(또는 ← 키)으로 열리는 `#cc-agview` 가 원래부터 그 자리였다 — 이번에 셋을 고쳤다. ① 처음 열 때 상한(200줄)만 그리고 자른 사실을 "이전 N줄 생략" 한 줄로 적는다(하루 돈 에이전트의 전문을 통째로 그리면 여는 데 몇 초가 걸리고 지금 하는 말은 맨 아래에 있다). ② 여는 순간 그 에이전트의 셈이 0이 된다. ③ 닫을 때의 자리가 다음 셈의 기준선이 된다.
3. **main 에 남는 것은 셈 하나다.** `agNewTick`(2.5초)이 에이전트마다 자기 offset 으로 증분을 받아 **줄 수만 세고 본문은 버린다**. 스트립 행 오른쪽에 초록 글자로 `새 N줄`(99+ 상한). 색면·띠 없이 글자로만. 이게 "지금 저기서 말하고 있다"의 신호이자 누를 자리를 가리키는 손이다. 셈의 규칙 둘: 처음 본 에이전트는 세지 않고 기준선만 잡는다(붙자마자 "새 300줄"은 새것이 아니라 과거다), 열어 둔 에이전트는 세지 않는다(읽는 중이다).

**곁들여 고친 것.** ① 스폰 줄을 그리면 스트립을 곧바로 맞춘다(600ms 묶음) — 10초 폴을 기다리면 "맡겼다"는 말과 "누를 자리"가 십 초 어긋난다. ② 세션에 붙는 즉시 `agTick()` 한 번 — 붙자마자 도는 에이전트가 없는 것처럼 보이지 않게. ③ `?agview` 진단 스위치 추가(?mpanel·?depall 선례) — 이 판은 클릭으로만 열려서 헤드리스 캡처로 확인할 길이 없었다.

**검증.** `tests/test_subagent_flow.py` 를 반려 방향으로 다시 썼다(26건 통과): main 판 계약(termAttach 에 /api/agentstream 없음 · 틱이 T.buf 를 건드리지 않음 · 정렬 없음 · 열린 판은 중복 집계 안 함), 에이전트 판 계약(폴링·상한·생략 표기·열면 0·닫으면 기준선·스트립 배지·스폰 즉시 반영), 셈 규칙 순수 실행(node), 실서버 통합(리드 파일의 10분 침묵 구간이 에이전트 파일에 있고 두 API 로 온다). 캡처: `scratchpad/r2-main.png`(main 판에 ⚙ 에이전트 줄이 없다), `scratchpad/r2-agview.png`·`r2-agtop.png`(그 에이전트의 판에서 로그가 흐른다 — Bash·Edit·결과까지).

**못 본 것.** `새 N줄` 배지는 지금 살아 있는(show=true) 에이전트가 없어 실화면 캡처로 못 봤다 — 셈 규칙은 node 실행으로, 렌더는 소스 계약으로 잠갔다.

**남긴 것(파일 소유 때문에 손대지 않음).** `web/app/ccrender.js` 는 다른 작업자가 잡고 있어(uncommitted) 건드리지 않았다. 그 안의 `subOrder`·`subTag`·`subBackfillPlan`·`subKey` 는 이번 되돌림으로 **쓰는 곳이 없어졌다** — 그 파일이 풀리면 지워야 한다(`subFollowPlan`·`subCap`·`SUB_BACKFILL_MAX` 는 계속 쓴다). 새로 만든 `subUnread`·`subNewMark` 는 같은 이유로 `web/app/terminal.js` 안에 자기 마커 블록으로 두었다 — 정리할 때 코어로 옮기면 된다.

### 2026-08-30T00:32:56+09:00 tdd (by nicehugepark)

개정(2차 반려 반영) — 말은 그 에이전트의 판에서 흐르고, main 판은 리드의 것으로 남는다. 1차 시나리오의 T1·T2·T6(main 병합)은 폐기한다.

- [x] R1 main 판이 깨끗하다: 붙을 때도 도는 중에도 서브에이전트의 줄이 리드 판에 들어오지 않는다(되돌려 읽기 없음, 폴러가 main 버퍼를 건드리지 않음)
- [x] R2 말은 그 판에서 흐른다: 스트립 행을 누르면(또는 ←) 그 에이전트의 로그가 열리고 2초마다 이어진다
- [x] R3 여는 값이 싸다: 처음 열 때 최근 상한만큼만 그리고, 자른 줄이 있으면 몇 줄을 생략했는지 적는다
- [x] R4 셈이 거짓말하지 않는다: 처음 본 에이전트는 기준선만 잡고(과거를 새 줄이라 부르지 않는다), 열어 둔 에이전트는 세지 않으며, 읽은 뒤에는 0에서 다시 센다
- [x] R5 신호가 스트립에 있다: 새 줄이 쌓이면 그 행이 "새 N줄"이라 말하고(색면 아닌 글자), 0이면 아무 말도 하지 않는다
- [x] R6 누를 자리가 늦지 않다: 스폰 줄을 그리면 스트립이 곧바로 맞춰지고, 세션에 붙는 즉시 목록을 한 번 받는다
- [x] R7 offset 은 원천마다 따로다: 에이전트마다 자기 자리에서 증분만 받고, 내려간 뒤에는 한 번만 더 받고 닫는다

## History
- 2026-08-29T10:38:32+09:00 created by nicehugepark (status: open)
- 2026-08-29T16:47:44+09:00 status: open -> in-progress (by nicehugepark) — drag 이동 [via dashboard]
- 2026-08-29T18:32:53+09:00 status: in-progress -> review (by nicehugepark) — 터미널 탭에서 리드가 서브에이전트에게 일을 맡긴 구간이 더는 빈칸이 아닙니다. F5 후 Terminal 탭을 열고 출력을 위로 훑어보세요. 리드 말 사이사이에 ⚙ backend-developer · … 같은 회색 줄이 시각 순으로 끼어 있으면 승인입니다. 그 구간이 여전히 비어 있거나 줄 순서가 뒤엉켜 있으면 반려입니다.
- 2026-08-29T22:46:24+09:00 status: review -> in-progress (by nicehugepark) — 반려: 이것 때문에 메인 리드 에이전트 터미널 창이 지져분해지고 있구나. 서브 에이전트가 스폰되면 터미널 하단에서 에이전트를 선택할 수 있게 되어있는데 그 터미널에서 로그를 출력해야지 [via dashboard]
- 2026-08-30T00:33:18+09:00 status: in-progress -> review (by nicehugepark) — 서브에이전트 로그를 리드 터미널 판에서 뺐습니다. F5 후 Terminal 탭을 보면 리드가 일을 맡긴 구간에 리드의 말만 이어집니다. 에이전트가 도는 동안에는 하단 스트립 행에 초록 글자로 "새 N줄"이 뜨고, 그 행을 누르면 그 판에서 로그가 흐릅니다(← 또는 esc 로 복귀). 그렇게 보이면 승인, main 판에 에이전트 줄이 다시 섞여 있으면 반려입니다.
- 2026-08-30T16:25:38+09:00 status: review -> done (by nicehugepark) — 승인 (메모 없음) [via dashboard]
