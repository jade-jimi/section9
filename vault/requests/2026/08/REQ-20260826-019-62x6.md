---
id: REQ-20260826-019-62x6
type: request
title: 대시보드 질문 타입 노출
summary: Docs 타입바·타입색·미답 표시·id 링크에 QST 타입 반영 (designer)
goal: Docs 타입바에서 question 으로 질문 문서를 걸러 볼 수 있고, 답 없는 질문이 카드에서 미답으로 구분된다
status: done
size: S
user: sjpark1
machine: DESKTOP-TEHV1KR
session: d3d60fdc
sessions: ["cb49b2cd", "d3d60fdc"]
agents: ["sub:architect:a8da0646"]
contributions: [{"actor": "sub:architect:a8da0646", "item": "결정 1 — 질문 문서는 훅이 사전에 만든다(즉석 인터랙션 제외 + 20자 이상), 답은 Stop 훅이 `last_qst` 폴", "started": "2026-08-26T19:40:02+09:00", "ended": "2026-08-26T19:40:02+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/cb49b2cd-601c-40bd-b9c4-d0afb0576d9f/subagents/agent-a8da064674cd81e78.jsonl"}]
project: section9
parent: REQ-20260826-017-62x6
relates: ["QST-20260826-003-62x6"]
tags: ["dashboard", "design", "docs"]
created: 2026-08-26T19:37:23+09:00
status_since: 2026-08-26T22:46:18+09:00
updated: 2026-08-26T22:46:18+09:00
priority: 50
---

## Original

QST(question) 타입이 CLI·훅·인덱스에 들어왔다(REQ-20260826-017). 대시보드가 이 타입을
모르면 사용자에겐 없는 것과 같다 — 화면 작업은 designer 몫이라 요구만 넘긴다.

필요한 것 (web/index.html):
1. **Docs 타입바에 question 추가** — `TYPE_ORDER = ["request","knowledge","session","project"]`
   에 question 을 넣는다. 순서는 사용 빈도 기준(현재 배열 규칙 유지).
2. **타입 색 토큰** — `--t-question` 을 스킨 4벌(기본/dark/calm/…) 모두에 정의하고
   `TCOLOR`/`TCOLOR_RAW` 에 등록. 색은 **면이 아니라 글자·마크·타이포**로 쓴다
   (색면 하이라이트 금지 · 카드 좌측 세로 띠 금지 · 무채색 미니멀 금지).
3. **미답 표시** — 카탈로그 row 에 `answered`(bool, question 전용) 필드가 이미 실려 온다.
   답이 없는 질문은 카드에서 구분돼야 한다("미답"). 이 타입의 값어치가 거기 있다.
   상태 배지가 아니라 질문 카드 안의 신호로(질문의 status 는 published 고정이다).
4. **id 링크 인식** — 본문 id 링크/추출 정규식 3곳이 `REQ|DOC|SES` 로 하드코딩돼 있다
   (web/index.html 1847·2657·2876 부근). QST 를 추가해야 문서 간 [[QST-…]] 이동이 된다.
5. (선택) 그래프 타입 집합 `gtypes` — 기본 표시 여부 판단은 designer 에 맡긴다.
   질문 노드가 그래프를 시끄럽게 만들면 기본 off 가 낫다.

참고: 설계 근거 DOC-20260826-011(질문=사건 vs 지식=규칙, 미답은 파생), 실제 문서 예
QST-20260826-001-62x6.


## Notes

### 2026-08-26T19:40:02+09:00 subagent (agent: sub:architect:a8da0646, by sjpark1)

결정 1 — 질문 문서는 훅이 사전에 만든다(즉석 인터랙션 제외 + 20자 이상), 답은 Stop 훅이 `last_qst` 폴백으로 자동 부착한다: 답변자의 재량에 맡기는 경로가 이번 사고에서 이미 한 번 무너졌고, `last_req`를 건드리지 않으므로 request 턴 동작은 불변이다.
결정 2 — QST는 사건(시점 고정·개정 없음), knowledge는 규칙(개정됨)이며 "답이 다음 작업을 구속하는가"만으로 갈라 QST→DOC 한 방향으로만 승격하고 규칙 전문은 복사하지 않는다; 미답은 별도 status가 아니라 `answer` 노트 유무에서 파생한다(진실을 둘로 만들면 "답했는데 미답"이 생기고 또 하나의 규율을 사람에게 요구한다).
구현 — `/home/sjpark1/section9/bin/s9`(TYPES·id 정규식·`_answered`/카탈로그 `answered`·`--unanswered`), `/home/sjpark1/section9/bin/s9-audit-prompt`(`is_durable_question`·QST 생성·포인터), `/home/sjpark1/section9/bin/s9-audit-response`(질문 턴 답 캡처), `/home/sjpark1/section9/tests/test_question_type.py`(15 OK) + `/home/sjpark1/section9/docs/01-document-format.md`; 근거 DOC-20260826-011-62x6, 도그푸딩 QST-20260826-001-62x6·DOC-20260826-013-62x6, 화면은 REQ-20260826-019-62x6(designer)로 분리하고 REQ-20260826-017-62x6은 review로 전이했다.

### 2026-08-26T19:48:37+09:00 response (by sjpark1)

화면 노출을 web/index.html 에 넣고 검증했다. 요구 5개 전부 + 계약 테스트 10건.

### 1. Docs 타입바

`TYPE_ORDER = ["request", "knowledge", "question", "session", "project"]` — question 을 **session 앞**에 뒀다. 순서 규칙이 "사용 빈도"인데 session 은 목록에서 자리만 지키는 축(`GRP_LIMIT_SESSION=5` 로 기본 노출까지 낮춰 둔 축)이고 질문은 열어보려고 만든 타입이다. 목록 그룹 맵·헤더 타입 필터 select 에도 같이 넣었다.

### 2. 타입색

`--t-question` 을 `--t-request` 가 정의된 **테마 6벌 전부**(paper 기본 · system-dark · carbon · phosphor · mist · graphite)에 넣었다. 요구는 4벌이었지만 한 벌이라도 빠지면 그 테마에서만 질문 노드가 무색이 된다 — 테스트가 `--t-request` 개수와 `--t-question` 개수의 동수를 강제한다(V5). 색은 장미(#9d174d 계열, 다크는 #e07a9f 계열): 파랑(request)·청록(knowledge)·호박(session)과 색상환에서 가장 멀다. **면에는 쓰지 않았다** — 글자색과 점 하나뿐이고, 세로 띠·색면 하이라이트는 없다.

### 3. 미답 표시

ADR 결정 3 그대로 **파생**으로 읽는다. 판정 함수는 `isAnswered(r, body)` 하나다:

- 뷰어는 본문을 들고 있으니 원본(`answer`/`response` 라벨 노트)에서 **직접** 읽는다. 정규식은 s9 의 `_answered()` 와 같은 것(`^### \S+ (?:answer|response)\b`)이고, V7 이 그 이음매를 붙잡는다 — 실제 `s9 note --label answer` 가 만든 본문을 index.html 에서 뽑아낸 정규식으로 다시 매치한다. 여기가 어긋나면 답을 달아도 화면은 영원히 미답이다.
- 목록은 본문이 없으니 카탈로그 `answered` 를 쓴다.

**3상이다: 답함 · 미답 · 모름.** 카탈로그에 `answered` 필드가 없는 경우(구 카탈로그, rebuild 전)를 미답이라 단정하지 않는다 — 그러면 답이 붙은 문서를 목록은 미답, 뷰어는 답함으로 읽어 같은 화면에서 두 진실이 생긴다. 이 타입이 없애려던 상태가 정확히 그것이다.

표기는 상태 배지를 재사용하되 **말을 바꿨다**: 질문의 `status` 는 published 고정이라 그 단어를 그대로 내면 사용자에게 아무 것도 말해주지 않는다. 그래서 질문 행·뷰어는 `미답`/`답함` 만 낸다. 색도 상태색이 아니라 "답이 남았는가"에서 온다 — 미답이 타입색으로 도드라지고 답한 질문은 `--faint` 로 물러난다. 훑을 때 눈에 걸려야 하는 쪽이 미답이다. 미답 문서를 열면 붙이는 명령까지 한 줄로 준다(`s9 note <id> '…' --label answer`).

### 4. id 링크 인식

요구는 "정규식 3곳에 QST 추가"였는데, **3곳을 상수 하나로 모았다**: `DOC_ID_PREFIX = "REQ|DOC|SES|QST"` + `DOC_ID_TAIL` 에서 인라인 링크화·`[[위키링크]]`·관계 필드 판정용 정규식 셋을 파생시킨다. 리터럴로 두면 다음 타입에서 또 한 곳이 빠지고, 그때 증상은 "질문 문서만 본문에서 클릭이 안 된다"는 조용한 갈라짐이다. V6 이 흩어진 리터럴(`REQ|DOC|SES)`)이 다시 생기는 것을 막는다.

### 5. 그래프 (선택 판단 → 기본 ON)

`GRAPH_TYPES` 배열 하나가 범례와 색 해석을 함께 소유하게 모으고(전에는 같은 리터럴이 두 곳), 기본 표시 집합 `gtypes` 에도 question 을 넣었다. **기본 off 를 고르지 않은 이유**: 질문이 그래프에 있어야 하는 이유가 곧 이 타입의 존재 이유다 — "이 제약이 어디서 나왔나"가 질문 노드에서 요청으로 이어지는 선으로 보인다. 시끄러워질 물량도 아니다(하루 몇 장, session 48건과 달리 기본 off 로 눌러둘 이유가 없다). 시끄러우면 범례에서 한 번 눌러 끈다.

### 검증

`python3 tests/ question` → **25 OK** (staff 의 bin/ 층 15건 + 이 작업의 화면 계약 10건). 전체 스위트 506건 중 실패 1건은 `test_link_integrity::test_serve_auto_repairs` — staff 의 감시자 코드가 `cmd_serve` 앞 2500자 창 밖으로 `link_audit(fix=True)` 를 밀어낸 것으로, 이 변경과 무관하고 bin/ 은 이 세션 봉투 밖이라 두었다.

회귀 하나를 고쳤다: `tests/test_ux_agents.py::test_b4_session_lower_priority` 가 목록 그룹 맵을 리터럴(`groups = {request:[],knowledge:[],session:[]}`)로 통째로 박고 있어 타입이 하나 늘자 깨졌다. 고정할 성질만 남겼다 — "session 그룹이 있고, 맨 뒤다".

캡처(무인 세션이라 F5 후 직접 확인 권장):

- `scratchpad/qst-final-docs.png` — Docs 뷰어. 타입바 `QUESTION 1`, 상태 행이 `● 답함`(장미 점), `published` 라는 단어는 안 보인다, RELATES 에 REQ-20260825-096-62x6·REQ-017 링크.
- `scratchpad/qst-unanswered.png` — 같은 문서의 미답 상태(답 노트 붙이기 전). `● 미답` + 답 붙이는 명령.
- `scratchpad/qst-graph.png` — 그래프 범례에 QUESTION 이 켜진 상태로 잡힌다(SESSION 은 꺼짐).
- `scratchpad/qst-board.png` — 회귀. Board 는 request 전용이라 질문이 섞이지 않는다.

### 확인 못 한 것 (한계)

**Docs 목록 행의 미답/답함 칩은 캡처로 못 봤다.** 헤드리스 캡처에서 Docs 목록 높이(`--docsh`)가 240px 하한으로 눌려 request 그룹 3행에서 잘린다 — 창 높이를 2600px 로 올려도 같다. 이 변경과 무관한 별개 현상이고(같은 URL·같은 skin 으로 09:35 에 찍힌 기존 캡처는 630px 로 정상, 그 사이 web/index.html 의 `sizeDocs` 는 한 줄도 안 바뀌었다), 실제 브라우저에서 재현되는지는 확인이 필요하다. 그 자리는 계약 테스트로 대신 잡아 뒀다(V10: 목록 행이 상태어를 그대로 찍지 않고 `statusLabel(r)` 을 거치고, 행 색이 `isAnswered(r)` 분기를 탄다). **REQ-20260826-004-62x6(Docs 목록 스크롤/높이)의 재발 가능성이라 별건으로 볼 필요가 있다.**

### 병행 작업 사고

이 요청과 REQ-20260826-017-62x6 에 두 세션이 동시에 붙었다(라이브 cb49b2cd · 무인 d3d60fdc). 그 과정에서 **내가 `tests/test_question_type.py` 를 덮어써 staff 의 15건을 날렸다.** 알아차린 즉시 상대 세션에 알렸고 `tests/test_question_qst.py` 로 복원됐다(25건 동시 통과 확인). 설계는 먼저 나온 DOC-20260826-011-62x6 을 정본으로 삼고 내가 같은 시각 쓴 중복 knowledge(DOC-20260826-012)는 걷어냈다. 재발 방지는 REQ-20260826-021-62x6 에서 다룬다.

### 2026-08-26T22:35:38+09:00 response (by sjpark1)

### 반려에 대한 답: 정상이 아니다 — 화면이 아니라 **수집 경로**가 반만 열려 있다

먼저 세어 봤다. 세션 로그에 갇혀 있는 비-request 발화가 **154줄**이다.

| 입구 | 코드 | 상태 | 세션 로그 |
|---|---|---|---|
| 터미널 프롬프트 | `bin/s9-audit-prompt` | 열림 (오늘 REQ-20260826-017-62x6 로 뚫렸다) | `question:` **74줄** |
| 대시보드 채팅 | `bin/s9` 의 `chat_audit()` | **닫힘** | `chat:` **80줄** |

`chat_audit()` 은 분류가 `request` 가 아니면 거기서 판단을 멈추고 `s9 log "chat: …"` 한 줄만 남긴다 — 질문이든 파편이든 똑같이. 프롬프트 훅이 쓰는 문서화 판정(`is_durable_question`)을 채팅 경로는 **아예 부르지 않는다.** 사용자는 주로 대시보드로 말하므로, 대시보드로 물은 질문은 한 장도 문서가 되지 않았다.

프롬프트 쪽도 오늘 뚫렸을 뿐이라 소급이 없다. 오늘 그 경로를 탄 질문은 2건이고 그 이전 154건은 60자로 잘린 채 세션 로그에 있다(답은 어디에도 문서로 없다).

**화면은 정상이었다** — 카탈로그에 1건이니 1건을 보였다. 그래서 이 반려의 수리 지점은 `bin/` 이고, 이 세션의 봉투 밖이다. 손대지 않고 아래 셋으로 나눠 처리했다.

---

### 1. 결함을 실증으로 못 박았다 (tests/ — 봉투 안)

`tests/test_question_intake.py` (신규 4건):

- **I1** — 대시보드로 들어온 '남을 질문'이 문서가 되는가. `chat_audit()` 을 직접 불러 재현한다. 지금은 안 된다 → `@unittest.expectedFailure` 로 뒀다. 스위트를 빨갛게 만들지 않으면서 결함을 명시하고, **채팅 경로가 열리는 순간 `unexpected success` 로 스스로 알린다.** 그때 데코레이터만 떼면 그대로 회귀 테스트가 된다.
- **I2** — 채팅 request 는 지금처럼 REQ 가 된다 (수리가 이걸 깨면 안 된다).
- **I3** — 짧은 확인 발화("이거 맞아?")는 아무 문서도 만들지 않는다 (질문 입구를 열다 채팅 카드가 잡음으로 뒤덮이는 것을 막는 경계).
- **I4** — 두 입구가 같은 자를 써야 한다. 채팅 경로는 이미 훅 모듈을 로드해 `classify` 를 쓰고 있고, **같은 모듈에 `is_durable_question` 이 있다** — 판정을 공유할 자리가 이미 만들어져 있다는 뜻이다.

### 2. 수리 제안 (bin/ — 봉투 밖, 적용하지 않음)

`bin/s9` 의 `chat_audit()` 에서 비-request 분기가 지금은 이렇다:

```python
if _chat_classifier(t) != "request":
    subprocess.run([me, "log", f"chat: {first[:60]}"], ...)
    return None
```

제안: 로그만 남기고 끝내지 말고 **질문이면 프롬프트 훅과 같은 기준으로 QST 를 만든다.** 판정 함수는 이미 로드한 그 모듈에 있다 — `_chat_classifier` 를 잡을 때 `mod.is_durable_question` 도 함께 잡아 두면 새 의존이 없다.

```python
cls = _chat_classifier(t)
if cls != "request":
    subprocess.run([me, "log", f"chat: {first[:60]}"], ...)
    if cls == "question" and _chat_durable(t):        # 훅과 같은 자
        title = first[:QUESTION_TITLE_MAX] + ("…" if len(first) > QUESTION_TITLE_MAX else "")
        out = subprocess.run([me, "new", "question", "--title", title,
                              "--tag", "auto-audit", "--user", sender or "dashboard"],
                             input=t, capture_output=True, text=True, env=env, timeout=15)
        # 채팅은 Stop 훅이 없다 — 답 부착 경로는 아래 주의 참고
    return None
```

**주의 두 가지.**

1. **답 부착 경로가 프롬프트 턴과 다르다.** 프롬프트 경로는 `bind last_qst` → Stop 훅이 답을 붙인다. 채팅은 서버가 만들고 답하는 주체는 수신 세션이라 Stop 훅이 이 QST 를 모른다. 그대로 두면 **채팅 질문이 전부 미답으로 쌓인다.** 수신함 줄(`inbox-<sid8>.jsonl`)에 `req` 를 싣듯 `qst` 를 함께 실어 보내고, 세션 훅이 그 값을 `last_qst` 로 바인딩하면 기존 Stop 훅 경로가 그대로 재사용된다 — 새 부착 로직이 필요 없다.
2. **`--user sender`** 를 넘겨야 한다(채팅 REQ 와 같은 규칙). 안 넘기면 서버 계정으로 기록된다.

### 3. 과거 154건 소급 이관 — 판단이 필요해 확인 포인트로 올린다

자동으로 하지 않았다. 결정이 갈리는 지점이 둘 있다.

- **원문이 잘려 있다.** 세션 로그의 `question:`/`chat:` 줄은 앞 60자뿐이다. 원문 전체와 그때의 답은 `streams/*.jsonl`(44개 세션, 132MB)에 있으므로 **복구는 가능하지만 도구가 필요하다**(`s9 backfill-questions` 류). 그건 `bin/` 이다.
- **잘린 질문 + 미답으로 154장을 만들면 잡음이다.** 답이 실제로는 있었는데(리드가 답했다) 문서로만 없는 것이라, 전부 미답으로 세우면 미답 목록의 신호가 죽는다. 이 타입의 값어치가 미답 목록에 있으므로 그건 자해다.

그래서 두 안 중 하나를 골라 달라 — (A) 도구를 만들어 streams 에서 원문·답까지 복구해 제대로 이관, (B) 소급은 포기하고 오늘부터 쌓이는 것만 관리. 둘 다 `bin/` 작업이라 이 세션이 정할 수 없다.

---

### 화면에서 고친 것 (web/ — 봉투 안)

반려를 계기로 화면에 실제 결함이 하나 드러났다. **총량만 보이고 "그중 몇 장이 답이 없나"가 안 보였다.** 이 타입을 만든 이유가 미답을 드러내는 것인데, 타입바는 `QUESTION 2` 라고만 말했다.

- 타입바의 question 항목에 미답 수를 병기한다 — `QUESTION 2 미답 1`. 총량은 회색으로 물러나 있고 미답만 타입색 잉크로 선다(면·띠 아님, 글자색과 무게만). **0이면 붙이지 않는다** — 없는 신호가 자리를 차지하면 있는 신호가 안 읽힌다.
- 처음엔 `2 미답 1` 로 숫자를 붙여 놨다가 `2`와 `1`이 한 덩어리로 읽혀 단어를 앞으로 옮겼다(`미답 1`). 숫자 사이에 글자가 들어가야 경계가 생긴다.

**구현 중 실제 버그 하나를 잡았다.** `isAnswered(r)` 가 타입을 보지 않아, 카탈로그가 비질문 행에도 실어 보내는 `answered: ""` 를 `!!""` = `false` 로 읽었다 — 그대로 세면 **요청 269건이 전부 미답으로 집계된다.** 판정 함수 첫 줄에 타입 가드를 넣고 V12 로 고정했다.

### 검증

- `python3 tests/ question` → **31 OK** (expected failure 1 = I1, 위의 결함). 내역: staff 의 bin/ 층 15 + 화면 계약 12 + 수집 경로 4.
- 새 계약 테스트: V11(타입바 미답 수 — 판정이 목록·뷰어와 같은 함수를 쓰는지, 0이면 안 그리는지, 색이 면이 아닌지), V12(비질문에 이 축이 없는지).
- 캡처: `scratchpad/qst-unans-bar2.png` (미답 1 — 타입바에 장미색으로 섬 · 뷰어 `● 미답` + 답 붙이는 명령), `scratchpad/qst-answered-bar.png` (답을 붙이자 `QUESTION 2` 로 미답 숫자가 사라짐 · 뷰어 `● 답함`).

### 도그푸딩

이번 반려 질문 자체를 문서로 남겼다 — **QST-20260826-003-62x6** "질문을 많이 한거같은데 정작 질문은 하나밖에 등록이 안되어있다. 정상인가?" 위 원인 규명을 `answer` 노트로 붙였고 이 요청과 `relates` 로 이었다. 질문이 **판정 화면의 반려 노트로 들어왔다는 것 자체가** 입구가 반만 열려 있다는 증거다 — 물을 자리가 없어서 반려 노트를 쓴 것이다.

## History
- 2026-08-26T19:37:23+09:00 created by sjpark1 (status: open)
- 2026-08-26T19:47:24+09:00 status: open -> in-progress (by sjpark1) — 무인 워커(d3d60fdc)가 착수 — 이 화면 작업은 REQ-017 병행 중 이미 구현·검증돼 있다
- 2026-08-26T19:48:49+09:00 status: in-progress -> review (by sjpark1) — 확인 포인트 (F5 후 Docs 탭): (1) 타입바 순서 — question 을 session 앞에 뒀다. 사용 빈도 기준 규칙에 맞는지, 아니면 knowledge 앞이 나은지 판단이 필요하다. (2) 미답 표기 — 질문 행·뷰어에서 published 라는 단어를 없애고 미답/답함만 낸다. 색은 미답이 장미색으로 도드라지고 답한 질문은 흐려진다(면 아님, 글자·점만). QST-20260826-001-62x6 이 답함, 미답 모습은 scratchpad/qst-unanswered.png. (3) 그래프 기본 ON 으로 정했다 — 질문 노드가 시끄러우면 기본 off 로 돌린다(범례에서 한 번 누르면 꺼진다). (4) 확인 못 한 것: Docs 목록 행의 미답 칩. 헤드리스 캡처에서 목록 높이가 240px 하한으로 눌려 잘린다 — 실제 브라우저에서 목록이 정상 높이인지, 질문 행에 미답/답함이 뜨는지 봐 달라. 이 변경과 무관한 현상으로 보이나 사실이면 REQ-20260826-004-62x6 재발이라 별건이 필요하다. (5) 사고 보고 — 병행 작업으로 staff 의 tests/test_question_type.py 15건을 내가 덮어썼다. 복원 완료(test_question_qst.py, 25건 동시 통과)했으나 경위는 노트에 남겼다.
- 2026-08-26T22:23:20+09:00 status: review -> in-progress (by sjpark1) — 반려: 질문을 많이 한거같은데 정작 질문은 하나밖에 등록이 안되어있다. 정상인가? [via dashboard]
- 2026-08-26T22:37:32+09:00 status: in-progress -> review (by sjpark1) — 먼저 물으신 것에 답한다 — 정상이 아니다. 질문이 문서로 들어오는 입구가 둘인데 하나가 막혀 있었다. 터미널에 직접 친 질문은 오늘부터 들어오고(그래서 2건), 대시보드 채팅으로 물은 질문은 한 장도 들어오지 않는다. 채팅을 받는 코드가 "이건 작업 요청이 아니네" 하고 판단을 거기서 멈춰버려서, 질문인지 아닌지를 아예 보지 않는다. 세어 보니 지금까지 세션 기록에만 남고 문서가 안 된 말이 154줄이다(터미널 74, 채팅 80). 화면 자체는 멀쩡했다 — 창고에 1건이 있으니 1건을 보여준 것이다. 막힌 곳은 bin/ 안이고 이 무인 세션은 거기를 못 고치게 되어 있어, 고칠 자리와 방법을 노트에 코드 수준으로 적어 두고 결함을 테스트로 못 박아 뒀다(tests/test_question_intake.py — 채팅 질문이 문서가 되는 순간 테스트가 스스로 알린다). 판단이 필요한 것 둘: (1) 과거 154줄을 소급해서 문서로 옮길 것인가. 원문은 60자로 잘려 있고 그때의 답은 세션 기록 원본(streams, 132MB)에 있어 꺼내려면 도구를 하나 만들어야 한다. 안 만들고 잘린 채로 옮기면 "답 없는 질문" 154장이 생기는데, 실제로는 답을 했던 것들이라 미답 목록이 거짓으로 가득 찬다 — 그럼 미답 표시가 쓸모없어진다. (A) 도구 만들어 제대로 옮긴다 (B) 소급은 접고 오늘부터 쌓이는 것만 본다. (2) 채팅 입구를 여는 작업을 어느 요청이 가져갈지. 이 요청은 화면 몫이라 별건이 맞다고 본다. 화면에서 확인할 것: Docs 탭 맨 윗줄에 QUESTION 옆으로 "미답 N"이 장미색으로 뜬다(답이 없는 질문 수). 지금은 둘 다 답이 달려 있어 안 보이는 게 정상이고, 미답이 있을 때 뜨는 모습은 scratchpad/qst-unans-bar2.png. 한 가지 못 본 것: QUESTION 글자를 눌러 그 타입만 보고 있을 때(선택 상태) 미답 숫자가 배경에 안 묻히는지 — 그 상태는 캡처로 만들 수 없어 눈으로 봐 달라(terminal·calm 스킨). 이번 질문 자체도 문서로 남겼다: QST-20260826-003-62x6.
- 2026-08-26T22:46:18+09:00 status: review -> done (by sjpark1) — 대시보드 승인 [via dashboard]
