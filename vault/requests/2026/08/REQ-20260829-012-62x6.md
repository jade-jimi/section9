---
id: REQ-20260829-012-62x6
type: request
title: 문서 선택이 내 뜻과 다르다
summary: Docs 탭에 들어가면 전에 보던 문서가 되살아나 목록 맨 위를 차지하고, 선택을 풀 길이 없다
goal: Docs 목록 맨 위의 줄이 무엇인지 이름으로 말하고, 선택을 풀 수 있다
status: done
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: b0cce452
sessions: ["b0cce452", "06e0cca1"]
agents: ["sub:subagent:abaf1a86", "sub:subagent:ae89d570"]
contributions: [{"actor": "sub:subagent:abaf1a86", "item": "Appending response note to REQ-20260829-012-62x6", "started": "2026-08-29T10:28:49+09:00", "ended": "2026-08-29T10:28:49+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/b0cce452-7815-44d0-8bb7-ce1e7b28c6da/subagents/agent-abaf1a86871ac4675.jsonl"}, {"actor": "sub:subagent:ae89d570", "item": "Reading REQ-20260829-012-62x6 rejection capture", "started": "2026-08-29T11:04:31+09:00", "ended": "2026-08-29T11:04:31+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/b0cce452-7815-44d0-8bb7-ce1e7b28c6da/subagents/agent-ae89d570becbd851c.jsonl"}]
project: section9
relates: ["REQ-20260828-009-62x6", "REQ-20260829-015-62x6"]
relates_why: {"REQ-20260828-009-62x6": "지금 보는 문서를 목록 맨 위에 못 박은 것이 009 의 결정인데, 그 못이 의도치 않게 되살아난 선택까지 맨 위에 세운다", "REQ-20260829-015-62x6": "사용자가 판정 자리에 그림을 못 붙여 이어 말하기로 우회하다 012 의 결함을 만났다 — 하나가 없어서 다른 하나가 드러났다"}
tags: ["auto-audit", "assets", "docs", "dashboard", "attached", "png", "image"]
created: 2026-08-29T10:10:18+09:00
status_since: 2026-08-29T11:42:13+09:00
updated: 2026-08-29T11:44:53+09:00
priority: 50
---

## Original

[선행 대화 — 직전 미문서화 메시지]
- (09:56) 002는 내가 이어서 말한게 있는데 캐치업이 안되는건가? 블럭상태라 그런건가?
- (10:04) 조용히 처리했다기엔 내가 지금 의문을 품을때까지 아무 반영이 안된거면 방치됐던거 아니야?

docs 탭에서 문서 목록이나 문서 선택이 내 의도와 다른 것 같다. 그리고 선택한 문서를 취소할 수도 있게 해줘.
[Image: assets/REQ-20260829-012-62x6/20260829T100941-image.png]

## Notes

### 2026-08-29T10:28:45+09:00 response (by nicehugepark)

**못 박기 자체는 옳다. 틀린 것은 그 줄이 자기가 뭔지 말하지 않은 것이다.**

캡처의 그 줄은 REQUEST 머리글 **위**에 이름 없이 홀로 서 있다. 이 목록의 덩어리는 전부 머리글을 갖는데(REQUEST·ARTICLE·KNOWLEDGE…) 이 줄만 없었다. 그러니 "목록의 1번"으로 읽힌다 — 고른 적 없는 문서가 목록 맨 앞에 와 있는 셈이니 "내 의도와 다르다"는 말씀이 정확하다.

**되살아나는 경로**: `selectedDoc` 은 탭을 옮겨도 지워지지 않고, 주소(`#docs/<문서>`)에도 실린다. 그래서 Docs 로 돌아오거나 새로고침하면 지난번에 읽던 문서가 그대로 열린다.

**경계는 이렇게 그었다** (REQ-20260828-009-62x6 의 결정은 그대로 둔다):

> 못 박기는 "내가 고른 것"의 표시가 아니라 **"지금 오른쪽에 열려 있는 것"**의 표시다.

그래서 되살아난 선택도 여전히 맨 위에 선다. 오른쪽 판이 실제로 그 문서를 그리고 있는데 목록이 그것을 숨기면 두 판이 서로 다른 말을 하고, 그게 009 가 고친 바로 그 결함이다. 대신 그 줄이 스스로를 밝히고, 그 자리에서 풀린다.

**고친 것 둘:**

① **머리글을 준다.** 새 컴포넌트를 만들지 않고 이 목록이 이미 쓰는 머리글(.grp — 붙박이·mono·아래 헤어라인) 그대로다. 다만 자간은 라틴 대문자용 `.1em` 을 벗겼다 — 한글에 그대로 걸면 "지 금  보 는  문 서" 로 벌어져 낱말이 흩어진다. 잉크는 한 급 올렸다: 이 줄은 구획 표시가 아니라 라벨이다.

② **푸는 손잡이를 그 머리글 안에 둔다.** 자리를 목록 쪽으로 정한 이유는, 푸는 대상 바로 위이고 목록의 상태(무엇이 맨 위에 서 있는가)를 목록에서 되돌리는 것이 자연스럽기 때문이다. 오른쪽 문서 판에 두면 그 판이 이미 쓰는 문서 행위(승인·반려·이어 말하기) 옆에 서서 "문서를 닫는다"가 아니라 "문서를 취소한다"로 읽힌다. 모양은 색면·테두리 없는 mono 글자 + hover 밑줄(`+ N개 더 보기`·`전문 보기` 와 같은 어휘). 잉크는 머리글에서 물려받게 했다 — 값을 박으면 머리글을 잉크로 뒤집는 스킨(grid)에서 글자가 배경에 묻힌다.

**풀면**: 머리글과 그 줄이 사라지고 목록은 REQUEST 부터 시작한다. 오른쪽은 처음 들어왔을 때와 같은 한 줄(`← 문서를 선택하세요`)로 돌아간다. 주소에서도 문서가 빠져 **새로고침해도 되살아나지 않는다** — 그것이 "취소"의 뜻이다.

**Esc 는 쓰지 않기로 했다.** 이 화면에서 Esc 는 **떠 있는 것**을 닫는 키다 — 호버카드·터미널 미리보기·대화상자·팔레트가 전부 그 층이고 전역 핸들러 하나가 그 층만 걷는다. 문서 선택은 떠 있는 것이 아니라 **판의 상태**이고, 오른쪽 화면 전체가 그것이다. 같은 키에 두 층을 얹으면 호버카드를 닫으려고 누른 Esc 가 읽던 문서까지 되돌리기 없이 지운다. 대신 손잡이가 진짜 button 이라 Tab 으로 닿고 Enter 로 눌리며, 키보드로 닿은 자리에 포커스 링이 보인다.

**눈으로 확인한 것** (첨부 순서대로): ① ledger — `지금 보는 문서 … 닫기` 머리글 아래에 그 줄이 서고, 그 아래부터 REQUEST ② calm 밝은 톤 ③ grid 밝은 톤 — 머리글을 잉크로 뒤집는 스킨에서도 `닫기` 가 묻히지 않는다 ④ 닫은 뒤의 화면 — 목록은 REQUEST 부터, 오른쪽은 빈 상태. terminal·glass 에서도 같은 모양을 확인했다.

계약은 tests/test_doc_pin_release.py 에 잠갔다(10건). REQ-20260828-009-62x6 의 시험 한 줄(`if (pin) list += rowHTML(pin)` 글자 그대로)은 "못 박은 줄이 그룹보다 먼저 선다"는 뜻으로 고쳐 썼다 — 결정은 그대로고 그 앞에 머리글이 한 줄 늘었을 뿐이다. 전체 1798건 중 실패는 리드가 진행 중인 `BARE_ASSET_RE`(REQ-008) 하나뿐.

**미검증 하나**: 헤드리스에 손이 없어 `닫기` 를 실제로 눌러 보지는 못했다. 눌린 뒤의 화면은 같은 상태(`#docs`)를 직접 열어 확인했고(④), 클릭 배선은 이 화면의 다른 손잡이(`이어 말하기`)와 같은 자리·같은 규칙으로 넣었다.

**곁가지 한 건**(이번 건과 무관, 앞서 REQ-009 노트에도 적었다): "서버가 옛 코드로 돌고 있습니다" 띠가 뜬 동안 문서 판의 높이가 그 띠만큼 짧게 잘린다 — 판 높이를 재는 시점이 띠보다 빨라서다. 위 캡처들에서도 아래쪽이 잘려 있다.

[Image: assets/REQ-20260829-012-62x6/d1-ledger.png]
[Image: assets/REQ-20260829-012-62x6/d2-calm.png]
[Image: assets/REQ-20260829-012-62x6/d3-grid.png]
[Image: assets/REQ-20260829-012-62x6/d6-released.png]

### 2026-08-29T10:28:49+09:00 subagent (agent: sub:subagent:abaf1a86, by nicehugepark)

Appending response note to REQ-20260829-012-62x6

### 2026-08-29T11:00:50+09:00 ask (by nicehugepark)

반려인데, 반려할 때 이미지를 붙여넣을 수 있는 기능을 만들어달라고 했었지만, 어느샌가 유실된 것 같다. 그래서 이어 말하기 기능으로 이미지와 함께 반려하는데, 지금 보고 있는 문서를 바꿨음에도 남아있다. 물론, 이 요청의 구체적인 내용은 그 내용이 아니었긴 한데... 같이 봐줘.
[Image: assets/REQ-20260829-012-62x6/20260829T105939-image.png]

### 2026-08-29T11:04:31+09:00 subagent (agent: sub:subagent:ae89d570, by nicehugepark)

Reading REQ-20260829-012-62x6 rejection capture

### 2026-08-29T11:14:22+09:00 response (by nicehugepark)

**고쳤다. 원인은 문서를 갈아탈 때 목록을 다시 그리지 않던 것이다.**

Docs 탭에 **이미 있는 상태에서** 다른 문서를 누르면 화면이 목록은 손대지 않고 오른쪽 판만 갈아 끼웠다. 그런데 못 박은 줄은 목록을 다시 그릴 때만 세워진다 — 그래서 왼쪽 「지금 보는 문서」 자리에는 지난 문서가 남고, 새 문서는 그룹 안에서 표식(•)만 달고 앉아 있었다. 주소로 곧장 연 화면은 늘 옳았기 때문에 지난번 확인에서 안 걸렸다.

이 결함은 제가 머리글에 이름을 붙이기 전부터 있던 것이다. 이름이 결함을 만든 게 아니라 **드러냈다** — 전에는 그 줄이 이름이 없어서 "지난 문서가 첫 줄에 남아 있다"는 사실이 그냥 목록의 일부로 보였다.

**고친 것**: 문서를 여는 길 하나(`docOpen`)가 어느 탭에서 눌렸든 목록과 오른쪽 판을 함께 다시 그린다. 다만 **사람이 누른 길과 15초 배경 갱신 길은 그대로 갈라 뒀다** — 사람이 고른 문서는 위에서부터 새로 펴고, 배경 갱신은 읽던 스크롤과 열어 둔 스트림을 지킨다. 목록 순서를 얼리는 조건은 손대지 않았다(15초마다 순서가 뒤섞이면 REQ-20260828-009-62x6 가 고친 결함이 돌아온다).

**눈으로 확인한 것**: 이 결함은 **누른 순간에만** 나서 헤드리스로는 볼 길이 없었다 — 그래서 이 저장소가 쓰는 진단 파라미터(`?dlg=`·`?pick=`)와 같은 길로 `?swap=<문서>` 를 하나 냈다. "012 를 열어 둔 채 014 로 갈아탄" 그 상황을 그대로 세워 캡처했다. 첨부 ① ledger ② calm — 둘 다 「지금 보는 문서」에 **014** 가 앉아 있고 오른쪽 판도 014 이며, 아래 REQUEST 그룹에 014 가 겹쳐 나오지 않는다.

계약은 tests/test_doc_pin_release.py 에 더 잠갔다(14건) — "문서를 바꾸면 목록도 다시 그린다", "사람이 누른 길과 폴링 길이 갈라져 있다", "얼음 조건은 그대로다", "갈아탄 화면을 손 없이 세워 볼 수 있다". doc 관련 회귀 통과.

[Image: assets/REQ-20260829-012-62x6/f-ledger-swap.png]
[Image: assets/REQ-20260829-012-62x6/f-calm-swap.png]

### 2026-08-29T11:29:50+09:00 response (by nicehugepark)

**반려가 맞다. 못은 박혔는데 따라가지 않았다.**

캡처의 오른쪽 판은 REQ-20260829-014-62x6 를 그리고 있는데 목록 맨 위 `지금 보는 문서` 아래에는 REQ-20260829-012-62x6 가 서 있다. 1차에서 그 줄에 이름과 손잡이는 주었지만, **줄이 갈아 끼워지는 순간을 빠뜨렸다.**

**왜 남았나**: 못 박은 줄은 `selectedDoc` 에서 파생되는데, 문서를 여는 길(`loadDoc`)은 목록 행의 선택 표시만 옮기고 목록을 **다시 짓지는 않았다**. 그래서 오른쪽 판만 새 문서로 갈리고 맨 위 줄은 아까 것이 남았다 — 009 가 고친 "두 판이 서로 다른 말을 한다"가 그대로 되돌아온 자리다.

**왜 저절로 안 나았나**: 15초 배경 갱신은 이것을 못 고친다. 그 갱신은 **문서 목록이 실제로 변했을 때만** 다시 그린다(refreshCatalog). 그래서 아무도 문서를 고치지 않는 동안 어긋남은 잠깐이 아니라 **영영** 남았다. 캡처가 바로 그 상태다.

**고친 자리는 하나다.** 문서를 여는 길목(`loadDoc`) — 링크·목록 행·보드 카드·그래프 점·터미널 미리보기가 전부 여기를 지난다(`docOpen` 이 그렇게 모아 두었다). 목록이 서 있는데 그 못이 지금 여는 문서와 다르면 목록부터 다시 짓고, 오른쪽 판은 그 재렌더가 부르는 같은 함수가 읽는다. 손잡이를 새로 만들지 않았으니 앞으로 문서를 여는 길이 하나 더 생겨도 이 규칙은 저절로 걸린다.

무한 재렌더는 두 곳에서 막았다: 다시 지은 목록의 못은 맞으므로 이 갈래를 두 번 타지 않고, 카탈로그에 없어 못이 아예 안 서는 문서도 기대값이 같아(둘 다 없음) 조용히 지나간다.

**눈으로 확인한 것** (첨부 두 장). 헤드리스에는 손이 없어 이 결함은 정지 화면으로 안 찍힌다 — 어긋남은 문서를 **바꾼 순간**에만 생긴다. 그래서 화면에 `?pinswap=<문서id>` 를 두었다: 목록이 선 뒤 그 줄을 **실제로 누른다**(사람이 누르는 그 길 — 위임 핸들러 → docOpen → loadDoc). `?pick=`·`?dlg=`·`?peek=` 가 낸 선례와 같은 종류다.
① `pin-before.png` — 첫 번째 요청을 열어 둔 화면. `지금 보는 문서` 아래에 그것이 서고, 아래 REQUEST 그룹에는 나머지 셋.
② `pin-after.png` — 그 목록에서 두 번째 요청을 누른 뒤. 못도 두 번째, 오른쪽 판도 두 번째, 첫 번째는 REQUEST 그룹으로 돌아갔다. 화면이 멎거나 목록이 두 번 그려지는 일도 없었다.

**워크트리에서 화면을 어떻게 봤나**: 본 저장소의 대시보드(9909)는 본 저장소의 `web/index.html` 을 낸다 — 워크트리 사본은 거기 안 실린다. 그래서 이 저장소가 이미 쓰는 격리 방식(`S9_ROOT`=임시 + portpool)으로 **이 사본의** web/ 를 내는 서버를 잠깐 세워 찍었다(`tests/test_terminal_api.py` 와 같은 짜임). 이름을 대야 도는 시험이다 — 브라우저를 띄우므로 전체 스위트에 매번 태우지 않는다: `python3 tests/ pin_swap_live`.

**계약**: `tests/test_doc_pin_release.py` 15건(5건 추가 — 못이 어긋나면 목록을 다시 짓는가 · 어긋날 때만 다시 짓는가(무한 재렌더 방지) · 기대값이 renderDocs 와 같은 규칙인가 · 무엇이 못 박혔나를 머리글 아래 줄에서 재는가 · 헤드리스가 정말 문서를 바꿀 수 있는가). 전체 1811건 중 실패 6건은 전부 이 사본의 사정이다 — `test_short_ref` 는 워크트리 스냅샷의 문서 말뭉치가 비어 0으로 나누고, `test_instance_init` 은 bin/(내 범위 밖)이다. 내 수정과 무관하고 수정 전에도 같다.

**함께 지적하신 그림 붙이기**: 판정 대화상자에 이미지를 붙일 수 없는 건은 REQ-20260829-015-62x6 으로 따로 서 있다(이 문서와 relates). 여기서는 손대지 않았다 — 이 요청의 반려 사유 본체는 못이 안 따라가는 것이고, 둘을 한 커밋에 섞으면 어느 쪽이 고쳐졌는지 판정이 갈린다.

[Image: assets/REQ-20260829-012-62x6/pin-before.png]
[Image: assets/REQ-20260829-012-62x6/pin-after.png]

### 2026-08-29T11:30:55+09:00 commit (by nicehugepark)

커밋 52d1164 — 워커 작업 자동 보존: REQ-20260829-012-62x6

## History
- 2026-08-29T10:10:18+09:00 created by nicehugepark (status: open)
- 2026-08-29T10:14:05+09:00 status: open -> in-progress (by nicehugepark) — Docs 목록의 못 박기 범위와 선택 해제 — designer 가 본다. ① 되살아난 선택도 맨 위를 차지해야 하는가(REQ-20260828-009-62x6 의 결정은 유지하고 경계만 긋는다) ② 선택을 푸는 자리·모양·Esc 충돌 확인.
- 2026-08-29T10:37:57+09:00 status: in-progress -> review (by nicehugepark) — Docs 목록 맨 위에 이름 없이 서 있던 줄에 「지금 보는 문서」 머리글과 「닫기」를 붙였습니다. F5 후 Docs 탭에서 문서를 하나 열고 닫기를 눌러 보세요. 목록에서 그 줄이 사라지고 새로고침해도 되살아나지 않으면 승인, 여전히 되살아나면 반려입니다.
- 2026-08-29T11:01:06+09:00 status: review -> in-progress (by nicehugepark) — 채팅 반려: 보고 있는 문서를 바꿨는데도 집힌 문서 표시가 남는다. 함께: 반려할 때 이미지를 붙일 수 있던 기능이 유실된 것 같다.
- 2026-08-29T11:29:55+09:00 status: in-progress -> review (by nicehugepark) — 목록 맨 위 「지금 보는 문서」가 이제 방금 연 문서를 따라갑니다. 고친 것이 서버에 올라간 뒤 F5 하고, Docs 탭에서 문서를 하나 연 다음 목록의 다른 문서를 눌러 보세요. 맨 위 줄과 오른쪽 판이 같은 문서를 가리키면 승인, 아까 읽던 문서가 맨 위에 그대로 남아 있으면 반려입니다.
- 2026-08-29T11:42:13+09:00 status: review -> done (by nicehugepark) — 승인 (메모 없음) [via dashboard]
