---
id: REQ-20260829-004-62x6
type: request
title: 포트 고갈과 doctor 의 눈
summary: 재부팅 뒤 또 의심되는 포트 고갈의 뿌리와, 진단하지 못한 s9 doctor 의 점검 범위
status: in-progress
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: a53ebee3
project: section9
children: ["REQ-20260829-005-62x6"]
relates_why: {}
tags: ["auto-audit", "session"]
created: 2026-08-29T00:54:53+09:00
status_since: 2026-08-29T00:58:42+09:00
updated: 2026-08-29T01:15:49+09:00
priority: 50
---

## Original

그리고 조금 전에 컴퓨터를 재시작했는데 아무래도 또 포트고갈이 된것같다. 아직도 근본적인 해결이 안된것같은데 해결하고, s9 doctor 명령어를 실행하면 s9가 실행하기 위한 시스템 전반적인 영역을 다 점검해줘. 고갈이 의심될 때 실행했었는데 딱히 진단이 되지 않았어.

## Notes

### 2026-08-29T01:00:27+09:00 response (by nicehugepark)

먼저 사실 확인: 지금 고갈은 없다.

s9 doctor 실측(01:06) — 신규 포트 공개 지연 0.25초, 윈도우 동적 포트 35/16384(0%), 고아 테스트 서버 0, 헤드리스 브라우저 0. 커널 임시 포트 범위 32768~60999 에 TIME-WAIT 3개뿐. 어느 지표로도 고갈이 아니다.

그러면 재부팅 직후 왜 고갈처럼 보였나 — REQ-003 과 같은 뿌리다. 00:46:15 부트에서 systemd-tmpfiles-setup 이 sysinit 을 73초 붙잡아 그동안 아무것도 리슨하지 못했다. s9 serve 는 00:47:2x 에야 떴다. 그 74초 동안 대시보드는 안 열리고 새 서버는 안 뜨는데 이미 뜬 것은 멀쩡한 상태 — DOC-20260826-008-62x6 이 '고갈의 지문'이라고 못박은 그 비대칭과 겉모습이 똑같다. 지문이 겹치는 다른 원인이 하나 더 있었던 것이다.

그래서 진짜 결함은 doctor 쪽이다: 사용자가 의심할 때 실행했더니 '정상' 한 줄만 나오고, 실제로 시스템을 못 쓰게 만든 원인(부트 지연)은 doctor 의 시야 밖이었다. doctor 가 보는 것은 지금 포트·캡처·서버 프로세스뿐이다.

### TDD 시나리오 — s9 doctor 의 시야를 시스템 전반으로

판정 함수는 순수 함수로 두고(입력 dict → 등급), 수집기는 따로 둔다. 실행: python3 tests/ doctor_system

- [ ] D1. 부트 — boot_verdict: systemd 미사용이면 ok(지연 없음). systemd 사용 시 userspace 10초 이상 warn, 30초 이상 critical. 최근 부트에 'Timed out waiting for user session' 흔적이 있으면 등급과 무관하게 critical 이고 사유 문구에 사용자 세션 실패가 나온다.
- [ ] D2. /tmp 위생 — tmp_verdict: 최상위 엔트리 2,000 초과 warn, 20,000 초과 critical. 디렉터리 아이노드가 1MB 를 넘으면 '과거 누적' 을 문구로 알린다(ext4 디렉터리는 줄지 않는다).
- [ ] D3. 디스크 — disk_verdict: 사용률 90% warn, 95% critical. / 와 S9_ROOT 둘 다 본다.
- [ ] D4. 서버 — serve_verdict: 리슨만 하고 응답이 없으면 critical, 응답이 2초 넘으면 warn, 정상이면 ok. '포트가 열려 있다'와 '실제로 답한다'를 구분한다.
- [ ] D5. 설치 — hooks_verdict: ~/.claude/settings.json 의 s9 훅과 git pre-commit 이 빠지면 warn 이고 어느 것이 빠졌는지 이름을 댄다. 워크트리(.git 이 파일)에서도 git 에게 물어 판정한다(REQ-001 과 같은 함정).
- [ ] D6. 도구 — deps_verdict: python3·git 이 없으면 critical, 캡처용 브라우저·powershell.exe 가 없으면 warn(그 기능만 죽는다).
- [ ] D7. 종합 — 네트워크가 정상이어도 시스템 점검 중 하나라도 ok 가 아니면 마지막 판정 줄이 '정상' 이라고 말하지 않고 가장 나쁜 항목을 지목한다. critical 이 하나라도 있으면 종료코드 1.
- [ ] D8. --json 에 system 배열이 포함되고 각 항목이 key·level·line 을 갖는다.
- [ ] 회귀: 기존 port_exhaustion·port_pool·port_guard·shot_hygiene 그대로 통과.

### 2026-08-29T01:15:49+09:00 response (by nicehugepark)

구현 — doctor 의 시야를 포트 밖으로 넓혔다 (bin/s9-doctor).

점검 축 일곱, 각각 ✓/△/✗ 한 줄:
- 부트: systemd 사용 여부와 userspace 소요, 사용자 세션이 실제로 섰는지(/run/user). 10초 warn · 30초 critical, 세션 실패는 무조건 critical.
- /tmp: 최상위 엔트리 수와 디렉터리 아이노드 크기. 2,000 warn · 20,000 critical, 1MB 넘는 디렉터리는 '과거 누적'으로 알린다.
- 디스크: / 와 S9_ROOT, 90% warn · 95% critical.
- s9 serve: '리슨한다' 와 '실제로 답한다' 를 가른다. 리슨만 하고 응답 없으면 critical(가장 헷갈리는 상태), 안 떠 있으면 warn(일부러 내린 것일 수 있다).
- 저장소: core.bare 와 작업 트리 사용 가능 여부 — 이번에 실제로 뒤집혀 커밋이 막혔다(REQ-005).
- 훅: claude 훅 8개와 git pre-commit. 훅 자리는 경로를 짓지 않고 git 에게 묻는다.
- 도구: python3·git(없으면 critical) / chrome·powershell.exe(없으면 warn — 그 기능만 죽는다).

가장 중요한 변화는 마지막 판정 줄이다. 전에는 포트만 보고 '정상' 한 줄이었다. 이제 네트워크가 정상이어도 다른 축에 걸린 게 있으면 '정상'이라 말하지 않고 나쁜 순서로 지목한다. critical 이 하나라도 있으면 종료코드 1 이라, 무인 호출도 넘기지 않는다.

실측(01:12) — 오늘 사고를 그대로 짚는다:
  ✗ 부트: systemd 사용자 세션이 서지 못했다 (userspace 77.0초)
  ✗ 저장소: core.bare=true 로 표시돼 있다 — status·add·commit 이 전부 거부된다
  네트워크는 정상이다(공개 지연 0.25초). 걸린 것은 다른 데 있다:

시험: tests/test_doctor_system.py 28개(판정은 순수 함수로 분리해 실제 시스템 상태에 기대지 않는다). 회귀 port_exhaustion·port_pool·port_guard·shot_hygiene·port_owner_wording·port_warn_names·port_warn_repeat 모두 통과.

## History
- 2026-08-29T00:54:53+09:00 created by nicehugepark (status: open)
- 2026-08-29T00:58:42+09:00 status: open -> in-progress (by nicehugepark) — 고갈 재발 원인과 doctor 점검 범위 확장
