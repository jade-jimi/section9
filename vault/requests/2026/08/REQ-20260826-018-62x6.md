---
id: REQ-20260826-018-62x6
type: request
title: 감시 기록 화면 노출
summary: serve 감시자가 남기는 사망 사유·재기동 이력을 대시보드에서 볼 수 있게
status: done
size: S
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: 8e60e4af
sessions: ["cb49b2cd", "8e60e4af"]
agents: ["sub:backend-developer:a9029e77", "sub:designer:afb99a15"]
contributions: [{"actor": "sub:backend-developer:a9029e77", "item": "첨부 추출 본문 보존", "started": "2026-08-26T23:58:45+09:00", "ended": "2026-08-27T00:15:42+09:00", "result": "stalled", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/8e60e4af-6e99-4b92-857b-fe3e651a913b/tasks/a9029e7741910035c.output", "reason": "197초째 진전 없음 (기준 180초)"}, {"actor": "sub:designer:afb99a15", "item": "감시 기록 화면 노출", "started": "2026-08-27T00:03:28+09:00", "ended": "2026-08-27T00:03:28+09:00", "result": "running", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/8e60e4af-6e99-4b92-857b-fe3e651a913b/tasks/afb99a15d71b7f033.output"}]
project: section9
parent: REQ-20260825-096-62x6
tags: ["dashboard"]
created: 2026-08-26T19:34:56+09:00
status_since: 2026-08-27T00:51:29+09:00
updated: 2026-08-27T01:03:45+09:00
priority: 50
---

## Original

감시자(REQ-20260825-096)가 state/serve-guard.log 에 JSONL 로 남기는 기록 — 사망 사유(시그널/종료코드), 직전 출력 tail, 재기동 횟수·백오프 — 을 지금은 파일을 열어야 볼 수 있다. 사람이 "대시보드가 왜 죽었나"를 물었을 때 대시보드 안에서 답이 보여야 한다.

요구: 서버 상태(감시 중/재기동 n회/마지막 사망 사유·시각)를 대시보드 어딘가에 표시. 자동 복구가 성공하면 REQ-032 의 재연결 표시가 자연스럽게 걷히는지도 이 작업에서 함께 확인한다.

구현 범위는 web/ 과 읽기 전용 API 다. 감시자 쪽 계약(파일 위치·필드: ts/port/event/rc/reason/ran_sec/fails/retry_in/tail)은 이미 고정돼 있다.

## Notes

### 2026-08-27T00:08:41+09:00 decision (by nicehugepark)

**읽기 전용 API 하나 — `GET /api/serveguard`.** 화면(web/index.html)은 이 명세대로 이미 구현돼 있고, 서버측은 리드가 `bin/s9` 에 넣는다(REQ-20260826-020-62x6 과 같은 파일이라 designer 는 손대지 않았다).

### 설계 원칙 두 가지

**① 서버는 사실만, 화면이 정책을.** `/api/serveinfo`(코드 낡음)는 `stale` 이라는 **판정**을 서버가 준다 — 지문 비교는 사실이기 때문이다. 반면 "이 사건을 사람에게 보여줄 만큼 최근인가"는 표시 정책이라 화면이 판단한다. 그래서 이 API 는 `state: "recovered"` 같은 필드를 주지 않는다. 대신 사실(감시자가 돌고 있는가 · 언제 죽었는가 · 몇 번 되살렸는가)만 준다.

**② 응답에 서버 시각(`now`)을 담는다.** 신선도 계산의 기준이 브라우저 시계면, 시계가 틀어진 기기에서 "6시간 전 사건"이 "방금"으로 보인다. 로그의 `ts` 는 전부 서버 시각이니 기준도 서버가 줘야 한다. 화면은 `now - ts` 로만 경과를 잰다.

### 요청

```
GET /api/serveguard
```
쿼리 없음. 인증/가시성 필터 없음(대시보드는 127.0.0.1 전용, 이미 `/api/serveinfo` 와 같은 등급).

### 응답 (항상 200, `application/json`)

```json
{
  "port": 9909,
  "now": "2026-08-27T00:20:11+09:00",
  "guard": "watching",
  "guard_pid": 545741,
  "guard_since": "2026-08-26T19:34:36+09:00",
  "restarts": 2,
  "last_death": {
    "ts": "2026-08-27T00:14:32+09:00",
    "event": "died",
    "msg": "9909 서버가 signal SIGTERM 로 죽었다 (47s 생존, 연속 1회) — 12s 뒤 재기동",
    "rc": -15,
    "reason": "signal SIGTERM",
    "ran_sec": 47.0,
    "fails": 1,
    "retry_in": 12,
    "tail": ["Traceback (most recent call last):", "  ..."]
  },
  "events": [ /* 최신순, 최대 30건 — last_death 와 같은 모양 */ ]
}
```

| 필드 | 값 | 뜻 |
|---|---|---|
| `port` | int | 이 서버가 듣는 포트 (`s9_port()`) |
| `now` | ISO8601 | 응답을 만든 서버 시각 — 화면 신선도 계산의 유일한 기준 |
| `guard` | `"watching"` / `"gave-up"` / `"none"` | 자동 복구가 지금 유효한가 |
| `guard_pid` | int / null | `_guard_alive(port)` 의 pid. watching 아니면 null |
| `guard_since` | ISO8601 / null | 마지막 `start` 이벤트의 `ts` |
| `restarts` | int | `guard_since` 이후 `died` 건수 = 자동으로 되살린 횟수 |
| `last_death` | object / null | 마지막 `died`·`gave-up`·`spawn-error` 이벤트 (tail 포함) |
| `events` | array | 최신순 최대 30건. 로그 줄의 필드를 **그대로** 통과 (모르는 키도 버리지 않는다) |

`guard` 판정 규칙 (셋 다 이미 있는 함수로 나온다):
- `_guard_alive(port)` 가 pid 를 주면 → `"watching"`
- 아니고, 마지막 `start` **이후에** `gave-up` 이벤트가 있으면 → `"gave-up"`
- 그 외 → `"none"`

### 실패·빈 상태

- `state/serve-guard.log` 가 없다 → `{"port":…, "now":…, "guard":"none", "guard_pid":null, "guard_since":null, "restarts":0, "last_death":null, "events":[]}` (200). 화면은 이 응답에 **아무것도 그리지 않는다**.
- JSON 파싱 실패한 줄 → 조용히 건너뛴다. 한 줄이 깨져도 나머지 기록은 살아야 한다.
- 로그가 커졌을 때: 파일 끝에서 뒤로 읽어 최근 30건만 파싱하는 편이 좋다(현재 크기론 전체 읽기도 무방).

### 크기

`tail` 은 기록 시점에 이미 `GUARD_TAIL_LINES`(20줄)로 잘려 있다. 30건 × 20줄이면 최악 ~60KB — 폴 주기가 20초라 감당된다. 더 줄여야 하면 `events` 건수를 깎되 `last_death.tail` 은 반드시 남겨라. "왜 죽었나"의 답이 거기 있다.

### 화면이 이 계약에 거는 것 (tests/test_serve_guard_ui.py)

- 화면은 `/api/serveguard` **하나만** 부른다. 로그 파일을 직접 읽지 않고, 사망 여부를 자체 추측하지 않는다.
- 응답이 없거나(404·무응답) 형태가 다르면 **직전 판정을 그대로 두고 조용히 넘어간다** — 서버 재기동 중의 무응답이 "사건 없음"으로 오독되면 안 된다. 그래서 이 API 가 아직 없는 지금도 화면은 아무 흔적을 남기지 않는다(회귀 안전).
- 신선도 판정에 `Date.now()` 를 쓰지 않고 `now` 필드를 쓴다.

### 2026-08-27T00:48:29+09:00 response (by nicehugepark)

**대시보드가 왜 죽었는지를 대시보드 안에서 답한다.** `web/index.html` 만 고쳤다 — 서버측 API 는 명세만 정하고 넘겼다(`bin/s9` 를 REQ-20260826-020-62x6 이 잡고 있어 손대지 않았다. 명세는 이 문서의 decision 노트에).

### 설계 판단 — 이 정보는 어디에 있어야 하는가

**자리: 코드 낡음 알림과 같은 헤더 3행.** 대안 셋을 놓고 골랐다.

| 안 | 장점 | 버린 이유 |
|---|---|---|
| **헤더 3행(채택)** | 이미 있는 어휘를 재사용, 학습 비용 0. "이 서버 자신에 대한 사실"이 한 자리에 모인다 | — |
| 터미널 탭의 연결 표시 옆 | 재연결 표시와 인과가 붙는다 | 그 탭에 들어가야만 보인다. "왜 죽었나"는 어느 탭에서든 나오는 질문이다. 게다가 터미널의 `stale` 은 **세션** 무응답이라 어휘가 충돌한다 |
| 별도 "서버" 탭·패널 | 자리가 넉넉하다 | 평소 빈 탭이고, 무슨 일이 있어도 스스로 나타나지 않는다 — 요구의 핵심을 못 맞춘다 |

**어휘 충돌은 주체로 갈랐다.** 위 알림이 말하는 재기동은 **사람이 하는 일**이고, 여기가 말하는 자동 복구는 **감시자가 하는 일**이다. 터미널의 `stale`(세션 무응답)은 또 다른 사실이라 그 단어는 쓰지 않았다. 셋이 같은 말을 쓰면 어느 것도 못 믿게 된다.

**나타나고 걷히는 규칙이 이 화면의 본체다.** 상시 표시하면 그 줄은 곧 배경이 되어 진짜 사고 때도 안 읽힌다. 그래서
- 사건이 없으면 **자리표시자조차 남기지 않는다**
- 되살아난 사건은 **6시간**, 자동 복구가 멈춘 사실은 **24시간** 동안만 말한다. 잠잠해지면 스스로 걷힌다 — 걷히는 것까지가 설계다
- 접으면 한 줄로 줄고, **다음 사건이 나면 다시 펼쳐진다**(접힘이 사건 시각에 묶여 있다)

### 상태 셋

| 상태 | 화면 |
|---|---|
| 조용함 | 아무 흔적 없음 |
| 되살아남 | `↻ 서버가 스스로 복구했습니다` · `15:39 에 멈췄다가 12초 뒤 다시 떴습니다 (signal SIGTERM). 지금까지 9번 되살렸습니다.` · [기록 보기] [접기] |
| 자동 복구가 멈춤 | `▲ 자동 복구가 멈췄습니다` · `서버가 10번 잇따라 자리잡지 못해 감시자가 물러났습니다 (exit 1). 지금 서버가 죽으면 다시 뜨지 않습니다.` · [`s9 serve --supervise`(복사)] [기록 보기] [접기] |

되살아난 사건은 **설명**이고 복구가 멈춘 것은 **경고**다 — 후자만 사람의 행동을 요구하므로 색·마크·명령 제공이 갈린다. 색 없이도 마크(↻/▲)와 문장으로 읽힌다.

**명령은 대신 실행하지 않는다.** 감시자를 다시 세울지는 사람의 판단이라 붙여넣을 문자열까지가 화면의 몫이다(코드 낡음 알림과 같은 원칙).

**기록 패널**은 [기록 보기]로 헤더 안에서 열린다. 다른 자리로 보내면 방금 읽은 알림과의 연결이 끊긴다 — 대신 높이를 묶고(44vh·최대 320px) 자체 스크롤을 준다. 한 행 = 시각 · 종류 · 사유/생존시간/잇따른 횟수/되살리기 대기, 그리고 **[직전 출력 N줄]** 로 죽기 직전 출력을 펼친다. 종류는 사용자 말로 옮긴다(`died`→멈춤, `gave-up`→감시 포기 …) — 원래 값을 그대로 두면 기록이 아니라 암호다. 8건 + `+ N개 더 보기`(밀도 규칙), 빈 기록도 한 줄로 말한다.

### 조용히 무너질 자리 셋을 계약으로 박았다

1. **무응답을 사건 없음으로 읽지 않는다.** 서버가 죽어 있는 동안 이 폴은 실패하는데, **그 순간이 이 알림이 존재하는 이유**다. 실패를 "아무 일 없음"으로 읽으면 알림은 정확히 필요할 때 침묵한다 → 응답이 없으면 직전 판정을 두고 다음 폴을 기다린다.
2. **신선도는 서버 시각으로 잰다.** 기록의 시각이 전부 서버 것이므로 기준(`now`)도 서버가 줘야 한다. `Date.now()` 를 쓰면 시계가 틀어진 기기에서 6시간 전 사고가 "방금"이 된다.
3. **화면이 자체 추측하지 않는다.** 판단 근거는 읽기 전용 API 하나이고 로그 파일을 직접 읽지 않는다.

### 함께 확인한 것 — 자동 복구 성공 시 재연결 표시가 걷히는가 (원문 요구)

**걷힌다.** 경로를 따라가 확인했다: `termTargetLoop` 의 5초 틱이 `ccFetch` 실패 시 `termStatus(T, null, "offline")` 로 `서버 재연결 중` 을 띄우고, 서버가 살아나면 다음 틱의 정상 응답이 `lv.textContent` 를 `live` 로, `lv.className` 을 `ccok` 으로, **`lv.title` 을 빈 문자열로** 덮는다. 대상 줄(`#cc-tgt`)의 `· 서버 대기` 도 `innerHTML` 로 덮인다. 남는 잔재가 없다 — 즉 별도 소거 코드 없이 정상 경로가 전부 되쓴다.

다만 이건 **코드 경로 추적**이지 실단절 재현이 아니다. 실단절은 헤드리스 캡처로 못 만든다(그래서 이 저장소에 `?conn=retry|down|poll` 훅이 이미 있다). 붙어 있는 서버를 죽여 확인하는 것은 다른 에이전트가 같은 시각 `bin/s9` 를 잡고 있어 하지 않았다.

### 검증

- `python3 tests/ guard_ui` → **20개 통과** (TDD: 19개 빨강 확인 후 구현)
- 전체 스위트 `python3 tests/` → **707개 통과** (실패 0, skip 1)
- 실브라우저 캡처 — 사고는 재현할 수 없으므로 상태 고정 훅 `?guard=recovered|attention|none` 을 뒀다(`?nosse`·`?mpanel`·`?conn` 선례):
  - `state/shots/018-states.png` — 세 상태 + 기록 패널 + 직전 출력 펼침 (light)
  - `state/shots/018-dark.png` — carbon. 여기서 `?guard=none` 이 **아무 흔적도 남기지 않는 것**을 눈으로 확인했다
  - `state/shots/018-tone-paper.png` · `018-tone-rest.png` (mist·graphite·phosphor) — **tone 6종 전부**
- 하니스: `web/verify-guard.html`

### 캡처에서 잡아 고친 결함 넷

테스트는 다 통과한 뒤에 캡처에서 나온 것들이다.
1. **한국어에 자간을 벌렸다** — 기록 헤더와 버튼을 mono + `letter-spacing` 으로 잡았더니 글자가 흩어져 보였다. 그 어휘는 시각·포트 번호 같은 라틴/숫자 메타의 것이다(REQ-20260826-039-62x6 에서 확정된 규칙). 본문체로 되돌리고 포트 번호에만 남겼다.
2. **조사가 어긋났다** — `${사유} 로` 는 값이 `signal SIGTERM` 이냐 `exit 1` 이냐에 따라 로/으로가 반드시 한쪽에서 틀린다. 사유를 **문장 끝 괄호**로 옮겨 조사를 없앴다.
3. `잇따라 1번째` — 첫 실패에 "1번째"는 정보가 아니다. 2회 이상일 때만 말한다.
4. **상태 고정 훅이 고정되지 않았다** — 폴마다 표본을 새로 만들어 시각이 바뀌는 바람에 펼쳐 둔 기록이 20초마다 접혔다. 표본을 캐시했다.

### 남긴 것

- **서버측 API 는 아직 없다.** 지금 화면은 `/api/serveguard` 가 404 라 아무것도 그리지 않는다 — 회귀 위험이 없는 상태로 대기 중이다. 리드가 decision 노트의 명세대로 넣으면 그대로 살아난다.
- 하니스 `web/verify-guard.html` 를 남겼다(`verify-*.html` 선례). 불필요하면 지워도 기능에 영향 없다.

### 2026-08-27T01:03:45+09:00 response (by nicehugepark)

**서버측을 designer 의 명세 그대로 넣었다** — `GET /api/serveguard` (`bin/s9`).

명세를 정한 쪽과 구현한 쪽이 다르고 파일도 달랐다. 그게 이번엔 우연이 아니라 **의도된 분리**였다: 같은 시각 다른 에이전트가 `bin/s9` 를 잡고 있어서, designer 에게 "서버는 손대지 말고 명세만 남기라"고 봉투를 씌웠다. 오늘 사고 다섯이 전부 같은 파일에 둘이 붙은 데서 났으니, 이번엔 애초에 겹치지 않게 잘랐다.

원칙 둘을 그대로 지켰다.

**① 서버는 사실만, 화면이 정책을.** `recovered`·`attention` 같은 판정 필드를 주지 않는다. "이 사건을 사람에게 보여줄 만큼 최근인가"는 표시 정책이고, 그게 서버와 화면 두 곳에 있으면 둘이 갈렸을 때 어느 쪽도 못 믿는다. 테스트 G3 이 그 필드들이 다시 생기는 것을 막는다.

**② 응답에 서버 시각을 담는다.** 신선도 기준이 브라우저 시계면 시계가 틀어진 기기에서 "6시간 전 사건"이 "방금"으로 보인다.

구현에서 판단한 것 둘:
- **되살린 횟수는 마지막 감시 시작 이후만 센다.** 어제 죽은 횟수를 오늘 감시자의 성적으로 보고하면 숫자가 거짓말을 한다.
- **깨진 줄은 조용히 건너뛴다.** 사고 기록은 사고 중에 쓰이므로 마지막 줄이 반쯤 쓰이다 만 상태가 정상이다. 그 한 줄 때문에 "왜 죽었나"를 통째로 못 읽으면 안 된다.
- 모르는 키는 버리지 않고 통과시킨다. 서버가 아는 키만 골라 내보내면 기록에 새 근거가 늘어도 화면은 영영 못 본다.

**실물 확인**: 실제 로그로 불러 봤더니 00:22 에 내가 보낸 SIGTERM 과 감시자의 재기동이 그대로 나온다 — `guard: watching` · `restarts: 1` · `last_death.reason: "signal SIGTERM"` · tail 20줄.

**테스트** `tests/test_serve_guard_api.py` 10건. 이 파일은 사실상 **두 에이전트 사이의 계약서**다 — 화면은 이 모양을 전제로 이미 쓰여 있고, 서버가 모양을 어겨도 화면은 조용히 아무것도 안 그리도록 designer 가 만들어 뒀다(무응답을 '사건 없음'으로 오독하지 않기 위해). 그래서 **어겨도 아무도 모른다.** G10 이 라우트가 실제로 붙어 있는지까지 본다.

## History
- 2026-08-26T19:34:56+09:00 created by nicehugepark (status: open)
- 2026-08-26T23:07:56+09:00 status: open -> in-progress (by nicehugepark) — drag 이동 [via dashboard]
- 2026-08-27T00:49:03+09:00 status: in-progress -> review (by nicehugepark) — 대시보드가 왜 죽었는지를 이제 대시보드 안에서 볼 수 있게 만들었습니다. 다만 화면만 끝났고 서버 쪽 한 조각이 아직 남아 있습니다 (아래 참고). 무엇을 만들었나: 서버를 지켜보는 감시자가 죽은 서버를 되살릴 때마다 남기던 기록 — 언제 왜 멈췄는지, 몇 초 만에 되살렸는지, 죽기 직전에 무슨 말을 남겼는지 — 을 지금까지는 파일을 열어야 볼 수 있었습니다. 그것을 화면 위쪽에 띄웁니다. 핵심은 "언제 나타나고 언제 걷히는가"입니다. 평소에는 아무 흔적도 남기지 않습니다. 늘 떠 있는 경고 줄은 며칠 지나면 아무도 안 읽게 되고, 그러면 진짜 사고가 났을 때도 안 읽힙니다. 그래서 사고가 났을 때만 나타나고, 잠잠해지면 (되살아난 일은 6시간, 자동 복구가 아예 멈춘 일은 24시간) 스스로 사라집니다. 두 가지를 다른 말로 구분했습니다: - 서버가 죽었다가 스스로 살아났다 → 설명입니다. 화면이 잠깐 끊겼던 이유를 알려줄 뿐 하실 일은 없습니다. - 자동 복구 자체가 멈췄다 → 경고입니다. 다음에 서버가 죽으면 아무도 되살리지 않습니다. 이때만 붙여넣을 명령을 함께 드립니다. '기록 보기'를 누르면 그 아래로 지난 기록이 펼쳐집니다. 각 줄에서 '직전 출력'을 누르면 죽기 직전에 서버가 뱉은 말이 나옵니다 — "왜 죽었나"의 진짜 답이 거기 있습니다. 이웃한 알림과 말이 겹치지 않게 했습니다. 바로 위 "서버가 옛 코드로 돌고 있습니다"는 사람이 직접 하는 재기동을 말하고, 이 줄은 감시자가 알아서 하는 자동 복구를 말합니다. 터미널 화면의 stale 은 또 다른 뜻(세션이 응답 없음)이라 그 단어는 쓰지 않았습니다. 확인 절차 (F5 로 새로고침한 뒤 — 화면 코드가 바뀌었습니다): 1. 평소 화면을 봅니다. 서버가 죽은 적이 없다면 아무 줄도 뜨지 않아야 맞습니다. 이게 정상입니다. 2. 실제 사고 없이 모양을 보시려면 주소 끝에 붙여 보세요: - http://127.0.0.1:9909/?guard=recovered — "서버가 스스로 복구했습니다" - http://127.0.0.1:9909/?guard=attention — "자동 복구가 멈췄습니다" (명령 버튼이 함께 뜹니다) - http://127.0.0.1:9909/?guard=none — 아무것도 안 뜨는 것이 맞습니다 3. '기록 보기'를 눌러 지난 기록이 펼쳐지는지, 그 안에서 '직전 출력'을 눌러 죽기 직전 내용이 나오는지 봅니다. 4. '접기'를 누르면 한 줄로 줄어듭니다. 새 사고가 나면 다시 펼쳐집니다. 5. 여섯 가지 화면 색상에서 다 확인했습니다: state/shots/018-states.png · 018-dark.png · 018-tone-paper.png · 018-tone-rest.png 함께 봐 달라고 하신 것 — 자동 복구가 성공하면 터미널의 "서버 재연결 중" 표시가 저절로 걷히는가: 걷힙니다. 서버가 살아나면 5초 안에 오는 다음 응답이 그 자리를 정상 표시로 덮어씁니다. 남는 찌꺼기가 없습니다. 다만 이건 코드 경로를 따라가 확인한 것이고, 실제로 서버를 죽였다 살려서 눈으로 본 것은 아닙니다 — 지금 다른 작업이 같은 서버 파일을 잡고 있어 건드리지 않았습니다. 아직 남은 것 (리드가 이어받아야 합니다): 화면은 다 됐는데 그 화면에 자료를 넘겨줄 서버 쪽 창구(/api/serveguard)가 아직 없습니다. 그래서 지금은 실제 사고가 나도 화면이 조용합니다 — 위 2번의 주소 방식으로만 모양을 볼 수 있습니다. 창구의 정확한 규격은 이 문서의 앞선 노트에 적어 두었습니다.
- 2026-08-27T00:51:29+09:00 status: review -> done (by nicehugepark) — [via dashboard]
