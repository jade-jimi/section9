---
id: REQ-20260827-083-62x6
type: request
title: 줌아웃이 구석으로 쏠린다
summary: 그래프를 줌아웃하면 내용이 한쪽 구석에 남고 되돌릴 방법이 없다
goal: 줌아웃하면 그래프가 화면 안에 남고, 한 동작으로 전체 보기로 돌아올 수 있다
status: done
size: M
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: b8fda8f9
sessions: ["3da5b9de", "d7f21881", "39a365af", "b8fda8f9"]
agents: ["sub:subagent:ab4367e7", "sub:subagent:a9c24a80", "sub:subagent:a3304e04", "sub:subagent:a335c5ad", "sub:designer:a20a4928"]
contributions: [{"actor": "sub:subagent:ab4367e7", "item": "Appending response note to REQ-20260827-083-62x6", "started": "2026-08-28T13:00:37+09:00", "ended": "2026-08-28T13:00:37+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/subagents/agent-ab4367e743da1714b.jsonl"}, {"actor": "sub:subagent:a9c24a80", "item": "Transitioning REQ-20260827-083-62x6 to review", "started": "2026-08-28T13:01:09+09:00", "ended": "2026-08-28T13:01:09+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/subagents/agent-a9c24a80c4d9732f8.jsonl"}, {"actor": "sub:subagent:a3304e04", "item": "Appending TDD note to REQ-20260827-083-62x6", "started": "2026-08-28T13:21:47+09:00", "ended": "2026-08-28T13:21:47+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/subagents/agent-a3304e044d256c2b5.jsonl"}, {"actor": "sub:subagent:a335c5ad", "item": "Transitioning REQ-20260827-083-62x6 to review", "started": "2026-08-28T13:28:52+09:00", "ended": "2026-08-28T13:28:52+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/subagents/agent-a335c5ad2d751ffe1.jsonl"}, {"actor": "sub:designer:a20a4928", "item": "`[2026-08-28 13:47 KST - designer]`", "started": "2026-08-28T13:29:50+09:00", "ended": "2026-08-28T13:29:50+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/subagents/agent-a20a492838c78a7cc.jsonl"}, {"actor": "sub:designer:a20a4928", "item": "`[2026-08-28 13:49:12 KST - designer]`", "started": "2026-08-28T13:34:10+09:00", "ended": "2026-08-28T13:34:10+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/subagents/agent-a20a492838c78a7cc.jsonl"}]
project: section9
relates_why: {}
tags: ["access", "assets", "dashboard", "attached", "png", "image"]
created: 2026-08-27T23:46:55+09:00
status_since: 2026-08-28T13:47:08+09:00
updated: 2026-08-28T13:47:08+09:00
priority: 50
---

## Original

사용자(23:45): "줌아웃을 하면 항상 대각선 방향으로 고정이되고 쏠리게 되는데 마음에 들지 않는다. 옵시디언도 이런식인가?" (QST-20260827-036, 캡처 첨부)

**커서 기준 줌 자체는 맞게 짜여 있다.** wheel 핸들러(web/index.html)가 표준 공식을 쓴다:

    st.tf.x = mx - (mx - st.tf.x) * k2 / st.tf.k

문제는 그 다음이다. 커서 기준으로 줄이면 내용은 **커서 쪽으로 모인다** — 화면 가장자리에서 굴리면 그래프가 그 구석에 남는다. 그게 정상 동작인데, **되돌릴 방법이 없다.** 전체 보기로 맞추는 동작이 없어서 손으로 끌어 되찾아야 한다. 사용자가 "고정이 된다"고 느낀 이유다.

옵시디언도 커서 기준으로 줌한다. 다른 것은 (1) 노드가 중앙으로 모이는 힘이 늘 작동해 줌아웃하면 가운데 뭉친 덩어리가 보이고, (2) 전체 보기로 돌아가는 동작이 있다는 점이다.

## Notes

### 2026-08-27T23:56:59+09:00 tdd (by nicehugepark)

### TDD 시나리오 — 계약 8건 전부 통과

- [ ] ① 커서 기준 줌 공식은 **그대로 둔다**. 옵시디언도 그렇게 하고, 고칠 것은
      그 줄이 아니었다.
- [ ] ② 전체 보기 — 경계 상자를 재서 여백(56px)을 두고 화면에 꽉 채운다.
      줌 한계(0.25~4) 안에 머문다.
- [ ] ③ 두 길로 닿는다: 빈 곳 더블클릭(노드 위는 문서 열기와 겹치므로 제외) ·
      손잡이 버튼. 조작법은 **이미 있는 안내 줄**에 한 항목으로 붙였다.
- [ ] ④ 순간이동하지 않는다 — 240ms 감속 이동(모션 규약 안). `prefers-reduced-motion`
      이면 옮기지 않고 바로 놓는다. 휠을 굴리면 이동은 멈춘다.
- [ ] ⑤ 그래프가 화면보다 작아지면 커서 기준을 놓고 가운데로 물린다. 아직 화면을
      넘치는 동안에는 커서 기준 그대로다. 한 번에 끌지 않고 여유에 비례해 섞는다
      (0.15~0.65) — 톡 튀지 않게.
- [ ] ⑥ 손잡이는 **거의 나갔을 때만**(보이는 넓이 35% 미만) 뜬다. 물리가 도는
      동안 깜빡이지 않게 400ms 뜸을 들이고, 돌아오면 즉시 사라진다.
- [ ] ⑦ 색면·세로 띠·색 하드코딩 없음. 잉크와 지면 토큰만.
- [ ] ⑧ 진단 파라미터 `?goff`(구석에 남은 상태) · `?gfit`(복귀 결과) ·
      `?gzoom=N`(구석 커서로 N번 줌아웃). 줌아웃 진단은 합성 WheelEvent 로
      **실제 휠 핸들러를 그대로 태운다** — 로직만 읽고 넘기지 않기 위해서다.

실행: `python3 tests/ graph_fit` 8 OK.

### 2026-08-27T23:57:00+09:00 designer (by nicehugepark)

**커서 기준 줌은 손대지 않았다.** 옵시디언도 커서 기준으로 줌하고, 그 공식은 맞게 짜여 있었다. 사용자가 "고정된다"고 느낀 진짜 이유는 **되돌릴 방법이 없었기 때문**이다 — 가장자리에서 줌아웃하면 그래프가 그 구석에 남는데, 전체 보기가 없어 손으로 끌어 되찾아야 했다.

두 가지를 더했다.

**1. 전체 보기.** 노드 경계 상자를 재서 여백 56px 을 두고 화면에 꽉 채운다. 240ms 감속으로 옮긴다 — 순간이동하면 어디로 갔는지 사람이 못 따라간다. 움직임을 줄여 달라는 설정(prefers-reduced-motion)이면 옮기지 않고 바로 놓는다. 닿는 길은 둘이다: **빈 곳 더블클릭**(노드 위 더블클릭은 문서 열기와 겹치므로 제외)과 **손잡이 버튼**. 조작법은 이미 있는 안내 줄에 한 항목으로 붙였다 — 새 안내 문구를 만들지 않았다.

**2. 줌아웃이 화면을 비우지 않게.** 그래프 전체가 화면보다 작아지는 순간부터 커서 기준을 놓고 화면 가운데로 물린다. 여유가 클수록 세게(0.15~0.65 비율로 섞어) 당겨, 줌아웃할수록 구석이 아니라 가운데로 모인다. 아직 화면을 넘치는 동안에는 커서 기준 그대로다 — 확대해서 한 곳을 들여다보는 중에 화면이 제멋대로 움직이면 안 된다.

**손잡이는 갇혔을 때만 뜬다.** 보이는 넓이가 35% 아래로 떨어지고 그 상태가 400ms 이어질 때만 나오고, 돌아오면 즉시 사라진다. 뜸을 들이는 이유는 물리가 도는 동안 노드가 계속 움직여 문턱 하나만 두면 깜빡이기 때문이다 — 깜빡이는 표시는 읽히지 않고 고장으로 보인다. 터미널의 "맨 아래로" 손잡이와 같은 손이다(REQ-20260827-061-62x6): 되돌릴 방법이 없어 갇힌 상태에서만 나오는 되돌리기.

검증: 진단 파라미터 셋으로 헤드리스에서 직접 봤다. `?goff`(구석에 남은 상태 — 손잡이가 뜬다) → `?goff&gfit`(복귀 — 가운데로 차고 손잡이가 사라진다) → `?gzoom=12`(화면 왼쪽 위 구석에 커서를 두고 12번 줌아웃). 줌아웃 진단은 합성 WheelEvent 를 캔버스에 보내 **실제 휠 핸들러를 그대로 태운다** — 로직만 읽고 "맞을 것이다"로 넘기지 않기 위한 것이다. 그 캡처에서 그래프는 왼쪽 위 구석이 아니라 화면 안쪽에 모여 있다.

[Image: assets/REQ-20260827-083-62x6/g-off.png]
[Image: assets/REQ-20260827-083-62x6/g-fit.png]
[Image: assets/REQ-20260827-083-62x6/g-zoom.png]

### 2026-08-28T00:04:29+09:00 designer (by nicehugepark)

**리드의 진단 정정 요청에 대한 실측 결과 — 커서 기준 줌은 실제로 먹고 있었습니다.**

로직만 읽고 넘기지 않으려고, 합성 휠 이벤트를 캔버스에 보내 **실제 휠 핸들러를 그대로 태우고** 노드 무리의 화면 중심을 재는 진단을 만들었습니다(`?gzoom=N&gat=<커서 자리>`, `?graw` 는 이번에 넣은 가운데 물림을 꺼서 고치기 전 동작만 남깁니다).

캔버스 1015×555(가운데 500,277)에서 열 번 줌아웃(배율 1 → 0.312):

    커서 자리          고치기 전(?graw)        지금
    왼쪽 위 (24,24)    493,277 → 168,101      500,277 → 365,204
    오른쪽 아래        493,277 → 722,399      493,277 → 524,292

**고치기 전에도 무리는 커서 쪽으로 갔습니다** — 왼쪽 위에 커서를 두면 왼쪽 위로, 오른쪽 아래에 두면 오른쪽 아래로. 커서 기준 줌은 정상이었습니다.

확대(줌인)도 재 봤습니다: 커서를 오른쪽 아래에 두고 여섯 번 확대하면 무리 중심이 500,277 → 171,104 으로 커서 **반대쪽**으로 밀려납니다. 커서를 가운데 두면 500,277 → 569,315 로 거의 제자리입니다. 둘 다 커서 기준 줌의 정상 거동입니다.

**그러면 사용자가 본 "항상 대각선으로 고정"은 무엇인가.** 커서가 화면 정중앙에 있는 일은 거의 없으므로, 줌아웃할 때마다 무리는 매번 어느 구석으로든 쏠립니다. 되돌릴 방법이 없으니 그 상태가 굳고, 여러 번 반복하면 "늘 같은 쪽으로 고정된다"로 읽힙니다. 그래서 고칠 자리는 줌 공식이 아니라 **되돌리는 길**이라는 판단은 그대로 유효합니다.

지금 값(오른쪽 열)을 보면 열 번 줌아웃해도 무리가 화면 안쪽에 남습니다. 왼쪽 위에서 굴린 경우 365,204 로 완전한 정중앙은 아닌데, 가운데로 맞추는 기준이 노드 무리의 무게중심이 아니라 **경계 상자의 가운데**이고 그 상자에 멀리 떨어진 몇 노드가 들어 있기 때문입니다. 화면 안에 온전히 들어와 있으므로 의도한 대로입니다.

리드가 함께 짚은 것 중 하나는 사실입니다: 물리 루프가 노드를 월드 좌표 (W/2, H/2) 로 당깁니다. 다만 그 힘은 alpha 가 0.006 아래로 식으면 멎으므로 정착 후의 줌에는 관여하지 않습니다. 15초마다 도는 목록 갱신이 그래프를 다시 세워 물리를 재점화하지만, 노드 좌표와 화면 변환을 그대로 복원하므로 눈에 띄는 되돌림은 없습니다.

### 2026-08-28T00:35:37+09:00 conflict (by nicehugepark)

**web/index.html 이 00:31 에 통째로 망가졌다가 스냅샷으로 되살렸다.**

00:30:47 캡처까지는 정상이었는데, 00:31:01 에 파일이 **503MB · 1,004만 줄**이
됐다. 내용은 대시보드가 아니라 REQ-20260827-073-62x6(아티클 문서 종류) 쪽 JS 조각
(`DOC_SECTIONS` · `docSectionAt` · `docSection` · `docWithout`)이 수백만 번
되풀이된 것뿐이었다 — 대시보드 원본은 한 줄도 남지 않았다. 그 시각 내 편집은
없었다(내 마지막 저장 00:30:19). 다른 주체의 폭주한 쓰기(전체 치환 또는 루프)로
보인다.

`s9 snapshot --restore web/index.html --at 20260828T003046` 으로 되살렸다
(518,309 바이트 = 내 마지막 저장 직후 크기, 8,663줄). 되살린 판에는
REQ-083 재작업분과 그 이전 미커밋 작업이 모두 살아 있다. 다만 그
`DOC_SECTIONS`/`docSectionAt` 계열 함수는 없다 — 00:31 에 들어오려던 그 변경은
정상적으로 안착한 적이 없고(안착했다면 파일이 멀쩡했을 것이다) 이번 복구로
사라졌다. **REQ-073 작업자는 그 편집을 다시 넣어야 한다.**

같은 파일에 여럿이 동시에 붙어 있다. 자동 스냅샷이 30초마다 도는 덕에
잃은 것이 30초어치뿐이었다.

### 2026-08-28T00:37:07+09:00 response (by nicehugepark)

**직접 눌러 봤다. 안 됐다. 왜 안 됐는지 세 가지가 나왔고 셋 다 고쳤다.**

앞선 검증은 `gFit()` 을 직접 불러 결과만 찍은 것이라 이 결함을 잡을 수 없었다.
그래서 이번엔 **그 좌표에서 실제로 이벤트를 받는 요소를 찾아**(`elementFromPoint`)
사람이 누르는 순서 그대로(누름→뗌→클릭 ×2 → 더블클릭) 보내는 진단을 만들고,
잰 값을 화면에 얹어 캡처 한 장이 곧 증거가 되게 했다. 첫 결과:

    tf0  k0.550 1159,557
    tf1  k0.550 1159,557      ← 그대로. 아무 데도 안 갔다
    fit started: true          ← 부르기는 불렸는데
    frames 2 → 3               ← 3.3초 동안 화면을 세 장 그렸다

**㉠ 옮기는 일을 애니메이션 루프에만 맡겼다.** 화면을 다시 그리는 일은 늘
도는 것이 아니다 — 배경 탭, 전원 절약, 캡처용 브라우저에서는 몇 초에 한 장이다.
그러면 "전체 보기"는 예약만 되고 그래프는 구석에 그대로 남는다. 사용자에게는
정확히 "눌러도 아무 일이 없다"로 보인다. 이제 약속한 것을 부드러움이 아니라
도착으로 잡았다: 옮기는 시간(240ms)이 지났는데 아직 가는 중이면 그냥 놓는다.

**㉡ 두 번 누른 것을 브라우저 판정 하나에만 맡겼다.** 이 판은 끌기를 놓치지
않으려고 누르는 순간 포인터를 붙잡아 둔다. 그 상태에서 두 번째 클릭이 같은
짝으로 묶이지 않으면 더블클릭은 조용히 사라진다 — 남는 건 "두 번 눌러도
안 된다"뿐이다. 이제 우리가 직접 센다: 빈 곳에서, 420ms 안에, 7px 안에서,
끌지 않고 두 번. 브라우저가 제 판정을 주는 환경에서는 겹쳐 불리지 않게 막았다.

**㉢ 맞출 자리를 화면이 아니라 딴 자로 쟀다.** 이 그래프는 깊이감을 주려고
노드마다 이동량을 다르게(0.72~1.24배) 그린다. 그런데 "어디로 맞출까"는 그
가중을 뺀 좌표로 계산하고 있었다 — 재는 자와 그리는 자가 달랐다. 이제 전체
보기·손잡이 판정·줌아웃 세 곳 모두 **실제로 그려지는 자리**로 잰다.

덤으로 손잡이 기준도 바꿨다. 상자 넓이로 재고 있어서, 확대만 해도(상자가 화면보다
커지므로) 손잡이가 떠 있었다 — 기본 화면에서도 떠 있었다. 그건 갇힌 게 아니라
들여다보는 중이다. 이제 **화면에 남은 노드의 비율**로 재고, 넷 중 셋 넘게
사라졌을 때만 뜬다.

**검증 — 헤드리스에서 실제로 눌러 본 결과 (프레임은 두세 장만 돌았다).**

    구석에 몰린 상태  보이는 노드 0%      손잡이 떠 있음
    빈 곳 더블클릭    0% → 98%           손잡이 사라짐
    손잡이 클릭       0% → 99%           손잡이 사라짐
    왼쪽 위에서 12번 줌아웃  무리 중심 567,297 → 453,244 (화면 가운데 567,297)
                            보이는 노드 94% → 100%
    기본 화면          손잡이 뜨지 않음

`python3 tests/ graph_fit` 12건 OK (계약 8건 + 이번 반려로 늘어난 3건).

[Image: assets/REQ-20260827-083-62x6/g2-stuck.png]
[Image: assets/REQ-20260827-083-62x6/g2-dblclick.png]
[Image: assets/REQ-20260827-083-62x6/g2-handle.png]
[Image: assets/REQ-20260827-083-62x6/g2-zoomout.png]
[Image: assets/REQ-20260827-083-62x6/g2-default.png]

### 2026-08-28T00:47:55+09:00 response (by nicehugepark)

**전체 스위트 결과** — `python3 tests/` 1,241건 중 실패 4건. 전부 이 요청과
무관한 다른 화면의 기존 실패다(이 작업 착수 전 실행에서도 같은 4건이었다):

- `test_doc_target` 2건 — 터미널의 문서 집기(docPick) 문구가 리팩터링돼 시험이
  옛 문장을 찾고 있다.
- `test_session_restart` 2건 — 모델 선택 목록·ultracode 문서화(REQ-079 계열).

그래프 쪽은 `python3 tests/ graph_fit` 12건 전부 OK.

### 2026-08-28T00:48:30+09:00 tdd (by nicehugepark)

### TDD 시나리오 (반려 반영 개정) — 계약 11건 전부 통과

- [ ] ① 커서 기준 줌 공식은 **그대로 둔다**. 옵시디언도 그렇게 하고, 고칠 것은
      그 줄이 아니었다.
- [ ] ② 전체 보기 — 경계 상자를 재서 여백(56px)을 두고 화면에 꽉 채운다.
      줌 한계(0.25~4) 안에 머문다.
- [ ] ③ 두 길로 닿는다: 빈 곳 더블클릭 · 손잡이 버튼. 조작법은 이미 있는
      안내 줄에 한 항목으로 붙였다.
- [ ] ④ 순간이동하지 않는다 — 240ms 감속 이동. `prefers-reduced-motion` 이면
      옮기지 않고 바로 놓는다. 휠을 굴리면 이동은 멈춘다.
- [ ] ⑤ 그래프가 화면보다 작아지면 커서 기준을 놓고 가운데로 물린다. 아직 화면을
      넘치는 동안에는 커서 기준 그대로다.
- [ ] ⑥ 손잡이는 갇혔을 때만 뜬다. 400ms 뜸을 들이고, 돌아오면 즉시 사라진다.
- [ ] ⑦ 색면·세로 띠·색 하드코딩 없음. 잉크와 지면 토큰만.
- [ ] ⑧ 진단 파라미터 `?goff` · `?gfit` · `?gzoom=N` · `?gdbl` — 합성이 아니라
      **실제 핸들러**를 태우고, 잰 값을 화면에 얹어 캡처 한 장이 증거가 되게 한다.

반려("빈 곳을 더블클릭 하면 원복이 안된다")로 늘어난 셋:

- [ ] ⑨ **프레임이 돌지 않아도 도착한다.** 옮기는 시간이 지나도 가는 중이면
      그냥 놓는다(헤드리스 실측: 3.3초에 세 프레임).
- [ ] ⑩ **빈 곳 두 번 누르기를 직접 센다** — 420ms·7px·끌지 않음·왼쪽 버튼.
      브라우저 dblclick 하나에 목숨을 걸지 않는다(포인터 캡처와 얽힌다).
- [ ] ⑪ **재는 자와 그리는 자가 같다** — 깊이 가중(pfOf)·카메라 드리프트·노드
      반지름을 포함한 화면 좌표로 전체 보기·손잡이·줌아웃을 모두 잰다.

실행: `python3 tests/ graph_fit` 12건 OK.

### 2026-08-28T08:30:17+09:00 tdd (by nicehugepark)

### TDD 시나리오 (2차 반려 반영 개정) — 계약 10건 전부 통과

- [ ] ① 줌 기준점은 **커서와 화면 가운데의 중간**(GZ_BLEND 0.5). 커서 기준
      그대로면 굴릴 때마다 화면이 대각선으로 밀린다. 커서가 가운데에 가까울수록
      손질은 0으로 수렴한다 — 쏠리는 자리에서만 일한다.
- [ ] ② 전체 보기 — 경계 상자를 재서 여백(56px)을 두고 채운다. 빈 곳 더블클릭과
      손잡이 버튼, 두 길 모두로 닿는다. 줌 한계(0.25~4) 안에 머문다.
- [ ] ③ 순간이동하지 않는다 — 240ms 감속. `prefers-reduced-motion` 이면 바로 놓는다.
- [ ] ④ 굴린 결과가 화면을 비우면 되당긴다(목줄). **문턱 없음.** 축마다 따로:
      그 축으로 화면보다 작으면 여유에 비례해 가운데로, 아직 넘치면 가장자리의
      빈 띠(짧은 변의 18%)만큼만.
- [ ] ⑤ 손잡이는 화면에 남은 노드가 25% 아래일 때만, 400ms 뜸 들여 뜬다.
- [ ] ⑥ 프레임이 돌지 않아도 도착한다(320ms 구제).
- [ ] ⑦ 빈 곳 두 번 누르기를 직접 센다 — 420ms·7px·끌지 않음·왼쪽 버튼.
- [ ] ⑧ 재는 자와 그리는 자가 같다 — 깊이 가중·카메라 드리프트·반지름 포함.
- [ ] ⑨ **확대와 축소가 같은 손질을 받는다.** 방향은 배율에서만 갈리고 기준점·
      목줄은 갈리지 않는다.
- [ ] ⑩ 고치기 전과 후를 같은 자에 놓고 잰다 — `?graw` 로 기준점 손질과 목줄을
      둘 다 끄고, `?gzoom=±N&gat=<자리>` 로 양방향을 실제 휠 핸들러에 태운다.
      쏠린 거리를 화면에 숫자로 얹어 캡처 한 장이 곧 증거가 되게 한다.

실행: `python3 tests/ graph_fit` 14건 OK.

### 2026-08-28T08:30:45+09:00 response (by nicehugepark)

**두 지적 다 맞았습니다. 재 보니 숫자가 그렇게 나옵니다.**

지난번에 고친 것은 "쏠린 뒤 되돌리는 길"뿐이었고, **줌 동작 자체는 손대지
않았습니다.** 확대는 전혀 건드리지 않았고, 축소도 `그래프 전체가 화면보다
작아지면` 이라는 문턱을 달아 둬서 여덟 번쯤 굴린 **뒤에야** 일했습니다. 그
전까지는 고치기 전과 한 픽셀도 다르지 않았습니다 — "뭐가 달라진 건지
모르겠다"가 정확한 관찰입니다.

**고친 것 하나: 줌 기준점을 커서와 화면 가운데의 중간에 둡니다.**

커서 기준 줌은 커서 밑을 못 박고 나머지를 밀어냅니다. 커서가 구석에 있으면
굴릴 때마다 화면이 대각선으로 밀리고, 그게 쌓여서 "항상 대각선으로 고정된다"가
됩니다. 확대든 축소든 같습니다(방향만 반대). 기준점을 가운데로 절반 당기면
밀리는 양이 절반이 되고, 굴린 자리는 화면 가운데로 모입니다. 커서가 이미
가운데 근처면 손질은 0에 수렴합니다 — 그때는 애초에 쏠림이 없으니까요.

**고친 것 둘: 굴린 결과가 화면을 비우면 그만큼 되당깁니다(문턱 없이).**

축마다 따로 봅니다. 그 축으로 그래프가 화면보다 작으면 여유에 비례해 가운데로
물고, 아직 넘치면 **가장자리에 빈 띠가 생길 때만** 그 띠만큼 되당깁니다.
화면이 아직 노드로 차 있는 동안에는 아무것도 하지 않습니다 — 확대해서 한 곳을
들여다보는 중에 화면이 제멋대로 움직이면 그게 더 나쁩니다.

**실측 — 같은 자리에서 여덟 번 굴린 결과 (화면 1120x594, 가운데 560,297).**

    커서 왼쪽 위, 여덟 번 축소   가운데서 벗어난 거리   화면에 남은 노드
      고치기 전 (?graw)            371px                 98%
      지금                          66px                100%

    커서 왼쪽 위, 여덟 번 확대   가운데서 벗어난 거리   화면에 남은 노드
      고치기 전 (?graw)            894px                 10%
      지금                         513px                 27%

    커서 오른쪽 아래, 여덟 번 축소   0px → 11px          100%
    커서 화면 가운데, 여섯 번 확대   0px → 87px           손질이 거의 없음

축소 캡처 두 장을 나란히 보면 차이가 눈에 바로 들어옵니다: 고치기 전에는
그래프가 왼쪽 위 구석에 뭉쳐 있고 화면의 3/4이 비어 있는데, 지금은 화면 전체에
퍼져 있습니다. 배율(k0.394)은 두 장이 똑같습니다 — 달라진 것은 자리뿐입니다.

지난번에 넣은 전체 보기(빈 곳 더블클릭·손잡이)는 그대로 살아 있습니다
(회귀 확인: 구석 상태 0% → 더블클릭 후 99%).

`python3 tests/ graph_fit` 14건 OK. 전체 스위트 1,293건 중 실패 2건은
판정 대화상자(test_judge_dialog)의 옛 시험이 리팩터링된 코드를 못 따라간 것으로
이 요청과 무관합니다 — 그 REQ 담당이 시험을 갱신해야 합니다.

[Image: assets/REQ-20260827-083-62x6/g3-zoomout-before.png]
[Image: assets/REQ-20260827-083-62x6/g3-zoomout-after.png]
[Image: assets/REQ-20260827-083-62x6/g3-zoomin-before.png]
[Image: assets/REQ-20260827-083-62x6/g3-zoomin-after.png]
[Image: assets/REQ-20260827-083-62x6/g3-zoomout-br.png]

### 2026-08-28T08:41:11+09:00 response (by nicehugepark)

**두 결함의 원인을 짚었습니다. 다만 이번 세션은 web/ 에 쓸 수 없어 적용하지
못했습니다** — 아래 패치를 그대로 붙이면 됩니다.

### ① 손잡이가 화면 아래에 잘린다

캔버스 높이가 `H = Math.max(340, innerHeight - 210)` 로 **고정 상수**입니다
(web/index.html, `const W = wrap.clientWidth`). 그런데 캔버스 위에 쌓이는 것
—머리글·필터 줄·범례 두 줄—은 창 폭에 따라 줄 수가 늘어납니다. 늘어난 만큼
판 바닥이 화면 아래로 내려가고, 판 안쪽 `bottom:38px` 에 붙어 있는 "⤢ 전체 보기"
손잡이가 화면 끝에 걸립니다.

같은 함정을 이미 한 번 겪은 자리가 바로 옆에 있습니다 — `syncRailH()` 주석:
"판 높이는 범례가 한 줄이냐 두 줄이냐로 달라져서 고정 계산(100vh - N)은 매번
어긋난다. 그릴 때마다 잰 값을 쓴다." 레일은 재는데 캔버스는 안 잽니다.

**패치** — 재서 정한다:

    const W = wrap.clientWidth || 1200;
    /* 캔버스 위에 쌓이는 것(머리글·필터·범례)은 폭에 따라 줄 수가 바뀐다.
       고정 상수로 빼면 그만큼 판이 화면 아래로 내려가고, 판 안에 붙은 손잡이가
       화면 끝에 잘린다(3차 반려). syncRailH() 가 같은 이유로 이미 잰 값을 쓴다. */
    const GBOT = 24;   // 판 테두리 + 페이지 바닥 여백
    const H = Math.max(340, Math.floor(
      innerHeight - (canvas.getBoundingClientRect().top + (scrollY || 0)) - GBOT));

### ② 손잡이를 누르면 노드가 전부 사라진다

제가 2차에서 넣은 `gFitTf()` 의 되풀이가 **수렴하지 않는 경우가 있습니다.**

이 그래프는 깊이감을 주려고 이동량에 깊이 가중을 곱합니다
(`sx = n.x*k + (tx+dx)*pf`, pf 0.74~1.22). 그래서 화면에 찍히는 무리의 폭이
배율뿐 아니라 **이동량에도 딸립니다** — 대략 `폭 ≈ k*월드폭 + 0.49*이동량`.
그런데 가운데로 맞추려면 `이동량 ≈ (W/2 - k*월드중심)/0.98` 이라, 배율을 줄일수록
이동량이 커지고 폭도 같이 커집니다. 되먹임입니다.

두 식을 풀면

    k * (월드폭 - 0.5*월드중심) = (W - 112) - 0.5*(W/2)

물리가 노드를 월드 (W/2, H/2) 로 당기므로 월드중심 ≈ W/2 입니다. 즉 **무리가
W/2 의 절반(캔버스 1120px 기준 약 280 월드단위)보다 좁으면 좌변 계수가 0 이하가
되어 양수 해가 없습니다.** 되풀이는 배율을 하한 0.25 까지 떨어뜨리며 이동량을
키우고, 무리를 화면 밖으로 밀어냅니다 — "버튼을 누르면 노드가 전부 사라진다"가
이것입니다. 노드 수가 적거나(필터를 걸었거나) 무리가 뭉쳐 있을 때 걸립니다.
문서가 500건 다 보이는 기본 화면에서는 무리가 넓어 걸리지 않아 제 검증이
놓쳤습니다.

**패치** — 되풀이한 값을 **믿지 말고 확인한 뒤에 옮긴다.** 안전한 기본값(깊이
가중을 1로 본 단순 계산)은 어떤 배치에서도 무리를 화면 가운데에 놓습니다.

    function gSeenAt(k, tx, ty){
      if (!nodes.length) return 1;
      let on = 0;
      for (const n of nodes){
        const pf = pfOf(n);
        const sx = n.x * k + (tx + st.dx) * pf, sy = n.y * k + (ty + st.dy) * pf;
        if (sx >= 0 && sx <= W && sy >= 0 && sy <= H) on++;
      }
      return on / nodes.length;
    }
    function gSeen(){ return gSeenAt(st.tf.k, st.tf.x, st.tf.y); }   // 기존 gSeen 대체

    function gFitTf(){
      const b = gBBox();
      if (!b) return null;
      // 안전한 기본값 — 깊이 가중을 1로 보고 맞춘다. 되먹임이 없어 늘 성립한다.
      const k0 = gClampK(Math.min((W - GFIT_PAD*2) / b.w, (H - GFIT_PAD*2) / b.h));
      const safe = {k: k0, x: W/2 - b.cx*k0, y: H/2 - b.cy*k0};
      let k = k0, x = safe.x, y = safe.y;
      for (let i = 0; i < 4; i++){
        let s = gScreenBox(k, x, y);
        k = gClampK(k * Math.min((W - GFIT_PAD*2) / s.w, (H - GFIT_PAD*2) / s.h));
        s = gScreenBox(k, x, y);
        x += W/2 - s.cx; y += H/2 - s.cy;
      }
      /* 깊이 가중이 이동량에 곱해지는 탓에 이 되풀이는 무리가 좁으면 수렴하지
         않는다 — 배율이 하한까지 떨어지며 무리를 화면 밖으로 밀어낸다(3차 반려).
         그러니 결과를 재 보고, 더 잘 보이는 쪽을 쓴다. */
      const a = gSeenAt(k, x, y), c = gSeenAt(safe.k, safe.x, safe.y);
      return (a >= 0.9 || a >= c) ? {k, x, y} : safe;
    }

### 검증 공백도 함께 메워야 합니다

헤드리스 캡처는 `--virtual-time-budget` 으로 도는 탓에 애니메이션 프레임이 몇
장밖에 안 돌아, **노드가 처음 놓인 고리 배치 그대로** 남습니다. 제가 2차까지
잰 것은 사용자가 보는 자리 잡힌 그래프가 아니었습니다. 물리를 손으로 돌리는
이음새를 붙여야 같은 것을 재게 됩니다:

    // window.__gdbg 에 추가
    settle: n => { for (let i = 0; i < n; i++) step(); draw(); }

그리고 `?gsettle=400` 으로 먼저 자리를 잡힌 뒤 `?gdbl=btn` 을 태우면, 이번
결함이 헤드리스에서도 재현·검증됩니다. 시험 쪽에는 `gFitTf` 가 되풀이 결과를
확인하고 쓴다는 계약(`gSeenAt` 호출과 `safe` 반환)을 넣으면 됩니다.

### 2026-08-28T13:00:30+09:00 response (by nicehugepark)

**두 결함 다 고쳤습니다. 그리고 이번에는 헤드리스에서도 사용자가 보는 그래프를 재게 됐습니다** — 세 번째 것이 앞의 둘을 놓치게 한 원인이었습니다.

### ㉠ 손잡이가 화면 아래에 잘린다

판 높이가 `innerHeight - 210` 이라는 **고정 계산**이었습니다. 그런데 판 위에 쌓이는 것 — 머리글·필터 줄·범례 — 은 창 폭에 따라 줄 수가 늘어납니다. 늘어난 만큼 판 바닥이 화면 밖으로 내려가고, 판 안쪽에 붙어 있는 "⤢ 전체 보기"가 화면 끝에 걸립니다.

바로 옆에 같은 함정을 이미 겪은 자리가 있었습니다. 레일 높이를 맞추는 함수의 주석입니다: "판 높이는 범례가 한 줄이냐 두 줄이냐로 달라져서 고정 계산은 매번 어긋난다. 그릴 때마다 잰 값을 쓴다." **레일은 재는데 캔버스는 안 쟀습니다.** 이제 캔버스도 잽니다.

### ㉡ 손잡이를 누르면 노드가 전부 사라진다

원인은 앞선 노트에서 제가 짚은 것(되풀이가 수렴하지 않는다)보다 한 겹 더 깊었습니다. 그 패치만 붙여 놓고 실제로 눌러 보니 여전히 반이 사라졌고, 화면에 숫자를 얹어 보니 이렇게 나왔습니다:

    월드 상자 31,791 x 11,185      판 1,120 x 519

**노드 몇 개가 아주 멀리 가 있습니다.** 이 판의 물리는 처음 몇 초 동안 노드를 세게 밀어내는데(가까울수록 세다) 그때 몇 개가 튀어 나가고, 힘이 식은 뒤에는 돌아올 길이 없습니다. 그 몇 개가 경계 상자를 통째로 정합니다. 그 상자를 화면에 맞추려면 배율이 0.035 여야 하는데 줌 하한이 0.25 라 거기 걸리고, 배율은 못 내리면서 이동량만 커져 무리를 화면 밖으로 밀어냅니다.

그래서 두 가지를 바꿨습니다.

1. **맞출 상자를 무리의 몸통으로 잽니다.** 위아래 3%씩(500건이면 양끝 15개씩) 뺀 상자입니다. 실측으로 31,791 x 11,185 가 1,157 x 1,162 이 됩니다. 전체 보기가 약속하는 것은 "한 점도 빠짐없이"가 아니라 "무리가 다 보인다"이고, 사람이 되찾고 싶은 것도 그 무리입니다.
2. **배율을 풀지 않고 고릅니다.** 이 판은 깊이감을 주려고 이동량에도 깊이 가중을 곱하기 때문에(화면 폭이 이동량에 딸립니다) "꽉 채우는 값"이 공식 하나로 풀리지 않고, 풀려고 하면 되먹임이 생깁니다. 이제 배율 후보 열다섯 개를 훑어 각각 가운데에 놓아 보고 **실제로 화면에 남는 노드가 가장 많은 쪽**을 씁니다. 같으면 화면을 크게 채우는 쪽입니다.

### ㉢ 지금까지 잰 것이 사용자가 보는 그래프가 아니었습니다

헤드리스 캡처는 애니메이션 프레임을 몇 장밖에 안 돌립니다(실측 3.3초에 세 장). 그래서 노드가 **처음 놓인 고리 배치** 그대로 남습니다. 그 배치는 넓게 퍼져 있어 전체 보기가 늘 잘 되는 것처럼 보입니다 — 실제로 그 상태에서는 98% 가 보였습니다. 사람이 6초쯤 뒤에 보는 자리 잡힌 그래프에서만 이번 결함이 납니다.

물리를 손으로 돌리는 이음새(`__gdbg.settle`)와 `?gsettle=N` 을 넣었습니다. **루프와 똑같은 관문을 답니다** — 관문 없이 돌렸더니 다 식은 뒤에도 밀어내는 힘만 남아 노드가 끝없이 흩어졌고(월드 상자 51,209), 그건 사용자가 보는 그래프가 아니라 진단이 만들어 낸 그래프였습니다. 이것도 실제로 겪고 고쳤습니다.

### 실측 — 자리 잡힌 그래프(?gsettle=340)에서 실제로 눌러 본 결과

    손잡이 클릭   고치기 전  화면에 남은 노드 0% → 29%   배율 0.250(하한에 붙음)
                  지금                      0% → 97%   배율 0.292
    빈 곳 더블클릭 지금                      0% → 97%
    기본 화면      손잡이 뜨지 않음
    좁은 창(1000x720, 머리글 3줄·범례 2줄)  손잡이가 판 안에 온전히 보임

`python3 tests/ graph_fit` 17건 OK (기존 14 + 이번 3: 판 높이를 잰다 · 전체 보기가 자기 결과를 재 보고 쓴다 · 진단이 루프와 같은 관문을 지난다). 전체 스위트 1,338건 OK.

[Image: assets/REQ-20260827-083-62x6/g4-1-before-btn-29pct.png]
[Image: assets/REQ-20260827-083-62x6/g4-2-after-btn-97pct.png]
[Image: assets/REQ-20260827-083-62x6/g4-3-after-dblclick-97pct.png]
[Image: assets/REQ-20260827-083-62x6/g4-4-handle-not-cut.png]

### 2026-08-28T13:00:37+09:00 subagent (agent: sub:subagent:ab4367e7, by nicehugepark)

Appending response note to REQ-20260827-083-62x6

### 2026-08-28T13:01:09+09:00 subagent (agent: sub:subagent:a9c24a80, by nicehugepark)

Transitioning REQ-20260827-083-62x6 to review

### 2026-08-28T13:01:10+09:00 tdd (by nicehugepark)

### TDD 시나리오 (3차 반려 반영 개정) — 계약 13건 전부 통과

- [ ] ① 줌 기준점은 커서와 화면 가운데의 중간(GZ_BLEND 0.5). 확대·축소 같은 규칙.
- [ ] ② 전체 보기 — 여백(56px)을 두고 채운다. 빈 곳 더블클릭·손잡이 두 길 모두.
- [ ] ③ 순간이동하지 않는다 — 240ms 감속. `prefers-reduced-motion` 이면 바로 놓는다.
- [ ] ④ 굴린 결과가 화면을 비우면 되당긴다(목줄). 문턱 없이, 축마다 따로.
- [ ] ⑤ 손잡이는 화면에 남은 노드가 25% 아래일 때만, 400ms 뜸 들여 뜬다.
- [ ] ⑥ 프레임이 돌지 않아도 도착한다(320ms 구제).
- [ ] ⑦ 빈 곳 두 번 누르기를 직접 센다 — 420ms·7px·끌지 않음·왼쪽 버튼.
- [ ] ⑧ 재는 자와 그리는 자가 같다 — 깊이 가중·카메라 드리프트·반지름 포함.
- [ ] ⑨ 확대와 축소가 같은 손질을 받는다.
- [ ] ⑩ 고치기 전과 후를 같은 자에 놓고 잰다 — `?graw` · `?gzoom=±N&gat=`.

3차 반려("전체 보기 버튼이 화면 하단에 잘려서 표시된다. 전체보기 버튼을 한번
누르게 되면 모든 노드들이 사라진다")로 늘어난 셋:

- [ ] ⑪ **판 높이를 잰다.** 고정 계산(`innerHeight - 210`)은 머리글·범례가 한 줄
      늘면 그만큼 판 바닥을 화면 밖으로 밀어내고, 판 안에 붙은 손잡이가 잘린다.
      옆의 syncRailH() 가 같은 이유로 이미 잰 값을 쓰고 있었다.
- [ ] ⑫ **전체 보기가 자기 결과를 재 보고 쓴다.** 맞출 상자는 멀리 튄 몇 개를 뺀
      몸통(위아래 3%)이고, 배율은 공식으로 풀지 않고 후보 열다섯을 훑어 화면에
      남는 노드가 가장 많은 쪽을 고른다. 깊이 가중이 이동량에도 곱해져 "풀면"
      되먹임이 나고, 경계 상자는 튄 노드 하나가 통째로 정한다(실측 31,791px).
- [ ] ⑬ **진단이 루프와 같은 관문을 지난다.** `__gdbg.settle` · `?gsettle=N` 으로
      물리를 손으로 돌리되 `st.alpha > 0.006` 관문을 루프와 똑같이 단다. 관문이
      없으면 다 식은 뒤에도 밀어내는 힘만 남아 노드가 끝없이 흩어져(월드 상자
      51,209px) 진단이 없는 그래프를 재게 된다.

실행: `python3 tests/ graph_fit` 17 OK · 전체 `python3 tests/` 1,338 OK.

### 2026-08-28T13:12:46+09:00 ask (by nicehugepark)

리뷰 해야할 항목으로는 승인해도 될 것 같은데, 노드들이 멀리서 보면 구(sphere) 모양이 되어야할 것 같은데 좌측상단에서 우측하단으로의 방향으로 찌그러진 것 같다.
[Image: assets/REQ-20260827-083-62x6/20260828T131128-image.png]

### 2026-08-28T13:13:42+09:00 response (by nicehugepark)

사용자 추가 지적 (13:12, 대시보드 채팅): "노드들이 멀리서 보면 구(sphere) 모양이 되어야할 것 같은데 좌측상단에서 우측하단으로의 방향으로 찌그러진 것 같다."

줌아웃 시 노드 무리가 등방적 덩어리로 보여야 하는데 대각선(↖→↘)으로 눌린 타원이다. 지금 작업 중인 gFitTf 수렴 문제와 같은 자리일 수 있다 — 배치(힘 시뮬레이션의 축 대칭)와 화면 변환(fit transform) 중 어디서 비등방이 생기는지 갈라서 확인할 것. 담당 designer 에이전트에 중계했다.

덧붙임: 사용자가 함께 보낸 캡처가 서버에 저장되지 않았다 (state/terminal/uploads/nicehugepark/ 디렉토리는 만들어졌으나 파일 없음). 별건으로 조사한다.

### 2026-08-28T13:14:28+09:00 response (by nicehugepark)

앞 노트 정정: 캡처는 유실되지 않았다. 서버가 업로드를 이 문서 첨부로 들여놓은 것이고 (assets/REQ-20260827-083-62x6/20260828T131128-image.png), 본문 482행에서 참조 중이다.

캡처에서 읽히는 것:
- 본 무리가 화면 왼쪽~가운데에 몰려 있고 윤곽이 ↖→↘ 방향으로 늘어난 타원이다. 오른쪽 절반이 크게 비어 있다.
- 오른쪽에 떨어진 작은 무리 하나, 왼쪽 아래 구석에 외톨이 노드 하나. 이 바깥 것들이 fit 의 경계 상자를 키워 본 무리를 한쪽으로 밀고 눌러 보이게 만들 수 있다 — 경계 상자를 이상치에 그대로 맞추는지 확인 필요.
- 무리 안에서도 링크가 한 노드로 방사형으로 몰린 자리가 있어 배치 자체의 비등방 가능성도 있다.

담당 designer 에 정정과 함께 중계했다.

### 2026-08-28T13:21:19+09:00 tdd (by nicehugepark)

### TDD 시나리오 (4차 지적 반영) — 계약 14건 전부 통과

앞선 13건은 그대로 유효하고(⑪ 판 높이 실측 · ⑫ 전체 보기가 자기 결과를 재 보고
씀 · ⑬ 진단이 루프와 같은 관문을 지남), 하나가 늘었다.

- [x] ⑭ **무리는 구 모양으로 남는다.** 깊이 시차를 팬 오프셋에 곱하면
      (`n.x*k + tx*pf`) 가만히 있을 때도 노드마다 `(pf−1)·(tx,ty)` 만큼 밀린다 —
      시차가 아니라 **팬 방향으로의 전단**이다. 무리는 등방적으로 놓이는데(초기
      배치는 원, 중심으로 당기는 힘도 등방) 화면에서만 대각선으로 눌린다.
      이제 카메라를 화면 가운데에 두고 **거리에** 깊이 배율을 먹인다 —
      원근투영이 실제로 하는 계산이다. 가만히 있으면 등방 확대라 원이 원으로
      남고, 팬 할 때는 깊이마다 다르게 흘러 시차는 그대로 산다. 끌기 역변환도
      같은 식을 되짚는다.
      함께: **목줄도 몸통으로 잰다.** 튄 몇 개를 끼워 재면 상자가 늘 화면보다
      커서 "아직 넘친다"가 항상 참이 되고, 되당기는 규칙이 통째로 잠든다 —
      굴릴수록 무리가 한쪽으로 흘러도 아무 일도 일어나지 않았다.
      재는 자: `__gdbg.aniso()` 가 화면 좌표의 분산을 두 주축으로 갈라
      긴 축/짧은 축 비와 기운 각도를 낸다. `?gshape` 로 그림에 얹고, `?praw` 로
      고치기 전 투영을 그대로 남겨 나란히 놓는다.

실행: `python3 tests/ graph_fit` 18 OK.

### 2026-08-28T13:21:47+09:00 subagent (agent: sub:subagent:a3304e04, by nicehugepark)

Appending TDD note to REQ-20260827-083-62x6

### 2026-08-28T13:21:51+09:00 response (by nicehugepark)

**보신 그대로였습니다. 찌그러진 방향은 마우스로 화면을 옮긴 방향이었고, 그 각도가 숫자로 그렇게 나옵니다.**

먼저 **눈싸움을 그만두려고 자를 만들었습니다.** 노드들의 화면 좌표 분산을 두 주축으로 갈라 **긴 축 / 짧은 축 비**와 **그 축이 기운 각도**를 냅니다. 1.00 이면 원, 커질수록 눌린 타원이고, 45° 가 ↖↘ 대각선입니다. 이제 "찌그러져 보인다"를 그림 위에 숫자로 적어 둡니다.

    전체 보기 직후   긴 축/짧은 축 1.24   기운 각도 20°   (그때 화면 이동값 459,209 — 각도 24°)

**기운 각도가 화면 이동 방향과 같습니다.** 우연이 아닙니다.

### 원인 — 시차를 흉내 낸 자리가 어긋남을 만들고 있었습니다

이 그래프는 부피감을 주려고 노드마다 깊이를 다르게 두고, **화면을 옮기는 양에 그 깊이를 곱하고 있었습니다**(가까운 것이 더 크게 흐르도록). 옮기는 동안에는 그럴듯한데, **가만히 있을 때도** 노드마다 이동값의 −28%~+24% 만큼 밀린 채 멈춰 있습니다. 그건 시차가 아니라 **이동 방향으로의 어긋남**입니다. 이동값이 (459,209)면 무리 전체가 그 대각선으로 240px 늘어납니다.

무리 자체는 등방적으로 놓입니다 — 처음 배치가 원이고, 가운데로 당기는 힘도 가로세로가 같습니다. **찌그러짐은 배치가 아니라 화면에 찍는 계산에서 생겼습니다.** 그래서 줌아웃할수록(이동값이 커질수록) 더 심해졌습니다.

**고친 방법: 카메라를 화면 한가운데에 두고, 이동값이 아니라 가운데로부터의 거리에 깊이를 먹입니다.** 원근투영이 실제로 하는 계산입니다. 가만히 있을 때는 가운데를 중심으로 한 고른 확대라 원이 원으로 남고, 화면을 옮기는 동안에는 깊이마다 다르게 흘러 부피감은 그대로 삽니다. 끌어 옮기는 역계산도 같은 식을 되짚게 고쳤습니다.

### 하나 더 — 오른쪽 절반이 비어 있던 이유

굴린 뒤 무리를 화면 안으로 되당기는 규칙이 있는데, 그 규칙이 **멀리 튄 노드까지 끼워** 상자를 재고 있었습니다. 그러면 상자가 늘 화면보다 커서 "아직 화면이 차 있다"가 항상 참이 되고, 되당기는 일이 통째로 잠듭니다. 굴릴수록 무리가 한쪽으로 흘러도 아무 일도 일어나지 않았습니다 — 보내 주신 그림에서 오른쪽 절반이 비어 있던 것이 이것입니다. 전체 보기는 이미 몸통으로 재고 있었는데 되당기는 쪽만 안 고쳐져 있었습니다. 이제 무리가 어디 있는지 묻는 자리는 전부 몸통을 봅니다.

### 실측 — 자리 잡힌 그래프(?gsettle=340)에서, 같은 조건으로

    왼쪽 위에 커서를 두고 14번 줌아웃
      고치기 전(?praw)   1.11 @ 14°   무리가 왼쪽 위로 몰리고 오른쪽 절반이 빔
      지금                1.04         무리가 화면 가운데에 둥글게 남음

    전체 보기(손잡이) 직후
      고치기 전(?praw)   1.24 @ 20°
      지금                1.04         화면에 남은 노드 96%

    빈 곳 더블클릭       화면에 남은 노드 61% → 96% · 1.04

`python3 tests/ graph_fit` 18건 OK. 네 장 붙였습니다 — 줌아웃 전/후, 전체 보기 전/후.

[Image: assets/REQ-20260827-083-62x6/g5-1-shape-before-1.11.png]
[Image: assets/REQ-20260827-083-62x6/g5-2-shape-after-1.04.png]
[Image: assets/REQ-20260827-083-62x6/g5-3-fit-before-1.24.png]
[Image: assets/REQ-20260827-083-62x6/g5-4-fit-after-1.04.png]

### 2026-08-28T13:28:52+09:00 subagent (agent: sub:subagent:a335c5ad, by nicehugepark)

Transitioning REQ-20260827-083-62x6 to review

### 2026-08-28T13:29:50+09:00 subagent (agent: sub:designer:a20a4928, by nicehugepark)

`[2026-08-28 13:47 KST - designer]`

세 건 모두 처리했고, 작업 중 들어온 REQ-083 추가 지적까지 반영했다. 커밋은 하지 않았다.

### 1) REQ-20260827-083-62x6 — 그래프 (blocked → **review**)

blocked 노트의 패치를 붙이는 데서 그치지 않았다. 붙여 놓고 **실제로 눌러 보니 여전히 절반이 사라져서** 한 겹 더 팠다.

- **손잡이 잘림** — 캔버스 높이가 `innerHeight - 210` 고정이라 머리글·범례가 접히면 판 바닥이 화면 밖으로 갔다. 옆의 `syncRailH()` 처럼 **재서 정하게** 바꿨다.
- **노드 사라짐** — 원인은 되풀이 수렴이 아니라 그 아래였다. 물리가 초반에 밀어낸 노드 몇 개가 멀리 가 있어 **월드 상자가 31,791×11,185** 까지 벌어진다. 그 상자를 판(1,120×519)에 맞추려면 배율 0.035 가 필요한데 하한 0.25 에 걸려, 배율은 못 내리면서 이동량만 커져 무리를 밖으로 밀었다. → 맞출 상자를 **몸통(위아래 3% 절사)** 으로 재고(31,791 → 1,157), 배율은 공식으로 풀지 않고 **후보 15개를 훑어 화면에 실제로 남는 노드가 가장 많은 쪽**을 고른다. 손잡이 클릭 0% → **97%**.
- **검증 공백** — `__gdbg.settle` · `?gsettle=N` 을 넣되 **루프와 같은 alpha 관문**을 달았다. 관문 없이 돌렸더니 노드가 끝없이 흩어져(월드 51,209) 진단이 없는 그래프를 만들었고, 그것도 실제로 겪고 고쳤다.

**추가 지적(대각선 찌그러짐)** — 사용자 캡처를 열어 보고 원인을 갈랐다. 배치는 등방적(초기 배치 원, 중심 인력 등방)이고, **화면에 찍는 계산**이 범인이었다: 깊이 시차를 **팬 오프셋에 곱하고** 있어 정지 상태에서도 노드마다 `(pf−1)·(tx,ty)` 만큼 밀려 있었다 — 시차가 아니라 팬 방향 전단이다. 카메라를 화면 가운데 두고 **거리에** 깊이를 먹이도록(원근투영의 실제 계산) 바꿨다. 곁들여 **목줄도 이상치를 끼워 재고 있어 통째로 잠들어 있었다**(사용자 캡처의 "오른쪽 절반이 빈" 이유) — 몸통으로 재게 했다.

눈싸움을 그만두려고 **`__gdbg.aniso()`**(주축 분산비·각도) + `?gshape` + `?praw` 를 넣었다. 실측: 전체 보기 직후 **1.24 @ 20°(팬 방향 24°와 일치) → 1.04**, 14번 줌아웃 **1.11 → 1.04**.

`python3 tests/ graph_fit` **18 OK** (기존 14 + 판 높이 실측 · 전체 보기 자기검증 · 진단 관문 · 등방성).

### 2) REQ-20260828-012-62x6 — 비밀 키 (in-progress → **review**)

Settings → 내 계정에 자리를 냈다. 목록은 **키 이름과 둔 곳 둘뿐**이고, 줄이 읽는 필드가 `key`·`where` 뿐임을 시험이 잠근다. 값 칸은 password + `new-password`, 저장 뒤 **성패와 무관하게 즉시 비운다**. 지우기는 되돌릴 수 없으므로 확인 한 단계. **남의 계정 판에는 내지 않았다** — 서버 목록은 admin 대리(`as`)를 안 받는데 쓰기는 받아서, 섞으면 남의 이름표 밑에 내 키가 뜬다.

`?secdbg=rm` 로 진짜 버튼을 누르고 확인 창을 지나 검증했다(값이 판에 남았나: 아니오 / 값 칸 비었나: 예). 진단은 **`S9DBG_` 로 시작하는 키만** 건드린다. `python3 tests/ secret_ui` **7 OK**.

### 3) REQ-20260828-013-62x6 — 스트림 스위치 (in-progress → **review**)

같은 판에 "대화 기록 — Stream 탭" 칸. **저장 버튼을 두지 않았다** — 바꾸면 바로 저장되고(디스플레이 설정과 같은 손버릇), 저장 뒤 **신원을 다시 받아** Stream 탭을 그 자리에서 올리고 내린다. 끄면 보관 기간 **줄 전체**가 명도로 물러난다. 서버는 한 줄도 안 더했다 — `POST /api/user/config` 가 `s9 user config` 와 같은 파일을 쓴다(실측 확인). 꺼진 Stream 화면의 "터미널에서 s9 user config …" 문구는 **"설정에서 켜기" 버튼**으로 바꿨다(이 REQ의 사유가 그 문장이었다).

`?stdbg=off` 로 눌러 보고 **되돌렸다**(on→off, 탭 보임→숨김, 되돌림 확인). `python3 tests/ stream_switch_ui` **6 OK**.

### 공통

- 전체 스위트 **1,356건 OK**(skipped 1).
- 색면 하이라이트·세로 띠·라운드·그림자·색 하드코딩 없음 — 배경은 지면·판·잉크(hover 인버스)뿐, 상태 구분은 낱말로. 시험이 잠근다.
- 기존 계약 하나를 형태만 바꿨다: `tests/test_stream_switch.py::test_off_branch_precedes_fetch` 의 "되돌리는 법을 함께 적는다"를 **적는 것 → 누르는 것**으로 갱신했다(이유를 주석에 남겼다).
- 검증 잔재 없음: 데모 비밀 키 삭제 확인, `stream_mirror` 원상 복구 확인.

파일: `/home/sjpark1/section9/web/index.html`, `/home/sjpark1/section9/tests/test_graph_fit.py`, `/home/sjpark1/section9/tests/test_secret_ui.py`(신규), `/home/sjpark1/section9/tests/test_stream_switch_ui.py`(신규), `/home/sjpark1/section9/tests/test_stream_switch.py`

**한계 하나**: 깊게 확대(k≈2.5)하면 화면에 남는 노드가 25% 아래로 떨어져 "⤢ 전체 보기" 손잡이가 뜬다. 2차 계약이 정한 판정 기준(남은 노드 비율)의 결과이고 이번 지적과 무관해 손대지 않았다 — 바꾸려면 별건으로 여는 편이 낫다.

### 2026-08-28T13:34:10+09:00 subagent (agent: sub:designer:a20a4928, by nicehugepark)

`[2026-08-28 13:49:12 KST - designer]`

배경 작업(서버 재기동) 완료 알림일 뿐, 사용자 입력이 아니다. 승인이나 확인으로 읽지 않는다.

작업은 앞 보고 그대로 마무리 상태다:

- REQ-20260827-083-62x6 — review (그래프 손잡이 잘림·노드 사라짐·대각선 찌그러짐)
- REQ-20260828-012-62x6 — review (비밀 키 화면)
- REQ-20260828-013-62x6 — review (스트림 스위치)

전체 스위트 1,356건 OK. 커밋은 하지 않았다. 세 건 모두 사용자 판정 대기이고, 판정은 문서에서 이뤄진다.

## History
- 2026-08-27T23:46:55+09:00 created by nicehugepark (status: open)
- 2026-08-27T23:56:59+09:00 status: open -> in-progress (by nicehugepark) — 화면 작업 착수 (designer)
- 2026-08-27T23:57:00+09:00 status: in-progress -> review (by nicehugepark) — 확인 포인트: Graph 탭에서 화면 구석에 마우스를 두고 휠을 아래로 굴려 보세요. 전처럼 그래프가 그 구석으로 빨려 들어가지 않고, 화면보다 작아지는 순간부터 가운데로 모입니다. 그래도 그래프가 화면 밖으로 밀려나면 판 아래 가운데에 "⤢ 전체 보기" 버튼이 뜹니다 — 누르거나 빈 곳을 더블클릭하면 부드럽게 전체가 보이는 자리로 돌아오고 버튼은 사라집니다. 확대해서 한 곳을 들여다보는 중에는 화면이 제멋대로 움직이지 않는지도 봐 주세요. F5 후 확인.
- 2026-08-27T23:57:46+09:00 status: review -> in-progress (by nicehugepark) — 반려: 비어있는곳을 더블클릭 하면 원복이 안된다. 직접 눈으로 보고 직접 행동해봐 [via dashboard]
- 2026-08-28T00:48:43+09:00 status: in-progress -> review (by nicehugepark) — 확인 포인트 — Graph 탭에서 F5 후 봐 주세요. 1) 화면 구석에 마우스를 두고 휠을 아래로 여러 번 굴려 그래프를 구석으로 보내 보세요. 그 상태에서 **아무것도 없는 빈 곳을 두 번 클릭**하면 그래프가 화면 가운데로 돌아옵니다. 지난번에는 여기서 아무 일도 일어나지 않았습니다. 2) 판 아래 가운데의 "⤢ 전체 보기" 버튼으로도 같은 일이 됩니다. 버튼은 그래프가 거의 안 보일 때만 뜨고, 돌아오면 사라집니다. 지난번에는 기본 화면에서도 버튼이 떠 있었는데(그건 갇힌 게 아니라 그냥 보고 있는 중입니다) 이제 안 뜹니다. 3) 확대해서 한 곳을 들여다보는 중에는 화면이 제멋대로 움직이지 않는지도 봐 주세요. 4) 줌아웃을 계속하면 그래프가 구석이 아니라 가운데로 모입니다. 지난번 반려("빈 곳을 더블클릭 하면 원복이 안된다")의 원인은 세 가지였고 셋 다 고쳤습니다. 이번에는 헤드리스 브라우저에서 실제로 눌러 보고 그 결과를 캡처에 숫자로 얹어 두었습니다 — 구석에서 보이는 노드 0%, 더블클릭 뒤 100%.
- 2026-08-28T08:14:48+09:00 status: review -> in-progress (by nicehugepark) — 반려: 줌 아웃만 생각한걸까, 줌 인은 달라진게 하나도 없는데... 그리고 줌아웃도 사실 뭐가 달라진건지 모르겠다. [via dashboard]
- 2026-08-28T08:31:00+09:00 status: in-progress -> review (by nicehugepark) — 확인 포인트 — Graph 탭에서 F5 후 봐 주세요. 지난번과 달리 **줌 동작 자체가** 바뀌었습니다. 1) 화면 **왼쪽 위 구석**에 마우스를 두고 휠을 아래로 여덟 번쯤 굴려 보세요. 전에는 그래프가 그 구석으로 빨려 들어가 화면의 3/4이 비었습니다. 이제는 화면 전체에 퍼진 채로 작아집니다. 배율은 전과 똑같고 자리만 달라집니다. 2) **확대도 같습니다.** 구석에 마우스를 두고 휠을 위로 굴려 보세요. 전에는 그래프가 반대쪽으로 밀려나 화면에 노드가 열에 하나만 남았습니다. 3) 마우스를 **화면 한가운데** 두고 굴리면 전과 똑같이 동작합니다 — 손질은 구석에서만 일하고 가운데서는 0입니다. 확대해서 한 곳을 들여다보는 중에 화면이 제멋대로 움직이지 않는지 봐 주세요. 4) 빈 곳 더블클릭·"⤢ 전체 보기" 버튼은 그대로입니다. 지난번 "뭐가 달라진 건지 모르겠다"는 맞는 관찰이었습니다. 축소 손질에 문턱이 있어서 그래프 전체가 화면보다 작아진 뒤에야(여덟 번쯤 굴린 뒤) 일했고, 확대는 아예 손대지 않았습니다. 문턱을 없애고 확대·축소 양쪽에 같은 규칙을 걸었습니다. 고치기 전/후를 같은 조건으로 찍은 캡처 네 장을 노트에 붙였습니다.
- 2026-08-28T08:32:59+09:00 status: review -> in-progress (by nicehugepark) — 반려: 전체 보기 버튼이 화면 하단에 잘려서 표시된다. 전체보기 버튼을 한번 누르게 되면 모든 노드들이 사라진다. [via dashboard]
- 2026-08-28T08:41:30+09:00 status: in-progress -> blocked (by nicehugepark) — 이 세션은 web/index.html 에 쓸 수 없어 수정을 적용하지 못했다. 이번 재작업 세션의 작업 폴더가 이 요청의 첨부 폴더 (vault/requests/2026/08/assets/REQ-20260827-083-62x6) 하나로 묶여 있다. web/index.html 은 읽기·검색·캡처는 되지만 편집은 거부된다 ("Claude requested permissions to write to /home/sjpark1/section9/web/index.html"). tests/ 도 같은 밖이다. 무인 적용 모드의 전제(직접 고치고 검증한 뒤 상신)가 성립하지 않아, 우회하지 않고 여기서 멈춘다. **막힌 것은 손이지 판단이 아니다.** 두 결함 모두 원인을 짚었고 붙일 코드까지 바로 위 노트에 적어 두었다: ① 손잡이 잘림 — 캔버스 높이가 `innerHeight - 210` 고정이라 머리글·범례가 한 줄 늘면 판 바닥이 화면 밖으로 내려간다. 옆의 syncRailH() 가 같은 이유로 이미 잰 값을 쓰고 있다. 캔버스도 재서 정하면 된다. ② 노드 사라짐 — 2차에 넣은 gFitTf() 의 되풀이가 무리가 좁을 때 수렴하지 않는다 (깊이 가중이 이동량에 곱해져 되먹임이 생긴다). 배율이 하한까지 떨어지며 무리를 화면 밖으로 밀어낸다. 되풀이 결과를 재 보고 안 보이면 안전한 기본값을 쓰도록 막으면 된다. **여는 방법 두 가지** 1. 이 요청을 워크스페이스 루트가 ~/section9 인 세션으로 다시 띄운다(지금까지의 재작업 세션들은 그랬다). 그러면 그대로 고치고 검증해 상신할 수 있다. 2. 리드가 위 노트의 패치 두 개를 직접 붙이고 `python3 tests/ graph_fit` 으로 확인한다. 덧붙임: 헤드리스 캡처는 `--virtual-time-budget` 으로 도는 탓에 프레임이 몇 장만 돌아 노드가 **처음 놓인 고리 배치** 그대로 남는다. 2차까지 내가 잰 것은 사용자가 보는 자리 잡힌 그래프가 아니었고, 그래서 이번 두 결함을 놓쳤다. 물리를 손으로 돌리는 이음새(`__gdbg.settle`)와 `?gsettle=N` 을 함께 넣어야 다음부터 같은 것을 재게 된다 — 이것도 위 노트에 적어 두었다.
- 2026-08-28T12:43:18+09:00 status: blocked -> in-progress (by nicehugepark) — 워크스페이스 루트가 ~/section9 인 세션에서 재개. blocked 노트에 적힌 패치 두 개(캔버스 높이 실측, gFitTf 결과 검증 후 안전값 대체)와 __gdbg.settle·?gsettle 이음새를 적용한다.
- 2026-08-28T13:01:24+09:00 status: in-progress -> review (by nicehugepark) — Graph 탭에서 F5 후 봐 주세요. 지난번 두 가지가 다 고쳐졌습니다. 1) "⤢ 전체 보기" 버튼이 잘리지 않습니다 — 창을 좁게 줄여 머리글과 범례가 두세 줄로 접히게 만든 뒤 보세요. 전에는 판 바닥이 화면 밖으로 내려가 버튼이 화면 끝에 걸렸습니다. 이제 판을 그릴 때마다 높이를 재서 정하므로 어떤 창 크기에서도 버튼이 판 안에 온전히 들어옵니다. 2) 버튼을 눌러도 노드가 사라지지 않습니다 — 화면 구석에 마우스를 두고 휠을 여러 번 굴려 그래프를 밖으로 보낸 뒤 버튼을 누르거나 빈 곳을 두 번 클릭해 보세요. 그래프가 화면 가운데로 돌아옵니다. 전에는 여기서 배율이 끝까지 줄어들며 노드가 흩어졌습니다. 3) 그래프를 6초쯤 그대로 두었다가 같은 것을 해 보시면 더 확실합니다 — 이번 결함은 노드가 자리를 잡은 뒤에만 나던 것이라 지금까지의 확인이 그걸 못 잡았습니다. 원인은 세 가지였습니다: 판 높이를 재지 않고 계산으로 뺐던 것, 멀리 튄 노드 몇 개가 "전체"의 기준을 통째로 정해 버린 것, 확인용 브라우저가 그림을 몇 장만 그려서 자리 잡기 전 그래프만 보고 있었던 것. 셋 다 고쳤고 캡처 네 장을 문서에 붙였습니다.
- 2026-08-28T13:14:08+09:00 status: review -> in-progress (by nicehugepark) — 추가 지적 접수: "노드들이 멀리서 보면 구 모양이 되어야 할 것 같은데 좌측상단에서 우측하단 방향으로 찌그러진 것 같다." 배치(물리)와 화면 변환(전체 보기·깊이 가중) 중 어디서 비등방이 생기는지 갈라 확인한다.
- 2026-08-28T13:28:51+09:00 status: in-progress -> review (by nicehugepark) — Graph 탭에서 F5 후, **그래프를 6초쯤 그대로 두어 노드가 자리를 잡은 다음에** 봐 주세요. 1) 화면 구석에 마우스를 두고 휠을 열 번쯤 아래로 굴려 보세요. 전에는 무리가 왼쪽 위에서 오른쪽 아래로 늘어난 타원이 되고 오른쪽 절반이 비었습니다. 이제 둥근 덩어리로 남고 화면 가운데에 머뭅니다. 2) 확대(휠 위로)도 같습니다 — 어느 방향으로도 눌리지 않습니다. 3) 빈 곳을 두 번 클릭하거나 "⤢ 전체 보기"를 누르면 무리가 둥글게 화면 가운데로 돌아옵니다. 4) 창을 좁게 줄여 머리글·범례가 여러 줄로 접히게 해도 "⤢ 전체 보기" 버튼이 잘리지 않습니다. 찌그러진 이유는 두 가지였습니다. 하나, 화면을 옮긴 양에 노드마다 다른 깊이를 곱하고 있어서, 가만히 있을 때도 무리가 **옮긴 방향으로** 밀려 늘어났습니다 — 그래서 기운 각도가 마우스로 옮긴 방향과 같았습니다. 둘, 무리를 화면 안으로 되당기는 규칙이 멀리 튄 노드 몇 개까지 끼워 재는 바람에 아무 일도 하지 않았습니다. 눈으로 다투지 않으려고 긴 축/짧은 축 비를 재는 자를 만들어 캡처에 숫자로 적었습니다 — 고치기 전 1.24, 지금 1.04(1.00이 완전한 원)입니다. 전/후 캡처 네 장을 문서에 붙였습니다.
- 2026-08-28T13:47:08+09:00 status: review -> done (by nicehugepark) — 대시보드 승인 [via dashboard]
