---
id: REQ-20260828-001-62x6
type: request
title: 테스트가 실서버 포트를 뺏는다
summary: 전체 테스트 실행 후 /tmp 작업공간에서 supervise 서버가 살아남아 9909를 잡고 404를 낸다
goal: 스위트가 끝난 뒤 대시보드 포트에 테스트 서버가 남지 않고, 남으면 스위트가 실패한다
status: done
size: M
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: 3da5b9de
sessions: ["64db8701", "3da5b9de"]
agents: ["lead:claude-opus-5"]
contributions: [{"actor": "lead:claude-opus-5", "item": "`[2026-08-28 07:31:25 KST - lead]`", "started": "2026-08-28T07:31:25+09:00", "ended": "2026-08-28T07:31:25+09:00", "result": "done", "transcript": ""}]
project: section9
relates_why: {}
tags: ["test", "dashboard", "design"]
created: 2026-08-28T00:06:43+09:00
status_since: 2026-08-28T07:38:07+09:00
updated: 2026-08-28T07:38:07+09:00
priority: 50
---

## Original

전체 스위트(`python3 tests/`)를 돌린 뒤 대시보드가 `web/index.html not found` 404 만 냈다.

원인: 테스트가 띄운 `s9 serve --supervise --port 9909` 프로세스가 **테스트가 끝난 뒤에도 살아남는다.** 그 프로세스들의 작업 디렉터리는 `/tmp/s9smx-*` 라 거기에는 `web/` 이 없다. 진짜 서버(cwd `~/section9`)와 같은 포트를 물고 번갈아 응답하니, 사용자 화면이 새로고침할 때마다 정상/404 를 오간다.

관측한 것:

    pid 948     cwd /home/sjpark1/section9      ← 진짜 서버
    pid 405141  cwd /tmp/s9smx-g9lfwdfu         ← 테스트 잔재
    pid 405176  cwd /tmp/s9smx-v3pzhxk7         ← 테스트 잔재

`--supervise` 라 자식만 죽이면 곧바로 되살아난다 — 감시자까지 함께 죽여야 멎었다. 지금은 손으로 정리했다(작업 디렉터리가 `/tmp` 인 것만 골라 종료).

고칠 방향 둘 중 하나:
- 테스트 서버는 **다른 포트**를 쓰게 한다(테스트 안에 19909 를 쓰는 자리가 이미 있는데 9909 로 뜬 경로가 남아 있다).
- 테스트 종료 시 자신이 띄운 감시자까지 반드시 거두게 한다.

designer 세션에서 화면 캡처가 갑자기 404 를 찍어 발견했다.

## Notes

### 2026-08-28T00:44:23+09:00 designer (by nicehugepark)

재현이 한 번 더 있었고, 이번엔 **화면 검증을 통째로 속였다.**

아티클 화면(REQ-20260827-073-62x6)을 고치고 캡처했는데 몇 번을 다시 찍어도 **고치기 전 화면**이 나왔다. 서버가 새 파일을 내주는 것은 curl 로 확인했고, DOM 을 떠 보면 새 구조가 있었다. 캡처만 옛 화면이었다.

원인은 이 문제였다. 테스트가 띄운 `/tmp/s9smx-*` 작업공간의 서버가 9909 를 함께 물고 있었고, 그 작업공간에는 **테스트 시작 시점의 web/index.html 사본**이 들어 있다. 요청이 그쪽으로 갈 때마다 옛 화면이 나온다. 404 만 나오는 것이 아니라 **그럴듯한 옛 화면**이 나오기 때문에, 코드를 고치고 눈으로 확인하는 절차가 조용히 무력해진다.

정리 방법도 기록해 둔다. `--supervise` 라 자식만 죽이면 되살아난다 — 작업 디렉터리가 `/tmp` 인 것을 골라 감시자까지 함께 죽여야 멎었다:

    for i in 1 2 3; do
      ps=$(for p in $(pgrep -f "s9 serve"); do
             c=$(readlink /proc/$p/cwd); case "$c" in /tmp/*) echo $p;; esac; done)
      [ -z "$ps" ] && break; kill -9 $ps; sleep 1
    done

이 세션에서 두 번 정리했고, 그 사이 스위트를 돌릴 때마다 다시 생겼다.

### 2026-08-28T07:31:25+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-08-28 07:31:25 KST - lead]`

반려 재작업 중입니다. 원인과 수정을 확인했고, 지금 전체 스위트를 돌려 검증 중입니다.

**찾은 원인**: `tests/test_session_mirror_switch.py` 의 EndToEnd 가 세션 시작 훅(`bin/s9-audit-session`)을 임시 작업공간에서 실행하는데, 그 훅의 `ensure_serve()` 는 `S9_PORT` 가 없으면 `state/port` → **9909** 로 떨어집니다. 임시 루트에는 그 파일이 없으니 매번 사용자 포트에 감시자를 세웠습니다. `/tmp/s9smx-*/state/serve-guard.9909.lock` 이 실행 횟수만큼 남아 있는 것으로 확인했습니다(같은 훅을 돌리는 다른 테스트 5개는 이미 `S9_PORT="1"` 로 격리돼 있었고, 이 파일만 빠져 있었습니다).

**적용한 수정**(전부 `tests/` 안):
- `test_session_mirror_switch.py` — env 에 `S9_PORT="1"` 격리 + tearDown 에서 훅이 세운 감시자를 직접 거둔다(자식만 죽으면 되살아나므로 감시자 pid 를 잠금 파일에서 읽어 종료).
- `tests/portpool.py` — 회수기 추가. **대시보드 대역(9909~9950) + 작업공간이 임시 디렉토리**인 `s9 serve` 만 고른다(진짜 서버는 작업공간이 저장소라 제외, 풀 포트 18800~ 테스트 서버도 제외 — 동시에 도는 다른 스위트를 죽이지 않기 위해). 감시자를 먼저 죽이고 3바퀴 확인.
- `tests/__main__.py` — 스위트 전후로 회수하고, 끝난 뒤 남은 게 있으면 **거뒀더라도 실패로 센다**(조용히 치우면 또 생긴다).
- `tests/test_port_pool.py` — 회수 기준·순서 단위 테스트와, 세션 훅을 돌리는 테스트가 포트를 격리했는지 보는 소스 규율 검사 추가.
- `test_compact_signal.py` — 같은 훅을 돌리므로 함께 격리.

**검증한 것**: 격리를 뺀 상태로 돌리면 러너가 실제로 두 개를 잡아내고 실패합니다(`[포트 회수/끝난 뒤] pid ... port 9909 작업공간 /tmp/s9smx-...`), 격리를 넣으면 잔재 없이 통과합니다. 새 규율 검사도 격리 없는 파일을 정확히 지목합니다. 전체 스위트 결과가 나오면 review 로 올리겠습니다.

### 2026-08-28T07:37:47+09:00 response (by nicehugepark)

**이미 구현돼 있었고, 확인했습니다.**

`tests/portpool.py` 와 `tests/__main__.py` 에 장치가 들어 있습니다. 제가 만든 것이
아니라 어젯밤 다른 주체가 넣은 것이고, 커밋은 안 돼 있었습니다.

**동작 확인**
- 스위트 **시작 전과 끝난 뒤** 두 번 `reap_stray_dashboard_servers()` 를 부릅니다.
  회수 기준이 둘을 함께 봅니다 — ① 대시보드 대역(9909~9950) 포트 ② 작업공간이
  임시 디렉토리. 그래서 진짜 서버(작업공간이 저장소)와 풀 포트(18800~)를 쓰는
  정상 테스트 서버는 안 걸립니다.
- `--supervise` 는 자식만 죽이면 되살아나므로 **감시자를 먼저** 죽이고 몇 바퀴
  돌며 확인합니다.
- **거뒀더라도 스위트를 실패로 셉니다.** 조용히 치우면 다음에 또 생기기 때문입니다.
- `tests/test_port_pool.py` 29건 통과. 방금 전체 스위트(1274건) 뒤에 잔재를
  실측했는데 `/tmp/s9smx-*` 서버 0개, 9909 를 무는 것은 진짜 서버 하나뿐입니다.

**왜 무서운 결함이었나**: 404 만 났으면 눈에 띕니다. 실제로는 **테스트 시작
시점의 web/index.html 사본**을 내주기 때문에, 화면을 고치고 캡처로 확인해도
**옛 화면이 통과합니다.** 검증 절차가 조용히 무력해지는 종류라 404 보다 나쁩니다.
디자이너가 아티클 화면을 고치고도 옛 화면이 계속 찍혀 한참 헤맨 것이 그것입니다.

## History
- 2026-08-28T00:06:43+09:00 created by nicehugepark (status: open)
- 2026-08-28T07:17:29+09:00 status: open -> in-progress (by nicehugepark) — drag 이동 [via dashboard]
- 2026-08-28T07:38:07+09:00 status: in-progress -> done (by nicehugepark) — 스위트 앞뒤로 회수하고 남으면 실패로 센다(감시자 먼저 종료). port_pool 29/29 · 전체 1274/1274 · 실측 잔재 0개
