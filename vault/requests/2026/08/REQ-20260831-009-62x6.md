---
id: REQ-20260831-009-62x6
type: request
title: 이어간 뒤 키보드 손이 없다
summary: ▶ 를 눌러 이어가면 버튼이 disabled 로 바뀌며 포커스가 body 로 떨어지고, 카드 재그리기가 그 버튼을 지워 키보드 사용자가 자기 자리를 잃는다
goal: 키보드로 ▶ 를 눌러 이어간 뒤에도 포커스가 그 카드의 손잡이(▶→⏸ 로 바뀐 그 자리)에 남는 것이 실브라우저로 확인된다
status: done
size: S
user: nicehugepark
machine: DESKTOP-TEHV1KR
agents: ["sub:subagent:a5d9f2d6", "sub:frontend-developer"]
contributions: [{"actor": "sub:subagent:a5d9f2d6", "item": "Confirming REQ-20260831-009-62x6 registration", "started": "2026-08-31T13:57:34+09:00", "ended": "2026-08-31T13:57:34+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/38ad98c8-51fd-4b50-a26a-495b58404817/subagents/agent-a5d9f2d638438cff5.jsonl"}, {"actor": "sub:frontend-developer", "item": "TDD 시나리오 (이어간 뒤 키보드 손이 없다)", "started": "2026-08-31T14:18:05+09:00", "ended": "2026-08-31T14:18:05+09:00", "result": "done", "transcript": ""}, {"actor": "sub:frontend-developer", "item": "009 구현 — 잠금을 `disabled` 에서 `aria-disabled` 로, 재그리기를 건너는 손", "started": "2026-08-31T14:30:02+09:00", "ended": "2026-08-31T14:30:02+09:00", "result": "done", "transcript": ""}, {"actor": "sub:frontend-developer", "item": "TDD 시나리오 (개정 — 구현 후 판정)", "started": "2026-08-31T14:30:19+09:00", "ended": "2026-08-31T14:30:19+09:00", "result": "done", "transcript": ""}, {"actor": "sub:frontend-developer", "item": "009 캡처 — 진짜 Enter 로 누른 직후(눌린 ▶ 에 포커스 링이 원 둘레에 남아 있다)와 재그리기 뒤.", "started": "2026-08-31T14:30:20+09:00", "ended": "2026-08-31T14:30:20+09:00", "result": "done", "transcript": ""}, {"actor": "sub:frontend-developer", "item": "곁가지 하나 더 (같은 결함의 나머지 절반)**: 잠금을 `disabled` 에서 `aria-disabled` 로 옮기자, 문", "started": "2026-08-31T14:33:27+09:00", "ended": "2026-08-31T14:33:27+09:00", "result": "done", "transcript": ""}]
project: section9
parent: REQ-20260830-049-62x6
relates_why: {}
tags: ["dashboard", "design", "index", "attached", "png", "image"]
created: 2026-08-31T13:57:25+09:00
status_since: 2026-08-31T15:07:54+09:00
updated: 2026-08-31T15:07:54+09:00
priority: 50
---

## Original

049 로 성공 경로의 창을 없애면서 드러난 자리. designer 판정(048 §3 A안)은 "포커스가 누른 버튼에 남고 접근명이 「이어가기」→「이어가는 중…」→「중단하기」로 바뀌는 것이 직접 조작의 정석"이라 했는데, 지금 구현은 그렇지 않다.

CDP 실측 (2026-08-31, http://127.0.0.1:9919 · Windows Chrome --remote-debugging-port):
  focusedBefore=True · name0=이어가기 → name1=이어가는 중… · disabled=True · activeAfter=BODY

뿌리는 card.js `faceDeed`(131행)의 `b.disabled = going` 이다. 눌린 버튼을 disabled 로 만들면 브라우저가 포커스를 걷어 body 로 보내고, 이어서 `refreshCatalog(true)` 가 카드를 다시 그려 그 버튼 개체 자체를 지운다. 접근명은 바뀌지만 disabled 요소의 이름 변경은 대개 보조기술에 통지되지 않는다.

049 가 만든 결함은 아니다 — 종전에도 창이 열릴 때 `document.activeElement` 는 이미 body 였고(dialog.js:175 `const back = document.activeElement`), 닫을 때 돌려보낸 자리도 body 였다. 다만 창이 사라진 지금은 이것이 **유일한 경로**라, 키보드 손에게는 아무 landmark 도 남지 않는다.

고칠 방향(둘 중 하나):
  ① disabled 대신 `aria-disabled` + 이미 있는 `wokePending` 연타 가드로 눌림을 막는다 — 포커스가 버튼에 남고 이름 변경이 통지된다.
  ② 재그리기 뒤 같은 문서의 손잡이(`[data-wake="id"],[data-stop="id"]`)를 찾아 포커스를 되돌린다.
①이 근원에 가깝고 ⏸(paintStop)·다른 .deed 손잡이에도 함께 듣는다. 다만 `faceDeed` 는 벨트의 공용 부품이고 designer 가 REQ-20260831-005/006 으로 그 영역을 재작업 중이라, 그 작업과 순서를 맞춰야 한다.

## Notes

### 2026-08-31T13:57:34+09:00 subagent (agent: sub:subagent:a5d9f2d6, by nicehugepark)

Confirming REQ-20260831-009-62x6 registration

### 2026-08-31T14:18:05+09:00 tdd (agent: sub:frontend-developer, by nicehugepark)

### TDD 시나리오 (이어간 뒤 키보드 손이 없다)

뿌리 실측(049 의 CDP): `faceDeed` 가 눌린 단추를 `disabled` 로 만들어 브라우저가 포커스를 걷고(→BODY), 이어서 `refreshCatalog(true)` 가 카드를 다시 그려 그 단추 개체 자체를 지운다. 잠금은 원래 `wokePending`/`stopPending` 이 지고 있으므로 `disabled` 는 **연타 방지에 필요하지 않다** — 시각·접근성만 축내고 있다.

- [ ] S1 눌린 직후 자리: ▶ 에 포커스를 주고 누르면 `document.activeElement` 가 여전히 그 손잡이다(BODY 가 아니다) — 049 에서 잰 실패값(activeAfter=BODY)의 역
- [ ] S2 이름이 통지된다: 접근명이 「이어가기」→「이어가는 중…」으로 바뀌고 그 요소가 여전히 포커스 가능하다(`disabled` 가 아니라 `aria-disabled=true`)
- [ ] S3 연타는 여전히 막힌다: 눌린 상태에서 다시 눌러도 `/api/wake` 가 두 번 나가지 않는다 — 잠금을 지는 것은 `wokePending` 이고, 그 관문은 그대로다
- [ ] S4 재그리기를 건너 자리가 남는다: 답이 온 뒤 카드가 다시 그려져도 포커스가 같은 문서의 벨트 손잡이(▶→⏸ 로 바뀐 그 자리)에 남는다
- [ ] S5 한 부품이 두 손잡이를 칠한다: 중단하기(paintStop)도 같은 `faceDeed` 를 지나므로 ⏸ 에서도 같은 결과다 — 갈래를 새로 만들지 않는다
- [ ] S6 그린 얼굴은 그대로: `busy` 클래스·툴팁·글리프가 종전과 같고, 벨트의 과녁·칠 계약(`ThePaintIsNotTheTarget` 6건)이 깨지지 않는다
- [ ] S7 되돌아온 자리만 되돌린다: 포커스 복원은 **눌린 그 손잡이가 포커스를 갖고 있었을 때만** 한다 — 남의 자리를 빼앗지 않는다
- [ ] S8 실브라우저(CDP): 실제 Chrome 에 Tab 으로 닿아 Enter 로 누르고, 눌린 직후와 재그리기 뒤의 `activeElement` 를 둘 다 잰다

### 2026-08-31T14:30:02+09:00 response (agent: sub:frontend-developer, by nicehugepark)

### 009 구현 — 잠금을 `disabled` 에서 `aria-disabled` 로, 재그리기를 건너는 손

**뿌리**: 잠금을 지는 것은 원래 `wokePending`/`stopPending` 이지 `disabled` 가 아니었다 — 누르는 자리 둘(`wakeDoc`·`stopDoc`)이 그 관문을 먼저 지난다. 그런데 `faceDeed` 가 `disabled` 도 함께 걸어, 브라우저가 눌린 단추에서 포커스를 걷어 body 로 보냈다. 필요 없는 속성 하나가 키보드 손을 떨어뜨리고 있었다.

**고친 자리** (전부 근원 한 곳씩)
- `web/app/card.js` `faceDeed` — `b.disabled = going` → `aria-disabled` 켜고/끄기. 그린 얼굴 쪽 4곳(`stopBtnHTML`·`wakeBtnHTML`·`driftBtnHTML`·idle stop)은 새 상수 `DEED_BUSY` 하나를 읽는다 — 칠하는 얼굴과 그린 얼굴이 같은 말을 하게.
- `web/css/actions.css` — `.acts button.wake[disabled]`·`.stop[disabled]` 선택자를 `[aria-disabled="true"]` 로. 옛 표시를 보고 있으면 눌린 얼굴이 통째로 사라진다.
- `web/app/card.js` `keepDeedFocus`·`deedHandle` 신설 — 재그리기가 손잡이 개체를 갈아 끼우므로 `aria-disabled` 만으로는 그 강을 못 건넌다. 규칙 셋: ① 내 자리였을 때만 되돌린다 ② 살아남았으면 아무 일도 안 한다 ③ **창이 서 있으면 손은 창의 것이다**(뒤의 카드로 포커스를 끌어오지 않는다). `wakeDoc` 의 `refreshCatalog(true)` 를 이것으로 감쌌다.

**실브라우저 실측** (CDP·Windows Chrome, 진짜 Tab 포커스 + 진짜 Enter 키 이벤트; `/api/wake` 로 나가는 소켓 한 겹만 막았다 — 라이브 저장소에 워커가 앉는 것을 피하려고. 서버 계약 자체는 tests/ 가 진짜 `wake_request` 로 잰다)

    누르기 전   name=이어가기, focused=true
    눌린 직후   name=이어가는 중…, aria-disabled=true, **domDisabled=false, active=SAME HANDLE**
    연타        Enter 두 번에 나간 요청 1건 — 관문은 그대로
    재그리기 뒤 oldHandleAlive=false, **landedOnHandle=true**, windowOpen=false
    남의 자리   다른 요소에 포커스가 있으면 stayedPut=true — 빼앗지 않는다
    ⏸ 갈래     같은 결과(active=SAME HANDLE) — 한 부품(faceDeed)이 두 손잡이를 칠한다

049 에서 잰 실패값이 `activeAfter=BODY` 였으니 그 자리가 뒤집혔다. 콘솔 오류 0.

**시험**: `tests/test_wake_handle.py::ThePressedHandKeepsItsPlace` 7건 신설(잠금이 포커스를 안 걷음 · 표시가 한 곳 · CSS 가 새 표시를 봄 · 연타 관문 불변 · 재그리기를 건넘 · 내 자리만 되돌림 · 창이 서면 물러섬). 기존 `test_stall_pair` 의 "되돌아온 ▶ 가 잠겨 있나" 판정은 `aria-disabled` 로 고쳐 조였다 — 종전 문자열 `"disabled"` 는 `aria-disabled` 에도 걸려 판정이 흐려진다. 표적 회귀 284건 통과(echo $? 0).

**남긴 한계**: 중단하기(⏸)는 성공해도 창이 서므로 손은 창으로 간다. 그 창을 닫을 때 s9dlg 가 돌려보낼 자리(`back`)는 이미 재그리기로 사라진 뒤라 포커스가 body 로 떨어진다 — 이 앱의 모든 창이 공유하는 자리(dialog.js:270 `back.isConnected`)라 깨우기만의 결함이 아니고, 고치려면 dialog.js 의 복원 규칙을 손대야 해서 이번 범위에 넣지 않았다.

### 2026-08-31T14:30:19+09:00 tdd (agent: sub:frontend-developer, by nicehugepark)

### TDD 시나리오 (개정 — 구현 후 판정)

- [x] S1 눌린 직후 자리: CDP 실측 `active=SAME HANDLE` — 049 에서 잰 `activeAfter=BODY` 의 역
- [x] S2 이름이 통지된다: 「이어가기」→「이어가는 중…」, `domDisabled=false` · `aria-disabled=true`
- [x] S3 연타는 여전히 막힌다: 진짜 Enter 두 번에 `/api/wake` 1건 — 관문(`wokePending`)은 그대로
- [x] S4 재그리기를 건너 자리가 남는다: `oldHandleAlive=false` 인데 `landedOnHandle=true`
- [x] S5 한 부품이 두 손잡이를 칠한다: ⏸(`data-stop`)에서도 `active=SAME HANDLE`
- [x] S6 그린 얼굴은 그대로: `ThePaintIsNotTheTarget` 6건 포함 표적 회귀 284건 통과, CSS 선택자를 새 잠금 표시로 옮겨 눌린 얼굴이 사라지지 않음을 캡처로 확인
- [x] S7 되돌아온 자리만 되돌린다: 다른 요소에 포커스가 있으면 `stayedPut=true`
- [x] S8 실브라우저(CDP): 진짜 Tab 포커스 + 진짜 Enter 키 이벤트로 눌린 직후와 재그리기 뒤를 둘 다 측정, 콘솔 오류 0

### 2026-08-31T14:30:20+09:00 response (agent: sub:frontend-developer, by nicehugepark)

009 캡처 — 진짜 Enter 로 누른 직후(눌린 ▶ 에 포커스 링이 원 둘레에 남아 있다)와 재그리기 뒤.

[Image: assets/REQ-20260831-009-62x6/009-cdp-pressed.png]
[Image: assets/REQ-20260831-009-62x6/009-cdp-redrawn.png]

### 2026-08-31T14:33:27+09:00 response (agent: sub:frontend-developer, by nicehugepark)

**곁가지 하나 더 (같은 결함의 나머지 절반)**: 잠금을 `disabled` 에서 `aria-disabled` 로 옮기자, 문서 머리 행동 띠(`.dacts`)의 「중단해 두기」가 눌린 티를 잃었다 — 그 버튼은 `.acts` 가 아니라 `.dacts` 안에 살아서 눌린 얼굴 규칙이 원래 닿지 않았고, 그동안은 `disabled` 가 주던 브라우저 기본 흐림이 그 자리를 대신하고 있었다. 규칙에 `.dacts` 갈래를 명시해 되돌렸다(web/css/actions.css) — 새 얼굴을 더한 것이 아니라, 빌려 쓰던 기본값을 이름 있는 규칙으로 옮긴 것이다. 표적 회귀 97건 통과.

## History
- 2026-08-31T13:57:25+09:00 created by nicehugepark (status: open)
- 2026-08-31T14:17:04+09:00 status: open -> in-progress (by nicehugepark) — 049 에서 CDP 실측으로 드러난 파생 결함 착수 — ▶ 를 눌러 이어간 뒤 포커스가 body 로 떨어진다. designer 의 005/006 벨트 재작업이 끝나 faceDeed 영역 순서 제약이 풀렸다. 방향 ①: disabled 대신 aria-disabled + 기존 wokePending/stopPending 연타 가드
- 2026-08-31T15:07:54+09:00 status: in-progress -> done (by nicehugepark) — goal 충족 근거: CDP 실측(진짜 Tab+Enter) — 누른 직후 포커스가 같은 손잡이에 남고(active=SAME HANDLE·domDisabled=false), 재그리기 뒤 손잡이로 복귀(landedOnHandle=true), 연타 2회에 요청 1건, 남의 자리 불변. disabled→aria-disabled 전환으로 ⏸·다른 .deed 에도 공통 적용. wake_handle 신설 14건 포함 인접 7스위트 리드 재실측 exit 0, 전체 748건 실패 0. 기계 검증 완료라 review 없이 닫음(verify-then-done)
