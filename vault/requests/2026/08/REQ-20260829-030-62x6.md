---
id: REQ-20260829-030-62x6
type: request
title: 자리를 사람에게 말한다
summary: 작업자가 워크트리 대신 본 저장소로 간 이유·대기 중인 이유가 카드와 창에 보인다
goal: 스폰이 자리를 바꾸거나 대기하면 그 사실과 이유가 화면에 보이고, 사람이 무엇을 하면 풀리는지 안다
status: in-progress
size: S
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: 911949e0
sessions: ["911949e0", "e42ebecc"]
agents: ["sub:designer:a8222a3d", "sub:subagent:a1a2f01b", "lead:claude-opus-5", "sub:subagent:a5fdef34"]
contributions: [{"actor": "sub:designer:a8222a3d", "item": "030 재작업 화면 몫", "started": "2026-08-30T00:38:09+09:00", "ended": "2026-08-30T00:59:19+09:00", "result": "stalled", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/911949e0-5d1b-4cd2-b08d-70fd0415a19a/tasks/a8222a3d67a8e02d4.output", "reason": "188초째 진전 없음 (기준 180초)"}, {"actor": "sub:subagent:a1a2f01b", "item": "Reading REQ-20260829-030-62x6.json file lease", "started": "2026-08-30T00:39:44+09:00", "ended": "2026-08-30T00:39:44+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/911949e0-5d1b-4cd2-b08d-70fd0415a19a/subagents/agent-a1a2f01b66c31d275.jsonl"}, {"actor": "lead:claude-opus-5", "item": "`[2026-08-30 00:44:56 KST - lead]`", "started": "2026-08-30T00:44:57+09:00", "ended": "2026-08-30T00:44:57+09:00", "result": "done", "transcript": ""}, {"actor": "sub:subagent:a5fdef34", "item": "Writing designer note to REQ-20260829-030-62x6", "started": "2026-08-30T01:06:40+09:00", "ended": "2026-08-30T01:06:40+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/911949e0-5d1b-4cd2-b08d-70fd0415a19a/subagents/agent-a5fdef3453e25082f.jsonl"}]
project: section9
parent: REQ-20260829-028-62x6
relates_why: {}
tags: ["index", "dashboard", "sync", "attached", "png", "image", "design", "docs", "terminal"]
created: 2026-08-29T17:06:04+09:00
status_since: 2026-08-29T22:37:09+09:00
updated: 2026-08-30T01:07:14+09:00
priority: 50
---

## Original

028 설계의 ⑥ 표출 몫 중 `web/index.html` 에 해당하는 부분. 부모에서 갈라 낸 이유는 그 설계가 세운 규칙 자체다 — `web/index.html` 이 지금 1,276줄 미커밋이고 다른 워크플로가 잡고 있어, 한 REQ 가 bin/s9 와 web/index.html 을 함께 만지면 서로를 덮는다. 이 설계의 구현이 이 설계의 규칙을 스스로 지키는 것이 첫 증명이다.

보일 것: 카드가 그 작업의 자리(본 저장소/워크트리)와 대기 사유를 말한다. 깨우기 창이 "누가 무엇을 잡고 있어 기다리는 중"을 사람 말로 답한다. 커밋하면 워크트리 격리가 돌아온다는 것을 읽을 수 있다.

## Notes

### 2026-08-29T20:34:18+09:00 workspace (by nicehugepark)

본 저장소에서 이어받는다 — 화면 작업이라 살아 있는 서버가 읽는 자리에서 해야 한다. 커밋하면 다음 라운드부터 작업자가 다시 자기 워크트리를 받는다.

### 2026-08-29T20:34:52+09:00 stop (by nicehugepark)

무인 작업자를 세웠다 (pid 450334, 세션 911949e0) — 리드의 designer 가 이미 그 파일(web/index.html)을 쓰고 있다 — 겹쳐 뜬 것이다 (REQ-20260829-016-62x6 의 구멍)

### 2026-08-29T20:40:51+09:00 designer (by nicehugepark)

### 무엇이 달라졌나

**카드가 자리를 말한다.** in-progress 카드의 메타 줄에 `본 저장소` / `워크트리` 한
마디가 선다 — 서버가 행에 실어 준 `workspace{kind,reason,wt,at}` 를 옮길 뿐이다.
손을 얹으면 사유와 푸는 법이 한 문장으로 나온다("…본 저장소에서 돈다 — 모두가 쓰는
파일이 아직 커밋되지 않아…. 커밋하면 다음 작업자부터 다시 워크트리로 간다").
문서 화면도 **같은 함수**를 불러 제목 줄에 같은 마디를 세운다.

**값이 없으면 아무것도 안 그린다.** 새 코드로 스폰이 아직 없는 문서에는 서버가 키를
안 싣는다 — 빈 칸도 "미상"도 두지 않았다. 오늘 이 저장소의 in-progress 아홉 건이
전부 그 상태라, 평소 판은 어제와 똑같이 보인다.

**커밋하면 격리가 돌아온다는 말은 헤더 칩 하나가 한다.** 손이 드는 자리(미커밋
코드·워크트리 쌓임)가 하나라도 있으면 브랜드 행에 `◇ 본 저장소에서 N건` 이 서고,
누르면 사유별 문서 주소와 푸는 법이 창으로 열린다. 풀 것이 없으면 아무 말도 없다.

**깨우기 창은 손대지 않는 것이 답이었다.** 화면은 지금도 `ok` 와 `message` 둘만
읽고, `ok=false` 는 `stop:false`(붉지 않은 창)로 선다 — 서버가 새로 더한
`action="waiting"` 은 그래서 이미 "차례를 기다린다"는 보통 창으로 나온다. 값을 읽기
시작하면 같은 말이 두 벌이 되므로 **읽지 않는 상태를 시험으로 못박았다**. 창을 짓는
자리만 `wakeDlg` 로 갈랐다(진단이 사람이 누를 때와 같은 함수를 부르게 하려는 것).

### 자리를 고른 근거 (기각한 대안)

- **카드에 새 줄** — 기각. "줄은 사람의 손을 요구하는 사실에만 준다"(REQ-20260827-017-62x6).
  자리는 읽고 나서 대개 할 일이 없고, 멈춤 줄·선행 대기 줄과 층위가 겹쳐 셋이 서면
  카드가 문장 벽이 된다.
- **모든 카드에 "커밋하면 풀린다"** — 기각. 그건 요청 한 건의 사정이 아니라 저장소의
  사정이다. 아홉 장에 같은 문장을 아홉 번 적으면 아홉 개의 사실이 아니라 배경이 된다.
  그래서 카드는 사실만, 할 일은 헤더 칩 하나.
- **툴팁만** — 기각. 손 없는 화면에서는 못 읽는다. 자리 낱말은 눈에 보이는 글자로,
  긴 설명만 손 위로.
- **메타 줄 맨 끝(태그 뒤)** — 기각. 좁은 열에서 태그와 함께 다음 줄로 밀려 `#태그`
  사이에 섞여 읽혔다. 지금은 단일 낱말 무리(보통·S·TDD) 뒤, 태그 앞이다.
- **문서 화면의 메타표 한 행** — 기각. 그 표는 문서 앞머리에 적힌 것들의 자리인데
  `workspace` 는 문서에 없는 값이다. 없는 필드가 있는 것처럼 보인다.
- **깨우기 창의 머리를 `action` 으로 갈라 쓰기** — 기각(위 참조).

### 색·재질

색면·세로 띠 없음. 자리는 mono 10px 글자 하나이고, 본 저장소만 한 급 진한 잉크
(`--muted`, 대비 6.4:1)로 둔다 — 낱말이 이미 둘을 가르므로 색이 유일한 단서는 아니다.
헤더 칩은 경고색을 쓰지 않는다(`sv-note` = 무채 잉크): 고장이 아니라 상태라, 옆의
`▲` 와 층위가 같아지면 둘 다 안 읽힌다.

### 진단 (진짜 함수를 부른다)

- `?ws` — in-progress 행에 사유 아홉 가지를 한 벌 얹는다. `?ws=main/dirty-spine`
  처럼 `kind/reason` 을 직접 줄 수도 있다. 자리 판정은 진단도 하지 않는다(서버 몫).
- `?ws&dlg=wslive` — **헤더 칩을 실제로 눌러** 창을 연다.
- `?dlg=wakewait` · `?dlg=wakespawn` — 서버가 줬을 답만 고정하고 창은 `wakeDlg`,
  즉 사람이 누를 때와 같은 함수가 짓는다.

### 시험

`tests/test_workspace_chip.py` **14건** 신규 — 없으면 안 그린다 · 도는 동안만 ·
낱말로 갈린다 · 사유와 푸는 법 · **서버의 사유 코드 전수 대조(bin/s9 에서 읽어 온다)** ·
모르는 코드도 안 무너진다 · 화면은 판정하지 않는다 · 저장소 사실은 한 번만 ·
할 일 없으면 침묵 · 두 화면 한 함수 · 줄이 아니라 칩 · 깨우기는 ok/message 만 ·
거절은 실패가 아니다 · 진단은 제 그림을 안 그린다.

따라간 기존 시험 둘: `test_wake_handle`(창 짓는 자리가 `wakeDlg` 로 갈렸다),
`test_stall_pair`(node 하네스에 `wsChip` 빈 스텁 — 자리 계약은 새 파일이 맡는다).

**전체 스위트 `Ran 2158 tests … OK (skipped=1)`, 410초. 회귀 0.**

### 남긴 것 · 못 한 것

- **카드는 "대기 중"을 말하지 못한다.** 대기 표식은 `state/auto_resume/wait/` 에만
  있고 카탈로그 행에 실리지 않는다 — 서버가 안 주는 것을 화면이 지어낼 수는 없다.
  대기는 계약대로 깨우기 창의 `message` 가 말한다. 카드에도 세우려면 서버가 행에
  실어 줘야 하고, 그건 bin/s9 몫이라 여기서 손대지 않았다.
- 오늘 이 저장소에는 진짜 `workspace` 값이 하나도 없다. 눈으로 본 것은 전부 `?ws`
  진단으로 세운 화면이고, 진짜 값이 실리는 것은 다음 스폰부터다.

[Image: assets/REQ-20260829-030-62x6/자리-카드-기본스킨.png]
[Image: assets/REQ-20260829-030-62x6/자리-카드-확대.png]
[Image: assets/REQ-20260829-030-62x6/자리-헤더칩-창.png]
[Image: assets/REQ-20260829-030-62x6/깨우기-대기-창.png]
[Image: assets/REQ-20260829-030-62x6/깨우기-자리를-말한다.png]
[Image: assets/REQ-20260829-030-62x6/자리-문서화면.png]
[Image: assets/REQ-20260829-030-62x6/자리-glass-다크.png]
[Image: assets/REQ-20260829-030-62x6/자리-terminal-라이트.png]

### 2026-08-29T20:49:29+09:00 commit (by nicehugepark)

커밋 ffb5257 — 클레임이 시계를 되감지 않고, 자리가 사람에게 보인다 (REQ-033·034·030·031·035)

### 2026-08-30T00:30:48+09:00 workspace (by nicehugepark)

본 저장소에서 이어받는다 — 화면 작업이라 살아 있는 서버가 읽는 자리에서 해야 한다. 커밋하면 다음 라운드부터 작업자가 다시 자기 워크트리를 받는다.

### 2026-08-30T00:36:11+09:00 stop (by nicehugepark)

무인 작업자를 세웠다 (pid 930234, 사람 911949e0) — 019 는 done 인데 작업자가 떠 bin/s9 를 잡고 대기 중인 일들을 막는다. 그 하나만 세우는 길이 막혀 있어(REQ-20260830-002-62x6) 전부 세운다 — 030 재작업은 리드가 designer 로 곧바로 다시 붙인다

### 2026-08-30T00:36:25+09:00 workspace (by nicehugepark)

본 저장소에서 이어받는다 — 화면 작업이라 살아 있는 서버가 읽는 자리에서 해야 한다. 커밋하면 다음 라운드부터 작업자가 다시 자기 워크트리를 받는다.

### 2026-08-30T00:39:44+09:00 subagent (agent: sub:subagent:a1a2f01b, by nicehugepark)

Reading REQ-20260829-030-62x6.json file lease

### 2026-08-30T00:43:49+09:00 response (by nicehugepark)

### 2차 반려 — "어떤 화면에서 확인하는지 설명을 봐도 모르겠다"

### 왜 못 찾았나 (셋이 겹쳤다)

① **낱말 하나는 태그로 읽힌다.** 1차는 카드 메타 줄에 「본 저장소」 넉 자만
   세웠다. 그 줄에는 이미 이름·급·크기·TDD·태그가 서 있어, 표 없는 낱말 하나는
   눈에 "또 하나의 태그"로 걸린다. 실제로 반려 시점 보드에 값이 붙은 카드가
   있었는데도(REQ-029) 사람이 못 찾았다.

② **손 위의 글은 찾은 사람에게만 열리는 문이다.** 사유·푸는 법을 전부 `title`
   귀띔에 뒀다. 못 찾았다는 반려에 귀띔은 답이 되지 못한다 — 이 화면은 같은
   값을 이미 한 번 치렀다(판정 창의 상태 이름을 귀띔에서 창 안 문장으로 내린
   REQ-20260828-007-62x6 반려).

③ **1차 확인 포인트가 없는 것을 누르라고 했다.** "오른쪽 위 ◇ 칩을 누르면"이라
   적었는데, 헤더 칩은 **사람 손이 드는 자리가 있을 때만** 선다(설계 그대로다).
   오늘 실제 사유는 전부 `live-verify` 라 풀 것이 없어 칩이 조용했다 — 사람은
   없는 것을 찾다 시간을 썼다. 화면 결함이 아니라 **내 안내문의 결함**이다.

### 고친 것

- **표를 붙였다.** 카드 칩이 `◇ 본 저장소` 가 된다. 표는 헤더 칩과 **같은 ◇** 다 —
  「◇ 본 저장소」와 「◇ 본 저장소에서 2건」이 한 가지를 말한다는 것을 표 하나가
  잇는다(표를 둘로 나누면 사람이 배울 것이 둘이 된다). 색면·테두리는 그대로 없다.
- **누를 수 있게 했다.** 칩을 누르면 그 카드 한 장의 자리가 창으로 열린다
  (`wsOpen`). 창은 세 줄이다 — 왜 그 자리인지 · **그래서 지금 이 화면에서
  확인되는지**(WS_MEANS) · 무엇을 하면 풀리는지(있을 때만). 귀띔은 그대로 두었다:
  빠른 쪽은 여전히 얹기만 하면 된다.
- **"그래서 나에게 무슨 뜻인가"를 새로 세웠다.** 자리 이름과 사유는 왜 저기
  앉았는지만 말할 뿐, 내가 보고 있는 화면에서 그 작업을 확인할 수 있는지는
  말하지 않았다. 그게 이 요청이 애초에 세우려던 사실이라 문장으로 내렸다.
  헤더 창의 맺음말도 같은 상수를 쓴다 — 두 창이 다른 말을 하지 않는다.
- 누르는 길은 `[data-wsat]` 위임 한 곳이고 카드보다 **먼저** 잡는다(카드 안의
  손잡이는 카드가 아니다 — 깨우기·세우기가 세운 규칙). 보드 카드와 문서 화면
  제목 줄의 같은 칩이 그 한 길로 들어온다. 키보드는 이 화면에 이미 있는
  `role="button"` → Enter/Space 핸들러가 그대로 집는다.
- 진단 `?dlg=wsat` 추가 — 그림을 짓지 않고 **칩을 실제로 누른다**(`?dlg=wslive`
  가 낸 선례). 손 없는 환경에서 눌린 화면은 이 길로만 남는다.

### 손댄 파일

`web/app/card.js`(WS_MARK·WS_MEANS·wsChip·wsOpen) · `web/app/events.js`(위임 한 줄) ·
`web/app/diag.js`(?dlg=wsat) · `web/css/board.css`(.wsm 표·hover·focus 링) ·
`tests/test_workspace_chip.py`.

### 시험

`tests/test_workspace_chip.py` 14 → **19건**. 새로 넷: 표가 카드와 헤더에 **같은
글자**로 선다 · 칩이 눌리고 카드보다 먼저 잡힌다 · 누르면 **그 한 건**을 말하고
자리에 따라 뜻이 갈린다 · 값이 없으면 **누른 뒤에도** 창을 안 연다.

### 눈으로 본 것

살아 있는 서버(9909)에서 넷을 찍었다 — 진짜 값이 붙은 카드(REQ-029)의
`◇ 본 저장소` · 칩을 눌러 열린 창(`?ws=main/dirty-overlap&dlg=wsat`) ·
헤더 칩 창(`?ws&dlg=wslive`) · 문서 화면 제목 줄의 같은 칩.

### 남긴 것 · 제안 (범위 밖이라 손대지 않음)

- **`live-verify` 는 헤더 칩을 세우지 않는다.** 오늘 도는 작업자는 전부 이
  사유라, 사람이 손댈 것이 없어 헤더가 조용한 것이 맞다. 다만 그 탓에 이 기능을
  **찾을 실마리가 카드 칩 하나뿐**인 날이 있다. 값이 붙은 카드가 적은 날에는
  `?ws` 진단이 유일한 전수 확인 길이다.
- **카드는 여전히 "대기 중"을 말하지 못한다.** 대기 표식은
  `state/auto_resume/wait/` 에만 있고 카탈로그 행에 실리지 않는다. 행에 실어
  주는 것은 `bin/s9 catalog_with_live` 몫이라 이 라운드 범위(web·tests) 밖이다.
- **캡처 중 한 번, 화면 조각 둘이 `$ is not defined` 로 죽는 것을 봤다**
  (`app/docs.js` · `app/events.js`, oops 배너). 바로 다음 캡처부터 재현되지 않는다
  — `$` 는 `app/state.js` 에서 세 번째로 실리므로 적재 경합으로 보인다. 이
  요청의 코드와는 무관하고(내 변경은 전부 핸들러 안이다) 조각 나누기·oops
  쪽 사안이라 손대지 않았다. 재현되면 REQ-027·036 계보로 따로 세울 일이다.

[Image: assets/REQ-20260829-030-62x6/ws2-b3.png]
[Image: assets/REQ-20260829-030-62x6/ws2-dlg.png]
[Image: assets/REQ-20260829-030-62x6/ws2-hdr.png]
[Image: assets/REQ-20260829-030-62x6/ws2-doc.png]
[Image: assets/REQ-20260829-030-62x6/ws2-probe.png]

### 2026-08-30T00:44:57+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-08-30 00:44:56 KST - lead]`

전체 스위트 완료 알림을 기다립니다. 결과가 오면 확인 후 review로 전이하겠습니다.

### 2026-08-30T01:06:40+09:00 subagent (agent: sub:subagent:a5fdef34, by nicehugepark)

Writing designer note to REQ-20260829-030-62x6

### 2026-08-30T01:07:14+09:00 designer (by nicehugepark)

### 먼저 센 것 — 이 표시는 오늘 몇 개나 서는가

`/api/catalog` 실측: **진행 중 7건 중 `workspace` 가 실린 것 2건**
(REQ-029·REQ-030, 둘 다 `main`/`live-verify`). 그리고 **반려 시각 22:37 에는 0건**
이었다 — 029 에 값이 붙은 것이 23:07, 030 이 00:36 이다. 사용자는 안내를 못 읽은
것이 아니라 **볼 것이 없는 판을 봤다.** 반려문의 절반이 여기서 설명된다.

다만 "거의 모든 날에 안 보이는 기능"은 아니다. 서버는 스폰이 일어날 때마다 그
문서에 이 값을 싣는다 — 기능이 산 지 두 시간이라 2건이고, 스폰이 쌓이면 진행 중
카드의 대부분이 갖는다. 그래서 **없는 값을 화면이 지어내는 쪽으로 가지 않았다.**

### 값이 없을 때 무엇을 할 것인가 (결정과 기각한 대안)

**결정: 그리지 않는 규율은 그대로 두고, 이미 서 있는 표시를 "찾을 수 있게" 만든다.**

- **기각 — 서버가 안 주면 화면이 저장소 사정으로 대신 말한다.** 자리는 서버의
  `workspace_decision` 하나가 정한다. 화면이 미커밋 여부를 보고 "아마 본 저장소"
  라고 말하기 시작하면 같은 판정이 두 벌이 되고, 그때부터 한 벌만 고쳐진다 —
  이 저장소가 판정 버튼·멈춤 술어에서 이미 세 번 밟은 실패다.
- **기각 — 진단으로만 두고 카드에서 뺀다.** 사람이 못 보는 기능은 없는 기능이다.
  2건이 이미 실데이터로 서 있는데 그것을 치우면 남는 것은 진단 파라미터뿐이다.
- **기각 — 헤더 칩을 늘 세운다.** 오늘 두 건의 사유(`live-verify`)에는 사람이 할
  일이 없다. 할 일 없는 자리에 상시 칩을 세우면 진짜 손이 필요한 날 안 읽힌다
  (018 이 이 칩에 세운 규칙 그대로). 헤더의 침묵은 유지한다.

### 무엇을 바꿨나 — 표시가 아니라 **재질**이 문제였다

2차는 표(◇)를 붙이고 누를 수 있게 만들었지만 **재질은 메타 줄의 어휘 그대로**였다.
mono 10px 흐린 글자는 옆의 `#태그`·`보통`·`S` 와 같은 급이라, 좁은 열에서 태그 줄로
밀려 내려가면 `#index` 바로 옆에 붙는다 — 560px 캡처에서 실제로 태그로 읽혔다.
표는 "무엇이 있다"까지고 "눌러 보라"가 아니다.

1. **칩에 점선 밑줄을 준다** (`web/css/board.css` `.wsat`). 이 화면에는 그 말을
   하는 문법이 이미 있다 — doclink, 그리고 같은 이유로 같은 밑줄을 쓰는 문서
   뷰어의 등급 낱말(`.prio.pfull .pname`). 칩이 그 셋째가 된다. 메타 줄에서 밑줄을
   가진 것은 이 칩뿐이라 태그와 한눈에 갈리고, 사람이 새로 배울 것은 없다.
   색면·테두리는 여전히 없다(`border-bottom` 이 아니라 `text-decoration` 인 이유:
   재질은 같고 줄바꿈될 때도 밑줄이 따라간다).
2. **밑줄 색을 글자색에서 파생시킨다**(`color-mix(currentColor 60%)`). 처음엔
   `--hairline` 을 빌렸는데 glass/carbon 에서 배경에 잠겼다(실측). 파생이면 어느
   톤·어느 스킨에서도 같은 관계가 선다.
3. **누를 면적을 22px 로 키운다.** 10px 글자의 과녁은 13px 다. 인라인 세로 패딩은
   줄 상자를 안 넓히고, 가로로 벌어진 만큼은 음수 여백으로 되돌려 **옆 태그가 한
   픽셀도 안 밀린다.**
4. **창의 첫 줄을 질문의 답으로 바꾼다** (`wsOpen`). 사람이 이 칩을 누르며 품은
   질문은 "왜 저기 앉았나"가 아니라 **"그래서 이걸 어느 화면에서 확인하나"** 다 —
   반려문이 그대로 그 문장이었다. 2차는 사유가 먼저였다. 이제 `WS_MEANS`(고친 것이
   이 화면에 바로 나타나는가) 한 줄이 잉크로 먼저 서고, 사유·푸는 법이 뒤따른다.
   한 창의 강조는 하나다 — 답만 올리고 나머지를 흐리게 내리지는 않았다(본문 대비).

### 손대지 않은 것과 그 이유

- **`본 저장소`·`워크트리` 낱말은 그대로다.** 내부 용어인 것은 맞지만 `WS_PLACE` 는
  `tests/test_workspace_chip.py` 가 문자열로 못 박고 있고, 그 파일은 이번 라운드에서
  내 몫이 아니다. 대신 **사람이 알아야 할 것은 낱말이 아니라 뜻**이라고 보고, 그
  뜻을 창 첫 줄로 올렸다.
- **창 안 말투가 섞여 있다** — 제목·답은 합쇼체("돕니다/나타납니다")인데 사유·푸는
  법은 반말("…일하게 된다/…간다")이다. 손 위 귀띔이 반말 문장이라 같은 문자열을
  두 자리가 나눠 쓰는 탓이다. 합쇼체로 통일하려면 `WS_WHY` 를 고쳐야 하고 그러면
  `test_workspace_chip` W16(`"커밋되지 않았다"`)이 깨진다 — **다음 라운드 몫으로
  남긴다.** 못 찾는 것에 견주면 작은 흠이라 시험을 깨면서 고치지 않았다.

### 겪은 것 (리드가 알아야 할 것)

- 이 REQ 에 **작업자가 겹쳐 떴다**: 00:36:25 에 자동 재작업 워커(pid 938747)가
  스폰돼 `card.js`·`diag.js`·`test_workspace_chip.py` 를 고치고 스위트를 돌리던
  중이었고, 00:38:09 에 리드의 designer 가 붙었다. 겹친 동안에는 파일을 건드리지
  않고 화면 확인만 했고, 워커가 죽은 뒤(00:45) 그 결과 위에 이어 고쳤다.
  **워커가 남긴 것은 하나도 지우지 않았다.**
- **`bin/s9` 가 다른 사람 손에서 몇 분간 문법이 깨진 상태였다** — 전체 스위트 중
  `s9 user add` 가 `SyntaxError: unterminated string literal (line 14206)` 로
  죽었다. 지금은 파싱된다.

### 시험

`tests/test_ws_findable.py` **9건 신규** — 누를 수 있다는 재질이 있다 · 그 문법의
원래 자리(doclink·등급 낱말)가 살아 있다 · 태그는 그 밑줄을 안 쓴다 · 색면·테두리는
여전히 없다 · 밑줄 색이 글자에서 파생한다 · 과녁이 글자보다 크다 · 그만큼 되돌려
판을 안 민다 · 창의 첫 줄이 답이다 · 답 문장이 두 벌이 아니다.

기존 `tests/test_workspace_chip.py` 19건은 손대지 않고 그대로 통과한다.

**전체 스위트 `Ran 2285 tests in 487.380s`, 실패 2 — 둘 다 `bin/s9`(다른 사람의
진행 중 편집)이고 web/ 는 0.** 그중 `test_stream_switch` 는 재실행에서 통과,
`test_worker_stop.test_nothing_else_grew_a_kill` 은 `worker_stop` 본문이 아직
쓰이는 중이라 남았다(REQ-20260830-002-62x6 작업). 화면 관련 117건은 전부 OK.

[Image: assets/REQ-20260829-030-62x6/24-실데이터-보드.png]
[Image: assets/REQ-20260829-030-62x6/25-실데이터-누른창.png]
[Image: assets/REQ-20260829-030-62x6/20-자리칩-보드.png]
[Image: assets/REQ-20260829-030-62x6/22-자리칩-좁은폭.png]
[Image: assets/REQ-20260829-030-62x6/21-자리칩-누른창.png]
[Image: assets/REQ-20260829-030-62x6/13b-glass-dark.png]
[Image: assets/REQ-20260829-030-62x6/23-자리칩-터미널다크.png]

## History
- 2026-08-29T17:06:04+09:00 created by nicehugepark (status: open)
- 2026-08-29T20:08:28+09:00 status: open -> in-progress (by nicehugepark) — drag 이동 [via dashboard]
- 2026-08-29T20:50:03+09:00 status: in-progress -> review (by nicehugepark) — F5 후, 진행 중인 카드의 태그 줄을 보세요 — 그 작업이 어디서 도는지(본 저장소 / 워크트리)가 한 마디로 서고, 손을 얹으면 왜 그 자리인지와 무엇을 하면 풀리는지가 나옵니다. 문서 화면 제목 줄에도 같은 마디가 있습니다. 화면 오른쪽 위 ◇ 칩을 누르면 지금 손이 드는 것들이 한 창에 모입니다. 값이 없는 카드에는 아무것도 안 그립니다. 읽고 무엇을 하면 되는지 알겠으면 승인, 무슨 말인지 모르겠으면 반려입니다.
- 2026-08-29T22:37:09+09:00 status: review -> in-progress (by nicehugepark) — 반려: 이건 어떤 화면에서 확인할 수 있는지 설명을 봐도 잘 모르겠다. [via dashboard]
