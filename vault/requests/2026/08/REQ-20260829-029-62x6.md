---
id: REQ-20260829-029-62x6
type: request
title: 우선순위가 곧 순서다
summary: 화면에서 우선순위를 조절할 수 있고, 올린 것이 실제로 제일 먼저 실행된다
goal: 화면에서 우선순위를 바꾸면 s9 next·작업자 스폰이 그 순서를 따른다
status: done
size: M
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: 9854e394
sessions: ["911949e0", "26cf1dfc", "a5a0449c", "9854e394"]
agents: ["sub:designer:a848238e", "sub:backend-developer:a740027e", "lead:claude-opus-5"]
contributions: [{"actor": "sub:designer:a848238e", "item": "030·031 화면 몫", "started": "2026-08-29T20:09:58+09:00", "ended": "2026-08-30T10:22:10+09:00", "result": "failed", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/911949e0-5d1b-4cd2-b08d-70fd0415a19a/tasks/a848238e8b9856212.output", "reason": "기록 자리가 사라졌다 (transcript 경로가 없다)"}, {"actor": "sub:backend-developer:a740027e", "item": "`[2026-08-29 22:12:40 KST - backend-developer]`", "started": "2026-08-29T22:03:57+09:00", "ended": "2026-08-29T22:03:57+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/911949e0-5d1b-4cd2-b08d-70fd0415a19a/subagents/agent-a740027ef12761cdd.jsonl"}, {"actor": "lead:claude-opus-5", "item": "`[2026-08-30 10:34:00 KST - lead]`", "started": "2026-08-30T10:34:00+09:00", "ended": "2026-08-30T10:34:00+09:00", "result": "done", "transcript": ""}, {"actor": "lead:claude-opus-5", "item": "`[2026-08-30 11:10:36 KST - lead]`", "started": "2026-08-30T11:10:36+09:00", "ended": "2026-08-30T11:10:36+09:00", "result": "done", "transcript": ""}]
project: section9
parent: REQ-20260829-027-62x6
relates_why: {}
tags: ["dashboard", "workflow", "index", "attached", "png", "image"]
created: 2026-08-29T17:00:52+09:00
status_since: 2026-08-31T16:17:16+09:00
updated: 2026-08-31T16:17:16+09:00
priority: 90
---

## Original

원문(2026-08-29 16:51, 프롬프트 하나에 요청 둘이 섞여 있던 것을 분리): "그리고 지금 자주 수정을 하는 web/index 파일이 너무 비대한데, 모듈화를 해라. 그리고 우선순위 기능을 화면에서 조절할 수 있게 하고, 우선순위가 바뀌면 제일 최우선으로 실행될 수 있게 설계를 제대로 하고, 구현해줘."

이 문서는 뒤쪽 절반이다 — 우선순위. 앞쪽(모듈화)은 부모 REQ-20260829-027-62x6.

두 겹이다. ① 화면에서 우선순위를 조절하는 손잡이 ② 바뀐 우선순위가 실제 실행 순서를 바꾸는 것.
②가 없으면 ①은 숫자놀음이다 — s9 next 와 무인 작업자 스폰이 그 값을 실제로 읽어야 한다.

## Notes

### 2026-08-29T20:09:11+09:00 decision (by nicehugepark)

착수 지시 접수. 순서를 뒤에 둔다 — 뒤쪽 절반(바뀐 우선순위가 실제 실행 순서를 바꾼다)이 `s9 next` 와 스폰 관문이라, 지금 그 자리를 쓰고 있는 033·034 와 같은 파일이다. 그 둘이 닫히는 대로 착수한다.

### 2026-08-29T22:03:57+09:00 subagent (agent: sub:backend-developer:a740027e, by nicehugepark)

`[2026-08-29 22:12:40 KST - backend-developer]`

### 결론

멈춤 판정을 `stall_verdict()` 한 자리로 모으고 다섯 상태로 갈랐다. 그리고 **원인이 요청서의 진단과 달랐다** — "서브에이전트가 등록을 안 한다"가 아니라 **엉뚱한 문서에 등록한다**였다.

### 실측으로 밝힌 진짜 원인

`vault/sessions/2026/08/SES-20260829-027-62x6.md` 136행:

```
20:09:58 위임 등록: REQ-20260829-029-62x6 ← sub:designer:a848238e (클레임)
```

그런데 REQ-029 의 기여 항목 이름이 `"item": "030·031 화면 몫"` 이다. `s9-audit-agent` 가 **정식 id 정규식만** 봐서 description 의 `030·031` 이 하나도 안 잡혔고, prompt 의 배경 id 는 여럿이라 "유일하지 않으면 고르지 않는다"에 걸려 클레임 경로로 물러났다. 세션 active_reqs `[027,024,025,029]` 중 최근 것이 뽑혔다.

**한 오귀속이 사고 둘을 낳는다.** 030 은 손이 안 보여 거짓 멈춤 + 겹친 스폰(요청서 ①②), 029 는 **없는 손이 보여** 진짜 멈춤이 가려졌다 — 요청서가 몰랐던 거울상이다.

### 어떤 신호를 진전으로 읽기로 했나

**"어느 문서인지 아는 손"만 진전이다.** `live_agents()`(바인딩 transcript mtime)는 이미 있었지만 **세션 단위**라 어느 요청인지 말하지 못한다. 그대로 진전으로 읽으면 리드 세션이 사는 동안 모든 경보가 꺼진다 — REQ-034 가 고친 병 그대로다. 그래서 손에 행선지를 붙였다(`agent_req = {transcript: REQ-id}`, 값 셋: 확정·추정(`""`)·없음). 행선지가 없는 살아 있는 손은 `unknown` 을 만들고, `unknown` 은 화면에서 감추지 않되(`quiet_mins` 그대로) **스폰만 막는다**(거부가 아니라 대기). 넓게 잡히는 것은 알고 한 선택이다 — 거짓 멈춤의 대가는 일하는 파일 위의 두 번째 손이고, 거짓 미상의 대가는 2분의 유예다.

### 고친 것 (파일:함수)

- `/home/sjpark1/section9/bin/s9`
  - `delegation_targets()`·`cmd_delegate_target()` **[신규]** — 귀속 판정의 유일한 자리. 문서 번호 세 자리를 풀고, 리드가 둘을 적었으면 둘 다 낸다. 날짜·포트는 번호로 안 센다. 클레임 폴백은 `src="클레임"`(추측)으로 표시.
  - `stall_verdict()` **[신규]** — `moving`/`attached`/`waiting`/`unknown`/`stalled`. `stall_mins()` 는 얇은 껍데기로 남았다.
  - `unassigned_hands()`·`_attributed_transcripts()` **[신규]**, `live_agents()` 에 `req` 추가.
  - `_bind_agent_hand()` **[신규]** + `s9 claim --agent-transcript P`(id 생략) — 붙일 문서를 못 골라도 손은 등록한다.
  - `_claim_req(agent_req_guess=)`·`cmd_claim(--guess)`, `_wait_info()` **[신규]**, `catalog_with_live()`, `stalled_requests(states=)`, `cmd_stalled`, `_wake_refusal()`, `_spawn_worker()`.
- `/home/sjpark1/section9/bin/s9-audit-agent` — 훅 안의 정규식·후보 조회를 전부 `s9 delegate-target` 한 번으로 대체(s9 호출 6~7 → 4, 훅 제한 20초). 지명된 **모든** 문서에 등록.

### 등록 없이 남는 경로 — **셋** (닫지 못했다)

1. 하네스가 `agentId`/`output_file` 을 안 주는 형태로 바뀌면 `parse_agent_result` 가 빈손 — 손도 문서도 못 남긴다. 하네스 계약 의존이라 여기서 닫으면 옆문이다.
2. `S9_AUDIT=off` 또는 훅 미설치 — 같은 이유로 실명.
3. 훅 타임아웃 — 호출 수를 줄여 여지를 넓혔지만 남는다.

닫은 것: 번호 지명 · 복수 지명 · "대상을 못 골랐을 때 손까지 사라지던 것" · "추측을 확정처럼 적던 것".

### 화면 몫으로 넘길 계약 (`web/index.html` 은 안 건드렸다)

`GET /api/catalog` in-progress 행에 `stall_state`("moving"|"attached"|"waiting"|"unknown"|"stalled") · `stall_why` · `quiet_mins` · `wait_kind`("inflight"|"held"|"hands") · `wait_why` · `wait_mins`. **`stalled_mins` 의 뜻이 좁아졌다** — `stall_state=="stalled"` 일 때만 값이 있다. `POST /api/wake` 의 `action` 에 `waiting`·`unknown` 추가.

### 시험

신규 `/home/sjpark1/section9/tests/test_stall_trust.py` 13건 · `tests/test_delegation_target.py` 8건 추가(D7 이 실사고 재현) · `test_stalled_claim.py`·`test_wake_reach.py` 2건 갱신. **전체 `python3 tests/` → Ran 2214 tests in 450s — OK (skipped=2)**.

### 리드가 대신 넣어 달라 한 패치 (REQ-027, 내 범위 밖)

`do_GET` 에 `/css/*.css`·`/app/*.js` 라우트를 넣었다(html 갈래와 같은 realpath 규율, 정확한 MIME). **캐시는 안 물렸다** — `_send()` 가 이미 `no-store` 이고, 조각은 자주 바뀌어 ETag 로 물리면 "고쳤는데 F5 해도 옛 화면"이 된다. `SPINE_UNDER = ("web/css/", "web/app/")` 도 넣었다(리드 판단이 맞다 — 로직이 조각으로 옮겨 갔는데 등뼈는 옛 한 파일만 셌다). `tests/ web_split` 13건 OK, 껍데기가 참조하는 조각 **38개 전부 200**, `s9 shot` 으로 보드가 그려지는 것 확인. 기록은 `s9 note REQ-20260829-027-62x6 --label backend-developer`.

### 리드가 알아야 할 것 둘

1. **기준선이 깨끗하지 않았다.** 착수 시점에 무인 작업자 pid 506116 이 REQ-024 로 `bin/s9` 를 고치고 있었고 리스도 잡고 있었다(`state/file_leases/REQ-20260829-024-62x6.json`). 물러나지 않고 앵커 치환만 썼고, 마칠 때 그쪽 훅 넷(`stop_request`·`stop_all_workers`·`worker_stop(owner=)`·`restart_session(stop_workers=)`)이 그대로 있는 것을 확인했다. **커밋할 때 두 작업의 변경이 한 diff 에 섞여 있다.** 기록은 REQ-036 의 `--label conflict` 노트에.
2. **화면 몫 하나**: `s9 shot` 캡처에서 in-progress 의 025·029 카드에 `멈춤 …` 줄이 없다. 서버는 두 행에 `stalled_mins`(113)를 그대로 싣는다 — 조각으로 가르면서 그 줄을 그리는 자리가 빠졌는지 REQ-027 쪽에서 확인이 필요하다.

### 못 한 것

`unknown` 은 모든 in-progress 요청에 걸린다 — 손의 행선지가 미상이라 좁힐 근거가 없다. transcript 본문을 읽어 REQ 언급을 찾으면 좁힐 수 있지만 매 판정마다 큰 파일을 읽는 값이라 안 했다. 손의 신선도 창(2분)이 천장이다. `workspace_decision`·`lease_*` 는 지시대로 읽기만 했다. 상태 전이는 하지 않았다.

### 2026-08-29T23:07:17+09:00 workspace (by nicehugepark)

본 저장소에서 이어받는다 — 화면 작업이라 살아 있는 서버가 읽는 자리에서 해야 한다. 커밋하면 다음 라운드부터 작업자가 다시 자기 워크트리를 받는다.

### 2026-08-30T10:34:00+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-08-30 10:34:00 KST - lead]`

구현과 검증을 마쳤고, 전체 테스트 스위트만 아직 도는 중입니다. 지금까지의 상태를 정리해 드립니다.

**한 일 (REQ-20260829-029-62x6 — 우선순위가 곧 순서다)**

이 요청은 두 겹인데, 뒤쪽 절반(②: 바뀐 값이 실제 실행 순서를 바꾼다)은 이미 서 있었습니다 — `work_order()` 가 `s9 next`(이어받기)와 무인 작업자 스폰 순서를 이미 정하고 있었습니다. 비어 있던 것은 앞쪽 절반(①: 화면에서 조절하는 손잡이)이었습니다. 값을 바꾸는 길이 hovercard가 가르치는 `s9 set … --priority high` 하나뿐이라, 순서를 정하는 판은 보드인데 정하는 자리는 터미널에 있었습니다.

- `web/app/const.js` — 표기가 곧 손잡이입니다. 등급 낱말(`보통`/`높음`/`긴급`)이 `<span>`에서 `<button data-prioset>`로 바뀌어 카드·Docs 목록·문서 뷰어 세 곳 다 눌립니다(한 함수라 갈릴 수 없습니다).
- `web/app/dialog.js` — `prioSet()`·`postPriority()`. 새 컴포넌트를 만들지 않고 이 화면에 있는 고르기 창(`s9dlg kind:"choose"`)을 씁니다. 확인 단계는 없습니다 — 되돌리기가 한 번 더 누르는 것뿐이라. 창 머리·제목은 판정 창과 같은 `dlgFor()` 로 짓습니다(조사까지 그 함수가 계산합니다).
- `web/app/events.js` — 문서 열기보다 **먼저** 잡고 전파를 끊습니다.
- `web/css/board.css` — 버튼 기본 옷을 벗겨 여태 보이던 글자 그대로입니다. 밑줄은 손이 얹혔을 때만 돋습니다.
- `bin/s9` — `apply_priority()`(문 하나)·`set_doc_priority()`·`POST /api/priority`. CLI `--priority` 도 같은 문을 지나므로 화면으로 바꾼 것만 History에 안 남는 일이 생길 수 없습니다. **`updated` 는 안 건드립니다** — 그 시각이 멈춤 판정의 시계(`quiet_mins`)라, 카드를 끌어 올리는 것만으로 멈춤 경보가 꺼지면 안 됩니다(REQ-20260829-034가 클레임에 대해 막은 그 병의 대칭).

**검증**

- `python3 tests/ priority` 70건 OK, 신규 `tests/test_priority_handle.py` 30건 OK — 실제 서버를 세워 `/api/priority` 를 두드리는 끝단 3건 포함(값 변경이 색인까지 가는지, 잘못된 값·없는 문서가 이유를 달고 거절되는지).
- 캡처 2장: `scratchpad/prio-chooser2.png`(카드의 `보통` 을 진짜로 눌러 창이 열린 화면 — `?dlg=priolive` 진단을 diag.js에 더했습니다), `scratchpad/prio-viewer.png`(문서 뷰어 표기가 안 부풀었는지).
- 전체 스위트는 백그라운드에서 도는 중이고, 결과가 나오면 이어 보고드리겠습니다.

**한 가지 알려 드릴 것**: 이 라운드의 봉투는 수정 범위를 web/·vault/·tests/ 로 한정했지만, 「네 차례」가 `bin/s9` 를 명시로 배정했고 리스도 이 REQ가 그 파일을 쥐고 있어 서버 라우트를 직접 넣었습니다 — 그것 없이는 손잡이가 눌러도 아무 데도 닿지 않습니다. 그리고 서버 쪽 변경이라 대시보드 상단 배너의 `s9 serve --restart` 로 재기동해야 실제로 동작합니다.

### 2026-08-30T11:10:36+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-08-30 11:10:36 KST - lead]`

전체 스위트가 아직 도는 중입니다(약 8분 소요, `tail` 로 묶어 놓아 끝나야 출력이 나옵니다). 완료되면 결과를 확인하고 `--label response` 노트와 review 전이까지 마치겠습니다.

지금까지 상황을 다시 정리해 드립니다.

**끝난 것**
- ① 화면 손잡이: 카드·Docs 목록·문서 뷰어의 등급 낱말(`보통`/`높음`/`긴급`)이 버튼이 되어, 누르면 등급 넷을 고르는 창이 뜹니다. 창은 새로 만들지 않고 이 화면의 고르기 창을 그대로 씁니다.
- ② 순서 반영: 이미 서 있던 `work_order()` 가 `s9 next` 와 무인 스폰 순서를 정하고 있었고, 화면이 바꾼 값이 색인까지 가는 것을 끝단 테스트로 잠갔습니다.
- 문(門)은 하나입니다 — CLI `--priority` 와 `POST /api/priority` 가 같은 `apply_priority()` 를 지나고, 바뀐 값은 History 한 줄로 남습니다. `updated` 는 건드리지 않습니다(멈춤 판정의 시계라, 카드를 올리는 것만으로 경보가 꺼지면 안 됩니다).
- 부분 검증: `python3 tests/ priority` 70건 OK, 신규 `tests/test_priority_handle.py` 30건 OK(실제 서버를 세워 `/api/priority` 를 두드리는 끝단 3건 포함). 캡처 `scratchpad/prio-chooser2.png`(손잡이를 진짜로 눌러 창이 열린 화면)·`scratchpad/prio-viewer.png`.

**남은 것**: 전체 스위트 결과 확인 → 노트 기록 → review 전이.

**알려 드릴 것**: 서버 라우트가 `bin/s9` 에 들어가 있어, 확인하실 때는 대시보드 상단 배너의 `s9 serve --restart` 로 재기동한 뒤 F5 해 주셔야 손잡이가 실제로 값을 바꿉니다. 그리고 `web/app/diag.js` 에 캡처용 진단(`?dlg=priolive`) 한 자리를 더했는데, 그 파일은 이번 라운드 배정 목록에 없어 노트로 남기겠습니다.

### 2026-08-30T11:38:13+09:00 response (by nicehugepark)

### 결론

두 겹 중 **뒤쪽 절반은 이미 서 있었다.** `work_order()` 가 `s9 next`(이어받기)와 무인 작업자 스폰 순서를 이미 정하고 있었다. 비어 있던 것은 앞쪽 절반 — 화면의 손잡이다. 값을 바꾸는 길이 hovercard 가 가르치는 `s9 set … --priority high` 하나뿐이라, **순서를 정하는 판은 보드인데 정하는 자리는 터미널에 있었다.**

### 고른 설계 — 표기가 곧 손잡이

손잡이를 따로 만들지 않고 **보이는 그 글자**(등급 낱말 `보통`/`높음`/`긴급`)를 누르게 했다. 두 번째 자리를 만들면 보이는 것과 누르는 것이 갈리고, 카드 한 장에 손잡이가 셋(집기·깨우기·순서)이 되면 카드가 손잡이 판이 된다.

여는 창은 새로 짓지 않고 이 화면에 이미 있는 고르기 창(`s9dlg kind:"choose"`)이다. **확인 단계는 없다** — 되돌리기가 한 번 더 누르는 것뿐인 일에 확인을 겹치면 손이 두 배로 든다(그 파일이 이미 세워 둔 규칙: 확인은 되돌릴 수 없는 것에만). 숫자 1~99 를 그대로 묻지 않는 것도 이 화면이 이미 내린 판단 그대로다(REQ-20260827-029-62x6) — 사람이 읽는 글자는 등급 낱말이고, 숫자를 고르게 하면 아무도 안 쓴다. 정확한 수가 필요한 사람은 CLI 로 넣을 수 있고 그 값도 등급 낱말로 읽힌다.

### 고친 것 (파일:함수)

- `web/app/const.js` `prioHTML()` — `<span>` → `<button data-prioset>`. 세 화면(보드 카드·Docs 목록 행·문서 뷰어)이 한 함수를 쓰므로 셋 다 눌린다. button 이라 키보드가 저절로 닿는다(전에는 tabindex 를 손으로 놓았다).
- `web/app/dialog.js` `prioSet()`·`postPriority()` **[신규]** — 고르기 창과 전송. `showPrioHover()` 마지막 줄의 CLI 안내를 "눌러서 바꾼다"로 교체. `s9choose()` 에 `doc`·`titleHtml` 을 열어 창 머리·제목을 판정 창과 **같은 곳**(`dlgFor`)에서 짓게 했다 — 창마다 제 문장을 지으면 조사가 어긋난다(1차 캡처에서 `REQ-…-001 를` 로 나왔다).
- `web/app/events.js` — `[data-prioset]` 위임. 문서 열기보다 **먼저** 잡고 전파를 끊는다.
- `web/css/board.css` `.prio` — 버튼 기본 옷(배경·테두리·패딩·제 글꼴)을 벗겨 여태 보이던 글자 그대로. 밑줄은 **손이 얹혔을 때만** 돋는다(306장이 늘 밑줄을 달면 판이 밑줄밭이 된다). 뷰어(`pfull`)만 상주.
- `web/app/diag.js` `?dlg=priolive` **[신규]** — 손잡이를 진짜로 누르는 캡처 경로(wsat 이 낸 선례).
- `bin/s9` `apply_priority()`·`set_doc_priority()` **[신규]**, `POST /api/priority` **[신규]**, `cmd_set()` 위임.

### 문(門)은 하나다

CLI `--priority` 도 화면도 `apply_priority()` 를 지난다. 두 벌이면 언젠가 화면으로 바꾼 것만 History 에 안 남는다. 바뀐 값은 `- <ts> priority: 50 -> 75 (by …) [via dashboard]` 한 줄로 남는다 — "왜 이게 위에 있나"는 나중에 반드시 묻는 질문이고, 답이 프론트매터의 현재값 하나뿐이면 아무도 판정할 수 없다.

**`updated` 는 안 건드린다.** 그 시각이 멈춤 판정의 시계(`stall_verdict` 의 `quiet_mins`)다. 순서를 바꾸는 것은 진전이 아니어서, 여기서 되감으면 **카드를 끌어 올리는 것만으로 멈춤 경보가 꺼진다** — REQ-20260829-034-62x6 가 클레임에 대해 막은 그 병의 대칭이다. `cmd_set` 도 우선순위만 바뀐 때는 시각을 안 찍는다.

### 시험

신규 `tests/test_priority_handle.py` **30건** — 손잡이 계약(표기가 손잡이인가·버튼인가·문서 열기보다 먼저 잡히는가·창을 새로 짓지 않았는가), 문 하나 계약(라우트가 `set_doc_priority` 를 지나는가·CLI 가 같은 문인가), 문의 행동(근거 기록·멱등·`updated` 불변·잘못된 값이 기존 값을 보존하는가·요청 문서만인가), **끝단 3건**(진짜 서버를 세워 `/api/priority` 를 두드려 값이 색인까지 가는지, 잘못된 값·없는 문서가 이유를 달고 거절되는지 — 소스 grep 만으로는 이름 하나 틀려도 통과하고 사람은 창에서 실패를 본다).

`python3 tests/ priority` 70건 OK. `s9choose` 를 쓰는 이웃(`account_and_model`·`dead_end`·`judge_dialog`·`session_pick`) 79건 OK.

**전체 `python3 tests/` → Ran 2335 in 456s, failures=20 errors=5.** 실패 25건은 전부 훅/세션 모듈(`subagent_attr`·`project_agents`·`question_qst`·`question_queue`·`response_stamp`·`session_attach`·`session_mirror_switch`)이고 **단독으로 다시 돌리면 8+47건 모두 OK** — 8분 도는 동안 다른 작업자가 같은 훅 파일을 고쳐서 잡힌 경합이다. 우선순위·화면·API 쪽 실패는 없다.

### 캡처

- `scratchpad/prio-chooser2.png` — 카드의 `보통` 을 **진짜로 눌러** 창이 열린 화면(`?dlg=priolive`). 「둘의 차이를 모르겠다」를 어느 자리에 둘까요 / 긴급 90 · 높음 75 · ●보통 50(지금 이것) · 낮음 25.
- `scratchpad/prio-viewer.png` — 문서 뷰어 `PRIORITY  보통 50/99` 가 안 부풀었는지.

### 범위 밖으로 나간 것 둘 (알린다)

1. `bin/s9` — 이 라운드 봉투는 web/·vault/·tests/ 로 한정했지만 「네 차례」가 이 파일을 명시로 배정했고 리스도 이 REQ 가 쥐고 있었다. 서버 라우트 없이는 손잡이가 눌러도 아무 데도 닿지 않아서 넣었다.
2. `web/app/diag.js` — 첫 라운드에 배정 밖이었다(둘째 라운드에 배정됐다). 캡처 경로 한 자리뿐이고 제품 동작에는 안 닿는다.

### 리드가 알아야 할 것

서버 쪽 변경이라 **`s9 serve --restart` 로 재기동해야 손잡이가 실제로 값을 바꾼다.** 재기동 전에는 창은 뜨지만 고르는 순간 실패 창이 뜬다(상단 배너가 이미 그 사실을 말하고 있다).

[Image: assets/REQ-20260829-029-62x6/prio-chooser2.png]
[Image: assets/REQ-20260829-029-62x6/prio-viewer.png]

### 2026-08-30T12:00:55+09:00 ask (by nicehugepark)

승인이나, 반려를 하려면 화면에 보이는 글자만 바뀌는 것이 아니라, 실제로 에이전트가 해당 요청에 대해서 작업을 캐치해야하고, 특히 긴급이나 높음의 경우 바로 시작되어야 하는데, 이 하네스 시스템에서 요청 깨우기 기능도 제대로 검증이 안됐는데 긴급하게 처리한다는 것이 가능한가??

### 2026-08-30T12:03:45+09:00 response (by nicehugepark)

**지적이 맞다. 지금 구조로는 "긴급하면 바로 시작된다"고 말할 수 없다.** 코드를 읽고 실측한 것을 그대로 적는다.

**① 도는 것 — 화면 글자만 바뀌는 것은 아니다.**
`rework_watch_tick`(bin/s9:6249)이 서버 안에서 **30초마다** 돈다. 반려(review→in-progress)와 대시보드 드래그 착수(open→in-progress [via dashboard])가 후보이고, 전이 뒤 **유예 30초**를 넘기고도 아무도 클레임하지 않으면 무인 작업자를 띄운다. 유예는 살아 있는 세션이 먼저 집을 기회다.

**② 우선순위는 이미 읽힌다 — 다만 "순서"만.**
그 틱은 `for r in work_order(load_catalog())` 로 돈다. `work_order`(bin/s9:827)는 가중치 내림차순, 같으면 오래 기다린 것 먼저다. 그러니 **같은 틱에 후보가 여럿일 때 누가 먼저 뜨는지**는 이미 우선순위가 정한다.

**③ 그런데 지연은 우선순위를 모른다 — 여기가 지적의 핵심이다.**
유예 30초도, 틱 주기 30초도 상수다. 긴급이든 보통이든 **최대 60초쯤을 똑같이 기다린다**. "긴급이면 바로"가 성립하려면 우선순위가 순서가 아니라 **지연**에도 작용해야 하는데, 지금은 그 배선이 없다.

**④ 예산도 우선순위를 모른다.**
per-REQ 쿨다운 + 시간당/일일 캡(bin/s9:2047)이 있고, 차면 **긴급도 그냥 막힌다**(`capped`). 이건 가정이 아니라 이 저장소가 이미 겪은 일이다 — 2026-08-29 저녁, 20슬롯 중 넷이 승인 후속에 쓰이고 나서 **사람이 누른 깨우기가 전부 capped 였다**(tests/test_wake.py W4 가 그 실측을 잡고 있다).

**⑤ 승인은 작업자를 띄우지 않는다 — 이건 의도한 것이다.**
REQ-20260830-002-62x6 에서 뺐다. 승인은 언제나 done 문서로 가는데 이 저장소는 승인마다 근거 메모를 요구하므로, 그 경로가 예외가 아니라 상시가 돼 끝난 문서의 작업자가 `bin/s9` 를 잡고 대기 중인 일들을 막았다. 승인 메모는 사라지지 않고 다음 리드 세션의 턴 앞에 놓인다.

**⑥ 그리고 깨우기가 아직 판정 대기다.** REQ-20260828-041-62x6 이 review 에 있다. "검증이 안 됐다"는 말 그대로 맞다.

---

### 그래서 남은 일 (이 요청의 뒤쪽 절반)

- **A. 긴급·높음은 틱을 기다리지 않는다.** 유예와 스폰을 전이 지점에 붙여, 우선순위가 높으면 유예를 0 으로 줄이고 워처의 다음 틱을 기다리지 않고 그 자리에서 띄운다. 보통 이하는 지금 그대로 — 유예는 살아 있는 세션을 위한 배려라 없애면 겹친 손이 돌아온다.
- **B. 예산이 우선순위를 안다.** 캡을 뚫는 것이 아니라, 긴급에 **예비 몫**을 남긴다. 뚫으면 캡을 만든 이유(워커 넷이 같은 문제에 같은 답을 따로 낸 2026-08-29 11:04 실사고)가 통째로 사라진다. per-REQ 쿨다운은 그대로 둔다 — 그것은 예산이 아니라 자기 위에 자기를 다시 띄우는 것을 막는 장치다.
- **C. 끝까지 가는 시험.** 지금 이 경로는 "코드가 그렇게 생겼다"까지만 잡혀 있다. 반려를 실제로 넣고 → 워처 틱을 돌려 → 작업자가 떴는지를 **한 시험 안에서** 확인해야 한다. 그게 없으니 사용자가 "검증이 안 됐다"고 말할 근거가 그대로 있다.

A 부터 착수한다.

### 2026-08-30T12:34:45+09:00 response (by nicehugepark)

### 뒤쪽 절반을 채웠다 — 우선순위가 **지연**과 **예산**에도 닿는다

앞선 라운드에서 ①(화면 손잡이)과 ②(순서)는 서 있었다. 사용자가 정확히 짚은 것은 ②가 순서**만** 바꾼다는 것이었다.

### A. 긴급·높음은 워처를 기다리지 않는다

- `PRIORITY_RUSH = 75` — 화면의 등급 경계(`prioTier`, 75부터 '높음')와 **같은 값**이다. 사람이 '높음'으로 올렸는데 시스템이 안 서두르면 그 손잡이는 거짓말이다. 시험이 두 값이 갈라지는 것을 막는다.
- `rework_grace(meta)` — 긴급·높음은 **유예 0**. 보통 이하는 종전 30초 그대로다. 그 30초는 낭비가 아니라 살아 있는 세션이 먼저 집을 기회이고(REQ-20260824-014), 없애면 이 저장소가 네 번 덴 사고 — 일하는 파일 위의 두 번째 손 — 이 돌아온다.
- `rework_kick(doc_id)` — 전이 **그 자리에서** 띄운다. 유예가 0 이어도 워처의 다음 틱까지 최대 30초를 더 기다렸는데, 그 30초는 '긴급'이라 눌러 놓고 아무 일도 안 일어나는 시간이다. `maybe_auto_resume` 이 이제 이것을 부른다.
- **판정을 새로 짓지 않았다.** 후보 여부(`rework_candidate`)·유예·스폰 관문(옵트인·머신·쿨다운·캡·리스)은 전부 워처가 쓰는 그 함수들이다. 워처가 막을 것은 즉시 경로에서도 막힌다. `rework_claimed()` 검사도 그대로라 **이미 집힌 것 위에는 뜨지 않는다**.

### B. 예산이 우선순위를 안다

`_rush_limits()` — 시간·일 예산의 마지막 두 자리를 긴급·높음 몫으로 **남긴다**(`auto_resume_rush_reserve`, 기본 2). **뚫지 않는다** — 뚫으면 캡을 세운 이유(워커 넷이 같은 문제에 같은 답을 따로 낸 2026-08-29 11:04 실사고)가 통째로 사라진다. 순위가 있는데 자리가 없으면 순위는 아무 말도 하지 않으므로, 보통 요청들이 예산을 바닥까지 긁는 것만 막는다. 예비에 막힌 사유는 "다 썼다"가 아니라 **"급한 것 몫으로 비워 뒀다 — 먼저 돌리려면 우선순위를 올려라"** 로 갈라 적었다. 두 사유를 같은 문장으로 말하면 사람은 올릴 필요가 없는 한도를 올리러 간다.

### C. 시험 — `tests/test_priority_handle.py` 의 `RushStart` 아홉 건

지연(유예 0 · 문턱이 화면과 같음 · 전이 자리에서 뜸 · 보통은 여전히 기다림 · 이미 집힌 것엔 안 뜸 · 워처가 안 집는 전이는 즉시 경로도 안 집음)과 예산(예비가 긴급에만 열림 · 상한 자체는 안 뚫림 · 예비가 상한을 삼키지 않음)을 잡는다. **스폰은 가짜로 바꿔 놓고 잰다** — 진짜 워커를 띄우는 시험은 시험이 아니라 사고다.

### 덤으로 잡은 것: 이 시험 파일이 옆 스위트를 넘어뜨리고 있었다

`mod()` 셋이 `os.environ.update(self.env)` 로 전역 환경을 갈아입고 **안 벗었다**. 스위트가 나란히 도는 판이라 옆 스위트의 자식 프로세스가 그 값을 물려받는다 — 실측으로 `test_delegation_target` 다섯이 함께 넘어졌고(따로 돌리면 통과) 새어 나간 것은 `S9_USER=tester` 였다. 그쪽 문서의 주인은 alice 다. `borrowed_env()` 로 모듈 읽는 동안만 빌려 입고 돌려주게 고쳤다. 11스위트 **209건 전부 통과**.

### 남는 것 (이 요청 밖)

승인(review→done)은 여전히 작업자를 띄우지 않는다 — REQ-20260830-002-62x6 가 값을 두 번 치르고 의도적으로 뺀 자리다. 사용자가 "승인이나 반려를 하려면 실제로 에이전트가 캐치해야 한다"고 한 것 중 승인 쪽은 이 요청이 아니라 그 결정을 다시 여는 별건이다.

### 2026-08-30T17:38:37+09:00 ask (by nicehugepark)

지금 029 요청을 긴급으로 바꿨는데, 어떤 상황인지 보고해.

### 2026-08-30T17:39:06+09:00 ask (by nicehugepark)

30일 029를 말한거였다. 우연히도 이 요청도 029였군.

### 2026-08-30T17:39:58+09:00 ask (by nicehugepark)

REQ-20260830-029-62x6 요청을 긴급으로 했으니 보고하라는 말이다. 긴급 기능이 잘 작동하는지.

### 2026-08-30T17:41:30+09:00 ask (by nicehugepark)

화면에서는 상단으로 올렸지만, 시스템적으로 빠르게 처리할 수있는 상태가 되게 하라는게 요청이잖아. 그게 됐냐고

## History
- 2026-08-29T17:00:52+09:00 created by nicehugepark (status: open)
- 2026-08-29T20:08:43+09:00 status: open -> in-progress (by nicehugepark) — drag 이동 [via dashboard]
- 2026-08-30T10:53:50+09:00 priority: 50 -> 90 (by nicehugepark) [via dashboard]
- 2026-08-30T11:38:22+09:00 status: in-progress -> review (by nicehugepark) — 카드에 적힌 우선순위 글자(보통·높음·긴급)가 이제 버튼입니다. 상단 배너의 [s9 serve --restart] 를 누르고 F5 한 뒤, Board 에서 아무 카드의 '보통' 을 눌러 '긴급' 을 골라 보세요. 그 카드가 그 열 맨 위로 올라오면 승인입니다. 창이 안 뜨거나 문서만 열리거나 실패 창이 뜨면 반려입니다.
- 2026-08-30T12:02:19+09:00 status: review -> in-progress (by nicehugepark) — 채팅 반려: 화면 글자만 바뀌는 것이 아니라 실제로 에이전트가 그 요청을 집어야 하고, 긴급·높음은 바로 시작돼야 한다 — 깨우기조차 검증이 안 된 상태에서 긴급 처리가 가능한가
- 2026-08-30T12:34:53+09:00 status: in-progress -> review (by nicehugepark) — 우선순위를 긴급이나 높음으로 올린 요청은 이제 반려하는 즉시 작업자가 붙습니다. 전에는 등급과 상관없이 최대 1분을 기다렸습니다. 서버를 다시 띄운 뒤(화면 위쪽 s9 serve --restart), 카드의 등급 글자를 눌러 긴급으로 올리고 반려해 보세요. 몇 초 안에 그 카드에 진행 중 줄과 세우기 버튼이 뜨면 승인, 1분 가까이 아무 일도 없으면 반려입니다.
- 2026-08-30T17:10:41+09:00 status: review -> in-progress (by nicehugepark) — 판정 카드 문구 갱신 경유 (REQ-20260830-031-62x6) — 재작업 아님: 낱말 하나(세우기→중단하기)만 현재 화면에 맞춘다
- 2026-08-30T17:10:42+09:00 status: in-progress -> review (by nicehugepark) — 우선순위를 긴급·높음으로 올린 요청은 반려하는 즉시 작업자가 붙습니다(전에는 등급과 무관하게 최대 1분 대기). 서버 재시작(화면 위 s9 serve --restart) 후, 카드의 등급 글자를 눌러 긴급으로 올리고 반려해 보세요. 몇 초 안에 진행 중 줄과 「중단하기」가 뜨면 승인, 1분 가까이 조용하면 반려입니다.
- 2026-08-31T16:17:16+09:00 status: review -> done (by nicehugepark) — 리드 직접 판정(사용자 위임 REQ-20260831-017-62x6): 긴급·높음 반려 시 즉시 워커 스폰(rush 킥)은 priority_handle 스위트가 지키고 금일 015 검증에서 exit 0 재실측. 워커 봉투 주입(015)도 같은 킥 지점을 지나며 회귀 없음 확인
