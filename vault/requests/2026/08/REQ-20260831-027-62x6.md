---
id: REQ-20260831-027-62x6
type: request
title: 프로젝트 생성·설정 API
summary: do_project_add/do_project_set 단일 경로 추출 + POST /api/project/add·set + history_tail — 권한(등록 사용자 생성·owner 자동, 설정 manage, status 는 own)과 History 기록 교정 포함
goal: 등록 사용자가 POST /api/project/add 로 프로젝트를 만들고(생성자 owner), manage 권한으로 /api/project/set 8필드를 고치며(status 는 own·History 기록), CLI 와 같은 단일 경로를 지나는 것이 시험으로 확인된다
status: in-progress
size: M
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: 38ad98c8
agents: ["sub:backend-developer:a44135a0", "sub:backend-developer"]
contributions: [{"actor": "sub:backend-developer:a44135a0", "item": "027 프로젝트 API 구현", "started": "2026-08-31T22:53:00+09:00", "ended": "2026-08-31T23:01:30+09:00", "result": "done", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/38ad98c8-51fd-4b50-a26a-495b58404817/tasks/a44135a053dac770c.output"}, {"actor": "sub:backend-developer", "item": "TDD 시나리오 (027 프로젝트 생성·설정 API)", "started": "2026-08-31T22:56:08+09:00", "ended": "2026-08-31T22:56:08+09:00", "result": "done", "transcript": ""}, {"actor": "sub:backend-developer", "item": "TDD 시나리오 (개정 — 전 시나리오 통과, 15/15)", "started": "2026-08-31T23:00:31+09:00", "ended": "2026-08-31T23:00:31+09:00", "result": "done", "transcript": ""}, {"actor": "sub:backend-developer", "item": "구현 완료 — 프로젝트 생성·설정 API (architect 설계 그대로, B2 프론트용 명세 포함)", "started": "2026-08-31T23:00:54+09:00", "ended": "2026-08-31T23:00:54+09:00", "result": "done", "transcript": ""}, {"actor": "sub:backend-developer:a44135a0", "item": "`[2026-08-31 23:01:09 KST - backend-developer]`", "started": "2026-08-31T23:01:30+09:00", "ended": "2026-08-31T23:01:30+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/38ad98c8-51fd-4b50-a26a-495b58404817/subagents/agent-a44135a053dac770c.jsonl"}]
project: section9
parent: REQ-20260831-026-62x6
relates_why: {}
tags: ["project", "access"]
created: 2026-08-31T22:52:30+09:00
status_since: 2026-08-31T22:52:38+09:00
updated: 2026-08-31T23:01:30+09:00
priority: 50
---

## Original

프로젝트 생성·설정 API

## Notes

### 2026-08-31T22:56:08+09:00 tdd (agent: sub:backend-developer, by nicehugepark)

### TDD 시나리오 (027 프로젝트 생성·설정 API)

정상
- [ ] S1. 생성: POST /api/project/add {slug:neo,name,...} (등록 사용자 alice) → 200 + PRJ id, vault/projects/neo.md 생성, 생성자 alice 가 owner, History 'created by alice', projects/neo/ 스캐폴드 존재, CLI add 와 같은 do_project_add 경로
- [ ] S2. 설정 8필드: maintainer 가 POST /api/project/set 으로 name/summary/customer/contact_name·email·phone·org 수정 → 200, GET /api/projects 반영 + History 에 set 줄 기록(기존 CLI set 의 History 미기록 비대칭 교정 확인)
- [ ] S3. status 보관/복원: owner 가 status=archived → 200, 복원 active → 200, History 기록. admin(비멤버)도 우회 성공
- [ ] S4. GET /api/projects history_tail: 프로젝트당 최근 History ≤10줄이 additive 필드로 실린다

경계
- [ ] E1. slug 중복: 기존 slug 로 add → 400 'already exists'
- [ ] E2. slug 형식: 허용문자([A-Za-z0-9._-]) 밖 → 400
- [ ] E3. 빈 필드: set 에 필드 0개 → 400 '변경할 필드 없음'; summary:'' 는 비우기로 정상 처리(200)
- [ ] E4. 마지막 owner: 멤버 API 의 마지막 owner 강등/제거 차단이 그대로 동작(회귀)

실패(거부 4종 — R3 옆문 금지: 판정은 do_* 안, 핸들러는 중계만)
- [ ] F1. 미등록 사용자 생성 거부: 미등록 actor 의 project add(CLI --user ghost)와 미등록 as 대리(API) 모두 거부
- [ ] F2. viewer 설정 거부: viewer 가 set name → 400 (manage 미달)
- [ ] F3. contributor status 거부: contributor 가 set status → 400
- [ ] F4. 비 owner status 거부: maintainer 가 set status → 400 (own 필요 — project_can 독스트링과 구현 불일치를 own 쪽으로 해소)

회귀
- [ ] R1. 기존 멤버 API(/api/project/member·rm)·CLI project add/set/member 하위 호환 — 기존 스위트 green (test_project_member_api·test_project_assets·test_project_agents)
- [ ] R2. GET /api/projects 기존 필드(members/member_active/member_total/contact_*) 불변 — additive-only
- [ ] R3. 인접 스위트 green: access_isolation·catalog_conditional·poll_snapshot·review_family

### 2026-08-31T23:00:31+09:00 tdd (agent: sub:backend-developer, by nicehugepark)

### TDD 시나리오 (개정 — 전 시나리오 통과, 15/15)

정상
- [x] S1. 생성: POST /api/project/add (alice) → 200+PRJ id, 생성자 owner, History, 스캐폴드, CLI 동일 경로 (+회귀 가드: 같은 날 생성 프로젝트 간 id 충돌 금지 — 아래 발견)
- [x] S2. 설정 8필드: maintainer set → 반영 + History 'set k=v (by bob via dashboard)' (미기록 비대칭 교정 확인)
- [x] S3. status own: owner 보관→admin(비멤버) 복원 성공, History 기록
- [x] S4. GET /api/projects history_tail ≤10줄 additive

경계
- [x] E1. slug 중복 400 'already exists' (검사를 lock 안으로 옮겨 동시 생성 race 도 봉인)
- [x] E2. slug 형식 위반 400
- [x] E3. 필드 0개 400 '변경할 필드 없음' / summary:'' 비우기 200
- [x] E4. 마지막 owner 강등 차단 회귀(멤버 API)

실패(거부 4종)
- [x] F1. 미등록 생성 거부 — CLI --user ghost rc≠0 + API as ghost 400
- [x] F2. viewer set 400
- [x] F3. contributor status 400
- [x] F4. maintainer status 400 (own 게이트 — 독스트링 쪽으로 해소)

회귀
- [x] R1. test_project_member_api·test_project_assets·test_project_agents·test_whoami·test_stream_isolation green (exit=0 실측)
- [x] R2. GET /api/projects 기존 필드 전수 불변(additive-only) — test_14
- [x] R3. 인접 스위트 green: test_access_isolation·test_catalog_conditional·test_poll_snapshot·test_review_family (exit=0 실측)

구현 중 발견·근원 수정: next_id('PRJ','projects')는 파일명 스캔인데 프로젝트 문서는 <slug>.md 라 기존 PRJ 순번을 못 봐 **같은 날 생성분 전부 -001 로 id 충돌**하던 기존 결함 → _next_project_id()가 frontmatter id 를 스캔(.trash 포함)하도록 교정, S1 에 회귀 가드.

### 2026-08-31T23:00:54+09:00 response (agent: sub:backend-developer, by nicehugepark)

### 구현 완료 — 프로젝트 생성·설정 API (architect 설계 그대로, B2 프론트용 명세 포함)

### 코드 (bin/s9, +201/-59)
- do_project_add / do_project_set 단일 경로 신설(do_member_set 패턴) — CLI(cmd_project add/set)와 서버 POST 가 같은 함수를 부른다. 핸들러는 중계만(판정·인가 전부 do_* 안, R3 옆문 없음).
- set 의 History 미기록 비대칭 교정: 이제 'set k=v ... (by actor[ via dashboard])' 한 줄이 남는다(값 80자 절단).
- 권한: 생성=등록 사용자 누구나+생성자 자동 owner(admin 은 as 대리 — do_POST 서두가 등록자만 통과), 설정=manage, **status 변경만 own**(project_can 독스트링 쪽으로 구현 해소).
- projects_data 에 history_tail(최근 ≤10줄, ## History 절만) additive 확장.
- **부수 근원 수정**: PRJ id 발번이 next_id 파일명 스캔이라 slug 파일명인 프로젝트에선 같은 날 생성분이 전부 -001 충돌(실측) → _next_project_id()가 frontmatter id 스캔(.trash 포함). 중복 slug 검사도 lock 안으로 이동(동시 생성 race 봉인).

### API 명세 (B2 프론트가 그대로 쓴다)
**POST /api/project/add** — req: {slug 필수([A-Za-z0-9._-]+), name?, summary?, customer?, contact_name?, contact_email?, contact_phone?, contact_org?, as?(admin 대리)}. 200: {ok:true, id:'PRJ-…', slug, owner, title}. 400: {ok:false, error:'invalid slug…'|'already exists: …'|'권한 없음: …미등록…'}. user 파라미터는 무시(서버 whoami 파생).
**POST /api/project/set** — req: {slug 필수, name?, summary?, customer?, status?(active|archived), contact_*4?}. 필드 미포함=미변경, ''=비우기, 현재값과 같은 값=무변경 취급(own 게이트도 안 섬). 전 필드 무변경이면 400 '변경할 필드 없음' — 폼은 바뀐 필드만 보내라. 200: {ok:true, slug, changed:[frontmatter 키…], status}. 400: 권한(manage/own)·not found·enum.
**GET /api/projects** — 프로젝트당 history_tail:[string] 추가(additive). 폴링 벨트 금지 유지: 탭 진입 1회 + 변이 성공 후 재조회. 변이는 _POLL_EPOCH 공통 처리로 스냅샷 자동 무효화.

### 시험
tests/test_project_api.py 신규 14케이스(정상4/경계4/거부4/회귀2) — python3 tests/ test_project_api → exit=0. 인접 회귀 exit=0: project_member_api·project_assets·project_agents·whoami·stream_isolation / access_isolation·catalog_conditional·poll_snapshot·review_family. 라이브 9909 무접촉(포트풀), git 무조작.

한계: 전체 스위트(--jobs 4)는 G1 게이트에서 리드/QA 몫. CLI set 의 거부가 die(rc 2)로 나가는 형식은 기존과 동일(문구는 do_* 로 일원화).

### 2026-08-31T23:01:30+09:00 subagent (agent: sub:backend-developer:a44135a0, by nicehugepark)

`[2026-08-31 23:01:09 KST - backend-developer]`

**결론 — REQ-20260831-027-62x6 구현 완료. TDD 15/15 초록, 인접 회귀 9개 스위트 exit=0 실측, 노트 3건(tdd 2세대·response·knowledge) 기록.**

### 무엇을 만들었나 (`/home/sjpark1/section9/bin/s9`, +201/-59)

1. **단일 경로 추출**: `do_project_add`·`do_project_set` 신설(do_member_set 패턴) — `cmd_project add/set` 과 신규 POST 핸들러가 같은 함수를 부른다. 핸들러는 ValueError 중계만(PM 리스크 R3 옆문 없음).
2. **POST `/api/project/add`·`/api/project/set`** — do_POST 의 member/rm 분기 바로 뒤. actor=서버 whoami, `as` 대리는 기존 서두 공통 처리, `_POLL_EPOCH` 자동 커버.
3. **권한(리드 G0 그대로)**: 생성=등록 사용자 누구나+생성자 자동 owner(admin 은 as 대리로 통과), 설정=manage, **status 변경만 own** — project_can 독스트링-구현 불일치를 own 쪽으로 해소. CLI 도 같은 경로라 함께 고쳐졌다.
4. **History 교정**: set 이 이제 `- <ts> set k=v … (by actor via dashboard)` 를 남긴다(값 80자 절단). add 의 via 표기도 통일.
5. **GET /api/projects `history_tail`** — 프로젝트당 ## History 최근 ≤10줄, additive-only(기존 필드 전수 불변을 test_14 가 단언).

### 구현 중 발견한 기존 결함 — 근원 수정

`next_id("PRJ","projects")` 는 **파일명**을 스캔하는데 프로젝트 문서는 `<slug>.md` 라 기존 순번을 0건 매칭 → **같은 날 만든 프로젝트가 전부 `-001` 로 id 충돌**하고 있었다(테스트 실측으로 확인). `_next_project_id()` 가 frontmatter id 를 스캔(.trash 포함)하도록 교정하고 회귀 가드를 test_01 에 심었다. 중복 slug 검사도 lock 안으로 옮겨 동시 생성 race 를 봉인했다. 재사용 교훈은 `DOC-20260831-006-62x6` (knowledge)로 남겼다.

### 시험 (`/home/sjpark1/section9/tests/test_project_api.py` 신규, 14케이스)

- 정상 4(생성/8필드+History/status own+admin 우회/history_tail) · 경계 4(slug 중복·형식, 빈 필드, 마지막 owner) · **거부 4종**(미등록 생성 CLI+API, viewer set, contributor status, maintainer status) · 회귀 2(CLI 동일 경로, 스키마 additive).
- `python3 tests/ test_project_api` → **exit=0** (무파이프 실측). 인접 회귀 전부 exit=0: project_member_api·project_assets·project_agents·whoami·stream_isolation / access_isolation·catalog_conditional·poll_snapshot·review_family. 검증은 포트풀 임시 포트 — 라이브 9909 무접촉, git 무조작.

### B2 프론트 인계 요지 (전체 명세는 REQ-027 response 노트)

- add 200 응답: `{ok, id, slug, owner, title}` / set 200: `{ok, slug, changed[], status}` / 거부는 전부 400 `{ok:false, error:"사유"}` — 화면은 error 문구를 그대로 그리면 된다.
- set 은 **바뀐 필드만 보내라**: 미포함=미변경, `""`=비우기, 현재값과 같은 값은 무변경 취급(전 필드 무변경이면 400 "변경할 필드 없음").
- /api/projects 는 폴링 벨트 금지 유지 — 탭 진입 1회 + 변이 성공 후 재조회.

한계: 전체 스위트(`--jobs 4`)는 G1 게이트에서 리드/QA 몫으로 남겼다.

## History
- 2026-08-31T22:52:30+09:00 created by nicehugepark (status: open)
- 2026-08-31T22:52:38+09:00 status: open -> in-progress (by nicehugepark) — W1 착수
