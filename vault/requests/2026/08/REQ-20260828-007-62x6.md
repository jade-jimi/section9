---
id: REQ-20260828-007-62x6
type: request
title: 판정 창 세 가지
summary: 문서 제목이 안 보임 · Enter 가 줄바꿈이 아니라 전송 · 화면을 옮겨도 남음
goal: 판정 창이 무엇을 판정하는지 제목으로 보이고, 큰 입력란에서 Enter 가 줄바꿈이며, 화면을 옮기면 닫힌다
status: review
size: M
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: b8fda8f9
sessions: ["3da5b9de", "b8fda8f9"]
agents: ["sub:designer", "sub:architect:a63e1df3", "sub:ux-writer:ac53d5c8", "sub:subagent:a17e007a"]
contributions: [{"actor": "sub:designer", "item": "화면 몫", "started": "2026-08-28T08:23:38+09:00", "ended": "2026-08-28T16:24:22+09:00", "result": "done", "transcript": ""}, {"actor": "sub:architect:a63e1df3", "item": "`[2026-08-28 15:31:02 KST - architect]`", "started": "2026-08-28T15:33:19+09:00", "ended": "2026-08-28T15:33:19+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/subagents/agent-a63e1df3957e247bb.jsonl"}, {"actor": "sub:ux-writer:ac53d5c8", "item": "`[2026-08-28 15:31:07 KST - ux-writer]`", "started": "2026-08-28T15:34:01+09:00", "ended": "2026-08-28T15:34:01+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/subagents/agent-ac53d5c8feb317dcb.jsonl"}, {"actor": "sub:designer", "item": "확정안 그대로 구현했습니다 — 그리고 화면·서버 사이의 결합을 함께 끊었습니다.", "started": "2026-08-28T16:24:22+09:00", "ended": "2026-08-28T16:24:22+09:00", "result": "done", "transcript": ""}, {"actor": "sub:subagent:a17e007a", "item": "Appending response note to REQ-20260828-007-62x6", "started": "2026-08-28T16:24:46+09:00", "ended": "2026-08-28T16:24:46+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/subagents/agent-a17e007a127d6a849.jsonl"}]
project: section9
relates_why: {}
tags: ["terminal", "audit", "dashboard", "attached", "png", "image", "docs"]
created: 2026-08-28T08:18:00+09:00
status_since: 2026-08-28T16:24:44+09:00
updated: 2026-08-28T16:25:49+09:00
priority: 50
---

## Original

사용자(08:17):

1. 팝업에 표시되는 문서제목이 코드로만 보이고, 제목은 보이지 않아서 무엇에 대해서 판정하려 했는지 모르겠다.
2. 판정 메시지 입력란이 큰데, 엔터키를 누르면 줄바꿈이 아니라 승인이든, 반려든 메시지가 전송이 되어 버린다.
3. 판정 팝업이 판정에만 사용되지 않고, 다른 기능에서도 공통 사용되는 팝업 기능으로 된 것 같은데 이것 자체는 나쁘지 않지만 다른 탭이나 화면으로 이동 후에도 계속 보이게 되는게 맞는건가? ux 경험상 불편하다.

**2번은 리드의 판단 오류다.** 디자이너가 처음에 Ctrl+Enter 확인으로 만들었는데, 리드가 "터미널 입력줄과 같아야 한다"며 Enter 확인으로 뒤집었다. 그 논거는 틀렸다 — 터미널 입력줄은 **한 줄짜리 보내기 상자**이고 판정 창은 **여러 줄 짜는 상자**다. 게다가 잘못 눌렀을 때의 값이 다르다: 채팅은 한 줄 더 치면 되지만 판정은 상태를 옮겨 버린다. 크기가 다르면 규칙도 다른 것이 맞다.

## Notes

### 2026-08-28T08:23:38+09:00 (agent: sub:designer, by nicehugepark)

리드가 designer 서브에이전트에 맡겼다. 같은 web/index.html 을 다른 주체가 함께 고치면 남의 미커밋 작업이 사라진다(REQ-20260826-021-62x6) — 무인 작업자를 겹쳐 띄우지 마라.

### 2026-08-28T08:40:55+09:00 designer (by nicehugepark)

지적 셋을 다 잡았습니다. `python3 tests/ judge_dialog` 13건 OK (계약 둘을 새로 썼고 하나를 더했습니다).

**1) 무엇을 판정하는지가 창 안에서 읽힙니다.**
문장을 짓는 자리를 `dlgFor()` 한 곳으로 모았습니다 — 반려·승인·전이·취소 넷이 각자 문장을 짓다 넷 다 id 만 적고 있었고, 그래서 한 군데를 고쳐도 나머지가 남았습니다.

- 주소(`REQ-20260827-071-62x6`)는 **머리**에 작은 글씨로. 카드의 id 줄과 같은 재료(mono 10px, 흐린 잉크)입니다.
- 제목은 **본문**에 크게: `「판정 대화상자」 을(를) 반려합니다`. 낫표는 집기 줄이 이미 쓰는 어휘입니다 — 제목이 동사로 끝나면 뒤에 붙는 조사와 엉켜 한 문장으로 읽히기 때문입니다.
- 전이 문구는 `→ done` 대신 `「제목」 을(를) done 상태로 옮깁니다` 로 바꿨습니다. 상태 이름은 조사를 고르지 못합니다("done 로"·"in-progress 으로"가 갈립니다) — "상태로"를 끼우면 어느 이름에도 문장이 섭니다.
- **긴 제목**: 60자에서 자르고 `…` 를 붙입니다. 자르는 것은 제목뿐이고 뒤따르는 동사는 남습니다 — 동사가 잘려 나가면 무엇을 하려는 창인지가 사라집니다. 진단 주소 `?dlg=long` 으로 그 경우를 볼 수 있습니다.
- 카탈로그에서 제목을 못 찾으면 예전처럼 id 로 적습니다(빈 낫표를 그리지 않습니다).

**2) Enter 는 줄바꿈, ⌘/Ctrl+Enter 가 확인입니다.**
되돌렸습니다. 힌트 줄도 두 줄로 바꿨고, 규칙 하나에 줄 하나라 좁은 폭에서도 낱말 한가운데가 끊기지 않습니다(REQ-20260827-081-62x6 에서 잡은 그 문제).

    Ctrl+Enter 로 반려하기
    Enter 로 줄바꿈

맥에서는 `⌘+Enter` 로 적습니다 — 자판에 없는 키 이름을 적으면 힌트가 거짓말이 됩니다. 코드에서도 손질 하나를 걷어냈습니다: 전에는 Ctrl+Enter 에 줄바꿈 문자를 손으로 끼워 넣었는데, 이제 맨 Enter 를 가로채지 않으므로 입력란이 스스로 줄을 넣습니다.

**3) 화면을 옮기면 닫힙니다 — 앞서 고쳤다고 본 것은 사람이 쓰지 않는 길이었습니다.**
재현해 보고 원인을 찾았습니다. 앞선 고침은 `applyRoute` 안에만 손을 댔는데, **사람이 화면을 옮기는 길 셋(헤더 탭 버튼·문서 링크와 카드·그래프 노드 클릭)은 하나도 그 함수를 거치지 않습니다.** 셋 다 화면 상태를 직접 바꾸고 `pushRoute()` 를 부릅니다. `applyRoute` 는 첫 진입과 뒤로가기에서만 돕니다. 그래서 진단 주소로는 닫혔는데 손으로는 안 닫혔습니다.

앞선 진단(`?dlgnav`)도 같은 이유로 통과했었습니다 — 주소창에 값을 넣어 확인했으니 사람이 쓰는 길이 아니었습니다. 지금은 **헤더 탭 버튼을 실제로 누릅니다.**

고친 방식은 "누가 옮겼는가"를 묻지 않습니다. 창은 열릴 때 자기가 선 화면 이름을 적어 두고, 화면이 바뀔 수 있는 길목(`pushRoute`·`render`·`applyRoute`)에서 견줘 다르면 닫습니다. 15초마다 도는 목록 갱신처럼 화면 이름이 그대로인 재그리기에는 닫히지 않습니다 — 사유를 쓰는 중에 창이 사라지면 그게 더 나쁩니다. 같은 탭 안에서 **다른 문서로 옮겨 가는 것도** 화면 이동으로 셉니다.

**본 것**: 10스킨 × 라이트/다크 20장 전부(첨부 3장째). 머리의 주소·본문의 제목·두 줄 힌트가 전부 서고, 어느 스킨에서도 잘리거나 겹치지 않습니다. 탭 이동 뒤 창이 사라지는 것은 graph·audit·docs 에서 확인했습니다(terminal 탭은 실시간 연결 때문에 헤드리스 캡처가 끝나지 않아 못 찍었습니다 — 같은 코드 경로입니다).

**판단한 것 하나**: 사유가 비어 있는 동안에는 힌트 자리가 "한 줄이라도 적어야 누를 수 있어요" 를 말하고 키 안내는 나오지 않습니다. 못 누르는 이유가 더 급하고, 이제 Enter=줄바꿈은 입력란의 당연한 동작이라 가르칠 것이 없다고 봤습니다. 한 글자만 적으면 키 안내가 나옵니다.

[Image: assets/REQ-20260828-007/a-reject.png]
[Image: assets/REQ-20260828-007/a-long.png]
[Image: assets/REQ-20260828-007/sheet.png]
[Image: assets/REQ-20260828-007/nav-docs.png]

### 2026-08-28T09:10:51+09:00 designer (by nicehugepark)

반려 반영. 용어를 정리했습니다 — `python3 tests/` 전체 1307건 통과.

**'전이'를 화면에서 전부 지웠습니다.** 창머리는 이제 승인·반려·상태 옮기기·취소가 모두 `판정` 하나입니다. 남은 '전이'는 코드 주석뿐이고, 테스트가 실행되는 줄에 그 낱말이 다시 들어오면 실패합니다.

버튼은 그 자리에서 일어나는 일을 말합니다: `반려하기` · `승인하기` · `상태 옮기기` · `취소하기`.

**승인하면 무엇이 되는지가 창 안 문장에 있습니다.** 전에는 마우스를 올려야 보이는 귀띔에만 있었습니다.

    「판정 창 세 가지」 을(를) 승인해 done 상태로 넘깁니다
    「판정 창 세 가지」 을(를) 반려해 in-progress 상태로 돌려보냅니다
    「판정 창 세 가지」 을(를) cancelled 상태로 옮깁니다

문장에 "…상태로"를 끼운 것은 조사 때문입니다. `done 으로`·`in-progress 로` 는 이름마다 갈려 어느 쪽으로 통일해도 절반이 틀립니다. "상태로"를 끼우면 어느 이름에도 문장이 섭니다.

**상태 이름은 번역하지 않았습니다.** `done`·`in-progress` 는 화면에만 있는 낱말이 아니라 문서 앞머리·CLI 출력·커밋 메시지에 같은 글자로 박혀 있는 **이름**입니다. 화면만 한글로 바꾸면 화면에서 본 말과 문서에서 읽는 말이 달라집니다.

**대신 "왜 여기만 한글이지"의 진짜 원인을 고쳤습니다 — 낱말이 아니라 생김새였습니다.** 같은 줄에 서는 두 종류를 한 글꼴로 그리고 있었습니다: `→ done` 은 상태의 **이름**이고 `✓ 승인` 은 사람이 하는 **일**인데, 둘 다 고정폭에 자간까지 걸려 같은 급으로 읽혔습니다. 이제

- 이름(`→ done`, `→ blocked`, 창 안 문장의 `done`)은 고정폭 식별자로,
- 행위(`✓ 승인`, `↺ 반려`, `⏸ 보류`, `→ 이어 말하기`)는 본문 글꼴로 섭니다.

한글에 자간을 거는 것은 이 제품의 디자인 문서가 이미 금하고 있던 것이기도 합니다.

**본 것**: 반려·승인·상태 옮기기·취소 네 창(첨부 1)과, 문서 화면의 두 경우 — review 문서(승인·반려가 본문 글꼴, 첨부 2)와 그 밖의 상태(`→ blocked · → cancelled · → done · → review` 가 고정폭, 첨부 3). 앞서 올린 셋(제목·Enter·화면 이동)은 그대로입니다.

[Image: assets/REQ-20260828-007/sheet4.png]
[Image: assets/REQ-20260828-007/act-review.png]
[Image: assets/REQ-20260828-007/act-REQ-20260828-007-62x6.png]

### 2026-08-28T14:47:25+09:00 response (by nicehugepark)

### 3차 반려 반영 — 판정을 한 곳으로 모았습니다

사용자: "보드 화면에서 승인을 할 때는 '승인하기'이고 문서에서 승인을 할 때에는
'상태옮기기' 라고 나온다. 판정 이 단계만 보거나, 국소적으로 판단하지말고,
전체적인 디자인, 흐름, 맥락을 다 챙기도록 해."

### 원인은 문구가 아니라 길이 둘이었다는 것입니다

    보드 카드의 ✓ 승인    data-approve  →  승인 창 ("승인하기")
    문서 화면의 ✓ 승인    data-trans    →  일반 이동 창 ("상태 옮기기")

**반려만 두 길이 한 함수를 쓰고 있었고 승인은 갈라져 있었습니다.** 그래서 같은
`✓ 승인` 버튼이 어느 화면에서 눌리느냐에 따라 다른 창을 띄웠습니다. 문구를
맞추는 것으로는 또 갈립니다 — 이 저장소가 반복해 배운 것과 같습니다:
**판정이 두 벌이면 한 벌만 고쳐집니다.**

### 고친 것

**하나. 판정을 `judgeAct` 한 함수로 모았습니다.** 승인·반려·상태 옮기기·취소가
전부 여기서만 지어집니다. 행동의 이름은 **어디서 왔는가**로 정해집니다 —
`review` 에서 나가는 것만 판정입니다. `in-progress → done` 은 일을 끝낸 것이지
승인이 아니므로 그대로 "상태 옮기기" 입니다.

**둘. 끌어 옮기기도 같은 길로 넣었습니다 (맥락을 챙기라는 말에 해당하는 부분).**
그 길만 창 없이 `drag 이동` 이라고 적혔습니다. 결과가 둘이었습니다 —
① **반려에 사유가 필수라는 규칙이 이 길로만 비껴갔고**(끌어 놓으면 사유 없이
반려됨) ② 같은 행동이 History 에 세 가지 말로 남았습니다(승인 / 반려 / drag 이동).
이제 review 에서 끌어 내면 버튼과 같은 창이 뜹니다. 판정이 아닌 이동은 예전처럼
바로 옮깁니다.

**셋. 없는 길을 가리키던 이름표를 걷어냈습니다.** `RVLABEL` 에 `⏸ 보류` 가
있었는데 상태머신은 `review → blocked` 를 주지 않습니다. 한 번도 그려진 적 없는
이름표였고, 없는 길을 가리키는 이름은 다음 사람이 있다고 믿게 만듭니다.

### 검증

- `judge_dialog` 18/18 (계약 2건 추가), `goto_dispatch` 4/4
- 전체 스위트 1358건 — 실패 2건은 `test_instance_init` 이고 **이 워크트리에서
  `s9 instance init` 이 `main` 가지를 못 찾아 나는 환경 실패**입니다. 제 변경은
  `web/index.html` 과 테스트 두 파일뿐이라 그 테스트가 읽는 것을 건드리지 않습니다.
  워크트리 도입(REQ-20260828-011-62x6)의 부작용이라 리드 몫으로 남깁니다.

### 정직하게 적어 둡니다 — 화면으로는 못 봤습니다

이 워크트리 사본을 띄워 눈으로 확인하려 했으나 서버 기동이 권한에 막혔고,
`file://` 로는 화면이 API 를 기다려 비어 나옵니다. 다만 **창의 생김새는 건드리지
않았습니다** — 이미 승인받은 승인 창 명세를 그대로 재사용했고, 바뀐 것은 문서
화면의 버튼이 그 창으로 가느냐입니다.

### 커밋이 안 됐습니다

이 워크트리에서 `git add`·`git commit` 이 권한에 막혔습니다. 변경은 작업 트리에만
있습니다 — **워크트리를 거두기 전에 리드가 커밋해야 사라지지 않습니다.**

    web/index.html · tests/test_judge_dialog.py · tests/test_goto_dispatch.py

### 2026-08-28T15:06:05+09:00 ask (by nicehugepark)

007은 우선 반려다. 이미지 처럼 버튼의 내용이 다르다고 피드백을 했는데 하나도 반영이 안됐네.
[Image: assets/REQ-20260828-007-62x6/20260828T150515-image.png]
[Image: assets/REQ-20260828-007-62x6/20260828T150532-image.png]

### 2026-08-28T15:22:22+09:00 commit (by nicehugepark)

커밋 78fe985 — 워크트리 워커의 일이 사라지지 않게 (REQ-20260828-011-62x6 후속)

### 2026-08-28T15:22:50+09:00 response (by nicehugepark)

반려 원인: 고침은 있었는데 화면에 닿지 않았다.

14:47 재작업은 실제로 이뤄졌다. 다만 그 무인 작업자가 **워크트리 안에서** 돌았고, 봉투(allowedTools)에 git 이 없어 **커밋하지 못했다.** 변경은 그 워크트리의 작업 트리에만 남았고 본 저장소에는 한 줄도 오지 않았다. 그래서 사용자 화면은 그대로였다. 그 세션이 상신 노트에 "워크트리 안에서 git 커밋도 막혀 변경이 작업 트리에만 있습니다 — 거두기 전에 커밋이 필요합니다" 라고 적어 뒀는데, 리드가 그것을 읽지 않고 review 로 둔 것이 이번 반려의 직접 원인이다.

꺼낸 절차: 그 워크트리에서 리드가 커밋(9fce8bb) → `s9 worktree merge` 로 본 저장소에 합침(864f08b). 충돌 없음. 전체 스위트 1385건 통과.

고침 내용 (합쳐진 것)
- 판정을 `judgeAct(id, to, from)` 한 함수로 모았다. 문서 화면의 버튼이 내는 `data-trans` 는 `id|to|현재상태` 라, review 문서에서 `→ done` 을 누르면 카드의 `✓ 승인` 과 **같은 창**이 뜬다("승인해 done 상태로 넘깁니다" · 버튼 "승인하기"). 반려도 같다.
- review 카드를 끌어 옮겨도 같은 창이 뜬다. 전에는 창 없이 넘어가며 History 에 "drag 이동" 으로만 남아, 사유 없는 반려가 그 길로만 빠져나갔다.
- History 의 말이 하나로 모인다 — 어디서 눌렀든 "승인: …" 또는 "대시보드 승인".

같은 실수가 반복되지 않게 (커밋 78fe985)
- 워크트리 워커 봉투에 add·commit·status·diff·log 를 넣었다. push·checkout·reset·stash 는 주지 않는다.
- git 훅 넷이 `--show-toplevel` 로 자리를 정하고 있어, 워크트리에서 커밋하면 post-commit 이 남기는 커밋 노트가 **워크트리 사본의 vault/** 에 쓰였다(이번에 실제로 그랬다). 이제 스크립트는 제 자리 것을 부르되 S9_ROOT 는 본 저장소로 못박는다.

### 2026-08-28T15:28:06+09:00 ask (by nicehugepark)

다른 문서들은 상태 전이 버튼이 영어로 되어있는데, 리뷰 문서만 한국어로 나와. approve, deny 이런식으로 되어야 하지 않을까? 디자이너, ux writer, architect 모두 같이 고민해봐. 디자인 요소이긴 한데.. 흐음..
[Image: assets/REQ-20260828-007-62x6/20260828T152617-image.png]

### 2026-08-28T15:33:19+09:00 subagent (agent: sub:architect:a63e1df3, by nicehugepark)

`[2026-08-28 15:31:02 KST - architect]`

### 결론 먼저

**판정은 별도 개념이 아니라 전이 그래프의 두 간선에 붙은 속성이다. 그러니 화면도 "판정 문법"과 "상태 문법" 두 벌을 두지 말고, 문법은 하나로 두고 판정 간선에만 이름표를 하나 더 붙여라 — `✓ 승인 → done` · `↺ 반려 → in-progress`.** `approve`/`deny`(사용자 안)는 반대한다: 없는 상태 이름을 발명하는 꼴이고, 사용자가 실제로 느낀 어긋남(언어가 아니라 문법)을 해소하지 못한다. 그리고 이 논의와 별개로, **지금 이 저장소에 실재하는 유일한 낱말 결합 사고는 화면이 쓰고 서버가 파싱하는 `"승인:"` 접두사다** — 라벨을 어떻게 정하든 그건 따로 끊어야 한다.

---

### 1. 판정은 모델에 존재하는가 — 존재한다. 다만 엔티티가 아니라 간선의 속성으로.

화면이 만들어 낸 개념이 아니다. `review` 에서 나가는 두 간선은 **서버가 이미 다른 취급을 한다**:

- `/home/sjpark1/section9/bin/s9:1180` — 승인(review→done)에 한해 goal/TDD 게이트 면제
- `bin/s9:1206`, `1852` — 반려(review→in-progress) 접수, 재작업 유예 등록
- `bin/s9:3981` — `kind_txt = {"in-progress": "반려", "done": "승인"}`, review 에서 나올 때만 통지
- `bin/s9:4245`, `6261` — 반려 후 미착수 탐지
- `bin/s9:6530` — 승인 메모 미확인 목록
- `web/index.html:3976` `judgeAct` 의 `judging = from === "review"`, `4436` `RVLABEL`, `4455` 게이트 회차 파싱

즉 "판정" 은 화면의 각색이 아니라 **열 자리 남짓에서 이미 코드가 믿고 있는 사실**이다. 화면은 그 사실을 정직하게 그리고 있을 뿐이다.

다만 지금 그 속성은 **속성이 아니라 열 개의 `if old == "review"` 로** 존재한다. 새 개념(`Judgment` 같은 것)을 세울 이유는 전혀 없다 — 그건 여섯 개짜리 상태머신 위에 두 번째 상태머신을 얹는 짓이다. 올바른 모델은 하나뿐이다: **전이가 개념이고, 특정 간선이 속성을 더 갖는다**(라벨 · 사유 필수 여부 · 통지 종류 · 게이트 면제).

### 2. 두 어휘를 함께 두는 비용 — 전제 하나를 바로잡아야 한다

프롬프트의 "낱말이 다섯 자리(화면·CLI·문서·History·커밋)에 흩어져 있다"는 **상태 이름에는 참이고 판정 낱말에는 과장이다.** 실측하면:

| 낱말 | 실제로 박히는 자리 |
|---|---|
| 상태 이름 (`done` 등) | YAML 앞머리 · CLI 인자 · History 전이 줄 · 커밋 메시지 · 화면 — **다섯. 전부** |
| 판정 낱말 (`승인`/`반려`) | 화면 버튼 · **History 노트 접두사** · 서버 통지 텍스트 — **셋** |

그래서 상태 이름은 절대 번역하면 안 된다는 designer 의 앞선 판단은 옳다(그건 이름이지 낱말이 아니다). 반면 판정 낱말은 소비자가 셋뿐이고, 그중 **진짜 결합은 딱 하나**다.

**그 하나가 지금 살아 있는 결함이다:**

```
web/index.html:3984   postStatus(id, "done", memo ? "승인: " + memo : "대시보드 승인")   ← 쓰는 쪽 (JS)
bin/s9:6535           if not memo.startswith("승인:"): continue                        ← 읽는 쪽 (Python)
```

화면이 문자열에 의미를 실어 보내고 서버가 그 한글 두 글자를 파싱한다. 언어도 다르고 파일도 다르고 테스트도 이 계약을 걸어 두지 않았다. 여기서 접두사를 건드리면 `s9 approvals` 가 **에러 없이 조용히 빈 목록**이 되고, 그에 매달린 승인 후속 스폰(`bin/s9:4199`)이 소리 없이 죽는다. 사용자가 "승인 메모에 후속 지시를 적었는데 아무 일도 안 일어난다"로 발견하게 된다 — 이 저장소가 REQ-20260824-028 에서 이미 한 번 겪은 유실의 재발이다.

주목할 점: `bin/s9:6530` 은 **바로 윗줄에서 이미 `(review, done)` 쌍을 확인하고 있다.** 한글 접두사는 "메모가 있느냐"의 대용으로만 쓰인다. 구조적으로 불필요한 결합이다.

### 3. 단일 출처를 둔다면 — 자리는 명확하고, 값은 지금 낮다

자리는 고민할 것도 없다. 상태머신이 이미 서버 소유이고 이미 화면에 내려간다:

```
bin/s9:63   TRANSITIONS = {...}
bin/s9:11020  /api/transitions  →  {k: sorted(v)}
web/index.html:3484  TRANS = await fetch("/api/transitions")
```

간선 속성표는 `TRANSITIONS` 바로 옆에 붙어야 하고, 같은 엔드포인트가 함께 내려야 한다:

```
EDGE = {("review","done"):        {act:"승인", verb:"승인하기", mark:"✓", note_prefix:"승인:", reason:"optional"},
        ("review","in-progress"): {act:"반려", verb:"반려하기", mark:"↺", note_prefix:"반려:", reason:"required"}}
```

**그런데 라벨 부분의 값은 지금 규모에서 낮다.** 읽는 쪽이 하나뿐인 표는 단일 출처가 아니라 그냥 간접층이다. 사용자 1명 · 화면 1개 · CLI 1개에서 라벨 레지스트리를 짓는 것은 문제보다 큰 해법이다. 표를 지어 얻는 것은 "`RVLABEL` 이 없는 간선을 가리키던 사고"(3차에서 `⏸ 보류` 제거)의 재발 방지인데, 그건 표 없이 **테스트 한 줄**로 더 싸게 잡힌다: `RVLABEL` 의 키가 `TRANSITIONS["review"]` 의 부분집합인가.

값이 높은 것은 표 전체가 아니라 그중 `note_prefix` 한 칸이다. 그리고 그건 표를 안 지어도 끊을 수 있다 — **쓰기 경로를 하나로**: 화면은 메모 원문만 보내고, `from == "review"` 를 아는 서버가 History 노트를 짓는다. eng-principles 의 "단일 쓰기 경로" 그대로다. 대략 서버 5줄 · 화면 2줄 · 계약 테스트 1건.

### 4. 세 갈래 각각, 앞으로 무엇이 어긋나는가

**A. `approve` / `deny` (사용자 안) — 반대**
- 없는 상태 이름을 발명한다. 소문자 영문 동사는 이 화면에서 곧 상태 식별자의 서체다. 사용자가 `s9 status <id> approve` 를 치게 되고, 그런 전이는 없다. 화면이 CLI 에 존재하지 않는 동사를 가르치는 셈.
- 그 어긋남을 메우려 `s9 approve` alias 를 만들고 싶어진다 → 상태 전이의 쓰기 경로가 둘이 된다. 이 저장소가 세 번 반복해 배운 그 실패("판정이 두 벌이면 한 벌만 고쳐진다").
- 화면은 영어인데 History·통지·워커 프롬프트는 계속 "승인:"·"반려됐다" 다. **지금은 화면과 History 가 같은 말인데 그게 깨진다.** 승인을 누르고 History 를 읽는 사람이 매핑을 손으로 한다.
- 결정적으로, **문제를 못 고친다.** 다른 버튼은 여전히 `→ done`(목적지 명사)이고 이것만 `approve`(행위 동사)다. 언어만 맞췄지 문법은 그대로 둘이다. 사용자가 다음 판정에서 같은 위화감을 다시 말할 확률이 높다 — 이미 서체로 한 번 고쳤는데(2차) 또 물어본 것이 그 증거다.

**B. 전부 상태 이름 (`→ done` · `→ in-progress`)**
- 일관성·번역 비용·CLI 대응은 최상. 진지한 후보다.
- 대신 **`→ in-progress` 가 반려라는 사실이 버튼에서 사라진다.** 사유 필수 · 워커 재스폰 · 담당 세션 통지가 달린 가장 무거운 간선이 "그냥 뒤로 옮기기"로 보인다. 위험한 버튼을 중립적으로 그리는 것은 오조작을 부른다(지금은 창이 막아 주지만, 누르기 전에 예측이 안 되는 건 여전히 나쁘다).
- `done` 으로 가는 길이 둘인데(`in-progress→done` = 일이 끝남, `review→done` = 사람이 승인함) 라벨이 같아지면 **서버가 다르게 취급하는 두 간선이 화면에서 구별 불가**가 된다. 게이트 면제가 붙은 쪽이 어느 쪽인지 화면이 못 말한다.
- 6개월 뒤 "승인 버튼이 어디 갔냐"로 되돌아온다.

**C. 현행 유지**
- `RVLABEL` 이 화면 상수라 상태머신이 바뀌면 조용히 뒤처진다(이미 반대 방향으로 한 번 터졌다).
- 위 §2 의 `승인:` 이중 소유가 그대로 남는다.
- 사용자의 위화감이 해소되지 않는다 — 그게 이 대화의 출발점이다.

### 5. 권고와 반증 조건

**권고: 문법을 하나로 통일한다. 모든 버튼이 목적지 상태를 보이고, 판정 간선만 그 앞에 행위 한 낱말이 더 붙는다.**

```
review 문서:     ✓ 승인 → done        ↺ 반려 → in-progress
그 밖의 문서:            → blocked            → cancelled            → done
```

- 모델을 그대로 그린다: 판정은 전이의 부분집합이고, 부분집합에 이름표가 하나 더 붙는다. 새 개념 없음.
- "왜 여기만 다르지"에 답한다 — 이제 다르지 않다. 더 붙었을 뿐이다.
- 상태 이름은 화면에서 사라지지 않으므로 CLI·YAML·History 와의 1:1 이 유지된다.
- 서체 구분(mono 이름 / 본문체 행위)은 designer 가 이미 세운 것을 그대로 쓴다. 한 버튼 안에 두 서체가 들어가는 것이 오히려 "이름 + 행위"를 눈으로 가른다.
- 버튼 폭이 늘어나는 것은 사실이다. 좁은 스킨에서 판정 카드가 감당하는지는 designer 의 판정 영역으로 넘긴다 — 안 되면 `✓ 승인` 만 남기고 목적지를 귀띔으로 내리는 후퇴안이 있다(현행 C 와 같아짐).

**함께 끊을 것(라벨 결정과 무관하게):** History 노트 접두사의 쓰기 경로를 서버로 모은다. 화면은 메모 원문만 보내고 `bin/s9` 가 `(from,to)` 를 보고 `승인: `/`반려: ` 를 짓는다. `bin/s9:6535` 의 `startswith("승인:")` 은 바로 윗줄의 `(review,done)` 판정과 중복이므로 걷어내고 "메모가 비었는가"로 바꾼다.

**세우지 말 것:** 라벨 레지스트리(`EDGE` 표의 UI 부분). 읽는 쪽이 하나인 표는 단일 출처가 아니다. 대신 계약 테스트 한 줄 — `RVLABEL` 의 키 ⊆ `TRANSITIONS["review"]`.

### 내가 틀렸다는 신호

1. **사용자가 다음 판정 화면에서 "`승인 → done` 은 같은 말을 두 번 하는 것 같다 / 장황하다"고 말한다.** → 중복이 실제로 중복으로 읽힌 것. B(전부 상태 이름)로 가고, 사유 필수·재스폰의 무게는 버튼이 아니라 창에서만 말한다.
2. **`s9 approvals` 가 최근 승인 중 메모가 있었는데도 0건을 낸다.** → §2 의 이중 소유가 터진 것. 내가 "값이 높은 건 표가 아니라 `note_prefix` 한 칸"이라고 한 판단이 맞았다는 증거이자, 지연시킨 대가.
3. **3개월 안에 `RVLABEL` 과 상태머신이 또 어긋난다**(새 간선에 라벨이 없어 조용히 `→ x` 로 떨어지고 아무도 눈치채지 못한다). → 테스트 한 줄로 충분하다는 내 판단이 틀렸다. 그때 라벨 표를 서버로 올린다.
4. **누군가 `s9 approve <id>` 를 만들고 싶어진다** — 즉 CLI 에서도 판정을 별도 동사로 부르는 것이 자연스럽다고 느껴진다. → 판정이 간선 속성이 아니라 일급 개념이었다는 뜻이고, 사용자의 A안이 옳았던 것.

---

참고: 지시대로 파일은 한 줄도 고치지 않았고 `s9 note` 도 남기지 않았다. 이 결론을 `REQ-20260828-007-62x6` 에 기록하는 것은 리드 몫이다.

주요 파일 위치: `/home/sjpark1/section9/web/index.html` (3976 `judgeAct`, 3984 노트 접두사, 4416 `targets`, 4422 `RVLABEL`), `/home/sjpark1/section9/bin/s9` (63 `TRANSITIONS`, 3981 통지 낱말, 6535 `startswith("승인:")`, 11020 `/api/transitions`).

### 2026-08-28T15:34:01+09:00 analysis (by nicehugepark)

architect 의견 (요약 · 결론 먼저)

**판정은 별도 개념이 아니라 전이 그래프의 두 간선에 붙은 속성이다.** 그러니 문법을 두 벌 두지 말고 하나로 두되 판정 간선에만 이름표를 하나 더 붙인다 — `✓ 승인 → done` · `↺ 반려 → in-progress`.

근거
- "판정" 은 화면이 만든 각색이 아니다. 서버가 이미 그 두 간선을 다르게 취급한다: 승인에 한해 goal/TDD 게이트 면제(bin/s9:1180) · 반려 접수와 재작업 유예(1206·1852) · 통지 낱말(3981) · 반려 후 미착수 탐지(4245·6261) · 승인 메모 미확인 목록(6530). 열 자리 남짓이 이미 그 사실을 믿고 있다. 다만 지금은 속성이 아니라 `if old == "review"` 열 개로 존재한다. 새 개념(Judgment)을 세울 이유는 없다 — 여섯 상태 위에 두 번째 상태머신을 얹는 짓이다.
- 낱말이 박히는 자리 수가 다르다. 상태 이름은 다섯 자리 전부(YAML·CLI 인자·History·커밋·화면)라 절대 번역하면 안 되고, 판정 낱말은 셋(화면 버튼·History 노트 접두사·서버 통지)이다.

사용자 안(approve/deny)에 반대하는 이유
- 없는 상태 이름을 발명한다. 소문자 영문 동사는 이 화면에서 곧 상태 식별자의 서체다. `s9 status <id> approve` 를 치게 되는데 그런 전이는 없다.
- 그 어긋남을 메우려 `s9 approve` alias 를 만들고 싶어지고, 그러면 상태 전이의 쓰기 경로가 둘이 된다 — 이 저장소가 세 번 배운 그 실패다.
- 화면만 영어가 되고 History·통지·워커 프롬프트는 "승인:"·"반려됐다" 로 남는다. 지금은 화면과 History 가 같은 말인데 그게 깨진다.
- **결정적으로 문제를 못 고친다.** 다른 버튼은 여전히 목적지 명사(`→ done`)이고 이것만 행위 동사(`approve`)다. 언어만 맞췄지 문법은 그대로 둘이다. 2차에서 서체로 한 번 고쳤는데 또 물어본 것이 그 증거다.

전부 상태 이름으로 통일(B안)의 대가
- `→ in-progress` 가 반려라는 사실이 버튼에서 사라진다. 사유 필수·워커 재스폰·담당 세션 통지가 달린 가장 무거운 간선이 "그냥 뒤로 옮기기"로 보인다.
- `done` 으로 가는 길이 둘인데(in-progress→done = 일이 끝남, review→done = 사람이 승인함) 라벨이 같아지면 서버가 다르게 취급하는 두 간선을 화면이 구별하지 못한다.

라벨과 무관하게 지금 살아 있는 결함 하나 (리드 실측 확인)
    web/index.html:4016  postStatus(id, "done", memo ? "승인: " + memo : "대시보드 승인")
    bin/s9:6535          if not memo.startswith("승인:"): continue
화면이 문자열에 의미를 실어 보내고 서버가 그 한글 두 글자를 파싱한다. 언어도 파일도 다르고 계약 테스트도 없다. 접두사를 건드리면 `s9 approvals` 가 에러 없이 빈 목록이 되고 승인 후속 스폰이 소리 없이 죽는다 — REQ-20260824-028 에서 겪은 유실의 재발. 게다가 6535 바로 윗줄이 이미 (review, done) 을 확인하므로 이 접두사 검사는 구조적으로 불필요하다. **쓰기 경로를 서버로 모으고("메모가 비었는가" 로 바꾼다) 계약 테스트를 건다.**

세우지 말 것: 라벨 레지스트리(간선 속성표의 UI 부분). 읽는 쪽이 하나뿐인 표는 단일 출처가 아니라 간접층이다. 대신 계약 테스트 한 줄 — RVLABEL 의 키 ⊆ TRANSITIONS["review"].

틀렸다는 신호: ① 사용자가 "승인 → done 은 같은 말을 두 번 하는 것 같다"고 하면 B안으로 ② s9 approvals 가 메모 있는 승인에 0건을 내면 접두사 결합이 터진 것 ③ 3개월 안에 RVLABEL 과 상태머신이 또 어긋나면 표를 서버로 올린다 ④ 누군가 `s9 approve` 를 만들고 싶어지면 판정이 일급 개념이었다는 뜻이고 사용자 안이 옳았던 것.

### 2026-08-28T15:34:01+09:00 subagent (agent: sub:ux-writer:ac53d5c8, by nicehugepark)

`[2026-08-28 15:31:07 KST - ux-writer]`

### 결론

**`approve`/`deny` 로 바꾸지 마라.** 지금 규칙(이름은 영문 mono · 사람이 하는 일은 한국어)이 더 정직하다. 그리고 `deny` 는 이 자리에서 **뜻이 틀린 낱말**이다 — 여기서 일어나는 일은 거절이 아니라 되돌려 보내 다시 하게 하는 것이고, 이 화면에는 이미 `거부` 라는 말이 **다른 뜻**으로 쓰이고 있다(서버가 전이를 못 받았을 때의 알림 `cap:"거부"`, `/home/sjpark1/section9/web/index.html:4017`).

다만 사용자의 지적은 **틀리지 않았다.** 고칠 것이 있다. 낱말이 아니라 **`✓ 승인` 버튼만 도착지 이름을 잃은 것**이 원인이다.

---

### 1. `approve`/`deny` 가 아닌 이유 — 세 가지

**하나. 화면의 영문은 번역이 아니라 인용이다.** 이 제품에서 영문이 서는 자리는 하나로 정해져 있다: 저장소가 **실제로 그 글자를 쓰는 곳** — 문서 앞머리 `status: review`, CLI 인자 `s9 status <id> done`, 커밋 메시지, History 줄 `status: review -> done`. `→ done` 은 사용자가 다른 데서 **대조할 원본이 있는** 글자다.

**둘. `approve` 에는 원본이 없다.** 이 저장소 어디에도 approve 라는 글자가 없다. 상태머신에 approve 라는 전이가 없고(`review → done` 이 있을 뿐), `s9 approve` 명령이 없고, History 에도 남지 않는다. 화면에만 있는 영어는 인용이 아니라 **새로 만든 외래어**다. 상태 이름을 한글로 안 바꾼 그 논리 — "화면에서 본 말과 문서에서 읽는 말이 달라진다" — 를 그대로 뒤집으면, 행위 낱말을 영어로 바꾸는 것도 같은 잘못이다. 방향만 반대다.

**셋. 영문으로 맞추면 겉만 같아지고 속이 어긋난다.** `done` 은 **결과 상태의 이름**이고 `approve` 는 **사람의 행동**이다. 한 줄에 `→ done` 과 `approve` 가 나란히 서면 사용자는 둘 다 이름으로 읽고, `approve` 라는 상태가 있다고 믿는다. 이 저장소는 이미 그 사고를 겪었다 — 상태머신에 없는 길을 가리키던 `⏸ 보류` 이름표가 3차 반려에서 걷혔다.

### 2. `deny` 라는 낱말 — 후보 견주기

여기서 실제로 일어나는 일: `review → in-progress`. 문서는 **살아 있고**, 사유가 필수이고, 담당 세션에 `[전이 통지]` 가 가고, 체크된 TDD 시나리오가 초기화되어 재검증이 강제되며(`/home/sjpark1/section9/bin/s9:1204`), 무인 작업자가 재개된다(`:1852`). 즉 이 버튼은 **되돌려 보내 다시 하게 하는** 버튼이다.

| 후보 | 판정 |
|---|---|
| `deny` | **틀렸다.** 권한·요청을 거절해 **끝내는** 말이다. deny 다음에 오는 것은 없다. 실제로는 문서가 다시 굴러가므로 버튼이 거짓말을 한다. 게다가 이 화면의 `거부`(서버가 전이를 못 받음)와 뜻이 겹친다 |
| `reject` | 낫지만 여전히 종결형이고, 같은 줄의 `→ cancelled`(폐기)와 겹쳐 읽힌다 |
| `request changes` | **뜻은 정확하다**(사유 필수라는 규칙과도 맞는다). 그러나 두 낱말이 `→ done` 옆에서 폭이 튄다 |
| `send back` | 뜻은 맞고 어디로 보내는지가 없다 |
| **`반려`** | **가장 정확하다.** 행정 어휘로 "낸 서류를 되돌려 보냄" — 판정이 끝난 것이 아니라 되돌아온 것이다. deny·reject 보다 이 자리의 사실에 가깝다 |

영문 후보 중 뜻만 보고 고르면 `request changes` 다. 그것조차 채택하지 않는 이유는 1번이다.

### 3. 통일한다면 — 통일할 축이 언어가 아니다

**(a) 전부 영문으로**
- 얻는 것: 한 줄이 한 글꼴로 정렬된다. 시각적 균질.
- **잃는 것 ①**: 이 제품에서 사람이 하는 일은 전부 한국어다 — `이어 말하기` · `상태 옮기기` · `그만두기` · `취소하기` · `승인하기`. 그 계열에서 둘만 영어로 튄다. 균질해지는 게 아니라 **불균질의 자리가 옮겨간다.**
- **잃는 것 ② (핵심)**: **승인·반려가 다른 상태 이동과 같은 무게로 읽힌다.** `→ blocked`·`→ cancelled` 는 내가 상태를 하나 옮기는 일이다. 승인·반려는 **판정**이다 — 사유가 필수이고, 남의 세션을 깨우고, 반려는 검증을 무효화해 재작업을 시작시킨다. 값이 다른 행동이다. 지금은 그 차이가 글꼴(본문체 vs mono)과 표식(✓/↺ vs →)으로 보인다. 영문으로 평평하게 만들면 그 신호가 사라지고, 판정이 "상태 하나 옮기는 것"으로 보인다. 잘못 눌렀을 때의 값이 다른데 버튼이 같아 보이는 것은 결함이다.
- 잃는 것 ③: 없는 상태 이름을 믿게 만든다(1-3).

**(b) 전부 한국어로** — 화면의 `완료` 와 문서의 `status: done`, CLI 의 `s9 status <id> done` 이 다른 글자가 된다. 이미 판정되어 확정된 것(REQ-20260828-007-62x6 2차).

**(c) 지금 규칙 유지** — 대가는 "한 줄에 두 글꼴이 선다" 하나다. 그것이 **불일치가 아니라 구분**이라는 게 화면에서 안 읽히면, 사용자는 지금처럼 "왜 여기만 한글이지"라고 묻는다.

→ **통일할 것은 낱말의 언어가 아니라 버튼의 종류다.** 지금 그 한 줄에는 성격이 셋 섞여 있다: 판정(승인·반려) · 상태 옮기기(`→ blocked` …) · 딴 데로 가기(`→ 이어 말하기`). 사용자가 본 것은 낱말 불일치지만 원인은 셋을 한 줄에 늘어놓은 것이다.

**그리고 진짜 원인은 이것이다: `→ done` 에는 도착지 이름이 있는데 `✓ 승인` 에는 없다.** 판정 창은 이미 `승인해 done 상태로 넘깁니다` 라고 제대로 말하는데, 그 말을 **버튼이 하지 않는다.** 사용자는 버튼만 보고 창을 열지 말지 정한다.

### 4. 권하는 최종 낱말 세트

**버튼 (보드 판정 카드 · 문서 화면 — 반드시 같은 글자로. 한쪽만 고치면 3차 반려가 되풀이된다)**

| 자리 | before | after |
|---|---|---|
| 판정 · 승인 | `✓ 승인` | **`✓ 승인 → done`** (승인=본문체, done=mono) |
| 판정 · 반려 | `↺ 반려` | **`↺ 반려 → in-progress`** |
| 그 밖의 이동 | `→ blocked` `→ cancelled` `→ done` `→ review` | 그대로 |
| 딴 데로 | `→ 이어 말하기` | **`⌨ 이어 말하기`** — `→` 가 "이 상태로 옮김"과 "저기로 감" 두 뜻으로 갈린다 |

`승인 → done` 의 어순은 History 줄(`review -> done`)·판정 창 문장과 같다. 그래서 줄의 모든 버튼이 끝에 상태 이름을 달아 한 계열로 읽히고, 판정 둘만 앞에 한국어 동사를 더 가져 **무게 차이는 남는다.** review 문서의 버튼은 어차피 둘뿐이라(`TRANS[review]` = done, in-progress) 폭 부담은 문서 화면에서 작다 — 보드 카드 폭은 designer 판단 몫으로 남긴다. 폭이 안 되면 차선은 mono 부분만 9px 로 낮추는 것이고, **버튼 글자를 두 화면에서 다르게 하는 것은 차선이 아니다.**

**창 머리(cap)** — 지금 넷 다 `판정` 인데, 3차에서 세운 원칙("review 에서 나가는 것만 판정")과 어긋난다. `in-progress → done` 창이 스스로를 `판정` 이라 부르면서 버튼은 `상태 옮기기` 라고 한다.

| 창 | before | after |
|---|---|---|
| 승인 · 반려 | `판정` | `판정` |
| 그 밖의 이동 · 취소 | `판정` | **`상태 옮기기`** |
| 전이 실패 알림 | `거부` | **`실패`** — "거부"는 사람이 판정에서 거절했다는 말로 읽히는데, 실제로는 서버가 못 받은 것이다 |

**창 안 문장** — 지금 것이 옳다. 한 곳만:

    before  「판정 창 세 가지」 을(를) 승인해 done 상태로 넘깁니다
    after   「판정 창 세 가지」를 승인해 done 상태로 넘깁니다

`을(를)` 은 이 화면에 남은 유일한 서식 편지투다. 제목은 동적이지만 조사는 계산할 수 있다 — 낫표 안 마지막 글자의 받침 유무(`(charCodeAt(0)-0xAC00)%28`)면 끝이고, 한글이 아니면 지금처럼 `을(를)` 로 물러서면 된다.

**확인 버튼** — `승인하기` · `반려하기` · `상태 옮기기` · `취소하기`, 닫기는 `그만두기`. 그대로 두라. 여기 **`취소하기`(cancelled 상태로 옮김)와 `그만두기`(창을 닫음)의 구분은 이미 정확하다** — 나중에 누가 "통일"하겠다며 둘을 합치지 않게 근거를 적어 둔다.

**History** — `승인: <메모>` · `반려: <사유>` 그대로. 바꾸면 안 되는 이유는 5번이다. 하나만:

    before  대시보드 승인        (메모 없는 승인)
    after   승인 (메모 없음)     ※ "대시보드"는 장소이지 내용이 아니고, [via dashboard] 표식이 이미 붙는다

이 값은 `승인:` 으로 **시작하지 않아야** 한다(5-8 참조). 바꾸기 전 `grep -rn '대시보드 승인'` 필수.

### 5. 낱말을 바꾼다면 어디까지 따라가야 하나

가정: `승인`/`반려` 를 다른 글자로 바꾼다. 같이 가야 할 자리 전부 — 이 목록 자체가 결론의 근거다.

1. `/home/sjpark1/section9/web/index.html:4422` `RVLABEL = {done: "✓ 승인", "in-progress": "↺ 반려"}` — 문서 화면 버튼
2. `:3896` 보드 판정 카드의 `✓ 승인` · `↺ 반려` 와 그 title 둘
3. `:4437` 문서 화면 버튼의 귀띔 생성식 — `rv.replace(/^\S+\s/,"") + "하면 …"` 로 **한국어 동사에 어미를 붙이는 문법**이다. 영문을 넣으면 `approve하면 done 상태가 됩니다` 가 나온다
4. `:3980` · `:3989` 판정 창 본문 문장(`승인해 …`, `반려해 …`) — 조사·어미가 한국어 동사에 물려 있다
5. `:3982` · `:3991` 확인 버튼(`승인하기`·`반려하기`)
6. `:3981` · `:3990` 창 안 설명문("비워 두어도 승인됩니다", "사유는 History 에 그대로 남습니다")
7. `:3984` · `:3993` **History 에 쓰이는 접두어** `"승인: " + memo` / `"반려: " + why` / `"대시보드 승인"`
8. **`/home/sjpark1/section9/bin/s9:6535` — `memo.startswith("승인:")`.** 미확인 승인 메모를 리드에게 물려 주는 경로가 이 한국어 접두어를 **파싱한다.** 화면 낱말만 바꾸면 이 기능이 소리 없이 죽는다(승인 메모에 담긴 후속 지시가 리드에게 전달되지 않음). 문구가 문구에서 끝나지 않는 지점이 여기다
9. `/home/sjpark1/section9/bin/s9:3981` `kind_txt = {"in-progress": "반려", "done": "승인"}` — 담당 세션에 가는 `[전이 통지]` 문구
10. `/home/sjpark1/section9/bin/s9-audit-prompt:589` · `:592` · `:608` · `:639`, `bin/s9-audit-session:240`~`:262` — 에이전트가 읽는 안내문("승인 의미면 …", "미확인 승인 메모", "반려성 지적이면 …")
11. `web/index.html:4457`~`:4468` 문서 상단 판정 이력 라벨(`반려`, "이전 판정 이력 … 확인 포인트·반려 사유") — 여기는 상태쌍으로 판별하므로 파서는 안 깨지지만 **표시 낱말이 갈라진다**
12. `tests/test_judge_dialog.py:375`(`ok:"승인하기"`, `ok:"반려하기"`, `ok:"상태 옮기기"`), `tests/test_review_gate.py:91`·`:111`(`"반려: …"` 를 심고 `"반려:"` 를 확인)
13. **되돌릴 수 없는 것**: `— 승인: …` · `— 반려: …` 가 이미 vault 전체의 History 에 박혀 있다. 낱말을 바꾸면 옛 기록과 새 기록이 다른 말이 된다

8번과 13번이 결정적이다. **`승인`/`반려` 는 화면 낱말이 아니라 이 저장소가 이미 쓰고 있는 기록 어휘다** — 상태 이름을 한글로 바꾸지 않는 그 이유와 정확히 같은 이유로, 이 낱말도 영문으로 바꾸지 않는다.

### 용어 사전 (이 결정으로 확정할 것)

| 개념 | 쓰는 말 | 쓰지 않는 말 |
|---|---|---|
| 상태의 이름 | `open` `in-progress` `review` `done` `blocked` `cancelled` (영문 mono, 문서·CLI·커밋과 같은 글자) | 열림 · 진행 중 · 완료 · 막힘 |
| review 에서 나가는 행위 | **판정** / `승인` · `반려` | approve · deny · reject · 결재 · 거절 |
| 그 밖의 상태 이동 | **상태 옮기기** | 전이(코드 말) · 판정 |
| 요청을 폐기 | `취소하기` → `cancelled` | 반려 · 거부 |
| 창을 닫기 | `그만두기` | 취소 |
| 서버가 전이를 못 받음 | **실패** | 거부 |

### 한계 — 정직하게

- 화면으로 못 봤다(읽기 전용 지시). `✓ 승인 → done` 이 보드 카드 폭에서 접히는지는 **designer 가 실제로 그려 확인해야** 한다. 접힌다면 답은 "두 화면을 다르게"가 아니라 "두 화면 다 다른 방식으로"다.
- `⌨` 표식이 이 제품의 계기판 언어에 맞는지도 designer 판단이다. 요지는 글리프가 아니라 **`→` 가 한 줄에서 두 뜻으로 쓰이고 있다**는 것이다.
- 이 보고는 파일을 고치지 않았고 `s9 note` 도 남기지 않았다(다른 세션이 문서를 쥐고 있음). 기록은 리드가 세 관점을 합칠 때 한 번에 남기는 편이 낫다.

### 2026-08-28T15:35:02+09:00 analysis (by nicehugepark)

ux-writer 의견 (요약 · 결론 먼저)

**approve/deny 로 바꾸지 마라. 다만 사용자의 지적은 틀리지 않았다 — 원인은 낱말이 아니라 `✓ 승인` 버튼만 도착지 이름을 잃은 것이다.** 권고는 architect 와 같다: `✓ 승인 → done` · `↺ 반려 → in-progress`.

approve/deny 가 아닌 이유
- **이 화면의 영문은 번역이 아니라 인용이다.** `→ done` 은 사용자가 문서 앞머리·CLI·커밋·History 에서 **대조할 원본이 있는** 글자다.
- **approve 에는 원본이 없다.** 상태머신에 approve 전이가 없고 `s9 approve` 도 없고 History 에도 안 남는다. 화면에만 있는 영어는 인용이 아니라 새로 만든 외래어다. 상태 이름을 한글로 안 바꾼 그 논리를 그대로 뒤집으면 행위를 영문으로 바꾸는 것도 같은 잘못이다.
- 한 줄에 `→ done` 과 `approve` 가 나란히 서면 사용자는 둘 다 이름으로 읽고 approve 라는 상태가 있다고 믿는다 — 상태머신에 없는 길을 가리키던 `⏸ 보류` 이름표가 3차에서 걷힌 그 사고다.

`deny` 라는 낱말 자체가 틀렸다
- 여기서 일어나는 일은 거절이 아니라 **되돌려 보내 다시 하게 하는 것**이다: 문서는 살아 있고, 사유가 필수이고, 담당 세션에 통지가 가고, 체크된 TDD 시나리오가 초기화되며(bin/s9:1204), 무인 작업자가 재개된다(:1852). deny 다음에 오는 것은 없는데 실제로는 문서가 다시 굴러가므로 버튼이 거짓말을 한다.
- 게다가 이 화면에는 이미 `거부` 가 **다른 뜻**으로 있다 — 서버가 전이를 못 받았을 때의 알림(web/index.html:4017 `cap:"거부"`). 뜻이 겹친다.
- 영문 후보 중 뜻만 보면 `request changes` 가 정확하지만 폭이 튀고, 위 이유로 채택하지 않는다. `반려` 는 행정 어휘로 "낸 서류를 되돌려 보냄" 이라 이 자리의 사실에 가장 가깝다.

전부 영문으로 통일할 때 잃는 것 (핵심)
- 이 제품에서 사람이 하는 일은 전부 한국어다(`이어 말하기`·`상태 옮기기`·`그만두기`·`취소하기`·`승인하기`). 둘만 영어가 되면 균질해지는 게 아니라 **불균질의 자리가 옮겨간다.**
- **승인·반려가 다른 상태 이동과 같은 무게로 읽힌다.** `→ blocked` 는 내가 상태를 하나 옮기는 일이고, 승인·반려는 사유가 필수이고 남의 세션을 깨우고 검증을 무효화한다. 잘못 눌렀을 때의 값이 다른데 버튼이 같아 보이는 것은 결함이다.

권하는 낱말 세트
- 버튼: `✓ 승인 → done`(승인=본문체, done=mono) · `↺ 반려 → in-progress` · 그 밖의 이동은 그대로 · `→ 이어 말하기` 는 **`⌨ 이어 말하기`** 로 (지금 `→` 가 "이 상태로 옮김" 과 "저기로 감" 두 뜻으로 갈린다)
- **보드 판정 카드와 문서 화면이 반드시 같은 글자여야 한다.** 한쪽만 고치면 3차 반려가 되풀이된다.
- 창 머리: 승인·반려만 `판정`, 그 밖의 이동·취소는 **`상태 옮기기`** (지금은 넷 다 `판정` 이라 3차에서 세운 원칙과 어긋난다). 전이 실패 알림의 `거부` 는 **`실패`** 로.
- 창 안 문장: `「제목」 을(를)` → **`「제목」를`** — 받침 유무로 조사를 계산할 수 있다(한글이 아니면 지금처럼 물러선다). 이 화면에 남은 유일한 서식 편지투다.
- History: `승인: <메모>`·`반려: <사유>` 그대로. `대시보드 승인` 만 **`승인 (메모 없음)`** 으로 — "대시보드" 는 장소이지 내용이 아니고 `[via dashboard]` 표식이 이미 붙는다. 단 이 값은 `승인:` 으로 시작하면 안 된다(아래 8번).
- `취소하기`(cancelled 로 옮김) 와 `그만두기`(창 닫기) 의 구분은 이미 정확하다 — 나중에 누가 "통일" 하겠다며 합치지 않게 근거를 남긴다.

낱말을 바꿀 때 따라가야 할 자리 (이 목록 자체가 바꾸지 말라는 근거다)
web/index.html: 4422 RVLABEL · 3896 보드 카드와 title 둘 · 4437 귀띔 생성식(`rv.replace(...)+"하면 …"` — 한국어 동사에 어미를 붙이는 문법이라 영문을 넣으면 `approve하면 …` 이 나온다) · 3980·3989 창 본문 · 3982·3991 확인 버튼 · 3981·3990 설명문 · 3984·3993 History 접두어 · 4457~4468 판정 이력 라벨
bin/s9: **6535 `memo.startswith("승인:")`** · 3981 kind_txt 통지 문구
bin/s9-audit-prompt: 589·592·608·639, bin/s9-audit-session: 240~262 (에이전트가 읽는 안내문)
tests: test_judge_dialog.py:375, test_review_gate.py:91·111
그리고 **되돌릴 수 없는 것**: `— 승인: …`·`— 반려: …` 가 이미 vault 전체 History 에 박혀 있다. 바꾸면 옛 기록과 새 기록이 다른 말이 된다.

→ **`승인`/`반려` 는 화면 낱말이 아니라 이 저장소가 이미 쓰고 있는 기록 어휘다.** 상태 이름을 한글로 바꾸지 않는 그 이유와 정확히 같은 이유로 이 낱말도 영문으로 바꾸지 않는다.

한계: 화면으로 못 봤다(읽기 전용 지시). `✓ 승인 → done` 이 보드 카드 폭에서 접히는지는 designer 가 실제로 그려 확인해야 한다.

### 2026-08-28T15:54:13+09:00 decision (by nicehugepark)

사용자 결정 (15:53): "안을 종합해서 진행하고, 이어 말하기도 뭔가 수정할게 있으면 같이 변경해."

확정안 (세 관점 종합)
- 판정 버튼: `승인 done` · `반려 in-progress` — 행위는 본문체 600, 상태 이름은 그 뒤 mono 9.5px muted. 글꼴 규칙을 버리지 않고 **한 자리에 붙인다.**
- **글리프(✓/↺)는 뺀다.** designer 실측: `✓ 승인 done` 은 보드 카드 215px 에서 두 줄로 감기고, 글리프를 빼면 한 줄에 든다. 글리프는 두 버튼을 가르려고 있던 것인데 이제 done/in-progress 가 그보다 강하게 가른다. 축약(`in-prog`)은 기각 — 어디에도 없는 글자를 만드는 순간 "같은 이름" 이라는 전제가 무너진다.
- **보드 판정 카드와 문서 화면이 반드시 같은 글자여야 한다.** 한쪽만 고치면 3차 반려가 되풀이된다.
- `→ 이어 말하기` 에서 `→` 를 뗀다. 두 어휘가 실제로 만나는 유일한 줄에서 그 화살표가 "다섯 번째 목적지" 로 위장한다. 글리프를 쓸지(⌨ 등) 말지는 designer 판단.
- 창 머리: 승인·반려만 `판정`, 그 밖의 이동·취소는 `상태 옮기기`. 전이 실패 알림의 `거부` 는 `실패` 로 (사람이 거절한 것이 아니라 서버가 못 받은 것이다).
- 창 안 문장의 `을(를)` → 받침으로 조사를 계산한다. 한글이 아니면 지금처럼 물러선다.
- History 의 `대시보드 승인` → `승인 (메모 없음)`. **단 이 값은 "승인:" 으로 시작하면 안 된다** (아래 결합 때문).
- 낱말 자체(`승인`/`반려`)는 바꾸지 않는다 — 이미 vault 전체 History 에 박힌 기록 어휘다.

함께 끊을 결합 (낱말과 무관하게 지금 참인 결함)
    web/index.html:4016  postStatus(id, "done", memo ? "승인: " + memo : "대시보드 승인")
    bin/s9:6535          if not memo.startswith("승인:"): continue
화면이 문자열에 의미를 실어 보내고 서버가 그 한글 두 글자를 파싱한다. 계약 테스트도 없다. **쓰기 경로를 서버로 모은다** — 화면은 메모 원문만 보내고 `(from,to)` 를 아는 서버가 접두어를 짓는다. 6535 는 바로 윗줄이 이미 (review, done) 을 확인하므로 "메모가 비었는가" 로 바꾼다. 계약 테스트를 건다.

세우지 말 것: 라벨 레지스트리. 읽는 쪽이 하나뿐인 표는 단일 출처가 아니라 간접층이다. 대신 계약 테스트 한 줄 — RVLABEL 의 키 ⊆ TRANSITIONS["review"].

### 2026-08-28T16:24:22+09:00 response (agent: sub:designer, by nicehugepark)

확정안 그대로 구현했습니다 — 그리고 화면·서버 사이의 결합을 함께 끊었습니다.

### 버튼 (보드 판정 카드 · 문서 화면 — 한 함수가 짓습니다)

    승인 done          반려 in-progress

행위는 본문체 600, 도착지 이름은 그 뒤 mono 9.5px muted. 글리프(✓/↺)는 뺐습니다 —
1440px 에서 카드 폭 215px, 1280px 에서 193px, 둘 다 한 줄에 듭니다(캡처 첨부).

**두 화면이 갈라질 수 없게 만들었습니다.** 보드 카드와 문서 화면이 각자 글자를
갖고 있어서 3차까지 한쪽만 고쳐졌습니다. 이제 `rvLabel(to)` 한 함수가 양쪽 글자를
짓고, 계약 테스트가 `RVLABEL` 이라는 옛 이름이 되살아나는 것까지 막습니다.

### `→ 이어 말하기` 에서 화살표를 뗐습니다

글리프는 붙이지 않았습니다. 같은 줄에서 `→ blocked` 의 화살표는 "이 상태로
옮김"인데 이어 말하기의 화살표는 "저기로 감"이었습니다 — 기호를 다른 기호로
바꾸면 뜻이 둘이라는 사실은 그대로입니다. 갈래가 바뀐다는 말은 그 옆의
헤어라인(.adiv)이 이미 하고 있습니다. 보드 카드의 손잡이(`→ 이어 말하기`)도
같은 글자로 맞췄습니다 — 같은 행동이 두 화면에서 다른 이름을 갖지 않게.

### 창

- 머리: 승인·반려만 `판정`, 그 밖의 이동·취소는 `상태 옮기기`, 전이 실패는 `실패`.
- 문장: `「제목」 을(를)` → `「제목」를` — 낫표 **안** 마지막 글자의 받침으로
  계산합니다(`(charCodeAt(0)-0xAC00)%28`). 잘린 긴 제목에서는 말줄임표 앞 글자로
  정합니다. 한글로 끝나지 않으면 지금처럼 `을(를)` 로 물러섭니다.
  프로젝트 멤버 제외 창에 남아 있던 같은 편지투도 같이 고쳤습니다.
- `?dlg=` 진단 견본도 같은 글자로 맞췄습니다 — 견본이 실물과 다르면 그것도 두 벌입니다.

### 함께 끊은 결합 (이것이 이 회차의 절반입니다)

    before  web/index.html : postStatus(id, "done", memo ? "승인: " + memo : "대시보드 승인")
            bin/s9         : if not memo.startswith("승인:"): continue
    after   web/index.html : postStatus(id, "done", memo)          ← 원문만
            bin/s9         : judge_note(old, new, memo)            ← (from,to) 를 아는 쪽이 짓고
                             judge_memo(note)                      ← 같은 쌍이 되읽는다

접두어를 정하는 근거는 화면에 없고 `(from, to)` 에 있습니다. 그 쌍을 아는 쪽은
서버뿐이므로 짓는 일도 서버가 합니다. `do_transition(..., judge=True, via="dashboard")`
가 판정 문구와 `[via dashboard]` 표식을 함께 붙입니다 — 화면이 문자열을 조립해
보내던 두 자리를 쓰기 경로 한 곳으로 모았습니다.

`approvals_unseen` 은 바로 윗줄이 이미 `(review, done)` 을 확인하므로 한글 접두어
파싱을 지우고 "메모가 비었는가" 하나만 봅니다. vault 에 이미 박힌 옛 꼴
(`대시보드 승인`·`승인: …`)도 `judge_memo` 가 같은 뜻으로 읽습니다.

History 의 `대시보드 승인` 은 `승인 (메모 없음)` 이 됐고, 이 값은 `승인:` 으로
시작하지 않습니다 — 메모 없는 승인이 메모 있는 승인으로 읽히면 후속 작업자가
빈 지시를 들고 깨어납니다.

### 계약 테스트 (tests/test_judge_dialog.py, 29건 통과)

- `RVDEED` 의 키 ⊆ `TRANSITIONS["review"]` — bin/s9 의 상태머신 원본을 직접 읽어 비교.
  라벨 레지스트리는 세우지 않았습니다(읽는 쪽이 하나뿐인 표는 간접층입니다).
- 보드·문서가 같은 함수를 쓰는가 / 옛 `RVLABEL` 이 되살아나지 않았는가
- `judge_note` ↔ `judge_memo` 왕복, 메모 없는 판정이 `승인:` 으로 시작하지 않는가,
  옛 기록이 같은 뜻으로 읽히는가
- **끝에서 끝까지**: 임시 저장소에 REQ 를 만들어 review 까지 올린 뒤 대시보드가
  하는 그대로 원문만 넘겨 `do_transition` 을 부르고, History 줄에
  `— 승인: 이대로 갑시다 [via dashboard]` 가 남는지 확인합니다.

### 곁가지로 고친 것 둘

- 창 머리 캡션의 자간을 0 으로 (`판정` 두 글자에선 안 보이던 것이 `상태 옮기기`
  다섯 글자에서 드러났습니다 — 트래킹은 로마자의 어휘입니다).
- 창 바닥 버튼(`승인하기`·`반려하기`·`상태 옮기기`·`그만두기`)을 본문체로.
  하나도 상태의 이름이 아닌데 `.acts` 기본 옷(mono+자간)을 입고 있었습니다 —
  방금 세운 규칙이 창 안에서만 다르면 그건 규칙이 아니라 우연입니다.

### 한계

- 전체 스위트 1427건 중 `test_response_stamp` 6건이 실패합니다. 이 작업과
  무관합니다 — 다른 세션이 `harness/common/PROTOCOL.md` 를 손대는 중이고
  (`date '+%Y-%m-%d %H:%M:%S KST'` 문구가 `%Z` 로 바뀌어 있습니다), 동시 편집
  규율에 따라 건드리지 않았습니다.
- 창 900px 처럼 아주 좁은 폭에서는 5열 보드의 카드가 130px 대로 눌려
  `반려 in-progress` 가 두 줄이 됩니다. 그 폭에서는 제목·태그도 함께 눌립니다.

[Image: assets/REQ-20260828-007-62x6/j-board.png]
[Image: assets/REQ-20260828-007-62x6/j-board-1280.png]
[Image: assets/REQ-20260828-007-62x6/j-doc.png]
[Image: assets/REQ-20260828-007-62x6/dlg-approve3.png]
[Image: assets/REQ-20260828-007-62x6/dlg-reject2.png]
[Image: assets/REQ-20260828-007-62x6/dlg-move.png]
[Image: assets/REQ-20260828-007-62x6/dlg-fail.png]
[Image: assets/REQ-20260828-007-62x6/j-paper.png]

### 2026-08-28T16:24:46+09:00 subagent (agent: sub:subagent:a17e007a, by nicehugepark)

Appending response note to REQ-20260828-007-62x6

### 2026-08-28T16:25:49+09:00 ask (by nicehugepark)

007 판정 하기 전에 다른 상태의 카드에 대한 상태 전이도 고려해서 판단한게 맞나? 전체 맥락을 다 본게 맞는건가?
[Image: assets/REQ-20260828-007-62x6/20260828T162525-image.png]

## History
- 2026-08-28T08:18:00+09:00 created by nicehugepark (status: open)
- 2026-08-28T08:41:20+09:00 status: open -> in-progress (by nicehugepark) — 디자이너 착수
- 2026-08-28T08:41:21+09:00 status: in-progress -> review (by nicehugepark) — 셋 다 화면에서 확인해 주세요. 판정 창은 Board 카드의 ✓승인·↺반려 버튼이나 문서 화면의 같은 버튼으로 엽니다. **F5 로 새로고침한 뒤** 봐 주세요. 1. **무엇을 판정하는지 보이나** — 창 맨 위 작은 글씨에 문서 번호, 그 아래 큰 글씨에 제목이 낫표로 들어갑니다: 「메모가 세션을 요구한다」 을(를) 반려합니다. 카드에서 열든 문서 화면에서 열든 같습니다. 제목이 아주 긴 경우는 http://127.0.0.1:9909/?dlg=long 으로 볼 수 있습니다 — 잘려도 뒤의 "반려합니다"가 남는지 봐 주세요. 2. **엔터가 줄바꿈인가** — 사유를 여러 줄로 쓰다 엔터를 눌러 보세요. 줄만 바뀌고 전송되지 않아야 합니다. 보낼 때는 Ctrl+Enter 입니다(버튼 옆 안내에 그렇게 적혀 있습니다). 3. **화면을 옮기면 닫히는가** — 창을 띄워 둔 채 위쪽 탭(Docs·Graph·Terminal 등)을 눌러 보세요. 창이 사라져야 합니다. 왼쪽 문서 목록에서 **다른 문서를 눌러도** 사라집니다. 반대로 가만히 두고 목록이 저절로 갱신되는 동안에는 사라지지 않아야 합니다(쓰던 사유가 날아가면 안 되니까요).
- 2026-08-28T08:44:19+09:00 status: review -> in-progress (by nicehugepark) — 반려: 승인,반려에 대한 판정인데, 전이 라는 용어가 갑자기 등장한다. 그리고 다른 상태에서는 open, in-progress, done인데 리뷰 단계에서만 문서 본문이든 카드든 한글로 승인/반려 라고 표시된다. 용어를 통일할 필요가 있다. [via dashboard]
- 2026-08-28T09:11:07+09:00 status: in-progress -> review (by nicehugepark) — **F5 로 새로고침한 뒤** 봐 주세요. 네 가지입니다. 1. **'전이'라는 말이 없나** — 문서 화면에서 `→ done` 같은 버튼을 눌러 보세요. 창 맨 위가 `판정` 이고 버튼이 `상태 옮기기` 입니다. 화면 어디에도 '전이'가 없어야 합니다. 2. **승인하면 무엇이 되는지 창 안에서 읽히나** — 승인 창에 "「제목」 을(를) 승인해 done 상태로 넘깁니다", 반려 창에 "…반려해 in-progress 상태로 돌려보냅니다" 라고 적힙니다. 마우스를 올리지 않아도 보여야 합니다. 3. **이름과 행위가 달라 보이나** — 문서 화면 위쪽 버튼 줄을 보세요. `→ blocked`·`→ done` 은 고정폭 글자(문서 앞머리·CLI 에 적히는 그 글자 그대로)이고, `✓ 승인`·`↺ 반려`·`→ 이어 말하기` 는 보통 글꼴입니다. 같은 줄에 있어도 두 종류로 읽히면 됩니다. · 상태 이름을 한글로 바꾸지 않은 이유: 그 글자는 문서 앞머리·CLI·커밋 메시지에 같은 글자로 박혀 있어서, 화면만 한글로 바꾸면 화면에서 본 말과 문서에서 읽는 말이 달라집니다. 이 판단이 뜻과 다르면 반려해 주세요. 4. **앞서 올린 셋** — 창 제목에 문서 제목이 보이는지, 사유 입력란에서 엔터가 줄바꿈인지(보내기는 Ctrl+Enter), 창을 띄운 채 탭을 옮기면 창이 닫히는지.
- 2026-08-28T14:29:31+09:00 status: review -> in-progress (by nicehugepark) — 반려: 보드 화면에서 승인을 할 때는 '승인하기'이고 문서에서 승인을 할 때에는 '상태옮기기' 라고 나온다. 판정 이 단계만 보거나, 국소적으로 판단하지말고, 전체적인 디자인, 흐름, 맥락을 다 챙기도록 해. [via dashboard]
- 2026-08-28T14:47:37+09:00 status: in-progress -> review (by nicehugepark) — F5 후 확인해 주세요. 1) 같은 승인이 같은 창을 여나 — 보드 카드의 ✓승인 을 눌러 보고, 같은 문서를 열어 문서 화면 위쪽의 ✓승인 도 눌러 보세요. 두 창의 머리·문장·버튼("승인하기")이 같아야 합니다. 전에는 문서 쪽만 "상태 옮기기"가 떴습니다. 반려도 같습니다. 2) 끌어 옮겨도 같은 창이 뜨나 — review 카드를 done 컬럼으로 끌어 놓아 보세요. 이제 승인 창이 뜹니다(전에는 창 없이 바로 넘어가며 History 에 "drag 이동"으로만 남았습니다). in-progress 컬럼으로 끌면 사유를 묻습니다 — 사유 없는 반려가 이 길로만 빠져나가고 있었습니다. review 가 아닌 카드는 예전처럼 바로 옮겨집니다. 3) History 의 말이 하나인가 — 승인한 문서를 열어 맨 아래 이력을 보세요. 어디서 눌렀든 "승인: …" 또는 "대시보드 승인"으로 남습니다. ※ 못 한 것 둘: 이 워크트리 사본을 띄워 눈으로 보려 했으나 서버 기동이 권한에 막혀 화면 캡처는 없습니다(창의 생김새는 건드리지 않았고 이미 승인받은 명세를 그대로 씁니다). 그리고 워크트리 안에서 git 커밋도 막혀 변경이 작업 트리에만 있습니다 — 거두기 전에 커밋이 필요합니다.
- 2026-08-28T15:06:12+09:00 status: review -> in-progress (by nicehugepark) — 채팅 반려: 버튼 문구 피드백이 하나도 반영되지 않았다 (캡처 2장)
- 2026-08-28T15:22:50+09:00 status: in-progress -> review (by nicehugepark) — **F5 로 새로고침한 뒤** 봐 주세요. 지난번에 안 보였던 이유부터: 고침은 되어 있었는데 작업자가 그것을 커밋하지 못해 본 저장소로 오지 않았습니다. 지금은 합쳐져 있습니다. 1. **같은 승인이 같은 창을 여나** — Board 카드의 `✓ 승인` 을 눌러 창을 보고 그만두기, 같은 문서를 열어 문서 화면 위쪽의 `→ done` 을 눌러 보세요. 두 창의 문장("「제목」 을(를) 승인해 done 상태로 넘깁니다")과 버튼("승인하기")이 같아야 합니다. 보내신 캡처에서 문서 쪽만 "done 상태로 옮깁니다 / 상태 옮기기" 였던 그 차이가 없어야 합니다. 반려도 같습니다. 2. **끌어 옮겨도 같은 창이 뜨나** — review 카드를 done 열로 끌어 놓아 보세요. 승인 창이 뜹니다. in-progress 열로 끌면 사유를 묻습니다. review 가 아닌 카드는 예전처럼 바로 옮겨집니다. 3. **이력의 말이 하나인가** — 승인한 문서를 열어 맨 아래 이력을 보세요. 어디서 눌렀든 "승인: …" 또는 "대시보드 승인" 으로 남습니다. 아직 다르게 보이는 자리가 있으면 그 화면을 캡처해 주세요.
- 2026-08-28T15:28:20+09:00 status: review -> in-progress (by nicehugepark) — 채팅 반려: review 문서만 버튼이 한국어(승인/반려)라 다른 상태의 영어 이름과 섞인다 — approve/deny 꼴로. designer·ux-writer·architect 함께 검토 요청
- 2026-08-28T16:24:44+09:00 status: in-progress -> review (by nicehugepark) — **F5 로 새로고침한 뒤** 봐 주세요. 네 가지입니다. 1. **버튼이 어디로 가는지 말하나** — Board 의 review 열 카드를 보세요. 버튼이 `승인 done` · `반려 in-progress` 로, 뒤에 도착할 상태 이름이 작은 고정폭 글씨로 붙어 있습니다. 체크 표시(✓)와 되돌림 표시(↺)는 뺐습니다 — 그걸 두면 좁은 카드에서 글자가 두 줄로 감겼습니다. 2. **같은 문서를 열어도 같은 글자인가** — 그 카드를 눌러 문서를 열고 제목 아래 버튼 줄을 보세요. `승인 done` · `반려 in-progress` 로 카드와 **같은 글자**여야 합니다. 지난 세 번은 여기가 매번 달랐습니다. 그 오른쪽, 세로 선 하나 건너 `이어 말하기` 가 있습니다 — 앞에 붙어 있던 화살표를 뗐습니다(옆의 `→ blocked` 와 뜻이 달라 헷갈리던 그 화살표입니다). 3. **창의 말** — `승인 done` 을 눌러 보세요. 창 맨 위가 `판정` 이고, 문장이 「제목」를 승인해 done 상태로 넘깁니다 로 읽혀야 합니다. `을(를)` 이 아니라 제목 끝소리에 맞는 조사 하나가 나옵니다. 그만두고, 이번엔 다른 문서에서 `→ blocked` 같은 버튼을 눌러 보세요 — 그 창은 머리가 `상태 옮기기` 입니다. 승인·반려만 판정이라고 부릅니다. 4. **승인한 뒤 이력** — 아무 문서나 승인해 보고(메모는 비워도 됩니다) 문서 맨 아래 이력을 보세요. 메모를 적었으면 `승인: 적은 말`, 비웠으면 `승인 (메모 없음)` 으로 남습니다. 전에 `대시보드 승인` 이라고 적히던 자리입니다 — 대시보드는 누른 장소이지 남길 내용이 아니어서 바꿨습니다. ※ 화면 밖에서 함께 고친 것: 승인 메모를 다음 작업자에게 물려 주는 기능이 화면이 보낸 글자에서 "승인:" 두 글자를 찾아 읽고 있었습니다. 이제 화면은 적으신 말만 보내고 서버가 문구를 짓습니다 — 버튼 글자를 다시 바꾸더라도 그 기능이 조용히 죽지 않습니다.
