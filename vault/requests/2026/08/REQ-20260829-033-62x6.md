---
id: REQ-20260829-033-62x6
type: request
title: 클레임을 푸는 길이 없다
summary: s9 claim 은 있는데 unclaim 이 없어, 죽은 세션이 문서에 남긴 클레임을 사람이 되돌릴 수 없다
goal: 문서에 남은 죽은 클레임을 근거와 함께 풀 수 있고, 그 이력이 문서에 남는다
status: done
size: S
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: 911949e0
agents: ["sub:backend-developer:aa5cccf1"]
contributions: [{"actor": "sub:backend-developer:aa5cccf1", "item": "`[2026-08-29 20:31:04 KST - backend-developer]`", "started": "2026-08-29T20:29:13+09:00", "ended": "2026-08-29T20:29:13+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/911949e0-5d1b-4cd2-b08d-70fd0415a19a/subagents/agent-aa5cccf14c7b52d44.jsonl"}]
project: section9
parent: REQ-20260828-041-62x6
relates_why: {}
tags: ["session", "workflow", "dashboard"]
created: 2026-08-29T18:25:28+09:00
status_since: 2026-08-29T20:49:48+09:00
updated: 2026-08-29T20:49:48+09:00
priority: 50
---

## Original

실사고 2026-08-29 17:36 (REQ-20260828-041 라운드2 검증 중): 우발적으로 뜬 무인 워커가 프롬프트 지시대로 `s9 last --add --session 0755c082` 로 클레임했고, 확인만 하고 물러났다. 그 세션은 죽었지만 문서에는 session=0755c082 · sessions=[…, 0755c082] 가 그대로 남았다.

실측한 것: 이 잔여 클레임이 깨우기를 막지는 **않는다**. sessions 는 캡처 귀속(_session_owns)에만 쓰이고, session(단수)이 쓰이는 catalog_with_live 의 세션 최근성 경로는 창이 120초라 죽으면 스스로 꺼진다.

그래도 남는 문제 둘.
1. 되돌릴 명령이 없다. `s9 claim` 은 있는데 푸는 짝이 없고, `s9 last --clear` 는 세션 쪽 포인터만 지운다 — 문서 프론트매터의 session/sessions 를 사람이 고칠 길은 파일을 직접 여는 것뿐이다.
2. 그래서 캡처 귀속이 늘어난 채로 굳는다: 죽은 세션이 owners 목록에 영구히 남아, pid 가 재사용된 다른 세션의 응답이 이 문서에 붙을 여지가 열린다.

고칠 자리 후보: `s9 claim <id> --release [--session <8자>]` 한 갈래를 더해 _merge_session_stamp 의 역연산을 두고, 이유를 History 에 남긴다. 지우는 것이 아니라 옮기는 것이므로 근거가 문서에 있어야 한다.

## Notes

### 2026-08-29T20:11:41+09:00 tdd (by nicehugepark)

### TDD 시나리오 (클레임 풀기)

실행: `python3 tests/ claim_release`

- [ ] R1. `s9 claim <id> --release --session <8자>` 가 `sessions` 에서 그 세션을 지운다.
- [ ] R2. `session`(단수)이 그 세션이면 남은 소유자 중 마지막으로 넘어가고, 남은 것이 없으면 빈다.
- [ ] R3. 근거가 History 에 남는다: 시각·누가·어느 세션·남은 소유자, `--reason` 을 주면 그 문장까지.
- [ ] R4. `_session_owns` 가 그 세션에 대해 False 로 돌아선다 — 죽은 세션의 캡처 귀속이 걷힌다.
- [ ] R5. 푸는 것도 진전이 아니다: `updated` 는 그대로다(034 와 대칭 — 풀었다고 멈춤 경보가 꺼지면 안 된다).
- [ ] R6. 세션 바인딩에서도 걷힌다: `active_reqs`·`claim_at` 에서 빠지고, `last_req` 가 그것이면 비워진다.
- [ ] R7. 소유가 아닌 세션으로 풀면 문서는 그대로고 현재 소유자를 알려준다 — 멱등이고 오류가 아니다.
- [ ] R8. `--session` 을 생략하면 `S9_SESSION` 을 쓴다.
- [ ] R9. 없는 문서·요청이 아닌 문서는 거부한다.

### 2026-08-29T20:27:44+09:00 backend-developer (by nicehugepark)

### 낸 것: `s9 claim <id> --release [--session <8자>] [--reason "..."]`

푸는 짝을 **거는 명령과 같은 자리**에 뒀다 — 갈라 두면 한쪽만 고쳐진다.

- `bin/s9:_release_session_stamp(meta, session)` — `_merge_session_stamp` 의 역연산. merge 가 세운 불변식을 그대로 지킨다: `sessions` 는 현재 `session` 을 포함하는 이력이고, 한 명 이하로 줄면 키가 사라진다. 단수 포인터가 풀린 그 세션이면 **남은 이력의 마지막**(가장 최근 승계자)에게 넘긴다.
- `bin/s9:release_doc_session(doc_id, session, user, reason)` — 문서 쓰기 + History 한 줄 + 바인딩 정리. 반환은 `{ok,id,released,session,owners,message}`.
- `bin/s9:_release_binding_claim(doc_id, session)` — 문서 도장만 걷으면 절반만 풀린다. `active_reqs` 는 live 판정(`catalog_with_live`)과 중복 스폰 차단(`rework_claimed`)의 근거라, 그대로 두면 죽은 세션이 계속 그 REQ 를 붙들고 있는 것으로 읽힌다. 그 세션 것만 걷고 다른 세션 바인딩·같은 세션의 다른 REQ 는 건드리지 않는다.
- `bin/s9:cmd_claim()` 에 `--release`·`--reason` 갈래, argparse 에 두 플래그.

### 두 가지 판단과 근거

**① 지우지 않고 옮긴다.** History 에 `- <시각> claim release <세션> (by <사람>) — 남은 소유자: a, b; 사유: …` 한 줄이 선다. 근거가 없으면 다음 사람은 "이 문서에 왜 주인이 없나"를 판정할 수 없다.

**② 푸는 것도 진전이 아니다 — `updated` 를 건드리지 않는다** (REQ-20260829-034-62x6 과 대칭). 되돌리는 명령이 멈춤 경보를 15분 끄면, 되돌릴 수 없는 상태를 하나 더 만드는 셈이다.

**멱등으로 두었다.** 소유가 아닌 세션으로 풀면 오류가 아니라 현재 소유자를 말해 준다 — 오타로 엉뚱한 세션을 넣었을 때 그것으로 알아챌 수 있다. 없는 문서·요청이 아닌 문서는 거부한다(exit 1).

**남는 경계 하나(설계상 의도):** 마지막 소유자를 풀면 도장이 아예 없는 상태가 되고, `_session_owns` 의 하위호환 규칙(도장 없는 구문서는 소유로 간주)이 다시 열린다. 이는 클레임 이전 상태로 되돌아간 것이라 새 구멍이 아니지만, 그래서 **여럿이 잡았던 문서는 죽은 것만 골라 푸는 것이 맞다**.

### 실물 예행 — 실사고의 그 문서로

`REQ-20260828-041-62x6` 프론트매터를 격리 ROOT 로 복사해 그대로 돌렸다(라이브 문서는 건드리지 않았다):

    session: 0755c082 / sessions: ["b8fda8f9","81526441","0755c082"]
      → session: 81526441 / sessions: ["b8fda8f9","81526441"]
      updated: 2026-08-29T18:59:15+09:00  (그대로)
      History: - … claim release 0755c082 (by …) — 남은 소유자: b8fda8f9, 81526441; 사유: …

**라이브 정리는 리드 몫으로 남긴다** (내 스코프는 `bin/s9`·`tests/` 다). 그 잔여 도장은 지금도 `vault/requests/2026/08/REQ-20260828-041-62x6.md` 에 있고, 명령은 이것이다:

    S9_SESSION=911949e0 /home/sjpark1/section9/bin/s9 claim REQ-20260828-041-62x6 \
      --release --session 0755c082 --reason "워커가 확인만 하고 물러났고 그 세션은 죽었다 (REQ-20260829-033-62x6)"

### 시험 — tests/test_claim_release.py (신규 15건)

Red 10건 확인 후 구현. R1 소유자 목록에서 빠진다 / R2·R2b 단수 포인터가 남은 소유자에게 넘어가고, 마지막이면 빈다 / R3·R3b History 근거(시각·누가·세션·남은 소유자·사유) / R4 `_session_owns` 가 그 세션에 False 로 돌아서고 남은 주인은 유지 / R5 `updated` 불변 + 멈춤 판정 그대로 20분 / R6·R6b 바인딩 `active_reqs`·`claim_at`·`last_req` 에서 빠지되 남의 것·다른 REQ 는 그대로 / R7·R7b 비소유 해제는 멱등이고 현재 소유자를 알려준다 / R8 `--session` 생략 시 `S9_SESSION` / R9·R9b 없는 문서·요청 아닌 문서 거부 / R9c 거는 갈래 회귀 없음.

`python3 tests/ claim_release` → Ran 15, OK. 전체는 REQ-034 노트에 숫자 그대로 적었다(Ran 2131, failures=6 — 전부 web/index.html 몫).

### 못 한 것

대시보드에 해제 손잡이는 만들지 않았다 — `web/index.html` 이 다른 사람 몫이라 손대지 않았다. 지금은 CLI 한 줄이 유일한 표면이고, 화면에 붙일 값이 있다고 보면 별도 REQ 가 맞다.

### 2026-08-29T20:28:18+09:00 tdd (by nicehugepark)

### TDD 시나리오 — 전부 초록 (python3 tests/ claim_release → Ran 15, OK)

- [x] R1. `sessions` 에서 그 세션이 빠진다
- [x] R2. 단수 포인터가 남은 소유자에게 넘어가고, 마지막이면 빈다
- [x] R3. History 에 근거 한 줄 (시각·누가·세션·남은 소유자·사유)
- [x] R4. `_session_owns` 가 그 세션에 False 로 돌아서고 남은 주인은 유지된다
- [x] R5. `updated` 불변 — 멈춤 판정은 그대로 20분
- [x] R6. 바인딩 `active_reqs`·`claim_at`·`last_req` 에서 빠지되 남의 것·다른 REQ 는 그대로
- [x] R7. 비소유 해제는 멱등이고 현재 소유자를 알려준다
- [x] R8. `--session` 생략 시 `S9_SESSION`
- [x] R9. 없는 문서·요청 아닌 문서는 거부, 거는 갈래는 회귀 없음

### 2026-08-29T20:29:13+09:00 subagent (agent: sub:backend-developer:aa5cccf1, by nicehugepark)

`[2026-08-29 20:31:04 KST - backend-developer]`

두 건 다 구현·검증 끝났다. 커밋은 하지 않았고 상태 전이도 하지 않았다.

### 고친 것 (파일:함수)

**REQ-034 — 클레임이 멈춤 시계를 되감는다**
- `/home/sjpark1/section9/bin/s9:stamp_doc_session()` — `meta["updated"] = now_iso()` 한 줄 삭제. 도장은 찍히되 진전의 시계는 서지 않는다.

**REQ-033 — 클레임을 푸는 길**
- `bin/s9:_release_session_stamp(meta, session)` (신규) — `_merge_session_stamp` 의 역연산
- `bin/s9:release_doc_session(doc_id, session, user, reason)` (신규) — 문서 + History + 바인딩
- `bin/s9:_release_binding_claim(doc_id, session)` (신규) — 그 세션의 `active_reqs`·`claim_at`·`last_req` 정리
- `bin/s9:cmd_claim()` + argparse — `--release` · `--reason`

### 034 에서 후보 ② 를 고른 근거

**실패 방향이 다르다.** 후보 ①(시계를 '실질 진전'으로 좁힌다)은 판정에 축을 하나 더 세우는데, 그 축이 어느 쓰기 자리 하나를 빠뜨리면 **일하는 문서가 멈춤으로 그려지고**, 그 카드의 깨우기가 일하는 손 위의 두 번째 손이 된다 — 이 저장소가 하루에 네 번 값을 치른 사고다. ② 는 쓰기 자리 하나에서 bump 를 빼는 것이라, 손대지 않은 모든 경로가 지금 동작(넓은 쪽 = 안전한 쪽)을 지킨다. 위험 표면이 **축 하나 대 줄 하나**다.

문서가 적어 둔 ② 의 대가("updated 의 뜻이 갈린다")는 실측하니 거의 값이 없었다: `updated` 쓰기 자리 14곳을 전수 확인했고 진전이 아닌 것은 `stamp_doc_session` 하나뿐이었다(나머지는 전이·노트·첨부·관계·멤버). ETag/캐시 무효화에 쓰는 자리는 없다. 클레임 시각도 잃지 않는다 — 바인딩의 `claim_at` 에 남는다.

**판정은 이미 한 자리였다.** 화면 `catalog_with_live` · CLI `stalled_requests` · 깨우기 `wake_request` 셋 다 `stall_mins()` 하나를 지난다. 모을 것이 없어 대신 시험(C4/C4b)으로 그 일치를 잠갔다.

### 시험

| 파일 | 건수 | Red | 결과 |
|---|---|---|---|
| `/home/sjpark1/section9/tests/test_claim_clock.py` (신규) | 10 | 4건 실패 확인 | OK |
| `/home/sjpark1/section9/tests/test_claim_release.py` (신규) | 15 | 10건 실패 확인 | OK |

C3 이 실사고 재현이다 — 20분 조용한 문서를 워커가 클레임한 직후 `stall_mins` 가 고치기 전엔 `None`(경보 꺼짐), 지금은 `20`.

### 전체 스위트 (숫자 그대로)

```
Ran 2131 tests in 391.006s
FAILED (failures=6, skipped=1)
```

**실패 6건은 전부 `web/index.html` 몫이고 내 변경과 무관하다.**
- `test_stall_pair` 4건 — node 로 뽑아 돌리는 조각 밖에 `wsChip()` 이 정의돼 `ReferenceError: wsChip is not defined`
- `test_wake_handle` 2건 — 새 `wakeDoc()` 이 `stop: false` 와 `title: d.message` 를 잃었다

둘 다 실패 지점이 index.html 문자열/JS 검사라 `bin/s9` 를 지나지 않는다. 화면을 고치고 있는 쪽의 작업 중 상태다.

### 실물 예행 (라이브 문서는 건드리지 않음)

실사고의 그 문서 `REQ-20260828-041-62x6` 프론트매터를 격리 ROOT 로 복사해 그대로 돌렸다:

```
session: 0755c082 / sessions: ["b8fda8f9","81526441","0755c082"]
  → session: 81526441 / sessions: ["b8fda8f9","81526441"]
  updated: 2026-08-29T18:59:15+09:00  (그대로)
  History: - … claim release 0755c082 (by …) — 남은 소유자: b8fda8f9, 81526441; 사유: …
```

**잔여 도장은 지금도 라이브에 있다** (`/home/sjpark1/section9/vault/requests/2026/08/REQ-20260828-041-62x6.md`). 내 스코프가 `bin/s9`·`tests/` 라 정리는 남겨 뒀다:

```
S9_SESSION=911949e0 /home/sjpark1/section9/bin/s9 claim REQ-20260828-041-62x6 \
  --release --session 0755c082 --reason "워커가 확인만 하고 물러났고 그 세션은 죽었다 (REQ-20260829-033-62x6)"
```

### 못 한 것

1. **대시보드 해제 손잡이 없음** — `web/index.html` 이 다른 사람 몫이라 손대지 않았다. 지금 표면은 CLI 한 줄뿐이고, 화면에 붙일 값이 있다면 별도 REQ 가 맞다.
2. **라이브 041 정리** — 위 명령 그대로. 리드 몫.
3. **화면 쪽 실패 6건** — 그 담당이 닫아야 한다.

### 설계상 남긴 경계 (구멍이 아님, 의도)

마지막 소유자를 풀면 도장이 아예 없는 상태가 되고 `_session_owns` 의 하위호환 규칙(도장 없는 구문서는 소유로 간주)이 다시 열린다. 클레임 이전 상태로 되돌아간 것이라 새 구멍은 아니지만, **여럿이 잡았던 문서는 죽은 것만 골라 푸는 것이 맞다**. 문서 노트에도 적어 뒀다.

기록은 `s9 note ... --label backend-developer` 로 두 문서에 남겼고, TDD 체크리스트도 초록으로 갱신했다 (033: 9/9, 034: 7/7). `bin/s9 serve --restart` 로 재기동해 9909 가 200 을 낸다.

### 2026-08-29T20:29:39+09:00 response (by nicehugepark)

실사고의 그 잔여 도장을 실제로 풀었다 (리드).

    s9 claim REQ-20260828-041-62x6 --release --session 0755c082 \
      --reason "워커가 확인만 하고 물러났고 그 세션은 죽었다"
    → 0755c082 클레임 해제 — 남은 소유자: b8fda8f9, 81526441

문서가 `session: 0755c082 · sessions: [b8fda8f9, 81526441, 0755c082]` 에서
`session: 81526441 · sessions: [b8fda8f9, 81526441]` 로 돌아왔고, **`updated` 는 그대로다**
(18:59:15) — 034 가 같은 커밋에서 그 bump 를 뺐기 때문이다. 두 요청이 한 자리에서
맞물린다: 도장을 푸는 것이 진전으로 세어지면 푸는 행위가 다시 경보를 끄게 된다.

History 에 누가·왜가 남았다. 지우는 것이 아니라 옮기는 것이므로 근거가 문서에 있어야 한다.

### 2026-08-29T20:49:29+09:00 commit (by nicehugepark)

커밋 ffb5257 — 클레임이 시계를 되감지 않고, 자리가 사람에게 보인다 (REQ-033·034·030·031·035)

## History
- 2026-08-29T18:25:28+09:00 created by nicehugepark (status: open)
- 2026-08-29T20:06:58+09:00 status: open -> in-progress (by nicehugepark) — drag 이동 [via dashboard]
- 2026-08-29T20:49:48+09:00 status: in-progress -> done (by nicehugepark) — 충족 근거: 커밋 ffb5257, 전체 2158건 OK. 실사고를 재현하는 계약(claim_clock C3: 20분 조용한 문서를 클레임한 직후 stall_mins 가 고치기 전 None → 지금 20)이 green 이고, 라이브 문서 REQ-041 의 잔여 도장을 실제 명령으로 풀어 updated 가 18:59:15 그대로임을 확인했다.
