---
id: REQ-20260829-035-62x6
type: request
title: 돌아오지 않은 작업 일곱
summary: 워크트리에서 끝난 작업이 본 저장소에 안 왔다 — 자동 커밋이 조용히 실패하는 동안 쌓였다
goal: 워크트리에 갇힌 일곱 건이 본 저장소에 들어오거나, 들어오면 안 되는 것으로 판정돼 정리된다
status: done
size: M
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: 911949e0
agents: ["sub:deep-diver:ad36c247", "sub:subagent:a91ca8e7"]
contributions: [{"actor": "sub:deep-diver:ad36c247", "item": "035 갇힌 워크트리 판정", "started": "2026-08-29T20:08:27+09:00", "ended": "2026-08-29T20:20:03+09:00", "result": "done", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/911949e0-5d1b-4cd2-b08d-70fd0415a19a/tasks/ad36c247dec4d2ed4.output"}, {"actor": "sub:subagent:a91ca8e7", "item": "Confirming note appended to REQ-20260829-035-62x6", "started": "2026-08-29T20:19:30+09:00", "ended": "2026-08-29T20:19:30+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/911949e0-5d1b-4cd2-b08d-70fd0415a19a/subagents/agent-a91ca8e74c94b10af.jsonl"}, {"actor": "sub:deep-diver:ad36c247", "item": "`[2026-08-29 20:41:12 KST - deep-diver]`", "started": "2026-08-29T20:20:03+09:00", "ended": "2026-08-29T20:20:03+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/911949e0-5d1b-4cd2-b08d-70fd0415a19a/subagents/agent-ad36c247dec4d2ed4.jsonl"}]
project: section9
relates_why: {}
tags: ["sync", "dashboard", "docs"]
created: 2026-08-29T19:46:17+09:00
status_since: 2026-08-29T20:59:04+09:00
updated: 2026-08-29T20:59:04+09:00
priority: 50
---

## Original

REQ-20260829-028-62x6 이 자동 커밋의 조용한 실패를 걷어내자마자 `s9 loose` 가 처음으로 이것을 보여 줬다 — **끝났는데 본 저장소에 안 온 작업 일곱 건**.

    REQ-20260827-012-62x6 게이트 문턱     미커밋 · 19시간째 · 자동 보존 실패(commit_gate·streams_untracked 시험 실패)
    REQ-20260828-017-62x6 비밀 바깥 경로   안 합친 커밋 1 · 27시간째
    REQ-20260829-001-62x6 게이트 시험      안 합친 커밋 1 · 10시간째
    REQ-20260829-011-62x6 카드가 그대로     안 합친 커밋 1 · 8시간째
    REQ-20260829-015-62x6 반려에 그림       안 합친 커밋 1 · 4시간째 (워크트리 둘)
    REQ-20260829-021-62x6 작업자 세우기     미커밋 · 2시간째 · 자동 보존 실패(worker_stop 시험 실패)

**함부로 합치면 안 된다.** 이것들은 저마다 낡은 기준선 위에 서 있고, 그중 여럿(011·015·001)은
같은 요청이 본 저장소에서 다시 구현돼 이미 커밋돼 있을 수 있다. 그대로 합치면 오늘 것을 되돌린다 —
DOC-20260829-001-62x6 이 지목한 그 실패다. 021 은 방금 본 저장소에서 새로 구현해 f0b51ee 로
들어갔으므로 그 워크트리는 버리는 쪽이 맞다.

한 건씩 판정해야 한다: 그 가지의 diff 가 본 저장소에 이미 있는가 / 없다면 지금 코드 위에서
여전히 맞는가 / 맞으면 합치고 아니면 이유를 적고 거둔다.

## Notes

### 2026-08-29T20:19:17+09:00 research (by nicehugepark)

`[2026-08-29 20:35 KST - deep-diver]` 워크트리 일곱 건 판정 근거. 본 저장소 main 에는 아무 쓰기도 하지 않았다(읽기 git + scratchpad 격리 클론만). 확인 후 워크트리 목록·브랜치·HEAD f0b51ee 그대로임을 확인했다.

### 결론 한 줄씩

| 워크트리 | REQ | 판정 | 근거 |
|---|---|---|---|
| w-829-001-62x6-1787963725 | 829-001 | **merge** | main 에 없는 /tmp 누수 한 줄 제거. `merge-tree` rc=0 무충돌 |
| w-827-012-62x6-1787929230 | 827-012 | **salvage** | `bin/s9-install` 훅 자리 판정만 건진다. 시험 몫은 main 이 더 낫게 다시 함 |
| w-829-015-62x6-1787984160 | 829-015 | **salvage** | 터미널 붙여넣기가 그림만 받는 구멍 한 곳. 나머지는 main 이 더 나감 |
| w-829-015-62x6-1787983294 | 829-015 | **salvage(작음)** | 판정 창 칩의 이모지 폴백 한 줄. 나머지는 015-B 와 중복 |
| w-829-011-62x6-1787971148 | 829-011 | **reap** | 화면 몫이 `eee34b1` 로 이미 main 에 있음(동일 코드). 시험만 다른 이름 |
| w-828-017-62x6-1787899993 | 828-017 | **reap** | `1812236` 로 이미 재구현·승인됨. 워크트리는 **기각된 폼 두 벌 설계** |
| w-829-021-62x6-1787989686 | 829-021 | **reap** | 유일한 파일이 main 커밋본과 **바이트 동일** |

### 건별 근거

### 1. w-829-001 (REQ-20260829-001-62x6) — merge
- 가지가 바꾼 것: `tests/test_commit_gate.py:117` 의 `cls.tmp = tempfile.mkdtemp(prefix="s9gate-")` 한 줄 삭제.
- main 에 있나: **없다.** `grep -n "cls\.tmp" tests/test_commit_gate.py` → 117 행에 그대로. 그 값은 어디서도 쓰이지 않고 지워지지도 않는다(죽은 누수).
- 실측: 격리 클론에서 `python3 tests/ commit_gate` 를 돌리자 러너가 `[임시자리 회수/끝난 뒤] 테스트가 남긴 1개를 거뒀다: s9gate-…` 를 찍었다 — **지금 main 에서 실제로 새고 있다.** REQ-20260829-003-62x6("테스트가 /tmp 를 흘리지 않는다")이 놓친 자리다.
- 충돌: `git merge-tree --write-tree main wt/w-829-001-…` rc=0, 충돌 파일 없음.

### 2. w-827-012 (내용은 REQ-20260829-001-62x6 재작업) — salvage
가지 이름은 827-012 이지만 미커밋 내용은 829-001 의 것이다. 세 조각인데 둘은 main 이 이미 더 낫게 했다:
- `tests/test_streams_untracked.py` 의 `_is_repo()` → main 은 `in_repo()` 로 같은 것을 함(`27f5855`). 중복.
- `tests/test_commit_gate.py` G7/G11 → main 은 `hook_state()` 가 `no-repo/missing/not-ours` 셋을 가르고 G11 을 별도 클래스로 뺐으며 훅 자리를 **제품 코드**(`bin/s9-doctor.git_hooks_dir`)에 위임한다. main 쪽이 낫다.
- **건질 것 하나**: `bin/s9-install:88` 이 아직 `os.path.join(ROOT, ".git", "hooks")` + `os.path.isdir` 이다. 워크트리의 `.git` 은 파일이라 **워크트리에서 `s9-install` 을 돌리면 "git repo 아님 — git hooks 생략"** 하고 훅을 안 심는다. 시험은 고쳤는데 설치자는 안 고친 반쪽이다. `bin/s9-doctor:712` 에 이미 올바른 `git rev-parse --git-path hooks` 판정이 있다.
  - 실측: 그 워크트리에서 `git rev-parse --git-path hooks` → `/home/sjpark1/section9/.git/hooks` (공용 훅, 존재함). 손으로 이은 경로는 없다.

**자동 커밋이 왜 막혔나 (원인 사슬, 재현함)**
1. 증상 — 게이트가 `commit_gate streams_untracked` 실패로 커밋을 멈춘다.
2. 왜 — 가지의 `test_g11_hooks_dir_is_asked_not_guessed` 가 실패한다.
3. 왜 — 그 시험이 임시 저장소를 만들며 `env={**os.environ, …}` 를 그대로 넘긴다. git 이 pre-commit 훅에게 물려준 `GIT_DIR` 이 `git -C <tmpdir>` 을 **이겨**, `init`·`add`·`commit` 이 전부 워크트리 자신의 git 디렉토리로 간다.
4. 왜 안 고쳐졌나 — 이 함정은 main 에서 `76069b9`(REQ-20260829-005-62x6)가 `GIT_ENV_VARS` + `_clean_git_env()` 로 이미 막았다. 이 워크트리는 그 커밋 **이전** 기준선(`c1c0deb`) 위에 서 있다.
5. 부수 피해 — 그 시험이 워크트리 인덱스에 `f` 를 스테이지해 놓고 임시 폴더를 지웠다. 지금 보이는 `AD f` 가 그것이다.

재현(격리 클론에 링크 워크트리를 만들어 그 패치를 얹고):
```
GIT_DIR=$(git rev-parse --absolute-git-dir) python3 tests/ commit_gate
→ FAIL: test_g11_hooks_dir_is_asked_not_guessed
   AssertionError: 1 != 0 : python3: can't open file '/tmp/s9gate-wt-…/main/bin/s9-guard'
→ 실행 뒤 인덱스에 `AD f` 가 생김 (실행 전엔 없었음)
GIT_DIR 없이 돌리면 11건 전부 OK.
```
→ **가지의 미완성 때문이지 main 이 바뀌어서가 아니다.** 그 시험은 버려도 된다.

### 3·4. w-829-015 둘 (REQ-20260829-015-62x6) — salvage
두 워크트리(15:13, 15:47)는 모두 **화면이 확장자 표를 들고 `[Image:]`/`[File:]` 을 짓는** 1차 접근이다. main 은 `e4eb5d2`(18:58)에서 그보다 나아갔다: 표기를 서버 `asset_mark()` 로 옮기고, `/api/status` 가 `atts` 를 받아 왕복을 하나로 줄이고, 라벨을 `ask`→`response` 로 바꾸고, `ATTACH_MAX`·`fmtSize`·`attTooBig` 와 `tests/test_judge_attach_kinds.py` 를 붙였다.
- 그대로 합치면 **되돌린다**: 두 번 두드리는 옛 흐름과 화면 쪽 `attMark` 가 되살아난다. `merge-tree` 도 `web/index.html`·`test_judge_attach.py`·`test_bare_asset_path.py` 세 파일 전부 충돌.
- 가지 시험을 main 트리에 대고 돌린 결과(015-A 8실패1에러 / 015-B 10실패1에러)는 전부 **이름 차이와 폐기된 흐름**이다: `ATT_IMG_RE`↔`isImageName`, `ATTACH_MAX_MB`↔`ATTACH_MAX`, `[Image:]`를 화면이 짓는지 서버가 짓는지. 결함이 아니다.
- `avif` 를 뺀 것도 main 의 **의도된 결정**이다(web/index.html:3190 — 서버 `TYPE_GROUPS["image"]` 와 목록을 맞추려고 뺐고 시험이 두 목록을 맞대어 본다). 되돌리면 안 된다.

**진짜로 안 온 것 둘**
- (015-B) `web/index.html:9873` 터미널 입력줄의 붙여넣기가 아직 `/^image\//` 로 거른다. 같은 터미널의 끌어놓기(9920)와 📎(9905)는 무엇이든 받는다. **pdf·mp4 를 붙여넣으면 조용히 사라진다** — 이 REQ 가 고치려던 바로 그 실패가 입구 하나에 남았다. 015-B 는 `attClipFiles()` 로 세 입구를 하나로 묶어 이미 고쳤다. main 에 이를 잡는 시험도 없다.
- (015-A) `web/index.html:2875` `.dlgatt .chip{font-family:var(--mono)}` 에 이모지 폴백이 없다. 칩의 🖼/📎(6346~6348)가 두부 상자로 떨어질 수 있다. 터미널(`.term` 774, `.ccterm` 795)은 `var(--mono),"Segoe UI Emoji","Noto Color Emoji"` 를 이미 쓴다. 015-A 가 그 스택을 칩에도 붙였다.

### 5. w-829-011 (REQ-20260829-011-62x6) — reap
- 가지가 바꾼 것: `.card.picked{outline:…}` 제거 + `.pkst` 를 11.5px/700/letter-spacing 로 키움.
- main 에 있나: **있다.** `web/index.html:372`(같은 주석) `:399`(`font-weight:700;font-size:11.5px;letter-spacing:.06em`) — `eee34b1`(11:52)이 같은 것을 했다. 가지 커밋은 12:01 로 9분 뒤에 찍힌 자동 보존본이다.
- 실측: 가지의 시험 파일을 **main 트리에 대고** 돌리니 11건 전부 통과. main 자신의 시험도 11건 통과. 시험 이름 셋만 다르다(`…never_redraws_the_card_outline`↔`…does_not_wear_a_border_either` 등). 남은 차이는 문체다.
- 충돌: `tests/test_pick_state_look.py` 충돌, `web/index.html` 은 양쪽이 같아서 무충돌.

### 6. w-828-017 (REQ-20260828-017-62x6) — reap
- 가지가 바꾼 것: `secret_write_dir`/`secret_put`/`secret_rm`/`_secret_where`, `/api/secrets` 에 `external_write`·`shadowed`, 그리고 화면에 **넣기 폼 두 벌**(`secList` / `secXList` 좌우).
- main 에 있나: **같은 요구가 `1812236`(8/28 16:02) 로 이미 들어갔고 사용자가 16:12 에 승인했다.** 이름만 다르다 — `secret_write()`(3198) · `secret_remove()`(3239) · `secret_where()`(3322) · `secret_shadowed()`(3176), 화면은 `.secwhere` radiogroup(13316~13327), 진단 손잡이 `?secwdbg`, 시험 `tests/test_secret_where.py`(23건)·`test_secret_location_key.py`.
- 결정적 근거: 문서의 designer 노트가 **"폼을 두 벌로 만들지 않은 이유"** 를 남겼다(값을 두 번 치게 됨 · 잠겼을 때 죽은 폼 · 어느 쪽이 이기는지 못 말함). 워크트리는 그 **기각된 설계**다. 노트가 댄 변경 파일 경로도 전부 `/home/sjpark1/section9/…` — 작업은 본 저장소에서 이뤄졌고 워크트리 사본은 18:13 에 자동 보존기가 주워 담은 잔재다.
- 합치면 승인된 설계를 뒤엎고 시험 23건을 되돌린다. `merge-tree` 도 5파일 전부 충돌.

### 7. w-829-021 (REQ-20260829-021-62x6) — reap
- 담긴 것: `tests/test_worker_stop.py` 하나(+커밋 메시지 임시파일 `.git-commit-msg.tmp`).
- `diff -q tests/test_worker_stop.py <워크트리>/tests/test_worker_stop.py` → **동일**. main 이 `f0b51ee` 로 같은 파일을 담았고 구현(`bin/s9:2335 worker_stop`, `14855 --stop/--why`)도 함께 들어갔다.
- 게이트가 왜 막혔나: 그 워크트리의 HEAD(`3e029be`)에는 `worker_stop` 이 없다 — **계약 먼저(TDD) 라 일부러 구현을 뺐다.** 워크트리의 커밋 메시지 본인이 그렇게 적었다("구현은 bin/s9 라 이 재작업 세션의 범위 밖이다. 계약만 잠그고 본 저장소에서 잇는다"). 격리 클론(3e029be + 그 파일)에서 재현: `Ran 12, FAILED (failures=4, skipped=8)` — `s9 workers --stop 이 파서에 없다`. main 트리에서는 12건 전부 OK.
- **가지의 미완성이고, 그 미완성은 이미 main 이 메웠다.** 건질 것 0.

### 리드가 칠 명령

```bash
cd /home/sjpark1/section9

### ① merge — 한 줄, 무충돌
bin/s9 worktree merge w-829-001-62x6-1787963725
python3 tests/ commit_gate            # 회수 메시지가 사라지는지 확인

### ② reap 셋 — 합칠 커밋이 있으므로 s9 worktree merge 를 쓰면 안 된다(합쳐 버린다)
for n in w-829-011-62x6-1787971148 w-828-017-62x6-1787899993; do
  git worktree remove state/worktrees/$n
  git branch -D wt/$n
  rm -f state/worktree_owners/$n.json
done
### 021 은 미커밋(=버릴 것)이라 --force 가 필요하다. 담긴 파일이 main 것과 동일함을 위에서 확인했다.
git worktree remove --force state/worktrees/w-829-021-62x6-1787989686
git branch -D wt/w-829-021-62x6-1787989686
rm -f state/worktree_owners/w-829-021-62x6-1787989686.json

### ③ salvage — 워크트리는 버리고 아래 세 조각만 손으로 옮긴다
###   (a) bin/s9-install:87-91  install_git_hooks() 가 .git/hooks 를 지어내는 것을
###       bin/s9-doctor:712 git_hooks_dir() 와 같은 질문으로 바꾼다 (+ makedirs)
###   (b) web/index.html:9873-9877  터미널 붙여넣기 필터 /^image\// 제거
###       (같은 터미널의 drop 9920 · 📎 9905 와 규칙을 맞춘다)
###   (c) web/index.html:2875  .dlgatt .chip 의 font-family 에
###       ,"Segoe UI Emoji","Noto Color Emoji" 를 붙인다 (.term 774 와 같은 스택)
git worktree remove --force state/worktrees/w-827-012-62x6-1787929230
git branch -D wt/w-827-012-62x6-1787929230
git worktree remove state/worktrees/w-829-015-62x6-1787983294
git worktree remove state/worktrees/w-829-015-62x6-1787984160
git branch -D wt/w-829-015-62x6-1787983294 wt/w-829-015-62x6-1787984160
rm -f state/worktree_owners/w-8{27-012-62x6-1787929230,29-015-62x6-1787983294,29-015-62x6-1787984160}.json
```

`s9 worktree merge` 는 미커밋이면 거부하고, 안 합친 커밋이 있으면 **합친다** — 그래서 reap 대상에는 쓰면 안 된다(위에서 git 으로 직접 거둔다).

### 확인 못 한 것
- salvage 세 조각을 실제로 얹은 뒤의 전체 스위트는 돌리지 않았다(고치는 것은 리드 몫이라 코드를 건드리지 않았다). 각 조각의 **필요성**만 현재 코드로 실증했다.
- (b)(c)는 브라우저로 눈 검증하지 않았다 — 코드 독해와 grep 근거다. (b)는 시험이 없으니 고칠 때 시험을 함께 붙이는 편이 낫다.

### 2026-08-29T20:19:30+09:00 subagent (agent: sub:subagent:a91ca8e7, by nicehugepark)

Confirming note appended to REQ-20260829-035-62x6

### 2026-08-29T20:20:03+09:00 subagent (agent: sub:deep-diver:ad36c247, by nicehugepark)

`[2026-08-29 20:41:12 KST - deep-diver]`

일곱 건 판정을 끝냈다. 기록은 `REQ-20260829-035-62x6` 에 `--label research` 로 붙였다. **본 저장소 main 에는 아무 쓰기도 하지 않았다** — 읽기 git + scratchpad 격리 클론(`git clone -s`)에서만 시험했고, 끝난 뒤 HEAD `f0b51ee`·워크트리 7개·브랜치 목록이 그대로임을 확인했다.

### 판정

| 워크트리 | REQ | 판정 | 한 문장 근거 |
|---|---|---|---|
| w-829-001-…963725 | 829-001 | **merge** | main 에 없는 `/tmp` 누수 한 줄 제거이고 `merge-tree` rc=0 무충돌이다 |
| w-827-012-…929230 | (내용은 829-001) | **salvage** | `bin/s9-install` 이 아직 워크트리에서 훅을 못 심는다 — 그 한 조각만 건진다 |
| w-829-015-…984160 | 829-015 | **salvage** | 터미널 붙여넣기가 아직 그림만 받는 구멍 하나가 main 에 남았다 |
| w-829-015-…983294 | 829-015 | **salvage(작음)** | 판정 창 칩의 이모지 폰트 폴백 한 줄뿐, 나머지는 위와 중복이다 |
| w-829-011-…971148 | 829-011 | **reap** | 화면 몫이 `eee34b1` 로 main 에 이미 같은 코드로 들어가 있다 |
| w-828-017-…899993 | 828-017 | **reap** | `1812236` 로 재구현·승인됐고 워크트리는 **기각된 "폼 두 벌" 설계**다 |
| w-829-021-…989686 | 829-021 | **reap** | 담긴 유일한 파일이 main 커밋본과 `diff -q` 로 **바이트 동일**이다 |

### 두 건의 자동커밋 실패 — 원인 사슬 (실측 재현)

**w-827-012**: 가지의 미완성 때문이다. `test_g11` 이 임시 저장소를 만들며 `env={**os.environ,…}` 를 그대로 넘겨, pre-commit 훅이 물려준 `GIT_DIR` 이 `git -C <tmpdir>` 을 이긴다. 격리 워크트리에서 재현했다:

```
GIT_DIR=$(git rev-parse --absolute-git-dir) python3 tests/ commit_gate
→ FAIL: test_g11 … AssertionError: 1 != 0 :
   python3: can't open file '/tmp/s9gate-wt-…/main/bin/s9-guard'
→ 실행 뒤 인덱스에 `AD f` 가 생긴다 (실행 전엔 없음 = 지금 보이는 그 `AD f`)
GIT_DIR 없이는 11건 전부 OK
```
main 은 이 함정을 `76069b9`(REQ-829-005)의 `GIT_ENV_VARS`+`_clean_git_env()` 로 이미 막았다. 워크트리는 그 이전 기준선(`c1c0deb`) 위에 있다.

**w-829-021**: 계약 먼저(TDD) 라 일부러 구현을 뺐다 — 커밋 메시지 본인이 그렇게 적었다. `3e029be` + 그 파일로 재현하니 `Ran 12, FAILED (failures=4)` / `s9 workers --stop 이 파서에 없다`. main 트리에서는 12건 OK. 그 미완성은 이미 `f0b51ee` 가 메웠다.

### 되돌릴 뻔한 것 (합치면 안 되는 이유)

- **015 둘**: main 이 `e4eb5d2`(18:58)에서 표기 판정을 서버 `asset_mark()` 로 옮기고 `/api/status` 가 `atts` 를 받게 해 왕복을 하나로 줄였다. 워크트리(15:13·15:47)는 화면이 확장자 표를 드는 1차 접근이다. 가지 시험이 main 에 대고 8~10건 실패하는데 **전부 이름 차이와 폐기된 흐름**이지 결함이 아니다. `avif` 를 뺀 것도 main 의 의도된 결정이다(`web/index.html:3190` — 서버 `TYPE_GROUPS["image"]` 와 목록을 맞춘 것, 시험이 둘을 맞대어 본다).
- **828-017**: 문서의 designer 노트가 "폼을 두 벌로 만들지 않은 이유"를 표로 남겼는데, 워크트리가 바로 그 두 벌 설계다.

### 건질 세 조각 (모두 지금 코드에서 필요성 실증)

1. `/home/sjpark1/section9/bin/s9-install:88` — `hooks_dir = os.path.join(ROOT, ".git", "hooks")` + `os.path.isdir`. 워크트리의 `.git` 은 파일이라 **워크트리에서 `s9-install` 을 돌리면 "git repo 아님 — git hooks 생략"** 하고 훅을 안 심는다. `/home/sjpark1/section9/bin/s9-doctor:712` 에 올바른 `git rev-parse --git-path hooks` 판정이 이미 있다. 시험은 고쳤는데 설치자는 안 고친 반쪽이다.
2. `/home/sjpark1/section9/web/index.html:9873-9877` — 터미널 붙여넣기만 `/^image\//` 로 거른다. 같은 터미널의 drop(9920)·📎(9905)는 무엇이든 받는다. pdf·mp4 붙여넣기가 조용히 사라지고, 이를 잡는 시험도 없다.
3. `/home/sjpark1/section9/web/index.html:2875` — `.dlgatt .chip{font-family:var(--mono)}` 에 이모지 폴백이 없어 칩의 🖼/📎(6346)가 두부 상자가 될 수 있다. `.term`(774)·`.ccterm`(795)은 `var(--mono),"Segoe UI Emoji","Noto Color Emoji"` 를 이미 쓴다.

### 명령에서 주의할 점 하나

`s9 worktree merge` 는 **안 합친 커밋이 있으면 합쳐 버린다** — reap 대상 넷(011·017·021·827-012)에 쓰면 오늘 작업을 되돌린다. 그 넷은 `git worktree remove` + `git branch -D` + `rm state/worktree_owners/<name>.json` 으로 직접 거둬야 한다. 미커밋이 남은 827-012·021 은 `--force` 가 필요하다. 전체 명령 묶음은 노트 말미에 그대로 적어 뒀다.

### 확인 못 한 것

- salvage 세 조각을 실제로 얹은 뒤의 전체 스위트는 돌리지 않았다(코드를 건드리지 않는 것이 이 조사의 범위였다). 각 조각의 **필요성**만 현재 코드로 실증했다.
- (2)(3)은 브라우저 눈 검증이 아니라 코드 독해·grep 근거다.

### 2026-08-29T20:21:27+09:00 response (by nicehugepark)

### 판정 넷을 실행했다 (리드)

deep-diver 의 판정을 받아 **실행은 리드가 했다** — 잘못 합치면 오늘 하루가 사라지는 자리라
조사자에게는 본 저장소 git 쓰기를 전부 금지했었다.

**합친 것 하나**: `REQ-20260829-001-62x6` — `test_commit_gate` 가 임시 저장소를 `/tmp` 에 남기던
한 줄 제거. `merge-tree` rc=0 무충돌이 실측돼 있었고, 실제 병합도 충돌 없이 붙었다(`69f3b98`).
10시간 동안 못 왔던 이유는 자동 커밋이 조용히 실패하고 있었기 때문이다.

**거둔 것 넷**: `829-011`(화면 몫이 `eee34b1` 로 이미 main 에 있다) · `828-017`(`1812236` 로
재구현·승인됐고 워크트리는 **기각된 "폼 두 벌" 설계**다) · `829-021`(담긴 파일이 main
커밋본과 바이트 동일) · `829-001`(합친 뒤). `s9 worktree merge` 를 쓰지 않고
`git worktree remove` + `git branch -D` + owner 파일 제거로 직접 거뒀다 — merge 는 안 합친
커밋이 있으면 합쳐 버려서 오늘 것을 되돌린다.

**고아 owner 파일 다섯**도 함께 지웠다(워크트리가 없는데 등록만 남아 있던 것 — 009·011 셋·012).

**워크트리 아홉 → 셋.** 총량 천장(6)에 걸려 죽어 있던 워크트리 자리가 다시 살아났다.

### 남은 셋 — 건질 조각이 있어 아직 안 거둔다

세 조각의 **필요성이 지금 코드로 실증돼 있다.** 얹은 뒤 전체 스위트를 돌려 확인하고,
그 다음에 거둔다.

1. `bin/s9-install:88` — `.git` 을 디렉토리로만 보므로 **워크트리에서 s9-install 을 돌리면
   훅을 안 심는다**(워크트리의 `.git` 은 파일이다). 올바른 판정이 `bin/s9-doctor:712` 에
   이미 있다 — 시험은 고쳤는데 설치자는 안 고친 반쪽이다. (w-827-012)
2. `web/index.html:9873` — 터미널 **붙여넣기만** `/^image\//` 로 거른다. 같은 터미널의
   drop·📎 는 무엇이든 받는다. pdf·mp4 붙여넣기가 조용히 사라지고 이를 잡는 시험도 없다. (w-829-015)
3. `web/index.html:2875` — `.dlgatt .chip` 에 이모지 폴백이 없어 칩의 🖼/📎 가 두부 상자가
   될 수 있다. `.term`·`.ccterm` 은 이미 폴백을 쓴다. (w-829-015 둘째)

2·3 은 `web/index.html` 이라 지금 designer(030·031)가 쓰는 중이다. 그 파일이 비면 얹는다.

### 자동커밋이 막혔던 두 건의 진짜 이유 (실측 재현)

- **w-827-012**: 가지의 미완성이다. `test_g11` 이 임시 저장소를 만들며 환경을 그대로 넘겨,
  pre-commit 훅이 물려준 `GIT_DIR` 이 `git -C <tmpdir>` 을 이긴다. main 은 이 함정을
  `76069b9` 의 `GIT_ENV_VARS`+`_clean_git_env()` 로 이미 막았다 — 워크트리가 그 이전 기준선이다.
- **w-829-021**: TDD 라 일부러 구현을 뺀 상태였다(커밋 메시지 본인이 그렇게 적었다).
  그 미완성은 `f0b51ee` 가 메웠다.

### 2026-08-29T20:22:25+09:00 response (by nicehugepark)

### 조각 하나를 건져 얹고 그 워크트리를 거뒀다 (리드)

`bin/s9-install` 의 `install_git_hooks` 가 `<repo>/.git/hooks` 를 **경로로 지어내고**
있었다. 워크트리의 `.git` 은 디렉토리가 아니라 파일이라, 워크트리에서 설치를 돌리면
"git repo 아님 — git hooks 생략" 하고 **말없이 훅을 안 심었다.** 무인 작업자는 워크트리에서
돌므로 그 자리에 훅이 없으면 커밋 게이트도 커밋 기록도 없다.

올바른 판정은 `bin/s9-doctor` 의 `git_hooks_dir()` 에 이미 있었다 — `git rev-parse --git-path
hooks` 로 **git 에게 묻는다**. 시험은 고쳤는데 설치자는 안 고친 반쪽이었고, 그 조각이
워크트리에 20시간 갇혀 있었다. 같은 함수를 같은 이름으로 `s9-install` 에 옮겼다.

계약 `tests/test_install_hooks_path.py` **4건**: 경로를 짓지 않고 묻는다 · 보통 저장소는
평소 자리 · **워크트리에서도 실재하는 절대경로**(워크트리의 `.git` 이 파일임을 시험이
먼저 확인한다) · 저장소가 아니면 여전히 빈 문자열.

그 워크트리(`w-827-012`)를 거뒀다. 남은 것은 `w-829-015` 둘이고, 건질 두 조각이 모두
`web/index.html` 이라 지금 designer 가 쓰는 그 파일이 비면 얹는다.

**워크트리 아홉 → 둘.**

### 2026-08-29T20:49:29+09:00 commit (by nicehugepark)

커밋 ffb5257 — 클레임이 시계를 되감지 않고, 자리가 사람에게 보인다 (REQ-033·034·030·031·035)

### 2026-08-29T20:59:03+09:00 commit (by nicehugepark)

커밋 c3f7f3d — 워크트리에 갇혀 있던 두 조각을 건지고 마지막 둘을 거둔다 (REQ-20260829-035-62x6)

## History
- 2026-08-29T19:46:17+09:00 created by nicehugepark (status: open)
- 2026-08-29T20:06:52+09:00 status: open -> in-progress (by nicehugepark) — drag 이동 [via dashboard]
- 2026-08-29T20:59:04+09:00 status: in-progress -> done (by nicehugepark) — 충족 근거: 일곱 건을 한 건씩 판정해 하나는 합치고(69f3b98) 넷은 거두고 둘에서 조각 셋을 건져 얹었다(ffb5257·이번 커밋). 워크트리 9 → 0, 고아 등록 5건도 정리. 전체 2163건 OK.
