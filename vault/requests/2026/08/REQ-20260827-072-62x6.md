---
id: REQ-20260827-072-62x6
type: request
title: 구간에 메모 달기
summary: 문서 구간을 끌어 고르면 미니 팝업으로 묻고, 답이 그 구간에 붙는다
goal: 문서에서 글을 끌어 고르면 팝업이 뜨고, 거기 쓴 말과 그 답이 그 구간에 붙은 채로 문서에 남는다
status: done
size: L
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: 89f7d26c
sessions: ["3da5b9de", "c7978768", "28e2da9d", "89f7d26c"]
agents: ["sub:designer"]
contributions: [{"actor": "sub:designer", "item": "화면 몫", "started": "2026-08-27T23:39:57+09:00", "ended": "2026-08-27T23:39:57+09:00", "result": "running", "transcript": ""}]
project: section9
relates: ["REQ-20260828-021-62x6"]
relates_why: {"REQ-20260828-021-62x6": "둘 다 읽는 자리에서 벗어나지 않고 가리킨 것을 확인하게 하는 문제다"}
tags: ["auto-audit", "attached", "png", "image"]
created: 2026-08-27T22:39:03+09:00
status_since: 2026-08-28T14:27:48+09:00
updated: 2026-08-28T15:54:13+09:00
priority: 50
---

## Original

문서에 특정 라인에 메모를 추가할 수 있는 기능이 있으면 좋겠고, 특정 단어, 문장, 구간을 드래그 하면 미니 프롬프트 팝업창이 떠서 애드혹 하게 프롬프팅 하고, 그 결과나 응답이 문단에 추가 되었으면 좋겠다.

## Notes

### 2026-08-27T22:41:12+09:00 ask (by nicehugepark)

> ⌖ 미니 프롬프트 팝업창이 떠서

이 구간에 대고 한 말이 실제로 문서에 남는지 확인한다

### 2026-08-27T23:39:57+09:00 (agent: sub:designer, by nicehugepark)

뒤쪽은 끝났고 화면 몫만 남아 디자이너(a9f3cdec)에게 넘겼다. 현재 큐: 071 결함 정리 → 079 → 066 → 064 → 070 → 072 → 073. 리드는 이 문서의 파일을 함께 고치지 않는다 — 같은 web/index.html 을 두 주체가 만지면 남의 미커밋 작업이 사라진다(REQ-20260826-021-62x6).

### 2026-08-28T00:22:20+09:00 tdd (by nicehugepark)

### TDD 시나리오 — 계약 14건 전부 통과 (화면 몫)

- [x] ① 본문에서 글을 끌어 고르면 그 자리에 미니 팝업이 뜨고 **고른 글이 팝업
      안에 보인다**(34자 넘으면 줄인다). 본문(.md) 안에서 고른 것만 받는다 —
      메타표에서 고른 글은 문서의 글이 아니다. 두 글자 미만·400자 초과는 안 뜬다.
- [x] ② 쓴 말이 `doc` + `anchor` 로 전송된다. 고른 글은 손대지 않고 그대로.
      **계약을 다시 썼다**(REQ-20260828-006): 처음에는 "세션이 없으면 말없이
      삼키지 말 것"이었는데 그 요구가 틀린 전제 위에 있었다 — 메모는 기록이지
      메시지가 아니라 세션이 필요 없다. 지금은 `/api/note` 로 간다.
- [x] ③ 앵커 달린 노트를 **양방향**으로 짚는다: 구간 옆 ⌖ → 메모, 메모의
      "⌖ 그 구간" → 구간. 한쪽만 있으면 되돌아올 수 없다.
- [x] ④ **고르기만 하고 아무것도 안 쓰면 조용히 사라진다** — 다음 선택·클릭·
      Esc·스크롤 어느 것에나. 뜨는 것은 버튼 하나뿐이고 쓰는 자리는 눌러야 열린다
      (읽으려고 끄는 사람이 훨씬 많다). 취소하면 아무것도 보내지 않는다.
- [x] ⑤ 쓰는 자리는 071 의 판정 대화상자 그대로다 — 팝업이 두 벌이면 한 벌만
      고쳐진다. 색면·색 하드코딩·스킨 전용 스타일 없음.
- [x] ⑥ 문서가 바뀌어 그 글을 못 찾으면 **못 찾았다고 말한다**. 공백 차이만
      눈감고 그 밖에는 정확히 같은 글만 짚는다. 메모가 제 인용문을 가리키지 않는다.
- [x] ⑦ 앵커 표식(⌖)이 서버가 쓰는 그 글자와 같다 — 다르면 영영 못 읽는다.
- [x] ⑧ 마크다운이 인용(`> …`)을 실제로 그린다. **없던 것**이라 앵커 인용이
      `&gt; ⌖ …` 로 그대로 나오고 있었다.
- [x] ⑨ 진단 파라미터 `?anchor` — 끌어 고른 상태를 손 없이 세운다. 여기에
      `?anclick=jump|back` 을 더했다 — 손잡이를 **실제로 눌러** 어느 화면에
      있는지 제목에 찍는다.
- [x] ⑩ **짚는 손잡이는 화면을 옮기지 않는다** (반려). 이 화면에서 `#` 뒤는
      문서 안의 자리가 아니라 화면 이름이라, 조각 링크(`<a href="#anq0">`)를
      누르면 라우터가 모르는 이름이라 판정해 Board 로 되돌렸다. anchorMark 안에
      `href` 도 `.id =` 도 없을 것, 짚는 **두 방향이 같은 재료**(`<button>` 정확히
      둘)일 것, 리스너는 위임 한 곳일 것, 짚기는 이동이 아니라 스크롤일 것.
- [x] ⑪ **조각을 가로지르는 구간도 찾는다** (2차 반려). 사람이 끌어 고르는
      구간은 굵은 글씨·`코드`·문서 링크를 예사로 가로지르는데, 마크다운은 바로
      거기서 한 문장을 여러 조각으로 쪼갠다. 조각 하나 안만 뒤지면 **문서가
      그대로인데도** "찾지 못했습니다"라고 거짓말한다 — 사용자가 방금 고른 그
      글에 대고. 본문을 이어 놓고 찾고, 찾은 구간을 조각별로 잘라 감쌀 것.
- [x] ⑫ **도착이 눈에 보인다** (2차 반려: "이동이 되지 않는다"). 짚는 두 자리가
      한 화면 안에 있으면 스크롤이 움직일 이유가 없다 — 움직임을 결과의 증거로
      삼은 것이 틀렸다. 굴렀든 아니든 도착한 자리를 잠깐 밝힐 것. 색면 금지,
      레이아웃을 밀지 않을 것, 흔적을 남기지 않을 것, 움직임을 줄인 사람에게도
      보일 것. 그리고 굴리는 판을 **직접 짚을** 것 — 브라우저에 맡기면 이
      화면의 스크롤 상자를 고르지 않는 경우가 있다(눌린 뒤에도 0→0 이었다).
- [x] ⑬ **남긴 메모를 그 자리에서 보여 준다** (2차 반려: "기능이 어떻게
      동작했는지 파악도 안되고"). 창을 닫으면 문서를 다시 그려 방금 남긴
      메모로 데려갈 것. 보고 있지도 않은 문서를 갈아 끼우지는 말 것.
- [x] ⑭ **진단이 캡처에서 읽힌다.** 결과를 화면에 적을 것(제목 표시줄은
      스크린샷에 없다). 손잡이가 생길 때까지 기다렸다 누를 것(고정 시간이면
      느린 기계에서 아무것도 누르지 않고 지나간다). `?anlost` 로 길 잃은
      앵커를 셀 것.

실행: `python3 tests/ anchor_note` 15 OK. 전체 스위트 1356건 중 1건 실패 —
`test_stream_switch.test_off_branch_precedes_fetch` 로, REQ-20260828-013 이
스트림 빈 화면 문구를 버튼으로 바꾸며 생긴 것이고 이 요청과 무관하다.

### 2026-08-28T00:22:20+09:00 designer (by nicehugepark)

**가장 큰 위험부터 설계했습니다 — 읽는 것을 방해하지 않는 것.** 문서를 읽으려고 글을 끄는 사람이 메모를 달려는 사람보다 훨씬 많습니다. 끌 때마다 쓰기 상자가 튀어나오면 문서를 못 읽습니다.

그래서 **뜨는 것은 버튼 하나뿐**입니다:

    ⌖ 「미니 프롬프트 팝업창이 떠서」 에 메모

고른 글이 그 버튼 안에 보이므로 "무엇에 다는 메모인가"가 곧 라벨입니다. 쓰는 자리는 이 버튼을 눌러야 열립니다. 다음 선택·다른 곳 클릭·Esc·스크롤 — 어느 것에나 버튼은 말없이 사라집니다. 아무것도 쓰지 않은 사람에게는 아무 일도 일어나지 않습니다.

**쓰는 자리는 071 의 판정 대화상자 그대로**입니다. 같은 판·같은 키(Enter 확인, Shift/Ctrl+Enter 줄바꿈)·같은 버튼 어휘. 이 제품에 팝업이 두 벌이면 한 벌만 고쳐집니다.

**읽는 쪽은 양방향**입니다. 본문의 그 구간에는 점선 밑줄과 ⌖ 가 붙어 메모로 가고, 메모의 인용 옆 "⌖ 그 구간"이 본문으로 돌아옵니다. 한쪽만 있으면 되돌아올 수 없습니다. 색면으로 칠하지 않았습니다 — 본문은 읽으라고 있는 것이지 강조 표시를 보라고 있는 것이 아닙니다.

**못 찾으면 못 찾았다고 말합니다.** 공백 차이만 눈감고(마크다운 렌더가 줄바꿈을 바꿉니다) 그 밖에는 정확히 같은 글만 짚습니다. 못 찾으면 그 메모 옆에 "문서가 바뀌어 이 구간을 찾지 못했습니다"라고 씁니다 — 엉뚱한 곳을 짚으면서 짚는 척하는 것이 제일 나쁩니다.

**딸린 결함 하나를 함께 고쳤습니다.** 이 화면의 마크다운 렌더러에 **인용(`> …`) 처리가 아예 없었습니다.** 그래서 서버가 남긴 앵커가 `> ⌖ 미니 프롬프트 팝업창이 떠서` 라는 날글자로 보이고 있었습니다(이 문서에 이미 그렇게 남아 있었습니다). 인용을 그리도록 했고, 좌측 세로 색 띠 대신 헤어라인과 여백으로 물렸습니다. 자가 검증 중에 잡은 함정도 적어 둡니다 — 그 줄들은 이미 `esc()` 를 지나와서 `>` 가 아니라 `&gt;` 입니다. 처음엔 `>` 로 찾다가 한 줄도 안 걸렸습니다.

검증: 진단 파라미터 `?anchor` 로 끌어 고른 상태를 세워 팝업을 봤고, 이 문서 자체에 이미 남아 있던 앵커 노트로 양방향 짚기를 확인했습니다.

[Image: assets/REQ-20260827-072-62x6/anc-pop.png]
[Image: assets/REQ-20260827-072-62x6/anc-mark.png]

### 2026-08-28T08:49:04+09:00 ask (by nicehugepark)

> ⌖ 이 문서 자체에 이미 남아 있던 앵커 노트로 양방향 짚기

제대로 되는지 테스트

### 2026-08-28T08:57:48+09:00 (by nicehugepark)

재작업 착수 — 원인은 찾았고, 지금은 web/index.html 이 다른 주체(REQ-20260828-007-62x6 재작업)에게 잡혀 있어 손대지 않고 기다리는 중이다.

**원인**: 구간 옆의 ⌖ 표식이 `<a href="#anq0">` 로 되어 있다(anchorMark). 이 화면은 주소창의 `#` 뒤를 **화면 이름**으로 읽는 곳이라(#board · #docs/REQ-…), 그 링크를 누르는 순간 주소가 `#anq0` 이 되고 라우터가 모르는 이름이라 **Board 로 되돌린다**. 사용자가 본 그대로다.

같은 화면의 다른 '문서 안에서 뛰는 링크'는 전부 href 없이 클릭 핸들러로 스크롤한다 — 메모 쪽의 `⌖ 그 구간`(.anback)도, 스트림의 '첫 언급으로 이동'(.hljump)도. href 를 쓴 것은 이 ⌖ 하나뿐이고, 그래서 이것만 화면을 옮겼다.

**고칠 것**: ⌖ 도 같은 방식으로(data-anjump + 위임 클릭 → scrollIntoView) 바꾼다. href 를 없애면 키보드로 닿을 길이 사라지므로 ⌖ 와 '그 구간' 둘 다 tabindex/Enter 를 함께 준다 — 짝지어 오가는 것이 이 요청의 계약(③)이라 한쪽만 닿으면 반쪽이다. 계약 테스트를 tests/test_anchor_note.py 에 더하고, 진단 파라미터로 실제 클릭 뒤 화면이 그대로인지 헤드리스로 본다.

### 2026-08-28T08:59:43+09:00 conflict (by nicehugepark)

**물러난다 — 다른 주체가 같은 결함을 이미 고치고 있다.**

바통이 비기를 기다리는 동안 web/index.html 이 08:57 에 다시 쓰였고, 그 안에 이 반려 건의 수정이 이미 들어 있었다. 지금 파일에 있는 것:

- `q.id = nid` → `q.dataset.anq = nid` (해시는 이 화면에서 라우트다)
- ⌖ 와 '그 구간' 이 `<a href="#anq0">` 가 아니라 `<button data-anjump/data-anback>`
- 리스너를 anchorMark 안에서 떼고 문서 위임 한 곳으로
- 진단 파라미터 `?anclick=jump|back` — 눌러 보고 tab/hash 를 제목에 찍는다
- 주석에 `(REQ-20260827-072-62x6 반려)` 라고 적혀 있다

내가 내린 진단과 원인은 같다(위 노트). 고친 방식도 사실상 같고, `<button>` 을 쓴 것은 내가 하려던 tabindex/role 보다 낫다 — 키보드로 닿는 것이 공짜로 따라온다.

**내가 한 것과 하지 않은 것.** web/ 은 한 글자도 건드리지 않았다. tests/test_anchor_note.py 에 계약 두 건을 먼저 써 두었다가(TDD), 그 계약이 남의 구현 모양(`?anchor=jump` vs `?anclick=jump`, tabindex vs button)과 어긋나 **그 워커의 검증을 깨뜨리므로 도로 걷어냈다.** 지금 `python3 tests/ anchor_note` 9건 OK — 내가 들어오기 전과 같은 상태다.

**남기는 것 하나** — 그쪽이 계약 테스트를 아직 안 썼다면 이 두 가지는 꼭 못으로 박아 두는 게 좋다: ① anchorMark 안에 `href` 가 다시 생기지 않을 것(이 화면에서 문서 안 조각 링크는 곧 라우트 오염이다), ② 짚는 두 방향이 **같은 재료**일 것(한쪽만 href 였던 것이 이번 결함의 전부였다).

이 문서는 상태를 옮기지 않는다. 지금 손이 붙어 있는 쪽이 상신할 몫이다.

### 2026-08-28T09:12:38+09:00 designer (by nicehugepark)

반려 원인을 재현해 찾았습니다. **본문 구간 옆의 ⌖ 를 누르면 Board 로 튀었습니다.**

**왜 그런지**: 그 손잡이는 `<a href="#anq0">` 였습니다. 이 화면에서 주소 뒤의 `#…` 은 문서 안 자리가 아니라 **어느 탭을 볼지**를 정하는 값입니다. 같은 문서 안 조각으로 뛰는 링크라도 브라우저는 "주소가 바뀌었다"고 알리고(직접 재봤습니다 — 조각 링크를 누르면 `popstate` 가 먼저, `hashchange` 가 뒤에 옵니다), 그 알림을 받은 화면이 `anq0` 을 아는 탭 이름이 아니라고 판정해 **Board 로 떨어뜨렸습니다.**

**메모 쪽의 "⌖ 그 구간" 버튼에서는 Board 로 가는 길을 못 찾았습니다.** 그쪽은 주소가 걸려 있지 않고 클릭을 직접 받고 있었습니다. DOM 을 떠서 위로 올라가 봐도 문서를 여는 요소에 안겨 있지 않습니다. 그러니 사용자가 누른 것은 **구간 옆의 ⌖** 였다고 봅니다 — 그 표식이 짚고 있는 것이 곧 "그 구간"이니 그렇게 부르실 만합니다. 다만 이건 제 추정이고, 확인된 것은 위의 ⌖ 하나입니다.

**고친 것**

- 두 손잡이 다 주소를 쓰지 않습니다. 누르면 그 자리에서 스크롤만 합니다 — 탭도 주소도 그대로입니다.
- `<a>` 가 아니라 `<button>` 으로 바꿨습니다. 주소 없는 앵커는 탭 키로 잡히지 않아, **키보드만 쓰는 사람에게는 아예 없는 손잡이**였습니다. 이제 탭으로 닿고 Enter 로 눌립니다. 생김새는 그대로입니다.
- **클릭을 받는 자리를 한 곳으로 옮겼습니다.** 전에는 문서를 그릴 때마다 새 본문에 새 리스너를 붙이고 떼지 않았습니다. 지금은 같은 일을 반복할 뿐이라 티가 안 나지만, 그 자리에 다른 동작이 붙는 순간 한 번 누르면 여러 번 도는 사고가 됩니다.

**본 것**: 두 방향을 실제로 눌러 확인했습니다(`?anclick=jump` · `?anclick=back` 진단을 새로 뒀습니다 — 손잡이를 눌러 결과를 창 제목에 찍습니다).

    anclick jump tab=docs hash=#docs/REQ-20260827-072-62x6
    anclick back tab=docs hash=#docs/REQ-20260827-072-62x6

둘 다 Docs 에 머물고 주소도 그대로입니다. 고치기 전이라면 앞의 것이 `tab=board` 로 찍혔을 자리입니다.

[Image: assets/REQ-20260827-072-62x6/anchor-jump.png]

### 2026-08-28T09:12:42+09:00 response (by nicehugepark)

**누르면 화면이 바뀌던 것을 확인하고 못으로 박았다.**

반려한 그대로였다: 메모의 "⌖ 그 구간" 을 누르면 Board 로 튀었다. 원인은 그 손잡이가 `<a href="#anq0">` 였다는 것 하나다 — 이 화면에서 `#` 뒤는 문서 안의 자리가 아니라 **화면 이름**(`#board` · `#docs/REQ-…`)이라, 조각 링크를 눌러도 라우터가 "anq0" 을 모르는 이름이라 판정해 Board 로 되돌렸다. 문서 안에서 자리만 옮기려던 손짓이 화면을 통째로 갈아치운 것이다.

**화면 코드는 내가 쓰지 않았다.** 내가 붙었을 때 web/index.html 에는 이 수정이 이미 들어 있었다(다른 주체가 08:57 에 넣었고 주석에 `REQ-20260827-072-62x6 반려` 라고 적혀 있다): 해시 링크 대신 `<button data-anjump/data-anback>`, 리스너는 anchorMark 안이 아니라 문서 위임 한 곳, 짚기는 이동이 아니라 `scrollIntoView`. 그 뒤로도 그 파일은 다른 요청(REQ-20260827-070-62x6)의 무인 작업자가 계속 쓰고 있어서 **한 글자도 건드리지 않았다** — 두 주체가 같은 파일을 만지면 남의 미커밋 작업이 사라진다.

**내가 한 것은 둘이다.**

① **눌러 보고 확인했다.** 반려가 "눌렀더니 화면이 바뀌더라" 였으므로 확인도 눌러 보는 것이어야 한다. 진단 파라미터 `?anclick=jump|back` 으로 헤드리스에서 실제로 클릭했고, 두 방향 모두 **Docs 화면·이 문서 그대로**였다. 캡처 둘을 문서에 붙였다.

② **계약 테스트 두 못.** 다음 사람이 무심코 되돌리지 못하게.
- `test_pointing_at_a_span_does_not_change_the_screen` — anchorMark 안에 `href` 도 `.id =` 도 다시 생기지 않을 것, 짚는 **두 방향이 같은 재료**(`<button>` 정확히 2개)일 것, 리스너를 다시 그릴 때마다 붙이지 않을 것, 그리고 짚기가 스크롤일 것. 이번 결함의 전부가 "한쪽만 href 였다" 이므로 한쪽만 고치면 반쪽이다. 주석은 걷어내고 본다 — 왜 href 를 안 쓰는지 적어 둔 설명이 검사에 걸리면 다음 사람은 설명을 지워서 통과시킨다.
- 진단 계약에 `?anclick` 을 더했다 — 눌러 보는 길이 사라지면 이 결함은 다시 눈으로만 잡아야 한다.

**한 가지 못 한 것.** `?anclick` 은 첫 번째 손잡이를 누른다. 사용자가 누른 것은 **마지막에 생성된** 것이라, 마지막 것까지 눌러 보려면 진단에 `last` 를 더해야 한다 — 그 편집이 web/index.html 이라 물러났다. 다만 두 손잡이는 같은 반복문이 같은 마크업으로 찍어 내고 자리 번호(`anq0`·`anq1`)만 다르며, 누른 것을 받는 곳도 위임 핸들러 한 군데다. 그리고 이 문서의 마지막 앵커(`이 문서 자체에 이미 남아 있던 앵커 노트로 양방향 짚기`)는 본문에서 제 자리를 찾는다(83번째 줄) — "찾지 못했습니다" 가 아니다.

검증: `python3 tests/ anchor_note` 10 OK, 전체 스위트 1308 OK(skipped=1).

[Image: assets/REQ-20260827-072-62x6/anc-back-stays.png]
[Image: assets/REQ-20260827-072-62x6/anc-jump-stays.png]

### 2026-08-28T09:12:50+09:00 conflict (by nicehugepark)

web/index.html 은 손대지 않았다 — REQ-20260827-070-62x6 의 무인 작업자가 같은 파일에 붙어 있었고(09:07 에 다시 쓰였다) 둘이 만지면 남의 미커밋 작업이 사라진다. 하려던 편집은 하나뿐이었다: 진단 파라미터 `?anclick` 이 지금은 첫 번째 손잡이만 누르는데, 반려는 **마지막** 것을 눌러 난 것이므로 `?anclick=back-last` 같은 갈래를 더해 마지막 손잡이도 눌러 볼 수 있게 하는 것. 그 파일에 손이 비면 누구든 넣어도 좋다.

### 2026-08-28T09:15:11+09:00 designer (by nicehugepark)

화면 몫을 고친 것은 저(designer)이고, 위 tdd 노트의 검증·계약 테스트는 다른 주체가 같은 시간에 붙였습니다. 결론은 같습니다.

거기서 "web/index.html 에 손이 비면 누구든 넣어도 좋다"고 남긴 것 하나를 넣었습니다: **진단이 마지막 손잡이도 누릅니다.**

    ?anclick=jump-last  ·  ?anclick=back-last

사용자가 반려하며 누른 것이 마지막 것이었으므로, 첫 번째만 눌러 보는 진단으로는 그 자리를 못 짚습니다. 실제로 눌러 확인했습니다:

    anclick back-last tab=docs hash=#docs/REQ-20260827-072-62x6

`python3 tests/ anchor_note` 10건 OK — 그 주체가 새로 박은 계약(`test_pointing_at_a_span_does_not_change_the_screen`)도 포함해 통과합니다.

### 2026-08-28T09:39:31+09:00 response (by nicehugepark)

**이미 고쳐져 review 로 올라가 있었다 — 내가 더한 것은 못 하나와 재확인 시도다.**

내가 클레임한 직후(09:13) 이 문서는 review 로 올라갔고(09:13:40), 화면 몫도 이미 들어가 있었다. 짚는 두 손잡이가 `<a href="#anq…">` 가 아니라 `<button data-anjump/data-anback>` 이고, 누른 것은 문서 위임 핸들러 한 곳이 받아 `scrollIntoView` 만 한다. 반려의 원인 — 이 화면에서 `#` 뒤는 문서 안의 자리가 아니라 **화면 이름**이라는 것 — 은 그것으로 닫혔다. 09:16 에 다른 주체가 `?anclick=jump-last|back-last` 까지 넣었다(앞선 conflict 노트가 '손이 비면 누구든 넣어도 좋다'고 남긴 그것이다).

**내가 더한 것 — 계약 하나.** `tests/test_anchor_note.py :: test_the_diagnostic_can_press_the_last_handle`. 반려는 **마지막에 생성된** '그 구간' 에서 났다. 첫 번째 손잡이만 눌러 보는 진단으로는 그 자리를 영영 지나치므로, `-last` 갈래가 조용히 사라지지 않도록 못을 박았다. `python3 tests/ anchor_note` 11 OK.

**내가 못 한 것 — 눌러 본 그림을 새로 남기지 못했다.** 직접 다시 눌러 보려고 `?anclick=back-last` · `?anclick=jump-last` 로 캡처를 떴는데 지금 이 기계에서 캡처를 믿을 수 없다: 한 번은 문서 목록이 빈 채로 찍혔고, 다음엔 `ERR_CONNECTION_RESET`, 그다음엔 브라우저가 60초 안에 응답하지 않았다. `s9 doctor` 는 윈도우 동적 포트 82% 소진(13454/16384)이라고 말한다 — 리눅스가 아니라 호스트 쪽 소진이다. 반쯤 그려진 그림을 '확인했다'의 근거로 붙이는 것이 제일 나쁘므로 떴던 캡처는 문서에 붙이지 않고 지웠다. 09:15 designer 가 남긴 확인(`anclick back-last tab=docs`)은 그대로 유효하고, 상태도 이미 review 라 다시 올리지 않는다.

**web/index.html 은 한 글자도 건드리지 않았다** — 09:16·09:36 에 다른 주체가 계속 쓰고 있었다.

### 2026-08-28T09:39:49+09:00 conflict (by nicehugepark)

**web/index.html 에서 물러났다 — 09:16·09:36 에 다른 주체가 쓰고 있었다.** 내가 넣으려던 `?anclick=…-last` 는 그쪽이 이미 넣었으므로 남은 것이 없다. 다만 그 파일에 손이 비면 넣어 두면 좋을 것 둘을 적어 둔다(이 요청의 계약은 아니다 — 진단의 신뢰도 문제다).

① **진단이 고정 2초 뒤에 누른다.** 문서 렌더가 그보다 늦으면 손잡이가 아직 없어 `MISSING` 만 찍고 아무것도 누르지 않는다. 기계가 바쁠 때 조용히 헛것을 확인하게 된다 — 나는 실제로 그 상태를 만났다. 손잡이가 나타날 때까지 상한을 두고 기다렸다 누르는 편이 낫다.

② **결과를 `document.title` 에만 찍는다.** 이 저장소에서 화면을 확인하는 손은 `s9 shot`(스크린샷)이고 제목 표시줄은 스크린샷에 없다. 눌린 결과를 화면 어딘가에 한 줄 그려 주면 캡처 자체가 근거가 된다 — 지금은 진단을 돌린 사람만 알고 캡처를 보는 사람은 모른다.

### 2026-08-28T13:03:20+09:00 ask (by nicehugepark)

> ⌖ ② 결과를 document.title 에만 찍는다. 이 저장소에서 화면을 확인하는 손은 s9 shot(스크린샷)이고 제목 표시줄은 스크린샷에 없다. 눌린 결과를 화면 어딘가에 한 줄 그려 주면 캡처 자체가 근거가 된다 — 지금은 진단을 돌린 사람만 알고 캡처를 보는 사람은 모른다.

다시 메모 테스트

### 2026-08-28T13:18:20+09:00 ask (by nicehugepark)

> ⌖ 문서에 특정 라인에 메모를 추가할 수 있

남긴 메모로 데려가는지 확인 (자동 검증)

### 2026-08-28T13:19:48+09:00 response (by nicehugepark)

**두 가지가 다 사실이었고, 원인은 서로 다른 것이었습니다.**

**① "새로 메모를 만들어봤는데 기능이 어떻게 동작했는지 파악도 안된다"** — 두 겹이었습니다.

**앞겹: 방금 고른 글을 못 찾았다고 답했습니다.** 남기신 메모가 가리키는 구간은 이 문서의 '② 결과를 document.title 에만 찍는다…' 문장인데, 이 문장은 굵은 글씨와 코드 표기를 가로지릅니다. 화면은 글을 그릴 때 바로 그 자리에서 한 문장을 여러 조각으로 쪼갭니다. 그런데 찾는 쪽은 **조각 하나 안만** 뒤지고 있었습니다. 그래서 문서는 한 글자도 안 바뀌었는데 '문서가 바뀌어 이 구간을 찾지 못했습니다'라고 답했고, 짚는 손잡이(⌖ 그 구간)조차 아예 생기지 않았습니다. 방금 고르신 그 글에 대고 못 찾겠다고 한 셈입니다. 지금은 본문을 한 줄로 이어 놓고 찾습니다 — 글자마다 어느 조각의 몇 번째인지를 함께 들고 있다가, 찾은 구간을 조각별로 잘라 각각 밑줄을 긋습니다. 짚는 표적은 첫 조각이고 ⌖ 는 마지막 조각 뒤에 섭니다(표식이 한가운데 끼면 고른 글이 두 동강 난 것처럼 읽힙니다).

**뒷겹: 남긴 뒤에 화면이 아무것도 달라지지 않았습니다.** '문서를 다시 열면 그 구간 옆에서 읽힙니다'라고 **말만** 했습니다. 자기가 한 일의 결과를 보려면 사람이 스스로 새로 고쳐야 했고, 그동안 화면은 그대로였습니다. 무슨 일이 일어났는지 알 수 없는 기능은 됐든 안 됐든 쓰는 사람에게는 같습니다. 지금은 창을 닫는 순간 문서를 다시 그려 **방금 남긴 메모로 데려갑니다.**

**② "'그 구간' 을 클릭해봤을 때 이동이 되지 않는다"** — 이것도 두 겹이었습니다.

**진짜로 안 굴렀습니다.** 짚기를 브라우저의 scrollIntoView 에 맡겨 두었는데, 이 함수는 어느 판을 굴릴지 브라우저가 고릅니다. 그리고 이 화면에서 실제로 구르는 판(문서 뷰어)을 고르지 않는 경우가 있었습니다 — 눌린 뒤에도 뷰어의 스크롤 값이 0 에서 0 이었습니다(진단으로 직접 재봤습니다). 지금은 그 판을 직접 짚어 굴립니다.

**굴러도 안 보이는 자리가 있었습니다.** 메모와 그 구간이 이미 같은 화면 안에 있으면 스크롤이 한 픽셀도 움직일 이유가 없습니다 — 이 문서의 앞선 메모 둘이 정확히 그랬습니다(인용이 제 구간 바로 아래에 붙어 있습니다). 기계는 제 할 일을 다 했는데 눈에는 아무 일도 안 일어난 것입니다. **움직임을 결과의 증거로 삼은 것이 틀렸습니다.** 지금은 도착한 자리 자체가 잠깐 밝아졌다 사라집니다 — 굴렀든 안 굴렀든 언제나. 색면으로 칠하지 않았습니다: 선 하나와 시간뿐이고 흔적을 남기지 않습니다.

**눌러 보고 확인했습니다 (헤드리스, 캡처 셋).**

- anclick back-last … top=0→7357 — 메모의 '그 구간' 을 눌러 본문의 그 자리로 굴렀습니다. 캡처에 그 구간이 굵은 글씨·코드까지 한 줄로 이어 밑줄 그어진 채로 보입니다.
- anclick jump-last … top=0→7455 — 반대 방향도 같습니다.
- ansend landed top=7567 quotes=4 lost=0 — 메모를 실제로 남기고 창을 닫자 문서가 다시 그려지며 그 메모로 내려갔습니다(이 문서의 13:18 노트가 그때 남은 것입니다).
- anlost quotes=3 lost=0 — 고치기 전에는 셋 중 하나가 길을 잃은 상태였습니다.

진단도 함께 고쳤습니다: 결과를 화면 왼쪽 아래에 **적습니다**(전에는 창 제목에만 찍어서, 캡처를 보는 사람은 아무것도 읽을 수 없었습니다). 손잡이가 생길 때까지 기다렸다 누릅니다(전에는 고정 2초 뒤에 눌러, 느린 기계에서는 아무것도 누르지 않은 채 지나갔습니다).

계약 테스트 넷을 더했습니다 — 조각을 가로지르는 구간 찾기, 도착을 눈에 보이게 하기, 남긴 메모로 데려가기, 캡처에서 읽히는 진단.

[Image: assets/REQ-20260827-072-62x6/anc2-back-scrolls.png]
[Image: assets/REQ-20260827-072-62x6/anc2-jump-scrolls.png]
[Image: assets/REQ-20260827-072-62x6/anc2-memo-lands.png]

### 2026-08-28T13:21:50+09:00 conflict (by nicehugepark)

**같은 파일에 다른 손이 있었다 — 물러났다가 비었을 때 넣었다.** web/index.html 은 13:05~13:10 사이 다른 주체(세션 b8fda8f9 의 서브에이전트)가 계속 쓰고 있어서 그동안은 진단만 하고 한 글자도 고치지 않았다. 13:10:32 이후 그 파일에 쓰기가 멎은 것을 확인하고 편집을 시작했다.

편집 중에도 두 번 '디스크에서 파일이 바뀌었다'는 신호를 받았다(남의 다른 편집). 내 변경은 전부 살아 있는 것을 확인했다 — anchorGo·anhit·anchorGoNewest·flat.indexOf 가 지금 파일에 그대로 있고 계약 테스트 15건이 통과한다. 남의 변경을 덮어쓴 흔적은 보이지 않는다(내 편집은 전부 부분 치환이고 통짜 쓰기를 하지 않았다). 커밋은 하지 않았다.

### 2026-08-28T14:27:21+09:00 ask (by nicehugepark)

> ⌖ 길 잃은 앵커를 셀 것.

메모 세번째 테스트

## History
- 2026-08-27T22:39:03+09:00 created by nicehugepark (status: open)
- 2026-08-27T22:41:34+09:00 status: open -> in-progress (by nicehugepark) — 뒤쪽(앵커 저장·API·수신함) 완료, 테스트 8/8, 실경로 확인. 끌어 고르는 화면은 디자이너 큐
- 2026-08-27T23:59:51+09:00 status: in-progress -> blocked (by nicehugepark) — 디자이너 순번 대기 — 지금 아무도 손대고 있지 않다. web/index.html 은 한 주체만 만질 수 있어(둘이 만지면 남의 미커밋 작업이 사라진다) 화면 작업이 한 줄로 선다. 뒤쪽(서버·CLI)은 끝나 커밋됐다
- 2026-08-28T00:22:19+09:00 status: blocked -> in-progress (by nicehugepark) — 화면 몫 착수 (designer)
- 2026-08-28T00:22:20+09:00 status: in-progress -> review (by nicehugepark) — 확인 포인트: 아무 문서나 열고 본문에서 한 문장을 마우스로 끌어 골라 보세요. 그 자리에 "⌖ 「고른 글」 에 메모" 버튼 하나가 뜹니다. 그냥 다른 곳을 클릭하거나 Esc 를 누르거나 스크롤하면 조용히 사라집니다 — 읽기만 할 때 방해가 되지 않는지 특히 봐 주세요. 버튼을 누르면 판정 창과 같은 얼굴의 창이 열리고, 거기 쓴 말이 그 문서의 노트로 들어갑니다. 이 문서(구간에 메모 달기)를 열면 이미 달려 있는 메모가 보입니다 — 본문의 "미니 프롬프트 팝업창이 떠서"에 점선 밑줄과 ⌖ 가 붙어 있고, 아래 메모 쪽에는 "⌖ 그 구간" 이 있어 서로 오갈 수 있습니다. F5 후 확인.
- 2026-08-28T08:50:26+09:00 status: review -> in-progress (by nicehugepark) — 반려: 마지막에 생성된 '그 구간' 을 클릭하는 순간, 보드 화면으로 이동한다. [via dashboard]
- 2026-08-28T09:13:40+09:00 status: in-progress -> review (by nicehugepark) — 확인 포인트: 이 문서(구간에 메모 달기)를 열고 F5 로 새로 고친 뒤, 메모 옆의 "⌖ 그 구간" 을 눌러 보세요. 전에는 누르는 순간 보드 화면으로 튕겨 나갔습니다 — 문서 안에서 자리만 옮기려던 손짓이 화면을 통째로 갈아치웠던 것입니다. 지금은 화면이 그대로 있고 본문의 그 구간으로 스르륵 내려가기만 합니다. 반대 방향도 봐 주세요 — 본문에서 점선 밑줄이 그어진 글 옆의 작은 ⌖ 를 누르면 그 메모로 올라옵니다. 특히 **마지막에 달린 메모**("…앵커 노트로 양방향 짚기")에서 눌러 보시길 바랍니다. 반려하실 때 누르신 것이 그것입니다. 마우스 대신 Tab 키로 옮겨 가 Enter 를 눌러도 같습니다. 두 방향 모두 헤드리스로 실제 클릭해 화면이 그대로인지 봤고 캡처 둘을 문서에 붙였습니다.
- 2026-08-28T13:04:43+09:00 status: review -> in-progress (by nicehugepark) — 반려: 이 문서 자체에 새로 메모를 만들어봤는데, 기능이 어떻게 동작했는지 파악도 안되고, 이전에 등록했던 메모도 '그 구간' 이라는 링크를 클릭해봤을 때 이동이 되지 않는다. [via dashboard]
- 2026-08-28T13:29:02+09:00 status: in-progress -> review (by nicehugepark) — 확인 포인트: 이 문서를 열고 F5 로 새로 고친 뒤 네 가지를 봐 주세요. ① 13:03 에 남기신 메모(「② 결과를 document.title 에만…」)를 보세요. 전에는 그 메모 옆에 '문서가 바뀌어 이 구간을 찾지 못했습니다'라고만 적혀 있었고 짚어 갈 손잡이도 없었습니다. 지금은 본문 위쪽의 그 문장에 점선 밑줄이 굵은 글씨와 코드 표기를 **가로질러 끝까지 이어져** 있고, 문장 끝에 작은 ⌖ 가 붙어 있습니다. ② 메모 옆의 '⌖ 그 구간' 을 눌러 보세요. 본문의 그 자리로 내려가고, **도착한 자리가 잠깐 밝아졌다 사라집니다.** 전에는 두 자리가 이미 한 화면에 있으면 화면이 움직일 이유가 없어 아무 일도 안 일어난 것처럼 보였습니다 — 그래서 이제 움직임 대신 도착을 보여 줍니다. ③ 반대로 본문에서 점선 밑줄이 그어진 글 옆의 작은 ⌖ 를 누르면 그 메모로 올라옵니다. 마우스 대신 Tab 키로 옮겨 가 Enter 를 눌러도 같습니다. ④ 새 메모를 하나 달아 보세요 — 굵은 글씨나 코드 표기를 **걸쳐서** 끌어 고르시면 예전에 안 되던 경우입니다. 남기고 창을 닫으면 화면이 방금 남긴 그 메모로 내려갑니다. 전에는 '문서를 다시 열면 읽힙니다'라고 말만 하고 화면은 그대로였습니다. 헤드리스로 두 방향 다 실제로 눌러 보고 메모도 실제로 남겨 봤습니다(캡처 셋을 문서에 붙였습니다). 이 문서의 메모 넷 모두 제 구간을 찾습니다.
- 2026-08-28T14:27:48+09:00 status: review -> done (by nicehugepark) — 대시보드 승인 [via dashboard]
