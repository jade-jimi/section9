---
id: REQ-20260826-026-62x6
type: request
title: 뒤로가기 이력 오동작
summary: graph→docs에서 뒤로가기가 board로 간다 — 브라우저 이력 처리 전면 검토
goal: 그래프에서 문서를 열고 뒤로가기를 누르면 그래프로 돌아온다
status: done
user: sjpark1
machine: DESKTOP-TEHV1KR
session: dc4f4d76
project: section9
tags: ["auto-audit", "docs", "dashboard"]
created: 2026-08-26T21:49:23+09:00
status_since: 2026-08-26T23:07:02+09:00
updated: 2026-08-26T23:49:49+09:00
priority: 50
---

## Original

graph > docs > 뒤로가기를 하면 board로 간다. 뒤로가기 기능 전면 검토해줘.

## Notes

### 2026-08-26T23:46:26+09:00 response (by sjpark1)

원인은 이력 처리 전반이 아니라 **한 줄 누락**이었다.

그래프 캔버스의 노드 클릭(`canvas.addEventListener("pointerup", …)`)은 `tab = "docs"` 로 바꾸고 문서를 그리면서 `pushRoute()` 를 부르지 않았다. 그래서 history 스택 꼭대기는 여전히 `#graph` 고, 뒤로가기는 그 아래 — 그래프로 들어오기 전 탭, 대개 board — 로 튄다. 화면만 문서로 가 있고 이력은 따라오지 않은 상태다.

헤더 버튼·`[data-doc]` 링크는 전부 `pushRoute()` 를 부른다. 그래서 "그래프에서 열었을 때만" 어긋났고 재현 조건이 잘 안 잡혔다.

**고침**(web/index.html): 노드 클릭 경로에 `pushRoute()` 추가.

**테스트** `tests/test_history_route.py` 3건 — 이 한 곳이 아니라 계약을 잡는다: `tab` 을 바꾸는 **모든** 대입 지점이 뒤이어 `pushRoute()` 를 부르는지 훑는다(한 곳만 막으면 다음에 다른 경로가 같은 식으로 샌다) · 그래프 노드 클릭 · popstate→applyRoute. 수정 전 리비전에서 red 재현 확인, 전체 스위트 통과. 캡처 `state/shots/026-graph.png`.

## History
- 2026-08-26T21:49:23+09:00 created by sjpark1 (status: open)
- 2026-08-26T22:08:26+09:00 status: open -> in-progress (by sjpark1) — 원인 특정. 그래프에서 **노드를 클릭해** 문서로 갈 때만 이력을 쌓지 않는다 — web/index.html 의 캔버스 pointerup 핸들러가 `selectedDoc = id; tab = "docs"` 로 상태만 바꾸고 `pushRoute()` 를 부르지 않는다. 그래서 URL 은 여전히 #graph 인 채 화면만 docs 가 되고, 뒤로가기는 그 앞 항목(#board)으로 간다. 사용자가 본 "graph > docs > 뒤로가기 = board" 가 정확히 이 경로다. 탭 버튼 클릭은 pushRoute 를 부르므로 정상이다 — 그래서 한 경로만 어긋나 보였다. 전면 검토 대상(구현 시 함께 볼 것): ① 캔버스 노드 클릭(누락) ② 문서 닫기(selectedDoc=null)가 이력을 남기지 않음 ③ 뒤로가기로 #graph 에 복귀했을 때 stopGraph 된 그래프가 다시 도는지 ④ 뒤로가기로 #docs/<id> 에 복귀했을 때 본문이 실제로 로드되는지(applyRoute 는 selectedDoc 만 세팅한다). 착수는 designer 가 web/index.html 작업을 마친 뒤로 미룬다 — 오늘 같은 파일 동시 편집으로 이미 사고가 났다(REQ-20260826-021-62x6).
- 2026-08-26T22:54:24+09:00 status: in-progress -> review (by sjpark1) — 그래프 탭에서 노드를 눌러 문서를 연 다음, 브라우저 뒤로가기를 눌러 보라. 예전에는 보드로 튀었는데 이제 그래프로 돌아와야 한다. F5 로 새로고침한 뒤 확인하라(브라우저가 옛 화면을 들고 있다).
- 2026-08-26T23:07:02+09:00 status: review -> done (by sjpark1) — 대시보드 승인 [via dashboard]
