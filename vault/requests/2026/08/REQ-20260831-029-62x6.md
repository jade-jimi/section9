---
id: REQ-20260831-029-62x6
type: request
title: 프로젝트 화면 문구 판정
summary: 새 화면 전 문구(진입점·생성 창·설정 라벨·role 낱말·확인 창)의 4인 판정 — ux-writer 형식·tech-writer 용어(role 업계어 거취 포함)·translator 어휘. 표만 산출, 코드 무접촉
goal: 새 화면의 전 문구가 판정 표(자리·확정 문안·근거)로 확정되어 B2 가 그대로 옮겨 적을 수 있다
status: done
size: S
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: 38ad98c8
agents: ["sub:ux-writer:ade5ba79", "sub:tech-writer:a97990a1", "sub:translator:a0f15c09", "sub:tech-writer", "sub:translator", "sub:subagent:a5e49d2a", "sub:ux-writer", "lead:claude-fable-5"]
contributions: [{"actor": "sub:ux-writer:ade5ba79", "item": "029 프로젝트 문구 형식", "started": "2026-08-31T22:53:33+09:00", "ended": "2026-08-31T23:01:50+09:00", "result": "done", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/38ad98c8-51fd-4b50-a26a-495b58404817/tasks/ade5ba7982a7c7e3d.output"}, {"actor": "sub:tech-writer:a97990a1", "item": "029 프로젝트 용어 판정", "started": "2026-08-31T22:53:42+09:00", "ended": "2026-08-31T23:00:20+09:00", "result": "done", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/38ad98c8-51fd-4b50-a26a-495b58404817/tasks/a97990a1dd5706fb2.output"}, {"actor": "sub:translator:a0f15c09", "item": "029 프로젝트 어휘 다듬기", "started": "2026-08-31T22:53:51+09:00", "ended": "2026-08-31T23:01:19+09:00", "result": "done", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/38ad98c8-51fd-4b50-a26a-495b58404817/tasks/a0f15c0919ea1bb12.output"}, {"actor": "sub:tech-writer", "item": "`[2026-08-31 23:12 KST - tech-writer]` 용어 판정 — 새 화면에 설 낱말 전수", "started": "2026-08-31T22:59:45+09:00", "ended": "2026-08-31T22:59:45+09:00", "result": "done", "transcript": ""}, {"actor": "sub:tech-writer:a97990a1", "item": "`[2026-08-31 23:14:02 KST - tech-writer]`", "started": "2026-08-31T23:00:20+09:00", "ended": "2026-08-31T23:00:20+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/38ad98c8-51fd-4b50-a26a-495b58404817/subagents/agent-a97990a1dd5706fb2.jsonl"}, {"actor": "sub:translator", "item": "[translator 어휘 판정 — 프로젝트 화면 문구 (자리 기준 독립 제안)]", "started": "2026-08-31T23:00:35+09:00", "ended": "2026-08-31T23:00:35+09:00", "result": "done", "transcript": ""}, {"actor": "sub:subagent:a5e49d2a", "item": "Confirming s9 note write to REQ-20260831-029-62x6", "started": "2026-08-31T23:01:10+09:00", "ended": "2026-08-31T23:01:10+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/38ad98c8-51fd-4b50-a26a-495b58404817/subagents/agent-a5e49d2a25cc7d81d.jsonl"}, {"actor": "sub:ux-writer", "item": "029 프로젝트 문구 형식(ux-writer)", "started": "2026-08-31T23:01:18+09:00", "ended": "2026-08-31T23:01:18+09:00", "result": "done", "transcript": ""}, {"actor": "sub:translator:a0f15c09", "item": "`[2026-08-31 22:53:53 KST - translator]`", "started": "2026-08-31T23:01:19+09:00", "ended": "2026-08-31T23:01:19+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/38ad98c8-51fd-4b50-a26a-495b58404817/subagents/agent-a0f15c0919ea1bb12.jsonl"}, {"actor": "lead:claude-fable-5", "item": "리드 판정(보관 갈래): translator 검산대로 현재 archived 는 파급이 없다 — 확인 창이 거짓을 말하지 않게 ", "started": "2026-08-31T23:01:48+09:00", "ended": "2026-08-31T23:01:48+09:00", "result": "done", "transcript": ""}, {"actor": "sub:ux-writer:ade5ba79", "item": "`[2026-08-31 22:58:14 KST - ux-writer]`", "started": "2026-08-31T23:01:50+09:00", "ended": "2026-08-31T23:01:50+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/38ad98c8-51fd-4b50-a26a-495b58404817/subagents/agent-ade5ba7982a7c7e3d.jsonl"}, {"actor": "lead:claude-fable-5", "item": "리드 중재 확정(3인 판정 합의 — B2 는 이 노트와 3개 표를 그대로 옮겨 적는다):", "started": "2026-08-31T23:02:44+09:00", "ended": "2026-08-31T23:02:44+09:00", "result": "done", "transcript": ""}]
project: section9
parent: REQ-20260831-026-62x6
relates_why: {}
tags: ["dashboard", "project"]
created: 2026-08-31T22:52:31+09:00
status_since: 2026-08-31T23:02:54+09:00
updated: 2026-08-31T23:02:54+09:00
priority: 50
---

## Original

프로젝트 화면 문구 판정

## Notes

### 2026-08-31T22:59:45+09:00 response (agent: sub:tech-writer, by nicehugepark)

`[2026-08-31 23:12 KST - tech-writer]` 용어 판정 — 새 화면에 설 낱말 전수

### 판정 원칙 둘 (이 화면에서 뽑아 쓴 것)

1. **화면이 이미 판정한 낱말은 다시 판정하지 않는다.** 이번 작업은 기능을 옮기는 일이지 어휘를 고치는 일이 아니다. 옮겨 가는 자리에서 낱말이 바뀌면 사용자에게는 "기능이 옮겨졌다"가 아니라 "말이 달라졌다"로 보인다.
2. **상태 축의 이름은 영문 원문, 사람이 하는 일은 한국어.** 새 규칙이 아니라 이 화면의 확립 문법이다 — `statusLabel()`(web/app/const.js:151)이 question 만 예외(답함/미답)로 두고 나머지 상태를 원문 그대로 돌려주고, REQ-20260828-007-62x6 반려가 「이름과 행위를 글꼴로 가른다」로 못박았다(`→ done` 은 mono 이름, `✓ 승인` 은 본문체 행위). 프로젝트 status 도 상태 축이므로 이 문법 안이다.

### ① role 넷 — **전부 그대로 (owner / maintainer / contributor / viewer)**

| 낱말 | 판정 | 근거 |
|---|---|---|
| owner | 그대로 | 값이 화면 밖에서도 같은 이름으로 불린다 — 프론트매터 `role: owner`, 같은 화면 아래 History 줄 `member update … role=owner`, CLI `--role owner`, 서버 거부 문구 「owner 변경은 owner만 가능」. 화면만 「소유자」로 옮기면 한 화면 위아래에 이름이 둘 선다(DOC-20260830-006-62x6 「같은 개념에 두 번째 이름을 만들지 않는다」) |
| maintainer | 그대로 | 같음. 순우리말/의역 되돌림(「관리자」)은 Settings 의 시스템 role `admin` 과 정면 충돌한다 — 두 축이 한 낱말을 갖는다 |
| contributor | 그대로 | 같음. 「기여자」는 국내 협업도구에서 확립되지 않았고, 확립 업계어를 되돌려 옮기는 것은 기계 직역과 같은 결함이다(s9-design 6절 · REQ-20260830-039-62x6) |
| viewer | 그대로 (+ 축 표시 필수) | 같음. **다만 Settings 의 시스템 role 에도 `viewer` 가 있다** — 한 사람이 시스템 member 이면서 프로젝트 viewer 일 수 있다. 그래서 문구에 축을 붙인다: 「viewer 입니다」(X) → 「이 프로젝트에서는 viewer 입니다」(O) |

**화면 정합**: 기존 멤버 표(#proj-info, web/app/app.js:172-196)가 이미 셀렉트 옵션에 네 값을 영문 그대로 그린다. Settings 사용자 관리도 `role: member` 식으로 영문 원문을 노출한다. 그대로 두는 쪽이 기존 표기와 일치하고, 옮기는 쪽이 새 표기를 만든다.

**대신 갚아야 할 것 — 뜻은 그 자리에서 준다** (writing-clarity: 기술용어는 처음 등장 시 한 줄 정의). 이 넷은 비개발자 독자가 위계를 짐작할 수 없다. 멤버 표 아래 한 줄 범례(권한 셀렉트를 볼 수 있는 maintainer 이상에게만, muted mono). 뜻은 `project_can()`(bin/s9:12640-12660) 실측:
owner 보관·삭제·owner 지정 · maintainer 설정과 멤버 · contributor 요청·문서 쓰기 · viewer 보기만
(문안 다듬기는 ux-writer 몫, 낱말과 순서와 사실관계는 이 줄이 확정)

### ② slug — **그대로, 단 생성 창에서 한 줄 정의를 붙인다**

| 자리 | 판정 |
|---|---|
| 생성 창 입력 라벨 | `slug` + 그 자리 도움말 한 줄 (「명령과 주소에서 이 프로젝트를 부르는 짧은 이름 — 영문·숫자·`.`·`_`·`-`」 취지, 문안은 ux-writer) |
| PRJ 문서 뷰 메타 격자 | `slug` (아래 ③의 필드명 문법) |
| 목록·필터·문맥 띠 | 값만 노출, 라벨 없음 — 판정 대상 아님 |

근거: 이 값은 사용자가 화면 밖에서 같은 이름으로 계속 만난다 — guide.html:508 이 이미 「`pay` 자리가 **slug**(프로젝트의 짧은 식별자)」로 가르치고, URL `?project=slug`, 경로 `projects/<slug>/`, CLI `s9 project member add <slug> …`(이 명령문은 **지금도 화면 빈 상태에 그대로 그려진다** — web/app/app.js:286). 화면에서만 「짧은 이름」으로 부르면 사용자가 곧바로 두 번째 이름을 배워야 한다.
**한계 명시**: slug 는 commit·branch 처럼 통용되는 말이 아니고 영어 원뜻으로 유추도 안 된다 — 그래서 한 줄 정의가 선택이 아니라 조건이다. 그럼에도 사용자가 반려하면, 대체는 화면 라벨만 바꾸는 일이 아니라 **guide·CLI·URL 을 함께 바꾸는 작업**이어야 한다. 화면만 옮기면 이름이 둘이 된다.

### ③ 기존 필드 낱말 — 유지 넷, 교정 둘, 정리 하나

| 낱말 | 판정 | 근거 |
|---|---|---|
| 고객 | 유지 | 기존 패널의 낱말. 옮겨가는 자리에서 바꿀 이유 없음 |
| 현업 담당자 | 유지 | SI 도메인 확립어. 담당자 계열은 10개 서비스 교차 확인된 관례어(DOC-20260830-006-62x6) |
| 참여일 · 직무 · 만료 | 유지 | 전부 평이한 국어이고 기존 표 헤더 그대로. 빈 만료의 「무기한」 표기도 유지 |
| 멤버 | 유지 (**「구성원」·「참여자」 금지**) | 기존 화면이 「멤버 1/1」·「멤버인 활성 프로젝트」로 이미 쓴다. 세 이름이 될 위험이 가장 큰 자리 |
| 권한 (열 이름) | 유지 | 기존 표 헤더. 값은 영문(①), 열 이름은 한국어 — 이름/행위 문법과 어긋나지 않는다(열 이름은 값의 종류를 말하는 자리) |
| **개요 → `summary`** | **정리** | 지금 같은 것이 두 이름이다 — #proj-info 는 「개요」, 모든 문서 뷰는 `summary`(web/app/docs.js:306). 패널이 문서 뷰로 합쳐지면 한 격자에 둘이 선다. 다수·공통인 `summary` 가 이긴다. 「개요」는 패널과 함께 사라진다 — 이번 통합의 부수 이득이다 |
| **멤버 표 「상태」 → 「참여 상태」** | **교정 (조건부)** | 통합 후 한 화면에 상태 축이 둘 선다: 프로젝트 status(active/archived)와 멤버십(활성/만료). 열 이름이 그냥 「상태」면 어느 축인지 모른다. 값 「활성」·「만료」는 그대로 |
| **만료 행 색 `--c-blocked` → `--muted`** | 지지 | designer 판정에 동의 — 만료는 고장이 아니다. 붉은 잉크는 낱말과 어긋난 신호다 |

**메타 격자의 두 문법이 만나는 자리 (frontend 인계 필수)**: 문서 뷰 메타 격자의 라벨은 **프론트매터 필드 이름 그대로**다(`status`·`summary`·`goal`·`project`·`blocked_by`·`workspace` — docs.js:295-310). 여기에 「고객」·「현업 담당자」가 들어오면 한 격자가 반은 영문 필드명, 반은 한국어가 된다. **경계에 뜻을 준다**: 위 블록 = 이 문서 종류만의 정보(한국어 라벨: 고객·현업 담당자), 아래 블록 = 모든 문서가 갖는 정보(영문 필드명: slug·status·summary·created/updated). 둘 사이 hairline 한 줄. 이 경계를 시각으로 세우는 것은 designer·frontend 몫이고, 낱말이 어느 쪽에 속하는지는 이 표가 확정한다.

### ④ active / archived — **값은 영문 그대로. 「보관」과 정면 충돌하므로 목적어로 가른다**

이번 화면의 **가장 위험한 낱말 충돌**이다. 문서 화면에는 이미 「보관」 어휘가 한 벌 서 있다 — 「보관」 단추 · 「보관 해제」 · 「보관함」 탭 · 「치운 것」(web/app/tidy.js). 그 「보관」은 **문서 한 장을 목록에서 내리는 일**이고, 프로젝트 `status: archived` 는 **프로젝트가 끝났다**는 뜻이다. 그런데 통합 후 PRJ 문서 화면에서는 **둘이 한 화면에 동시에 선다**: 위쪽 메타 격자의 status 와 아래쪽 「이 문서 치우기 · 보관」 단추. status 를 「보관됨」으로 옮기는 순간, 사용자가 그 아래 「보관」 단추를 누르는 것이 합리적이면서 틀린 행동이 된다.

| 자리 | 판정 |
|---|---|
| status 값 (격자·목록 배지·문맥 띠) | **`active` / `archived` 영문 그대로.** 다른 문서의 상태 배지와 같은 문법(대소문자 포함)을 따르고 프로젝트만 다르게 쓰지 않는다 |
| status 를 바꾸는 확인 창의 단추 | **한국어 + 목적어**: 「프로젝트 보관」. 행위는 한국어라는 문법을 지키면서, 목적어가 아래쪽 「이 문서 치우기」와 갈라 준다 |
| 목록의 접힌 묶음 줄 | 「+ 보관된 프로젝트 N개」 — 목적어를 붙여 문서 보관함과 가른다. designer 초안 「+ 보관됨 2개」는 상태값의 두 번째 이름이 되므로 쓰지 않는다 |

이 갈래는 화면이 이미 살고 있는 짝(`done` 배지 + 「✓ 승인」 단추)과 정확히 같은 구조다.

### ⑤ 이 화면이 짓기 쉬운 조어 — 미리 막는 목록

지으면 반려다. 시금석(DOC-20260830-006-62x6) 위반 항목을 함께 적는다.

| 지을 법한 말 | 걸리는 데 | 대신 |
|---|---|---|
| 「맡은 이」·「지기」·「일손」 (owner) | ④ 묶는 이름을 은유로 | owner |
| 「가꿈이」·「이바지꾼」 (maintainer/contributor) | 확립 업계어의 순우리말 되돌림 | maintainer / contributor |
| 「관리자」 (maintainer) | 시스템 role `admin` 과 한 낱말 충돌 | maintainer |
| 「보기 권한」 (viewer) | 값의 두 번째 이름 | 이 프로젝트에서는 viewer |
| 「짧은 이름표」·「부름말」·「부르는 이름」 (slug) | ③ 그 자리에서 지은 합성어 | slug + 한 줄 정의 |
| 「묵힘」·「보관터」 (archived) | ③·④ 동시 위반 | archived (값) / 프로젝트 보관 (행위) |
| 「구성원」·「참여자」 (멤버) | 같은 개념 세 번째 이름 | 멤버 |
| 「참여 만료일」 | 이미 「만료」가 있다 | 만료 |
| 「소속 프로젝트」·「내 프로젝트 방」 | 화면에 없는 사물을 부름(시금석 ① 개정판) | 기존 「MY PROJECTS」 유지 |

### translator 인계 — 옮길 것 목록

**옮길 것은 셋뿐이다.** 나머지는 전부 「그대로」이거나 이미 한국어다.
1. role 넷의 뜻 범례 한 줄 (위 ①의 사실관계 그대로, 사람 말로)
2. slug 의 한 줄 정의 (생성 창)
3. viewer 권한 안내문에서 축을 드러내는 문장 (「이 프로젝트에서는 viewer 입니다 — 멤버를 바꾸려면 maintainer 이상이어야 합니다」 취지)

**옮기지 말 것**: owner · maintainer · contributor · viewer · slug · active · archived · summary. 이 여덟은 값이거나 필드 이름이고, 화면 밖에서 같은 이름으로 계속 불린다.

코드·git 무접촉. 이 노트가 산출물 전부다.

### 2026-08-31T23:00:20+09:00 subagent (agent: sub:tech-writer:a97990a1, by nicehugepark)

`[2026-08-31 23:14:02 KST - tech-writer]`

### 결론 — 옮길 것은 셋뿐, 나머지 여덟은 그대로 둔다

판정표를 `REQ-20260831-029-62x6` 에 `--label response --agent tech-writer` 로 남겼다(5681자). 코드·git 무접촉.

**판정을 끌어낸 두 원칙**은 내가 지은 것이 아니라 화면에서 뽑았다.
① 기능을 옮기는 작업에 어휘 개정을 딸려 보내지 않는다 — 사용자에겐 "말이 달라졌다"로 보인다.
② **상태 축의 이름은 영문 원문, 사람이 하는 일은 한국어** — `statusLabel()`(web/app/const.js:151)이 question 만 예외로 두고 나머지 상태를 원문으로 돌려주고, REQ-20260828-007-62x6 반려가 「이름과 행위를 글꼴로 가른다」로 못박은 그 규칙이다.

### 다섯 항목 판정

**① role 넷 — 전부 그대로.** designer 의 무게에 동의하되 근거는 "업계어라서"보다 강한 것이 있다: 값이 **같은 화면 위아래에서** 같은 이름으로 불린다 — 프론트매터 `role: owner`, 바로 아래 History 줄 `member update … role=owner`, CLI `--role owner`, 서버 거부 문구 「owner 변경은 owner만 가능」. 화면만 옮기면 한 화면에 이름이 둘 선다. 기존 멤버 표(app.js:172-196)와 Settings 사용자 관리가 이미 영문 원문을 노출하므로 **그대로 두는 쪽이 기존 표기와 일치하고 옮기는 쪽이 새 표기를 만든다**. 「관리자」(maintainer)는 시스템 role `admin` 과 정면 충돌해 별도로 막았다.
갚아야 할 것: 비개발자는 이 넷의 위계를 짐작할 수 없다 → 멤버 표 아래 뜻 범례 한 줄(`project_can()` bin/s9:12640-12660 실측 기반).
숨은 함정: **`viewer` 는 시스템 role 에도 있다** — 문구에 축을 붙여야 한다(「이 프로젝트에서는 viewer」).

**② slug — 그대로, 단 생성 창에 한 줄 정의가 조건.** guide.html:508 이 이미 slug 로 가르치고, URL·경로·CLI 가 전부 그 이름이며, **그 CLI 명령문이 지금도 화면 빈 상태에 그대로 그려진다**(app.js:286). 화면만 「짧은 이름」으로 바꾸면 사용자가 곧장 두 번째 이름을 배운다.

**③ 기존 필드 — 유지 넷(고객·현업 담당자·참여일/직무/만료·멤버), 정리 하나, 교정 둘.**
- **「개요」→ `summary`**: 지금 같은 것이 이미 두 이름이다(패널 「개요」 vs 모든 문서 뷰 `summary`). 통합하면 한 격자에 둘이 선다 — 다수·공통이 이긴다. 「개요」는 패널과 함께 사라진다.
- **멤버 표 「상태」→「참여 상태」**: 통합 후 한 화면에 상태 축이 둘(프로젝트 active/archived · 멤버십 활성/만료) 서므로 열 이름이 축을 말해야 한다.
- **frontend 인계 필수**: 문서 뷰 메타 격자 라벨은 **프론트매터 필드 이름 그대로**다(docs.js:295-310 — status·summary·goal·project). 여기에 「고객」이 들어오면 한 격자가 반은 영문 필드명, 반은 한국어가 된다. 해법은 **경계에 뜻을 주는 것**: 위 = 이 문서 종류만의 정보(한국어), 아래 = 모든 문서 공통(영문 필드명), 사이에 hairline 한 줄.

**④ active/archived — 이번 화면의 가장 위험한 충돌.** 문서 화면에 「보관」 어휘가 이미 한 벌 있고(tidy.js: 보관·보관 해제·보관함), 그건 **문서 한 장을 목록에서 내리는 일**이다. 통합 후 PRJ 문서 화면에서는 위쪽 status 와 아래쪽 「이 문서 치우기 · 보관」 단추가 **한 화면에 동시에 선다** — status 를 「보관됨」으로 옮기면 사용자가 그 아래 「보관」을 누르는 것이 합리적이면서 틀린 행동이 된다.
판정: 값은 `active`/`archived` 영문, 행위 단추는 **목적어를 붙여** 「프로젝트 보관」, 목록 접힘 줄은 「+ 보관된 프로젝트 N개」(designer 초안 「+ 보관됨 2개」는 상태값의 두 번째 이름이라 반려). 화면이 이미 사는 짝 — `done` 배지 + 「✓ 승인」 단추 — 과 정확히 같은 구조다.

**⑤ 조어 위험 9건**을 미리 막아 표로 남겼다(맡은 이/지기·가꿈이·관리자·보기 권한·짧은 이름표·묵힘·구성원·참여 만료일·내 프로젝트 방), 각각 시금석 위반 항목과 대체어 포함.

### translator 인계 — 옮길 것 3

1. role 넷의 뜻 범례 한 줄  2. slug 의 한 줄 정의(생성 창)  3. viewer 안내문에서 축을 드러내는 문장

**옮기지 말 것 8**: owner · maintainer · contributor · viewer · slug · active · archived · summary.

### 한계

slug 는 commit·branch 처럼 통용되는 말이 아니고 영어 원뜻으로 유추도 안 된다 — 한 줄 정의가 선택이 아니라 조건인 이유다. 사용자가 그럼에도 반려하면, 대체는 화면 라벨만 바꾸는 일이 아니라 **guide·CLI·URL 을 함께 바꾸는 작업**이어야 한다. 화면만 옮기면 이름이 둘이 된다.

문서: `/home/sjpark1/section9/vault/requests/2026/08/REQ-20260831-029-62x6.md`

### 2026-08-31T23:00:35+09:00 response (agent: sub:translator, by nicehugepark)

[translator 어휘 판정 — 프로젝트 화면 문구 (자리 기준 독립 제안)]

### 결론 먼저

세 가지가 이번 화면의 어휘를 망칠 자리다.

1. **「보관」 문안은 지금 쓰면 거짓말이다.** 실측: 프로젝트 `status=archived` 는 PRJ 문서
   frontmatter 한 칸만 바꾼다. 소속 문서를 보관함으로 보내지도(`s9:11487` 문서 archive 는
   전혀 다른 경로), 새 요청을 막지도(`project_can` 은 status 를 보지 않는다, `s9:12640-12660`),
   목록에서 내리지도 않는다(`projects_data`, `s9:15178` — 전량 반환). "문서가 모두 보관함으로
   접힙니다"는 A(백엔드)가 **접힘을 실제로 구현할 때만** 참이다. 아래 표에 갈래별 문안을 넣고
   조건을 못박았다.
2. **현행 멤버 제거 창의 「접근만 끊깁니다」도 부정확하다.** `read` 는 누구나 허용이라
   (`s9:12645`) 뺀 사람도 문서를 그대로 읽는다. 끊기는 것은 요청 올리기·설정 고치기다.
   지금 문장은 사용자가 "저 사람이 이제 못 본다"고 믿게 만든다.
3. **서버 거부 문장이 반말 CLI 어투 그대로 화면에 뜬다** — "마지막 owner는 제거할 수 없다
   (다른 owner를 먼저 지정하라)"가 `거부: …` 로 찍힌다(`app.js` wireMemberControls fail).
   대시보드는 존댓말이다. 서버 문장을 존댓말로 바꾸면 CLI 어투가 깨지니, **화면이 아는 사유는
   화면 문장으로 갈아 끼우는 쪽**이 맞다(표 2-c).

옮김의 기준은 셋이다 — ① 화면 사전 우선(새 낱말 금지) ② 정상은 말하지 않는다 ③ 되돌아오지
않는 문장은 버린다(역번역 검산).

---

### 표 1. 새 프로젝트 만들기 — 다이얼로그

| 자리 | 초안 / 직역 후보 | 제안 문안 | 역번역 검산 |
|---|---|---|---|
| cap | `생성` | **`프로젝트`** | "Project" — 창머리는 명사 2~4자가 이 화면의 약속(멤버·삭제·영구·판정·계정) |
| title | `새 프로젝트 생성` | **`새 프로젝트를 만듭니다`** | "Creates a new project" — 명사형 종결(`생성`)은 버튼·메뉴의 말투고, 창머리는 결과를 예고하는 평서문이다(`N건을 휴지통으로 보냅니다`) |
| desc | `프로젝트를 생성합니다. 생성자는 owner 권한을 부여받습니다.` | **`만든 사람이 owner 가 됩니다. 만들고 나면 그 프로젝트 문서로 갑니다.`** | "You become the owner; you land on the project document" — `부여받습니다`는 피동 한자어 연쇄. 옮기면 「됩니다」 한 낱말로 끝난다 |
| 필드1 라벨 | `표시명` | **`이름`** | "Name" — `표시명`은 display name 직역이다. 사람은 프로젝트를 그냥 이름으로 부른다 |
| 필드1 placeholder | (없음) | **`예: 결제 개편`** | 빈 칸이 스스로 예시가 된다. 화면 사전에 이미 있는 틀: `키 이름 (예: OPENAI_API_KEY)`·`주제 (예: 말투)` |
| 필드2 라벨 | `slug` | **`slug`** (tech-writer 판정 대기 — 옮기지 않음) | 판정 없이 도착한 낱말은 옮기지 않는다 |
| 필드2 placeholder | `slug 입력` | **`예: payment-2026`** | 실측 규칙 `[A-Za-z0-9._-]+`(`s9:12914`)과 어긋나지 않는 예시 |
| 필드2 보조줄 | `URL 및 CLI에서 사용되는 식별자입니다` | **`주소와 명령에 쓰는 짧은 이름입니다. 만든 뒤에는 바꿀 수 없습니다.`** | "Short name used in URLs and commands; cannot be changed later" — 뒷문장은 실측이다: `project set` 의 필드 목록에 slug 가 없다(`s9:13018`). 이 창에서 유일하게 되돌릴 수 없는 입력이라 반드시 말해야 한다 |
| 필드3 라벨 | `개요` | **`개요`** (유지) | 이미 `#proj-info` 격자에 서 있는 낱말이다. 같은 개념에 두 번째 이름을 만들지 않는다 |
| 필드3 placeholder | (없음) | **`예: 카드 결제 흐름을 새로 짠다`** | 한 줄 요약이라는 것을 예시가 말한다 |
| 필드4 라벨 | `고객` | **`고객`** (유지) | 같은 이유 — 격자 라벨과 한 세계 |
| 필드4 placeholder | (없음) | **`예: 아이티센 · 비우면 내부 일감`** | 비워도 된다는 사실을 별도 안내문 없이 그 칸이 말한다(`비우면 무기한` 과 같은 손버릇) |
| ok | `생성` / `확인` | **`프로젝트 만들기`** | 버튼은 결과를 말한다(동사+목적). 「확인」 단독 금지 |
| cancel | `취소` | **`그만두기`** (고정) | 이 화면 전량이 `그만두기` 다 |

보조: 담당자 4필드를 생성에서 빼는 designer 판정에 동의한다 — 만들 때 필요한 정보가 아니고,
빈 칸 넷은 "채워야 하나" 하는 망설임만 만든다.

---

### 표 2. 확인 창 두 벌 — 멤버 빼기

**(a) 남을 뺄 때** (현행 `app.js:257`)

| 자리 | 현행 | 제안 문안 | 역번역 검산 |
|---|---|---|---|
| cap | `멤버` | **`멤버`** (유지) | — |
| title | `nicehugepark를 section9 에서 뺍니다` | **`nicehugepark 님을 「Section9 하네스 시스템」에서 뺍니다`** | 두 곳을 고친다. ① `section9`(slug)은 기계 이름이다 — 사람은 프로젝트를 제목으로 안다. ② `section9 에서` 는 조사 앞 공백(맞춤법 오류). 제목으로 바꾸면 붙여 쓴다. 조사는 기존 `josa()` 가 이미 처리한다 |
| desc | `프로젝트 문서는 그대로 남고, 이 사람의 접근만 끊깁니다.` | **`이 프로젝트에 요청을 올리거나 설정을 고칠 수 없게 됩니다. 문서는 그대로 남고, 다시 넣을 수 있습니다.`** | "loses contribute/manage; documents stay; re-addable" — 현행의 `접근`은 access 직역이자 **사실 오류**다(`read` 는 누구나 허용, `s9:12645`). 뺀 사람도 문서는 읽는다 |
| ok | `멤버 빼기` | **`멤버 빼기`** (유지) | 이미 동사+목적이다 |
| cancel | `그만두기` | **`그만두기`** (유지) | — |

**(b) 자기가 나갈 때** — 같은 창을 쓰면 안 된다. 남을 빼는 것은 관리 행위고, 나가는 것은
자기 자리를 버리는 행위다. 한국어에서 이 둘은 다른 동사를 쓴다(빼다 / 나가다).

| 자리 | 직역 후보 | 제안 문안 | 역번역 검산 |
|---|---|---|---|
| cap | `멤버` | **`나가기`** | 창머리가 어느 쪽 일인지부터 가른다 |
| title | `자신을 프로젝트에서 제거합니다` | **`「Section9 하네스 시스템」에서 나갑니다`** | "You leave this project" — `자신을 제거`는 영어 remove yourself 직역이다. 한국어로 자기가 자기를 제거하지 않는다, 나간다 |
| desc | `제거 후에는 재참여가 불가능합니다` | **`나가면 이 프로젝트에 요청을 올리거나 설정을 고칠 수 없습니다. 다시 들어오려면 남은 멤버가 넣어 주어야 합니다.`** | "after leaving you lose contribute/manage; only a remaining member can re-add you" — 「불가능합니다」가 아니라 무엇을 하면 되는지까지 말한다(에러·경고는 '무엇이 왜 + 할 일') |
| ok | `나가기` | **`나가기`** | — |
| cancel | `그만두기` | **`그만두기`** | — |

**(c) 거부 문장** — 서버 원문을 그대로 찍지 말 것. 알려진 사유 셋을 화면 문장으로.

| 서버 원문(`s9:12846·12895·12882`) | 화면 문안 | 검산 |
|---|---|---|
| `마지막 owner는 제거할 수 없다 (다른 owner를 먼저 지정하라)` | **`마지막 owner 는 뺄 수 없습니다 — 다른 사람을 owner 로 올린 뒤에 빼 주세요.`** | 무엇이 왜 + 할 일. 자기가 나가는 창이면 `…뺄 수 없습니다` 를 `…나갈 수 없습니다` 로 |
| `마지막 owner는 강등할 수 없다` | **`마지막 owner 의 권한은 내릴 수 없습니다 — 다른 사람을 owner 로 올린 뒤에 바꿔 주세요.`** | `강등`은 군대·인사 낱말이다. 화면은 권한을 올리고 내린다 |
| `권한 없음: '…'(미참여)는 멤버를 변경할 수 없다 (maintainer 이상 필요)` | **`멤버를 바꾸려면 maintainer 이상이어야 합니다. 지금은 이 프로젝트의 멤버가 아닙니다.`** | 사람 이름과 내부 판정어(`미참여`)를 지운다 — 자기가 누구인지는 자기가 안다 |

---

### 표 3. status 바꾸기 — 「보관」은 사건이다

**⚠ 선행 확인**: 아래 (가)는 A(백엔드)가 **보관 파급을 실제로 구현할 때만** 쓸 수 있다.
지금 구현은 아무것도 접지 않는다(위 결론 1). 파급이 없는데 확인 창을 세우면 겁만 주는 창이다.

| 갈래 | 자리 | 제안 문안 | 역번역 검산 |
|---|---|---|---|
| **(가) 문서까지 접힌다면** | cap | **`보관`** | 화면 사전에 이미 있다(`tidy.js` 「보관함」·「보관 해제」) |
| | title | **`「Section9 하네스 시스템」을 보관합니다`** | "Archives this project" |
| | desc | **`이 프로젝트의 문서가 모두 보관함으로 들어갑니다. 지워지지 않고, 「보관 해제」로 되돌릴 수 있습니다.`** | "all its documents move to the archive; nothing is deleted; reversible via Unarchive" — 되돌리는 낱말은 반드시 **「보관 해제」**다. 화면에 이미 그 버튼이 있다(`tidy.js:166`) — 「보관 풀기」 같은 새 낱말을 지으면 같은 개념에 이름이 둘이 된다 |
| | ok | **`보관하기`** | — |
| **(나) 목록에서만 내려간다면** | desc | **`프로젝트 고르기 목록에서 내려갑니다. 문서와 요청은 그대로 있고, 다시 「사용 중」으로 바꾸면 돌아옵니다.`** | "drops out of the project picker; documents and requests stay; reversible" — `tidy.js:229` 의 확정 문장 "보관한 문서는 그대로 있습니다. 목록에서만 내려와 있습니다." 와 같은 결 |
| **(다) 지금처럼 파급이 없다면** | — | **확인 창을 세우지 않는다.** 인라인 셀렉트로 즉시 저장하고 끝낸다 | 아무 일도 안 일어나는 변경에 "정말 하시겠습니까"를 물으면 다음번 진짜 경고를 안 읽는다 |
| 되돌리는 쪽(공통) | title | **`「…」의 보관을 해제합니다`** | 되돌림은 확인 창 없이. 되돌리는 일에는 확인을 걸지 않는다 |

status 값 라벨: `active` → **표기하지 않음**, `archived` → **`보관됨`**.
근거: `active` 는 정상이다. 정상은 줄을 얻지 않는다(s9-design 「카드 사실 줄」). 그리고 `active`
에 붙일 좋은 한국어 낱말이 없다는 사실 자체가 그 라벨이 필요 없다는 신호다 — 「활성」은
멤버 상태 칸이 이미 쓰고 있어 두 뜻이 겹치고, 「진행 중」은 Board 컬럼 이름과 충돌한다.

---

### 표 4. 관리 요약의 수치 문장

한국어의 수는 **단위 명사와 함께** 읽힌다. 그리고 `N / M` 은 분수·비율로 읽히지 "M 중 N"으로
읽히지 않는다 — 현행 `멤버 1/1` 이 분모를 잃는 이유다.

| 자리 | 초안 / 현행 | 제안 문안 | 역번역 검산 |
|---|---|---|---|
| 목록 셋째 줄 | `멤버 1 · 열린 요청 3 · 4분 전` | **`멤버 5명 · 남은 요청 12건 · 4분 전`** | "5 members · 12 unfinished requests · last activity 4 min ago" — `열린`은 open 직역인데 화면 사전은 이미 open 을 「착수를 기다리는」으로 푼다(빈 상태 문구). 그 자리에 넣기엔 길고, 실제로 세는 것은 open+in-progress+blocked+review = 안 끝난 것이다. 화면에 이미 사는 낱말 「남은 것」(`const.js`)·「미완료」를 쓴다 |
| 멤버 표 캡션 | `MEMBERS 1 활성 / 1` | **`멤버 3명 — 활성 2 · 만료 1`** / 만료 0이면 **`멤버 3명`** | "3 members — 2 active, 1 expired" — 만료가 없을 때 `만료 0` 을 세우지 않는다. 없는 문제를 보고하는 꼴이 된다 |
| Board 위 문맥 띠 | `● … · section9 · ACTIVE · 멤버 1 · 요청 45` | **`● Section9 하네스 시스템 · section9 · 멤버 1명 · 남은 요청 12건`** | `요청 45`는 무엇의 45인지 말하지 않는다(전체? 남은 것?). 그리고 `ACTIVE` 를 뺀다 — 정상은 말하지 않는다. `보관됨` 일 때만 세운다 |
| 접힌 보관 줄 | `+ 보관됨 2개` | **`+ 보관된 프로젝트 2개`** | "2 archived projects" — 「보관됨」 단독은 상태값이 그대로 새어 나온 것이다. 무엇이 2개인지 말한다 |
| 멤버 상태 칸 | `활성` / `만료` | **유지** | 이미 서 있고 짧다. designer 의 색 판정(`--c-blocked` → `--muted`)에 동의 — 만료는 고장이 아니다 |
| 만료일 빈 칸 | `무기한` | **유지** | 좋은 낱말이다. 입력 칸 쪽 `비우면 무기한` 도 그대로 |

---

### 표 5. 빈 상태

| 자리 | 초안 / 현행 | 제안 문안 | 역번역 검산 |
|---|---|---|---|
| 프로젝트 목록 0개 | `프로젝트가 없습니다` | **`아직 만든 프로젝트가 없습니다 — 위 「+ 새 프로젝트」를 누르면 여기에 줄이 쌓입니다.`** | "No projects yet — press + New project above and rows will accumulate here" — 화면에 이미 있는 틀을 그대로 쓴다: `아직 넣은 비밀이 없습니다 — 아래에 키 이름과 값을 넣으면 여기에 줄이 쌓입니다.`(userform). 빈 상태는 안내가 아니라 다음 행동을 주는 자리 |
| 필터 때문에 0개 | `결과 없음` | **`조건에 맞는 프로젝트가 없습니다 — 검색어나 필터를 지워 보세요.`** | "Nothing matches — clear the search or filters" — 위와 **반드시 다른 문장**이어야 한다. 같은 문장을 쓰면 "하나도 안 만들었나?" 하고 만들러 간다 |
| 멤버 표 0명 | `멤버 없음` | **표 자리에 추가 행만 세운다 + 위 한 줄 `지금 활성 멤버가 없습니다 — 아래 줄에서 넣으면 여기에 쌓입니다.`** | "No active members — add one in the row below" — 「아직」이 아니라 「지금」이다. 만든 사람이 자동 owner 라 이 상태는 처음부터 비어 있는 것이 아니라 **비게 된** 것이다(사실상 도달 불가: 마지막 owner 가드가 막는다, `s9:12894`) |
| 넣을 사람이 없음 | `추가할 등록 사용자 없음` | **`등록된 사람이 모두 이미 멤버입니다 — 새 사람은 Settings › 사용자 관리에서 admin 이 먼저 등록합니다.`** | "Everyone registered is already a member — a new person must be registered by an admin first" — 현행은 막다른 길이다(그래서 어쩌라고). 실측: 사용자 등록은 admin 만(`settings.js:100`) |
| 뷰어 권한 | (편집 컨트롤 disabled 나열) | **아무 문구도 세우지 않는다** — 컨트롤을 그리지 않는다(designer 판정 동의) | 회색 버튼과 "권한이 없습니다"는 둘 다 할 수 없는 일을 화면에 남겨 두는 것이다 |

---

### 어휘 수준 근거 · 사전 정합

**이 화면의 말투 실측** (`web/app/*.js` 전수): 존댓말 종결(`~습니다`·`~해 주세요`·`~어요`),
창머리 cap 은 2~4자 명사, 확인 창 취소는 예외 없이 `그만두기`, ok 는 동사+목적
(`영구 삭제`·`휴지통으로`·`멤버 빼기`·`지우기`), desc 는 "무엇이 남고 무엇이 사라지는가 +
되돌릴 수 있는가", 빈 상태는 `… 없습니다 — <다음 행동>` 의 em dash 한 틀.
**위 제안 전량이 이 틀 안에 있고, 새 틀을 만든 곳은 없다.**

**기존 사전과 겹쳐 확인한 낱말**: 보관 · 보관함 · 보관 해제 (`tidy.js`) / 멤버 · 권한 · 직무 ·
참여일 · 만료 · 활성 · 무기한 (`app.js` 멤버 표) / 고객 · 현업 담당자 · 개요 (`#proj-info` 격자) /
그만두기 · 닫기 · 계속 (`dialog.js`) / 사용자 관리 · 내 계정 · 디스플레이 (`settings.js`) /
남은 것 (`const.js`). **새로 지은 낱말 0개.**

**걷어낸 번역투**: `생성/제거/부여받다/식별자/재참여 불가능` 같은 한자어 연쇄 → `만들기/빼기/
됩니다/짧은 이름/다시 들어오려면`. `표시명`(display name)·`접근`(access)·`열린`(open) 같은
직역 대응. `자신을 제거합니다`(remove yourself)라는 영어 재귀 구문. `N 활성 / M` 이라는
영어 수치 표기. 「~되었습니다」는 한 곳도 쓰지 않았다 — 이 창들은 전부 **일어날 일**을
예고하지 이미 일어난 일을 보고하지 않는다.

**tech-writer 판정 대기로 남긴 것**(판정 없이 옮기지 않음): `slug`, `owner`/`maintainer`/
`contributor`/`viewer`, `admin`, `Settings`. 위 문안에서 전부 원어 그대로 두었고, 조사를
붙여 쓰지 않고 한 칸 띄웠다(`owner 가`·`maintainer 이상`) — 화면의 기존 손버릇과 같다.
판정이 「옮긴다」로 나오면 해당 칸만 갈아 끼우면 되게 문장 구조를 짜 두었다.

### 한계

- **보관 파급은 미확정 사실이다.** 표 3 (가)를 쓰려면 A 가 접힘을 구현해야 한다. 구현이
  없으면 (다)로 간다 — 확인 창 자체를 세우지 않는다. 이 판정을 하지 않은 채 (가) 문안을
  옮겨 적으면 화면이 사용자에게 거짓을 말한다.
- 프로젝트가 실물로 1개뿐이라 목록 셋째 줄의 수치 문장이 N개에서 어떻게 읽히는지 못 봤다.
  「남은 요청 12건」의 실제 집계 정의(어느 status 를 세는가)는 A·B2 가 확정해야 이 문장이
  참이 된다.
- ux-writer 와 병렬로 썼다. 형식(창 구조·버튼 배열·글자 수 상한)이 부딪히면 그쪽이 틀이고
  이 표는 그 틀에 채울 낱말이다 — 낱말만 뽑아 옮겨 넣어도 성립하도록 자리 기준으로 적었다.
- 코드·git 무접촉.

### 2026-08-31T23:01:10+09:00 subagent (agent: sub:subagent:a5e49d2a, by nicehugepark)

Confirming s9 note write to REQ-20260831-029-62x6

### 2026-08-31T23:01:18+09:00 response (agent: sub:ux-writer, by nicehugepark)

### 결론 — 문안은 다 확정했다. 다만 **문구가 못 쓸 자리가 셋** 나왔다

designer 스펙의 모든 자리에 문안을 붙였다(아래 표). 그런데 실물 코드를 짚어 보니
**초안이 약속하는 사실 셋이 지금 참이 아니다** — 문구가 아니라 그 자리의 설계를
먼저 정해야 문안이 선다.

1. **archived 는 지금 아무 일도 하지 않는다.** `bin/s9` 에서 `status` 는
   `active|archived` 검증만 받고 저장될 뿐이고(13024행), 카탈로그·헤더 필터·문서
   목록 어디도 이 값을 보지 않는다(`/api/projects` 15989행은 멤버십으로만 거른다,
   `fillProjects()` 는 status 를 안 본다). 그러니 「보관하면 문서가 목록에서
   내려갑니다」는 **거짓말이 된다**. → 확인 창 문안을 두 벌로 냈다(A안: 실제로
   내려가게 만든 뒤 / B안: 표시만 남을 때). B안이면 **확인 창 자체를 없앤다** —
   아무 일도 안 일어나는 데 묻는 창은 사용자를 한 번 더 세울 뿐이다.
2. **slug 는 고칠 수 없다.** `s9 project set` 이 받는 필드는 여덟이고
   (13016행: name·summary·customer·status·contact_name/org/email/phone) 거기에
   slug 가 없다. designer 와이어프레임의 `SLUG` 행은 **읽기 전용**이다. 그래서
   「설정 인라인 8필드」의 실제 여덟은 slug 를 빼고 **이름(title)** 을 넣은 조합이다.
3. **제거/빼기가 한 화면에 둘이다.** 지금 단추는 「− 제거」인데 확인 창은
   「멤버 빼기」다(`app.js:203, 261`). 같은 개념에 두 낱말 — 사전 위반. 아래에서
   하나로 통일했다.

곁가지로 잡은 어투 결함 셋(전부 아래 표에 문안 있음): 실패 줄의 「거부:」 접두 ·
「서버에 연결할 수 없습니다」(다른 창은 전부 「서버에 닿지 못했습니다」) · 서버의
반말 CLI 사유 문장을 존댓말 화면에 그대로 싣는 것.

---

### 1. Docs › PROJECT 진입점

| 자리 | 확정 문안 | 근거 |
|---|---|---|
| 목록 머리 단추 | `+ 프로젝트 만들기` | 버튼은 동사+목적(s9-design 6절). designer 초안 「+ 새 프로젝트」는 명사구라 **눌렀을 때 무엇이 생기는지**를 말하지 않는다. 빈 상태에서도 홀로 서야 하므로 목적어를 붙인 한 벌로 통일 |
| 〃 aria-label | `프로젝트 만들기 — 새 프로젝트 문서를 만듭니다` | 아이콘(+)이 뜻을 지고 있는 단추는 이름을 따로 준다(7절) |
| 빈 상태 (프로젝트 0) | `아직 프로젝트가 없습니다 — 멤버로 넣은 사람만 그 안의 요청·문서를 봅니다.` + 단추 `+ 프로젝트 만들기` | 빈 상태는 안내가 아니라 다음 행동(3절). 「없습니다 — <이 물건이 무엇을 해 주나>」는 이 화면의 확립 문법(`userform.js:349` 「아직 넣은 비밀이 없습니다 — …여기에 줄이 쌓입니다」) |
| 보관 묶음 접힘 | 접힘 `+ 보관한 프로젝트 N개` / 펼침 `− 접기` | designer 초안 「보관됨」은 피동 어미 상태 표시라 **단추(행동)** 에 못 쓴다. `+ N개 더 보기`가 이 목록의 접기 문법 |
| 행 셋째 줄 | `멤버 3 · 열린 요청 12 · 마지막 활동 4분 전` | 「4분 전」만 두면 **무엇이** 4분 전인지 없다. 「열린 요청」은 done·cancelled 아닌 것 — 띠·요약과 같은 낱말로 같은 것을 센다(B2 에서 수를 낱말에 맞출 것) |
| 행 상태 글자 | active = **아무 것도 안 그린다** / archived = `보관` | 정상 진행의 서술은 줄 자격이 없다(REQ-20260830-040-62x6). 점이 이미 말한 것을 글자로 되풀이하지 않는다. 색 없이 읽혀야 하는 쪽은 예외인 **보관**뿐 |

### 2. 생성 다이얼로그

| 자리 | 확정 문안 | 근거 |
|---|---|---|
| cap | `프로젝트` | s9dlg 의 cap 은 창의 갈래를 한 낱말로(「멤버」·「삭제」·「판정」과 같은 층) |
| title | `새 프로젝트를 만듭니다` | 이 화면 확인 창의 확립 어법은 **일어날 일의 평서문**(「이 문서를 휴지통으로 보냅니다」) |
| desc | `만든 사람이 owner가 됩니다. 멤버·현업 담당자는 만든 뒤 문서에서 채웁니다.` | 필드가 넷뿐인 까닭을 그 자리에서 답하고, 성공 뒤 문서로 넘어가는 이동을 미리 예고한다(4절 인과) |
| 필드 1 | 라벨 `이름` · placeholder `고객사 포털 개편` | placeholder 는 라벨 되풀이가 아니라 **예시**. 「표시명」은 시스템 용어 |
| 필드 2 | 라벨 `slug` · placeholder `portal-2026` · 도움말 `주소와 폴더 이름이 됩니다. 영문·숫자와 . _ - 만.` | 실제 규칙 `[A-Za-z0-9._-]+`(s9:12914) 그대로. 한글 자동 로마자화 없음 → 왜 못 쓰는지를 **치기 전에** 말한다(에러보다 안내가 싸다) |
| 필드 3 | 라벨 `개요(선택)` · placeholder `이 프로젝트가 무엇을 하는지 한 줄` | 선택 필드에만 「(선택)」 — 이 화면 선례(`직무(선택)`). 필수에 별표를 달면 범례가 필요해진다 |
| 필드 4 | 라벨 `고객(선택)` · placeholder `내부(자체)` | 〃 |
| 확인 단추 | `만들기` | 제목이 이미 목적어를 세웠다 — 창 안에서는 동사만(`지우기`·`영구 삭제`와 같은 층) |
| 취소 단추 | `그만두기` | 전 창 확립어. 「취소」는 이 화면에 없다 |
| 에러 · 이름 빈칸 | `이름을 적어 주세요.` | 비난 금지. 무엇을 하면 되는지만 |
| 에러 · slug 빈칸 | `slug 를 적어 주세요 — 이 이름으로 폴더가 생깁니다.` | 왜 비울 수 없는지 한 마디 |
| 에러 · slug 형식 | `slug 에는 영문·숫자와 . _ - 만 쓸 수 있습니다. 예: portal-2026` | 원인 한 문장 + 해결(예시) 한 문장. 「잘못된 형식입니다」류 금지 |
| 에러 · slug 중복 | `이미 portal 프로젝트가 있습니다 — 다른 slug 를 적어 주세요.` | designer 초안 「이미 있는 이름입니다」는 **무엇과** 부딪혔는지를 안 말한다 |
| 실패 줄 (서버) | `프로젝트를 만들지 못했습니다 — <사유>` | 확립 실패 어법(`session.js:515`). 입력값은 말없이 남긴다 |
| 실패 줄 (연결) | `서버에 닿지 못했습니다` | 확립어. `app.js:244` 의 「서버에 연결할 수 없습니다」는 이 자리 하나만 어긋나 있다 → 함께 고칠 것 |
| 권한 없음 | **문안 없음 — 단추를 안 그린다** | 회색 단추는 눌릴 것 같은 거짓 약속(3절) |

### 3. PRJ 문서 뷰 — 설정 인라인 라벨 8

**형식 판정 먼저**: 이 격자는 문서 뷰의 메타 표(`docs.js:295~`)다. 거기 라벨은
전부 **프론트매터 키 그대로**(status·summary·goal·project·priority·size)이고 mono
대문자로 그려진다. 여기에만 한국어 라벨을 섞으면 **한 표에 사전이 둘**이 된다.
그래서 라벨은 키를 따르고, **사람 말은 값·placeholder·도움말이 진다**.
(현재 `#proj-info` 의 한국어 라벨 「고객/현업 담당자/개요」가 옮겨 오면서 바뀌는
쪽이다 — 격자가 그쪽에 맞추는 것이 아니다.)

| # | 라벨 | 값 자리 문안 | 근거 |
|---|---|---|---|
| 1 | `title` | 제목 줄에서 그 자리 편집 · placeholder `프로젝트 이름` | 문서 제목이 곧 이 필드다 — 격자에 한 번 더 두면 같은 값이 두 자리 |
| 2 | `status` | `활성` / `보관` (셀렉트) | 4절 참조 |
| 3 | `summary` | placeholder `이 프로젝트가 무엇을 하는지 한 줄` | 생성 창과 같은 말 — 같은 필드는 두 자리에서 같은 예시를 준다 |
| 4 | `customer` | placeholder `내부(자체)` | 〃 |
| 5 | `contact_name` | placeholder `이름` | 담당자 넷은 한 줄에 나란히 — 라벨은 키, 칸 이름은 placeholder 가 진다 |
| 6 | `contact_org` | placeholder `소속` | 〃 |
| 7 | `contact_email` | placeholder `이메일` | 「메일」 아님 — 이 화면 확립어는 「회사 이메일」(userform) |
| 8 | `contact_phone` | placeholder `전화` | 〃 |
| — | `slug` | **읽기 전용** · 값 옆 흐린 한 줄 `주소·폴더 이름이라 만든 뒤에는 바꾸지 않습니다` | `s9 project set` 이 slug 를 안 받는다(13016행). 고칠 수 없는 칸을 입력처럼 그리면 눌러 보고 나서야 안다 |
| — | `created` / `updated` | 읽기 전용 | |
| 저장 실패 | `<필드 이름>을 바꾸지 못했습니다 — <사유>` | 지금 `fail()` 의 `거부: ` 접두(`app.js:230`)는 **사람을 나무라는 말**이다. 「…하지 못했습니다 — 까닭」이 이 화면의 확립 실패 어법 |
| 서버 사유 | 화면이 쓸 문장을 화면이 쓴다(서버의 CLI 문장 전달 금지) | 서버 사유는 반말체다(`권한 없음: … 변경할 수 없다`) — 존댓말 창에 그대로 실으면 한 문장 안에서 어투가 갈린다 |
| 권한 없음(뷰어) | 표·격자 아래 한 줄 `보기 권한입니다 — 설정과 멤버를 바꾸려면 maintainer 이상이어야 합니다.` | 무엇이 안 되나 + 왜(권한) + 무엇이 있어야 하나. 컨트롤은 그리지 않는다 |

### 4. status 확인 창 — **archived 는 사건일 때만 창을 받는다**

| 갈래 | 확정 문안 | 근거 |
|---|---|---|
| **A안** (B2 가 보관을 실제 동작으로 만들 때: 헤더 필터·문서 목록에서 내려감) | cap `프로젝트` / title `이 프로젝트를 보관합니다` / desc `요청·문서는 그대로 있고, 목록과 프로젝트 필터에서만 내려갑니다. 언제든 다시 활성으로 되돌립니다.` / ok `보관하기` / cancel `그만두기` | 결과 서술(무엇이 사라지지 **않는지**를 먼저) + 되돌릴 수 있음. 파괴가 아니므로 `safe:true`(초점을 취소에) 쓰지 않는다 |
| **B안** (보관이 표시로만 남을 때) | **확인 창 없음.** 셀렉트에서 바로 바꾸고, 값 옆에 흐린 한 줄 `보관은 아직 표시일 뿐입니다 — 목록과 필터에는 그대로 남습니다.` | 아무 일도 안 일어나는 데 묻는 창은 손만 늘린다(4절). 지금 코드가 이 상태다 |
| 보관 → 활성 | **확인 창 없음** · 셀렉트 즉시 저장 | 되돌리는 방향은 파괴가 아니다 |
| 낱말 충돌 경고 | PRJ 문서 뷰 하단 「이 문서 치우기 · **보관** · 삭제」와 status 의 **보관**이 한 화면에 둘 | 같은 낱말 단추가 둘이면 어느 쪽이 진짜인지 묻게 된다(designer 가 삭제 단추 중복을 금한 그 논리). 권고: **PRJ 문서에서는 하단 「보관」을 그리지 않는다** — 프로젝트의 보관은 status 가 진다. 판정은 designer 몫 |

### 5. 멤버 표

**낱말 통일 판정 — 「제거」로 간다(「빼기」 폐기).** 단추 「− 제거」와 창
「멤버 빼기」가 지금 함께 서 있고 하나를 골라야 한다. 국내 협업 도구가
「추가/제거」 짝으로 확립돼 있어(Jira·Notion·Slack 한국어) 순우리말로 되돌리는
것은 s9-design 6절이 기계 직역과 같은 결함으로 보는 그 패턴이다. **단, 자기
자신에게는 「나가기」** — 주어가 바뀌면 다른 동작이고, 그것 역시 확립 업계어다.

| 자리 | 확정 문안 | 근거 |
|---|---|---|
| 추가 단추 | `+ 추가` (현행 유지) | 표 머리·캡션이 목적어를 세웠다 |
| 제거 단추(남) | `− 제거` · aria-label `<이름> 멤버 제거` | 행마다 같은 글자라 스크린리더가 어느 행인지 모른다(7절) |
| 제거 확인 창(남) | cap `멤버` / title `<이름>을 <slug> 에서 제거합니다` / desc `프로젝트 문서는 그대로 남고, 이 사람의 접근만 끊깁니다. 나중에 다시 추가할 수 있습니다.` / ok `멤버 제거` / cancel `그만두기` | 앞 두 문장은 현행 그대로 좋다(사라지지 않는 것을 먼저 말한다). **되돌릴 수 있음 한 마디를 더한다** — 5초 취소가 없는 자리일수록 되돌아올 길을 말해야 손이 떨리지 않는다 |
| 자기 자신 단추 | `− 나가기` | 무게가 다르면 낱말이 달라야 한다. 「나를 제거」는 사람이 자기에게 쓰지 않는 말 |
| 자기 자신 확인 창 | cap `멤버` / `safe:true` / title `<slug> 에서 나갑니다` / desc `나가면 이 프로젝트의 요청·문서를 더 볼 수 없습니다. 다시 들어오려면 남은 maintainer 이상이 넣어 주어야 합니다.` / ok `나가기` / cancel `그만두기` | 결과가 **나에게** 온다 — 초점은 「그만두기」에서 시작(REQ-20260830-008-62x6 규율) |
| 마지막 owner · 제거 | 단추 disabled · title `마지막 owner는 뺄 수 없습니다 — 먼저 다른 사람을 owner로 올려 주세요.` | 못 하는 일 + 까닭 + **지금 할 수 있는 일** 3요소 |
| 마지막 owner · 나가기 | 단추 disabled · title `마지막 owner는 나갈 수 없습니다 — 먼저 다른 사람을 owner로 올려 주세요.` | 〃 |
| owner 행 셀렉트 disabled | title `owner 권한은 owner만 바꿀 수 있습니다.` | 현행 `owner 변경은 owner만 가능`은 조각 문장(명사형 종결)이라 화면 어투와 어긋난다 |
| owner 제거 disabled | title `owner는 owner만 제거할 수 있습니다.` | 〃 |
| 빈 상태 ① 멤버 0 · 넣을 사람 있음 | `아직 멤버가 없습니다 — 멤버로 넣은 사람만 이 프로젝트를 봅니다. 아래에서 골라 추가해 주세요.` (+ 추가 행은 표 자리에 그대로) | 「아무도 못 본다」는 admin 때문에 참이 아니다 → **규칙**을 말한다. 안내가 아니라 다음 행동 |
| 빈 상태 ② 등록 사용자 0 | `넣을 수 있는 사람이 아직 없습니다 — 먼저 Settings › 사용자 관리에서 사용자를 등록해 주세요.` (링크) | 후보가 없는데 폼을 보여 주면 막다른 길(3절) |
| 빈 상태 ③ 모두 이미 멤버 | `모두 이미 멤버입니다` | 현행 「추가할 등록 사용자 없음」은 고장처럼 읽힌다. 이건 **좋은 소식**이다 |
| 표 캡션 | 만료 0이면 `멤버 3명` · 만료 있으면 `멤버 3명 — 활성 2 · 만료 1` | designer 초안 「MEMBERS 1 활성 / 1」은 소리로 「1 활성 슬래시 1」이다(caption 은 읽히는 문장). 정상은 말하지 않는다 |
| 상태 열 | `활성` / `만료` (현행 유지) | |
| 만료 임박 툴팁 | `12일 뒤 만료` | 「만료 14일 이내」는 기준선이지 사실이 아니다. 시간은 사람 단위로(6절) |
| 만료일 입력 | title `비우면 무기한` (현행 유지) | |
| 저장 실패 줄 | `멤버를 바꾸지 못했습니다 — <사유>` / 연결 실패 `서버에 닿지 못했습니다` | 3절과 같은 판정 |

### 6. Board/Graph 문맥 띠

| 자리 | 확정 문안 | 근거 |
|---|---|---|
| 띠 본문 (active) | `● Section9 하네스 시스템 · section9 · 멤버 1 · 열린 요청 45` | **ACTIVE 를 뺀다** — 정상은 말하지 않는다(REQ-20260830-040-62x6). 점이 이미 그 말을 했고, 띠의 결정은 "지금 무엇을 보고 있나"지 "상태가 무엇인가"가 아니다 |
| 띠 본문 (archived) | `● … · 보관 · 멤버 1 · 열린 요청 45` | 정상이 아닌 것만 글자를 받는다 |
| 문서로 가는 링크 | `프로젝트 문서 열기` (doclink 문법, **화살표 없음**) | 점선 밑줄이 "누르면 열린다"를 이미 약속한다 — `→` 는 겹말이고 이 화면 어휘에 없다 |

### 7. 관리 요약 — 같은 것은 같은 낱말로 센다

세 자리(목록 셋째 줄 · 문맥 띠 · 문서 상단)가 같은 수를 말한다. 낱말을 한 벌로
고정한다: **`멤버 N` · `열린 요청 N` · `마지막 활동 <사람 시간>`**.
근거: 한 낱말이 자리마다 다른 것을 세면(요청 45 ↔ 열린 요청 3) 사용자는 어느
쪽이 맞는지 알 수 없다. 수를 낱말에 맞추는 것은 B2 몫이다.
문서 상단의 현행 `${status} · 멤버 1/1` 도 이 한 벌로 바꾼다(`1/1` 은 분수로 읽힌다).

### 8. 용어 사전 갱신분

**신설** — `프로젝트 만들기`(생성) · `보관`(프로젝트 status=archived 의 화면말) ·
`나가기`(자기 자신을 멤버에서 뺌) · `열린 요청`(done·cancelled 아닌 요청) ·
`마지막 활동`.

**통일** — 「빼기」 **폐기**, 「제거」로 (단추·창·aria-label 세 자리). 확립 업계어
「추가/제거」 짝을 유지한다.

**충돌 경고 둘**
- **담당** — DOC-20260831-005-62x6 의 일 축 어휘(담당·일손·무인 작업)와, 프로젝트의
  「현업 담당자」(고객 쪽 사람)가 같은 뿌리를 쓴다. 규칙: 프로젝트 쪽은 **반드시
  「현업 담당자」 전체 어구**로 쓰고 단독 「담당」·「담당자」로 줄이지 않는다.
- **보관** — 문서 치우기(tidy)의 보관과 프로젝트 status 의 보관이 한 화면에.
  4절의 권고(문서 뷰 하단 「보관」 미표시)로 푼다.

**결이 맞는지 확인함** — 확립 사전의 「맡은 창·일손·무인 작업·저절로/이어받다」는
전부 **일이 도는 축**의 말이고, 이번 문안은 **사람·조직 축**(멤버·권한·고객)이라
겹치는 낱말이 없다. 프로젝트 멤버를 「일손」이라 부르지 않는다 — 그들은 계정이지
지금 도는 프로세스가 아니다.

### 9. 한계 · 넘기는 것

- **tech-writer 몫(옮길지 둘지)**: `slug` · role 넷(`owner`/`maintainer`/
  `contributor`/`viewer`) · 메타 격자 라벨을 프론트매터 키로 두는 안 자체.
  내가 판정한 것은 **형식**이다 — 격자 라벨은 한 벌이어야 한다(8개만 한국어로
  옮기는 안은 반려). 낱말 자체의 거취는 tech-writer 가 이긴다.
- **translator 몫**: 위에서 옮기기로 판정된 것의 어휘 수준.
- **못 본 것**: 프로젝트가 1개뿐이라 N개 목록·보관 접힘·「N개 더 보기」를 실물로
  못 봤다. 문안 길이가 좁은 폭에서 줄바꿈을 만드는지는 B2 캡처에서 봐야 한다.
- 코드·git 무수정. 인용한 행 번호는 전부 실제로 열어 확인했다.

### 2026-08-31T23:01:19+09:00 subagent (agent: sub:translator:a0f15c09, by nicehugepark)

`[2026-08-31 22:53:53 KST - translator]`

판정 표를 REQ 문서에 기록했다 (9,700자, `--label response --agent translator`). 코드·git 무접촉.
문서: `/home/sjpark1/section9/vault/requests/2026/08/REQ-20260831-029-62x6.md`

### 결론 — 이번 화면의 어휘를 망칠 자리 셋

**1. 「보관」 문안은 지금 쓰면 거짓말이다 (가장 중요)**

리드가 예시로 준 "이 프로젝트의 문서가 모두 보관함으로 접힙니다"를 검산하려 구현을 읽었는데, **지금 그런 일은 일어나지 않는다**. 프로젝트 `status=archived` 는 PRJ 문서 frontmatter 한 칸만 바꾼다 —

- 문서 보관은 완전히 다른 경로다 (`bin/s9:11487`, 문서별 `archived` 칸)
- `project_can` 은 status 를 보지 않는다 (`bin/s9:12640-12660`) — 새 요청도 안 막힌다
- `projects_data` 는 archived 프로젝트도 전량 반환한다 (`bin/s9:15178`) — 목록에서 내려가지도 않는다

그래서 갈래 셋으로 문안을 나눴다: **(가) 문서까지 접히게 A가 구현하면** 그 문장을 쓰고, **(나) 목록에서만 내려가면** 다른 문장, **(다) 지금처럼 파급이 없으면 확인 창 자체를 세우지 않는다**(아무 일도 안 일어나는 변경에 겁을 주면 다음번 진짜 경고를 안 읽는다). 판정 없이 (가)를 옮겨 적으면 화면이 사용자에게 거짓을 말한다.

**2. 현행 멤버 제거 창의 「접근만 끊깁니다」는 사실 오류다**

`read` 는 누구나 허용이라(`bin/s9:12645`) 뺀 사람도 문서를 그대로 읽는다. 끊기는 것은 요청 올리기·설정 고치기뿐이다. `접근`(access 직역)을 걷어내고 결과를 그대로 서술했다:

> 이 프로젝트에 요청을 올리거나 설정을 고칠 수 없게 됩니다. 문서는 그대로 남고, 다시 넣을 수 있습니다.

**3. 서버 거부 문장이 반말 CLI 어투 그대로 화면에 뜬다**

`마지막 owner는 제거할 수 없다 (다른 owner를 먼저 지정하라)` 가 `거부: …` 로 찍힌다. 대시보드는 존댓말이다. 서버를 존댓말로 바꾸면 CLI 어투가 깨지니 **화면이 아는 사유는 화면 문장으로 갈아 끼우는 쪽**을 권했고, 알려진 사유 셋의 화면 문안을 표로 냈다.

### 표 다섯 벌 (자리 기준 · 원문/제안/역번역 검산)

| 표 | 자리 수 | 대표 교정 |
|---|---|---|
| 1. 생성 다이얼로그 | 13 | `표시명`→`이름`, placeholder 넷 전부 `예: …`, slug 보조줄에 **"만든 뒤에는 바꿀 수 없습니다"**(실측: `project set` 필드에 slug 없음) |
| 2. 확인 창 두 벌 + 거부 | 14 | 남을 뺄 때/자기가 나갈 때는 **다른 동사**(빼다/나가다) — `자신을 제거합니다`는 영어 재귀 구문 직역. title 의 slug→제목, 조사 앞 공백 교정 |
| 3. status(보관) | 갈래 3 | 되돌림은 반드시 **「보관 해제」**(`tidy.js:166` 에 이미 있는 버튼) — 「보관 풀기」류 새 낱말 금지. `active` 라벨은 **표기하지 않음**(정상은 말하지 않는다) |
| 4. 수치 문장 | 6 | `멤버 1/1`→`멤버 3명 — 활성 2 · 만료 1`(만료 0이면 뒷부분 없음). `열린 요청`(open 직역)→`남은 요청 12건` |
| 5. 빈 상태 | 5 | 진짜 0개와 필터로 0개는 **반드시 다른 문장**. `추가할 등록 사용자 없음`(막다른 길)→ admin 등록 경로까지 |

### 어휘 수준 근거

`web/app/*.js` 말투를 전수 실측해 틀을 뽑았다: 존댓말 종결, cap 은 2~4자 명사, cancel 은 예외 없이 `그만두기`, ok 는 동사+목적, desc 는 "무엇이 남고 무엇이 사라지는가 + 되돌릴 수 있는가", 빈 상태는 `… 없습니다 — <다음 행동>` em dash 한 틀. **제안 전량이 이 틀 안이고 새 틀은 0개, 새로 지은 낱말도 0개**다. 기존 사전과 겹쳐 확인한 낱말 15개(보관/보관함/보관 해제, 멤버·권한·직무·참여일·만료·활성·무기한, 고객·현업 담당자·개요 …)를 문서에 나열했다.

걷어낸 번역투: `생성/제거/부여받다/식별자/재참여 불가능` 한자어 연쇄, `표시명`(display name)·`접근`(access)·`열린`(open) 직역, `N 활성 / M` 영어 수치 표기. **「~되었습니다」는 한 곳도 쓰지 않았다** — 이 창들은 전부 일어날 일을 예고하지 이미 일어난 일을 보고하지 않는다.

### tech-writer 판정 대기로 남긴 것

`slug` · `owner`/`maintainer`/`contributor`/`viewer` · `admin` · `Settings` — **판정 없이 옮기지 않았다.** 전부 원어 그대로 두고 조사를 한 칸 띄웠으며(`owner 가`·`maintainer 이상`, 화면 기존 손버릇), 판정이 「옮긴다」로 나오면 **그 칸만 갈아 끼우면 되게** 문장 구조를 짰다.

### 한계

- 표 3 (가)는 A(백엔드)가 보관 파급을 구현할 때만 유효 — 리드가 A와 함께 판정해야 한다.
- 「남은 요청 12건」의 집계 정의(어느 status 를 세는가)는 A·B2 확정 사항.
- ux-writer 와 병렬 — 형식이 부딪히면 그쪽이 틀이고 이 표는 그 틀에 채울 낱말이다. 낱말만 뽑아 옮겨 넣어도 성립하도록 자리 기준으로 적었다.

### 2026-08-31T23:01:48+09:00 response (agent: lead:claude-fable-5, by nicehugepark)

리드 판정(보관 갈래): translator 검산대로 현재 archived 는 파급이 없다 — 확인 창이 거짓을 말하지 않게 (나) 채택: 보관의 실효과는 「프로젝트 목록에서 접힘(+ 보관된 프로젝트 N개)」 하나로 한정하고(B 화면 렌더 몫, 서버 캐스케이드 없음 — A 범위 불변), 확인 창 문안도 그 사실만 서술한다. 문서 접힘·권한 차단 캐스케이드는 이번 MVP 밖(필요 실증 시 별도 REQ). 멤버 제거 문구의 사실 오류 교정(접근≠읽기 차단)과 서버 거부 사유의 화면 문장 치환도 translator 안대로 B2 에 싣는다

### 2026-08-31T23:01:50+09:00 subagent (agent: sub:ux-writer:ade5ba79, by nicehugepark)

`[2026-08-31 22:58:14 KST - ux-writer]`

### 결론 — 전 자리 문안 확정. 다만 **초안이 약속하는 사실 셋이 지금 참이 아니다**

판정 표는 REQ 문서에 붙였다 (`--label response --agent ux-writer`, 9,566자):
`/home/sjpark1/section9/vault/requests/2026/08/REQ-20260831-029-62x6.md`

### 리드가 B2 전에 먼저 정해야 하는 것 셋 (전부 코드로 확인)

1. **archived 는 지금 아무 일도 하지 않는다.** `bin/s9:13024` 는 `active|archived` 검증만 하고 저장할 뿐이고, `/api/projects`(15989행)는 멤버십으로만 거르며 `fillProjects()` 는 status 를 보지 않는다. 그러니 「보관하면 목록에서 내려갑니다」는 **거짓말이 된다** → 확인 창 문안을 A안(실제 동작으로 만든 뒤)·B안(표시로만 남을 때)으로 냈고, **B안이면 확인 창 자체를 없앤다**. 아무 일도 안 일어나는 데 묻는 창은 손만 늘린다.
2. **slug 는 고칠 수 없다.** `s9 project set` 이 받는 필드는 여덟이고(13016행: name·summary·customer·status·contact_name/org/email/phone) slug 가 없다. designer 와이어프레임의 `SLUG` 행은 읽기 전용이다 — 「인라인 8필드」의 실제 여덟은 slug 를 빼고 **이름(title)** 을 넣은 조합.
3. **「제거」와 「빼기」가 한 화면에 둘이다** (`app.js:203` 단추 「− 제거」 / `app.js:261` 창 「멤버 빼기」). 사전 위반 → **「제거」로 통일**(확립 업계어 「추가/제거」 짝 유지, 순우리말 되돌림은 s9-design 6절이 금한 패턴). **단 자기 자신은 「나가기」** — 주어가 바뀌면 다른 동작이고 그것도 확립 업계어다.

### 곁가지로 잡은 어투 결함 셋 (문안 포함)
- 실패 줄의 `거부: ` 접두(`app.js:230`) → 「…하지 못했습니다 — 까닭」
- `서버에 연결할 수 없습니다`(`app.js:244`) → 확립어 `서버에 닿지 못했습니다` (다른 창은 전부 이쪽)
- 서버의 반말 CLI 사유(`권한 없음: … 변경할 수 없다`)를 존댓말 창에 그대로 싣는 것

### 판정 몇 개 (designer 초안과 갈린 곳)
- 「+ 새 프로젝트」 → **`+ 프로젝트 만들기`** (명사구는 눌렀을 때 무엇이 생기는지 안 말한다)
- 띠·행의 `ACTIVE` **삭제** — 정상은 말하지 않는다(REQ-20260830-040-62x6). `보관`만 글자를 받는다
- `프로젝트 문서 열기 →` 의 **화살표 삭제** (doclink 점선 밑줄이 이미 한 약속, 겹말)
- 캡션 「MEMBERS 1 활성 / 1」 → **`멤버 3명 — 활성 2 · 만료 1`**(만료 0이면 `멤버 3명`) — caption 은 소리로 읽힌다
- 메타 격자 라벨은 **문서 뷰의 확립 문법(프론트매터 키, mono 대문자)** 한 벌. 8개만 한국어로 옮기는 안은 반려 — 한 표에 사전이 둘이 된다. 낱말 거취는 tech-writer 가 이긴다
- 빈 상태 **세 벌**(designer 초안은 두 벌): 「모두 이미 멤버입니다」가 빠져 있었다 — 현행 「추가할 등록 사용자 없음」은 좋은 소식을 고장처럼 읽힌다

### 사전 충돌 경고 둘
- **담당** — DOC-20260831-005-62x6 의 일 축(담당·일손·무인 작업)과 프로젝트의 「현업 담당자」(고객 쪽 사람)가 뿌리를 공유. 규칙: 프로젝트 쪽은 **반드시 「현업 담당자」 전체 어구**, 단독 「담당」으로 줄이지 않는다.
- **보관** — 문서 치우기(tidy)의 보관과 프로젝트 status 의 보관이 한 화면에. 권고: **PRJ 문서에서는 하단 「보관」을 그리지 않는다**(프로젝트의 보관은 status 가 진다) — 판정은 designer 몫.

확립 사전(맡은 창·일손·무인 작업·저절로/이어받다)과는 축이 갈려 겹치는 낱말이 없다 — 그쪽은 일이 도는 축, 이번 문안은 사람·조직 축이다. 프로젝트 멤버를 「일손」이라 부르지 않는다.

코드·git 무수정. 인용한 행 번호는 전부 열어 확인했다.

### 2026-08-31T23:02:44+09:00 response (agent: lead:claude-fable-5, by nicehugepark)

리드 중재 확정(3인 판정 합의 — B2 는 이 노트와 3개 표를 그대로 옮겨 적는다):
1. 보관: 실효과 = 목록 접힘(B 렌더, 「+ 보관된 프로젝트 N개」) 하나. 되돌림이 한 번 클릭(보관 해제)이므로 ux-writer B안 절반 채택 — **확인 창을 세우지 않는다**(되돌리기 규율). 문서·권한 파급 없음이 사실이므로 어떤 문안도 그 이상을 약속하지 않는다.
2. PRJ 문서 하단의 문서 치우기 「보관」 단추는 그리지 않는다 — 프로젝트의 보관은 status 가 진다(한 화면 보관 둘 충돌 해소, ux-writer 권고 채택).
3. slug 는 읽기 전용(set 필드에 없음 — API 실측 일치), 인라인 여덟에는 title 이 들어간다. 생성 창에 slug 한 줄 정의 + "만든 뒤에는 바꿀 수 없습니다".
4. 낱말: role 넷·slug·active/archived·summary 원문 유지(tech-writer), 멤버 동사 「추가/제거」 통일 + 자기 자신은 「나가기」, 「현업 담당자」는 전체 어구로만, ACTIVE 라벨 비표기, 「+ 프로젝트 만들기」, 캡션 「멤버 N명 — 활성 a · 만료 b」, 빈 상태 세 벌, role 뜻 범례 한 줄(translator 문안), 멤버 제거 desc 는 translator 사실 교정본, 서버 거부 사유는 화면 문장 치환.

## History
- 2026-08-31T22:52:31+09:00 created by nicehugepark (status: open)
- 2026-08-31T22:52:38+09:00 status: open -> in-progress (by nicehugepark) — W1 착수
- 2026-08-31T23:02:54+09:00 status: in-progress -> done (by nicehugepark) — goal 충족 근거: 새 화면 전 문구가 3인 판정 표(ux-writer 형식 9,566자·tech-writer 용어 5,681자·translator 어휘 9,700자) + 리드 중재 노트로 확정 — B2 가 그대로 옮겨 적을 수 있는 상태. 문안이 약속할 뻔한 거짓 셋(보관 파급·slug 편집·이중 동사)을 코드 실측으로 미리 잡아 중재에 반영
