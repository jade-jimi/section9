---
id: REQ-20260830-029-62x6
type: request
title: 필수 시험만 남기는 정리
summary: 2,400건이 계속 늘면 결국 느려진다 — 겹치는 시험을 접고 필수 계약만 남기는 선별·계층화(스모크/필수/전체)
goal: 시험이 계층(스모크·필수·전체)으로 나뉘고 겹침 제거의 근거가 문서화되며, 필수 계층만으로 커밋 게이트가 돈다
status: in-progress
size: M
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: 9854e394
sessions: ["9854e394", "bb9cf06f"]
agents: ["sub:quality-assurance:a8b889f1", "sub:subagent:a4ac1268", "worker:auto-resume", "sub:subagent:af99d66b"]
contributions: [{"actor": "sub:quality-assurance:a8b889f1", "item": "029 시험 선별 분석 qa", "started": "2026-08-30T17:09:29+09:00", "ended": "2026-08-30T17:16:03+09:00", "result": "done", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/9854e394-1479-4180-9021-a3ed4bdae40f/tasks/a8b889f11395e763f.output"}, {"actor": "sub:subagent:a4ac1268", "item": "Writing note to REQ-20260830-029-62x6", "started": "2026-08-30T17:15:39+09:00", "ended": "2026-08-30T17:15:39+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/9854e394-1479-4180-9021-a3ed4bdae40f/subagents/agent-a4ac1268612e57a47.jsonl"}, {"actor": "sub:quality-assurance:a8b889f1", "item": "`[2026-08-30 17:15:21 KST - quality-assurance]`", "started": "2026-08-30T17:16:03+09:00", "ended": "2026-08-30T17:16:03+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/9854e394-1479-4180-9021-a3ed4bdae40f/subagents/agent-a8b889f11395e763f.jsonl"}, {"actor": "worker:auto-resume", "item": "범위 밖 패치 제안 (s9-guard 배선·C6 소생)", "started": "2026-08-30T17:56:18+09:00", "ended": "2026-08-30T17:56:18+09:00", "result": "done", "transcript": ""}, {"actor": "worker:auto-resume", "item": "2차 TDD 시나리오 6건", "started": "2026-08-30T17:56:44+09:00", "ended": "2026-08-30T17:56:44+09:00", "result": "done", "transcript": ""}, {"actor": "worker:auto-resume", "item": "2차: 뮤테이션 판독·근거 문서화·게이트 패치 제안", "started": "2026-08-30T17:57:05+09:00", "ended": "2026-08-30T17:57:05+09:00", "result": "done", "transcript": ""}, {"actor": "sub:subagent:af99d66b", "item": "Recording results to REQ-20260830-029-62x6", "started": "2026-08-30T18:05:12+09:00", "ended": "2026-08-30T18:05:12+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/9854e394-1479-4180-9021-a3ed4bdae40f/subagents/agent-af99d66b382d66fd2.jsonl"}, {"actor": "sub:quality-assurance:a8b889f1", "item": "`[2026-08-30 18:03:39 KST - quality-assurance]`", "started": "2026-08-30T18:05:14+09:00", "ended": "2026-08-30T18:05:14+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/9854e394-1479-4180-9021-a3ed4bdae40f/subagents/agent-a8b889f11395e763f.jsonl"}]
project: section9
children: ["DOC-20260830-005-62x6"]
derived_from: REQ-20260830-027-62x6
relates_why: {}
tags: ["test"]
created: 2026-08-30T16:23:45+09:00
status_since: 2026-08-30T18:00:31+09:00
updated: 2026-08-30T18:05:14+09:00
priority: 90
---

## Original

사용자 원문(16:23): 결국 전체 테스트 건수가 많아지면 많아질수록 느려질 수밖에 없으니 최소한의 필수 테스트들만 정리하는 작업들도 있어야겠다. — 27의 병렬화·선택 실행이 자리 잡은 뒤 착수(선별은 실측 위에서 해야 한다).

## Notes

### 2026-08-30T17:08:55+09:00 response (by nicehugepark)

착수 계획: ① 실측 — 파일별 실행 시간·건수 분포를 한 번의 계측 실행으로 수집 ② 분석(quality-assurance 위임) — 같은 계약을 겹쳐 검사하는 시험 후보, 계층 정의(스모크: 수십 초 안의 핵심 계약 / 필수: 커밋 게이트 = --changed 선택 / 전체: --jobs 4) ③ 합의 후 정리 반영. 겹침 제거는 근거 문서화가 조건(무엇을 왜 접었나) — 침묵 삭제 금지.

### 2026-08-30T17:15:39+09:00 subagent (agent: sub:subagent:a4ac1268, by nicehugepark)

Writing note to REQ-20260830-029-62x6

### 2026-08-30T17:15:49+09:00 quality-assurance (by nicehugepark)

「필수 시험만 남기는 정리」 분석 (분석만, 삭제 없음).

① 계측: 전체 실측 282초(real 4m42 / user 11m23, --jobs 4; 분석 작업과 동시 실행이라 오늘 앞선 실측 242·252초보다 김). 2,440 tests / 227 파일. AST 근사로 s9 subprocess 런타임 호출 총 ~2,029회 ≈ 직렬 324초 — 비용의 대부분이 프로세스 기동이라는 REQ-027 결론 재확인. 상위 비싼 파일(추정 s9 호출수): delegation_target 72 · agent_health 68 · dashboard_chat 67 · resume_item 63 · claim_release 62 · wake_effect 56 · worker_worktree 50 · sync_mode 48 · secret_where 45 · question_qst 44 · note_guard 43 · secret_store 42 · auto_resume_gate 41 · review_point_len 40 · wake 38 · s9_sync 34 · project_assets 34 · secret_external_path 33 · note_attach 33 · priority_handle 30(38 tests, setUp마다 init+new×2 — 빠른 시험 규칙 위반 패턴) · claim_terminal 30 · user_rename 29. 부수 발견 flaky: test_priority_handle DoorBehaviour setUp에서 s9 new가 rc=0인데 stdout 빈 값 → .split()[0] IndexError (679-test 샤드에서 1회, exit=1). 병렬 부하 시 출력 유실 의심 — 별도 추적 필요.

② 겹침 후보(접기/남기기):
- 「stall 판정은 단일 함수」 3곳(stall_pair f1 / stall_visible s5 / stall_trust c6): 접기 — 같은 소스 grep 계약, 한 곳이면 회귀 잡힘 / 남기기 — 파일별 반려 라운드 맥락뿐, 케이스를 stall_trust로 이관하면 해소.
- 「화면이 분을 직접 재지 않는다」 2곳(stall_pair f5 / wake_handle): 접기 — 동일 계약 / 남기기 — 카드·손잡이 각도 차이가 미미해 이관 가능.
- 「붙은 손이 wake를 막는다」 4파일(stall_trust c4·c8 / wake w2·w3 / wake_reach w5b / heartbeat h3): 남김 — 신호원이 다르다(위임 세션·pid 클레임·최근 진전·하트비트). 단 한 스위트로 모아 setUpClass 공유 가치.
- claim 만료(wake_effect r3a~d / wake_reach w2·w2b): 부분 겹침 — r3c와 w2가 같은 각도, 1~2케이스 접기 가능.
- 오늘 태어난 5스위트(commit_drift·heartbeat·jobfile·changed_select·jobs_shard): 계약 축이 기존과 다르다(커밋 증거·손길·잡 가시화·러너 자체) — 삭제 대상 아님. commit_drift t8만 wake_effect r8e와 인접.
- UI 스타일 계약 반복: hex 하드코딩 금지 16파일·색면 금지 4파일·var(--panel) 8파일 — 계약은 하나, 검사 대상 CSS 블록이 달라 삭제보다 helper 공용화가 답. "%s() 를 찾지 못했다" helper 23파일 복붙 — tests/fixtures 모듈로 추출.

③ 계층 제안:
- 스모크(--smoke, 목표 30초): test_state_truth · test_catalog_atomic · test_commit_gate · test_note_guard · test_relates_why · test_review_point_len · test_changed_select · test_jobs_shard · test_stall_trust · test_wake · test_closed_no_worker · test_stdlib_only (12파일 ~124 tests, 추정 s9 호출 ~110 ≈ 18초 + 로직, --jobs 4면 여유). __main__.py에 SMOKE 튜플 + --smoke 스위치.
- 필수(커밋 게이트): --changed 단독으로는 부족 판정 — bin/s9·bin/s9-* 변경이 전체 폴백이라 주 산출물이 bin/s9 단일 파일인 이 리포에선 백엔드 작업 대부분 이득 없음(시험 파일·web 조각 변경에만 유효). 게이트 = 스모크 ∪ --changed (--gate 스위치): changed가 []여도 스모크는 돈다. 근본 개선은 bin/s9의 명령 단위 매핑(별도 REQ감).
- 전체: --jobs 4, review/done 전·야간. green 스탬프는 전체만(현행 유지).

④ 정리 순서와 위험:
1) 접기 전 뮤테이션 확인 — grep형 시험은 리팩터로 이미 죽은(항상 통과) 것일 수 있다: 대상 문자열을 일부러 바꿔 실패하는지 본 뒤 접는다.
2) 케이스 이관 → 파일 병합 순 — 중복 계약은 케이스 단위로 모으고, 원 파일 docstring의 REQ/실사고 앵커를 함께 옮긴다(회귀 계약 소실 방지). 커밋마다 전체 green 후 스탬프.
3) stall/wake 7파일(~103 tests)은 반려-라운드 기준에서 기능 기준(판정/게이트/화면)으로 재편 — 케이스 보존, setUpClass 공유로 s9 호출 절감.
4) 파일명 변경 시 SERIAL 튜플·SMOKE 목록 동기화. 최대 위험은 계약 소실(앵커 이관으로 완화)과 flaky 은폐(priority_handle 건 원인 규명 선행).

### 2026-08-30T17:16:03+09:00 subagent (agent: sub:quality-assurance:a8b889f1, by nicehugepark)

`[2026-08-30 17:15:21 KST - quality-assurance]`

**REQ-20260830-029-62x6 분석 보고** (근거 전문은 `s9 note REQ-20260830-029-62x6` 기록, 로그 `/tmp/claude-1000/-home-sjpark1-section9/9854e394-1479-4180-9021-a3ed4bdae40f/scratchpad/fullrun.log`)

① **계측**: 전체 282초(real 4m42, --jobs 4, 분석과 동시 실행이라 실측 242·252초보다 김), 2,440건/227파일, exit=1 — flaky 1건: `test_priority_handle` setUp에서 `s9 new` rc=0인데 stdout 빈 값 → IndexError(병렬 부하 출력 유실 의심, 추적 필요). AST 근사 s9 호출 ~2,029회 ≈ 직렬 324초 — 병목은 프로세스 기동. 상위: delegation_target 72 · agent_health 68 · dashboard_chat 67 · resume_item 63 · claim_release 62 · wake_effect 56 · worker_worktree 50 · sync_mode 48 (이하 노트에 22위까지).

② **겹침**: 접기 가능 — 「stall 단일판정」 3곳(pair f1/visible s5/trust c6), 「화면이 분을 안 잰다」 2곳(pair f5/wake_handle), claim 만료 1~2케이스(wake_effect r3c≈wake_reach w2). 남김 — 「붙은 손이 wake 거부」 4파일은 신호원이 다름(세션·pid·진전·하트비트). 오늘 태어난 5스위트는 계약 축이 달라 삭제 대상 아님. UI 스타일 grep(헥스 금지 16파일 등)은 삭제보다 helper 공용화(23파일 복붙 해소).

③ **계층**: 스모크 `--smoke` 12파일(state_truth·catalog_atomic·commit_gate·note_guard·relates_why·review_point_len·changed_select·jobs_shard·stall_trust·wake·closed_no_worker·stdlib_only, ~124건, 추정 20초대). 필수 게이트 — **--changed 단독 부족 판정**: bin/s9(-*) 변경이 전체 폴백이라 백엔드 작업 대부분 무효. `--gate` = 스모크 ∪ --changed 제안. 전체는 현행 --jobs 4, green 스탬프 전체만.

④ **순서/위험**: 뮤테이션 확인(죽은 grep 시험 판별) → 케이스 이관+앵커 docstring 동반 이동 → stall/wake 7파일 기능 기준 재편(케이스 보존) → SERIAL·SMOKE 목록 동기화. 최대 위험은 회귀 계약 소실과 flaky 은폐 — priority_handle 원인 규명이 선행이다.

### 2026-08-30T17:23:14+09:00 response (by nicehugepark)

### 1차 반영 (계층 스위치)

QA 분석 채택분을 구현: ① `--smoke`(핵심 계약 12파일)·`--gate`(스모크 ∪ --changed, QA 판정: --changed 단독은 bin/s9 변경 전체 폴백 탓에 게이트로 부족) 러너 스위치 ② patterns() 가 정확한 파일명을 부분일치로 넓히던 결함 수정(test_wake.py 가 wake 계열 전부를 끌어와 계층의 뜻이 사라지던 것 — --changed 의 과선택도 같은 뿌리) ③ flaky 선행 과제: test_priority_handle 의 new() 가 rc·stderr 를 삼켜 IndexError 로 둔갑하던 것을 소리 나게 고침(다음 발생 시 원인이 잡힌다 — 원인 자체는 미확정으로 정직하게 남김).

실측: --smoke 직렬 86초 / --smoke --jobs 4 = **30초** (QA 추정 20초대에 근접 — wake·stall_trust 가 스모크에서 무겁다, 2차 정리 때 재검토). 남은 2차: 뮤테이션 확인 → 겹침 이관(stall 단일판정 3곳 등) → stall/wake 7파일 재편 — 회귀 계약 소실이 최대 위험이라 별도 라운드로.

### 2026-08-30T17:44:06+09:00 workspace (by nicehugepark)

본 저장소에서 이어받는다 — 자기 도구를 고치는 일이라 샌드박스가 사본에서 범위를 자른다. 커밋하면 다음 라운드부터 작업자가 다시 자기 워크트리를 받는다.

### 2026-08-30T17:48:49+09:00 commit (by nicehugepark)

커밋 96fb4c8 — 오후의 판 — 낱말이 결과를 말하고, Enter 가 안전한 쪽에 서고, calm 이 숨을 쉰다

### 2026-08-30T17:56:18+09:00 conflict (agent: worker:auto-resume, by nicehugepark)

이 라운드에 받은 코드 파일이 bin/s9 뿐이라 아래 둘은 적용하지 못했다 — 패치 제안으로 남긴다 (worker:auto-resume bb9cf06f).

**① 커밋 게이트에 필수 계층 배선 (bin/s9-guard, goal의 마지막 조각)**

현재 `staged_tests_gate` 는 커밋에 담긴 시험 파일만 돌린다 → 필수 계층이 게이트로 돌지 않는다. 제안: 담긴 시험 ∪ 스모크로 확장 (합집합 경로는 러너가 이미 지원, `python3 tests/ --smoke runner_patterns --jobs 4` 실측 녹색 17:53).

```python
### staged_tests_gate 안, names 계산 뒤:
    r = subprocess.run(["python3", "tests/", "--smoke", "--jobs", "4", *names],
                       capture_output=True, text=True, cwd=ROOT, timeout=600,
                       env=clean_git_env())
    # names 가 비어도 스모크는 돈다 — 조기 return(if not names) 을 지운다.
```

`--gate`(스모크 ∪ --changed)가 아니라 `--smoke ∪ names` 를 권한다: bin/s9 는 미커밋 변경이 잦아 --changed 가 매번 전체 폴백(4분대)로 밀리고, 그러면 s9-guard 독스트링의 경고("전체 스위트를 매 커밋에 물리면 규율이 먼저 죽는다") 그대로가 된다. 스모크 ∪ 담긴 시험은 30~40초로 유계다. --gate 전체 폴백은 review/done 전 수동 실행 몫으로 남긴다.

**② 죽은 시험 소생 (tests/test_stall_trust.py C6)**

뮤테이션 실측(17:51): stall_mins 의 `stall_verdict(` 호출을 깨도 C6 이 통과한다 — 독스트링의 "`stall_verdict()`" 문구가 grep 을 만족시킨다. 소생 제안: 본문에서 독스트링·주석을 걷어낸 뒤 검사하거나, 앵커를 실행문으로 좁힌다.

```python
        i = src.index("def stall_mins(")
        body = src[i:src.index("\ndef ", i + 10)]
        code = re.sub(r'"""[\s\S]*?"""', "", body)   # 독스트링 제거 — 주석이 계약을 대신 만족하면 죽은 시험이 된다
        code = re.sub(r"(?m)#.*$", "", code)
        self.assertIn("stall_verdict(", code,
                      "stall_mins 가 판정을 따로 갖고 있다")
```

같은 병이 bin/s9 를 grep 하는 다른 시험에도 있을 수 있다 — 접기 라운드(DOC-20260830-005-62x6 순서 ①)에서 파이썬판 `_code` 헬퍼를 tests/fixtures 로 두고 일괄 적용을 권한다.

### 2026-08-30T17:56:44+09:00 tdd (agent: worker:auto-resume, by nicehugepark)

TDD 시나리오 (2차 라운드 — 계층 검증·뮤테이션 판독, 전부 실측 통과 2026-08-30 17:45~17:55)

- [x] 정상: --smoke 가 12파일 계층만 돌고 녹색이다 (--jobs 4, exit 0)
- [x] 정상: 러너가 정확 파일명을 부분일치로 넓히지 않는다 (test_runner_patterns 10건 녹색)
- [x] 경계: 스모크 ∪ 지정 조각 합집합 실행이 동작한다 (`python3 tests/ --smoke runner_patterns --jobs 4` 녹색 — s9-guard 배선 제안의 전제)
- [x] 실패: 의미 불변 뮤테이션에 산 시험은 빨개진다 — stalled_requests 주석 뮤테이션에 stall_visible S5 즉시 실패, 같은 실행의 나머지 26건 녹색(격리 증명)
- [x] 실패: 죽은 시험이 판별된다 — stall_mins 호출 뮤테이션에 stall_trust C6 통과(독스트링이 grep 만족) → 소생 패치를 conflict 노트로 제안
- [x] 회귀: 뮤테이션 복원 후 bin/s9 md5 원복(fc60b04c…) · stall_visible/stall_trust 기준선 27건 녹색 · jobfile/runner_patterns 재실행 녹색, priority_handle flaky 재발 없음

### 2026-08-30T17:57:05+09:00 response (agent: worker:auto-resume, by nicehugepark)

### 2차 라운드 (깨우기 재개, worker bb9cf06f)

이 라운드의 코드 몫이 bin/s9 하나라 2차를 "적용"이 아니라 "판정과 근거"로 진행했다 — 접기 자체(tests/ 이관·재편)는 다른 작업자와의 충돌을 피해 손대지 않았다.

① **뮤테이션 확인(2차 순서 1번) 완료** — 의미 불변 뮤테이션(공백·주석: 동작 동일, grep 문자열만 변화)을 bin/s9 에 넣었다 빼는 방식. 동시 작업자가 그 순간 실행해도 무해하다. 결과: stall_visible S5 생존, **stall_trust C6 죽음 실증**(독스트링 "stall_verdict()" 가 grep 을 만족 — stall_mins 가 판정을 두 벌로 만들어도 못 잡는다), stall_pair·wake_handle 은 _grab+_code 구조로 생존. bin/s9 는 md5 로 바이트 원복 확인(fc60b04c…).

② **겹침 제거 근거 문서화(goal 2조각) 완료** — DOC-20260830-005-62x6 「시험 계층과 겹침 근거」: 계층 정의, 접기 판정표(모을 것/남길 것/공용화할 것), 뮤테이션 판독법과 "grep 시험은 주석 걷은 코드만 봐야 한다" 교훈, 접기 라운드 순서(C6 소생 선행).

③ **커밋 게이트 배선(goal 3조각)은 패치 전달로** — 게이트는 bin/s9-guard 에 살아서 범위 밖. conflict 노트에 패치 제안: 담긴 시험 ∪ 스모크(`--smoke ∪ names`, 30~40초 유계). --gate(∪ --changed)를 안 권한 이유: bin/s9 미커밋이 잦아 매번 전체 폴백 4분대 — 게이트 규율이 먼저 죽는다. 합집합 경로는 실측 녹색.

검증: --smoke --jobs 4 exit 0 / runner·jobfile·priority_handle 계열 재실행 녹색(flaky 재발 없음) / 뮤테이션 전후 기준선 27건 녹색.

남은 일(리드): conflict 노트 패치 2건 적용 → 접기 라운드(케이스 이관·stall/wake 재편·fixtures 공용화)는 파일 충돌이 풀린 뒤 별도 라운드 또는 파생 REQ 로.

### 2026-08-30T18:04:59+09:00 quality-assurance (by nicehugepark)

2차(정리 본체) 실행 결과 — 범위 tests/ 준수, 코드·시험 삭제 0건.

① 뮤테이션 확인 (bin/s9 일시 변형→표적 실행→복원, 매회 git diff 무변경 실증):
- M1(stall_mins 의 stall_verdict 호출 제거): stall_trust c6 이 Green 으로 남는 dead-spot 실증 — assertIn("stall_verdict(") 이 docstring 산문에 걸려 있었다. 행동 시험 c7 이 대신 Red. → c6 을 산문이 흉내 못 내는 호출문 정규식(return stall_verdict\() 으로 강화하고, 재뮤테이션으로 Red 전환 확인 (clean Green / mutant Red / 복원 diff 무).
- M2(stalled_requests 의 stalled_mins 읽기 제거): stall_visible s5 Red — 살아 있음 실증.
- web/index.html 표적(stall_pair f5·wake_handle)의 뮤테이션은 designer 동시 작업(web/·해당 시험 파일 편집 중 확인)으로 보류 — 동시 편집 규율.

② 겹침 이관: **정독 결과 접을 것이 없다.** grep 수준에서 겹침으로 보인 세 쌍이 정독에서는 층이 갈린다 — 단일판정 3곳은 정의(c6)/CLI 소비자(s5)/화면 JS 술어(f1)로 상보, r3c↔w2 는 --apply 통합/유닛 유예 경계로 상보(--apply 가 stalled 를 건너뛰는 회귀는 r3c 만 잡는다). "판단이 갈리면 접지 말고 남겨라" 지침대로 케이스 보존, 대신 세 쌍 모두에 층 구분과 접지 않는 이유를 docstring 교차 참조로 박아 재지목을 막았다(test_stall_trust·test_stall_visible·test_wake_effect·test_wake_reach). f5↔wake_handle 쌍은 제외 지시대로 목록만: 둘 다 "화면이 분을 직접 재지 않는다"를 web JS 에서 grep — 다음 라운드 이관 후보.

③ helper 공용화: tests/websrc.py 신설(FN_RE·HEX_RE — 방법만, 계약은 각 파일에). 의미 동일 `_fn` 복붙 16파일을 위임으로 교체, 헥스 하드코딩 금지 한 줄 18곳을 websrc.no_hex 로 — 총 23파일, 134 삽입/85 삭제. 들여쓰기 변형(`\n  \}`·self.code) 6파일은 매칭 범위가 달라 의도적으로 남김(모듈 docstring 에 이유 명시). helper 자체의 Red 능력은 크래프트 입력으로 실증(없는 함수→Red, 헥스→Red).

④ 러너 동기화: SMOKE 12·SERIAL 6 전부 실재 파일 확인(파일명 변경 없어 추가 조치 불요).

검증: 손댄 27개 스위트 표적 실행 441건 OK(29초) · --smoke 직렬 59초/--jobs 4 와 26초(30초 목표는 --jobs 4 조합에서만 달성 — 러너가 --smoke 에 jobs 기본값을 주는 개선 여지, 리드 판단 몫) · 전체 --jobs 4 실측 4m47s exit=1: 실패 2건(catalog_atomic a3·claim_terminal b1) 모두 setUp 의 s9 호출이 SyntaxError — 실행 중 다른 세션이 bin/s9 를 실시간 편집한 순간을 읽은 것(6766 대괄호 미폐합→13284 콜론 누락, 서로 다른 두 순간, 현재 bin/s9 컴파일 정상·M 상태). 두 스위트 재실행 7건 OK exit=0. 내 변경과 무관 실증: 내 뮤테이션은 실행 전 diff-clean 복원, 위치(9138·9183)도 불일치. 시험 건수 before/after: 삭제 0, 총수 변화는 동시 작업자 추가분(2,440→2,461). **전체 green 스탬프는 bin/s9 편집 세션이 손을 뗀 뒤 리드가 전체 재실행으로 확정할 것.** 이전 실행의 priority_handle flaky 는 이번 실행에서 미재현 — 별도 추적 유지.

### 2026-08-30T18:05:12+09:00 subagent (agent: sub:subagent:af99d66b, by nicehugepark)

Recording results to REQ-20260830-029-62x6

### 2026-08-30T18:05:14+09:00 subagent (agent: sub:quality-assurance:a8b889f1, by nicehugepark)

`[2026-08-30 18:03:39 KST - quality-assurance]`

**REQ-20260830-029-62x6 2차(정리 본체) 완료** — 범위 tests/ 준수, 시험 삭제 0건. 상세는 REQ 노트에 기록됨.

**① 뮤테이션**: bin/s9 일시 변형→표적 실행→복원(매회 `git diff` 무변경 실증). 성과 — `test_stall_trust.py` c6의 **dead-spot 실증**: `assertIn("stall_verdict(")` 이 docstring 산문에 걸려 호출을 지워도 Green이었다(행동 시험 c7이 대신 잡음). c6을 호출문 정규식으로 강화하고 재뮤테이션으로 Red 전환 확인. s5는 살아있음 실증. web 표적(f5·wake_handle) 뮤테이션은 designer가 web/·해당 시험을 편집 중이라 보류.

**② 이관**: **정독 결과 접을 것이 없다** — 단일판정 3곳은 정의/CLI/화면 JS의 상보 층, r3c↔w2는 --apply 통합/유닛 경계로 상보("판단이 갈리면 남겨라" 지침 적용). 4개 파일에 층 구분 교차 참조 docstring을 박아 재지목을 막았다. f5↔wake_handle 쌍은 지시대로 목록만 남김(다음 라운드 후보).

**③ 공용화**: `/home/sjpark1/section9/tests/websrc.py` 신설, `_fn` 복붙 16파일 + 헥스 금지 18곳 → 총 23파일 이관(134+/85-). 변형 6파일은 매칭 범위가 달라 의도적 보존. helper의 Red 능력은 크래프트 입력으로 실증.

**④ 동기화**: SMOKE 12·SERIAL 6 전부 실재 확인(파일명 변경 없음).

**검증**: 손댄 27스위트 441건 OK(29초) · --smoke: 직렬 59초 / `--jobs 4` 와 26초(목표 달성은 jobs 조합에서만 — 러너 기본값 개선 여지는 리드 몫) · 전체 --jobs 4: 4m47s exit=1, 실패 2건 모두 **다른 세션의 bin/s9 실시간 편집 순간을 읽은 SyntaxError**(내 변경·뮤테이션과 위치·시점 불일치 실증), 재실행 7건 OK exit=0. **전체 green 스탬프는 bin/s9 편집 세션 종료 후 리드 재실행으로 확정 필요.** 커밋·상태 전이는 리드 몫으로 남긴다.

변경 파일: `/home/sjpark1/section9/tests/websrc.py`(신규), `test_stall_trust.py`·`test_stall_visible.py`·`test_wake_effect.py`·`test_wake_reach.py`(계약 강화·교차 참조), 그 외 23파일 helper 이관.

## History
- 2026-08-30T16:23:45+09:00 created by nicehugepark (status: open)
- 2026-08-30T17:08:33+09:00 status: open -> in-progress (by nicehugepark) — drag 이동 [via dashboard]
- 2026-08-30T17:37:31+09:00 priority: 50 -> 90 (by nicehugepark) [via dashboard]
- 2026-08-30T17:57:13+09:00 status: in-progress -> blocked (by nicehugepark) — 패치 적용 대기(리드): 이 라운드 코드 몫이 bin/s9 뿐이라 goal 마지막 조각인 커밋 게이트 배선(bin/s9-guard)과 죽은 시험 소생(tests/test_stall_trust.py C6)을 적용하지 못했다 — 패치 전문은 conflict 노트에. 해제 조건: 리드가 패치 2건 적용 후 python3 tests/ --smoke --jobs 4 녹색과 커밋 시 게이트 30~40초 실측 확인 → 그때 review 상신(계층·근거 문서는 DOC-20260830-005-62x6 로 완료).
- 2026-08-30T18:00:31+09:00 status: blocked -> in-progress (by nicehugepark) — 거짓 선행 복구 (REQ-20260830-036-62x6): published 지식 문서는 선행이 될 수 없다 — 무인 작업자의 blocked 노트에 적힌 DOC id 를 자동 수집기가 선행으로 잡은 오염을 걷었다. 2차 정리는 리드의 QA 위임이 계속 진행 중
