---
id: REQ-20260825-086-62x6
type: request
title: 진행 점멸등 오판
summary: in-progress 카드의 live 판정이 uid 접미사 불일치·위임 에이전트 미등록으로 실제 진행을 못 잡음
status: done
size: S
user: sjpark1
machine: DESKTOP-TEHV1KR
session: d332535d
project: section9
relates: ["REQ-20260826-013-62x6"]
tags: ["dashboard", "index", "session"]
created: 2026-08-25T22:10:52+09:00
status_since: 2026-08-26T12:43:28+09:00
updated: 2026-08-26T13:22:56+09:00
---

## Original

사용자 지적: in-progress에 등록은 됐는데 실제 작동 중인지 녹색점멸이 안 켜지는 것들은 왜 그런건가? 병렬진행이 안되나? 066은 왜 호박점멸등이야?

실측 원인 2건:
(1) 세션 바인딩 active_reqs에 접미사 없는 짧은 id가 저장된다(예: REQ-20260825-080). 카탈로그 id는 REQ-20260825-080-62x6 이라 live_map의 active.get(r[id]) 매칭이 실패 → 직접 증거 없음으로 회색. 접미사 없는 구 id(048)만 우연히 맞아 녹색이 켜졌다.
(2) 리드가 위임한 in-process 에이전트(designer/staff-engineer)는 클레임(active_reqs)도 agent_transcript_path도 등록되지 않아 병렬 진행이 화면에 전혀 안 보인다.
(3) 066의 호박은 auto_resume 스폰 마커(600초 창) 때문인데, 그 워커는 fable 한도로 즉사했다 — 즉 현재 앰버는 기동 사실만 보증하고 생존은 보증하지 않는다.

## Notes

### 2026-08-26T00:24:22+09:00 response (by sjpark1)

결함 3건 모두 코드로 닫혔다 — `python3 tests/ live_signal` 12/12 green. 다만 (2)는 이 머신에서 아직 발효되지 않았다(아래).

### 결함별 상태
(1) uid 접미 불일치 — 닫힘. `id_alias()`/`canon_id()`/`binding_req_ids()` 로 짧은 id 등록을 정식 id 로 정규화(L1). 후보가 둘 이상인 짧은 형태는 매핑하지 않는다(잘못 집는 것보다 못 찾는 편이 낫다). 같은 원인으로 깨졌던 `rework_claimed` 의 중복 스폰도 함께 닫혔다(L9). 새로 저장되는 값은 애초에 정식 id 로 정규화되고(L6), 떠나는 전이는 짧은 id 등록도 제거한다(L8).
(2) 위임 에이전트 미등록 — 코드는 닫힘. `s9 claim <REQ> --agent-transcript <경로>` 가 클레임+transcript 를 등록하고(L2), transcript 는 리스트로 누적돼 병렬 위임 여러 건이 서로 덮어쓰지 않는다(L7). 자동 등록은 REQ-20260825-088-62x6 의 PostToolUse(Agent) 훅(bin/s9-audit-agent)이 맡는다.
(3) 죽은 워커 앰버 — 닫힘. 스폰 마커에 pid 를 기록하고(L10/L11/L12), 생존하지 않으면 앰버 대신 `live_kind=spawn_failed` + 워커 로그 마지막 줄을 사유로 노출한다(L3). pid 가 살아 있어도 claude 프로세스가 아니면 재사용으로 보고 실패 처리(L5). pid 없는 구 마커는 하위호환으로 기존 앰버 유지(L4).

### 발효되지 않은 지점 (판정 필요)
`~/.claude/settings.json` 에 **PostToolUse(Agent) 훅이 없다.** 설치된 훅은 UserPromptSubmit·SessionStart·Stop·SessionEnd·SubagentStop 5개뿐인데, 정본 `harness/claude/hooks.json` 에는 PostToolUse(matcher=Agent) 가 있다. 즉 설정이 REQ-20260825-088-62x6 이전 상태로 굳어 있어 위임 자동 등록이 실제로는 한 번도 돌지 않는다 — 결함 (2)의 증상(병렬 위임이 화면에 안 보임)이 코드 수정과 무관하게 계속된다.
실측 근거: 지금 이 세션(cb49b2cd)의 바인딩 active_reqs 에 내가 위임받은 095/080/086 이 없다.
또 하나: 설치된 훅 5개가 전부 `/tmp/claude-1000/.../d332535d-.../scratchpad/wt/bin/` 를 가리킨다 — 임시 워크트리 경로다. 지워지면 감사 훅이 통째로 죽는다.
`bin/s9-install` 을 돌리면 둘 다 정리되지만, 워크트리 경로가 의도된 것일 수 있어 임의로 실행하지 않았다 — 리드 판단이 필요하다.

### 2026-08-26T00:24:51+09:00 tdd (by sjpark1)

TDD 시나리오 (진행 점멸등 정합) — `python3 tests/ live_signal` 12/12 OK

결함 (1) uid 접미 불일치
- [x] L1 짧은 id 로 저장된 과거 active_reqs 도 정식 id 카드와 매칭돼 녹색(direct)이 켜진다
- [x] L6 짧은 id 로 클레임해도 바인딩에는 정식 id 로 저장된다 (원천 정규화)
- [x] L8 짧은 id 로 등록된 값도 떠나는 전이에서 제거된다 (유령 클레임 방지)
- [x] L9 클레임된 REQ 는 워처가 중복 스폰하지 않는다 (rework_claimed 가 짧은 id 도 인식)

결함 (2) 위임 에이전트 미등록
- [x] L2 s9 claim 한 줄로 위임 에이전트 진행이 직접 증거(direct)가 된다
- [x] L7 claim 은 agent_transcript_path 를 리스트로 누적한다 — 병렬 위임이 서로 덮어쓰지 않고, 같은 REQ 중복 등록도 없다

결함 (3) 죽은 워커 앰버
- [x] L3 죽은 워커는 앰버가 아니라 spawn_failed 로 구분되고 사유(워커 로그 마지막 줄)가 노출된다
- [x] L4 pid 없는 구 마커는 기존대로 앰버 (하위호환)
- [x] L5 pid 가 살아 있어도 claude 가 아니면 죽은 것으로 본다 (pid 재사용 방어)
- [x] L10 pid 기록이 기존 마커(쿨다운 카운터)를 덮어쓰지 않는다
- [x] L11 pid 가 정수가 아니면 조용히 건너뛴다 — 스폰을 깨뜨리지 않는다
- [x] L12 워커 스폰 경로가 하나로 유지되고 그 경로가 pid 를 기록한다 (새 스폰 경로 누락 방지)

미검증으로 남는 것: (2)의 **자동** 등록 경로(PostToolUse Agent 훅)는 이 머신 ~/.claude/settings.json 에 훅이 설치돼 있지 않아 실환경에서 한 번도 돌지 않았다 — 위 L2/L7 은 CLI 계약만 검증한다. 상세는 response 노트 참조.

### 2026-08-26T00:30:30+09:00 response (by sjpark1)

리드 후속 — 네가 남긴 "훅이 실환경에 없다"를 처리했다. 커밋 8201440.

확인한 실상은 보고보다 덜 나쁘고 다른 종류였다: settings.json 에는 이벤트마다 훅이 **두 개씩** 있었다 — 임시 워크트리 경로(/tmp/.../scratchpad/wt/bin/, 이미 삭제됨) 하나와 정상 경로(/home/sjpark1/section9/bin/) 하나. 즉 감사는 정상 경로로 계속 돌고 있었고, 워크트리 항목은 죽은 경로를 부르는 no-op 이라 증상이 없었다. "감사가 통째로 멎는다"는 아니었지만, 지워야 할 쓰레기인 건 맞다.

지워지지 않은 이유가 진짜 결함이었다: s9-install 이 자기 소유 훅을 **경로**(/section9/bin/)로만 식별해서, 다른 위치(워크트리)에서 설치된 자기 훅을 못 알아봤다. 재설치를 몇 번 해도 정리되지 않는 구조였다. 식별을 스크립트 이름(s9-audit)으로 바꿔 어느 경로에서 설치됐든 회수되게 했다.

그리고 PostToolUse(Agent) 훅은 실제로 없었다 — 지금 설치했다. settings.json 최종 상태: 훅 6개, 전부 정상 경로, 워크트리 항목 0, remoteControlAtStartup 은 원래 값(true) 그대로 두고 model 은 claude-opus-5[1m] 유지. 설치 전 백업을 떠뒀다.

다만 이 세션에는 아직 적용되지 않는다(훅은 세션 시작 시 로드된다) — 위임 자동 등록이 실제로 도는지는 다음 세션에서 확인해야 한다. 그 확인이 남았다는 뜻이지, 코드가 안 됐다는 뜻은 아니다.

## History
- 2026-08-25T22:10:52+09:00 created by sjpark1 (status: open)
- 2026-08-25T22:28:40+09:00 status: open -> in-progress (by sjpark1) — 위임: staff-engineer (sub) — bin/s9 live 판정 3결함. 지시는 나갔는데 상태 전이를 빠뜨려 open으로 남아 있었다(사용자 지적 지점)
- 2026-08-26T00:21:25+09:00 status: in-progress -> review (by sjpark1) — 진행 점멸등 결함 3건 코드 완료 — python3 tests/ live_signal 12/12. 확인 포인트(중요) ① ~/.claude/settings.json 에 PostToolUse(Agent) 훅이 없어 위임 자동 등록이 실제로는 돌지 않는다 — 정본 harness/claude/hooks.json 에는 있다. bin/s9-install 재실행이 필요한지 판단 바란다 ② 설치된 훅 5개가 전부 /tmp/.../scratchpad/wt/bin/ 임시 워크트리를 가리킨다 — 의도된 것인지, 지워지면 감사가 통째로 멎는다 ③ 이 세션(cb49b2cd) 바인딩 active_reqs 에 위임받은 095/080/086 이 없다 = 결함 (2)의 증상이 지금도 재현 중이다.
- 2026-08-26T12:43:28+09:00 status: review -> done (by sjpark1) — 대시보드 승인 [via dashboard]
