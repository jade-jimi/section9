---
id: REQ-20260829-015-62x6
type: request
title: 반려에 그림을 붙일 수 없다
summary: 판정 대화상자에 이미지 붙여넣기가 없어 반려 사유를 그림으로 못 댄다
goal: 판정 대화상자에서 그림을 붙여 반려할 수 있다
status: done
size: M
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: 01c62d83
sessions: ["b0cce452", "05195c75", "01c62d83"]
agents: ["sub:designer:a5467476", "sub:designer:a2d2ee75"]
contributions: [{"actor": "sub:designer:a5467476", "item": "`[2026-08-29 11:15:42 KST - designer]`", "started": "2026-08-29T11:45:39+09:00", "ended": "2026-08-29T11:45:39+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/b0cce452-7815-44d0-8bb7-ce1e7b28c6da/subagents/agent-a546747613cc0a1ef.jsonl"}, {"actor": "sub:designer:a2d2ee75", "item": "`[2026-08-29 15:52:41 KST - designer]`", "started": "2026-08-29T15:40:10+09:00", "ended": "2026-08-29T15:40:10+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/01c62d83-dc55-4f60-ac60-a5399e5ff656/subagents/agent-a2d2ee7529c00a07f.jsonl"}]
project: section9
relates: ["REQ-20260829-012-62x6"]
relates_why: {"REQ-20260829-012-62x6": "사용자가 판정 자리에 그림을 못 붙여 이어 말하기로 우회하다 012 의 결함을 만났다 — 하나가 없어서 다른 하나가 드러났다"}
tags: ["assets", "dashboard", "index", "attached", "png", "image", "terminal"]
created: 2026-08-29T11:03:07+09:00
status_since: 2026-08-29T16:35:02+09:00
updated: 2026-08-29T16:35:02+09:00
priority: 50
---

## Original

사용자: "반려할 때 이미지를 붙여넣을 수 있는 기능을 만들어달라고 했었지만, 어느샌가 유실된 것 같다. 그래서 이어 말하기 기능으로 이미지와 함께 반려하는데…"

확인한 것: 클립보드 이미지 붙여넣기 처리는 지금 터미널 입력창 한 곳에만 있다(web/index.html:8880 의 paste 리스너). 판정 대화상자는 s9dlg({kind:'prompt'}) 로 글자만 받는다(web/index.html:5046·5060·5076). 그래서 화면에서 반려하려면 사유를 글로만 쓰거나, 터미널로 건너가 '이어 말하기'로 그림과 함께 보내는 우회를 해야 한다 — 사용자는 그 우회를 쓰고 있고, 그 과정에서 REQ-20260829-012 의 결함을 만났다.

반려 사유가 화면 결함일 때 그림 한 장이 문단 열 줄보다 정확하다. 지금은 그 길이 판정 자리에 없다.

## Notes

### 2026-08-29T11:29:12+09:00 response (by nicehugepark)

**판정 창이 그림을 받는다 — 터미널로 건너갈 일이 없어졌다.**

붙여넣기(Ctrl+V)와 끌어놓기 둘 다 받는다. 받은 그림은 창 바닥에 칩 한 줄로 서고, 반려 사유와 함께 그 문서에 첨부로 남는다. 그림을 뺄 수도 있고, 올리는 중에는 확인 버튼이 눌리지 않는다(반쯤 올라간 그림으로 반려가 나가면 증거가 빈 채로 남는다).

**승인 쪽에도 같은 길을 줬다.** 그리고 상태 옮기기 창에도. 판정 창은 어차피 한 컴포넌트이고, 반려만 받게 하면 사람은 **어느 창이 그림을 받는지 외워야** 한다. 규칙은 하나다 — 글을 받는 창은 전부 그림도 받는다. 승인에도 "이렇게 나왔다"는 그림을 남길 이유가 있다.

**두 벌로 만들지 않았다.** 올리는 길은 터미널 입력창이 쓰는 그 엔드포인트 하나 그대로다.

**순서와 실패를 정했다**: 그림이 먼저 문서에 붙고, 그다음에 상태가 옮겨진다 — 반려는 무인 재작업 작업자를 깨우는데 그가 문서를 읽을 때 그림이 이미 있어야 한다. 그리고 **그림을 못 붙이면 상태를 옮기지 않는다.** 증거 없는 반려를 만들지 않는 것이 이 요청의 값이다.

**서버 몫으로 남긴 것 둘** (bin/s9 는 리드가 만지고 있어 손대지 않았다):
① `/api/status` 가 `atts` 를 받아 do_transition 안에서 함께 붙이면 화면이 두 번 두드릴 일이 없다. 지금은 `/api/note` 로 사유+그림을 남기고 `/api/status` 로 전이한다 — 두 번의 왕복이고, 그래서 앞의 것이 실패하면 뒤를 막는 처리가 필요했다.
② `/api/note` 는 노트 라벨을 `ask` 로 박아 둔다. 그래서 반려 근거가 문서에 **질문**으로 적힌다. 라벨을 인자로 받게 해 주시면 `response`(또는 `judge`)로 남길 수 있다.

**눈으로 확인한 것** (첨부 순서대로): ① ledger — 그림 두 장이 칩으로 선 반려 창(하나는 올라간 것, 하나는 올라가는 중). 올라가는 중이라 `반려하기` 가 눌리지 않고 바닥이 "그림을 올리는 중이에요" 라고 말한다 ② calm ③ terminal 밝은 톤 — 칩이 터미널의 어두운 자체 색을 빌리지 않고 이 창의 잉크를 쓴다(빌렸다면 밝은 테마에서 글자가 묻는다) ④ 아직 아무것도 안 붙인 창 — 첨부 줄이 아예 없고 빈 자리도 남지 않는다.

설명은 한 곳으로 모았다. 바닥 힌트에도 적었더니 힌트가 세 줄로 쌓였는데, 힌트는 읽는 글이 아니라 손이 훑는 라벨이라 셋이면 아무도 안 읽는다 — 설명 줄에 한 문장으로 두고 창 셋이 같은 상수를 쓴다.

그림이 붙은 창은 손이 있어야 생기는 화면이라 `?dlg=rejectatt` 로 세워 볼 수 있게 했다(`?dlg=` 가 낸 선례 그대로). 계약은 tests/test_judge_attach.py 에 잠갔다(11건).

[Image: assets/REQ-20260829-015-62x6/h1-ledger-att.png]
[Image: assets/REQ-20260829-015-62x6/h2-calm-att.png]
[Image: assets/REQ-20260829-015-62x6/h3-terminal-att.png]
[Image: assets/REQ-20260829-015-62x6/h4-empty.png]

### 2026-08-29T11:45:39+09:00 subagent (agent: sub:designer:a5467476, by nicehugepark)

`[2026-08-29 11:15:42 KST - designer]`

네 건 모두 마쳤다. 커밋하지 않았고 `git add -A`·`checkout`·`stash` 도 쓰지 않았다. 전체 시험 **1829건 통과**(skipped 1).

**① REQ-20260829-009-62x6 반려 — 손잡이가 카드 안에서 편다**

맞는 지적이었다. 카드 전체가 이미 그 문서로 가는 손잡이인데 같은 목적지를 하나 더 얹었으니 없느니만 못했다. 세 줄로 자르는 것은 두고, 손잡이를 **그 자리에서 펴는 손잡이**로 바꿨다. 호버 전개로 돌아가지 않았다 — 눌러서 열고 눌러서 접는다. 말씀하신 두 문제는 함께 풀었다: 펼친 글이 카드를 밀어 올리는 대신 **자기 상자 안에서 스크롤한다**(약 열네 줄 천장). 그래서 카드 한 장이 열을 차지하지 않고 승인·반려가 상자 바로 아래에 남는다. 여는 길은 열 머리의 `+ N개 더 보기` 가 쓰는 그 문법을 빌려서 15초 갱신에도 펼친 것이 접히지 않는다. 펼 것이 없으면 손잡이는 안 뜬다.

**② REQ-20260829-012-62x6 반려 — 문서를 갈아탈 때 목록을 다시 그린다**

리드가 짚은 원인이 맞았다. Docs 에 **이미 있는 상태**에서 다른 문서를 누르면 목록을 손대지 않고 오른쪽만 갈아 끼웠고, 못 박은 줄은 목록을 다시 그릴 때만 세워진다. 문서를 여는 길 하나가 이제 어느 탭에서 눌리든 둘을 함께 그린다. 사람이 누른 길과 15초 폴링 길은 갈라 뒀고(`docFresh`), 얼음 조건은 손대지 않았다. 이 결함은 **누른 순간에만** 나서 헤드리스로 볼 길이 없었기에 `?swap=<문서>` 를 냈다 — "012 를 연 채 014 로 갈아탄" 그 상황을 세워 캡처했다.

**③ REQ-20260829-015-62x6 신규 — 판정 창이 그림을 받는다**

붙여넣기와 끌어놓기 둘 다. 칩은 뺄 수 있고, 올리는 중에는 확인이 안 눌린다. **승인·상태 옮기기 창에도 같은 길을 줬다** — 반려만 받게 하면 어느 창이 그림을 받는지 외워야 한다. 올리는 길은 터미널이 쓰는 그 엔드포인트 하나 그대로다. 그림이 먼저 문서에 붙고 그다음에 상태가 옮겨진다(반려가 깨우는 작업자가 문서를 읽을 때 그림이 있어야 한다), **못 붙이면 상태를 옮기지 않는다**.

서버 몫 둘을 남긴다 — `bin/s9` 는 손대지 않았다:
- `/api/status` 가 `atts` 를 받아 `do_transition` 안에서 함께 붙이면 화면이 두 번 두드릴 일이 없다. 지금은 `/api/note` → `/api/status` 두 왕복이다.
- `/api/note` 가 노트 라벨을 `ask` 로 박아 둔다. 그래서 반려 근거가 문서에 **질문**으로 적힌다. 라벨을 인자로 받게 해 주시면 된다.

**④ REQ-20260829-011-62x6 반려 — 테두리를 걷어냈다**

1차에서 절반만 적용했다. 이 화면에서 실선 잉크 테두리는 **키보드 포커스**의 말이고(카드·목록 행·버튼·스크롤 상자가 전부 그것으로 "지금 여기 있습니다"를 말한다) 점선은 "여기에 놓으세요"다. 지속 상태가 그 재료를 빌리면 1차에서 고친 것과 똑같은 잘못이다 — 포커스를 옮겨도 안 풀리는 포커스. 집힘은 이제 낱말 하나로만 말하고, 테두리가 없어진 만큼 그 낱말을 한 급 크게·굵게·자간을 벌려 흐린 id 줄에서 혼자 튀게 했다. 키보드 포커스 링은 그대로 뒀다. 좁은 창(620px) 한 열 다섯 장에서 스캔해 찾아지는 것을 캡처로 확인했다.

**남의 시험 셋을 고쳤다** — 전부 뜻은 지키고 글자만 옮긴 것이다: `test_review_hover`(호버 전개 요구 → "접었으면 펼 수단을 짝으로"), `test_doc_title_anchor` 한 줄, `test_judge_dialog`(인자 개수 → "접두어를 화면이 짓지 않는다"), `test_paste_fold`(붙여넣기 핸들러가 둘이 되어 터미널 것을 집도록). `test_tab_active_sync`(리드가 방금 추가한 것)는 **건드리지 않고** 대신 내 주석을 함수 밖으로 빼서 그 계약의 창을 되살렸다 — 이유를 코드에 적어 뒀다.

새 시험 셋: `/home/sjpark1/section9/tests/test_judge_attach.py`(11) · `/home/sjpark1/section9/tests/test_doc_pin_release.py`(14) · `/home/sjpark1/section9/tests/test_pick_state_look.py`(11). 갱신: `/home/sjpark1/section9/tests/test_review_clamp.py`(16).

캡처는 네 REQ 문서에 각각 3~4장씩 붙였다(ledger·calm·terminal·glass, 밝은 톤과 어두운 톤, 좁은 창 포함). 못 본 것 둘도 노트에 적었다: 실제 마우스 hover 와 `닫기`·`접기` 실제 클릭은 헤드리스에 손이 없다.

곁가지로 두 번 적어 둔 것 하나 — "서버가 옛 코드로 돌고 있습니다" 띠가 뜬 동안 Docs 문서 판 높이가 그 띠만큼 짧게 잘린다(`sizeDocs` 가 띠보다 먼저 잰다). 이번 캡처들에서도 아래가 잘려 있다.

### 2026-08-29T15:03:51+09:00 tdd (by nicehugepark)

- [x] A1. 그림이 아닌 것도 붙는다 — .pdf·.docx·.mp4 를 붙여넣거나 끌어놓으면 칩으로 선다(지금은 조용히 버려진다)
- [x] A2. 표식이 갈린다 — 그림은 [Image:], 그 밖은 [File:]. 문서 뷰가 그 둘을 다르게 그리므로 영상에 [Image:] 를 붙이면 깨진 그림이 뜬다
- [x] A3. 그 규칙이 한 곳에 있다 — 지금 sendChat 안에만 인라인으로 있고 postStatus 는 [Image:] 를 박아 둔다. 두 벌이면 한 벌만 고쳐진다
- [x] A4. 크기 한도가 판정 창에도 있다 — 터미널은 30MB 에서 막는데 판정 창은 안 막아 서버 400 을 받고서야 안다. 같은 상수를 쓴다
- [x] A5. 말이 그림만 가리키지 않는다 — 힌트·칩·올리는 중·실패 문구가 '그림' 이면 문서·영상을 붙일 수 있다는 것을 아무도 모른다
- [x] A6. 헤드리스로 셋을 본다 — ?dlg=rejectatt 가 그림·문서·영상을 한 화면에 세운다
- [x] 회귀: 기존 판정 창 계약 11건(붙여넣기·끌어놓기·창 셋·업로드 길 하나·올리는 중 잠금·칩 빼기·순서·실패 시 중단·칩 재료)이 그대로다

### 2026-08-29T15:13:57+09:00 commit (by nicehugepark)

커밋 5187f83 — 워커 작업 자동 보존: REQ-20260829-015-62x6

### 2026-08-29T15:33:59+09:00 response (by nicehugepark)

### 서버 몫을 갚았다 — 표기·라벨·한 번의 왕복 (리드)

1차 designer 가 "서버 몫으로 남긴다"고 적어 둔 둘에, 이번 반려가 요구하는
하나를 더해 셋을 `bin/s9` 에서 끝냈다. 계약은 `tests/test_judge_attach_kinds.py`
11건에 잠갔다.

**① 무엇으로 적을지는 파일이 정한다 — `asset_mark(path)`.**
`[Image:]` 냐 `[File:]` 이냐는 파일의 성질이지 화면의 취향이 아니다. 판정 창은
붙인 것을 **무조건** `[Image:]` 로 적었고 문서 뷰어는 그 표기를 `<img>` 로
그린다 — 영상이나 CSV 를 붙이면 깨진 그림 한 칸이 남는다. 붙지 않은 것보다
나쁘다. 확장자 목록은 이미 있는 `TYPE_GROUPS` 하나에서 온다(두 벌로 만들면 한
벌만 고쳐진다). 모르는 확장자는 그림이라고 우기지 않고 `File` 이다.

**② 판정 근거가 '질문'으로 적히지 않는다.** `/api/note` 가 라벨을 `ask` 로
박아 두어, 판정 창에서 댄 반려 근거가 문서에 **질문**으로 남았다. 나중에 그
문서를 읽는 사람은 답해야 할 질문과 판정의 근거를 구별할 수 없다. 이제
`chat_append_doc(..., label=)` 을 받고 `/api/note` 가 그것을 넘긴다. 아무
낱말이나 되지는 않는다 — `NOTE_LABELS` 안의 것만(그 밖은 조용히 `ask` 로).
이어 말하기의 기본값은 `ask` 그대로다.

**③ 붙이기와 전이가 한 번에 간다.** `/api/status` 가 `atts` 를 받는다. 첨부가
**먼저** 붙고 그다음에 전이한다 — 반려는 무인 재작업 작업자를 깨우는데, 그가
문서를 읽을 때 근거가 이미 있어야 한다. 못 붙이면 전이도 일어나지 않는다.
기본 라벨은 `response`.

**실측(실서버 9909, 임시 문서로 확인 후 제거).** 한 번의 호출로 mp4 와 png 를
붙이고 상태를 옮겼다. 문서에 남은 것:

    ### … response (by nicehugepark)
    [File: assets/…/screen.mp4]
    [Image: assets/…/acct-live.png]

영상은 `File`, 그림은 `Image`, 라벨은 `response`. 셋 다 의도대로다.

**화면 몫으로 남는 것**: 판정 창의 붙여넣기·끌어놓기가 지금은 그림만 받는다 —
문서·영상도 받아야 하고, 칩이 그림이 아닌 것도 말할 수 있어야 하며,
`/api/status` 에 `atts` 를 실어 한 번에 보내면 된다(표기는 서버가 짓는다).
30MB 한도를 넘는 파일은 왜 못 붙는지 크기와 함께 말해야 한다.

### 2026-08-29T15:40:10+09:00 subagent (agent: sub:designer:a2d2ee75, by nicehugepark)

`[2026-08-29 15:52:41 KST - designer]`

019 끝났습니다. 그림이 실패하면 **스스로 다시 겁니다**. `web/index.html` 만 고쳤고 계약 시험 `tests/test_asset_retry.py` 를 새로 뒀습니다.

### 큐를 둘지는 재서 정했습니다

19장짜리 문서의 실제 첨부 주소로 직접 측정했습니다.

| 전략 | 결과 |
|---|---|
| 한꺼번에 19장 | 성공 9~11 / **실패 8~10** (증상 재현) |
| 동시 상한 4 / 6 / 8 | 여전히 **1~5장 실패** |
| 20ms 간격 | 19 / 0 |
| **재시도(120·320·800ms + 지터)** | **5회 시도 모두 19/19** |

**작은 큐는 두지 않았습니다.** 상한은 듣지 않습니다 — 벼랑은 "몇 개가 떠 있나"가 아니라 **"같은 순간에 몇 개가 도착하나"**라, 앞의 것이 끝나 자리가 나는 순간 여러 개가 다시 같은 순간에 출발합니다. 반대로 간격을 벌리면 전부 통과하고, 재시도의 백오프+지터가 정확히 그 간격을 만듭니다. 브라우저가 이미 동시 연결을 여섯으로 묶으므로 큐를 또 얹으면 같은 일을 두 번 하면서 첫 그림만 늦어집니다. 이 표를 `ATT_BACKOFF` 위 주석에 적어 뒀고, 시험이 그 기록의 존재와 "큐가 안 들어왔는지"를 붙잡습니다.

### 만든 것

**재시도** — `attImg` 을 md2html 안의 지역 함수에서 맨 위 함수 하나로 꺼냈습니다(재시도와 못 받은 자리가 거기 매여야 두 벌이 안 생깁니다). `error` 는 거품처럼 안 올라오므로 **문서에 잡는 단계로 한 번만** 답니다. 재시도 주소만 `&r=` 로 가릅니다 — 실패가 캐시에 물리면 다시 걸어도 같은 답이 오고, 성공한 응답은 리드가 붙이는 ETag/max-age 를 그대로 타야 합니다.

**깜빡임 없음** — 그림과 못 받았을 때의 자리를 한 덩이로 처음부터 함께 냅니다. 기다리는 동안은 조용히 `파일이름 · 그림을 다시 받는 중… (1/3)`.

**고치다 찾은 결함**: `hidden` 이 지고 있었습니다. `.attlink{display:inline-block}` 이 브라우저 기본 `[hidden]{display:none}` 보다 세서 **감췄다고 믿은 깨진 그림이 자리 위에 겹쳐 서 있었습니다.** 첫 캡처에서 눈으로 잡아 고쳤습니다.

**사람의 자리** — 점선 한 겹 안에 `파일이름 · 그림을 받지 못했습니다 — 한 번에 여러 장을 부르면 몇 장이 밀립니다. · [다시] · 새 탭에서 열기`. `다시` 는 링크 밖에 둡니다(안에 두면 링크가 먼저 열립니다). 내부 용어는 시험이 금지합니다.

### 검증 — 실브라우저에서 재시도가 실제로 돌았습니다

- 보통: `그림 19 · 뜸 19 (다시 걸어서 0) · 못 받음 0`
- 다른 실행: `뜸 16 (**다시 걸어서 4**)` — **브라우저도 벼랑을 만나고 재시도가 네 장을 건져 왔습니다.** 고치기 전이었다면 그 네 장이 깨진 칸이었습니다.

캡처: `/tmp/claude-1000/-home-sjpark1-section9/01c62d83-dc55-4f60-ac60-a5399e5ff656/scratchpad/shots19/` (`1-문서-그림19장-전부.png` · `2-다시받는중.png` · `3-못받은자리.png` · `4-못받은자리-다크.png` · `5-고치기전.png`) — 앞 넷은 문서에 그림으로 붙였습니다.

진단 주소: `?attstat`(몇 장 왔나) · `?attfail=N`(앞 N 장의 첫 요청만 빗나가게 — 재시도가 도는 것까지 보입니다) · `&attdead`(끝내 못 받은 자리) · `&attslow`(지나가는 "받는 중"을 멈춰 세워 찍기).

### 시험

새 계약 `asset_retry` 12건 OK. 곁 스위트 22종(`assets`·`bare_asset_path`·`asset_text_*`·`attachment_classify`·`note_attach`·`judge_attach`·`doc_table`·`article_screen`·`account_and_model`·`account_switch`·`session_restart`·`judge_dialog`·`term_conn`·`stdlib_only` 등) 전부 OK.

`test_bare_asset_path` 한 줄을 고쳤습니다 — 계약("표기와 맨 경로가 같은 함수로 그림을 낸다")은 그대로고 **찾는 모양**만 바뀐 것(지역 화살표 → 함수 선언)이라, 경위를 docstring 에 적었습니다.

### 앞선 작업(017) 전체 스위트 결과

전체 스위트가 끝났습니다: 실패 6 · 에러 20 — **전부 `test_judge_attach_kinds`(리드가 15:02 에 새로 쓴 Red TDD, `asset_mark()` 미구현)와 `test_live_worker_scope` setUpClass(포트로 붙는 시험, 환경성)** 입니다. 제 변경 영역에서는 회귀 0입니다.

### 남긴 것

헤드리스 캡처는 사람이 보는 것보다 덜 깨집니다(좁은 창에서 `loading="lazy"` 가 아래쪽을 안 부르고, 동시 연결이 여섯으로 묶입니다). "고치기 전"을 캡처로 재현하지는 못했고, 대신 파이썬 실측으로 벼랑을 재현하고(9~11/19) 고친 뒤 실브라우저에서 재시도가 네 장을 건져 오는 것을 배너 숫자로 확인했습니다.

**다음 것(REQ-20260829-015-62x6 반려 재작업 — 첨부가 그림만이 아니다) 넘겨 주시면 받겠습니다.**

바뀐 파일: `/home/sjpark1/section9/web/index.html` · `/home/sjpark1/section9/tests/test_asset_retry.py`(새로) · `/home/sjpark1/section9/tests/test_bare_asset_path.py`

### 2026-08-29T15:46:20+09:00 response (by nicehugepark)

**붙는 것은 그림만이 아니게 했다 (반려 반영).**

무엇이 잘못돼 있었나. 판정 창은 받을 때 그림이 아니면 **조용히 버렸다** — pdf 를 끌어다 놓으면 아무 일도 안 일어나고, 왜인지도 말해 주지 않았다. 그리고 어쩌다 그림 아닌 것이 붙어도, 전이가 붙이는 표식이 `[Image:]` 로 박혀 있었다 — 문서 뷰는 그 표식을 <img> 로 그리므로 mp4 는 **깨진 그림**으로 떴다.

고친 것 여섯.
① 판정 창은 갈래로 거르지 않는다. 되돌려보내는 조건은 크기 한도 하나뿐이다.
② 표식이 갈린다 — 그림은 `[Image:]`, 문서·영상은 `[File:]`. 서버(ingest_assets)는 예전부터 둘 다 알고 있었고 문서 뷰도 둘을 다르게 그린다. 몰랐던 것은 화면뿐이었다.
③ **규칙을 한 곳으로 모았다** — `ATT_IMAGE_EXT` · `attIsImage` · `attMark`. 앞서는 두 벌이었다: 터미널 보내기는 확장자를 보고 갈랐는데 판정 창은 표식을 박아 두었다. 그래서 한 벌만 고쳐져 있었다. 맨 경로 인식(BARE_ASSET_RE)도 이제 같은 목록을 읽는다.
④ 크기 한도(30MB)도 같은 상수를 쓴다. 판정 창에 없으면 다 읽어 올려 보내고 서버 400 을 받고서야 '너무 크다'를 안다 — 그동안 사람은 기다린다.
⑤ 말이 그림만 가리키지 않는다. 안내는 `그림·문서·영상을 붙여넣거나 끌어 놓으면 함께 붙습니다`, 올리는 중·실패 문구는 '파일'. 터미널 칩도 mp4 를 `[Image #1]` 이라 부르지 않는다.
⑥ 곁가지 하나: 터미널 **붙여넣기**만 그림을 골라 받고 있었다(끌어놓기·📎 는 이미 아무 파일이나 받았다). 같은 입구 셋의 규칙이 달랐던 것이라 함께 맞췄다.

**눈으로 확인한 것**: `?dlg=rejectatt` 가 이제 셋을 한 화면에 세운다 — png(올라간 것) · pdf(올라간 것) · mp4(올라가는 중). 마지막 것이 올라가는 중이라 `반려하기` 가 눌리지 않고 바닥이 '파일을 올리는 중이에요' 라고 말한다. 첨부 두 장은 ledger 밝은 톤과 terminal(모노) 톤이다.

**시험**: tests/test_judge_attach.py 19건(기존 계약 11건 + A1~A6). 그중 하나는 확장자 목록을 화면에서 꺼내 와 실제 파일명에 대 본다 — 있는가가 아니라 무엇을 어느 쪽으로 보내는가를 잰다(pdf·docx·mp4·mov·log.txt·shot.png.mp4 는 그림이 아니다). tests/test_bare_asset_path.py 8건도 같은 상수를 읽도록 맞췄다.

**전체 스위트**: 1837건 중 6건 실패가 남는데 전부 이 워크트리의 사정이다 — test_short_ref 4건은 이 자리에 카탈로그(state/index)가 없어 코퍼스가 0이고, test_instance_init 2건은 원격 푸시 대상이 없어서다. 첨부와 무관하고, 이 변경 전에도 같았다.

**서버 몫은 그대로 남는다**(bin/ 밖이라 손대지 않았다): ① `/api/status` 가 atts 를 받으면 왕복이 하나로 준다 ② `/api/note` 가 라벨을 `ask` 로 박아 두어 반려 근거가 '질문'으로 적힌다.

커밋: 워크트리 가지 wt/w-829-015-62x6-1787984160.

[Image: assets/REQ-20260829-015-62x6/a1-ledger-kinds.png]
[Image: assets/REQ-20260829-015-62x6/a2-terminal-dark.png]

### 2026-08-29T15:47:15+09:00 response (by nicehugepark)

**합칠 때 유의**(리드 몫): 이 워크트리가 도는 사이 다른 세션(01c62d83)의 designer 가 REQ-019 로 본 저장소의 `web/index.html` 을 고쳤다 — `attImg` 를 md2html 안에서 맨 위 함수로 꺼내고 재시도를 붙였다는 노트가 이 문서에 실려 있다(15:40 subagent 노트, 내용은 019 건이다).

내 변경은 `web/index.html` 의 다른 자리들이다: ① BARE_ASSET_RE 바로 위의 새 상수 블록과 그 정규식 한 줄 ② DLG_ATTACH_HINT ③ s9dlg 의 첨부 받는 자리 ④ postStatus ⑤ termAttRender·termUpload·sendChat·터미널 붙여넣기 ⑥ dlgPreview 의 rejectatt. md2html 의 치환 규칙 자체(`[Image: assets/…]` · `[File: …]` 줄)는 손대지 않았다.

겹칠 만한 곳은 첨부 렌더 근처 하나뿐이라 충돌은 크지 않을 것으로 본다. 다만 합친 뒤 `python3 tests/ bare_asset_path judge_attach asset_retry` 를 한 번 돌려야 한다 — tests/test_bare_asset_path.py 가 이제 화면에서 `ATT_IMAGE_EXT` 를 꺼내 쓰므로, 두 변경이 함께 들어가지 않으면 그 시험이 먼저 빨개진다.

### 2026-08-29T15:47:33+09:00 commit (by nicehugepark)

커밋 e2555fd — 워커 작업 자동 보존: REQ-20260829-015-62x6

### 2026-08-29T15:57:26+09:00 designer (by nicehugepark)

붙는 것이 **그림만이 아니게** 됐습니다. `web/index.html` 만 고쳤습니다(계약은 `tests/test_judge_attach.py` 30건).

### 1. 무엇이든 받습니다

붙여넣기·끌어놓기에서 종류로 거르던 자리를 없앴습니다. **목록에 담기기 전에 빠져나가는 길은 이제 하나뿐이고 그것은 크기입니다** — 시험이 그 하나만 남았는지를 셉니다. 실패한 CSV·로그·녹화 영상이 그대로 붙습니다. 붙여넣기는 `kind === "file"` 이면 무엇이든 받고, 글자 붙여넣기는 예전처럼 그냥 흘려 보냅니다.

### 2. 표기는 서버가 짓습니다 — 화면은 흉내 내지 않습니다

`postStatus` 가 `[Image: …]` 를 박아 넣던 자리를 걷어냈습니다. 이제 `/api/status` 에 `{id, to, note, atts, label:"response"}` 를 **한 번** 보내고 결과만 읽습니다. 왕복이 둘에서 하나로 줄었고, 그래서 "붙이는 것이 먼저"와 "못 붙이면 옮기지 않는다"를 화면이 손으로 엮을 일이 없어졌습니다 — 그 둘은 이제 서버가 한 몸으로 보장합니다. **시험이 그 계약을 서버 쪽에서도 붙잡습니다**(`chat_append_doc` 이 `do_transition` 보다 앞인지): 화면에서 뺐는데 서버에도 없으면 계약이 조용히 사라진 것을 아무도 모릅니다.

라벨은 `response` 입니다. 앞서는 반려 근거가 문서에 **질문**으로 적혀, 읽는 사람이 답할 질문과 판정 근거를 못 갈랐습니다.

**실서버로 확인했습니다.** 임시 문서를 만들어 화면이 보내는 그대로(png·csv·mp4 셋 + `label:"response"`) 한 번 보냈고, 문서에 이렇게 남았습니다 — `[Image: …png]` · `[File: …csv]` · `[File: …mp4]`, 구획은 `response`. 문서 화면에서 png 는 그림으로, csv·mp4 는 📎 칩으로 떴고 **깨진 그림 칸이 하나도 없습니다**(csv 는 본문 발췌까지 붙었습니다). 확인 뒤 그 문서는 `s9 rm` 으로 지웠습니다.

### 3. 고치다 찾은 결함 둘

**① "무엇이 그림인가"가 화면에 세 벌이었고 실제로 어긋나 있었습니다.** 터미널 채팅 글의 표기 · 맨 경로 렌더(`BARE_ASSET_RE`) · 칩의 앞머리 글자가 각자 목록을 들고 있었는데, 하나는 `heic` 를 모르고 다른 하나는 **서버가 모르는 `avif` 를 알고** 있었습니다. `IMAGE_EXTS` 한 상수로 모았고, 시험이 그 목록을 서버 `TYPE_GROUPS["image"]` 와 한 줄씩 맞대어 봅니다. (`avif` 는 서버가 그림으로 치지 않아 화면에서도 뺐습니다 — 둘 다 넣으려면 서버 목록을 늘리는 것이 먼저입니다. 이 저장소에서 `.avif` 를 만드는 자리는 없습니다.)

**② "파일을 올리지 못했어요"는 한 번도 보인 적이 없었습니다.** 1차가 실패한 자리에서 곧장 `hint` 에 써 넣었는데 바로 뒤따르는 `sync()` 가 덮어썼습니다. 이유를 상태(`attWarn`)로 들고 있게 고쳤습니다 — 다음에 무언가 성공적으로 붙을 때까지 남고, 평소 힌트를 덮지 않고 그 위에 얹힙니다(둘 다 참이니까).

### 4. 한도는 사람 말로

`녹화-재현.mp4 · 52MB — 한 번에 붙일 수 있는 것은 30MB 까지입니다.` **다 올린 뒤가 아니라 올리기 전에** 막습니다(시험이 그 순서를 붙잡습니다) — 30MB 를 다 올린 뒤 400 을 받으면 기다린 시간이 통째로 헛것이 됩니다. 한도 값과 그 문장은 판정 창과 터미널이 **같은 곳**에서 가져갑니다(전에는 터미널이 `30 * 1024 * 1024` 를 따로 적어 두었습니다). 설명 줄에도 한도를 미리 적었습니다: "파일은 붙여넣거나 끌어 놓으면 함께 붙습니다 — 그림·문서·영상 무엇이든, 한 개 30MB까지."

**한도를 올리자는 제안은 하지 않습니다.** 30MB 면 1080p 화면 녹화 30초쯤이고, 반려 근거로는 그 길이가 오히려 적당합니다(더 긴 영상은 보는 사람이 어디를 봐야 할지 모릅니다). 그리고 첨부는 vault 옆에 그대로 쌓여 git 저장소를 무겁게 합니다. 더 큰 것이 실제로 필요해지는 사례가 나오면 그때 근거와 함께 올리는 것이 낫습니다.

### 5. 칩이 종류를 말합니다

앞머리 글자 하나가 그림(🖼)과 그 밖(📎)을 가릅니다 — 이 저장소가 문서 뷰에서 이미 쓰는 그 둘이고, 그것이 **문서에서 무엇으로 보일지**를 그대로 말합니다. 종류를 낱말로 또 적지 않은 이유는 화면이 확장자 표를 한 벌 더 들지 않기 위해서입니다: 이름에 확장자가 이미 적혀 있고, 무엇으로 적힐지는 서버가 정합니다. 마우스를 올리면 "그림 — 문서에 그림으로 붙습니다"라고 낱말로도 말합니다. 터미널 첨부 칩도 같은 어휘로 맞췄습니다(전에는 `[Image #1]` 이라 붙인 것이 무엇이든 그림이라고 우겼습니다).

### 6. 영상에 재생을 붙이지 않았습니다 — 붙이지 않는 것이 답이라고 봤습니다

근거 넷입니다. ① `[File:]` 를 `<video>` 로 바꾸려면 **화면이 다시 확장자를 판정해야 합니다** — 방금 서버로 옮긴 판단을 되가져오는 것이고, 이번 반려의 뿌리가 그것입니다. ② 판정 화면에서 영상은 근거물이지 콘텐츠가 아닙니다. ③ 한 문서에 30MB짜리 플레이어가 여럿 서면 문서가 무거워집니다 — 바로 어제 그림 열아홉 장에서 연결이 잘렸습니다(REQ-20260829-019-62x6). ④ 📎 칩을 새 탭에서 열면 브라우저가 제 플레이어로 재생합니다: 클릭 한 번이 더 들 뿐 잃는 것이 없습니다. 필요해지면 그때 붙이는 것이 지금 붙였다 되돌리는 것보다 쌉니다.

### 시험

`python3 tests/ judge_attach` **30건** — 기존 11건의 뜻은 그대로 두고 셋을 다시 썼습니다(④⑤가 화면에서 서버로 옮겨 간 것이라, 옮겨 간 자리를 붙잡도록). 새로 잠근 것: 종류로 거르지 않는가 · 표기를 화면이 짓지 않는가 · 왕복이 하나인가 · 라벨이 `response` 인가 · 순서와 중단이 서버에 있는가 · 칩이 종류를 말하는가 · 말이 그림만 가리키지 않는가 · **그림 확장자 목록이 서버와 같은가** · 한도를 크기와 함께 말하는가 · 올리기 **전에** 막는가 · 못 붙인 이유가 다시 그려도 남는가 · 진단 창에 그림·문서·영상 셋이 서는가.

시험이 주석이 아니라 **알맹이**를 보도록 `_code()` 를 넣었습니다 — 옛 방식을 왜 버렸는지 적어 둔 주석이 그 옛 방식의 흔적으로 잡히면, 사람은 기록을 지워 시험을 통과시킵니다.

곁 스위트: `judge_attach_kinds`(리드의 서버 계약) · `bare_asset_path` · `asset_retry` · `assets` · `asset_text_ui` · `attachment_classify` · `note_attach` · `paste_fold` · `judge_dialog` · `review_gate` · `account_and_model` · `account_switch` · `session_restart` · `term_conn` · `stdlib_only` · `shot_hygiene` 전부 OK. `test_bare_asset_path` 의 상수 치환 목록에 `IMAGE_EXTS` 를 더했습니다(계약이 아니라 시험이 정규식을 조립하는 방법입니다 — 경위를 그 자리에 적었습니다).

### 진단 주소

`?dlg=rejectatt` — **그림·문서·영상 셋이 한 화면에** 섭니다(하나는 올라가는 중이라 `반려하기` 가 잠깁니다). `?dlg=reject&dlgbig=52` — 한도를 넘는 파일을 붙인 순간. 그림을 따로 그리지 않고 **진짜 `take()` 를 부릅니다**(크기에서 막히면 파일을 읽지도 않으므로 가짜 파일 하나면 됩니다).

### 검증

ledger·glass·terminal × 라이트/다크로 직접 봤고, 실서버 왕복 한 번의 결과를 문서 화면에서 눈으로 확인했습니다.

[Image: assets/REQ-20260829-015-62x6/1-판정창-그림문서영상.png]
[Image: assets/REQ-20260829-015-62x6/2-terminal-다크.png]
[Image: assets/REQ-20260829-015-62x6/3-glass.png]
[Image: assets/REQ-20260829-015-62x6/4-한도초과-사람말.png]
[Image: assets/REQ-20260829-015-62x6/5-실제로-보낸-결과.png]

## History
- 2026-08-29T11:03:07+09:00 created by nicehugepark (status: open)
- 2026-08-29T11:14:34+09:00 status: open -> in-progress (by nicehugepark) — 반려 대화상자에 그림을 붙일 길을 낸다 — 터미널 입력창이 이미 쓰는 붙이기 경로를 그대로 빌린다(두 벌 금지). designer 가 화면 몫만; 서버 손이 필요하면 어디가 필요한지 적어 둔다.
- 2026-08-29T11:53:10+09:00 status: in-progress -> review (by nicehugepark) — 반려할 때 그림을 붙일 수 있습니다. 승인·상태 옮기기 창에서도 됩니다. F5 후 review 카드의 반려를 누르고, 사유 칸에 이미지를 붙여넣거나 끌어다 놓아 보세요. 칩이 뜨고 반려 후 그 문서에 그림이 남으면 승인, 붙여넣기가 안 먹거나 그림이 안 남으면 반려입니다.
- 2026-08-29T15:01:01+09:00 status: review -> in-progress (by nicehugepark) — 반려: 반려에 첨부할 수 있는 이미지가, 그림 이미지일수도, 문서 파일일수도, 영상파일일수도 있다. [via dashboard]
- 2026-08-29T16:05:31+09:00 status: in-progress -> review (by nicehugepark) — 판정 창에 붙는 것이 그림만이 아니게 됐습니다. 카드의 "반려"를 누르고 파일을 붙여넣거나 끌어 놓아 보세요 — 그림은 🖼, 문서·영상은 📎로 서고 30MB를 넘으면 올리기 전에 크기와 함께 막습니다. 붙인 채 반려하면 문서에 그림은 그림으로, csv·mp4는 파일 칩으로 남습니다. F5 후 확인. 셋이 다 붙고 문서에서 깨진 칸 없이 보이면 승인, 하나라도 버려지거나 깨지면 반려.
- 2026-08-29T16:35:02+09:00 status: review -> done (by nicehugepark) — 승인 (메모 없음) [via dashboard]
