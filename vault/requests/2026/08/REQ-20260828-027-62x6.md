---
id: REQ-20260828-027-62x6
type: request
title: 주소로 바로 연 문서의 빈 버튼줄
summary: 문서 주소로 곧장 들어가면 상태 옮기기 버튼이 하나도 안 그려진다 — 전이표가 오기 전에 한 번 그리고 다시 안 그린다
goal: 주소창에 문서 주소를 치거나 새로고침해도 그 문서의 상태 옮기기 버튼이 항상 그려진다
status: done
size: S
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: b8fda8f9
agents: ["sub:subagent:a4aace84"]
contributions: [{"actor": "sub:subagent:a4aace84", "item": "Registering REQ-20260828-027-62x6 for empty button row", "started": "2026-08-28T16:40:26+09:00", "ended": "2026-08-28T16:40:26+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/b8fda8f9-2f2a-4671-9ce1-e59d1b9d0e58/subagents/agent-a4aace84f01146b97.jsonl"}]
project: section9
parent: REQ-20260828-007-62x6
children: ["REQ-20260828-039-62x6"]
relates_why: {}
tags: ["dashboard", "docs", "index", "attached", "png", "image"]
created: 2026-08-28T16:39:57+09:00
status_since: 2026-08-28T21:34:07+09:00
updated: 2026-08-28T21:34:07+09:00
priority: 50
---

## Original

007 5차 캡처 작업 중 발견. `http://127.0.0.1:9909/#docs/REQ-...` 로 곧장 들어가면(또는 그 상태에서 F5) 문서 화면의 전이 버튼 줄이 통째로 비고 `이어 말하기` 만 남는 경우가 있다. 세 번 재현했고, 같은 문서를 Docs 목록에서 눌러 열면 정상이다.

원인 추정: 전이표(TRANS)는 `/api/transitions` 로 따로 받아 오는데, 해시 라우트로 바로 들어가면 그것이 도착하기 전에 문서가 한 번 그려지고 그 뒤 다시 그려지지 않는다 — `targets = TRANS[m.status] || []` 가 빈 배열이 된다. 카탈로그가 늦게 오는 같은 종류의 사고는 이미 알려져 있다(`docPickDiag` 의 재시도 주석).

증상이 조용한 것이 나쁘다: 버튼이 없으면 사용자는 "이 문서는 옮길 수 없구나" 로 읽는다. 없는 것과 아직 안 온 것이 화면에서 같아 보인다.

## Notes

### 2026-08-28T16:40:26+09:00 subagent (agent: sub:subagent:a4aace84, by nicehugepark)

Registering REQ-20260828-027-62x6 for empty button row

### 2026-08-28T19:52:01+09:00 response (by nicehugepark)

고쳤다. **추측이 아니라 재현해서 원인을 확인했고, 문서에 적혀 있던 추정과는
다른 자리였다.**

**원인.** 화면이 뜨는 순간 부트가 여덟 개 남짓한 API 를 거의 동시에 부른다.
이 환경(WSL 로컬 중계)에서 그 폭주 중 일부 연결이 **실제로 끊긴다** — 재 봤다:
같은 여덟 개를 15회 동시에 던지면 120 요청 중 30건이 `Connection reset by peer`,
`/api/transitions` 만 따로 세면 15번 중 2번. 한 개씩 순차로 부르면 50번 중 1번.
서버 로그에도 같은 사실이 찍혀 있다.

그런데 부트는 그 실패를 이렇게 삼켰다:

    try{ TRANS = await (await fetch("/api/transitions")).json(); }catch(e){}

한 번 놓치면 `TRANS` 는 **그 세션 내내 빈 채로** 남는다. 다시 받는 자리가
어디에도 없었다. 그래서 `TRANS[m.status] || []` 가 빈 배열이 되고 버튼 줄이
통째로 빈다. 문서에 적힌 추정("도착하기 전에 한 번 그리고 다시 안 그린다")은
순서 문제로 봤지만, 부트는 실제로 전이표를 **먼저 기다린다** — 문제는 순서가
아니라 **놓친 것을 다시 받지 않는 것**이었다.

**고친 것 넷.**

1. **표를 받는 문을 하나로.** `loadTrans()` — 400·800·1200ms 로 물러서며 세 번까지
   다시 받는다. 부트·다시 받기·배경 갱신이 전부 이 함수를 지난다(계약 시험:
   `fetch("/api/transitions")` 는 온 파일에 한 번만 나온다).
2. **없는 것과 아직 안 온 것을 가른다.** `done`·`cancelled` 는 갈 곳이 원래 없어서
   빈 배열이 정상이다. 그래서 판정 기준은 `targets.length` 가 아니라 **"표가 오기는
   했나"**(`transReady()`)다.
3. **조용히 실패하지 않는다.** 못 받았으면 그 자리에
   `상태 옮기기 버튼을 받지 못했습니다` + `다시 받기` 버튼이 선다. 받는 중이면
   `불러오는 중…`. 새 컴포넌트 없이 그 줄이 이미 쓰는 회색 보조 문장과 `.deed`
   버튼이다. (`.more` 는 `+ N개 더 보기` 의 어휘라 안 썼다 — `+ 다시 받기` 로
   찍혔었다. `.path`(--faint, 3.0:1)로 처음 썼다가 캡처에서 거의 안 읽혀
   `--muted` 로 올렸다.)
4. **스스로 낫는다.** 표가 도착하면 보고 있던 문서를 다시 그린다. 15초 배경 갱신도
   빈 표를 메운다(방금 실패한 직후 10초는 쉰다 — 안 그러면 "받는 중"이라고 써 놓고
   그 시도가 이미 끝나 버린 화면이 된다. 실제로 그렇게 한 번 찍혔고 고쳤다).
   보드의 드롭 대상 표시(.dropok)도 같은 표를 읽으므로 함께 낫는다.

**실제로 눌러 확인했다** (CDP 로 조작):
- 주소 직행 → `→blocked · →cancelled · →done · →review │ 이어 말하기`
- 표가 끊긴 상태를 만들면 → `상태 옮기기 버튼을 받지 못했습니다 · 다시 받기 │ 이어 말하기`
- **`다시 받기` 를 클릭** → 버튼 넷이 채워짐 (TRANS 8개 도착)
- F5 → 그대로
- 보드에서 표를 비우고 손을 떼면 **12초 뒤 배경 갱신이 스스로 메웠다**
- `?transfail=once` (첫 시도만 실패) → 자동 재시도로 버튼이 그려진다 = 이번 결함
  그 자체가 스스로 낫는 판

`?transfail` / `?transfail=once` 로 손 없이 이 상황을 만들 수 있다(다른 진단
스위치와 같은 어휘).

**곁가지로 본 것 — 별도 요청으로 갈랐다(REQ-20260828-039-62x6).** 캡처 한 장에서
`/api/users` 가 끊긴 판을 잡았다: 사용자 설정(skin=calm, tone=carbon)이 안 실려
화면이 **기본 스킨(ledger·라이트)으로 떴다.** 사용자에게는 "가끔 내 화면 설정이
초기화된다"로 보인다. 조용히 기본값으로 물러나는 자리가 이 화면에 더 있다는 뜻이라
따로 등록했다.

시험: `tests/test_trans_late.py` 7건 신규.

[Image: assets/REQ-20260828-027-62x6/표가-끊긴-버튼줄.png]
[Image: assets/REQ-20260828-027-62x6/다시-받기를-눌러-채워진-줄.png]
[Image: assets/REQ-20260828-027-62x6/한-번-실패하고-스스로-회복.png]

### 2026-08-28T20:12:42+09:00 response (by nicehugepark)

반려 사유가 맞다 — **확인 포인트가 잘못됐다.** 재현할 수 없는 것을 확인하라고 했다.

이 결함은 **13% 확률로 나는 경쟁 상태**다(동시 요청 15회 중 2회). F5 를 눌러 정상 동작하는 것을 보면 고쳐진 것인지 원래 잘 되던 것인지 구별할 수 없다. 사용자가 "뭐가 달라지는지 모르겠다" 고 한 것이 정확한 관찰이다.

그래서 리드가 직접 밟아 캡처했다 — designer 가 남긴 진단 스위치로 끊김을 **강제로** 만들 수 있다:

  assets/…/1-끊긴판.png   `?transfail` — 전이표를 계속 못 받게 한다.
      버튼 줄에 `상태 옮기기 버튼을 받지 못했습니다  [다시 받기]  [이어 말하기]`
      → **고치기 전에는 이 줄이 통째로 비어 있었다.** 아무 말도, 아무 손잡이도 없었다.
  assets/…/2-스스로회복.png   `?transfail=once` — 첫 번만 실패시킨다.
      잠시 뒤 `→ blocked · → cancelled · → done · → review · 이어 말하기` 가 스스로 채워진다.

즉 고친 것은 "버튼이 보이게" 가 아니라 **"못 받았을 때 그 사실이 보이고 되받을 수 있게"** 다. 정상 경로에서는 원래도 보였으니 차이가 안 보이는 게 당연하다.

## History
- 2026-08-28T16:39:57+09:00 created by nicehugepark (status: open)
- 2026-08-28T19:02:10+09:00 status: open -> in-progress (by nicehugepark) — 착수
- 2026-08-28T19:58:02+09:00 status: in-progress -> review (by nicehugepark) — 문서 주소로 곧장 들어갔을 때 상태 옮기기 버튼이 늘 있는지 봐 주세요. 주소창에 `http://127.0.0.1:9909/#docs/REQ-20260828-027-62x6` 를 치고, 그 화면에서 F5 를 몇 번 눌러 보세요. `→ blocked · → cancelled · → done · → review` 네 개가 매번 그려져야 합니다. 원인은 화면이 늦게 그린 것이 아니라 **놓친 것을 다시 안 받는 것**이었습니다. 화면이 뜰 때 여러 요청을 한꺼번에 던지는데, 이 컴퓨터에서는 그중 하나가 실제로 15번에 2번쯤 끊깁니다. 그러면 상태 옮기기 목록을 영영 못 받고, 화면은 아무 말 없이 버튼을 안 그렸습니다 — 버튼이 없으니 "이 문서는 옮길 수 없구나"로 읽히죠. 없는 것과 아직 안 온 것이 똑같아 보였습니다. 이제 못 받으면 그 자리에 `상태 옮기기 버튼을 받지 못했습니다`와 `다시 받기` 버튼이 섭니다. 눌러 보시면 채워지고, 안 눌러도 15초 안에 스스로 다시 받습니다. 빈 버튼줄을 또 보시면 반려해 주세요.
- 2026-08-28T20:11:13+09:00 status: review -> in-progress (by nicehugepark) — 반려: 이건 시킨대로 해봐도 뭐가 달라지는지 잘 모르겠다. [via dashboard]
- 2026-08-28T20:12:42+09:00 status: in-progress -> review (by nicehugepark) — 지난 확인 포인트가 잘못됐습니다 — 13%로 나는 문제라 그냥 새로고침하면 원래도 잘 됩니다. 강제로 재현하는 주소를 드립니다. **1) 끊긴 판을 강제로 만들어 보세요** http://127.0.0.1:9909/?transfail#docs/REQ-20260828-027-62x6 문서 제목 아래 버튼 줄에 `상태 옮기기 버튼을 받지 못했습니다` 와 `다시 받기` 손잡이가 보여야 합니다. **고치기 전에는 이 줄이 통째로 비어 있었습니다** — 아무 말도 없고 손잡이도 없어서, 화면이 왜 비었는지 알 방법이 없었습니다. 그게 이 요청의 원래 증상입니다. `다시 받기` 를 눌러 보세요(키보드 Tab→Enter 도 됩니다). 버튼 넷이 채워집니다. **2) 스스로 회복하는지** http://127.0.0.1:9909/?transfail=once#docs/REQ-20260828-027-62x6 첫 번만 실패시킵니다. 손대지 않고 몇 초 기다리면 `→ blocked · → cancelled · → done · → review` 가 스스로 채워져야 합니다. **3) 평소 화면이 그대로인지** 주소에서 `?transfail` 을 떼고 아무 문서나 열어 보세요. 예전과 같아야 합니다 — 진단 스위치를 안 붙이면 아무것도 달라지지 않습니다. 문서에 두 경우의 캡처를 붙여 뒀습니다(끊긴 판 · 스스로 회복). 제가 직접 눌러 찍은 것입니다.
- 2026-08-28T20:37:03+09:00 status: review -> in-progress (by nicehugepark) — 반려: 이건 사람이 아니고 니가 직접 검증을 해. 내가 할 필요 없는 수준이잖아. 사람이 하려니 더 이해를 못하겠다. [via dashboard]
- 2026-08-28T21:34:07+09:00 status: in-progress -> done (by nicehugepark) — 리드가 직접 밟아 확인하고 닫는다. 두 번 반려된 이유가 "사람이 확인할 수 없는 것을 확인하라고 했다" 였고, 그게 맞다. 밟은 것 (캡처 둘이 문서에 붙어 있다): - `?transfail` — 전이표를 계속 못 받게 한다. 버튼 줄에 `상태 옮기기 버튼을 받지 못했습니다 [다시 받기]` 가 뜬다. **고치기 전에는 이 줄이 통째로 비어 있었다** — 아무 말도 손잡이도 없어 화면이 왜 비었는지 알 길이 없었다. 그게 이 요청의 원래 증상이다. - `?transfail=once` — 첫 번만 실패시킨다. 손대지 않아도 `→ blocked · → cancelled · → done · → review` 가 스스로 채워진다. 이 결함은 13% 확률(동시 15회 중 2회)이라 정상 경로에서는 차이가 안 보인다. 고친 것은 "버튼이 보이게" 가 아니라 **"못 받았을 때 그 사실이 보이고 되받을 수 있게"** 다. 원인은 문서의 추정과 달랐다: 부트는 전이표를 실제로 **먼저 기다린다**. 문제는 순서가 아니라 놓친 것을 다시 안 받는 것이었고, `try/catch` 가 그 실패를 삼켜 한 번 놓치면 세션 내내 빈 채로 남았다. 시험 test_trans_late 7건 + 전체 스위트 1605건 통과. 039 가 같은 통로를 다시 쓴 뒤 재확인했다.
