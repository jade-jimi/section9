---
id: REQ-20260827-002-62x6
type: request
title: 커밋 게이트가 옆문을 못 본다
summary: 동시 편집 차단이 무인 워커만 보고 서브에이전트는 못 본다
goal: 커밋 게이트가 무인 워커와 서브에이전트를 함께 본다
status: done
size: S
user: sjpark1
machine: DESKTOP-TEHV1KR
session: 8e60e4af
project: section9
parent: REQ-20260826-021-62x6
children: ["REQ-20260827-012-62x6"]
tags: ["workflow", "sync"]
created: 2026-08-27T00:02:12+09:00
status_since: 2026-08-27T08:25:32+09:00
updated: 2026-08-27T10:07:44+09:00
priority: 50
---

## Original

요구: 도는 주체를 한 곳에서 세고, 커밋 게이트가 워커+서브에이전트 합집합을 본다. 근거는 노트 참조.

## Notes

### 2026-08-27T00:02:21+09:00 response (by sjpark1)

REQ-20260826-021-62x6 로 넣은 커밋 게이트(C4)는 **무인 워커만 본다.** `s9 workers` 가 `state/auto_resume/*.json` 마커를 읽는데, 리드가 띄운 **서브에이전트**(designer·backend 등)는 그 마커를 만들지 않는다.

그런데 오늘 실제로 파일을 동시에 만진 조합은 워커만이 아니었다 — 리드가 `web/index.html` 을 커밋하려던 그 시각에 designer 서브에이전트가 같은 파일을 고치고 있었다. 이번엔 내가 손으로 그 파일을 스테이지에서 빼서 넘어갔지만, **그건 규율이지 장치가 아니다.** 021 이 없애려던 것이 정확히 "규율에 기대는 상태"다.

말하자면 021 은 문을 하나 잠그고 옆문을 열어 뒀다.

**요구**: 도는 주체를 한 곳에서 센다. 서브에이전트도 자기가 무엇을 잡고 있는지 남기고(에이전트 등록은 이미 `contributions`·`agents` 로 문서에 있다), `s9 workers` 가 워커와 서브에이전트를 함께 보여주며, 커밋 게이트가 그 합집합을 본다.

주의할 것: 넓히면 게이트가 자주 걸린다. 자주 걸리는 게이트는 우회가 습관이 되고, 습관이 되면 없는 것과 같다 — 그래서 "도는 주체가 있다"가 아니라 **"그 주체가 이 파일을 만졌다"** 로 좁혀야 실용적이다. 스냅샷(C2)이 이미 파일별 변경 이력을 갖고 있으니 거기서 근거를 얻을 수 있다.

### 2026-08-27T01:10:55+09:00 decision (by sjpark1)

**신호를 찾았다 — 다음 사람이 다시 파지 않도록 적어 둔다.**

서브에이전트는 리드 세션 안에서 도는 자식이라 별도 프로세스가 없다. 그래서 `s9 workers`(무인 워커용 pid 마커)로는 안 잡힌다. 그런데 **바인딩의 `agent_transcript_path` 가 살아 있는 신호였다.**

방금 실측(01:09):

    8e60e4af  a3523b3e19a5a2e81.output  age=4188s   ← designer(029·039), 끝남
    8e60e4af  a9029e7741910035c.output  age=3430s   ← backend(020), 끝남
    8e60e4af  afb99a15d71b7f033.output  age=480s    ← designer(018·008), 방금 끝남

**도는 동안에는 이 mtime 이 초 단위로 갱신된다.** 즉 "이 세션에 지금 붙어 있는 서브에이전트가 있는가"는 `agent_transcript_path` 중 mtime 이 신선한 것이 있는지로 답할 수 있다. 워커 마커와 합집합을 만들면 오늘 난 사고 다섯의 주체를 모두 덮는다.

**다만 주의 하나**: 그 목록에 옛 판본이 남긴 **글자 단위로 쪼개진 데이터**가 섞여 있다(REQ-20260827-011-62x6). 그 중 `"/"` 는 실제로 존재해서 mtime 을 갖는다 — 신선도 판정에 그대로 쓰면 **루트 디렉토리의 mtime 이 "서브에이전트가 살아 있다"로 읽힐 수 있다.** 011 을 먼저 닫거나, 최소한 길이 1짜리 원소를 걸러야 한다.

**그리고 넓힐 때의 함정**(원 노트에도 적었지만 다시): 자주 걸리는 게이트는 우회가 습관이 되고, 습관이 되면 없는 것과 같다. "도는 주체가 있다"가 아니라 **"그 주체가 이 파일을 만졌다"** 로 좁혀야 실용적이다. 스냅샷(REQ-20260826-021-62x6 C2)이 파일별 변경 이력을 이미 갖고 있으니 거기서 근거를 얻을 수 있다.

오늘 이 구멍을 두 번 손으로 메웠다 — 034 에서 워커가 쓰던 테스트 파일을 덮을 뻔했고(C1 이 잡았다), 018 진행 중 `web/index.html` 을 커밋할 뻔했다(내가 diff 를 눈으로 읽어 막았다). **두 번째는 장치가 아니라 규율이었고, 그게 이 문서가 존재하는 이유다.**

### 2026-08-27T01:34:40+09:00 response (by sjpark1)

막았다. 옆문도 같은 문이 됐다.

**신호**: 바인딩의 `agent_transcript_path` mtime. 서브에이전트는 리드 세션 안에서 도는 자식이라 프로세스가 따로 없어 pid 로는 못 잡는데, 그 기록 파일은 **일하는 동안 초 단위로 갱신된다.** 실측으로 확인한 그대로다.

**고친 자리는 한 곳뿐이다.** `s9 workers` 가 무인 워커와 서브에이전트를 함께 낸다. 커밋 훅(`bin/s9-guard`)은 판정을 스스로 하지 않고 `s9 workers` 에게 묻고 있었으므로 — **주체 종류가 늘었는데 훅은 한 줄도 안 바뀌었다.** 판정을 두 벌로 두지 않은 값이 여기서 돌아왔다.

메시지만 넓혔다: "무인 작업자가 도는 중에" → "다른 작업자가 붙어 있는 중에". 종류를 말하던 문장이 존재를 말하게 됐다.

**게이트가 과하게 걸리지 않는 이유**: 판정은 여전히 **곱**이다 — (담으려는 파일이 90초 안에 바뀜) × (다른 작업자가 붙어 있음). 서브에이전트가 놀고 있으면 파일이 안 바뀌므로 안 걸린다. 지금 designer 가 대기 상태로 살아 있는데 커밋이 막히지 않는 것이 그 확인이다. 언제나 걸리는 게이트는 우회가 습관이 되고, 습관이 되면 없는 것과 같다.

**REQ-20260827-011-62x6 을 먼저 닫은 이유가 여기 있다.** 그 목록에 글자 단위로 쪼개진 옛 데이터가 남아 있었고 그중 `"/"` 는 실제로 존재하는 디렉토리다. 정규화 없이 이 판정을 넣었으면 **루트의 mtime 이 "에이전트가 살아 있다"로 읽혀 게이트가 영원히 걸렸을 것**이다. 테스트 L3 이 그 자리를 지킨다.

**남은 한 겹은 정직하게 적어 둔다.** 이 게이트는 "누가 붙어 있다"까지만 알고 **"그 사람이 이 파일을 만졌다"는 모르다.** 귀속 신호가 없다 — 파일 mtime 은 누가 바꿨는지 말해 주지 않는다. 그래서 곱의 한쪽은 여전히 근사다. 실용적으로는 충분하다고 판단했다(오늘 사고 다섯이 전부 이 곱을 만족했다). 더 좁히려면 스냅샷(REQ-20260826-021-62x6 C2)에 기록 주체를 함께 남겨야 하는데, 그건 이 요청의 범위가 아니고 값어치도 아직 불확실하다.

**테스트**: `tests/test_commit_gate.py` G8(훅이 s9 에게 묻고, 메시지가 종류가 아니라 존재를 말한다) · `tests/test_binding_shape.py` L1~L4(신선한 기록은 살아있음 · 오래된 것은 아님 · 쪼개진 쓰레기가 살아있음을 지어내지 않음 · 끝난 세션 제외).

### 2026-08-27T01:34:40+09:00 tdd (by sjpark1)

- [x] G8. 훅이 판정을 스스로 하지 않고 s9 에게 묻는다 · 메시지가 종류가 아니라 존재를 말한다
- [x] L1. 신선한 기록 = 일하는 중
- [x] L2. 오래된 기록은 아니다 (끝난 에이전트가 영원히 게이트를 잡으면 안 된다)
- [x] L3. 쪼개진 옛 데이터가 '살아 있음'을 지어내지 않는다 (011 이 막힘이었던 이유)
- [x] L4. 끝난 세션 제외
실동작 확인: 대기 중인 designer 서브에이전트가 `s9 workers` 에 잡히고, 그럼에도 커밋은 막히지 않는다(파일이 안 바뀌었으므로).

## History
- 2026-08-27T00:02:12+09:00 created by sjpark1 (status: open)
- 2026-08-27T01:32:48+09:00 status: open -> in-progress (by sjpark1) — 리드 착수 — 011 로 막힘 풀림
- 2026-08-27T01:37:15+09:00 status: in-progress -> review (by sjpark1) — 커밋이 남의 작업을 삼키는 것을 막는 장치가 이제 한쪽 눈만 뜨고 있지 않다. 어제 만든 그 장치는 "무인 작업자"만 볼 수 있었는데, 정작 오늘 파일을 동시에 만진 조합은 작업자가 아니라 내가 띄운 도우미(designer)와 나였다 — 그때는 내가 직접 눈으로 확인해서 막았다. 그건 습관이지 장치가 아니다. 확인: 터미널에서 `bin/s9 workers` 를 쳐 보라. 지금 붙어 있는 것이 무인 작업자든 도우미든 함께 나와야 한다. 그리고 지금처럼 도우미가 대기만 하고 있을 때는 커밋이 막히지 않아야 한다 — 파일을 안 건드리고 있으니까. 언제나 막히는 장치는 사람이 곧 꺼 버린다.
- 2026-08-27T08:25:32+09:00 status: review -> done (by sjpark1) — 대시보드 승인 [via dashboard]
