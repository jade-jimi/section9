---
id: REQ-20260827-008-62x6
type: request
title: 문서 뷰어에 표가 없다
summary: 문서 본문 렌더러가 마크다운 표를 파이프째 보여준다 — 터미널만 고쳐졌다
goal: 문서 본문의 마크다운 표가 표로 그려지고, 렌더러가 두 벌로 갈리지 않는다
status: done
user: sjpark1
machine: DESKTOP-TEHV1KR
session: 8e60e4af
agents: ["sub:subagent:a764361e", "sub:subagent:a4630a1d", "sub:designer:afb99a15"]
contributions: [{"actor": "sub:subagent:a764361e", "item": "Appending response note to REQ-20260827-008-62x6", "started": "2026-08-27T00:59:45+09:00", "ended": "2026-08-27T00:59:45+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/8e60e4af-6e99-4b92-857b-fe3e651a913b/subagents/agent-a764361e525f30c05.jsonl"}, {"actor": "sub:subagent:a4630a1d", "item": "Transitioning REQ-20260827-008-62x6 to review", "started": "2026-08-27T01:00:33+09:00", "ended": "2026-08-27T01:00:33+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/8e60e4af-6e99-4b92-857b-fe3e651a913b/subagents/agent-a4630a1d9c6ffda2b.jsonl"}, {"actor": "sub:designer:afb99a15", "item": "`[2026-08-27 00:56:12 KST - designer]`", "started": "2026-08-27T01:00:57+09:00", "ended": "2026-08-27T01:00:57+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/8e60e4af-6e99-4b92-857b-fe3e651a913b/subagents/agent-afb99a15d71b7f033.jsonl"}, {"actor": "sub:designer:afb99a15", "item": "`[2026-08-27 01:01:12 KST - designer]`", "started": "2026-08-27T01:01:20+09:00", "ended": "2026-08-27T01:01:20+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/8e60e4af-6e99-4b92-857b-fe3e651a913b/subagents/agent-afb99a15d71b7f033.jsonl"}, {"actor": "sub:designer:afb99a15", "item": "`[2026-08-27 01:28:05 KST - designer]`", "started": "2026-08-27T01:28:13+09:00", "ended": "2026-08-27T01:28:13+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/8e60e4af-6e99-4b92-857b-fe3e651a913b/subagents/agent-afb99a15d71b7f033.jsonl"}]
project: section9
derived_from: REQ-20260826-029-62x6
relates: ["REQ-20260826-001-62x6"]
tags: ["auto-audit", "docs"]
created: 2026-08-27T00:36:32+09:00
status_since: 2026-08-27T08:25:24+09:00
updated: 2026-08-27T08:25:24+09:00
priority: 50
---

## Original

[선행 대화 — 직전 미문서화 메시지]
- (00:28) REQ-20260826-038 이건 되고 있는게 맞는건가...

QST-20260827-001 문서 본문에 표가 깨져서 나온다. 모든 문서에서 md 표가 깨지지 않도록 md 문법 잘 챙겨줘.

## Notes

### 2026-08-27T00:37:33+09:00 response (by sjpark1)

확인했다. **문서 뷰어의 마크다운 렌더러에 표가 아예 없다.**

`web/index.html` 의 문서 본문 렌더러는 줄 단위로 코드펜스(```)·헤딩(#)·목록(-,*)·문단만 가른다. 표를 나타내는 갈래가 없으므로 `| a | b |` 줄은 그대로 문단에 떨어져 파이프째 나온다.

**이게 방금 고친 것과 같은 결함의 다른 자리다.** REQ-20260826-029-62x6 에서 designer 가 터미널 렌더러(`ccText`)에 같은 구멍을 막았다 — 인라인 규칙이 줄 안쪽만 다뤄 표·헤딩이 통째로 없던 것. 지금 사용자가 본 것은 **문서 뷰어 쪽의 같은 구멍**이다. 렌더러가 두 벌이라 한쪽만 고쳐졌다.

**그래서 고치는 방식이 중요하다: 표 파서를 두 번째로 쓰지 마라.** designer 가 029 에서 만든 블록 처리(`ccBlocks`)를 **문서 뷰어도 같이 부르게** 해야 한다. 두 벌로 두면 오늘 이 프로젝트에서 반복된 그 실패를 또 밟는다 — 한 이름이 두 뜻을 갖거나, 같은 규칙이 두 자리에 갈려 한쪽만 고쳐지는 것. 오늘만 세 번 났다(data-goto 의 두 뜻 · 질문 판정자의 두 입구 · 그리고 이 렌더러 두 벌).

주의할 이음매 둘:
- 문서 뷰어는 **첨부 칩·문서 id 링크·위키링크** 를 placeholder 로 잡아 두고 나중에 복원한다. 블록 처리가 그 placeholder 를 깨면 첨부와 링크가 죽는다. 029 가 "인라인 뒤·복원 앞"에 넣은 이유가 그것이니 같은 순서를 지켜라.
- 표 안에 코드(`` `...` ``)·문서 id 가 들어오는 일이 흔하다(이 문서 노트들이 그렇다). 셀 안 강조가 살아야 한다.

발견 경로도 적어 둔다: 이 결함은 **채팅 질문이 QST 로 기록되기 시작한 덕분에** 눈에 띄었다(REQ-20260826-033-62x6 + 서버 재시작). 답이 문서에 남으니 그 문서를 열었고, 열었더니 표가 깨져 있었다. 기록이 안 남던 동안에는 이 결함도 보이지 않았다.

### 2026-08-27T00:47:26+09:00 decision (by sjpark1)

정정: 위 노트의 "designer 가 039 재작업으로 잡고 있다"는 낡았다. 039 는 00:37 승인됐고, 지금 web/index.html 을 잡고 있는 것은 REQ-20260826-018-62x6(감시 기록 화면)이다. 기다리는 대상을 요청 이름으로 적은 것이 실수다 — 실제로 기다리는 것은 파일이므로 파일을 적어야 낡지 않는다.

### 2026-08-27T00:59:34+09:00 response (by sjpark1)

**표 파서를 한 벌로 모으고, 문서 뷰어가 그 하나를 부르게 했다.** `web/index.html` 만 고쳤다.

### 결함의 진짜 이름은 "렌더러가 두 벌"이다

문서 뷰어의 `md2html` 은 줄 단위로 코드펜스·헤딩·목록·문단만 갈랐다. 표 갈래가 아예 없어 `| a | b |` 는 문단에 떨어져 파이프째 화면에 나왔다.

REQ-20260826-029-62x6 에서 터미널 뷰(`ccText`→`ccBlocks`)의 같은 구멍을 막았는데, **같은 규칙이 두 자리에 갈려 있어 한쪽만 고쳐졌다.** 그래서 두 번째 파서를 쓰지 않았다.

**표를 읽는 규칙 전체를 `mdTable(lines, i, cls, cell)` 하나로 뽑아** 두 렌더러가 같이 부른다. 두 화면의 차이는 인자 둘로만 남는다.

| | 터미널 뷰 (`ccBlocks`) | 문서 뷰어 (`md2html`) |
|---|---|---|
| `cls` | `cctbl` — tone 무관 상시 다크(`--cc-*`) | `mdtbl` — tone 따라가는 문서 토큰 |
| `cell` | 없음 (인라인 규칙이 이미 끝난 HTML) | `inline` — 원문이라 여기서 건다 |

**스타일까지 공유하지는 않았다.** 문서 뷰어가 `--cc-*` 를 빌려 쓰면 그 토큰이 없어 어두운 fallback 으로 떨어지고, 밝은 tone 에서 글자가 배경에 묻힌다. 029 가 터미널에 fallback 을 붙인 그 이유가 여기서는 반대로 작동한다.

### 이음매 셋

1. **첨부·문서 링크 자리표시자** — 문서 뷰어는 첨부 칩·문서 id·위키링크를 자리표시자로 잡아 두고 `inline()` 끝에서 되돌린다. 그래서 셀을 통째로 `inline()` 에 넘겼다: 잡고 되돌리는 일이 **한 셀 안에서 닫히므로** 블록 처리가 자리표시자를 밟을 자리가 없다. (터미널 쪽은 029 가 잡은 "인라인 뒤·복원 앞" 순서를 그대로 뒀다 — 그쪽은 코드펜스가 자리표시자라 그 순서라야 파이프 오탐이 안 난다.)
2. **셀 안 강조** — 표 안에 인라인 코드·문서 id 가 들어오는 일이 흔하다. 셀을 `inline()` 에 통과시켜 살렸다. 캡처로 센 결과 018 노트의 표에서 칸 39개 중 강조 29개가 살아 있다.
3. **코드펜스 안의 파이프** — 문서 뷰어는 펜스를 줄 단위로 먼저 가르므로 코드 갈래가 표 갈래보다 앞에 있어야 한다. 그 순서를 계약으로 박았다.

### 039 에서 배운 것을 여기에도

기존 계약이 이 결함을 통과시킨 이유는 "마크업이 있는가"만 봤기 때문이다. 그래서 계약을 두 층으로 세웠다.

- **단위 계약 15개** (`tests/test_doc_table.py`) — 첫 계약이 "표가 보이는가"가 아니라 **"규칙이 한 곳에만 있는가"** 다: 구분행 판별·칸 나누기가 `mdTable` 밖에서 쓰이면 실패, 마크다운 표 마크업을 짓는 자리가 둘이면 실패, 두 렌더러가 그 하나를 부르지 않으면 실패. 두 번째 파서를 쓰는 순간 빨간불이 켜진다.
- **실브라우저 확인** (`web/verify-doctable.html`) — 문서를 실제로 열어 ① 표가 `<table>` 로 떴는가 ② **본문에 파이프 줄이 남아 있지 않은가** ③ 셀 안 강조가 살아 있는가 를 센다. ②가 사용자가 본 그 증상 자체다.

`tests/test_terminal_table.py`(029)의 헬퍼도 함께 고쳤다 — 계약의 **뜻은 그대로** 두고, 규칙이 사는 자리(`ccBlocks` → `ccBlocks`+`mdTable`)만 따라가게 했다. 그러지 않으면 리팩터가 남의 계약을 조용히 깨고 지나간다.

### 검증

- `python3 tests/ doc_table` → **15개 통과** (TDD: 14개 빨강 확인 후 구현)
- `python3 tests/ terminal_table` → 14개 통과 (리팩터 직후 4개가 깨졌고, 보는 자리를 따라가게 고쳐 되살렸다)
- 전체 스위트 `python3 tests/` → **732개 통과** (실패 0, skip 1)
- 실브라우저 — 문서 4건(`REQ-20260826-032-62x6` · `REQ-20260826-018-62x6` · `REQ-20260826-039-62x6` · `QST-20260827-001-62x6`)에서 **결함 0건**, 본문에 남은 파이프 줄 **0개**:
  - `state/shots/008-verify.png` — 하니스 집계 + 실화면(다크)
  - `state/shots/008-light.png` — paper tone. 3열 표와 칸 안 코드가 전부 살아 있다
- 색면 0 · 얼룩말 0 · 색 하드코딩 0(전부 토큰) · 넓은 표는 본문을 밀지 않고 자기 안에서 가로 스크롤

### 남긴 것

- 터미널 뷰의 표는 **계약 14개로만** 확인했다. 실화면 캡처를 안 한 이유는 `s9 shot` 이 `#terminal` 에서 무한 대기하기 때문이다(REQ-20260827-001-62x6, 미수정). 마크업과 호출 인자가 리팩터 전과 같음은 계약이 보증한다.
- 하니스 `web/verify-doctable.html` 를 남겼다(`verify-*.html` 선례).

### 2026-08-27T00:59:45+09:00 subagent (agent: sub:subagent:a764361e, by sjpark1)

Appending response note to REQ-20260827-008-62x6

### 2026-08-27T01:00:33+09:00 subagent (agent: sub:subagent:a4630a1d, by sjpark1)

Transitioning REQ-20260827-008-62x6 to review

### 2026-08-27T01:00:57+09:00 subagent (agent: sub:designer:afb99a15, by sjpark1)

`[2026-08-27 00:56:12 KST - designer]`

세 건을 끝냈다. 전부 `review` 로 올렸고 커밋은 안 했다.

### REQ-20260826-039-62x6 「범례 무반응」 — 승인됨

리드의 추정 셋(덮개·render 예외·버튼도 죽음)이 **전부 틀렸다**. iframe + 실제 `MouseEvent` 하니스로 재현한 결과: 클릭은 hit-test를 통과했고, 핸들러도 돌았고, JS 오류 0건이었다.

진짜 원인은 **범례에 취소선이 그어진 항목이 셋인데 안내가 어느 것인지 짚지 않은 것**이다. 사용자는 한글 "질문"을 영문 `QUESTION`으로 스스로 옮겨야 했고, 아닌 것을 누르면 그릴 노드가 여전히 0이라 같은 문장이 다시 그려졌다 — 글자도 그림도 그대로. 게다가 주 문장의 "위쪽"이 시선을 범례로 보내 바로 아래 버튼을 건너뛰게 만들었다.

고친 것 넷: 주 문장에서 위치 지시 제거 · 범례 라벨 축자 표기 · 눌러야 할 항목에 잉크 점선 지목 · 헛클릭 인정 줄. 계약 8개 추가.

### REQ-20260826-018-62x6 「감시 기록 화면 노출」

자리는 **코드 낡음 알림과 같은 헤더 3행**을 골랐다(대안 비교는 노트에). 어휘는 주체로 갈랐다 — 사람이 하는 **재기동** vs 감시자가 하는 **자동 복구**. 핵심은 나타나고 걷히는 규칙이다: 사건이 없으면 흔적 0, 복구 6시간·복구 중단 24시간 뒤 스스로 걷힘.

**API는 명세만 정하고 넘겼다** (`bin/s9` 미수정 — decision 노트에 `GET /api/serveguard` 전체 스키마). 지금은 404라 화면이 조용하다. 상태 확인은 `?guard=recovered|attention|none`.

원문 요구였던 재연결 표시 걷힘은 **걷힌다** — 정상 응답이 `textContent`·`className`·`title`·대상 줄을 전부 되쓴다. 다만 코드 경로 추적이지 실단절 재현은 아니다.

### REQ-20260827-008-62x6 「문서 뷰어에 표가 없다」

표 파서를 두 번째로 쓰지 않았다. 규칙 전체를 `mdTable(lines, i, cls, cell)` **하나로 뽑아** 두 렌더러가 같이 부른다. 차이는 인자 둘(`cls`=스타일 계열, `cell`=셀 렌더러)뿐이다. 스타일은 나눴다 — 문서 뷰어가 `--cc-*`를 빌리면 어두운 fallback으로 떨어져 밝은 tone에서 묻힌다.

리팩터 직후 **029의 계약 4건이 깨졌다.** 뜻은 그대로 두고 보는 자리만 따라가게 고쳐 되살렸다.

### 리드가 알아야 할 것

- **`web/index.html` 에 세 건이 겹쳐 있다** (+466줄). 영역은 완전히 분리돼 있어 hunk 단위로 나눌 수 있다: 039=`.gtype.want`/`.gempty`/`graphEmptyState`/클릭 핸들러, 018=`.gw-*`/`#guard` 마크업/`checkGuard`~`gwDemo`, 008=`mdTable`/`.mdtbl`/`md2html` 루프.
- `bin/s9`·`tests/test_shot_blank.py` 변경은 **내 것이 아니다**(다른 에이전트).
- 하니스 4개를 남겼다: `web/verify-gempty-click.html`·`verify-gempty-tones.html`·`verify-guard.html`·`verify-doctable.html`. 마지막 것은 문서를 열어 "본문에 남은 파이프 줄"을 세는 회귀 도구다.
- REQ-20260827-007-62x6(범례 키보드 접근)은 등록만 하고 손대지 않았다.
- 전체 스위트 **732건 통과**(실패 0, skip 1).

주요 파일: `/home/sjpark1/section9/web/index.html` · `/home/sjpark1/section9/tests/test_doc_table.py` · `/home/sjpark1/section9/tests/test_serve_guard_ui.py` · `/home/sjpark1/section9/tests/test_graph_empty.py` · `/home/sjpark1/section9/state/shots/` (039-diag-k·039-fix-k·039-tone-a/b/c · 018-states·018-dark·018-tone-paper·018-tone-rest · 008-verify·008-light)

### 2026-08-27T01:01:20+09:00 subagent (agent: sub:designer:afb99a15, by sjpark1)

`[2026-08-27 01:01:12 KST - designer]`

배경 작업 알림일 뿐 새 지시는 없다. 해당 대기 작업이 기다리던 값은 이미 읽었다 — 전체 스위트 **732건 통과**(실패 0, skip 1)이고, 그 수치는 REQ-20260827-008-62x6 의 response 노트에 이미 반영돼 있다.

세 건(039 승인 / 018 review / 008 review) 모두 마무리된 상태 그대로다. 추가로 진행한 작업은 없다.

### 2026-08-27T01:28:13+09:00 subagent (agent: sub:designer:afb99a15, by sjpark1)

`[2026-08-27 01:28:05 KST - designer]`

마지막 남은 중복 대기 루프가 정리된 알림이다. 작업 상태는 그대로다.

- REQ-20260826-039-62x6 — 승인됨
- REQ-20260826-018-62x6 — review (서버측 `/api/serveguard` 는 명세만 넘김)
- REQ-20260827-008-62x6 — review
- REQ-20260827-007-62x6 — open (등록만, 미착수)

`web/index.html` 에 세 건이 겹쳐 있으니 커밋 시 hunk 를 나누면 된다. 리드의 다음 지시를 기다린다.

## History
- 2026-08-27T00:36:32+09:00 created by sjpark1 (status: open)
- 2026-08-27T00:49:16+09:00 status: open -> in-progress (by sjpark1) — designer 착수 — 표 파서를 한 곳(mdTable)으로 모아 터미널 뷰와 문서 뷰어가 같이 부르게 한다
- 2026-08-27T01:00:09+09:00 status: in-progress -> review (by sjpark1) — 문서 본문의 표가 이제 표로 보입니다. 파이프(|)가 그대로 흐르던 것을 고쳤습니다. 원인: 표를 읽는 규칙이 이 대시보드에 두 벌 있었습니다. 어제 터미널 화면 쪽 한 벌을 고쳤는데, 문서 본문은 다른 한 벌을 쓰고 있어서 그대로 남아 있었습니다. 문서 본문 쪽에는 표를 알아보는 규칙이 아예 없었습니다. 그래서 규칙을 새로 하나 더 만들지 않았습니다. 두 벌을 한 벌로 합쳐서 터미널 화면과 문서 본문이 같은 규칙을 씁니다. 두 벌로 두면 다음에 또 한쪽만 고쳐집니다 — 어제 오늘 이 프로젝트에서 세 번 난 사고가 전부 그 모양이었습니다. 표 안에 들어 있던 것들도 그대로 살렸습니다. 칸 안의 코드 글자, 다른 문서로 가는 링크, 굵은 글씨가 죽지 않습니다. 표만 만들어 놓고 알맹이가 사라지면 고친 게 아니라서요. 확인 절차 (F5 로 새로고침한 뒤 — 화면 코드가 바뀌었습니다): 1. Docs 에서 QST-20260827-001-62x6 을 엽니다. 표가 있던 자리에 파이프 문자가 아니라 진짜 표가 보여야 맞습니다. 2. 표 안의 칸을 보세요. 회색 배경의 코드 글자나 밑줄 그어진 문서 번호가 그대로 살아 있어야 합니다. 그 문서 번호를 누르면 해당 문서로 갑니다. 3. 다른 문서도 열어 보세요 — REQ-20260826-032-62x6(점 색 의미 표), REQ-20260826-018-62x6(필드 규격 표 3개)에 표가 여럿 있습니다. 4. 표가 화면보다 넓으면 표 안에서만 좌우로 밀립니다. 본문 전체가 옆으로 밀리면 안 됩니다. 5. 코드 블록(``` 로 감싼 부분) 안에 파이프가 있어도 표로 바뀌면 안 됩니다. 그대로 코드로 남아야 맞습니다. 6. 화면 색상을 바꿔서(설정 톱니 → tone) 표 글자가 배경에 묻히지 않는지 봐 주세요. 밝은 색과 어두운 색 양쪽에서 확인했습니다. 색을 칠하지 않았습니다. 줄무늬 배경 없이 얇은 선으로만 행을 나눴고, 숫자만 든 열은 자릿수가 세로로 맞도록 오른쪽에 붙였습니다. 캡처: state/shots/008-verify.png (문서 4건 자동 점검 — 본문에 남은 파이프 줄 0개) · state/shots/008-light.png (밝은 색상에서 3열 표) 한 가지 못 한 것: 터미널 화면 쪽 표는 눈으로 다시 보지 못했습니다. 캡처 도구가 터미널 화면에서 멈춰 버리는 문제가 아직 안 고쳐져서입니다(REQ-20260827-001-62x6). 대신 그쪽이 쓰던 규칙 14가지가 그대로인지 자동 검사로 확인했습니다.
- 2026-08-27T08:25:24+09:00 status: review -> done (by sjpark1) — 대시보드 승인 [via dashboard]
