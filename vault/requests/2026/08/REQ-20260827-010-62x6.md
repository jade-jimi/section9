---
id: REQ-20260827-010-62x6
type: request
title: 스트림 화면의 시각 보정
summary: 문서 기록은 실제 시각으로 고쳐졌지만 Stream 탭은 모델이 쓴 도장을 그대로 그린다
status: done
size: S
user: sjpark1
machine: DESKTOP-TEHV1KR
session: 3da5b9de
sessions: ["8e60e4af", "3da5b9de"]
agents: ["sub:designer:a6d27a6e", "sub:subagent:a5c43a41", "sub:subagent:aa59210d"]
contributions: [{"actor": "sub:designer:a6d27a6e", "item": "화면 3건 — 시각·키보드·첨부본문", "started": "2026-08-27T07:54:00+09:00", "ended": "2026-08-27T08:36:52+09:00", "result": "done", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/8e60e4af-6e99-4b92-857b-fe3e651a913b/tasks/a6d27a6e33feda190.output"}, {"actor": "sub:subagent:a5c43a41", "item": "Transitioning REQ-20260827-010-62x6 to review", "started": "2026-08-27T08:33:50+09:00", "ended": "2026-08-27T08:33:50+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/8e60e4af-6e99-4b92-857b-fe3e651a913b/subagents/agent-a5c43a415f9b0844a.jsonl"}, {"actor": "sub:designer:a6d27a6e", "item": "`[2026-08-27 08:36:08 KST - designer]`", "started": "2026-08-27T08:36:52+09:00", "ended": "2026-08-27T08:36:52+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/8e60e4af-6e99-4b92-857b-fe3e651a913b/subagents/agent-a6d27a6e33feda190.jsonl"}, {"actor": "sub:subagent:aa59210d", "item": "Appending TDD note to REQ-20260827-010-62x6", "started": "2026-08-27T14:15:45+09:00", "ended": "2026-08-27T14:15:45+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/3da5b9de-895e-4435-9e4c-262ae0d58d32/subagents/agent-aa59210d5f1886de3.jsonl"}]
project: section9
parent: REQ-20260826-038-62x6
relates: ["REQ-20260827-009-62x6"]
tags: ["dashboard", "design", "index", "attached", "png", "image", "terminal"]
created: 2026-08-27T00:46:32+09:00
status_since: 2026-08-27T16:53:51+09:00
updated: 2026-08-27T16:53:51+09:00
priority: 50
---

## Original

요구는 노트 참조. web/index.html — designer 작업 후.

## Notes

### 2026-08-27T00:46:55+09:00 response (by sjpark1)

REQ-20260826-038-62x6 에서 갈랐다. 그쪽은 **문서에 남는 기록**을 고쳤고 그건 실제 데이터로 검증됐다(22:37 노트 13분 37초 어긋남 → 00:26 노트 초 단위 일치). 여기는 남은 한 겹이다.

**대시보드 Stream 탭이 실시간으로 뿌리는 텍스트는 여전히 모델이 쓴 도장 그대로다.** 그 화면은 문서 노트가 아니라 transcript 를 직접 읽어 그리기 때문에, Stop 훅의 보정이 닿지 않는다. 그래서 같은 응답이 두 자리에서 다른 시각을 말한다 — 문서에서는 맞고, 스트림에서는 프롬프트가 도착한 시각이다.

고치는 방향은 분명하다: **대시보드는 그 이벤트의 실제 시각을 이미 갖고 있다.** 모델이 쓴 도장 줄을 화면이 자기 시각으로 대체해 그리면 된다. 이름(역할)은 모델이 쓴 것을 살려야 한다 — 누가 말하는지는 모델만 아는 사실이고, 여기서 눌러 쓰면 위임 보고가 리드의 말로 둔갑한다(Stop 훅이 같은 이유로 이름을 보존한다).

**038 에서 이걸 붙들고 있지 않고 가른 이유**: 요구("반드시 정확한 시간이 출력되어야 한다")는 영속 기록에서 이미 충족됐고, 남은 것은 `web/index.html` 이라 designer 가 잡고 있어 지금 손댈 수 없다. 충족된 것을 미완으로 붙들어 두면 상태가 흐려진다 — 사용자가 "038 은 아직도 완료를 못 가는 이유가 뭐냐"고 물은 것이 그 흐려짐의 증거다. 그건 내 판단 미스다.

### 2026-08-27T08:33:20+09:00 designer (by sjpark1)

Stream 탭이 도장 시각을 스스로 고쳐 그린다. web/index.html 만 고쳤다.

**한 일**: 이벤트를 그리는 자리(renderEvents)에서, 응답 머리에 도장이 있으면 그 시각만 이 이벤트의 실제 시각으로 바꿔 그린다.

- **이름은 모델이 쓴 것을 그대로 살린다.** 캡처 그룹 2(이름)를 그대로 다시 쓰고, 캡처 그룹 1(모델이 쓴 시각)은 버린다. 누가 말했는지는 모델만 아는 사실이라 화면이 눌러 쓰면 위임 보고가 리드의 말이 된다 — bin/s9-audit-response 의 correct_stamp 가 이름을 보존하는 이유와 같다.
- **머리에 있는 것만 도장이다.** 정규식은 `^` 로 첫 글자에만 걸고 m·g 플래그를 쓰지 않는다. 본문 중간에 예시로 적힌 같은 모양을 고치면 문서가 자기 설명과 어긋난다.
- **접힌 요약과 펼친 본문이 한 문자열을 본다.** 예전 코드는 요약을 원문에서 잘라 썼다 — 한쪽만 고치면 같은 화면 안에서 두 시각이 갈라진다.
- **고쳤다는 사실을 감추지 않는다.** 어긋남이 1분(훅과 같은 기준)을 넘은 도장에만 점선 밑줄을 얹고, 마우스를 올리면 "모델은 여기에 22:24:18 이라고 적어 넣었다 — 이 말이 실제로 남은 시각과 13분 37초 어긋난다"고 말한다. 색면이 아니라 점선인 것은 문서 링크와 같은 어휘라서다.

**실물 확인**: 세션 d3d60fdc 의 22:37 응답 — 모델이 22:24:18 이라고 적었고 실제는 22:37:55 인, REQ-20260826-038-62x6 노트에 나오는 바로 그 건이다. 화면이 `[2026-08-26 22:37:55 KST - lead]` 로 그렸고 이벤트 머리표(08-26 22:37)와 맞는다. 이름 lead 는 그대로다.
캡처: \\wsl.localhost\Ubuntu\tmp\claude-1000\-home-sjpark1-section9\8e60e4af-6e99-4b92-857b-fe3e651a913b\scratchpad\shots\1-stream-stamp.png

**손대지 않은 곳**: Terminal 탭은 그대로 뒀다. 그 화면은 기록 뷰어가 아니라 터미널 재현이라, 모델이 실제로 친 글자를 그대로 보여주는 것이 그 화면의 약속이다. 판단이 다르면 반려해 달라.

**테스트**: tests/test_stream_stamp_ui.py 신규 8건 (red 확인 후 구현). 정규식은 화면 소스에서 꺼내 실제로 돌려 검사한다 — 머리에만 걸리는지, 줄머리마다 걸리지 않는지(m 플래그 금지), 이름 캡처가 살아 있는지. 전체 787건 green.

### 2026-08-27T08:33:50+09:00 subagent (agent: sub:subagent:a5c43a41, by sjpark1)

Transitioning REQ-20260827-010-62x6 to review

### 2026-08-27T08:36:52+09:00 subagent (agent: sub:designer:a6d27a6e, by sjpark1)

`[2026-08-27 08:36:08 KST - designer]`

세 건 모두 구현·검증·상신 끝났습니다. 커밋은 리드 몫으로 남깁니다.

### 결론

| REQ | 한 일 | 상태 |
|---|---|---|
| REQ-20260827-010-62x6 스트림 시각 | `renderEvents` 가 도장의 **시각만** 이벤트 실제 시각으로 다시 쓴다. 이름은 모델 것 보존 | review |
| REQ-20260827-007-62x6 범례 키보드 | `.gtype` span → `<button>` + `aria-pressed` + 포커스 링 복원. 덤으로 보드 카드 Enter/Space 활성화 | review |
| REQ-20260827-005-62x6 첨부 본문 | 첨부 칩 아래 발췌 블록 + 전문 펼치기, 검색 조각에 출처 파일명 | review |

전체 테스트 **787건 green**. 신규 계약 25건(8+7+10), 전부 red 확인 후 구현.

### 건별 핵심

**① 시각 보정** — 정규식은 `^` 에 m·g 플래그 없음(본문 중간 예시는 손대지 않음), 캡처 2(이름)만 되쓰고 캡처 1(모델 시각)은 버림. 접힌 요약과 펼친 본문이 한 문자열을 보게 고쳤습니다 — 예전 코드는 요약을 원문에서 잘라 써서 한쪽만 고치면 화면 안에서 두 시각이 갈라졌습니다. 1분 넘게 어긋난 도장에만 점선 밑줄 + 무엇을 고쳤는지 툴팁.
**실물**: 세션 `d3d60fdc` 의 22:37 응답(모델 22:24:18 / 실제 22:37:55, 038 노트의 그 건)이 `22:37:55 KST - lead` 로 그려지고 이벤트 머리표와 일치.
**손대지 않은 곳**: Terminal 탭 — 기록 뷰어가 아니라 터미널 재현이라 모델이 친 글자 그대로가 그 화면의 약속이라 봤습니다. 노트에 판단을 청해 뒀습니다.

**② 키보드** — 진짜 버튼이라 Tab·Enter/Space·포커스 링이 브라우저에서 따라오고, 클릭 핸들러는 그대로라 마우스와 키보드가 한 경로를 씁니다. `.gtype.want`(빈 화면 안내의 점선 지목)가 특이도로 포커스 링을 덮던 것을 명시 복원 — **짚기는 점선, 포커스는 실선**. 픽셀 검사로 focus 시 아웃라인이 끊김 없는 실선임을 확인했습니다.
보드 카드는 `tabindex=0 role=button` 이라 **닿기는 하는데 눌리지 않던** 상태였습니다(포커스만 삼키니 못 닿는 것보다 나쁨) — 전역 keydown 하나로 이어 붙였고, Enter 로 문서가 실제로 열리는 것을 캡처했습니다.
**범위**: `a.doclink`(href 없는 앵커)·목록 행은 "행 전체가 포커스 단위인가", "300건을 Tab 300번으로 둘 것인가"부터 정해야 해서 **REQ-20260827-013-62x6 「문서 링크 키보드 접근」** 으로 분리 등록했습니다.

**③ 첨부 본문** — 발췌 3줄/260자 → `전문 보기` → 상한 20,000자(넘으면 밝힘), 펼친 상자는 `tabindex=0` 이라 키보드로도 스크롤됩니다. **검색해서 들어오면 찾던 문구가 있는 자리를 폅니다**(맨 앞 세 줄은 대개 표지). 검색 결과 조각에는 `📎 파일명` 을 붙였습니다 — 파일명 없이 줄 번호만 보이면 문서 본문의 그 줄로 읽힙니다.
본문은 `/api/asset-text` 한 곳에서만 가져오고(화면이 경로를 조립하면 계약 테스트가 막습니다), 뽑은 글자는 전부 escape.
**판단을 청한 것**: 글자를 뽑아 둔 것 없는 첨부에는 아무것도 그리지 않습니다(없는 게 정상인데 첨부마다 알리면 있는 쪽이 안 읽힘).

### 리드가 알아야 할 것

- **대시보드 서버가 동시 요청에서 이따금 연결을 끊습니다.** 직접 재현: `127.0.0.1:9909` 에 동시 12요청 → 6건이 연결 리셋(curl exit 56/7). 제 첨부 본문 요청은 스트림 화면과 같은 처방(1회 재시도)을 넣었지만, **이건 화면 문제가 아니라 서버 문제**입니다. `bin/s9` 는 안 건드렸으니 별건 등록이 필요하면 리드가 판단해 주세요.
- 검증은 headless 가 클릭·Tab 을 못 해서, `web/index.html` 을 그대로 서빙하면서 끝에 조작 스크립트만 덧붙이는 프록시를 scratchpad 에 띄워 실제로 눌러 보고 캡처했습니다(리포 파일은 손대지 않음, 종료 시 정리 완료). 도구는 `/tmp/claude-1000/-home-sjpark1-section9/8e60e4af-6e99-4b92-857b-fe3e651a913b/scratchpad/proxy.py`, 의존성 없는 PNG 크롭이 같은 폴더 `pngcrop.py` 입니다 — 다음 화면 작업에서 재사용 가치가 있으면 knowledge 로 올리겠습니다.
- 제가 만진 파일: `/home/sjpark1/section9/web/index.html`, `/home/sjpark1/section9/tests/test_stream_stamp_ui.py`(신규), `/home/sjpark1/section9/tests/test_legend_keyboard.py`(신규), `/home/sjpark1/section9/tests/test_asset_text_ui.py`(신규), `/home/sjpark1/section9/tests/test_graph_empty.py`(마크업 정규식 1줄 — span→button 으로 바뀌어 태그 무관으로). `bin/s9`·`tests/test_binding_shape.py` 의 변경은 제 것이 아닙니다.
- 캡처 8장: `\\wsl.localhost\Ubuntu\tmp\claude-1000\-home-sjpark1-section9\8e60e4af-6e99-4b92-857b-fe3e651a913b\scratchpad\shots\`

### 2026-08-27T13:14:16+09:00 response (by sjpark1)

반려 사유대로 **직접 찍어서 봤고, 원인을 찾았다.** 다만 고칠 자리가 이 세션의 봉투 밖이라 적용하지 못한다.

### 직접 본 것

세 장을 찍었다(`state/shots/010-rework-1.png` · `010-rework-2.png` · `010-tall.png`).

- `#stream` 만 열면 오른쪽은 안내문뿐이다 — 세션을 골라야 본문이 나온다.
- 세션을 골라 1440×900 으로 찍으면 **본문이 위아래로 잘려 있다.** 첫 줄이 글자 중간에서 시작한다.
- 같은 화면을 1200×2600 으로 찍으면 **본문이 통째로 다 나온다.** 즉 내용이 잘려 저장된 게 아니라 **열자마자 맨 아래로 내려가 있어서** 앞부분이 안 보이는 것이다.

### 원인 — 끝난 세션이 `live` 로 표시된다

캡처에 그대로 찍혀 있다: `session 9a2bd342-… · 21 events · ● live ☑ follow`.

그 세션은 **어제 22:14 에 끝났다.** 바인딩을 열어 보면 명시적으로 적혀 있다:

    state/sessions/DESKTOP-TEHV1KR__9a2bd342.json
      "ended": "1"

그런데 스트림 API 는 `live: true` 를 준다. `bin/s9` 의 `resolve_stream_path()` 가 **"바인딩이 있고 transcript 파일이 디스크에 남아 있으면 살아있다"** 로 판정하기 때문이다:

    tp = b.get("transcript_path", "")
    if tp and os.path.exists(tp):
        return tp, True  # 라이브 원본 우선

`ended` 도, 마지막 활동 시각도 보지 않는다. **한 번이라도 돌았고 파일이 남아 있으면 영원히 live 다.**

그 결과가 화면에서 둘로 나타난다:

1. `term.scrollTop = d.live ? term.scrollHeight : 0` — 열자마자 맨 아래로 뛴다. 그래서 **본문 앞부분이 안 보인다.** (`web/index.html`)
2. `follow` 체크박스가 켜진 채 **2.5초 폴링이 영원히 돈다.** 죽은 세션이라 새 이벤트는 0 이지만, 화면은 "지금 살아 있고 따라가는 중"이라고 계속 말한다.

`live` 가 올바로 `false` 였다면 같은 줄이 `term.scrollTop = 0` 이 되어 **본문 첫 줄부터 보인다.** 반려 사유의 앞 절반("본문이 다 보이지도 않고")이 이것 하나로 풀린다.

### 고치는 자리 — `bin/s9` (이 세션 봉투 밖)

이 저장소에는 이미 같은 판정을 제대로 하는 장치가 있다. `chat_target()` 이 쓰는 그대로다:

    if b.get("ended") or not chat_live(b): continue

제안: `resolve_stream_path()` 의 그 분기를 아래로 바꾼다.

    tp = b.get("transcript_path", "")
    if tp and os.path.isfile(tp) and not b.get("ended") and chat_live(b):
        return tp, True          # 진짜로 도는 세션만 live
    if tp and os.path.isfile(tp):
        return tp, False         # 파일은 읽되 live 라고 말하지 않는다

두 번째 줄이 중요하다 — **끝난 세션도 본문은 읽을 수 있어야 한다.** live 를 끄는 것이지 접근을 막는 게 아니다. 그리고 `exists` 가 아니라 `isfile` 이어야 한다(REQ-20260827-018-62x6 에서 배운 것과 같은 자리 — 디렉토리가 통과한다).

계약 테스트 자리도 정해 둔다: `ended` 가 박힌 바인딩에 대해 `stream_events()` 가 `live=False` 를 주는가 · 그래도 이벤트는 그대로 오는가.

### 확인하지 못한 것 — 정직하게

반려 사유 뒤 절반("스크롤도 안되고")은 **재현하지 못했다.** 코드상 `.term` 은 `max-height:calc(100vh - 150px)` + `overflow-y:auto` 로 내부 스크롤이 걸려 있고, 폴링도 "위로 읽는 중이면 방해하지 않는다"는 가드를 갖고 있다. 헤드리스 캡처는 **굴릴 수가 없어서** 그 가드가 실제로 도는지 확인할 방법이 이 봉투 안에 없다.

그래서 측정용 프록시를 하나 썼다(`scratchpad/measure.py`) — 대시보드를 그대로 서빙하면서 페이지 끝에 측정 스크립트만 얹어, 실제 높이·스크롤 가능 여부·굴려 본 결과를 화면에 글자로 찍는다. **이 세션은 `s9` 로 시작하는 명령만 허용돼 실행하지 못했다.** 리드 세션에서 `python3 scratchpad/measure.py 9971` 로 띄우고 그 포트를 찍으면 바로 답이 나온다.

**추정으로 스크롤 CSS 를 건드리지 않았다.** 오늘 이 저장소에서 배운 것이 그것이다 — 재현하지 못한 것을 고치면 고쳤는지도 알 수 없다.

### 2026-08-27T14:08:30+09:00 tdd (by sjpark1)

TDD 시나리오 — 리드 이어받기 (실행: python3 tests/ stream_live_ended)

워커의 진단을 그대로 계약으로 옮긴다. `resolve_stream_path()` 가 **'바인딩이 있고
transcript 파일이 남아 있으면 살아있다'** 로 판정한다 — `ended` 도 마지막 활동도
보지 않는다. 그래서 어제 끝난 세션이 `● live` 로 뜨고, 화면은
`term.scrollTop = d.live ? scrollHeight : 0` 이라 **열자마자 맨 아래로 뛴다.**
사용자가 본 '본문이 다 보이지도 않고' 가 이것이다.

정상
  N1. `ended` 가 박힌 바인딩의 세션은 `live=false` 로 온다.
  N2. **그래도 이벤트는 그대로 온다** — live 를 끄는 것이지 접근을 막는 게 아니다.
      끝난 세션의 기록을 못 읽게 되면 이 화면의 존재 이유가 사라진다.

경계
  B1. 살아 있는 세션(ended 없음 + 프로세스 생존)은 예전대로 `live=true`.
  B2. `transcript_path` 가 디렉토리를 가리키면 통과하지 않는다 — `exists` 가 아니라
      `isfile` 이다 (REQ-20260827-018-62x6 에서 같은 자리를 이미 한 번 배웠다).

회귀
  R1. 바인딩이 없는 세션은 예전대로 스트림 미러로 폴백하고 `live=false`.

별건으로 남는 것
  반려 사유 뒤 절반('스크롤도 안되고')은 워커가 재현하지 못했다 — 헤드리스 캡처는
  굴릴 수가 없어서다. 워커가 만들어 둔 `scratchpad/measure.py` 를 리드가 직접
  띄워 확인한다.

### 2026-08-27T14:15:32+09:00 tdd (by sjpark1)

TDD 시나리오 — 스크롤 결함(반려 사유 뒤 절반), designer (실행: python3 tests/ term_scroll)

리드 실측이 원인을 확정했다. `.term` 은 **자기 자신이 스크롤 컨테이너**다
(`max-height:calc(100vh - 150px); overflow-y:auto`). calm 스킨이 둥근 모서리를
자르려고 같은 요소에 `overflow:hidden` 을 얹어 그 스크롤을 덮었다 —
2057px 중 654px 만 보이고 1376px 이 영원히 닿을 수 없었다.

이 저장소는 **같은 함정을 이미 한 번 밟았다**: 바로 세 줄 위 `[data-skin="calm"]
.doclist` 가 REQ-20260826-004-62x6 에서 똑같은 이유로 `overflow-x:hidden;overflow-y:auto`
로 고쳐졌다. 그때 옆에 있던 `.term` 은 같이 고쳐지지 않았다. 그러니 이번 계약은
"calm 을 고쳤다"가 아니라 **"어느 스킨도 `.term` 의 세로 흐름을 덮지 못한다"** 여야
재발이 잡힌다.

정상
  N1. calm 에서 `.term` 의 computed `overflow-y` 가 `auto` 다 — 실브라우저 실측.
      굴려서 scrollTop 이 실제로 움직이고, 마지막 이벤트의 아래끝이 상자 안에 든다.
  N2. calm 의 둥근 모서리·그림자는 그대로다. `border-radius:var(--calm-r)` +
      `box-shadow:var(--calm-shadow)` 유지, 가로는 여전히 잘린다
      (`overflow-x:hidden`). 스크롤을 살리자고 스킨 성격을 지우지 않는다.

경계
  B1. `.ccterm` 은 자기가 스크롤 컨테이너가 아니다 — 안에 `.ccout{flex:1;
      overflow-y:auto}` 를 따로 둔다. 그래서 `.ccterm` 의 `overflow:hidden` 은
      모서리만 자르고 스크롤은 건드리지 않는다. **실측으로 확인한다** — 추측 금지.
  B2. `.streamwrap` 에 `overflow:hidden` 을 거는 다섯 스킨(soft·glass·cobalt·
      slate·cork)은 **부모**를 자를 뿐이라 자식 `.term` 의 자체 스크롤은 산다.
      다섯 전부 실측한다.

회귀
  R1. 계약 테스트: `[data-skin=…] .term` 계열 선언에 `overflow:hidden` 또는
      `overflow-y:hidden` 이 다시 나타나면 실패한다. 스킨 10종 전수 스캔.
  R2. 베이스 `.term` 의 `max-height` + `overflow-y:auto` 자체가 사라져도 실패한다
      (스크롤을 죽이는 두 번째 경로).
  R3. `.ccterm` 이 자체 스크롤 자식(`.ccout` overflow-y:auto)을 잃으면 실패한다 —
      B1 의 면책 근거가 사라지는 순간을 잡는다.

검증: 10종 × `#stream/9a2bd342` 를 measure 프록시로 실측 캡처. 육안이 아니라
computed overflow-y·scrollTop 이동·마지막 이벤트 잘림량을 글자로 찍어 확인한다.

### 2026-08-27T14:15:45+09:00 subagent (agent: sub:subagent:aa59210d, by sjpark1)

Appending TDD note to REQ-20260827-010-62x6

### 2026-08-27T14:28:07+09:00 response (by sjpark1)

반려 사유 뒤 절반 — **"스크롤도 안되고"** 를 고쳤다. calm 스킨 한정 결함이었고, 스킨
10종을 전부 실측해 다른 데는 없음을 확인했다. `web/index.html` 만 만졌다.

### 무엇이었나

`.term`(Stream 본문)은 **자기 자신이 스크롤 상자**다 — 화면 높이만큼만 보여주고
나머지는 안에서 굴려 읽게 돼 있다. calm 은 **둥근 모서리를 자르려고** 바로 그 상자에
`overflow:hidden` 한 줄을 얹었다. 모서리와 함께 **아래로 흐르는 길까지 잘렸다.**

1440×900 실측으로 2057px 중 **654px 만 보이고 1403px 이 영원히 닿을 수 없었다.**
사용자의 기본 스킨이 calm 이라, 이 결함은 사용자에게만 보였다.

이 저장소는 **같은 함정을 이미 한 번 밟았다.** 세 줄 위 `[data-skin="calm"] .doclist`
가 REQ-20260826-004-62x6 에서 똑같은 이유로 `overflow-x:hidden;overflow-y:auto` 로 고쳐졌는데,
바로 옆에 있던 `.term` 은 같이 고쳐지지 않았다. 그래서 처방도 그때와 같게 맞췄다 —
**가로만 자르고 세로는 흐르게.**

### 스킨의 성격은 지우지 않았다

스크롤을 살리자고 calm 을 각지게 만들지 않았다. 스크롤 상자도 `border-radius` 로
클리핑되므로 **둥근 모서리 12px 과 그림자가 그대로 산다** — 실측으로 확인
(`term 라운드 = 12px / 그림자 = 있음`). 색면·세로 띠는 쓰지 않았고, 새로 생긴
시각 요소는 오른쪽 가장자리의 스크롤바 하나뿐이다. 그건 다른 9개 스킨에서 이미
쓰던 그 스크롤바다(전역 8px, `--border` 색) — 새 문법을 만들지 않았다.

### 스킨 10종 전수 실측 (추측 없음)

프록시로 실제 computed 값을 화면에 찍고, **실제로 끝까지 굴려 본 뒤** 되돌아오는지까지
쟀다. `#stream/9a2bd342` · 1440×900.

    skin       streamwrap.overflow   term.overflowY   끝까지 굴림      스크롤
    calm       visible               auto (X:hidden)  1403 / 1403px    살아있음
    ledger     visible               auto             1403 / 1403px    살아있음
    soft       hidden                auto             1627 / 1627px    살아있음
    glass      hidden                auto             1403 / 1403px    살아있음
    cobalt     hidden                auto             1403 / 1403px    살아있음
    slate      hidden                auto             1403 / 1403px    살아있음
    cork       hidden                auto             1403 / 1403px    살아있음
    terminal   visible               auto             1403 / 1403px    살아있음
    grid       visible               auto             1366 / 1366px    살아있음
    field      visible               auto             1478 / 1478px    살아있음

**`.streamwrap` 에 `overflow:hidden` 을 거는 다섯 스킨(soft·glass·cobalt·slate·cork)은
멀쩡하다** — 거기는 **부모**를 자를 뿐이고, 자식인 `.term` 은 자기 스크롤을 그대로
갖는다. calm 이 달랐던 건 **자르는 대상이 스크롤 상자 자기 자신**이었기 때문이다.
같은 `overflow:hidden` 이라도 어디에 거느냐가 갈랐다.

### Terminal 탭(`.ccterm`)은 함정이 아니다 — 재 봤다

같은 줄에 묶여 있어 의심스러웠지만, `.ccterm` 은 **자기가 스크롤 상자가 아니다.**
안에 `.ccout{flex:1;overflow-y:auto}` 를 따로 두고 그쪽이 구른다. 그래서 여기의
`overflow:hidden` 은 모서리만 자른다. 실측: `ccterm.overflow = hidden` 이면서
`ccout` 은 5949px 중 471px 을 보여주고 **5478px 을 끝까지 굴러 내려간다.**
고칠 것이 없어 안 고쳤고, 대신 **그 면책의 근거가 사라지는 순간**(`.ccout` 이 세로
스크롤을 잃는 순간)을 계약 테스트로 잡아 뒀다.

### 계약 테스트 — `tests/test_term_scroll.py` (5건, red 확인 후 green)

계약을 "calm 을 고쳤다"가 아니라 **"어느 스킨도 `.term` 의 세로 흐름을 덮지 못한다"**
로 적었다. 주어가 `.term` 인 모든 규칙을 훑으므로 **앞으로 생길 스킨도 자동으로 걸린다.**

  - 주어가 `.term` 인 어떤 규칙도 `overflow(-y): hidden|clip|scroll` 을 선언하지 않는다
  - 베이스 `.term` 이 높이 상한 + `overflow-y:auto` 를 유지한다 (스크롤을 죽이는 두 번째 경로)
  - calm 의 `border-radius`·`box-shadow` 가 살아 있고 가로는 계속 잘린다
    — 스크롤을 살리자고 스킨 성격을 지우는 것도 결함으로 본다
  - 설정에서 고를 수 있는 스킨 10종이 전부 `[data-skin=…]` 표기를 써 스캔에 걸린다
  - `.ccterm` 이 자체 스크롤 자식(`.ccout`)을 잃으면 실패한다

red 확인: 고치기 전 3건 실패(calm 규칙을 정확히 지목). 전체 **906건 green**(기준선 901 + 5).

### 첨부 (순서대로)

① calm 실측 — `overflowY = auto`, 1403px 끝까지 굴러가고, 라운드 12px·그림자 유지
② 고치기 전 / ③ 고친 뒤 — 같은 화면 같은 크기. 검은 상자 **오른쪽 가장자리**를 보면
   ②에는 없던 스크롤바가 ③에 생겼다. 아래쪽에 닿을 길이 생겼다는 뜻이다
④ Terminal 탭 실측 — `ccterm` 은 잘라도 `ccout` 이 5478px 을 굴러 내려간다
⑤ cork — `.streamwrap` 을 자르는 다섯 스킨의 대표. 부모를 잘라도 본문은 구른다

검증 도구는 scratchpad 에만 뒀고(리포 파일 무손상), 프록시 두 개는 모두 종료했다.

[Image: assets/REQ-20260827-010-62x6/m-calm.png]
[Image: assets/REQ-20260827-010-62x6/calm-before-top.png]
[Image: assets/REQ-20260827-010-62x6/calm-after-top.png]
[Image: assets/REQ-20260827-010-62x6/m-calm-terminal.png]
[Image: assets/REQ-20260827-010-62x6/m-cork.png]

### 2026-08-27T14:33:18+09:00 response (by sjpark1)

리드 마감 — 서버 쪽 절반 적용·커밋·재기동까지 끝냈다.

**실환경 확인 (14:32, 서버 재기동 후)**

    9a2bd342 (어제 22:14 에 끝난 세션) → live=False · events=21

워커가 짚은 바로 그 세션이다. 이제 `● live` 가 아니라 `mirror(턴 종료 시점)` 으로
뜨고, 21개 이벤트는 그대로 읽힌다 — **live 를 끈 것이지 접근을 막은 게 아니다.**

**두 절반이 각각 어디서 왔는지**
- 본문이 안 보임 → `bin/s9` 의 `resolve_stream_path` (커밋 d8dfcfc, 계약 5건)
- 스크롤이 안 됨 → calm 스킨의 `.term{overflow:hidden}` (커밋 22454b5, 계약 5건, designer)

**리드가 직접 확인한 것**: designer 의 보고를 그대로 믿지 않고 프록시를 다시 띄워
`?skin=calm` 으로 재봤다 — `term.overflowY = auto`, `term 스크롤 먹힘 = true`,
둥근 모서리·그림자 유지. 전체 906건 OK.

**이 건에서 남길 것**: 반려 사유가 두 문장이면 원인도 둘일 수 있다. 여기서는
'본문이 안 보인다'와 '스크롤이 안 된다'가 각각 서버 판정과 스킨 CSS 였고,
한쪽만 고쳤으면 사용자는 같은 화면을 다시 반려했을 것이다.

## History
- 2026-08-27T00:46:32+09:00 created by sjpark1 (status: open)
- 2026-08-27T07:51:27+09:00 status: open -> in-progress (by sjpark1) — 착수
- 2026-08-27T08:33:37+09:00 status: in-progress -> review (by sjpark1) — 스트림 화면의 시각을 화면이 스스로 고쳐 그리게 했습니다. **F5 후** Stream 탭에서 확인해 주세요. 확인 포인트 셋입니다. ① **같은 응답이 두 시각을 말하지 않는가** — Stream 탭에서 세션 하나를 고르면, 응답 머리의 `[…]` 시각이 바로 위 이벤트 머리표(예: [08-26 22:37])와 같아야 합니다. 예전에는 여기가 몇 분씩 어긋났습니다. 재현하기 좋은 자리: 목록에서 d332535d… 로 시작하는 27MB 짜리 세션(2026-08-26)을 고르고 "22:37" 로 검색창을 걸면 그 줄만 남습니다. ② **누가 말했는지가 바뀌지 않았는가** — 시각만 고치고 이름(lead·designer 같은 역할명)은 모델이 쓴 그대로 둡니다. 위임된 에이전트의 보고가 리드의 말로 둔갑하면 그건 결함입니다. ③ **고친 자리에 점선 밑줄이 보이는가** — 1분 넘게 어긋났던 도장에만 옅은 점선이 깔립니다. 마우스를 올리면 모델이 원래 뭐라고 적었는지와 몇 분 어긋났는지를 말해 줍니다. 문서 링크의 점선과 같은 어휘로, 색면은 쓰지 않았습니다. 함께 봐 주실 판단 하나: **Terminal 탭은 일부러 손대지 않았습니다.** 그 화면은 기록을 다시 읽는 곳이 아니라 터미널을 그대로 재현하는 곳이라, 모델이 실제로 친 글자를 그대로 보여주는 게 맞다고 봤습니다. 거기서도 시각이 맞아야 한다고 보시면 반려해 주세요. 캡처: \\wsl.localhost\Ubuntu\tmp\claude-1000\-home-sjpark1-section9\8e60e4af-6e99-4b92-857b-fe3e651a913b\scratchpad\shots\1-stream-stamp.png
- 2026-08-27T13:07:42+09:00 status: review -> in-progress (by sjpark1) — 반려: Stream 화면은 본문이 다 보이지도 않고, 스크롤도 안되고.. 다시 확인해주고 직접 확인할 수 있는것들은 직접 확인 해. [via dashboard]
- 2026-08-27T13:14:29+09:00 status: in-progress -> blocked (by sjpark1) — 직접 찍어서 원인을 찾았는데, 고칠 자리가 이 무인 세션이 손댈 수 있는 범위 밖이라 멈춘다. 원인: 어제 끝난 세션이 화면에 "live"로 표시된다 — 그래서 스트림을 열자마자 맨 아래로 뛰고 본문 앞부분이 안 보인다. 세션 기록에는 "끝났다"고 분명히 적혀 있는데, 서버가 그걸 안 보고 "기록 파일이 남아 있으면 살아있다"로 판정한다. 그 한 줄만 고치면 열었을 때 첫 줄부터 보인다. 고칠 파일이 bin/s9 라 이 세션은 제안만 남길 수 있다(노트에 정확한 위치와 패치를 적어 뒀다 — 이 저장소에 이미 있는 판정 함수를 쓰면 된다). 스크롤이 안 된다는 두 번째 지적은 재현하지 못했다. 헤드리스 캡처는 굴릴 수가 없어서다. 굴려 보는 측정 도구를 scratchpad/measure.py 로 만들어 뒀는데 이 세션은 실행 권한이 없다 — 리드가 그걸 띄우면 바로 답이 나온다. 추정으로 CSS 를 건드리지 않았다.
- 2026-08-27T14:08:30+09:00 status: blocked -> in-progress (by sjpark1) — 리드가 이어받는다 — 워커가 원인을 정확히 짚었고(끝난 세션이 live 로 표시돼 열자마자 맨 아래로 뛴다) 고칠 자리가 bin/s9 라 봉투 밖이었다. 스크롤 재현도 측정 도구로 직접 돌린다.
- 2026-08-27T14:28:27+09:00 status: in-progress -> review (by sjpark1) — 반려하신 두 가지 중 **스크롤이 안 되던 것**을 고쳤습니다. **F5 후** Stream 탭에서 봐 주세요. ① **본문을 마우스 휠로 아래까지 굴려지는가** — 세션 하나(예: 9a2bd342)를 고르면 검은 본문 상자 안에서 휠이 먹어야 합니다. 예전에는 화면 높이만큼(654px)만 보이고 나머지 1403px 이 영원히 닿을 수 없었습니다. 상자 **오른쪽 가장자리에 가느다란 스크롤바**가 생겼는데, 그게 "아래에 더 있다"는 표시입니다. ② **모서리가 여전히 둥글고 그림자가 있는가** — 스크롤을 살리자고 이 스킨을 각지게 만들지 않았습니다. 둥근 모서리 12px 과 그림자는 그대로고, 새로 생긴 건 스크롤바 하나뿐입니다. 다른 스킨에서 이미 쓰던 그 스크롤바입니다. ③ **Terminal 탭은 원래대로인가** — 같은 CSS 줄에 묶여 있어 함께 살펴봤는데, 거기는 안쪽에 따로 구르는 부분이 있어서 멀쩡했습니다. 그래서 손대지 않았습니다. 굴려 보시고 예전과 같으면 정상입니다. **왜 사용자님께만 보였나**: 기본 스킨이 calm 이라 그렇습니다. 스킨 10종을 전부 직접 재 봤고 무너진 건 calm 하나였습니다(측정값과 캡처를 문서에 그림으로 붙여 뒀습니다). 앞으로 어느 스킨에서든 같은 자리가 다시 막히면 테스트가 잡습니다. 함께 봐 주실 것: **본문이 다 안 보이던 것**은 리드가 서버 쪽에서 고친 부분이라, 열자마자 첫 줄부터 보이는지도 같이 확인해 주시면 두 지적이 한 번에 닫힙니다.
- 2026-08-27T16:53:51+09:00 status: review -> done (by sjpark1) — [via dashboard]
