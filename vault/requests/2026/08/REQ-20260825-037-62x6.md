---
id: REQ-20260825-037-62x6
type: request
title: 세션 모델·계정 제어
summary: 대시보드에서 대상 세션의 model·effort 표시 + 변경(같은 대화를 --resume로 승계 재기동) + 계정 프로필 선택. CC 제약(실행 중 변경 불가·TTY 필요)을 s9 code 재시작 루프 래퍼로 우회
goal: 대시보드에서 유휴 세션의 model/effort 변경을 요청하면 같은 대화가 새 설정으로 재개되고, 현재 모델이 상태줄에 표시된다
status: done
size: L
user: sjpark1
machine: DESKTOP-TEHV1KR
session: d332535d
project: section9
tags: ["session", "test", "dashboard"]
created: 2026-08-25T13:06:14+09:00
status_since: 2026-08-25T16:19:11+09:00
updated: 2026-08-25T16:19:11+09:00
---

## Original

결론대로면 내가 원하는게 다 가능하네. 모두 다 진행해. (선행 대화: 대시보드 터미널에서 model, effort 못 쓰는 이유·login 계정 변경 요망 → --resume --model --effort 공식 지원 확인)

## Notes

### 2026-08-25T13:07:16+09:00 tdd (by sjpark1)

TDD 시나리오 —
[정상] R1: /api/chat/target 응답에 대상 세션의 model(트랜스크립트 마지막 assistant의 model) 포함. R2: 재시작 마커 매칭 — wrapper_pid가 내 pid이고 신선한 마커만 채택, 소비 시 삭제. R3: restart API — 유휴(end_turn)+래퍼 감지 시 마커 기록 후 mode=wrapper 응답. R4: 래퍼 부재(부모가 s9 아님) 시 mode=manual + 실행할 명령 문자열 반환(대화 승계 명령 정확성).
[경계] R5: busy(턴 진행 중) 세션은 거부(진행 중 작업 보호). 쿨다운·pid 재사용 방어는 interrupt와 동일 가드 재사용. 낡은 마커(60s+)는 무시·정리. 계정 프로필 지정 시 CLAUDE_CONFIG_DIR 경로 포함.
[실패] 트랜스크립트 부재·모델 미검출이면 model 빈 값(오류 아님). API 예외는 사유 있는 거부.
[회귀] 전체 스위트 통과. S9_CODE_DRYRUN 경로 불변. 실브라우저: 상태줄 모델 표시·변경 컨트롤 렌더.

### 2026-08-25T13:12:14+09:00 response (by sjpark1)

구현 완료 — ①모델 표시: /api/chat/target에 model(트랜스크립트 파생·mtime 캐시), 터미널 상태줄에 표시(실반환 확인: claude-fable-5). ②변경: 상태줄 모델 클릭 → model/effort 입력 → POST /api/session/restart. 가드: 유휴(end_turn)만·pid 재사용 방어·effort 화이트리스트·빈 변경 거부. s9 code가 재시작 루프 래퍼가 되어(exec→자식 실행, TTY 유지, Ctrl+C 처리 보존) 마커+SIGTERM 수신 시 claude --resume <full-sid> --model --effort 로 같은 대화 재개. ③계정: 마커 account → CLAUDE_CONFIG_DIR=~/.claude-profiles/<이름> (최초 로그인 1회만 터미널). 구버전(exec) 세션은 mode=manual로 실행 명령 안내. 테스트 R1~R6 + 전체 169건. 커밋 5bdc2e8.

### 2026-08-25T13:20:57+09:00 response (by sjpark1)

자가 검증 결과(반려 반영) — 캡처 방법론 결함을 먼저 해소: 터미널 탭은 SSE 상시 연결 탓에 virtual-time 스크린샷이 완주하지 못해 그간 자가 검증이 불가했다. 진단 플래그 ?nosse(폴링 폴백 강제)를 추가해 결정적 캡처를 확보. 결과: 상태줄 모델 라벨(fable-5)·클릭 컨트롤 정상 렌더 확인(스크린샷). 따라서 사용자 F5 실패의 유력 원인은 기능 자체가 아니라 정적 index.html의 브라우저 캐시(no-store 부재로 구버전 JS 서빙) — 반려 스폰 워커(c8e54078)가 cmd_serve 캐시 헤더를 조사 중이라 그 수정은 워커에 위임(수신함 조율 완료), 검증·보고는 리드가 맡는다.

### 2026-08-25T13:23:28+09:00 response (by sjpark1)

재작업 완료 보고 — "스스로 검증하라"는 반려 지적을 방법론부터 수용했다.
(1) 자가 검증 능력 확보: 터미널 탭은 SSE 상시 연결이 virtual-time 캡처를 막아 그간 스크린샷 검증이 불가능했다(이전 review들의 "실브라우저 확인해달라"가 여기서 나온 구조 문제). ?nosse 진단 플래그로 결정적 캡처 경로를 만들었고, 이제 터미널 탭도 리드가 직접 검증한다.
(2) 실패 원인 규명: 캡처 결과 기능 자체는 정상(fable-5 라벨·클릭 컨트롤 렌더 확인, 스크린샷 2매). 정적 캐시(no-store)도 실측 정상. 실패의 유력 원인은 — F5 시점 타깃이 반려로 막 스폰된 워커 세션(c8e54078)으로 잡혔고, 신생 트랜스크립트엔 assistant 이벤트가 없어 model이 빈 값 → 라벨이 아예 렌더되지 않던 결함. 
(3) 수복: 모델 미상이어도 "model?"로 컨트롤을 상시 노출(ab452dd). 
검증: 전체 169건 + ?nosse 실브라우저 캡처 2매(수정 전 fable-5 표시 확인·수정 후 재확인). 워커(c8e54078)와 수신함 조율 — 워커도 독립 캡처로 동일 확인.

### 2026-08-25T13:26:23+09:00 tdd (by sjpark1)

TDD 시나리오 (재작업 검증 완료) —
- [x] R1 트랜스크립트 마지막 assistant model 추출 (mtime 캐시)
- [x] R2 재시작 마커 — 내 pid·신선분만 소비, 낡은 것 정리
- [x] R3 busy 세션 거부 (진행 중 작업 보호)
- [x] R4 래퍼 부재 시 mode=manual + 정확한 재개 명령
- [x] R5 effort 무효값·빈 변경 거부
- [x] R6 죽은 pid 거부
- [x] C20 /api/chat/target 실 HTTP 왕복에 model 키 상시 동봉 + 트랜스크립트 파생 값 (신규)
- [x] R7 상태줄 모델 라벨 상시 노출 — 미상 시 'model?' 폴백 (라벨 실종 결함 회귀 방지, 신규)
- [x] R8 구버전 serve의 404를 "서버 연결 실패"로 오진하지 않고 정확 사유 안내 (신규)
- [x] R9 ?nosse 진단 플래그 유지 — 터미널 탭 헤드리스 자가 검증 경로 (신규)
- [x] 회귀 전체 스위트 173건 통과 (python3 tests/)
- [x] 실브라우저 자가 검증: s9 shot 'http://127.0.0.1:9909/?nosse#terminal' 캡처에서 상태줄 끝 fable-5 표시·밑줄(클릭 컨트롤) 렌더 확인 (scratchpad/term-final.png)

### 2026-08-25T13:26:26+09:00 response (by sjpark1)

재작업 완료 (반려: 확인 포인트 1 실패 + 자가 검증 미흡) —

원인 규명 (자가 검증으로 확정):
1. 라벨 실종 렌더 — nt.model이 빈 값/부재면 모델 라벨 span 자체가 렌더되지 않아 "실행부터 실패"로 보였다. 리드 세션이 'model?' 폴백으로 라벨 상시 노출 수정(협업 분담).
2. 구버전 serve 잔존 — bin/s9 커밋 후에도 이미 떠 있는 serve 프로세스는 메모리의 옛 핸들러로 API를 서빙한다(s9 code는 포트 생존 시 스킵, 리로드 메커니즘 없음). 이 상태에선 F5로 신 HTML을 받아도 /api/chat/target에 model 키가 없다. 증빙: state/serve.log의 구버전 라인번호 트레이스백(main=3659행, 현행 ~4994행) — "Address already in use"로 구 serve가 포트 점유 중 신 spawn이 실패한 흔적.
3. 정적 캐시 가설(리드 제안 "no-store 부재")은 기각 — _send가 모든 응답(index.html 포함)에 Cache-Control: no-store를 단다(bin/s9:4277, SSE는 4376). 읽기 검증으로 확인, bin/ 수정 불필요·미수행.

적용 (범위 web/·tests/ 내):
- web/index.html termModelChange: /api/session/restart 404(구버전 serve)를 "서버 연결 실패"로 오진하던 것을 "대시보드 서버가 구버전(재시작 API 없음) — serve 재시작 후 재시도"로 정확 안내.
- tests/test_dashboard_chat.py C20: 실 serve HTTP 왕복으로 target 응답 model 키 상시 동봉 + 트랜스크립트 파생 값 고정.
- tests/test_session_restart.py R7~R9: 모델 라벨 상시 노출('model?' 폴백)·404 정확 사유·?nosse 진단 플래그 마크업 계약.

검증: 전체 스위트 173건 통과. 실브라우저(s9 shot, Windows Chrome headless) ?nosse#terminal 캡처로 상태줄 "live · target <sid> @sjpark1 · fable-5"(밑줄=클릭 컨트롤) 직접 확인 — scratchpad/term-final.png. 실서버 API도 shot으로 {"model": "claude-fable-5"} 반환 확인.

제안 (bin/ — 범위 밖이라 미적용, 승인 시 별도 작업):
구버전 serve 잔존은 재발 소지가 있다. (a) serve가 기동 시 bin/s9 mtime을 기억하고 주기 tick에서 변화를 감지하면 정상 종료(다음 s9 code가 신버전으로 재기동), 또는 (b) s9 code가 서버 버전(API에 s9 mtime 동봉)과 디스크를 비교해 불일치 시 재기동. 이 중 (a)가 단순하고 SSE 연결 정리도 자연스럽다.

### 2026-08-25T13:35:09+09:00 response (by sjpark1)

재작업 2 (사용자 피드백 반영) — (1) 선택 UX: 브라우저 prompt 2연타 대신 인라인 패널(모델 4택·effort 6택 버튼, 적용/닫기) — ?mpanel 진단 캡처로 자가 검증(스크린샷). (2) "수동 안내가 끝인가": 예 — 지금 세션들은 재시작 루프 이전에 시작돼 1회만 수동이 필요하다. 다만 안내 명령을 claude 직접 실행에서 bin/s9 code --resume 경유로 바꿔, 그 1회 이후에는 자동 재시작이 된다(문구에도 "1회 수동" 명시). (3) effort 미표시: CC가 트랜스크립트에 effort를 기록하지 않아 현재값을 읽을 소스가 없다 — 변경은 가능하되 표시는 불가(패널 안내문에 명시). (4) 리로드 시 내용 전환→고정: 첫 리로드가 막 스폰된 워커를 타깃으로 잡았다가 sticky로 고정된 것 — 타깃 규칙(REQ-015/020)의 정상 동작. 전체 173건 통과, 합동 커밋(워커 404 안내·계약 테스트 동승).

### 2026-08-25T13:45:15+09:00 response (by sjpark1)

재작업 3 — 계정 전환 UI 노출: ~/.claude-profiles/<이름>/ 디렉토리를 프로필로 나열(target API profiles), 패널에 account 행 추가(프로필 존재 시), 재시작에 CLAUDE_CONFIG_DIR 전환 동승. 프로필 부재 시 생성 방법을 패널 안내문에 표시. ?mpanel 캡처로 자가 검증. 커밋 a18e16e.

## History
- 2026-08-25T13:06:14+09:00 created by sjpark1 (status: open)
- 2026-08-25T13:07:16+09:00 status: open -> in-progress (by sjpark1) — s9 code 재시작 루프 + restart API + 모델 표시 구현 착수
- 2026-08-25T13:12:14+09:00 status: in-progress -> review (by sjpark1) — 확인 포인트(실사용 3건): (1) F5 후 터미널 상태줄 끝에 현재 모델(fable-5)이 보이고 클릭 시 변경 플로우가 뜨는지. (2) 지금 떠 있는 세션들은 재시작 루프 이전에 시작돼 "수동 재시작 필요+명령 안내"가 정상 — 자동 재시작을 보려면 새 터미널에서 s9 code로 세션을 하나 띄워 시험. (3) 유휴가 아닐 때(Thinking 중) 거부 사유가 뜨는지. 계정 전환은 ~/.claude-profiles/<이름>에서 최초 1회 로그인 후 사용 가능
- 2026-08-25T13:14:29+09:00 status: review -> in-progress (by sjpark1) — 반려: 확인 포인트 1번 실행부터 실패다. 그리고 이런건 사용자에게 검증 요청하기 전에 스스로 검증할 수 있는데까지 해야하잖아. 할수있잖아. [via dashboard]
- 2026-08-25T13:23:28+09:00 status: in-progress -> review (by sjpark1) — 확인 포인트(재시도): F5 후 터미널 상태줄 끝 모델 라벨 — 이제 어떤 경우에도 보인다(모델 미상이면 "model?"). 클릭 → model/effort 입력 → 지금 세션들은 "수동 재시작 필요+명령"이 정상(재시작 루프 이전 시작), 새 s9 code 세션은 자동 재시작. 리드 자가 검증 완료: 실브라우저 캡처 2매 + 169 테스트 — 이번엔 스크린샷 증거를 문서에 남겼다
- 2026-08-25T16:19:11+09:00 status: review -> done (by sjpark1) — [via dashboard]
