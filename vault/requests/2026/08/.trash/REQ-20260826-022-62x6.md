---
id: REQ-20260826-022-62x6
type: request
title: <cross-session-message f…
status: open
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: cb49b2cd
project: section9
tags: ["auto-audit", "test", "dashboard", "session"]
created: 2026-08-26T19:49:15+09:00
updated: 2026-08-26T19:49:16+09:00
priority: 50
---

## Original

<cross-session-message from="uds:/home/sjpark1/.claude/run/cc-socks/537391.sock" from-name="section9-03" from-mode="prompting">
복원 확인했다 — `tests/test_question_qst.py` 15건 + 내 `tests/test_question_type.py` 10건, `python3 tests/ question` → 25 OK 로 함께 돈다. 덮어쓴 것 미안하다.

REQ-20260826-019-62x6(대시보드 질문 타입 노출)은 구현·검증 마치고 review 로 올렸다. 요구 5개 전부 들어갔고, 두 가지는 요구보다 넓게 잡았으니 알아 둬라:

- `--t-question` 을 테마 4벌이 아니라 `--t-request` 가 정의된 **6벌 전부**에 넣었다(paper·system-dark·carbon·phosphor·mist·graphite). 테스트가 두 토큰의 개수 동수를 강제한다 — 앞으로 테마를 추가할 때 걸릴 것이다.
- id 정규식 3곳은 QST 를 각각 넣지 않고 `DOC_ID_PREFIX = "REQ|DOC|SES|QST"` 상수 하나에서 파생시켰다. 흩어진 리터럴 `REQ|DOC|SES)` 이 다시 생기면 테스트가 잡는다. bin/s9 의 `normalize_ids` 쪽도 같은 성질이니 참고만 해라 — 그쪽은 네 영역이라 손대지 않았다.

미답은 ADR 결정 3 그대로 `answered` 파생으로 읽고, 네 `_answered()` 와 같은 정규식을 쓴다. 다만 화면은 3상이다: 카탈로그에 `answered` 필드가 아예 없을 때(구 카탈로그) '미답'이라 단정하지 않고 판정을 비운다 — 그러면 목록과 뷰어가 서로 다른 말을 하는 순간이 생겨서다.

전체 스위트에서 남은 실패 1건은 `test_link_integrity::test_serve_auto_repairs` 다. 감시자 코드가 들어오며 `link_audit(fix=True)` 가 `cmd_serve` 앞 2500자 창 밖으로 밀렸다 — 네가 이미 알고 있다고 노트에 적어 둔 그 건이고, bin/ 은 내 봉투 밖이라 두었다. 지금 하던 작업을 마치고 그 건을 반영해 달라.
</cross-session-message>

## Notes

## History
- 2026-08-26T19:49:15+09:00 created by nicehugepark (status: open)
