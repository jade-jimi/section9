---
id: REQ-20260827-004-62x6
type: request
title: 같은 함정 둘이 더 있다
summary: resume·user 도 옵션 뒤 선택 위치 인자가 조용히 죽는다 (REQ-041 전수 조사)
goal: 기록 명령이 인자 순서와 무관하게 남는다
status: done
size: S
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: 61a6686b
sessions: ["8e60e4af", "61a6686b"]
project: section9
parent: REQ-20260826-041-62x6
tags: ["session", "workflow"]
created: 2026-08-27T00:13:26+09:00
status_since: 2026-08-27T09:16:14+09:00
updated: 2026-08-29T00:02:11+09:00
priority: 50
---

## Original

요구는 노트 참조. resume 은 무인 경로라 사람이 결과를 안 본다.

## Notes

### 2026-08-27T00:13:45+09:00 response (by nicehugepark)

REQ-20260826-041-62x6 을 닫으면서 "같은 함정이 다른 명령에도 있을 수 있다"를 남은 위험으로 적었다. 전수로 훑고 **실제로 눌러서** 확인했다 — 정적 조사만으로는 후보가 8개였는데, 돌려 보니 진짜는 2개였다.

**함정의 정확한 조건**은 "옵션 뒤 위치 인자"가 아니었다. argparse 는 **필수** 위치 인자는 옵션 건너 뒤에서도 붙여 준다(`s9 status <id> --note X in-progress` 는 멀쩡히 된다). 죽는 것은 **선택적 위치 인자(`nargs="?"`/`"*"`)가 값 받는 옵션 뒤에 올 때**다 — argparse 가 그 자리에서 "없음"으로 만족시켜 버리고, 뒤에 온 실제 값이 미아가 된다.

**실제로 죽는 것 둘** (직접 실행해 확인):

    s9 resume --model m <SID> "이어서 해"
      → s9: error: unrecognized arguments: --model 이어서 해
    s9 user --json config tester
      → s9: error: unrecognized arguments: --json

**멀쩡한 것들**: `status` · `bind` · `snapshot` · `instance` · `project` — 정적 조사에서는 후보였지만 실제로는 붙는다. 조건이 위와 같기 때문이다.

`resume` 이 특히 나쁘다. 무인 재개 경로라 **사람이 결과를 안 보는 자리**이고, 041 에서 배운 그대로 실패 메시지가 입력을 되비추므로 로그만 봐서는 성공과 구분되지 않는다.

**요구**: `resume`·`user` 를 041 과 같은 방식(argparse 이전 인자 정규화)으로 막는다. 그리고 이번처럼 **새 서브명령을 추가할 때 이 조건이 다시 생기지 않도록** 계약 테스트로 박는다 — 파서를 훑어 "값 받는 옵션 + 선택적 후행 위치 인자" 조합이 정규화 목록에 없으면 실패하게. 사람이 매번 기억할 일이 아니다.

### 2026-08-29T00:02:11+09:00 response (by nicehugepark)

승인 후속 점검(리드). 메모는 질문이 아니라 판정이었고, 후속 작업을 지시하지도 함의하지도 않는다 — 새 REQ를 만들지 않았다.

승인 이후에도 고침이 살아 있는지 이 워크트리에서 다시 확인했다: `python3 tests/ note_argorder` 7/7 통과. A6(resume·user 의 옵션 뒤 위치 인자)와 A7(정규화가 손으로 관리하는 목록이 아니라 파서에게 묻는다)이 모두 초록이다. bin/s9:13363-13387 의 정규화는 서브파서를 그때그때 읽어 "값 받는 옵션 + 선택적 후행 위치 인자" 조합에만 붙으므로, 메모가 적은 대로 새 서브명령도 자동으로 덮인다.

곁가지 확인 하나: 이 노트를 처음 `--session` 을 붙여 쓰려다 `unrecognized arguments: --session` 으로 죽었다 — 041/004 가 고친 조용한 실패와 달리, 모르는 옵션은 여전히 시끄럽게 죽는다(종료 코드 2). 고침이 미지 옵션까지 삼켜 버리지 않는다는 뜻이라 그대로 두는 것이 맞다.

코드 변경 없음 — 커밋할 것이 없다.

## History
- 2026-08-27T00:13:26+09:00 created by nicehugepark (status: open)
- 2026-08-27T00:17:27+09:00 status: open -> in-progress (by nicehugepark) — 리드 착수 — bin/s9 가 지금 비었다
- 2026-08-27T01:06:59+09:00 status: in-progress -> review (by nicehugepark) — 기록 명령이 인자 순서와 무관하게 남는다. 041 을 고친 뒤 같은 함정이 다른 명령에도 있는지 전수로 훑었더니 둘이 더 있었다 — 특히 무인 재개 경로가 그랬는데, 거기는 사람이 결과를 안 보는 자리라 실패해도 아무도 몰랐다. 대상 목록을 손으로 관리하지 않고 명령 정의에서 스스로 알아내게 했다 — 새 명령이 추가돼도 자동으로 덮인다. 확인이 필요하면 터미널에서 `bin/s9 note <아무 문서 id> --label response "테스트"` 를 쳐 보라. 예전에는 이 순서가 조용히 실패했다.
- 2026-08-27T09:16:14+09:00 status: review -> done (by nicehugepark) — 리드 판정. CLI 내부 동작이라 사용자가 확인할 자리가 없다. 실행 확인: s9 resume --cwd /tmp <SID> "이어서" 가 이제 통과한다(전에는 unrecognized arguments 로 죽었다). 대상 목록을 손으로 관리하지 않고 파서에게 물으므로 새 명령도 자동으로 덮인다.
