---
id: REQ-20260828-014-62x6
type: request
title: 재시작 후 터미널 no session
summary: 컴퓨터 재시작 후 s9 code로 세션을 띄워도 대시보드 터미널이 라이브 세션을 못 잡고 no session 상태에 머문다
status: in-progress
size: M
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: b8fda8f9
project: section9
relates_why: {}
tags: ["session", "terminal", "dashboard"]
created: 2026-08-28T11:35:14+09:00
status_since: 2026-08-28T11:35:20+09:00
updated: 2026-08-28T11:56:12+09:00
priority: 50
---

## Original

재현: 컴퓨터 재시작 → ~/section9/bin/s9 code 실행(세션 기동, inbox tail arm 완료) → 대시보드 Terminal 탭을 열면 "라이브 클로드 세션이 없습니다" + 입력줄 "no live session · no session · waiting". 화면에서 [여기서 세션 깨우기]를 누르면 새 터미널 창이 하나 더 열려 세션이 또 기동되고 그 세션도 inbox tail 을 arm 하지만, 대시보드는 여전히 no session 이다. 즉 이미 살아있는 세션 2개(inbox tail 프로세스 확인됨)를 대시보드가 인식하지 못한다.

## Notes

### 2026-08-28T11:38:35+09:00 plan (by nicehugepark)

원인: 전역 ~/.claude/settings.json 의 section9 훅 8개가 전부 사라진 워크트리 경로(/home/sjpark1/section9/state/worktrees/probe/bin/…)를 가리킨다. 훅 명령은 `2>/dev/null || true` 로 실패를 삼키므로 조용한 no-op — SessionStart 가 세션 바인딩을 안 써서 대시보드가 살아있는 세션을 못 본다(실제로 b8fda8f9 바인딩은 s9 new request 의 claim 이 만들 때까지 없었고, 1bb25630 은 아직도 없다). 프롬프트 audit·응답 기록도 같이 죽어 있었다.

자가 치유(cmd_code preflight)가 안 돈 이유: hooks_installed() 가 settings.json 을 **문자열 포함**으로 본다 — `"s9-audit-prompt" in txt and ROOT in txt`. 죽은 워크트리 경로는 ROOT(/home/sjpark1/section9)를 부분문자열로 품고 있어 "설치됨"으로 읽혔다. 방어는 있었는데 눈이 문자열이었다.

TDD 시나리오
- N1: settings.json 훅이 실재하는 <ROOT>/bin/s9-audit-prompt 를 가리키면 hooks_installed() == True.
- B1: 훅 경로가 ROOT 하위이지만 **없는 파일**(지워진 워크트리 잔재)이면 hooks_installed() == False → cmd_code preflight 가 재설치한다.
- B2: 살아있는 훅이 하나도 없는 빈 프로필이면 False (기존 계약 유지).
- B3: 링크된 git 워크트리의 bin/s9-install 을 실행해도 settings.json 에는 **메인 저장소** 경로가 적힌다 (worktree 경로 재발 차단).

### 2026-08-28T11:56:12+09:00 response (by nicehugepark)

고침 (테스트 먼저, python3 tests/ hook_target_alive)

1) bin/s9 `hooks_installed()` — settings.json 을 문자열이 아니라 JSON 으로 읽고, section9 훅(s9-audit-*)의 스크립트 경로를 실제로 열어 본다. 죽은 훅이 하나라도 있으면 미설치로 친다(재설치가 이름으로 소유를 알아보고 잔재까지 걷어가므로 한 번에 낫는다). 살아 있어도 남의 워크스페이스 경로면 이 ROOT 는 미설치. 부수 함수 `hook_script_path()` 추가.
2) bin/s9-install `main_worktree_root()` — 링크된 git 워크트리에서 실행돼도 메인 저장소 경로를 심는다(`git rev-parse --git-common-dir` 의 부모). 전역 settings.json 은 워크트리보다 오래 산다. S9_ROOT 명시는 그대로 존중.
3) 살아있는 환경 복구: bin/s9-install 재실행 → 훅 8개 전부 /home/sjpark1/section9/bin/ 으로 교체(중복 없음), hooks_installed()==True.

검증
- 훅 실체 검증: SessionStart 훅에 가짜 세션 payload 를 넣어 실행 → 바인딩(entry=code, attach_pid) 정상 생성 확인. 흔적(바인딩·수신함·SES 문서)은 즉시 회수.
- 자가 치유 종단 검증: 죽은 워크트리 경로만 있는 임시 CLAUDE_CONFIG_DIR 로 `s9 code` 실행 → "훅 미설치 감지 → s9-install" 이 돌고 훅 8개가 살아 있는 경로로 재설치됨.
- /api/chat/target 이 이 세션을 live/listening 으로 보고, 대시보드 Terminal 입력줄이 "no live session" 대신 활성.
- 전체 스위트 1323건 통과. 기존 픽스처 3개(test_profile_hooks·test_code_bootstrap·test_s9_code_args)가 쓰던 가짜 settings.json 모양(`{"note": "s9-audit-prompt <root>"}`)을 실제 훅 블록 + 실재 스크립트로 교체 — 옛 계약(문자열 포함)을 그대로 두면 이 결함을 다시 못 잡는다.

부수 발견: 세션 3da5b9de 수신함에 미전달 채팅 3건이 갇혀 있었다(08:30 워크트리 검토와 구현 / 08:36 세션 껐다켜기·시크릿 기능이 안 보인다 / 08:45 스트림을 세션에서 쓸지 말지). 훅이 죽어 그 세션이 수신 대기를 못 돌린 결과다.

## History
- 2026-08-28T11:35:14+09:00 created by nicehugepark (status: open)
- 2026-08-28T11:35:20+09:00 status: open -> in-progress (by nicehugepark) — 재시작 후 라이브 세션 미인식 원인 조사 착수
