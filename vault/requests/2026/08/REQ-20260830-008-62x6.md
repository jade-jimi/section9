---
id: REQ-20260830-008-62x6
type: request
title: 맨 엔터가 세우기에 닿는다
summary: 세우기 확인 창의 기본 엔터가 그만두기가 아니라 세우기에 떨어진다
goal: 잃는 것이 있는 확인 창의 맨 Enter 가 물러나는 쪽에 서고, 그 규칙이 전수 계약 시험으로 지켜진다
status: done
size: S
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: 9854e394
sessions: ["a5a0449c", "9854e394"]
agents: ["sub:frontend-developer:ab5cedcf"]
contributions: [{"actor": "sub:frontend-developer:ab5cedcf", "item": "008 세우기 창 기본 초점", "started": "2026-08-30T17:12:31+09:00", "ended": "2026-08-30T17:35:38+09:00", "result": "stalled", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/9854e394-1479-4180-9021-a3ed4bdae40f/tasks/ab5cedcf7a24cf364.output", "reason": "204초째 진전 없음 (기준 180초)"}]
project: section9
relates: ["REQ-20260830-007-62x6", "DOC-20260830-004-62x6"]
relates_why: {"REQ-20260830-007-62x6": "이 창의 문구를 다시 쓰다 발견한 인접 결함이다 — 글자가 아니라 기본 초점의 문제라 따로 뗐다", "DOC-20260830-004-62x6": "이 요청에서 세운 초점 규칙과 그것을 지키는 대장 시험을 지식으로 남긴 것"}
tags: ["dashboard", "attached", "png", "image"]
created: 2026-08-30T11:31:36+09:00
status_since: 2026-08-30T17:49:45+09:00
updated: 2026-08-30T18:27:00+09:00
priority: 50
---

## Original

세우기 확인 창(`?work=12&dlg=stopask`)은 바닥이 「그대로 두기 / 세우기」인데 `safe` 가 설정돼 있지 않아 맨 Enter 가 「세우기」에 닿는다. 창을 읽지 않고 Enter 를 치는 손이 도는 작업을 세운다.

세우기는 되돌릴 수 없는 일은 아니다 — 카드가 멈춤으로 바뀐 뒤 깨우기로 되살릴 수 있다. 그래서 REQ-20260830-007(문구 재작성) 범위에서는 손대지 않고 여기로 뗐다. 다만 "되살릴 수 있다"와 "실수로 눌러도 괜찮다"는 다른 말이다: 그 사이에 도는 작업이 하던 일을 잃는다.

고칠 자리: `s9dlg` 의 `safe` 를 세워 기본 초점을 「그대로 두기」에 두면 된다. 같은 문법을 쓰는 다른 확인 창들이 어느 쪽에 두고 있는지 함께 훑어라 — 한 창만 다르면 그 차이가 곧 손의 습관을 배신한다.

## Notes

### 2026-08-30T17:14:59+09:00 tdd (by nicehugepark)

### TDD 시나리오 (test_dialog_safe.py — test_dialog_voice.py 의 정적 계약 형식)

기준 하나: **되돌려도 그 사이에 잃는 것이 있으면 물러나는 쪽에서 시작한다.**
파괴적(되돌릴 수 없는 소실) 또는 중단적(도는 일을 끊음) → `safe: true`.
되돌리면 원상복구고 잃는 것이 없으면 → 주 행동에 초점.

- [ ] S1 (정상·전수): web/app/*.js 의 `kind:"confirm"` 창을 전부 뽑아 (파일·ok·cancel)
      키로 대장(census)과 맞춘다 — 대장에 없는 창이 하나라도 있으면 실패.
      새 확인 창을 만드는 사람이 초점을 **정하지 않고는** 지나갈 수 없다.
- [ ] S2 (정상): 대장이 safe 로 표시한 창은 실제로 `safe: true` 를 들고 있고,
      아닌 창은 들고 있지 않다.
- [ ] S3 (경계): `safe` 는 confirm 에만 선다. alert 은 물러날 버튼이 없고
      prompt 는 초점이 입력 상자로 가므로, 거기 붙은 safe 는 지키지 못할 약속이다.
- [ ] S4 (회귀): 세우기 창(card.js `ok: STOP_LABEL` / `cancel: "그대로 두기"`)이
      safe 를 들고 있다 — 맨 Enter 가 「중단하기」에 닿던 그 자리.
- [ ] S5 (회귀·거울): 진단 주소가 세우는 거울 창(diag.js `shapes`)도 같은 대장을
      따른다 — 거울만 옛 초점이면 캡처가 거짓을 증언한다.
- [ ] S6 (화면): `?work=12&dlg=stopask` 캡처에서 초점 테두리가 「그대로 두기」에,
      바닥 힌트가 `Enter 로 그대로 두기` 로 서 있다.

전수 훑기 결과(원칙에 어긋나 함께 세울 창): userform.js 비밀 지우기(되살릴 수 없음),
tidy.js 영구 삭제(되돌릴 수 없음), restart.js 두 창(도는 작업 중단), diag.js busy 거울.
그대로 두는 창: app.js 멤버 빼기(문서는 남고 다시 넣으면 원상복구), card.js 취소하기·
tidy.js 휴지통으로(되돌리면 그대로), userform.js 그래도 넣기(넣을 뿐).

### 2026-08-30T17:22:20+09:00 response (by nicehugepark)

### 세우기 창의 맨 Enter 가 「그대로 두기」에 선다

`safe: true` 를 세웠다. 낱말은 그대로다 — 초점만 옮겼다. 바닥 힌트도 같은 깃발을
읽으므로 `Enter 로 그대로 두기` 로 함께 바뀐다(dialog.js 가 이미 그렇게 짓고 있었다).

### 같은 문법을 쓰는 확인 창 전수 (13개)

기준 하나로 갈랐다 — **되돌려도 그 사이에 잃는 것이 있으면 물러나는 쪽에서
시작한다**. 파괴적(되돌릴 수 없는 소실)이거나 중단적(도는 일을 끊음)이면 safe,
되돌리면 원상복구고 잃는 것이 없으면 주 행동에 둔다.

| 창 | 자리 | 바닥 | 전 | 후 | 왜 |
|---|---|---|---|---|---|
| 진행 중인 자동 작업 중단 | card.js:648 | 그대로 두기 / 중단하기 | 중단하기 | **그대로 두기** | 되살려도 하던 일은 잃는다 (이 REQ) |
| 세션이 일하는 중 | restart.js:77 | 그대로 두기 / 중단하고 바꾸기 | 중단하고 | **그대로 두기** | 이 세션의 하던 일을 끊는다 |
| 자동 작업 N건 중단 | restart.js:151 | 그만두기 / 중단하고 바꾸기 | 중단하고 | **그만두기** | 한 번에 여러 건을 끊는다 |
| 영구 삭제 | tidy.js:293 | 그만두기 / 영구 삭제 | 영구 삭제 | **그만두기** | 제목이 스스로 적는다 — 되돌릴 수 없다 |
| 비밀 키 삭제 | userform.js:445 | 그만두기 / 지우기 | 지우기 | **그만두기** | 지운 값은 되살릴 수 없다 |
| 세션 중단 거울 | diag.js busy | 그대로 두기 / 멈추고 바꾸기 | 멈추고 | **그대로 두기** | 위 창의 진단 거울 |
| 빈 계정 자리 지우기 | session.js:333 · diag.js acctrm | 그만두기 / 지우기 | 그만두기 | 그대로 | 이미 서 있던 유일한 창 |
| 멤버 빼기 | app.js:257 | 그만두기 / 멤버 빼기 | 멤버 빼기 | 그대로 | 문서는 남고, 다시 넣으면 원상복구 |
| cancelled 로 옮기기 | card.js:939 · diag.js cancel | 그만두기 / 취소하기 | 취소하기 | 그대로 | 창이 적는다 — 다시 옮기면 된다 |
| 휴지통으로 | tidy.js:320 | 그만두기 / 휴지통으로 | 휴지통으로 | 그대로 | 휴지통에서 되돌린다 |
| 가려진 키에 그래도 넣기 | userform.js:390 | 그만두기 / 그래도 넣기 | 그래도 넣기 | 그대로 | 값을 넣을 뿐이고 넣어도 쓰이지 않는다 |

여섯을 고치고 다섯을 그대로 두었다. **safe 가 서 있던 창은 열셋 중 둘뿐**이었다 —
깃발이 창마다 손으로 켜지는 것이라 새 창을 만드는 사람이 있는 줄도 몰랐다.

### 다음 창이 또 빠지지 않게

`tests/test_dialog_safe.py` (test_dialog_voice.py 의 정적 계약 형식). 확인 창을
소스에서 전수로 뽑아 **대장(CENSUS)** 과 맞춘다: 대장에 없는 창이 하나라도 있으면
실패한다 — 초점을 정하지 않고는 새 확인 창이 못 지난다. safe 가 confirm 밖(alert·
prompt)에 붙는 것도 막는다(그 종류는 초점을 그리로 옮기지 않으므로 지킬 수 없는
약속이다). 6 tests OK.

### 곁가지 (고치지 않음 — 낱말은 이 REQ 밖이다)

`diag.js` 의 `busy` 거울이 아직 「멈추고 바꾸기」다. 본 창(restart.js)은
REQ-20260830-007-62x6 에서 「중단하고 바꾸기」로 바뀌었는데 거울만 옛 낱말로 남았다.
글자가 두 벌이라 한 벌만 고쳐진 자리다.

캡처: ledger 라이트 · carbon 다크 · 진단 거울(busy). 셋 다 초점 테두리가 물러나는
버튼에 서고 바닥이 `Enter 로 그대로 두기` 로 적는다.

[Image: assets/REQ-20260830-008-62x6/stopask-zoom.png]
[Image: assets/REQ-20260830-008-62x6/stopask-carbon.png]
[Image: assets/REQ-20260830-008-62x6/dlg-busy.png]

### 2026-08-30T17:22:30+09:00 tdd (by nicehugepark)

### TDD 시나리오 — 결과

- [x] S1 전수 대장 대조 (test_f1) — 확인 창 13개가 모두 대장에 있다
- [x] S2 대장대로 초점 (test_f2)
- [x] S3 safe 는 confirm 에만 (test_f3)
- [x] S4 세우기 창 회귀 (test_f4) — Red 확인 후 Green
- [x] S5 진단 거울도 같은 대장 (test_f1·f2 가 diag.js 를 함께 읽는다)
- [x] S6 캡처로 초점·힌트 확인 (response 노트의 그림 셋)

Red: `card.js: 「STOP_LABEL」 창이 물러나는 쪽에서 시작하지 않는다` (2 failures)
Green: `python3 tests/ dialog_safe` → Ran 6 tests, OK
회귀: dialog(42) · restart_reach(15) · stall_pair(18) · session_restart(21) ·
judge_attach(30) 전부 OK

### 2026-08-30T17:31:52+09:00 response (by nicehugepark)

### 시험 결과 (전체 실행)

- `python3 tests/ dialog_safe` → Ran 6, OK (exit 0)
- 직접 닿는 스위트 전부 OK: dialog(42) · restart_reach(15) · stall_pair(18) ·
  session_restart(21) · judge_attach(30) · dead_end(19) · secret_where(23)
- `python3 tests/ --jobs 4` (전체 228파일) → exit 1, **실패 1건**:
  `test_port_pool.NoEphemeralBind` 가 `test_doctor_system.py:279` 이 임시 포트를
  연다고 잡았다.

그 실패는 **이 작업과 무관하다.** 내가 만진 것은 web/app 의 JS 다섯 파일과
`tests/test_dialog_safe.py` 하나뿐이고, test_doctor_system.py 는 건드리지 않았다
(다른 작업자가 지금 잡고 있는 파일이다). 새로 넣은 시험은 소켓을 열지 않는다 —
소스를 읽어 문자열만 견주는 정적 계약이라 서버도 포트도 쓰지 않는다.

### 2026-08-30T17:48:49+09:00 commit (by nicehugepark)

커밋 96fb4c8 — 오후의 판 — 낱말이 결과를 말하고, Enter 가 안전한 쪽에 서고, calm 이 숨을 쉰다

### 2026-08-30T18:27:00+09:00 response (by nicehugepark)

곁가지 반영(리드): diag.js 의 busy 진단 거울이 아직 「멈추고 바꾸기」였다 — 본 창(REQ-20260830-007-62x6)과 같은 「중단하고 바꾸기」로 맞춤. 거울은 본 창을 비춰야 진단이 참말이 된다. dialog·restart 표적 green

## History
- 2026-08-30T11:31:36+09:00 created by nicehugepark (status: open)
- 2026-08-30T17:11:21+09:00 status: open -> in-progress (by nicehugepark) — drag 이동 [via dashboard]
- 2026-08-30T17:49:45+09:00 status: in-progress -> done (by nicehugepark) — goal 충족 — 확인 창 13개 전수 중 잃는 것이 있는 6창(세우기·재시작 2종·영구 삭제·비밀 키 삭제·진단 거울)의 safe 를 세웠고, test_dialog_safe 의 전수 대장(CENSUS)이 새 창 누락까지 계약으로 문다(Red→Green 확인). 4스킨 캡처·표적 174건 green, 전체 --jobs final exit=0 커밋 96fb4c8. 기계 검증이라 리드가 닫는다
