---
id: REQ-20260829-034-62x6
type: request
title: 클레임이 멈춤 시계를 되감는다
summary: 깨운 워커가 클레임만 하고 죽으면 updated 가 갱신돼 그 카드는 15분간 다시 못 깨운다
goal: 클레임 한 줄이 진전으로 세어지지 않아, 한 일이 없는 문서의 멈춤 경보가 꺼지지 않는다
status: done
size: M
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: 911949e0
agents: ["sub:backend-developer:aa5cccf1"]
contributions: [{"actor": "sub:backend-developer:aa5cccf1", "item": "033·034 클레임 수명 구현", "started": "2026-08-29T20:07:50+09:00", "ended": "2026-08-29T20:20:57+09:00", "result": "stalled", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/911949e0-5d1b-4cd2-b08d-70fd0415a19a/tasks/aa5cccf14c7b52d44.output", "reason": "210초째 진전 없음 (기준 180초)"}]
project: section9
parent: REQ-20260828-041-62x6
relates_why: {}
tags: ["workflow", "dashboard", "session"]
created: 2026-08-29T18:25:56+09:00
status_since: 2026-08-29T20:49:49+09:00
updated: 2026-08-29T20:49:49+09:00
priority: 50
---

## Original

실측 2026-08-29 17:36~ (REQ-20260828-041 라운드2). 멈춤 판정 stall_mins() 의 시계는 문서의 updated 하나다. 그런데 클레임(stamp_doc_session)은 문서를 쓰면서 updated 를 갱신한다.

그래서 이런 순서가 성립한다: ① 사람이 깨운다 → ② 워커가 뜨자마자 프롬프트 지시대로 클레임한다(updated 갱신) → ③ 워커가 아무것도 못 하고 죽는다 → ④ 카드는 15분 동안 "아직 멈춘 게 아니다 — 방금 문서가 움직였다"로 답한다. **한 일이 없는데 경보만 꺼진다.**

라운드2가 좁혀 놓은 자리와 이어진다. spawn_failed(pid 사망)로 판정되는 동안은 잣대가 STALLED_DEAD_WIN(180초)로 좁혀지므로 3분 뒤 다시 누를 수 있다. 그러나 그 판정은 스폰 마커의 창(SPAWN_WIN 600초) 안에서만 서고, 워커가 클레임 후 마커 창 밖에서 조용히 죽으면 다시 updated 잣대로 돌아간다.

고칠 방향 후보(둘 다 트레이드오프가 있다):
① 진전의 시계를 updated 가 아니라 "실질 진전"(노트·전이·기여 결과 변화)으로 좁힌다 — 클레임 한 줄은 진전이 아니다. 대신 판정이 무거워지고 축이 하나 더 는다.
② 클레임이 updated 를 건드리지 않게 한다(문서는 쓰되 시계는 그대로). 단순하지만 updated 의 뜻이 "문서가 바뀐 시각"에서 갈라진다 — 다른 화면과 정렬이 함께 달라진다.

어느 쪽이든 stall_mins 한 자리에서 끝나야 한다. 판정이 두 벌이 되면 이 REQ 가 되풀이해 겪은 그 어긋남(CLI 는 멈췄다, 화면은 돈다)이 다시 난다.

## Notes

### 2026-08-29T20:11:30+09:00 tdd (by nicehugepark)

### TDD 시나리오 (클레임과 멈춤 시계)

실행: `python3 tests/ claim_clock`

- [ ] C1. 클레임은 시계를 되감지 않는다: `updated` 가 20분 전인 in-progress 문서를 새 세션이 `stamp_doc_session` 으로 클레임 → `updated` 그대로.
- [ ] C2. 그래도 도장은 찍힌다: 같은 호출 뒤 `session` 이 새 세션이고 `sessions` 에 옛 세션이 남는다.
- [ ] C3. 실사고 재현: 20분 조용한 문서를 워커가 클레임한 직후 `stall_mins` 가 여전히 20분(멈춤)을 답한다 — 오늘은 None(안 멈춤)을 답한다.
- [ ] C4. 판정은 한 자리: 같은 문서에 대해 화면(`catalog_with_live` 의 `stalled_mins`)·CLI(`stalled_requests`)·깨우기(`wake_request` 의 거부/허용)가 어긋나지 않는다.
- [ ] C5. 진짜 진전은 시계를 움직인다: 노트를 붙이면(`updated` 갱신) `stall_mins` 가 None.
- [ ] C6. 멱등: 같은 세션의 반복 클레임은 문서를 다시 쓰지 않는다(기존 동작 보존).
- [ ] C7. 경계: 도장 없는 옛 문서·빈 세션 문자열에서 아무것도 깨지지 않는다.

### 2026-08-29T20:27:07+09:00 backend-developer (by nicehugepark)

### 고른 길: 후보 ② — 클레임이 진전의 시계를 건드리지 않는다

`bin/s9:stamp_doc_session()` 에서 `meta["updated"] = now_iso()` 한 줄을 뺐다. 도장(session/sessions)은 그대로 찍히고 문서도 쓰이지만 진전의 시계는 서지 않는다.

**후보 ①(진전의 시계를 "실질 진전"으로 좁힌다)을 버린 이유는 실패 방향이다.** ① 은 판정에 축을 하나 더 세우는데, 그 축이 어느 쓰기 자리 하나를 빠뜨리면 **일하는 문서가 멈춤으로 그려진다** — 그 카드의 깨우기가 곧 일하는 손 위의 두 번째 손이고, 이 저장소가 하루에 네 번 값을 치른 그 사고다(REQ-20260826-021-62x6). ② 는 쓰기 자리 **하나에서 bump 를 빼는 것**이라, 손대지 않은 모든 경로가 지금 동작(넓은 쪽 = 안전한 쪽)을 그대로 지킨다. 위험 표면이 축 하나 대 줄 하나다.

문서에 적힌 ② 의 대가("updated 의 뜻이 갈린다")는 실측해 보니 값이 거의 없었다. 이 저장소에서 `updated` 는 이미 파일 mtime 이 아니라 **진전의 시계**로만 읽힌다 — `stall_mins()` 가 그것 하나로 판정하고, 전수 확인한 `updated` 쓰기 자리 14곳 중 진전이 아닌 것은 `stamp_doc_session` 하나뿐이었다(나머지는 전이·노트·첨부·관계 수정·멤버 변경). ETag/캐시 무효화에 쓰는 자리는 없다(`asset_etag` 는 파일 해시). 클레임 시각도 잃지 않는다: 세션 바인딩의 `claim_at` 에 그대로 남는다(REQ-20260828-005-62x6).

### 판정은 한 자리인가 — 확인함

화면 `catalog_with_live()` → 행마다 `stalled_mins` / CLI `stalled_requests()` → 그 행을 읽어 거를 뿐 / 깨우기 `wake_request()` → 같은 `catalog_with_live(stall_win=win)` 행을 찾아 `stalled_mins` 를 읽는다. **셋 다 `stall_mins()` 하나를 지난다** — 모을 것이 없었다. 시험 C4/C4b 가 세 자리의 답이 어긋나지 않는 것을 잠근다(멈춤일 때 손잡이가 "moving" 으로 거절하지 않고, 안 멈췄을 때 셋 다 거절한다).

### 시험 — tests/test_claim_clock.py (신규 10건)

Red 4건 확인 후 구현. C1 클레임이 `updated` 를 안 건드린다 / C2 그래도 도장은 찍힌다 / C3 **실사고 재현**: 20분 조용한 문서를 워커가 클레임한 직후에도 `stall_mins` 가 20분(고치기 전엔 None) / C4·C4b 화면·CLI·깨우기 일치 / C5·C5b 노트·전이는 여전히 시계를 움직인다 / C6 같은 세션 반복 클레임은 문서를 다시 쓰지 않는다 / C7·C7b 도장 없는 옛 문서·빈 세션 안전, 클레임 시각은 `claim_at` 에 남는다.

`python3 tests/ claim_clock` → Ran 10, OK.

### 전체 스위트

`python3 tests/` → **Ran 2131 in 391s, failures=6, skipped=1**. 실패 6건은 전부 `web/index.html` 몫이고 내 변경과 무관하다 — test_stall_pair 4건은 node 로 뽑아 돌리는 조각 밖에 `wsChip()` 이 정의돼 생기는 `ReferenceError`, test_wake_handle 2건은 `wakeDoc()` 이 `stop: false`·`title: d.message` 를 잃은 것. 둘 다 지금 화면을 고치고 있는 쪽의 작업 중 상태이며, 실패 지점이 전부 index.html 문자열 검사라 `bin/s9` 는 지나가지 않는다.

### 못 한 것

`web/index.html` 은 규율대로 손대지 않았다 — 화면 쪽 6건은 그 담당이 닫아야 한다.

### 2026-08-29T20:28:18+09:00 tdd (by nicehugepark)

### TDD 시나리오 — 전부 초록 (python3 tests/ claim_clock → Ran 10, OK)

- [x] C1. 클레임이 `updated` 를 되감지 않는다
- [x] C2. 그래도 승계 도장(session/sessions)은 찍힌다
- [x] C3. 실사고 재현 — 20분 조용한 문서를 워커가 클레임한 직후에도 멈춤 20분 (고치기 전엔 None)
- [x] C4. 화면·CLI·깨우기가 같은 답 (멈춤일 때 손잡이가 moving 으로 거절하지 않고, 안 멈췄을 때 셋 다 거절)
- [x] C5. 노트·전이는 여전히 시계를 움직인다
- [x] C6. 같은 세션의 반복 클레임은 문서를 다시 쓰지 않는다
- [x] C7. 도장 없는 옛 문서·빈 세션에서 안전하고, 클레임 시각은 바인딩 `claim_at` 에 남는다

### 2026-08-29T20:49:29+09:00 commit (by nicehugepark)

커밋 ffb5257 — 클레임이 시계를 되감지 않고, 자리가 사람에게 보인다 (REQ-033·034·030·031·035)

## History
- 2026-08-29T18:25:56+09:00 created by nicehugepark (status: open)
- 2026-08-29T20:06:55+09:00 status: open -> in-progress (by nicehugepark) — drag 이동 [via dashboard]
- 2026-08-29T20:49:49+09:00 status: in-progress -> done (by nicehugepark) — 충족 근거: 커밋 ffb5257, 전체 2158건 OK. 실사고를 재현하는 계약(claim_clock C3: 20분 조용한 문서를 클레임한 직후 stall_mins 가 고치기 전 None → 지금 20)이 green 이고, 라이브 문서 REQ-041 의 잔여 도장을 실제 명령으로 풀어 updated 가 18:59:15 그대로임을 확인했다.
