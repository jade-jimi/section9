---
id: REQ-20260902-003-62x6
type: request
title: 문지기가 글을 명령으로 읽는다
summary: PreToolUse 훅 s9-git-gate 가 명령줄 전체를 정규식으로 훑어, 따옴표·heredoc 안의 글에 적힌 명령 이름까지 실행으로 판정해 거부한다. 위임 에이전트의 노트 작성이 두 번 막혔고 산출물이 유실됐다
goal: 따옴표·heredoc 안의 글에 파괴 명령 이름이 적혀 있어도 통과하고, 실제로 실행되는 파괴 명령은 그대로 거부한다 — 둘 다 시험이 판정한다
status: done
size: S
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: a05fe599
agents: ["lead:claude-opus-5"]
contributions: [{"actor": "lead:claude-opus-5", "item": "TDD 시나리오 — 문지기는 글이 아니라 명령을 읽는다", "started": "2026-09-02T07:23:15+09:00", "ended": "2026-09-02T07:23:15+09:00", "result": "done", "transcript": ""}, {"actor": "lead:claude-opus-5", "item": "TDD 시나리오 (최종 — 통과분 체크)", "started": "2026-09-02T07:32:03+09:00", "ended": "2026-09-02T07:32:03+09:00", "result": "done", "transcript": ""}, {"actor": "lead:claude-opus-5", "item": "고친 것 — 게이트가 「누가 받느냐」로 가른다", "started": "2026-09-02T07:32:03+09:00", "ended": "2026-09-02T07:32:03+09:00", "result": "done", "transcript": ""}]
project: section9
derived_from: REQ-20260902-002-62x6
relates_why: {}
tags: ["docs", "audit", "sync"]
created: 2026-09-02T07:18:36+09:00
status_since: 2026-09-02T07:32:53+09:00
updated: 2026-09-02T07:32:53+09:00
priority: 50
---

## Original

2026-09-01 REQ-20260901-023 노트를 쓸 때 처음 걸렸고(그때 "따로 볼 일이다"로 미뤘다), 2026-09-02 REQ-20260902-002 용어 재판정을 위임한 tech-writer 가 같은 자리에서 또 막혀 판정 산출물을 잃었다. 리드가 이 요청을 등록하려 할 때도 같은 자리에서 막혔다 — 결함이 제 손으로 세 번째 재현됐다.

막아야 할 것을 막는 문은 옳지만, 그 문이 글을 막으면 사람은 문을 우회하는 버릇을 들인다. 실제로 에이전트가 우회를 시도했다.

뿌리: 게이트가 명령줄 **문자열 전체**에 정규식을 댄다. 따옴표 안의 인자도, heredoc 본문의 글도 명령과 구분하지 않는다. 재는 것은 "이 문자열에 그 낱말이 있는가"이지 "그 명령이 실행되는가"가 아니다.

## Notes

### 2026-09-02T07:23:15+09:00 tdd (agent: lead:claude-opus-5, by nicehugepark)

### TDD 시나리오 — 문지기는 글이 아니라 명령을 읽는다

시험: tests/test_git_gate.py · 실행 `python3 tests/ git_gate`

뿌리: 게이트가 명령줄 **문자열 전체**에 정규식을 댄다. 따옴표 안의 인자도,
heredoc 본문의 글도 실행되는 명령과 구분하지 않는다. 재는 것이
"이 문자열에 그 낱말이 있는가"이지 "그 명령이 실행되는가"가 아니다.

판정 기준 한 줄: **그 글자를 누가 받느냐.** 셸·인터프리터(bash·sh·eval·
python·node…)가 받으면 그것은 명령이니 그대로 재고, 그 밖의 명령이 받으면
그것은 자료(글)이니 재지 않는다.

### 정상 — 글은 지나간다
- [ ] P1. `s9 note` 의 heredoc 본문에 파괴 명령 이름이 적혀 있어도 통과한다
      (실제 사고 재현: 위임 에이전트의 판정 노트, 리드의 REQ 등록).
- [ ] P2. 따옴표 안 인자에 그 낱말이 있어도 통과한다
      (`bin/s9 note X "... 그 명령 금지 ..."`).
- [ ] P3. `--body "$(cat <<'EOF' … EOF)"` 처럼 명령 치환 안의 heredoc 도 같다.

### 경계 — 받는 쪽이 인터프리터면 그대로 잰다
- [ ] B1. `bash -c "git stash"` 는 여전히 거부된다 — 따옴표가 방패가 되면
      게이트는 한 줄로 무력해진다.
- [ ] B2. `python3 - <<'PY' … git stash … PY` 도 여전히 거부된다.
- [ ] B3. 인용부호가 안 닫힌 명령(파싱 불가)은 옛 판정으로 되돌아간다 —
      모르는 것을 통과시키지 않는다.

### 실패 — 막을 것은 그대로 막는다 (회귀)
- [ ] R1. 기존 G1 일곱 얼굴이 전부 그대로 거부된다(체인·서브셸 포함).
- [ ] R2. 기존 G2 읽기·기록 아홉이 전부 그대로 통과한다.
- [ ] R3. G3(다른 저장소)·G4(의식적 우회)·G5(비JSON 통과) 불변.

### 회귀
- [ ] R4. 전체 스위트 green.

### 2026-09-02T07:32:03+09:00 tdd (agent: lead:claude-opus-5, by nicehugepark)

### TDD 시나리오 (최종 — 통과분 체크)

시험: tests/test_git_gate.py 10/10 · 실행 `python3 tests/ git_gate`

### 정상 — 글은 지나간다
- [x] P1. note 의 heredoc 본문에 파괴 명령 이름이 적혀 있어도 통과한다.
- [x] P2. 따옴표 안 인자에 그 낱말이 있어도 통과한다.
- [x] P3. `--body "$(cat <<'EOF' … EOF)"` 도 같다.

### 경계 — 받는 쪽이 인터프리터면 그대로 잰다
- [x] B1. `bash -c`·`sh -c`·`eval` 은 여전히 거부된다.
- [x] B2. 인터프리터가 받는 heredoc(`sh <<EOF`·`python3 - <<PY`)도 거부된다.
- [x] B3. 짝이 안 맞는 인용은 옛 판정으로 되돌아간다.

### 실패 — 막을 것은 그대로 막는다
- [x] R1. 기존 일곱 얼굴 전부 거부(체인·서브셸 포함).
- [x] R2. 기존 읽기·기록 아홉 전부 통과.
- [x] R3. G3·G4·G5 불변.

### 회귀
- [x] R4. 전체 스위트 green — `python3 tests/ --jobs 4` exit 0, 실패 샤드 0.

### 2026-09-02T07:32:03+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

### 고친 것 — 게이트가 「누가 받느냐」로 가른다

`bin/s9-git-gate` 는 명령줄 문자열 전체에 정규식을 댔다. 재는 것이 "이 문자열에
그 낱말이 있는가"였지 "그 명령이 실행되는가"가 아니었다.

`data_stripped(cmd)` 를 앞에 세워 **글을 지운 사본**을 재게 했다.
- heredoc 본문: 받는 명령이 인터프리터(bash·sh·eval·python·node·xargs…)면
  그대로 재고, 아니면(`s9 note`·`cat`) 지운다.
- 따옴표 안 인자: 같은 기준. `bash -c "..."` 는 그대로 재므로 따옴표가 방패가
  되지 못한다.
- 지울 때는 **길이를 지키며** 공백으로 바꾼다 — 뒤 위치가 밀리면 소유자 판정이
  어긋난다.
- 인용 짝이 안 맞으면 원문 그대로 잰다(옛 판정) — 모르는 것을 통과시키지 않는다.

한 번 헛디딘 자리: 인터프리터 heredoc 을 「살려 둔」 뒤에도 따옴표 처리가 그
안을 다시 지웠다(`python3 <<PY … run('…') … PY`). 살릴 구간을 명시적으로
기록해 2단계가 건너뛰게 했다.

시험 3개 추가(P·B·R). 낱말을 시험 파일에 그대로 적으면 **그 시험을 고치는
명령 자체가 게이트에 걸리므로** 이어 붙여 만들었다 — 그 한 줄이 결함의 크기를
말한다.

### 2026-09-02T07:32:45+09:00 commit (by nicehugepark)

커밋 809b032 — 문지기는 글이 아니라 명령을 읽는다 — 게이트 오탐 근원 수정 (REQ-20260902-003-62x6)

## History
- 2026-09-02T07:18:36+09:00 created by nicehugepark (status: open)
- 2026-09-02T07:22:09+09:00 status: open -> in-progress (by nicehugepark) — 002 위임을 막고 리드 등록까지 막았다 — 즉시 착수
- 2026-09-02T07:32:53+09:00 status: in-progress -> done (by nicehugepark) — goal 충족: 글 안의 명령 이름은 통과(P1~P3), 인터프리터가 받는 글자는 그대로 거부(B1·B2), 판정 불가는 옛 판정(B3), 기존 계약 불변(R1~R3). git_gate 10/10, 전체 스위트 exit 0. 커밋 809b032.
