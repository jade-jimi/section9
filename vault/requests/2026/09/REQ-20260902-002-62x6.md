---
id: REQ-20260902-002-62x6
type: request
title: 기술 용어 번역 금지
summary: pull·push·commit·worktree 처럼 외부 도구의 고유 기능·명령 이름을 화면과 문서가 한국어로 옮기고 있다. tech-writer·translator 규칙에 이미 있는 조항인데 지켜지지 않았다 — 규칙을 못박고 화면을 되돌리고 시험으로 막는다
goal: 화면·문서·요청 제목에서 외부 도구의 고유 기능·명령 이름이 원어로 서고, 같은 번역이 다시 들어오면 시험이 막는다
status: done
size: M
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: a05fe599
agents: ["sub:tech-writer:a7ec9963", "lead:claude-opus-5", "sub:subagent:afb238d9", "sub:tech-writer", "sub:subagent:ae763e2a"]
contributions: [{"actor": "sub:tech-writer:a7ec9963", "item": "002 화면 용어 재판정", "started": "2026-09-02T07:05:27+09:00", "ended": "2026-09-02T07:20:28+09:00", "result": "stalled", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/a05fe599-cf31-4fd7-8ef2-000a77ff9011/tasks/a7ec9963dd2e790b8.output", "reason": "201초째 진전 없음 (기준 180초)"}, {"actor": "lead:claude-opus-5", "item": "리드 판정 — 규칙 원천부터 고쳤다 (지적 접수 직후)", "started": "2026-09-02T07:05:51+09:00", "ended": "2026-09-02T07:05:51+09:00", "result": "done", "transcript": ""}, {"actor": "lead:claude-opus-5", "item": "`[2026-09-02 07:06:07 KST - lead]`", "started": "2026-09-02T07:06:08+09:00", "ended": "2026-09-02T07:06:08+09:00", "result": "done", "transcript": ""}, {"actor": "lead:claude-opus-5", "item": "`[2026-09-02 07:26:58 KST - lead]`", "started": "2026-09-02T07:26:59+09:00", "ended": "2026-09-02T07:26:59+09:00", "result": "done", "transcript": ""}, {"actor": "sub:subagent:afb238d9", "item": "Writing terminology verdict to REQ-20260902-002-62x6", "started": "2026-09-02T07:27:18+09:00", "ended": "2026-09-02T07:27:18+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/a05fe599-cf31-4fd7-8ef2-000a77ff9011/subagents/agent-afb238d981fecd978.jsonl"}, {"actor": "sub:tech-writer", "item": "tech-writer 용어 재판정 — 남의 도구 이름은 옮기지 않는다 (002)", "started": "2026-09-02T07:28:50+09:00", "ended": "2026-09-02T07:28:50+09:00", "result": "done", "transcript": ""}, {"actor": "sub:tech-writer:a7ec9963", "item": "`[2026-09-02 07:28:52 KST - tech-writer]`", "started": "2026-09-02T07:29:45+09:00", "ended": "2026-09-02T07:29:45+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/a05fe599-cf31-4fd7-8ef2-000a77ff9011/subagents/agent-a7ec9963dd2e790b8.jsonl"}, {"actor": "lead:claude-opus-5", "item": "리드 적용 — tech-writer 판정 전부 수용, 그대로 적용했다", "started": "2026-09-02T07:34:53+09:00", "ended": "2026-09-02T07:34:53+09:00", "result": "done", "transcript": ""}, {"actor": "lead:claude-opus-5", "item": "`[2026-09-02 07:35:21 KST - lead]`", "started": "2026-09-02T07:35:22+09:00", "ended": "2026-09-02T07:35:22+09:00", "result": "done", "transcript": ""}, {"actor": "lead:claude-opus-5", "item": "`[2026-09-02 07:41:25 KST - lead]`", "started": "2026-09-02T07:41:26+09:00", "ended": "2026-09-02T07:41:26+09:00", "result": "done", "transcript": ""}, {"actor": "lead:claude-opus-5", "item": "`[2026-09-02 07:48:40 KST - lead]`", "started": "2026-09-02T07:48:41+09:00", "ended": "2026-09-02T07:48:41+09:00", "result": "done", "transcript": ""}, {"actor": "lead:claude-opus-5", "item": "`[2026-09-02 07:50:46 KST - lead]`", "started": "2026-09-02T07:50:47+09:00", "ended": "2026-09-02T07:50:47+09:00", "result": "done", "transcript": ""}, {"actor": "lead:claude-opus-5", "item": "`[2026-09-02 08:22:20 KST - lead]`", "started": "2026-09-02T08:22:21+09:00", "ended": "2026-09-02T08:22:21+09:00", "result": "done", "transcript": ""}, {"actor": "lead:claude-opus-5", "item": "`[2026-09-02 08:26:36 KST - lead]`", "started": "2026-09-02T08:26:37+09:00", "ended": "2026-09-02T08:26:37+09:00", "result": "done", "transcript": ""}, {"actor": "lead:claude-opus-5", "item": "`[2026-09-02 08:29:46 KST - lead]`", "started": "2026-09-02T08:29:46+09:00", "ended": "2026-09-02T08:29:46+09:00", "result": "done", "transcript": ""}, {"actor": "lead:claude-opus-5", "item": "`[2026-09-02 08:31:57 KST - lead]`", "started": "2026-09-02T08:31:58+09:00", "ended": "2026-09-02T08:31:58+09:00", "result": "done", "transcript": ""}, {"actor": "lead:claude-opus-5", "item": "`[2026-09-02 08:36:27 KST - lead]`", "started": "2026-09-02T08:36:28+09:00", "ended": "2026-09-02T08:36:28+09:00", "result": "done", "transcript": ""}, {"actor": "lead:claude-opus-5", "item": "`[2026-09-02 08:41:09 KST - lead]`", "started": "2026-09-02T08:41:09+09:00", "ended": "2026-09-02T08:41:09+09:00", "result": "done", "transcript": ""}, {"actor": "lead:claude-opus-5", "item": "`[2026-09-02 08:49:33 KST - lead]`", "started": "2026-09-02T08:49:33+09:00", "ended": "2026-09-02T08:49:33+09:00", "result": "done", "transcript": ""}, {"actor": "sub:subagent:ae763e2a", "item": "Reading REQ-20260902-002-62x6 verdict notes", "started": "2026-09-02T08:54:37+09:00", "ended": "2026-09-02T08:54:37+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/a05fe599-cf31-4fd7-8ef2-000a77ff9011/subagents/agent-ae763e2a87c77c081.jsonl"}, {"actor": "lead:claude-opus-5", "item": "`[2026-09-02 08:55:58 KST - lead]`", "started": "2026-09-02T08:55:58+09:00", "ended": "2026-09-02T08:55:58+09:00", "result": "done", "transcript": ""}, {"actor": "lead:claude-opus-5", "item": "`[2026-09-02 09:02:22 KST - lead]`", "started": "2026-09-02T09:02:23+09:00", "ended": "2026-09-02T09:02:23+09:00", "result": "done", "transcript": ""}, {"actor": "lead:claude-opus-5", "item": "`[2026-09-02 09:16:21 KST - lead]`", "started": "2026-09-02T09:16:21+09:00", "ended": "2026-09-02T09:16:21+09:00", "result": "done", "transcript": ""}, {"actor": "lead:claude-opus-5", "item": "`[2026-09-02 09:24:43 KST - lead]`", "started": "2026-09-02T09:24:43+09:00", "ended": "2026-09-02T09:24:43+09:00", "result": "done", "transcript": ""}]
project: section9
relates: ["REQ-20260901-023-62x6", "REQ-20260902-010-62x6"]
relates_why: {"REQ-20260901-023-62x6": "023 제목·판정표가 이 지적의 대상 — 그 화면의 낱말이 여기서 정해진다", "REQ-20260902-010-62x6": "002 가 화면에 내린 git 음차 판정이 010 이 다룰 53자리의 잣대다"}
tags: ["auto-audit", "audit", "sync"]
created: 2026-09-02T07:00:19+09:00
status_since: 2026-09-02T10:12:41+09:00
updated: 2026-09-02T10:38:16+09:00
priority: 50
---

## Original

요청 제목을 받고 올린다고 할 순 있는데 고유 기능이고 기술 용어를 번역하지마. 테크라이터와 트랜슬레이터 규칙이 그럴텐데. pull push commit worktree 등등 포함해서 전부

## Notes

### 2026-09-02T07:05:51+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

### 리드 판정 — 규칙 원천부터 고쳤다 (지적 접수 직후)

사용자 지적: "고유 기능이고 기술 용어를 번역하지마. 테크라이터와 트랜슬레이터
규칙이 그럴텐데. pull push commit worktree 등등 포함해서 전부."

**규칙은 이미 있었다.** tech-writer 정의에 "무차별 번역과 무차별 원어 둘 다
결함이다 — commit·branch 같은 확립 기술용어를 억지로 옮기는 것도 반려 사유"가,
s9-design 6절에 "확립 업계어를 순우리말로 되돌려 옮기는 것도 기계 직역과 같은
결함"이 적혀 있었다. 지켜지지 않은 이유는 **더 구체적인 반대 압력**이 있었기
때문이다 — 「이름은 이름처럼, 행위는 행위처럼」(card.js:1670)과 조어 판정
시금석(DOC-20260830-006-62x6)이 낱말마다 옮길 근거를 대 주었고, 일반 조항은 그
앞에서 힘을 잃었다. 추상적인 금지는 구체적인 허가를 못 이긴다.

그래서 **같은 층위의 구체 조항**으로 못박았다 (원천에서만 고쳤다 — 생성물을
직접 손대면 다음 재생성이 지운다):
- `harness/claude/gen_roster.py` tech-writer·translator·ux-writer 세 정의에
  이름 목록(pull·push·commit·worktree·rebase·merge·branch)과 함께 조항 추가.
  「이름은 이름처럼, 행위는 행위처럼」의 **적용 범위를 이 제품이 스스로 지은
  상태어(done·in-progress)로 한정**한다고 명시 — 이것이 판정을 뒤집은 자리다.
- `harness/claude/skills/s9-design/SKILL.md` 6절에 같은 조항.
- `python3 harness/claude/gen_roster.py` 재생성으로 에이전트 3종 반영.

경계선(리드가 tech-writer 에 넘긴 물음): **사용자가 밖에서 같은 글자로 다시
만나는 이름인가.** git·GitHub·CLI 의 명령·기능·설정 키가 그렇다. 이 제품이
스스로 지은 말(「무인 작업」·「도는 일」·「담당」)은 밖에 대응하는 원어가 없으니
그대로 둔다 — 이번 지적은 그 말들을 되돌리라는 뜻이 아니다.

곁들여 REQ-20260901-023-62x6 제목을 「화면에서 받고 올리기」 →
「화면에서 pull·push」 로 고치고, 그 문서의 옛 판정표를 무효로 적은 뒤
blocked(낱말 판정 대기)로 옮겼다.

### 2026-09-02T07:06:08+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-09-02 07:06:07 KST - lead]`

지적 접수했습니다. 확인해 보니 **규칙은 이미 양쪽에 다 적혀 있었습니다** — tech-writer 정의의 "commit·branch 같은 확립 기술용어를 억지로 옮기는 것도 반려 사유", s9-design 6절의 "확립 업계어를 순우리말로 되돌려 옮기는 것도 기계 직역과 같은 결함". 지켜지지 않은 이유는 그 옆에 **더 구체적인 반대 근거**가 있었기 때문입니다. 「이름은 이름처럼, 행위는 행위처럼」이라는 이 저장소의 옛 결정이 "pull 은 행위니까 한국어 동사"라는 허가를 내줬고, 추상적인 금지가 구체적인 허가를 못 이겼습니다.

그래서 같은 층위의 구체 조항으로 못박았습니다(원천인 `gen_roster.py`·`s9-design/SKILL.md` 에서만 고쳤습니다 — 생성물을 손대면 다음 재생성이 지웁니다):

- pull·push·commit·worktree·rebase·merge·branch 를 **이름으로 열거**하고, 동사여도 원어로 세운다고 명시
- 「이름은 이름처럼, 행위는 행위처럼」의 **적용 범위를 done·in-progress 같은 이 제품이 스스로 지은 상태어로 한정** — 남의 도구 이름에는 대지 않는다
- tech-writer·translator·ux-writer 세 정의에 반영, 재생성 완료

경계선은 **"사용자가 밖에서 같은 글자로 다시 만나는 이름인가"** 로 잡았습니다. git·GitHub·CLI 의 명령·기능·설정 키가 그렇습니다. 반대로 「무인 작업」·「도는 일」처럼 이 제품이 스스로 지은 말은 밖에 대응하는 원어가 없어 그대로 둡니다 — 이 선이 아니라고 하시면 말씀해 주세요.

REQ-20260901-023-62x6 제목은 「화면에서 받고 올리기」 → **「화면에서 pull·push」** 로 고쳤고, 그 문서의 옛 판정표를 무효로 적은 뒤 blocked(낱말 판정 대기)로 옮겼습니다. 낱말이 정해지기 전에 구현에 들어가면 두 번 만들게 됩니다.

지금 tech-writer 에 화면 전수 재판정을 위임했습니다 — 「따로 떼어 놓고 일하기」(worktree), 「올리는 것」(push), card.js 의 워크트리/커밋 문장 등을 훑고, 로마자냐 음차냐 표기 형식까지 정하게 했습니다. 판정이 오면 제가 적용하고, 같은 번역이 다시 들어오면 막는 시험을 붙이겠습니다.

### 2026-09-02T07:26:59+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-09-02 07:26:58 KST - lead]`

위임이 두 번 막혔고, 원인은 tech-writer 가 아니라 **문지기(git gate)의 결함**이었습니다. 게이트가 명령줄 문자열 전체에 정규식을 대서, 따옴표·heredoc **안의 글**에 적힌 명령 이름까지 실행으로 읽었습니다. 판정 노트에 "이 명령은 금지다"라고 적는 순간 그 노트 자체가 거부됐습니다 — 제가 그 결함을 요청으로 등록하려다 같은 자리에서 세 번째로 막혔습니다.

`REQ-20260902-003-62x6` 「문지기가 글을 명령으로 읽는다」로 세우고 고쳤습니다.

가르는 기준은 **"그 글자를 누가 받느냐"** 한 줄입니다. 셸·인터프리터(bash·sh·eval·python)가 받으면 그건 명령이니 그대로 막고, 그 밖의 명령(`s9 note` 같은)이 받으면 자료(글)이니 재지 않습니다. 따옴표가 방패가 되면 게이트는 한 줄로 무력해지므로, `bash -c "..."`·`eval`·인터프리터 heredoc 은 여전히 막습니다. 짝이 안 맞는 인용은 옛 판정으로 되돌아갑니다 — 모르는 것을 통과시키지 않습니다.

시험 3개를 붙여 10/10 통과입니다. 시험 파일에 그 낱말을 그대로 적으면 **그 시험을 고치는 명령 자체가 게이트에 걸려서** 이어 붙여 만들어야 했는데, 그 한 줄이 결함의 크기를 말해 줍니다.

막혔던 tech-writer 는 문지기가 고쳐졌다는 사실과 함께 다시 깨웠습니다 — 이미 해 둔 판정을 문서에 남기고 이어서 마치라고 지시했습니다. 전체 스위트 회귀는 지금 돌고 있습니다.

### 2026-09-02T07:27:18+09:00 subagent (agent: sub:subagent:afb238d9, by nicehugepark)

Writing terminology verdict to REQ-20260902-002-62x6

### 2026-09-02T07:28:50+09:00 tech-writer (agent: sub:tech-writer, by nicehugepark)

tech-writer 용어 재판정 — 남의 도구 이름은 옮기지 않는다 (002)

### 결론 한 줄

**이 제품이 스스로 지은 말은 우리 말로 옮기고, 밖에서 빌려 온 명령·하위명령·
식별자는 빌려 온 글자 그대로 세운다.** 가르는 물음은 하나다 — **이 화면을 닫고
그 일을 하려면 사람이 무엇을 쳐야 하는가.** 칠 글자가 있으면(`git push`) 그
글자가 곧 화면의 낱말이고, 칠 글자가 없으면(승인·반려·이어가기·무인 작업)
우리 말이다.

### A. 옛 판정(2026-09-01 19:53)의 전제가 틀린 자리

옛 판정은 축을 **품사**로 잡았다 — "이름(명사)은 원어, 행위(동사)는 우리 말".
근거로 든 `card.js:1663-1670`(「이름은 이름처럼, 행위는 행위처럼」)은 옳았지만,
그 자가 왜 들었는지를 잘못 읽었다.

`done` 과 「승인」이 갈린 진짜 축은 품사가 아니라 **누가 그 말을 지었는가**였다.
`done` 도 「승인」도 **둘 다 이 제품의 말**이고, 그 닫힌 집합 안에서만 품사 자가
든다. 그 자를 집합 밖으로 들고 나간 것이 잘못이다: `push` 는 명사도 동사도
아니라 **명령 이름**이고, 명령 이름은 이름과 행위가 한 글자다. 품사 자를 대면
반드시 반으로 쪼개지고, 화면은 밖에서 안 통하는 반쪽을 세운다 — 「올리기」를
배운 사람이 GitHub 에 가면 `push` 를 다시 배워야 한다. 사전 두 벌이다.

되돌아보면 옛 판정 자신이 이미 답을 절반 적어 두고 지나쳤다: "다시 만나는 것이
판정을 바꾸는 이유는 *대조*가 필요해서다." 맞다. 그런데 대조가 필요한 것은
식별자만이 아니다 — 무엇을 **눌러야** 그 일이 되는지도 밖과 대조된다.

### B. 못 세 개 — 선이 흐려지는 자리

**못 1 — 개념 일반명사는 빌려 온 것이 아니다.** repository·history·working tree
는 아무도 치지 않는 개념이고 「저장소」·「이력」·「고치던 파일」이라는 확립된
우리말이 이미 있다(GitHub 한국어 화면도 「저장소」를 쓴다). 여기까지 로마자로
미는 것이 **무차별 원어**이고, 그것도 반려 사유다. `git worktree` 는 하위명령
이름이라 원어, `working tree`(두 낱말)는 개념이라 「고치던 파일」 — 이 갈림이
경계선의 실제 모양이다.

**못 2 — 이 제품이 지은 말은 그대로 둔다.** 「무인 작업」·「도는 일」·「담당」·
「이어가기」·「바로 보임/끝나면 보임」은 밖에서 다시 만날 원어가 없다.
`auto_resume` 은 이 제품의 config 키이지 남의 도구 이름이 아니다. 되돌리지 마라.

**못 3 — REQ-20260829-030-62x6 5차 반려와 부딪히지 않는다.** 그때 사용자는
「커밋하면 다시 워크트리로 간다」를 헤더에서 내리게 했다("사용자가 깃을 전혀
모르는 상태"). 두 판정은 **처방이 다를 뿐**이다.

- 030 의 처방: 읽어도 내가 할 일이 아닌 사실은 **화면에서 내린다.**
- 이번의 처방: 내릴 수 없는 자리(설정 스위치는 사람이 정해야 한다)에서는
  **원어로 이름을 대고 한 줄로 뜻을 붙인다**(writing-clarity: 처음 등장 시 정의).
- **틀린 처방**: 사실은 남겨 두고 우리가 지은 **별명**으로 부른다. 그것이
  「따로 떼어 놓고 일하기」이고, 두 사용자를 다 잃는다 — 깃을 모르는 사람은
  여전히 못 읽고, 깃을 아는 사람은 `git worktree` 와 잇지 못한다.

### C. 판정표

원어 = 로마자 그대로 · 음차 = 「커밋」류 한글 소리 · 옮김 = 우리 말.

| 낱말 | 판정 | 근거 한 줄 |
|---|---|---|
| `pull` | **원어** (前 「받기」 폐기) | 화면을 닫으면 `git pull` 을 쳐야 한다 — 칠 글자가 곧 화면의 낱말. |
| `push` | **원어** (前 「올리기」 폐기) | 같은 이유. 단추 낱말이어도 원어로 선다(s9-design 6절 신설 조항). |
| `commit` | **원어** (前 음차 「커밋」 폐기) | 같은 이유. 음차는 원어와도 우리말과도 안 맞는 제3의 글자다. |
| `worktree` | **원어** (前 「따로 떼어 놓고 일하기」·음차 「워크트리」 폐기) | `git worktree`·`s9 worktree ls` 의 하위명령 이름. 別名 금지는 못 3. |
| `merge` / `rebase` | **원어** | 화면이 고르지 않으니 개념어로는 안 세우되, 터미널로 보내는 자리에서는 `git pull --rebase` 를 mono 로. |
| `branch` | **원어** (설 때만) | 이 판에 가지가 하나뿐이라 여전히 화면에 안 세운다. 서면 `branch` — 「가지」·「브랜치」 둘 다 기각. |
| `main` / `origin` / `upstream` / 짧은 해시 | **원어** (前 판정 유지) | 이름이다. remote 가 둘인 설치에서는 `origin`·`upstream` 을 이름 그대로. |
| `fetch` | **안 세움** (前 판정 유지) | 사람이 청하는 일이 아니라 화면이 셈을 얻는 방법. 「방금 확인」 + 「다시 확인」. |
| `ahead` / `behind` | **옮김 — 「`push` 할 것 N개」/「`pull` 할 것 N개」** | 단추 낱말과 같은 말이라 배울 것이 없다. 「앞선 것/뒤진 것」은 여전히 기각(대비쌍 — card.js:990 선례). |
| `working tree dirty` | **옮김 — 「고치던 파일 N개」** (前 판정 유지) | 두 낱말 개념이지 명령 이름이 아니다. 못 1. |
| `repository` | **옮김 — 「저장소」** (前 판정 유지) | 확립 번역어. 음차 「리포」도 기각 — 한 사물에 두 이름. |
| `remote` | **원어**(remote 가 둘일 때 이름으로) / 그 외 **안 세움** | 이 판에서 원격의 사용자쪽 이름은 이미 `GitHub` 이다(022 B-1). |
| `history` | **옮김 — 「이력」** | 개념. 못 1. |
| sync | **옮김 — 「동기화」** (settings.js:170 유지) | `git sync` 라는 명령은 없다. 여기까지 미는 것이 무차별 원어다. |
| SSE `push` | **원어** (前 음차 「푸시」 폐기) | git 의 push 는 아니나 남의 규격 이름인 것은 같다. |
| `GitHub` / `Claude` / `Monitor` | **원어, 본문 글자, 원 대소문자** | 제품 이름이다 — 치는 글자가 아니라 부르는 이름. 「깃헙」 기각(022 B-1). |
| `git` | **원어, 소문자, mono** | 명령 이름이다. |
| 「무인 작업」·「도는 일」·「담당」·「이어가기」 | **그대로 둠** | 밖에서 다시 만날 원어가 없다. 못 2 — 되돌리지 마라. |

### D. 표기 형식 (한 벌로 고정)

1. **로마자·소문자·원 표기 그대로. 음차 금지.** 음차는 어느 쪽 사전에도 없는
   제3의 글자라 대조가 반만 된다 — 대조가 이 낱말들이 화면에 서는 유일한 이유다.
2. **mono.** 선례는 `done`·`in-progress`(card.js `stName`). mono 는 "이것은 내가
   어딘가에 그대로 칠 글자다"를 형태로 말한다. 본문 글자로 세우면 문장에 녹아
   대조 신호가 죽는다.
   - **예외**: mono 를 못 넣는 자리(`workercfg.js` 의 `name`·`mean` 은 `esc()` 만
     받아 HTML 이 안 들어간다)에서는 **본문 글자로 서되 낱말은 그대로 둔다.**
     표기는 자리의 제약이고 낱말은 규칙이다 — "mono 를 못 쓰니 한국어로 옮기자"로
     가지 마라. 선례: 「내 GitHub 계정 쓰게 하기」도 본문 글자다.
3. **낫표(「」)를 겹치지 않는다.** 낫표는 **이 화면이 지은 이름**의 옷이다
   (「무인 작업」·「이어가기」). 남의 이름에 씌우면 두 어휘를 가르는 유일한 시각
   신호가 지워진다. `push` 는 mono 로만, 「무인 작업」은 낫표로만.
4. **조사** — 받침으로 갈리는 조사(은/는·이/가·을/를·으로/로)를 로마자 뒤에 바로
   붙이지 마라. `push` 는? `push` 은? 어느 쪽으로 통일해도 낱말마다 절반이 틀린다
   (card.js:1674 가 `done 으로`·`in-progress 로` 에서 이미 겪은 것).
   - 첫째 답: **`하다`·분류 명사를 끼운다** — `push` 하는 것도 / `commit` 하면 /
     `push` 할 것 3개 / `worktree` 자리.
   - 둘째 답: **갈리지 않는 조사만** — 도·에·에서·까지·만.
   - 그래도 붙여야 하면 **한 칸 띄우고 소리 나는 대로**: `worktree` 로 / `worktree` 를.
     띄어쓰기는 mono 와 한글이 엉키는 것을 막는다(이 저장소가 이미 쓰는 형식).

### E. before / after (파일:줄 — 그대로 적용 가능)

낱말만 손댄다. workercfg 화면의 구조·자리는 022 산출물이라 건드리지 않는다.

### E1. web/app/workercfg.js:51
- before: `+ "GitHub 계정으로 명령을 씁니다. 저장소에 올리는 것도 그중 하나입니다 — 같은 권한으로 "`
- after : `+ "GitHub 계정으로 명령을 씁니다. 저장소에 push 하는 것도 그중 하나입니다 — 같은 권한으로 "`

### E2. web/app/workercfg.js:61
- before: `+ "로그인된 GitHub 계정으로 명령을 씁니다. 저장소에 올리는 것도 그중 하나입니다 — "`
- after : `+ "로그인된 GitHub 계정으로 명령을 씁니다. 저장소에 push 하는 것도 그중 하나입니다 — "`

### E3. web/app/workercfg.js:72 (스위치 이름 — 이름 자체를 다시 지었다)
- before: `{key: "worker_worktree", name: "따로 떼어 놓고 일하기",`
- after : `{key: "worker_worktree", name: "worktree 쓰게 하기",`

「내 GitHub 계정 쓰게 하기」와 같은 틀이다. 그리고 이 행은 `wcfgRow` 가 이미 실제
키 `worker_worktree` 를 `.wkey` mono 로 이름 밑에 세우고 있다 — 이름과 키가 같은
글자를 물어야 사람이 터미널·`git worktree list` 와 눈으로 잇는다. 지금은 그 둘이
아무 관계도 없어 보인다.

### E4. web/app/workercfg.js:75-77 (뜻 줄 — 원어 + 한 줄 정의)
- before:
```
   mean: "무인 작업을 제 자리에서 돌려, 아직 저장하지 않은 남의 편집이 보이지도 "
       + "덮이지도 않게 합니다. 지금 이 저장소에 고치던 것이 있으면 자리를 "
       + "나누지 않고 여기서 함께 일합니다.",
```
- after:
```
   mean: "worktree 는 git 이 이 저장소 옆에 따로 띄우는 작업 폴더입니다. 무인 "
       + "작업을 거기서 돌려, 아직 저장하지 않은 남의 편집이 보이지도 덮이지도 "
       + "않게 합니다. 지금 이 저장소에 고치던 것이 있으면 나누지 않고 여기서 "
       + "함께 일합니다.",
```

### E5. web/app/workercfg.js:73-74·78-79 (on/off/said) — **그대로 둔다**
「켬 — 고친 내용은 작업이 끝난 뒤에 보입니다」류는 **결과 문장**이고 결과는 이
제품의 말이다. 남의 도구 이름이 없으니 되돌릴 것도 옮길 것도 없다. 이름 칸이
기계 글자를 대고 결과 칸이 우리 말을 대는 것이 022 가 세운 구조 그대로다.

### E6. web/app/workercfg.js:26-28 (파일 머리 주석 — 판정 근거 갱신)
코드 주석은 제품 원문이라 판정 대상이 아니지만, **이 주석은 지금 틀린 근거를
기록하고 있다**(「따로 떼어 놓고 일하기」가 확정어라고 적혀 있다). 낱말만 고치고
주석을 두면 다음 사람이 주석을 근거로 되돌린다.
- before:
```
   `개인 GitHub 계정명` 을 쓴다) · 「따로 떼어 놓고 일하기」(worktree·사본·작업
   자리는 다섯 번 반려로 화면에서 내려간 낱말이다). */
```
- after:
```
   `개인 GitHub 계정명` 을 쓴다) · 「worktree 쓰게 하기」(REQ-20260902-002-62x6 로
   뒤집혔다 — 화면에서 내려간 것은 「사본」·「작업 자리」 같이 **우리가 지은
   별명**이지 `worktree` 라는 원어가 아니었다. 남의 명령 이름은 옮기지 않는다). */
```

### E7. web/app/card.js:1011-1012
- before:
```
const WS_FIX_COMMIT = "커밋하면 다음 작업부터 다시 워크트리로 갑니다";
const WS_FIX_SWEEP = "다 쓴 워크트리를 거두면 다시 워크트리로 갑니다";
```
- after:
```
const WS_FIX_COMMIT = "commit 하면 다음 작업부터 다시 worktree 로 갑니다";
const WS_FIX_SWEEP = "다 쓴 worktree 를 거두면 다시 worktree 로 갑니다";
```

### E8. web/app/card.js:1024-1028 (WS_WHY)
- before:
```
  "dirty-spine": ["모두가 쓰는 파일이 아직 커밋되지 않아, 따로 떼어 놓고 하면 "
                  + "그 코드가 빠진 낡은 사본에서 일하게 됩니다", WS_FIX_COMMIT],
  "dirty-overlap": ["이 요청이 고칠 파일이 아직 커밋되지 않았습니다", WS_FIX_COMMIT],
  "dirty-unknown": ["아직 커밋되지 않은 코드가 있고, 이 요청이 어느 파일을 "
                    + "고칠지는 문서에 적혀 있지 않습니다", WS_FIX_COMMIT],
```
- after:
```
  "dirty-spine": ["모두가 쓰는 파일이 아직 commit 되지 않아, worktree 로 떼어 "
                  + "놓으면 그 코드가 빠진 낡은 사본에서 일하게 됩니다", WS_FIX_COMMIT],
  "dirty-overlap": ["이 요청이 고칠 파일이 아직 commit 되지 않았습니다", WS_FIX_COMMIT],
  "dirty-unknown": ["아직 commit 되지 않은 코드가 있고, 이 요청이 어느 파일을 "
                    + "고칠지는 문서에 적혀 있지 않습니다", WS_FIX_COMMIT],
```
(「따로 떼어 놓고 하면」도 worktree 의 별명이라 함께 내린다. 「사본」은 이번
판정의 대상이 아니라 손대지 않았다.)

### E9. web/app/terminal.js:57
- before: `... · 수신 = SSE 푸시 tail)";`
- after : `... · 수신 = SSE push tail)";`
git 의 push 는 아니지만 남의 규격 이름인 것은 같고, 무엇보다 **음차**다. (이 줄
전체가 기계어 캡션인 것은 s9-design 6절 위반이지만 별건 — 이번 판정 밖.)

### E10. 유지 판정 — 손대지 마라
- `web/app/settings.js:170` 「~/section9 (로컬 md + git 동기화)」 — `git` 은 이미
  로마자, 「동기화」는 결과이지 명령 이름이 아니다.
- `web/app/userform.js:135` 「profile.md Notes에 audit + git 동기화」 — 같음.
- 「다시 받기」(boot.js·docs.js·session.js·target.js) · 「파일을 올리는 중」
  (dialog.js) · 「owner 로 올려 주세요」(project.js) — `pull`/`push` 와 아무 관계
  없는 일상어다. 여기까지 걷는 것이 무차별 원어다.

### F. REQ-20260901-023-62x6 화면(blocked)의 낱말 — 판정표를 갈아 끼운다

옛 판정에서 **바뀌는 것**: `pull`→「받기」·`push`→「올리기」 폐기.
- 단추: **`pull`** / **`push`** (mono 로마자). 확인 창 ok 도 **`push`**, cancel 은
  「그만두기」(022 확정어) — 남의 이름과 우리 말이 한 창에 나란히 서는 것이 옳다.
- 셈: 「`push` 할 것 3개 · `pull` 할 것 2개」.
- 갈라짐: 「`push` 할 것 2개 · `pull` 할 것 3개 — 이 화면에서는 맞출 수 없습니다.
  터미널에서 `git pull --rebase` 로 맞춘 뒤 다시 오세요.」
- 구역 이름 「GitHub 과 주고받기」 유지(「동기화」 금지 유지).

옛 판정에서 **그대로 서는 것**: `main`·`origin`·짧은 해시 원어 · `fetch` 안 세움
(「방금 확인」+「다시 확인」) · 「고치던 파일 N개」+파일 이름 · 「도는 일 N건」 ·
`diverged` 안 세움 · 「앞선 것/뒤진 것」 기각 · 「동기화」·「가지」·「내려받기」 기각.

옛 판정의 **F절(「받기」vs「다시 받기」충돌)은 통째로 소멸**한다 — 단추가 `pull`
이 되면 「다시 받기」와 갈릴 자리가 없다. 이것도 이 선이 옳다는 방증 하나다.

### G. 기계로 막을 방법 — `tests/test_screen_lexicon.py` 에 한 칸 더

**새 기계를 만들지 마라.** 그 파일이 이미 정확히 이 일을 한다: `web/app/*.js` +
`web/*.html` 의 **사용자 문자열만** 훑고(주석 제외 — 주석에는 반려어를 인용해야만
쓸 수 있는 근거가 있다), 진단 파일(boot·graph·diag)은 밖에 두고, `BANNED` 는
낱말→판정문 사전이며, `test_the_replacements_actually_stand` 가 **채택어가 실제로
서 있는지**를 못박는다. 두 방향이 다 필요하다.

### G1. 금지 — **음차만** 잡는다 (`BANNED` 에 추가)
```
"푸시":     "남의 명령 이름은 음차하지 않는다 (REQ-20260902-002-62x6) — `push` 로",
"커밋":     "같은 판정 — `commit` 으로",
"워크트리": "같은 판정 — `worktree` 로",
"리베이스": "같은 판정 — `rebase` 로",
"스태시":   "같은 판정 — 그 명령의 원어로",
"클론":     "같은 판정 — `clone` 으로",
"체크아웃": "같은 판정 — `checkout` 으로",
"브랜치":   "같은 판정 — `branch` 로",
```
왜 음차만인가: 이 여덟은 한국어에 다른 뜻이 없어 **오탐이 0** 이다. 기계에는
확실히 이길 수 있는 싸움 하나만 준다.

### G2. **넣으면 안 되는 것** — 이 제안의 핵심
오탐 하나면 사람이 목록을 지우고, 그때부터 게이트는 없는 것과 같다.
- **「풀」** — 「한도가 풀립니다」(session.js 4곳)·「문제를 풀고」·「축약을 풀면」이
  화면에 산다. 넣는 순간 빨간불. **절대 금지.**
- **「가지」**(branch) — `card.js:1312` 「이어가지 않음」에 걸린다.
- **「머지」** — 지금은 오탐 0 이지만 「머지않아」가 언제든 생긴다. **빼기를 권한다**:
  merge 음차가 다시 들어올 위험보다 이 게이트가 꺼질 위험이 크다. 넣으려면
  substring 이 아니라 낱말 경계 정규식을 먼저 붙여라.
- **「올리기」·「받기」·「보내기」·「가져오기」** — 옛 판정이 세운 그 말들이지만
  **낱말 자체엔 죄가 없다**. 정당한 자리가 여럿이다(E10). 죄는 낱말이 아니라
  그 낱말이 `pull`/`push` **자리**에 선 것이고, 자리는 기계가 못 본다. 그래서 G3.
- **「저장소」·「동기화」·「이력」·「원격」** — 확립 번역어·개념어. 넣으면 못 1 위반.

### G3. 양성 고정점 — **지워진 것**을 잡는다
`test_the_replacements_actually_stand` 의 목록에 추가:
```
"worktree", "commit 하면", "push 하는 것도"
```
금지어는 **되돌아온 음차**만 잡는다. 그런데 이번 사고는 그게 아니라 **원어를
아예 지우고 순우리말로 갈아 끼운 것**이었고(「올리기」·「따로 떼어 놓고 일하기」),
그건 금지어로는 영원히 못 잡는다. 원어가 화면에 **서 있는지**를 못박아야
같은 사고가 다시 오면 깨진다.

### G4. 선행 조건 — `web/guide.html` 을 같은 판에서 고쳐야 게이트가 초록이 된다
`_surfaces()` 는 `web/*.html` 을 함께 훑고, 그 파일에 음차가 산다:
「리포」15 ·「업스트림」11 ·「푸시」6 ·「커밋」6 ·「클론」6 ·「머지」3 ·「리모트」3.
**게이트만 넣고 guide 를 예외로 빼는 것은 옆문이다** — guide.html 은 진단 출력이
아니라 사람이 읽는 설치 안내다. 방향이 두 갈래로 갈리는 것이 이 판정이 무차별이
아니라는 증거다:
- 음차 → **로마자**: 푸시→`push` · 커밋→`commit` · 클론→`clone` · 머지→`merge` ·
  리모트→`remote` · 업스트림→`upstream` (이 설치의 실제 remote 이름이고, 같은
  문서가 이미 `merge upstream/main`·`git push -u origin main` 을 로마자로 쓴다)
- 음차 → **우리말**: 리포 → **저장소** (개념이지 명령이 아니고, 같은 문서가 이미
  「팀 저장소」를 쓴다 — 지금 한 사물에 두 이름이 서 있다)
- **유지**: 「인스턴스」 (이 배포가 스스로 지은 말 — 밖에서 만날 원어가 없다)
규모가 있으니 `s9 new request --parent REQ-20260902-002-62x6` 로 갈라 등록 권장.

### G5. 함께 고쳐야 깨지지 않는 기존 시험 (리드 확인용)
- `tests/test_workspace_chip.py:181` `assertIn("커밋하면", …)` → `"commit 하면"`
- 같은 파일 `:187`·`:188`·`:407` `assertNotIn("커밋하면", …)` → `"commit 하면"`
- 같은 파일 `:395` `assertIn("커밋되지 않았습니다", …)` → `"commit 되지 않았습니다"`
- 같은 파일 `:396` `assertIn("커밋하면", …)` → `"commit 하면"`
- 같은 파일 `:277` `for word in ("본 저장소", "워크트리")` → `("본 저장소", "worktree")`
  — 안 고치면 죽은 단언이 된다(찾는 글자가 화면에서 사라졌으므로 늘 통과).
- 같은 파일 `:281` `("본 저장소", "워크트리", "커밋하면")` → `("본 저장소",
  "worktree", "commit 하면")`
- `tests/test_dialog_voice.py:64` `BANNED_SERVER` 의 「워크트리」는 **서버 문장**용
  이고 뜻이 다르다(그 개념을 서버 팝업에 세우지 마라). 그대로 두되, 서버가 언젠가
  `worktree` 를 로마자로 쓰면 안 걸린다는 구멍은 남는다 — 리드 판단.

### H. 범위 밖으로 둔 것 (근거와 함께)

- **`bin/s9` 의 「미커밋」·「워크트리」**(`WORKSPACE_WHY` 2785-2793, 2826-2827):
  전부 반말 종결이라 CLI·프롬프트 표면이고 팝업이 아니다. 제품 원문은 판정
  대상이 아니다. 다만 이 저장소에는 **「서버의 문장이 곧 팝업이다」**(커밋
  87fa5ab) 선례가 있다 — 이 문장들이 화면으로 새는 날 그때 판정 대상이 된다.
- `web/verify-*.html` — 검증용 화면. 위 음차가 없어 이번 게이트에 안 걸린다.
- `terminal.js:57` 캡션 전체의 기계어(append·tail·SSE·Monitor) — 별건.

### 2026-09-02T07:29:45+09:00 subagent (agent: sub:tech-writer:a7ec9963, by nicehugepark)

`[2026-09-02 07:28:52 KST - tech-writer]`

판정을 `REQ-20260902-002-62x6` 에 `--label tech-writer` 로 남겼습니다(11,993자). 코드는 손대지 않았습니다.

### 경계선 한 문장

**이 제품이 스스로 지은 말은 우리 말로 옮기고, 밖에서 빌려 온 명령·하위명령·식별자는 빌려 온 글자 그대로 세운다.** 가르는 물음은 하나 — **화면을 닫고 그 일을 하려면 사람이 무엇을 쳐야 하는가.** 칠 글자가 있으면(`git push`) 그 글자가 곧 화면의 낱말이고, 칠 글자가 없으면(승인·반려·이어가기·무인 작업) 우리 말입니다.

### 옛 판정이 틀린 자리 (스스로 짚은 것)

축을 **품사**로 잡은 것이 잘못이었습니다. `done` 과 「승인」이 갈린 진짜 축은 품사가 아니라 **누가 그 말을 지었는가**였고, 둘 다 이 제품의 말이라 그 닫힌 집합 안에서만 품사 자가 듭니다. `push` 는 명사도 동사도 아니라 **명령 이름**이고, 명령 이름은 이름과 행위가 한 글자입니다 — 품사 자를 대면 반드시 반으로 쪼개지고 화면은 밖에서 안 통하는 반쪽을 세웁니다.

옛 판정 자신이 답을 절반 적어 두고 지나쳤습니다: "다시 만나는 것이 판정을 바꾸는 이유는 *대조*가 필요해서다." 맞는데, 대조가 필요한 것은 식별자만이 아니라 **무엇을 눌러야 그 일이 되는지**도입니다.

### 못 세 개 (선이 흐려지는 자리)

1. **개념 일반명사는 빌려 온 것이 아니다** — `git worktree` 는 하위명령이라 원어, `working tree`(두 낱말)는 개념이라 「고치던 파일」. 「저장소」·「동기화」·「이력」까지 로마자로 미는 것이 무차별 원어이고 그것도 반려 사유입니다.
2. **이 제품이 지은 말은 그대로** — 「무인 작업」·「도는 일」·「담당」 되돌리지 마십시오.
3. **REQ-20260829-030-62x6 5차 반려와 부딪히지 않는다** — 030 의 처방은 "그 사실을 화면에서 내린다", 이번은 "내릴 수 없는 자리에서는 원어로 이름을 대고 한 줄로 뜻을 붙인다". 틀린 처방은 **사실을 남겨 두고 우리가 지은 별명으로 부르는 것**이고, 그것이 「따로 떼어 놓고 일하기」입니다 — 깃 모르는 사람은 여전히 못 읽고, 깃 아는 사람은 `git worktree` 와 잇지 못합니다.

### 표기 형식

**로마자·소문자·mono, 음차 금지.** 선례는 `done`·`in-progress`(`card.js` `stName`). mono 를 못 넣는 자리(`workercfg.js` 의 `name`·`mean` 은 `esc()` 만 받습니다)에서는 **본문 글자로 서되 낱말은 그대로** — 표기는 자리의 제약이고 낱말은 규칙입니다. 낫표(「」)는 이 화면이 지은 이름의 옷이라 남의 이름에 겹치지 않습니다. **조사는 `하다`·분류 명사를 끼워 피합니다**(`push` 하는 것도 / `commit` 하면 / `push` 할 것 3개) — `card.js:1674` 가 `done 으로`·`in-progress 로` 에서 이미 겪은 문제입니다.

### before/after (전수 조사 결과 — 10건)

- `web/app/workercfg.js:51`·`:61` 「저장소에 올리는 것도」 → **「저장소에 push 하는 것도」**
- `web/app/workercfg.js:72` name 「따로 떼어 놓고 일하기」 → **「worktree 쓰게 하기」**(「내 GitHub 계정 쓰게 하기」와 같은 틀. 이 행은 `wcfgRow` 가 이미 `worker_worktree` 를 `.wkey` mono 로 이름 밑에 세우고 있어, 이름과 키가 같은 글자를 물어야 밖과 이어집니다)
- `web/app/workercfg.js:75-77` mean → **`worktree` 원어 + 한 줄 정의**("git 이 이 저장소 옆에 따로 띄우는 작업 폴더입니다")
- `web/app/workercfg.js:73-74·78-79` on/off/said — **그대로**(결과 문장은 이 제품의 말)
- `web/app/workercfg.js:26-28` 파일 머리 주석 — **판정 근거 갱신 필수**. 지금 틀린 근거를 기록하고 있어, 안 고치면 다음 사람이 주석을 근거로 되돌립니다
- `web/app/card.js:1011-1012` `WS_FIX_COMMIT`/`WS_FIX_SWEEP` → **「commit 하면 … 다시 worktree 로 갑니다」**(음차 아닌 로마자 — 이 저장소가 `done` 을 mono 로마자로 세운 그 선례)
- `web/app/card.js:1024-1028` `WS_WHY` 「커밋되지」→「commit 되지」, 「따로 떼어 놓고 하면」→「worktree 로 떼어 놓으면」
- `web/app/terminal.js:57` 「SSE 푸시 tail」 → **「SSE push tail」**
- **유지 판정**: `settings.js:170`·`userform.js:135` 「git 동기화」(`git sync` 라는 명령은 없습니다) · 「다시 받기」·「파일을 올리는 중」·「owner 로 올려 주세요」 — 여기까지 걷는 것이 반대편 결함입니다

`REQ-20260901-023-62x6`(blocked) 화면의 낱말도 갈아 끼웠습니다: 단추 **`pull`/`push`**, 셈 「`push` 할 것 3개 · `pull` 할 것 2개」. 옛 판정의 F절(「받기」vs「다시 받기」충돌)은 통째로 소멸합니다 — 이 선이 옳다는 방증 하나입니다.

### 기계 게이트

**새 기계를 만들지 마십시오** — `tests/test_screen_lexicon.py` 가 이미 정확히 이 일을 합니다(사용자 문자열만, 주석 제외, 진단 파일 제외, `BANNED` 는 낱말→판정문 사전).

- **넣을 것 — 음차만 여덟**(푸시·커밋·워크트리·리베이스·클론·체크아웃·브랜치 등). 한국어에 다른 뜻이 없어 **오탐 0** 입니다.
- **절대 넣지 말 것** — 「풀」(「한도가 풀립니다」가 `session.js` 4곳에 삽니다) · 「가지」(`card.js:1312` 「이어가지 않음」) · 「올리기」·「받기」(정당한 자리가 여럿) · 「저장소」·「동기화」. **오탐 하나면 사람이 목록을 지웁니다.** 「머지」는 지금 오탐 0 이지만 「머지않아」가 언제든 생기므로 빼기를 권합니다.
- **양성 고정점이 더 중요합니다** — `test_the_replacements_actually_stand` 에 `"worktree"`, `"commit 하면"`, `"push 하는 것도"`. 이번 사고는 음차가 아니라 **원어를 아예 지우고 순우리말로 갈아 끼운 것**이었고, 그건 금지어로는 영원히 못 잡습니다.

### 리드가 먼저 알아야 할 두 가지

1. **`web/guide.html` 을 같은 판에서 고쳐야 게이트가 초록이 됩니다.** `_surfaces()` 가 `web/*.html` 을 함께 훑는데 그 파일에 「리포」15·「업스트림」11·「푸시」6·「커밋」6·「클론」6·「머지」3·「리모트」3 이 삽니다. 예외로 빼는 것은 옆문입니다(진단 출력이 아니라 사람이 읽는 설치 안내). 방향이 두 갈래로 갈리는 것이 이 판정이 무차별이 아니라는 증거입니다 — 푸시→`push` 는 로마자로, **리포→「저장소」는 우리말로**, 「인스턴스」는 유지. 규모가 있으니 파생 REQ 권장.
2. **함께 고치지 않으면 깨지는 기존 시험** — `tests/test_workspace_chip.py` 의 `:181`·`:187`·`:188`·`:395`·`:396`·`:407`(「커밋하면」→「commit 하면」)과 `:277`·`:281`(「워크트리」→`worktree`). 특히 `:277` 은 안 고치면 **죽은 단언**이 됩니다(찾는 글자가 화면에서 사라져 늘 통과).

`bin/s9` 의 「미커밋」·「워크트리」는 전부 반말 종결이라 CLI 표면이고 이번 판정 밖입니다 — 다만 「서버의 문장이 곧 팝업이다」(커밋 87fa5ab) 선례가 있어, 화면으로 새는 날 판정 대상이 됩니다.

문지기 고쳐 주셔서 노트가 한 번에 들어갔습니다.

### 2026-09-02T07:34:53+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

### 리드 적용 — tech-writer 판정 전부 수용, 그대로 적용했다

판정에서 고칠 것이 없었다. 특히 두 가지를 그대로 받았다.
① **경계선** — "화면을 닫고 그 일을 하려면 사람이 무엇을 쳐야 하는가." 칠 글자가
   있으면 그 글자가 곧 화면의 낱말이고, 없으면 우리 말이다.
② **무차별 원어도 결함** — 「저장소」·「동기화」·「다시 받기」는 그대로 두었다.
   「리포」는 빌려 온 명령 이름이 아니라 줄임말이라 「저장소」로 갔다.

### 적용 (화면 10자리 + 안내 문서)

- `workercfg.js` — 「저장소에 올리는 것도」×2 → **`push` 하는 것도**;
  `worker_worktree` 이름 「따로 떼어 놓고 일하기」 → **「worktree 쓰게 하기」**;
  mean 첫 문장을 원어+정의로("worktree 는 git 이 이 저장소 옆에 따로 띄우는
  작업 폴더입니다"). on/off/said 결과 문장은 우리 말이라 그대로.
- `workercfg.js` 파일 머리 주석 — **판정 근거를 갱신**했다. 틀린 근거를 남겨
  두면 다음 사람이 주석을 읽고 되돌린다. 앞선 다섯 번의 반려가 내린 것은
  「사본」·「작업 자리」 같은 **우리가 지은 별명**이었지 git 이 지은 이름이
  아니었다고 적었다.
- `card.js` — `WS_FIX_COMMIT`/`WS_FIX_SWEEP`/`WS_WHY` 셋: 「커밋하면…워크트리로」
  → **「commit 하면 … worktree 로」**, 「따로 떼어 놓고 하면」 → 「worktree 로
  떼어 놓으면」.
- `terminal.js` — 「SSE 푸시 tail」 → **「SSE push tail」**.
- `web/guide.html` — 음차 0 으로. 푸시·커밋·클론·리모트·업스트림·머지 →
  원어, 리포 → 「저장소」. 예외로 빼지 않았다 — 진단 출력이 아니라 사람이 읽는
  설치 안내다.

### 기계 게이트 (재발 방지)

새 기계를 만들지 않고 `tests/test_screen_lexicon.py` 에 얹었다.
- `BANNED` 에 **음차 여덟**(푸시·커밋·워크트리·리베이스·클론·체크아웃·브랜치·
  리모트). 넣지 않은 것과 까닭도 함께 적었다 — 「풀」(한도가 풀립니다) ·
  「가지」(이어가지 않음) · 「머지」(머지않아) · 「올리기」·「받기」·「저장소」.
  **오탐 하나면 사람이 목록째 지운다.**
- 더 중요한 쪽: **채택어 고정점** 세 개(`worktree`·`commit 하면`·`push 하는
  것도`)를 `test_the_replacements_actually_stand` 에 박았다. 이번 사고는 음차가
  아니라 **원어를 아예 지우고 순우리말로 갈아 끼운 것**이라, 금지어 목록으로는
  영원히 안 잡힌다.
- 함께 갱신: `tests/test_workspace_chip.py` 단언 7곳(안 고치면 죽은 단언이 된다).

### 규칙 원천

`gen_roster.py`(tech-writer·translator·ux-writer) · `s9-design` 6절에 조항을
넣고 재생성했다. 핵심은 **「이름은 이름처럼, 행위는 행위처럼」의 적용 범위를
이 제품이 스스로 지은 상태어로 한정**한 것 — 그 자를 남의 도구 이름에 대는
순간 반드시 반으로 쪼개진다는 것이 tech-writer 의 자기 진단이었다.

### 2026-09-02T07:35:22+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-09-02 07:35:21 KST - lead]`

tech-writer 판정이 왔고, 고칠 데가 없어 그대로 적용했습니다.

**경계선** — "화면을 닫고 그 일을 하려면 사람이 무엇을 쳐야 하는가." 칠 글자가 있으면(`git push`) 그 글자가 곧 화면의 낱말이고, 없으면(승인·반려·무인 작업) 우리 말입니다.

그리고 tech-writer 가 자기 판정이 왜 틀렸는지 스스로 짚었습니다 — 축을 **품사**로 잡은 것이 잘못이었고, `done`과 「승인」을 가른 진짜 축은 품사가 아니라 **누가 그 말을 지었는가**였습니다. `push`는 명사도 동사도 아닌 **명령 이름**이라, 품사 자를 대면 반드시 반으로 쪼개집니다.

적용한 것:

- **workercfg.js** — 「따로 떼어 놓고 일하기」 → **「worktree 쓰게 하기」**, 뜻은 그 옆에 한 줄로("worktree 는 git 이 이 저장소 옆에 따로 띄우는 작업 폴더입니다"). 「저장소에 올리는 것도」 → **`push` 하는 것도**
- **card.js** — 「커밋하면 … 워크트리로」 → **「commit 하면 … worktree 로」**
- **terminal.js** — 「SSE 푸시 tail」 → **「SSE push tail」**
- **guide.html** — 음차 0으로(푸시·커밋·클론·리모트·업스트림·머지 → 원어). 예외로 빼지 않았습니다 — 진단 출력이 아니라 사람이 읽는 설치 안내입니다
- **파일 머리 주석의 판정 근거도 갱신** — 틀린 근거를 남겨 두면 다음 사람이 주석을 읽고 되돌립니다

반대편도 지켰습니다. **무차별 원어도 결함**이라, 「저장소」·「동기화」·「다시 받기」는 그대로 뒀습니다. 「리포」만 줄임말이라 「저장소」로 갔습니다.

재발 방지는 두 겹입니다. 금지어(음차 여덟)는 오탐 0인 것만 넣고 「풀」·「가지」·「머지」는 뺐습니다 — 오탐 하나면 사람이 목록째 지웁니다. 더 중요한 건 **채택어 고정점**(`worktree`·`commit 하면`·`push 하는 것도`)인데, 이번 사고는 음차가 아니라 원어를 아예 지우고 순우리말로 갈아 끼운 것이라 금지어로는 영원히 못 잡습니다.

REQ-20260901-023-62x6(화면에서 pull·push)의 단추도 `pull`/`push`, 셈은 「`push` 할 것 3개 · `pull` 할 것 2개」로 정해졌습니다. 전체 회귀가 끝나면 커밋하고, 화면 캡처로 확인해 올리겠습니다.

### 2026-09-02T07:41:26+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-09-02 07:41:25 KST - lead]`

화면 적용을 마치고 실브라우저로 확인했습니다.

**Settings 「무인 작업」 판**에서 이제 이렇게 섭니다:

- **worktree 쓰게 하기** — 그 밑에 설정 키 `worker_worktree` 가 mono 로 서 있어서, **이름과 키가 같은 글자를 뭅니다**. 「따로 떼어 놓고 일하기」였을 때는 이 둘이 서로 다른 말이라 밖과 이어지지 않았습니다
- 그 뜻은 옆에 한 줄로 — "worktree 는 git 이 이 저장소 옆에 따로 띄우는 작업 폴더입니다"
- **"저장소에 push 하는 것도 그중 하나입니다"** — 음차(「푸시」)도 옮김(「올리는 것」)도 아닌 원어입니다

기존 시험 중 옛 판정을 못박고 있던 것들도 함께 갱신했습니다. `test_worker_settings` 의 `test_push_is_called_by_the_screen_name` 은 원래 "「푸시」 대신 「올리는 것」으로 쓴다"를 지키고 있었는데, 이제 **음차도 옮김도 둘 다 거부하고 원어를 요구**합니다. `test_workspace_chip` 의 단언 7곳도 「commit 하면」·「worktree」로 바꿨습니다 — 안 고치면 찾는 글자가 화면에서 사라져 죽은 단언이 됩니다.

한 가지 사과드릴 일이 있습니다. 서버를 재시작하려고 `pkill -f "s9 serve --host"` 를 썼는데, 그 명령줄 자체가 같은 글자를 담고 있어서 **제 셸까지 잡았습니다**. supervisor 가 서버를 즉시 되살려 피해는 없습니다만, 경솔했습니다.

전체 회귀가 끝나면 커밋하겠습니다. REQ-20260901-022-62x6(설정 화면)가 아직 확인 대기 중인데 그 화면의 낱말이 이번에 바뀌었으니, 판정하실 때 F5 후 새 낱말로 보시게 됩니다 — 그 문서에도 적어 두겠습니다.

### 2026-09-02T07:47:18+09:00 commit (by nicehugepark)

커밋 98b4d8a — 남의 도구가 지은 이름은 그대로 세운다 — 화면 용어 원어 복원 (REQ-20260902-002-62x6)

### 2026-09-02T07:48:23+09:00 commit (by nicehugepark)

커밋 c66752c — 용어 판정 개정을 지식·관련 문서에 남긴다 (REQ-20260902-002-62x6)

### 2026-09-02T07:48:41+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-09-02 07:48:40 KST - lead]`

전체 스위트 green 이고, 세 건 커밋했습니다.

| REQ | 상태 | 커밋 |
|---|---|---|
| 002 기술 용어 번역 금지 | **review** (판정 부탁드립니다) | `98b4d8a` `c66752c` |
| 003 문지기가 글을 명령으로 읽는다 | **done** | `809b032` |
| 001 확인 창 초점 대장 누락 | **done** | `01ea47e` |

**확인 부탁드릴 것 하나** — F5 후 Settings → 「무인 작업」에서 넷째 줄이 **「worktree 쓰게 하기」**로 서고, 그 밑 회색 글자(`worker_worktree`)와 같은 낱말을 씁니다. 셋째 줄 설명에는 「저장소에 **push** 하는 것도」가 보입니다.

곁들여 처리한 것:

- **guide.html 도 음차 0**으로 만들었습니다(푸시·커밋·클론·리모트·업스트림·머지 → 원어). 「리포」만 「저장소」로 갔는데, 빌려 온 명령 이름이 아니라 줄임말이기 때문입니다
- **DOC-20260830-006-62x6**(화면 조어 판정 시금석)에 적용 범위를 못박았습니다 — 그 시금석이 남의 도구 이름에까지 대어진 것이 이번 사고의 뿌리라, 문서를 고치지 않으면 다음 사람이 같은 자리에서 또 옮깁니다
- **022·023 문서에도 낱말 변경을 적었습니다** — 확인 대기 중인 판정이 옛 낱말을 기준으로 이뤄지지 않도록

REQ-20260901-023-62x6(화면에서 pull·push)은 낱말이 정해졌으니 blocked 를 풀 수 있습니다. 다만 그 문서는 022 프론트가 착지한 뒤에 구현한다고 못박아 뒀고 022 가 아직 판정 대기라, **022 를 승인하시면 023 구현에 들어가겠습니다.**

### 2026-09-02T07:50:47+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-09-02 07:50:46 KST - lead]`

보드에서 옮기신 두 건, 다 집었습니다.

**REQ-20260901-025-62x6 「도는 일은 끝까지 지켜본다」 → done**

원문("돌고 있는 중이라면 무사히 끝날때까지 일단 지켜본다")은 그때 한 번의 답으로 끝날 말이 아니라 작업 방식이라, 세션이 바뀌어도 남는 자리에 넣었습니다 — `pref_작업방식`. 이번 응답의 훅 주입에 이미 실려 온 것으로 작동을 확인했습니다.

기계 쪽은 이미 같은 편이었습니다: 백그라운드 작업은 끝나면 알림이 오고, 반려 워처는 **클레임되지 않은** 것만 스폰하며, 대화형 세션에 SIGINT 는 킬이라는 앞선 판정(DOC-20260830-007-62x6)도 같은 계열입니다. 남은 건 리드의 규율뿐이었고, 그걸 문서가 아니라 **매 턴 주입되는 설정**으로 옮긴 것이 이 요청의 답입니다.

**REQ-20260901-024-62x6 「폴마다 프로세스 전수 훑기」 → 착수**

방금 닫은 020(연결 누수)의 자식입니다. 앞을 막던 것이 치워지자 다음 병목이 드러난 건데, 폴 한 바퀴가 **세션 수만큼** `/proc` 을 전수 훑고 그것이 **동시 요청 수만큼** 겹칩니다. 실측 스택 100개 중 32개가 그 안에 있었고 16개가 뒤에 줄 서 있어, 요청이 90~180초씩 걸립니다.

backend-developer 에 위임했습니다. 짐작으로 고치지 말고 먼저 스택으로 재라고, 그리고 캐시가 답인지는 **신선도 비용을 재고 판정**하라고 지시했습니다 — 주석이 캐시를 안 하는 이유를 명시해 뒀기 때문입니다. 끝나면 알림이 오니 들여다보며 흔들지 않고, 착지하면 제가 직접 재서 보고하겠습니다.

### 2026-09-02T08:22:21+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-09-02 08:22:20 KST - lead]`

물어보신 것부터 — **「무인 작업」 = background worker 해석**, tech-writer 에 넘겼습니다(`REQ-20260902-005-62x6`). 지적하신 "여러 군데서 쓰인다"가 이 물음의 핵심이라, 쓰이는 자리를 전수로 훑어 ① 사람 없이 뜨는 헤드리스 세션 ② 그 세션이 하는 일 ③ 그것을 켜고 끄는 정책 — 이 셋 중 무엇을 가리키는지 자리마다 가르라고 했습니다. 셋이 섞여 있다면 그 자체가 답입니다. 판정 오면 바로 전하겠습니다.

**REQ-20260901-024-62x6 (폴마다 프로세스 전수 훑기)** 는 backend-developer 가 마쳤고, 제가 직접 재서 확인했습니다.

답이 TTL 캐시가 아니라 **범위**였던 게 핵심입니다. 낡음을 사지 않고 중복만 지웁니다 — 요청 하나가 도는 동안만 표를 나눠 쓰고(`proc_scope`), 동시에 겹친 훑기는 하나로 접습니다.

제 실측(에이전트 보고와 별개로 다시 잰 것):
- `proc` 계기의 **동시 진입 최고치 1** (전 32)
- `/api/chat/target` 8동시 **12.75초 → 0.19초**
- 새 시험 `test_proc_scan.py` **17/17**

**다만 검증 중에 다른 것을 봤습니다.** 실서버가 순차 요청의 약 7%를 붙자마자(12~16ms) 끊습니다. 갈래를 안 가리고 서버 로그에 예외도 없습니다. 두 후보를 실측으로 지웠습니다:

- **회선 아닙니다** — 맨 파이썬 서버 대조 39/40, 같은 시각 s9 는 33/40
- **024 변경 아닙니다** — 같은 코드를 새 포트에 띄워 A/B: HEAD 코드 120/120, 새 코드 120/120, 둘 다 깨끗

남은 건 **오래 살아 감시자가 붙은 그 인스턴스에만 있는 무엇**입니다. 020 이 고친 병(연결이 쌓여 스레드를 못 놓음)과는 다른 병이고, 020 의 계기로는 안 보입니다(live 4·threads 7 로 건강합니다). `REQ-20260902-006-62x6` 으로 세워 뒀습니다 — 짐작으로 고치지 말고 누가 끊는지 먼저 지목해야 합니다.

024 는 전체 회귀가 끝나는 대로 커밋하고 닫겠습니다.

### 2026-09-02T08:26:37+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-09-02 08:26:36 KST - lead]`

**맞게 읽으셨습니다.** 「무인 작업」은 서버(또는 눌러서 띄운 ▶)가 만드는 background worker 프로세스 그것이고, 화면에서 그 낱말이 선 62자리를 전부 훑어 보니 **자리마다 다른 것을 가리키지도 않았습니다** — 전부 같은 한 물건입니다(pid 가 있고, 죽일 수 있고, `s9 workers` 가 세는 그것).

화면이 「워커」라고 안 쓰는 이유는 하나입니다. **이 화면에서 「background」는 아무것도 가르지 못합니다** — 대시보드에서 도는 것이 셋인데(무인 작업 · 맡은 창 · 나눠 맡은 일손) 셋 다 화면 밖에서 돕니다. ⏸ 를 누를 때 답이 갈리는 축은 "어디서 도나"가 아니라 **"말 걸 사람이 있나"**입니다. 무인 작업은 끊으면 거기서 끝나고, 맡은 창·일손은 "전하면" 마치고 멈춥니다. 「무인」이 그 축을 이름에 싣습니다.

**다만 "여러 군데서 쓰인다"고 느끼신 건 착각이 아닙니다.** 가리키는 건 하나인데 **부르는 글자가 표면마다 셋**이었습니다:

- 대시보드는 「무인 작업」(47자리)
- 가이드는 「무인 작업**자**」와 「**워커**」 — 둘째·셋째 이름입니다
- 그리고 **서버가 ▶ 실패 창으로 내보내는 문장 셋은 아직 「자동 작업」**이라고 말합니다 — 8/31 에 폐기한 그 오칭입니다

셋째가 진짜 결함이고, 뿌리가 하나 더 있습니다. 낱말 게이트(`test_screen_lexicon`)가 **`web/` 만 훑습니다.** 이 저장소는 이미 「서버의 문장이 곧 팝업이다」(커밋 `87fa5ab`)를 배웠는데, 그 교훈이 게이트의 눈에는 안 들어가 있었습니다. `bin/s9` 의 사용자 메시지도 게이트가 보게 하는 것이 재발 방지입니다.

지금 REQ-024 회귀가 `bin/s9` 를 재고 있어서, **그게 끝난 뒤에** 여섯 자리를 고치겠습니다 — 도는 걸 흔들지 않는 게 규율이니까요. 순서는 024 커밋 → 낱말 여섯 자리 + 게이트 확장입니다.

### 2026-09-02T08:29:46+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-09-02 08:29:46 KST - lead]`

「무인」은 내리겠습니다. 이름이 주인 귀에 거슬리면 그걸로 실패한 이름입니다.

다만 **「백그라운드 프로세스」로 바꿔도 문제 하나가 그대로 남습니다.** 이 대시보드에서 도는 건 셋인데 — 그 프로세스 · 맡은 창 · 나눠 맡은 일손 — **셋 다 백그라운드**입니다. 그래서 「백그라운드」가 아무것도 가르지 못하고, 「프로세스」로 바꿔도 나머지 둘도 그렇게 불릴 자격이 생깁니다. ⏸ 를 누를 때 답이 갈리는 건 "어디서 도나"가 아니라 "말 걸 사람이 있나"라서요.

대안 하나를 놓고 골라 주시면 좋겠습니다:

**① `worker` (원어, 소문자)** — 오늘 아침에 세운 그 자를 그대로 댄 결과입니다. "화면을 닫고 그 일을 하려면 무엇을 쳐야 하는가" → 이 물건은 `s9 workers` 로 셉니다. 남의 도구가 아니라 **우리 CLI 가 이미 그렇게 부르는 이름**이라, 화면이 같은 글자를 쓰면 터미널과 이어집니다. 「무인 작업 맡기기」는 「`worker` 맡기기」, 「무인 작업이 쓰는 모델」은 「`worker` 가 쓰는 모델」이 됩니다.

**② 「백그라운드 프로세스」** — 말씀하신 대로. 익숙하고 오해가 없지만, 위의 못 가름이 남고 낱말이 깁니다(설정 판의 행 이름으로 쓰기엔 무겁습니다).

제 추천은 **①** 입니다. 오늘 pull·push·worktree 에 댄 자와 같은 자이고, 이 물건만 콕 집습니다.

고르시면 화면 62자리와 가이드·서버 문장까지 한 번에 갈아 끼우겠습니다. 그때 tech-writer 가 찾아낸 결함 셋(가이드의 「무인 작업자」·「워커」 혼용, 서버가 팝업으로 내보내는 폐기어 「자동 작업」, 낱말 게이트가 `bin/s9` 를 안 보는 것)도 같이 정리합니다 — 지금은 도착지가 정해져야 움직일 수 있어서 멈춰 뒀습니다.

### 2026-09-02T08:31:58+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-09-02 08:31:57 KST - lead]`

정당한 반문입니다. 제 주장의 전제를 겨누셨습니다 — 셋을 갈라야 한다는 것부터가 검증된 적 없는 제 말이었습니다.

`REQ-20260902-007-62x6` 으로 세우고 **product-owner 에 독립 판정을 맡겼습니다.** 제가 직접 보면 제 주장을 변호하게 되니, 지시문에 "리드의 주장을 변호하지 말고 깨뜨려 보라, 틀렸으면 틀렸다고 판정하라"고 못박았습니다. 화면에 이름이 셋 서 있다는 사실도 근거가 아니라 검토 대상이라고 했습니다 — 누군가 그렇게 지어 놓았을 뿐일 수 있으니까요.

판정 갈래는 셋으로 두었습니다:

- **(A)** 갈릴 이유가 있다 → 어느 자리에서 어떤 결정이 갈리는지 구체적으로
- **(B)** 한두 자리뿐이다 → 나머지는 상위어 하나로
- **(C)** 갈릴 이유가 없다 → 이름 하나면 된다

특히 **⏸ 를 눌렀을 때 실제로 결과가 다른지를 코드로 확인**하라고 했습니다. 제가 "무인 작업은 끊으면 끝, 맡은 창·일손은 전하면 마치고 멈춘다"고 말했는데, 그게 참인지 아니면 같은 자리에 이름만 셋인지가 이 판정의 핵심입니다.

그리고 하나 짚어 둘 게 있습니다 — 이미 셋을 묶는 상위어 **「도는 일」**이 8/31 결정으로 서 있습니다. 그게 있다면 (B)나 (C)가 답일 가능성이 낮지 않습니다. 그 사실도 판정에 넣으라고 했습니다.

판정 오는 대로 전하겠습니다. 이름을 무엇으로 할지(`worker` / 백그라운드 프로세스 / 그 밖)도 그 답에서 따라 나오게 하는 게 순서라, 낱말 교체는 그때까지 멈춰 뒀습니다.

### 2026-09-02T08:36:28+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-09-02 08:36:27 KST - lead]`

범위를 그렇게 그으신 게 정확합니다 — 서버가 무엇을 알고 무엇을 로그로 남기느냐는 손댈 일이 아니고, **화면이 사람에게 무엇을 보여 주는가**만 논점입니다. 두 문제를 섞어서 "시스템이 셋을 구분하니 화면도 셋을 불러야 한다"고 말한 게 제 오류였습니다.

`REQ-20260902-008-62x6` 으로 세우고 **designer 에 UX 관점 검토**를 맡겼습니다. 007(product-owner)과 렌즈를 갈라 놨습니다:

- **007 — 제품 관점**: 셋에 따라 사용자의 *결정*이 갈리는가
- **008 — 화면 관점**: 무엇이 눈에 들어오고, 무엇을 누르게 되고, 무엇을 못 찾는가

designer 에는 소스만 읽고 판정하지 말고 **실제 화면을 찍어서 눈으로 보라**고 했습니다. 그리고 자리마다 하나만 묻게 했습니다 — **"그 이름을 지우면 사람이 못 하게 되는 것이 있는가."** 없으면 그 이름은 화면에 있을 이유가 없습니다.

판정이 (B)나 (C)로 나오면 **그 자리의 화면이 어떻게 달라지는지 그림까지** 내라고 했습니다. "이름을 지웁시다"만으로는 무엇이 그 자리에 서는지 알 수 없으니까요.

여기서도 제 잠정 읽기는 그대로 넘겼되 "그대로 받지 말고 직접 판정하라"고 붙였습니다 — 오늘 제 판단이 두 번 뒤집혔으니 세 번째도 뒤집힐 수 있습니다.

둘 다 오면 나란히 놓고 전하겠습니다.

### 2026-09-02T08:41:09+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-09-02 08:41:09 KST - lead]`

두 가지를 함께 보고드립니다.

**① 제가 틀렸습니다 — 007 판정이 코드로 반증했습니다.**

제가 "셋 다 백그라운드다"라고 반박했는데, **코드는 반대**입니다. `bin/s9:3481` 주석: *"스폰된 워커는 headless 라 inbox tail 도 attach_pid 도 없어 chat_live 가 서지 않는다."* 맡은 창은 터미널 탭에 보이고 나눠 맡은 일손도 눌러서 열어 볼 수 있는데, **서버가 띄우는 그것만 볼 창이 없습니다.** 「백그라운드」는 아무것도 못 가르는 낱말이 아니라 **코드가 실제로 긋는 그 선**이었습니다.

그리고 처음 물으신 것 — 셋을 갈라 알아야 하나 — 의 답은 **자리 9개 전수에서 셋이 갈리는 자리 0개**입니다. ⏸ 툴팁·확인 창에서만 갈리는데 거기서도 **셋이 아니라 둘**이고(끊긴다 / 전하면 마치고 멈춘다), 「맡은 창」과 「나눠 맡은 일손」은 확인 창 버튼 글자까지 같습니다. 무게도 12배 차이입니다 — 「무인 작업」 47자리 vs 나머지 둘은 상수 하나 안의 4문장씩. 개념이 아니라 단추 하나의 각주였습니다.

**예외는 설정 판 하나**입니다. 거기는 지금 도는 것이 아니라 **아직 태어나지 않은 것**에 권한을 주는 자리라 상태로는 가리킬 수 없습니다. 다만 거기서 필요한 이름은 **하나**입니다.

이름 권고는 **「백그라운드 작업」**입니다(「프로세스」는 컴퓨터 사정을 말하고 여덟 자라 47자리에 무겁습니다). 제가 밀었던 `worker` 는 "일하는 것"이라 축을 못 싣습니다 — 맡은 창도 일하니까요. designer 의 화면 관점 판정이 오면 합쳐서 최종안을 올리겠습니다.

**② 기본 세팅이 개인 선호에 있던 것 — 고쳤습니다** (`REQ-20260902-009-62x6`).

지적이 정확했고, 제 분류 오류였습니다. `pref_*` 는 audit 훅이 **그 사용자에게만** 주입하므로, 하네스 기본값이 거기 있으면 다른 설치·다른 사용자·Gemini/Codex 에는 그 규율이 아예 없습니다.

셋(말투 · 용어 · 작업방식)을 `PROTOCOL.md` 의 **「말과 태도」** 절로 올리고 CLAUDE.md 를 재동기화했습니다. 개인 설정 슬롯의 셋은 비웠습니다 — 이제 거기엔 그 사람만의 것이 남습니다. 사본이 드리프트하면 red 가 되는 계약 시험 둘도 붙였습니다.

첨부하신 이미지는 업로드 경로에 도착하지 않았습니다(`state/terminal/uploads/…083736-image.png` 없음). 글만으로 판정이 서서 진행했지만, 첨부가 유실되는 경로라면 따로 볼 일이라 노트에 적어 뒀습니다.

### 2026-09-02T08:49:33+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-09-02 08:49:33 KST - lead]`

두 판정이 같은 곳에 도착했고, 화면 쪽이 한 걸음 더 갔습니다.

**핵심은 이겁니다: 세 이름을 지탱하던 구별이 화면에 한 번도 그려진 적이 없습니다.** 제가 근거로 든 ⏸ 툴팁 4벌 문장이 전부 `title` 속성이라, 손을 1초 얹고 기다린 사람에게만 뜨고 터치에서는 아예 없습니다. designer 가 실포인터로 눌러 찍어서 확인했습니다. 카드가 그리는 것은 점 하나·⏸ 하나·경과 시간 하나뿐이고 네 갈래가 화소 단위로 같습니다 — **"실행 중인가 멈췄는가만 중요하지 않나"는 새 요구가 아니라 화면이 이미 내린 답이었습니다.**

한 가지만 보태야 합니다. **상태는 둘이 아니라 셋**입니다 — 멈춘 것 안에서 「저절로 다시 이어진다」와 「내가 눌러야 이어진다」가 갈립니다. 이건 화면 밖에서 알 길이 없고 다음 행동을 바꾸는 유일한 구별이라, 점의 얼굴로 이미 갈려 있습니다.

**제 설명도 절반만 맞았습니다.** 설정 판이 예외인 이유를 "권한을 주는 자리라서"라고 했는데, 진짜 이유는 **거기서만 그것이 눈에 안 보이기 때문**입니다. 맡은 창도 일손도 Terminal 탭에서 눌러 볼 수 있는데 서버가 띄우는 그것만 볼 창이 없습니다. **이름은 볼 창이 없는 것의 대용품**입니다. 그래서 하나면 되고, 그 하나도 판 제목에 한 번이면 됩니다 — 지금 그 판 한 화면에 같은 낱말이 **열 번** 서 있습니다.

그래서 **이름 싸움이 거의 사라집니다.** 낱말이 화면에서 열 번→한 번이 되면 `worker` 냐 「백그라운드 작업」이냐의 무게가 크게 줍니다. 제가 밀었던 `worker` 는 철회합니다 — "일하는 것"이라 축을 못 싣습니다(맡은 창도 일하니까요). 권고는 **「백그라운드 작업」**입니다.

바뀔 화면은 이렇습니다:

| 자리 | 지금 | 바꾼 뒤 |
|---|---|---|
| 카드·헤더·Terminal | 이름 없음 | 그대로 |
| ⏸ 손 위 글 | 4벌, 주체를 부름 | 2벌, 무주어 |
| 문서 예고 줄 | 「무인 작업이 떠서 이어받습니다」 | 「이 요청은 저절로 이어집니다」 |
| 계정 교체 창 | 「진행 중인 무인 작업 3건」 | 「이 창 밖에서 도는 작업 3건」 |
| 설정 판 | 이름 10회 | 제목에 1회 |

이대로 갈까요? 승인하시면 이 표대로 고치고, 함께 드러난 결함 넷(가이드의 「무인 작업자」·「워커」 혼용, 서버가 팝업으로 내보내는 폐기어 「자동 작업」, `terminal.js` 의 넷째 이름 「에이전트」, 낱말 게이트가 `bin/s9` 를 안 보는 것)도 같은 판에서 정리하겠습니다.

### 2026-09-02T08:52:41+09:00 ask (by nicehugepark)

백그라운드 작업, 백그라운드 에이전트 뭐든 제대로 검토를 해줘. 리드 에이전트 아니고 관련 에이전트들이 모여서 판단해. 그리고 이것 관련 요청들이 여러개 열려있는데 순서를 잘 정하거나 통합하거나 하고.

### 2026-09-02T08:54:37+09:00 subagent (agent: sub:subagent:ae763e2a, by nicehugepark)

Reading REQ-20260902-002-62x6 verdict notes

### 2026-09-02T08:55:58+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-09-02 08:55:58 KST - lead]`

판정에서 손 떼고, 순서·통합부터 정리했습니다.

**요청 정리** — 이 계보가 여섯 개로 흩어져 있었습니다. 판정이 끝난 둘을 닫고, 하나를 주 요청으로 세웠습니다.

| 요청 | 상태 | 역할 |
|---|---|---|
| **005 무인 작업 이름 재판정** | **진행 중 (주 요청)** | 이름과 적용을 여기서 결정 |
| 007 셋을 갈라 알 이유가 있나 | **done** | 산출물이 판정 그 자체 → 005 의 입력 |
| 008 상태만 보이면 되는가 | **done** | 같음 → 005 의 입력 |
| 002 기술 용어 번역 금지 | review | pull·push 는 이미 적용·커밋됨 |
| 022 설정을 화면에서 바꾼다 | review | 그 판의 낱말이 005 에서 바뀜 |
| 023 화면에서 pull·push | blocked | 022 승인 뒤 구현 |

007·008 은 005 의 `derived_from` 으로 이었습니다 — 나중에 "왜 이렇게 정했나"를 문서에서 되짚을 수 있게요.

**판정단** — 셋이 각자 판정을 005 문서에 남기고, **tech-writer 가 그 셋을 읽어 최종 판정**하는 구조로 세웠습니다. 리드는 조율만 하고 이름을 고르지 않습니다.

- **translator** — 어휘 수준. 이 제품의 사용자는 관리자가 준 URL 로 들어오는 팀원이고 개발자가 아닐 수 있습니다. 그 사람에게 「백그라운드」가, 「에이전트」가 통하는 말인지. 음차 조항이 이 낱말에 걸리는지 안 걸리는지도 못박게 했습니다
- **ux-writer** — 문장. **낱말만 놓고 고르지 말고 문장이 고르게 하라**고 했습니다. 후보를 실제 문장에 넣어 보고 어디서 걸리는지 보고하는 형식입니다. 설정 판은 전체 문안을 다시 쓰게 했습니다
- **designer** — 화면. 이름이 실제로 서는 세 자리에 후보를 얹어 길이·줄바꿈·충돌을 캡처로 확인합니다. 특히 **「에이전트」는 이 화면에 이미 다른 뜻으로 서 있습니다**(`terminal.js` 가 나눠 맡은 일손을 그렇게 부릅니다) — 그 충돌을 판정하게 했습니다

셋에게 같은 물음 하나를 더 던졌습니다: **「무인」이 지고 있던 축(볼 창이 없다)을 잃지 않으면서 거슬리지 않는 말이 있는가, 아니면 그 축은 이름이 아니라 옆의 한 줄이 져야 하는가.** 이게 이 판정의 진짜 갈림길로 보입니다.

넷이 다 들어오면 최종 판정만 전하겠습니다.

### 2026-09-02T09:02:23+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-09-02 09:02:22 KST - lead]`

판정 셋 중 둘이 들어왔고, **둘 다 「백그라운드 작업」**입니다. 근거는 서로 다른 데서 나왔습니다.

**translator (어휘 수준)** — 「백그라운드」가 통하는 근거가 개발 용어가 아니라 **그 사람이 매일 보는 휴대폰 설정 화면**이라는 겁니다. 안드로이드 「백그라운드 제한」, iOS 「백그라운드 앱 새로 고침」, 윈도우 「백그라운드 앱」. 배터리 화면을 한 번 연 사람은 이 낱말을 "화면에 안 보이는데 계속 도는 것"으로 이미 배웠고, **그 일상 뜻이 코드가 긋는 선과 같습니다.** 음차 조항은 안 걸린다고 한 줄로 못박았습니다 — "음차 반려는 밖에서 그 글자를 그대로 쳐야 하는 이름에만 걸린다. `git background` 라는 명령은 없다."

**ux-writer (문장)** — designer 의 "화면에 안 그려진다"가 **반쪽**이라고 정정했습니다. `title`·`aria-label` 은 **낭독기에는 그려집니다.** "카드는 이름 0"은 마우스를 쓰는 눈 있는 사람에게만 참이었습니다. 그래서 이 이름은 유물로 줄어들 수 없고 **소리 내어 읽히는 문장 안에서 성립해야** 하는데, 그 자가 후보를 갈랐습니다 — `worker` 는 낭독기가 철자로 뱉어서 탈락, 「에이전트」는 같은 화면이 이미 다른 것을 그렇게 부르고 있어 탈락입니다.

**한 가지 사용자님께 여쭐 것이 있습니다.** 말씀하신 건 「백그라운드 **프로세스**」였는데 판정단은 「백그라운드 **작업**」으로 한 글자를 바꿨습니다. 까닭은 「프로세스」가 컴퓨터 사정을 말하고(사람 사정이 아니라), 「작업」은 이 화면이 이미 쓰는 말이라는 것입니다. translator 가 "주인의 귀가 최종심이니 이 바꿈을 반드시 드러내라"고 넘겼습니다 — **「프로세스」가 더 나으시면 말씀해 주십시오.**

designer 판정이 남았고, 그다음 tech-writer 가 셋을 읽고 최종 판정합니다. 적용할 때 걸릴 것 셋도 이미 잡혔습니다: 확인 창 갈래를 합치면 확인 없이 중단되는 안전 함정 하나, 개칭을 막는 시험 하나, 개칭하면 조용히 죽는 시험 하나.

곁들여 **REQ-20260902-009-62x6(기본 세팅) 는 done 으로 닫았습니다**(`a807a02`).

### 2026-09-02T09:16:21+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-09-02 09:16:21 KST - lead]`

판정단 셋이 다 들어왔고, **셋 다 「백그라운드 작업」**입니다. 서로 다른 자로 재서 같은 답에 닿았습니다.

- **어휘** — 「백그라운드」가 통하는 근거는 개발 용어가 아니라 휴대폰 설정 화면입니다(안드로이드 「백그라운드 제한」, iOS 「백그라운드 앱 새로 고침」). 그 일상 뜻이 코드가 긋는 선과 같습니다
- **문장** — `title`·`aria-label` 은 낭독기에는 읽힙니다. "카드는 이름 0"은 마우스 쓰는 눈 있는 사람에게만 참이었고, 그래서 이 이름은 **소리 내어 읽히는 문장 안에서 성립해야** 합니다. `worker` 는 낭독기가 철자로 뱉어서 탈락입니다
- **기하** — 살아 있는 DOM 에서 글자마다 실제 렌더 줄을 복원해 쟀습니다. 「백그라운드 에이전트」는 확인 창(432px 고정)에서 **이름이 반으로 갈립니다**. 그리고 「작업」이 이긴 결정적 근거는 **되돌리기 비용**입니다 — 받침을 지켜서 화면의 조사 52곳이 그대로 유효하니, 낱말 하나의 치환이고 틀리면 한 번에 되돌아옵니다

지금 **tech-writer 가 셋을 읽고 최종 판정** 중입니다. "셋이 모였다고 자동 통과시키지 말고 함께 놓친 것이 없는지 보고, 있으면 뒤집으라"고 했습니다.

**여쭐 것 하나는 그대로 열려 있습니다** — 말씀하신 건 「백그라운드 **프로세스**」인데 판정단이 「작업」으로 한 글자 바꿨습니다. 그 바꿈을 최종 보고 첫머리에 드러내라고 지시했습니다. 주인의 귀가 최종심이니, 「프로세스」가 나으시면 말씀만 주십시오.

적용 전에 치워야 할 것 하나도 찾았습니다: Terminal 탭에서 `/` 를 치면 **`/tasks 백그라운드 작업 목록`이 이미 서 있습니다.** 그건 클로드 코드 자체의 background task 지 우리 것이 아니라, 그 줄을 비켜 놓지 않으면 오늘 걷어내는 것과 똑같은 결함(한 글자가 두 개념을 덮는 것)을 새로 만듭니다.

### 2026-09-02T09:24:43+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-09-02 09:24:43 KST - lead]`

최종 판정 나왔습니다 — **「백그라운드 작업」**.

**먼저 말씀하신 낱말과 다른 점 하나.** 말씀하신 건 「백그라운드 **프로세스**」였고 판정은 「백그라운드 **작업**」으로 한 글자 바꿨습니다. 까닭 둘입니다. ① 「프로세스」는 컴퓨터가 자기를 세는 단위라, 이 화면이 **이미 팝업 금지어로 세워 둔 무리**에 듭니다(`pid`·`머신`·`본 저장소`와 같은 계열). ② 뒷말 「작업」을 두면 화면에 붙은 **조사 52곳이 그대로 맞습니다** — 뒷말을 갈면 그 52곳을 손으로 고쳐야 하고, 조사 하나 틀린 문장은 어떤 시험도 못 잡습니다. 되돌리시려면 뒷말만 바꾸면 되고, 값은 그 52곳입니다.

**판정단이 모였다고 자동 통과시키지 않았습니다.** tech-writer 가 셋이 함께 놓친 것 넷을 찾았고 하나는 뒤집었습니다:

- **뒤집음** — designer·ux-writer 가 창머리로 제안한 「작업 자리」는 **오늘 아침 내려간 별명**입니다. 파일 주석이 그렇게 적어 두었는데 둘 다 못 봤습니다
- 깨지는 시험이 **넷 더** 있습니다(ux-writer 는 하나만 짚었습니다)
- **자기 앞선 주장도 철회**했습니다 — `worker` 옹호를요. "밖에서 다시 안 만나는 글자를 원어로 세우는 것이 무차별 원어다"

그리고 이게 제일 중요한 발견입니다. **이건 재발입니다.** 팝업 금지어 목록에 **「무인 작업자」가 이미 들어 있습니다** — REQ-20260830-007-62x6 에서 사용자님이 "일반 사용자는 이해할 수 없다"고 하신 그 창입니다. **같은 물건의 이름을 두 번째로 거부하신 것**이고, 그런데 아무도 재발 방지를 세우지 않아서 오늘 또 온 겁니다. 이번엔 게이트를 세웁니다.

「무인 재작업」(흐름 이름)은 **새 이름을 안 짓습니다.** 이름은 볼 창이 없는 것에만 주고, 흐름은 문장이 말합니다 — 「반려하면 저절로 다시 합니다.」

적용 목록은 순서까지 정해져 문서에 있습니다: 선행(`/tasks` 줄 비우기) → 설정 판 → 카드·문서 → 서버 문장 → 가이드 두 벌 → 시험. 되돌릴 수 없는 중단이 확인 창 없이 실행되는 안전 함정 하나도 목록 안에 잡혀 있습니다.

**「작업」으로 갈지, 「프로세스」로 되돌릴지만 말씀해 주시면 바로 적용에 들어가겠습니다.**

## History
- 2026-09-02T07:00:19+09:00 created by nicehugepark (status: open)
- 2026-09-02T07:03:18+09:00 status: open -> in-progress (by nicehugepark) — 사용자 지적 — tech-writer 판정표가 pull·push 를 옮긴 것을 뒤집는다. 규칙 원천부터 고친다
- 2026-09-02T07:47:36+09:00 status: in-progress -> review (by nicehugepark) — 화면 낱말을 원어로 되돌렸습니다. F5 후 Settings → 「무인 작업」을 보세요 — 넷째 줄이 「worktree 쓰게 하기」로 서고 그 밑 회색 글자(worker_worktree)와 같은 낱말을 씁니다. 셋째 줄 설명에는 「저장소에 push 하는 것도」가 보입니다. 낱말이 밖에서 쓰는 말과 이어지면 승인, 여전히 어색하면 그 줄을 캡처해 반려해 주세요.
- 2026-09-02T10:12:41+09:00 status: review -> done (by nicehugepark) — 사용자 지시("스스로 진행할 수 있는 것은 진행")에 따라 리드가 닫는다. goal 충족: 화면·문서·요청 제목에서 pull·push·commit·worktree 가 원어로 서고(캡처로 확인: worktree 쓰게 하기 / push 하는 것도), 같은 번역이 다시 들어오면 test_screen_lexicon 이 막는다(음차 8 BANNED + 채택어 고정점 3). 전체 스위트 green. 커밋 98b4d8a·c66752c.
