---
id: REQ-20260901-004-62x6
type: request
title: 전이 유실 — 출력과 문서 불일치
summary: 2026-09-01 02:03 REQ-20260831-028 done 전이가 CLI 출력으로는 성공(in-progress -> done)했으나 문서 History·status 에 남지 않았다 — 같은 명령의 앞 전이(030 done)는 정상 기록. 잠금 하 원자 쓰기 계약이 깨진 자리를 규명한다
goal: 유실 재현 경로가 규명되고, 전이 기록이 원자적으로 남거나 실패가 출력으로 드러나는 회귀 시험이 선다
status: done
size: M
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: 3ed7d9fd
sessions: ["38ad98c8", "ccfb1280", "3ed7d9fd"]
agents: ["sub:deep-diver:aa7130d5", "sub:deep-diver:a6d4aa35", "sub:subagent:a73931cf", "sub:deep-diver", "sub:subagent:a67dd1ad"]
contributions: [{"actor": "sub:deep-diver:aa7130d5", "item": "004 전이 유실 규명", "started": "2026-09-01T10:24:55+09:00", "ended": "2026-09-01T11:24:45+09:00", "result": "failed", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/38ad98c8-51fd-4b50-a26a-495b58404817/tasks/aa7130d5ceb3f02ac.output", "reason": "기록 자리가 사라졌다 (transcript 경로가 없다)"}, {"actor": "sub:deep-diver:a6d4aa35", "item": "004 전이 유실 재규명", "started": "2026-09-01T12:52:49+09:00", "ended": "2026-09-01T13:08:27+09:00", "result": "done", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/3ed7d9fd-1679-421a-a506-cec3973f8c08/tasks/a6d4aa35a1a38a9db.output"}, {"actor": "sub:subagent:a73931cf", "item": "Writing findings note to REQ-20260901-004-62x6", "started": "2026-09-01T13:06:38+09:00", "ended": "2026-09-01T13:06:38+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/3ed7d9fd-1679-421a-a506-cec3973f8c08/subagents/agent-a73931cfcfccc359b.jsonl"}, {"actor": "sub:deep-diver", "item": "결론 — 잠금은 죄가 없다. 유실은 s9 밖에서 왔다", "started": "2026-09-01T13:07:36+09:00", "ended": "2026-09-01T13:07:36+09:00", "result": "done", "transcript": ""}, {"actor": "sub:subagent:a67dd1ad", "item": "Verifying note landed in REQ-20260901-004-62x6", "started": "2026-09-01T13:08:12+09:00", "ended": "2026-09-01T13:08:12+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/3ed7d9fd-1679-421a-a506-cec3973f8c08/subagents/agent-a67dd1adb5d2e09dd.jsonl"}, {"actor": "sub:deep-diver:a6d4aa35", "item": "Verified — note landed intact (168-line document), and the working tre", "started": "2026-09-01T13:08:27+09:00", "ended": "2026-09-01T13:08:27+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/3ed7d9fd-1679-421a-a506-cec3973f8c08/subagents/agent-a6d4aa35a1a38a9db.jsonl"}]
project: section9
children: ["REQ-20260901-009-62x6", "REQ-20260901-010-62x6"]
relates_why: {}
created: 2026-09-01T10:24:31+09:00
status_since: 2026-09-01T13:33:01+09:00
updated: 2026-09-01T13:33:01+09:00
priority: 50
---

## Original

실측: 02:03 명령 「s9 status REQ-20260831-030 done && s9 status REQ-20260831-028 done」 둘 다 성공 출력, 030 은 기록됨, 028 은 History 에 부재(현재 status in-progress·status_since 22:52 유지). 그 시각 028 문서에는 B2 에이전트 노트(~01:34)와 리드 검수 노트(01:29)가 이미 있었음. 의심: 동시 쓰기 lost update 또는 전이 후 다른 쓰기의 낡은 사본 덮어쓰기

## Notes

### 2026-09-01T11:30:40+09:00 workspace (by nicehugepark)

본 저장소에서 이어받는다 — 미커밋 코드가 이 저장소의 등뼈에 걸쳐 있어 워크트리 기준선이 낡았다. 커밋하면 다음 라운드부터 작업자가 다시 자기 워크트리를 받는다.

### 2026-09-01T13:06:38+09:00 subagent (agent: sub:subagent:a73931cf, by nicehugepark)

Writing findings note to REQ-20260901-004-62x6

### 2026-09-01T13:07:36+09:00 response (agent: sub:deep-diver, by nicehugepark)

### 결론 — 잠금은 죄가 없다. 유실은 s9 밖에서 왔다

02:05:06 의 done 전이는 **정상으로 기록됐다**. 출력도 문서도 그 순간엔 맞았다.
8시간 뒤 다른 에이전트의 `git stash push` 가 미커밋이던 그 전이를 통째로 걷어
갔고, `git stash drop` 이 그것을 버렸다. 읽기-수정-쓰기 창도, 훅의 동시 append 도,
스냅샷 복원도 아니었다.

### 원인 사슬 (1차 증거 — 버려진 stash 커밋 원문)

버려진 stash `0042d6dd` 가 **아직 살아 있고, 잃어버린 두 전이를 그대로 담고 있다**:

    git log -1 --format='%H %ci %s' 0042d6dd
    → 0042d6dd 2026-09-01 10:21:09 +0900 On main: prjtab-wip
    git show 0042d6dd:vault/requests/2026/08/REQ-20260831-028-62x6.md | grep -E '^status:'
    → status: done          (status_since/updated 둘 다 2026-09-01T02:05:06)
    → - 2026-09-01T02:05:06+09:00 status: in-progress -> done (by nicehugepark) — goal 충족 근거: …

시각표 (에이전트 트랜스크립트는 UTC 기록이다 — `01:21:08Z` = **10:21:08 KST**.
직전 조사자가 이 지점에서 시각을 02시대로 찾다 헤맸다):

| 시각(KST) | 무슨 일 |
|---|---|
| 02:04:17 | 커밋 97d379f — 이 시점 028·030 은 **in-progress** 로 커밋됨 |
| 02:05:06 | 028·030 done 전이 — 정상 기록. 02:05:19 스냅샷이 `status: done` 을 담고 있다 (독립 증거) |
| 10:21:08 | `frontend-developer`(026 Projects 탭, agent a1c7d99c)가 시험을 깨끗한 트리에서 돌려 보려고 `git stash push -m "prjtab-wip"` — **미커밋이던 vault/ 변경 4파일이 함께 걷혀 가고** 작업 트리는 97d379f(=in-progress)로 되돌아감. reflog `10:21:09 reset: moving to HEAD` 가 그 자국 |
| 10:21:2x | 그 20초 사이 s9 훅이 `users/nicehugepark/profile.md` 와 SES 문서를 다시 씀 |
| 10:21:43 | `git stash pop` 실패 — "would be overwritten by merge: users/nicehugepark/profile.md, vault/sessions/…". pop 은 **전부 아니면 전무**라 vault/ 도 함께 안 돌아옴. 첫 pop 은 `>/dev/null 2>&1` 로 출력이 버려져 실패가 보이지도 않았다 |
| 10:22:03 | `git checkout stash@{0} -- <web/·tests/ 자기 파일 12개>` — **자기 것만** 건짐. vault/ 는 목록에 없었다 |
| 10:22:08 | `git stash drop` — 남은 vault/ 변경(전이 2건 + 노트 다수)이 사라짐 |

왜 아무도 몰랐나(원인의 원인): ① 되돌려진 문서는 git 기준 clean 이 되어 **더는
스냅샷도 뜨지 않는다** ② 되돌린 주체가 s9 가 아니라 잠금·History 어디에도 흔적이
없다 ③ 그 에이전트의 봉투에 `git stash 금지` 문구가 **없었다**(그 트랜스크립트에
0회 등장 — 금지는 무인 워커 봉투에만 있고 역할 에이전트 위임에는 안 붙는다).

### 실제 피해 범위 — 보고된 2건이 아니라 4파일 113건

새로 만든 탐지기가 사전 지식 없이 정확히 이것만 찾아냈다:

    REQ-20260831-028-62x6   전이 1건   (11:31 재전이로 상태는 복구, 02:05:06 기록 자체는 소실)
    REQ-20260831-030-62x6   전이 1건 + 커밋 노트 1건   (상동)
    REQ-20260831-026-62x6   노트 20건 (09:07~09:39, tdd·conflict·subagent 포함) ← **지금도 유실 중**
    SES-20260831-001-62x6   세션 기록 90건 (09:08~10:20)                        ← **지금도 유실 중**

### 고친 것 (bin/s9 — 셋 다 한 경계에서)

**① `write_doc` 원자 교체.** 옛 코드는 `open(path,"w")` 로 **먼저 비우고** 그 안에서
`fm_dump` 를 불렀다. 그 사이에 죽으면 남는 건 빈 파일이다 — 실측으로 확인했다
(구코드 대조 시험에서 문서가 `''` 가 됐다). 카탈로그는 이미 tmp+os.replace 였는데
(REQ-20260825-049-62x6) 정작 그 원천인 문서 쓰기가 아니었다 — 파생물이 원천보다 튼튼한
거꾸로 선 구조. 부르는 자리 36곳이 한 번에 고쳐진다. 권한(0644)은 물려준다.

**② 전이 사후 확인.** `do_transition` 이 쓴 것을 디스크에서 다시 읽어 status 와
History 줄을 확인하고, 없으면 성공을 말하지 않는다(RuntimeError → `cmd_status` 가
읽을 수 있는 실패로 종료, 성공 줄은 안 찍힘). 이번 사고를 막지는 못했겠지만
(그때는 실제로 잘 남았다), "출력은 성공인데 문서엔 없음"이라는 상태 자체를
불가능하게 한다.

**③ `s9 snapshot --audit` — 유실 탐지.** 막을 수 없는 것은 드러나게 한다. 30초마다
뜨는 스냅샷이 이미 진실의 독립 사본이라, 지금 문서와 맞대면 보인다. 실 vault
전수 **0.87초**, 유실 있으면 exit 1, 되살리기 명령까지 출력.

설계에서 두 번 접었다(둘 다 실측으로 교정):
- **최신 스냅만 보면 눈이 먼다** — 되돌려진 문서에 뒤늦게 한 줄이 붙으면 그 낡은
  내용이 다시 스냅으로 뜬다(실사고 10:27 스냅이 정확히 그랬다). 보관 벌수 전부의
  합집합과 맞댄다.
- **자국의 신원은 글자가 아니라 시각이다** — 처음엔 줄을 글자로 맞댔더니 유실
  **466건**이 나왔고 전부 거짓이었다: `sjpark1 → nicehugepark` 이름 바꾸기 한 번이
  모든 History 줄의 `(by …)` 를 고쳤을 뿐. 거짓 경보를 내는 탐지기는 꺼진
  탐지기다. 시각으로 바꾸니 466 → 4건, 그 4건이 전부 진짜였다.

### 회귀 시험 — tests/test_transition_durability.py (신규 10건)

    python3 tests/ transition_durability   →  Ran 10 tests in 0.746s / OK / exit=0

**구코드 대조로 물리는지 확인했다**(통과만 하는 시험은 시험이 아니다). 옛 코드
사본에 같은 시험을 걸면 3 FAIL + 4 ERROR — d1_b 는 문서가 통째로 `''` 가 되고,
d2_a·d3_* 는 계약 자체가 없어 못 선다. 격리 후 개별 재확인도 전부 실패:

    OLD  d2_a(안 남은 전이를 성공으로 보고) FAIL · d3_a(되돌림 탐지) FAIL · d3_c ERROR
    NEW  10/10 OK

인접 계약 회귀 185건(claim_clock·state_truth·review_family·catalog_atomic·snapshot·
judge_dialog·dep_edges·poll_snapshot·wake_effect 등) exit=0. 실 vault·git 무조작,
임시 파일 잔여 0.

### 리드 몫으로 남긴 것

**(1) 지금도 유실 중인 110건 — 되살릴 수 있다(내 권한 밖이라 손대지 않음).**
`vault/requests/2026/08` 수정 금지 지시를 받아 실행하지 않았다. stash 커밋은
unreachable 이라 `git gc` 가 언젠가 지운다 — **되살리려면 서두르는 편이 좋다**:

    S9_SESSION=<세션> bin/s9 snapshot --audit          # 무엇이 없는지 먼저 본다
    bin/s9 snapshot --restore vault/requests/2026/08/REQ-20260831-026-62x6.md --at 20260901T095515
    bin/s9 snapshot --restore vault/sessions/2026/08/SES-20260831-001-62x6.md --at 20260901T102046

주의: 026·SES 는 그 뒤로도 새 노트가 붙었다. 통째 restore 는 그 새 노트를 덮는다
(restore 는 지금 것을 먼저 떠 두므로 되돌릴 수는 있다). 안전한 길은 stash 원문에서
없는 자국만 골라 붙이는 것 — 원문은 `git show 0042d6dd:<경로>`.

**(2) 탐지기를 언제 돌릴 것인가 — 판단이 갈려 배선하지 않았다.** 아무도 안 돌리는
탐지기는 없는 탐지기다. 후보 셋(비용 실측 0.87초):
  ㄱ. `s9 digest`(세션 시작) 끝에 붙인다 — 세션마다 1회, 리드가 바로 본다. 세션
     시작이 0.9초 느려진다.
  ㄴ. serve 워처가 이미 30초마다 `snapshot_dirty()` 를 돈다 — 그 옆에서 주기적
     (예: 10분)으로 감사해 로그·수신함에 올린다. 사람의 협조가 필요 없다.
  ㄷ. 수동 전용(지금 상태) — 사고를 의심할 때만.
개인적으로는 **ㄴ + ㄱ**를 권한다(ㄴ가 본체, ㄱ은 눈에 띄는 자리). 다만 화면·알림에
무엇을 띄울지가 설계 결정이라 리드 판정으로 남긴다.

**(3) 뿌리는 아직 열려 있다 — 파괴적 git 명령에 기계 게이트가 없다.** 이번 건은
"규율 문구가 그 에이전트 봉투에 없었다"가 직접 원인이고, 문구가 있었어도
REQ-20260826-021-62x6 이 이미 적어 둔 대로 **규율은 지켜지지 않을 때를 대비하지 못한다.**
`bin/s9-guard` 는 git **pre-commit** 훅이라 작업 트리를 되돌리는 명령
(`stash`/`checkout --`/`reset --hard`/`restore`)은 아예 보지 못한다. 두 갈래:
  ㄱ. PreToolUse Bash 훅으로 이 리포 안에서의 그 명령들을 막는다(리드는 예외 변수로
     통과). 확실하지만 settings.json·설치 경로를 건드리는 하네스 변경이다.
  ㄴ. 역할 에이전트 봉투에도 무인 워커와 같은 금지 문구를 붙인다(gen_roster 원천).
     싸지만 ①과 같은 종류의 방어라 또 새어 나갈 수 있다.
둘 다 이 REQ의 범위 밖이라 손대지 않았다 — 별건 REQ 로 세울지 리드 판정.

### 확신도

원인 규명 **높음** — 추정이 아니라 버려진 stash 커밋 원문·reflog·에이전트
트랜스크립트 원문·스냅샷 4개가 같은 시각표를 가리킨다. 남은 미지수: 10:27:06 에
되돌려진 028 에 무엇이 한 줄 붙었는지(유실 판정에는 영향 없음).

### 2026-09-01T13:08:12+09:00 subagent (agent: sub:subagent:a67dd1ad, by nicehugepark)

Verifying note landed in REQ-20260901-004-62x6

### 2026-09-01T13:08:27+09:00 subagent (agent: sub:deep-diver:a6d4aa35, by nicehugepark)

Verified — note landed intact (168-line document), and the working tree carries only my two intended changes.

`[2026-09-01 13:08:24 KST - deep-diver]`

### 결론: 잠금 계약은 깨지지 않았다 — 유실은 s9 밖에서 왔다

02:05:06 의 done 전이는 **정상으로 기록됐다.** 출력도 문서도 그 순간엔 맞았다. 8시간 뒤 다른 에이전트의 `git stash push` 가 미커밋이던 그 전이를 걷어 갔고, `git stash drop` 이 그것을 버렸다.

**1차 증거 — 버려진 stash 커밋이 아직 살아 있고, 잃어버린 전이를 그대로 담고 있다:**

```
git log -1 --format='%H %ci %s' 0042d6dd
→ 0042d6dd 2026-09-01 10:21:09 +0900 On main: prjtab-wip
git show 0042d6dd:vault/requests/2026/08/REQ-20260831-028-62x6.md | grep '^status:'
→ status: done      (status_since·updated 둘 다 2026-09-01T02:05:06)
```

직전 조사자가 막힌 지점은 **시각 단위**였다 — 에이전트 트랜스크립트는 UTC 기록이라 `01:21:08Z` 가 실제로는 **10:21:08 KST** 다. 02시대에서 범인을 찾고 있었다.

사슬: 02:04:17 커밋(028·030 이 in-progress 로 박제) → 02:05:06 done 전이 정상 기록 → 10:21:08 `git stash push`(트리가 커밋 시점으로 되돌아감) → 10:21:43 `git stash pop` 실패(그 20초 사이 훅이 다시 쓴 파일 둘 때문에, pop 은 전부 아니면 전무) → 10:22:03 `git checkout stash@{0} -- <자기 web/·tests/ 파일만>` → 10:22:08 `git stash drop`.

아무도 몰랐던 이유가 셋: 되돌려진 문서는 git 기준 clean 이 되어 **더는 스냅샷도 안 뜬다**; 되돌린 주체가 s9 가 아니라 잠금·History 어디에도 흔적이 없다; 그 에이전트 봉투에 `git stash 금지` 문구가 **0회** 등장한다(금지는 무인 워커 봉투에만 있고 역할 에이전트 위임에는 안 붙는다).

### 피해는 보고된 2건이 아니라 4파일 113건

새 탐지기가 사전 지식 없이 정확히 이것만 찾아냈다. **026 노트 20건과 SES 기록 90건은 지금도 유실 중**이다(028·030 은 11:31 재전이로 상태만 복구).

### 고친 것 — `/home/sjpark1/section9/bin/s9`

1. **`write_doc` 원자 교체.** 옛 코드는 `open(path,"w")` 로 **먼저 비우고** 그 안에서 `fm_dump` 를 불렀다 — 구코드 대조 시험에서 문서가 실제로 `''` 가 됐다. 카탈로그는 이미 tmp+os.replace 였는데(REQ-20260825-049-62x6) 그 원천인 문서 쓰기가 아니었다. 부르는 자리 36곳이 한 번에 고쳐진다.
2. **전이 사후 확인.** `do_transition` 이 쓴 것을 디스크에서 다시 읽어 확인하고, 없으면 성공을 말하지 않는다. "출력은 성공인데 문서엔 없음"이라는 상태 자체를 불가능하게 한다.
3. **`s9 snapshot --audit`.** 막을 수 없는 것은 드러나게 한다. 실 vault 전수 **0.87초**, 유실 시 exit 1 + 되살리기 명령.

설계에서 두 번 접었고 둘 다 실측이 교정했다 — 최신 스냅만 보면 눈이 멀고(10:27 스냅이 정확히 유실본이었다), 자국을 **글자**로 맞대면 이름 바꾸기 한 번 때문에 거짓 유실 **466건**이 나온다. 시각을 신원으로 삼아 466 → 4건, 그 4건이 전부 진짜였다.

### 회귀 시험 — `/home/sjpark1/section9/tests/test_transition_durability.py` (신규 10건)

`python3 tests/ transition_durability` → **Ran 10 tests in 0.746s / OK / exit=0**

통과만 하는 시험은 시험이 아니라서 **구코드 사본에 같은 시험을 걸어 물리는지 확인했다**: 3 FAIL + 4 ERROR, 격리 후 개별 재확인도 d2_a·d3_a FAIL / d3_c ERROR. 인접 계약 185건 exit=0. 실 vault·git 무조작, 커밋 없음.

### 리드 판정이 필요한 셋 (손대지 않았다)

1. **유실 110건은 되살릴 수 있다.** `vault/requests/2026/08` 수정 금지 지시라 실행하지 않았다. stash 커밋이 unreachable 이라 `git gc` 가 언젠가 지운다 — **서두르는 편이 좋다.** 명령과 주의사항(통째 restore 는 이후 붙은 새 노트를 덮는다)은 문서 노트에 적었다.
2. **탐지기를 언제 돌릴 것인가.** 아무도 안 돌리는 탐지기는 없는 탐지기다. serve 워처(이미 30초마다 `snapshot_dirty()` 를 돈다) + `s9 digest` 조합을 권하지만, 화면·알림에 무엇을 띄울지가 설계 결정이라 배선하지 않았다.
3. **뿌리는 아직 열려 있다.** `bin/s9-guard` 는 git **pre-commit** 훅이라 작업 트리를 되돌리는 명령을 아예 보지 못한다. 규율 문구는 REQ-20260826-021-62x6 이 이미 적어 둔 대로 "지켜지지 않을 때를 대비하지 못한다" — 별건 REQ 감이다.

전부 `REQ-20260901-004-62x6` 에 `response`/`deep-diver` 노트로 기록했다(5,522자).

### 2026-09-01T13:29:15+09:00 commit (by nicehugepark)

커밋 5dd0952 — 쓴 것을 다시 읽기 전에는 성공을 말하지 않는다 — 전이 유실 규명·원자화·복원 (REQ-20260901-004-62x6)

## History
- 2026-09-01T10:24:31+09:00 created by nicehugepark (status: open)
- 2026-09-01T10:24:38+09:00 status: open -> in-progress (by nicehugepark) — deep-diver 위임 — 읽기 전용 규명
- 2026-09-01T13:33:01+09:00 status: in-progress -> done (by nicehugepark) — goal 충족: ① 재현 경로 규명 — 유실은 s9 밖(위임 에이전트의 stash push→pop 실패→선별 checkout→drop, 10:21), 1차 증거 = 버려진 stash 커밋 0042d6dd(rescue/stash-0042d6dd 브랜치로 보호). ② 원자성 — write_doc tmp+os.replace 교체(36개 호출처), do_transition 디스크 사후 확인, 실패는 출력으로 드러남. ③ 회귀 — test_transition_durability 10건 green, 구코드 사본에 3F+4E 로 무는 것 실측, 전체 스위트 exit 0. ④ 피해 복원 — 4파일 113건 자국 병합, snapshot --audit exit 0. 커밋 5dd0952. 후속: REQ-20260901-009-62x6(탐지기 배선)·010(git 기계 게이트)
