---
id: REQ-20260901-024-62x6
type: request
title: 폴마다 프로세스 전수 훑기
summary: 대시보드 폴 한 바퀴가 세션 수만큼 /proc 을 전수 훑고 그것이 동시 요청 수만큼 겹친다 — 실측 스레드 32개가 동시에 훑고 16개가 뒤에서 대기, 요청이 90~180초 걸린다
goal: 대시보드 폴 한 바퀴가 /proc 을 한 번만 훑는다 — 동시 요청이 늘어도 훑기 횟수가 함께 늘지 않고, 스택 덤프에서 proc_table 동시 진입이 1로 떨어진다
status: done
size: M
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: a05fe599
sessions: ["8a0d83ad", "a05fe599"]
agents: ["sub:backend-developer:a7b5120f", "sub:backend-developer", "sub:subagent:a5d93c33", "sub:subagent:a187b53e"]
contributions: [{"actor": "sub:backend-developer:a7b5120f", "item": "024 proc 전수 훑기 병목", "started": "2026-09-02T07:50:28+09:00", "ended": "2026-09-02T08:11:51+09:00", "result": "stalled", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/a05fe599-cf31-4fd7-8ef2-000a77ff9011/tasks/a7b5120fa9b002946.output", "reason": "204초째 진전 없음 (기준 180초)"}, {"actor": "sub:backend-developer", "item": "TDD 시나리오 — 폴 한 바퀴가 /proc 을 한 번만 훑는다", "started": "2026-09-02T07:55:07+09:00", "ended": "2026-09-02T07:55:07+09:00", "result": "done", "transcript": ""}, {"actor": "sub:backend-developer", "item": "TDD 시나리오 (최종 — 통과분 체크)", "started": "2026-09-02T08:15:03+09:00", "ended": "2026-09-02T08:15:03+09:00", "result": "done", "transcript": ""}, {"actor": "sub:subagent:a5d93c33", "item": "Appending final TDD note to REQ-20260901-024-62x6", "started": "2026-09-02T08:15:22+09:00", "ended": "2026-09-02T08:15:22+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/a05fe599-cf31-4fd7-8ef2-000a77ff9011/subagents/agent-a5d93c33175d5e22c.jsonl"}, {"actor": "sub:backend-developer", "item": "뿌리는 확인됐고, 답은 TTL 캐시가 아니라 **범위**였다", "started": "2026-09-02T08:15:48+09:00", "ended": "2026-09-02T08:15:48+09:00", "result": "done", "transcript": ""}, {"actor": "sub:subagent:a187b53e", "item": "Appending response note to REQ-20260901-024-62x6", "started": "2026-09-02T08:15:55+09:00", "ended": "2026-09-02T08:15:55+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/a05fe599-cf31-4fd7-8ef2-000a77ff9011/subagents/agent-a187b53ea39d8b709.jsonl"}, {"actor": "sub:backend-developer:a7b5120f", "item": "`[2026-09-02 08:15:53 KST - backend-developer]`", "started": "2026-09-02T08:16:23+09:00", "ended": "2026-09-02T08:16:23+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/a05fe599-cf31-4fd7-8ef2-000a77ff9011/subagents/agent-a7b5120fa9b002946.jsonl"}]
project: section9
parent: REQ-20260901-020-62x6
children: ["REQ-20260902-004-62x6"]
relates_why: {}
tags: ["session", "terminal", "dashboard"]
created: 2026-09-01T21:44:06+09:00
status_since: 2026-09-02T08:28:16+09:00
updated: 2026-09-02T08:28:16+09:00
priority: 50
---

## Original

REQ-20260901-020(연결 누수) 수정 뒤 스레드 스택을 다시 뜨자, 앞을 막고 있던 것이
치워지면서 **다음 병목**이 드러났다. 실측(2026-09-01 21:45, kill -USR1 로 뜬 스택 100개):

  33  readinto (다음 요청 대기 — 정상)
  32  _proc_table_read <- proc_table <- _inbox_watch_alive   ← /proc 전수 훑기
  16  gate(대기) <- do_GET                                    ← 위 무리를 기다림
  13  glob._iterdir

`proc_table` 은 주석에 적힌 대로 **`ps` 갈래에서만 캐시하고 `/proc` 갈래는
캐시를 건너뛴다** — "/proc 은 싸다"는 전제다. 그런데 `session_rows` 가 바인딩마다
`chat_live` 와 `_inbox_watch_alive` 를 각각 부르므로, 폴 한 바퀴가 세션 수만큼
/proc 전수 훑기를 하고, 그것이 동시 요청 수만큼 겹친다. 실측에서 32개 스레드가
동시에 /proc 을 훑고 16개가 그 뒤에 줄 서 있었다.

증상: 요청이 90~180초씩 응답을 못 받는다(lastrcv 중앙값 94초). 연결이 새는 것이
아니라 **핸들러가 느린 것**이라, 020 의 유휴 상한은 여기 걸리지 않는다(설계대로).

고칠 방향(판정 필요): /proc 갈래에도 같은 TTL 캐시를 주면 폴 한 바퀴에 한 번으로
줄어든다. 다만 주석이 캐시를 안 하는 이유를 명시해 두었으니, 신선도 비용을
재고 나서 결정할 일이다.

## Notes

### 2026-09-02T07:55:07+09:00 tdd (agent: sub:backend-developer, by nicehugepark)

### TDD 시나리오 — 폴 한 바퀴가 /proc 을 한 번만 훑는다

#### S0. 규명 (코드 전 실측 — 2026-09-02 07:5x, 서버 pid 546565, 프로세스 66개)
- [x] S0a. `session_rows()` 한 번 = `/proc` 전수 훑기 **228회**, 1.538s.
      (바인딩 156개 × `chat_live` + `_inbox_watch_alive` 각각)
- [x] S0b. `catalog_with_live()` 한 번 = **43회**, 0.074s.
- [x] S0c. `/api/chat/target` 한 요청 = **76회**, 0.046s — 이 갈래는 신선도
      계약(C9/C18) 때문에 폴 스냅샷 게이트 **밖**이라, 동시 요청 수만큼
      그대로 곱해진다. 실측 스택의 `_proc_table_read` 32개가 이 자리다.
- [x] S0d. 동시성이 곱한다: `/api/sessions` 8동시 0.22s → 32동시 1.27s (약 6배).

#### 정상
- [ ] S1. 한 요청 스코프 안에서 `proc_table()` 을 N번 불러도 실제 `/proc`
      전수 훑기는 **1회**다 (계기 `reads` 델타 = 1).
- [ ] S2. `/api/chat/target` 한 요청의 훑기 = 1회 (전: 76).
- [ ] S3. `/api/sessions` 한 요청의 훑기 = 1회 (전: 228).

#### 경계
- [ ] S4. 동시 K(=16) 요청에서 **동시 진입 최대치 = 1** — 단일비행이 겹친
      훑기를 하나로 접는다. 계기 `max_inflight` 로 기계가 판정한다.
- [ ] S5. 스코프 **밖**(CLI 한 번 실행, 백그라운드 워처 스레드)은 종전대로
      매 호출 신선하다 — 캐시가 CLI 의미를 바꾸지 않는다.
- [ ] S6. 오래 사는 요청(SSE, 300초)은 스코프가 굳지 않는다 — 매 beat 마다
      다시 훑어, 5분짜리 연결이 5분 낡은 표를 들고 있지 않다.

#### 실패
- [ ] S7. 훑기가 예외로 죽어도 단일비행 대기자가 갇히지 않는다 — 대기자는
      깨어나 스스로 훑거나 빈 표를 받고, 다음 호출은 정상 복귀한다.
- [ ] S8. `proc_cache_clear()` 는 스코프 메모까지 지운다 — 진단·시험이
      "다시 뜬다"고 말할 때 실제로 다시 뜬다.

#### 회귀 (신선도 계약을 깨지 않는다)
- [ ] S9. `test_dashboard_chat` C9: tail 을 죽이고 0.2초 뒤 **새 요청**이
      `live=False` 를 본다 (요청마다 새로 훑으므로 계약이 산다).
- [ ] S10. `test_dashboard_chat` C18: 수신 대기 세션 우선순위 그대로.
- [ ] S11. `test_platform_live` (ps/win/none 갈래 흉내) 전부 green.
- [ ] S12. `test_poll_snapshot` T5·T6·T7 (TTL=0 기본, chat_target 비게이트,
      session_rows 게이트) 그대로 green.
- [ ] S13. 전체 스위트 `python3 tests/ --jobs 4` 종료코드 0 (실측).

#### 계기 (사람이 스택을 뜨지 않아도 되게)
- [ ] S14. `/api/serveinfo` 가 `proc: {reads, max_inflight, backend}` 를 낸다 —
      020 의 `conns` 계기판과 같은 자리. 이후 이 병목의 재발은 스택 덤프가
      아니라 이 숫자로 잡는다.

### 2026-09-02T08:15:03+09:00 tdd (agent: sub:backend-developer, by nicehugepark)

### TDD 시나리오 (최종 — 통과분 체크)

시험 파일: `tests/test_proc_scan.py` (17건). Red 증거: 같은 파일을
`git show HEAD:bin/s9` 로 뜬 옛 코드에 대고 돌리면 **17건 중 14건이 red**,
green 3건은 «깨면 안 되는 계약» 쪽이다 (S5 스코프 밖 신선 · S7 실패 복구 ·
S9 tail 종료 반영).

#### S0. 규명 (코드 전 실측 — 2026-09-02 07:5x, 바인딩 156개 · 프로세스 66개)
- [x] S0a. `session_rows()` = `/proc` 전수 훑기 **228회** · 1.538s
- [x] S0b. `catalog_with_live()` = **43회** · 0.074s
- [x] S0c. `/api/chat/target` 한 요청 = **76회** · 0.046s (폴 게이트 밖)
- [x] S0d. 동시성이 곱한다 — 실서버 `/api/chat/target` 1건 0.07s ·
      8동시 12.75s · 24동시 37.8s(+연결 리셋 2)

#### 정상
- [x] S1. 요청 스코프 안에서 `proc_table()` 을 20번 불러도 훑기는 1회
- [x] S2. `/api/chat/target` 한 요청의 훑기 = 1회 (전: 76)
- [x] S3. `/api/sessions` 한 요청의 훑기 = 1회 (전: 228)
- [x] S3b. `/api/catalog` 한 요청의 훑기 = 1회 (전: 43)

#### 경계
- [x] S4. 동시 16(모듈)·8(실서버) 요청에서 **동시 진입 최고치 = 1**
- [x] S5. 스코프 밖(CLI·백그라운드 스레드)은 종전대로 매 호출 신선
- [x] S5b. 스코프가 요청 밖으로 새지 않는다 / S5c. 스레드 사이로 새지 않는다
- [x] S6. `proc_scope_reset()` 이 스코프 안에서도 표를 버린다
- [x] S6b. SSE 루프가 바퀴마다 버린다 (소스 계약)
- [x] S6c. 문은 `handle_one_request` 한 곳 (소스 계약 — 새 메서드도 자동 통과)

#### 실패
- [x] S7. 훑기가 예외로 죽어도 대기자가 30초 상한까지 갇히지 않고, 다음
      호출이 정상 복귀한다
- [x] S8. `proc_cache_clear()` 가 스코프 메모까지 지운다

#### 회귀 (신선도 계약을 깨지 않는다)
- [x] S9. tail 을 죽이면 다음 요청이 곧바로 안다 (`test_proc_scan` 자체 +
      `test_dashboard_chat` C9)
- [x] S10. `test_dashboard_chat` C18 그대로 green
- [x] S11. `test_platform_live` (ps/win/none 갈래) 그대로 green
- [x] S12. `test_poll_snapshot` T5·T6·T7 그대로 green
- [x] S13. 전체 스위트 `python3 tests/ --jobs 4` → `echo $?` = **0** (실측)

#### 계기
- [x] S14. `/api/serveinfo` 가 `proc: {backend, reads, shared, max_inflight}`
      를 낸다 — 실서버 실측 `{'backend':'proc','reads':11,'shared':25,
      'max_inflight':1}` (요청 66건이 실제 훑기 11번)

### 2026-09-02T08:15:22+09:00 subagent (agent: sub:subagent:a5d93c33, by nicehugepark)

Appending final TDD note to REQ-20260901-024-62x6

### 2026-09-02T08:15:48+09:00 response (agent: sub:backend-developer, by nicehugepark)

### 뿌리는 확인됐고, 답은 TTL 캐시가 아니라 **범위**였다

#### 1. 실측으로 확정한 뿌리 (코드 착수 전)

문서의 짐작이 맞았고, 숫자까지 쟀다 (바인딩 156개 · 프로세스 66개):

| 자리 | `/proc` 전수 훑기 | 걸린 시간 |
|---|---|---|
| `session_rows()` | **228회** | 1.538s |
| `catalog_with_live()` | **43회** | 0.074s |
| `/api/chat/target` 한 요청 | **76회** | 0.046s |

그리고 **동시성이 그것을 곱한다**. 실서버(옛 코드) `/api/chat/target`:
1건 0.07s → 8동시 **12.75s** → 24동시 **37.8s**(연결 리셋 2건). 부하 중
`s9 shot` 은 60초 타임아웃.

`/api/sessions`·`/api/catalog` 는 REQ-20260831-002-62x6 의 폴 스냅샷 게이트가 이미
막고 있었다(24동시 0.13s). 즉 **남아서 곱하고 있던 자리는 `/api/chat/target`
하나** — 신선도 계약(C9/C18) 때문에 일부러 게이트 밖에 둔 그 갈래다.
실측 스택의 `_proc_table_read` 32개가 이 자리다.

#### 2. 판정: TTL 캐시를 쓰지 않았다

`proc_table` 주석이 캐시를 안 하는 이유를 명시해 뒀고, 그 이유가 여전히
유효하다 — TTL 은 판정을 ttl 초 낡게 만들고, 그 낡음이 «tail 을 죽였는데
화면이 아직 live» 를 만든다(`test_dashboard_chat` C9/C18 이 붙잡는 계약).
게다가 TTL 로는 목표를 못 맞춘다: `session_rows` 한 바퀴가 1.5초라 TTL 1초로도
그 안에서 여러 번 다시 뜬다.

**낡음을 사지 않고 중복만 지우는 두 겹**을 썼다.

① **요청 스코프** (`proc_scope`, `bin/s9`) — 요청 하나가 도는 동안만 표를
나눠 쓰고 끝나면 버린다. 표는 언제나 그 요청이 시작된 뒤에 뜬 것이므로
신선도 계약이 구조적으로 산다. 문은 `Handler.handle_one_request` **한 곳**이다
(메서드마다가 아니라 — 나중에 붙는 메서드도 자동으로 지나야 병목이 옆문으로
돌아오지 않는다. 연결 상한을 `parse_request` 한 곳에 둔 것과 같은 이유).
5분을 사는 SSE 응답은 바퀴마다 `proc_scope_reset()` 으로 버린다.

② **단일비행** (`_proc_table_shared`) — 동시에 겹친 훑기를 하나로 접는다.
대기자가 받는 표는 자기가 도착하기 **직전**에 뜬 것이라, 낡음의 상한이
훑기 한 번의 소요(수 ms)다. 훑기가 예외로 죽으면 대기자는 갇히지 않고 스스로
뜬다 — 한 번의 실패가 표를 영구히 비워 두면 생존 판정이 전부 거짓이 된다.

CLI·백그라운드 스레드는 스코프 밖이라 **종전 그대로 매 호출 신선**하다.
`ps`/`win` 갈래의 TTL 1초 캐시도 그대로다.

#### 3. 전후 실측 (같은 자·같은 부하)

| 잰 것 | 전 | 후 |
|---|---|---|
| `session_rows()` 훑기 | 228회 | **1회** |
| `catalog_with_live()` 훑기 | 43회 | **1회** |
| `/api/chat/target` 훑기 | 76회 | **1회** |
| `/api/chat/target` 8동시 | 12.75s | **2.58s** |
| `/api/chat/target` 24동시 | 37.8s (리셋 2) | **9.8s (리셋 0)** |
| 스택 덤프의 `_proc_table_read` | 32개 | **0개** |
| `proc_table` 동시 진입 | 32 | **1** (계기 실측) |
| `s9 shot` (조용할 때) | 4.2s | 3.3s |

계기 실측(요청 66건): `proc: {backend: proc, reads: 11, shared: 25,
max_inflight: 1}` — 66요청이 실제 훑기 11번을 만들었고 25번은 접혔다.

#### 4. 남은 것 — 다음 병목은 `/proc` 이 아니다 (파생 REQ 등록)

고친 뒤 같은 자리에서 `kill -USR1` 로 다시 뜬 스택(27스레드, 24동시 중):

    19  glob._iterdir <- streams_glob <- _binding_activity_paths
                      <- chat_alive <- chat_live <- chat_target
     2  readinto (다음 요청 대기 — 정상)
     0  _proc_table_read                       ← 이 REQ 로 사라졌다

`chat_target` 이 바인딩마다 streams 디렉토리를 glob 한다 — 요청당 73회.
남은 9.8초와 «부하 중 `s9 shot` 60초 타임아웃»이 그 자리다.
→ **REQ-20260902-004-62x6 「바인딩마다 스트림 훑기」** 로 분리 등록했다
(부모 = 이 문서). 024 가 쓴 두 겹이 그대로 맞는 모양이다.

#### 5. 검증

- 새 시험 `tests/test_proc_scan.py` 17건 green. 같은 파일을
  `git show HEAD:bin/s9` 로 뜬 옛 코드에 대고 돌리면 **14건 red** — green 3건은
  깨면 안 되는 계약(스코프 밖 신선 · 실패 복구 · tail 종료 반영) 쪽이다.
- `test_platform_live` · `test_poll_snapshot` · `test_dashboard_chat` ·
  `test_serve_concurrency` 79건 green.
- 전체 스위트 `python3 tests/ --jobs 4` → `echo $?` = **0** (파이프로 가리지 않고 실측).
- 실서버는 감시자(pid 703)에게 맡겨 되살렸다 — 감시자는 건드리지 않았다.
  지금 도는 서버(pid 584921)가 새 코드다.

#### 6. 고친 자리

`bin/s9` 한 파일. `proc_scope`/`proc_scope_reset`/`_proc_table_shared`/
`proc_stat` 신설, `proc_table` 을 스코프→(TTL)→단일비행 순으로 갈랐고,
`Handler.handle_one_request` 에 스코프 문, SSE 루프에 리셋,
`/api/serveinfo` 에 `proc` 계기를 얹었다. 커밋은 하지 않았다.

### 2026-09-02T08:15:55+09:00 subagent (agent: sub:subagent:a187b53e, by nicehugepark)

Appending response note to REQ-20260901-024-62x6

### 2026-09-02T08:16:23+09:00 subagent (agent: sub:backend-developer:a7b5120f, by nicehugepark)

`[2026-09-02 08:15:53 KST - backend-developer]`

REQ-20260901-024-62x6 를 마쳤다. 뿌리는 문서의 짐작대로였고, **답은 TTL 캐시가 아니라 범위**였다.

### 실측으로 확정한 뿌리 (코드 착수 전)

| 자리 | `/proc` 전수 훑기 | 시간 |
|---|---|---|
| `session_rows()` | **228회** | 1.538s |
| `catalog_with_live()` | **43회** | 0.074s |
| `/api/chat/target` 한 요청 | **76회** | 0.046s |

그리고 동시성이 그것을 곱한다. 실서버(옛 코드) `/api/chat/target`: 1건 0.07s → 8동시 **12.75s** → 24동시 **37.8s**(연결 리셋 2). 부하 중 `s9 shot` 60초 타임아웃.

중요한 갈림: `/api/sessions`·`/api/catalog` 는 REQ-20260831-002-62x6 의 폴 스냅샷 게이트가 이미 막고 있었다(24동시 0.13s). **남아서 곱하던 자리는 `/api/chat/target` 하나** — 신선도 계약(C9/C18) 때문에 일부러 게이트 밖에 둔 갈래다. 실측 스택의 32개가 거기다.

### 판정

TTL 캐시는 안 썼다. 주석이 적어 둔 이유가 여전히 유효하고(낡음이 «tail 죽였는데 아직 live» 를 만든다), 애초에 목표를 못 맞춘다 — `session_rows` 한 바퀴가 1.5초라 TTL 1초로도 여러 번 다시 뜬다. 대신 **낡음을 사지 않고 중복만 지우는 두 겹**:

1. **요청 스코프** `proc_scope` — 요청 하나가 도는 동안만 표를 나눠 쓴다. 표는 언제나 요청 시작 뒤에 뜬 것이라 신선도 계약이 구조적으로 산다. 문은 `Handler.handle_one_request` **한 곳**(메서드마다가 아니라 — 새 메서드도 자동 통과). SSE 는 바퀴마다 `proc_scope_reset()`.
2. **단일비행** `_proc_table_shared` — 겹친 훑기를 하나로 접는다. 대기자가 받는 표는 자기 도착 직전 것(낡음 상한 = 훑기 1회, 수 ms). 훑기가 예외로 죽어도 대기자가 갇히지 않는다.

CLI·백그라운드 스레드는 스코프 밖이라 종전대로 매 호출 신선하다.

### 전후

| | 전 | 후 |
|---|---|---|
| `session_rows` / `catalog_with_live` / `chat_target` 훑기 | 228 / 43 / 76 | **1 / 1 / 1** |
| `/api/chat/target` 8동시 · 24동시 | 12.75s · 37.8s | **2.58s · 9.8s** |
| 스택의 `_proc_table_read` | 32개 | **0개** |
| `proc_table` 동시 진입 | 32 | **1** |

계기 실측(요청 66건): `proc: {reads: 11, shared: 25, max_inflight: 1}`. goal 충족.

### 남은 것 — 파생 REQ 등록

고친 뒤 다시 뜬 스택(27스레드, 24동시 중): `_proc_table_read` **0개**, 대신 **19개가 `glob._iterdir ← streams_glob ← _binding_activity_paths ← chat_alive ← chat_target`**. `chat_target` 이 바인딩마다 streams 디렉토리를 glob 한다 — 요청당 73회. 남은 9.8초와 «부하 중 shot 60초 타임아웃»이 그 자리다. → **REQ-20260902-004-62x6 「바인딩마다 스트림 훑기」** 를 이 문서의 자식으로 등록했다(open).

### 검증

- 새 시험 `/home/sjpark1/section9/tests/test_proc_scan.py` 17건 green. **Red 증거**: 같은 파일을 `git show HEAD:bin/s9` 로 뜬 옛 코드에 대고 돌리면 14건 red — green 3건은 깨면 안 되는 계약(스코프 밖 신선 · 실패 복구 · tail 종료 반영) 쪽이다.
- `platform_live`·`poll_snapshot`·`dashboard_chat`·`serve_concurrency` 79건 green.
- 전체 스위트 `python3 tests/ --jobs 4` → `echo $?` = **0** (파이프로 가리지 않고 실측).
- 실서버는 감시자(pid 703)에게 맡겨 되살렸다 — 감시자는 건드리지 않았다. 지금 도는 pid 584921 이 새 코드고 `stale: false`.

고친 자리는 `/home/sjpark1/section9/bin/s9` 한 파일(+131/-5). 커밋은 하지 않았다. REQ 문서에 tdd(체크 완료)·response 노트로 남겼다.

**한계**: 부하 중 `s9 shot` 은 여전히 60초 타임아웃이다 — 그것은 이 REQ 가 아니라 파생 REQ-20260902-004-62x6 의 몫이고, 그 사실을 노트에 명시했다.

### 2026-09-02T08:28:16+09:00 commit (by nicehugepark)

커밋 8054a92 — 한 요청 = 한 번의 훑기 — 폴마다 /proc 전수 훑던 병목 (REQ-20260901-024-62x6)

## History
- 2026-09-01T21:44:06+09:00 created by nicehugepark (status: open)
- 2026-09-02T07:48:46+09:00 status: open -> in-progress (by nicehugepark) — drag 이동 [via dashboard]
- 2026-09-02T08:28:16+09:00 status: in-progress -> done (by nicehugepark) — goal 충족(리드 재실측): proc 계기의 동시 진입 최고치 1(전 32), 폴 한 바퀴 훑기 228/43/76 → 1/1/1, /api/chat/target 8동시 12.75초 → 0.19초. test_proc_scan 17/17, 전체 스위트 exit 0. 커밋 참조. 남은 부하(24동시 9.8초)는 다른 자리(streams glob) — REQ-20260902-004-62x6 로 분리.
