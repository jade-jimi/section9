---
id: REQ-20260901-012-62x6
type: request
title: 계정 전환 후 fable 회귀
summary: 계정을 전환하면 기본 모델이 opus 가 아니라 fable 로 선다 — 「기본 모델 fable 금지」(기확정 정책, settings·auto_resume_model·s9code_args 3곳 opus 일치)가 계정 전환 경로에서 재발. 8/30 profile 에 auto_resume_model=fable (by dashboard model change) 기록도 같은 경로 의심
goal: 계정 전환 후에도 기본 모델이 opus 로 유지되고, fable 이 기본이 되는 경로가 회귀 시험으로 봉쇄된다
status: in-progress
size: M
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: 3ed7d9fd
agents: ["sub:deep-diver:aa45e7e8", "sub:subagent:aaaa1e46", "sub:deep-diver", "sub:backend-developer:aa2563e0", "sub:backend-developer"]
contributions: [{"actor": "sub:deep-diver:aa45e7e8", "item": "011·012 전환 교착 규명", "started": "2026-09-01T14:13:26+09:00", "ended": "2026-09-01T14:30:07+09:00", "result": "stalled", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/3ed7d9fd-1679-421a-a506-cec3973f8c08/tasks/aa45e7e818256dd24.output", "reason": "192초째 진전 없음 (기준 180초)"}, {"actor": "sub:subagent:aaaa1e46", "item": "Showing REQ-20260901-012-62x6 document", "started": "2026-09-01T14:25:04+09:00", "ended": "2026-09-01T14:25:04+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/3ed7d9fd-1679-421a-a506-cec3973f8c08/subagents/agent-aaaa1e462d0e818a7.jsonl"}, {"actor": "sub:deep-diver", "item": "012 fable 회귀 원인 규명", "started": "2026-09-01T14:25:56+09:00", "ended": "2026-09-01T14:25:56+09:00", "result": "done", "transcript": ""}, {"actor": "sub:backend-developer:aa2563e0", "item": "011·012 서버 구현", "started": "2026-09-01T14:51:48+09:00", "ended": "2026-09-01T15:34:04+09:00", "result": "stalled", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/3ed7d9fd-1679-421a-a506-cec3973f8c08/tasks/aa2563e04bb349301.output", "reason": "814초째 진전 없음 (기준 180초)"}, {"actor": "sub:backend-developer", "item": "TDD 시나리오 (서버 — 012 P2+P4)", "started": "2026-09-01T14:57:31+09:00", "ended": "2026-09-01T14:57:31+09:00", "result": "done", "transcript": ""}, {"actor": "sub:backend-developer", "item": "TDD 시나리오 (서버 — 012 P2+P4) · 검증 결과", "started": "2026-09-01T15:17:29+09:00", "ended": "2026-09-01T15:17:29+09:00", "result": "done", "transcript": ""}, {"actor": "sub:backend-developer", "item": "서버 구현 완료 (backend-developer) — P2+P4. 수정 범위 `bin/s9` · `tests/` 한정, 커밋", "started": "2026-09-01T15:18:45+09:00", "ended": "2026-09-01T15:18:45+09:00", "result": "done", "transcript": ""}]
project: section9
parent: REQ-20260901-011-62x6
relates_why: {}
tags: ["session", "dashboard", "test", "attached", "py", "code", "workflow"]
created: 2026-09-01T14:12:22+09:00
status_since: 2026-09-01T14:12:43+09:00
updated: 2026-09-01T15:18:45+09:00
priority: 50
---

## Original

사용자: 계정 전환을 하면 왜 기본 모델이 opus가 아니고 fable로 되는거지? 기본 모델로 fable은 절대로 하지않는 요청이 처리가 됐었는데.

## Notes

### 2026-09-01T14:25:04+09:00 subagent (agent: sub:subagent:aaaa1e46, by nicehugepark)

Showing REQ-20260901-012-62x6 document

### 2026-09-01T14:25:56+09:00 response (agent: sub:deep-diver, by nicehugepark)

원인 규명 (deep-diver) — 코드·git 무수정. 실측 재현 포함.

### 결론 한 줄

대시보드에서 모델을 **한 번** 고르면 그 선택이 곧바로 **사용자 영속 정책**이 된다.
`_persist_model_choice`(bin/s9:2524)가 `auto_resume_model` 과 `s9code_args` 를
그 값으로 덮어쓰고, 계정 전환이 여는 새 창은 `s9 code` 이며 `s9 code` 는
`s9code_args` 를 읽는다. 8/30 13:36 의 fable 한 번이 그 뒤 모든 기동의 기본이 됐다.

### 사슬 (증거 포함)

1. 사용자가 겪은 것: 계정을 전환하면 기본 모델이 fable.
2. 계정 전환으로 뜨는 창은 `wake_session`(bin/s9:5539)이 여는
   `CLAUDE_CONFIG_DIR=… bin/s9 code` 다. 계정 전환 재시작도 `s9 code` 래퍼 안이다.
3. `cmd_code`(bin/s9:13678)는 `base_cmd = ["claude"] + s9code_args.split() + …`
   로 기동한다. 그 값이 `--permission-mode auto --model fable` 이었다.
4. 그 값을 쓴 것은 `_persist_model_choice` 다. `users/nicehugepark/profile.md`
   에 그 자국이 그대로 있다:

       2026-08-30T13:36:02 config auto_resume_model=fable (by dashboard model change)
       2026-08-30T13:36:02 config s9code_args=--permission-mode auto --model fable (by dashboard model change)

   `"(by dashboard model change)"` 문자열은 코드 전체에서 bin/s9:2543·2548 두
   자리에만 있다 — 둘 다 `_persist_model_choice` 안이다. 다른 경로는 없다.
5. 그 함수는 `restart_session`(bin/s9:5643)이 부른다. **거부 갈래를 지난 직후,
   재시작이 실제로 성공하기 전에** 부른다 — 래퍼가 없어 manual 로 끝나도,
   SIGTERM 이 실패해도 설정은 이미 바뀌어 있다.

임시 S9_ROOT 실증(사용자 tester, 사전값 opus):

    이전: {'s9code_args': '--permission-mode auto --model opus', 'auto_resume_model': 'opus'}
    _persist_model_choice("tester", "fable") → ['auto_resume_model', 's9code_args']
    이후: {'s9code_args': '--permission-mode auto --model fable', 'auto_resume_model': 'fable'}
    프로필 기록: config auto_resume_model=fable (by dashboard model change)
                 config s9code_args=--permission-mode auto --model fable (by dashboard model change)
    무인 워커 모델 인자: ['--model', 'fable']

실서비스 profile.md 의 8/30 두 줄과 **글자까지 같다.** 재현 확정.

### 원인의 원인 (왜 이 코드가 이렇게 됐나)

- 3단계: 이 승격은 실수가 아니라 **의도된 설계**다. REQ-20260825-080-62x6 이
  "대시보드에서 고른 모델이 다음 기동에서 되돌아간다"를 고치면서 넣었고, 함수
  주석이 그 이유를 적어 두었다.
- 4단계: 되돌아가던 진짜 이유는 **세션·계정 범위의 저장처가 없다**는 것이다.
  모델은 이 창의 성질인데 저장할 자리가 사용자 설정뿐이라, 세션 1회 선택을
  사용자 정책 자리에 적는 것 말고 길이 없었다. 범위 불일치가 뿌리다.
  (같은 문제를 계정은 다르게 처리한다 — cmd_code 의 재시작 루프는 고른 계정을
  **프로세스 메모리**에만 들고 간다. 창이 닫히면 사라진다. 두 축이 서로 다른
  잘못된 극단에 있다: 모델은 너무 오래 남고, 계정은 너무 빨리 사라진다.)
- 5단계: 「사용자가 선언한 정책」과 「방금 한 번 고른 값」을 담는 칸이 하나뿐이다.
  마지막에 쓴 쪽이 이긴다. `actor="dashboard model change"` 는 이력 표시일 뿐
  판정에 쓰이지 않는다 — 사용자가 8/25 에 "기본 모델 fable 절대 금지"라고 정한
  사실이 시스템 어디에도 **정책으로** 남아 있지 않다(사람의 기억과 이력 줄뿐).

### 곁가지 — 같은 사슬이 011 의 조건을 만들었다

`_spawn_model_args`(bin/s9:2487)는 무인 워커의 `--model` 을 `auto_resume_model`
로 못 박는다. 그 함수 주석이 밝히는 존재 이유가 바로 "**fable 한도가 소진되면
스폰된 워커가 전부 즉시 죽는다**(2026-08-25 실사고 — 모델 변경조차 막혔다)"인데,
같은 REQ 가 넣은 승격이 그 값을 fable 로 되돌려 놓았다. 8/25 사고를 막으려고
만든 장치가 8/30 에 그 사고의 조건을 스스로 복원한 셈이다. 리드가 방금 opus 로
원복해 두었지만, 원복은 원인 제거가 아니다 — 대시보드에서 모델을 한 번 고르는
순간 같은 두 줄이 다시 쓰인다.

### 지금 상태의 시험이 이 동작을 **정답으로 고정**하고 있다

`tests/test_session_restart.py:228 test_m1_persist_both_places` 가
"대시보드 변경이 두 저장처를 같은 값으로 맞춘다"를 단언한다. 즉 이것은 놓친
버그가 아니라 시험으로 못 박힌 결정이다 — 고치려면 **시험을 함께 개정**해야
하고, 그러지 않으면 수정이 곧바로 빨간 줄로 되돌아온다. 이 사실을 모르고
착수하면 "시험이 반대한다"에서 멈춘다.

### 수정 방향 (구현하지 않음 — 대안별 값·비용)

- **P1. 범위를 제자리로.** 모델 선택을 사용자 설정이 아니라 ① 세션 바인딩
  (이 창의 다음 재시작들) ② 계정 프로필 (그 계정의 기본) 에 적는다. 값: 범위
  불일치라는 뿌리를 없앤다 — 한 창의 선택이 다른 창·무인 워커·다른 계정으로
  새지 않는다. 비용: 저장처가 늘고, 셋의 우선순위(명령행 > 세션 > 계정 > 사용자)
  를 한 곳에 정의해 모든 기동 경로가 그것만 읽게 해야 한다(가장 큰 작업).
- **P2. 정책과 최근 선택을 가른다.** `model_policy`(사람만 쓴다) 와
  `model_last`(대시보드가 쓴다) 두 칸으로 나누고, 정책이 있으면 정책이 이긴다.
  값: "fable 은 절대 기본으로 두지 않는다" 가 **기계가 지키는 문장**이 된다.
  비용: 칸 하나와 우선순위 규칙 하나가 늘고, 사용자가 정책을 바꾸려면 정책
  자리에서 바꿔야 한다는 것을 화면이 가르쳐야 한다. P1 없이도 단독으로 듣는다.
- **P3. 승격을 선택으로.** 모델 창에서 고를 때 "앞으로의 기본으로도 둘까요?"
  를 **기본 꺼짐**으로 묻는다. 값: 부수효과가 사람의 결정이 된다(가장 작은 변경).
  비용: 자주 쓰는 길에 단추가 하나 늘고, 안 누르면 REQ-20260825-080-62x6 이 고쳤던
  "되돌아감"이 (그 창에 한해) 돌아온다 — P1 이나 P2 와 함께 가야 값이 산다.
- **P4. 최소 지혈(오늘 바로).** `_persist_model_choice` 가 `s9code_args`
  (=계정 기본 기동 인자)는 건드리지 않게 한다. 값: 계정 전환·새 창이 사용자
  기본으로 돌아온다 — 사용자가 신고한 증상 자체가 사라진다. 비용:
  `auto_resume_model`(무인 워커) 은 여전히 덮여 남는다 — 그쪽이 더 위험한
  자리(위 곁가지)라 P4 만으로 닫으면 안 된다.
- **P5. 회귀 시험(어느 안을 고르든 필수).** ① 대시보드 모델 선택 뒤에도 사용자
  정책 키가 그대로다 ② 그 뒤 새 `s9 code` 의 base_cmd 가 여전히 정책 모델이다
  ③ 무인 워커 `--model` 이 정책 모델이다 ④ `test_m1_persist_both_places` 는
  삭제가 아니라 **새 계약으로 개정**한다(무엇이 어디에 남아야 하는지를 다시 못
  박는 자리다).
- 권고: **P2 + P4 를 먼저**(사용자가 선언한 정책이 기계로 지켜지고 증상이 즉시
  사라진다), **P1 은 별건 REQ 로** — 기동 경로가 여럿이라 한 번에 옮기면
  「어느 모델로 떴는지 아무도 모르는」 구간이 생긴다.

### 확신도
**높음.** 1차 증거로 재현했다 — profile.md 의 실기록, 문자열 유일 출처 grep
(bin/s9:2543·2548), 임시 S9_ROOT 에서 글자까지 같은 재현, 그리고 그 동작을
고정하고 있는 시험의 위치. 남은 미지수: 8/30 13:36 에 사용자가 왜 fable 을
골랐는지(그 직전 라운드가 "모델 선택지에 fable 이 빠져 되돌아갈 수 없다"를
고친 REQ 였으므로 그 확인 클릭이었을 가능성이 높다) — 결론에는 영향 없다.
오히려 그 점이 요지를 굳힌다: **확인차 한 번 눌러 본 것이 영속 정책이 됐다.**

재현 스크립트를 이 노트에 붙인다(임시 S9_ROOT 생성·정리 포함).

[File: assets/REQ-20260901-012-62x6/repro_persist.py]

### 2026-09-01T14:57:31+09:00 tdd (agent: sub:backend-developer, by nicehugepark)

### TDD 시나리오 (서버 — 012 P2+P4)

실행: `python3 tests/ session_restart` · `python3 tests/ model_policy`

계약 한 줄: **정책 칸은 사람이 선언한 것만 쓴다. 대시보드의 1회 선택은 제 칸(`last_model_choice`)에 남고, 정책이 있으면 정책이 이긴다.**

- [ ] T1. 정책 불변(P4 핵심): 대시보드 모델 선택이 `auto_resume_model` 을 **쓰지 않는다** — 8/30 의 `auto_resume_model=fable (by dashboard model change)` 두 줄이 다시 나올 수 없다
- [ ] T2. 기동 인자 불변(P4): 같은 선택이 `s9code_args` 도 **쓰지 않는다** — 계정 전환이 여는 새 `s9 code` 창이 사용자 기본으로 돌아온다
- [ ] T3. 최근 선택은 남는다(P2): 그 선택은 `last_model_choice` 한 칸에 기록되고 반환값(`saved`)이 그 칸만 말한다
- [ ] T4. 정책이 이긴다: 정책 opus + 최근선택 fable → 판정 모델은 opus (무인 워커·s9 code 양쪽)
- [ ] T5. 정책이 없으면 최근 선택: 정책 미선언 + 최근선택 opus → opus (REQ-20260825-080-62x6 이 고친 「되돌아감」이 되살아나지 않는다)
- [ ] T6. 선언된 기동 인자도 선언이다: `s9code_args --model opus` 만 있고 `auto_resume_model` 이 없으면 opus 가 이긴다
- [ ] T7. `_spawn_model_args` 의 8/25 취지 부활: 정책이 fable 이 아닌 한 워커 `--model` 이 fable 로 뜨지 않는다 — 대시보드 클릭 하나가 워커 모델을 바꾸는 길이 끊긴다
- [ ] T8. 명시 선언은 여전히 통한다: `s9 user config <이름> auto_resume_model fable` 은 사람의 선언이므로 그대로 쓰인다(잠그는 것이 아니라 출처를 가르는 것)
- [ ] T9. 빈 입력 불변: 모델 없이 effort/account 만 바꾸면 설정을 한 칸도 안 쓴다 (기존 M3 보존)
- [ ] T10. 쓰기 실패 격리: 설정 쓰기가 터져도 재시작은 진행된다 (기존 M4 보존)
- [ ] T11. **`tests/test_session_restart.py:228 test_m1_persist_both_places` 개정**: 「두 저장처를 같은 값으로 맞춘다」를 「정책 두 칸은 그대로, 최근선택 칸만 바뀐다」로 다시 못 박는다(삭제 아님 — 무엇이 어디에 남아야 하는지의 자리)
- [ ] T12. 기동 경로 실측: `S9_CODE_DRYRUN=1 s9 code` 의 base_cmd 가 정책 모델로 뜬다 (읽는 자리가 실제로 판정을 지난다)

### 2026-09-01T15:17:29+09:00 tdd (agent: sub:backend-developer, by nicehugepark)

### TDD 시나리오 (서버 — 012 P2+P4) · 검증 결과

실행 실측: `python3 tests/ session_restart` 28/28 OK (exit=0). 회귀 이웃: `s9_code_args` 9/9 · `wake` 99/99 · `spawn_reason` 14/14 · `worker_claim` 6/6 · `auto_resume_gate` 2/2 전부 OK.

- [x] T1. 정책 불변: 대시보드 모델 선택이 `auto_resume_model` 을 쓰지 않는다
- [x] T2. 기동 인자 불변: `s9code_args` 도 쓰지 않는다
- [x] T3. 최근 선택은 `last_model_choice` 한 칸에만 기록되고 `saved` 가 그 칸만 말한다
- [x] T4. 정책이 이긴다: 정책 opus + 최근선택 fable → opus (`resolved_model`·`_spawn_model_args`)
- [x] T5. 정책이 없으면 최근 선택이 쓰인다 (080 이 고친 「되돌아감」 보존)
- [x] T6. 선언된 기동 인자도 선언이다: `s9code_args --model opus` 만 있어도 opus 가 이긴다
- [x] T7. 8/25 취지 부활: 정책이 fable 이 아닌 한 워커 `--model` 이 fable 로 안 뜬다
- [x] T8. 명시 선언은 그대로 통한다 (`auto_resume_model=fable` 은 사람의 선언이므로 존중)
- [x] T9. 모델 없이 effort/account 만 바꾸면 설정을 한 칸도 안 쓴다 (기존 M3 보존)
- [x] T10. 설정 쓰기 실패가 재시작을 안 깨뜨린다 (기존 M4 보존)
- [x] T11. `test_m1_persist_both_places` **개정 완료** → `test_m1_choice_records_without_touching_policy`: 「두 저장처를 같은 값으로」 → 「정책 두 칸은 그대로, 최근선택 칸만」. `test_m1b_restart_persists` → `test_m1b_restart_records_choice` 도 같은 계약으로 개정
- [x] T12. 기동 경로: `code_launch_args()` 가 `s9code_args --model fable` + 정책 opus → `--permission-mode auto --model opus`. **편차 보고** — `S9_CODE_DRYRUN` 서브프로세스 대신 `cmd_code` 에서 그 조립을 `code_launch_args()` 로 뽑아 직접 호출로 실측했다(cmd_code 는 대시보드 감시자를 스폰하는 부수효과가 있어 단위 시험에 부적합). 읽는 자리가 실제로 이 함수 하나를 지나는지는 소스 감시로 함께 못 박았다

덤(계약 감시): `auto_resume_model` 읽기 1곳 · `last_model_choice` 읽기 2곳(판정·기록)만 존재함을 시험이 센다 — 두 벌이 되면 한 벌만 고쳐진다.

### 2026-09-01T15:18:45+09:00 response (agent: sub:backend-developer, by nicehugepark)

서버 구현 완료 (backend-developer) — P2+P4. 수정 범위 `bin/s9` · `tests/` 한정, 커밋 없음.

### 고친 것: 칸을 갈랐다
`_persist_model_choice` → **`_record_model_choice`** 로 이름과 하는 일을 함께 바꿨다. 이제 대시보드 모델 선택은 `last_model_choice` **한 칸에만** 쓴다. `auto_resume_model` 과 `s9code_args` 는 **사람의 명시 선언(`s9 user config`)만** 쓴다 — 8/30 의 `(by dashboard model change)` 두 줄이 다시 나올 수 없다.

읽는 쪽은 **`resolved_model(owner)` 한 곳**이 정한다:
    auto_resume_model  (선언된 정책)      ← 이기면 여기서 끝
    s9code_args --model (선언된 기동 인자) ← 사람이 쓴 것이므로 이것도 정책
    last_model_choice  (창 하나의 1회 선택) ← 정책이 없을 때만
반환은 `(모델, 출처)` 로 출처("policy"|"last")까지 말한다.

두 소비처가 그 한 판정을 지난다.
· `_spawn_model_args(owner)` — 무인 워커의 `--model`. 종전엔 `auto_resume_model` 을 곧장 읽었고, 대시보드가 그 칸을 덮어써서 **8/25 사고를 막으려고 만든 가드가 그 사고의 조건을 스스로 복원**하고 있었다. 이제 클릭 하나가 워커 모델을 바꾸는 길이 끊겼다.
· `code_launch_args(owner)`(신설, `cmd_code` 가 인라인으로 하던 조립을 뽑았다) — `s9 code` 기동 인자. `s9code_args` 에 남은 낡은 `--model fable` 이 있어도 정책이 있으면 정책으로 갈아 넣는다. 계정 전환이 여는 새 창이 사용자 기본으로 돌아온다.

### 시험 개정 (요청받은 그 자리)
`tests/test_session_restart.py:228 test_m1_persist_both_places` 가 옛 승격을 정답으로 고정하고 있었다 — **삭제가 아니라 개정**했다.
· `test_m1_choice_records_without_touching_policy` — 「정책 두 칸은 그대로, 최근선택 칸만 바뀐다」를 새로 못 박는다(정책 칸에 자국이 남으면 실패).
· `test_m1b_restart_persists` → `test_m1b_restart_records_choice` — restart 경로가 쓰는 것이 `{"last_model_choice": …}` **하나뿐**임을 단언.
· 신설 `TestModelPolicyWins` 7건 — 우선순위·워커 보호·명시 선언 존중 + 「읽는 자리가 하나인지」 소스 감시(`auto_resume_model` 읽기 1곳, `last_model_choice` 2곳).
실측: `python3 tests/ session_restart` 28/28 OK. 이웃 회귀 `s9_code_args` 9/9 · `wake` 99/99 · `spawn_reason` 14/14 · `worker_claim` 6/6 · `auto_resume_gate` 2/2 · `stop_reach` 35/35 전부 OK.

### 지금 사용자 설정 상태 (읽기만 함, 변경 안 했다)
`users/nicehugepark/config/settings.json` 은 `auto_resume_model=opus` · `s9code_args=--permission-mode auto --model opus` — 리드가 원복해 둔 그대로다. 새 코드에서는 이 값이 정책이므로 대시보드에서 무엇을 골라도 워커·새 창은 opus 로 뜬다.

### 한계
· P1(범위 재배치 — 세션/계정 저장처)은 손대지 않았다. 별건 REQ 로 남는 것이 리드 판정이었고, 지금은 "정책이 있으면 정책이 이긴다"로 증상과 위험이 함께 닫힌다.
· 정책이 **아예 없는** 사용자는 여전히 최근 선택이 다음 기동에 쓰인다(080 이 고친 「되돌아감」을 되살리지 않기 위한 의도된 폴백). 그 사용자에게 fable 금지를 기계로 지키게 하려면 `s9 user config <이름> auto_resume_model <모델>` 로 **선언**해야 한다.
· 사용자 설정 값 자체는 하나도 안 고쳤다 — 그건 사람의 칸이다.

## History
- 2026-09-01T14:12:22+09:00 created by nicehugepark (status: open)
- 2026-09-01T14:12:43+09:00 status: open -> in-progress (by nicehugepark) — 011 소집 라운드에서 병렬 착수
