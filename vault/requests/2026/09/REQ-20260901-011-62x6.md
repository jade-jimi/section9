---
id: REQ-20260901-011-62x6
type: request
title: 한도 소진 세션 전환 불능
summary: "실측(캡처 7장): fable 한도 100% 세션에서 모델·계정 전환이 「지금 이 세션이 일하는 중이라 바꾸지 않았습니다」로 반복 거부 — 한도로 멈춘 턴은 끝나지도 멈추지도 못해(Esc 중단도 그 세션이 소화 불가) 대시보드만으로는 탈출 불가, 로컬 터미널로만 풀렸다. 재시작 후 「세션이 돌아온 것을 확인하지 못했습니다(1m31s)」도 발생"
goal: 한도 소진(모델 응답 불가) 세션에서 대시보드만으로 모델·계정 전환과 재시작이 되는 것이 실측으로 확인된다
status: done
size: L
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: 3ed7d9fd
agents: ["sub:deep-diver:aa45e7e8", "sub:deep-diver", "lead:claude-fable-5", "sub:backend-developer:aa2563e0", "sub:backend-developer"]
contributions: [{"actor": "sub:deep-diver:aa45e7e8", "item": "011·012 전환 교착 규명", "started": "2026-09-01T14:13:26+09:00", "ended": "2026-09-01T14:26:55+09:00", "result": "done", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/3ed7d9fd-1679-421a-a506-cec3973f8c08/tasks/aa45e7e818256dd24.output"}, {"actor": "sub:deep-diver", "item": "011 전환 교착 원인 규명", "started": "2026-09-01T14:24:39+09:00", "ended": "2026-09-01T14:24:39+09:00", "result": "done", "transcript": ""}, {"actor": "sub:deep-diver:aa45e7e8", "item": "`[2026-09-01 14:26:41 KST - deep-diver]`", "started": "2026-09-01T14:26:55+09:00", "ended": "2026-09-01T14:26:55+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/3ed7d9fd-1679-421a-a506-cec3973f8c08/subagents/agent-aa45e7e818256dd24.jsonl"}, {"actor": "lead:claude-fable-5", "item": "리드 종합(4인 판정 채택·구현 계약) — ① busy 판정: deep-diver A+D 채택 — 한도·합성 이벤트 판독을 공", "started": "2026-09-01T14:51:10+09:00", "ended": "2026-09-01T14:51:10+09:00", "result": "done", "transcript": ""}, {"actor": "sub:backend-developer:aa2563e0", "item": "011·012 서버 구현", "started": "2026-09-01T14:51:48+09:00", "ended": "2026-09-01T15:33:05+09:00", "result": "stalled", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/3ed7d9fd-1679-421a-a506-cec3973f8c08/tasks/aa2563e04bb349301.output", "reason": "814초째 진전 없음 (기준 180초)"}, {"actor": "sub:backend-developer", "item": "TDD 시나리오 (서버 — 011)", "started": "2026-09-01T14:57:10+09:00", "ended": "2026-09-01T14:57:10+09:00", "result": "done", "transcript": ""}, {"actor": "sub:backend-developer", "item": "TDD 시나리오 (서버 — 011) · 검증 결과", "started": "2026-09-01T15:17:09+09:00", "ended": "2026-09-01T15:17:09+09:00", "result": "done", "transcript": ""}, {"actor": "sub:backend-developer", "item": "서버 구현 완료 (backend-developer) — 리드 종합 채택안 ①~⑤ 그대로. 수정 범위 `bin/s9` · `te", "started": "2026-09-01T15:18:12+09:00", "ended": "2026-09-01T15:18:12+09:00", "result": "done", "transcript": ""}]
project: section9
children: ["REQ-20260901-012-62x6", "REQ-20260901-013-62x6", "REQ-20260901-014-62x6", "REQ-20260901-015-62x6", "REQ-20260901-017-62x6"]
relates_why: {}
tags: ["auto-audit", "session", "assets", "terminal", "attached", "png", "image", "py", "code", "project"]
created: 2026-09-01T14:10:39+09:00
status_since: 2026-09-01T16:00:58+09:00
updated: 2026-09-01T16:08:17+09:00
priority: 50
---

## Original

대시보드 터미널에서 claude02.pfe 클로드 계정을 fable-5 모델로 사용 중 모델 사용한다고 꽉 차서 다른 계정으로 전환을 시도했더니 계정 전환이 되지 않았다. 혹시 계정 전환에도 llm 호출이 발생이 되는데 fable 사용한도가 다 되어서 문제가 되나 싶어 opus로 모델 전환을 시도했는데, 그 마저도 실패했다. 결국 로컬 터미널에서 모델을 전환을 시도하니 대시보드 터미널도 모델 전환은 되었지만, 계정 전환은 실패했다. 로컬 터미널에서 계정을 전환하고 다시 시작한 상태이다. 그런데 계정 전환을 하면 왜 기본 모델이 opus가 아니고 fable로 되는거지? 기본 모델로 fable은 절대로 하지않는 요청이 처리가 됐었는데 말이다. 그리고 화면 우측에 나타나는 모델전환, 계정전환에 대한 문구 역시 노출되는 시간 등에 대해서 점검이 필요한 듯하다. 관련 에이전트들을 모두 소집해서 다시 제대로 처리하도록 해라. 그리고 계정 전환을 하는 화면 자체도 이상하다. 계정이 없다가 나타나거나 있는데 사라지거나 한다.
[Image: assets/REQ-20260901-011-62x6/20260901T140552-account7.png]
[Image: assets/REQ-20260901-011-62x6/20260901T140553-account.png]
[Image: assets/REQ-20260901-011-62x6/20260901T140553-account2.png]
[Image: assets/REQ-20260901-011-62x6/20260901T140553-account3.png]
[Image: assets/REQ-20260901-011-62x6/20260901T140553-account4.png]
[Image: assets/REQ-20260901-011-62x6/20260901T140553-account5.png]
[Image: assets/REQ-20260901-011-62x6/20260901T140553-account6.png]

## Notes

### 2026-09-01T14:24:39+09:00 response (agent: sub:deep-diver, by nicehugepark)

원인 규명 (deep-diver) — 뿌리 셋. 전부 실측 재현했고, 코드·git 은 한 줄도 안 고쳤다.

### 뿌리 1 — 한도 소진 응답이 「턴 진행 중」으로 읽힌다 (전환 거부의 전부)

한도에 막힌 턴은 트랜스크립트에 이렇게 끝난다(a8186437 원문):

    {"type":"assistant","message":{"model":"<synthetic>","stop_reason":"stop_sequence",
     "content":[{"type":"text","text":"You've reached your Fable 5 limit. …"}]}}

「일하는 중」을 정하는 곳은 `bin/s9:4182 _transcript_busy` 하나뿐이다. 마지막
메시지 이벤트가 assistant 면 `stop_reason != "end_turn"` 이 곧 busy 다 —
`stop_sequence != end_turn` 이므로 **busy**. `restart_session`(bin/s9:5631)이
그 한 줄로 모델·계정·깊이 전환을 전부 거부한다. 계정 전환이 LLM 을 부르기
때문이 아니다(전환 자체는 모델을 안 쓴다). **게이트가 읽는 파일이 한도로
오염돼 있었을 뿐이다.**

실측 재현 — a8186437 트랜스크립트를 시점별로 잘라 그 함수에 물었다:

    [286] 12:37:05 assistant stop=stop_sequence busy=True
    [292] 12:38:52 assistant stop=stop_sequence busy=True
    [298] 12:39:03 assistant stop=stop_sequence busy=True
    [304] 12:39:54 assistant stop=stop_sequence busy=True
    [309] 12:40:48 user(/model 로컬 실행 stdout) busy=True   ← 로컬에서 모델을 바꾼 직후에도 여전히 거부
    [324] 12:41:28 assistant stop=end_turn       busy=False  ← opus 가 실제 턴을 한 번 끝내고서야 풀림

사용자가 겪은 12:37~12:41 전 구간이 그대로 재현된다. [309]가 「로컬에서 모델을
바꿨는데도 대시보드 계정 전환은 계속 실패」의 답이다 — 모델을 바꾼 사실이
아니라 **턴이 하나 끝난 사실**만이 이 게이트를 연다.

### 뿌리 2 — 유일한 탈출구가 소진된 그 자원을 쓴다 (교착의 완성)

「중단하고 바꾸기」는 수신함에 `kind=interrupt` 줄을 넣는다(bin/s9:16926,
web/app/restart.js:90). 그 줄은 **정상 전달됐다** — 트랜스크립트에 12:37:03 ·
12:38:50 · 12:39:02 · 12:39:52 네 번의 Monitor task-notification 이 찍혀 있다.
전달은 성공했고 소화가 불가능했다: 지시를 읽고 멈추려면 모델이 한 턴을 돌아야
하는데 그 모델이 한도다. 게다가 시도할 때마다 합성 한도 응답이 한 줄 더 쌓여
busy 도장이 새로 찍힌다 — restart.js:105 의 25회×0.6초=15초 재시도는 원리상
수렴할 수 없다. 「15초 안에 끝나지 않았습니다」는 정확한 보고였고, 영원히 참인
보고였다. (SIGINT 경로는 REQ-20260830-047-62x6 에서 세션을 통째로 죽여 폐지됐으므로
남은 길이 이것 하나다.)

### 왜 이렇게 됐나 — 원인의 원인

같은 파일을 읽는 이웃 둘은 이미 알고 있다.
· `session_model`(bin/s9:5418)은 `model` 이 `<` 로 시작하는 **합성 이벤트를
  건너뛴다** — 상태줄이 오해할까 봐(REQ-082).
· `stream_end_info`(bin/s9:15609)는 꼬리에 `reached your`+`limit` 이 있으면
  **「사용 한도 소진」**이라고 이름을 붙인다(REQ-20260901-006-62x6, 어제 추가).
`_transcript_busy` 만 묻지 않는다. 한 파일에 대해 "이 세션이 지금 무엇을 하고
있나"를 세 함수가 각자 손으로 파싱하고, 그중 **게이트만 낡았다.** 뿌리는 판정
로직이 아니라 공유 판독기가 없다는 것이다.

### 뿌리 3 — 계정 전환 재시작은 원리상 대화를 이어받을 수 없다 (「돌아온 것 확인 실패」)

계정 전환은 `CLAUDE_CONFIG_DIR` 을 프로필로 바꿔 `claude --resume <full-sid>`
를 다시 띄운다(cmd_code 재시작 루프 · `_account_env`). 그런데 **대화 기록은
설정 디렉토리마다 따로 산다**(`<CLAUDE_CONFIG_DIR>/projects/<cwd-key>/<sid>.jsonl`).
a8186437 의 기록은 `~/.claude/projects/` 에만 있고, 넘어간 claude01 프로필에는
`projects/` 디렉토리 자체가 없다.

    $ CLAUDE_CONFIG_DIR=<빈 프로필> claude --resume a8186437-7f5e-… -p "say ok"
    No conversation found with session ID: a8186437-7f5e-4278-86cb-29951ca7e349   (exit 1)

    # 그 대화 jsonl 을 프로필의 projects/-home-sjpark1-section9/ 로 복사한 뒤 같은 명령
    Not logged in · Please run /login                                             (exit 1)

두 번째 출력이 증명한다 — 못 찾는 것이 원인이고, **복사만 하면 찾는다.**

이번 실측 흔적도 그대로 맞는다: claude01 프로필의 `settings.json·agents·skills`
가 12:41 스탬프(= `_account_env` 가 그 자리에서 s9-install 을 돌린 시각),
`history.jsonl` 에 12:41:36 부트스트랩 프롬프트 1줄, 그리고 그 프로필의
`.claude.json` 이 기록한 `lastSessionId` 는 a8186437 이 아니라 **60e39ff2**다.
같은 대화가 이어진 것이 아니라 낯선 세션이 하나 뜬 것이다. 그 60e39ff2 의
바인딩은 `{session, ses_doc, ended}` 뿐이고(transcript_path·entry·attach_pid
전부 없음), SES-20260901-005-62x6 에는 12:45:04 의 `session end [other]` 한 줄만 있다.

여기에 판정 기준 결함이 겹친다.
· 화면의 완료 판정은 **옛 sid 로 못 박혀 있다** — restart.js:42
  `if (w.sid && nt.sid !== w.sid) return;`. 재개가 새 sid 로 갈리면(위처럼)
  성공해도 영원히 "아직 안 돌아왔다"이고, 90초 뒤 「돌아온 것을 확인하지
  못했습니다」가 뜬다. 사용자가 본 1m 31s 가 그 90초다.
· s9 쪽 연속성(active_reqs·claim·entry=code·SES 문서)도 전부 sid 를 열쇠로
  쓴다. sid 가 갈리는 순간 클레임과 문서가 고아가 된다.
· 90초 예산 안에 `_account_env` 의 동기 s9-install(첫 전환 시)과 1.2MB
  트랜스크립트를 든 CC 기동이 함께 들어간다 — 성공하는 경우에도 빠듯하다.
· 그리고 이 모든 것이 **SIGTERM 을 먼저 쏜 뒤**에 벌어진다. 지금 코드는 이어질
  수 있는지 확인하지 않고 세션부터 내린다.

### 수정 방향 (구현하지 않음 — 리드 배정용, 대안이 갈리면 값·비용 병기)

### 뿌리 1·2 (busy 판정과 교착)
- **A. 합성 이벤트를 턴 판정에서 제외** — `message.model` 이 `<` 로 시작하는
  assistant 행은 세지 않는다. 코드가 이미 가진 규칙(`session_model`)을 게이트도
  쓰게 하는 것뿐. 값: 이번 사고 즉시 해소, 회귀 위험 최소. 비용: 거의 없음.
  한계: 화이트리스트를 한 모양 더 늘리는 것이라 다음 미지의 모양은 또 막힌다.
- **B. 유휴를 긍정형으로 정의** — busy 는 "마지막이 도구 결과 대기이거나
  assistant `stop_reason=tool_use`" 일 때만. 값: 미지의 stop_reason 이 늘 busy
  로 굳는 구조를 끝냄. 비용: 목록을 잘못 잡으면 **도는 턴에 SIGTERM 이 나간다**
  (이 가드의 존재 이유가 그것). A 보다 회귀 위험이 크니 시험을 먼저 늘려야 한다.
- **C. 사람의 우회로 — busy 창에 「그래도 바꾸기」.** `--resume` 이 대화를
  보존하므로 잃는 것은 진행 중이던 그 턴의 미기록 출력뿐이고, 창이 그 사실을
  말하면 된다. 값: **오판정 하나가 교착이 되는 구조 자체를 없앤다** — A·B 가
  놓치는 다음 모양에도 듣는다. 비용: 살아 있는 턴을 사람이 끊을 수 있게 됨
  (기본값 아님·두 번째 단추·물러나는 쪽이 Enter).
- **D. 한도를 이름으로 말하기** — `stream_end_info` 가 이미 쓰는 꼬리 서명을
  게이트가 함께 읽어, 거부 대신 「이 세션은 한도로 멈춰 있습니다 — 멈출 것이
  없으니 그대로 바꿉니다」로 곧장 진행. 값: 이 갈래에서 사람이 판단할 일이
  사라진다. 비용: 문구 문자열에 의존하므로 문구가 바뀌면 눈이 먼다 — 그래서
  D 는 C 를 대체하지 못하고 C 위에 얹혀야 한다.
- 권고: **A+D 를 한 판독기로 묶고(세 함수가 같은 것을 보게), C 를 항상 열어
  둔다.** B 는 별건으로 시험을 먼저 세운 뒤.

### 뿌리 3 (계정 전환 재시작)
- **E. 이어받게 만든다** — 전환 전에 대화 jsonl 을 대상 프로필의
  `projects/<cwd-key>/` 로 복사(또는 하드링크). 위 실험이 이 길이 통함을
  증명했고, 같은 모양의 코드가 이미 있다(`s9 resume`, bin/s9:11624). 값: 화면이
  하는 약속(「같은 대화가 그 계정으로 이어집니다」)이 참이 된다. 비용: 사본이
  둘이 되고 이후 갈린다(원본을 읽기 전용 이력으로 취급하면 감수 가능).
- **F. 못 이어받으면 약속하지 않는다** — `restart_session` 에 선행 확인:
  account 가 있고 대상 프로필에 그 대화가 없으면(그리고 E 로 옮기지 않을
  거라면) **SIGTERM 전에** 사유와 함께 거부. 값: 오늘처럼 세션부터 죽이고
  나중에 실패를 알리는 순서를 뒤집는다. 비용: 거의 없음. E 를 하든 안 하든
  먼저 넣을 지혈.
- **G. 완료 판정을 sid 가 아니라 세션 계보로** — 래퍼가 재개 후 새 sid 를 마커
  옆에 적고(예: `restart-done-<옛sid>.json`), 바인딩에 `resumed_from` 을 남긴다.
  화면은 옛↔새를 잇고, 클레임·SES 문서도 따라간다. 값: sid 가 갈리는 모든
  재시작(계정 전환 포함)에서 고아 문서가 사라진다. 비용: 마커 규약 한 겹 추가.
- **H. 대안(권고하지 않음)** — 프로필을 옮기지 말고 `~/.claude` 안의 자격증명만
  바꾸기(사용자가 12:45 에 손으로 한 것이 이것). 값: resume 이 그냥 된다.
  비용: 계정별 설정·이력 격리가 사라지고, 두 계정 동시 세션이 불가능해지며,
  도는 CC 밑에서 자격증명 파일을 바꾸는 것은 지원되는 동작이 아니다.

### 확신도
뿌리 1·2·3 모두 **높음** — 1차 증거(트랜스크립트 원문·프로필 파일 스탬프·CLI
실행 출력)로 재현했다. 남은 미지수 하나: 60e39ff2 가 왜 트랜스크립트를 한 줄도
남기지 않고 12:45 에 끝났는지(사용자가 그 창을 직접 닫았을 가능성이 크다) —
이 결론에는 영향이 없다. 그리고 캡처의 헤더 칩이 14:05 에도 claude02 를 말하는
것은 별건이다: `~/.claude/.credentials.json` 은 12:45 에 claude01 로 바뀌어
있다(문구·노출 시간을 보는 designer·ux-writer 쪽 소관으로 넘긴다).

재현 스크립트 둘을 이 노트에 붙인다 — `_transcript_busy` 합성응답 오판정
(정상/한도/반복/진짜진행중/사용자턴 5케이스)과, 실측 트랜스크립트를 시점별로
잘라 물어보는 재생 스크립트. 임시 S9_ROOT 에서만 돌고 정리까지 한다.

[File: assets/REQ-20260901-011-62x6/repro_busy.py]
[File: assets/REQ-20260901-011-62x6/repro_real.py]

### 2026-09-01T14:26:55+09:00 subagent (agent: sub:deep-diver:aa45e7e8, by nicehugepark)

`[2026-09-01 14:26:41 KST - deep-diver]`

### 결론 — 뿌리 넷, 전부 1차 증거로 재현했다. 코드·git 무수정, 노트만 남겼다.

기록: `s9 note REQ-20260901-011-62x6`(6394자 + 재현 스크립트 2개), `s9 note REQ-20260901-012-62x6`(4832자 + 스크립트 1개).

---

### ① 전환 거부 — 한도 소진 응답이 「턴 진행 중」으로 읽힌다

한도에 막힌 턴은 트랜스크립트에 **합성 assistant 이벤트**로 끝난다(a8186437 원문):

```
{"type":"assistant","message":{"model":"<synthetic>","stop_reason":"stop_sequence",
 "content":[{"type":"text","text":"You've reached your Fable 5 limit. …"}]}}
```

「일하는 중」을 정하는 곳은 `/home/sjpark1/section9/bin/s9:4182 _transcript_busy` 하나다 — 마지막 메시지 이벤트가 assistant 면 `stop_reason != "end_turn"` 이 곧 busy. `stop_sequence != end_turn` → **busy**. `restart_session`(bin/s9:5631)이 이 한 줄로 모델·계정·깊이 전환을 전부 거부한다. **계정 전환이 LLM을 부르기 때문이 아니다** — 게이트가 읽는 파일이 한도로 오염돼 있었을 뿐이다.

실측 트랜스크립트를 시점별로 잘라 물은 재생 결과(사용자가 겪은 구간 그대로):

```
[286] 12:37:05 stop=stop_sequence busy=True
[292] 12:38:52 stop=stop_sequence busy=True
[298] 12:39:03 stop=stop_sequence busy=True
[304] 12:39:54 stop=stop_sequence busy=True
[309] 12:40:48 user(/model stdout) busy=True   ← 로컬에서 모델 바꾼 직후에도 여전히 거부
[324] 12:41:28 stop=end_turn      busy=False  ← opus가 실제 턴을 한 번 끝내고서야 풀림
```

[309]가 「로컬에서 모델을 바꿨는데도 대시보드 계정 전환은 계속 실패」의 답이다: 모델을 바꾼 사실이 아니라 **턴이 하나 끝난 사실**만이 이 게이트를 연다.

### ② 교착 — 유일한 탈출구가 소진된 그 자원을 쓴다

「중단하고 바꾸기」의 `kind=interrupt` 는 **정상 전달됐다**(트랜스크립트에 12:37:03·12:38:50·12:39:02·12:39:52 네 번의 Monitor task-notification). 전달은 성공, **소화가 불가능**: 지시를 읽고 멈추려면 모델이 한 턴을 돌아야 하는데 그 모델이 한도다. 게다가 시도마다 합성 한도 응답이 한 줄 더 쌓여 busy 도장이 갱신된다 — `web/app/restart.js:105` 의 25회×0.6초=15초 재시도는 **원리상 수렴 불가**. 「15초 안에 끝나지 않았습니다」는 정확했고 영원히 참인 보고였다.

**원인의 원인**: 같은 파일을 읽는 이웃 둘은 이미 안다 — `session_model`(bin/s9:5418)은 `<`로 시작하는 합성 model을 건너뛰고, `stream_end_info`(bin/s9:15609)는 꼬리의 `reached your`+`limit`을 「사용 한도 소진」이라 부른다(어제 REQ-20260901-006-62x6). `_transcript_busy`만 묻지 않는다. **공유 판독기가 없어 게이트만 낡았다.**

### ③ 「세션이 돌아온 것 확인 실패(1m 31s)」 — 계정 전환 재시작은 원리상 대화를 못 잇는다

대화 기록은 설정 디렉토리마다 따로 산다(`<CLAUDE_CONFIG_DIR>/projects/<cwd-key>/<sid>.jsonl`). a8186437의 기록은 `~/.claude/projects/`에만 있고, 넘어간 claude01 프로필엔 `projects/` 자체가 없다. 실행 증명:

```
CLAUDE_CONFIG_DIR=<빈 프로필> claude --resume a8186437-… -p "say ok"
  → No conversation found with session ID: a8186437-…   (exit 1)
그 jsonl 을 프로필의 projects/-home-sjpark1-section9/ 로 복사 후 같은 명령
  → Not logged in · Please run /login                   (exit 1)
```

두 번째가 증명한다 — 못 찾는 것이 원인이고 **복사만 하면 찾는다.** 흔적도 일치: claude01 프로필의 `settings.json·agents·skills`가 12:41 스탬프(`_account_env`의 s9-install), `history.jsonl`에 12:41:36 부트스트랩 1줄, 그 프로필 `.claude.json`의 `lastSessionId`는 a8186437이 아니라 **60e39ff2**. 그 바인딩은 `{session, ses_doc, ended}`뿐이고 SES-20260901-005엔 12:45:04 `session end [other]` 한 줄만 있다.

겹친 판정 결함: 완료 판정이 **옛 sid에 못 박혀** 있다(`restart.js:42` `if (w.sid && nt.sid !== w.sid) return;`) → 새 sid로 갈리면 성공해도 영원히 "안 돌아옴", 90초 뒤 그 붉은 줄. s9의 클레임·SES·entry=code도 전부 sid가 열쇠라 함께 고아가 된다. 그리고 이 모두가 **SIGTERM을 먼저 쏜 뒤** 벌어진다.

### ④ 계정 전환 후 fable — 세션 1회 선택이 사용자 영속 정책으로 승격된다

`"(by dashboard model change)"` 문자열은 코드 전체에서 `bin/s9:2543·2548` 두 자리뿐, 둘 다 `_persist_model_choice`(bin/s9:2524) 안이다. 그것이 `auto_resume_model` + `s9code_args`를 덮고, 계정 전환이 여는 창은 `s9 code`이며 `cmd_code`(bin/s9:13678)가 `s9code_args`를 읽는다. 임시 S9_ROOT 재현이 profile.md의 8/30 두 줄과 **글자까지 같다.**

- 3단계: 실수가 아니라 REQ-20260825-080의 **의도된 설계**(주석에 이유가 적혀 있다).
- 4단계: 진짜 이유는 **세션·계정 범위의 저장처가 없다**는 것 — 범위 불일치. (대칭으로, 계정 선택은 래퍼 프로세스 메모리에만 남아 창이 닫히면 사라진다. 모델은 너무 오래 남고 계정은 너무 빨리 사라진다.)
- 5단계: 「선언된 정책」과 「방금 한 번 고른 값」의 칸이 하나뿐이라 마지막 쓴 쪽이 이긴다. actor 라벨은 이력일 뿐 판정에 안 쓰인다.
- **곁가지**: `_spawn_model_args`(bin/s9:2487)의 존재 이유가 "fable 한도 소진 시 워커가 전부 즉시 죽는다(8/25 실사고)"인데, 같은 REQ가 넣은 승격이 그 값을 fable로 되돌려 **8/25 사고의 조건을 스스로 복원**했다. ①의 무대가 여기서 만들어졌다.
- **착수 전 필독**: `tests/test_session_restart.py:228 test_m1_persist_both_places`가 이 승격을 정답으로 못 박고 있다. 고치려면 시험을 **개정**해야 하고, 모르면 "시험이 반대한다"에서 멈춘다.

---

### 수정 방향 (구현 안 함 — 노트에 값·비용 병기)

- **011**: A 합성이벤트 제외(값 큼·비용 없음) + D 한도를 이름으로 말하기 → **한 판독기로 묶고**, C 「그래도 바꾸기」를 항상 열어 둔다(오판정 하나가 교착이 되는 구조 자체를 제거). B 긍정형 정의는 회귀 위험이 커 별건.
- **재시작**: F 못 이어받으면 SIGTERM 전에 거부(즉시 지혈) → E 대화 jsonl을 대상 프로필로 복사(같은 모양이 `bin/s9:11624 s9 resume`에 이미 있다) → G 완료 판정을 sid가 아니라 세션 계보로.
- **012**: **P2(정책/최근선택 분리) + P4(`s9code_args`는 안 건드림)를 먼저**, P1(범위 재배치)은 별건 REQ. P5 회귀 시험 4종 필수.

확신도 **높음**(전부 1차 증거). 남은 미지수 둘: 60e39ff2가 트랜스크립트 없이 12:45에 끝난 경위(사용자가 창을 닫았을 가능성), 그리고 캡처 헤더 칩이 14:05에도 claude02를 말하는 것 — `~/.claude/.credentials.json`은 12:45에 claude01로 바뀌어 있다. 후자는 문구·노출 담당(designer·ux-writer) 소관으로 넘긴다.

### 2026-09-01T14:51:10+09:00 response (agent: lead:claude-fable-5, by nicehugepark)

리드 종합(4인 판정 채택·구현 계약) — ① busy 판정: deep-diver A+D 채택 — 한도·합성 이벤트 판독을 공유 판독기 하나로 세우고(_transcript_busy·session_model·stream_end_info 가 같은 눈을 쓴다), 한도로 굳은 턴은 busy 가 아니라 「한도 소진」이라는 제 이름의 갈래다. ② 탈출구: C 채택 — 「그래도 바꾸기」(강제 전환)를 거부 창에 항상 연다. 오판정 하나가 교착이 되는 구조 제거. ③ 재시작: F(대상 프로필이 대화를 못 이어받으면 SIGTERM 전에 거부) → E(대화 jsonl 을 대상 프로필 projects/ 로 복사, s9 resume 의 그 모양) → G(완료 판정을 sid 못박기에서 세션 계보로). ④ 012: P2+P4 — 정책(사용자 선언)과 최근 선택(세션 1회)의 칸을 가르고 s9code_args·auto_resume_model 은 명시 선언만 쓴다. test_session_restart m1 개정 필수. ⑤ 서버↔화면 계약: 전환/재시작 거부 응답에 why_kind("busy"|"limit"|"no_resume") · limit{model,resets_at} · attempt(회차) 를 싣는다 — 화면(014 구현)은 이 필드를 우선 쓰고 없으면 usageLast 폴백. ⑥ 014: designer 처방 1·2·3 + ux-writer 문안 벌 + D1(칩 폭)·D2(재렌더)·D5(감시 수명 통일)·V1(RESTART_SAY 갈래 보강)·V2(칩-줄 문장 통일) 채택. 실행 불가 행동을 버튼 이름으로 약속하지 않는다(한도 갈래에 「중단하고 바꾸기」 금지). 배정: 서버 = backend-developer(011·012, bin/s9+tests), 화면 = frontend-developer(014, web/+tests) 병렬 — 파일 영역 불겹침, 커밋은 리드

### 2026-09-01T14:57:10+09:00 tdd (agent: sub:backend-developer, by nicehugepark)

### TDD 시나리오 (서버 — 011)

실행: `python3 tests/ transcript_read` · `python3 tests/ session_restart`

### 공유 판독기 (관문 한 곳)
- [ ] S1. 정상: 마지막이 실제 assistant `stop_reason=end_turn` → busy False · limit False · model=그 모델
- [ ] S2. 진행 중: 마지막이 실제 assistant `stop_reason=tool_use` → busy True / 마지막이 사용자 턴 → busy True / `[Request interrupted…]` 사용자 턴 → busy False
- [ ] S3. 한도(핵심 회귀): 마지막이 `model=<synthetic>` + `stop_reason=stop_sequence` + "You've reached your Fable 5 limit." → **busy False · limit True · limit_model="Fable 5"**. 같은 줄이 4번 쌓여도 같다(사용자가 겪은 12:37~12:41 모양)
- [ ] S4. 서명 다중화: 문구가 없어도 `error="rate_limit"`/`apiErrorStatus=429` 면 limit — 문구 하나에만 기대지 않는다
- [ ] S5. 판독 불가: 파일 부재·빈 파일·깨진 JSON → busy False(신호를 안 보내는 쪽), 예외 없음
- [ ] S6. 단일 관문: `_transcript_busy`·`session_model`·`stream_end_info` 셋이 같은 판독기를 부른다 — 합성/한도 서명 문자열이 판독기 밖에 중복 정의돼 있지 않다(소스 감시)
- [ ] S7. 이웃 계약 보존: `session_model` 은 여전히 `<synthetic>` 을 건너뛴 마지막 실모델을 낸다(R15 불변) · `stream_end_info` 는 여전히 `end_why="사용 한도 소진"` 을 낸다(test_stream_end 불변)

### 거부 응답 계약 (why_kind · limit · attempt)
- [ ] S8. busy 거부: 진짜 진행 중 → ok False · `why_kind="busy"` · reason 유지
- [ ] S9. attempt 계수: 같은 사유로 연속 거부하면 attempt 1→2→3, 다른 사유/성공이면 초기화
- [ ] S10. limit 갈래: 한도로 굳은 세션의 전환은 **거부하지 않고 진행** — ok True · `why_kind="limit"` · `limit={model,resets_at}`. (멈출 것이 없으니 멈추라고 하지 않는다)

### 강제 전환 (그래도 바꾸기)
- [ ] S11. force: busy 인데 `force=True` → 진행 · `forced=True` · `discarded_turn=True`(하던 턴은 버려진다는 사실을 응답이 말한다)
- [ ] S12. force 없이는 불변: 같은 입력에 force 없으면 여전히 거부(기본값은 보호 쪽)

### 재시작 3단 (F·E·G)
- [ ] S13. F 선행 거부: 계정 전환인데 대상 프로필이 그 대화를 못 이어받고 옮길 수도 없음 → ok False · `why_kind="no_resume"` · **SIGTERM 미발사**(os.kill 호출 0회) · 마커 미기록
- [ ] S14. E 복사: 원본 트랜스크립트가 있으면 대상 프로필 `projects/<cwd-key>/<full-sid>.jsonl` 로 복사한 뒤 진행 · `resume_copied=True`
- [ ] S15. E 비파괴: 대상에 이미 같은 이름의 대화가 있으면 덮지 않는다(복사 생략, 진행)
- [ ] S16. E 대칭: 프로필→@home 방향도 같은 규칙으로 옮긴다(한 방향만 되는 길을 만들지 않는다)
- [ ] S17. G 계보 기록: 재시작이 래퍼 pid 를 열쇠로 계보 기록을 남긴다(옛 full sid·옛 8자·시각)
- [ ] S18. G 계보 잇기: 같은 래퍼 pid 밑에 새 sid 바인딩이 나타나면 새 바인딩에 `resumed_from`, 옛 바인딩에 `resumed_to` 가 채워지고 계보 기록은 소비된다(멱등)
- [ ] S19. G 전달: `/api/chat/target` 응답이 `resumed_from`·`resumed_to` 를 싣는다(화면의 sid 못박기 해제 재료)
- [ ] S20. G 유효기간: 낡은 계보 기록은 잇지 않고 정리한다 — 엉뚱한 세션이 남의 계보를 물지 않는다

### 회귀 (실사고 그 모양)
- [ ] S21. 실사고 재현: 한도 합성 응답으로 끝난 트랜스크립트 + 살아 있는 attach → `restart_session(model=...)` 이 「턴 진행 중」으로 거부하지 않는다 (2026-09-01 12:37~12:41 교착의 봉쇄)
- [ ] S22. 보호는 남는다: 진짜 도는 턴(tool_use)에는 여전히 SIGTERM 이 나가지 않는다

### 2026-09-01T15:17:09+09:00 tdd (agent: sub:backend-developer, by nicehugepark)

### TDD 시나리오 (서버 — 011) · 검증 결과

실행 실측: `python3 tests/ transcript_read` 9/9 OK · `python3 tests/ restart_contract` 19/19 OK · `python3 tests/ session_restart` 28/28 OK · `python3 tests/ stream_end` 4/4 OK · `python3 tests/ restart_reach` 15/15 OK (전부 exit=0 실측)

### 공유 판독기 (관문 한 곳) — tests/test_transcript_read.py
- [x] S1. 정상: 마지막이 실제 assistant end_turn → busy False · limit False · model=그 모델
- [x] S2. 진행 중: tool_use → busy True / 사용자 턴 → busy True / `[Request interrupted…]` → busy False (문자열·블록 양쪽)
- [x] S3. 한도(핵심 회귀): `<synthetic>` + stop_sequence + "reached your Fable 5 limit" → busy False · limit True · limit_model="Fable 5". 4번 쌓여도 같다
- [x] S4. 서명 다중화: `error=rate_limit` / `apiErrorStatus=429` / 문구 — 셋 중 하나면 한도
- [x] S5. 판독 불가(부재·빈 파일·깨진 JSON) → busy False, 예외 없음
- [x] S6. 단일 관문: `_transcript_busy`·`session_model`·`stream_end_info` 셋이 `transcript_read` 를 부른다(함수 몸 감시) + 서명 문자열이 판독기 밖에 중복 정의 없음
- [x] S7. 이웃 계약 보존: `session_model` 은 합성 표기를 건너뛴 실모델(R15 불변) · `stream_end_info` 는 `end_why="사용 한도 소진"`(test_stream_end 4/4 불변)

### 거부 응답 계약 — tests/test_restart_contract.py
- [x] S8. busy 거부: ok False · why_kind="busy" · SIGTERM 미발사
- [x] S9. attempt 1→2→3, 성공하면 초기화
- [x] S10. limit 갈래: 거부하지 않고 진행 · ok True · why_kind="limit" · limit={model,resets_at}

### 강제 전환
- [x] S11. force: busy 인데 진행 · forced True · discarded_turn True
- [x] S12. force 없으면 여전히 거부 (기본값은 보호 쪽)

### 재시작 3단
- [x] S13. F: 못 이어받으면 ok False · why_kind="no_resume" · **os.kill 0회** · 마커 미기록
- [x] S14. E: 대상 프로필 `projects/<cwd-key>/<full>.jsonl` 로 복사 후 진행 · resume_copied True
- [x] S15. E 비파괴: 대상에 이미 있으면 덮지 않는다 (`s9 resume` 과 같은 규칙)
- [x] S16. E 대칭: 프로필→@home 도 같은 규칙
- [x] S17. G: 래퍼 pid 를 열쇠로 계보 기록
- [x] S18. G: 새 sid 바인딩에 resumed_from · 옛 바인딩에 resumed_to, 기록 소비(멱등). **덤: 클레임(active_reqs·claim_at)도 함께 건넌다** — 미클레임으로 보이면 워처가 겹쳐 스폰한다(REQ-20260823-083 엣지)
- [x] S19. `/api/chat/target` 이 lineage_link() 를 지나고 resumed_from·resumed_to 를 싣는다
- [x] S20. 낡은 계보 기록은 잇지 않고 정리

### 회귀 — 실측 트랜스크립트 재생
- [x] S21. a8186437 원문을 deep-diver 와 **같은 지점**에서 잘라 재생: [280]·[286]·[292]·[298]·[304] 전부 `busy=True` → **`busy=False limit=True limit_model="Fable 5"`**. [324] end_turn 은 그대로 busy=False
- [x] S22. 보호 유지: 진짜 도는 턴(tool_use)에는 여전히 SIGTERM 이 안 나간다

### 2026-09-01T15:18:12+09:00 response (agent: sub:backend-developer, by nicehugepark)

서버 구현 완료 (backend-developer) — 리드 종합 채택안 ①~⑤ 그대로. 수정 범위 `bin/s9` · `tests/` 한정, web/ 무수정, 커밋 없음.

### 1. 공유 판독기 — 게이트가 이웃과 같은 눈을 쓴다
`transcript_read(path)`(bin/s9:4283 근방) 하나가 한 파일을 읽고 다섯을 답한다: `busy` · `limit` · `limit_seen` · `limit_model`/`resets_at` · `model`. `_transcript_busy`·`session_model`·`stream_end_info` 셋이 전부 이 함수만 부른다(각자 하던 손파싱 삭제, `_model_cache` 폐지 — 캐시도 판독기 안 mtime+size 로 통일).
· 한도 판정은 서명 셋(`error="rate_limit"` / `apiErrorStatus=429` / `reached your…limit` 문구) 중 하나 — 문구 하나에만 기대면 문구가 바뀌는 날 눈이 먼다.
· 합성 assistant 이벤트(`model` 이 `<` 로 시작)는 턴을 끝내려고 끼워진 줄이지 도는 턴이 아니다 → busy 아님.
· 미러 등 모르는 모양을 위해 꼬리 8KB 원문 서명 검사를 `limit_seen` 폴백으로 남겼다(종전 `stream_end_info` 동작 보존).

### 2. 거부 응답 계약 (화면이 소비할 필드)
`restart_session` 의 거부/진행 응답에 `why_kind`("busy"|"limit"|"no_resume") · `limit{model,resets_at}` · `attempt`(같은 사유 연속 회차, `state/terminal/restart-try-<sid8>.json` 에 서버가 센다. 사유가 바뀌거나 한 번 지나가면 초기화) 를 싣는다. **한도 갈래는 거부가 아니라 진행이다** — ok=True + why_kind="limit" 로 나가므로, 화면은 "멈출 것이 없어 그대로 바꿉니다"를 쓸 수 있다.

### 3. 강제 전환
`restart_session(..., force=True)` / `POST /api/session/restart {"force": true}`. busy 거부와 no_resume 거부를 둘 다 지난다. 응답이 잃는 것을 말한다: `forced` · `discarded_turn`(하던 턴의 미기록 출력) · `discarded_conversation`(대화를 못 이어받는 경우). 기본값은 여전히 보호 쪽이다.

### 4. 재시작 3단
· **F** — `_ensure_resumable()` 가 **SIGTERM 전에** 판정한다. 못 이어받으면 마커도 안 쓰고 신호도 안 쏜다(종전엔 세션부터 내리고 나중에 실패를 알렸다).
· **E** — 대화 jsonl 을 대상 프로필 `projects/<cwd-key>/<full-sid>.jsonl` 로 복사한다. cwd-key 는 **원본 트랜스크립트의 부모 디렉토리 이름**을 그대로 쓴다(`s9 resume` 의 `cwd.replace("/","-")` 와 같은 값이되 더 안전한 출처). 대상에 이미 있으면 덮지 않는다. 방향 대칭(프로필↔@home).
· **G** — 재시작이 `state/terminal/lineage-<래퍼pid>.json` 을 남기고, `/api/chat/target` 이 매 폴마다 `lineage_link()` 로 잇는다. **열쇠는 래퍼 pid** 다: `--resume` 은 새 sid 를 만들지만(a8186437→60e39ff2) 재시작 루프를 든 `s9 code` 프로세스는 그대로다. 새 바인딩에 `resumed_from`, 옛 바인딩에 `resumed_to` 가 남고 폴 응답이 둘을 싣는다. **덤으로 클레임(active_reqs·claim_at)도 건넌다** — 미클레임으로 보이면 워처가 그 REQ 에 무인 작업자를 겹쳐 스폰한다. 바인딩 읽고-고쳐-쓰기는 락 안에서 하고, 락을 못 잡으면 다음 폴이 다시 한다(멱등).
· `_restart_cmd` 는 마커에 resume 이 없으면 `--resume` 을 안 붙인다(강제 전환으로 대화 없이 계정만 옮기는 갈래).

### 실측 증거 (실사고 구간 재생)
a8186437 원문을 deep-diver 와 같은 지점에서 잘라 물었다: `[280][286][292][298][304]` 가 전부 `busy=True` → **`busy=False limit=True limit_model="Fable 5"`** 로 바뀌었고, `[324]` end_turn 은 그대로 busy=False. 그 구간이 교착의 전부였다.

### 한계·미검증
· E 복사는 SIGTERM **전에** 일어난다 — 복사와 프로세스 종료 사이에 쓰인 마지막 줄은 사본에 안 들어간다(한도·유휴 세션에서는 쓰이는 것이 없다). 복사를 뒤로 미루면 래퍼가 먼저 claude 를 띄우는 경쟁이 생겨 이 순서를 택했다.
· `resets_at` 은 실측 트랜스크립트에 그 문장이 없어 **실데이터로 못 채워 봤다**(정규식만 있고 빈 문자열로 나간다). 짐작하지 않는다.
· G 의 잇기는 **폴이 돌아야** 일어난다(대시보드가 떠 있어야 한다). 유효기간 15분.
· 실서버 재기동은 안 했다(리드 몫) — 지금 도는 serve 는 옛 코드다.

### 리드에게 (범위 밖 관측)
`tests/test_restart_reach.py:test_the_server_guard_is_untouched` 를 개정했다(가드는 그대로지만 판정 출처가 `_transcript_busy(tp)` → `transcript_read(tp)` 로 옮겼으므로). 같은 파일의 화면 계약 두 건은 그 시각 frontend-developer 가 고치는 중이었고, 지금은 15/15 OK 다.

### 2026-09-01T15:59:21+09:00 commit (by nicehugepark)

커밋 4a67e07 — 한도로 굳은 턴은 「일하는 중」이 아니다 — 전환 교착·정책 승격·인코딩 근원 수정 (REQ-20260901-011-62x6 · 012 · 016)

## History
- 2026-09-01T14:10:39+09:00 created by nicehugepark (status: open)
- 2026-09-01T14:12:10+09:00 status: open -> in-progress (by nicehugepark) — 사용자 소집 지시 — deep-diver(전환 거부·busy 판정 경로 규명) + designer·ux-writer(문구·노출 실측) 병렬 착수, 종합 후 구현
- 2026-09-01T16:00:58+09:00 status: in-progress -> done (by nicehugepark) — goal 충족: 실사고 트랜스크립트(a8186437) 구간 재생에서 종전 busy 오독 전 지점이 limit 갈래로 판정(공유 판독기 transcript_read), 한도 갈래는 거부가 아니라 진행(ok=True·why_kind=limit) + 강제 전환(force) + 재시작 3단(SIGTERM 전 판정·대화 복사·계보 승계)이 표적 75건·전체 회귀 exit 0 으로 실측. 화면 짝(014)은 별도 review. 커밋 4a67e07
