---
id: REQ-20260901-017-62x6
type: request
title: 계정 전환 3중 잔결함
summary: "011·014 착지 직후(16:0x, 새 코드) 실전환에서: ① claude02 로 바꿨는데 계정 정보는 claude01(전환 미적용 또는 되돌아옴) ② 상단 「세션 다시 시작 중」 칩이 안 걷힘 ③ 재시작이 정책(opus)이 아니라 fable 유지로 떠 한도 계정에서 다시 먹통 — 재시작된 세션이 곧 이 리드 세션(3ed7d9fd)이라 흔적이 신선함"
goal: 세 증상 각각의 원인이 규명되고, 전환 후 계정·칩·모델이 실측으로 맞는다
status: review
size: M
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: 3ed7d9fd
agents: ["sub:deep-diver:aa24a005", "sub:deep-diver", "sub:subagent:a56ac561", "sub:backend-developer:ac53016b", "sub:subagent:aa737f60", "sub:backend-developer"]
contributions: [{"actor": "sub:deep-diver:aa24a005", "item": "017 전환 잔결함 규명", "started": "2026-09-01T16:09:39+09:00", "ended": "2026-09-01T16:23:17+09:00", "result": "done", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/3ed7d9fd-1679-421a-a506-cec3973f8c08/tasks/aa24a0055bf668b2a.output"}, {"actor": "sub:deep-diver", "item": "017 3중 잔결함 규명", "started": "2026-09-01T16:22:24+09:00", "ended": "2026-09-01T16:22:24+09:00", "result": "done", "transcript": ""}, {"actor": "sub:subagent:a56ac561", "item": "Appending note to REQ-20260901-017-62x6", "started": "2026-09-01T16:22:53+09:00", "ended": "2026-09-01T16:22:53+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/3ed7d9fd-1679-421a-a506-cec3973f8c08/subagents/agent-a56ac5611ccbe470e.jsonl"}, {"actor": "sub:deep-diver:aa24a005", "item": "`[2026-09-01 16:09:40 KST - deep-diver]` (규명 완료 · 노트 기록: `s9 note REQ-", "started": "2026-09-01T16:23:17+09:00", "ended": "2026-09-01T16:23:17+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/3ed7d9fd-1679-421a-a506-cec3973f8c08/subagents/agent-aa24a0055bf668b2a.jsonl"}, {"actor": "sub:backend-developer:ac53016b", "item": "017 서버 수정 구현", "started": "2026-09-01T16:24:31+09:00", "ended": "2026-09-01T16:51:31+09:00", "result": "done", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/3ed7d9fd-1679-421a-a506-cec3973f8c08/tasks/ac53016be5bbe5102.output"}, {"actor": "sub:subagent:aa737f60", "item": "Checking REQ-20260901-017-62x6 metadata", "started": "2026-09-01T16:29:10+09:00", "ended": "2026-09-01T16:29:10+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/3ed7d9fd-1679-421a-a506-cec3973f8c08/subagents/agent-aa737f60461fe0dc3.jsonl"}, {"actor": "sub:backend-developer", "item": "TDD 시나리오 (서버측 R1·R2a·R3·R4·R5·R6 + 곁가지 둘)", "started": "2026-09-01T16:29:21+09:00", "ended": "2026-09-01T16:29:21+09:00", "result": "done", "transcript": ""}, {"actor": "sub:backend-developer", "item": "TDD 시나리오 (개정 · 서버측 구현 완료분 체크)", "started": "2026-09-01T16:50:02+09:00", "ended": "2026-09-01T16:50:02+09:00", "result": "done", "transcript": ""}, {"actor": "sub:backend-developer", "item": "서버측 R1·R2a·R3·R4·R5·R6 + 곁가지 둘을 구현했다. 범위는 `bin/s9` · `tests/` 뿐이고 web/", "started": "2026-09-01T16:50:55+09:00", "ended": "2026-09-01T16:50:55+09:00", "result": "done", "transcript": ""}, {"actor": "sub:backend-developer:ac53016b", "item": "`[2026-09-01 16:50:58 KST - backend-developer]`", "started": "2026-09-01T16:51:31+09:00", "ended": "2026-09-01T16:51:31+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/3ed7d9fd-1679-421a-a506-cec3973f8c08/subagents/agent-ac53016be5bbe5102.jsonl"}]
project: section9
parent: REQ-20260901-011-62x6
relates: ["QST-20260901-005-62x6"]
relates_why: {"QST-20260901-005-62x6": "017 확인 포인트의 「한도 찬 계정 사전 차단」 문구가 낳은 의미 질문"}
tags: ["auto-audit", "session", "assets", "dashboard", "attached", "png", "image"]
created: 2026-09-01T16:07:09+09:00
status_since: 2026-09-01T17:02:54+09:00
updated: 2026-09-01T17:56:59+09:00
priority: 50
---

## Original

계정을 claude02.pfe로 전환했더니 전환이 성공했던 것 같은데 화면 상단에는 전환 중이라고 표시, 계정정보는 claud01.pfe로 표시 되는 문제, 그리고 fable 모델로 시작되어 한도가 만료가 된 계정이라 아무것도 안되는 문제가 있었다.
[Image: assets/REQ-20260901-017-62x6/20260901T160617-image.png]

## Notes

### 2026-09-01T16:22:24+09:00 response (agent: sub:deep-diver, by nicehugepark)

deep-diver 규명 — 세 증상은 서로 다른 뿌리 셋이고, 그중 둘은 이번 라운드 수정이 **닿지 못한 자리**다.

### 결론 (먼저)

1. **계정 전환은 실패하지 않았다. 성공했고, 사용자가 되돌렸다.** 16:03:46~16:05:32 이 세션은 실제로 claude02 프로필로 떠 있었다. 그런데 헤더 계정·사용량 칩은 `claude_usage()` 가 `~/.claude.json`·`~/.claude/.credentials.json` 를 **하드코딩**해 읽어(bin/s9:6396~6440, cfg_dir 인자가 아예 없다) 언제나 홈 계정(claude01)을 찍는다. 화면이 "안 바뀌었다"고 말하니 사용자는 다시 계정 창을 열었고, **claude02 로 붙어 있는 동안 그 창에서 고를 수 있는 줄은 `@home`(=claude01) 하나뿐**이라(실측 재현 A) 그 클릭이 전환을 되돌렸다. lineage 의 `account:"@home"` 은 그 두 번째 요청이다.
2. **fable 은 「유지」 갈래의 잘못이 아니라 래퍼가 12:45:36 에 얼려 둔 인자다.** `cmd_code` 는 `extra = code_launch_args()` 를 **루프 밖에서 한 번** 계산하고(bin/s9:14079, `while True:` 는 14100), 매 재시작은 그 `base_cmd` 를 재사용한다. 래퍼 pid 128187 은 12:45:36 기동 — 그때 `s9code_args` 는 아직 `--permission-mode auto --model fable`(08-30 13:36 대시보드가 정책 칸을 덮은 그 값)이었고, 정책 복구(09-01 14:12:57 → opus)도 011·012 코드 수정도 그 뒤다. 마커의 model 이 빈 값("유지")이면 `_restart_cmd` 는 base 를 손대지 않으므로 fable 이 그대로 나간다 — 실측 재현 C 가 관측된 명령줄과 토큰·순서까지 일치.
3. **「자동 복구 1회」는 세션과 무관하다.** `/api/guard` 의 `restarts` = 마지막 감시 시작(12:45:14) 이후 `died` 이벤트 수(bin/s9:15022). 해당 이벤트는 13:30:16 의 **대시보드 서버** 재기동 하나다. 세션을 되살린 주체는 없다 — 되살린 것은 사용자의 두 번째 클릭이다.
4. **칩이 안 걷힌 것은 「완료 판정의 유일한 증거」가 `listening` 이기 때문이다.** `restartWatch` 는 `nt.listening && 8s 경과` 에서만 done 을 낸다(web/app/restart.js:43). claude02 세션은 기동 3초 만에 Fable 한도 합성응답을 받고(16:03:49·16:03:52·16:03:54) 첫 턴을 못 끝내 **Monitor tail 을 arm 하지 못했다** → listening 이 영원히 거짓 → 칩은 90초를 세다 "돌아온 것을 확인하지 못했습니다"로 갈 운명이었다. 캡처(1분 8초)는 그 중간이다. 세션은 **떠 있었는데** 화면은 "안 돌아왔다"고 말하려던 참이었다.

### 실측 타임라인 (전부 1차 증거)

| 시각 | 사실 | 출처 |
|---|---|---|
| 12:45:36 | 래퍼 `python3 bin/s9 code` pid 128187 기동 — 이 시점 bin/s9 를 메모리에 적재, `s9code_args=--model fable` | `ps -eo lstart`, users/nicehugepark/profile.md:129~134 |
| 14:12:57 | 정책 복구 `auto_resume_model=opus` · `s9code_args=… --model opus` | profile.md:133~134 |
| 15:20:50 | bin/s9 마지막 수정(=011·012·014 수정 포함) | `stat bin/s9` |
| 16:01:30 | 홈(claude01) 세션 마지막 활동, 모델 `claude-fable-5` | 홈 트랜스크립트 |
| 16:03:43~45 | **전환 #1**: restart(account=claude02) → 마커·SIGTERM | 트랜스크립트 경계, 프로필 mtime |
| 16:03:46 | claude02 프로필로 재기동, sid 는 **3ed7d9fd 그대로** | `~/.claude-profiles/claude02…/history.jsonl` |
| 16:03:49~54 | `You've reached your Fable 5 limit.` ×2 → 첫 턴 실패, Monitor 미arm | claude02 프로필 트랜스크립트(캡처의 그 두 줄) |
| ~16:04:53 | **사용자 캡처** (칩 1분 8초 = 16:03:45+68s) | 첨부 이미지 |
| 16:04:34 | 사용자가 그 죽은 세션에 `/usage` 입력 | claude02 history.jsonl |
| 16:05:31 | **전환 #2**: restart(account="@home", model="") → lineage·SIGTERM | state/terminal/lineage-128187.json |
| 16:05:33 | 재기동 pid 476911 — `--model fable`, CLAUDE_CONFIG_DIR **없음**(=claude01) | `ps`, `/proc/476911/environ` |
| 16:05:51 | Monitor arm → 이후 정상 응답 (claude01 의 fable 은 5% 라 멀쩡) | 홈 트랜스크립트, 캡처 헤더 |

캡처가 16:06:17 업로드인데 칩이 1분 8초인 모순은 이렇게 풀린다 — 캡처는 **전환 #1 중(≈16:04:53)** 에 찍혔고, 사용자는 전환 #2 까지 겪은 뒤 그 그림을 붙였다. 캡처 화면의 스트림 순서(부트스트랩 → 응답까지 3s → 한도 → task 통지 → 한도)가 claude02 트랜스크립트와 정확히 같은 것이 증거다.

### 재현 (격리, `S9_ROOT`/HOME 임시 · 실서버·리드세션 무해, 정리 완료)

**A. claude02 로 붙어 있을 때 계정 창이 주는 줄** — `account_rows(<claude02 프로필>, settle=False)`
```
key=@home                       email=claude01.pfe@dev.itcen.com  current=False ready=True also=[…/claude01.pfe@dev.itcen.com]
key=claude02.pfe@dev.itcen.com  email=claude02.pfe@dev.itcen.com  current=True  ready=True
switchable = 1  ->  고를 수 있는 줄: ['@home']
```
같은 메일이 두 자리(홈 · claude01 프로필)에 있어 서버가 한 줄로 합치고, 그 합쳐진 줄의 key 가 `@home` 이다. 창은 그 줄을 `claude01.pfe@dev.itcen.com [기본]` 으로 그린다 — **사람 눈에 "claude01" 인 줄을 누르면 `@home` 이 나간다.** 그리고 claude02 는 `current` 라 `claudeAccountSwitch` 가 조기 반환한다(session.js:337). 즉 그 창에서 누를 수 있는 유일한 결과가 "claude01 로 되돌리기"다.

**B. 헤더가 읽는 자리** — `claude_usage()` 안의 경로가 전부 `os.path.expanduser("~")` 고정. `claude_home()`(CLAUDE_CONFIG_DIR 을 아는 함수)조차 안 쓴다. 캐시 무효화 열쇠도 `~/.claude/.credentials.json` 의 mtime 이라 **프로필 전환으로는 캐시가 갱신조차 안 된다.**

**C. 재시작 인자 조립** — `_restart_cmd(base, {"resume": sid, "model": ""})`
```
base(12:45:36 정책) = ['claude','--permission-mode','auto','--model','fable','<bootstrap>']
결과              = ['claude','--permission-mode','auto','--model','fable','<bootstrap>','--resume','3ed7d9fd-…']
```
`ps` 로 본 pid 476911 의 명령줄과 완전 일치. 마커가 모델을 말하면(`model="opus"`) base 의 `--model` 을 걷어내고 opus 를 붙이는 것도 확인 — 즉 **경로는 멀쩡하고, 들어간 값이 낡았다.**

### 뿌리의 뿌리 — 왜 세 번 다 「고쳤는데 안 고쳐졌」나

세 증상의 공통 조상이 하나 있다. **재시작 계약은 두 프로세스에 걸쳐 있고, 둘의 코드·설정 나이가 따로 논다.**
- 서버(pid 475253, 16:03:45 기동)는 bin/s9 15:20:50 판 = **수정 반영됨**.
- 래퍼(pid 128187, 12:45:36 기동)는 그 전 판을 메모리에 든 채, 게다가 그때 읽은 인자까지 얼려 들고 있다 = **수정도 정책 복구도 미반영**.

이 하네스는 그 함정을 이미 한 번 배웠다 — `code_stamp()` 주석: "서버는 기동 시점 코드를 메모리에 들고 돈다 … 아무 신호가 없으면 그 어긋남이 며칠도 간다. 실제로 12시간 갔다"(REQ-20260826-011-62x6). 그런데 지문을 남기는 대상은 `RUNNING_FILES`(bin/s9 · s9-audit-prompt)를 **서버가** 확인하는 것뿐이다. `s9 code` 래퍼는 같은 성질의 장수 프로세스이고 재시작 계약의 나머지 절반인데, 지문도 경고도 없다. 화면의 "서버 재기동 필요" 칩에 대응하는 "**세션 창 다시 열기 필요**"가 없다.

### 수정 방향 (구현하지 않음 · 대안과 비용)

**R1 — 헤더 계정/사용량이 세션 계정을 따라가게 (증상 ① 근원, 최우선)**
`claude_usage(cfg_dir=None)` 로 인자화하고 `/api/usage` 가 `session_cfg_dir(chat_target(None))` 를 넘긴다. 캐시 열쇠는 `(cfg_dir, cred_mtime)`. 비용 작음(한 함수 + 한 호출부), 이득 큼: 화면이 계정을 두고 스스로와 모순되는 일이 사라지고, **R4 의 전제(대상 계정의 한도를 아는 것)가 여기서 생긴다**. 대안(헤더에 "이 화면 계정" 이라고 이름만 붙이기)은 증상 완화일 뿐 — 사람은 여전히 어느 쪽이 참인지 물어야 한다.

**R2 — 「유지」의 뜻을 고정한다 (증상 ③ 근원)**
셋 다 유효하고 서로 배타적이지 않다.
- R2a *(가장 싼 것)* 루프 안에서 매 회 `code_launch_args()` 재계산: `cmd = _restart_cmd(["claude"] + code_launch_args() + 사용자인자 + [BOOTSTRAP], m)`. 정책 복구가 **다음 재시작부터** 먹는다. 래퍼가 낡은 코드면 이 고침 자체가 안 실린다는 한계.
- R2b *(근원)* 재시작 = **래퍼 re-exec**. 마커를 소비한 뒤 `os.execv` 로 현재 bin/s9 의 `code` 를 다시 든다(acct 는 마커/환경으로 이월, fd·TTY 는 exec 가 보존). 서버·래퍼의 코드 나이가 재시작마다 맞춰지므로 "고쳤는데 안 고쳐졌다"가 구조적으로 사라진다. 비용: 루프 상태(acct)를 파일/env 로 외부화, 재진입 시 preflight 중복 실행 회피.
- R2c *(계약 명확화)* 서버가 「유지」를 빈 값으로 흘리지 말고 `session_model(b)` 로 **지금 그 세션의 모델을 실어** 마커에 적는다. 그러면 base_cmd 가 아무리 낡아도 값이 새 것이라 관측된 사고가 성립하지 않는다. 다만 "지금 fable 이면 계속 fable" 이라 R4 가 함께 있어야 한다.

**R3 — 래퍼에도 낡은 코드 지문 (재발 방지)**
`s9 code` 가 기동 시 `running_code_stamp()` 를 세션 바인딩(또는 `state/terminal/code-<sid8>.json`)에 남기고, `/api/chat/target` 이 `stale` 을 실어 준다. 화면은 이미 있는 어휘("기동 시점 코드로 돌고 있습니다")를 재사용해 **세션 칩**으로 세운다. 비용 작음. 이것이 없으면 R2a·R2c 를 넣어도 다음 사람이 같은 자리에서 "왜 내 수정이 안 먹지"를 다시 겪는다.

**R4 — 한도로 굳을 재시작을 미리 막는다 (증상 ③의 사용자 피해)**
R1 이 서면 `restart_session` 이 "대상 계정 × 나갈 모델"의 한도를 볼 수 있다. 100% 면 `why_kind:"limit_target"` 으로 **거부하고 대안을 말한다**(모델을 바꿔 가기 / 그래도 가기). 지금은 서버도 화면도 대상 계정의 한도를 모른다 — 화면의 한도 판정(`restartLimit`)조차 홈 계정 사용량을 본다. 대안(경고만)은 이 사용자가 겪은 "아무것도 안 되는 창"을 못 막는다.

**R5 — 완료 판정에 두 번째 증거를 준다 (증상 ②)**
`listening`(Monitor tail)은 **첫 턴이 성공해야** 생기는 2차 산물이다. 세션이 떴는지는 서버가 1차로 안다: 래퍼 pid 의 자식 claude 생존 + 바인딩의 `attach_pid` 갱신 + 트랜스크립트 성장. `/api/chat/target` 이 `alive`(프로세스 근거)와 `listening`(수신 근거)을 **갈라서** 주고, 칩은 alive 면 "돌아왔습니다 — 아직 수신 대기 전"으로, 그 상태로 굳으면 그 사실(예: 한도)을 말한다. 지금처럼 하나로 묶으면 "떠 있는데 안 떴다"가 계속 나온다.

**R6 — `@home` 의 뜻이 두 곳에서 갈린다 (같은 계열, 아직 안 물었지만 한 줄 옆)**
`_profile_dir("@home")` = `claude_home()` = `CLAUDE_CONFIG_DIR or ~/.claude` (**서버 환경 의존**). 래퍼의 `@home` 갈래 = "CLAUDE_CONFIG_DIR 을 지운다" (**언제나 ~/.claude**). 이번엔 serve 에 그 변수가 없어 안 물렸지만, `cmd_code` 가 감시자를 띄울 때 `env=` 없이 Popen 해 **환경을 상속**하므로(bin/s9:14038), 프로필 계정으로 연 세션이 대시보드를 처음 띄우면 그 순간부터 `@home` 줄이 그 프로필을 가리킨다. 그때 서버는 트랜스크립트를 프로필 X 로 복사하고 래퍼는 ~/.claude 로 띄운다 — 두 정의가 어긋나는 자리. 리드 질문의 "서버 재기동마다 기본 줄이 바뀌는 함정인가"에 대한 답: **이번 사고의 원인은 아니었지만(실측: serve 475253 에 CLAUDE_CONFIG_DIR 없음), 함정 자체는 실재하고 정상 사용 경로 하나 건너에 있다.** 정의를 한 곳(`_profile_dir`)으로 모으고 래퍼가 그것을 쓰게 하라.

### 곁가지로 드러난 것 (별건 등록 후보)

- `_consume_restart_marker` 의 glob `restart-*.json` 이 **`restart-try-<sid8>.json` 까지 잡는다**(bin/s9:5787). 60초(RESTART_FRESH_SEC) 넘은 try 기록은 래퍼가 지운다 — 그런데 회차 창은 600초(RESTART_ATTEMPT_WIN)다. 011 이 방금 넣은 "같은 벽 몇 번째" 카운터가 래퍼에 의해 조용히 1 로 되돌아갈 수 있다. 접두사를 갈라라(`restart-try/` 하위 디렉토리 또는 `rtry-`).
- **lineage 는 이번 경우 작동하지 않는다.** `claude --resume` 이 sid 를 새로 만든다는 전제인데(REQ-011 G), 이번 두 번의 재시작은 sid 가 `3ed7d9fd` 그대로였다(claude02 history.jsonl · 현재 바인딩 동일). `lineage_link` 는 `sid == old` 를 건너뛰므로 잇지도, 기록을 소비하지도 않는다 — `resumed_from` 미기록의 정체가 이것이다(고장이 아니라 **가정 밖**). 같은 sid 로 돌아온 경우를 "이미 이어진 것"으로 판정해 기록을 소비하도록 해야 파일이 900초 떠다니지 않는다.
- `_lineage_path(wrapper_pid)` 는 **래퍼당 한 파일**이라 두 번째 재시작이 첫 번째를 덮었다. 그래서 전환 #1 의 흔적이 state/ 에 하나도 안 남았고, 이 조사는 프로필 디렉토리 mtime 과 프로필 쪽 트랜스크립트로 복원해야 했다. 재시작 이력은 append(JSONL) 가 맞다 — 관측 비용 거의 0, 다음 조사 비용 크게 절감.
- 재사용 지식 후보: **"메모리에 코드를 들고 도는 프로세스는 서버 하나가 아니다 — `s9 code` 래퍼도 그렇고, 그쪽은 설정까지 얼려 든다."** `code_stamp` 주석이 남긴 교훈의 미적용 사례로 knowledge 화 가치가 있다(리드 판단에 맡김).

### 확신도

- ① 헤더가 홈 계정 고정 = **높음** (코드 직접 · 인자 부재 · 캐시 열쇠까지 확인).
- ① 사용자가 되돌리게 된 경위 = **기계적으로 높음**(요청이 `@home` 을 실었고 그 값을 낼 수 있는 UI 경로가 하나뿐임을 재현 A 로 확인) / **의도는 중간**(클릭 로그가 없다 — 사용자가 "claude02 를 다시 고르려" 했는지 "일단 claude01 로 돌아가려" 했는지는 문서로 확정 불가).
- ② 칩 = **높음** (claude02 세션이 첫 턴을 못 끝냈고 tail 이 없었다는 것이 트랜스크립트로 확정).
- ③ fable = **매우 높음** (재현 C 가 명령줄과 완전 일치).
- ④ 자동 복구 = **높음** (`restarts` 집계 정의 + serve-guard.log 실물).

### 남은 미지수

- claude02 계정의 Fable 한도가 **언제부터** 100% 였는지 — 그 계정의 usage 를 서버가 조회한 적이 없어(R1 의 부재가 곧 이 공백) 기록이 없다. R1 이 서면 이 질문은 다음부터 자동으로 답이 남는다.
- 16:03:45 의 서버 기동 주체(리드의 16:00:24 재기동과 별개로 한 번 더 떴다) — serve-guard.log 에 `died` 가 없다. 이번 사고와는 무관하나 지문/감시자 기록이 서로를 못 설명하는 자리라 언젠가 볼 값어치는 있다.

### 2026-09-01T16:22:53+09:00 subagent (agent: sub:subagent:a56ac561, by nicehugepark)

Appending note to REQ-20260901-017-62x6

### 2026-09-01T16:23:17+09:00 subagent (agent: sub:deep-diver:aa24a005, by nicehugepark)

`[2026-09-01 16:09:40 KST - deep-diver]` (규명 완료 · 노트 기록: `s9 note REQ-20260901-017-62x6 --label response --agent deep-diver`, 9,100자)

### 결론

세 증상은 뿌리가 셋이고, **그중 둘은 이번 라운드 수정이 물리적으로 닿지 못한 자리**다.

**① 계정 — 전환은 성공했다. 화면이 거짓말을 했고, 사용자가 되돌렸다.**
16:03:46~16:05:32 이 세션은 **실제로 claude02 프로필로 떠 있었다**(claude02 프로필의 history.jsonl·트랜스크립트·credentials mtime 삼중 확인). 그런데 헤더 계정/사용량 칩은 `claude_usage()` 가 `~/.claude.json`·`~/.claude/.credentials.json` 를 **하드코딩**해 읽는다 — cfg_dir 인자가 아예 없고, `claude_home()`(CLAUDE_CONFIG_DIR 을 아는 함수)조차 안 쓰며, 캐시 무효화 열쇠까지 홈의 credentials mtime 이라 프로필 전환으로는 갱신조차 안 된다. 그래서 화면은 언제나 claude01 을 찍는다.
그 상태에서 사용자가 계정 창을 다시 열면 — **claude02 로 붙어 있는 동안 고를 수 있는 줄은 `@home` 하나뿐이다**(재현 A). claude02 는 `current` 라 조기 반환되고, 남은 한 줄은 화면에 `claude01.pfe@dev.itcen.com [기본]` 으로 그려지지만 실제 전송값은 `@home` 이다. lineage 의 `account:"@home"` 은 이 두 번째 요청이고, 그것이 전환을 되돌렸다.

**② 칩 — 세션은 돌아왔는데 화면의 유일한 증거가 「첫 턴 성공의 부산물」이었다.**
`restartWatch` 는 `nt.listening && 8s` 에서만 done 을 낸다. claude02 세션은 기동 3초 만에 Fable 한도 합성응답을 받아(16:03:49·52·54 — 캡처의 그 두 줄이 바로 claude02 세션의 것이다) 첫 턴을 못 끝냈고 **Monitor tail 을 arm 하지 못했다** → listening 영원히 거짓 → 90초 뒤 "돌아온 것을 확인하지 못했습니다"로 갈 운명이었다. 캡처(1분 8초)는 그 중간이다. 캡처 시각 모순도 여기서 풀린다: 캡처는 **전환 #1 중(≈16:04:53)** 에 찍혔고 16:06:17 에 붙여졌다.

**③ fable — 「유지」의 잘못이 아니라 래퍼가 12:45:36 에 얼려 둔 인자다.**
`cmd_code` 는 `extra = code_launch_args()` 를 **`while True:` 밖에서 한 번** 계산하고 매 재시작이 그 `base_cmd` 를 재사용한다. 래퍼 pid 128187 은 12:45:36 기동 — 그때 `s9code_args` 는 아직 fable 이었고, 정책 복구(14:12:57 opus)도 011·012 코드 수정(bin/s9 15:20:50)도 그 뒤다. 재현 C 가 관측된 명령줄과 토큰·순서까지 일치:
```
_restart_cmd(['claude','--permission-mode','auto','--model','fable','<bootstrap>'], {'model':'', 'resume':sid})
 -> ['claude','--permission-mode','auto','--model','fable','<bootstrap>','--resume','3ed7d9fd-…']
```

**④ 자동 복구 1회 — 세션과 무관.** `/api/guard` 의 `restarts` = 마지막 감시 시작(12:45:14) 이후 `died` 수. 해당 이벤트는 13:30:16 의 **대시보드 서버** 재기동 하나다. 세션을 되살린 주체는 없다 — 사용자의 두 번째 클릭이다.

### 뿌리의 뿌리

**재시작 계약이 두 프로세스에 걸쳐 있고, 둘의 코드·설정 나이가 따로 논다.** 서버(475253, 16:03:45 기동) = bin/s9 15:20:50 판, 수정 반영됨. 래퍼(128187, 12:45:36 기동) = 그 전 판을 메모리에 든 채 **그때 읽은 인자까지 얼려** 들고 있음. 이 하네스는 `code_stamp()` 주석에서 이미 그 교훈을 적어 뒀지만("서버는 기동 시점 코드를 메모리에 들고 돈다 … 실제로 12시간 갔다"), 지문 대상이 서버뿐이다. `s9 code` 래퍼는 같은 성질의 장수 프로세스이자 재시작 계약의 나머지 절반인데 지문도 경고도 없다.

### 수정 방향 (구현 안 함 · 노트에 대안·비용 포함)

- **R1** `claude_usage(cfg_dir)` 인자화 + `/api/usage` 가 `session_cfg_dir` 전달, 캐시 열쇠 `(cfg_dir, cred_mtime)`. ①의 근원이자 **R4 의 전제**. 최우선.
- **R2** 「유지」의 뜻 고정 — a) 루프 안에서 `code_launch_args()` 재계산(싸다) b) **재시작 = 래퍼 re-exec**(근원: 코드 나이가 재시작마다 맞는다) c) 서버가 빈 값 대신 `session_model(b)` 를 마커에 실어 보냄.
- **R3** 래퍼에도 낡은 코드 지문 → 세션 칩 "창 다시 열기 필요". 없으면 다음 사람이 또 "왜 내 수정이 안 먹지"를 겪는다.
- **R4** R1 위에서 「대상 계정 × 나갈 모델」 한도 100% 면 `why_kind:"limit_target"` 으로 거부 + 대안 제시. 지금은 화면의 한도 판정조차 홈 계정 사용량을 본다.
- **R5** `/api/chat/target` 이 `alive`(프로세스 근거)와 `listening`(수신 근거)을 갈라서 주기 — ②의 근원.
- **R6** `@home` 의 뜻이 두 곳에서 갈린다: `_profile_dir("@home")`=`claude_home()`(서버 환경 의존) vs 래퍼의 "CLAUDE_CONFIG_DIR 제거"(항상 ~/.claude). **리드 질문에 대한 답: 이번 원인은 아니었으나(serve 475253 에 그 변수 없음 실측) 함정은 실재한다** — `cmd_code` 가 감시자를 `env=` 없이 Popen 해 환경을 상속하므로, 프로필 계정으로 연 세션이 대시보드를 처음 띄우면 그 순간부터 `@home` 이 그 프로필을 가리킨다.

### 곁가지 (별건 후보)

- `_consume_restart_marker` 의 glob `restart-*.json` 이 **`restart-try-*.json` 까지 잡아** 60초 넘은 회차 기록을 지운다 — 회차 창은 600초. 011 이 방금 넣은 "같은 벽 몇 번째"가 래퍼에 의해 1 로 되돌아갈 수 있다.
- **lineage 는 이번 경우 작동하지 않았다**: 두 재시작 모두 sid 가 `3ed7d9fd` 그대로였고(`--resume` 이 새 sid 를 만든다는 011 G 의 전제 밖), `lineage_link` 는 `sid == old` 를 건너뛴다 → `resumed_from` 미기록의 정체. 고장이 아니라 가정 밖.
- `_lineage_path(wrapper_pid)` 가 래퍼당 한 파일이라 **두 번째 재시작이 첫 번째를 덮었다** — 전환 #1 의 흔적이 state/ 에 하나도 없어 프로필 mtime 으로 복원해야 했다. append(JSONL) 가 맞다.

### 확신도·미지수

①(코드) 높음 / ①(사용자 의도) 중간 — 클릭 로그가 없어 "claude02 를 다시 고르려 했는지, claude01 로 돌아가려 했는지"는 확정 불가(요청이 `@home` 을 실었고 그 값을 낼 UI 경로가 하나뿐이라는 것까지가 확정). ② 높음 · ③ 매우 높음 · ④ 높음.
미지수: claude02 의 Fable 한도가 언제부터 100% 였는지 — 서버가 그 계정 usage 를 조회한 적이 없다(R1 부재가 곧 이 공백).

재현은 임시 HOME(`…/scratchpad/dd17-repro`)에서 돌리고 삭제했다. 코드·git 무수정, 리드 세션·실서버 무해. 노트 원문: `/tmp/claude-1000/-home-sjpark1-section9/3ed7d9fd-1679-421a-a506-cec3973f8c08/scratchpad/dd17-note.md`

### 2026-09-01T16:29:10+09:00 subagent (agent: sub:subagent:aa737f60, by nicehugepark)

Checking REQ-20260901-017-62x6 metadata

### 2026-09-01T16:29:21+09:00 tdd (agent: sub:backend-developer, by nicehugepark)

### TDD 시나리오 (서버측 R1·R2a·R3·R4·R5·R6 + 곁가지 둘)

시험 파일: `tests/test_switch_residue.py` (신규) · 기존 계약 개정: `tests/test_account_switch.py` · `test_account_merge.py` · `test_restart_contract.py`

### R1 — 사용량·계정이 **그 세션의 설정 디렉토리**를 따라간다
- [ ] R1-1 정상: `claude_usage(cfg_dir=<프로필A>)` 가 A 의 `.claude.json`·`.credentials.json` 을 읽는다 (이메일=A 계정, 토큰은 A 것)
- [ ] R1-2 정상: 같은 서버 프로세스에서 A → B 로 cfg_dir 을 바꿔 부르면 **B 의 계정**이 나온다 (캐시가 A 를 재사용하지 않는다)
- [ ] R1-3 경계: `cfg_dir=None` 이면 종전대로 `~/.claude`(=`account_home_dir`) 를 본다 — 기존 U1~U4 계약 불변
- [ ] R1-4 캐시 열쇠: 열쇠가 `(cfg_dir, cred_mtime)` 이다 — A 의 자격증명 mtime 이 바뀌면 A 만 무효, B 캐시는 살아 있다
- [ ] R1-5 회귀(증상 ①): `/api/claude/usage` 가 `session_cfg_dir(chat_target(sid))` 를 넘긴다 (소스 계약)
- [ ] R1-6 실패: 대상 프로필에 자격증명이 없으면 200 + `ok:false` + 사유 (500 금지)

### R2a — 「유지」가 얼어붙은 인자를 쓰지 않는다
- [ ] R2a-1 회귀(증상 ③): 래퍼 기동 시점 정책이 `--model fable` 이어도, 정책이 `opus` 로 바뀐 뒤의 재시작 인자에는 `--model opus` 가 실린다 (`code_restart_cmd`)
- [ ] R2a-2 경계: 마커가 모델을 말하면 그 값이 이긴다 (정책보다 방금 고른 값)
- [ ] R2a-3 계약: `cmd_code` 의 `while True:` 루프 **안**에서 인자를 다시 만든다 (소스 계약 — 루프 밖 base 재사용 금지)

### R3 — 래퍼도 제 코드 나이를 말한다
- [ ] R3-1 정상: 래퍼가 기동 시 `state/terminal/code-<pid>.json` 에 지문을 남긴다
- [ ] R3-2 회귀: 지문 이후 `bin/s9` 가 바뀌면 `wrapper_code_age(pid)["stale"]` 이 참, 안 바뀌면 거짓
- [ ] R3-3 경계: 지문이 없는 옛 래퍼는 **기동 시각 vs 파일 mtime** 으로 판정한다 (근거 `basis="started"`)
- [ ] R3-4 경계: 아무 근거도 못 읽으면 낡았다고 **단정하지 않는다** (`{}` — 거짓 경고 금지)
- [ ] R3-5 계약: `/api/chat/target` 과 재시작 응답에 `stale_wrapper`(+`stale_wrapper_parts`) 가 실린다

### R4 — 한도로 굳을 전환을 미리 막는다
- [ ] R4-1 회귀(증상 ③ 피해): 대상 계정 × 나갈 모델의 한도가 100% 면 `ok:false` + `why_kind:"limit_target"` + `limit{model,resets_at,percent,account}`, **SIGTERM 이 나가지 않는다**
- [ ] R4-2 정상: 대상 계정의 그 모델이 한도 밖이면 그대로 진행한다
- [ ] R4-3 경계: 「유지」(model="") 면 **지금 세션의 모델**로 판정한다 (실사고 그 모양)
- [ ] R4-4 문: `force=True` 는 이 벽도 지난다
- [ ] R4-5 실패: 대상 계정 사용량을 모르면(조회 실패·자격증명 없음) **막지 않는다** — 모르는 것으로 사람을 세우지 않는다

### R5 — 「떠 있다」와 「듣고 있다」를 가른다
- [ ] R5-1 정상: `/api/chat/target` 이 `alive` 와 `listening` 을 **따로** 싣는다
- [ ] R5-2 회귀(증상 ②): tail 이 없어도(첫 턴 실패) attach 프로세스가 살아 있으면 `alive:true, listening:false`
- [ ] R5-3 경계: `ended` 세션은 어느 신호로도 `alive` 가 되지 않는다
- [ ] R5-4 불변: `live` 는 종전 뜻 그대로 (`listening or alive`)

### R6 — `@home` 의 뜻은 한 벌이다
- [ ] R6-1 회귀: `CLAUDE_CONFIG_DIR` 이 프로필로 설정된 환경에서도 `_profile_dir("@home")` 이 `~/.claude` 를 가리킨다
- [ ] R6-2 정상: `account_rows` 의 `@home` 줄도 같은 자리를 말한다
- [ ] R6-3 계약: `cmd_code` 가 감시자를 띄울 때 `CLAUDE_CONFIG_DIR` 을 **명시적으로 걷어낸다**

### 곁가지
- [ ] X1-1 회귀: `_consume_restart_marker` 가 회차 기록(`rtry-*.json`)을 지우지 않는다 — 낡아도 남는다
- [ ] X1-2 정상: 제 래퍼를 지목한 진짜 마커는 그대로 소비된다
- [ ] X2-1 정상: 재시작 두 번이면 계보 기록이 **두 줄**이다 (덮어쓰기 아님)
- [ ] X2-2 회귀: `--resume` 이 **같은 sid** 로 돌아온 경우(attach_pid 만 바뀜) 그 기록이 소비된다 — 900초 떠다니지 않는다
- [ ] X2-3 불변: 새 sid 로 갈린 경우의 잇기·클레임 승계는 종전 그대로

### 2026-09-01T16:50:02+09:00 tdd (agent: sub:backend-developer, by nicehugepark)

### TDD 시나리오 (개정 · 서버측 구현 완료분 체크)

시험: `tests/test_switch_residue.py`(신규 37건) · 개정 `tests/test_restart_contract.py` · `test_account_switch.py` · `test_account_merge.py`
실행: `python3 tests/ switch_residue restart_contract compact_signal session_restart` → 91건 OK / 확장 실행 172건 OK

### R1 — 사용량·계정이 그 세션의 설정 디렉토리를 따라간다
- [x] R1-1 정상: `claude_usage(<프로필A>)` 가 A 의 자격증명으로 A 를 묻는다 (토큰 비노출)
- [x] R1-2 정상: A → B 로 바꿔 부르면 B 의 계정이 나온다 (캐시가 A 를 재사용하지 않는다)
- [x] R1-3 경계: 인자 없으면 `@home` — 기존 U1~U4 계약 불변 (test_claude_usage 5건 그대로 통과)
- [x] R1-4 캐시 열쇠 `(cfg_dir, cred_mtime)`: A 의 무효화가 B 캐시를 버리지 않는다
- [x] R1-5 계약: `/api/claude/usage` 가 `session_cfg_dir` 를 넘긴다
- [x] R1-6 실패: 자격증명 없으면 200 + ok:false, 업스트림 미호출
- [x] (추가) 판정 경로는 `offline=True` 로 네트워크를 기다리지 않는다
- [x] (추가) 계정 창을 여는 순간 줄마다 사용량을 데운다 (R4 의 전제)

### R2a — 「유지」가 얼어붙은 인자를 쓰지 않는다
- [x] R2a-1 회귀: 정책이 fable→opus 로 바뀌면 다음 재시작 인자에 opus 가 실린다
- [x] R2a-2 경계: 마커가 모델을 말하면 그 값이 이긴다 (`--model` 한 번만)
- [x] R2a-3 계약: 인자를 만드는 자리가 `while True:` 루프 **안**이다

### R3 — 래퍼도 제 코드 나이를 말한다
- [x] R3-1·R3-2 지문을 남기고, 그 뒤 코드가 바뀌면 `stale`(+`parts`)
- [x] R3-3 경계: 지문 없는 옛 래퍼는 기동 시각 vs mtime (`basis="started"`)
- [x] R3-4 경계: 근거가 없으면 `{}` — 낡았다고 단정하지 않는다
- [x] R3-5 계약: `/api/chat/target` · 재시작 응답에 `stale_wrapper`(+parts·wrapper_pid)
- [x] (추가) 실프로세스의 기동 시각을 실제로 읽는다 / 죽은 래퍼의 지문은 치운다

### R4 — 한도로 굳을 전환을 미리 막는다
- [x] R4-1 회귀: 대상 계정×모델이 100% 면 `why_kind:"limit_target"` + limit{model,percent,resets_at,account}, SIGTERM·마커 없음
- [x] R4-2 정상: 40% 면 그대로 진행
- [x] R4-3 경계: 「유지」면 지금 세션 모델로 판정 / 나갈 모델이 다르면 그 모델로
- [x] R4-4 문: `force=True` 는 이 벽도 지난다
- [x] R4-5 실패: 모르면 막지 않는다 · effort 만 바꾸는 길은 묻지도 않는다

### R5 — 「떠 있다」와 「듣고 있다」를 가른다
- [x] R5-1 계약: 폴이 `alive` 와 `listening` 을 따로 싣는다
- [x] R5-2 회귀: tail 이 없어도 attach 프로세스가 살아 있으면 `alive`
- [x] R5-3 경계: `ended` 는 어느 신호로도 alive 가 아니다
- [x] R5-4 불변: `live` 는 종전 뜻 (`listening or alive`) — platform_live 회귀 통과

### R6 — `@home` 의 뜻은 한 벌이다
- [x] R6-1 회귀: CLAUDE_CONFIG_DIR 이 프로필이어도 `_profile_dir("@home")` = `~/.claude`
- [x] R6-2 정상: `account_rows` 의 `@home` 줄도 같은 자리
- [x] R6-3 계약: 감시자·서버를 띄울 때 그 변수를 걷어낸다

### 곁가지
- [x] X1-1 회귀: 마커 수거가 회차 기록(`rtry-*`·옛 `restart-try-*`)을 지우지 않는다
- [x] X1-2 정상: 제 래퍼를 지목한 마커는 그대로 소비된다
- [x] X2-1 정상: 재시작 두 번이면 두 줄 (덮어쓰기 아님)
- [x] X2-2 회귀: 같은 sid 로 돌아온 재시작도 기록이 닫힌다(`done:"same-sid"`)
- [x] X2-3 불변: 새 sid 잇기·클레임 승계 종전대로 (restart_contract S18·S18b)
- [x] (추가) 살아 있는 계보는 최신 하나 (`superseded`) · 아직 안 뜬 재시작은 열어 둔다

미구현(이번 범위 밖, 별건 후보): R2b 래퍼 re-exec · R2c 마커에 현재 모델 싣기 · 화면(web/) 문안.

### 2026-09-01T16:50:55+09:00 response (agent: sub:backend-developer, by nicehugepark)

서버측 R1·R2a·R3·R4·R5·R6 + 곁가지 둘을 구현했다. 범위는 `bin/s9` · `tests/` 뿐이고 web/ 은 손대지 않았다. 커밋하지 않았다.

### 무엇을 고쳤나 (파일: /home/sjpark1/section9/bin/s9)

**R1 — 헤더가 그 세션의 계정을 말한다.** `claude_usage(cfg_dir=None, ttl=60, offline=False)`. 경로 하드코딩(`~/.claude.json`·`~/.claude/.credentials.json`)을 걷어내고 이메일 판독은 `account_of(cfg)` 한 곳에 맡겼다(기본 자리만 `.claude.json` 이 바깥이라는 규칙을 두 벌로 만들지 않으려고). 메모리 캐시는 `_usage_cache[cfg_dir]` 슬롯으로, 머신 공유 파일 캐시도 `~/.cache/s9/usage-<sha1(cfg)[:12]>.json` 으로 갈랐다 — 한 파일을 돌려쓰면 A 의 사용량이 B 로 샌다. `/api/claude/usage` 는 `claude_usage(session_cfg_dir(chat_target(sid)))`.

**R2a — 「유지」는 지금 정책이다.** `code_base_cmd(claude_args)` / `code_restart_cmd(claude_args, m)` 를 새로 두고 `cmd_code` 의 `while True:` 루프가 매 회 `code_restart_cmd` 를 부른다. 12:45 에 얼린 `--model fable` 이 16:05 의 재시작으로 나가던 통로가 닫혔다.

**R3 — 래퍼도 제 코드 나이를 말한다.** `s9 code` 가 기동 시 `state/terminal/code-<pid>.json` 에 `running_code_stamp()` 를 남긴다(`wrapper_stamp_write`, 죽은 래퍼 것은 `wrapper_stamp_sweep`). 판정은 `wrapper_code_age(pid)`: ① 지문이 있으면 지문 비교(`basis="stamp"`, 파일 단위 `parts`) ② 없으면 **프로세스 기동 시각 vs 파일 mtime**(`basis="started"`) — 지금 도는 pid 128187 처럼 지문 기능 이전에 뜬 래퍼가 곧 가장 낡은 래퍼라, 여기서 침묵하면 겨냥한 자리가 빈다. ③ 아무 근거도 없으면 `{}`(단정하지 않는다). `/api/chat/target` 에 `wrapper_pid`·`stale_wrapper`·`stale_wrapper_parts`, 재시작 응답에도 `stale_wrapper`.

**R4 — 한도로 굳을 자리로 안 보낸다.** `usage_limit_hit(usage, model)` 이 모델 한정 100%(`scope_name` 매칭, `claude-fable-5`↔`Fable 5`) 또는 범위 없는 100%(계정 전체)를 잡는다. `restart_session` 은 계정·모델이 실제로 바뀔 때만, 대상 `cfg_dir`(=`_profile_dir(account)` 또는 세션의 현재 자리)과 **나갈 모델**(`model or session_model(b)` — 「유지」면 지금 모델)로 물어 `why_kind:"limit_target"` + `limit{model,percent,resets_at,account}` 으로 사전 거부한다. force 는 지난다.

  · **설계 결정 하나**: 이 판정은 `offline=True` 로 **캐시만 본다**. 사람이 단추를 누른 자리에 10초 업스트림 타임아웃을 끼워 넣지 않기 위해서다. 대신 계정 창을 여는 순간(`/api/accounts`)이 `accounts_warm_usage(rows)` 로 줄마다 뒤에서 데운다 — 사람의 명시 동작에서만 도는 자리라 5초 폴이 업스트림을 때리는 일이 없다. 판정이 받아들이는 나이는 `USAGE_DECIDE_TTL=900`(100% 는 분 단위로 안 풀린다). 모르면 막지 않는다.

**R5 — 「떠 있다」와 「듣고 있다」.** `chat_alive(b)` 를 새로 갈라냈다(프로세스·기록 근거만). `chat_live` 는 뜻이 그대로다 — `listening or chat_alive` 로 다시 쓰여 판정 경로가 한 곳이다. `/api/chat/target` 에 `alive` 를 `listening` 옆에 실었다.

**R6 — `@home` 의 뜻.** `account_home_dir()`(언제나 `~/.claude`)를 세워 `_profile_dir("@home")`·`account_rows`·세션 줄의 계정 폴백이 그것을 쓴다. `claude_home()` 은 "이 프로세스의 설정"이라는 제 뜻으로 남았다(훅 설치 판정 등). 더해 `cmd_code` 가 감시자를 띄울 때 `CLAUDE_CONFIG_DIR` 을 명시적으로 걷어낸다.

**곁가지 ① 마커 수거.** 회차 기록 이름을 `rtry-<sid8>.json` 으로 갈랐고(`restart-` 이름 공간 밖), `_consume_restart_marker` 는 `wrapper_pid` 를 가진 파일만 마커로 본다 — 남의 파일은 읽지도 지우지도 않는다. 옛 이름의 자리는 `_restart_attempt_clear` 가 한 번 치운다.

**곁가지 ② 계보.** `lineage-<pid>.jsonl` 에 **append**. 기록에 `from_pid`(그때의 attach pid)를 실어, `--resume` 이 **같은 sid** 로 돌아온 경우를 "attach 프로세스가 갈렸다"는 증거로 판정해 닫는다(`done:"same-sid"`) — 900초 떠다니던 그 기록의 정체. 기록은 지우지 않고 `done` 으로 닫는다(파일이 곧 그 래퍼의 재시작 이력 = 다음 조사 자료). 살아 있는 계보는 최신 하나뿐이라 앞선 미결은 `superseded`. 갈아쓰기는 원자적(임시 이름은 glob 밖). 파일은 래퍼가 죽을 때 `wrapper_stamp_sweep` 이 치운다.

### 검증

- 신규 `tests/test_switch_residue.py` 37건 + `restart_contract`·`compact_signal`·`session_restart` 91건 OK
- 영향권 확장 실행 172건 OK (`stop_reach account_merge account_switch code_bootstrap worker_claim session_wake` 포함), 별도로 `platform_live chat_target_worker hook_target_alive compacting_visible account_flicker profile_hooks` 75건 OK, `claude_usage` 5건 OK
- 실서버·리드 세션·실 래퍼(pid 128187)는 건드리지 않았다. 시험은 네트워크를 쓰지 않는다(urlopen 대역).

### 기존 계약 개정 (근거 주석 포함)

- `test_restart_contract.py`: 무대에 `account_home_dir`·`claude_usage` 목을 추가(실기계 계정 상태가 판정에 새지 않게), S17 은 이력의 마지막 줄을 읽고, S20 은 "파일 삭제" 대신 "그 줄이 `expired` 로 닫힘 + 다시 후보가 되지 않음"을 본다.
- `test_account_switch.py`·`test_account_merge.py`: `@home` 자리를 `account_home_dir` 로 세운다.

### 한계·미검증

- **R2b(래퍼 re-exec)·R2c(마커에 현재 모델 싣기) 는 안 했다** — 리드 지시대로 별건 후보. R2a 는 래퍼가 낡은 코드를 들고 있으면 그 고침 자체가 안 실린다는 한계가 그대로 남고, 그래서 R3(창 다시 열기 신호)가 짝이다.
- `pid_start_time` 의 `ps` 갈래(맥)는 이 기계에서 실행 검증 불가 — `/proc` 갈래만 실측했다(리눅스/WSL). 못 읽으면 0.0 → 판정 안 함이라 실패 모드는 안전하다.
- R4 는 캐시가 비어 있으면(계정 창을 안 열고 API 를 직접 두드리는 경로) 막지 않는다 — 설계상 그렇다.
- 화면(web/) 은 손대지 않았다: `alive`·`stale_wrapper`·`why_kind:"limit_target"` 은 **필드만** 실었고 문안·표시는 designer/frontend 몫이다.

### 2026-09-01T16:51:31+09:00 subagent (agent: sub:backend-developer:ac53016b, by nicehugepark)

`[2026-09-01 16:50:58 KST - backend-developer]`

REQ-20260901-017-62x6 서버측 여섯(R1·R2a·R3·R4·R5·R6) + 곁가지 둘 구현 완료. 범위는 `bin/s9`·`tests/` 뿐, web/ 무수정, 커밋 없음. TDD 노트 선작성 → 전 시나리오 체크, 최종 보고는 `--label response --agent backend-developer` 로 REQ 문서에 남겼다.

### 고친 것 — `/home/sjpark1/section9/bin/s9`

- **R1** `claude_usage(cfg_dir=None, ttl=60, offline=False)`. 하드코딩 경로를 걷어내고 이메일 판독은 `account_of(cfg)` 에 맡겼다(기본 자리만 `.claude.json` 이 바깥이라는 규칙을 두 벌로 만들지 않으려고). 메모리 캐시는 cfg_dir 슬롯, 파일 캐시도 `~/.cache/s9/usage-<sha1[:12]>.json` 으로 갈랐다 — 한 파일을 돌려쓰면 A 의 사용량이 B 로 샌다. `/api/claude/usage` 가 `session_cfg_dir(chat_target(sid))` 를 넘긴다.
- **R2a** `code_base_cmd()` / `code_restart_cmd()` 를 세우고 `cmd_code` 의 `while True:` **안**에서 매 회 재계산. 12:45 에 얼린 `--model fable` 이 나가던 통로가 닫혔다.
- **R3** 래퍼가 기동 시 `state/terminal/code-<pid>.json` 에 지문을 남기고(`wrapper_stamp_write`), `wrapper_code_age(pid)` 가 ① 지문 비교 ② 지문이 없으면 **프로세스 기동 시각 vs 파일 mtime**(`basis="started"` — 지문 기능 이전에 뜬 래퍼가 곧 가장 낡은 래퍼라 여기서 침묵하면 겨냥한 자리가 빈다) ③ 근거 없으면 `{}`. 폴과 재시작 응답에 `stale_wrapper`·`stale_wrapper_parts`·`wrapper_pid`.
- **R4** `usage_limit_hit()` + `restart_session` 사전 거부(`why_kind:"limit_target"`, `limit{model,percent,resets_at,account}`), force 는 통과. **설계 결정**: 이 판정은 `offline=True` 로 캐시만 본다 — 사람이 단추를 누른 자리에 10초 업스트림 타임아웃을 끼워 넣지 않으려고. 대신 계정 창을 여는 순간(`/api/accounts`)이 `accounts_warm_usage()` 로 줄마다 뒤에서 데운다(5초 폴이 아니라 사람의 명시 동작에서만 돈다). 모르면 막지 않는다.
- **R5** `chat_alive(b)` 를 갈라내고 `chat_live = listening or chat_alive` 로 다시 써 판정 경로를 한 곳에 뒀다. 폴에 `alive` 를 `listening` 옆에.
- **R6** `account_home_dir()`(언제나 `~/.claude`)을 세워 `_profile_dir("@home")`·`account_rows`·세션 줄 폴백이 쓰고, `claude_home()` 은 "이 프로세스의 설정"이라는 제 뜻으로 남겼다. `cmd_code` 의 감시자 Popen 은 `CLAUDE_CONFIG_DIR` 을 명시적으로 걷어낸다.
- **곁가지 ①** 회차 기록 이름을 `rtry-<sid8>.json` 으로 갈랐고, `_consume_restart_marker` 는 `wrapper_pid` 를 가진 파일만 마커로 본다(옛 이름 자리는 clear 가 한 번 치운다).
- **곁가지 ②** `lineage-<pid>.jsonl` append. 기록에 `from_pid` 를 실어 **같은 sid 로 돌아온 재시작**을 "attach pid 가 갈렸다"는 증거로 닫는다(`done:"same-sid"`) — 900초 떠다니던 그 기록의 정체. 기록은 지우지 않고 `done` 으로 닫고(파일이 곧 재시작 이력), 살아 있는 계보는 최신 하나(`superseded`), 갈아쓰기는 원자적, 파일은 래퍼가 죽을 때 sweep.

### 검증 (실행)

- 신규 `/home/sjpark1/section9/tests/test_switch_residue.py` 37건 + `restart_contract`·`compact_signal`·`session_restart` = 91건 OK
- 영향권 확장 172건 OK(`stop_reach account_merge account_switch code_bootstrap worker_claim session_wake` 포함), `platform_live chat_target_worker hook_target_alive compacting_visible account_flicker profile_hooks` 75건 OK, `claude_usage` 5건 OK
- 실서버·리드 세션·실 래퍼(pid 128187) 무접촉. 시험은 네트워크를 쓰지 않는다(urlopen 대역).

### 기존 계약 개정 (근거 주석 포함)

`tests/test_restart_contract.py` — 무대에 `account_home_dir`·`claude_usage` 목 추가(실기계 계정 상태가 판정에 새지 않게), S17 은 이력의 마지막 줄을 읽고, S20 은 "파일 삭제" 대신 "그 줄이 `expired` 로 닫히고 다시 후보가 되지 않음"을 본다. `tests/test_account_switch.py`·`tests/test_account_merge.py` — `@home` 자리를 `account_home_dir` 로.

### 한계

R2b(래퍼 re-exec)·R2c(마커에 현재 모델)는 지시대로 미구현 — R2a 는 래퍼가 낡은 코드를 들고 있으면 그 고침 자체가 안 실린다는 한계가 남고, R3 이 그 짝이다. `pid_start_time` 의 `ps` 갈래(맥)는 이 기계에서 실행 검증 불가(`/proc` 갈래만 실측; 못 읽으면 판정 안 함). R4 는 계정 창을 안 연 경로에서는 막지 않는다(설계상). 화면은 **필드만** 실었고 문안은 designer/frontend 몫이다.

### 2026-09-01T17:00:20+09:00 commit (by nicehugepark)

커밋 8190e16 — 화면은 세션이 붙은 그 계정을 말한다 — 전환 잔결함 셋의 근원 수정 (REQ-20260901-017-62x6)

## History
- 2026-09-01T16:07:09+09:00 created by nicehugepark (status: open)
- 2026-09-01T16:08:18+09:00 status: open -> in-progress (by nicehugepark) — 리드 1차 실측 후 deep-diver 규명 — 흔적: lineage-*.json·restart-try-*.json·credentials mtime·이 세션 바인딩
- 2026-09-01T17:02:54+09:00 status: in-progress -> review (by nicehugepark) — 전환하실 때 화면이 거짓을 보이던 뿌리를 고쳤습니다. F5 후 계정을 바꿔 보세요 — 이제 오른쪽 위 계정·사용량이 세션이 실제로 붙은 계정을 말하고, 한도가 다 찬 계정으로는 바꾸기 전에 막으며 사유를 알려줍니다. 참고로 지금 터미널 창은 12:45에 뜬 옛 창이라 「창이 옛 코드」 표시가 뜰 수 있습니다 — 그 창을 닫고 s9 code 로 새로 여시면 전부 새 코드로 돕니다. 계정 표시가 실제와 맞으면 승인, 여전히 어긋나면 캡처와 함께 반려해 주세요.
