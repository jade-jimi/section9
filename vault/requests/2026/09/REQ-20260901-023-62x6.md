---
id: REQ-20260901-023-62x6
type: request
title: 화면에서 pull·push
summary: 대시보드에서 손으로 git pull·push 를 할 수 있게 한다. 지금은 터미널을 열어 명령을 쳐야 하고, 화면은 이 저장소가 GitHub 과 얼마나 벌어졌는지도 말하지 않는다. REQ-20260901-022 의 설정 화면과 같은 자리에 붙인다
goal: 대시보드에서 원격과의 차이(앞선 것·뒤진 것)를 읽고, 손으로 받기·올리기를 실행해 그 결과를 화면에서 확인할 수 있다 — 실패하면 왜 못 했는지가 화면에 선다
status: in-progress
size: M
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: a05fe599
sessions: ["8a0d83ad", "a05fe599"]
agents: ["lead:claude-opus-5", "sub:designer:a77ad793", "sub:ux-writer:a0112950", "sub:tech-writer:adc8fb1c", "sub:tech-writer", "sub:subagent:ae151b7b", "sub:ux-writer", "sub:subagent:a2b040b1", "sub:designer", "sub:frontend-developer:a880931a", "sub:subagent:a5eb6ce4", "sub:frontend-developer", "sub:subagent:a5fc5c7b", "sub:subagent:a3d385f1"]
contributions: [{"actor": "lead:claude-opus-5", "item": "리드가 먼저 못박는 제약 (설계·구현이 이 위에서 움직인다)", "started": "2026-09-01T19:46:29+09:00", "ended": "2026-09-01T19:46:29+09:00", "result": "done", "transcript": ""}, {"actor": "sub:designer:a77ad793", "item": "023 받고 올리기 화면 설계", "started": "2026-09-01T19:47:03+09:00", "ended": "2026-09-02T00:17:29+09:00", "result": "failed", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/8a0d83ad-568f-4f69-9585-f816dfe479f7/tasks/a77ad79388defe4c1.output", "reason": "기록 자리가 사라졌다 (transcript 경로가 없다)"}, {"actor": "sub:ux-writer:a0112950", "item": "023 받고 올리기 문구", "started": "2026-09-01T19:47:27+09:00", "ended": "2026-09-01T19:55:14+09:00", "result": "done", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/8a0d83ad-568f-4f69-9585-f816dfe479f7/tasks/a01129504b9b9421f.output"}, {"actor": "sub:tech-writer:adc8fb1c", "item": "023 낱말 판정과 번역", "started": "2026-09-01T19:47:47+09:00", "ended": "2026-09-01T19:54:33+09:00", "result": "done", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/8a0d83ad-568f-4f69-9585-f816dfe479f7/tasks/adc8fb1c633e8c262.output"}, {"actor": "lead:claude-opus-5", "item": "`[2026-09-01 19:48:12 KST - lead]`", "started": "2026-09-01T19:48:12+09:00", "ended": "2026-09-01T19:48:12+09:00", "result": "done", "transcript": ""}, {"actor": "sub:tech-writer", "item": "tech-writer 용어 판정 — 화면에서 받고 올리기 (023)", "started": "2026-09-01T19:53:55+09:00", "ended": "2026-09-01T19:53:55+09:00", "result": "done", "transcript": ""}, {"actor": "sub:subagent:ae151b7b", "item": "Appending verdict note to REQ-20260901-023-62x6", "started": "2026-09-01T19:53:59+09:00", "ended": "2026-09-01T19:53:59+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/8a0d83ad-568f-4f69-9585-f816dfe479f7/subagents/agent-ae151b7b3e3b79792.jsonl"}, {"actor": "sub:tech-writer:adc8fb1c", "item": "`[2026-09-01 19:47:49 KST - tech-writer]`", "started": "2026-09-01T19:54:33+09:00", "ended": "2026-09-01T19:54:33+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/8a0d83ad-568f-4f69-9585-f816dfe479f7/subagents/agent-adc8fb1c633e8c262.jsonl"}, {"actor": "sub:ux-writer", "item": "받고 올리기 화면 문구 (ux-writer · REQ-20260901-023-62x6)", "started": "2026-09-01T19:54:36+09:00", "ended": "2026-09-01T19:54:36+09:00", "result": "done", "transcript": ""}, {"actor": "sub:subagent:a2b040b1", "item": "Appending copy spec to REQ-20260901-023-62x6", "started": "2026-09-01T19:54:40+09:00", "ended": "2026-09-01T19:54:40+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/8a0d83ad-568f-4f69-9585-f816dfe479f7/subagents/agent-a2b040b15cef28ef6.jsonl"}, {"actor": "sub:ux-writer:a0112950", "item": "`[2026-09-01 19:47:30 KST - ux-writer]`", "started": "2026-09-01T19:55:14+09:00", "ended": "2026-09-01T19:55:14+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/8a0d83ad-568f-4f69-9585-f816dfe479f7/subagents/agent-a01129504b9b9421f.jsonl"}, {"actor": "lead:claude-opus-5", "item": "리드 판정 — tech-writer 판정 수용, 리드 문구 하나 정정", "started": "2026-09-01T19:55:28+09:00", "ended": "2026-09-01T19:55:28+09:00", "result": "done", "transcript": ""}, {"actor": "lead:claude-opus-5", "item": "리드 판정 — 누를 자격 (ux-writer 가 물은 제약 7)", "started": "2026-09-01T19:56:18+09:00", "ended": "2026-09-01T19:56:18+09:00", "result": "done", "transcript": ""}, {"actor": "lead:claude-opus-5", "item": "`[2026-09-01 19:56:38 KST - lead]`", "started": "2026-09-01T19:56:39+09:00", "ended": "2026-09-01T19:56:39+09:00", "result": "done", "transcript": ""}, {"actor": "sub:designer", "item": "023 받고 올리기 화면 설계", "started": "2026-09-01T19:59:16+09:00", "ended": "2026-09-01T19:59:16+09:00", "result": "done", "transcript": ""}, {"actor": "sub:designer:a77ad793", "item": "`[2026-09-01 19:47:05 KST - designer]`", "started": "2026-09-01T20:00:05+09:00", "ended": "2026-09-01T20:00:05+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/8a0d83ad-568f-4f69-9585-f816dfe479f7/subagents/agent-a77ad79388defe4c1.jsonl"}, {"actor": "lead:claude-opus-5", "item": "리드 판정 4 — designer 착지, 023 설계 확정", "started": "2026-09-01T20:04:01+09:00", "ended": "2026-09-01T20:04:01+09:00", "result": "done", "transcript": ""}, {"actor": "lead:claude-opus-5", "item": "`[2026-09-01 20:04:24 KST - lead]`", "started": "2026-09-01T20:04:25+09:00", "ended": "2026-09-01T20:04:25+09:00", "result": "done", "transcript": ""}, {"actor": "lead:claude-opus-5", "item": "`[2026-09-01 20:57:55 KST - lead]`", "started": "2026-09-01T20:58:03+09:00", "ended": "2026-09-01T20:58:03+09:00", "result": "done", "transcript": ""}, {"actor": "lead:claude-opus-5", "item": "`[2026-09-01 21:04:30 KST - lead]`", "started": "2026-09-01T21:04:33+09:00", "ended": "2026-09-01T21:04:33+09:00", "result": "done", "transcript": ""}, {"actor": "lead:claude-opus-5", "item": "`[2026-09-01 21:05:17 KST - lead]`", "started": "2026-09-01T21:05:19+09:00", "ended": "2026-09-01T21:05:19+09:00", "result": "done", "transcript": ""}, {"actor": "lead:claude-opus-5", "item": "`[2026-09-01 21:08:56 KST - lead]`", "started": "2026-09-01T21:08:56+09:00", "ended": "2026-09-01T21:08:56+09:00", "result": "done", "transcript": ""}, {"actor": "lead:claude-opus-5", "item": "`[2026-09-01 21:16:48 KST - lead]`", "started": "2026-09-01T21:16:49+09:00", "ended": "2026-09-01T21:16:49+09:00", "result": "done", "transcript": ""}, {"actor": "lead:claude-opus-5", "item": "`[2026-09-01 21:17:00 KST - lead]`", "started": "2026-09-01T21:17:00+09:00", "ended": "2026-09-01T21:17:00+09:00", "result": "done", "transcript": ""}, {"actor": "lead:claude-opus-5", "item": "사용자 반려 — 낱말 판정을 뒤집는다 (2026-09-02, REQ-20260902-002-62x6 로 별건 착수)", "started": "2026-09-02T07:03:39+09:00", "ended": "2026-09-02T07:03:39+09:00", "result": "done", "transcript": ""}, {"actor": "sub:frontend-developer:a880931a", "item": "REQ-20260901-023 pull·push 화면 구현", "started": "2026-09-02T10:50:36+09:00", "ended": "2026-09-02T10:59:26+09:00", "result": "stalled", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/a05fe599-cf31-4fd7-8ef2-000a77ff9011/tasks/a880931a9e7d73867.output", "reason": "198초째 진전 없음 (기준 180초)"}, {"actor": "sub:subagent:a5eb6ce4", "item": "Reading REQ-20260901-023-62x6 spec notes", "started": "2026-09-02T10:51:07+09:00", "ended": "2026-09-02T10:51:07+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/a05fe599-cf31-4fd7-8ef2-000a77ff9011/subagents/agent-a5eb6ce44182e6539.jsonl"}, {"actor": "sub:frontend-developer", "item": "TDD 시나리오 — 화면에서 pull·push (frontend-developer)", "started": "2026-09-02T10:55:23+09:00", "ended": "2026-09-02T10:55:23+09:00", "result": "done", "transcript": ""}, {"actor": "sub:subagent:a5fc5c7b", "item": "Writing TDD scenarios to REQ-20260901-023-62x6", "started": "2026-09-02T10:55:24+09:00", "ended": "2026-09-02T10:55:24+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/a05fe599-cf31-4fd7-8ef2-000a77ff9011/subagents/agent-a5fc5c7b58182a812.jsonl"}, {"actor": "sub:subagent:a3d385f1", "item": "Updating TDD checkboxes in REQ-20260901-023-62x6", "started": "2026-09-02T11:56:10+09:00", "ended": "2026-09-02T11:56:10+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/a05fe599-cf31-4fd7-8ef2-000a77ff9011/subagents/agent-a3d385f180a30723e.jsonl"}, {"actor": "sub:frontend-developer", "item": "TDD 시나리오 (개정 — 전부 통과)", "started": "2026-09-02T11:56:24+09:00", "ended": "2026-09-02T11:56:24+09:00", "result": "done", "transcript": ""}, {"actor": "sub:frontend-developer", "item": "구현 — 화면에서 pull·push (frontend-developer)", "started": "2026-09-02T11:57:16+09:00", "ended": "2026-09-02T11:57:16+09:00", "result": "done", "transcript": ""}, {"actor": "sub:frontend-developer:a880931a", "item": "`[2026-09-02 11:57:22 KST - frontend-developer]`", "started": "2026-09-02T12:00:08+09:00", "ended": "2026-09-02T12:00:08+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/a05fe599-cf31-4fd7-8ef2-000a77ff9011/subagents/agent-a880931a9e7d73867.jsonl"}]
project: section9
parent: REQ-20260901-022-62x6
relates: ["REQ-20260902-002-62x6"]
relates_why: {"REQ-20260902-002-62x6": "023 제목·판정표가 이 지적의 대상 — 그 화면의 낱말이 여기서 정해진다"}
tags: ["auto-audit", "sync", "attached", "png", "image", "workflow", "design", "terminal"]
created: 2026-09-01T19:44:23+09:00
status_since: 2026-09-02T10:49:53+09:00
updated: 2026-09-02T12:00:08+09:00
priority: 50
---

## Original

깃헙 풀 푸시를 수동으로 할 수 있는 기능도 같이 붙으면 좋겠는데

## Notes

### 2026-09-01T19:46:29+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

### 리드가 먼저 못박는 제약 (설계·구현이 이 위에서 움직인다)

「손으로 받고 올린다」는 화면 문제이기 전에 **안전 문제**다. 이 저장소에는
사람이 안 보는 자리에서 도는 작업자가 있고, 작업 트리가 늘 깨끗하지도 않다.
아래는 협상 대상이 아니다.

1. **화면이 커밋하지 않는다.** 올리는 것은 **이미 커밋된 것**뿐이다. 화면이
   `add`·`commit` 을 대신하면, 누구도 무엇을 올렸는지 모르는 커밋이 생긴다 —
   이 저장소가 이미 겪은 사고 계열이다(남의 미커밋 편집을 삼킴).
2. **도는 작업자가 있으면 올리지 않는다.** 거부하고 **왜**를 말한다. 지금
   무엇이 도는지는 서버가 이미 안다(대시보드가 「도는 일 N건」을 그린다).
3. **받기(pull)는 작업 트리가 깨끗할 때만.** 더러우면 거부하고 무엇이 걸리는지
   파일 이름으로 말한다. 치워 두었다 되돌리는 명령을 화면이 대신 쓰지 마라 —
   그것이 이 저장소가 남의 작업을 잃은 그 명령이고, bin/s9-git-gate 가 막는
   이유다.
4. **갈라진 역사는 화면이 합치지 않는다.** 앞선 것과 뒤진 것이 둘 다 0이
   아니면 거부하고 사람에게 넘긴다. 화면이 merge/rebase 를 고르면 안 된다.
5. **올리기는 바깥으로 나가는 일이다** — 확인 창을 지난다. 받기는 되돌릴 수
   있으니 확인 없이.
6. **누르기 전에 상태가 보인다.** 원격과 얼마나 벌어졌는지(앞선 것 N · 뒤진 것
   N), 작업 트리가 깨끗한지가 단추 옆에 이미 서 있어야 한다 — 눌러 봐야
   아는 화면은 만들지 않는다.
7. 권한: 본인 계정의 일이 아니라 **이 저장소의 일**이다. 누가 눌러도 되는지를
   설계가 답해야 한다(admin 만인가, 등록 사용자 전부인가).

### 자리

REQ-20260901-022-62x6 가 세운 Settings 좌측 목록의 새 구역들과 같은 판이다.
「무인 작업」 구역에 넣을 것인지, 「시스템」인지, 새 구역인지는 designer 판정.
022 의 결정(행 부품·무게 세 겹·즉시 저장·색면 금지)을 그대로 물려받는다.

### 곁가지 — 문지기의 오탐 하나

이 노트를 처음 쓸 때 bin/s9-git-gate 가 거부했다. 노트 **본문에** 그 명령
이름이 적혀 있었을 뿐인데, 문지기가 명령줄 전체를 훑어 글까지 명령으로 읽었다.
막아야 할 것을 막는 문은 옳지만, 그 문이 글을 막으면 사람은 문을 우회하는
버릇을 들인다. 따로 볼 일이다.

### 2026-09-01T19:48:12+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-09-01 19:48:12 KST - lead]`

접수했습니다 — `REQ-20260901-023-62x6` 「화면에서 받고 올리기」, 022의 자식으로 세우고 착수했습니다.

**먼저 제가 제약을 못박았습니다.** 이건 화면 문제이기 전에 안전 문제라서요. 이 저장소엔 사람이 안 보는 자리에서 도는 작업자가 있고 작업 트리가 늘 깨끗하지도 않습니다.

- **화면이 커밋하지 않습니다** — 올리는 건 이미 커밋된 것뿐입니다. 화면이 `add`/`commit`을 대신하면 누구도 무엇을 올렸는지 모르는 커밋이 생깁니다.
- **도는 작업자가 있으면 올리지 않습니다** — 거부하고 왜인지 말합니다.
- **받기는 작업 트리가 깨끗할 때만** — 더러우면 걸리는 파일 이름을 말하고 멈춥니다. 치워 뒀다 되돌리는 명령을 화면이 대신 쓰지 않습니다(그게 이 저장소가 작업을 잃은 그 명령입니다).
- **역사가 갈라지면 화면이 합치지 않습니다** — 사람에게 넘깁니다. merge냐 rebase냐를 화면이 고르면 안 됩니다.
- **올리기는 확인 창을 지나고, 받기는 안 지납니다** — 되돌릴 수 있느냐가 기준입니다.
- **누르기 전에 상태가 보입니다** — 앞선 것 N·뒤진 것 N·미커밋 파일 수가 단추 옆에 이미 서 있어야 합니다. 눌러 봐야 아는 화면은 안 만듭니다.

designer·ux-writer·tech-writer 셋을 세웠습니다(이번엔 낱말 수가 적고 022에서 어휘 기준이 이미 서서 tech-writer가 최종 낱말까지 정합니다 — translator를 따로 세우지 않았습니다). **구현은 022 프론트가 착지한 뒤에 붙입니다** — 같은 파일을 두 에이전트가 동시에 고치면 한쪽이 삼켜집니다.

---

**전달 못 된 메시지 하나** (18:02, 다른 세션 수신함에 갇혀 있었습니다): "'창이 옛 코드'는 무슨 기능이지"

지금 답해도 유효한 질문이라 답합니다. 그건 **경고 표시**입니다. 세션을 띄운 터미널 창이 그때의 코드·설정을 그대로 들고 있어서, 대시보드에서 「다시 시작」을 눌러도 그 창이 만드는 인자가 옛것이라 고친 게 안 실리는 상태를 말합니다. 실제 사고가 있었습니다 — 12:45에 뜬 창이 `--model fable`을 얼려 두고 있어서, 화면에서 모델을 바꿔도 계속 fable로 떴습니다.

판정 근거는 둘이고 정확한 쪽이 먼저입니다: 창이 기동할 때 남긴 지문과 지금 디스크의 파일을 견주거나(파일 단위로 압니다), 지문이 없는 옛 창이면 프로세스가 뜬 시각과 파일 수정 시각을 견줍니다. 근거가 없으면 아무 말도 하지 않습니다.

**푸는 법**은 그 표시의 손 위 글이 말하는 그대로입니다 — 그 터미널 창을 닫고 `cd ~/section9 && bin/s9 code`로 새로 여시면 됩니다.

### 2026-09-01T19:53:55+09:00 response (agent: sub:tech-writer, by nicehugepark)

tech-writer 용어 판정 — 화면에서 받고 올리기 (023)

### 결론 한 줄

**이름은 이름처럼, 행위는 행위처럼** — 이 저장소가 `done`/`in-progress` 를 두고 이미 세운 규칙(web/app/card.js:1663-1670)이 이 화면의 전부를 가른다. `main`·`origin`·짧은 해시는 **이름**이라 로마자 그대로 서고, `pull`·`push` 는 사람이 누르는 **행위**라 한국어 동사가 된다. 상태(ahead/behind/diverged/dirty)는 낱말을 얻지 않고 **셈과 결과**로 말한다.

리드의 물음 — "push·commit·branch 는 worktree 와 다르다, 사용자가 GitHub 웹사이트·다른 도구에서 다시 만난다. 그 사실이 판정을 바꾸나?"

**바꾼다. 단 명사만 바꾸고 동사는 안 바꾼다.** 다시 만나는 것이 판정을 바꾸는 이유는 *대조*가 필요해서다 — 화면의 `main` 과 GitHub 가지 목록의 `main` 이 같은 글자여야 사람이 둘을 잇는다. 그 대조는 **식별자**에만 필요하다. 반면 단추가 약속하는 것은 대조가 아니라 결과이고, 결과는 내 말로 읽혀야 한다. 이 저장소는 이 둘을 이미 갈라 놨다: 상태 이름은 mono 로 `done` 그대로, 그 상태로 옮기는 행위는 문장 속 「승인」·「반려」(card.js:1670 "이름은 이름처럼, 행위는 행위처럼"). 023 은 새 규칙이 아니라 그 규칙의 적용이다.

### 022 와의 정합 — 셋 다 그대로 적용된다

1. **GitHub 은 로마자** (022 B-1). 재실측: 화면 문자열에 「깃헙」 0건, `GitHub` 은 userform.js:37-38 · notice.js:32 · workercfg.js:47~67 에 산다. 023 도 로마자. (이 REQ 본문의 「깃헙」은 요청서의 말이지 화면의 말이 아니다.)
2. **git 내부 개념은 결과로 말한다** (022 B-2, REQ-20260830-001-62x6, DOC-20260830-006-62x6 개정①). worktree 를 내린 그 칼이 여기서는 working tree·remote·merge/rebase 를 벤다.
3. **키/명령 이름은 곁들임** (022 A). 다만 023 의 곁들임은 **쉴 때가 아니라 막힐 때** 선다 — 아래 D.

### A. 판정표

| 낱말 | 판정 | 근거 한 줄 |
|---|---|---|
| `pull` | **옮김 — 「받기」** | 단추가 약속하는 것은 이름의 대조가 아니라 나에게 일어날 결과다(card.js:1670). |
| `push` | **옮김 — 「올리기」** | 같은 이유. 그리고 「푸시」는 못 쓴다 — 아래 B. |
| `commit` | **곁들임(행위로만) / 셈은 안 세움** | 화면이 커밋하지 않으므로(리드 제약 1) 이 낱말이 참인 자리는 "사람이 터미널에서 할 일" 하나뿐 — WS_FIX_COMMIT(card.js:1011)이 이미 그 자리에서 「커밋하면…」을 쓴다. 셈의 단위로는 안 세운다: 「커밋 3개 앞섬」은 숫자 하나 읽자고 개념 하나를 배우게 한다. |
| `branch` (부류 이름) | **화면에 안 세움** | 이 판에 가지가 하나뿐이라 부류 이름이 할 일이 없다. 「가지」는 시금석 ④(묶는 이름을 은유로) — 기각. 「브랜치」는 사람이 여기서 고를 일이 없는 개념 — 세우면 없는 선택지를 광고한다(s9-design 1절). |
| `main` | **그대로 (mono)** | 이름이다. GitHub 가지 목록·CLI 출력에 같은 글자로 박혀 있고, 이름은 번역하지 않는다(card.js:1669). 화면 표기는 「GitHub 의 `main`」. |
| `origin` / `remote` | **화면에 안 세움 (이름이 둘 이상일 때만 그대로)** | 화면 문자열에 「원격」 0건, `origin` 0건(guide.html 뿐). 이 판에서 원격의 사용자쪽 이름은 이미 **GitHub** 이고, 022 B-1 이 그것을 확정했다 — 여기에 「원격」을 얹으면 한 사물에 두 번째 이름이 생긴다. 022 B-3(`gh` 를 홑낱말로 안 세움)과 같은 꼴. 다만 원격이 둘인 설치(guide.html:589 upstream+origin)에서는 이름이 실제로 갈리므로 그때는 `origin`·`upstream` 을 **번역하지 말고 이름 그대로** 세운다. |
| `ahead` / `behind` | **옮김 — 「올릴 것 N개」/「받을 것 N개」** | 「앞선 것/뒤진 것」은 기각(리드 문구지만 반려한다): 대비로만 뜻이 서는 한 쌍이라 어느 쪽이 나인지를 매번 되짚어야 한다 — 「본 저장소↔워크트리」가 "둘의 차이를 잘 모르겠네"로 반려된 그 구조(card.js:990). 「올릴 것/받을 것」은 그 셈이 **어느 단추를 살리는지**를 바로 말하고, 단추의 낱말과 같은 말이라 배울 것이 없다. |
| `working tree dirty` / `uncommitted` | **옮김 — 「고치던 파일 N개」 + 파일 이름** | 「작업 트리」는 worktree 와 같은 부류의 내부 자리 이름 — 안 세운다(022 B-2·C-4: 「작업 자리」·「사본」은 이미 기각어). 「고치던 것」은 **022 가 같은 판에 이미 세운 말**(workercfg.js:72 "지금 이 저장소에 고치던 것이 있으면") — 그 말에서 목적어만 붙였다. |
| `merge` / `rebase` | **곁들임(명령 글자로만)** | 화면이 고르지 않는 일이니(리드 제약 4) 개념 낱말로는 안 세운다. 대신 막힌 사람을 터미널로 보내야 하므로 그 자리에서 `git pull --rebase` 를 mono 로 한 번 — 022 의 키 곁들임과 같은 형식(userform.js:47 timezone 이 표준). |
| `diverged` | **화면에 안 세움 — 두 셈이 동시에 0이 아닌 것으로 말한다** | 이 상태에 이름을 지어 주는 순간 사람은 이름을 배우고도 무엇을 할지 모른다. 「올릴 것 2개 · 받을 것 3개」가 곧 그 상태이고, 그 두 줄이 이미 서 있다(리드 제약 6). 낱말 0개로 끝나는 자리다. |
| `fetch` | **화면에 안 세움 — 신선도로만 보인다** | 사람이 청하는 일이 아니라 화면이 셈을 얻는 방법이다. 보여야 하는 것은 행위가 아니라 **언제 확인한 셈인가**: 「방금 확인」/「3분 전 확인」(s9-design 6절 사람 기준 시각) + 단추 「다시 확인」. |
| `repository` | **옮김 — 「저장소」(이미 확정)** | settings.js:170 「저장소 ~/section9」, userform.js 전역. 새로 정할 것 없음. |

### B. 「푸시」를 쓸 수 없는 이유 — 022 의 잔재 둘을 고쳐야 한다

`web/app/workercfg.js:51`·`:61` 이 지금 화면에 **「푸시는 그중 하나입니다」**를 세우고 있다. 023 이 같은 판에 「올리기」를 세우면 **한 화면 한 개념 두 이름**이 되고, 그것이 022 C 절이 막으려던 바로 그 결함이다. 둘 중 하나로 통일해야 하는데, 이기는 쪽은 「올리기」다.

- 근거 ① (주): push 는 이 화면에서 **사람이 누르는 행위**다 — 이름이 아니라 행위이므로 한국어 동사가 규칙이다(card.js:1670).
- 근거 ② (보조): 「푸시」는 화면에 이미 다른 뜻으로 한 번 서 있다 — terminal.js:57 「수신 = SSE 푸시 tail」(서버가 밀어 주는 그 푸시). 그 줄 자체가 기계어 캡션이라 강한 근거는 아니지만, 같은 글자가 두 뜻을 지고 있다는 사실은 남는다.
- 근거 ③: 022 자신의 결이 그렇다. 같은 항목의 다른 칸은 전부 한국어 동사다(「권한 주기」·「파일 직접 고치기」·「따로 떼어 놓고 일하기」). 「푸시」만 음차로 남았다.

→ 고칠 문장(코드는 안 만졌다, 리드·frontend 몫): `mean`·`ask.desc` 의 "푸시는 그중 하나입니다" → **"저장소에 올리는 것도 그중 하나입니다"**. 023 의 단추와 같은 말이 된다.

### C. 버린 후보와 버린 이유

| 버린 것 | 왜 |
|---|---|
| 「앞선 것/뒤진 것」 | 대비쌍 — 어느 쪽이 나인지 매번 되짚어야 한다(card.js:990 의 실패 재연). 리드 문구지만 반려한다. |
| 「가지」(branch) | 은유로 지은 묶는 이름 — 시금석 ④. |
| 「본선」(main) | 조어에 가깝고, 무엇보다 `main` 은 이름이라 번역 대상이 아니다. |
| 「원격」 | 화면 문자열 0건이고, 그 사물의 화면 이름은 이미 GitHub 이다(022 B-1). |
| 「가져오기/내보내기」 | 파일 import/export 의 말이라 저장소끼리 오가는 일에 잘못 걸린다. |
| 「내려받기」 | 뜻은 맞지만 「올리기」와 짝이 안 맞고(내려받기↔올려주기), 받기가 받아 오는 것은 파일이 아니라 기록이다. |
| 「동기화」 | **settings.js:170 이 이미 그 말을 서버가 알아서 하는 일에 쓴다**(「로컬 md + git 동기화」). 사람이 손으로 누르는 일에 같은 말을 얹으면 "그럼 지금까지 저절로 되던 것 아니었나"가 된다. |
| 「병합/재배치」 | merge/rebase 의 직역 — 화면이 고르지 않는 일에 이름을 주는 꼴. |
| 「역사가 갈라졌습니다」 | 「역사」는 git 의 말이지 사람의 말이 아니다. 두 셈이 이미 그 사실이다. |
| 「아직 저장하지 않은 파일」 | 거짓이다 — 사람은 편집기에서 저장을 했다. (022 workercfg.js:70 의 「아직 저장하지 않은 남의 편집」은 남의 편집을 밖에서 부르는 자리라 넘어가지만, 내 파일을 나에게 그렇게 부르면 안 된다.) |
| 「커밋 N개」 | 숫자 하나 읽자고 개념 하나를 배우게 한다. 셈은 「올릴 것/받을 것」으로. |

### D. 곁들임을 어디에 두나 — 022 와 다른 점 하나

022 의 곁들임은 **쉴 때** 선다(터미널에서 그 키를 다시 쳐야 하니까). 023 의 곁들임은 **막힐 때만** 선다. 이 화면의 존재 이유가 "터미널을 안 열게 하는 것"이라, 잘 되는 자리에 `git push` 를 곁들이면 화면이 제 이유를 부정한다. 반대로 화면이 거절하는 자리(갈라짐·고치던 파일·도는 일)는 사람이 반드시 터미널로 가야 하는 자리이므로, 거기서는 칠 글자를 mono 로 정확히 준다. 형식은 userform.js:47(timezone)의 `.path` 급 그대로.

### E. 표면별 적용 지점 (문장 초안 — 최종 다듬음은 ux-writer 몫)

자리는 designer 판정(022 가 세운 Settings 좌측 목록의 어느 구역인지). 구역/덩이 이름은 **「GitHub 과 주고받기」** 를 권한다(「동기화」 금지 — C 표).

- 쉴 때 · 같음: `GitHub 의 main 과 같습니다 · 방금 확인` + 「다시 확인」
- 올릴 것만: `올릴 것 3개 · 받을 것 없음` → 단추 **「올리기」**(확인 창을 지난다 — 제약 5)
- 받을 것만: `받을 것 2개` → 단추 **「받기」**(확인 없이 — 제약 5)
- 둘 다(diverged): `올릴 것 2개 · 받을 것 3개 — 이 화면에서는 맞출 수 없습니다. 터미널에서 ` + mono `git pull --rebase` + ` 로 맞춘 뒤 다시 오세요.`
- 고치던 파일(pull 거절): `고치던 파일이 3개 있어 받지 않았습니다 — web/app/card.js, bin/s9 …`
- 도는 일(push 거절): `지금 도는 일이 2건 있어 올리지 않았습니다` (「도는 일」= DOC-20260831-005-62x6 규칙 7 확정어. 「자동 작업」·「워커」 금지)
- 올릴 것 목록을 편다면 한 줄 = `커밋 제목 + 짧은 해시(mono)` — 해시는 이름이라 그대로. GitHub 화면과 눈으로 맞출 수 있는 유일한 글자다.
- 확인 창(push): ok **「올리기」** / cancel **「그만두기」**(022 확정어)

### F. 구현이 실측할 것 하나 — 「받기」 vs 「다시 받기」

이 화면 사전에 **「다시 받기」가 이미 있다** — 못 받은 데이터를 다시 부르는 단추(boot.js:134, docs.js:420, session.js:222). 뜻은 다르지만 결이 가깝다("저기 것을 여기로"). 판정은 **「받기」 유지**다: 기존 셋은 전부 「다시」를 달고 있고 목적어를 이름으로 밝힌다(「계정 목록 다시 받기」), 023 의 「받기」는 「올리기」와 한 쌍으로 GitHub 덩이 안에 선다 — 홀로 서지 않으면 갈릴 자리가 없다. 다만 **「받기」가 GitHub 덩이 밖에 홀로 서는 순간 이 판정은 깨진다**. 캡처로 확인할 것.

또 하나: 「올리기」는 project.js:156-158 의 「owner 로 올려 주세요」(승격)와 글자가 겹친다. 그쪽은 문장 속 서술어이고 이쪽은 단추 낱말이라 갈릴 자리가 없다고 본다 — 같은 판에 서지도 않는다.

### 2026-09-01T19:53:59+09:00 subagent (agent: sub:subagent:ae151b7b, by nicehugepark)

Appending verdict note to REQ-20260901-023-62x6

### 2026-09-01T19:54:33+09:00 subagent (agent: sub:tech-writer:adc8fb1c, by nicehugepark)

`[2026-09-01 19:47:49 KST - tech-writer]`

판정을 `REQ-20260901-023-62x6` 에 `--label response --agent tech-writer` 로 남겼다(6,408자). 요지는 다음과 같다.

### 가른 축 — 이 저장소가 이미 세운 규칙 하나

리드의 물음("push·commit·branch 는 worktree 와 다르다 — 사용자가 GitHub 웹사이트에서 다시 만난다, 그 사실이 판정을 바꾸는가")에 대한 답: **바꾼다, 단 명사만 바꾸고 동사는 안 바꾼다.**

다시 만나는 것이 판정을 바꾸는 이유는 *대조* 때문이다 — 화면의 `main` 과 GitHub 가지 목록의 `main` 이 같은 글자여야 사람이 둘을 잇는다. 그 대조는 **식별자**에만 필요하다. 단추가 약속하는 것은 대조가 아니라 결과다. 이 저장소는 이 둘을 이미 갈라 뒀다 — `web/app/card.js:1663-1670`: "done 은 … 커밋 메시지에 같은 글자로 박혀 있는 **이름**이고, 이름은 번역하지 않는다 … **이름은 이름처럼, 행위는 행위처럼**". 상태 이름은 mono `done` 그대로, 그 상태로 옮기는 행위는 「승인」·「반려」. 023 은 새 규칙이 아니라 그 규칙의 적용이다.

### 판정

| 낱말 | 판정 | 근거 |
|---|---|---|
| pull / push | **옮김 — 「받기」/「올리기」** | 사람이 누르는 행위다 |
| commit | **곁들임(행위로만)** · 셈으로는 **안 세움** | 화면이 커밋 안 하니 참인 자리는 "터미널에서 할 일" 뿐(`card.js:1011` WS_FIX_COMMIT 이 이미 그 자리) |
| branch(부류) | **안 세움** | 가지가 하나뿐 — 없는 선택지를 광고하는 꼴 |
| main | **그대로 (mono)** | 이름이다 |
| origin / remote | **안 세움** (원격 둘이면 이름 그대로) | 화면 문자열 「원격」·`origin` 0건, 그 사물의 화면 이름은 이미 GitHub(022 B-1) — 022 B-3 `gh` 와 같은 꼴 |
| ahead / behind | **옮김 — 「올릴 것 N개」/「받을 것 N개」** | 리드 문구 「앞선 것/뒤진 것」은 **반려** — 대비로만 뜻이 서는 쌍이라 「본 저장소↔워크트리」가 "둘의 차이를 잘 모르겠네"로 반려된 그 구조(`card.js:990`) |
| dirty / uncommitted | **옮김 — 「고치던 파일 N개」+파일 이름** | 022 가 같은 판에 이미 쓴 말(`workercfg.js:72` "고치던 것") |
| merge / rebase | **곁들임(명령 글자로만)** | 화면이 안 고르니 개념어로는 안 세우되, 막힌 사람에게 `git pull --rebase` 를 mono 로 |
| diverged | **안 세움 — 두 셈이 곧 그 상태** | 「올릴 것 2개 · 받을 것 3개」가 이미 그 사실. 낱말 0개로 끝난다 |
| fetch | **안 세움 — 신선도로만** | 「방금 확인」/「3분 전 확인」 + 「다시 확인」 |

### 022 와 어긋나는 자리 하나 — 고쳐야 한다 (코드는 안 만졌다)

`web/app/workercfg.js:51`·`:61` 이 지금 화면에 **「푸시는 그중 하나입니다」**를 세우고 있다. 023 이 같은 판에 「올리기」를 세우면 한 화면에 한 개념 두 이름 — 022 C절이 막으려던 그 결함이다. **「올리기」가 이긴다**: ① push 는 여기서 행위다 ② 「푸시」는 `terminal.js:57` 「SSE 푸시」로 이미 다른 뜻을 한 번 지고 있다 ③ 022 자신의 다른 칸은 전부 한국어 동사다(「권한 주기」·「파일 직접 고치기」). → `mean`·`ask.desc` 의 "푸시는 그중 하나입니다" → **"저장소에 올리는 것도 그중 하나입니다"**.

### 버린 후보 (일부)

「동기화」 — `settings.js:170` 이 이미 **서버가 알아서 하는 일**에 쓴다(「로컬 md + git 동기화」). 사람이 누르는 일에 얹으면 "그럼 지금까지 저절로 되던 것 아니었나"가 된다. 「가지」·「본선」(은유/이름 번역), 「원격」(화면 0건), 「가져오기/내보내기」(파일 import 의 말), 「내려받기」(짝이 안 맞음), 「병합/재배치」, 「역사가 갈라졌습니다」(「역사」는 git 의 말), 「아직 저장하지 않은 파일」(거짓 — 사람은 편집기에서 저장했다), 「커밋 N개」.

### 곁들임의 자리가 022 와 다르다

022 의 곁들임은 **쉴 때** 선다(그 키를 터미널에서 다시 쳐야 하니까). 023 의 곁들임은 **막힐 때만** 선다 — 이 화면의 존재 이유가 "터미널을 안 열게 하는 것"이라 잘 되는 자리에 `git push` 를 곁들이면 화면이 제 이유를 부정한다. 형식은 `userform.js:47`(timezone)의 `.path` 급 그대로.

### 구현이 실측할 것

**「받기」 vs 「다시 받기」** — 이 화면 사전에 「다시 받기」가 이미 있다(`boot.js:134`, `docs.js:420`, `session.js:222`). 판정은 「받기」 유지다: 기존 셋은 전부 「다시」를 달고 목적어를 밝히며, 023 의 「받기」는 「올리기」와 한 쌍으로 GitHub 덩이 안에 선다. **「받기」가 그 덩이 밖에 홀로 서는 순간 이 판정은 깨진다** — 캡처로 확인할 것.

문서: `/home/sjpark1/section9/vault/requests/2026/09/REQ-20260901-023-62x6.md`

### 2026-09-01T19:54:36+09:00 response (agent: sub:ux-writer, by nicehugepark)

### 받고 올리기 화면 문구 (ux-writer · REQ-20260901-023-62x6)

### 결론 먼저 — 판정 셋

리드가 판정하라고 준 세 낱말은 **셋 다 이 화면에 못 쓴다.**

1. **「최신」 기각.** 이 화면 문자열에 0건이고(있는 건 `최신순` 하나), 무엇보다
   **거짓이 되기 쉬운 말**이다. 화면이 아는 것은 "마지막으로 확인한 그때 같았다"
   뿐인데 「최신입니다」는 지금 이 순간을 보증한다. 참인 문장으로 바꾼다 —
   **「이 컴퓨터와 GitHub 이 같습니다」** + 언제 확인했는지.
2. **「동기화」 기각.** 화면에 두 번 있으나 둘 다 시스템 제 동작을 적은 `.path`
   급 각주다(`settings.js:170` "로컬 md + git 동기화", `userform.js:135`) —
   사람이 누르는 행동의 이름으로 선 적이 없다. 게다가 **이 화면이 절대 안 하는
   일을 약속하는 낱말**이다: 동기화는 양쪽을 알아서 맞춘다는 뜻인데, 리드 제약 4가
   "갈라진 역사는 화면이 합치지 않는다"이다. 이름이 거부 조건과 정면으로 어긋난다.
3. **「커밋을 푸시」 반만 기각.** 「커밋」은 **남긴다** — 이미 화면 낱말이다
   (`card.js:1026` "이 요청이 고칠 파일이 아직 커밋되지 않았습니다",
   `WS_FIX_COMMIT` "커밋하면 …"). 「푸시」는 **문장에서만** 산다(`workercfg.js:51`
   "푸시는 그중 하나입니다" — 022 가 방금 세운 이웃 절). **단추 이름으로는 못 선다** —
   단추는 「올리기」, 기계 이름 `push` 는 022 규칙대로 `.path` 급 곁들임으로 내린다.

그리고 **「원격」은 한 번도 쓰지 않는다.** 깃을 모르는 사람에게 뜻이 없고, 가리키는
실물에 이미 이름이 있다 — `GitHub`. 「원격」은 mono 곁들임 `origin/main` 안에만 산다.

### 자리 1 — 절 머리

022 의 문법 그대로: 절 머리가 주어를 한 번 세우고, 아래 행들이 물려받는다.

| 자리 | 문장 | 언제 서나 |
|---|---|---|
| 절 이름 | **GitHub** | 늘 |
| 절 뜻 (`.desc` 급) | 이 컴퓨터의 저장소와 GitHub 사이에서 커밋을 손으로 받고 올립니다. | 늘 |
| 절 뜻 둘째 줄 | 올라가는 것은 이미 커밋된 것뿐입니다 — 커밋은 이 화면이 대신하지 않습니다. | 늘 |

둘째 줄이 리드 제약 1 을 **먼저** 말한다. 나중에 거절문으로 처음 알리면 사람은
"왜 안 해 주지"로 읽고, 미리 말하면 "그렇구나"로 읽는다. 부정형 데뷔를 피해
긍정 사실(올라가는 것은 커밋된 것)이 앞에 서고 제약이 줄표 뒤에 붙는다.

### 자리 2 — 상태 줄 (누르기 전에 이미 서 있다)

두 줄이다. **윗줄은 사람 문장, 아랫줄은 mono 곁들임.**

| 값 | 윗줄 (본문체·존댓말) | 언제 서나 |
|---|---|---|
| 앞선 0 · 뒤진 0 · 미커밋 0 | **이 컴퓨터와 GitHub 이 같습니다 — 올릴 것도 받을 것도 없습니다.** | 가장 흔한 화면. 기본 |
| 앞선 3 · 뒤진 0 | **올릴 커밋 3개가 있습니다.** | 올리기가 살아 있을 때 |
| 앞선 0 · 뒤진 2 | **받을 커밋 2개가 있습니다.** | 받기가 살아 있을 때 |
| 앞선 3 · 뒤진 2 | **올릴 커밋 3개와 받을 커밋 2개가 서로 다른 갈래에 쌓였습니다.** | 거부 ③ (아래) |
| 미커밋 4 (앞의 어느 줄에든 덧붙는 둘째 문장) | **아직 커밋하지 않은 파일이 4개 있습니다.** | 미커밋 > 0 일 때만 |
| 아랫줄 (`.path` 급 mono) | `main → origin/main · 방금 확인` / `· 3분 전 확인` | 늘 |

- **숫자에 이름을 붙였다.** 「앞선 것 3」이 아니라 **「올릴 커밋 3개」** 다 — 무엇의
  앞이냐를 되묻게 하지 않고, 바로 아래 단추(「올리기」)와 같은 낱말로 이어진다.
  뒤진 것도 같다: **「받을 커밋 2개」**.
- **셋 다 0일 때 숫자를 세우지 않는다.** `0 · 0 · 0` 은 "없음"이 아니라 "못 읽어옴"
  으로 읽힌다. 그 자리는 문장이 진다.
- **미커밋 0 이면 그 문장은 안 선다.** 카드 사실 줄 규칙 그대로 —
  「정상이 아니다」/「당신이 할 일이 있다」일 때만 줄이 선다(REQ-20260830-040-62x6).
- 「방금 / N분 전」은 `project.js:86-88` 의 확정 낱말이다. 새로 짓지 않았다.

### 자리 3 — 손잡이 둘

| 자리 | 낱말 | 언제 서나 |
|---|---|---|
| 받기 단추 | **받기** | 늘 (못 할 때는 잠긴 얼굴) |
| 올리기 단추 | **올리기** | 늘 (못 할 때는 잠긴 얼굴) |
| 두 단추 곁들임 (`.path` 급 mono, 단추 아래 한 번) | `pull` · `push` | 늘 |

- 이 저장소의 단추는 짧은 동사다(「이어가기」·「깨우기」·「권한 주기」·「그만두기」).
  목적어는 바로 위 상태 줄이 이미 말했다 — 「커밋 받기」로 늘리면 겹말이다.
- **`pull`·`push` 는 로마자.** 「푸시」 한글이 화면에 4건 있으나 전부 **결과를 설명하는
  산문** 안이고(`workercfg.js:51·61`, `guide.html:613` "푸시 실패"), 컨트롤 이름이 아니다.
  「풀」은 화면 0건이고 국어에서 다른 뜻으로 먼저 읽힌다. 짝을 이루는 컨트롤에 한쪽만
  한글을 쓸 수는 없으니 **둘 다 로마자**로 간다 — `GitHub` 을 로마자로 둔 022 판정과 같은 결.
  → **tech-writer 확인 요망**(한글 「푸시」와 로마자 `push` 가 한 화면 안에 공존해도 되는가).

### 자리 4 — 올리기 확인 창

`s9dlg({kind:"confirm", stop:true, …})`. 022 의 gh 창과 같은 부품·같은 결이다.

```
머리(cap)   올리기
제목        커밋 3개를 GitHub 에 올릴까요?
본문        이 컴퓨터의 main 을 GitHub 의 origin/main 으로 올립니다.
            올라간 커밋은 다른 사람도 받아 가므로, 여기서는 되돌리지 못합니다.
목록        (descHtml 안 `.path` 급 mono, 최신 3개까지)
            a1b2c3d  전환 표시 개편·계정 창 키
            8190e16  화면은 세션이 붙은 그 계정을 말한다
            4a67e07  한도로 굳은 턴은 「일하는 중」이 아니다
            + 2개 더 보기
단추        [ 올리기 ]   [ 그만두기 ]
```

- **제목이 개수를, 본문이 어느 갈래에서 어디로를 진다.** 리드가 요구한 "무엇이
  나가는지" 셋이 창 안에 다 있다.
- **겁주지 않는 법 = 형용사를 빼고 결과만 적는 것.** 「주의」·「위험」·「영구히」를
  쓰지 않았다. "다른 사람도 받아 갑니다"는 사실이고, 그 사실이 이미 무겁다.
  (022 확정: "무게는 문장이 진다. 붉은색·느낌표·경고 아이콘을 쓰지 않는다")
- **커밋 목록은 있으면 좋고 없으면 그만이다** — 「무엇이 나가는지」를 개수가 아니라
  글자로 보여 주는 유일한 자리라 권한다. 「+ N개 더 보기」는 이 화면의 확정 문법.
- **받기는 창이 없다.** 리드 제약 5. 대신 결과 한 줄이 무엇이 바뀌었는지 말한다.
- `ok` 가 단추 이름(「올리기」)을 그대로 되풀이하는 것은 의도다 — 022 의
  「권한 주기」가 같은 손버릇이고, 「확인」은 무엇을 확인하는지 말하지 않는다.

### 자리 5 — 못 하는 넷

**규율 셋을 먼저 못박는다.**

① **넷 다 고장이 아니다.** 팝업이 필요하면 `kind:"alert"` + **`stop:false`**.
   이 저장소는 이미 그 문을 만들어 뒀다 — `dialog.js:185-189`: "설명하는 알림만
   `stop:false` 로 물러선다. 깨우기의 `busy`·`capped`·`moving` 은 오류가 아니라 설명인데
   한 창짜리 알림이면 무엇이든 빨강이 됐다." 눈썹 잉크는 막힌 일에만.
② **누르기 전에 선다.** 리드 제약 6 — 넷 다 단추 **옆**에 서는 것이 본자리고,
   팝업은 렌더와 클릭 사이에 상태가 바뀐 경우의 뒷문일 뿐이다.
③ **문장은 한 벌이다.** 그 두 자리에 **같은 글자**가 선다. 022 가 세운 규율
   ("문장을 두 벌 짓지 않는다")의 재적용.

| # | 때 | 문장 | 어느 단추가 잠기나 |
|---|---|---|---|
| ① | 무인 작업이 돌고 있다 | **지금 도는 일 2건이 있습니다 — 올라갈 것이 아직 바뀌고 있어, 끝나면 올릴 수 있습니다.** | 올리기 |
| ② | 아직 커밋하지 않은 파일이 있다 | **아직 커밋하지 않은 파일이 3개 있습니다 — 받으면 덮일 수 있어서, 커밋하면 받을 수 있습니다.**<br>(아래 `.path` 급 mono 파일 이름 3개 + `+ N개 더 보기`) | 받기 |
| ③ | 갈래가 갈렸다 | **올릴 커밋 3개와 받을 커밋 2개가 서로 다른 갈래에 쌓였습니다 — 어느 쪽을 먼저 둘지는 사람이 정할 일이라, 화면이 대신 합치지 않습니다.** | 받기·올리기 둘 다 |
| ④ | 올릴 것이 없다 / 받을 것이 없다 | **문장을 새로 짓지 않는다.** 상태 줄이 이미 말했다 — 단추만 잠긴 얼굴로 선다. | 해당 단추 |

**하나씩의 근거**

- ① **「도는 일 2건」은 지어낸 말이 아니다** — 헤더 칩이 쓰는 그 글자다
  (`notice.js:396` `` `도는 일 ${jobs.length}건 · ${mx}분째` ``). 같은 숫자를 두
  이름으로 부르지 않으니, 사람이 고개를 들면 위에서 같은 「2건」을 본다.
  "올라갈 것이 아직 바뀌고 있어"가 **왜**의 전부다 — 무인 작업이 커밋을 더 얹으면
  방금 창에서 본 개수가 거짓이 된다. 「위험」·「충돌」 같은 겁주는 말은 없다.
- ② **파일 이름을 말한다**(리드 제약 3). 개수는 상태 줄이, 이름은 여기가 진다.
  「덮다」는 이 저장소의 낱말이다(`workercfg.js` "보이지도 덮이지도 않게").
  "커밋하면 받을 수 있습니다"는 `WS_FIX_COMMIT`("커밋하면 다음 작업부터 …")과 같은
  문형이다 — 할 일이 있는 자리에만 할 일을 적는다.
  **치워 두었다 되돌리는 방법은 화면에 한 글자도 안 적는다.** 안 쓴다는 말조차
  없는 개념을 데뷔시키는 일이고, 데뷔시키면 사람은 그 길을 찾아 나선다.
- ③ **「갈래」는 이미 화면에 있다**(`docs.js:398` "여기서 갈래가 바뀐다").
  「병합」·「머지」·「리베이스」는 한 글자도 안 쓴다 — 화면이 고르지 않는 것을
  이름으로 불러 주면 "화면이 안다"는 뜻이 된다. 사람이 무엇을 해야 하는지는
  **「합친 뒤에 다시 오시면 올릴 수 있습니다」** 한 줄을 둘째 문장으로 붙여도 좋다
  (권함 — 셋 중 유일하게 사람이 자리를 뜨는 거부다).
- ④ **여기서 문장을 지으면 상태 줄과 두 벌이 된다.** "올릴 것이 없습니다"는
  「이 컴퓨터와 GitHub 이 같습니다 — 올릴 것도 받을 것도 없습니다」의 반쪽이다.
  경주(클릭 순간 값이 달라짐)로 굳이 팝업이 필요하면 **상태 줄 그 문장을 그대로**
  띄운다(`stop:false`).

### 자리 6 — 도는 중

| 자리 | 문장 | 언제 서나 |
|---|---|---|
| 받기 단추 (잠김) | **받는 중…** | 누른 뒤 |
| 올리기 단추 (잠김) | **올리는 중…** | 창에서 「올리기」를 누른 뒤 |
| 경과 (단추 옆 `.path` 급) | `4초째` → `1분째` | 3초 넘어갈 때부터 |

- `…는 중…` 은 이 화면의 잠긴 얼굴 문법이다(`card.js:12` `WAKE_GOING = "이어가는 중…"`).
- **경과를 붙이는 이유**: 정적 문구는 멈춘 것으로 읽힌다(s9-design 3절).
  「N분째」는 `format.js:63` 의 확정 형식이다 — 새로 짓지 않았다.
- ⚠ **겹침 하나**: `dialog.js:244` 가 첨부 파일 올림에 이미 「올리는 중…」을 쓴다.
  다른 화면(문서 첨부 칩)이라 실사용에서 마주치지 않지만, 낱말 사전에 적어 둔다.

### 자리 7 — 끝난 뒤 (`secmsg` 자리 인라인 · 팝업 금지 — 022 확정)

| 때 | 문장 |
|---|---|
| 받았다 | **커밋 2개를 받았습니다 — 파일 7개가 바뀌었습니다.** |
| 받았는데 화면 파일이 바뀌었다 | 위 문장 + **화면을 새로 고치면 바뀐 것이 보입니다.** (바뀐 것에 `web/` 가 있을 때만) |
| 받을 것이 없었다 | **이 컴퓨터와 GitHub 이 같습니다 — 받을 것이 없었습니다.** |
| 올렸다 | **커밋 3개를 GitHub 에 올렸습니다.** |
| GitHub 이 받지 않았다 | **✕ GitHub 이 받지 않았습니다 — <서버가 말한 까닭>. 올라간 것은 없습니다.** (`stop:true`) |
| 서버에 못 닿았다 | **✕ 서버에 닿지 못했습니다 — 잠시 뒤 다시 눌러 주세요. 서버가 재기동 중일 수 있습니다.** (`dialog.js` 기존 문장 재사용) |
| 누를 자격이 없다 | **등록한 계정만 받고 올릴 수 있습니다 — Settings 의 내 계정에서 이름을 고르세요.** → **리드 판정 대기**(제약 7: admin 만인가 등록 사용자 전부인가). admin 한정이면 022 의 확정문을 그대로 쓴다: `@<이름> 본인이나 admin 만 …` |

- **아무것도 안 바뀐 경우가 실패가 아니다.** 「받을 것이 없었습니다」는 상태 줄과
  같은 문장 가족이고 ✕ 를 달지 않는다.
- **「올라간 것은 없습니다」가 실패문의 핵심이다.** 반쯤 나갔는지가 사람이 가장
  먼저 묻는 것인데, 까닭만 적으면 그 질문에 답이 없다.
- 「새로 고치다」는 화면에 아직 없는 말이라 **tech-writer 확인 요망**
  (대안: 「F5 를 누르면」 — PROTOCOL 이 확인 포인트에서 쓰는 말).

### before / after

| 지금 | 바꾼 뒤 |
|---|---|
| 화면이 원격과의 차이를 말하지 않는다. 터미널을 열어 명령을 쳐야 안다 | `이 컴퓨터와 GitHub 이 같습니다 — 올릴 것도 받을 것도 없습니다` / `main → origin/main · 방금 확인` |
| (없던 화면) 흔한 대안: 「최신입니다 ✓」 | 참인 문장 + 확인한 시각. 무엇이 같은지, 언제 기준인지를 말한다 |
| (없던 화면) 흔한 대안: 붉은 「Push failed: repository is busy」 | `지금 도는 일 2건이 있습니다 — 올라갈 것이 아직 바뀌고 있어, 끝나면 올릴 수 있습니다` (설명·`stop:false`, 누르기 전에 이미 서 있다) |
| (없던 화면) 흔한 대안: 「변경사항이 있어 pull 할 수 없습니다」 | `아직 커밋하지 않은 파일이 3개 있습니다 — 받으면 덮일 수 있어서, 커밋하면 받을 수 있습니다` + 파일 이름 |

### 용어 사전 갱신분

- **신설** 「받기」(`pull`) · 「올리기」(`push`) · 「올릴 커밋 N개」(ahead) ·
  「받을 커밋 N개」(behind) · 「아직 커밋하지 않은 파일 N개」(dirty)
- **재사용(새로 짓지 않음)** 「커밋」(`card.js:1026`) · 「저장소」(`settings.js:170`) ·
  「갈래」(`docs.js:398`) · 「덮다」(`workercfg.js`) · 「도는 일 N건」(`notice.js:396`) ·
  「무인 작업」(DOC-20260831-005-62x6) · `GitHub`(`userform.js:30`) ·
  「그만두기」(`dialog.js:209`) · 「방금·N분 전」(`project.js:86-88`) ·
  「N분째」(`format.js:63`) · 「…는 중…」(`card.js:12`)
- **금지** 「최신」 · 「동기화」 · 「원격」 · 「깃헙」 · 「풀」 · 「병합·머지·리베이스」 ·
  「변경사항」(이 화면은 「고친 것」·「바뀐 것」을 쓴다) · 붉은 실패 옷을 입은 거부 넷

### tech-writer · translator 로 넘길 것

1. **`pull`·`push` 로마자 vs 「푸시」 한글** — 산문 안 한글 4건과 컨트롤 곁들임
   로마자가 한 화면에 공존한다. 내 판단은 로마자지만 최종 낱말은 tech-writer 몫.
2. **「이 컴퓨터」 vs 「이 저장소」** — 상태 줄의 주어. `workercfg.js` 는 "지금 이
   저장소에", "이 컴퓨터에 로그인된" 을 둘 다 쓴다. GitHub 도 저장소를 갖고 있어
   「이 저장소와 GitHub 이 같습니다」가 살짝 겉돈다는 것이 「이 컴퓨터」를 고른 이유.
3. **「새로 고치다」 vs 「F5 를 누르면」** — 화면에 아직 없는 말.
4. **절 이름 「GitHub」** — 022 의 절 이름은 전부 한국어다(「무인 작업」·「새 창」·
   「대화 기록」). 고유명 하나만 로마자로 서도 되는가. 대안 「GitHub 받고 올리기」.

### 한계

- 배치·단추 자리·잠긴 얼굴의 생김새는 designer 몫이다. 이 문서는 낱말과 문장만 정한다.
- 상태 줄이 "언제 기준"인지는 화면이 실제로 GitHub 을 다시 물어보는가에 달려 있다.
  다시 묻는 손잡이가 따로 있으면 아랫줄이 `· 방금 확인`으로 갱신되고, 없으면
  「확인」이 무엇을 뜻하는지가 흐려진다 — **designer·backend 판정 필요**.
- 거부 ①의 「도는 일 2건」이 헤더 칩과 같은 숫자원(`jobs`)을 쓰는지 확인해야 한다.
  다른 원을 쓰면 한 화면이 같은 것을 두 숫자로 말한다.

### 2026-09-01T19:54:40+09:00 subagent (agent: sub:subagent:a2b040b1, by nicehugepark)

Appending copy spec to REQ-20260901-023-62x6

### 2026-09-01T19:55:14+09:00 subagent (agent: sub:ux-writer:a0112950, by nicehugepark)

`[2026-09-01 19:47:30 KST - ux-writer]`

문구를 확정해 `REQ-20260901-023-62x6` 의 response 노트에 남겼다(8882자). 요지다.

### 리드가 판정하라 한 세 낱말 — 셋 다 기각

- **「최신」** — 화면 문자열 0건(있는 건 `최신순` 하나)이고, 무엇보다 거짓이 되기 쉽다. 화면이 아는 것은 "마지막으로 확인한 그때 같았다"뿐인데 「최신입니다」는 지금 이 순간을 보증한다. → **「이 컴퓨터와 GitHub 이 같습니다」** + 확인한 시각.
- **「동기화」** — 화면에 2건 있으나 둘 다 시스템 제 동작을 적은 `.path` 급 각주다(`settings.js:170`, `userform.js:135`). 사람이 누르는 행동의 이름으로 선 적이 없고, **이 화면이 절대 안 하는 일을 약속한다** — 리드 제약 4(갈라진 역사는 합치지 않는다)와 이름이 정면으로 어긋난다.
- **「커밋을 푸시」** — 「커밋」은 남긴다(이미 화면 낱말: `card.js:1026` "아직 커밋되지 않았습니다"). 「푸시」는 산문에서만 산다(`workercfg.js:51` — 022 가 방금 세운 이웃 절). 단추는 **「올리기」**, `push` 는 022 규칙대로 `.path` 급 곁들임.

덤으로 **「원격」을 한 글자도 안 쓴다** — 가리키는 실물에 이미 이름이 있다(`GitHub`). 「원격」은 mono 곁들임 `origin/main` 안에만.

### 핵심 문장들

| 자리 | 문장 |
|---|---|
| 셋 다 0 (가장 흔한 화면) | **이 컴퓨터와 GitHub 이 같습니다 — 올릴 것도 받을 것도 없습니다.** / `main → origin/main · 방금 확인` |
| 숫자 | **올릴 커밋 3개** · **받을 커밋 2개** · **아직 커밋하지 않은 파일 4개** — 「앞선 것 3」이 아니라 단추와 같은 낱말로 |
| 단추 | **받기** / **올리기** (`pull` · `push` 곁들임) |
| 확인 창 | 제목 `커밋 3개를 GitHub 에 올릴까요?` · 본문 `이 컴퓨터의 main 을 GitHub 의 origin/main 으로 올립니다. 올라간 커밋은 다른 사람도 받아 가므로, 여기서는 되돌리지 못합니다.` · 단추 `[올리기] [그만두기]` |

**셋 다 0일 때 숫자를 세우지 않는다** — `0 · 0 · 0` 은 "없음"이 아니라 "못 읽어옴"으로 읽힌다. 그 자리는 문장이 진다.

### 못 하는 넷 — 붉지 않게

이 저장소가 이미 문을 만들어 뒀다: `dialog.js:185-189` 의 **`stop:false`** ("설명하는 알림만 물러선다 — 깨우기의 `busy`·`capped`·`moving` 은 오류가 아니라 설명인데 한 창짜리 알림이면 무엇이든 빨강이 됐다"). 넷 다 그 문으로 간다. 그리고 리드 제약 6 대로 **본자리는 팝업이 아니라 단추 옆**이고, 두 자리에 **같은 글자**가 선다(022 의 "문장을 두 벌 짓지 않는다").

- ① `지금 도는 일 2건이 있습니다 — 올라갈 것이 아직 바뀌고 있어, 끝나면 올릴 수 있습니다.` (「도는 일 N건」은 헤더 칩의 그 글자 — `notice.js:396`)
- ② `아직 커밋하지 않은 파일이 3개 있습니다 — 받으면 덮일 수 있어서, 커밋하면 받을 수 있습니다.` + 파일 이름. **치워 두었다 되돌리는 방법은 한 글자도 안 적는다** — 안 쓴다는 말조차 없는 개념을 데뷔시킨다.
- ③ `올릴 커밋 3개와 받을 커밋 2개가 서로 다른 갈래에 쌓였습니다 — 어느 쪽을 먼저 둘지는 사람이 정할 일이라, 화면이 대신 합치지 않습니다.` (「갈래」는 `docs.js:398` 에 이미 있다. 「병합·머지·리베이스」는 안 쓴다 — 이름을 불러 주면 "화면이 안다"는 뜻이 된다)
- ④ **문장을 새로 짓지 않는다.** 상태 줄이 이미 말했다 — 단추만 잠긴 얼굴로.

### 022 와의 정합

`web/app/workercfg.js`(022 구현 중)를 읽어 실제 확정어를 맞췄다 — 「무인 작업」·`GitHub` 로마자·「그만두기」·「덮다」·「고치다」·`s9dlg` 확인 창 형식·secmsg 인라인(팝업 금지). 새로 지은 낱말은 「받기」·「올리기」와 세 숫자 이름뿐이고, 나머지 11개는 전부 기존 화면 문자열에서 그대로 가져왔다(근거 파일·행 번호를 노트에 달았다).

### 넘길 것

- **tech-writer**: `pull`·`push` 로마자 vs 한글 「푸시」(산문 4건과 공존) · 「이 컴퓨터」 vs 「이 저장소」 · 「새로 고치다」 vs 「F5」 · 절 이름을 로마자 `GitHub` 로 둬도 되는가
- **리드**: 누를 자격(제약 7) — 등록 사용자 전부인가 admin 만인가. 문장 두 벌을 노트에 준비해 뒀다
- **designer/backend**: 상태 줄의 "확인" 시각이 갱신되려면 GitHub 을 다시 묻는 손잡이가 필요한가 · 거부 ①의 숫자가 헤더 칩과 같은 원(`jobs`)을 쓰는지

코드는 손대지 않았다. 산출물 원본: `/tmp/claude-1000/-home-sjpark1-section9/8a0d83ad-568f-4f69-9585-f816dfe479f7/scratchpad/023-copy.md` (동일 내용이 REQ 노트에 영속)

### 2026-09-01T19:55:28+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

### 리드 판정 — tech-writer 판정 수용, 리드 문구 하나 정정

tech-writer 가 **리드가 쓴 낱말을 반려했다. 받아들인다.**

「앞선 것 / 뒤진 것」 — 내가 제약 노트에 쓴 말이다. 반려 근거가 옳다: 이 쌍은
**대비로만 뜻이 서서** 둘을 나란히 놓기 전에는 어느 쪽이 무엇인지 모른다.
이 저장소가 이미 그 구조로 반려당한 적이 있다(`card.js:990` — 「본 저장소 ↔
워크트리」가 "둘의 차이를 잘 모르겠네"로). 대신 **「올릴 것 N개」/「받을 것
N개」** — 단추의 낱말(올리기·받기)과 같은 어간을 쓰므로 셈과 손잡이가 한 몸이
된다.

덤으로 `diverged` 가 낱말 없이 사라졌다: 「올릴 것 2개 · 받을 것 3개」가 이미
갈라진 역사 그 자체다. 이 화면은 그 상태를 이름 부르지 않고 **셈으로** 말한다.

### 수용한 판정 (그대로 간다)

- pull/push → 「받기」/「올리기」 · dirty → 「고치던 파일 N개」 + 파일 이름
- `main` 은 mono 그대로(이름이다) · branch·origin·remote·fetch·diverged 는
  화면에 안 세움 · merge/rebase 는 막힌 사람에게 명령 글자로만
- 가른 축이 이 저장소의 기존 규칙이라는 것도 맞다 —
  「이름은 이름처럼, 행위는 행위처럼」(`card.js:1663-1670`). 023 은 새 규칙이
  아니라 그 규칙의 적용이다.
- 곁들임의 자리가 022 와 다르다는 것도 수용: 022 의 키 이름은 **쉴 때** 서고,
  023 의 명령 글자는 **막힐 때만** 선다. 잘 되는 자리에 명령을 곁들이면 이
  화면이 제 존재 이유(터미널을 안 열게 하는 것)를 부정한다.

### 022 로 넘긴 것

`workercfg.js:51`·`:61` 의 「푸시는 그중 하나입니다」 → 「저장소에 올리는 것도
그중 하나입니다」. 022 프론트 에이전트가 그 파일을 쥐고 있어 **리드가 직접
고치지 않고 그 에이전트에 지시를 중계**했다(진행 중 작업을 마치고 반영하도록).

### 구현이 실측할 것 (tech-writer 인계)

「받기」가 「올리기」와 한 쌍으로 GitHub 덩이 **안**에 설 때만 이 판정이 선다 —
그 덩이 밖에 홀로 서면 화면에 이미 있는 「다시 받기」 셋(boot.js:134 ·
docs.js:420 · session.js:222)과 부딪힌다. 캡처로 확인할 것.

### 2026-09-01T19:56:18+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

### 리드 판정 — 누를 자격 (ux-writer 가 물은 제약 7)

**둘 다 admin 만.** 등록 사용자 전부가 아니다.

근거는 짐작이 아니라 방금 확인한 사실이다:

1. **이 저장소의 origin 은 PUBLIC 이다** — `gh repo view` 실측:
   `nicehugepark/section9` · `visibility: PUBLIC`. 즉 **올리기는 인터넷에
   공개하는 일**이다. bin/s9:252-257 이 이미 그 무게를 적어 뒀다: 비밀이
   어디 있는지를 담은 파일이 git 에 추적되고 있고 "다음 push 한 번이면
   공개된다".
2. **이건 계정의 일이 아니라 인스턴스의 일이다.** 022 의 스위치들은 「내
   계정에서 무엇이 일어나는가」라 본인이 바꾼다(`/api/user/config` 가 이미
   본인·admin 으로 가른 그 선). 받기·올리기는 **이 기계의 저장소 전체**를
   바꾼다 — 지금 그 위에서 도는 모든 세션과 작업자가 함께 영향을 받는다.
   내 것이 아닌 것을 내 판단으로 바꾸는 자리다.
3. 받기도 admin 인 이유: 받기는 되돌릴 수 있어서 확인 창을 안 지날 뿐,
   **도는 서버의 작업 트리를 갈아 끼우는 일**이다. 확인의 무게와 자격의
   무게는 다른 축이다.

admin 이 아닌 사람에게는 단추를 **숨기지 않는다** — 못 누르는 얼굴로 두고
이유를 말한다(이 저장소가 계정 창에서 이미 세운 규칙: 못 고르는 줄을 숨기지
않고 못 누르게 둔다). 문장은 ux-writer 가 준비해 둔 두 벌 중 자격 쪽을 쓰되,
`bin/s9:17699` 의 「본인 또는 admin만 설정 변경 가능」 같은 문장이 아닌 말로
쓴다.

### 두 사람이 갈린 자리 하나 — tech-writer 쪽

단추 옆 `pull`·`push` 곁들임: ux-writer 는 `.path` 급으로 늘 곁들이자 하고,
tech-writer 는 **막힐 때만** 세우자 한다. **tech-writer 쪽이다.** 근거가 더
구체적이다 — 이 화면의 존재 이유가 "터미널을 안 열게 하는 것"인데 잘 되는
자리에 명령 글자를 붙이면 화면이 제 이유를 부정한다. 022 의 키 이름 곁들임은
**그 키를 터미널에서 다시 쳐야 하기 때문에** 늘 서는 것이라 자가 다르다.

### 수용한 것

- 「최신」·「동기화」·「커밋을 푸시」 셋 다 기각 — 근거가 각각 실측이다.
  특히 「동기화」가 **이 화면이 절대 안 하는 일(갈라진 것 합치기)을 약속한다**는
  지적이 정확하다.
- **셋 다 0일 때 숫자를 세우지 않는다** — `0 · 0 · 0` 은 "없음"이 아니라
  "못 읽어옴"으로 읽힌다. 그 자리는 문장이 진다. 가장 흔한 화면이라 이 판단이
  이 요청에서 가장 값이 크다.
- 거부 넷은 전부 `stop:false`(설명하는 알림) — 고장이 아니라 설명이다.
- 「치워 두었다 되돌리는 방법」은 한 글자도 안 적는다 — 안 쓴다는 말조차
  없는 개념을 데뷔시킨다.

### 2026-09-01T19:56:39+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-09-01 19:56:38 KST - lead]`

023의 tech-writer와 ux-writer가 착지했습니다. 판정할 것이 셋 있었고 다 닫았습니다.

**제 문구가 반려당했고 받아들였습니다.** 제가 제약에 쓴 「앞선 것 / 뒤진 것」이 tech-writer에게 기각됐습니다 — 이 쌍은 둘을 나란히 놓기 전엔 어느 쪽이 무엇인지 모르는 말이고, 이 저장소가 정확히 그 구조로 반려당한 적이 있습니다(「본 저장소 ↔ 워크트리」가 "둘의 차이를 잘 모르겠네"로). 대신 **「올릴 커밋 3개」/「받을 커밋 2개」** — 단추 낱말과 같은 어간이라 셈과 손잡이가 한 몸이 됩니다. 덤으로 "역사가 갈라졌다"는 낱말 없이 사라졌습니다. 그 두 숫자가 이미 그 사실이니까요.

**누를 자격 → 둘 다 admin만입니다.** 짐작이 아니라 방금 확인했습니다 — 이 저장소의 origin은 **PUBLIC**입니다. 즉 올리기는 인터넷에 공개하는 일입니다. 그리고 022의 스위치들은 "내 계정에서 무엇이 일어나는가"라 본인이 바꾸지만, 받고 올리는 건 **이 기계의 저장소 전체**를 바꿔서 그 위에서 도는 모든 세션이 함께 영향을 받습니다. 내 것이 아닌 걸 내 판단으로 바꾸는 자리입니다. admin이 아닌 사람에게 단추를 숨기진 않고, 못 누르는 얼굴로 두고 이유를 말합니다.

**ux-writer의 가장 값진 판단** — 셋 다 0일 때(가장 흔한 화면) **숫자를 안 세웁니다.** `0 · 0 · 0`은 "없음"이 아니라 "못 읽어옴"으로 읽히니까요. 그 자리는 문장이 집니다: 「이 컴퓨터와 GitHub이 같습니다 — 올릴 것도 받을 것도 없습니다」.

「동기화」를 기각한 근거도 정확합니다 — 그 이름은 **이 화면이 절대 안 하는 일**(갈라진 것 합치기)을 약속합니다.

022 프론트가 쥐고 있는 파일에 문구 충돌이 하나 있어서(같은 개념이 「푸시」와 「올리기」 두 이름으로 설 뻔했습니다) 그 에이전트에 교정을 중계했습니다 — 하던 작업을 마치고 반영하도록.

designer만 남았습니다.

### 2026-09-01T19:59:16+09:00 response (agent: sub:designer, by nicehugepark)

### 화면에서 받고 올리기 — 구조·시각 설계 (designer)

REQ-20260901-022-62x6 가 세운 판(좌측 목록 구역 · `.metatbl`+`.uf`/`.acts` 행 부품 ·
무게 세 겹 · 즉시 저장 · 색면 금지)을 그대로 물려받는다. **새 CSS 컴포넌트는 한 줄**
(상태 문장), 나머지는 이미 화면에 서 있는 부품이다.

### 0. 지금 화면을 보고 잡은 사실 (캡처 첨부)

- `#settings/worker` 를 1531×684 로 뜬 결과 — 022 의 행 부품이 이미 서 있다:
  첫 칸 `한국어 이름 + mono 키 이름`, 둘째 칸 `손잡이 → 뜻 줄`. 좌측 목록은 넉 줄
  (디스플레이 / 내 계정 / 무인 작업 / 사용자 관리)까지만 보이고 「시스템」은 잘린다.
- `#settings/about`(시스템) 판의 **첫 줄이 `저장소  ~/section9 (로컬 md + git 동기화)`** 다.
  화면이 「동기화」를 말하면서 동기화하는 손잡이는 하나도 없다 — 이 요청이 메우는 구멍이
  그 줄 옆의 빈자리다.
- 확인 창은 1531×684 에서 머리·본문·바닥 세 띠로 서고 바닥이 안 잘린다(`?dlg=long` 캡처).
  본문이 구르는 띠라(`.dlgbox>.dlgbody{overflow:auto}`) 커밋 목록을 창에 넣어도 된다.
- **곁가지 결함 하나** — 좌측 목록은 창을 1500px 로 키워도 **같은 자리에서 잘린다**
  (`.doclist{max-height:var(--docsh,…)}` 가 Settings 에서 창 높이를 안 따른다). 지금도
  「시스템」이 안 보인다. 이 설계가 여섯째 줄을 더하므로 별건으로 다뤄야 한다 —
  이 REQ 가 고칠 것은 아니지만, 아래 1절의 「부제가 상태를 겸한다」는 그 결함이 남아
  있는 동안 절반만 참이다.

### 1. 자리 — 좌측 목록에 여섯째 줄 「저장소」, 「사용자 관리」와 「시스템」 사이

    디스플레이 / 내 계정 / 무인 작업 / 사용자 관리 / **저장소** / 시스템

판단의 축은 리드가 못박은 그대로다: **이건 계정의 설정이 아니라 저장소의 상태와 행위다.**
022 의 목록 순서 근거(나 → 나 → 남 → 전체)를 이어 쓰면 「저장소」는 **모두의 것** 자리,
즉 「사용자 관리」 뒤다. 「시스템」보다 앞인 이유는 하나다 — 시스템은 읽기만 하는 안내판이고
저장소는 손잡이가 있는 판이다. 자주 누르는 것이 앞이다.

**대안 비교**

- (A·채택) 새 구역 「저장소」. 좌측 목록 부제가 상태를 겸한다 — **판을 열지 않아도 거리가
  보인다**(022 의 `무인 작업 / 맡김 · GitHub 권한 켬` 이 세운 문법 그대로). goal 의 절반
  (「원격과의 차이를 읽고」)을 목록 층에서 이미 답한다. 구조 비용은 라우팅 한 줄.
- (B) 「시스템」 안의 덩이. **기각.** 저장소 행이 거기 있고 판이 짧다는 것은 맞지만,
  그 판은 통계·설계 문서 링크가 사는 **읽기 전용 안내판**이다. 안내를 보러 온 손이
  「바깥으로 나가는 단추」 옆을 지나게 된다. 부제도 상태를 못 겸한다 —
  「저장소·통계·문서 안내」에 숫자를 끼우면 축이 섞인다.
- (C) 「무인 작업」 안. **기각.** 그 판의 모든 값은 `s9 user config` 로 **내 계정에** 저장되는
  내 설정이다. 원격과의 거리는 계정에 저장되지 않고 누구에게나 같다. 한 판에 섞으면
  "이것도 내 설정인가"가 생기고, 022 가 애써 가른 두 축에 셋째 축을 몰래 얹는다.
- (D) 헤더·Board 에 상시 칩. **기각.** 며칠에 한 번 누르는 값을 늘 띄울 이유가 없고,
  헤더 2행 구조에 컨트롤을 더 우겨넣지 말라는 규칙이 이미 있다.

**부제** — 기본 `GitHub 과 주고받기`. 조용하지 않은 상태면 그것이 부제를 대신한다:
`올릴 것 3개` · `받을 것 2개 · 고치던 파일 2개`. 부제는 **원격에 묻지 않는다** —
마지막으로 물어본 값 기준이고, 한 번도 안 물어봤으면 기본 문장 그대로 선다(모르는 것을
아는 척하지 않는다).

**이름** — 「저장소」. 시스템 판이 이미 그 낱말로 이 개체를 부른다(같은 것에 두 이름 금지).
구역 이름은 개체의 이름이지 행위 이름이 아니라는 022 리드 판정을 따랐다. 최종은
tech-writer·translator 몫.

### 2. 상태 줄 — 한 줄, 0 인 마디는 지운다

**상태는 표 한 행이다** — 022 의 `.metatbl.wtbl` 행 그대로.

    ┌ 첫 칸(190px) ──┬ 둘째 칸 ────────────────────────────────────────┐
    │ 지금            │ 올릴 것 3개 · 고치던 파일 2개      [ 지금 확인 ] │
    │ main→origin/main│ 원격에는 5분 전에 물어봤습니다.                  │
    │                 │ 고치던 파일: web/app/settings.js · bin/s9        │
    └─────────────────┴─────────────────────────────────────────────────┘

- 첫 칸 = 이름 `지금` + 그 아래 mono `main → origin/main`(022 의 「이름 + 기계 글자」 자리).
- 둘째 칸 첫 줄 = **상태 문장** (본문 12.5px, 숫자만 `<b>`). 셋을 숫자 칩 셋으로 세우지
  않는 이유: 강조가 셋이면 강조가 없는 것과 같고, 칩은 곧 색면을 부른다.
- 둘째 줄(`.wsay > .wmean`, mono 11px `--muted`) = **값의 나이** + 고치던 파일 이름.
- 갈라졌을 때만 `.wfact`(`--c-blocked` 글자색, 면 없음) 한 줄이 더 선다. 이 판에서 **색이
  쓰이는 자리는 이 한 줄뿐이다**.

**얼굴 넷**

| 상태 | 상태 문장 |
|---|---|
| 0·0·깨끗 (가장 흔함) | `원격과 같습니다 — 올릴 것도 받을 것도 없습니다.` (`--muted`, 한 문장) |
| 올릴 것만 | `올릴 것 **3**개` (+ `· 고치던 파일 **2**개`) |
| 받을 것만 | `받을 것 **2**개` |
| 둘 다 (갈라짐) | `올릴 것 **3**개 · 받을 것 **2**개` + `.wfact` 「역사가 갈라졌습니다 — 합치는 일은 화면이 하지 않습니다.」 |

**가장 흔한 화면(셋 다 0)이 무엇을 말하나** — 조용한 한 문장 하나, 흐린 잉크, 아무것도
소리치지 않는다. ✓ 배지도, 초록 점도, 「정상」이라는 낱말도 없다. 022 의 사실 줄 규칙
(「정상이 아니다」 또는 「당신이 할 일이 있다」일 때만 줄이 선다)의 그대로의 재적용이다.
그 아래 손잡이 둘은 잠겨 있고 저마다 사유를 말한다 — 이 판의 평상시 얼굴은 **글 넉 줄**이다.

**언제 다시 재나 — 값이 두 벌이고 나이도 두 벌이다**

원격에 묻는 일에는 값이 든다(네트워크·인증). 그래서 **값싼 것과 비싼 것을 갈라 잰다**.

| 무엇 | 어떻게 | 언제 |
|---|---|---|
| 고치던 파일 · 올릴 것 | 이 컴퓨터 안에서만 (`--porcelain`, 커밋 세기) | **판이 보이는 동안 10초마다** + 열 때 |
| 받을 것 | **원격에 묻는다** | ① 사람이 「지금 확인」을 누를 때 ② 받기·올리기를 누를 때(먼저 물어본다) ③ 받기·올리기가 끝난 직후 |

- **주기적 자동 물어보기는 하지 않는다.** 대시보드는 열어 둔 채 며칠 가는 화면이라
  30초마다 물으면 하루 2,880번 원격을 두드린다. 저장소 거리는 Board 의 「지금 무엇이
  도는가」와 달리 **올리려고 마음먹었을 때** 알면 되는 값이다.
- 값싼 쪽만 10초로 돌리는 것은 live follow 규율(화면 가시 + 요소 존재 + 탭 이탈 시 정리)을
  그대로 따른다 — 다른 창에서 커밋하면 이 화면이 조용히 낡기 때문이다.
- **나이를 숨기지 않는다.** 「받을 것」 숫자 아래 늘 한 줄: `원격에는 5분 전에 물어봤습니다`
  / 60초 안이면 `방금 원격에 물어봤습니다` / 한 번도 안 물어봤으면
  `원격에는 아직 물어보지 않았습니다`. **「지금 확인」은 아무것도 바꾸지 않는 손잡이**라
  확인 창이 없다.
- 대안 (B) 열 때마다 무조건 원격에 묻기: **기각** — 설정을 보러 잠깐 들른 사람에게 매번
  몇 초를 물린다. (C) 물어본 적 없음을 0 으로 그리기: **기각** — 화면이 모르는 것을
  「없다」로 말하는 것은 이 저장소가 이미 한 번 반려한 거짓말이다(REQ-20260901-013-62x6).

### 3. 손잡이 둘 — 한 줄이 아니라 두 행, 나란히 서되 받기가 먼저

**둘은 각각 `.metatbl.wtbl` 한 행이다** — 상태 표와 같은 부품, 첫 칸에 이름과 **실제로
돌아가는 명령**(mono).

    ┌───────────────────┬──────────────────────────────────────────────┐
    │ 받기               │ [ 커밋 2개 받기 ]                            │
    │ (앞으로만 붙이는    │ 원격에 새로 올라온 것을 이 저장소로 가져옵니다. │
    │  방식 · mono)      │                                              │
    ├───────────────────┼──────────────────────────────────────────────┤
    │ 올리기             │ [ 커밋 3개 올리기 ]                          │
    │ (mono)             │ 이미 커밋한 것을 GitHub 으로 내보냅니다.       │
    └───────────────────┴──────────────────────────────────────────────┘

첫 칸의 mono 줄에 **화면이 실제로 돌리는 명령을 그대로 적는다.** 022 의 근거
(터미널에서 그 글자를 치던 사람이 화면에서 눈으로 찾을 수 있어야 한다)가 여기서는 한 겹
더 세다 — 그 줄이 곧 **안전 고지**다: 숨은 강제 옵션도, 치워 두었다 되돌리는 명령도,
역사를 합치는 선택도 거기 없다는 것을 사람이 눈으로 확인한다. 앞으로만 붙이는 방식으로
받는다는 것이 글자에 박혀 있어, 리드 제약 4(갈라진 역사는 화면이 합치지 않는다)가
문장이 아니라 **기계로** 지켜짐을 보여 준다.

**되돌릴 수 있는 것과 바깥으로 나가는 것의 무게 차이 — 색면 없이 셋으로**

1. **낱말과 숫자**: 라벨이 무엇이 몇 개 움직이는지 말한다(`커밋 3개 올리기`). 뜻 줄의
   동사가 방향을 말한다 — 받기는 「가져옵니다」, 올리기는 「내보냅니다」.
2. **마찰**: 올리기만 확인 창을 지난다(5절). 받기는 없다 — 되돌릴 수 있는 쪽에 마찰을
   물리지 않는다(022 의 gh 스위치가 세운 그 규칙: 켤 때만 창, 끌 때는 없다).
3. **순서**: 받기가 위, 올리기가 아래. **읽는 순서가 곧 안전한 순서**다(받고 나서 올린다).

**판 위에서 두 단추는 같은 옷을 입는다** — `.acts button.deed`(본문체 600, 1px 보더,
hover 반전). 대안 (B) 「올리기만 잉크 채움(`.dlgyes` 처럼)」은 **기각**이다: (a) 상시
반전은 이 화면에서 hover 의 얼굴이라 늘 손이 얹힌 것처럼 읽히고, (b) 상시 강조는 사흘이면
벽지가 되며, (c) **무엇이 주 행동인지는 상태가 정한다** — 받을 것이 있으면 받기가 주
행동이고 올릴 것만 있으면 올리기가 주 행동이다. 칠로 못박은 위계는 절반의 경우 틀린다.
이 판에서 **주 강조는 「지금 잠기지 않은 단추」 하나**이고, 잉크 채움은 확인 창 안에서만
쓴다(`.dlgyes` 는 이미 그 옷이다).

대안 (C) 둘을 멀리 갈라 두 덩이로: **기각** — 둘은 한 축의 두 방향이라 갈라 두면
「먼저 받고 올린다」는 순서가 안 읽힌다. 다만 ▶/⏸ 같은 **화살표 글리프는 쓰지 않는다** —
재생기 글리프는 「한 축의 두 방향, 하나만 참」을 약속하는데 받기와 올리기는 동시에 참일
수 있다(REQ-20260830-042-62x6 의 배타 규칙). **글리프 없는 낱말 단추 둘.** 부수 이득: 새 기하가
없으므로 125/150% 배율에서 갈릴 자리도 없다.

### 4. 못 하는 순간의 얼굴 — 잠그고, 그 옆에서 이유를 말한다. 둘 다 한다

**둘 중 하나가 아니다.** 이 저장소가 판정 창에서 이미 쓰는 답이 그것이다 —
`dialog.js` 의 `yes.disabled = …` 와 `.dlghint` 는 **함께** 선다: 못 누르게 하고, 왜 못
누르는지를 단추 옆에 글로 세운다.

- 눌리게 두고 실패 창을 띄우면 「눌러 봐야 아는 화면」이 된다 — 리드 제약 6 을 정면으로 어긴다.
- 잠그기만 하면 disabled 단추는 이유를 말하지 않아 「고장인가」가 된다.

**잠금은 `disabled` 가 아니라 `aria-disabled="true"`** 다 — REQ-20260831-009-62x6 가 깨우기
단추에서 세운 규율(`disabled` 는 포커스를 걷어 키보드 손을 body 로 떨어뜨린다). 과녁은
그대로 두고 손맛만 물러선다(`--muted` 잉크 + `--hairline` 보더, 색면 없음). 그래야 키보드
손이 잠긴 단추에 닿고, `aria-describedby` 로 묶인 사유 줄이 읽힌다.

**사유가 서는 자리 = 그 행의 뜻 줄 자리(`.wmean`).** 새 칸을 만들지 않는다. 즉 뜻 줄은
상태에 따라 얼굴을 바꾼다 — **열려 있으면 무엇을 하는지, 막혔으면 왜 못 하는지.** 색으로
가르지 않고 **문법으로** 가른다(`…합니다` ↔ `…해서 …할 수 없습니다`). 잠긴 단추의 잉크
농도가 그 위에 한 겹 더 얹힌다.

| 막는 것 | 어느 단추 | 사유 줄 (초안) |
|---|---|---|
| 도는 작업자 있음 | 올리기 (받기도) | `무인 작업 1건이 돌고 있습니다 — 끝난 뒤에 올릴 수 있습니다.` + `도는 일 보기` (`.more`, Board 로) |
| 작업 트리 더러움 | 받기 | `고치던 파일 2개가 있습니다 — 커밋하거나 되돌린 뒤에 받을 수 있습니다.` |
| 역사 갈라짐 | 둘 다 | `원격에도 여기에도 새 커밋이 있습니다 — 합치는 일은 화면이 하지 않습니다. 터미널에서 정리해 주세요.` |
| 올릴 것 없음 | 올리기 | `올릴 것이 없습니다 — 커밋한 것이 있어야 올립니다.` |
| 받을 것 없음 | 받기 | `받을 것이 없습니다 — 원격에 새로 올라온 것이 없습니다.` |
| 권한 없음 | 둘 다 | `저장소를 바꾸는 일은 admin 만 합니다.` |
| 남의 시점으로 보는 중 | 둘 다 | `지금 @user1 시점으로 보는 중입니다 — 내 시점으로 돌아가면 열립니다.` |
| 원격이 없음 | 둘 다 | `이 저장소에는 원격이 없습니다.` + 붙이는 명령 한 줄(미등록 계정 판이 `s9 user add` 를 적는 손버릇 그대로) |

사유는 **다음 행동을 준다** — 빈 상태 규율(안내가 아니라 다음 행동)의 재적용이다.
「고치던 파일」의 이름은 상태 줄이 이미 들고 있으므로(2절) 사유 줄은 **숫자만** 말한다 —
같은 목록을 두 번 적지 않는다.

**판정은 서버가 한다.** 화면이 네 조건을 다시 세면 규칙이 두 벌이 되어 언젠가 갈린다
(전이가 반드시 `do_transition` 한 문을 지나는 그 규율). 화면은 서버가 준
`can.pull{ok,why}` / `can.push{ok,why}` 를 **그대로** 그린다 — 「서버의 문장이 곧 팝업이다」
(REQ-20260901-018-62x6)의 연장. 그리고 누른 순간 서버가 **다시 판정한다**: 화면이 「없다」고
보여 준 사이에 작업자가 뜰 수 있다. 그 재판정은 「눌러 봐야 아는 화면」이 아니라 **경합의
마지막 문**이다.

### 5. 도는 중 · 끝난 뒤

**누르기 직전 — 올리기만 확인 창** (`s9dlg`, 네이티브 confirm 금지)

    {kind:"confirm", stop:true, cap:"올리기",
     title:"커밋 3개를 GitHub 으로 올릴까요?",
     descHtml: "origin/main 이 바뀝니다. 올린 것은 이 화면에서 되돌릴 수 없습니다."
             + "<div class='wsfix'>…커밋 제목(mono) 최대 5줄 + 「외 N개」…</div>",
     ok:"올리기", cancel:"그만두기"}

`stop:true` 는 022 의 gh 스위치와 같은 판단이다 — 되돌릴 수 없이 바깥으로 나가는 일.
**창 안에 올릴 커밋 제목을 보여 주는 것이 이 설계에서 가장 값싼 안전장치다**: 사람이
무엇을 내보내는지 보고 누른다. `.wsfix` 는 이미 있는 창 안 목록 자리이고, 5줄로 자르면
1531×684 에서도 세 띠가 안 잘린다(0절 캡처로 확인). 받기는 창이 없다.

**도는 중** — 몇 초 걸린다.

- 누른 단추: `.acts button.deed.busy` — **이미 있는 「숨」**(1.4s opacity 맥박,
  `prefers-reduced-motion` 대응 완료). 새 스피너를 만들지 않는다. 옅히기만 하면 잠긴
  단추와 구별이 안 된다(그건 「못 누른다」는 뜻이고 이건 「하고 있다」는 뜻이다).
- 라벨은 **바뀌지 않는다** — 폭이 흔들리면 레이아웃이 흔들린다.
- 다른 단추도 함께 잠기고 사유는 `지금 받는 중입니다.`
- `.wmsg`(이미 `role="status" aria-live="polite"`)가 **단계와 경과**를 말한다:
  `원격에 묻는 중… 2초` → `받는 중… 5초`. 정적 문구는 멈춘 것처럼 읽히므로 경과를 1초마다
  갱신한다. 30초를 넘기면 한 마디 더: `느립니다 — 원격이 응답하지 않을 수 있습니다.`
  (카드의 「오래 걸림」 규칙의 재적용.)
- **높이는 안 흔들린다** — `.wsay` 가 뜻 줄과 결과 줄을 같은 격자 칸에 겹쳐 두는 022 의
  해법을 그대로 물려받는다.

**끝난 뒤 — 결과는 두 자리에 살고, 수명이 다르다**

1. **그 행의 `.wmsg`** — `커밋 2개를 받았습니다.` **3초 뒤 뜻 줄로 되돌아온다**
   (사라지는 것이 아니다 · 022 규율 그대로). 즉각 피드백의 몫.
2. **상태 줄** — 서버 응답이 **새 상태를 함께 준다**(두 번 묻지 않는다). 상태 줄이 즉시
   다시 그려지는 것이 곧 「무엇이 바뀌었나」의 답이다: `원격과 같습니다`.
3. **무엇이 들어왔나** — 상태 덩이 아래 `.more` 텍스트 버튼 한 줄:
   `+ 방금 받은 커밋 2개 보기` → 펴면 mono 목록(짧은 해시 + 제목). **기본 접힘.**
   이 줄은 **다음 조작이나 탭을 떠날 때까지** 남고, 새로고침하면 사라진다 — 영속 기록은
   문서와 저장소 역사가 이미 들고 있으므로 화면이 세 번째 사본을 만들지 않는다.

**실패** — `.wmsg.bad`(`--c-blocked` 글자색)에 **서버가 준 문장 그대로**, 되돌아오지 않는다
(사람이 읽고 조치할 것이 남아 있다). 팝업 금지·인라인(`postJSONRaw` 의 그 규율). 뜻 줄
자리에는 다음 행동 한 줄이 선다(예: 올리기가 거절당하면 `먼저 받은 뒤에 다시 올려 주세요.`).
연결 끊김은 `서버에 연결할 수 없습니다` 가 이미 있다.

### 6. 권한 — 손잡이는 admin, 상태는 등록 사용자 모두

- **읽기(상태 줄·「지금 확인」)는 등록 사용자 모두.** 「읽기 = 자유」는 이 대시보드가 이미
  세운 규칙이고, 거리를 아는 데는 위험이 없다.
- **받기·올리기는 admin 만.** 근거 둘: ① 이 일은 내 계정의 설정이 아니라 **모두의
  저장소**를 바꾼다 — 「사용자 관리」가 admin 전용인 그 축이다. ② **누르는 사람과 서명하는
  계정이 다르다** — 올리기는 이 컴퓨터에 로그인된 GitHub 계정으로 나간다(022 의 gh 문장이
  이미 그 사실을 말한다). member 가 눌러도 바깥에 남는 얼굴은 그 계정이다. 화면이 남의
  이름으로 서명하게 두지 않는다.
- **대리(asUser)로 남의 시점을 볼 때는 둘 다 잠긴다.** 저장소를 바꾸는 일을 대리로 하지
  않는다 — 비밀 키가 대리를 안 받는 것과 같은 축.

### 7. 나머지 얼굴들

- **미등록 계정** — 「내 계정」·「무인 작업」이 쓰는 그 문장(터미널 등록 안내). 상태 줄은
  보여 준다(저장소는 계정과 무관하다).
- **목록을 못 받음** — 이미 있는 `다시 불러오기` 단추.
- **저장소가 아님 / 원격 없음 / 명령 없음** — 상태 문장 자리에 서버 문장, 단추 둘 잠김.
- **고치던 파일 500개** — 앞 3개 이름 + `+ 497개 더 보기`(`.more`), 펴도 50줄에서 자르고
  `…외 N개`. 무한 목록 금지.
- **올릴 커밋 200개** — 라벨 `커밋 200개 올리기`, 창 목록은 5개 + `외 195개`.
- **빈 상태는 없다** — 두 손잡이는 어떤 저장소에서도 늘 서고, 못 하면 사유를 말한다.

### 8. 서버에 요구하는 것 (설계가 정하는 계약 · 구현 몫)

- `GET /api/git/state?ask_remote=0|1` →
  `{branch, upstream, ahead, behind, dirty:[경로…], dirty_n, asked_at, remote_ok,
    workers:N, can:{pull:{ok,why}, push:{ok,why}}}`.
  **`can` 을 서버가 판정한다**(4절). `ask_remote=0` 이 기본 — 값싼 쪽만.
- `POST /api/git/pull` · `POST /api/git/push` → 같은 판정을 **다시** 하고, 통과하면 돌린다.
  응답 `{ok, said, commits:[{hash,title}], state:{…}}` — 실패는 `{ok:false, error:"<문장>"}`.
- 서버가 **아예 못 하게 짜인다**: 커밋하지 않는다 · 치워 두었다 되돌리지 않는다 ·
  합치는 방식을 고르지 않는다(앞으로만 붙이는 방식 고정) · 강제 옵션 없음. 리드 제약
  1·3·4 는 문장이 아니라 코드의 모양이어야 한다.

### 9. 이 설계가 손대지 않는 것 · 새로 만드는 것

**재사용** — `.docs/.doclist` 구역 한 줄, `.metatbl.wtbl` 행(첫 칸 190px·이름+mono),
`.wsay/.wmean/.wmsg`(겹친 격자 · `aria-live`), `.wfact`(`--c-blocked` 글자색),
`.acts button.deed`(+`.busy` 숨 · `aria-disabled` 잠금), `.more` 텍스트 버튼,
`.path.secnote` 판 머리, `s9dlg` + `.wsfix` + `.dlgyes`.

**신규 CSS는 하나** — 상태 문장 한 줄(`.gstate`, 본문 12.5px, 숫자 `<b>`). 그것뿐이다.

**금지 준수** — 색면 하이라이트 없음 · 좌측 세로 띠 없음 · 무채색 미니멀 아님(색은
갈라짐 한 줄의 **글자색** 하나에만 산다) · 네이티브 confirm/alert 없음 · 새 마크·글리프
없음 → **배율 검증은 표 선 두께만** 보면 된다(1·1.25·1.5).

### 10. 문구 초안 (ux-writer · tech-writer · translator 판정 대상 — 자리와 형식만 내가 정했다)

| 자리 | 초안 |
|---|---|
| 구역 이름 / 부제 | `저장소` / `GitHub 과 주고받기` |
| 판 머리 계약 | `이 저장소와 GitHub 사이를 손으로 맞춥니다. 화면은 커밋하지 않습니다 — 이미 커밋한 것만 올리고, 받기는 고치던 파일이 없을 때만 합니다.` |
| 상태 이름 | `지금` |
| 조용한 상태 | `원격과 같습니다 — 올릴 것도 받을 것도 없습니다.` |
| 나이 | `원격에는 5분 전에 물어봤습니다` / `방금 원격에 물어봤습니다` / `원격에는 아직 물어보지 않았습니다` |
| 재기 단추 | `지금 확인` |
| 받기 | 이름 `받기` · 라벨 `커밋 2개 받기` · 뜻 `원격에 새로 올라온 것을 이 저장소로 가져옵니다.` |
| 올리기 | 이름 `올리기` · 라벨 `커밋 3개 올리기` · 뜻 `이미 커밋한 것을 GitHub 으로 내보냅니다.` |
| 결과 | `커밋 2개를 받았습니다.` / `커밋 3개를 올렸습니다.` |
| 결과 펴보기 | `방금 받은 커밋 2개 보기` |

「앞선 것/뒤진 것」 대신 **「올릴 것/받을 것」** 을 쓴 이유: 사람이 알고 싶은 것은 방향이
아니라 **자기가 할 수 있는 일**이고, 그 낱말이 아래 두 단추의 라벨과 그대로 이어진다.
같은 개념에 두 이름을 만들지 않는다.

### 11. 구현이 실측할 것

- 상태 문장이 폭 1100px(좌측 목록 330px 를 뺀 나머지)에서 몇 줄이 되는지, 고치던 파일이
  30개일 때 첫 줄이 안 밀리는지.
- 「지금 확인」·받기·올리기의 **hover·focus·잠김·busy 얼굴을 실포인터(CDP)로** 찍을 것 —
  정적 캡처에는 쉼만 나온다. 특히 `.deed.busy` 의 숨이 terminal 처럼 통째로 반전하는
  스킨에서 배경에 묻히지 않는지.
- 스킨 5벌 × 톤 4벌에서 `.wfact` 의 `--c-blocked` 대비.
- 창에 커밋 5줄을 넣은 상태로 1531×684 에서 바닥 띠가 안 잘리는지.
- 10초 폴링이 탭을 떠날 때 정리되는지(live follow 규율).

[Image: assets/REQ-20260901-023-62x6/about-684.png]
[Image: assets/REQ-20260901-023-62x6/worker-684.png]
[Image: assets/REQ-20260901-023-62x6/dlg-long-684.png]

### 2026-09-01T20:00:05+09:00 subagent (agent: sub:designer:a77ad793, by nicehugepark)

`[2026-09-01 19:47:05 KST - designer]`

REQ-20260901-023-62x6 의 구조·시각 설계를 문서에 남겼다 (`--label response --agent designer`, 캡처 3장 첨부). 022 의 결정을 그대로 물려받아 **새 CSS는 한 줄**이다.

**근거로 삼은 화면.** 「시스템」 판의 **첫 줄이 `저장소 ~/section9 (로컬 md + git 동기화)`** 다 — 화면이 「동기화」를 말하면서 손잡이는 하나도 없다. 022 의 행 부품(`.metatbl.wtbl`, 첫 칸 이름+mono 키, 겹친 격자 `.wsay`)은 이미 서 있고, 확인 창은 1531×684 에서 세 띠로 안 잘린다.

**1. 자리 — 좌측 목록 여섯째 줄 「저장소」**, 「사용자 관리」와 「시스템」 사이. 축은 리드가 못박은 그대로다: 계정의 설정이 아니라 **저장소의 상태와 행위**. 022 의 순서 근거(나→나→남→전체)에서 「모두의 것」 자리이고, 시스템보다 앞인 이유는 시스템은 읽기만 하는 안내판이기 때문이다. 「무인 작업」 기각 — 그 판의 모든 값은 `s9 user config` 로 **내 계정에** 저장되는 내 설정인데 원격과의 거리는 누구에게나 같다. 부제가 상태를 겸한다(`올릴 것 3개`).

**2. 상태 줄 — 한 줄, 0인 마디는 지운다.** 숫자 칩 셋이 아니라 문장 하나(본문체, 숫자만 `<b>`). 셋 다 0인 **가장 흔한 얼굴**은 `원격과 같습니다 — 올릴 것도 받을 것도 없습니다.` 흐린 잉크 한 문장, ✓배지도 초록 점도 없다. 갈라졌을 때만 `.wfact`(글자색 `--c-blocked`)가 한 줄 더 선다 — **이 판에서 색이 쓰이는 유일한 자리**. 재는 시점은 **값이 두 벌**이다: 고치던 파일·올릴 것은 이 컴퓨터 안에서 10초마다, **받을 것은 원격에 물어야 하므로 손으로 누를 때·받기/올리기 직전·끝난 직후만**. 주기적 자동 fetch는 기각(며칠 열어 두는 화면이라 하루 2,880번을 두드린다). 대신 **나이를 숨기지 않는다** — `원격에는 5분 전에 물어봤습니다`.

**3. 손잡이 둘 — 두 행, 받기가 위.** 첫 칸 mono 에 **실제로 돌아가는 명령을 그대로 적는다** — 그 줄이 안전 고지다(숨은 강제 옵션도, 치워 두는 명령도, 합치는 선택도 거기 없음이 눈에 보인다). 무게 차이는 색면 없이 셋: 라벨의 숫자와 동사(가져옵니다/내보냅니다) · **올리기만 확인 창** · 읽는 순서가 안전한 순서. **판 위에서 두 단추는 같은 옷**이다 — 「올리기만 잉크 채움」은 기각: 무엇이 주 행동인지는 **상태가 정한다**(받을 것이 있으면 받기가 주 행동). 이 판의 주 강조는 「지금 잠기지 않은 단추」 하나고, 잉크 채움은 창 안(`.dlgyes`)에서만 쓴다. 화살표 글리프 없음(▶/⏸ 배타 규칙 — 둘은 동시에 참일 수 있다).

**4. 못 하는 순간 — 잠그고, 옆에서 이유를 말한다. 둘 다 한다.** 이 저장소가 판정 창에서 이미 쓰는 답(`yes.disabled` + `.dlghint` 가 함께 선다). 잠금은 `disabled` 가 아니라 **`aria-disabled`**(REQ-20260831-009-62x6: `disabled` 는 포커스를 걷어 키보드 손이 사유에 못 닿는다). 사유가 서는 자리는 **그 행의 뜻 줄(`.wmean`)** — 새 칸 없음. 뜻 줄이 상태에 따라 얼굴을 바꾸고, 색이 아니라 **문법**으로 갈린다(`…합니다` ↔ `…해서 …할 수 없습니다`). 여덟 갈래 사유 문구 초안을 표로 냈다. **판정은 서버가 하고**(규칙 두 벌 금지) 누른 순간 다시 판정한다 — 경합의 마지막 문.

**5. 도는 중·끝난 뒤.** 새 스피너 없이 이미 있는 `.deed.busy` 숨(reduced-motion 대응 완료), 라벨은 불변(폭 흔들림 금지), `.wmsg`(`aria-live`)가 **단계+경과**를 말한다(`받는 중… 5초`, 30초 넘으면 「느립니다」). 결과는 두 자리에 살고 수명이 다르다: `.wmsg` 는 3초 뒤 뜻으로 되돌아오고, **상태 줄이 다시 그려지는 것이 곧 「무엇이 바뀌었나」**이며, 받은 커밋 제목은 `+ 방금 받은 커밋 2개 보기`(`.more`, 접힘)로 탭을 떠날 때까지 남는다. 실패는 서버 문장 그대로, 인라인, 되돌아오지 않음.

**6. 권한** — 상태는 등록 사용자 모두, **손잡이는 admin 만**. 근거 둘: 모두의 저장소를 바꾸는 일이라 「사용자 관리」와 같은 축이고, **누르는 사람과 서명하는 계정이 다르다**(올리기는 이 컴퓨터의 GitHub 계정으로 나간다). 대리(asUser) 중에는 둘 다 잠긴다.

**넘기는 것.**
- 문구는 초안만(10절 표) — 구역 이름 「저장소」, 「올릴 것/받을 것」(앞선 것/뒤진 것 대신) 포함 ux-writer·tech-writer·translator 판정 몫.
- 서버 계약 초안(8절): `GET /api/git/state` 가 `can.pull/push{ok,why}` 를 **판정해서** 주고, POST 둘이 다시 판정한다. 커밋·치워두기·합치기 선택·강제 옵션은 서버가 **아예 못 하게** 짜인다.

**곁가지 결함 하나 (별건)** — 좌측 목록이 창을 1500px 로 키워도 **같은 자리에서 잘려 「시스템」이 안 보인다**(`.doclist{max-height:var(--docsh,…)}` 가 Settings 에서 창 높이를 안 따르는 것으로 보인다). 이 설계가 여섯째 줄을 더하므로, 고치기 전까지 「부제가 상태를 겸한다」는 절반만 참이다.

파일: `/home/sjpark1/section9/vault/requests/2026/09/REQ-20260901-023-62x6.md` · 설계 원문 `/tmp/claude-1000/-home-sjpark1-section9/8a0d83ad-568f-4f69-9585-f816dfe479f7/scratchpad/design-023.md` · 캡처는 문서에 그림으로 붙었다.

### 2026-09-01T20:04:01+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

### 리드 판정 4 — designer 착지, 023 설계 확정

셋의 결론이 서로 어긋나지 않는다. 확정한다.

**자리 = 좌측 목록 여섯째 줄 「저장소」** (사용자 관리와 시스템 사이).
designer 가 든 근거가 리드 제약의 축과 같다 — 계정의 설정이 아니라 **저장소의
상태와 행위**다. 「무인 작업」 판 기각도 옳다: 그 판의 값은 전부 `s9 user
config` 로 **내 계정에** 저장되는 내 설정인데, 원격과의 거리는 누구에게나 같다.
그리고 근거로 든 실측이 이 요청의 정당성 자체다 — 「시스템」 판의 **첫 줄이
이미 `저장소 ~/section9 (로컬 md + git 동기화)`** 인데 손잡이가 하나도 없다.
화면이 「동기화」를 말하면서 아무것도 못 하게 해 뒀다.

**두 단추는 판 위에서 같은 옷이다.** designer 가 「올리기만 잉크 채움」을
스스로 기각한 근거를 받는다 — 무엇이 주 행동인지는 **상태가 정한다**(받을 것이
있으면 받기가 주 행동이다). 잉크 채움은 창 안에서만 쓴다.

**잠금은 `disabled` 가 아니라 `aria-disabled`.** REQ-20260831-009-62x6 가 이미
배운 것: `disabled` 는 포커스를 걷어 **키보드 손이 사유에 못 닿는다**. 사유를
옆에 세워 놓고 그 사유에 닿지 못하게 하는 것은 안 세운 것과 같다.

**재는 시점이 두 벌**인 것도 받는다. 고치던 파일·올릴 것은 이 컴퓨터 안에서
자주, **받을 것은 원격에 물어야 하므로 손으로 누를 때와 받기/올리기 앞뒤만.**
주기적 자동 fetch 기각 — 며칠 열어 두는 화면이라 하루 2,880번을 두드린다.
대신 **나이를 숨기지 않는다**(「원격에는 5분 전에 물어봤습니다」). 값이 드는
일을 조용히 반복하지 않으면서도 화면이 거짓말하지 않는 유일한 자를 골랐다.

**판정은 서버가 한다.** `can.pull/push{ok,why}` 를 서버가 판정해 주고, 누른
순간 **다시** 판정한다. 규칙을 두 벌 두지 않는 이 저장소의 규율 그대로이고,
경합(그 사이에 작업자가 뜨는 것)의 마지막 문이 거기다. 서버는 커밋·치워두기·
합치기 선택·강제 옵션을 **아예 못 하게** 짜인다 — 리드 제약 1~4 가 코드의
모양이 된다.

**권한**은 designer 도 admin 만으로 왔고 근거 하나를 더 댔다: **누르는 사람과
서명하는 계정이 다르다** — 올리기는 이 컴퓨터의 GitHub 계정으로 나간다.
대리(asUser) 중에는 둘 다 잠근다. 받는다.

### 아직 확인 못 한 것 (구현이 확인할 것)

designer 가 곁가지로 「좌측 목록이 창을 키워도 같은 자리에서 잘려 시스템이 안
보인다」를 보고했다. **리드가 재현하지 못했다** — 캡처가 60초 타임아웃으로
두 번 실패했다(022 프론트가 같은 화면에서 캡처를 돌리는 중이고, 서버가
REQ-20260901-020-62x6 의 끊김을 겪고 있다). 코드상으로는 `--docsh` 를 재는
`sizeDocs()` 가 Docs 탭에서만 불리고 Settings 는 CSS 대체값
`calc(100vh - 150px)` 로 떨어지는데, 1000px 화면이면 850px 라 다섯 줄이
잘릴 자리가 아니다. **재현되면 별건으로 세운다.** 이 설계가 여섯째 줄을
더하므로 구현이 반드시 실측할 것.

### 순서

구현은 REQ-20260901-022-62x6 의 프론트가 착지한 뒤다 — 같은 파일(userform.js ·
settings.js · workercfg.js · bin/s9)을 두 에이전트가 동시에 고치면 한쪽이
삼켜진다.

### 2026-09-01T20:04:25+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-09-01 20:04:24 KST - lead]`

designer까지 착지해서 023 설계를 확정했습니다. 셋이 서로 어긋나지 않았습니다.

**자리 → 좌측 목록에 「저장소」 구역 신설**(사용자 관리와 시스템 사이). designer가 든 근거가 이 요청의 정당성 자체입니다 — 지금 「시스템」 판의 **첫 줄이 이미 `저장소 ~/section9 (로컬 md + git 동기화)`** 인데 손잡이가 하나도 없습니다. 화면이 "동기화"를 말해 놓고 아무것도 못 하게 해 뒀습니다.

**두 단추는 같은 옷을 입습니다.** designer가 「올리기만 잉크 채움」을 스스로 기각했는데 근거가 맞습니다 — 무엇이 주 행동인지는 **상태가 정합니다**(받을 게 있으면 받기가 주 행동입니다). 잉크 채움은 창 안에서만 씁니다.

**잠금은 `disabled`가 아니라 `aria-disabled`입니다.** 이 저장소가 이미 배운 것 — `disabled`는 포커스를 걷어서 **키보드 손이 사유에 못 닿습니다.** 사유를 옆에 세워 놓고 거기 닿지 못하게 하면 안 세운 것과 같습니다.

**원격에 묻는 값을 아끼되 거짓말은 안 합니다.** 「올릴 것」과 「고치던 파일」은 이 컴퓨터 안에서 자주 재고, **「받을 것」은 원격에 물어야 하니 손으로 누를 때와 받기/올리기 앞뒤만** 잽니다. 주기적 자동 확인은 기각했습니다 — 며칠 열어 두는 화면이라 하루 2,880번을 두드리게 됩니다. 대신 **나이를 숨기지 않습니다**: 「원격에는 5분 전에 물어봤습니다」.

**판정은 서버가 하고, 누른 순간 다시 합니다.** 규칙을 두 벌 두지 않는 규율이고, 그 사이에 작업자가 뜨는 경합의 마지막 문이 거기입니다. 서버는 커밋·치워두기·합치기 선택·강제 옵션을 **아예 못 하게** 짭니다 — 제가 못박은 제약이 코드의 모양이 됩니다.

**확인 못 한 것 하나** — designer가 곁가지로 "좌측 목록이 창을 키워도 잘려서 「시스템」이 안 보인다"를 보고했는데, **제가 재현하지 못했습니다.** 캡처가 60초 타임아웃으로 두 번 실패했습니다(022 프론트가 같은 화면에서 캡처를 돌리는 중이고, 서버가 020의 끊김을 겪고 있습니다). 코드상으로는 잘릴 자리가 아니라서 확정하지 않고 구현이 실측하도록 넘겼습니다.

구현은 022 프론트가 착지한 뒤에 붙입니다. 지금 넣으면 같은 파일을 두 에이전트가 고쳐서 한쪽이 삼켜집니다.

### 2026-09-01T20:58:03+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-09-01 20:57:55 KST - lead]`

022 화면이 착지했습니다. 캡처로 확인했습니다 — Settings 좌측에 「무인 작업」 판이 서고, `기타: {…}` JSON 한 줄은 화면에서 사라졌습니다. 행마다 한국어 이름 + 기계 키 이름 + 뜻 한 줄이 서고, 색면은 한 군데도 없습니다. 처음 물으셨던 그 스위치는 이제 「내 GitHub 계정 쓰게 하기」로 서 있고, 뜻 줄이 "저장소에 올리는 것도 그중 하나입니다 — 같은 권한으로 저장소 설정을 바꾸거나 저장소를 지울 수도 있습니다"라고 말합니다. 커밋을 올리는 중입니다.

**그런데 검증 중에 020의 뿌리를 잡았습니다.** 이게 지금 가장 큰 문제입니다.

서버를 **재시작한 지 10분** 만에 이렇습니다:

```
스레드 143 · 열린 fd 198 · 받아들이는 큐 32
9909 소켓: ESTAB 129 · CLOSE-WAIT 11 · LAST-ACK 8
```

연결이 쌓이고 안 걷힙니다. 이 서버는 연결 하나에 스레드 하나인데, keep-alive로 물린 연결 129개가 각자 "다음 요청"을 기다리며 앉아 있고 받아들이는 큐는 32개뿐입니다. 큐가 차면 커널이 새 연결을 그냥 리셋합니다. **CLOSE-WAIT 11건이 결정적입니다** — 상대는 이미 끊었는데 서버가 그 소켓을 안 닫고 있습니다.

지난번 진단 문서는 이걸 **회선 탓**으로 적어 뒀는데, 이번 실측은 다른 답을 가리킵니다. 회선이 아니라 **놓아 주지 않는 스레드**입니다.

지금 이것 때문에 대시보드가 헤드리스로 아예 안 뜹니다(60~100초 타임아웃). 낮에는 같은 URL이 10초에 찍혔습니다. Chrome은 멀쩡합니다 — `file://`로는 2.5초입니다.

**중간에 제가 틀렸던 것 하나** — Windows에 chrome.exe가 450개 있어서 캡처 도구가 브라우저를 새는 줄 알았습니다. 죽이기 전에 확인해 보니 **전부 사용자분의 실제 Chrome 브라우저**였고, 캡처가 남긴 것은 0개였습니다. 회수 도구는 제 몫을 하고 있었습니다. 확인 안 하고 죽였으면 브라우저를 통째로 닫을 뻔했습니다.

020은 backend-developer에 넘겼습니다 — 실측 숫자와 함께, 우회(화면이 `Connection: close`를 강제) 금지, SSE처럼 오래 살아야 하는 연결과 폴링 연결을 같은 자로 재지 말 것을 못박아서요.

023(받고 올리기) 구현은 020이 잡힌 뒤에 붙입니다. 지금 붙여 봐야 화면을 볼 수가 없습니다.

### 2026-09-01T21:04:33+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-09-01 21:04:30 KST - lead]`

022 커밋이 **게이트에 막혔습니다.** 진짜 회귀를 하나 잡았고, 막힌 게 맞습니다.

`test_wake.py:212`가 차단 메시지에 `auto_resume`이라는 **원시 키가 들어 있는지**를 조건으로 걸고 있었습니다. 그런데 022의 판정이 정확히 그 키를 걷어냈습니다 — 서버가 사용자에게 기계 이름을 외우게 하는 것이 이 요청이 없애려던 증상이었으니까요. 옛 계약이 시험에 박혀 있던 것입니다.

시험을 약하게 만들지 않고 **재는 자리를 옮겼습니다**: 이제 "무엇을 켜야 하는지"를 원시 키가 아니라 **화면의 이름**(「무인 작업 맡기기」)으로 재고, "어디서 켜는지"(Settings)까지 함께 잽니다. 둘 다 있어야 사람이 실제로 켤 수 있습니다. 옛 자를 그대로 뒀다면 기계 키를 노출하는 것이 통과 조건이 되는 셈이었습니다.

구현 에이전트가 `test_wake`를 안 돌렸고 게이트가 잡았습니다 — 게이트가 제 몫을 했습니다.

지금 그 수정으로 시험을 다시 돌리는 중인데, 020의 서버 문제 때문에 브라우저를 쓰는 시험이 평소보다 훨씬 느립니다(49건에 213초). 020이 잡히기 전까지는 이 저장소의 모든 검증이 이 속도입니다.

결과 나오는 대로 커밋하고 이어 보고하겠습니다.

### 2026-09-01T21:05:19+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-09-01 21:05:17 KST - lead]`

`test_wake` 99건 green입니다. 그 수정을 함께 담아 022 커밋을 다시 올리는 중입니다 — 게이트가 스모크까지 다시 돌리므로 몇 분 걸립니다.

기다리는 동안 정리하면, 지금 상태는 이렇습니다.

| 요청 | 상태 |
|---|---|
| 019 팝업 하단 잘림 | done · 승인받음 |
| 021 CSS 구역 자름 | done · 커밋 `2c57757` |
| 022 설정 화면 | 구현·검증 끝, 커밋 게이트 통과 대기 |
| 023 화면에서 받고 올리기 | 설계·문구 확정, 구현은 020 뒤로 |
| 020 서버 연결 누수 | backend-developer 작업 중 |

020이 지금 병목입니다. 그것 때문에 대시보드가 헤드리스로 안 뜨고, 브라우저를 쓰는 시험이 평소의 몇 배로 느려지고, 023 구현을 시작해도 화면을 볼 수가 없습니다. 그래서 023을 먼저 붙이지 않고 순서를 바꿨습니다.

커밋 결과와 020 진행이 오는 대로 이어 보고하겠습니다.

### 2026-09-01T21:08:56+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-09-01 21:08:56 KST - lead]`

022 커밋 `1473eb0`, 푸시 완료. 확인 대기로 올렸습니다.

**보실 것** — F5 후 Settings 왼쪽 목록에서 「무인 작업」을 눌러 보세요. 처음 물으셨던 그 스위치가 「내 GitHub 계정 쓰게 하기」로 서 있고, 줄마다 무엇을 여는지 한 줄로 말합니다. 켜면 확인 창이 한 번 뜹니다(끌 때는 안 뜹니다). 이름과 설명이 읽히고 값이 실제로 바뀌면 승인, 어색하거나 안 바뀌면 캡처와 함께 반려해 주세요.

이건 취향·방향 판단이 필요한 자리라 review로 올렸습니다. 낱말을 넷이 붙어 정했지만 결국 쓰실 분이 읽어 봐야 아는 것들입니다.

**오늘 이 줄기에서 나온 것 정리**

질문 하나("깃헙 푸시 설정 어디서 바꾸지")에서 시작해 다섯 건이 나왔습니다.

- `019` 팝업 하단 잘림 — done, 승인
- `021` CSS 시험 구역이 빈 줄에서 잘리던 것 — done
- `022` 설정 화면 — 방금 확인 요청
- `023` 화면에서 받고 올리기 — 설계·문구 확정, 구현 대기
- `020` 서버 연결 누수 — 작업 중, 지금 병목

020은 backend-developer가 잡고 있습니다. 그게 끝나야 023 구현을 붙이고 화면을 볼 수 있습니다. 진행되는 대로 알리겠습니다.

### 2026-09-01T21:16:49+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-09-01 21:16:48 KST - lead]`

022 에이전트가 뒷정리를 마치고 종료했습니다 — 남은 시험 프로세스·캡처 브라우저 0개, 격리 서버 전부 내림, 대시보드는 살아 있습니다. 할 일은 없습니다.

그쪽이 리드 판단으로 남겨 둔 둘은 이미 처리했습니다 — 차단 문구의 자리 이름을 「무인 작업」으로 맞춘 것은 옳은 판단이라 그대로 두었고(스위치가 그 자리로 옮겼으니 옛 이름을 남기면 서버가 사람을 없는 자리로 보냅니다), `/app/all.js`가 빈 몸으로 나가는 선행 결함은 새 REQ를 만들지 않고 020으로 흡수했습니다. 같은 뿌리입니다 — 연결이 리셋되니 묶음이 반만 도착하고, 그러면 껍데기의 되돌리기 그물이 조각을 다시 꽂아 같은 선언이 두 번 섭니다.

020 진행되는 대로 알리겠습니다.

### 2026-09-01T21:17:00+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

`[2026-09-01 21:17:00 KST - lead]`

중복 알림이라 조치할 것이 없습니다. 022는 이미 커밋 `1473eb0`으로 올라갔고 확인 대기 상태입니다.

020(서버 연결 누수) 결과를 기다리는 중입니다.

### 2026-09-02T07:03:39+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

### 사용자 반려 — 낱말 판정을 뒤집는다 (2026-09-02, REQ-20260902-002-62x6 로 별건 착수)

사용자 지적: "고유 기능이고 기술 용어를 번역하지 마라. pull push commit worktree
등등 포함해서 전부."

**19:53 tech-writer 판정표 A 의 다음 칸은 무효다** — `pull`→「받기」, `push`→
「올리기」, `commit` 셈 금지, `branch` 안 세움, `merge`/`rebase` 개념어 금지.
판정의 논리(「이름은 이름처럼, 행위는 행위처럼」)는 이 저장소의 `done`/`in-progress`
에는 맞지만, git 명령 이름에는 적용하지 않는다: 사용자가 GitHub·터미널·다른
도구에서 **같은 글자로 다시 만나는 고유 기능 이름**이기 때문이다. tech-writer
스스로 물었던 그 질문("다시 만나는 사실이 판정을 바꾸나")의 답이 "명사만"이 아니라
**동사까지**였다.

살아 남는 것: 리드 제약 7개(안전), designer 의 자리·구조 판정, 상태를 셈으로
말한다는 원칙. 바뀌는 것: 단추와 상태 문구의 낱말.

제목도 「화면에서 받고 올리기」 → 「화면에서 pull·push」 로 바꿨다.
낱말 재판정은 REQ-20260902-002-62x6 에서 화면 전체를 한 번에 훑어 정한다 —
023 은 그 사전이 확정된 뒤 구현에 들어간다.

### 2026-09-02T10:51:07+09:00 subagent (agent: sub:subagent:a5eb6ce4, by nicehugepark)

Reading REQ-20260901-023-62x6 spec notes

### 2026-09-02T10:55:23+09:00 tdd (agent: sub:frontend-developer, by nicehugepark)

### TDD 시나리오 — 화면에서 pull·push (frontend-developer)

착수 전 작성. 서버 갈래는 **실제 git 저장소를 임시로 만들어**(원격도 임시
bare) 각 상태를 재현한다 — 모의 객체로는 「앞으로만 붙이는 방식」이 진짜로
지켜지는지 증명하지 못한다.

### A. 서버 — 상태 읽기 `GET /api/git/state`

- [ ] S1 같음(정상): 임시 저장소·원격이 같은 자리 → `ahead 0 · behind 0 · dirty 0`,
      `can.pull.ok=false` · `can.push.ok=false`, 두 why 가 「없습니다」로 끝난다.
- [ ] S2 push 만(정상): 로컬에 commit 1 → `ahead 1 · behind 0`,
      `can.push.ok=true` · `can.pull.ok=false`.
- [ ] S3 pull 만(정상): 원격에 commit 1 → `ahead 0 · behind 1`,
      `can.pull.ok=true` · `can.push.ok=false`.
- [ ] S4 갈라짐(경계): 양쪽에 commit → `ahead>0 && behind>0` → **둘 다 ok=false**,
      why 가 「화면이 대신 합치지 않습니다」를 담는다 (리드 제약 4).
- [ ] S5 더러운 트리(경계): 추적 파일 수정 → `dirty_n>0` 이고 `dirty` 에 **파일 이름**,
      `can.pull.ok=false`(리드 제약 3). push 는 dirty 로 막히지 않는다.
- [ ] S6 도는 일(경계): 살아 있는 잡/백그라운드 작업 1건 → **둘 다 ok=false**,
      why 에 「도는 일 N건」 (리드 제약 2 · 헤더 칩과 같은 글자).
- [ ] S7 권한 없음(경계): actor 가 admin 이 아니면 둘 다 ok=false, why 가 admin 을 말한다.
- [ ] S8 대리(경계): `as=<남>` 으로 물으면 둘 다 ok=false — 저장소를 대리로 바꾸지 않는다.
- [ ] S9 원격 없음/저장소 아님(경계): upstream 이 없으면 둘 다 ok=false + 붙이는 명령 한 줄.
- [ ] S10 값싼 쪽만(계약): `ask_remote=0`(기본)은 **fetch 를 부르지 않는다** —
      호출 인자를 기록해 `fetch` 가 0회임을 잰다. `ask_remote=1` 일 때만 1회.

### B. 서버 — 실행 `POST /api/git/pull` · `/api/git/push`

- [ ] S11 pull 정상: behind 1 → ok, 응답의 새 상태가 `0 · 0`, `commits` 에 받은 것.
- [ ] S12 push 정상: ahead 1 → ok, **임시 bare 원격의 main 이 로컬 HEAD 와 같아진다**.
- [ ] S13 재판정(경계): 화면이 살아 있다고 그린 뒤 트리가 더러워졌으면 pull 이 거부되고,
      거부 문장이 A 의 why 와 **같은 글자**다(문장 두 벌 금지).
- [ ] S14 명령 모양(안전·회귀): `git_run` 이 `add·commit·stash·checkout·switch·reset·
      restore·clean·rebase·merge·cherry-pick·revert` 와 `--force/-f/--hard` 를 **거부한다**.
      리드 제약 1·3·4 가 문장이 아니라 코드의 모양임을 이 시험이 지킨다.
- [ ] S15 앞으로만(안전): pull 이 실제로 `pull --ff-only` 로 돈다 — 인자를 잰다.
      ff 가 불가능한 상태에서는 실패가 그대로 화면 문장이 된다.
- [ ] S16 인증·불통 실패(실패): push/fetch 가 실패하면 `{ok:false, error:<서버 문장>}` 이고
      화면에 **왜 못 했는지가 선다**(빈 실패 금지).

### C. 화면

- [ ] S17 낱말: 새 문자열에 음차 0 — `tests/test_screen_lexicon.py` 통과
      (「푸시」·「커밋」·「브랜치」 금지, 단추는 `pull`/`push`, 셈은 「push 할 것 N개」).
- [ ] S18 확인 창: push 창이 `test_dialog_safe.py` CENSUS 에 등재되고 F1~F3 통과.
      판정 근거는 아래 「safe 판정」.
- [ ] S19 잠금: 못 누르는 단추가 `disabled` 가 아니라 `aria-disabled="true"` 이고,
      `aria-describedby` 로 사유 줄에 묶인다 — 키보드 손이 사유에 닿는다.
- [ ] S20 실브라우저: 여섯 얼굴(같음·push 만·pull 만·갈라짐·더러운 트리·거부)을
      캡처하고, hover·focus·busy 는 실포인터(CDP)로 찍는다.
- [ ] S21 폴링: 판을 떠나면 10초 타이머가 정리된다(요소 존재 + 화면 가시 규율).
- [ ] S22 회귀: 화면 코드에 `add`·`commit`·치우는 명령을 부르는 자리가 0.

### push 확인 창의 safe 판정 (대장의 기준으로)

기준은 「되돌려도 그 사이에 잃는 것이 있으면 물러나는 쪽」이다. push 는 소실이
아니라 **되돌릴 수 없는 공개**다 — 이 저장소의 origin 은 PUBLIC 이고(리드 19:56
실측), 한 번 나간 것은 남이 이미 가져갈 수 있다. 되돌리려면 역사를 다시 쓰는
명령이 필요한데 **이 화면은 그것을 아예 못 하게 짜인다**. 022 의 「권한 주기」가
같은 축에서 safe 인 것보다 한 급 더 확정적이다 → **safe: true**.

### 2026-09-02T10:55:24+09:00 subagent (agent: sub:subagent:a5fc5c7b, by nicehugepark)

Writing TDD scenarios to REQ-20260901-023-62x6

### 2026-09-02T11:56:10+09:00 subagent (agent: sub:subagent:a3d385f1, by nicehugepark)

Updating TDD checkboxes in REQ-20260901-023-62x6

### 2026-09-02T11:56:24+09:00 tdd (agent: sub:frontend-developer, by nicehugepark)

### TDD 시나리오 (개정 — 전부 통과)

`python3 tests/ git_panel` 32건 green. 서버 갈래는 **진짜 저장소를 임시로
세우고**(작업 저장소 + bare 원격 + 남의 clone) 각 상태를 실제로 만든 뒤 쟀다.

### A. 상태 읽기 `GET /api/git/state`
- [x] S1 같음 — `0 · 0 · 0`, 두 단추 다 「없습니다」로 잠김
- [x] S2 push 만 — `ahead 1`, push 살아 있고 `push_commits` 에 제목
- [x] S3 pull 만 — `behind 1`, pull 살아 있음
- [x] S4 갈라짐 — 둘 다 잠기고 「합치는 일은 화면이 하지 않습니다」 + 칠 글자
- [x] S4b 사유가 셈을 다시 적지 않는다 (상태 줄이 이미 들었다)
- [x] S5 더러운 트리 — 파일 이름과 개수, pull 만 잠김(push 는 무관)
- [x] S5b 미추적은 세지 않는다 — 세면 이 저장소의 pull 은 영영 안 열린다
- [x] S6 도는 일 — 실제 잡 파일을 세워 `도는 일 1건`, 둘 다 잠김
- [x] S7 권한 없음 — 둘 다 잠기되 **읽기는 열려 있다**(거리는 그대로 보인다)
- [x] S8 대리 — `@user1 시점` 으로 둘 다 잠김
- [x] S9 원격 없음 / S9b 저장소 아님 — 붙이는 명령 한 줄과 함께 잠김
- [x] S10 값싼 쪽은 원격에 안 묻는다 — 부른 명령을 세어 `fetch` 0회 확인
- [x] S10b 나이를 숨기지 않는다 (`asked_sec`)

### B. 실행 `POST /api/git/pull` · `/api/git/push`
- [x] S11 pull — 앞으로만 붙고, 바뀐 파일 목록·새 상태가 함께 온다
- [x] S11b `web/` 가 바뀌면 「새로 고치면 보입니다」가 붙는다
- [x] S12 push — **임시 bare 원격의 main 이 실제로 움직였다**
- [x] S13 누른 순간 재판정 — 거절 문장과 사유 줄이 **같은 글자**
- [x] S13b 자격 없는 누름은 거절되고 원격이 안 움직인다
- [x] S15 pull 은 `pull --ff-only` 로만 돈다 (실인자 검증)
- [x] S15b 한 번 누르는 동안 부른 git 명령 **전부**에 위험한 낱말 0
- [x] S16 원격이 사라진 자리 — 빈 실패를 만들지 않는다

### C. 명령의 모양 (제약 ①③④가 코드의 모양이 된다)
- [x] S14 금지 하위명령 17개가 `git_run` 에서 터진다
- [x] S14b 금지 깃발(`--force`·`-f`·`--rebase`·`--autostash`·`--prune`)
- [x] S14c 네트워크 셋은 **모양까지** 정확해야 지나간다
- [x] S14d 모르는 하위명령 거부

### D. 화면
- [x] S17 낱말 게이트 `test_screen_lexicon` 통과 — 음차 0
- [x] S18 확인 창이 CENSUS 에 등재, `safe: true` (F1~F5 통과)
- [x] S19 잠금은 `aria-disabled` — **실키보드(CDP Tab)로 잠긴 pull 에 도달**하고
      포커스 링이 보이는 것을 캡처로 확인
- [x] S20 실브라우저 캡처 — 일곱 얼굴 + hover·focus·busy(실포인터 CDP)
- [x] S21 폴링 정리 — 판을 떠나고 11초 뒤 타이머 null 실측
- [x] S22 화면 코드에 위험한 갈래 0 · 서버로 나가는 자리 하나

### 이 시험이 실제로 잡은 결함 넷 (전부 Red → Green)
1. `_git_out` 이 stdout 을 통째로 `strip()` 해 `status --porcelain` 의 앞
   상태 칸이 밀렸다 — 파일 이름이 `a.txt` 대신 `.txt` 로 나왔다(S5).
2. 잠긴 단추가 손을 얹자 **살아 있는 단추처럼 반전**됐다 — 스킨 규칙이 같은
   특이도로 뒤에 실려 잠금 규칙을 덮었다(캡처로 잡고 `.acts.gacts` 로 조임).
3. 판을 다시 열면 손잡이가 한 벌씩 더 물려 push 한 번이 창을 둘 열었다.
4. `ev.target.closest` — 텍스트 노드가 오면 이 조각이 통째로 죽는다
   (`test_event_target_gate` 가 잡았다).

### 2026-09-02T11:57:16+09:00 response (agent: sub:frontend-developer, by nicehugepark)

### 구현 — 화면에서 pull·push (frontend-developer)

Settings 좌측 목록에 여섯째 줄 **「저장소」**가 섰다. 판정은 서버가 하고,
화면은 그 문장을 그대로 그린다. 아래 그림이 일곱 얼굴 전부다.

### 리드 제약 일곱이 코드의 모양이 된 자리

| 제약 | 어디서 지켜지나 |
|---|---|
| ① 화면이 commit 하지 않는다 | `git_run` 허용 목록이 `commit` 을 **부르기 전에** 거부한다. 화면 코드에도 그 낱말이 0이다(시험이 지킨다) |
| ② 도는 일이 있으면 손대지 않는다 | 헤더 칩이 쓰는 그 숫자(`jobs_running` + 백그라운드 작업)로 둘 다 잠근다 |
| ③ pull 은 트리가 깨끗할 때만 | 잠그고 파일 이름을 말한다. **치워 두었다 되돌리는 명령은 허용 목록 밖이라 부를 수조차 없다** |
| ④ 갈라진 갈래는 안 합친다 | `pull` 은 `("pull","--ff-only")` **정확히 이 모양**만 지나간다 — 방식을 고를 인자 자체가 없다 |
| ⑤ push 만 확인 창 | `s9dlg` + `safe:true`(대장 등재). pull 은 창이 없다 |
| ⑥ 누르기 전에 상태가 보인다 | 셈·작업 트리·나이가 단추 **위**에 이미 서 있고, 좌측 목록 부제가 판을 열지 않아도 말한다 |
| ⑦ 누를 자격 | 손잡이는 admin 만, **읽기는 등록 사용자 모두**. 대리(asUser) 중에는 둘 다 잠긴다 |

### 낱말 (오늘 확정된 사전 그대로)

단추 `pull`/`push`(원어·소문자·mono), 셈 「push 할 것 N개 · pull 할 것 N개」,
덩이 「GitHub 과 주고받기」, 이름은 mono(`main → origin/main`·짧은 해시).
「받기」·「올리기」·「푸시」·「커밋」은 한 글자도 없다 — 낱말 게이트가 지킨다.
반대로 「저장소」·「고치던 파일」·「갈래」는 우리 말로 뒀다(무차별 원어도 결함).

**곁들임은 막힐 때만** 선다: 갈라졌을 때 `git pull --rebase`, 짝이 없을 때
`git push -u origin main`. 잘 되는 자리에는 명령이 없다 — 그 자리에 붙이면
이 화면이 제 존재 이유(터미널을 안 열게 하는 것)를 부정한다.

### 값을 두 벌로 갈라 잰다

고치던 파일·push 할 것은 이 컴퓨터 안에서 **판이 보이는 동안 10초마다**,
pull 할 것은 GitHub 에 물어야 하므로 「지금 확인」과 pull·push 앞뒤에만.
주기적 자동 확인은 안 한다(며칠 열어 두면 하루 2,880번). 대신 나이를 숨기지
않는다 — 「방금 / N분 전에 GitHub 에 물어봤습니다」.

### 실브라우저에서 잡아 고친 결함 넷

1. **잠긴 단추가 hover 에서 살아 있는 단추처럼 반전됐다.** 스킨 규칙
   (`[data-skin] .acts button:hover`)이 같은 특이도로 뒤에 실려 잠금 규칙을
   덮었다. `.acts.gacts` 로 한 급 조였다 — 카드의 `.deed.wake` 가 같은 이유로
   조여 둔 자리다. **CSS 캐시를 안 끄면 고친 CSS 를 안 받은 화면을 찍고
   통과시킨다**는 것도 여기서 배웠다(CDP `setCacheDisabled`).
2. `_git_out` 이 stdout 을 통째로 `strip()` 해 porcelain 의 앞 상태 칸이
   밀렸다 — 파일 이름이 `.txt` 로 나왔다. 잣대가 아니라 근원을 고쳤다.
3. **판을 다시 열면 손잡이가 한 벌 더 물렸다** — 구역을 갈아도 `#sview` 는
   살아남는다. push 한 번이 창을 둘 열던 자리.
4. `.cfg-h` 의 대문자화가 `GitHub` 을 `GITHUB` 으로 바꿨다. 이름의 대소문자는
   그 이름의 일부다(「깃헙」을 거부하고 그 글자를 세운 그 축).

### 남는 것 · 판단이 필요한 것

- **calm 스킨에서 잠김/살아 있음이 테두리 밝기로만 갈린다.** calm 이 모든
  `.acts button` 을 `--muted` 잉크로 그려서다 — 카드의 `▶`·`⏸` 도 같은
  형편이라 이 판만 다르게 하지 않았다. 가르는 힘은 문법이 진다(「…합니다」 ↔
  「…해서 …할 수 없습니다」)와 hover 반전 여부. 나머지 아홉 스킨은 잉크와
  테두리 둘 다 갈린다(실측표는 아래).
- **designer 가 보고한 「좌측 목록이 잘려 시스템이 안 보인다」는 재현되지
  않았다.** 여섯째 줄을 더한 뒤에도 창 높이 760px 에서 여섯 줄 + 「가이드
  열기」가 전부 보인다.
- 판 머리 계약 줄이 어절 한가운데서 줄바꿈된다(「않습니 / 다」). **이 판만의
  일이 아니라 `.secnote` 를 쓰는 모든 판이 그렇다** — 한 판만 고치면 이웃한 두
  판이 서로 달라지므로 손대지 않았다. 별건으로 볼 값이 있다.
- 가이드(`web/guide.html`·`docs/guide.md`)에는 이 판을 아직 안 적었다.

### 시험

`python3 tests/ git_panel` **32/32**, `dialog_safe`·`screen_lexicon`·
`dialog_voice`·`event_target_gate` 통과. 전체 `python3 tests/ --jobs 4` 는
`dashboard_chat`(5초 타임아웃)과 `port_pool`(bind 경합)만 부하에서 흔들렸고
**단독으로 각각 25/25 · 29/29 통과** — 둘 다 이 변경과 무관한 병렬 흔들림이다.

### 만진 파일

`web/app/repo.js`(신규) · `web/css/density.css` · `web/app/settings.js` ·
`web/app/app.js` · `web/index.html` · `bin/s9`(git 판정·실행·API 셋) ·
`tests/test_git_panel.py`(신규) · `tests/test_dialog_safe.py`(대장 등재).
커밋하지 않았다.

[Image: assets/REQ-20260901-023-62x6/face-same.png]
[Image: assets/REQ-20260901-023-62x6/face-push.png]
[Image: assets/REQ-20260901-023-62x6/face-pull.png]
[Image: assets/REQ-20260901-023-62x6/face-split.png]
[Image: assets/REQ-20260901-023-62x6/face-dirty.png]
[Image: assets/REQ-20260901-023-62x6/face-jobs.png]
[Image: assets/REQ-20260901-023-62x6/face-denied.png]
[Image: assets/REQ-20260901-023-62x6/dlg-push.png]
[Image: assets/REQ-20260901-023-62x6/hand-hover-pull-locked.png]
[Image: assets/REQ-20260901-023-62x6/hand-focus-pull.png]
[Image: assets/REQ-20260901-023-62x6/state-busy.png]
[Image: assets/REQ-20260901-023-62x6/state-done-open.png]
[Image: assets/REQ-20260901-023-62x6/dpr-1_25.png]
[Image: assets/REQ-20260901-023-62x6/skin-terminal-carbon.png]

### 2026-09-02T12:00:08+09:00 subagent (agent: sub:frontend-developer:a880931a, by nicehugepark)

`[2026-09-02 11:57:22 KST - frontend-developer]`

REQ-20260901-023-62x6 구현을 마쳤습니다. Settings 좌측 목록에 여섯째 줄 **「저장소」**가 서고, `pull`·`push` 두 손잡이가 붙었습니다. 커밋하지 않았습니다.

### 리드 제약 일곱이 코드의 모양이 된 자리

git 을 부르는 문은 `/home/sjpark1/section9/bin/s9` 의 `git_run` **하나**이고, 거기에 허용 목록이 섭니다. 금지를 주석으로 적지 않고 **부르기 전에 터지게** 했습니다.

```python
GIT_FORBIDDEN = {"add", "commit", "stash", "checkout", "switch", "reset",
                 "restore", "clean", "rebase", "merge", ...,
                 "-f", "--force", "--hard", "--rebase", "--autostash", ...}
### 네트워크 셋은 **모양까지** 정확해야 지나간다
if verb == "pull" and shape != ("pull", "--ff-only"): raise ValueError(...)
if verb == "push" and shape != ("push",):             raise ValueError(...)
```

`pull` 이 허용 목록에 있다는 것만으로는 `pull --rebase` 를 못 막습니다 — 합치는 방식을 고를 인자 자체가 없어야 제약 ④가 지켜집니다. ②는 헤더 칩이 쓰는 그 숫자(`jobs_running` + 백그라운드 작업)로 둘 다 잠그고, ⑦은 손잡이만 admin·읽기는 전원·대리 중에는 둘 다 잠금입니다. 판정(`can`)은 서버 한 곳에 살고, **누른 순간 다시 판정**합니다.

### 실브라우저·시험이 잡아 고친 결함 넷

1. **잠긴 `pull` 이 손을 얹자 살아 있는 단추처럼 잉크로 반전됐습니다.** 스킨 규칙(`[data-skin] .acts button:hover`)이 같은 특이도로 뒤에 실려 제 잠금 규칙을 덮었습니다. `.acts.gacts` 로 한 급 조였습니다. 곁들여 배운 것: **CDP 캐시를 안 끄면 고친 CSS 를 안 받은 화면을 찍고 통과시킵니다** — 처음 두 번의 "고쳤다" 판정이 거짓이었습니다.
2. `_git_out` 이 stdout 을 통째로 `strip()` 해 `status --porcelain` 의 앞 상태 칸이 밀렸습니다 — 파일 이름이 `a.txt` 대신 `.txt` 로 나왔습니다. 잣대가 아니라 근원(`_git_out`/`_git_one` 분리)을 고쳤습니다.
3. **판을 다시 열면 손잡이가 한 벌씩 더 물렸습니다** — 구역을 갈아도 `#sview` 는 살아남습니다. push 한 번이 창을 둘 열던 자리.
4. `` /`([^`]+)`/g `` 의 백틱 **홀수 개**가 대장 스캐너를 desync 시켜, 제 확인 창이 `test_dialog_safe` 에서 통째로 사라졌습니다("화면에 없는 창"). 게으른 수량자로 짝을 맞췄고, 다음 사람을 위해 그 함정을 코드 옆에 적었습니다.

### 검증

- `python3 tests/ git_panel` **32/32** — 서버 갈래는 **진짜 저장소를 임시로 세워**(작업 저장소 + bare 원격 + 남의 clone) 재현했습니다. S12 는 임시 원격의 `main` 이 실제로 움직인 것을 확인합니다.
- `dialog_safe`(push 창 CENSUS 등재·`safe: true`) · `screen_lexicon`(음차 0) · `dialog_voice` · `event_target_gate` 통과.
- 실브라우저: 일곱 얼굴 캡처 + **실포인터(CDP)** hover·**실키보드 Tab** 으로 잠긴 단추 도달·busy 맥박·배율 1/1.25/1.5·스킨 10 × 톤 대조. 폴링은 판을 떠나고 11초 뒤 타이머 null 실측.
- 전체 `python3 tests/ --jobs 4` 는 `dashboard_chat`(5초 타임아웃)과 `port_pool`(bind 경합)만 부하에서 흔들렸고 **단독 25/25 · 29/29 통과** — 이 변경과 무관합니다.

### 리드가 판단할 것

- **서버 재기동이 필요합니다** — `bin/s9` 를 고쳤습니다(9909 는 손대지 않았고, 지금 리슨은 하는데 응답이 없습니다: `s9 doctor` 가 REQ-20260901-020-62x6 으로 지목하는 그 상태입니다).
- **designer 가 보고한 「좌측 목록 잘림」은 재현되지 않았습니다** — 여섯째 줄을 더한 뒤에도 800px 창에서 여섯 줄 + 「가이드 열기」가 전부 보입니다.
- calm 스킨에서만 잠김/살아 있음이 **테두리 밝기로만** 갈립니다(calm 이 모든 `.acts button` 을 `--muted` 로 그립니다). 카드의 `▶`·`⏸` 도 같은 형편이라 이 판만 다르게 하지 않았습니다 — 가르는 힘은 뜻 줄의 문법과 hover 반전 여부가 집니다.
- 판 머리 계약 줄이 어절 한가운데서 끊깁니다. **`.secnote` 를 쓰는 모든 판이 그렇습니다** — 한 판만 고치면 이웃이 서로 달라지므로 손대지 않았습니다. 별건 값이 있습니다.
- 가이드(`web/guide.html`·`docs/guide.md`)에는 아직 안 적었습니다.

### 만진 파일

신규 `/home/sjpark1/section9/web/app/repo.js` · `/home/sjpark1/section9/tests/test_git_panel.py`
수정 `/home/sjpark1/section9/bin/s9` · `/home/sjpark1/section9/web/css/density.css` · `/home/sjpark1/section9/web/app/settings.js` · `/home/sjpark1/section9/web/app/app.js` · `/home/sjpark1/section9/web/index.html` · `/home/sjpark1/section9/tests/test_dialog_safe.py`

기록은 `REQ-20260901-023-62x6` 에 `--label tdd`(개정·전부 체크)와 `--label response`(캡처 14장 첨부)로 남겼습니다. 임시 서버·캡처 브라우저는 전부 거뒀습니다(고아 0 · 헤드리스 0).

## History
- 2026-09-01T19:44:23+09:00 created by nicehugepark (status: open)
- 2026-09-01T19:45:39+09:00 status: open -> in-progress (by nicehugepark) — 사용자: 022 설정 화면에 같이 붙으면 좋겠다. 설계·문구를 먼저 세우고, 구현은 022 프론트가 착지한 뒤에 붙인다(같은 파일 경합 회피)
- 2026-09-02T07:03:39+09:00 status: in-progress -> blocked (by nicehugepark) — 낱말 판정 대기 — 사용자가 pull·push 번역을 반려했다. 화면 전체 용어 재판정(REQ-20260902-002-62x6)이 끝나야 단추·상태 문구가 정해진다. 해제 조건: 002 의 용어 사전 확정
- 2026-09-02T10:12:41+09:00 status: blocked -> in-progress (by nicehugepark) — [auto] 의존 해제: REQ-20260902-002-62x6 완료로 자동 재개
- 2026-09-02T10:13:03+09:00 status: in-progress -> blocked (by nicehugepark) — 002 완료로 의존이 자동 해제됐으나 아직 착수 불가 — 005 개칭 적용이 같은 파일(설정 판 workercfg.js·settings.js)을 고치는 중이다. 해제 조건: 005 적용 커밋. 낱말은 확정됐다(단추 pull/push, 셈 「push 할 것 N개 · pull 할 것 N개」)
- 2026-09-02T10:49:53+09:00 status: blocked -> in-progress (by nicehugepark) — 해제 조건 충족 — 005 개칭 적용이 커밋됐다(6a82f33). 낱말 확정: 단추 pull/push(원어·mono), 셈 「push 할 것 N개 · pull 할 것 N개」, 덩이 이름 「GitHub 과 주고받기」. 리드 제약 7개·designer 자리 판정·ux-writer 문안은 이 문서의 9/1 노트에 있다. 구현 착수(frontend-developer 위임)
