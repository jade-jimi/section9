---
id: REQ-20260901-013-62x6
type: request
title: 계정 목록 깜빡임
summary: 계정 전환 창의 계정 줄이 없다가 나타나거나 있다가 사라진다(캡처 첨부) — 5초 폴이 창을 다시 그리며 목록이 흔들리는 것으로 의심
goal: 계정 창을 열어 둔 채로 목록이 늘거나 줄지 않고, 깜빡임 재현 경로가 회귀로 봉쇄된다
status: in-progress
size: S
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: 3ed7d9fd
agents: ["sub:frontend-developer:ab4871e8", "sub:frontend-developer", "sub:subagent:a867ca02"]
contributions: [{"actor": "sub:frontend-developer:ab4871e8", "item": "013 계정 목록 깜빡임 수정", "started": "2026-09-01T14:13:43+09:00", "ended": "2026-09-01T14:39:10+09:00", "result": "stalled", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/3ed7d9fd-1679-421a-a506-cec3973f8c08/tasks/ab4871e8c33ae0864.output", "reason": "189초째 진전 없음 (기준 180초)"}, {"actor": "sub:frontend-developer", "item": "원인 — 「한 번 끊긴 연결」을 창이 「계정이 없다」로 옮긴다", "started": "2026-09-01T14:24:04+09:00", "ended": "2026-09-01T14:24:04+09:00", "result": "done", "transcript": ""}, {"actor": "sub:frontend-developer", "item": "TDD 시나리오 (계정 목록 깜빡임)", "started": "2026-09-01T14:24:24+09:00", "ended": "2026-09-01T14:24:24+09:00", "result": "done", "transcript": ""}, {"actor": "sub:frontend-developer", "item": "고친 것과 재 본 것", "started": "2026-09-01T14:47:39+09:00", "ended": "2026-09-01T14:47:39+09:00", "result": "done", "transcript": ""}, {"actor": "sub:subagent:a867ca02", "item": "Attaching captures to REQ-20260901-013-62x6 note", "started": "2026-09-01T14:47:52+09:00", "ended": "2026-09-01T14:47:52+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/3ed7d9fd-1679-421a-a506-cec3973f8c08/subagents/agent-a867ca02ecf46a7b7.jsonl"}, {"actor": "sub:frontend-developer", "item": "TDD 시나리오 — 검증 결과 (개정)", "started": "2026-09-01T14:47:56+09:00", "ended": "2026-09-01T14:47:56+09:00", "result": "done", "transcript": ""}]
project: section9
parent: REQ-20260901-011-62x6
relates_why: {}
tags: ["session", "dashboard", "assets", "attached", "png", "image"]
created: 2026-09-01T14:12:23+09:00
status_since: 2026-09-01T14:12:43+09:00
updated: 2026-09-01T14:47:56+09:00
priority: 50
---

## Original

사용자: 계정 전환을 하는 화면 자체도 이상하다. 계정이 없다가 나타나거나 있는데 사라지거나 한다.

## Notes

### 2026-09-01T14:24:04+09:00 response (agent: sub:frontend-developer, by nicehugepark)

### 원인 — 「한 번 끊긴 연결」을 창이 「계정이 없다」로 옮긴다

계정 창은 **열 때 한 번만** 목록을 받는다(`claudeAccountSwitch` → `ccFetch("/api/accounts", 5000)`).
`ccFetch` 는 재시도가 없어, 요청이 한 번 끊기면 `d = null` 이 되고 `acctShape(null)` 이
**줄을 통째로 지운 창**(계정 0줄 · "계정 목록을 받지 못했습니다")을 그린다. 다음에 열면
멀쩡히 다시 뜬다 — 사용자가 본 "없다가 나타나거나 있다가 사라진다" 가 이것이다.

**실측 (2026-09-01 14:2x, 실서버 9909)**

- `/api/accounts` 60회 순차 호출 → **4회 ConnectionResetError (6.7%)**, 나머지 56회는
  4줄로 모양이 완전히 동일. 즉 목록 자체는 흔들리지 않는다 — 흔들리는 것은 **연결**이다.
- 같은 시각 `/api/whoami` 0.11s · `/api/claude/usage` 0.03s 인데
  `/api/accounts` 와 `/api/chat/target` 은 **8초를 넘겨도 응답 없음**인 구간이 있었다.
  둘은 `session_cfg_dir()`(/proc 읽기) + `account_rows()`(프로필 전수 + `_live_cfg_dirs`
  프로세스 전수 + settle 시 파일시스템 rename)를 지나는 유이한 경로다.

이 저장소는 **같은 벼랑을 이미 문서화·처방**해 두었다(DOC-20260827-004-62x6 · REQ-20260829-019-62x6):
이 환경의 루프백이 같은 순간에 도착한 연결을 자르고, 처방은 **클라이언트 재시도
(120·320ms + 지터)** 다. 그림(`attImg`)과 부트 보급(`loadSupply`)은 그 처방을 받고 있는데
**계정·세션 고르기 창만 맨 `ccFetch` 한 발**로 남아 있었다.

**아니었던 것 (배제)**

- 열려 있는 창을 배경 폴이 다시 그리는 결함은 **없다**. `dlg.innerHTML` 을 쓰는 곳은
  `s9dlg`/`s9choose` 둘뿐이고, 5초 `termTargetLoop` 도 60초 `refreshUsage` 도 창을
  건드리지 않는다. `?dlg=acctlive` 로 창을 열고 1.8s·6.8s·12.8s·18.8s 에 찍은 캡처가
  네 장 모두 같은 4줄이었다.
- 목록 내용이 시각마다 다른 것(사용자 캡처 3줄 ↔ 지금 4줄)은 별개의 서버 사실이다:
  `@home` 은 `claude_home()` = **serve 프로세스가 물려받은 `CLAUDE_CONFIG_DIR`** 이라
  대시보드를 어느 계정에서 띄웠느냐로 바뀌고, 같은 메일이면 `@home` 줄과 프로필 줄이
  한 줄로 합쳐진다(`also`). 이건 bin/s9 쪽 판단이라 손대지 않는다 — 아래 「리드에게」.

### 리드에게 (내 수정 범위 밖 · bin/s9)

`/api/accounts` 와 `/api/chat/target` 이 매 호출마다 프로세스 전수(`proc_env_table`)와
프로필 전수를 돈다. 5초 폴이 그 위에 있어 서버가 바쁠 때 둘만 8초+ 로 늘어진다.
계정 창 깜빡임의 절반은 이 무게이고, 나머지 절반(REQ-011 의 live/stale 깜빡임)도 같은
경로를 공유한다. 캐시/경량화는 서버 쪽 판단이라 남겨 둔다.

### 2026-09-01T14:24:24+09:00 tdd (agent: sub:frontend-developer, by nicehugepark)

### TDD 시나리오 (계정 목록 깜빡임)

- [ ] S1. 정상 — 계정 창은 목록을 재시도가 붙은 길(`ccFetchTry`)로 받는다. `claudeAccountSwitch` 안에 맨 `ccFetch("/api/accounts"` 호출이 남아 있지 않다.
- [ ] S2. 재시도 — `ccFetchTry` 는 답이 `null` 이면 백오프(120·320ms)에 **지터를 섞어** 다시 걸고, 한 번이라도 답이 오면 즉시 그 답을 돌려준다(성공 뒤 더 안 건다).
- [ ] S3. 실패 경로 — 세 번 다 실패해야 `null` 이다. 그때만 창이 "받지 못했습니다"를 말하고, 그 창은 막다른 길이 아니다: `다시 받기` 한 손잡이가 서고 `＋ 계정 추가`(서버가 안 답하는 판에서는 눌러도 안 되는 것)는 서지 않는다.
- [ ] S4. 경계 — 재시도 대기는 사람이 기다리는 시간이라 한도가 있다: 시도당 대기가 5초를 넘지 않고, 시도 수는 3을 넘지 않는다(최악 8초 안).
- [ ] S5. 회귀(이 건의 핵심) — 열어 둔 창을 배경이 다시 그리지 않는다: `claudeAccountSwitch`·`acctShape`·`acctItems` 가 어떤 `setInterval` 안에서도 불리지 않고, 창을 그리는 `dlg.innerHTML` 대입은 `s9dlg`/`s9choose` 두 곳뿐이다.
- [ ] S6. 회귀(형제 자리) — 같은 결함이 세션 고르기 창(`/api/sessions`)과 고른 세션에 붙는 길(`/api/chat/target?sid=`)에도 있었다. 셋 다 재시도로 연다 — 한 곳만 고치면 다음 사람이 옆자리에서 넘어진다.
- [ ] S7. 손 없이 재현 — `?apifail=accounts:once` 로 첫 요청만 빗나가게 하면 창이 **정상으로 채워지고**(재시도가 메웠다), `?apifail=accounts` 면 끝내 못 받은 창이 선다. boot.js 가 이미 쓰는 그 어휘를 그대로 쓴다(새 스위치를 만들지 않는다).
- [ ] S8. 실브라우저 — 창을 열어 둔 채 10초 이상 관찰해 줄 수·표식이 그대로임을 캡처로 확인한다.

### 2026-09-01T14:47:39+09:00 response (agent: sub:frontend-developer, by nicehugepark)

### 고친 것과 재 본 것

**고친 곳은 하나다** — 창이 목록을 받는 그 한 발. `web/app/terminal.js` 에
`ccFetchTry(url, ms, key)` 를 세우고(시도 3 · 물러섬 120·320ms + 지터 — attach.js 가
실측으로 정한 그 자), 고르는 창 셋이 그 길로 지나간다: 계정(`/api/accounts`),
세션(`/api/sessions`), 고른 세션에 붙는 길(`/api/chat/target?sid=`). 폴(5초
termTargetLoop)은 여기를 안 지난다 — 폴의 재시도는 다음 tick 이고, 못 받은 한 번에는
`offline` 이라는 설계된 얼굴이 있다. 여기는 사람이 눌러 기다리는 자리라 얼굴이 없었다.

**같은 부하에서 옛 길과 새 길을 나란히 잰 값** (실브라우저 안에서, 같은 순간에 8개를
함께 던져 이 환경의 벼랑을 실제로 만들며 40회 반복 × 2세트):

| | 목록을 못 받은 횟수 |
|---|---|
| 한 발 `ccFetch` (옛 길) | 6 / 40 · 3 / 40 |
| 세 발 `ccFetchTry` (새 길) | **0 / 40 · 0 / 40** |

**창을 열어 둔 채 21초** (CDP 로 브라우저 하나를 붙들고 1.5초마다 목록을 읽음):
표본 14개, **서로 다른 모습 1개** — 줄 수·표식·흐림이 하나도 안 움직였다.
`● claude01…(기본/지금 이것) · ○ claude02… · · 새-계정(로그인 전) · · 새-계정-3(로그인 전)`.

**끝내 못 받은 창에 나가는 문을 달았다.** 여태 이 처지는 줄도 없고 할 수 있는 일도
없었다(닫기뿐). 이제 `↻ 계정 목록 다시 받기` 하나가 선다. `＋ 계정 추가` 는 안 세운다 —
서버가 답하지 않는 판에서는 그것도 눌러야 안 되는 일이라, 권하면 창이 두 번 거짓말을
한다. 문구는 한 사실을 한 번씩만 말하게 갈랐다: 무엇이 안 됐나(설명) / 언제 되나(줄) /
무엇을 누르나(손잡이). 이 파일이 이미 세워 둔 규칙("한 창이 같은 사실을 세 번 말하면
사람은 셋 다 흘려 읽는다")을 내가 어겼다가 되돌린 자리다.

**손 없이 재현된다** — 새 스위치를 짓지 않고 boot.js 의 `?apifail` 어휘를 그대로 쓴다.
`?dlg=acctlive&apifail=accounts:once` 면 첫 요청이 끊겼는데도 목록이 정상으로 채워지고
(재시도가 메웠다), `?apifail=accounts` 면 끝내 못 받은 창이 선다.

**배율 불변** — 새 기하를 만들지 않았다(기존 `.dlgact` 컴포넌트 재사용). 그래도 1 ·
1.25 · 1.5 로 찍어 렌더 화소를 눈으로 봤다: 세 배율 모두 같은 모양, 포커스 링도 1px
정상. 캡처 첨부.

**시험**: `python3 tests/ account_flicker` 12건 신설(Red 9건 확인 후 Green).
회귀 확인 `python3 tests/ account_flicker account_switch account_and_model session_pick
dead_end dialog_safe screen_lexicon web_split` → **119건 OK**. 전체 스위트는 리드 몫이라
안 돌렸다.

[Image: assets/REQ-20260901-013-62x6/watch-open.png]
[Image: assets/REQ-20260901-013-62x6/final-recovered.png]
[Image: assets/REQ-20260901-013-62x6/final-lost.png]
[Image: assets/REQ-20260901-013-62x6/scale-125.png]
[Image: assets/REQ-20260901-013-62x6/scale-15.png]

### 2026-09-01T14:47:52+09:00 subagent (agent: sub:subagent:a867ca02, by nicehugepark)

Attaching captures to REQ-20260901-013-62x6 note

### 2026-09-01T14:47:56+09:00 tdd (agent: sub:frontend-developer, by nicehugepark)

### TDD 시나리오 — 검증 결과 (개정)

- [x] S1. 정상 — 계정 창이 `ccFetchTry("/api/accounts", 2500, "accounts")` 로 받는다. 맨 `ccFetch` 는 남아 있지 않다.
- [x] S2. 재시도 — 답이 `null` 이면 120·320ms + 지터로 다시 걸고, 한 번이라도 오면 즉시 돌려준다(`if (d != null) return d;`). 실브라우저 실측: 같은 부하에서 옛 길 6/40·3/40 실패 → 새 길 **0/40·0/40**.
- [x] S3. 실패 경로 — 세 번 다 실패해야 `null`. 그때만 창이 "받지 못했습니다"를 말하고, `↻ 계정 목록 다시 받기` 하나가 선다(`＋ 계정 추가` 는 안 선다). 캡처 확인.
- [x] S4. 경계 — 시도 3회 · 시도당 2500ms(≤5000). 최악 대기 약 7.9초.
- [x] S5. 회귀 — `claudeAccountSwitch`·`acctShape`·`acctItems` 가 어떤 `setInterval` 안에서도 안 불린다. `dlg.innerHTML` 대입은 `s9dlg`·`s9choose` 둘뿐. 시험이 지킨다.
- [x] S6. 회귀(형제) — 세션 고르기(`/api/sessions`)와 고른 세션에 붙는 길(`/api/chat/target?sid=`)도 같은 재시도로 연다.
- [x] S7. 손 없이 재현 — `?apifail=accounts:once` → 정상으로 채워짐 · `?apifail=accounts` → 못 받은 창. 새 스위치 없이 boot.js 어휘 재사용.
- [x] S8. 실브라우저 — 창을 21초 열어 둔 채 1.5초마다 14회 표본, **서로 다른 모습 1개**(줄 수·표식 불변). 배율 1·1.25·1.5 캡처도 같은 모양.

`python3 tests/ account_flicker` 12건 신설 · 회귀 묶음 119건 OK.

## History
- 2026-09-01T14:12:23+09:00 created by nicehugepark (status: open)
- 2026-09-01T14:12:43+09:00 status: open -> in-progress (by nicehugepark) — 011 소집 라운드에서 병렬 착수
