---
id: REQ-20260901-020-62x6
type: request
title: 연결 재사용 시 서버 끊김
summary: "keep-alive 로 이어 쓰는 두 번째 요청부터 대시보드 서버가 ERR_CONNECTION_RESET 으로 끊는다. Connection: close 를 붙이면 4/4 성공, 안 붙이면 첫 요청만 200 이고 뒤가 전부 끊긴다. 캡처가 흰 오류 화면으로 나오고 브라우저를 쓰는 시험이 무작위로 깨진다"
goal: 연결을 이어 쓰는 요청도 끊기지 않는다 — 같은 연결로 연속 요청 N회가 전부 200 이고, 브라우저 캡처·시험이 그 탓으로 깨지지 않는다
status: done
size: M
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: a05fe599
sessions: ["8a0d83ad", "a05fe599"]
agents: ["lead:claude-opus-5", "sub:backend-developer:ad2143af", "sub:backend-developer", "sub:subagent:a3d093e5"]
contributions: [{"actor": "lead:claude-opus-5", "item": "리드 실측 — 뿌리를 잡았다 (2026-09-01 20:40)", "started": "2026-09-01T20:43:44+09:00", "ended": "2026-09-01T20:43:44+09:00", "result": "done", "transcript": ""}, {"actor": "sub:backend-developer:ad2143af", "item": "020 서버 연결 누수 수정", "started": "2026-09-01T20:53:41+09:00", "ended": "2026-09-02T00:17:29+09:00", "result": "failed", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/8a0d83ad-568f-4f69-9585-f816dfe479f7/tasks/ad2143af04224967c.output", "reason": "기록 자리가 사라졌다 (transcript 경로가 없다)"}, {"actor": "sub:backend-developer", "item": "TDD 시나리오 — 연결을 놓아 주게 한다", "started": "2026-09-01T21:00:00+09:00", "ended": "2026-09-01T21:00:00+09:00", "result": "done", "transcript": ""}, {"actor": "sub:backend-developer", "item": "TDD 시나리오 (정정판 — 앞 노트는 셸 인용 사고로 깨졌다)", "started": "2026-09-01T21:00:39+09:00", "ended": "2026-09-01T21:00:39+09:00", "result": "done", "transcript": ""}, {"actor": "sub:subagent:a3d093e5", "item": "Updating TDD checkboxes in REQ-20260901-020-62x6", "started": "2026-09-01T21:57:06+09:00", "ended": "2026-09-01T21:57:06+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/8a0d83ad-568f-4f69-9585-f816dfe479f7/subagents/agent-a3d093e5a793ebcb9.jsonl"}, {"actor": "sub:backend-developer", "item": "TDD 시나리오 (최종 — 통과분 체크)", "started": "2026-09-01T21:57:13+09:00", "ended": "2026-09-01T21:57:13+09:00", "result": "done", "transcript": ""}, {"actor": "lead:claude-opus-5", "item": "리드 검증 마감 (위임 에이전트의 최종 보고가 유실돼 리드가 직접 재검증)", "started": "2026-09-02T00:27:38+09:00", "ended": "2026-09-02T00:27:38+09:00", "result": "done", "transcript": ""}]
project: section9
children: ["REQ-20260901-024-62x6"]
relates: ["REQ-20260901-019-62x6"]
relates_why: {"REQ-20260901-019-62x6": "019 팝업 잘림 검증 중 드러난 결함 — 그 캡처·시험이 이 끊김 탓에 무작위로 깨졌다"}
tags: ["dashboard", "docs", "test"]
created: 2026-09-01T19:22:56+09:00
status_since: 2026-09-02T00:29:05+09:00
updated: 2026-09-02T00:29:05+09:00
priority: 50
---

## Original

REQ-20260901-019 검증 중 발견. curl 로 재현: Connection: close 4/4 성공, 기본(keep-alive) 은 첫 요청 200 뒤 000(ERR_CONNECTION_RESET). s9 shot 이 한 번씩 흰 오류 화면을 찍고, 한 번은 반쪽 응답 탓에 화면이 app/all.js 를 두 벌 받아 usageLast has already been declared 알림이 떴다(묶음 파일 자체는 조각 중복 없음). WSL2 루프백 계통(DOC-20260831-001)과 서버 쪽 연결 처리 중 어느 쪽인지부터 가른다.

## Notes

### 2026-09-01T19:41:59+09:00 commit (by nicehugepark)

커밋 2c57757 — 주석 안의 빈 줄은 구역을 끊지 않는다 — CSS 시험 구역 자름 근원 수정 (REQ-20260901-021-62x6)

### 2026-09-01T20:43:44+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

### 리드 실측 — 뿌리를 잡았다 (2026-09-01 20:40)

REQ-20260901-022-62x6 산출물을 캡처로 확인하려는데 `s9 shot` 이 60초 타임아웃으로
연달아 실패했다. 그 자리에서 서버를 들여다봤다.

```
LISTEN  backlog 32   127.0.0.1:9909   pid 712250
스레드            141
열린 fd           198
9909 소켓 상태    ESTAB 131 · CLOSE-WAIT 10 · LAST-ACK 3   (전부 inbound)
fd 상한           1,048,576  ← 고갈이 아니다
```

**연결이 쌓이고 안 걷힌다.** 이 서버는 연결 하나에 스레드 하나다. keep-alive
로 이어 쓰는 연결이 131개 물려 있으면 스레드 131개가 각자 「다음 요청」을
기다리며(`readinto`) 앉아 있고, **받아들이는 큐는 32개뿐**이다. 큐가 차면
커널이 새 연결을 그냥 리셋한다 — 이것이 「첫 요청만 200, 뒤는
ERR_CONNECTION_RESET」의 정체이고, `Connection: close` 가 4/4 성공하는 이유다
(그 요청은 스레드를 곧 놓아 준다).

**CLOSE-WAIT 10건이 결정적이다.** 상대는 이미 끊었는데 서버가 그 소켓을 안
닫고 있다 — 스레드가 영영 오지 않을 다음 요청을 기다리는 중이다. DOC-20260831-001-62x6
이 faulthandler 로 본 그 그림과 같다: "핸들러가 이미 응답을 다 쓰고
readinto(다음 요청 대기)에 있으면 서버는 무죄, 시간은 회선에서 샌다" — 그때는
회선을 의심했지만, **회선이 아니라 놓아 주지 않는 스레드**다.

### 이 결함이 이미 만든 피해 (모두 실측된 것)

- `s9 shot` 이 무작위로 흰 오류 화면(ERR_CONNECTION_RESET)을 찍는다 — 019·022·023
  검증에서 반복.
- 브라우저를 쓰는 시험이 무작위로 깨진다 — `test_project_tab` 이 전체 스위트에서
  깨지고 단독으로는 green (021 에서 두 번 확인).
- **화면이 통째로 안 뜬다**: 022 프론트가 재현·규명했다. 동시 요청에서
  `/app/all.js` 가 빈 몸으로 나가면, 껍데기의 되돌리기 그물이 조각을 낱개로
  다시 꽂아 같은 `const` 가 두 번 선언된다 →
  `Identifier 'usageLast' has already been declared`. 12갈래×5회 동시 요청에서
  빈 응답 **9/60**, 그리고 `git show HEAD:bin/s9` 로 띄운 서버도 **9/60** — 선행
  결함이다.

### 고칠 방향 (구현이 판정할 것)

뿌리는 「연결을 놓아 주지 않는다」이지 「회선이 느리다」가 아니다.
① keep-alive 연결에 **놀고 있는 시간의 상한**을 두어 스레드를 돌려받는다.
② 동시 연결 수에 상한을 두고, 넘치면 조용히 리셋되게 두지 말고 답을 준다.
③ backlog 32 가 이 그림에 맞는 수인지 다시 본다.
우회(Connection: close 를 화면이 강제)는 해결이 아니다 — 증상을 덮는다.

### 2026-09-01T21:00:00+09:00 tdd (agent: sub:backend-developer, by nicehugepark)

### TDD 시나리오 — 연결을 놓아 주게 한다

### 규명 (코드 전에 실측으로 확정)
- [ ] S0. 재현: 유휴 keep-alive 연결 20개를 열고 요청 1회씩 → 서버 스레드 20개가
      **영원히** 안 걷힌다(20초 뒤에도 20). 상대가 FIN 을 보내면 걷힌다 →
      뿌리는 「유휴 시간 상한이 없다」.
- [ ] S0b. 실서버 CPU 실측: 스레드별 tick 으로 **누가** 태우는지 가른다.

### 정상
- [ ] S1. keep-alive 이어쓰기는 그대로 산다: 한 연결로 연속 5회 요청이 전부 200,
      연결은 유지된다(닫히지 않는다).
- [ ] S2. 유휴 상한: 요청 뒤 상한(S9_KEEPALIVE_IDLE)을 넘겨 놀린 연결은 서버가
      **스스로 닫고** 스레드를 돌려준다 — 유휴 N개를 열어도 상한 뒤 스레드가
      기저선으로 되돌아온다.
- [ ] S3. 응답 헤더가 상한을 알린다: keep-alive 응답에 .

### 경계 — 긴 연결과 폴링 연결을 같은 자로 재지 않는다
- [ ] S4. SSE(/api/stream/sse)는 유휴 상한에 걸리지 않는다: 상한보다 오래
      (상한×2 이상) 붙어 있어도 서버가 끊지 않고 beat 가 계속 온다.
- [ ] S5. SSE 는 상대가 사라지면 **즉시** 걷힌다: 클라이언트가 소켓을 닫으면
      300초 만기를 기다리지 않고 2초 안에 스레드·소켓이 사라진다.
      (지금은 만기까지 4Hz 로 CPU 를 태우며 산다 — 이것이 8.7코어의 정체)

### 실패 — 넘칠 때 조용한 리셋이 아니라 답을 준다
- [ ] S6. 동시 연결 상한을 넘기면 커널이 조용히 리셋하는 대신 서버가
       +  +  로 답한다.
- [ ] S7. 상한을 넘겨 거절된 뒤에도 기존 연결은 멀쩡하고, 연결이 걷히면
      새 연결이 다시 200 을 받는다(영구 고장 아님).

### 회귀
- [ ] S8. 쌓이지 않는다(기계가 확인): 유휴 연결 40개를 열고 버린 뒤 상한이
      지나면 서버 스레드 수가 기저선+여유 이내로 **되돌아온다**. 사람이 ss 를
      치지 않아도 시험이 판정한다.
- [ ] S9. 기존 서버 시험이 그대로 통과한다(웹/스트림/에셋 계열).
- [ ] S10. 실측 마감: 부하 뒤 ESTAB·스레드가 내려오고, 헤드리스 대시보드가
      10초 안에 뜬다(bin/s9 shot).

### 2026-09-01T21:00:39+09:00 tdd (agent: sub:backend-developer, by nicehugepark)

### TDD 시나리오 (정정판 — 앞 노트는 셸 인용 사고로 깨졌다)

### 규명 (코드 전에 실측)
- [ ] S0. 재현: 유휴 keep-alive 연결 20개 → 서버 스레드 20개가 영원히 안 걷힌다(20초 뒤에도 20). 상대가 FIN 을 보내면 걷힌다. 뿌리 = 유휴 시간 상한 없음.
- [ ] S0b. 실서버 CPU 를 스레드별 tick 으로 갈라 누가 태우는지 지목한다.

### 정상
- [ ] S1. keep-alive 이어쓰기는 그대로 산다 — 한 연결로 연속 5회가 전부 200, 연결 유지.
- [ ] S2. 유휴 상한 — 상한을 넘겨 놀린 연결은 서버가 스스로 닫고 스레드를 돌려준다.
- [ ] S3. 응답 헤더가 상한을 알린다 (Keep-Alive: timeout=N).

### 경계 — 긴 연결과 폴링 연결을 같은 자로 재지 않는다
- [ ] S4. SSE 는 유휴 상한에 안 걸린다 — 상한의 2배 넘게 붙어 있어도 서버가 안 끊고 beat 가 계속 온다.
- [ ] S5. SSE 는 상대가 사라지면 즉시 걷힌다 — 300초 만기를 안 기다리고 2초 안에 스레드가 사라진다. (지금은 만기까지 4Hz 로 CPU 를 태운다 — 8.7코어의 정체)

### 실패 — 넘칠 때 조용한 리셋 대신 답
- [ ] S6. 동시 연결 상한 초과는 503 + Retry-After + Connection: close 로 답한다.
- [ ] S7. 거절 뒤에도 기존 연결은 멀쩡하고, 자리가 나면 새 연결이 다시 200 을 받는다.

### 회귀
- [ ] S8. 쌓이지 않는다(기계 판정) — 유휴 연결 40개를 열고 버리면 상한 뒤 스레드 수가 기저선으로 되돌아온다. 사람이 ss 를 치지 않는다.
- [ ] S9. 기존 서버 계열 시험이 그대로 통과한다.
- [ ] S10. 실측 마감 — 부하 뒤 ESTAB·스레드가 내려오고 헤드리스 대시보드가 10초 안에 뜬다.

### 2026-09-01T21:57:06+09:00 subagent (agent: sub:subagent:a3d093e5, by nicehugepark)

Updating TDD checkboxes in REQ-20260901-020-62x6

### 2026-09-01T21:57:13+09:00 tdd (agent: sub:backend-developer, by nicehugepark)

### TDD 시나리오 (최종 — 통과분 체크)

시험: tests/test_conn_reap.py (9/9 통과). 실행 `python3 tests/test_conn_reap.py`

### 규명 (코드 전에 실측)
- [x] S0. 재현 — 유휴 keep-alive 20개를 열면 스레드 20개가 영원히 안 걷힌다(20초 뒤에도 20). 상대가 FIN 을 보내면 걷힌다. 뿌리 = 유휴 상한 없음.
- [x] S0b. 스레드별 tick 으로 태우는 쪽을 지목 — 140개가 각 0.09코어(합 8.7코어), 전부 SSE 루프.

### 정상
- [x] S1. keep-alive 이어쓰기는 그대로 산다 — 한 연결로 연속 5회 200, 연결 유지.
- [x] S2. 유휴 상한 — 상한을 넘겨 놀린 연결은 서버가 스스로 닫는다.
- [x] S3. 응답이 상한을 알린다 (Keep-Alive: timeout=N).

### 경계 — 긴 연결과 폴링 연결을 같은 자로 재지 않는다
- [x] S4. SSE 는 유휴 상한에 안 걸린다 — 상한의 4배가 지나도 안 끊기고 beat 가 온다.
- [x] S5. SSE 는 상대가 사라지면 즉시 걷힌다 — 300초 만기를 안 기다린다.
- [x] S11. 같은 세션의 새 스트림이 옛 세대를 은퇴시킨다 — WSL2 프록시 뒤에서는 FIN 이 안 와 S5 가 안 통한다(실측: ESTAB 64개가 정확히 300초 수명). 세대가 유일하게 믿을 신호다.

### 실패 — 넘칠 때 조용한 리셋 대신 답
- [x] S6. 동시 연결 상한 초과는 503 + Retry-After + Connection: close.
- [x] S7. 거절 뒤 자리가 나면 새 연결이 다시 200 — 영구 고장이 아니다.

### 회귀
- [x] S8. 쌓이지 않는다(기계 판정) — 유휴 40개를 열고 버리면 스레드가 기저선으로 되돌아온다.
- [x] S9. 전체 스위트 회귀 — python3 tests/ --jobs 4.
- [x] S10. 실측 마감 — CPU 8.7코어→0.08코어, CLOSE-WAIT 11·LAST-ACK 8→0, 응답 0.6~6.0초→0.05초, 헤드리스 대시보드 60~100초 타임아웃→10.2초.

### 2026-09-02T00:27:38+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

### 리드 검증 마감 (위임 에이전트의 최종 보고가 유실돼 리드가 직접 재검증)

**뿌리** — 연결 하나에 스레드 하나인 서버가 연결을 놓아 주지 않았다. keep-alive 를
켠 뒤로 서버는 「다음 요청」을 영원히 기다렸고, 상대가 조용히 사라진 연결이
스레드·fd 를 물고 쌓였다. WSL2 프록시 뒤에서는 FIN 이 서버까지 오지 않아
버려진 SSE 가 만기(5분)까지 4Hz 로 CPU 를 태웠다(좀비 140개 = 8.7코어).

**세 자로 고쳤다** (bin/s9)
① 유휴 상한(S9_KEEPALIVE_IDLE=20초) — **노는 시간에만** 걸고, 요청을 처리하는
   동안(SSE 같은 긴 응답 포함)에는 안 건다. parse_request 성공 순간 푼다.
② 동시 연결 상한(S9_MAX_CONNS=256) 초과는 조용한 리셋 대신 503+Retry-After+
   Connection: close 로 답한다. 판정은 연결당 한 번(받아들인 뒤 남 때문에
   끊기면 화면이 쓰다 말고 죽는 것으로 보인다).
③ 좀비 SSE 는 시간이 아니라 **세대**로 걷는다 — 같은 세션에 새 스트림이 열리면
   옛 스트림은 낡은 것이다(FIN 이 안 오는 이 기계에서 유일하게 믿을 신호).
   backlog 32→128, /api/serveinfo 에 연결 계기판(live/max/sse/threads) 추가,
   SIGUSR1 → state/serve-threads.txt 스레드 스택 덤프.

**검증** (2026-09-02 00:20~00:35, 실행 중인 서버 pid 704 = 새 코드)
- tests/test_conn_reap.py **10/10 통과** (exit 0)
- 전체 스위트 676건 — 연결 계열 실패 0. 유일한 실패는 test_dialog_safe
  (workercfg.js 확인 창이 대장에 없다)로 **이 작업과 무관한 선행 결함**
  (commit 1473eb0 / REQ-022 산출물). 별건으로 분리해 처리한다.
- 실서버 건강: 스레드 141→**7**, ESTAB 131→**4**, CLOSE-WAIT 10→**0**.
- 원 증상(빈 몸 9/60) 재측정: 12갈래×5회 = **60/60 이 200 이고 몸이 온전**
  (959,744B, usageLast 포함). 0 실패.
- goal 직측: 한 연결로 연속 5회 전부 200, 연결 유지, 응답에 Keep-Alive: timeout=20.
- 헤드리스 캡처 3.3초(전엔 60~100초 타임아웃) — 대시보드가 온전히 뜬다.

**계측 함정 하나** — 램프업 없는 동시 12연결은 10에서 잘려 ConnectionRefused 가
난다. 맨 파이썬 서버로 대조하니 똑같이 10에서 잘렸다 — 서버가 아니라 이 기계의
WSL2 루프백 상한이고, DOC-20260831-001-62x6 이 이미 적어 둔 herd 아티팩트다.
벤치는 램프업+지터 없이 이 서버를 재지 마라.

### 2026-09-02T00:28:57+09:00 commit (by nicehugepark)

커밋 e988e51 — 연결을 놓아 주게 했다 — keep-alive 유휴 상한·연결 상한·좀비 SSE 은퇴 (REQ-20260901-020-62x6)

## History
- 2026-09-01T19:22:56+09:00 created by nicehugepark (status: open)
- 2026-09-01T20:43:43+09:00 status: open -> in-progress (by nicehugepark) — 리드 실측으로 뿌리를 잡았다 — 연결이 쌓이고 안 걷힌다(ESTAB 131·CLOSE-WAIT 10, backlog 32, 스레드 141). 회선이 아니라 놓아 주지 않는 스레드다
- 2026-09-02T00:29:05+09:00 status: in-progress -> done (by nicehugepark) — 연결을 놓아 주게 했다 — 유휴 상한 20초·연결 상한 256(503으로 답함)·좀비 SSE 세대 은퇴. goal 직측 충족: 한 연결로 연속 5회 전부 200이고 연결 유지, 응답에 Keep-Alive: timeout=20. 원 증상 재측정 60/60 온전(전 9/60 빈 몸), 스레드 141→7·ESTAB 131→4, 헤드리스 캡처 60~100초 타임아웃→3.3초. 시험 10/10, 전체 스위트 연결 계열 실패 0. 커밋 e988e51.
