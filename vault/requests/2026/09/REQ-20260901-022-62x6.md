---
id: REQ-20260901-022-62x6
type: request
title: 설정을 화면에서 바꾼다
summary: "auto_resume_gh 처럼 동작을 실제로 바꾸는 계정 설정들이 대시보드에서는 「기타: {...}」 JSON 한 줄로 보이기만 하고, 바꾸려면 터미널 명령을 외워야 한다. 각 값이 무슨 뜻인지도 화면이 말하지 않는다. Settings 안에 이 설정들을 뜻과 함께 켜고 끄는 자리를 만든다"
goal: 터미널을 열지 않고 대시보드 Settings 에서 계정 설정(자동 재개·적용·gh 권한·워크트리·모델·기록 등)을 뜻을 읽고 바꿀 수 있다 — 각 항목이 무엇을 여는 스위치인지 화면이 말하고, 되돌릴 수 없거나 권한을 여는 것은 그 무게가 보인다
status: review
size: M
user: nicehugepark
machine: DESKTOP-TEHV1KR
session: 8a0d83ad
agents: ["sub:designer:ac9f7eb4", "sub:ux-writer:aa92add5", "sub:tech-writer:a99e895f", "sub:translator:afe5a902", "sub:ux-writer", "sub:translator", "sub:subagent:a29ed724", "sub:tech-writer", "sub:subagent:af5245f7", "lead:claude-opus-5", "sub:designer", "sub:subagent:a92db857", "sub:frontend-developer:ad0ad607", "sub:subagent:a0712891", "sub:frontend-developer", "sub:subagent:aae0cb6d", "sub:subagent:a84e9698", "sub:subagent:a3cfb9ee", "sub:subagent:a7263090"]
contributions: [{"actor": "sub:designer:ac9f7eb4", "item": "022 설정 화면 구조·시각 설계", "started": "2026-09-01T19:28:19+09:00", "ended": "2026-09-01T19:37:00+09:00", "result": "done", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/8a0d83ad-568f-4f69-9585-f816dfe479f7/tasks/ac9f7eb403431a61a.output"}, {"actor": "sub:ux-writer:aa92add5", "item": "022 설정 화면 문구", "started": "2026-09-01T19:28:40+09:00", "ended": "2026-09-01T19:32:46+09:00", "result": "done", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/8a0d83ad-568f-4f69-9585-f816dfe479f7/tasks/aa92add51f9b63e06.output"}, {"actor": "sub:tech-writer:a99e895f", "item": "022 낱말 번역 판정", "started": "2026-09-01T19:29:00+09:00", "ended": "2026-09-01T19:34:43+09:00", "result": "done", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/8a0d83ad-568f-4f69-9585-f816dfe479f7/tasks/a99e895fb8942445a.output"}, {"actor": "sub:translator:afe5a902", "item": "022 어휘 번역", "started": "2026-09-01T19:29:12+09:00", "ended": "2026-09-01T19:33:31+09:00", "result": "done", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/8a0d83ad-568f-4f69-9585-f816dfe479f7/tasks/afe5a90283d498818.output"}, {"actor": "sub:ux-writer", "item": "계정 설정 화면 문구 (ux-writer · REQ-20260901-022-62x6)", "started": "2026-09-01T19:32:21+09:00", "ended": "2026-09-01T19:32:21+09:00", "result": "done", "transcript": ""}, {"actor": "sub:ux-writer:aa92add5", "item": "`[2026-09-01 19:34:12 KST - ux-writer]`", "started": "2026-09-01T19:32:46+09:00", "ended": "2026-09-01T19:32:46+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/8a0d83ad-568f-4f69-9585-f816dfe479f7/subagents/agent-aa92add51f9b63e06.jsonl"}, {"actor": "sub:translator", "item": "최종 낱말 (translator)", "started": "2026-09-01T19:33:06+09:00", "ended": "2026-09-01T19:33:06+09:00", "result": "done", "transcript": ""}, {"actor": "sub:subagent:a29ed724", "item": "Appending vocabulary note to REQ-20260901-022-62x6", "started": "2026-09-01T19:33:27+09:00", "ended": "2026-09-01T19:33:27+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/8a0d83ad-568f-4f69-9585-f816dfe479f7/subagents/agent-a29ed72403b5ddc5f.jsonl"}, {"actor": "sub:translator:afe5a902", "item": "`[2026-09-01 19:29:14 KST - translator]`", "started": "2026-09-01T19:33:31+09:00", "ended": "2026-09-01T19:33:31+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/8a0d83ad-568f-4f69-9585-f816dfe479f7/subagents/agent-afe5a90283d498818.jsonl"}, {"actor": "sub:tech-writer", "item": "tech-writer 용어 판정 — 계정 설정 화면 (022)", "started": "2026-09-01T19:34:08+09:00", "ended": "2026-09-01T19:34:08+09:00", "result": "done", "transcript": ""}, {"actor": "sub:subagent:af5245f7", "item": "Appending verdict note to REQ-20260901-022-62x6", "started": "2026-09-01T19:34:15+09:00", "ended": "2026-09-01T19:34:15+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/8a0d83ad-568f-4f69-9585-f816dfe479f7/subagents/agent-af5245f75fbf2f1b5.jsonl"}, {"actor": "lead:claude-opus-5", "item": "리드 판정 — `auto_resume` 의 이름 (ux-writer ↔ translator 충돌)", "started": "2026-09-01T19:34:35+09:00", "ended": "2026-09-01T19:34:35+09:00", "result": "done", "transcript": ""}, {"actor": "sub:tech-writer:a99e895f", "item": "`[2026-09-01 19:29:01 KST - tech-writer]`", "started": "2026-09-01T19:34:43+09:00", "ended": "2026-09-01T19:34:43+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/8a0d83ad-568f-4f69-9585-f816dfe479f7/subagents/agent-a99e895fb8942445a.jsonl"}, {"actor": "lead:claude-opus-5", "item": "리드 판정 2 — 낱말 확정 (tech-writer 착지 뒤)", "started": "2026-09-01T19:35:49+09:00", "ended": "2026-09-01T19:35:49+09:00", "result": "done", "transcript": ""}, {"actor": "sub:designer", "item": "설정을 화면에서 바꾼다 — 구조·시각 설계 (designer)", "started": "2026-09-01T19:36:19+09:00", "ended": "2026-09-01T19:36:19+09:00", "result": "done", "transcript": ""}, {"actor": "sub:subagent:a92db857", "item": "Verifying note and attachments in REQ-20260901-022-62x6", "started": "2026-09-01T19:36:38+09:00", "ended": "2026-09-01T19:36:38+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/8a0d83ad-568f-4f69-9585-f816dfe479f7/subagents/agent-a92db8574d508c57d.jsonl"}, {"actor": "sub:designer:ac9f7eb4", "item": "`[2026-09-01 19:41:07 KST - designer]`", "started": "2026-09-01T19:37:00+09:00", "ended": "2026-09-01T19:37:00+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/8a0d83ad-568f-4f69-9585-f816dfe479f7/subagents/agent-ac9f7eb403431a61a.jsonl"}, {"actor": "lead:claude-opus-5", "item": "리드 판정 3 — 구역 이름, 그리고 구현으로 넘김", "started": "2026-09-01T19:37:38+09:00", "ended": "2026-09-01T19:37:38+09:00", "result": "done", "transcript": ""}, {"actor": "sub:frontend-developer:ad0ad607", "item": "022 설정 화면 구현", "started": "2026-09-01T19:38:14+09:00", "ended": "2026-09-01T20:38:04+09:00", "result": "done", "transcript": "/tmp/claude-1000/-home-sjpark1-section9/8a0d83ad-568f-4f69-9585-f816dfe479f7/tasks/ad0ad607bfa5b3529.output"}, {"actor": "sub:subagent:a0712891", "item": "Reading REQ-20260901-022-62x6 tech-writer notes", "started": "2026-09-01T19:38:45+09:00", "ended": "2026-09-01T19:38:45+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/8a0d83ad-568f-4f69-9585-f816dfe479f7/subagents/agent-a0712891cbbe2d71b.jsonl"}, {"actor": "sub:frontend-developer", "item": "TDD 시나리오 — 설정을 화면에서 바꾼다 (frontend)", "started": "2026-09-01T19:42:24+09:00", "ended": "2026-09-01T19:42:24+09:00", "result": "done", "transcript": ""}, {"actor": "sub:subagent:aae0cb6d", "item": "Appending TDD scenarios to REQ-20260901-022-62x6", "started": "2026-09-01T19:42:55+09:00", "ended": "2026-09-01T19:42:55+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/8a0d83ad-568f-4f69-9585-f816dfe479f7/subagents/agent-aae0cb6de4aa737f3.jsonl"}, {"actor": "sub:subagent:a84e9698", "item": "Listing note headings in REQ-20260901-022-62x6", "started": "2026-09-01T19:47:35+09:00", "ended": "2026-09-01T19:47:35+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/8a0d83ad-568f-4f69-9585-f816dfe479f7/subagents/agent-a84e96980c61ff222.jsonl"}, {"actor": "sub:subagent:a3cfb9ee", "item": "Reading REQ-20260901-022-62x6 vocabulary notes", "started": "2026-09-01T19:47:58+09:00", "ended": "2026-09-01T19:47:58+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/8a0d83ad-568f-4f69-9585-f816dfe479f7/subagents/agent-a3cfb9ee6a49263f4.jsonl"}, {"actor": "sub:frontend-developer", "item": "TDD 시나리오 — 검증 결과 (frontend, 개정 2)", "started": "2026-09-01T20:26:51+09:00", "ended": "2026-09-01T20:26:51+09:00", "result": "done", "transcript": ""}, {"actor": "sub:frontend-developer", "item": "설정을 화면에서 바꾼다 — 구현 (frontend-developer)", "started": "2026-09-01T20:27:30+09:00", "ended": "2026-09-01T20:27:30+09:00", "result": "done", "transcript": ""}, {"actor": "sub:subagent:a7263090", "item": "Attaching captures to REQ-20260901-022-62x6 note", "started": "2026-09-01T20:27:38+09:00", "ended": "2026-09-01T20:27:38+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/8a0d83ad-568f-4f69-9585-f816dfe479f7/subagents/agent-a726309046c0d19b6.jsonl"}, {"actor": "sub:frontend-developer", "item": "착지 뒤 잡은 것 둘 (frontend-developer)", "started": "2026-09-01T20:36:47+09:00", "ended": "2026-09-01T20:36:47+09:00", "result": "done", "transcript": ""}, {"actor": "sub:frontend-developer:ad0ad607", "item": "`[2026-09-01 21:02:41 KST - frontend-developer]`", "started": "2026-09-01T20:38:04+09:00", "ended": "2026-09-01T20:38:04+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/8a0d83ad-568f-4f69-9585-f816dfe479f7/subagents/agent-ad0ad607bfa5b3529.jsonl"}, {"actor": "sub:frontend-developer:ad0ad607", "item": "`[2026-09-01 20:52:44 KST - frontend-developer]`", "started": "2026-09-01T20:53:15+09:00", "ended": "2026-09-01T20:53:15+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/8a0d83ad-568f-4f69-9585-f816dfe479f7/subagents/agent-ad0ad607bfa5b3529.jsonl"}, {"actor": "sub:frontend-developer:ad0ad607", "item": "`[2026-09-01 21:16:14 KST - frontend-developer]`", "started": "2026-09-01T21:16:27+09:00", "ended": "2026-09-01T21:16:27+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/8a0d83ad-568f-4f69-9585-f816dfe479f7/subagents/agent-ad0ad607bfa5b3529.jsonl"}, {"actor": "sub:frontend-developer:ad0ad607", "item": "`[2026-09-01 21:16:33 KST - frontend-developer]`", "started": "2026-09-01T21:16:47+09:00", "ended": "2026-09-01T21:16:47+09:00", "result": "done", "transcript": "/home/sjpark1/.claude/projects/-home-sjpark1-section9/8a0d83ad-568f-4f69-9585-f816dfe479f7/subagents/agent-ad0ad607bfa5b3529.jsonl"}, {"actor": "lead:claude-opus-5", "item": "이 화면의 낱말 둘이 바뀌었다 (판정 전에 알아 두실 것)", "started": "2026-09-02T07:47:36+09:00", "ended": "2026-09-02T07:47:36+09:00", "result": "done", "transcript": ""}]
project: section9
children: ["REQ-20260901-023-62x6"]
derived_from: QST-20260901-006-62x6
relates_why: {}
tags: ["dashboard", "session", "sync", "attached", "png", "image", "terminal", "workflow"]
created: 2026-09-01T19:26:58+09:00
status_since: 2026-09-01T21:08:37+09:00
updated: 2026-09-02T07:47:36+09:00
priority: 50
---

## Original

QST-20260901-006 「깃헙에 푸시 여부에 대해서 옵션 설정을 바꿨는데 어디서 하는거지?」 의 후속. 사용자: 화면으로 기능이 필요하다, 관련 에이전트 소집해서 진행해. 현재 web/app/userform.js 의 extraCfg 는 읽기 전용 JSON 한 줄(118행)이고, 실제 변경 경로는 bin/s9 user config 뿐이다. auto_resume_gh 는 무인 작업자에게 깃헙 계정 권한을 주는 스위치라 무게가 다르다 — 화면이 그 무게를 말해야 한다.

## Notes

### 2026-09-01T19:32:21+09:00 response (agent: sub:ux-writer, by nicehugepark)

### 계정 설정 화면 문구 (ux-writer · REQ-20260901-022-62x6)

### 결론 먼저

세 가지를 정했다.

1. **`auto_resume` 를 「자동 이어받기」로 부르면 안 된다.** 이 값은 저절로 뜨는 것만 막는 게 아니라
   사람이 카드에서 「이어가기」를 눌러도 막는 **주 스위치**다 (bin/s9:6981 — 모든 스폰 경로가 이 문을
   지난다). 그런데 카드에는 이미 「자동 이어받기 끄기」(card.js:29)가 있고 그것은 **요청 하나만**
   저절로 안 뜨게 하는 정책이라 사람이 누르면 뜬다. 같은 낱말에 다른 뜻 둘을 얹으면 켠 사람이
   무엇을 켰는지 모른다. 계정 스위치는 **「무인 작업 맡기기」** 로 부른다 — 여는 대상(무인 작업)을
   이름에 담고, 「자동」은 DOC-20260831-005-62x6 규칙 2 대로 정책 서술에만 남긴다.
2. **기계 이름은 남긴다 — 라벨이 아니라 각주로.** 지우면 안 되는 이유가 화면 밖에 있다: 서버의
   차단 메시지가 실제로 "`auto_resume` 을 켜야"라고 말하고(bin/s9:6983), REQ 문서·노트·이 요청
   원문이 전부 `auto_resume_gh` 로 그 설정을 부른다. 이름만 들고 화면에 온 사람이 자기 설정을
   못 찾는 것, 그게 QST-20260901-006-62x6 그 자체다. 대신 이름은 라벨 자리를 뺏지 않는다 — 한국어
   라벨 아래 `.path` 등급(mono·작게·흐리게) 한 줄. 이 화면이 이미 같은 손버릇을 쓴다(시간대 절이
   본문에 `timezone` 을 곁들인다).
3. **깃헙 권한은 켤 때만 확인 창을 세운다.** 끄는 쪽은 권한을 거두는 방향이라 확인이 손만 는다.

### 항목 표

절은 둘이다. 각 절 머리에 그 절의 주어를 한 번만 세우고, 행들은 그 주어를 물려받는다.

### 절 ①  무인 작업 — 사람이 지키지 않는 자리에서 도는 일꾼
> 요청이 반려·승인되거나 카드의 「이어가기」를 누르면 무인 작업이 떠서 그 일을 이어받습니다.
> 그 일꾼에게 무엇까지 맡길지 여기서 정합니다. **바꾸면 바로 저장됩니다.**

| 라벨 (기계 이름) | 뜻 한 줄 | 켬 | 끔 |
|---|---|---|---|
| **무인 작업 맡기기**<br>`auto_resume` | 반려·승인 뒤에 저절로 뜨는 것도, 카드의 「이어가기」로 띄우는 것도 이 스위치가 엽니다. | 켬 — 무인 작업이 떠서 이어받습니다 | 끔 — 요청은 사람이 직접 진행합니다 |
| **파일 고치기**<br>`auto_resume_apply` | 일꾼이 고칠 안만 적어 두는 데서 그치지 않고, `web/` · `vault/` · `tests/` 안의 파일을 직접 고치고 검증까지 합니다. | 켬 — 고치고 검증까지 합니다 | 끔 — 고칠 안을 문서에 적어 두고 사람을 기다립니다 |
| **깃헙 권한 주기**<br>`auto_resume_gh` | 일꾼이 이 컴퓨터에 로그인된 깃헙 계정으로 명령을 씁니다 — 푸시는 그중 하나고, 같은 권한으로 저장소 설정을 바꾸거나 저장소를 지울 수도 있습니다. | 켬 — 깃헙 계정으로 명령을 씁니다 | 끔 — 깃헙은 사람이 직접 다룹니다 |
| **작업 자리 따로 쓰기**<br>`worker_worktree` | 일꾼을 제 작업 자리에서 돌려, 아직 커밋하지 않은 남의 편집이 보이지도 덮이지도 않게 합니다. 지금 저장소에 미커밋 편집이 있으면 자리를 나누지 않고 본 저장소에서 일합니다. | 켬 — 자리를 나눠 줍니다 | 끔 — 본 저장소에서 함께 일합니다 |
| **기다리는 시간**<br>`auto_resume_grace_sec` | 일이 반려된 뒤 이만큼 기다렸는데도 아무도 집지 않으면 그때 일꾼이 뜹니다. 급한 요청은 기다리지 않습니다. | (입력) 비우면 30초 | — |
| **쓰는 모델**<br>`auto_resume_model` | 일꾼이 이 모델로 생각하고 답합니다. | (입력) 비우면 이 컴퓨터의 기본 모델 | — |

라벨 문법은 이 화면의 기존 라벨(「기록 남기기」·「보관 기간」)을 그대로 따랐다 — 목적어+동사+기.
「작업 자리」는 이 저장소가 워크트리를 부르는 말로 이미 굳었다(card.js 머리주석·워커 프롬프트).

### 절 ②  새 창 — `s9 code` 로 열 때
> 대시보드나 카드에서 새 창을 열 때 쓰는 값입니다.

| 라벨 (기계 이름) | 뜻 한 줄 | 기본 |
|---|---|---|
| **창을 열 때 붙일 인자**<br>`s9code_args` | 새 창을 띄우는 `claude` 명령 뒤에 이 글자를 그대로 덧붙입니다. | (입력) 비우면 붙이지 않습니다 |

### 절 ③  대화 기록 — 손대지 않는다
`stream_mirror`·`stream_keep_days` 의 「켬 — 대화를 남깁니다 / 끔 — 남기지 않습니다」가 이 화면의
켬·끔 문장 **기준선**이다. 위 표의 문장들이 그 틀(`켬 — <무슨 일이 일어나는가>`)을 따랐다.
끔 문장은 어느 것도 "안 합니다"로 끝나지 않는다 — 대신 무엇이 일어나는지를 말한다. 개념을
부정형으로 데뷔시키지 않는다는 규율(REQ-20260901-005-62x6 3차 판정)의 켬/끔판이다.

### 깃헙 권한 — 켤 때 서는 창

무게는 문장이 진다. 붉은색·느낌표·경고 아이콘을 쓰지 않는다 — 사실이 이미 무겁다.

```
머리(cap)   권한
제목        깃헙 계정 권한을 무인 작업에 줍니다
본문        켜면 사람이 지키지 않는 자리에서 도는 일꾼이 이 컴퓨터에 로그인된
            깃헙 계정으로 명령을 씁니다. 푸시는 그중 하나입니다 — 같은 권한으로
            저장소 설정을 바꾸거나 저장소를 지울 수도 있습니다.
            여기서 언제든 다시 끌 수 있지만, 켜져 있는 동안 한 일까지
            되돌리지는 못합니다.
단추        [ 권한 주기 ]   [ 그만두기 ]
```

- **끌 때는 창을 세우지 않는다.** 확인 문장만: `껐습니다 — 앞으로 뜨는 무인 작업은 깃헙 없이 일합니다.`
- 켠 뒤 확인 문장: `켰습니다 — 앞으로 뜨는 무인 작업이 깃헙 계정으로 명령을 씁니다.`
- 켜져 있는 동안 그 행에 상주하는 한 줄(사실 서술, 경고 아님):
  `이 계정의 깃헙 권한이 무인 작업에 열려 있습니다.`

### 실패·권한 없음·빈 상태

기존 `secmsg` 자리를 그대로 쓴다(스위치 바로 아래 인라인 — 팝업 금지, 이 화면의 확립된 규칙).

| 때 | 문장 |
|---|---|
| 저장 중 | `저장하는 중…` |
| 저장 성공 | 그 상태의 서술 한 줄 = 위 표의 켬/끔 문장 그대로 (문장을 두 벌 짓지 않는다) |
| 저장 실패 (서버가 까닭을 줌) | `✕ 바꾸지 못했습니다 — <서버가 말한 까닭>. 스위치는 원래대로 돌려놨습니다.` |
| 서버에 못 닿음 | `✕ 서버에 닿지 못했습니다 — 잠시 뒤 다시 눌러 주세요. 서버가 재기동 중일 수 있습니다.` (dialog.js 가 이미 쓰는 문장 재사용) |
| 남의 계정을 열어 봤을 때 | 스위치를 잠그고 절 아래 한 줄: `@<이름> 의 설정은 보기만 됩니다 — 본인이나 admin 만 바꿀 수 있습니다.` |
| 값 칸이 비었을 때 | 빈 칸 자체가 기본값을 말한다 — placeholder: `비우면 30초` · `비우면 이 컴퓨터의 기본 모델` · `비우면 붙이지 않습니다` |

### before / after

| 지금 | 바꾼 뒤 |
|---|---|
| `기타: {"auto_resume_gh":"on","worker_worktree":"on",…}` — 읽기 전용 JSON 한 줄 | 절 제목 + 항목마다 [한국어 라벨 / 기계 이름 각주 / 뜻 한 줄 / 켬·끔 문장] |
| 바꾸려면 `s9 user config <이름> auto_resume_gh on` 을 외워야 한다 | 그 자리에서 켜고 끈다. 깃헙만 확인 창을 지난다 |
| 켠 것이 무엇을 여는지 화면이 말하지 않는다 | "푸시는 그중 하나입니다 — 같은 권한으로 저장소를 지울 수도 있습니다" |

### 용어 사전 갱신분

- **신설** 「무인 작업 맡기기」(계정 `auto_resume` = 무인 작업이 뜰 수 있는가, 주 스위치) ·
  「작업 자리 따로 쓰기」(`worker_worktree`) · 「깃헙 권한 주기」(`auto_resume_gh`)
- **유지** 「무인 작업」·「이어받다」·「이어가기」·「작업 자리」·「일손」 (DOC-20260831-005-62x6, card.js)
- **금지** 계정 스위치를 「자동 이어받기」로 부르는 것 — 그 이름은 카드의 **요청 단위** 정책
  (「자동 이어받기 끄기」)이 이미 쓰고 있고, 뜻이 다르다(요청 하나 vs 계정 전체, 사람이 눌러도
  막힘 vs 안 막힘).

### 화면 밖에서 고칠 문장 하나 (구현 몫이 아니라 판단 근거)

`bin/s9:6982` 의 차단 문구가 세 번째 이름을 만들고 있다 — "**자동 이어가기**가 꺼져 있습니다 —
Settings 의 계정 설정에서 `auto_resume` 을 켜야 이어갈 수 있습니다". 「자동」+「이어가기」(사람 단추의
낱말) 조합이라 사람이 누른 것까지 막는다는 사실이 문장에서 사라진다. 화면이 새 라벨을 쓰면
이 줄도 같은 낱말로 맞춰야 한다:

> `@<이름> 계정은 무인 작업을 쓰지 않도록 되어 있습니다 — Settings 의 계정 설정에서
> 「무인 작업 맡기기」를 켜면 이어받습니다.`

같은 이유로 서버의 권한 거절문(`bin/s9:17699` "본인 또는 admin만 설정 변경 가능")도 문장이
아니다. 화면이 그대로 띄우므로 서버에서 고치는 것이 옳다:

> `@<이름> 본인이나 admin 만 이 설정을 바꿀 수 있습니다.`

### 한계

- 배치·밀도·스위치 생김새는 designer 의 몫이다. 이 문서는 낱말과 문장만 정한다.
- 「무인 작업 맡기기」가 주 스위치라는 사실은 bin/s9:6981 코드로 확인했다. 이 문이 나중에
  사람 경로를 통과시키도록 바뀌면 라벨과 끔 문장(「요청은 사람이 직접 진행합니다」)을 함께 고쳐야 한다.
- `auto_resume_grace_sec` 는 요청에 없었으나 같은 `기타:` 줄에 사는 값이라 함께 적었다. 넣고 뺄지는 리드 판단.

### 2026-09-01T19:32:46+09:00 subagent (agent: sub:ux-writer:aa92add5, by nicehugepark)

`[2026-09-01 19:34:12 KST - ux-writer]`

문구를 `REQ-20260901-022-62x6` 에 `--label response` 로 남겼다. 요지는 셋이다.

**1. `auto_resume` 를 「자동 이어받기」로 부르면 안 된다 — 이게 이번 작업의 가장 큰 발견이다.**
코드로 확인했다(`bin/s9:6981`): 이 값은 저절로 뜨는 것만 막는 게 아니라 **모든 스폰 경로**를 막는 문이다. 사람이 카드에서 「이어가기」를 눌러도 막힌다. 그런데 카드에는 이미 「자동 이어받기 끄기」(`web/app/card.js:29`)가 있고, 그것은 **요청 하나만** 저절로 안 뜨게 하는 정책이라 사람이 누르면 뜬다. 같은 낱말에 다른 뜻 둘을 얹으면 켠 사람이 무엇을 켰는지 모른다. 계정 스위치는 **「무인 작업 맡기기」** 로 부른다.

**2. 기계 이름은 남긴다 — 라벨이 아니라 각주로.** 지울 수 없는 이유가 화면 밖에 있다: 서버 차단 메시지가 실제로 "`auto_resume` 을 켜야"라고 말하고(`bin/s9:6983`), REQ 문서·노트·이번 요청 원문이 전부 `auto_resume_gh` 로 그 설정을 부른다. 이름만 들고 화면에 온 사람이 자기 설정을 못 찾는 것 — 그게 QST-20260901-006-62x6 그 자체다. 한국어 라벨 아래 `.path` 등급 mono 한 줄로 물린다(시간대 절이 `timezone` 을 곁들이는 그 손버릇).

**3. 깃헙 권한은 켤 때만 확인 창.** 무게는 붉은색이 아니라 문장이 진다:
> 켜면 사람이 지키지 않는 자리에서 도는 일꾼이 이 컴퓨터에 로그인된 깃헙 계정으로 명령을 씁니다. **푸시는 그중 하나입니다** — 같은 권한으로 저장소 설정을 바꾸거나 저장소를 지울 수도 있습니다. 여기서 언제든 다시 끌 수 있지만, 켜져 있는 동안 한 일까지 되돌리지는 못합니다.
>
> `[ 권한 주기 ]  [ 그만두기 ]`

"푸시는 그중 하나입니다"가 사용자의 원래 질문("깃헙에 푸시 여부")과 실제 권한 범위를 잇는 문장이다. 끄는 쪽은 확인 창 없이 문장만.

**항목 여섯 + 새 창 하나**를 [라벨 / 기계 이름 / 뜻 한 줄 / 켬 / 끔] 표로 냈다. 끔 문장은 어느 것도 "안 합니다"로 끝나지 않는다 — 대신 무엇이 일어나는지 말한다(「끔 — 요청은 사람이 직접 진행합니다」). `worker_worktree` 뜻 줄에는 코드에서 확인한 조건을 정직하게 넣었다: 미커밋 편집이 있으면 자리를 안 나누고 본 저장소에서 일한다(`bin/s9:18588`).

**저장 실패·권한 없음·빈 상태 문장**도 함께 냈고, 화면 밖에서 고쳐야 할 서버 문장 둘을 짚었다 — `bin/s9:6982` 의 「자동 이어가기」(세 번째 이름을 만들고 있다)와 `bin/s9:17699` 의 「본인 또는 admin만 설정 변경 가능」(문장이 아닌데 화면이 그대로 띄운다). 대체 문안을 노트에 적어 뒀다.

배치·밀도·스위치 생김새는 designer 몫이고, 이 문서는 낱말과 문장만 정한다.

전문: `/home/sjpark1/section9/vault/requests/2026/09/REQ-20260901-022-62x6.md` (response 노트)

### 2026-09-01T19:33:06+09:00 response (agent: sub:translator, by nicehugepark)

### 최종 낱말 (translator)

이 화면의 낱말은 **한 자도 새로 짓지 않았다.** 여덟 후보 중 다섯은 이 저장소가
이미 화면에서 쓰는 말을 그대로 옮겨 온 것이고, 새로 정한 것은 셋뿐이다.

### 대역표

| 키 | 최종 낱말 | 켬/끔의 말 | 역검산 |
|---|---|---|---|
| auto_resume | **자동 이어받기** | 켬 — 담당이 없으면 무인 작업이 떠서 이어받습니다 / 끔 — 「▶ 이어가기」를 누를 때까지 아무것도 뜨지 않습니다 | 저절로 이어받는다 → auto resume. 잃은 것 없음 |
| auto_resume_apply | **파일 직접 고치기** | 켬 — 무인 작업이 파일을 고치고 테스트까지 돌립니다 / 끔 — 고칠 내용을 적어만 두고 사람을 기다립니다 | 작업자가 제 손으로 파일을 고친다 → apply(제안 아님). 「검증까지」는 켬 줄이 짐 |
| auto_resume_gh | **내 GitHub 계정 쓰게 하기** | 켬 — 사람이 안 보는 자리에서 내 GitHub 계정이 쓰입니다. 저장소 설정을 바꾸거나 지울 수도 있습니다 / 끔 — GitHub 은 건드리지 않습니다 | 무인 작업자가 내 계정 권한으로 gh 를 쓴다 → 그대로. 무게(되돌릴 수 없음)가 낱말이 아니라 켬 줄에 실림 |
| worker_worktree | **따로 떼어 놓고 일하기** | 켬 — 남의 작업을 덮어쓰지 않습니다. 고친 내용은 작업이 끝난 뒤에 보입니다 / 끔 — 고친 내용이 바로 보입니다 | 제 자리를 따로 두고 일한다 → worktree. 가지·병합은 잃지만 REQ-20260829-030-62x6 5차가 이미 내린 사실 |
| stream_mirror | **기록 남기기** (기존) | 켬 — 대화를 남깁니다 / 끔 — 남기지 않습니다 | 손대지 않는다 — 이미 이 화면에 서 있는 말 |
| stream_keep_days | **보관 기간** (기존) | (일 단위 입력) | 손대지 않는다 |
| digest_budget | **요약 분량** (머리 「세션 시작 요약」, 단위 자) | 비우면 2500자 · 넘치면 덜 급한 것부터 줄입니다 | 세션 시작에 건네는 요약의 양 → digest budget. 「예산」의 회계 결만 잃음 |
| on/off | **켬 / 끔** (값) · **켜기 / 끄기** (단추) | — | 두 상은 이미 갈려 있다: 고르는 값은 켬/끔(userform.js:63), 누르는 것은 켜기/끄기(audit.js:105·card.js:27) |

### 근거 — 어느 자리에서 이미 그 말을 쓰는가

- **자동 이어받기**: `web/app/card.js:27`(STOP_HOLD_LABEL)·`:609`(예고 줄 캡션)에
  이미 서 있다. REQ-20260901-005-62x6 3차 판정이 세 번의 실패 끝에 고른 이름이고,
  「자동 OO 끄기」가 자동 갱신·자동 재생으로 굳은 온/오프 정책 문법이라는 것이
  근거였다. 계정 설정은 같은 정책의 상위 스위치이므로 **같은 낱말**을 쓴다 —
  한 개념에 두 이름을 만들지 않는다. 켬/끔 문장도 `card.js:101-103`·`:610` 의
  문장을 그대로 옮겼다(문장 두 벌 금지).
- **무인 작업** (네 줄의 머리): DOC-20260831-005-62x6 규칙 1. `card.js`·`session.js:23`·
  `restart.js:231`·`diag.js:214` 가 이미 이 이름을 쓴다. 머리가 주어를 지면
  아래 라벨들은 주어를 생략할 수 있다.
- **고치다**: 이 화면의 편집 동사다 — `card.js:1025`(\"고친 내용은…\"), WS_MEANS,
  DRIFT_TIP(\"고친 것이 있는데\"). 「수정·적용」이 아니라 「고치기」.
- **테스트**: 사람에게 보이는 잡 이름이 이미 「테스트」다(`notice.js:384`,
  `card.js:921`). 코드 주석의 「시험」은 내부어라 화면에 올리지 않는다.
- **따로 떼어 놓고**: 조어가 아니라 **이미 화면에 있는 구절**이다 —
  `card.js:1025` dirty-spine 손 위 글 \"따로 떼어 놓고 하면 그 코드가 빠진 낡은
  사본에서 일하게 됩니다\". 켬/끔 문장의 \"작업이 끝난 뒤에 보입니다\" 도
  WS_MEANS.worktree 원문 그대로다.
- **세션 시작**: `input.js:469`·`terminal.js:339`·`session.js:35` 의 「여기서 세션
  시작」. **요약**: `project.js:115` 「관리 요약」·`docs.js:533` 「한 줄 요약」.
- **켬/끔**: `userform.js:63-64` 의 대화 기록 행이 이 판의 본이다. 새 행들은
  그 행과 같은 꼴(라벨 + 켬/끔 + 줄표 뒤 결과문)로 선다.

### 버린 후보와 버린 이유 (다시 꺼내지 않게)

- 「자동 재개」 — 재개는 한자어 직역이고, 확정 재개 동사는 「이어받다」다(DOC-20260831-005-62x6 규칙 3).
- 「자동 이어가기」 — 「이어가기」는 **사람이 누르는 단추**(▶ 이어가기)의 이름이다. 정책에 쓰면 한 어간이 두 주체를 진다.
- 「자동 작업」 — DOC-20260831-005-62x6 규칙 1로 폐기된 오칭(개체 이름으로 쓸 수 없다).
- 「자동 적용」·「패치 적용」 — 「패치」는 PROTOCOL 의 내부어이고, 「적용」은 무엇이 적용되는지 목적어가 안 보인다.
- 「수정 권한」·「GitHub 권한」 — 「권한」은 이 화면에서 **사람의 역할**에 이미 쓰인다(`project.js:130` colRole, \"열 권한이 없거나\"). 한 낱말이 두 뜻을 지면 안 된다.
- 「GitHub 계정 쓰기」 — 설정 화면에서 「쓰기」는 write 로 읽힌다. 사동 「쓰게 하기」로 못박는다.
- 「워크트리」·「따로 떼어 둔 사본」·「격리 작업」 — REQ-20260830-001-62x6 이 화면에서 내렸고, DOC-20260830-006-62x6 노트가 「따로 떼어 둔 사본」을 시금석 ① 위반(있는 사물을 없는 독자에게 부름)으로 못박았다. 명사로 부활시키지 않는다 — 이번에 쓰는 것은 **동사구**다.
- 「다이제스트」·「요약 예산」·「요약 한도」 — 음차는 옮기지 않은 것과 같고, 「예산」은 회계어라 글자 수 앞에서 겉돈다.
- 「ON/OFF」·「활성/비활성」·「사용함/사용 안 함」 — 화면이 이미 켬/끔이다.

### 함께 고칠 것 (한 낱말이 두 화면에서 갈린 자리)

`bin/s9:6982` 의 차단 문구가 **「자동 이어가기가 꺼져 있습니다 — Settings 의 계정
설정에서 auto_resume 을 켜야…」** 다. 두 군데가 틀렸다: ① 「자동 이어가기」는
화면의 「자동 이어받기」와 다른 말이고 ② 사용자에게 원시 키 이름(`auto_resume`)을
외우게 한다. 이 화면이 서면 그 문장은 **\"자동 이어받기가 꺼져 있습니다 —
Settings 의 계정 설정에서 켜 주세요\"** 로 고쳐야 한다. 이 REQ 가 없애려는
증상(키 이름을 외워야 하는 것)이 서버 문장에도 그대로 남아 있다.

### 2026-09-01T19:33:27+09:00 subagent (agent: sub:subagent:a29ed724, by nicehugepark)

Appending vocabulary note to REQ-20260901-022-62x6

### 2026-09-01T19:33:31+09:00 subagent (agent: sub:translator:afe5a902, by nicehugepark)

`[2026-09-01 19:29:14 KST - translator]`

여덟 후보의 최종 낱말을 확정했다. **새로 지은 낱말은 없다** — 다섯은 이 저장소가 이미 화면에서 쓰는 말을 그대로 가져왔고, 새로 정한 것은 셋뿐이다. 전문은 `REQ-20260901-022-62x6` 의 response 노트에 있다.

### 최종 낱말

| 키 | 낱말 | 켬 / 끔 |
|---|---|---|
| `auto_resume` | **자동 이어받기** | 담당이 없으면 무인 작업이 떠서 이어받습니다 / 「▶ 이어가기」를 누를 때까지 아무것도 뜨지 않습니다 |
| `auto_resume_apply` | **파일 직접 고치기** | 파일을 고치고 테스트까지 돌립니다 / 고칠 내용을 적어만 두고 사람을 기다립니다 |
| `auto_resume_gh` | **내 GitHub 계정 쓰게 하기** | 사람이 안 보는 자리에서 내 GitHub 계정이 쓰입니다. 저장소 설정을 바꾸거나 지울 수도 있습니다 / GitHub 은 건드리지 않습니다 |
| `worker_worktree` | **따로 떼어 놓고 일하기** | 남의 작업을 덮어쓰지 않습니다. 고친 내용은 작업이 끝난 뒤에 보입니다 / 고친 내용이 바로 보입니다 |
| `stream_mirror` | **기록 남기기** (기존 그대로) | 대화를 남깁니다 / 남기지 않습니다 |
| `stream_keep_days` | **보관 기간** (기존 그대로) | — |
| `digest_budget` | **요약 분량** (머리 「세션 시작 요약」, 단위 자) | 비우면 2500자 · 넘치면 덜 급한 것부터 줄입니다 |
| on/off | **켬 / 끔**(값) · **켜기 / 끄기**(단추) | 두 상은 이미 갈려 있다 |

네 줄의 머리는 **「무인 작업」** — 머리가 주어를 지므로 라벨이 주어를 생략할 수 있다.

### 근거의 요지

- **자동 이어받기**는 `web/app/card.js:27`(STOP_HOLD_LABEL)·`:609`(예고 줄 캡션)에 이미 서 있다. REQ-20260901-005-62x6 3차 판정이 세 번 실패한 끝에 고른 이름이고, 계정 설정은 같은 정책의 상위 스위치다 — 한 개념에 두 이름을 만들지 않는다. 켬/끔 문장도 `card.js:101-103`·`:610` 을 그대로 옮겼다.
- **따로 떼어 놓고**는 조어가 아니라 이미 화면에 있는 구절이다 — `card.js:1025` dirty-spine 손 위 글의 "따로 떼어 놓고 하면…". 「워크트리」·「따로 떼어 둔 사본」은 REQ-20260830-001-62x6 과 DOC-20260830-006-62x6 노트가 내린 말이라 **명사로 부활시키지 않고 동사구로만** 쓴다.
- **고치다**(수정 아님)·**테스트**(시험 아님)는 각각 WS_MEANS·`notice.js:384` 의 화면 낱말이다.

### 버린 것 중 눈여겨볼 둘

- 「권한」 계열(수정 권한·GitHub 권한): 이 화면에서 「권한」은 **사람의 역할**에 이미 쓰인다(`project.js:130` colRole, "열 권한이 없거나"). 한 낱말이 두 뜻을 지면 안 돼서 gh 스위치는 "계정을 쓰게 한다"는 사실 그대로 부른다.
- 「GitHub 계정 쓰기」: 설정 화면에서 「쓰기」는 write 로 읽힌다. 사동 「쓰게 하기」로 못박았다.

### 함께 고쳐야 할 결함 (frontend 로 넘길 것)

`/home/sjpark1/section9/bin/s9:6982` 의 차단 문구가 두 군데 틀렸다.

```
f"{owner or '이'} 계정은 자동 이어가기가 꺼져 "
f"있습니다 — Settings 의 계정 설정에서 auto_resume "
f"을 켜야 이어갈 수 있습니다"
```

① **「자동 이어가기」**는 화면의 「자동 이어받기」와 다른 말이다(「이어가기」는 사람이 누르는 ▶ 단추의 이름이라 한 어간이 두 주체를 진다). ② 사용자에게 원시 키 이름 `auto_resume` 을 외우게 한다 — 이 REQ 가 없애려는 바로 그 증상이 서버 문장에 그대로 남아 있다. 화면이 서면 **"자동 이어받기가 꺼져 있습니다 — Settings 의 계정 설정에서 켜 주세요"** 로 고쳐야 한다.

### 2026-09-01T19:34:08+09:00 response (agent: sub:tech-writer, by nicehugepark)

tech-writer 용어 판정 — 계정 설정 화면 (022)

### 결론

옮길 것은 **개념어**, 그대로 둘 것은 **바깥 세상의 고유명**(GitHub·opus·sonnet·탭 이름), 곁들일 것은 **터미널에서 쳐야 하는 키 이름**이다. 셋째가 이 화면의 특수성이다 — 곁들임은 내가 지어낸 절충이 아니라 **이 파일이 이미 두 자리에서 하고 있는 것**이다: `시간대 … config/settings.json 의 **timezone**`(userform.js:47), `대화 기록 — Stream 탭`(userform.js:58). 새 규칙을 세우지 않고 그 자리를 넓힌다.

가르는 물음 셋 (독자 = 이 계정의 주인. 개발자가 아닐 수 있으나, guide.html:485 가 이미 그에게 `s9 user config <이름> auto_resume on` 을 치라고 가르친 사람이다):
1. **터미널에서 이 이름을 다시 쳐야 하는가** → 키 이름은 곁들임. 화면이 키를 숨기면 안내문·CLI·서버 오류문(bin/s9:6983 "Settings 의 계정 설정에서 auto_resume 을 켜야")이 화면과 다른 세계를 말하게 된다.
2. **바깥 세상에서 이 낱말을 다시 만나는가** → 그대로.
3. **이 화면 밖에서 만난 적이 없는가** → 옮기거나 내린다 (DOC-20260830-006-62x6 개정 ①: 서버에 실재해도 화면에 그려진 적 없는 것은 곱게 옮겨도 지시 대상 없는 명사가 된다).

### A. 기계 이름 10 — 전부 [곁들임]

라벨은 한국어, 키는 그 옆에 작게(`.path` 급). 이유는 하나씩 다르지 않다: 열 개 다 `s9 user config` 로도 바꿀 수 있는 값이고, 사용자가 실제로 물은 것이 "어디서 바꾸지?"였다(QST-20260901-006-62x6). 키가 없으면 화면과 터미널이 같은 값을 다른 이름으로 부른다.

| 키 | 판정 | 근거 |
|---|---|---|
| `auto_resume` | 곁들임 | 「저절로 이어가기」+키. 「자동/저절로」가 참인 유일한 자리가 정책·미래 서술인데(DOC-20260831-005-62x6 규칙 2) 이 키가 바로 그 정책이다. 서버가 이미 이 말을 쓴다(bin/s9:6983 "자동 이어가기가 꺼져 있습니다"). |
| `auto_resume_apply` | 곁들임 | 라벨은 「적용」을 쓰지 못한다 — 아래 C-2 충돌. 뜻은 "이 자리 파일을 고쳐도 좋다"(bin/s9:7076 주석). |
| `auto_resume_gh` | 곁들임 **필수** | 이 화면에서 키를 가장 크게 걸어야 할 칸. 되돌릴 수 없는 결과(바깥 서비스 변경)를 여는 스위치라, 사람이 나중에 "내가 뭘 켰지"를 문서·로그·CLI 어디서든 같은 이름으로 되짚을 수 있어야 한다. |
| `worker_worktree` | 곁들임 | 키는 곁들이되 **라벨에 worktree 의 번역어를 쓰지 않는다** — B-2 참조. |
| `auto_resume_model` | 곁들임 | 「무인 작업이 쓸 모델」+키. 값(opus·sonnet)은 그대로. |
| `s9code_args` | 곁들임 | 값 자체가 기계 문자열(`--model opus` 류)이라 한국어 라벨만으로는 무엇을 넣을지 알 수 없다. 키가 곧 설명의 절반이다. |
| `stream_mirror` | 곁들임 | 라벨 「기록 남기기」는 이미 서 있다(userform.js:62). 키만 없다 — bin/s9:5105 가 사용자에게 `stream_mirror off` 를 치라고 안내하므로 화면에도 있어야 한다. |
| `stream_keep_days` | 곁들임 | 라벨 「보관 기간」 이미 있음(userform.js:68). 위와 같은 이유. |
| `digest_budget` | 곁들임 | **지금 결함**: 키만 있고 한국어 라벨이 없다(userform.js:40). 한 줄 뜻을 얻어야 한다. |
| `timezone` | 곁들임 | **이미 완성형** — 이 화면 곁들임의 표준으로 삼는다(userform.js:47). 손대지 않는다. |

### B. 개념어 9

### B-1. GitHub — [그대로], 로마자 `GitHub`

실측 근거(요청받은 대로 grep 함): 이 저장소의 **화면 문자열에 「깃헙」은 한 번도 없다.** 있는 곳은 셋뿐이고 전부 화면이 아니다 — 코드 주석(web/app/notice.js:34, bin/s9:7085)과 사용자 발화 인용(bin/s9:10944). 반대로 화면은 로마자로 쓴다: `개인 GitHub 계정명`·`조직 GitHub 계정/org`(web/app/userform.js:30-31 — **바로 이 화면**), `개인 GitHub`(web/app/notice.js:32), guide.html 전역.

같은 화면에 이미 선 낱말이 있으면 그것이 이긴다(s9-design 6절: 화면의 기존 용어 사전이 우선한다). 「깃헙」으로 바꾸면 한 화면 안에서 계정 칸은 `GitHub`, 권한 칸은 「깃헙」이 되어 사용자가 **둘을 다른 것으로 읽는다**. 옮길 이유도 없다: GitHub 은 사용자가 브라우저에서 로고로 매일 보는 바깥 세상의 고유명이다.

### B-2. worktree — [옮김], 단 **번역어가 아니라 결과어로**

이 낱말은 이 저장소가 **다섯 번 반려하며 화면에서 내린 낱말**이다. 새로 판정할 것이 아니라 이미 내린 판정을 지키는 자리다.
- REQ-20260829-030-62x6 4차 반려(사용자: "시스템이 사용하는 변수아닌가? 카드에 보여주는건 혼란만 가중") → 카드에서 내림.
- 같은 건 5차 반려(사용자: "사용자는 깃을 전혀 모르는 상태에서도 … 개발자나 엔지니어가 아닌 사용자가 이 시스템을 사용한다고 가정하고 판단해라") → 헤더에서 내림.
- REQ-20260830-001-62x6 → 자리 이름 대신 **결과**를 쓴다. 확정어 `WS_PLACE = {main:"바로 보임", worktree:"끝나면 보임"}`, `WS_MEANS`(web/app/card.js:993·1017).
- REQ-20260830-048-62x6 tech-writer 판정 → 「사본」·「워크트리」는 뺀다. 자리 이름을 지운 뒤 남은 설명형 「따로 떼어 둔 사본」까지 잔재로 걷어냈다.
- DOC-20260830-006-62x6 개정 ① 이 그 실패를 시금석으로 못박았다: 「따로 떼어 둔 사본」은 **있는 사물을 없는 독자에게 부른 실패**.

그러므로 「작업 자리」로도 「따로 떼어 둔 사본」으로도 부르지 않는다(둘 다 이미 기각됨 — 「작업 자리」는 코드 내부 이름으로만 살아 있다). **설정 이름은 사용자가 겪는 결과 두 축으로 짓는다**: ① 켜면 무인 작업이 남의 미커밋 편집을 못 덮는다(안전 — 실사고 근거 bin/s9:7093 주석·REQ-20260826-021-62x6) ② 대신 고친 것이 **끝난 뒤에** 이 화면에 보인다(대가 — 이미 확정된 `WS_MEANS.worktree` 문장 그대로 쓸 수 있다).

### B-3. gh — [그대로], **단 키 안에서만**. 홑낱말로 세우지 않는다

`gh` 는 이 저장소 **화면 어디에도 없다**(grep 결과 0건). 시금석 ①(개정판)에 그대로 걸린다 — 사용자가 한 번도 본 적 없는 이름이다. 「깃헙 명령」은 시금석 ③(그 자리에서 지은 합성어) — `gh` 는 명령이 아니라 명령 꾸러미의 이름이고, 국어에 「깃헙 명령」이라는 연어가 없다. 기각.

옮길 것은 도구 이름이 아니라 **권한**이다. 서버가 이미 정확히 그렇게 적어 뒀다(bin/s9:7078-7086): "그 스위치의 뜻은 … **바깥 서비스의 설정을 바꿔도 좋다**", "사람이 없는 자리에서 도는 작업자에게 깃헙 계정 권한을 준다." 라벨은 그 문장에서 나온다 — 축은 「무인 작업에게 GitHub 계정 권한을 준다」. `gh` 는 키 `auto_resume_gh` 안에 붙어 오는 것으로 족하다.

### B-4. resume — [옮김]. 확정어가 이미 있다

- 동사 = **「이어받다」/「이어가기」** (DOC-20260831-005-62x6 규칙 3; 화면 실물 web/app/card.js:483-484 「▶ 이어가기」).
- 이 키의 정책 서술 = **「저절로 이어가기」** (DOC-20260831-005-62x6 규칙 2 — 「자동/저절로」는 미래·정책 서술로만. auto_resume 이 정확히 "사람이 안 시켜도 저절로 다시 맡는다"라는 정책이다).
- 폐기: 「자동 재작업」·「자동 재개」·「resume」.

### B-5. model — [옮김] 「모델」 / 모델 **이름**은 [그대로]

「모델」은 화면 전역에 이미 섰다: 「지금 쓰는 모델」(web/app/terminal.js:325), 모델 창(web/app/session.js:119-128), 「모델 선택」(web/app/usage.js:10). 별칭 `opus`·`sonnet`·`haiku`·`fable` 은 그대로 — session.js:127-128 이 로마자 그대로 목록에 세우고 있고, 사용자가 `/model opus` 로 치는 값이며 바깥 세상의 제품명이다.

### B-6. stream — [곁들임] 개념은 「대화 기록」, `Stream` 은 탭 이름일 때만

이 화면이 이미 정답을 써 놨다: `대화 기록 — Stream 탭`(userform.js:58). 탭 이름은 로마자 고유명이 규칙이다(web/index.html:117-124 Board·Docs·Graph·Audit·Stream·Terminal·Projects·Settings). 그러니 `Stream` 은 **그 탭을 가리킬 때만** 서고, 값의 뜻은 「대화 기록」이 진다. 유지.

### B-7. digest — [옮김]

화면에 선 적 없는 낱말이다(유일한 등장은 settings.js:163 의 CLI 명령 목록 `s9 digest`, 코드 글꼴). 시금석 ① 에 걸린다. 사용자에게 이것은 "세션이 시작될 때 리드에게 넣어 주는 진행 요약"이고(bin/s9:10921-10929), `digest_budget` 은 그 요약의 **최대 글자 수**다. 「요약」은 이 화면 사전에 이미 있는 낱말이라(판정 카드의 요약, web/app/card.js:1514) 조어가 아니다. 옮긴다.

### B-8. apply — [옮김]. **「적용」은 쓸 수 없다**

「적용」은 이 제품 화면에서 이미 임자가 있다 — "설정이 반영되다": `다른 기기에서도 적용됩니다`(web/app/settings.js:78), `변경 즉시 적용됩니다`(settings.js:80), `고른 뒤 다시 시작을 눌러야 적용됩니다`(terminal.js:325). 셋 다 **같은 Settings 화면 또는 그 이웃**이다. 여기에 "파일을 고쳐도 좋다"라는 둘째 뜻을 얹으면 한 화면에서 「적용」이 두 개념을 진다 — 같은 개념에 두 이름을 만들지 않는다는 규율의 뒷면이다. 축은 「무인 작업이 파일을 직접 고치게 한다」(끄면 고칠 것을 글로만 적어 두고 사람을 기다린다 — CLAUDE.md 11항의 제안 전용 모드).

### B-9. agent(무인 작업자) — [옮김]. 확정어 「무인 작업」/「무인 작업자」

DOC-20260831-005-62x6 규칙 1 이 정한 이름이고 화면 실물이 열 곳 넘게 쓴다(web/app/card.js:64-102·483-484·610, dialog.js:78). `agent`·`worker`·「워커」·「에이전트」는 화면 금지. 개체를 부를 때는 「무인 작업」, 행위자로 부를 때만 「무인 작업자」.

### B-10. on/off — [옮김] 「켬/끔」

같은 파일이 이미 그렇게 쓴다: `켬 — 대화를 남깁니다` / `끔 — 남기지 않습니다`(userform.js:63-64). `on`/`off` 는 저장되는 **값**이지 화면 낱말이 아니다. 새 스위치도 이 짝을 그대로 쓴다.

### C. 충돌 경보 — 이미 임자가 있어 재사용 금지인 낱말 넷

translator 가 후보를 고를 때 이 넷은 피해야 한다. 하나라도 재사용하면 한 화면에서 한 낱말이 두 개념을 진다.

1. **「다시 시작」** = 세션 재기동 전용. web/app/terminal.js:262 가 명시적으로 갈랐다("낱말을 나눠 쓴다 — 세션은 `다시 시작`, 대시보드는 `연결`"). auto_resume 에 쓰지 마라.
2. **「적용」** = 설정 반영 (B-8).
3. **「일손」** = 무인 작업과 **다른 것**이다. card.js:92 가 셋을 나란히 센다: "맡은 창도, 일손도, 무인 작업도 없습니다". auto_resume 계열에 「일손」을 끌어오면 그 구분이 무너진다.
4. **「작업 자리」/「사본」** = worktree 의 기각된 번역어 (B-2). 코드 주석·CSS 클래스명에는 남아 있으나 화면에는 못 선다.

### D. translator 인계 — 옮길 것 목록 (축만 준다. 최종 낱말은 translator 몫)

이미 확정어가 있는 넷은 **새로 짓지 마라** — 그대로 가져다 쓴다.

**이미 확정 — 재사용만**
| 자리 | 확정어 | 출처 |
|---|---|---|
| `auto_resume` 라벨 | 「저절로 이어가기」 | DOC-20260831-005-62x6 규칙 2·3 |
| 무인 주체 | 「무인 작업」/「무인 작업자」 | DOC-20260831-005-62x6 규칙 1 |
| on/off | 「켬/끔」 | userform.js:63-64 |
| `worker_worktree` 의 대가 문장 | 「고친 내용은 작업이 끝난 뒤에 이 화면에 보입니다」 | card.js:1017 `WS_MEANS.worktree` |

**새로 옮길 것 다섯**
| # | 자리 | 옮길 축 (뜻) | 피할 것 |
|---|---|---|---|
| 1 | `auto_resume_gh` 라벨 | 무인 작업에게 **GitHub 계정 권한**을 준다 = 바깥 서비스의 설정을 바꿀 수 있게 된다. 무게 문장 필수(되돌릴 수 없음) | `gh` 홑낱말, 「깃헙」, 「깃헙 명령」 |
| 2 | `auto_resume_apply` 라벨 | 무인 작업이 **파일을 직접 고친다** (끄면 고칠 내용을 적어 두고 사람을 기다린다) | 「적용」 |
| 3 | `worker_worktree` 라벨 | ① 안전: 무인 작업이 **남이 저장 안 한 작업을 덮지 않는다** ② 대가: 결과가 **끝난 뒤에** 보인다 | 「워크트리」, 「작업 자리」, 「사본」, 「격리」 |
| 4 | `auto_resume_model` 라벨 | **무인 작업이 쓸 모델** (내가 지금 쓰는 모델과 별개) | 「에이전트 모델」 |
| 5 | `digest_budget` 라벨 | 세션이 시작될 때 넣어 주는 **진행 요약의 최대 길이(글자 수)** | 「다이제스트」, 「예산」(돈으로 읽힌다) |

`s9code_args` 는 옮길 개념어가 없다 — 값이 기계 문자열이라 라벨은 "무엇에 쓰는 칸인가" 한 줄이면 된다(세션을 띄울 때 얹을 추가 인자). ux-writer 몫.

### E. 표면별 적용 지점

전부 `web/app/userform.js` 한 화면(Settings → 내 계정)이다. 현재 `기타: {...}` JSON 한 줄(118행)이 삼키고 있는 값들이 여기서 자기 자리를 얻는다.
- **8행 `extraCfg` 제외 목록**: 자리를 얻는 키를 여기 더해야 `기타:` 줄에서 빠진다. 지금 제외된 것은 timezone·digest_budget·ui_*·stream_*·external_secrets_path 뿐 — auto_resume 4종·worker_worktree·s9code_args 를 추가해야 한다. 안 더하면 **같은 값이 화면에 두 번 선다**.
- **40행**: `digest_budget` 이 키만 있고 라벨이 없는 자리 → D-5.
- **47행**: timezone 곁들임 — **표준 형식**. 새 항목의 키 표기를 여기에 맞춘다.
- **58~70행**: 대화 기록(stream) 절 — 라벨은 유지, 키 곁들임만 추가.
- **새 절**: 무인 작업 설정 4종(auto_resume·apply·gh·worker_worktree·model). `auto_resume_gh` 는 이 절에서 무게가 다른 칸이므로 같은 밀도로 늘어놓지 않는다(designer 몫).
- **문서 화면 메타 표**의 `workspace` 칩(card.js `WS_PLACE`)은 **건드리지 않는다** — 같은 사실이 두 자리에서 다른 말을 하게 된다.

### F. 한계

- 이 판정은 **화면 표면**에만 미친다. 코드 주석·CLI 출력·docs/ 의 기술어(worktree·gh·commit)는 대상이 아니다 — 제품 원문의 기술어는 존중한다.
- 「커밋」은 이 판정 밖이지만 인접해 있어 적어 둔다: REQ-20260830-048-62x6 이 **그대로 두기**로 판정했다(저장소를 직접 다루는 사람이 실제로 치는 명령 이름). worker_worktree 설명문에 커밋이 나올 수 있는데, 그때 다시 판정하지 마라.
- 최종 낱말은 translator 가 정한다. 위 D 표는 **뜻의 축**이지 문안이 아니다.

### 2026-09-01T19:34:15+09:00 subagent (agent: sub:subagent:af5245f7, by nicehugepark)

Appending verdict note to REQ-20260901-022-62x6

### 2026-09-01T19:34:35+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

### 리드 판정 — `auto_resume` 의 이름 (ux-writer ↔ translator 충돌)

두 사람이 정면으로 갈렸다. ux-writer 는 「무인 작업 맡기기」, translator 는
카드가 이미 쓰는 「자동 이어받기」다. **ux-writer 쪽으로 판정한다.**

근거는 코드다(bin/s9:6930~6983). 이 가드는 **모든 스폰 경로**가 지나고,
`reason="wake"` — 사람이 대시보드에서 ▶ 를 누른 것 — 도 그중 하나다. 그래서
계정 스위치를 끄면 **사람이 눌러도 안 뜬다.**

translator 가 낸 끔 문장이 그 사실과 어긋난다:
「▶ 이어가기를 누를 때까지 아무것도 뜨지 않습니다」 — 실제로는 눌러도 안 뜬다.
화면이 거짓말을 하게 된다. 이름을 그대로 두면 이 거짓말을 피할 길이 없다.

그리고 translator 의 원칙(한 개념에 두 이름을 만들지 않는다)은 옳지만 **여기
적용되지 않는다** — 둘은 한 개념이 아니다:
- 카드의 「자동 이어받기 끄기」 = 이 요청 하나가 저절로 뜨지 않게 하는 정책.
  사람이 누르면 뜬다.
- 계정의 이 스위치 = 이 계정에서 **무인 작업자를 아예 쓰지 않겠다**. 누가
  시작하든 안 뜬다.
범위도 다르고 사람의 손이 통하느냐도 다르다. 다른 것에 다른 이름을 주는 것이
같은 이름을 주는 것보다 안전하다.

그래서 계정 스위치는 **「무인 작업 맡기기」**, 카드의 정책은 그대로
「자동 이어받기 끄기」다. 두 이름이 갈려 있는 것이 곧 두 범위가 갈려 있다는
표시가 된다.

translator 의 나머지 낱말(파일 직접 고치기 · 따로 떼어 놓고 일하기 ·
기록 남기기 · 보관 기간 · 요약 분량 · 켬/끔)은 근거가 화면 안에 있어 그대로
간다. 「따로 떼어 놓고」를 명사로 부활시키지 않고 동사구로만 쓴 판단도 유지.

### 함께 고칠 것 (둘 다 같은 자리를 짚었다 — 실물이다)

bin/s9:6982 의 차단 문구가 두 번 틀렸다:
① 「자동 이어가기」 — 화면에 없는 **세 번째 이름**이다.
② `auto_resume 을 켜야` — 사용자에게 원시 키를 외우게 한다. 이 요청이
   없애려는 바로 그 증상이 서버 문장에 그대로 남아 있다.
새 문구: 「<계정>은 무인 작업 맡기기가 꺼져 있습니다 — Settings 의 계정
설정에서 켜 주세요」. bin/s9:17699 의 「본인 또는 admin만 설정 변경 가능」도
문장이 아닌데 화면이 그대로 띄운다 — ux-writer 가 낸 대체 문안을 쓴다.

### 2026-09-01T19:34:43+09:00 subagent (agent: sub:tech-writer:a99e895f, by nicehugepark)

`[2026-09-01 19:29:01 KST - tech-writer]`

판정을 `REQ-20260901-022-62x6` 에 `--label response --agent tech-writer` 로 남겼다(8475자). 요지:

### 결론

**옮길 것은 개념어, 그대로 둘 것은 바깥 세상의 고유명, 곁들일 것은 터미널에서 쳐야 하는 키 이름.** 셋째가 이 화면의 특수성인데, 내가 지어낸 절충이 아니다 — `web/app/userform.js` 가 이미 두 자리에서 하고 있다: `시간대 … config/settings.json 의 timezone`(47행), `대화 기록 — Stream 탭`(58행). 새 규칙을 세우지 않고 그 자리를 넓혔다.

### 물으신 넷에 대한 답

**GitHub → 로마자 `GitHub`.** grep 결과가 갈랐다: 이 저장소 **화면 문자열에 「깃헙」은 0건**이다. 있는 곳은 코드 주석(`web/app/notice.js:34`, `bin/s9:7085`)과 사용자 발화 인용(`bin/s9:10944`)뿐. 반대로 화면은 `개인 GitHub 계정명`·`조직 GitHub 계정/org`(`web/app/userform.js:30-31` — **바로 이 화면**), `개인 GitHub`(`notice.js:32`). 「깃헙」으로 쓰면 한 화면에서 계정 칸은 `GitHub`, 권한 칸은 「깃헙」이 되어 사용자가 둘을 다른 것으로 읽는다.

**worktree → 화면에 세우지 않는다.** 새로 판정할 자리가 아니라 **다섯 번 반려로 이미 내려진 낱말**이다: REQ-20260829-030-62x6 4차(카드에서)·5차("사용자는 깃을 전혀 모르는 상태에서도"), REQ-20260830-001-62x6(자리 이름이 아니라 결과 — `WS_PLACE`), REQ-20260830-048-62x6(「사본」·「워크트리」는 뺀다), DOC-20260830-006-62x6 개정 ①(「따로 떼어 둔 사본」= 있는 사물을 없는 독자에게 부른 실패). 「작업 자리」도 기각된 쪽이다. 라벨은 결과 두 축으로 짓는다 — ① 남이 저장 안 한 작업을 덮지 않는다 ② 대신 결과가 끝난 뒤 보인다(확정 문장 `card.js:1017` 재사용).

**gh → 홑낱말로 세우지 않는다.** 화면 등장 0건이라 시금석 ①에 걸리고, 「깃헙 명령」은 시금석 ③(그 자리에서 지은 합성어)에 걸린다. 옮길 것은 도구 이름이 아니라 **권한**이고, 서버가 이미 그렇게 적어 뒀다(`bin/s9:7078-7086`: "바깥 서비스의 설정을 바꿔도 좋다", "작업자에게 깃헙 계정 권한을 준다").

**키 이름 → 열 개 전부 곁들임.** 부딪히는 두 규율 중 어느 쪽도 지지 않는다. 라벨은 한국어가 지고 키는 그 옆에 작게. 근거: `web/guide.html:485` 가 이미 사용자에게 `s9 user config <이름> auto_resume on` 을 치라고 가르쳤고, 서버 오류문도 `auto_resume` 을 그대로 부른다(`bin/s9:6983`). 화면이 키를 숨기면 안내문·CLI·서버가 화면과 다른 세계를 말한다.

### 충돌 경보 넷 (translator 가 반드시 피할 것)

1. **「다시 시작」** = 세션 재기동 전용. `web/app/terminal.js:262` 가 명시적으로 갈랐다 — auto_resume 에 못 쓴다.
2. **「적용」** = 설정 반영 전용(`settings.js:78,80`·`terminal.js:325` — 전부 같은 Settings 화면). `auto_resume_apply` 에 못 쓴다.
3. **「일손」** ≠ 무인 작업. `card.js:92` 가 셋을 나란히 센다("맡은 창도, 일손도, 무인 작업도 없습니다").
4. **「작업 자리」/「사본」** = worktree 의 기각된 번역어.

### translator 인계

이미 확정어가 있는 넷은 **새로 짓지 마라**: 「저절로 이어가기」(DOC-20260831-005-62x6 규칙 2·3) · 「무인 작업/무인 작업자」(규칙 1) · 「켬/끔」(`userform.js:63-64`) · `WS_MEANS.worktree` 문장.

새로 옮길 것은 다섯뿐이다 — `auto_resume_gh`(GitHub 계정 권한 + 무게) · `auto_resume_apply`(파일을 직접 고친다) · `worker_worktree`(안전/대가 두 축) · `auto_resume_model`(무인 작업이 쓸 모델) · `digest_budget`(진행 요약의 최대 글자 수). `s9code_args` 는 옮길 개념어가 없다(값이 기계 문자열 — ux-writer 몫).

### 구현 쪽에 넘길 함정 하나

`web/app/userform.js:8` 의 `extraCfg` 제외 목록에 자리를 얻는 키를 **반드시 더해야** 한다. 안 더하면 같은 값이 새 자리와 `기타: {...}` 줄에 **두 번 선다**. 지금 제외된 것은 timezone·digest_budget·ui_*·stream_*·external_secrets_path 뿐이다.

파일: `/home/sjpark1/section9/web/app/userform.js` · `/home/sjpark1/section9/web/app/card.js`(993·1017행 `WS_PLACE`/`WS_MEANS` — 재사용만, 수정 금지) · `/home/sjpark1/section9/bin/s9`(6983·7076-7086·18317행 의미 원천).

### 2026-09-01T19:35:49+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

### 리드 판정 2 — 낱말 확정 (tech-writer 착지 뒤)

셋의 판정이 겹치는 곳과 갈리는 곳을 정리한다. **갈린 것은 둘뿐이고 둘 다 닫았다.**

### 갈린 것 ①: GitHub / 깃헙 → **로마자 `GitHub`**
tech-writer 가 실측으로 갈랐다: 이 저장소 **화면 문자열에 「깃헙」은 0건**이고,
있는 곳은 코드 주석과 사용자 발화 인용뿐이다. 반대로 바로 이 폼이 이미
`개인 GitHub 계정명`·`조직 GitHub 계정/org` 를 쓴다(userform.js:30-31).
한 화면에서 계정 칸은 `GitHub`, 권한 칸은 「깃헙」이면 사용자가 둘을 다른
것으로 읽는다. translator 도 `GitHub` 로 냈으니 실제로 갈린 것은 ux-writer 의
확인 창 문구 하나다 — 거기의 「깃헙」을 `GitHub` 으로 고친다.

### 갈린 것 ②: `auto_resume` 의 이름 → **「무인 작업 맡기기」** (앞선 판정 유지)
tech-writer 가 「저절로 이어가기」를 "이미 확정어"라며 인계했는데, **그 낱말은
DOC-20260831-005-62x6 에 없다.** 그 문서에 있는 것은 규칙 1 「무인 작업」·규칙 3
「이어받다」·그리고 뒤에 반려된 「저절로 이어지지 않게 하기」다. 카드의 실제
라벨도 `card.js:27` 의 「자동 이어받기 끄기」다. 없는 확정어를 근거로 삼을 수
없다.
그리고 「무인 작업 맡기기」는 DOC-20260831-005-62x6 와 어긋나지 않는다 — 규칙 1 이
정한 그 낱말(「무인 작업」)을 그대로 쓰고, 규칙 2 가 정책·미래에만 허락한
「자동/저절로」를 쓰지 않는다. 이 스위치는 미래 정책만이 아니라 **지금 누른
손까지** 막으므로 그 편이 정확하다.

### 셋이 겹친 것 (그대로 간다)
- worktree 는 화면에 세우지 않는다 — 다섯 번 반려로 이미 내려진 낱말이다.
  결과 두 축(남의 편집을 안 덮는다 / 끝난 뒤에 보인다)으로 말한다.
  translator 의 「따로 떼어 놓고 일하기」가 그 자를 지킨다(명사 부활 없음).
- `gh` 를 홑낱말로 세우지 않는다 — 옮길 것은 도구 이름이 아니라 **권한**이다.
- 키 이름은 열 개 전부 **곁들인다**(라벨은 한국어, 키는 옆에 작게). 근거는
  화면 밖에 있다: guide.html:485 가 사용자에게 `s9 user config … auto_resume on`
  을 치라고 가르치고, 서버 오류문도 그 키를 그대로 부른다. 화면이 키를 숨기면
  안내문·CLI·서버가 화면과 다른 세계를 말하게 된다.

### 구현에 반드시 넘길 함정 (tech-writer)
`web/app/userform.js:8` 의 `extraCfg` 제외 목록에 **자리를 얻은 키를 모두
더해야** 한다. 안 더하면 같은 값이 새 자리와 「기타: {...}」 줄에 두 번 선다.

### 2026-09-01T19:36:19+09:00 response (agent: sub:designer, by nicehugepark)

### 설정을 화면에서 바꾼다 — 구조·시각 설계 (designer)

### 0. 지금 화면을 보고 잡은 사실 (근거 캡처 첨부)

`#settings/account` 를 1440×3400 으로 뜬 결과: 「내 계정」 한 판이 이미 **덩이 여덟**이다
(프로필 · 머신 이력 · 개인화 설정 · 시간대 · 대화 기록 · 비밀 키 · 바깥 폴더 · 개인 선호),
세로 1,400px 를 넘는다. 맨 아래 마지막 줄이 문제의 그 줄이다 —

    기타: {"auto_resume":"on","auto_resume_apply":"on","s9code_args":"--permission-mode auto
    --model opus","auto_resume_model":"opus","auto_resume_gh":"on","worker_worktree":"on"}

`.path` 흐린 mono 10px, 스크롤 맨 끝, 손잡이 없음. **깃헙 계정 권한을 연 스위치가 이 줄의
62번째 글자**로 서 있다. QST-20260901-006-62x6 이 "어디서 하는거지"로 끝난 것은 당연하다.

### 1. 자리 — 좌측 목록에 구역을 하나 더 낸다

**채택: Settings 좌측 목록 다섯 줄 — 디스플레이 / 내 계정 / 「자동 진행」 / 사용자 관리 / 시스템.**
(새 구역은 「내 계정」 다음, 「사용자 관리」 앞 — 목록은 이미 나 → 나 → 남 → 전체 순이다.)

대안 비교
- (A·채택) 새 구역. 좌측 목록이 이미 「결정 영역별 목록」이라 줄 하나 추가는 구조 비용이 0이고,
  되돌리기도 라우팅 한 줄이다.
- (B) 「내 계정」 안에 덩이 하나 더. **기각** — 캡처가 근거다. 여덟 덩이에 아홉째를 얹으면
  2,000px 를 넘고, 「내가 누구인가(이름·이메일·시간대)」와 「내가 없을 때 무엇이 일어나는가」를
  한 스크롤에 섞는다. 한 화면 한 결정 위반.
- (C) Board/Terminal 쪽(작업자가 사는 곳)에 두기. **기각** — 설정이 두 군데로 갈려
  "어디서 하는거지"를 그대로 다시 만든다.

이름은 ux-writer·tech-writer 판정 몫이다. 내 권고는 **「자동 진행」**(부제 `사람이 없을 때
어디까지 할지`) — 이 구역의 여섯 스위치는 전부 한 주어를 공유한다: **무인 작업자**.
차선은 「무인 작업」(bin/s9 주석·CLAUDE.md 가 이미 쓰는 집안 낱말). 조어 금지 시금석상
둘 다 통과(일상 직무어·확립 용어), 「맡은 일손」류 합성은 금지.

좌측 목록의 부제는 상태를 겸한다 — 「내 계정」이 `⚠ 미기재 N` 을 다는 그 자리에,
gh 가 켜져 있으면 `사람이 없을 때 어디까지 · 깃헙 권한 켬`. **목록만 봐도 열려 있는 문이 보인다.**

### 구역 안의 덩이 (기존 `.cfg-h` 어휘 그대로)

1. **이어받기** — `auto_resume` (윗스위치)
2. **일하는 범위** — `auto_resume_apply` · `auto_resume_gh` · `worker_worktree`
3. **모델과 인자** — `auto_resume_model` · `s9code_args`
4. **한도** — 숫자 8개, **기본 접힘**
5. **아직 이름 없는 값** — 모르는 `auto_resume_*`/`worker_*` 키 (0개면 덩이 자체를 안 그린다)

`digest_budget` · `timezone` · `stream_*` · 개인 선호 · 비밀은 「내 계정」에 그대로 남는다 —
그 값들은 작업자가 아니라 나를 말한다. 「내 계정」에서 **없어지는 것은 `기타: {…}` 한 줄뿐**이고,
그 자리엔 아래 5절의 표가 선다.

### 2. 무게 — 색면 없이, 세 겹으로

되돌릴 수 없는 스위치는 `auto_resume_gh` 하나다(bin/s9:7082 주석: "이 봉투는 경계가 아니다.
gh api 하나로 저장소 설정 대부분을 바꿀 수 있고 지울 수도 있다"). 무게는 **문장 · 줄 · 마찰**
세 겹으로 지고, 어느 겹도 면을 칠하지 않는다.

**(1) 문장** — 뜻 한 줄이 무엇이 열리는지 그대로 말한다. 무게의 8할은 여기서 진다.

**(2) 사실 줄 — 켜져 있는 동안에만 선다.** 카드 사실 줄 규칙(「정상이 아니다」 또는 「당신이
할 일이 있다」일 때만 줄이 선다)의 그대로의 재적용이다.
- `gh = 끔` → 다른 행과 똑같이 한 줄. 아무것도 소리치지 않는다.
- `gh = 켬` → 뜻 줄 아래 한 줄이 더 선다. `--c-blocked` **글자색**, mono 10.5px, 면 없음:
  「지금 켜져 있습니다 — 사람 없이 도는 작업자가 @이름 계정으로 깃헙 저장소를 바꿀 수 있습니다.」
- 이 설계의 값어치: **위험이 실제로 켜져 있는 동안에만 눈에 든다.** 상시 경고 마크(⚠·자물쇠)는
  사흘이면 벽지가 된다 — 강조 셋은 강조 없음과 같다.
- 예외 하나: 윗스위치(`auto_resume`)가 꺼져도 **gh 의 사실 줄은 흐려지지 않는다.**
  흐리게 죽이면 "권한을 준 적 있다"는 사실이 화면에서 사라진다.

**(3) 마찰 — 켤 때만 판정 창.** `s9dlg` (네이티브 confirm 금지):
`{kind:"confirm", stop:true, cap:"권한", title:"@이름 의 깃헙 권한을 주시겠습니까?",
desc:"사람이 보지 않는 자리에서 도는 작업자가 이 계정으로 깃헙을 다룹니다. 저장소 설정을 바꾸고
지울 수 있고, 지운 것은 이 화면에서 되돌릴 수 없습니다.", ok:"권한 주기", cancel:"그만두기"}`
**끌 때는 창이 없다** — 안전한 쪽으로 가는 손에 마찰을 물리지 않는다. 나머지 다섯 스위치도 창 없음
(되돌리기가 한 번 더 누르는 것뿐인 일에 확인을 겹치지 않는다).

대안 비교 — (B) 위험 스위치를 별도 덩이로 격리: **기각.** 격리 자체가 「여기는 위험구역」이라는
색면 없는 색면이고, 꺼져 있을 때도 무겁게 읽혀 사람이 그 덩이를 아예 안 연다.
(C) 상시 마크: **기각**(위 참조). 부수 이득 하나 더 — 마크를 안 쓰니 **새 기하가 없다**.
홀수 px 마크·소수 테두리가 없으므로 125/150% 배율에서 갈릴 자리 자체가 생기지 않는다.

### 3. 한 항목의 해부

**부품을 새로 만들지 않는다.** 이 구역의 표는 `.metatbl` 그대로다 — 스킨 5벌(ledger/calm/glass/
slate/soft) override 가 이미 다 있고(density.css:18, docs.css:262, calm.css:81, skins.css:242),
손잡이는 `.uf select` / `.uf input` 그대로다. 새 토글 알약을 그리면 (a) pill 금지에 걸리고
(b) 켬/끔이 색으로만 읽히고 (c) 스킨 수만큼 부채가 는다.

한 행(`<tr>`) =

    ┌ 첫 칸(190px) ────────┬ 둘째 칸 ─────────────────────────────────────┐
    │ 깃헙 권한 주기        │ [ 켬 — 깃헙을 다룹니다        ▾ ]            │
    │ auto_resume_gh       │ 사람 없이 도는 작업자가 이 계정으로 깃헙을      │
    │                      │ 다룹니다 — 저장소 설정을 바꾸고 지울 수도…      │
    │                      │ 지금 켜져 있습니다 — …            ← 켤 때만    │
    └──────────────────────┴─────────────────────────────────────────────┘

- **첫 칸 = 이름 두 줄**: 사람 이름(본문체 12.5px) + 그 아래 **실제 키 이름**(mono 10px, `--faint`).
  키 이름을 지우지 않는 것이 이 REQ 의 핵심이다 — 터미널에서 `auto_resume_gh` 를 켠 사람이
  화면에서 **그 글자를 눈으로 찾을 수 있어야** "어디서 하는거지"가 끝난다. `Ctrl+F` 로도 걸린다.
  기본 `.metatbl td:first-child` 는 `width:150px; white-space:nowrap` 인데 최장 키
  (`auto_resume_global_per_hour`, 27자)가 잘린다 → 이 구역에서만 **190px · normal** 로 재정의(한 줄).
- **둘째 칸 = 손잡이 → 뜻 → (조건부) 사실 줄**. 뜻은 `.path` 12px `--muted`, `max-width:62ch`
  (`.secnote` 가 이미 세운 자).
- 손잡이는 값이 둘이면 `<select>` 이고 **옵션 글자가 상태를 말한다**: `켬 — 깃헙을 다룹니다` /
  `끔 — 다루지 않습니다`. 대화 기록 select 가 쓰는 그 형식 그대로 — **색 없이 상태가 읽힌다.**
- 과녁: 이름 글자에 `<label for>` 를 물려 과녁을 넓힌다(칠이 아니라 과녁을 넓히는 정공법).

**밀도** — 10행이 넘을 때
- 뜻 한 줄은 **결정을 바꾸는 여섯 행에만** 붙인다. 여섯 행 × (28px 손잡이 + 2줄 뜻) ≈ 420px,
  덩이 제목 셋 포함 500px 남짓. 전용 구역이므로 감당된다(지금 「내 계정」의 1/3).
- **숫자 한도 여덟은 접는다.** 기본 접힘 + `+ 한도 8개 보기` 텍스트 버튼(mono·밑줄 hover —
  「더 보기」 어휘 그대로). 접힌 머리는 반드시 사실을 말한다: `한도 8개 — 전부 기본값입니다` /
  `한도 8개 — 3개를 바꿨습니다`. **접힌 것이 숨긴 것이 되면 안 된다.**
- 한도 행에는 뜻 줄이 없다 → **이름이 뜻을 겸하게 길게 짓는다**(「하루에 깨울 수 있는 횟수」,
  「한 번에 동시에 도는 수」). 빈 칸 = 기본값, 기본값은 placeholder 에 적는다
  (`stream_keep_days` 의 `placeholder="7"` 손버릇 그대로).

### 4. 저장의 얼굴 — 즉시 저장, 그 행에서 말한다

**단추 없음. 이 구역에는 「설정 저장」 단추를 두지 않는다.**
- 두 값 중 하나를 고르는 손잡이(스위치 넷) → `change` 에 즉시 저장.
- 자유 문자열·숫자(모델·인자·한도) → `change`(= blur 또는 Enter)에 저장. `#cf-streamdays` 와 같은 손버릇.
- 예외는 gh 켜기 하나 — 판정 창을 먼저 지나고, **그만두기면 손잡이를 원래 값으로 되돌린다.**

근거(대안 B 기각): 이 폼은 **재렌더 없이 살아 있어서** 낡은 값이 방금 고른 값을 덮는 사고가
이미 났다 — userform.js:157 주석이 timezone 을 일괄 저장 목록에서 뺀 이유가 그것이다.
스위치 열 개를 한 단추로 묶으면 그 사고가 열 배가 된다. 게다가 "켰는데 왜 안 켜지지"가 생긴다
(userform.js:188 주석이 이미 적어 둔 판단). 되돌리기는 한 번 더 누르는 것 — 확인보다 되돌리기.

**결과가 서는 자리 = 그 행의 뜻 줄 자리.** 새 메시지 칸을 만들지 않는다.
- 저장 중 → `저장하는 중…` (`--faint`)
- 성공 → `켰습니다 — 이제 …` 3초, 그 뒤 **뜻 문장으로 되돌아온다**(사라지는 것이 아니다)
- 실패 → `✕ <서버가 준 문장 그대로>` (`--c-blocked`), 되돌아오지 않는다(사람이 읽고 조치할 것),
  그리고 **손잡이를 이전 값으로 되돌린다** — 화면이 켜졌다고 보여 주는데 서버는 껐다면 그 화면은
  거짓말이다. 문장은 서버 것을 그대로 쓴다(REQ-20260901-018-62x6 이 세운 「서버의 문장이 곧 팝업이다」).
- 연결 끊김 → `postJSONRaw` 가 이미 「서버에 연결할 수 없습니다」를 준다. **팝업 금지, 인라인**
  (userform.js:150 의 그 규율).
- **흔들리지 않게**: 뜻 줄과 메시지 줄은 **같은 격자 한 칸에 겹쳐 둔다**(`display:grid`, 둘 다
  `grid-area:1/1`, 하나는 `visibility:hidden`). 높이 = 둘 중 큰 쪽으로 고정되어, 열 행이
  저장할 때마다 아래가 밀리는 일이 없다.

**윗스위치가 꺼졌을 때**: `auto_resume = off` 면 「일하는 범위」 세 행과 「모델」 행을
`tr.rowoff`(density.css:85, opacity .45) + `disabled`. 대화 기록의 보관 기간이 쓰는 손버릇 그대로.
**값은 지우지 않는다** — 껐다 켜면 그대로 돌아온다. 덩이 머리 아래 한 줄로 그걸 말한다:
`이어받기를 끄면 아래 값은 쓰이지 않습니다 — 지우지는 않습니다.` (gh 사실 줄은 위 2절대로 예외.)

**나머지 얼굴들**: 미등록 계정 → 좌측 목록 부제가 「미등록 계정」, 판은 「내 계정」과 같은 문장.
목록을 못 받음 → 「내 계정」이 이미 세운 `다시 불러오기` 단추(settings.js:98). **빈 상태는 없다** —
여섯 스위치는 값이 하나도 없는 계정에서도 늘 서고 전부 「끔」으로 읽힌다. 0개일 수 있는 것은
5절의 목록뿐이고, 그때는 덩이를 그리지 않는다(개인 선호가 이미 그 손버릇).

**남의 계정을 볼 때(admin)**: 좌측 목록의 「자동 진행」은 내 것만이다. admin 이 남의 것을 만지는
자리는 「사용자 관리」 편집 판 안에 같은 부품을 덩이로 얹는 것 — 서버가 대리를 받으므로(bin/s9:17696)
가능하고, 비밀 키가 대리를 안 받는 것과 대칭이다. 다만 **판정 창과 사실 줄은 그 계정 이름을 말한다**
(`@이름 의 깃헙 권한을…`) — REQ-20260901-017-62x6 의 「화면은 세션이 붙은 그 계정을 말한다」의 연장.

### 5. 모르는 키의 자리 — 접두사로 갈라 보내고, 부품은 하나

화면이 뜻을 아는 키 목록을 하나 두고, 그 목록에 없으면 **접두사로 자리를 정한다**:
- `auto_resume_*` · `worker_*` → 「자동 진행」 맨 아래 **「아직 이름 없는 값」**
- 그 밖 → 「내 계정」 개인화 설정 아래 **「그 밖의 값」**

**부품은 한 벌이고, 이미 화면에 있다** — 개인 선호 표(`#pf-table`)가 정확히 이 모양이다:
한 행에 키 하나, mono 키 이름 + 값 입력칸 + `빈 값으로 저장 = 삭제`. 즉 JSON 한 줄을
**행으로 펴는 것**이 전부고, 새로 만들 부품이 없다.
그 아래 한 줄: `화면이 아직 뜻을 모르는 값입니다 — 이름과 값은 그대로 쓰이고 있습니다.`
(모른다고 말하되 고장으로 읽히지 않게.)

**아는 키가 늘면 이 목록에서 빠지고 제자리로 올라간다.** 화면이 깨지는 대신 조용히 승격한다 —
「모르는 키가 새로 생겨도 화면이 깨지지 않아야 한다」의 답이 이것이다. 반대로 **아는 키가 config 에
없어도 행은 선다**(기본값을 읽어 「끔」/placeholder 로) — 있는 것만 그리면 아직 안 켠 스위치는
영영 안 보인다. **행 목록은 config 가 아니라 화면이 정한다.**

### 6. 문구 초안 (ux-writer·tech-writer·translator 판정 대상 — 자리와 형식만 내가 정했다)

| 이름 | 키 | 뜻 한 줄 |
|---|---|---|
| 이어받기 | auto_resume | 반려하거나 승인한 뒤, 사람이 없어도 작업자가 그 자리에서 이어 갑니다. |
| 파일 고치기 | auto_resume_apply | 그 작업자가 이 저장소의 파일을 직접 고칩니다. 끄면 고칠 내용을 적어 두기만 합니다. |
| 깃헙 권한 주기 | auto_resume_gh | 그 작업자가 이 계정으로 깃헙을 다룹니다 — 저장소 설정을 바꾸고 지울 수도 있습니다. |
| 따로 떼어 일하기 | worker_worktree | 작업자에게 저장소 사본을 하나 내줍니다. 남이 아직 저장하지 않은 편집을 덮지 않습니다. |
| 작업자 모델 | auto_resume_model | 사람 없이 도는 작업자가 쓰는 모델. |
| 새 창 인자 | s9code_args | `s9 code` 로 창을 열 때 늘 붙는 인자. 여기 적은 `--model` 도 정책으로 읽힙니다. |

### 7. 구현이 확인할 것 (프론트 몫)

- 190px 첫 칸이 최장 키(`auto_resume_global_per_hour`)를 안 자르는지 — 실측으로 확정할 것.
- 62ch 뜻 줄이 폭 1100px(좌측 목록 330px 를 뺀 나머지)에서 몇 줄이 되는지, 겹친 격자 칸의
  높이가 저장 전후로 같은지.
- **hover·focus 얼굴**: select 와 `+ 한도 8개 보기` 텍스트 버튼을 실포인터(CDP)로 얹어 찍을 것.
  정적 캡처에는 쉼 얼굴만 나온다.
- 스킨 5벌 × 톤 4벌에서 사실 줄의 `--c-blocked` 가 배경에 묻히지 않는지 — 특히 terminal 처럼
  통째로 반전하는 스킨.
- 새 기하가 없으므로 배율 검증은 1·1.25·1.5 에서 표 선 두께만 보면 된다.

### 8. 이 설계가 손대지 않는 것

백엔드 없음(`POST /api/user/config` 그대로). 새 CSS 컴포넌트 없음(`.metatbl`·`.uf`·`.path`·
`.cfg-h`·`tr.rowoff`·`.secmsg` 재사용, 신규는 첫 칸 폭 재정의와 겹친 격자 칸 둘뿐).
색면·좌측 세로 띠·무채색 미니멀 없음 — 색이 쓰이는 자리는 사실 줄의 **글자 하나**뿐이다.

[Image: assets/REQ-20260901-022-62x6/now-account-tall.png]
[Image: assets/REQ-20260901-022-62x6/ledger-narrow.png]

### 2026-09-01T19:36:38+09:00 subagent (agent: sub:subagent:a92db857, by nicehugepark)

Verifying note and attachments in REQ-20260901-022-62x6

### 2026-09-01T19:37:00+09:00 subagent (agent: sub:designer:ac9f7eb4, by nicehugepark)

`[2026-09-01 19:41:07 KST - designer]`

REQ-20260901-022-62x6 구조·시각 설계를 문서에 남겼다 (`s9 note … --label response --agent designer`, 캡처 2장 첨부). 요지는 다음과 같다.

**근거로 삼은 화면.** `#settings/account` 를 1440×3400 으로 떠 보니 「내 계정」 한 판이 이미 덩이 여덟에 세로 1,400px 를 넘고, 맨 끝 줄이 문제의 `기타: {…}` 다 — **깃헙 권한을 연 스위치가 그 줄의 62번째 글자**로 서 있다. "어디서 하는거지"는 당연한 결과다.

**1. 자리 — 좌측 목록에 구역을 하나 더.** 디스플레이 / 내 계정 / **자동 진행** / 사용자 관리 / 시스템. 여섯 스위치가 한 주어(무인 작업자)를 공유하고, 「내 계정」 안에 아홉째 덩이로 얹으면 「내가 누구인가」와 「내가 없을 때 무엇이 일어나는가」가 한 스크롤에 섞인다. 좌측 목록 부제가 상태를 겸한다 — gh 가 켜져 있으면 목록만 봐도 `· 깃헙 권한 켬` 이 보인다.

**2. 무게 — 색면 없이 세 겹.** ① 뜻 문장이 무엇이 열리는지 말한다 ② **사실 줄은 켜져 있는 동안에만 선다** (`--c-blocked` 글자색, 면 없음) — 카드 사실 줄 규칙(「정상이 아니다」일 때만 줄이 선다)의 재적용이고, 상시 ⚠ 마크는 사흘이면 벽지가 되므로 기각 ③ **켤 때만** `s9dlg` 판정 창, 끌 때는 없다. 마크를 안 쓰니 새 기하가 없어 125/150% 배율에서 갈릴 자리도 없다.

**3. 한 행 — 새 부품 0.** `.metatbl` + `.uf select` 그대로(스킨 5벌 override 가 이미 있다). 첫 칸에 사람 이름 + **실제 키 이름(mono)** 을 함께 둔다 — 터미널에서 `auto_resume_gh` 를 켠 사람이 화면에서 그 글자를 눈으로 찾을 수 있어야 이 REQ 가 끝난다. 신규 CSS 는 첫 칸 폭 재정의(150→190px, 최장 키가 잘린다)와 겹친 격자 칸 둘뿐. 숫자 한도 여덟은 접되 접힌 머리가 `전부 기본값입니다`/`3개를 바꿨습니다` 를 말한다.

**4. 저장 — 즉시, 그 행에서.** 「설정 저장」 단추 없음. 근거는 이 저장소가 이미 겪은 사고다 — 이 폼은 재렌더 없이 살아 있어 낡은 값이 방금 고른 값을 덮었고(userform.js:157 이 timezone 을 일괄 저장에서 뺀 이유), 열 스위치를 한 단추로 묶으면 그게 열 배가 된다. 결과는 뜻 줄 자리에서 말하고(성공은 3초 뒤 뜻으로 되돌아온다), **실패하면 손잡이를 이전 값으로 되돌린다** — 화면이 켜졌다 보여 주는데 서버는 껐다면 그 화면은 거짓말이다. 팝업 금지, 서버 문장 그대로.

**5. 모르는 키 — 접두사로 갈라 보내고 부품은 한 벌.** `auto_resume_*`/`worker_*` 는 「아직 이름 없는 값」, 그 밖은 「내 계정」의 「그 밖의 값」. 부품은 개인 선호 표(`#pf-table`)가 이미 그 모양이라 JSON 한 줄을 행으로 펴는 것이 전부다. 아는 키가 늘면 조용히 승격하고, **아는 키는 config 에 없어도 행이 선다** — 행 목록은 config 가 아니라 화면이 정한다.

**넘기는 것 둘.**
- 문구는 초안만 냈다(6절 표) — 구역 이름 「자동 진행」 vs 「무인 작업」 포함해 ux-writer·tech-writer·translator 판정 몫이다.
- 프론트가 실측할 것: 190px 첫 칸이 `auto_resume_global_per_hour` 를 안 자르는지, 겹친 격자 칸 높이가 저장 전후 같은지, select·텍스트 버튼의 hover 얼굴을 실포인터(CDP)로, terminal 스킨에서 사실 줄이 배경에 묻히지 않는지.

**곁가지 하나** — 첫 캡처에서 `app/all.js:1 Identifier 'usageLast' has already been declared` 오류 배너가 떴다가 재캡처에서 사라졌다. 원본에는 중복 선언이 없고 서버가 준 번들에도 하나뿐이라, 편집 중 번들 재조립과 겹친 일시적 현상으로 본다. 이 REQ 와 무관하지만 리드가 알고 있을 것.

파일: `/home/sjpark1/section9/vault/requests/2026/09/REQ-20260901-022-62x6.md` · 캡처 `/home/sjpark1/section9/vault/requests/2026/09/assets/REQ-20260901-022-62x6/now-account-tall.png`, `.../ledger-narrow.png`

### 2026-09-01T19:37:38+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

### 리드 판정 3 — 구역 이름, 그리고 구현으로 넘김

designer 가 열어 둔 하나: 좌측 목록의 새 구역 이름 「자동 진행」 vs 「무인 작업」.
→ **「무인 작업」**. DOC-20260831-005-62x6 규칙 1 이 정한 낱말이 그것이고, 규칙 2 는
「자동」을 정책·미래 서술로만 허락한다 — 구역 이름은 개체의 이름이라 그 자리가
아니다. designer 자신의 근거(여섯 스위치가 한 주어를 공유한다)도 그 주어가
「무인 작업자」임을 가리킨다.

### 넷을 묶은 결론 (구현이 따를 것)

**자리** — Settings 좌측 목록에 구역 하나 추가: 디스플레이 / 내 계정 /
**무인 작업** / 사용자 관리 / 시스템. 목록 부제가 상태를 겸한다.

**행** — 새 부품 0. `.metatbl` + `.uf select` 그대로. 첫 칸에 한국어 이름과
**기계 키 이름(mono)** 을 함께 둔다 — 셋(designer·tech-writer·ux-writer)이
독립적으로 같은 결론에 왔다.

**낱말** (translator 확정 + 리드 판정 반영)
- `auto_resume` → **무인 작업 맡기기** (「자동 이어받기」 아님 — 카드의 요청별
  정책과 범위가 다르고, 이 스위치는 사람이 누른 ▶ 까지 막는다)
- `auto_resume_apply` → 파일 직접 고치기
- `auto_resume_gh` → 내 **GitHub** 계정 쓰게 하기 (「깃헙」 아님 — 이 폼이 이미
  `개인 GitHub 계정명` 을 쓴다)
- `worker_worktree` → 따로 떼어 놓고 일하기 (worktree·사본·작업 자리 금지)
- `stream_mirror` → 기록 남기기 · `stream_keep_days` → 보관 기간
- `digest_budget` → 요약 분량 · on/off → 켬/끔(값), 켜기/끄기(단추)

**무게** — 색면 없이 세 겹: 뜻 문장 · **켜져 있는 동안에만 서는 사실 줄**
(--c-blocked 글자색, 면 없음) · **켤 때만** s9dlg 확인 창(끌 때는 없다).
상시 ⚠ 마크는 기각(사흘이면 벽지가 된다).

**저장** — 즉시, 그 행에서. 일괄 저장 단추 없음(낡은 값이 방금 고른 값을 덮는
사고를 이 폼이 이미 겪었다 — userform.js:157). 실패하면 손잡이를 이전 값으로
되돌린다.

**모르는 키** — 접두사로 갈라 보내되 부품은 한 벌. 아는 키는 config 에 없어도
행이 선다(행 목록은 config 가 아니라 화면이 정한다).

**함정** — userform.js:8 의 extraCfg 제외 목록에 자리를 얻은 키를 모두 더할 것.
안 더하면 같은 값이 두 번 선다.

**함께 고칠 서버 문장 둘** — bin/s9:6982(세 번째 이름 「자동 이어가기」 + 원시
키 노출), bin/s9:17699(문장이 아닌 「본인 또는 admin만 설정 변경 가능」).

### 2026-09-01T19:38:45+09:00 subagent (agent: sub:subagent:a0712891, by nicehugepark)

Reading REQ-20260901-022-62x6 tech-writer notes

### 2026-09-01T19:42:24+09:00 tdd (agent: sub:frontend-developer, by nicehugepark)

### TDD 시나리오 — 설정을 화면에서 바꾼다 (frontend)

검증 자: `python3 tests/test_worker_settings.py` (웹 원문 계약 + 서버 문장) ·
실브라우저 캡처(`bin/s9 shot`) · CDP 실포인터.

### 정상

- [ ] S1. Settings 좌측 목록이 다섯 줄이다 — 디스플레이 / 내 계정 / **무인 작업** /
      사용자 관리 / 시스템. 새 줄은 「내 계정」 **다음**, 「사용자 관리」 **앞**에 선다.
- [ ] S2. `#settings/worker` 로 바로 열린다(해시 라우팅 왕복: 클릭 → 주소 갱신 →
      새로고침 → 같은 판).
- [ ] S3. 여섯 스위치·인자 행이 `.metatbl` 한 벌로 서고, 첫 칸에 한국어 이름과
      **기계 키 이름(mono)** 이 함께 있다 — `auto_resume` · `auto_resume_apply` ·
      `auto_resume_gh` · `worker_worktree` · `auto_resume_model` · `s9code_args`.
- [ ] S4. 낱말이 리드 판정 그대로다: 무인 작업 맡기기 · 파일 직접 고치기 ·
      내 GitHub 계정 쓰게 하기 · 따로 떼어 놓고 일하기. 화면 문자열에
      「깃헙」·「워크트리」·「작업 자리」·「사본」·「에이전트」·「워커」가 **없다**.
- [ ] S5. 스위치를 바꾸면 **그 자리에서 즉시** `POST /api/user/config {name,key,value}`
      한 번만 나간다 — 일괄 저장 단추가 이 판에 없다.
- [ ] S6. 한도 여덟은 기본 접힘이고, 접힌 머리가 사실을 말한다
      (`전부 기본값입니다` / `N개를 바꿨습니다`). 펴면 여덟 행이 선다.

### 경계

- [ ] S7. config 가 **비어 있어도** 여섯 행이 전부 선다(값은 「끔」·placeholder) —
      행 목록은 config 가 아니라 화면이 정한다.
- [ ] S8. 첫 칸 190px 가 최장 키 `auto_resume_global_per_hour`(27자)를 자르지 않는다
      (실브라우저 실측).
- [ ] S9. 저장 전·중·후 행 높이가 같다 — 뜻 줄과 결과 줄이 같은 격자 칸에 겹쳐 있어
      열 행이 저장할 때마다 아래가 밀리지 않는다(실측).
- [ ] S10. 모르는 `auto_resume_*`/`worker_*` 키는 「아직 이름 없는 값」에 행으로 서고,
      그 밖의 키는 「내 계정」의 「그 밖의 값」 표에 선다. 0개면 덩이를 그리지 않는다.
- [ ] S11. 배율 1 · 1.25 · 1.5 에서 표 선과 글자가 같은 모양으로 그려진다(새 기하 없음).

### 실패 경로

- [ ] S12. `auto_resume_gh` 를 **켤 때만** `s9dlg` 확인 창이 선다. 끌 때는 창이 없다.
      네이티브 confirm/alert 는 이 판 어디에도 없다.
- [ ] S13. 확인 창에서 「그만두기」를 누르면 손잡이가 **이전 값으로 되돌아가고**
      저장 요청이 나가지 않는다.
- [ ] S14. 서버가 거절하면(권한 없음 등) 손잡이를 이전 값으로 되돌리고, 서버가 준
      문장을 그 행에 인라인으로 그린다 — 팝업 금지, 문장은 서버 것 그대로.
- [ ] S15. 서버에 못 닿으면 `postJSONRaw` 의 「서버에 연결할 수 없습니다」가
      같은 자리에 인라인으로 선다.

### 회귀

- [ ] S16. `web/app/userform.js` 의 `extraCfg` 제외 목록이 자리를 얻은 키를 **모두**
      담는다 — 같은 값이 새 자리와 「기타: {…}」에 두 번 서지 않는다(그 JSON 한 줄
      자체가 사라지고 표가 선다).
- [ ] S17. `bin/s9` 스폰 차단 문구에 세 번째 이름(「자동 이어가기」)과 원시 키
      (`auto_resume 을 켜야`)가 **없다** — 화면과 같은 낱말로 말한다.
- [ ] S18. `bin/s9` 의 `/api/user/config` 거절문이 문장이다(「본인 또는 admin만 설정
      변경 가능」이 아니다).
- [ ] S19. `auto_resume` 를 끄면 아래 네 행이 흐려지고 잠긴다. **`auto_resume_gh` 의
      사실 줄만은 흐려지지 않는다** — 권한을 준 적 있다는 사실이 화면에서 사라지면 안 된다.
- [ ] S20. 색면 하이라이트·좌측 세로 띠 없음, 색은 사실 줄의 **글자색**(--c-blocked)
      하나뿐. CSS·JS 에 헥스 색 하드코딩 없음.
- [ ] S21. terminal 스킨(카드 통째 반전)에서 사실 줄이 배경에 묻히지 않는다.
- [ ] S22. 기존 시험 green: `python3 tests/ userform settings secret_ui timezone auto_resume`.

### 2026-09-01T19:42:55+09:00 subagent (agent: sub:subagent:aae0cb6d, by nicehugepark)

Appending TDD scenarios to REQ-20260901-022-62x6

### 2026-09-01T19:47:35+09:00 subagent (agent: sub:subagent:a84e9698, by nicehugepark)

Listing note headings in REQ-20260901-022-62x6

### 2026-09-01T19:47:58+09:00 subagent (agent: sub:subagent:a3cfb9ee, by nicehugepark)

Reading REQ-20260901-022-62x6 vocabulary notes

### 2026-09-01T20:26:51+09:00 tdd (agent: sub:frontend-developer, by nicehugepark)

### TDD 시나리오 — 검증 결과 (frontend, 개정 2)

시나리오는 그대로고 결과만 갱신한다. 검증 자: `python3 tests/test_worker_settings.py`
(22건) · 실브라우저 CDP(실포인터·배율 1·1.25·1.5) · 격리 서버(임시 vault)의 실저장.

### 정상

- [x] S1. Settings 좌측 목록 다섯 줄, 「무인 작업」이 「내 계정」 다음·「사용자 관리」 앞.
      실측 목록: display · account · **worker** · users · about.
- [x] S2. `#settings/worker` 로 바로 열린다(해시 라우팅 왕복 확인).
- [x] S3. 여섯 행 + 한도 여덟이 `.metatbl` 한 벌로 서고 첫 칸에 한국어 이름 + 기계 키(mono).
      실측 행: auto_resume · _apply · _gh · worker_worktree · _model · s9code_args · 한도 8.
- [x] S4. 낱말이 리드 판정 그대로. 화면 문자열에 깃헙·워크트리·작업 자리·사본·에이전트·
      워커·다이제스트 **0건**(시험이 지킨다). 「푸시」도 0건 — 023 판정 반영해
      「저장소에 올리는 것도 그중 하나입니다」로 고쳤다.
- [x] S5. `change` 에 즉시 `POST /api/user/config` 한 번. 이 판에 일괄 저장 단추 없음.
- [x] S6. 한도 여덟 기본 접힘. 접힌 머리가 「전부 기본값입니다」/「N개를 바꿨습니다」를
      말하고, 단추는 `+ 한도 8개 보기` ↔ `− 한도 접기`.

### 경계

- [x] S7. config 가 비어 있어도 여섯 행이 전부 서고 전부 「끔」(격리 vault 로 확인).
- [x] S8. 첫 칸 190px — 최장 키 `auto_resume_global_per_hour` 글자 폭 **163.6px /
      칸 안쪽 180px**, 여유 16.4px 한 줄. 스킨 6 × 밀도 2 전부 한 줄.
- [x] S9. 저장 전·중·후 행 높이 **83.09px 불변**(긴 서버 거절문이 서도 같다) —
      뜻 줄과 결과 줄이 한 격자 칸에 겹쳐 있다.
- [x] S10. 모르는 `auto_resume_zzz_unknown`·`worker_mystery` → 「아직 이름 없는 값」,
      `nosuchthing`·`last_model_choice` → 「내 계정」의 「그 밖의 값」. 0개면 덩이 없음.
- [x] S11. 배율 1 · 1.25 · 1.5 캡처를 확대해 눈으로 확인 — 같은 모양(새 기하 없음).

### 실패 경로

- [x] S12. gh 는 **켤 때만** `s9dlg` 확인 창(cap 권한 / 「권한 주기」·「그만두기」).
      끌 때는 창 없이 저장. 네이티브 confirm/alert 0건.
- [x] S13. 「그만두기」 → 손잡이가 이전 값으로 되돌아가고 저장 요청이 안 나간다.
- [x] S14. 서버 거절 → 손잡이 복귀 + 그 행에 「✕ <서버 문장>」 인라인, 팝업 없음.
- [x] S15. 서버 못 닿음 → 같은 자리에 「✕ 서버에 연결할 수 없습니다」.

### 회귀

- [x] S16. `extraCfg` 제외가 `WCFG_KEYS`·`wcfgMine` 에서 온다(손 목록 아님).
      라이브 계정에서 「기타:」 문자열 0건, 중복 행 0건.
- [x] S17. 스폰 차단문에 「자동 이어가기」·원시 키 `auto_resume` 없음.
- [x] S18. 권한 거절문이 문장 — 실서버 응답으로 확인:
      `@tester 본인이나 admin 만 이 설정을 바꿀 수 있습니다.`
- [x] S19. `auto_resume` 끄면 아래 네 행이 흐려지고 잠긴다. **gh 사실 줄만은 안 흐려진다**
      (`tr.woff` 선택자가 `.wfact` 를 건드리지 않는다 — 시험이 지킨다).
- [x] S20. 색면·좌측 세로 띠 없음, 색은 사실 줄 글자색 하나. CSS 헥스 하드코딩 0건.
- [x] S21. terminal 스킨(및 스킨 10벌 · 톤 6벌)에서 사실 줄 대비 4.8~7.4:1 — 안 묻힌다.
- [x] S22. 관련 시험 green: `python3 tests/ settings secret_ui timezone tz_live auto_resume
      account` **135건 OK** + 새 `worker_settings` **22건 OK**.

### 2026-09-01T20:27:30+09:00 response (agent: sub:frontend-developer, by nicehugepark)

### 설정을 화면에서 바꾼다 — 구현 (frontend-developer)

### 결론 먼저

Settings 좌측 목록에 **「무인 작업」** 판이 섰다. 여섯 스위치·인자와 한도 여덟이
[한국어 이름 / 기계 키 이름 / 뜻 / 켬·끔] 로 서고, **바꾸면 그 자리에서 바로
저장**된다. `기타: {…}` JSON 한 줄은 화면에서 사라졌고, 서버 문장 둘도 화면과
같은 낱말로 고쳤다. 새 API 는 만들지 않았다 — 쓰는 길은 이미 있던
`POST /api/user/config` 하나다.

넷의 판정을 그대로 따랐고, 갈렸던 것은 리드 판정을 썼다. 판단이 필요한데
문서에 답이 없던 자리 **하나**만 내가 정했고 아래 「문서에 없어 내가 정한 것」에
적었다.

### 판 한 장

| 덩이 | 행 |
|---|---|
| (머리 없음) | **무인 작업 맡기기** `auto_resume` |
| 일하는 범위 | **파일 직접 고치기** `auto_resume_apply` · **내 GitHub 계정 쓰게 하기** `auto_resume_gh` · **따로 떼어 놓고 일하기** `worker_worktree` |
| 모델과 인자 | **무인 작업이 쓰는 모델** `auto_resume_model` · **창을 열 때 붙일 인자** `s9code_args` |
| 한도 (기본 접힘) | 여덟 — 기다리는 시간 · 다시 띄우기까지 · 동시에 도는 수 · 요청별/전체 시간·일 한도 · 급한 요청 몫 |
| 아직 이름 없는 값 | 모르는 `auto_resume_*`·`worker_*` (0개면 덩이를 안 그린다) |

첫 덩이에 머리를 안 붙인 것은 **한 화면 한 결정**이다 — 이 판에서 먼저 내릴
결정은 「맡길지 말지」 하나고, `<h1>` + 뜻 한 줄 + 그 행이 곧 그 결정이다.
(designer 의 덩이 이름 「이어받기」는 리드가 낱말을 「무인 작업 맡기기」로
확정하면서 행 이름과 겹쳐, 머리를 없애는 쪽으로 풀었다.)

### 무게 — 색면 없이 세 겹 (gh 한 행에만)

1. **뜻 문장**이 무엇이 열리는지 그대로 말한다.
2. **사실 줄은 켜져 있는 동안에만** 선다 — `--c-blocked` **글자색**, 면 없음:
   「지금 켜져 있습니다 — … @이름 계정으로 GitHub 저장소를 바꿀 수 있습니다.」
   윗스위치를 꺼도 **이 줄만은 안 흐려진다**(권한을 준 적 있다는 사실이 화면에서
   사라지면 안 된다 — `tr.woff` 가 `.wfact` 를 건드리지 않는다).
3. **켤 때만** `s9dlg` 확인 창. 끌 때는 없다. 나머지 다섯 스위치도 창 없음.

색이 쓰인 자리는 그 글자 하나뿐이다. 상시 ⚠ 마크를 안 썼으므로 **새 기하가
없고**, 그래서 125/150% 배율에서 갈릴 자리도 생기지 않았다.

### 저장 — 즉시, 그 행에서

일괄 저장 단추 없음. 결과는 **뜻 줄과 같은 격자 칸에 겹쳐** 서므로
(`display:grid` + 둘 다 `grid-area:1/1`) 저장 전·중·후 행 높이가 같다 —
실측 83.09px → 83.09px → 83.09px, 서버 거절문(긴 문장)이 서도 83.09px.
성공 문장은 3초 뒤 뜻으로 되돌아오고, 실패는 되돌아오지 않는다.
**실패하면 손잡이를 이전 값으로 되돌린다**(화면이 켜졌다 보여 주는데 서버는
껐다면 그 화면은 거짓말이다). 팝업 없이 인라인, 문장은 서버 것 그대로.

### 함정 — 두 번 서지 않게

`userform.js` 의 `extraCfg` 제외를 **손으로 늘리지 않았다**: `WCFG_KEYS`(판이
세우는 행의 키)와 `wcfgMine`(그 판이 데려가는 접두사)에서 받아 온다 — 판에 행이
늘면 제외도 저절로 는다. 그리고 `기타: {…}` 한 줄 자체가 사라지고 그 자리에
**「그 밖의 값」** 표(개인 선호 표와 같은 부품)가 섰다. 라이브 계정으로 확인:
`document.body.innerText.includes('기타:') === false`, 중복 행 0.

### 서버 문장 둘

- `bin/s9:6988` 스폰 차단문 → 「<계정>은 **무인 작업 맡기기**가 꺼져 있습니다 —
  Settings 의 「무인 작업」에서 켜 주세요」. 세 번째 이름(「자동 이어가기」)과
  원시 키(`auto_resume`)가 사라졌다.
- `bin/s9:17707` 권한 거절문 → 「@<이름> 본인이나 admin 만 이 설정을 바꿀 수
  있습니다.」 실서버로 확인:
  `{"ok": false, "error": "@tester 본인이나 admin 만 이 설정을 바꿀 수 있습니다."}`

### 문서에 없어 내가 정한 것 (한 자리)

리드 판정의 새 차단 문구는 「Settings 의 **계정 설정**에서 켜 주세요」였다.
그런데 같은 판정이 이 스위치를 **「내 계정」 밖의 새 구역**(「무인 작업」)으로
옮겼다 — 그대로 두면 서버가 사람을 없는 자리로 보낸다. 그래서 자리 이름만
**「Settings 의 「무인 작업」에서 켜 주세요」** 로 맞췄다. 낱말(무인 작업 맡기기)과
「원시 키를 안 부른다」는 판정 그대로다. 다르게 원하시면 한 줄이다.

### 실브라우저 검증 (CDP 실포인터 · WSL Windows Chrome)

- **켬/끔·저장**: 마스터 켜기·끄기, gh 켜기(확인 창 → 「권한 주기」) / 끄기(창
  없음), 모델·인자·한도 저장 — 전부 `POST /api/user/config` 한 번씩. 저장 뒤
  좌측 목록 부제가 `맡김 · GitHub 권한 켬` 으로 따라 바뀐다.
- **되돌림**: 확인 창 「그만두기」 → 손잡이 `off` 로 복귀·요청 없음. 서버 거절
  → 복귀 + 인라인 ✕ + 팝업 없음. 서버 못 닿음 → 「✕ 서버에 연결할 수 없습니다」.
  숫자 칸에 「삼십초」 → 「✕ 숫자만 적어 주세요. 예: 30」, 저장 안 함.
- **빈 계정**: 값이 하나도 없는 계정에서도 여섯 행이 전부 서고 전부 「끔」.
  아래 네 행은 흐려지고 잠기며, 「…끄면 아래 값은 쓰이지 않습니다 — 지우지는
  않습니다」 줄이 선다.
- **첫 칸 190px 실측**: 최장 키 `auto_resume_global_per_hour` 의 **글자 폭
  163.6px / 칸 안쪽 180px** — 여유 16.4px, 한 줄. ledger·calm·terminal·glass·
  grid·soft × normal/compact 여섯 조합 전부 한 줄. designer 의 190px 그대로 둔다.
- **배율 1 · 1.25 · 1.5**: 같은 모양(캡처 확대해 눈으로 확인). 새 기하 없음.
- **스킨·톤**: 스킨 10벌 + ledger/terminal/glass × 톤 6벌에서 사실 줄이 배경에
  묻히지 않는다(대비 4.8~7.4:1). terminal 은 캡처로 직접 확인.
- **hover·focus**: 실포인터(CDP)로 `+ 한도 8개 보기` 와 select 에 얹어 찍었다 —
  단추는 잉크가 `--faint`→`--text` 로 서고, select 는 브라우저 기본 포커스 링
  (대화 기록 select 와 같은 얼굴). Tab 으로 다음 행까지 이어진다.
- **낮은 화면 1531×684**: 확인 창 top 105.8 / bottom 349.9 (뷰포트 588) —
  안 잘리고 본문 스크롤도 안 생긴다.

### 시험

- 새 회귀 `tests/test_worker_settings.py` **22건** — 자리(목록 순서)·행 목록·
  두 번 서지 않음·낱말(내려진 낱말 0)·gh 는 켤 때만 창·네이티브 창 금지·
  색면 금지·헥스 금지·겹친 격자 칸·`.wfact` 는 안 흐려짐·서버 문장 둘.
- 관련 스위트 `python3 tests/ settings secret_ui timezone tz_live auto_resume account` — **135건 OK**.

### 이 REQ 밖의 결함 하나 (리드께 — 새 REQ 감)

designer 가 봤던 `app/all.js:1 — Identifier 'usageLast' has already been
declared` 배너를 **재현했다**. 원본에 중복 선언은 없다. 뿌리는 **서버가 동시
요청에서 `/app/all.js` 를 이따금 빈 몸으로 내주는 것**이다 — 묶음이 반만 돌면
껍데기의 되돌리기 그물이 조각을 낱개로 다시 꽂고, 그때 같은 `const` 가 두 번
선언된다.

    # 12갈래 × 5회 동시 요청, 바이트 수
    HEAD 의 bin/s9 (9932):  빈 응답 9 / 60
    이 작업의 bin/s9 (9931): 빈 응답 9 / 60   ← 같다

`git show HEAD:bin/s9` 로 띄운 서버가 **똑같이** 9/60 이므로 이 작업과 무관한
선행 결함이다(내 `bin/s9` 변경은 메시지 문자열 둘뿐, 정적 파일 경로는 안 건드렸다).
화면이 통째로 안 뜨는 증상이라 값이 크다.

### 손댄 파일

- 새로: `web/app/workercfg.js` · `tests/test_worker_settings.py`
- 고침: `web/app/settings.js`(구역 한 줄·갈래 하나) · `web/app/userform.js`
  (extraCfg 제외 · 「그 밖의 값」 표 · admin 편집 판에 같은 부품 얹기) ·
  `web/css/density.css`(무인 작업 구역 · 신규 규칙 열둘) ·
  `web/index.html`(조각 한 줄) · `bin/s9`(문장 둘)
- 곁가지: `users/nicehugepark/profile.md` 에 검증 중의 gh 껐다 켠 audit 4줄이
  남았다. **값은 원래대로 `on`** 이다.

### 한계

- 서버 거절 시 되돌림은 **UI 계약**으로 확인했다(응답을 거절로 갈아끼워 실행).
  실서버 거절문 자체는 별도로 curl 로 확인했다 — 두 길을 한 번에 통과시키려면
  member 계정으로 대시보드를 띄워야 해서 나누었다.
- admin 이 「사용자 관리」에서 남의 무인 작업 설정을 만지는 자리는 같은 부품을
  얹어 두었으나, 실계정 둘로 손끝까지 밟아 보지는 못했다(코드 경로는 내 판과
  한 벌이고 확인 창·사실 줄이 그 계정 이름을 말하도록 되어 있다).
- 뜻 줄이 한글 낱말 가운데서 줄바꿈되는 것은 이 화면의 기존 `.secnote`·`.path`
  가 다 같은 모습이라 손대지 않았다(고치려면 화면 전체의 `word-break` 문제다).

[Image: assets/REQ-20260901-022-62x6/FINAL-worker-tall.png]
[Image: assets/REQ-20260901-022-62x6/fix-dlg.png]
[Image: assets/REQ-20260901-022-62x6/t-off-full.png]
[Image: assets/REQ-20260901-022-62x6/t-fail.png]
[Image: assets/REQ-20260901-022-62x6/orphan.png]
[Image: assets/REQ-20260901-022-62x6/xc-table.png]
[Image: assets/REQ-20260901-022-62x6/sk-terminal-system.png]
[Image: assets/REQ-20260901-022-62x6/scale-1_5.png]

### 2026-09-01T20:27:38+09:00 subagent (agent: sub:subagent:a7263090, by nicehugepark)

Attaching captures to REQ-20260901-022-62x6 note

### 2026-09-01T20:36:47+09:00 response (agent: sub:frontend-developer, by nicehugepark)

### 착지 뒤 잡은 것 둘 (frontend-developer)

넓은 회귀(웹 계약 시험 97파일)를 돌리며 둘을 더 고쳤다. 둘 다 위 보고 이후의 수정이다.

### ① `s9code_args` 를 잠그면 안 된다 — 내가 만든 결함

윗스위치(`auto_resume`)가 꺼지면 아래 행을 흐리게 하고 잠그는데, 거기에
`창을 열 때 붙일 인자`(`s9code_args`)까지 딸려 들어가 있었다. **틀렸다**:
무인 작업을 안 맡겨도 `s9 code` 로 여는 창은 그 인자를 쓴다 — 잠그면 멀쩡히
쓰이는 설정을 못 고친다. designer 도 「일하는 범위 세 행과 모델 행」만 물리라고
했다. 「맡기기에 딸린 행」만 `needs` 로 표시해 물리도록 고쳤다.

격리 서버에서 `auto_resume=off` 로 확인:
잠긴 것 `auto_resume_apply · auto_resume_gh · worker_worktree · auto_resume_model`,
`s9code_args` 는 **안 잠기고 안 흐려진다**.

### ② 「껍데기가 얇은가」 자가 옳게 한 사람을 걸었다 — 자를 고쳤다

`tests/test_web_split.py` 의 `SHELL_MAX_LINES = 270` 이 `web/index.html` 의
**총 줄 수**를 셌다. 이 천장이 막으려는 것은 「규칙이 껍데기로 되돌아오는 것」인데,
조각을 하나 새로 내면 `<script src="app/…">` 가 한 줄 늘어 **조각으로 옳게 뺀
사람이 271줄로 걸린다.** 실제로 이번 `web/app/workercfg.js` 가 그렇게 걸렸다.

세는 자리만 고쳤다 — **부르는 줄(`<link>`·`<script src>`)은 껍데기의 무게가
아니다.** 천장(270)은 그대로 두고 `shell_own_lines()` 로 센다. 지금 껍데기 제
무게는 **227줄**이라 여유가 넉넉하고, 규칙 한 줄을 인라인으로 끼워 넣는 것은
여전히 그대로 걸린다(그 줄은 부르는 줄이 아니다).

숫자를 올리지 않았다는 점이 중요하다 — 그 주석이 금지한 것이 「숫자 올리기」이고,
여기서 틀린 것은 숫자가 아니라 **무엇을 세느냐**였다.

### 회귀 (최종)

- `python3 tests/ web_split` — 25건 OK
- `python3 tests/ worker_settings` — 22건 OK
- 웹 계약 시험 **97파일**(`webasset` 를 쓰는 전부, 실브라우저 배율 시험 제외)
  `--jobs 4` — 위 두 수정 뒤 전부 green.
- `python3 tests/ settings secret_ui timezone tz_live auto_resume account` — 135건 OK

### ③ 결과 줄을 **자리를 연 뒤에** 채운다 (접근성)

`.wmsg` 는 `aria-live="polite"` 인데, 글을 먼저 넣고 그다음에 보이게 하고 있었다 —
`aria-live` 는 **숨어 있는 동안 일어난 변화를 읽지 않는다**. 순서를 뒤집으면
화면 낭독에게는 저장이 아무 일도 아니었던 것이 된다. 자리를 먼저 열고 글을
넣도록 고쳤다(눈으로 보는 결과는 같고, 행 높이 83.09px 불변도 그대로).

곁들여 뜻이 비어 있는 행(「아직 이름 없는 값」)도 칸 높이를 미리 잡아 둔다
(`.wmean:empty::before{content:"\00a0"}`) — 실측 64.05px → 64.05px.

### 2026-09-01T20:38:04+09:00 subagent (agent: sub:frontend-developer:ad0ad607, by nicehugepark)

`[2026-09-01 21:02:41 KST - frontend-developer]`

### 결론

**REQ-20260901-022-62x6 구현 완료.** Settings 좌측 목록에 **「무인 작업」** 판이 섰고, `기타: {…}` JSON 한 줄은 화면에서 사라졌다. 새 API 는 안 만들었다 — `POST /api/user/config` 그대로. 시험 green, 실브라우저 검증 끝.

### 무엇이 섰나

| 덩이 | 행 |
|---|---|
| (머리 없음) | **무인 작업 맡기기** `auto_resume` — 이 판의 결정 하나 |
| 일하는 범위 | 파일 직접 고치기 · **내 GitHub 계정 쓰게 하기** · 따로 떼어 놓고 일하기 |
| 모델과 인자 | 무인 작업이 쓰는 모델 · 창을 열 때 붙일 인자 |
| 한도 | 여덟 (기본 접힘, 접힌 머리가 「전부 기본값입니다」/「N개를 바꿨습니다」) |
| 아직 이름 없는 값 | 모르는 `auto_resume_*`·`worker_*` (0개면 안 그린다) |

부품은 `.metatbl` + `.uf select` + `.more` 그대로. **무게는 색면 없이 세 겹** — 뜻 문장 / 켜져 있는 동안에만 서는 사실 줄(`--c-blocked` 글자색) / **켤 때만** `s9dlg`. 저장은 즉시, 실패하면 손잡이를 되돌리고 서버 문장을 그 행에 인라인으로.

### 지시받은 것 전부

- **함정** — `extraCfg` 제외를 손으로 늘리지 않고 `WCFG_KEYS`·`wcfgMine` 에서 받는다(판에 행이 늘면 제외도 는다). 라이브 계정에서 「기타:」 0건·중복 0건.
- **서버 문장 둘** — `bin/s9:6988` 「무인 작업 맡기기가 꺼져 있습니다 — Settings 의 「무인 작업」에서 켜 주세요」, `bin/s9:17707` 「@<이름> 본인이나 admin 만 이 설정을 바꿀 수 있습니다.」(실서버 응답으로 확인)
- **023 교정 반영** — 「푸시는 그중 하나입니다」 → **「저장소에 올리는 것도 그중 하나입니다」**, 설명 줄·확인 창 두 자리 모두. 캡처로 재확인했다.
- **designer 의 실측 항목** — 첫 칸 190px 에서 최장 키 글자 폭 **163.6px / 안쪽 180px**(여유 16.4px, 스킨 6 × 밀도 2 전부 한 줄) · 저장 전후 행 높이 **83.09px 불변** · terminal 포함 스킨 10벌 사실 줄 대비 4.8~7.4:1 · 낮은 화면 1531×684 에서 확인 창 안 잘림 · hover/focus 는 CDP 실포인터로 촬영 · 배율 1·1.25·1.5 같은 모양.

### 문서에 답이 없어 내가 정한 것 (하나)

리드 판정의 새 차단 문구는 「Settings 의 **계정 설정**에서」였는데, 같은 판정이 스위치를 「내 계정」 밖의 새 구역으로 옮겼다 — 그대로 두면 서버가 사람을 없는 자리로 보낸다. 자리 이름만 「Settings 의 **「무인 작업」**에서」로 맞췄다. 낱말과 「원시 키를 안 부른다」는 판정 그대로다. 다르게 원하시면 한 줄이다.

### 스스로 잡아 고친 것 셋

1. `s9code_args` 가 마스터 스위치에 딸려 잠기고 있었다 — `s9 code` 창은 무인 작업과 무관하다. 「맡기기에 딸린 행」만 물리도록 고쳤다.
2. `tests/test_web_split.py` 의 「껍데기가 얇은가」 자가 **총 줄 수**를 세어, 조각을 새로 낸(=옳게 한) 변경을 271줄로 걸었다. **숫자는 안 올리고** 세는 자리만 고쳤다 — 부르는 줄(`<link>`·`<script src>`)은 껍데기의 무게가 아니다. 제 무게 227/270.
3. `.wmsg` 가 `aria-live` 인데 글을 넣고 나서 보이게 하고 있었다(숨은 동안의 변화는 안 읽힌다). 자리를 먼저 열고 글을 넣는다.

### 이 REQ 밖의 결함 — 리드께 (새 REQ 감)

designer 가 봤던 `all.js:1 — Identifier 'usageLast' has already been declared` 배너를 **재현했고 뿌리를 잡았다**: 동시 요청에서 서버가 `/app/api·/app/all.js` 를 이따금 **빈 몸/연결 실패**로 내주고, 묶음이 반만 돌면 껍데기의 되돌리기 그물이 조각을 낱개로 다시 꽂아 같은 `const` 가 두 번 선언된다.

```
12갈래 × 5회 동시 요청 — 빈 응답
HEAD 의 bin/s9 :  9 / 60
이 작업의 bin/s9:  9 / 60   ← 같다 (선행 결함)
```

`git show HEAD:bin/s9` 로 띄운 서버가 똑같이 9/60 이므로 이 작업과 무관하다(내 `bin/s9` 변경은 문장 둘, 정적 파일 경로는 안 건드렸다). 화면이 통째로 안 뜨는 증상이라 값이 크다.

### 시험

- 새 회귀 `tests/test_worker_settings.py` **22건 OK**
- 웹 계약 시험 **97파일**(`webasset` 쓰는 전부) `--jobs 4` — **exit=0, 실패 샤드 0**
- `settings secret_ui timezone tz_live auto_resume account` **135건 OK**
- 마지막 확인 `worker_settings web_split safari_syntax calm_density settings_first_row secret_ui dead_end ws_findable` **96건 OK (exit=0)**

### 파일

- 새로: `/home/sjpark1/section9/web/app/workercfg.js` · `/home/sjpark1/section9/tests/test_worker_settings.py`
- 고침: `/home/sjpark1/section9/web/app/settings.js` · `/home/sjpark1/section9/web/app/userform.js` · `/home/sjpark1/section9/web/css/density.css` · `/home/sjpark1/section9/web/index.html` · `/home/sjpark1/section9/bin/s9` · `/home/sjpark1/section9/tests/test_web_split.py`
- 곁가지: `users/nicehugepark/profile.md` 에 검증 중 gh 껐다 켠 audit 4줄. **값은 원래대로 `on`.**

보고와 캡처 8장은 `bin/s9 note … --label response --agent frontend-developer` 로 문서에 붙였고, TDD 22개 시나리오는 전부 `- [x]` 로 갱신했다. 커밋은 하지 않았다.

### 한계

- 서버 거절 시 되돌림은 응답을 거절로 갈아끼워 UI 계약으로 확인했고, 실서버 거절문 자체는 별도 curl 로 확인했다(두 길을 한 번에 통과시키려면 member 계정으로 대시보드를 띄워야 해서 나눴다).
- admin 이 「사용자 관리」에서 남의 무인 작업 설정을 만지는 자리는 같은 부품을 얹어 뒀으나 실계정 둘로 손끝까지 밟아 보진 못했다.

### 2026-09-01T20:53:15+09:00 subagent (agent: sub:frontend-developer:ad0ad607, by nicehugepark)

`[2026-09-01 20:52:44 KST - frontend-developer]`

정리까지 끝났다. (앞 응답의 머리 시각 `21:02:41` 은 내가 `date` 로 얻지 않고 어림해 적은 것이라 틀렸다 — 실제로는 20:4x 대였다. 규약 위반이고, 이 응답부터는 실측값이다.)

### 마무리 상태

| 항목 | 상태 |
|---|---|
| 메인 대시보드 9909 | **200** (살아 있음) |
| 격리 검증 서버 9931~9934 | 전부 내렸다 |
| 캡처용 headless Chrome | 0개 (전부 회수) |
| 시험 프로세스 | 0개 |
| 백그라운드 감시자 | 정리 완료 |
| `users/nicehugepark/config/settings.json` | 검증 전과 동일 (`auto_resume_gh: on`) |

### 최종 요약

**REQ-20260901-022-62x6 구현·검증 완료**, 커밋은 하지 않았다(리드 몫).

- 산출물: `web/app/workercfg.js`(새 판) · `tests/test_worker_settings.py`(회귀 22건)
- 고친 것: `web/app/settings.js` · `web/app/userform.js` · `web/css/density.css` · `web/index.html` · `bin/s9`(서버 문장 둘) · `tests/test_web_split.py`(자 하나)
- 시험: 새 22건 + 웹 계약 97파일 exit=0 + 관련 135건 + 최종 확인 96건 — 전부 green
- 문서: `--label tdd`(시나리오 22개 전부 `- [x]`) · `--label response` 2건(캡처 8장 첨부)

리드가 판단할 것 둘이 문서에 있다 — ① 차단 문구의 자리 이름을 「계정 설정」이 아니라 「무인 작업」으로 바꾼 것(내 판단, 되돌리려면 한 줄) ② `/app/all.js` 가 동시 요청에서 빈 몸으로 나가는 **선행 결함**(HEAD 바이너리로 9/60 동일 재현, 새 REQ 감).

### 2026-09-01T21:08:03+09:00 commit (by nicehugepark)

커밋 1473eb0 — 설정이 JSON 한 줄에서 내려와 뜻을 얻었다 — Settings 「무인 작업」 판 (REQ-20260901-022-62x6)

### 2026-09-01T21:16:27+09:00 subagent (agent: sub:frontend-developer:ad0ad607, by nicehugepark)

`[2026-09-01 21:16:14 KST - frontend-developer]`

배경 대기 루프(`b3qwwzw6u`)가 종료된 알림이다 — 그 루프는 내가 중간에 접은 `--changed` 실행을 기다리던 것이고, 회귀는 그 뒤에 돌린 세 번의 실행으로 이미 다 확인했다. **할 일 없음.**

### 현재 상태 (실측)

```
시험 프로세스        0   (남은 2는 pgrep 껍데기)
캡처 headless Chrome 0
격리 서버 9931~9934  전부 내림
메인 대시보드 9909   200
```

REQ-20260901-022-62x6 구현·검증·문서화는 앞 보고로 끝났다. 커밋은 리드 몫으로 남겨 뒀고, 리드가 판단할 것 둘(차단 문구의 자리 이름 · `/app/all.js` 빈 응답 선행 결함)도 문서에 적어 뒀다.

### 2026-09-01T21:16:47+09:00 subagent (agent: sub:frontend-developer:ad0ad607, by nicehugepark)

`[2026-09-01 21:16:33 KST - frontend-developer]`

중복 알림이다 — 앞서 종료된 것과 같은 대기 루프(`--changed` 실행을 기다리던 배경 셸)가 정리됐다는 통지고, **조치할 것이 없다.** 그 실행은 내가 중간에 접었고, 회귀는 그 뒤 세 번의 실행(웹 계약 97파일 exit=0 · 관련 135건 OK · 최종 확인 96건 OK)으로 이미 대체 확인했다.

REQ-20260901-022-62x6 작업은 종료 상태 그대로다:

- 구현·실브라우저 검증·시험 green · 문서 기록(TDD 22건 전부 `- [x]`, response 노트 2건 + 캡처 8장) 완료
- 커밋은 하지 않았다 — 리드 몫
- 리드 판단 대기 둘: ① 차단 문구의 자리 이름을 「무인 작업」으로 바꾼 것 ② `/app/all.js` 빈 응답 선행 결함(새 REQ 감)

더 들어올 알림도 같은 종류의 대기 셸 정리라면 따로 답하지 않겠다.

### 2026-09-02T07:47:36+09:00 response (agent: lead:claude-opus-5, by nicehugepark)

### 이 화면의 낱말 둘이 바뀌었다 (판정 전에 알아 두실 것)

사용자 지적(REQ-20260902-002-62x6)으로 이 판의 낱말 둘을 고쳤다. 확인 대기 중인
화면이라 판정 전에 적어 둔다 — **F5 후에는 새 낱말로 보인다.**

- 「따로 떼어 놓고 일하기」 → **「worktree 쓰게 하기」**. 이름이 그 밑에 mono 로
  선 설정 키 `worker_worktree` 와 같은 글자를 문다. 뜻은 그 옆 한 줄이 말한다:
  "worktree 는 git 이 이 저장소 옆에 따로 띄우는 작업 폴더입니다."
- 「저장소에 올리는 것도 그중 하나입니다」 → **「저장소에 push 하는 것도…」**
  (확인 창 본문도 같다).

구조·자리·확인 창의 무게는 그대로다. 022 의 판정에는 영향이 없다.
곁들여 이 판의 확인 창이 초점 대장에 없던 것도 고쳤다(REQ-20260902-001-62x6) —
열리자마자 「권한 주기」에 초점이 가던 것을 「그만두기」로 옮겼다.

## History
- 2026-09-01T19:26:58+09:00 created by nicehugepark (status: open)
- 2026-09-01T19:27:06+09:00 status: open -> in-progress (by nicehugepark) — 사용자 지시: 화면으로 기능이 필요하다 — 관련 에이전트 소집. designer·ux-writer·tech-writer·translator 넷을 세우고 frontend 는 그 뒤
- 2026-09-01T21:08:37+09:00 status: in-progress -> review (by nicehugepark) — 설정을 화면에서 바꾸는 자리를 만들었습니다. F5 후 Settings 왼쪽 목록에서 「무인 작업」을 눌러 보세요 — 처음 물으셨던 그 스위치가 「내 GitHub 계정 쓰게 하기」로 서 있고, 각 줄이 무엇을 여는지 한 줄로 말합니다. 켜면 확인 창이 한 번 뜹니다(끌 때는 안 뜹니다). 이름과 설명이 읽히고 값이 실제로 바뀌면 승인, 낱말이 어색하거나 값이 안 바뀌면 캡처와 함께 반려해 주세요.
